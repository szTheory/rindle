---
phase: 17-api-surface-boundary-audit
plan: 04
type: execute
wave: 3
depends_on: [02, 03, 05]
files_modified:
  - lib/rindle.ex
  - lib/rindle/live_view.ex
  - lib/rindle/internal/variant_failure_logger.ex
  - README.md
  - guides/getting_started.md
  - test/rindle/api_surface_boundary_test.exs
  - test/rindle/live_view_test.exs
  - test/install_smoke/docs_parity_test.exs
  - .planning/phases/17-api-surface-boundary-audit/17-BREAKING-CHANGE-DECISION.md
autonomous: true
requirements: [API-01, API-02, API-03, API-05]
tags: [facade, semver, docs, compatibility]

must_haves:
  truths:
    - "Rindle exposes `verify_completion/2` as the preferred public verification name while `verify_upload/2` survives as an additive compatibility shim on `0.1.x`"
    - "`complete_multipart_upload/3` stays unchanged because it is the transport-specific multipart step from D-10, not part of the direct-upload naming cleanup"
    - "`log_variant_processing_failure/3` stops being documented public API and delegates to a hidden internal module"
    - "README.md and guides/getting_started.md teach the facade-first path with exactly `Rindle` and `Rindle.Profile` as the first-tier concepts"
    - "A boundary decision artifact records the D-03 storage-adapter override, the `0.1.x` compatibility posture, and the `v0.2.0` deferral line"
  artifacts:
    - path: lib/rindle.ex
      provides: "Preferred facade alias, compatibility shim, and hidden logging shim"
      contains: "def verify_completion"
    - path: lib/rindle/internal/variant_failure_logger.ex
      provides: "Hidden implementation module behind the public logging shim"
      contains: "@moduledoc false"
    - path: .planning/phases/17-api-surface-boundary-audit/17-BREAKING-CHANGE-DECISION.md
      provides: "Recorded semver and boundary reconciliation for Phase 18 and future releases"
      contains: "Storage.Local"
  key_links:
    - from: lib/rindle.ex
      to: lib/rindle/upload/broker.ex
      via: "verify_completion/2 delegates to Broker.verify_completion/2"
      pattern: "Broker\\.verify_completion"
    - from: lib/rindle/live_view.ex
      to: lib/rindle.ex
      via: "LiveView completion path uses the preferred facade verification function"
      pattern: "Rindle\\.verify_completion"
    - from: README.md and guides/getting_started.md
      to: test/install_smoke/docs_parity_test.exs
      via: "Facade-first onboarding assertions"
      pattern: "Rindle\\.(initiate_upload|verify_completion|attach|url)"
    - from: lib/rindle.ex
      to: lib/rindle/internal/variant_failure_logger.ex
      via: "Thin public shim delegates hidden implementation"
      pattern: "VariantFailureLogger"
---

<objective>
Finish the public-surface cleanup by aligning the facade names, hiding the observability helper, rewriting the onboarding docs, and recording the semver decision.

Purpose: After Plans 02 and 03 hide the internal modules, this plan locks the remaining high-signal adopter surface before Phase 18 documents it in depth. It is the phase's compatibility and narrative reconciliation step.

Output: A preferred `verify_completion/2` facade, a hidden logging helper implementation, facade-first docs parity, and a recorded Phase 17 breaking-change decision artifact.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-api-surface-boundary-audit/17-CONTEXT.md
@.planning/phases/17-api-surface-boundary-audit/17-RESEARCH.md
@.planning/phases/17-api-surface-boundary-audit/17-VALIDATION.md
@.planning/phases/17-api-surface-boundary-audit/17-02-SUMMARY.md
@.planning/phases/17-api-surface-boundary-audit/17-03-SUMMARY.md
@lib/rindle.ex
@lib/rindle/upload/broker.ex
@lib/rindle/live_view.ex
@README.md
@guides/getting_started.md
@test/rindle/api_surface_boundary_test.exs
@test/rindle/live_view_test.exs
@test/install_smoke/docs_parity_test.exs

<interfaces>
From lib/rindle.ex:
```elixir
def verify_upload(session_id, opts \\ [])
def complete_multipart_upload(session_id, parts, opts \\ [])
def log_variant_processing_failure(asset_id, variant_name, reason)
def attach(asset_or_id, owner, slot, _opts \\ [])
def url(profile, key, opts \\ [])
def upload(profile_module, upload, opts \\ [])
```

From lib/rindle/upload/broker.ex:
```elixir
def verify_completion(session_id, opts \\ [])
def complete_multipart_upload(session_id, parts, opts \\ [])
```

From lib/rindle/live_view.ex:
```elixir
case Rindle.verify_upload(session_id) do
  {:ok, %{asset: _asset}} -> ...
end
```
This call site must be updated to the preferred facade alias in this plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the preferred facade verification alias and align LiveView/tests</name>
  <files>lib/rindle.ex, lib/rindle/live_view.ex, test/rindle/api_surface_boundary_test.exs, test/rindle/live_view_test.exs</files>
  <behavior>
    - `Rindle.verify_completion/2` becomes the canonical direct-upload verification name.
    - `Rindle.verify_upload/2` remains available on `0.1.x` as a compatibility shim and is marked as legacy rather than removed.
    - `Rindle.complete_multipart_upload/3` remains unchanged.
  </behavior>
  <action>Add `Rindle.verify_completion/2` as a thin facade delegate to `Broker.verify_completion/2` per D-09. Keep `verify_upload/2` as an additive compatibility shim that delegates to `verify_completion/2`, and use docs-only deprecation signaling with `@doc deprecated: "Use verify_completion/2"` rather than the warning-emitting `@deprecated` attribute so `0.1.x` callers are guided without new compiler-noise surprises. Update `Rindle.LiveView` to call `Rindle.verify_completion/2` and revise module docs/tests so the preferred name appears everywhere new guidance is taught. Extend `test/rindle/api_surface_boundary_test.exs` and `test/rindle/live_view_test.exs` to assert the new alias and the continued compatibility export.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/rindle/api_surface_boundary_test.exs test/rindle/live_view_test.exs -x</automated>
  </verify>
  <done>`verify_completion/2` is exported and preferred, `verify_upload/2` still works as a compatibility shim, LiveView uses the preferred alias, and targeted tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hide the logging helper, rewrite onboarding docs, and record the semver decision</name>
  <files>lib/rindle.ex, lib/rindle/internal/variant_failure_logger.ex, README.md, guides/getting_started.md, test/rindle/api_surface_boundary_test.exs, test/install_smoke/docs_parity_test.exs, .planning/phases/17-api-surface-boundary-audit/17-BREAKING-CHANGE-DECISION.md</files>
  <behavior>
    - `Rindle.log_variant_processing_failure/3` no longer appears as documented public surface but remains callable through a thin shim in `0.1.x`.
    - README and getting-started docs teach the facade-first path with `Rindle` and `Rindle.Profile` as the introductory concepts.
    - The Phase 17 decision artifact explicitly reconciles D-03 against the older storage-adapter wording and records the `0.1.x`/`v0.2.0` boundary.
  </behavior>
  <action>Create `lib/rindle/internal/variant_failure_logger.ex` with `@moduledoc false` and move the actual logger call there. Change `Rindle.log_variant_processing_failure/3` into a narrow delegating shim marked `@doc false` per D-12/D-13. Rewrite `README.md` and `guides/getting_started.md` so the default onboarding path starts with `Rindle` and `Rindle.Profile`, demonstrates `Rindle.initiate_upload`, `Rindle.verify_completion`, `Rindle.attach`, and `Rindle.url`, and pushes `Rindle.Upload.Broker` down into advanced or reference material instead of the first-run path. Create `.planning/phases/17-api-surface-boundary-audit/17-BREAKING-CHANGE-DECISION.md` with sections for: the locked public allowlist, the D-03 override that keeps `Rindle.Storage.Local`/`Rindle.Storage.S3` public, the additive `0.1.x` changes shipped in Phase 17, and the explicit deferrals to `v0.2.0` for removing `verify_upload/2` and the logging shim if still warranted.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/rindle/api_surface_boundary_test.exs test/rindle/live_view_test.exs test/install_smoke/docs_parity_test.exs -x && mix docs --warnings-as-errors && rg -n "Storage\\.Local|Storage\\.S3|verify_completion/2|verify_upload/2|0\\.1\\.x|v0\\.2\\.0" .planning/phases/17-api-surface-boundary-audit/17-BREAKING-CHANGE-DECISION.md</automated>
  </verify>
  <done>The logging helper implementation is hidden, onboarding docs pass the new parity test, and the phase directory contains a written semver/boundary decision artifact that records the D-03 reconciliation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| facade API -> adopters | Naming and helper visibility determine the stable library contract |
| implementation helper -> public shim | Internal logging behavior remains callable through a narrow compatibility wrapper |
| planning artifact -> future phases | The semver decision file becomes the reference contract for Phase 18 and later cleanup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-10 | T | `Rindle.verify_upload/2` compatibility path | mitigate | Keep the old name as a delegate on `0.1.x` and mark it legacy instead of removing or renaming in place |
| T-17-11 | I | `Rindle.log_variant_processing_failure/3` | mitigate | Move implementation into a hidden internal module and mark the public shim `@doc false` so docs stop advertising it as operator API |
| T-17-12 | R | Breaking-change determination | mitigate | Write a phase-local decision artifact that names the D-03 override and the `v0.2.0` deferral boundary explicitly |
</threat_model>

<verification>
Finish with the targeted test bundle, a full docs build, and an `rg` check over the decision artifact so both code and recorded posture are verifiable.
</verification>

<success_criteria>
- `verify_completion/2` exists on `Rindle` and is the preferred documented name
- `verify_upload/2` still works on `0.1.x` as a compatibility shim
- `log_variant_processing_failure/3` is no longer part of the documented public surface
- README/getting-started docs pass facade-first parity checks
- `17-BREAKING-CHANGE-DECISION.md` records the D-03 storage-adapter override and the `v0.2.0` deferral posture
</success_criteria>

<output>
After completion, create `.planning/phases/17-api-surface-boundary-audit/17-04-SUMMARY.md`
</output>

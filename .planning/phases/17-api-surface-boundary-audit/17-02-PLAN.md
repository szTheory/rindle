---
phase: 17-api-surface-boundary-audit
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - mix.exs
  - lib/rindle/config.ex
  - lib/rindle/security/filename.ex
  - lib/rindle/security/mime.ex
  - lib/rindle/security/storage_key.ex
  - lib/rindle/security/upload_validation.ex
  - lib/rindle/profile/validator.ex
  - lib/rindle/profile/digest.ex
  - lib/rindle/storage/capabilities.ex
autonomous: true
requirements: [API-03, API-04]
tags: [api-boundary, exdoc, internal-modules]

must_haves:
  truths:
    - "Infrastructure and helper modules named in D-05 stop appearing as public API surface"
    - "ExDoc groups reflect the layered support tiers from D-14 without hiding the D-03 public adapter modules"
    - "The storage-adapter contradiction is resolved in favor of D-03: `Rindle.Storage.Local` and `Rindle.Storage.S3` stay public and grouped under storage adapters"
  artifacts:
    - path: mix.exs
      provides: "ExDoc module grouping contract for the intentional public surface"
      contains: "groups_for_modules"
    - path: lib/rindle/config.ex
      provides: "Internal runtime config module hidden from published docs"
      contains: "@moduledoc false"
    - path: lib/rindle/storage/capabilities.ex
      provides: "Capability helper hidden from public docs while public storage adapter modules remain visible"
      contains: "@moduledoc false"
  key_links:
    - from: mix.exs
      to: public modules from D-03/D-04
      via: "groups_for_modules defines the visible API tiers"
      pattern: "groups_for_modules"
    - from: test/rindle/api_surface_boundary_test.exs
      to: lib/rindle/config.ex and helper modules
      via: "Code.fetch_docs assertions"
      pattern: "@moduledoc false"
    - from: mix.exs
      to: lib/rindle/storage/local.ex and lib/rindle/storage/s3.ex
      via: "Storage Adapters group preserves public visibility per D-03"
      pattern: "Rindle\\.Storage\\.(Local|S3)"
---

<objective>
Hide the infrastructure/helper modules that are implementation-only and define the ExDoc grouping contract for the visible public surface.

Purpose: Phase 18 cannot safely add broad `@doc` coverage until the docs generator itself knows which modules are intentionally public. This plan handles the D-05 helper modules plus the D-14 module grouping in one coherent slice.

Output: One `mix.exs` ExDoc grouping update and `@moduledoc false` coverage across the infrastructure/helper modules listed here.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-api-surface-boundary-audit/17-CONTEXT.md
@.planning/phases/17-api-surface-boundary-audit/17-RESEARCH.md
@.planning/phases/17-api-surface-boundary-audit/17-VALIDATION.md
@.planning/phases/17-api-surface-boundary-audit/17-01-SUMMARY.md
@mix.exs
@lib/rindle/config.ex
@lib/rindle/security/filename.ex
@lib/rindle/security/mime.ex
@lib/rindle/security/storage_key.ex
@lib/rindle/security/upload_validation.ex
@lib/rindle/profile/validator.ex
@lib/rindle/profile/digest.ex
@lib/rindle/storage.ex
@lib/rindle/storage/local.ex
@lib/rindle/storage/s3.ex
@lib/rindle/storage/capabilities.ex
@test/rindle/api_surface_boundary_test.exs

<interfaces>
From mix.exs:
```elixir
defp docs do
  [
    main: "Rindle",
    extras: [...],
    groups_for_extras: [...]
  ]
end
```

From the locked context:
```text
D-03 public modules include Rindle.Storage, Rindle.Storage.Local, and Rindle.Storage.S3.
D-05 hidden helper modules include Rindle.Config, Rindle.Security.*, Rindle.Profile.Validator, Rindle.Profile.Digest, and Rindle.Storage.Capabilities.
D-14 requires groups_for_modules with explicit public tiers.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hide the internal runtime and helper modules from ExDoc per D-05/D-07</name>
  <files>lib/rindle/config.ex, lib/rindle/security/filename.ex, lib/rindle/security/mime.ex, lib/rindle/security/storage_key.ex, lib/rindle/security/upload_validation.ex, lib/rindle/profile/validator.ex, lib/rindle/profile/digest.ex, lib/rindle/storage/capabilities.ex</files>
  <action>Add `@moduledoc false` to each implementation-only helper module in this slice. Keep function bodies and runtime behavior unchanged; this plan is about visibility, not logic changes. Do not touch `Rindle.Storage`, `Rindle.Storage.Local`, or `Rindle.Storage.S3` beyond ensuring they remain documented, because D-03 explicitly overrides the older generic wording that suggested hiding them.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/rindle/api_surface_boundary_test.exs -x</automated>
  </verify>
  <done>The boundary harness passes for the helper-module denylist and still reports the public storage adapter modules as visible.</done>
</task>

<task type="auto">
  <name>Task 2: Define ExDoc module groups for the layered public surface</name>
  <files>mix.exs</files>
  <action>Add `groups_for_modules` to the `docs/0` config in `mix.exs` and organize the intentionally public modules into explicit tiers per D-14. Use the locked public surface as the source of truth: `Facade`, `Profiles`, `Upload`, `Delivery`, `Optional Integrations`, `Extension Points`, `Storage Adapters`, `Operations`, and `Data Types` are acceptable names. Include `Rindle.Storage`, `Rindle.Storage.Local`, and `Rindle.Storage.S3` in the visible storage group, and keep Mix tasks plus the two public maintenance workers in the operations group. Do not rely on omission alone to hide internal modules; those modules must already be hidden through `@moduledoc false`.</action>
  <verify>
    <automated>mix docs --warnings-as-errors</automated>
  </verify>
  <done>Generated docs build cleanly and reflect explicit public tiers without exposing the helper modules hidden in Task 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source modules -> generated docs | Module doc metadata becomes the published API contract |
| maintainer intent -> support tiers | ExDoc grouping translates the locked boundary into adopter-visible navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | I | Internal helper modules | mitigate | Apply `@moduledoc false` directly to each helper module instead of depending on group omission or prose |
| T-17-05 | T | `mix.exs` docs config | mitigate | Encode D-14 tiers in `groups_for_modules` so the public navigation matches the locked support boundary |
| T-17-06 | R | Storage-adapter contradiction | mitigate | Keep `Rindle.Storage.Local` and `Rindle.Storage.S3` explicitly listed in the storage group and covered by the boundary harness |
</threat_model>

<verification>
Run the boundary harness after Task 1 and `mix docs --warnings-as-errors` after Task 2.
</verification>

<success_criteria>
- All helper modules in this plan are hidden with `@moduledoc false`
- `mix.exs` contains an explicit `groups_for_modules` contract
- The storage adapters remain documented public surface per D-03
</success_criteria>

<output>
After completion, create `.planning/phases/17-api-surface-boundary-audit/17-02-SUMMARY.md`
</output>

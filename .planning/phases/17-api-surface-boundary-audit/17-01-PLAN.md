---
phase: 17-api-surface-boundary-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/rindle/api_surface_boundary_test.exs
  - test/install_smoke/docs_parity_test.exs
autonomous: true
requirements: [API-01, API-02, API-03, API-04]
tags: [api-boundary, exdoc, docs, nyquist]

must_haves:
  truths:
    - "Wave 0 exists for the missing API-surface audit coverage in 17-VALIDATION.md before boundary edits begin"
    - "The test harness encodes the locked public allowlist from D-03/D-04 and the hidden denylist from D-05/D-06, including the D-03 override that keeps Rindle.Storage.Local and Rindle.Storage.S3 public"
    - "Docs parity assertions go RED until README.md and guides/getting_started.md are rewritten to the facade-first path from D-02/D-15"
  artifacts:
    - path: test/rindle/api_surface_boundary_test.exs
      provides: "Boundary audit harness for public module visibility, compatibility-shim naming, and hidden-helper expectations"
      contains: "Code.fetch_docs"
    - path: test/install_smoke/docs_parity_test.exs
      provides: "Facade-first README/getting-started parity assertions aligned to Phase 17"
      contains: "Rindle.verify_completion"
  key_links:
    - from: test/rindle/api_surface_boundary_test.exs
      to: mix.exs
      via: "Compiled-doc visibility assertions must match ExDoc publication behavior"
      pattern: "Code\\.fetch_docs"
    - from: test/rindle/api_surface_boundary_test.exs
      to: lib/rindle.ex
      via: "Facade export assertions for verify_completion/2, verify_upload/2, and log_variant_processing_failure/3"
      pattern: "function_exported\\?"
    - from: test/install_smoke/docs_parity_test.exs
      to: README.md and guides/getting_started.md
      via: "Shared facade-first onboarding assertions"
      pattern: "Rindle\\.(initiate_upload|verify_completion|attach|url)"
---

<objective>
Create the missing Wave 0 validation harness for Phase 17 before any implementation work lands.

Purpose: `17-VALIDATION.md` is currently non-compliant because the phase has no dedicated API-boundary test artifact. This plan makes the public-vs-internal contract executable first so later plans can prove they honored D-03 through D-15 instead of relying on manual review.

Output: One new API-boundary ExUnit module and one updated docs parity test that both compile and run RED until the implementation plans land.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-api-surface-boundary-audit/17-CONTEXT.md
@.planning/phases/17-api-surface-boundary-audit/17-RESEARCH.md
@.planning/phases/17-api-surface-boundary-audit/17-VALIDATION.md
@mix.exs
@lib/rindle.ex
@lib/rindle/upload/broker.ex
@README.md
@guides/getting_started.md
@test/install_smoke/docs_parity_test.exs
@test/rindle_test.exs
@test/rindle/live_view_test.exs

<interfaces>
From lib/rindle.ex:
```elixir
def initiate_upload(profile, opts \\ [])
def verify_upload(session_id, opts \\ [])
def attach(asset_or_id, owner, slot, _opts \\ [])
def url(profile, key, opts \\ [])
def upload(profile_module, upload, opts \\ [])
def log_variant_processing_failure(asset_id, variant_name, reason)
```

From lib/rindle/upload/broker.ex:
```elixir
def verify_completion(session_id, opts \\ [])
def complete_multipart_upload(session_id, parts, opts \\ [])
```

From test/install_smoke/docs_parity_test.exs:
```elixir
assert doc =~ "Broker.initiate_session"
assert doc =~ "Broker.verify_completion"
```
These assertions are intentionally out of date for Phase 17 and should be replaced with facade-first checks per D-02/D-15.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the API surface audit harness from the locked boundary lists</name>
  <files>test/rindle/api_surface_boundary_test.exs</files>
  <behavior>
    - The test file fails RED until later plans hide internal modules and add the preferred facade alias.
    - The harness asserts D-03 public modules remain documented, including `Rindle.Storage.Local` and `Rindle.Storage.S3` despite older roadmap wording.
    - The harness asserts D-05/D-06 internal modules resolve to hidden docs and that `Rindle.log_variant_processing_failure/3` becomes a hidden compatibility shim while remaining callable.
  </behavior>
  <action>Create `test/rindle/api_surface_boundary_test.exs` with helper functions that normalize `Code.fetch_docs/1` results into visible/hidden assertions. Cover three groups explicitly: 1. the D-03/D-04 public allowlist (`Rindle`, `Rindle.Profile`, `Rindle.Upload.Broker`, `Rindle.Delivery`, `Rindle.Storage`, `Rindle.Storage.Local`, `Rindle.Storage.S3`, `Rindle.LiveView`, `Rindle.HTML`, `Rindle.Authorizer`, `Rindle.Analyzer`, `Rindle.Scanner`, `Rindle.Processor`, the two public maintenance workers, and the five domain schema modules); 2. the D-05/D-06 hidden denylist (`Rindle.Config`, `Rindle.Security.*`, `Rindle.Profile.Validator`, `Rindle.Profile.Digest`, `Rindle.Storage.Capabilities`, FSM modules, `Rindle.Domain.StalePolicy`, `Rindle.Ops.*`, and internal pipeline workers); 3. facade export expectations for `verify_completion/2`, `verify_upload/2`, `complete_multipart_upload/3`, and `log_variant_processing_failure/3`. Record the D-03 reconciliation in a test name or comment so later executors do not accidentally hide the storage adapters.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/rindle/api_surface_boundary_test.exs -x</automated>
  </verify>
  <done>The new test module compiles, runs, and fails only on missing Phase 17 implementation work rather than syntax or setup errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recast docs parity tests around the facade-first onboarding story</name>
  <files>test/install_smoke/docs_parity_test.exs</files>
  <behavior>
    - The updated parity test fails RED until README.md and guides/getting_started.md teach `Rindle`/`Rindle.Profile` first per D-02.
    - The assertions require `Rindle.verify_completion/2` as the canonical verification verb and treat broker-first onboarding as advanced-only material.
  </behavior>
  <action>Replace the current broker-first lifecycle assertions in `test/install_smoke/docs_parity_test.exs` with facade-first checks. Require both docs to mention `Rindle` and `Rindle.Profile` as the two first-tier concepts, require the first-run path to show `Rindle.initiate_upload`, `Rindle.verify_completion`, `Rindle.attach`, and `Rindle.url`, and keep the existing adopter-owned Repo, default Oban, explicit migrations, and “multipart is advanced” assertions. Add negative assertions that the introductory sections do not present `Rindle.Upload.Broker` as the default first-run entrypoint.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/install_smoke/docs_parity_test.exs -x</automated>
  </verify>
  <done>The parity test compiles, runs, and fails because the docs still teach the pre-Phase-17 story, not because the test itself is malformed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer -> public docs/tests | Boundary decisions become adopter-visible promises through tests and generated docs |
| compiled docs -> ExDoc site | Hidden-vs-public status crosses from source metadata into published API output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | T | `test/rindle/api_surface_boundary_test.exs` | mitigate | Encode the D-03/D-04 allowlist and D-05/D-06 denylist directly in the test so later edits cannot silently widen or shrink the contract |
| T-17-02 | I | README/getting-started parity | mitigate | Require facade-first wording in smoke tests before docs are edited, preventing accidental publication of stale broker-first onboarding |
| T-17-03 | R | D-03 storage-adapter contradiction | mitigate | Make the reconciliation executable by asserting `Rindle.Storage.Local` and `Rindle.Storage.S3` remain visible in the boundary harness |
</threat_model>

<verification>
Run the two RED-stage commands individually. Both should compile and fail only because Phase 17 implementation has not landed yet.
</verification>

<success_criteria>
- `test/rindle/api_surface_boundary_test.exs` exists and encodes the public/internal boundary lists from `17-CONTEXT.md`
- `test/install_smoke/docs_parity_test.exs` requires the facade-first onboarding story from D-02/D-15
- Wave 0 coverage for API-01/API-02/API-03/API-04 is no longer missing in execution
</success_criteria>

<output>
After completion, create `.planning/phases/17-api-surface-boundary-audit/17-01-SUMMARY.md`
</output>

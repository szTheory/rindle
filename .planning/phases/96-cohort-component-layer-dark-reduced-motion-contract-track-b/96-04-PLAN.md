---
phase: 96-cohort-component-layer-dark-reduced-motion-contract-track-b
plan: 04
type: execute
wave: 2
depends_on: ["96-01"]
files_modified:
  - brandbook/src/cohort-design-system-data.mjs
  - brandbook/src/cohort-contrast.mjs
  - .github/workflows/ci.yml
autonomous: true
requirements: [COHORT-06]

must_haves:
  truths:
    - "A net-new cohort-contrast.mjs gate asserts the light + dark Cohort contrast pairs at their stated minimums and exits non-zero on any failure"
    - "The gate parity-checks every --ck-* value in COHORT_CONTRAST_PAIRS byte-equal against the value scanned from cohort.css (drift = hard fail)"
    - "A coverage loop fails on a MISSING required {context × theme} pair, not only a failing one"
    - "A hand-rolled brace-depth literal scanner fails on any hex/rgb/rgba/hsl outside the token-definition blocks"
    - "The gate runs as a fast node step in the adoption-demo-e2e lane BEFORE the browser run, never in brandbook-tokens"
  artifacts:
    - path: "brandbook/src/cohort-design-system-data.mjs"
      provides: "COHORT_CONTRAST_PAIRS array with per-pair theme: light|dark + MIN_TARGET_PX=44"
      contains: "COHORT_CONTRAST_PAIRS"
    - path: "brandbook/src/cohort-contrast.mjs"
      provides: "cohort.css block resolver, WCAG ratio, coverage loop, parity check, literal scanner, pass/fail exit"
      contains: "process.exit"
    - path: ".github/workflows/ci.yml"
      provides: "Cohort contrast + literal gate node step in adoption-demo-e2e before the Playwright run"
      contains: "cohort-contrast.mjs"
  key_links:
    - from: "cohort-contrast.mjs resolver"
      to: "cohort.css :root / [data-theme] blocks"
      via: "block extractor scanning --ck-* → hex per theme"
      pattern: "data-theme"
    - from: "COHORT_CONTRAST_PAIRS pair values"
      to: "cohort.css scanned values"
      via: "byte-equal parity assertion (D-96-18)"
      pattern: "parity"
    - from: ".github/workflows/ci.yml adoption-demo-e2e"
      to: "cohort-contrast.mjs"
      via: "node step before 'Run adoption demo Playwright suite' (ci.yml:746-747)"
      pattern: "node brandbook/src/cohort-contrast.mjs"
---

<objective>
Author the net-new Cohort WCAG gate as a sibling of the admin pattern: a hand-maintained
`cohort-design-system-data.mjs` literal sink (`COHORT_CONTRAST_PAIRS`, each pair carrying
`theme: 'light'|'dark'`) and a `cohort-contrast.mjs` script that resolves `--ck-*` directly
from `cohort.css` per theme, asserts each pair clears its minimum, fails on a MISSING required
pair (coverage loop), byte-equal parity-checks the pair values against `cohort.css`, and runs a
brace-depth literal scanner — then wire it as a fast node step in the `adoption-demo-e2e` lane
before the browser run.

Purpose: Satisfies UI-SPEC acceptance gates 4 (grep-clean tokens, mechanically) and 5 (light +
dark contrast pairs pass). The parity + coverage + literal scanner are the hand-authored-file
equivalent of the generated lane's `git diff --exit-code`. Runs in Wave 2, parallel with the
primitives plan (no file overlap).
Output: `cohort-contrast.mjs` + `cohort-design-system-data.mjs` + a CI node step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-cohort-component-layer-dark-reduced-motion-contract-track-b/96-UI-SPEC.md
@.planning/phases/96-cohort-component-layer-dark-reduced-motion-contract-track-b/96-CONTEXT.md
@.planning/phases/96-cohort-component-layer-dark-reduced-motion-contract-track-b/96-PATTERNS.md
@.planning/phases/96-cohort-component-layer-dark-reduced-motion-contract-track-b/96-01-SUMMARY.md

<interfaces>
<!-- Analog shapes to copy (NEVER import admin-* into cohort-* — D-94-05/06). -->
From admin-design-system-data.mjs (pair-object shape to copy, :72-79):
- `export const CONSOLE_CONTRAST_PAIRS = [ { fg, bg, theme, min, context }, … ]`
- light+dark twinning: same `fg`/`bg`, two rows differing only in `theme` (:84-96)
- `export const MIN_TARGET_PX = 44` (:70)

From admin-contrast.mjs (structure to copy; :1-71):
- imports (node:fs/path/url) + `import { COHORT_CONTRAST_PAIRS } from './cohort-design-system-data.mjs'`
- `lum(hex)` sRGB-linearize (:27-34) + `ratio(a,b)` (:35-39) — COPY VERBATIM (per-script copy convention)
- coverage loop: required-context list, fail on missing (:43-49)
- per-pair assert + `if (failures) process.exit(1)` (:51-71)
- REPLACE the resolver: admin derefs tokens.json; Cohort must parse cohort.css blocks instead.

From admin-gallery-check.mjs (literal-scan analog, :96-102):
- `stripCssComments = (text) => text.replace(/\/\*[\s\S]*?\*\//g, '')` then assert a regex absent.

From cohort.css (Plan 01 output — the resolver source):
- light block `:root, [data-theme="light"]` and dark block `[data-theme="dark"]` (+ the
  `:root:not([data-theme])` auto-fallback) hold every `--ck-*` literal. Resolve `theme:'light'`
  pairs against the light block, `theme:'dark'` against the dark block.

From ci.yml adoption-demo-e2e lane:
- Node is set up at the "Set up Node" step (~:696-699); the browser run is "Run adoption demo
  Playwright suite" → `bash scripts/ci/adoption_demo_e2e.sh` (:746-747). Insert the new node
  step BEFORE that. brandbook-tokens is at :1144+ and is fenced to rindle-admin.css — DO NOT
  touch it.
</interfaces>

<constraints>
- D-96-01/02: net-new sibling files. DO NOT extend `brandbook/src/contrast.mjs` (theme-blind,
  tokens.json-driven). DO NOT route `--ck-*` through tokens.json. Cohort hard-codes its own
  light+dark hexes in the data module and resolves from cohort.css — never from tokens.json.
- D-96-03/21: the gate runs in the `adoption-demo-e2e` lane (Node already set up) as a fast
  node step BEFORE the Playwright run. NEVER in `brandbook-tokens` (regenerate-then-git-diff is
  meaningless for a hand-authored file).
- D-96-18: parity check — every `--ck-*` value in `COHORT_CONTRAST_PAIRS` must byte-equal the
  value scanned from the matching cohort.css block; drift = hard fail.
- D-96-19: coverage loop fails on a MISSING {context × theme} pair, not only a failing one.
- D-96-20: literal scanner is a hand-rolled ~30-line brace-depth Node pass (NOT stylelint);
  allow hex/rgb/rgba/hsl ONLY inside `:root`, `[data-theme="light"]`, `[data-theme="dark"]`,
  and the nested `:root:not([data-theme])` under `prefers-color-scheme`; allow
  currentColor/transparent/color-mix anywhere; fail elsewhere. Reuse one shared block extractor
  for both the resolver and the scanner.
- D-96-23: the pair list encodes `--ck-faint` as 3.0 decorative-only and `--ck-muted` as the
  4.5 readable-secondary pair — matching the UI-SPEC contrast table (lines 165-172).
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author cohort-design-system-data.mjs (the COHORT_CONTRAST_PAIRS literal sink)</name>
  <read_first>
    - brandbook/src/admin-design-system-data.mjs (read fully — the exact export shape, the
      light+dark twinning at :84-96, the status `.map` fan-out at :101-115, `MIN_TARGET_PX` at :70)
    - examples/adoption_demo/priv/static/assets/cohort.css (Plan 01 output — the actual
      light + dark `--ck-*` hex values the pairs must byte-equal)
    - 96-UI-SPEC.md "WCAG gate" contrast table (lines 163-178) + the D-96-23 correction note
    - 96-PATTERNS.md "cohort-design-system-data.mjs" section (lines 29-71) — the exact pair
      table to encode (lines 58-69)
  </read_first>
  <action>
    Create `brandbook/src/cohort-design-system-data.mjs` exporting `COHORT_CONTRAST_PAIRS`: a
    flat array of `{ fg, bg, theme, min, context }` objects encoding the UI-SPEC table (each
    text-on-surface pair gets a `light` AND a `dark` row, twinned like admin :84-96). Encode:
    `--ck-ink` on `--ck-surface` (4.5), `--ck-muted` on `--ck-surface` (4.5, readable
    secondary — the D-96-23 replacement for `--ck-faint`), `--ck-on-brand` on `--ck-btn-bg`
    (4.5, button text), `--ck-brand-strong` on `--ck-tint` (4.5 large/icon), each status
    (`ready`/`processing`/`quarantine`/`info`) on its own surface (4.5) via a `.map` fan-out
    mirroring admin :101-115, `--ck-ink` on `--ck-surface-2` (4.5 stat/nested), and
    `--ck-faint` on `--ck-bg` at **3.0** (decorative only, D-96-23). Use the bare token names
    (e.g. `ck-ink`) as `fg`/`bg` keys; the resolver in Task 2 maps them to `--ck-*` and reads
    cohort.css. Also `export const MIN_TARGET_PX = 44` (mirror admin :70). Each `context` string
    must contain a stable keyword the coverage loop checks (e.g. `table`, `stat`, `form`,
    `tabs`, `detail`, `toolbar`, `status`, `button`, `body`, plus `dark` for dark rows).
  </action>
  <acceptance_criteria>
    - `node -e "import('./brandbook/src/cohort-design-system-data.mjs').then(m => { console.log(m.COHORT_CONTRAST_PAIRS.length, m.MIN_TARGET_PX); })"` prints a pair count ≥ 14 (≥ 7 pair groups × 2 themes) and `44`.
    - Every pair carries a theme: `node -e "import('./brandbook/src/cohort-design-system-data.mjs').then(m => process.exit(m.COHORT_CONTRAST_PAIRS.every(p => p.theme === 'light' || p.theme === 'dark') ? 0 : 1))"` exits 0.
    - Both themes present: the array contains ≥ 1 `theme:'light'` and ≥ 1 `theme:'dark'` row (assert via the same node one-liner counting each).
    - D-96-23: there is a `ck-faint` pair with `min: 3` and a `ck-muted` pair with `min: 4.5` (`grep -c "fg: 'ck-faint'" brandbook/src/cohort-design-system-data.mjs` ≥ 1 with `min: 3` nearby; `grep -c "ck-muted" …` ≥ 1).
    - NO tokens.json import: `grep -c 'tokens.json\|admin-design-system-data' brandbook/src/cohort-design-system-data.mjs` returns 0.
  </acceptance_criteria>
  <done>
    `COHORT_CONTRAST_PAIRS` encodes every UI-SPEC pair in both themes with the D-96-23 roles,
    plus `MIN_TARGET_PX`, hand-authored with no tokens.json/admin coupling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author cohort-contrast.mjs (resolver + WCAG + coverage + parity + literal scan) and wire the CI step</name>
  <read_first>
    - brandbook/src/admin-contrast.mjs (read fully — imports/`lum`/`ratio` :1-39 to copy
      verbatim, coverage loop :43-49, per-pair assert + exit :51-71; the resolver :11-25 is the
      part to REPLACE)
    - brandbook/src/admin-gallery-check.mjs (:96-102 `stripCssComments` + assert idiom for the
      literal scanner)
    - brandbook/src/cohort-design-system-data.mjs (Task 1 output)
    - examples/adoption_demo/priv/static/assets/cohort.css (Plan 01 output — the blocks the
      resolver parses; confirm exact selectors `:root, [data-theme="light"]`,
      `[data-theme="dark"]`, `:root:not([data-theme])`)
    - .github/workflows/ci.yml (the adoption-demo-e2e lane: "Set up Node" ~:696, "Run adoption
      demo Playwright suite" :746-747; the brandbook-tokens lane :1144+ to AVOID)
    - 96-PATTERNS.md "cohort-contrast.mjs" section (lines 75-126) + "ci.yml" section (lines
      356-372)
  </read_first>
  <action>
    Create `brandbook/src/cohort-contrast.mjs`. Copy verbatim from `admin-contrast.mjs`: the
    node imports, `lum(hex)` (:27-34), `ratio(a,b)` (:35-39), the coverage-loop shape (:43-49),
    and the per-pair assert + `if (failures) process.exit(1)` (:51-71). REPLACE the resolver
    (D-96-02/18): write a shared `extractBlock(css, selector)` brace-depth helper that returns
    the body of a top-level block by selector, then `resolve(name, theme)` that reads the light
    block (`:root, [data-theme="light"]`) for `theme:'light'` and the dark block
    (`[data-theme="dark"]`) for `theme:'dark"`, mapping `ck-x` → the `--ck-x: <value>;`
    declaration. Implement the coverage loop (D-96-19) against a hard-coded required
    `{context × theme}` matrix derived from the UI-SPEC table (table/stat/form/tabs/detail/
    toolbar/status/focus × light+dark) — push a FAIL + increment failures on a MISSING context,
    not only a failing ratio. Add the parity check (D-96-18): for each `--ck-*` referenced by a
    pair, assert the value the resolver returned byte-equals the value scanned from the matching
    cohort.css block; drift → push FAIL + `process.exit(1)`. Add the literal scanner (D-96-20):
    a separate ~30-line pass reusing `extractBlock`/comment-strip — strip comments, walk
    top-level blocks by brace depth, classify each by selector, allow hex/`rgb`/`rgba`/`hsl`
    ONLY inside `:root`, `[data-theme="light"]`, `[data-theme="dark"]`, and the nested
    `:root:not([data-theme])` under `prefers-color-scheme`; allow currentColor/transparent/
    color-mix anywhere; fail elsewhere. The script's overall exit is non-zero if ANY of
    contrast/coverage/parity/literal fails. Finally, in `.github/workflows/ci.yml` add a node
    step named "Cohort contrast + literal gate" running `node brandbook/src/cohort-contrast.mjs`
    in the `adoption-demo-e2e` lane, positioned AFTER "Set up Node" and BEFORE "Run adoption
    demo Playwright suite" (ci.yml:746-747). DO NOT touch the brandbook-tokens lane.
  </action>
  <acceptance_criteria>
    - UI-SPEC gate 5 (contrast passes both themes): `node brandbook/src/cohort-contrast.mjs` exits 0 and prints `cohort contrast: N/N pairs pass` with no FAIL lines (against the Plan 01 cohort.css).
    - Resolver reads cohort.css, not tokens.json: `grep -c 'tokens.json' brandbook/src/cohort-contrast.mjs` returns 0; `grep -c 'cohort.css' brandbook/src/cohort-contrast.mjs` returns ≥ 1.
    - D-96-19 coverage loop present + fails on omission: temporarily removing a required pair from the data module makes `node brandbook/src/cohort-contrast.mjs` exit non-zero with a "missing" FAIL line (executor verifies once, then restores). `grep -ci 'missing' brandbook/src/cohort-contrast.mjs` returns ≥ 1.
    - D-96-18 parity present: `grep -ci 'parity\|byte\|!==' brandbook/src/cohort-contrast.mjs` returns ≥ 1; mutating a pair value to differ from cohort.css makes the script exit non-zero (executor verifies once, then restores).
    - D-96-20 literal scanner present + catches a planted literal: `grep -c 'stripCss\|brace\|depth\|/#\[0-9a-fA-F\]\|rgba\\?\\(' brandbook/src/cohort-contrast.mjs` returns ≥ 1; the scanner fails when a hex is planted in a rule body (executor verifies once against a temp copy, then restores).
    - UI-SPEC gate 4 (mechanical): the scanner passes clean against the Plan 01 cohort.css (`node brandbook/src/cohort-contrast.mjs` exits 0 — literals only in token blocks).
    - D-96-03 lane placement: `grep -c 'cohort-contrast.mjs' .github/workflows/ci.yml` returns 1, and it appears in the `adoption-demo-e2e` job (before the Playwright step) NOT in `brandbook-tokens` — confirm with `awk '/adoption-demo-e2e:/{f=1} /brandbook-tokens:/{f=0} f && /cohort-contrast.mjs/' .github/workflows/ci.yml` returns the line, and `awk '/brandbook-tokens:/{f=1} f && /cohort-contrast.mjs/' .github/workflows/ci.yml` returns nothing.
    - CI YAML still valid: `node -e "require('js-yaml')" 2>/dev/null && node -e "const y=require('js-yaml');y.load(require('fs').readFileSync('.github/workflows/ci.yml','utf8'));console.log('ok')" || python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml')); print('ok')"` prints `ok`.
  </acceptance_criteria>
  <done>
    `cohort-contrast.mjs` resolves `--ck-*` from cohort.css per theme, passes the light+dark
    pairs at their minimums, fails on a missing required pair (coverage), byte-equal
    parity-checks against cohort.css, runs a hand-rolled literal scanner, and is wired as a fast
    node step in the `adoption-demo-e2e` lane before the browser run — never in brandbook-tokens.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → repo files | the node scripts read repo files (cohort.css) and execute in CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-08 | Tampering | cohort-contrast.mjs CI execution | mitigate | Scripts only `readFileSync` repo-local files (cohort.css, the data module) and compute — no network, no shell-out, no eval, no install. Hand-authored, reviewed in PR. |
| T-96-09 | Elevation of Privilege | new ci.yml step | mitigate | A single `node` invocation in an existing lane; no new secrets, no new permissions, no new third-party action. brandbook-tokens lane untouched (separate-build-step boundary preserved). |
| T-96-10 | Tampering (supply chain) | npm installs | mitigate | NONE introduced — the scripts use only Node built-ins (`node:fs`/`node:path`/`node:url`); no new package install, so the package-legitimacy gate is N/A. |

No high-severity threats: pure-Node, built-in-only, read-only CI scripts. No package installs
(package legitimacy gate not triggered).
</threat_model>

<verification>
- UI-SPEC gates 4 + 5 pass mechanically: `node brandbook/src/cohort-contrast.mjs` exits 0
  against the Plan 01 cohort.css (contrast + coverage + parity + literal all green).
- The three negative tests (planted missing-pair / drifted-value / planted-literal) each make
  the script exit non-zero, then are restored.
- The CI step lands in `adoption-demo-e2e` before the Playwright run, not in brandbook-tokens;
  YAML is valid.
</verification>

<success_criteria>
- Net-new `cohort-design-system-data.mjs` + `cohort-contrast.mjs` siblings (no tokens.json /
  admin coupling).
- Gate asserts light+dark pairs, fails on missing pair, parity-checks byte-equal, scans literals.
- Wired as a fast node step in `adoption-demo-e2e` before the browser run; brandbook-tokens
  untouched.
</success_criteria>

<output>
Create `.planning/phases/96-cohort-component-layer-dark-reduced-motion-contract-track-b/96-04-SUMMARY.md` when done
</output>

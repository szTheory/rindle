---
phase: 96-cohort-component-layer-dark-reduced-motion-contract-track-b
plan: 05
type: execute
wave: 4
depends_on: ["96-03", "96-04"]
files_modified:
  - examples/adoption_demo/e2e/cohort-styleguide.spec.js
autonomous: true
requirements: [COHORT-06]

must_haves:
  truths:
    - "A Playwright spec drives /styleguide: reduced-motion probe, theme toggle (light+dark), polish gate, and colorScheme auto-fallback probe"
    - "The reduced-motion computed probe runs BEFORE assertAdminPolish injects freezeMotion and proves .ck-reveal resolves to opacity:1/transform:none/animation-name:none"
    - "assertAdminPolish is reused UNCHANGED with root:[data-ck-root] and the .ck-* interactiveSelectors, in warn/report mode this phase"
    - "A component-existence loop asserts each of the 6 L1 + 4 L2 primitives is visible at /styleguide via data-ck-section"
    - "emulateMedia is called only AFTER goto/waitForLiveSocket"
  artifacts:
    - path: "examples/adoption_demo/e2e/cohort-styleguide.spec.js"
      provides: "Cohort styleguide e2e: reduced-motion probe, theme toggle + polish (light+dark), component-existence loop, auto-fallback probe, rendered contrast"
      contains: "assertAdminPolish"
  key_links:
    - from: "cohort-styleguide.spec.js"
      to: "e2e/support/admin-polish.js assertAdminPolish"
      via: "require + call with { root: '[data-ck-root]', interactiveSelectors }"
      pattern: "root: \"\\[data-ck-root\\]\""
    - from: "cohort-styleguide.spec.js selectCohortTheme"
      to: "/styleguide data-ck-theme toggle"
      via: "click + assert data-theme flipped"
      pattern: "data-ck-theme"
---

<objective>
Author the net-new `cohort-styleguide.spec.js` Playwright spec that drives `/styleguide` in the
exact D-96-21 order — `emulateMedia(reduce)` → reduced-motion computed probe (before
`freezeMotion`) → `emulateMedia(no-preference)` → toggle-light → polish → toggle-dark → polish
→ `emulateMedia(colorScheme:dark)` auto-fallback probe — reusing `assertAdminPolish` unchanged
against `[data-ck-root]`/`.ck-*` in warn mode, plus a component-existence loop and a rendered
contrast assertion.

Purpose: Satisfies UI-SPEC acceptance gates 1 (component existence at `/styleguide`), 3
(reduced-motion settles `.ck-reveal`), and 6 (polish harness over `[data-ck-root]`), and proves
the auto-fallback distinct from the explicit `[data-theme]` contract. Rides the existing
Playwright suite step (Plan 04 wired the node gate; no new browser job).
Output: `examples/adoption_demo/e2e/cohort-styleguide.spec.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-cohort-component-layer-dark-reduced-motion-contract-track-b/96-UI-SPEC.md
@.planning/phases/96-cohort-component-layer-dark-reduced-motion-contract-track-b/96-CONTEXT.md
@.planning/phases/96-cohort-component-layer-dark-reduced-motion-contract-track-b/96-PATTERNS.md
@.planning/phases/96-cohort-component-layer-dark-reduced-motion-contract-track-b/96-03-SUMMARY.md
@.planning/phases/96-cohort-component-layer-dark-reduced-motion-contract-track-b/96-04-SUMMARY.md

<interfaces>
<!-- Contracts the spec consumes (do not re-explore). -->
From e2e/support/admin-polish.js (reuse UNCHANGED — D-94-07 seam):
- `assertAdminPolish(page, { root, interactiveSelectors, surface, viewport })` (:459-506); admin
  calls it at admin-screenshots.spec.js:77-79; warn idiom (`OVERLAP_ENFORCED=false`) at :30,:487-498.
- `freezeMotion` is injected inside assertAdminPolish (:63-72) — it MASKS the reduced-motion
  probe, so the probe MUST run before the first assertAdminPolish call.
- exposes `assertReadableContrast` for rendered contrast over a root.

From e2e/support/liveview.js:
- `waitForLiveSocket(page)` (module.exports at :7).

From e2e/support/admin.js (theme-toggle click→assert analog, :57-62):
- `selectAdminTheme(page, theme)`: click `[data-theme-control]`, assert `data-theme` flipped +
  `aria-pressed`. Cohort mirrors with `selectCohortTheme` using `[data-ck-theme="…"]` →
  asserts `[data-ck-root]` has `data-theme` + control `aria-pressed="true"`.

From StyleguideLive (Plan 03 output):
- root `<div class="ck" data-ck-root data-theme={@theme}>`; toggle buttons `data-ck-theme="light|dark"`;
  sections `data-ck-section="table|stat|form|tabs|detail|toolbar|…"` + the four L2 group names;
  `.ck-reveal` content present.

From admin-gallery-check.mjs (component-existence loop analog, :79-94):
- `assertVisible` loop over a required matrix; emulateMedia precedent at :346,:352.

From e2e/support/cohort.js (existing Cohort e2e helpers — may add `selectCohortTheme` here or
keep it local to the spec).
</interfaces>

<constraints>
- D-96-06: reuse `assertAdminPolish` UNCHANGED with `{ root: "[data-ck-root]",
  interactiveSelectors: [".ck-btn", ".ck-tab", ".ck-input", ".ck-select", "[data-ck-theme]"] }`.
  DO NOT edit admin-polish.js. Run in WARN/report mode this phase (warn→fail is Phase 102) —
  the spec asserts the gate RAN and reports, it does not hard-fail on offenders this phase.
- D-96-21: spec order is exactly `goto` → `waitForLiveSocket` → `emulateMedia(reduce)` →
  reduced-motion computed probe (BEFORE any freeze) → `emulateMedia(no-preference)` →
  toggle-light → polish → toggle-dark → polish → `emulateMedia(colorScheme:dark)` auto-fallback
  probe. Call `emulateMedia` ONLY AFTER `goto`/`waitForLiveSocket` (Playwright drops it across
  nav — issue #31328).
- D-96-08: reduced-motion + auto-fallback are exercised via `page.emulateMedia({ reducedMotion,
  colorScheme })`, not a CSS-only static panel. The reduce probe asserts `.ck-reveal` computed
  `opacity:1` / `transform:none` / `animation-name:none`; the colorScheme:dark probe proves the
  media fallback distinct from the explicit `[data-theme]`.
- D-96-19 (gate 1): a component-existence loop asserts each of the 6 L1 + 4 L2 primitives is
  visible at `/styleguide` via its `data-ck-section` marker — assert on the test markers, never
  on `.ck-*` styling classes (D-96-16).
- Chromium-only suite (matches the existing harness); use `require(...)` CommonJS like the
  sibling specs.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author cohort-styleguide.spec.js — ordered emulateMedia/probe/toggle/polish drive + component-existence + rendered contrast</name>
  <read_first>
    - examples/adoption_demo/e2e/admin-screenshots.spec.js (read fully — the structure +
      assertAdminPolish call at :77-79 + emulateMedia usage)
    - examples/adoption_demo/e2e/support/admin-polish.js (:459-506 the signature, :63-72
      freezeMotion injection, :30/:487-498 warn idiom, the `assertReadableContrast` export)
    - examples/adoption_demo/e2e/support/admin.js (:57-62 `selectAdminTheme` click→assert)
    - examples/adoption_demo/e2e/support/liveview.js (`waitForLiveSocket`)
    - examples/adoption_demo/e2e/support/cohort.js (existing helper module)
    - brandbook/src/admin-gallery-check.mjs (:79-94 the assertVisible existence-loop shape,
      :346/:352 emulateMedia precedent)
    - 96-PATTERNS.md "cohort-styleguide.spec.js" section (lines 271-314) — the exact call shape,
      `selectCohortTheme`, the ordered drive, the component-existence loop
    - 96-UI-SPEC.md "Machine-Verifiable Acceptance" gates 1/3/6 (lines 260-276)
  </read_first>
  <action>
    Create `examples/adoption_demo/e2e/cohort-styleguide.spec.js` (CommonJS, Chromium).
    `require` `{ test, expect }` from `@playwright/test`, `assertAdminPolish` (+
    `assertReadableContrast` if exported) from `./support/admin-polish`, `waitForLiveSocket`
    from `./support/liveview`. Define a local `selectCohortTheme(page, theme)` that clicks
    `[data-ck-theme="${theme}"]`, then `await expect(page.locator("[data-ck-root]"))
    .toHaveAttribute("data-theme", theme)` and asserts the control `aria-pressed="true"`. In
    one `test`, drive in the EXACT D-96-21 order: `await page.goto("/styleguide")` →
    `await waitForLiveSocket(page)` → `await page.emulateMedia({ reducedMotion: "reduce" })` →
    REDUCED-MOTION PROBE: read `.ck-reveal` computed style (`getComputedStyle` via
    `page.locator(".ck-reveal").first().evaluate(...)`) and assert `opacity === "1"`,
    `transform === "none"`, and `animation-name === "none"` — this runs BEFORE the first
    `assertAdminPolish` so `freezeMotion` cannot mask it (gate 3) →
    `await page.emulateMedia({ reducedMotion: "no-preference" })` → `selectCohortTheme(page,
    "light")` → `await assertAdminPolish(page, { root: "[data-ck-root]", interactiveSelectors:
    [".ck-btn", ".ck-tab", ".ck-input", ".ck-select", "[data-ck-theme]"], surface:
    "styleguide-light" })` → `selectCohortTheme(page, "dark")` → `assertAdminPolish(... surface:
    "styleguide-dark")` → `await page.emulateMedia({ colorScheme: "dark" })` and probe the
    auto-fallback: assert the root's computed background/ink reflects the dark token set while
    no explicit `[data-theme]` is forced (distinct path from the toggle). Add the
    COMPONENT-EXISTENCE loop (gate 1, D-96-19): iterate the required matrix `["table", "stat",
    "form", "tabs", "detail", "toolbar", "data-table-block", "stat-row", "detail-panel",
    "tabbed-section"]` and `await expect(page.locator('[data-ck-section="' + name + '"]')
    .first()).toBeVisible()`. Add a rendered-contrast assertion via `assertReadableContrast`
    over `[data-ck-root]` if available (catches cascade bugs the token-pair gate misses). Keep
    the polish gate in WARN mode (do not hard-fail on offenders — assert it ran/reported per the
    admin-polish warn idiom).
  </action>
  <acceptance_criteria>
    - D-96-06 reuse UNCHANGED: `grep -c 'root: "\[data-ck-root\]"' …/cohort-styleguide.spec.js` returns ≥ 2 (light + dark calls); admin-polish.js is NOT modified (`git diff --name-only` does not list `e2e/support/admin-polish.js`).
    - D-96-06 interactiveSelectors: `grep -c '.ck-btn\|.ck-tab\|.ck-input' …/cohort-styleguide.spec.js` returns ≥ 1.
    - D-96-21 ordering: in the spec, `emulateMedia` first appears AFTER `goto` and `waitForLiveSocket` (`grep -n 'goto\|waitForLiveSocket\|emulateMedia' …/cohort-styleguide.spec.js` shows goto+waitForLiveSocket line numbers BEFORE the first emulateMedia line); the `.ck-reveal` probe line number is BEFORE the first `assertAdminPolish` line number.
    - Gate 3 reduced-motion probe: `grep -c 'reducedMotion: "reduce"' …/cohort-styleguide.spec.js` returns 1 and the probe asserts `opacity`, `transform`, and `animation-name` on `.ck-reveal` (`grep -c 'ck-reveal' …/cohort-styleguide.spec.js` ≥ 1; `grep -c 'animation-name\|animationName' …/cohort-styleguide.spec.js` ≥ 1).
    - colorScheme auto-fallback probe: `grep -c 'colorScheme: "dark"' …/cohort-styleguide.spec.js` returns 1.
    - Gate 1 component-existence loop: `grep -c 'data-ck-section' …/cohort-styleguide.spec.js` returns ≥ 1 and the iterated matrix includes the 6 L1 + 4 L2 names (`grep -c 'table\|stat\|form\|tabs\|detail\|toolbar\|data-table-block\|stat-row\|detail-panel\|tabbed-section' …/cohort-styleguide.spec.js` ≥ 1).
    - selectCohortTheme asserts the flip: `grep -c 'toHaveAttribute("data-theme"' …/cohort-styleguide.spec.js` returns ≥ 1; `grep -c 'aria-pressed' …/cohort-styleguide.spec.js` returns ≥ 1.
    - The spec runs green against a booted demo: `cd examples/adoption_demo && (mix phx.server &) ; sleep 8 ; npx playwright test e2e/cohort-styleguide.spec.js --project=chromium ; kill %1` exits 0 (executor may instead run `bash scripts/ci/adoption_demo_e2e.sh` filtered to this spec if simpler; the spec must pass).
  </acceptance_criteria>
  <done>
    `cohort-styleguide.spec.js` drives `/styleguide` in the exact D-96-21 order, proves the
    reduced-motion contract before any freeze, runs the polish gate (warn mode) over
    `[data-ck-root]` in both themes via the toggle, asserts the auto-fallback path, loops the
    component-existence matrix, and passes against the booted demo.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright runner → /styleguide | the spec drives a browser against the static demo page in CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-11 | Tampering | reused assertAdminPolish | mitigate | Reused UNCHANGED via `{root, interactiveSelectors}` params (D-94-07 seam); the spec adds no new privileged behavior. admin-polish.js stays byte-identical (asserted). |
| T-96-12 | Denial of Service | flaky e2e | accept | Chromium-only, deterministic harness (`workers:1`, `freezeMotion`, `animations:disabled`); the reduced-motion probe runs before freeze by design, not by timing. Warn-mode this phase. |

No high-severity threats: a read-only browser spec over a static reference page; no new package
install (uses the existing `@playwright/test` already in the harness — package legitimacy gate
N/A).
</threat_model>

<verification>
- UI-SPEC gates 1, 3, 6 pass via the spec: component-existence loop, `.ck-reveal` reduced-motion
  probe (before freeze), and the `assertAdminPolish` run over `[data-ck-root]` in both themes.
- The colorScheme auto-fallback probe proves the media path distinct from `[data-theme]`.
- The spec passes against a booted demo; `admin-polish.js` is unmodified.
</verification>

<success_criteria>
- `cohort-styleguide.spec.js` drives `/styleguide` in the exact D-96-21 order.
- Reduces-motion probe before freeze; polish gate (warn) over `[data-ck-root]`/`.ck-*` in
  light + dark via the toggle; auto-fallback probe; component-existence loop over the 6 L1 + 4
  L2 primitives.
- `assertAdminPolish` reused unchanged; spec passes against the booted demo.
</success_criteria>

<output>
Create `.planning/phases/96-cohort-component-layer-dark-reduced-motion-contract-track-b/96-05-SUMMARY.md` when done
</output>

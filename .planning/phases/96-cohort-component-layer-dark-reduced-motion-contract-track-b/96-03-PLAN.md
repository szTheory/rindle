---
phase: 96-cohort-component-layer-dark-reduced-motion-contract-track-b
plan: 03
type: execute
wave: 3
depends_on: ["96-02"]
files_modified:
  - examples/adoption_demo/lib/adoption_demo_web/live/styleguide_live.ex
  - examples/adoption_demo/lib/adoption_demo_web/router.ex
autonomous: true
requirements: [COHORT-06]

must_haves:
  truths:
    - "/styleguide is a reachable LiveView route in the existing :browser scope"
    - "The page shell carries data-ck-root and data-theme on the per-LiveView .ck div (not on <body>)"
    - "A theme toggle button group flips data-theme=light|dark on the root via phx-click (server-owned, no localStorage)"
    - "Every Level-1 primitive + the four Level-2 compositions render in the gallery, each in a section carrying stable data-ck-section / data-ck-state markers"
    - "Gallery rows use real Cohort fiction (lesson-video processing, quarantined upload, empty member list), not lorem"
  artifacts:
    - path: "examples/adoption_demo/lib/adoption_demo_web/live/styleguide_live.ex"
      provides: "StyleguideLive: .ck shell with data-ck-root/data-theme, theme toggle, server-owned sort state, seeded fiction, all primitive sections"
      contains: "data-ck-root"
    - path: "examples/adoption_demo/lib/adoption_demo_web/router.ex"
      provides: "live(\"/styleguide\", StyleguideLive, :index) in the :browser scope"
      contains: "/styleguide"
  key_links:
    - from: "router.ex :browser scope"
      to: "StyleguideLive"
      via: "live(\"/styleguide\", StyleguideLive, :index)"
      pattern: "StyleguideLive"
    - from: "StyleguideLive theme toggle"
      to: ".ck root data-theme"
      via: "phx-click set_theme handler assigning :theme"
      pattern: "data-theme={@theme}"
    - from: "StyleguideLive sort header"
      to: "ck_table sort_by/sort_dir assigns"
      via: "phx-click set_sort handler (server-owned sort state, D-96-15)"
      pattern: "set_sort"
---

<objective>
Build `StyleguideLive` and add the `/styleguide` route: a developer-facing gallery rendering
every Level-1 primitive + the four Level-2 compositions across light/dark, with the
`data-ck-root`/`data-theme` polish seam on the per-LiveView `.ck` div, a server-owned theme
toggle, server-owned table sort, stable `data-ck-section`/`data-ck-state` test markers, and
real seeded Cohort fiction.

Purpose: This is the proof surface — the VIS-04 audit reference and the route the Plan 05
Playwright spec drives. Satisfies UI-SPEC acceptance gate 1 (primitives reachable at
`/styleguide`) and provides the `data-ck-root` seam for gate 6. Composes only from the Plan 02
primitives.
Output: A reachable `/styleguide` LiveView gallery.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-cohort-component-layer-dark-reduced-motion-contract-track-b/96-UI-SPEC.md
@.planning/phases/96-cohort-component-layer-dark-reduced-motion-contract-track-b/96-CONTEXT.md
@.planning/phases/96-cohort-component-layer-dark-reduced-motion-contract-track-b/96-PATTERNS.md
@.planning/phases/96-cohort-component-layer-dark-reduced-motion-contract-track-b/96-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor builds against (do not re-explore). -->
From cohort_components.ex (Plan 02 output — the primitives to render):
- `ck_table` (server-owned `sort_by`/`sort_dir` + `:col`/`rows`), `ck_stat`, `ck_field`/`ck_input`/`ck_select` (consume `Phoenix.HTML.FormField`), `ck_tabs`, `ck_detail`, `ck_toolbar`, plus existing `ck_button`/`badge`/`cohort_nav`/`hero`.

From launchpad_live.ex (shell analog to copy):
- Module head: `defmodule AdoptionDemoWeb.LaunchpadLive do use AdoptionDemoWeb, :live_view import AdoptionDemoWeb.CohortComponents` (:1-11)
- `mount/3` returning `{:ok, assign(socket, …)}` (:72-83)
- `render/1` emitting `<div class="ck"> <.cohort_nav active={:home} /> <main class="ck__wrap"> …` (:88)

From router.ex (the :browser scope to extend):
- `scope "/", AdoptionDemoWeb do pipe_through(:browser) live("/", LaunchpadLive, :index) … end` (:20-32) — add the `/styleguide` line here.

From admin-gallery.mjs (gallery IA + theme-toggle markup analog — shape only, not importable):
- One panel per component; theme picker button group at :347-350; `data-rindle-admin-component`/`data-rindle-admin-state` markers at :147,:356 (Cohort uses `data-ck-section`/`data-ck-state`).
</interfaces>

<constraints>
- D-96-05: `data-ck-root` + `data-theme={@theme}` go on the per-LiveView `.ck` div (mirror
  launchpad_live.ex:88), NEVER on `<body>` in root.html.heex (a body-level root would scoop
  daisyUI inner-app chrome into the single-root polish query).
- D-96-04: add `/styleguide` as ONE `live(...)` line in the EXISTING `:browser` scope; the
  demo already boots a real Phoenix server with seeds in CI — no harness change.
- D-96-07/16: theme is SERVER state (`assign(:theme)` + `phx-click`), no localStorage (avoids
  the e2e race). Toggle is a button group setting `data-theme="light"|"dark"`, each button with
  `aria-pressed` + a `data-ck-theme` marker.
- D-96-16: emit stable `data-ck-section` / `data-ck-state` test markers SEPARATE from the BEM
  styling classes — the spec asserts on these, never on `.ck-*` styling classes.
- D-96-22: seed the gallery with REAL Cohort fiction (a lesson-video row going `processing`, a
  quarantined upload, an empty member list), not lorem. Add a second empty-state copy variant
  ("never-populated" vs "filtered") for Phases 99/100. Form fields demo real semantics via
  `to_form` + `Phoenix.HTML.FormField` (D-96-15). Sort state is server-owned (D-96-15).
- Copy: use the exact UI-SPEC Copywriting Contract strings for empty/error/loading/button
  samples (lines 237-244) — `Nothing here yet`, `No records match this view…`, `Apply filters`,
  `Save changes`, `{Field} is required.`
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StyleguideLive shell with the data-ck-root seam, server theme toggle, and server-owned sort; wire the route</name>
  <read_first>
    - examples/adoption_demo/lib/adoption_demo_web/live/launchpad_live.ex (shell analog — read
      :1-11 module head, :72-83 mount, :88 `<div class="ck">` shell, :159-206 `cast_icon`
      private-clause icon idiom)
    - examples/adoption_demo/lib/adoption_demo_web/router.ex (:20-32 the `:browser` scope)
    - examples/adoption_demo/lib/adoption_demo_web/components/cohort_components.ex (Plan 02 —
      the primitive function signatures to call)
    - 96-PATTERNS.md "StyleguideLive" section (lines 129-174) — module skeleton, mount+theme
      handler, shell-with-seam markup, theme-toggle markup, test markers
    - 96-UI-SPEC.md "Theme Contract" + "Net-new surfaces this phase" (lines 42-48, 127-141)
  </read_first>
  <action>
    Create `examples/adoption_demo/lib/adoption_demo_web/live/styleguide_live.ex` as
    `AdoptionDemoWeb.StyleguideLive` (`use AdoptionDemoWeb, :live_view`; `import
    AdoptionDemoWeb.CohortComponents`). `mount/3` assigns `page_title: "Cohort styleguide"`,
    `theme: "light"`, server-owned `sort_by`/`sort_dir`, and the seeded fiction (see Task 2).
    Add `handle_event("set_theme", %{"theme" => t}, socket) when t in ~w(light dark)` →
    `assign(socket, theme: t)` and `handle_event("set_sort", %{"key" => k}, socket)` toggling
    `sort_dir` and setting `sort_by` (validate `k` against the known column set). `render/1`
    emits `<div class="ck" data-ck-root data-theme={@theme}>` with `<.cohort_nav active={nil}
    />` and a `<main class="ck__wrap">`; inside, a theme toggle `role="group"
    aria-label="Theme"` with two buttons (`phx-click="set_theme"` `phx-value-theme="light|dark"`,
    `aria-pressed={@theme == …}`, `data-ck-theme="light|dark"`, ≥44px). Add the route line
    `live("/styleguide", StyleguideLive, :index)` inside the existing `:browser` scope in
    `router.ex`. (Primitive sections are filled in Task 2.)
  </action>
  <acceptance_criteria>
    - Route reachable: `grep -c 'live("/styleguide", StyleguideLive' …/router.ex` returns 1.
    - D-96-05 seam on the `.ck` div, NOT body: `grep -c 'data-ck-root' …/styleguide_live.ex` returns ≥ 1; `grep -c 'data-theme={@theme}' …/styleguide_live.ex` returns 1; `grep -rc 'data-ck-root' examples/adoption_demo/lib/adoption_demo_web/components/layouts/ examples/adoption_demo/lib/adoption_demo_web/*root*.heex 2>/dev/null` returns 0 (not on body/root layout).
    - D-96-07 server theme toggle: `grep -c 'set_theme' …/styleguide_live.ex` returns ≥ 2 (handler + phx-click); `grep -c 'data-ck-theme' …/styleguide_live.ex` returns ≥ 2; `grep -c 'aria-pressed' …/styleguide_live.ex` returns ≥ 2; `grep -c 'localStorage' …/styleguide_live.ex` returns 0.
    - D-96-15 server-owned sort: `grep -c 'set_sort\|sort_by\|sort_dir' …/styleguide_live.ex` returns ≥ 3.
    - `cd examples/adoption_demo && mix compile --warnings-as-errors` exits 0.
    - Route boots: `cd examples/adoption_demo && mix test test/adoption_demo_web/ 2>/dev/null || true` does not regress (compile-clean is the hard gate; a LiveView render smoke is added in Plan 05 e2e).
  </acceptance_criteria>
  <done>
    `/styleguide` is wired in the `:browser` scope, `StyleguideLive` renders the `.ck` shell
    with the `data-ck-root`/`data-theme` seam, the server theme toggle flips `data-theme`, and
    sort state is server-owned.
  </done>
</task>

<task type="auto">
  <name>Task 2: Render all primitive + Level-2 sections with seeded fiction and stable test markers</name>
  <read_first>
    - examples/adoption_demo/lib/adoption_demo_web/live/styleguide_live.ex (Task 1 output)
    - examples/adoption_demo/lib/adoption_demo_web/components/cohort_components.ex (Plan 02 —
      every primitive signature + slots)
    - examples/adoption_demo/lib/adoption_demo_web/live/launchpad_live.ex (:159-206 icon clause
      idiom for any styleguide-local icons)
    - 96-UI-SPEC.md Component Inventory tables (lines 189-211) — the EXACT states to render per
      primitive; Copywriting Contract (lines 237-244) for empty/error/loading/button copy
    - 96-PATTERNS.md "Gallery IA + seeded fiction (D-96-22)" (lines 170-172) + "Stable test
      markers" (lines 168, 391-393)
  </read_first>
  <action>
    Fill `render/1` with one `<section data-ck-section="{name}" data-ck-state="{state}">` per
    primitive variation-group, calling the Plan 02 components: `ck_table` (default / hover-row /
    zebra / sortable header asc-desc-none driven by `@sort_by`/`@sort_dir` / empty / loading-
    skeleton), `ck_stat` (default / status accent / empty `—` / skeleton), the form set
    (default / focus-visible / disabled / error — wired through `to_form` so `ck_field` gets a
    real `Phoenix.HTML.FormField`), `ck_tabs` (default / hover / selected / focus-visible /
    disabled), `ck_detail` (default / multi-row / empty), `ck_toolbar` (default / wrapped /
    primary+quiet). Then render the four Level-2 compositions per UI-SPEC lines 200-207:
    Data-table block (`ck_toolbar` + `ck_table` + `badge` + empty), Stat row (`.ck-grid` + N×
    `ck_stat`), Detail panel (`.ck-panel` + `ck_detail` + `badge`), Tabbed section (`ck_tabs` +
    per-panel `ck_table`/`ck_detail`). Seed REAL Cohort fiction in `mount/3` (D-96-22): a
    lesson-video row in `processing`, a quarantined upload row, an empty member list (with both
    a "never-populated" and a "filtered" empty-state copy variant). Use the exact UI-SPEC copy:
    empty heading `Nothing here yet`, empty body `No records match this view. Adjust filters or
    seed demo data.`, button samples `Apply filters` / `Save changes`, error `{Field} is
    required.`. Use stable `data-ck-section`/`data-ck-state` markers on every section, separate
    from the `.ck-*` styling classes (D-96-16) — these are what the Plan 05 spec asserts on.
  </action>
  <acceptance_criteria>
    - UI-SPEC gate 1 reachability: `grep -o 'data-ck-section="[a-z-]*"' …/styleguide_live.ex | sort -u` lists at least `table`, `stat`, `form`, `tabs`, `detail`, `toolbar` AND the four Level-2 groups (e.g. `data-table-block`, `stat-row`, `detail-panel`, `tabbed-section`) — ≥ 10 distinct sections.
    - Each primitive is actually invoked: `grep -c '<.ck_table\|<.ck_stat\|<.ck_field\|<.ck_tabs\|<.ck_detail\|<.ck_toolbar' …/styleguide_live.ex` returns ≥ 6.
    - Seeded fiction, not lorem: `grep -ci 'processing\|quarantine\|lesson\|member' …/styleguide_live.ex` returns ≥ 3; `grep -ci 'lorem\|ipsum' …/styleguide_live.ex` returns 0.
    - Exact UI-SPEC copy present: `grep -c 'Nothing here yet' …/styleguide_live.ex` returns ≥ 1; `grep -c 'No records match this view' …/styleguide_live.ex` returns ≥ 1; `grep -c 'Apply filters\|Save changes' …/styleguide_live.ex` returns ≥ 1.
    - Form uses real FormField semantics: `grep -c 'to_form' …/styleguide_live.ex` returns ≥ 1.
    - Test markers separate from styling: `grep -c 'data-ck-state' …/styleguide_live.ex` returns ≥ 6.
    - `cd examples/adoption_demo && mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>
    All six L1 primitives + four L2 compositions render in `data-ck-section`-marked sections
    with the full per-primitive state set, real seeded Cohort fiction, and the exact UI-SPEC
    copy contract.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /styleguide LiveView | a net-new PUBLIC route in the demo app; theme/sort events cross to the server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-05 | Information Disclosure | public /styleguide route | accept | The page renders only static, hand-seeded demo fiction (no real user data, no DB rows beyond the in-mount fixtures). It is a developer reference surface in the example app, intentionally public like the existing `/` launchpad. No PII, no secrets. |
| T-96-06 | Tampering | set_theme / set_sort params | mitigate | `set_theme` guards `t in ~w(light dark)`; `set_sort` validates the key against the known column set — invalid input is ignored, not reflected. No dynamic dispatch. |
| T-96-07 | Spoofing | route mount | accept | Reuses the existing `:browser` pipeline of the demo app (same posture as every other demo route); the demo is explicitly an adoption sandbox. |

No high-severity threats: a static developer reference page with enum-guarded events, no data
mutation, no auth-protected resource exposed.
</threat_model>

<verification>
- UI-SPEC gate 1: every L1 + L2 component renders in a `data-ck-section` group at `/styleguide`
  (greps above); the Plan 05 spec + Plan 04 component-existence loop mechanically confirm
  reachability.
- D-96-05 seam on `.ck` div not body; D-96-07 server toggle; D-96-15 server sort — all grep-gated.
- `cd examples/adoption_demo && mix compile --warnings-as-errors` exits 0.
</verification>

<success_criteria>
- `/styleguide` reachable in the `:browser` scope; `StyleguideLive` shell carries the
  `data-ck-root`/`data-theme` seam on the `.ck` div.
- Server theme toggle flips `data-theme`; server-owned sort drives the table header `aria-sort`.
- All ten components render with full state sets, stable test markers, real seeded fiction, and
  the exact UI-SPEC copy.
</success_criteria>

<output>
Create `.planning/phases/96-cohort-component-layer-dark-reduced-motion-contract-track-b/96-03-SUMMARY.md` when done
</output>

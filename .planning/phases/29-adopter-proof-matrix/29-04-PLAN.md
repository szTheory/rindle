---
phase: 29-adopter-proof-matrix
plan: 04
type: execute
wave: 4
depends_on: ["29-01", "29-02", "29-03"]
files_modified:
  - README.md
  - guides/getting_started.md
  - guides/operations.md
  - test/install_smoke/docs_parity_test.exs
  - test/install_smoke/release_docs_parity_test.exs
autonomous: true
requirements:
  - PROOF-04
must_haves:
  truths:
    - "Public docs describe the same package-consumer proof matrix that CI and release-facing commands actually run."
    - "README stays narrow, getting-started stays the canonical deep guide, and operations stays a thin operator index."
    - "Docs parity remains executable and repo-native."
  artifacts:
    - path: "README.md"
      provides: "Narrow adopter-facing package-consumer quickstart language."
      contains: "package-consumer"
    - path: "guides/getting_started.md"
      provides: "Canonical deep guide aligned to the proved image-only and AV package-consumer flows."
      contains: "published artifact"
    - path: "guides/operations.md"
      provides: "Operator-facing guidance linked to the proved package-consumer matrix."
      contains: "mix rindle"
    - path: "test/install_smoke/docs_parity_test.exs"
      provides: "Exact docs parity for the package-consumer matrix."
      contains: "README"
---

<objective>
Lock the public documentation surface to the actual package-consumer matrix
proved by the generated-app harness and CI.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-adopter-proof-matrix/29-RESEARCH.md
@README.md
@guides/getting_started.md
@guides/operations.md
@test/install_smoke/docs_parity_test.exs
@test/install_smoke/release_docs_parity_test.exs
@scripts/install_smoke.sh
@scripts/public_smoke.sh
</context>

<tasks>

<task type="execute">
  <name>Task 1: Teach the proved package-consumer matrix in README and getting-started</name>
  <files>README.md, guides/getting_started.md, test/install_smoke/docs_parity_test.exs</files>
  <read_first>
    - README.md
    - guides/getting_started.md
    - test/install_smoke/docs_parity_test.exs
    - scripts/install_smoke.sh
    - scripts/public_smoke.sh
  </read_first>
  <action>Update `README.md` and `guides/getting_started.md` so they describe the Phase 29 truth instead of only the older install smoke posture. Keep `README.md` narrow and `guides/getting_started.md` deep, but explicitly teach that Rindle now proves two outside-in package-consumer paths from installed artifacts: image-only and AV-enabled. Where relevant, distinguish built-artifact proof from published-version proof without turning user docs into maintainer-only release docs. Extend `test/install_smoke/docs_parity_test.exs` with exact assertions that these docs mention the package-consumer/generated-app posture, the image-only plus AV matrix, and the public commands or concepts adopters/operators rely on to validate those flows.</action>
  <acceptance_criteria>
    - `README.md` mentions the package-consumer proof posture without becoming a release runbook.
    - `guides/getting_started.md` references the proved image-only and AV flows from installed artifacts.
    - `test/install_smoke/docs_parity_test.exs` contains exact assertions for the Phase 29 package-consumer matrix language.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/install_smoke/docs_parity_test.exs</automated>
  </verify>
</task>

<task type="execute">
  <name>Task 2: Keep operations and release-doc parity aligned to the new proof matrix</name>
  <files>guides/operations.md, test/install_smoke/release_docs_parity_test.exs</files>
  <read_first>
    - guides/operations.md
    - test/install_smoke/release_docs_parity_test.exs
    - scripts/release_preflight.sh
    - scripts/public_smoke.sh
  </read_first>
  <action>Update `guides/operations.md` only where needed so operator-facing docs point to the right package-consumer proof and day-two commands without duplicating the canonical install guide. Extend `test/install_smoke/release_docs_parity_test.exs` with exact assertions for any new release/public-smoke wording introduced by Phase 29, especially if release preflight or public-smoke commands become part of the maintainer contract for proving the published artifact. Keep the docs split disciplined: maintainer publish/runbook details stay in release docs, while operations remains a thin runtime/operator index.</action>
  <acceptance_criteria>
    - `guides/operations.md` stays a thin index and cross-links the right proof-related operator guidance.
    - `test/install_smoke/release_docs_parity_test.exs` contains exact assertions for any new maintainer-facing public-smoke or preflight wording added by Phase 29.
    - docs parity remains ExUnit-based rather than shell-only.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/install_smoke/release_docs_parity_test.exs</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proved package-consumer matrix -> public docs | Docs can drift away from the image-only and AV proof matrix if they are not locked to executable tests. |
| maintainer-facing proof commands -> adopter-facing docs | Public docs must not collapse into a release runbook while still describing the real proof posture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-10 | Repudiation | README / getting-started parity | mitigate | Extend repo-native docs parity tests with exact package-consumer matrix assertions. |
| T-29-11 | Information Disclosure | maintainer-only release detail in adopter docs | mitigate | Keep README/getting-started narrow and put maintainer-oriented public-smoke wording in the release-doc parity surface only where needed. |
| T-29-12 | Tampering | operations guide sprawl | mitigate | Keep `guides/operations.md` as a thin index and cross-link proof-related operator guidance instead of re-authoring the install guide there. |
</threat_model>

<verification>
Run `mix test test/install_smoke/docs_parity_test.exs test/install_smoke/release_docs_parity_test.exs`.
</verification>

<success_criteria>
PROOF-04 is satisfied when README, getting-started, and operations guidance all
describe the same package-consumer matrix that the generated-app proof and CI
actually enforce.
</success_criteria>

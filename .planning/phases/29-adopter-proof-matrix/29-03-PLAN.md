---
phase: 29-adopter-proof-matrix
plan: 03
type: execute
wave: 3
depends_on: ["29-01", "29-02"]
files_modified:
  - .github/workflows/ci.yml
  - scripts/install_smoke.sh
  - scripts/public_smoke.sh
  - scripts/release_preflight.sh
autonomous: true
requirements:
  - PROOF-03
must_haves:
  truths:
    - "CI proves the package-consumer matrix explicitly instead of relying on one ambiguous smoke lane."
    - "At least one real S3-compatible path remains in the matrix."
    - "Release-preflight and published-version smoke continue to reuse the same package-consumer proof seams."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Named jobs/steps for the package-consumer proof matrix."
      contains: "package-consumer"
    - path: "scripts/release_preflight.sh"
      provides: "Shared preflight entrypoint that still exercises the package-consumer proof."
      contains: "install_smoke.sh"
    - path: "scripts/public_smoke.sh"
      provides: "Published-version proof path."
      contains: "public smoke test"
---

<objective>
Wire the image-only and AV package-consumer proofs into CI and release-facing
entrypoints as an explicit matrix across built and published artifacts.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-adopter-proof-matrix/29-RESEARCH.md
@.planning/phases/29-adopter-proof-matrix/29-PATTERNS.md
@.github/workflows/ci.yml
@scripts/install_smoke.sh
@scripts/public_smoke.sh
@scripts/release_preflight.sh
@test/install_smoke/generated_app_smoke_test.exs
</context>

<tasks>

<task type="execute">
  <name>Task 1: Make the package-consumer proof matrix explicit in CI</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - scripts/install_smoke.sh
    - scripts/public_smoke.sh
    - test/install_smoke/generated_app_smoke_test.exs
  </read_first>
  <action>Update `.github/workflows/ci.yml` so the package-consumer proof matrix is explicit and readable. Preserve the existing service-backed posture with Postgres and MinIO. Add or restructure named steps/jobs so CI clearly proves: the built-artifact image-only generated-app path, the AV-enabled generated-app path, and the real S3-compatible storage path already exercised through MinIO. Keep failure attribution clear; do not bury the matrix inside one opaque shell command. Reuse the existing `package-consumer` lane where that keeps the workflow readable, but prefer explicit commands or mode flags that show which proof is being run.</action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains named package-consumer proof steps or jobs for image-only and AV flows.
    - `.github/workflows/ci.yml` still provisions Postgres and MinIO for the real S3-compatible path.
    - the workflow remains readable without duplicating full setup logic in multiple unrelated jobs.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "package-consumer|public smoke|install_smoke|generated_app_smoke|MinIO" .github/workflows/ci.yml</automated>
  </verify>
</task>

<task type="execute">
  <name>Task 2: Keep release-preflight and published-version proof aligned to the same matrix</name>
  <files>scripts/release_preflight.sh, scripts/public_smoke.sh, scripts/install_smoke.sh</files>
  <read_first>
    - scripts/release_preflight.sh
    - scripts/public_smoke.sh
    - scripts/install_smoke.sh
    - .github/workflows/ci.yml
  </read_first>
  <action>Extend the release-facing shell entrypoints so the same package-consumer matrix can be exercised outside the workflow YAML. `scripts/release_preflight.sh` should continue to build/unpack the package, run package metadata and docs parity, and then call the appropriate generated-app proof commands for the baseline image-only path while keeping the AV/public-smoke path accessible through a documented companion command. `scripts/public_smoke.sh` should remain the published-version entrypoint and must be capable of selecting the AV proof path when a published version is under test. Keep the shell logic thin and mode-driven; the source of truth stays in ExUnit.</action>
  <acceptance_criteria>
    - `scripts/release_preflight.sh` still calls the generated-app smoke path.
    - `scripts/public_smoke.sh` remains the published-version entrypoint and supports the Phase 29 AV proof mode.
    - no second independent package-consumer harness is introduced in shell.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/release_preflight.sh</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow YAML -> proof matrix | CI can claim broad proof while actually running only one path if the matrix is not explicit. |
| release/public smoke scripts -> ExUnit truth | Shell wrappers must not become a second independent implementation of package-consumer proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-07 | Repudiation | `.github/workflows/ci.yml` | mitigate | Make image-only and AV package-consumer proof steps explicit and named in CI. |
| T-29-08 | Tampering | release/public smoke shell wrappers | mitigate | Keep shell entrypoints thin and mode-driven over the same generated-app tests. |
| T-29-09 | Denial of Service | CI matrix sprawl | mitigate | Reuse existing Postgres/MinIO service setup and avoid duplicating full workflow bootstraps across many jobs. |
</threat_model>

<verification>
Run `bash scripts/release_preflight.sh` and verify `.github/workflows/ci.yml` contains explicit package-consumer matrix steps for built and AV/public proof.
</verification>

<success_criteria>
PROOF-03 is satisfied when CI and release-facing entrypoints explicitly prove
the package-consumer matrix across the baseline image-only path and the AV path
on at least one real S3-compatible storage setup.
</success_criteria>

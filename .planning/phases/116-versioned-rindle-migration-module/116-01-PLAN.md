---
phase: 116-versioned-rindle-migration-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/rindle/migration_test.exs
  - test/rindle/api_surface_boundary_test.exs
  - test/install_smoke/package_metadata_test.exs
autonomous: true
requirements:
  - MIGRATE-01
  - MIGRATE-02
must_haves:
  truths:
    - "MIGRATE-01: `Rindle.Migration.up/1` and `down/1` have executable contract tests for version pinning, idempotency, default `public` prefix, marker recording, and scoped rollback."
    - "MIGRATE-02: contract tests prove the Rindle migration path never creates, mutates, or drops host-owned `oban_jobs`."
    - "Legacy packaged migration filenames remain present for existing adopters while the bundled Oban migration is treated as non-authoritative."
  artifacts:
    - path: "test/rindle/migration_test.exs"
      provides: "Focused migration API contract tests"
      contains: "Rindle.Migration.up(version: 1)"
    - path: "test/rindle/api_surface_boundary_test.exs"
      provides: "Public API visibility lock for `Rindle.Migration`"
      contains: "Rindle.Migration"
    - path: "test/install_smoke/package_metadata_test.exs"
      provides: "Package compatibility lock for legacy migration filenames"
      contains: "20260424205942_create_oban_tables.exs"
  key_links:
    - from: "test/rindle/migration_test.exs"
      to: "lib/rindle/migration.ex"
      via: "contract calls `Rindle.Migration.up/1` and `down/1`"
      pattern: "Rindle\\.Migration\\.(up|down)"
    - from: "test/install_smoke/package_metadata_test.exs"
      to: "priv/repo/migrations/20260424205942_create_oban_tables.exs"
      via: "package metadata asserts legacy filename is still shipped"
      pattern: "20260424205942_create_oban_tables"
---

<objective>
Create the Wave 1 RED contract for the public migration API and legacy package boundary.

Purpose: Lock MIGRATE-01 and MIGRATE-02 before implementation so `Rindle.Migration` cannot drift into a package-path runner or an Oban owner.
Output: Failing contract tests for the new API, public docs boundary, and preserved legacy migration package filename.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/116-versioned-rindle-migration-module/116-CONTEXT.md
@.planning/phases/116-versioned-rindle-migration-module/116-RESEARCH.md
@.planning/phases/116-versioned-rindle-migration-module/116-PATTERNS.md
@.planning/phases/116-versioned-rindle-migration-module/116-VALIDATION.md
@.planning/phases/116-versioned-rindle-migration-module/116-UI-SPEC.md
@test/rindle/domain/migration_test.exs
@test/rindle/api_surface_boundary_test.exs
@test/install_smoke/package_metadata_test.exs
@priv/repo/migrations/20260424205942_create_oban_tables.exs
</context>

## Artifacts this phase produces

- Public module: `Rindle.Migration`.
- Public functions: `Rindle.Migration.up/1`, `Rindle.Migration.down/1`.
- Hidden implementation modules: `Rindle.Migration.Options`, `Rindle.Migration.V1`.
- Internal helper functions: `Rindle.Migration.Options.validate!/1`; `Rindle.Migration.V1.up/1`, `down/1`, `current_version/0`, `marker_table/0`, `rindle_tables/0`, `catalog_requirements/0`.
- Supported migration options: `:version` with supported value `1`; `:prefix` with default `"public"`.
- Marker table: `rindle_migration_versions`, owned by Rindle, prefix-aware, not Ecto `schema_migrations`.
- Doctor/status identifiers: existing `doctor.migrations.pending` and `doctor.migrations.unresolved` retained where practical; new `doctor.rindle_schema.ready` and `doctor.oban_jobs.ready`; runtime errors `{:setup_incomplete, :rindle_schema}` and `{:setup_incomplete, :oban_jobs}`.
- New source paths: `lib/rindle/migration.ex`, `lib/rindle/migration/options.ex`, `lib/rindle/migration/v1.ex`, `test/rindle/migration_test.exs`.
- Updated legacy path: `priv/repo/migrations/20260424205942_create_oban_tables.exs` becomes a compatibility stub while preserving the filename.
- Docs snippets: pinned host migration calling `Rindle.Migration.up(version: 1)` and `down(version: 1)`; host-owned `Oban.Migration` migration; default schema remains `public`; rollback backup note; legacy packaged migrations remain valid.

<tasks>

<task type="auto">
  <name>Task 1: Add RED migration API contract tests</name>
  <files>test/rindle/migration_test.exs</files>
  <read_first>
    - .planning/phases/116-versioned-rindle-migration-module/116-CONTEXT.md
    - .planning/phases/116-versioned-rindle-migration-module/116-RESEARCH.md
    - .planning/phases/116-versioned-rindle-migration-module/116-PATTERNS.md
    - .planning/phases/116-versioned-rindle-migration-module/116-VALIDATION.md
    - test/rindle/domain/migration_test.exs
    - priv/repo/migrations/20260424155129_create_media_assets.exs
    - priv/repo/migrations/20260425090000_create_media_attachments.exs
    - priv/repo/migrations/20260425090100_create_media_variants.exs
    - priv/repo/migrations/20260425090200_create_media_upload_sessions.exs
    - priv/repo/migrations/20260425090300_create_media_processing_runs.exs
    - priv/repo/migrations/20260506120000_create_media_provider_assets.exs
    - priv/repo/migrations/20260507160000_extend_media_upload_sessions_for_resumable.exs
    - priv/repo/migrations/20260424205942_create_oban_tables.exs
  </read_first>
  <action>Create `test/rindle/migration_test.exs` as the focused RED contract for MIGRATE-01 and MIGRATE-02. Use `Rindle.DataCase, async: false` because the tests run DDL. Cover `Rindle.Migration.up(version: 1)` creating the current Rindle-owned tables idempotently, defaulting `prefix` to `"public"`, recording `rindle_migration_versions`, making primary keys and table options explicit, and never creating `oban_jobs` per D-04, D-05, D-07, D-10, D-12, D-19, D-20, D-35, and D-42. Cover `Rindle.Migration.down(version: 1)` as destructive but scoped: it drops only Rindle-owned tables and the marker table in reverse dependency order, leaves `oban_jobs` intact, and succeeds when called after a completed `up/1` per D-11. Add option validation cases for unknown options, invalid versions, and non-string prefixes raising clear `ArgumentError` per D-08 and D-45. Do not call or assert `Ecto.Migrator.run/4` as the implementation path per D-12 and D-18.</action>
  <verify>
    <automated>set +e; output=$(mix test test/rindle/migration_test.exs --seed 0 2&gt;&amp;1); status=$?; printf '%s\n' "$output"; test $status -ne 0 &amp;&amp; printf '%s\n' "$output" | rg 'Rindle\\.Migration|rindle_migration_versions|oban_jobs|ArgumentError'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `test/rindle/migration_test.exs` exists and references `Rindle.Migration.up(version: 1)`, `Rindle.Migration.down(version: 1)`, `rindle_migration_versions`, `prefix: "public"`, and `oban_jobs`.
    - Behavior assertion: the focused test command is RED in this plan because `Rindle.Migration` does not exist yet, and the failure output names the missing migration API or required contract identifiers.
    - Scope assertion: no test in this file teaches the legacy package-path migrator as the fresh install path.
  </acceptance_criteria>
  <done>The migration API RED contract exists and fails for the missing implementation, with assertions covering idempotent up, scoped down, marker behavior, option validation, explicit table ownership, and no Rindle-owned Oban table creation.</done>
</task>

<task type="auto">
  <name>Task 2: Lock public API and legacy package compatibility contracts</name>
  <files>test/rindle/api_surface_boundary_test.exs, test/install_smoke/package_metadata_test.exs</files>
  <read_first>
    - .planning/phases/116-versioned-rindle-migration-module/116-CONTEXT.md
    - .planning/phases/116-versioned-rindle-migration-module/116-PATTERNS.md
    - test/rindle/api_surface_boundary_test.exs
    - test/install_smoke/package_metadata_test.exs
    - mix.exs
    - priv/repo/migrations/20260424205942_create_oban_tables.exs
  </read_first>
  <action>Extend the public surface boundary so `Rindle.Migration` is a documented public module with visible `up/1` and `down/1`, while helper modules such as `Rindle.Migration.Options` and `Rindle.Migration.V1` stay hidden per D-04, D-13, and D-18. Extend package metadata proof so the legacy packaged migration directory and the exact `20260424205942_create_oban_tables.exs` filename remain shipped for compatibility per D-14, D-16, D-17, D-41, and D-42. Do not add dependencies, public install tasks, Igniter integration, or migration generator assertions per D-09 and D-44.</action>
  <verify>
    <automated>set +e; output=$(mix test test/rindle/api_surface_boundary_test.exs test/install_smoke/package_metadata_test.exs --seed 0 2&gt;&amp;1); status=$?; printf '%s\n' "$output"; test $status -ne 0 &amp;&amp; printf '%s\n' "$output" | rg 'Rindle\\.Migration|20260424205942_create_oban_tables|up/1|down/1'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `test/rindle/api_surface_boundary_test.exs` includes `Rindle.Migration` in the public allowlist and asserts `up/1` and `down/1` docs visibility.
    - Source assertion: helper modules under `Rindle.Migration.*` are treated as hidden implementation modules.
    - Source assertion: `test/install_smoke/package_metadata_test.exs` asserts the Oban legacy migration filename still ships in the package.
    - Behavior assertion: the combined test command is RED until the module and compatibility stub exist.
  </acceptance_criteria>
  <done>Public API and package compatibility RED contracts are in place without authorizing any generator, public install Mix task, dependency, or breaking legacy-file removal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host Ecto migration -&gt; Rindle migration API | Host-controlled keyword options cross into Rindle-owned DDL. |
| Rindle rollback -&gt; host database | Destructive DDL must stay scoped to Rindle-owned tables. |
| package metadata -&gt; adopter legacy history | Existing adopters rely on shipped legacy migration filenames for historical inspection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-01 | Tampering | `Rindle.Migration` options | mitigate | RED tests require `ArgumentError` for unknown options and invalid `:version`/`:prefix` before implementation. |
| T-116-02 | Tampering / Denial of Service | `Rindle.Migration.down/1` | mitigate | RED tests require rollback to drop only Rindle-owned tables and leave `oban_jobs` intact. |
| T-116-03 | Tampering | legacy migration package files | mitigate | Package test requires the legacy Oban migration filename to remain shipped while later plans neutralize its behavior. |
| T-116-04 | Information Disclosure | public docs boundary | mitigate | Public API test exposes only `Rindle.Migration`; helper modules remain hidden. |
| T-116-SC | Tampering | package installs | accept | No npm, pip, cargo, or Hex install task is planned; Phase 116 uses existing locked dependencies only. |
</threat_model>

<verification>
- RED command for migration API: `mix test test/rindle/migration_test.exs --seed 0` fails and names missing `Rindle.Migration` contract.
- RED command for API/package contract: `mix test test/rindle/api_surface_boundary_test.exs test/install_smoke/package_metadata_test.exs --seed 0` fails until implementation exists.
</verification>

<success_criteria>
- MIGRATE-01 has failing tests for versioned, idempotent `Rindle.Migration.up/1` and `down/1` with default `public` prefix and marker behavior.
- MIGRATE-02 has failing tests proving Rindle does not own `oban_jobs`.
- Legacy packaged migration filenames remain contractually preserved.
</success_criteria>

<output>
Create `.planning/phases/116-versioned-rindle-migration-module/116-01-SUMMARY.md` when done.
</output>

---
phase: 116-versioned-rindle-migration-module
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - test/install_smoke/docs_parity_test.exs
  - test/install_smoke/support/generated_app_helper.ex
  - test/install_smoke/generated_app_smoke_test.exs
autonomous: true
requirements:
  - MIGRATE-01
  - MIGRATE-02
must_haves:
  truths:
    - "MIGRATE-01: README, getting-started, and upgrading docs are contract-locked to pinned `Rindle.Migration` install snippets."
    - "MIGRATE-02: docs and generated-app proof require host-owned `Oban.Migration` and prove Rindle does not create `oban_jobs`."
    - "Generated Phoenix app proof uses normal host migrations rather than Rindle's legacy package migration directory for fresh installs."
  artifacts:
    - path: "test/install_smoke/docs_parity_test.exs"
      provides: "Docs parity lock for the Phase 116 install and upgrade copy"
      contains: "Rindle.Migration.up(version: 1)"
    - path: "test/install_smoke/support/generated_app_helper.ex"
      provides: "Generated-app host migration writer for Oban and Rindle setup"
      contains: "Rindle.Migration.up(version: 1)"
    - path: "test/install_smoke/generated_app_smoke_test.exs"
      provides: "Generated-app proof assertions for host-owned Oban and Rindle migrations"
      contains: "host_oban_migration_ran?"
  key_links:
    - from: "test/install_smoke/docs_parity_test.exs"
      to: "README.md"
      via: "source assertions over README `## Migrations`"
      pattern: "Rindle\\.Migration\\.up\\(version: 1\\)"
    - from: "test/install_smoke/support/generated_app_helper.ex"
      to: "test/install_smoke/generated_app_smoke_test.exs"
      via: "helper writes report fields asserted by smoke test"
      pattern: "host_oban_migration_ran\\?"
---

<objective>
Create the Wave 1 RED proof contract for documentation and generated-app install behavior.

Purpose: Ensure the public onboarding path teaches normal host migrations, separates Rindle-owned tables from host-owned Oban, and rejects the old greenfield package-path flow.
Output: Failing docs parity and generated-app proof updates that later implementation must satisfy.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/116-versioned-rindle-migration-module/116-CONTEXT.md
@.planning/phases/116-versioned-rindle-migration-module/116-RESEARCH.md
@.planning/phases/116-versioned-rindle-migration-module/116-PATTERNS.md
@.planning/phases/116-versioned-rindle-migration-module/116-VALIDATION.md
@.planning/phases/116-versioned-rindle-migration-module/116-UI-SPEC.md
@.planning/phases/115-versioning-readme-positioning/115-01-SUMMARY.md
@README.md
@guides/getting_started.md
@guides/upgrading.md
@test/install_smoke/docs_parity_test.exs
@test/install_smoke/support/generated_app_helper.ex
@test/install_smoke/generated_app_smoke_test.exs
</context>

## Artifacts this phase produces

- Public module: `Rindle.Migration`.
- Public functions: `Rindle.Migration.up/1`, `Rindle.Migration.down/1`.
- Hidden implementation modules: `Rindle.Migration.Options`, `Rindle.Migration.V1`.
- Internal helper functions: `Rindle.Migration.Options.validate!/1`; `Rindle.Migration.V1.up/1`, `down/1`, `current_version/0`, `marker_table/0`, `rindle_tables/0`, `catalog_requirements/0`.
- Supported migration options: `:version` with supported value `1`; `:prefix` with default `"public"`.
- Marker table: `rindle_migration_versions`, owned by Rindle, prefix-aware, not Ecto `schema_migrations`.
- Doctor/status identifiers: existing `doctor.migrations.pending` and `doctor.migrations.unresolved` retained where practical; new `doctor.rindle_schema.ready` and `doctor.oban_jobs.ready`; runtime errors `{:setup_incomplete, :rindle_schema}` and `{:setup_incomplete, :oban_jobs}`.
- New source paths: `lib/rindle/migration.ex`, `lib/rindle/migration/options.ex`, `lib/rindle/migration/v1.ex`, `test/rindle/migration_test.exs`.
- Updated legacy path: `priv/repo/migrations/20260424205942_create_oban_tables.exs` becomes a compatibility stub while preserving the filename.
- Docs snippets: pinned host migration calling `Rindle.Migration.up(version: 1)` and `down(version: 1)`; host-owned `Oban.Migration` migration; default schema remains `public`; rollback backup note; legacy packaged migrations remain valid.

<tasks>

<task type="auto">
  <name>Task 1: Add RED docs parity assertions for migration ownership copy</name>
  <files>test/install_smoke/docs_parity_test.exs</files>
  <read_first>
    - .planning/phases/116-versioned-rindle-migration-module/116-CONTEXT.md
    - .planning/phases/116-versioned-rindle-migration-module/116-UI-SPEC.md
    - .planning/phases/116-versioned-rindle-migration-module/116-PATTERNS.md
    - .planning/phases/115-versioning-readme-positioning/115-01-SUMMARY.md
    - README.md
    - guides/getting_started.md
    - guides/upgrading.md
    - guides/operations.md
    - guides/troubleshooting.md
    - test/install_smoke/docs_parity_test.exs
  </read_first>
  <action>Update docs parity tests so README `## Migrations`, `guides/getting_started.md` step 3, and `guides/upgrading.md` `## Unreleased / Next` require the Phase 116 reader journey per D-02, D-03, D-28, D-29, D-30, D-31, D-32, D-33, D-37, D-38, and D-39. Assert pinned snippets use `Rindle.Migration.up(version: 1)` and `Rindle.Migration.down(version: 1)`, adjacent copy names `Oban.Migration`, `oban_jobs`, `prefix: "public"` or default `public`, `mix ecto.migrate`, and `mix rindle.doctor`, and rollback copy uses backup/destructive wording from UI-SPEC. Assert the old raw package-directory greenfield flow is absent from README, getting-started, and the new Unreleased upgrade note while allowing legacy compatibility text in intentionally scoped historical sections per D-14, D-15, D-24, D-31, D-33, D-41, and D-43. Do not add docs CSS, icons, frontend dependencies, custom admonition plugins, or a public install task per D-09, D-39, D-40, and D-44.</action>
  <verify>
    <automated>set +e; output=$(mix test test/install_smoke/docs_parity_test.exs --seed 0 2&gt;&amp;1); status=$?; printf '%s\n' "$output"; test $status -ne 0 &amp;&amp; printf '%s\n' "$output" | rg 'Rindle\\.Migration\\.up\\(version: 1\\)|Oban\\.Migration|oban_jobs|Unreleased|Migrations'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: docs parity requires `Rindle.Migration.up(version: 1)` and `Rindle.Migration.down(version: 1)` in README, getting-started, and the Unreleased upgrade note.
    - Source assertion: docs parity requires host-owned `Oban.Migration`, `oban_jobs` ownership copy, default `public`, and `mix rindle.doctor`.
    - Source assertion: docs parity forbids the old raw package-directory greenfield flow in the three fresh-install surfaces while preserving intentionally scoped legacy compatibility content.
    - Behavior assertion: `mix test test/install_smoke/docs_parity_test.exs --seed 0` is RED until docs are updated.
  </acceptance_criteria>
  <done>Docs parity RED contract locks Phase 116 documentation UX, terminology, safety copy, and rejected old greenfield install path.</done>
</task>

<task type="auto">
  <name>Task 2: Add RED generated-app migration proof</name>
  <files>test/install_smoke/support/generated_app_helper.ex, test/install_smoke/generated_app_smoke_test.exs</files>
  <read_first>
    - .planning/phases/116-versioned-rindle-migration-module/116-CONTEXT.md
    - .planning/phases/116-versioned-rindle-migration-module/116-RESEARCH.md
    - .planning/phases/116-versioned-rindle-migration-module/116-PATTERNS.md
    - .planning/phases/116-versioned-rindle-migration-module/116-VALIDATION.md
    - test/install_smoke/support/generated_app_helper.ex
    - test/install_smoke/generated_app_smoke_test.exs
    - scripts/install_smoke.sh
  </read_first>
  <action>Change generated-app proof fixtures so fresh generated apps create and run three normal host migration files: the existing install-smoke marker migration, a host-owned Oban migration calling `Oban.Migration.up()` / `down(version: 1)`, and a Rindle migration calling `Rindle.Migration.up(version: 1)` / `down(version: 1)` per D-05, D-10, D-13, D-15, D-28, D-32, and D-34. Update report fields and smoke assertions to prove `host_migration_ran?`, `host_oban_migration_ran?`, `rindle_migration_ran?`, `rindle_created_oban_jobs? == false`, and that the fresh install resolver is not the legacy package-directory resolver. Keep the legacy upgrade preparer scoped to legacy upgrade proof, where old packaged migration history can still be used intentionally per D-14, D-17, D-24, and D-41.</action>
  <verify>
    <automated>set +e; output=$(mix test test/install_smoke/generated_app_smoke_test.exs --seed 0 2&gt;&amp;1); status=$?; printf '%s\n' "$output"; test $status -ne 0 &amp;&amp; printf '%s\n' "$output" | rg 'host_oban_migration_ran\\?|rindle_migration_ran\\?|rindle_created_oban_jobs\\?|Rindle\\.Migration|Oban\\.Migration'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: generated helper writes separate host Oban and Rindle migration files for fresh install proof.
    - Source assertion: generated app report includes `host_oban_migration_ran?`, `rindle_migration_ran?`, and `rindle_created_oban_jobs?`.
    - Source assertion: fresh install smoke assertions reject the legacy package-directory resolver while legacy upgrade proof remains intentionally scoped.
    - Behavior assertion: generated-app smoke test is RED until `Rindle.Migration` exists and docs/runtime setup are implemented.
  </acceptance_criteria>
  <done>Generated-app RED proof requires normal host migrations, separate Oban ownership, and no fresh-install package-path resolver.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -&gt; adopter terminal | Markdown snippets become commands and migrations in host apps. |
| generated app helper -&gt; proof database | Test harness creates real migrations and observes table ownership. |
| legacy upgrade proof -&gt; existing adopter history | Historical compatibility copy must not cause destructive repair guidance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-09 | Tampering | docs snippets | mitigate | Docs parity requires pinned `Rindle.Migration` snippets and rejects the old greenfield package-directory flow. |
| T-116-10 | Tampering / Denial of Service | generated-app migrations | mitigate | Generated proof requires host-owned Oban and Rindle migrations to be separate and observable. |
| T-116-11 | Tampering | legacy compatibility copy | mitigate | Tests preserve legacy package-path use only in scoped upgrade proof, not fresh install docs. |
| T-116-12 | Information Disclosure | docs styling/extensions | accept | Phase 116 has no browser UI or custom docs rendering changes; portable Markdown only. |
| T-116-SC | Tampering | package installs | accept | No package-manager install task is planned. |
</threat_model>

<verification>
- RED docs parity: `mix test test/install_smoke/docs_parity_test.exs --seed 0`.
- RED generated proof: `mix test test/install_smoke/generated_app_smoke_test.exs --seed 0` or the smallest generated-app smoke invocation available locally.
</verification>

<success_criteria>
- MIGRATE-01 has failing docs parity and generated-app proof for pinned `Rindle.Migration` host migrations.
- MIGRATE-02 has failing docs parity and generated-app proof for host-owned `Oban.Migration` and no Rindle-created `oban_jobs`.
- UI-SPEC copy, hierarchy, and no-frontend/no-generator boundaries are locked in tests before docs are edited.
</success_criteria>

<output>
Create `.planning/phases/116-versioned-rindle-migration-module/116-03-SUMMARY.md` when done.
</output>

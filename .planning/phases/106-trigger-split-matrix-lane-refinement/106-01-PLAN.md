---
phase: 106-trigger-split-matrix-lane-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/106-trigger-split-matrix-lane-refinement/106-LANE-CLASSIFICATION.md
  - CONTRIBUTING.md
  - RUNNING.md
autonomous: true
requirements: [LANE-04]
must_haves:
  truths:
    - "An A–E bucket classification doc exists that places EVERY current ci.yml lane into exactly one of keep / optimize / move-to-nightly / label-gated-PR-lane / off-critical-path (no quarantine/delete entries)"
    - "CONTRIBUTING.md exists and contains the trust/speed paragraph naming the on-PR representative gate and the after-merge / nightly lanes, with the ≤7-minute target stated"
    - "mux-soak is classified as 'label-gated PR lane', explicitly NOT 'move-to-nightly'"
    - "coverage is documented as off the PR critical path (advisory telemetry; mix coveralls stays as the gating test invocation, no per-PR coverage-% gate)"
  artifacts:
    - path: ".planning/phases/106-trigger-split-matrix-lane-refinement/106-LANE-CLASSIFICATION.md"
      provides: "A–E bucket test-value classification backing every lane placement (LANE-04)"
      min_lines: 40
    - path: "CONTRIBUTING.md"
      provides: "The explicit trust/speed tradeoff label (LANE-04 requirement: must appear in CONTRIBUTING)"
      contains: "after merge"
  key_links:
    - from: ".planning/phases/106-trigger-split-matrix-lane-refinement/106-LANE-CLASSIFICATION.md"
      to: ".github/workflows/ci.yml"
      via: "each bucket entry names a real ci.yml job; placement decisions are traceable to D-02..D-14"
      pattern: "package-consumer|mux-soak|gcs-soak|compat-matrix|Dialyzer"
  prohibitions:
    - "Doc must NOT classify mux-soak as nightly/move-to-nightly (D-14: it stays a label-gated PR lane in ci.yml)"
    - "Doc must NOT list any quarantine (D) or delete (E) entry (CONTEXT: none identified) — do not invent one"
    - "This plan must NOT edit .github/workflows/ci.yml or any workflow file (no YAML lane changes here)"
    - "Must NOT introduce a per-PR coverage-% gate (D-07: coverage measurement is advisory telemetry)"
---

<objective>
LANE-04 (documentation + label half): author the A–E keep/optimize/move-to-nightly/quarantine/delete
classification doc that backs every lane placement in this phase, create the CONTRIBUTING.md trust/speed
label, and align RUNNING.md's lane-severity table forward-references. This plan is the conceptual spine the
three ci.yml/nightly.yml plans (106-02/03/04) implement; it touches NO workflow YAML so it runs in parallel.

Purpose: LANE-04 requires a documented test-value classification behind every lane move and an explicit
trust/speed tradeoff labeled in CONTRIBUTING (and the PR). This is the "why" record that makes the lane
moves auditable and the headline ≤7-min cut defensible.
Output: 106-LANE-CLASSIFICATION.md (A–E buckets), CONTRIBUTING.md (trust/speed paragraph), RUNNING.md note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-trigger-split-matrix-lane-refinement/106-CONTEXT.md
@.planning/phases/103-observability-baseline/103-BASELINE.md
</context>

## Artifacts this phase produces (this plan)

- NEW file `.planning/phases/106-trigger-split-matrix-lane-refinement/106-LANE-CLASSIFICATION.md`
- NEW file `CONTRIBUTING.md` (repo root — none exists today)
- Edit to existing `RUNNING.md` (`## Maintainer: CI lane severity` section)

<tasks>

<task type="auto">
  <name>Task 1: Author the A–E lane-value classification doc (LANE-04)</name>
  <files>.planning/phases/106-trigger-split-matrix-lane-refinement/106-LANE-CLASSIFICATION.md</files>
  <read_first>
    - .planning/phases/106-trigger-split-matrix-lane-refinement/106-CONTEXT.md (the `<specifics>` A–E bucket seed at ~:326-330; the D-02..D-14 placement rationale)
    - .planning/phases/103-observability-baseline/103-BASELINE.md §1 (per-job avg/p95 — the wall-clock evidence each placement cites)
    - RUNNING.md `## Maintainer: CI lane severity` (the existing job inventory the buckets must cover)
    - .github/workflows/ci.yml (job-name list: quality, optional-dependencies, integration, contract, proof, package-consumer, adoption-demo-unit, cohort-demo-smoke, adoption-demo-e2e, adopter, mux-soak, gcs-soak, package-consumer-gcs-live, brandbook-tokens)
  </read_first>
  <action>
    Create the classification doc with five labeled buckets matching the CONTEXT seed exactly. Bucket A
    (keep on PR): integration, contract, proof, adopter, adoption-demo-unit, brandbook-tokens, the scoped
    `image`-only package-consumer, plus quality (both cells) and optional-dependencies. Bucket B (optimize):
    matrixify the 5 install-smoke profiles into `package-consumer-full`, extract Dialyzer into an owned job.
    Bucket C (move-to-nightly): broad OTP×Elixir compat matrix, gcs-soak, package-consumer-gcs-live, the
    owned gating Dialyzer lane. A distinct "label-gated PR lane" bucket: mux-soak ONLY (state explicitly it
    is NOT nightly — no PR label exists on a schedule, per D-14). Bucket "off-critical-path": full package
    matrix + release_preflight + hex.publish --dry-run, adoption-demo-e2e, cohort-demo-smoke, coverage
    measurement. State that NO quarantine (D) or delete (E) entries were identified. For each entry give a
    one-line rationale citing the governing decision id (D-02 through D-14) and, where relevant, the 103
    baseline number (e.g. package-consumer 550s avg / 887s p95 long pole). Add a short header paragraph: the
    classification backs LANE-04 and every lane placement in plans 106-02/03/04. Reference per D-01 the
    Tokio model (representative gate on PR, breadth post-merge/nightly).
  </action>
  <verify>
    <automated>test -f .planning/phases/106-trigger-split-matrix-lane-refinement/106-LANE-CLASSIFICATION.md && grep -q 'mux-soak' .planning/phases/106-trigger-split-matrix-lane-refinement/106-LANE-CLASSIFICATION.md && grep -qi 'label-gated' .planning/phases/106-trigger-split-matrix-lane-refinement/106-LANE-CLASSIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists with ≥5 bucket headings (keep / optimize / move-to-nightly / label-gated PR lane / off-critical-path).
    - Every current ci.yml job name appears in exactly one bucket (grep each job name resolves to one section).
    - `mux-soak` appears under the label-gated bucket and the doc states it is NOT nightly.
    - Doc states no quarantine/delete entries identified.
    - At least the D-01, D-04, D-05, D-08, D-10, D-13, D-14, D-17 decision ids are cited as rationale.
  </acceptance_criteria>
  <done>The A–E classification doc places every lane with a decision-cited rationale, mux-soak is label-gated (not nightly), and quarantine/delete are explicitly empty.</done>
</task>

<task type="auto">
  <name>Task 2: Create CONTRIBUTING.md with the trust/speed label (LANE-04)</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - .planning/phases/106-trigger-split-matrix-lane-refinement/106-CONTEXT.md (`<specifics>` LANE-04 trust/speed label draft at ~:312-325 — the ready paragraph to adopt)
    - RUNNING.md `## Maintainer: CI lane severity` (vocabulary / lane names must cohere)
  </read_first>
  <action>
    Create CONTRIBUTING.md at the repo root (none exists today). Include a "## CI: what runs on your PR vs
    after merge" section carrying the CONTEXT trust/speed draft: the on-PR representative gate (compile
    warnings-as-errors + full test suite on both supported Elixir/OTP cells, optional-dependency compile,
    integration with MinIO, contract + proof, adopter lifecycle, the storage-free adoption-demo unit suite,
    the token to CSS drift gate, and one representative `image` package-consumer install-smoke) targeting
    roughly seven minutes; then the after-merge (push:main) / nightly lanes (the full five-profile
    package-consumer matrix + release preflight + hex.publish dry-run, the Playwright browser E2E, the
    Docker-compose cold-start smoke, the broad OTP×Elixir compatibility matrix, the owned Dialyzer lane, the
    real-API GCS/Mux soak lanes). State the why (expensive / browser-or-Docker-flaky / live third-party
    dependent), that a regression in a moved lane is caught on main within one merge (blocks the NEXT merge,
    not the author), and that the full release-verification gate always runs provably before any Hex publish.
    Keep CONTRIBUTING-authoring depth lean — the deeper `mix ci` + local-command docs are Phase 107 (HARD-03),
    note that as a forward pointer. Keep `lib/` and behavior untouched (docs only). LANE-04 also requires the
    trust/speed label "in the PR" (criterion 4) — author the CONTRIBUTING paragraph as a copy-pasteable block
    and add an explicit forward-pointer note (here and in this plan's success criteria) that the `/gsd-ship`-time
    step MUST paste the trust/speed paragraph into the PR body so the PR-side half of the requirement is not
    lost (no YAML, no automation added here — this is a ship-time handoff reminder).
  </action>
  <verify>
    <automated>test -f CONTRIBUTING.md && grep -qi 'after merge' CONTRIBUTING.md && grep -qiE 'seven minutes|7 ?min|≤ ?7' CONTRIBUTING.md && grep -qi 'image' CONTRIBUTING.md</automated>
  </verify>
  <acceptance_criteria>
    - CONTRIBUTING.md exists at repo root.
    - Contains the on-PR lane list including the representative `image` package-consumer smoke and the ≤7-minute target.
    - Contains the "after merge / nightly" lane list including the full 5-profile matrix, Playwright E2E, Docker cold-start, compat matrix, Dialyzer, and real-API soak lanes.
    - Names the trust/speed rationale (expensive / flaky / live-third-party) and the "caught within one merge, blocks the next merge not the author" framing.
  </acceptance_criteria>
  <done>CONTRIBUTING.md carries the explicit trust/speed label distinguishing on-PR from after-merge/nightly lanes with the ≤7-min target and the regression-MTTD framing.</done>
</task>

<task type="auto">
  <name>Task 3: Forward-reference the trigger split in RUNNING.md lane-severity table</name>
  <files>RUNNING.md</files>
  <read_first>
    - RUNNING.md `## Maintainer: CI lane severity` (:34-78 — the existing table + static-analysis policy block)
    - .planning/phases/106-trigger-split-matrix-lane-refinement/106-CONTEXT.md (D-08 split, D-10 PR scoping, D-17 Dialyzer ownership)
  </read_first>
  <action>
    Add a short note under the `## Maintainer: CI lane severity` heading (do NOT rewrite the whole table) that
    the package-consumer lane is split by trigger in Phase 106: a lean representative `image`-only
    `package-consumer` on PR (stays merge-blocking via CI Summary) and a `package-consumer-full` on
    push:main/release that runs the full 5-profile matrix + release preflight + hex.publish dry-run and is NOT
    a required PR check; that the broad compat matrix, gcs-soak, package-consumer-gcs-live, and the owned
    gating Dialyzer lane move to a separate `nightly.yml` (`name: Nightly`); and that mux-soak stays here as a
    label-gated PR lane. Update only the cells whose severity/trigger language now drifts (Dialyzer row, the
    package-consumer rows, gcs-soak / package-consumer-gcs-live rows) so RUNNING.md coheres with the
    LANE-04 classification — leave merge-blocking PR lanes (quality, integration, contract, proof, adopter)
    unchanged in severity. Cross-link 106-LANE-CLASSIFICATION.md. Do NOT change the source-of-truth claim
    that ci.yml keeps `name: CI`.
  </action>
  <verify>
    <automated>grep -qi 'nightly' RUNNING.md && grep -qi 'package-consumer-full' RUNNING.md && grep -c 'name: CI' RUNNING.md >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - RUNNING.md `## Maintainer: CI lane severity` section mentions the package-consumer split, `package-consumer-full`, and the separate `nightly.yml`.
    - mux-soak is still described as a label-gated PR lane (not moved to nightly).
    - The doc still asserts ci.yml is the source of truth and does not claim any rename of `name: CI` or the filename.
  </acceptance_criteria>
  <done>RUNNING.md's lane-severity section forward-references the Phase-106 trigger split and Dialyzer/nightly moves while keeping the unchanged merge-blocking PR lanes and the ci.yml naming invariant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → reader | These artifacts inform maintainer/contributor behavior; a wrong classification could mislead a lane move, but no executable surface crosses here |
| repo → published package | CONTRIBUTING.md / RUNNING.md / .planning docs must NOT enter the Hex `files:` allowlist (they are repo-internal / dev docs) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-01-I | Information disclosure | CONTRIBUTING.md / RUNNING.md content | accept | Public dev docs by design; contain no secrets and describe only public CI topology |
| T-106-01-T | Tampering | classification doc drifting from actual ci.yml lanes | mitigate | Acceptance criteria grep each real job name; plans 02/03/04 implement exactly what this doc classifies |
| T-106-01-SC | Tampering (supply chain) | npm/mix installs | accept | This plan installs NO packages (docs-only); no Package Legitimacy Gate applies |

Note (deferred, Phase 107 / HARD-02): action SHA-pinning + full supply-chain hardening is out of scope here.
This plan adds no `continue-on-error`, no workflow trigger, and no executable surface.
</threat_model>

<verification>
- `test -f CONTRIBUTING.md` and `test -f .planning/phases/106-trigger-split-matrix-lane-refinement/106-LANE-CLASSIFICATION.md`.
- `git diff --name-only` shows ONLY the three doc files (no `.github/workflows/*` touched in this plan).
- `grep -i 'label-gated' 106-LANE-CLASSIFICATION.md` resolves to the mux-soak entry.
- No new dependency added (no mix.lock / package-lock churn).
</verification>

<success_criteria>
- A–E classification doc places every current ci.yml lane with a decision-cited rationale; quarantine/delete empty.
- CONTRIBUTING.md carries the trust/speed label with the on-PR vs after-merge/nightly split and ≤7-min target.
- LANE-04 PR-side half (criterion 4): CONTRIBUTING carries a copy-pasteable trust/speed block AND a forward-pointer notes the `/gsd-ship`-time step must paste that paragraph into the PR body (PR-side label not lost).
- RUNNING.md coheres with the trigger split without altering the `name: CI` / filename invariant.
- Zero workflow YAML edited in this plan (LANE-01/02/03 YAML lives in 106-02/03/04).
</success_criteria>

<output>
Create `.planning/phases/106-trigger-split-matrix-lane-refinement/106-01-SUMMARY.md` when done
</output>

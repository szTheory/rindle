---
id: 71-02
plan_number: 02
plan: 02
phase: 71
type: execute
wave: 2
depends_on: [71-01]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [CI-02]
must_haves:
  truths:
    - "`package-consumer` job failure fails the workflow (no job-level continue-on-error)."
    - "`adopter` doctor and lifecycle test step failures fail the job (no step-level continue-on-error on those steps)."
    - "Every remaining non-blocking lane has a `# Phase 71 (CI proof honesty):` comment pointing to RUNNING.md."
    - "`package-consumer` and `adopter` job headers state they are merge-blocking release-readiness lanes."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "honest CI severity wiring"
      min_phase71_comments: 8
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "RUNNING.md"
      via: "Phase 71 comment blocks"
      pattern: "RUNNING.md"
---

<objective>
Ship CI-02 and ROADMAP criterion 4: remove misleading continue-on-error from
`package-consumer` and `adopter`, document all remaining advisory/soak lanes.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-71-03 | Fork PR leaks secrets via soak lanes | high | No change to mux/gcs secret gating; comments document fork-safe behavior |
| T-71-04 | Removing COE blocks all PRs on flaky lane | medium | Intentional; only package-consumer + adopter; advisory lanes unchanged |
| T-71-05 | Removing wrong COE line breaks unrelated job | low | grep-only acceptance criteria target exact lines from CONTEXT |
</threat_model>

<context>
@.planning/phases/71-ci-proof-honesty/71-CONTEXT.md
@.planning/phases/71-ci-proof-honesty/71-RESEARCH.md
@.planning/phases/71-ci-proof-honesty/71-PATTERNS.md
@RUNNING.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove continue-on-error from package-consumer and adopter</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - .planning/phases/71-ci-proof-honesty/71-CONTEXT.md
  </read_first>
  <action>
In `.github/workflows/ci.yml`:

1. **D-04:** Delete the job-level `continue-on-error: true` line on the
   `package-consumer` job (currently immediately after `runs-on: ubuntu-22.04`
   around line 298). Do not add any replacement COE.

2. **D-05:** On the `adopter` job, delete `continue-on-error: true` from:
   - step `Verify AV runtime with public doctor task` (~515)
   - step `Run adopter tests` (~522)
   Do NOT add job-level `continue-on-error` on `adopter` (none exists today).

3. **D-11:** Update the `package-consumer` job comment block (lines ~299–302)
   to state this job is **merge-blocking** release-readiness (install-smoke +
   release preflight). Point to `RUNNING.md` CI lane severity section.

4. **D-11:** Update the `adopter` job comment block (~427–433) to state
   **merge-blocking** after step COE removal. Keep existing `needs:` documentation.

Do NOT remove step-level COE from `quality`, `contract`, `gcs-soak` test step,
or job-level COE on `package-consumer-gcs-live`.
  </action>
  <acceptance_criteria>
    - `rg -n '^  package-consumer:' .github/workflows/ci.yml -A5` output does NOT contain `continue-on-error`
    - `rg -n 'Verify AV runtime with public doctor task' .github/workflows/ci.yml -A3` output does NOT contain `continue-on-error`
    - `rg -n 'Run adopter tests' .github/workflows/ci.yml -A2` output does NOT contain `continue-on-error`
    - `rg -n '^  adopter:' .github/workflows/ci.yml -A8` output does NOT contain job-level `continue-on-error`
    - `rg 'merge-blocking' .github/workflows/ci.yml` matches package-consumer and adopter comment areas
  </acceptance_criteria>
  <verify>rg -n 'continue-on-error' .github/workflows/ci.yml</verify>
</task>

<task type="auto">
  <name>Task 2: Add Phase 71 comment blocks at all remaining non-blocking lanes</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - RUNNING.md
    - .planning/phases/71-ci-proof-honesty/71-CONTEXT.md
  </read_first>
  <action>
Add `# Phase 71 (CI proof honesty):` multi-line comment blocks immediately
**before** each of the following (mirror mux-soak / gcs-soak style). Each block
must state **why** the lane stays non-blocking and end with "See RUNNING.md
`## CI lane severity` for the full matrix."

Targets (CONTEXT D-08 / D-10):

**quality job — step-level advisory steps** (add one block before the first
advisory step `Credo (strict)` covering all advisory steps in that job, OR
individual blocks per step — prefer one consolidated block before Credo listing
all advisory steps: Credo, Doctor, AV doctor, Coveralls, Dialyzer):

Reason: dialyzer/credo/coverage policy — intentional advisory static analysis.

**contract job — before `Run contract tests` step:**

Reason: contract ExUnit suite is advisory; AV hygiene gate remains blocking.

**gcs-soak job — before `Run GCS integration tests` step** (step has COE):

Reason: optional provider proof when secrets present; test step advisory.

**package-consumer-gcs-live job — before job definition:**

Reason: secret-gated live GCS install-smoke; job-level COE when secrets missing.

Do NOT add Phase 71 blocks on `package-consumer` or `adopter` merge-blocking
lanes (those get merge-blocking headers in Task 1, not non-blocking explanations).

Update existing `mux-soak` and `gcs-soak` job header blocks to append a single
line: `# Phase 71 (CI proof honesty): see RUNNING.md ## CI lane severity` OR
merge into existing block without duplicating fork-secret documentation.
  </action>
  <acceptance_criteria>
    - `rg -c 'Phase 71 \\(CI proof honesty\\)' .github/workflows/ci.yml` prints count >= 5
    - `rg 'RUNNING.md' .github/workflows/ci.yml` exits 0
    - `rg -n 'Credo \\(strict\\)' .github/workflows/ci.yml -B5` shows Phase 71 comment within 5 lines above
    - `rg -n 'Run contract tests' .github/workflows/ci.yml -B5` shows Phase 71 comment within 5 lines above
    - `rg -n 'Run GCS integration tests' .github/workflows/ci.yml -B5` shows Phase 71 comment within 5 lines above
    - `rg -n 'package-consumer-gcs-live:' .github/workflows/ci.yml -B5` shows Phase 71 comment within 5 lines above
  </acceptance_criteria>
  <verify>rg -n 'Phase 71 \\(CI proof honesty\\)' .github/workflows/ci.yml</verify>
</task>

</tasks>

<verification>
1. Confirm package-consumer and adopter COE removals via grep (acceptance criteria above)
2. `rg -c 'Phase 71 \\(CI proof honesty\\)' .github/workflows/ci.yml` — expect >= 5
3. `mix test test/install_smoke/docs_parity_test.exs` (docs from wave 1 still green)
</verification>

<success_criteria>
- CI-02: package-consumer and adopter failures fail the workflow
- ROADMAP criterion 4: non-blocking lanes have Phase 71 explanatory comments
- Advisory COE on quality/contract/gcs-soak/package-consumer-gcs-live unchanged
</success_criteria>

---
id: 71-01
plan_number: 01
plan: 01
phase: 71
type: execute
wave: 1
depends_on: []
files_modified:
  - RUNNING.md
  - test/install_smoke/docs_parity_test.exs
autonomous: true
requirements: [CI-01]
must_haves:
  truths:
    - "Maintainers can read merge-blocking vs advisory vs secret-gated soak severity for every `ci.yml` job from RUNNING.md."
    - "The CI matrix lives immediately after the intro paragraph and before `## Verify The Runtime` without relocating FFmpeg content."
    - "The matrix links to `.github/workflows/ci.yml` as source of truth and documents `release.yml` gate-ci-green BYPASS behavior."
  artifacts:
    - path: "RUNNING.md"
      provides: "CI lane severity matrix"
      contains: "## CI lane severity"
    - path: "test/install_smoke/docs_parity_test.exs"
      provides: "CI matrix doc drift guard"
      contains: "CI lane severity"
  key_links:
    - from: "RUNNING.md"
      to: ".github/workflows/ci.yml"
      via: "source of truth link in matrix intro"
      pattern: ".github/workflows/ci.yml"
---

<objective>
Ship CI-01: the maintainer-facing CI lane severity matrix in `RUNNING.md` plus a
docs parity assertion so the matrix cannot silently rot.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-71-01 | Matrix documents wrong severity after future YAML edits | low | Link to ci.yml; docs_parity test; Phase 71 comments in plan 02 |
| T-71-02 | Accidental FFmpeg section relocation breaks existing parity test | low | Insert only between intro and `## Verify The Runtime`; run docs_parity |
</threat_model>

<context>
@.planning/phases/71-ci-proof-honesty/71-CONTEXT.md
@.planning/phases/71-ci-proof-honesty/71-RESEARCH.md
@.planning/phases/71-ci-proof-honesty/71-PATTERNS.md
@RUNNING.md
@test/install_smoke/docs_parity_test.exs
@.github/workflows/ci.yml
@.github/workflows/release.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `## CI lane severity` matrix to RUNNING.md</name>
  <files>RUNNING.md</files>
  <read_first>
    - RUNNING.md
    - .planning/phases/71-ci-proof-honesty/71-CONTEXT.md
    - .github/workflows/ci.yml
    - .github/workflows/release.yml
  </read_first>
  <action>
Insert a new section `## CI lane severity` **immediately after** the intro block
(lines 1–12, ending with "matrix both of those entrypoints link to.") and
**before** the existing `## Verify The Runtime` heading. Do not move or rewrite
FFmpeg install matrix content below.

Section structure:
1. One short intro paragraph explaining that this matrix is maintainer-facing,
   that `.github/workflows/ci.yml` is the source of truth, and that branch
   protection settings live outside the repo.
2. A markdown table with columns: **Job**, **Severity**, **When it runs**, **Notes**.

Include these rows (one row per job or step split per CONTEXT D-07):

| Job / step | Severity | Notes summary |
|------------|----------|---------------|
| `quality` — Compile, Check formatting | merge-blocking | Both matrix cells (Elixir 1.15/26, 1.17/27) |
| `quality` — Credo | advisory | step-level `continue-on-error` |
| `quality` — Doctor (full, raise) | advisory | step-level COE |
| `quality` — Verify AV runtime with public doctor task | advisory | step-level COE |
| `quality` — Run tests with coverage | advisory | step-level COE |
| `quality` — Dialyzer | advisory | step-level COE |
| `integration` | merge-blocking | `needs: quality` |
| `contract` — Run AV hygiene gate | merge-blocking | `scripts/assert_av_hygiene.sh` |
| `contract` — Run contract tests | advisory | step-level COE |
| `package-consumer` | merge-blocking | install-smoke matrix + release preflight (COE removed in plan 02) |
| `adopter` | merge-blocking | canonical lifecycle + doc parity (step COE removed in plan 02) |
| `mux-soak` | secret-gated soak | label `streaming`; blocking when job runs (no COE) |
| `gcs-soak` | secret-gated soak | repo + secrets; test step advisory |
| `package-consumer-gcs-live` | secret-gated soak | job-level COE; skips when secrets empty |

Add a subsection **Release train** documenting `.github/workflows/release.yml`
`gate-ci-green`: when `ci.yml` conclusion is not `success` or wait times out,
the step logs `(BYPASSED)` and publish continues (~204–214). State explicitly
that tightening this is out of scope for v1.15.

Add a subsection **Post-merge checklist** with D-12: verify GitHub branch
protection required checks include `package-consumer` and `adopter` if green-main
honesty should hold in practice.
  </action>
  <acceptance_criteria>
    - `rg '## CI lane severity' RUNNING.md` exits 0
    - `rg 'merge-blocking' RUNNING.md` exits 0
    - `rg 'advisory' RUNNING.md` exits 0
    - `rg 'secret-gated soak' RUNNING.md` exits 0
    - `rg 'package-consumer' RUNNING.md` exits 0
    - `rg 'adopter' RUNNING.md` exits 0
    - `rg 'mux-soak' RUNNING.md` exits 0
    - `rg '.github/workflows/ci.yml' RUNNING.md` exits 0
    - `rg 'BYPASSED' RUNNING.md` exits 0
    - `rg '## Verify The Runtime' RUNNING.md` exits 0
    - First `## Verify The Runtime` line number is greater than line containing `## CI lane severity`
  </acceptance_criteria>
  <verify>rg -n '## CI lane severity|## Verify The Runtime' RUNNING.md</verify>
</task>

<task type="auto">
  <name>Task 2: Add docs_parity test for CI severity section</name>
  <files>test/install_smoke/docs_parity_test.exs</files>
  <read_first>
    - test/install_smoke/docs_parity_test.exs
    - RUNNING.md
    - .planning/phases/71-ci-proof-honesty/71-PATTERNS.md
  </read_first>
  <action>
Add a new test after the existing `"running guide publishes the durable FFmpeg
install matrix"` test:

```elixir
test "running guide publishes the CI lane severity matrix", %{running: running} do
  for snippet <- [
        "## CI lane severity",
        "merge-blocking",
        "advisory",
        "secret-gated soak",
        "package-consumer",
        "adopter",
        ".github/workflows/ci.yml"
      ] do
    assert running =~ snippet
  end
end
```

Do not remove or weaken existing FFmpeg matrix assertions.
  </action>
  <acceptance_criteria>
    - `mix test test/install_smoke/docs_parity_test.exs` exits 0
    - `rg 'CI lane severity matrix' test/install_smoke/docs_parity_test.exs` exits 0
  </acceptance_criteria>
  <verify>mix test test/install_smoke/docs_parity_test.exs</verify>
</task>

</tasks>

<verification>
1. `rg '## CI lane severity' RUNNING.md`
2. `mix test test/install_smoke/docs_parity_test.exs`
</verification>

<success_criteria>
- RUNNING.md contains complete severity matrix per CONTEXT D-07
- docs_parity_test.exs asserts CI section strings
- FFmpeg section unchanged and still passes existing parity test
</success_criteria>

---
phase: 75
plan: 02
type: execute
wave: 2
depends_on:
  - 75-01
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - CI-03

must_haves:
  truths:
    - "Adopter partial doc grep step removed from ci.yml"
    - "Adopter job comment describes lifecycle-only scope"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "Adopter lane without redundant doc grep"
      contains: "canonical adopter lifecycle"
  key_links:
    - from: proof job
      to: docs_parity_test.exs
      via: "supersedes adopter bash grep subset"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-75-02-1 | Removing grep drops AV onboarding coverage | medium | docs_parity_test already asserts doctor/facade tokens |
| T-75-02-2 | Accidentally breaking adopter lifecycle step | high | Only delete grep step; keep lifecycle_test.exs step |
</threat_model>

<objective>
Remove redundant adopter partial doc grep; adopter job is lifecycle-only. Full doc parity
runs in merge-blocking `proof` job.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-merge-blocking-proof-lanes/75-CONTEXT.md
@.planning/phases/75-merge-blocking-proof-lanes/75-RESEARCH.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove adopter doc grep and update job header</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (adopter job ~426-575)
    - .planning/phases/75-merge-blocking-proof-lanes/75-CONTEXT.md (D-03)
  </read_first>
  <action>
    In `adopter` job:

    1. **Delete** entire step `Verify AV onboarding docs stay on the public facade path`
       (the multi-line `run: |` bash block with `search_fixed` / `search_regex` helpers,
       approximately lines 521–574).

    2. **Update** job header comment from:
       `canonical adopter lifecycle + doc parity`
       to:
       `canonical adopter lifecycle` (lifecycle proof only; doc parity is merge-blocking in `proof` job).

    3. **Do not** change `needs:`, MinIO setup, doctor step, or `Run adopter tests` step.
  </action>
  <acceptance_criteria>
    - `! grep -q 'Verify AV onboarding docs stay on the public facade path' .github/workflows/ci.yml`
    - `grep -c 'canonical adopter lifecycle' .github/workflows/ci.yml` outputs at least `1`
    - `grep -c 'search_fixed' .github/workflows/ci.yml` outputs `0` (helpers removed with step)
    - `grep -c 'Run adopter tests' .github/workflows/ci.yml` outputs at least `1`
  </acceptance_criteria>
  <verify>
    <automated>! grep -q 'Verify AV onboarding docs stay on the public facade path' .github/workflows/ci.yml</automated>
  </verify>
</task>

</tasks>

<verification_criteria>
- Adopter grep gone; lifecycle test step preserved
</verification_criteria>

## PLANNING COMPLETE

---
phase: 75
plan: 04
type: execute
wave: 3
depends_on:
  - 75-02
  - 75-03
files_modified:
  - test/install_smoke/docs_parity_test.exs
autonomous: true
requirements:
  - CI-03

must_haves:
  truths:
    - "docs_parity_test locks proof job presence in RUNNING.md"
    - "Both proof test files pass locally"
  artifacts:
    - path: test/install_smoke/docs_parity_test.exs
      provides: "RUNNING.md proof lane regression lock"
      contains: "proof"
  key_links:
    - from: test/install_smoke/docs_parity_test.exs
      to: RUNNING.md
      via: "CI lane severity matrix asserts"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-75-04-1 | RUNNING.md drifts after phase | low | Token asserts in docs_parity_test (D-04) |
</threat_model>

<objective>
Extend docs_parity_test to lock RUNNING.md proof lane documentation; verify both proof
test targets green locally.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-merge-blocking-proof-lanes/75-CONTEXT.md
@test/install_smoke/docs_parity_test.exs
@RUNNING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend docs_parity_test for proof job in RUNNING.md</name>
  <files>test/install_smoke/docs_parity_test.exs</files>
  <read_first>
    - test/install_smoke/docs_parity_test.exs (`running guide publishes the CI lane severity matrix`)
    - RUNNING.md (proof row)
  </read_first>
  <action>
    Extend test `"running guide publishes the CI lane severity matrix"` snippet list with:

    ```elixir
    "`proof`",
    "docs_parity_test.exs",
    "batch_owner_erasure_task_test.exs",
    ```

    Add **new test** `"running guide documents proof job as merge-blocking"`:

    ```elixir
    test "running guide documents proof job as merge-blocking", %{running: running} do
      assert running =~ "`proof`"
      assert running =~ "merge-blocking"
      refute running =~ "Canonical lifecycle + doc parity"
    end
    ```

    (Use refute only if stale exact phrase still exists after 75-03; if already removed, omit refute.)
  </action>
  <acceptance_criteria>
    - `grep -c 'running guide documents proof job' test/install_smoke/docs_parity_test.exs` outputs at least `1`
    - `grep -c 'batch_owner_erasure_task_test' test/install_smoke/docs_parity_test.exs` outputs at least `1`
    - `mix test test/install_smoke/docs_parity_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/install_smoke/docs_parity_test.exs</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Local verify batch owner erasure task proof</name>
  <files></files>
  <action>
    Run `mix test test/rindle/batch_owner_erasure_task_test.exs` — confirms proof job's second
    step will pass in CI (Postgres via test_helper Repo).
  </action>
  <acceptance_criteria>
    - `mix test test/rindle/batch_owner_erasure_task_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rindle/batch_owner_erasure_task_test.exs</automated>
  </verify>
</task>

</tasks>

<verification_criteria>
- Both proof test files green
- RUNNING.md proof lane locked in docs_parity_test
</verification_criteria>

## PLANNING COMPLETE

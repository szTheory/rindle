---
phase: 75
plan: 03
type: execute
wave: 2
depends_on:
  - 75-01
files_modified:
  - RUNNING.md
autonomous: true
requirements:
  - CI-03

must_haves:
  truths:
    - "RUNNING.md CI matrix documents proof job as merge-blocking"
    - "Adopter row no longer claims doc parity"
    - "Post-merge checklist mentions Proof required check"
  artifacts:
    - path: RUNNING.md
      provides: "Updated CI lane severity matrix"
      contains: "`proof`"
  key_links:
    - from: RUNNING.md
      to: .github/workflows/ci.yml
      via: "source of truth link"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-75-03-1 | Matrix drift from ci.yml | low | 75-04 docs_parity lock; link to ci.yml |
| T-75-03-2 | Branch protection docs incomplete | medium | Post-merge checklist names `Proof` job |
</threat_model>

<objective>
Update RUNNING.md CI lane severity matrix and post-merge checklist for the new `proof` job.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-merge-blocking-proof-lanes/75-CONTEXT.md
@RUNNING.md
@.planning/phases/71-ci-proof-honesty/71-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update RUNNING.md CI matrix and checklist</name>
  <files>RUNNING.md</files>
  <read_first>
    - RUNNING.md (`## CI lane severity` and `### Post-merge checklist`)
    - .planning/ROADMAP.md (Phase 75 success criteria)
  </read_first>
  <action>
    In `## CI lane severity` table:

    1. **Add row** after `contract` rows (before `package-consumer`):

       | `proof` | merge-blocking | `needs: quality` | `docs_parity_test.exs` + `batch_owner_erasure_task_test.exs`; Postgres only; Elixir 1.17/OTP 27 |

    2. **Update** `adopter` row Notes column from
       `Canonical lifecycle + doc parity` →
       `Canonical adopter lifecycle only (doc parity in \`proof\` job)`.

    3. In `### Post-merge checklist`, extend required checks list to include **`Proof`**
       (GitHub check name matches job `name: Proof`) alongside `package-consumer` and `adopter`.

    Preserve FFmpeg sections below matrix — insert only within CI severity section.
  </action>
  <acceptance_criteria>
    - `grep -c '`proof`' RUNNING.md` outputs at least `2`
    - `grep -c 'docs_parity_test' RUNNING.md` outputs at least `1`
    - `grep -c 'batch_owner_erasure_task_test' RUNNING.md` outputs at least `1`
    - `grep 'adopter' RUNNING.md | grep -c 'doc parity'` outputs `0` OR only appears in "doc parity in proof" clarification
    - `grep -c 'Proof' RUNNING.md` outputs at least `1` (checklist)
  </acceptance_criteria>
  <verify>
    <automated>grep -q '`proof`' RUNNING.md</automated>
  </verify>
</task>

</tasks>

<verification_criteria>
- Matrix and checklist reflect proof lane
</verification_criteria>

## PLANNING COMPLETE

---
phase: 109-subprocess-epipe-hardening
plan: 02
type: execute
wave: 2
depends_on: [109-01]
files_modified:
  - test/rindle/av/subprocess_epipe_canary_test.exs
  - .github/workflows/nightly.yml
  - .planning/PROJECT.md
  - .github/workflows/ci.yml
autonomous: true
requirements: [EPIPE-05, TRUTH-01]
must_haves:
  truths:
    - "An advisory behavioral canary probes the UNGUARDED MuonTrap.cmd/3 (never Subprocess.run/3) across 500 iterations and asserts MuonTrap #98 :epipe STILL reproduces; when upstream fixes #98 it fails LOUDLY with a message naming the shim file/function, this canary file, the regression test, Application.spec(:muontrap, :vsn), and the #98 URL (D-11)."
    - "The canary is tagged @moduletag :canary + :av and is NEVER in the merge-blocking PR gate; it runs only in the nightly lane via --include canary with continue-on-error: true (advisory-only, D-12)."
    - "PROJECT.md security-invariant 13's stale 'Rambo on macOS/Windows dev' kill clause is replaced with the D-15 MuonTrap-only-on-every-platform / cgroups-Linux-only / no-Rambo wording, matched on text strings not line numbers (TRUTH-01, D-15/D-17)."
    - "PROJECT.md Key-Decisions row '(FFmpex + MuonTrap)' becomes '(MuonTrap runner; argv built in-house, not FFmpex)' (TRUTH-01, D-16)."
    - "A merge-blocking CI grep step proves the TRUTH-01 edit landed and guards regression: 'Rambo' absent from the invariant-13 region and 'FFmpex + MuonTrap' absent from PROJECT.md."
  artifacts:
    - "test/rindle/av/subprocess_epipe_canary_test.exs — advisory behavioral tripwire"
    - ".github/workflows/nightly.yml — advisory --include canary step in compat-matrix (continue-on-error: true)"
    - ".planning/PROJECT.md — invariant-13 + Key-Decisions row corrected (Tier A only)"
    - ".github/workflows/ci.yml — merge-blocking TRUTH-01 grep guard step"
  key_links:
    - "The canary's @moduletag :canary is harmless ONLY because Plan 01 excluded :canary from the default suite (test_helper.exs) — depends_on: 109-01."
    - "The nightly --include canary step overrides the test_helper.exs exclude for that one invocation; continue-on-error keeps a still-reproducing bug from reddening the lane."
    - "TRUTH-01 enforcement is a CI grep step (NOT an ExUnit test reading .planning/) to avoid the Phase 111 LOCK-05 'no test reads a .planning/ path' meta-test."
---

<objective>
Ship the cleanup signal and the TRUTH-01 docs correction. The advisory behavioral canary
(`test/rindle/av/subprocess_epipe_canary_test.exs`) probes the UNGUARDED `MuonTrap.cmd/3` and
asserts MuonTrap #98 STILL reproduces — binding the shim's removal signal to the actual upstream
behavior change, not a version proxy (D-10/D-11). It is advisory-only: routed to the existing
Nightly lane via `--include canary` with `continue-on-error: true`, and kept out of the PR gate by
Plan 01's `:canary` exclude (D-12). In the same pass, correct PROJECT.md security-invariant 13's
stale "Rambo on macOS/Windows dev" clause and the Key-Decisions "(FFmpex + MuonTrap)" row to the
real MuonTrap-only path (TRUTH-01, D-15/D-16/D-17, Tier A only), guarded by a merge-blocking CI grep.

Purpose: a future maintainer gets a loud, accurate "you can remove the shim now" signal when #98
ships upstream, plus project DNA that matches the real code path. The approach is research-LOCKED
(D-10..D-17); this plan translates it.
Output: the canary test, the nightly advisory step, the PROJECT.md edits, and the CI grep guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/109-subprocess-epipe-hardening/109-CONTEXT.md
@.planning/phases/109-subprocess-epipe-hardening/109-RESEARCH.md
@.planning/phases/109-subprocess-epipe-hardening/109-PATTERNS.md
@.planning/phases/109-subprocess-epipe-hardening/109-VALIDATION.md

# The shim this canary signals removal of (from Plan 01)
@lib/rindle/av/subprocess.ex
# Analogs (read for house style)
@test/rindle/api_surface_boundary_test.exs
@.github/workflows/nightly.yml
</context>

<artifacts>
## Artifacts this plan produces

| Symbol / File | Kind | Notes |
|---------------|------|-------|
| `Rindle.AV.SubprocessEpipeCanaryTest` | NEW test file | `@moduletag :canary` + `:av`, `async: false`, 500 iters, probes UNGUARDED `MuonTrap.cmd/3` |
| `:canary` tag (advisory) | ExUnit tag (consumed) | gated OUT of default suite by Plan 01; opted IN only by the nightly `--include canary` step |
| nightly `Subprocess :epipe cleanup canary (advisory)` step | `nightly.yml` edit | in `compat-matrix`, `continue-on-error: true`, `--include canary` |
| PROJECT.md invariant-13 + Key-Decisions corrections | docs edit | Tier A only; text-string match; archived v1.4 artifacts left intact |
| TRUTH-01 grep guard step | `ci.yml` edit | merge-blocking; asserts `Rambo` absent from invariant 13 + `FFmpex + MuonTrap` absent |
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Write the advisory behavioral canary test</name>
  <files>test/rindle/av/subprocess_epipe_canary_test.exs</files>
  <read_first>
    - 109-RESEARCH.md §3 lines 256-321 (the canary file VERBATIM: probe, 500 iters, detection logic, loud failure message)
    - 109-PATTERNS.md "subprocess_epipe_canary_test.exs" section (api_surface_boundary_test.exs tripwire analog + @moduletag form + Application.spec(:muontrap, :vsn))
    - test/rindle/api_surface_boundary_test.exs:1-4 (behavioral-tripwire structure to mirror)
    - lib/rindle.ex:147 (Application.spec(:app, :vsn) idiom for the failure message)
    - 109-CONTEXT.md D-10/D-11/D-12 (reject version tripwire; behavioral canary probing UNGUARDED MuonTrap.cmd/3; advisory-only, :canary tag, out of PR gate)
  </read_first>
  <action>
    Create `test/rindle/av/subprocess_epipe_canary_test.exs` as
    `defmodule Rindle.AV.SubprocessEpipeCanaryTest`, `use ExUnit.Case, async: false`, with
    `@moduletag :canary` and `@moduletag :av` (the module-level tag form, mirroring how
    api_surface_boundary_test.exs scopes its concern, per PATTERNS). Copy the canary body from
    RESEARCH §3 lines 268-316 VERBATIM: probe the UNGUARDED
    `MuonTrap.cmd("sh", ["-c", "yes | head -n 100000"], into: "", stderr_to_stdout: true)` —
    NEVER `Subprocess.run/3` (the canary must observe the RAW bug, not the shielded path, D-11) —
    across `@iters 500`, with `Process.flag(:trap_exit, true)`, detecting reproduction via BOTH the
    `receive {:EXIT, _port, :epipe} after 0` mailbox form AND the `catch :exit, :epipe` /
    `:exit, {:epipe, _}` synchronous form, halting on first reproduction. Assert `reproduced?` with
    the loud multi-line failure message (D-11 verbatim) that names: the issue URL
    (`https://github.com/fhunleth/muontrap/issues/98`), `Application.spec(:muontrap, :vsn)`, and the
    three files to delete on removal (`lib/rindle/av/subprocess.ex` run_isolated/5 + run/3
    delegation; this canary file; the deterministic regression test
    `test/rindle/av/subprocess_epipe_test.exs`) + "bump the :muontrap pin to the fixed version".
    Add a short `# NOTE`/moduledoc explaining it is ADVISORY (probabilistic → never gates a PR,
    D-12) and is the live removal signal coupled to the shim's NOTE block.
    Do NOT add `@tag :regression` (this is advisory, NOT merge-blocking).
  </action>
  <verify>
    <automated>mix test test/rindle/av/subprocess_epipe_canary_test.exs --include canary --seed 0</automated>
  </verify>
  <done>The canary currently reproduces #98 (passes with `--include canary` against muontrap 1.7.0); a bare `mix test` (no `--include canary`) does NOT run it (proven by Plan 01's `:canary` exclude); the failure message names all four removal coordinates.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the advisory canary into the nightly lane</name>
  <files>.github/workflows/nightly.yml</files>
  <read_first>
    - .github/workflows/nightly.yml:39-116 (the compat-matrix job; the "Run tests with coverage"/`mix coveralls` step at :115-116)
    - .github/workflows/nightly.yml:118-123 (the documented advisory-vs-gating continue-on-error convention)
    - 109-RESEARCH.md §4 step 2 lines 353-370 (the exact advisory step YAML; --include canary overrides the test_helper.exs exclude for this one invocation)
    - 109-CONTEXT.md D-12 (advisory-only; the nightly lane is the advisory home)
  </read_first>
  <action>
    In `.github/workflows/nightly.yml`, add ONE new step to the `compat-matrix` job immediately
    after the `Run tests with coverage` (`mix coveralls`) step at :115-116, per RESEARCH §4 step 2:
    `- name: Subprocess :epipe cleanup canary (advisory)` with `continue-on-error: true` and
    `run: mix test test/rindle/av/subprocess_epipe_canary_test.exs --include canary`. The
    `--include canary` overrides the default exclude for this invocation; `continue-on-error: true`
    keeps the canary purely informational (a still-reproducing bug must NEVER red the lane, and the
    eventual loud failure when #98 is fixed surfaces via the Nightly Summary + a maintainer eyeball,
    not a gate, D-12). Match the indentation/step style of the surrounding compat-matrix steps. Do
    NOT change `name: Nightly`, the cron trigger, any other job, or add the canary to ci.yml's PR
    lanes (it must stay off the merge gate).
  </action>
  <verify>
    <automated>grep -c "subprocess_epipe_canary_test.exs --include canary" .github/workflows/nightly.yml   # expect 1</automated>
  </verify>
  <done>The advisory step exists in `compat-matrix` with `continue-on-error: true` and `--include canary`; `name: Nightly`, the cron, and ci.yml are unchanged; the YAML parses (no indentation break).</done>
</task>

<!-- planner-discipline-allow: FFmpex + MuonTrap -->
<!-- planner-discipline-allow: Rambo on macOS -->
<task type="auto">
  <name>Task 3: Correct PROJECT.md TRUTH-01 prose + add a merge-blocking CI grep guard</name>
  <files>.planning/PROJECT.md, .github/workflows/ci.yml</files>
  <read_first>
    - .planning/PROJECT.md:456-459 (the stale invariant-13 kill clause: "(MuonTrap on Linux; Rambo on macOS / Windows dev).")
    - .planning/PROJECT.md:503 (the Key-Decisions row containing "(FFmpex + MuonTrap)")
    - 109-CONTEXT.md D-15 lines 143-154 (the EXACT new invariant-13 text — apply verbatim, preserve the Rindle.tmp/ + Ops-reaper sentence) + D-16 + D-17 (Tier A only; text-string match)
    - 109-RESEARCH.md §6 lines 399-437 (byte-accuracy verification of the D-15 wording against the C source; the OLD clause text to match)
    - 109-PATTERNS.md ".planning/PROJECT.md" section (the two text-string targets) and "docs-parity affirm-new / deny-stale" pattern
    - .github/workflows/ci.yml (find the quality lane / a cheap merge-blocking step to host the grep guard)
  </read_first>
  <action>
    PROJECT.md (Tier A only, match on text strings NOT line numbers, D-17):
    (1) Invariant 13 — replace the kill clause
        "No transcode is allowed without an enforceable parent-death subprocess kill (MuonTrap on
        Linux; Rambo on macOS / Windows dev)." with the D-15 verbatim text (CONTEXT.md lines
        143-154): MuonTrap is the sole subprocess runner on every platform
        (`Rindle.AV.Subprocess.run/3` → `MuonTrap.cmd/3`); its POSIX port wrapper kills the child
        when the BEAM dies on both Linux and macOS dev; cgroup resource caps (memory / CPU) are
        Linux-only and gated on `:os.type() == {:unix, :linux}`; on macOS the kill guarantee holds
        without cgroup caps; there is no Rambo dependency. PRESERVE the `Rindle.tmp/` + scheduled
        `Rindle.Ops` reaper sentence verbatim.
    (2) Key-Decisions row — replace the literal `(FFmpex + MuonTrap)` with
        `(MuonTrap runner; argv built in-house, not FFmpex)` (D-16).
    Do NOT touch PROJECT.md lines 58-59 (milestone description — already accurate) or any archived
    artifact under `.planning/research/v1.4/*` / `milestones/v1.4-*` (D-17 — rewriting
    point-in-time records would falsify history).
    ci.yml (merge-blocking TRUTH-01 guard): add ONE cheap grep step to an existing PR-gating lane
    (e.g. the `quality` job) that fails if the stale strings reappear — assert `Rambo` is absent
    from PROJECT.md's invariant-13 region AND the literal `FFmpex + MuonTrap` is absent from
    PROJECT.md (e.g. `! grep -q "FFmpex + MuonTrap" .planning/PROJECT.md` and a region-scoped
    `Rambo` check). This is a CI grep step (NOT an ExUnit test that reads `.planning/PROJECT.md`) on
    purpose: Phase 111 LOCK-05 adds a merge-blocking meta-test that fails if ANY test reads a
    `.planning/` path, so a doc-assertion ExUnit test would conflict — the grep guard satisfies
    TRUTH-01 merge-blocking enforcement without introducing a `.planning/`-reading test. Keep
    `name: CI` + the ci.yml filename byte-unchanged (release-train coupling, hard invariant).
    The CHANGELOG `fix:` note (D-14) is satisfied by the conventional-commit message at phase
    commit time (release-please auto-generates the entry) — do NOT hand-edit CHANGELOG.md.
  </action>
  <verify>
    <automated>bash -c 'grep -q "MuonTrap is the sole subprocess runner on every platform" .planning/PROJECT.md && ! grep -q "FFmpex + MuonTrap" .planning/PROJECT.md && ! grep -A3 "parent-death subprocess" .planning/PROJECT.md | grep -q "Rambo on macOS" && echo TRUTH-01-OK'</automated>
  </verify>
  <done>PROJECT.md invariant 13 reads the D-15 MuonTrap-only-on-every-platform wording (no "Rambo"); the Key-Decisions row reads "(MuonTrap runner; argv built in-house, not FFmpex)"; the ci.yml grep guard step exists in a PR-gating lane and `name: CI`/filename are unchanged; archived v1.4 artifacts untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BEAM process ↔ UNGUARDED `MuonTrap.cmd/3` (canary probe) | The canary deliberately crosses the raw `:epipe` boundary (no shim) to observe whether MuonTrap #98 still reproduces — purely observational, never on a production path. |
| docs (PROJECT.md) ↔ CI grep guard | The TRUTH-01 grep step is the boundary that keeps the corrected invariant-13/Key-Decisions prose from regressing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-109-05 | Denial of Service | canary `:epipe` reproduction probe | low | accept | The canary runs UNGUARDED `MuonTrap.cmd/3` deliberately; it is `async: false`, `:canary`-tagged out of the PR gate, and `continue-on-error: true` in nightly — a reproducing OR non-reproducing canary can never red a gate (D-12). No production caller uses this path. |
| T-109-06 | Repudiation / Information disclosure | stale security-invariant-13 prose | low | mitigate | TRUTH-01 corrects the doc to the real MuonTrap-only kill path (no false Rambo/Windows claim); a merge-blocking CI grep guard prevents regression. Tier-A only — archived point-in-time records intentionally left intact (D-17). |
| T-109-07 | Tampering | argv / security layer | low | accept | Unchanged — this plan touches no `lib/` argv path (only a test, a CI YAML, a doc, and a CI grep). Security invariants 8–13 byte-equivalent by construction. |
| T-109-08 | Tampering | npm/pip/cargo/hex installs | n/a | accept | No new dependency. The canary's `sh -c "yes | head -n N"` is a STATIC, non-user-controlled command — no injection surface. |

> ASVS L1, block on `high`. No high-severity threat is introduced. The phase-level high threat
> (T-109-01, the `:epipe` DoS) is mitigated in Plan 01; this plan only adds the advisory removal
> signal and the docs-truth fix.
</threat_model>

<verification>
## Phase checks (this plan)

- `mix test test/rindle/av/subprocess_epipe_canary_test.exs --include canary --seed 0` — canary currently reproduces #98 (passes against muontrap 1.7.0).
- `mix test test/rindle/av/subprocess_epipe_canary_test.exs` (NO `--include canary`) runs ZERO tests — proves the `:canary` exclude (Plan 01) keeps it off the PR gate.
- `grep -c "subprocess_epipe_canary_test.exs --include canary" .github/workflows/nightly.yml` returns `1`; the step has `continue-on-error: true`.
- TRUTH-01 grep: `MuonTrap is the sole subprocess runner on every platform` present; `FFmpex + MuonTrap` absent; `Rambo on macOS` absent from the invariant-13 region.
- `git diff` shows no `.github/workflows/ci.yml` change to `name: CI` or the filename; no `lib/` change in this plan.

## Wave-merge gate (full phase, run after both plans land)

- `mix test` (full default suite; excludes `:canary`) green — includes the two UNMODIFIED flaking tests (D-09): `mix test test/rindle/processor/ffmpeg_test.exs:32 test/rindle/ops/lifecycle_repair_test.exs:122` pass byte-identical.
- `mix test test/rindle/security/` green (argv/caps byte-equivalent, EPIPE-03).
- `git diff` confirms `test/rindle/processor/ffmpeg_test.exs` and `test/rindle/ops/lifecycle_repair_test.exs` are byte-unchanged.
</verification>

<success_criteria>
- The advisory canary probes the UNGUARDED `MuonTrap.cmd/3`, reproduces #98 today, and fails loudly (naming the shim/function, this file, the regression test, `Application.spec(:muontrap, :vsn)`, and the #98 URL) when upstream fixes it (D-11, EPIPE-05 cleanup signal).
- The canary never gates a PR: `:canary` excluded from the default suite (Plan 01) and routed to nightly via `--include canary` + `continue-on-error: true` (D-12).
- PROJECT.md invariant 13 + Key-Decisions row corrected to the MuonTrap-only path, Tier A only, text-string matched; archived artifacts intact (TRUTH-01, D-15/D-16/D-17).
- A merge-blocking CI grep guard proves the TRUTH-01 edit landed and guards regression — without an ExUnit test reading `.planning/` (Phase 111 LOCK-05 compatibility).
</success_criteria>

<output>
Create `.planning/phases/109-subprocess-epipe-hardening/109-02-SUMMARY.md` when done.
</output>
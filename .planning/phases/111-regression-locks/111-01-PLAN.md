---
phase: 111-regression-locks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/install_smoke/install_smoke_preflight_test.exs
  - test/planning_path_hygiene_test.exs
autonomous: true
requirements: [LOCK-01, LOCK-05]
must_haves:
  truths:
    - "A merge-blocking ExUnit test fails if scripts/install_smoke.sh drops the phx.new probe or self-install"
    - "A merge-blocking ExUnit test fails if the phx.new self-install ever moves after the generated-app smoke invocation"
    - "A merge-blocking ExUnit test fails if any test/**/*.exs reads a .planning/ path at runtime"
    - "Both new tests run in the default suite (quality lane) with no exclude tag"
  artifacts:
    - test/install_smoke/install_smoke_preflight_test.exs
    - test/planning_path_hygiene_test.exs
  key_links:
    - "LOCK-01 reads scripts/install_smoke.sh (shipped artifact), never a .planning/ path"
    - "LOCK-05 globs test/**/*.exs and asserts files != [] (anti-vacuous)"
---

<objective>
Ship two standalone shipped-artifact meta-tests in the merge-blocking `quality` lane:
LOCK-01 (locks the `install_smoke.sh` phx.new self-install guard) and LOCK-05 (locks the
`.planning/`-path decoupling across the whole `test/` tree).

Purpose: Make the 2026-06-26 phx.new self-install fix and the install-smoke-path decoupling
fix undeletable — a future edit that removes the guard or re-couples a test to `.planning/`
fails CI on the PR, not silently on `main`.
Output: `test/install_smoke/install_smoke_preflight_test.exs` (LOCK-01),
`test/planning_path_hygiene_test.exs` (LOCK-05).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Assertion-target shipped artifact (LOCK-01)
@scripts/install_smoke.sh

# House meta-test idioms to mirror
@test/install_smoke/package_metadata_test.exs
@test/install_smoke/ci_lane_split_test.exs
@test/async_safety_guard_test.exs
</context>

<artifacts_this_phase_produces>
This plan introduces these NEW symbols (excluded from any drift / pre-existing-content checks):
- ExUnit module `Rindle.InstallSmoke.InstallSmokePreflightTest` (file `test/install_smoke/install_smoke_preflight_test.exs`)
- ExUnit module `Rindle.PlanningPathHygieneTest` (file `test/planning_path_hygiene_test.exs`)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: LOCK-01 — install_smoke.sh phx.new self-install preflight meta-test</name>
  <files>test/install_smoke/install_smoke_preflight_test.exs</files>
  <read_first>
    - scripts/install_smoke.sh (probe `:31` `mix phx.new --version`; self-install `:33` `MIX_ENV=dev mix archive.install hex phx_new --force`; smoke invocations `:56`/`:59` `test/install_smoke/generated_app_smoke_test.exs`)
    - test/install_smoke/package_metadata_test.exs (`@repo_root Path.expand("../..", __DIR__)`, `@install_smoke_script`, `setup_all` returning `install_smoke_script:` ctx key, the `command_position/2` order-index helper at `:298-303`, `=~` literal-assert idiom)
    - test/install_smoke/ci_lane_split_test.exs (`:9-21` — the SHIPPED-artifacts-ONLY `@moduledoc` convention; avoid mutable `@vX` tag assertions)
  </read_first>
  <action>
    Create a NEW sibling module `Rindle.InstallSmoke.InstallSmokePreflightTest` in
    `test/install_smoke/install_smoke_preflight_test.exs` per D-01 (planner's discretion chose a new
    sibling over folding, to keep the lock self-documenting). Use `use ExUnit.Case, async: true`, no
    exclude tag (default suite → merge-blocking `quality` lane). Carry a `@moduledoc` mirroring
    `ci_lane_split_test.exs:9-21` stating this asserts SHIPPED artifacts only and does NOT couple to
    `.planning/` paths. Set `@repo_root Path.expand("../..", __DIR__)` and
    `@install_smoke_script Path.join(@repo_root, "scripts/install_smoke.sh")`; read it once in
    `setup_all` returning `%{install_smoke_script: File.read!(@install_smoke_script)}`.

    Add ONE test (implements LOCK-01) that asserts: (a) the script contains the probe literal
    `mix phx.new --version`; (b) the script contains the self-install literal
    `mix archive.install hex phx_new --force`; (c) the install precedes the smoke by ORDER INDEX.
    For the order index use `:binary.match(script, "mix archive.install hex phx_new") |> elem(0)`
    (the BARE substring with NO `MIX_ENV=dev ` prefix and NO `--force` suffix, so cosmetic edits to
    the live line do not break the order check — RESEARCH Anti-Pattern #1), and
    `:binary.match(script, "generated_app_smoke_test.exs") |> elem(0)`; assert install_idx < smoke_idx.
    `:binary.match` RAISES if a substring is absent — that is the intended loud failure for a deleted
    guard. Give each assertion a descriptive message. Reads `scripts/install_smoke.sh` ONLY — never a
    `.planning/` path (so LOCK-05 stays green). Per LOCK-01.
  </action>
  <verify>
    <automated>mix test test/install_smoke/install_smoke_preflight_test.exs</automated>
  </verify>
  <done>
    Test GREEN against current `main` (the guard is present and ordered). The test would RED if the
    `mix phx.new --version` probe, the `mix archive.install hex phx_new --force` self-install, or the
    install-before-smoke ordering were removed from `scripts/install_smoke.sh`. Module uses
    `async: true`, no exclude tag, reads no `.planning/` path.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: LOCK-05 — .planning/ path hygiene meta-test over test/**/*.exs</name>
  <files>test/planning_path_hygiene_test.exs</files>
  <read_first>
    - test/async_safety_guard_test.exs (`Path.wildcard` glob skeleton; `assert files != []` anti-vacuous guard at `:57-59`; `flat_map → sort → assert offenders == []` offender-list at `:95-107`; `failure_message/1` file:line message style at `:491-510`)
    - test/install_smoke/ci_lane_split_test.exs (`:9-21` SHIPPED-artifacts-ONLY `@moduledoc` convention)
  </read_first>
  <action>
    Create a NEW top-level module `Rindle.PlanningPathHygieneTest` in
    `test/planning_path_hygiene_test.exs` per D-01. Use `use ExUnit.Case, async: true`, no exclude
    tag. Carry the SHIPPED-artifacts-only `@moduledoc`. Set `@test_root Path.expand("..", __DIR__)`
    and glob `Path.wildcard(Path.join(@test_root, "test/**/*.exs"))` — note the BROADER `*.exs`
    (superset of `*_test.exs`, catches support/helper files), per RESEARCH Anti-Pattern #4. In
    `setup_all`, compute the file list and `assert files != []` with a descriptive message BEFORE
    scanning (anti-vacuous — RESEARCH Pitfall 5; research's Lock C snippet OMITS this, so ADD it to
    match `async_safety_guard_test.exs:59`); return `%{files: files}`.

    Add ONE test (implements LOCK-05) that, for each file, splits on newlines with
    `Enum.with_index(1)`, filters lines matching the regex that detects a runtime `.planning/` read,
    and builds a sorted `file:line: trimmed` offender list, then `assert offenders == []` with a
    `failure_message/1`-style message. Build the detection regex from these three call-form
    fragments combined with the literal directory token so the match requires BOTH a file-read call
    AND the planning directory on the same line: the read calls are `File.read!`, `File.exists?`,
    and `Path.expand`; the directory token is the dot-planning path segment. Construct the regex at
    runtime (e.g. via `Regex.compile!` from interpolated parts, or assemble the alternation
    programmatically) so the literal dot-planning directory token does NOT appear as a bare written
    substring that this very test's own scan would later flag — i.e. the test file must not itself
    contain a line that both calls one of those read functions AND names the planning dir.
    IMPORTANT: this test reads files under `test/` ONLY (the glob root), never a `.planning/` path.
    Per LOCK-05.
  </action>
  <verify>
    <automated>mix test test/planning_path_hygiene_test.exs</automated>
  </verify>
  <done>
    Test GREEN on current `main` (live grep confirms zero `.planning/` runtime reads under `test/`).
    The test REDs if any `test/**/*.exs` file reads a `.planning/` path via `File.read!`,
    `File.exists?`, or `Path.expand`. `assert files != []` present (fails loudly on an empty glob).
    Module is `async: true`, no exclude tag, and itself reads no `.planning/` path.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate → main | A regression in a shipped artifact (install_smoke.sh guard, or a re-coupled .planning/ path) reaching `main` undetected |

This phase ships only internal meta-tests over shipped artifacts. No untrusted input, no new
attack surface, no `lib/` change. The realistic security-relevant threat is a **false-confidence
regression** — a lock that vacuously passes and lets the original flaw silently return.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-111-01 | Tampering | LOCK-05 glob (`test/**/*.exs`) | medium | mitigate | `assert files != []` so an empty/mis-rooted glob fails loudly instead of vacuously passing (Task 2) |
| T-111-02 | Repudiation | LOCK-01 order-index | low | mitigate | `:binary.match` RAISES on an absent substring + order-index proves sequencing, not just presence (Task 1) |
| T-111-03 | Tampering | new meta-tests themselves | medium | mitigate | both tests read SHIPPED paths only (`scripts/`, `test/`); LOCK-05 would catch a `.planning/` coupling introduced by either new test in the SAME run |
</threat_model>

<verification>
- `mix test test/install_smoke/install_smoke_preflight_test.exs test/planning_path_hygiene_test.exs` GREEN.
- Anti-theater RED proof (per 111-VALIDATION sign-off): temporarily mutate `scripts/install_smoke.sh`
  (delete the `mix archive.install hex phx_new --force` line) → LOCK-01 REDs; revert. Temporarily add
  a throwaway `test/scratch_planning_read.exs` that reads a `.planning/` path → LOCK-05 REDs; delete it.
- Both modules use `async: true` with no exclude tag (default suite → `quality` lane).
- Re-grep `grep -rn "install_smoke\|planning" test/` after authoring to confirm no OTHER test's literal changed (OBS-02 content-drift guard).
</verification>

<success_criteria>
- LOCK-01 and LOCK-05 meta-tests exist, GREEN on `main`, and provably RED against their respective regressions.
- Both ride the existing merge-blocking `quality` lane (no new required check, no `setup_branch_protection.sh` change).
- Neither test reads a `.planning/` path. Zero `lib/` change.
</success_criteria>

<output>
Create `.planning/phases/111-regression-locks/111-01-SUMMARY.md` when done.
</output>

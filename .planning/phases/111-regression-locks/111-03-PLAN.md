---
phase: 111-regression-locks
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/adoption_demo/e2e/support/admin-polish.js
  - brandbook/src/admin-gallery-check.mjs
autonomous: true
requirements: [LOCK-03]
must_haves:
  truths:
    - "A single exported focusVisibly(page, locator) helper in admin-polish.js owns the Tab-first keyboard-modality focus workaround"
    - "All three former raw focus({ focusVisible: true }) call sites now call focusVisibly"
    - "The gallery consumes focusVisibly via the existing adoptionRequire(...admin-polish.js) import — no new module system"
    - "Post-dedupe, the call-form focus({ focusVisible: true }) appears exactly once (inside the helper) in admin-polish.js and zero times in the gallery"
  artifacts:
    - "examples/adoption_demo/e2e/support/admin-polish.js (focusVisibly helper + module.exports entry)"
    - "brandbook/src/admin-gallery-check.mjs (consumes focusVisibly)"
  key_links:
    - "focusVisibly preserves site 1's blur-first-if-active behavior (semantic, not cosmetic)"
    - "Site 2's state-reading evaluate is kept separate; only the Tab+focus prelude is extracted"
    - "focusVisibly added to module.exports; gallery destructures it from the existing adoptionRequire import"
---

<objective>
Dedupe the Tab-first focus-visible keyboard-modality workaround into ONE exported helper
focusVisibly(page, locator) in admin-polish.js, and route all three former raw
focus({ focusVisible: true }) call sites (two in admin-polish.js, one in admin-gallery-check.mjs)
through it.

Purpose: Remove the "fixed in two places, forget the third" footgun that produced the original
2026-06-26 flake. Post-dedupe the raw modality call lives in exactly one place — the precondition
LOCK-04 (Plan 04) asserts.
Output: admin-polish.js (new focusVisibly helper + export), admin-gallery-check.mjs (consumes the helper).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Edit targets (re-verify the live line anchors before editing)
@examples/adoption_demo/e2e/support/admin-polish.js
@brandbook/src/admin-gallery-check.mjs
</context>

<artifacts_this_phase_produces>
NEW symbol introduced by this plan (excluded from drift checks):
- JS helper focusVisibly exported from examples/adoption_demo/e2e/support/admin-polish.js (module.exports)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: LOCK-03 — define focusVisibly in admin-polish.js and replace both local sites</name>
  <files>examples/adoption_demo/e2e/support/admin-polish.js</files>
  <read_first>
    - examples/adoption_demo/e2e/support/admin-polish.js — Site 1 assertFocusVisibleTokens (Tab press with .catch at :428; the focus at :432 lives inside an item.evaluate that ALSO does `if (document.activeElement === element) element.blur();`); Site 2 assertFocusVisibleVsPointer (Tab press :927; the focus at :929 is the FIRST statement of a LARGER item.evaluate((el, contract) => {...}) that reads computed styles and RETURNS state); module.exports block (:1099-1128, includes assertFocusVisibleTokens, assertFocusVisibleVsPointer, assertConsistentRhythm, assertNoHorizontalScroll). RE-VERIFY all anchors live — the file is ~1128 lines.
    - NOTE: a COMMENT at :425 contains the prose form of the modality option wrapped in backticks. Do NOT remove or alter comment text; only the CODE call sites are deduped. The post-dedupe LOCK-04 count keys off the CODE call-form (an el.focus(...) expression), which never appears in that comment.
  </read_first>
  <action>
    Define a single helper `async function focusVisibly(page, locator)` in admin-polish.js. It MUST:
    (1) press Tab to enter keyboard modality, swallowing the press error to match existing sites
    (`await page.keyboard.press("Tab").catch(() => {});`); (2) run a locator.evaluate that first
    blurs the element if it is the active element, then calls element.focus with the focusVisible
    option set true, with a trailing .catch swallow. The blur-first-if-active step is LOAD-BEARING
    (RESEARCH Pitfall 2 — site 1 relies on it; making the helper always blur-first is idempotent and
    safe for all three sites). This helper body is the ONLY code location in the repo that may contain
    the el.focus call-form after dedupe.

    Add focusVisibly to the module.exports object (:1099-1128), adjacent to the focus exports
    (assertFocusVisibleTokens, assertFocusVisibleVsPointer) — same convention the gallery already
    imports via adoptionRequire.

    Replace Site 1 (:428-434): remove the inline Tab + blur-first + focus block and call
    `await focusVisibly(page, item);` (the helper preserves the blur-first behavior identically).

    Replace Site 2 (:927-929): the focus is the first statement inside a LARGER state-reading
    item.evaluate((el, contract) => {...}) that RETURNS computed-style state (RESEARCH Pitfall 3).
    Do NOT wholesale-replace that evaluate. Call `await focusVisibly(page, item);` for the Tab+focus
    prelude, THEN keep the existing state-reading item.evaluate (now without the leading focus line,
    since focus persists across evaluate calls) — preserve its parameters, body, and return value
    exactly otherwise.

    Leave the per-script pure WCAG-math helpers untouched (only the stateful modality workaround is
    deduped, per D-02). Do NOT alter the :425 explanatory comment. Per LOCK-03.
  </action>
  <verify>
    <automated>node --check examples/adoption_demo/e2e/support/admin-polish.js && node -e "const m=require('./examples/adoption_demo/e2e/support/admin-polish.js'); process.exit(typeof m.focusVisibly==='function'?0:1)" && test "$(grep -cE 'focus[(][{] focusVisible: true [}][)]' examples/adoption_demo/e2e/support/admin-polish.js)" = "1"</automated>
  </verify>
  <done>
    focusVisibly is exported and is a function. node --check passes (valid JS). The call-form
    el.focus with focusVisible appears exactly ONCE in admin-polish.js (inside the helper). Both
    former local sites call focusVisibly; site 2's state-reading evaluate and its return value are
    preserved. The :425 comment is unchanged.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: LOCK-03 — consume focusVisibly in the gallery, remove the raw site</name>
  <files>brandbook/src/admin-gallery-check.mjs</files>
  <read_first>
    - brandbook/src/admin-gallery-check.mjs — the adoptionRequire(...admin-polish.js) destructure import (:25-26, currently `{ assertConsistentRhythm, assertNoHorizontalScroll }`); Site 3 (the gallery focus site: Tab press at :173 with NO .catch, and the focus at :178 via locator.evaluate(...el.focus with focusVisible...)). A COMMENT at :170 contains the prose form of the modality option in backticks — leave it untouched. RE-VERIFY anchors live.
    - depends on Plan 03 Task 1 having added focusVisibly to admin-polish.js module.exports (same plan, prior task).
  </read_first>
  <action>
    Add focusVisibly to the existing adoptionRequire(...admin-polish.js) destructure at :25-26
    (alongside assertConsistentRhythm, assertNoHorizontalScroll) — NO new module system, reuse the
    already-load-bearing import path. Replace Site 3: remove both the Tab press (:173) and the raw
    locator.evaluate(...el.focus...) (:178) and replace them with a single
    `await focusVisibly(page, locator);` per iteration. The helper centralizes the Tab-press .catch
    swallow (the gallery's bare Tab press at :173 had none — adopting the helper's swallow is correct
    and intended, per D-02 discretion / RESEARCH). Post-edit the gallery must contain ZERO call-form
    el.focus-with-focusVisible occurrences (LOCK-04 in Plan 04 asserts this). Do NOT alter the :170
    comment. Per LOCK-03.
  </action>
  <verify>
    <automated>node --check brandbook/src/admin-gallery-check.mjs && test "$(grep -cE 'focus[(][{] focusVisible: true [}][)]' brandbook/src/admin-gallery-check.mjs)" = "0"</automated>
  </verify>
  <done>
    The gallery destructures focusVisibly from the admin-polish.js adoptionRequire import and calls
    it at the former Site 3. ZERO call-form el.focus-with-focusVisible occurrences remain in the
    gallery. node --check passes. The :170 comment is unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness source → CI gate | A duplicated/forgotten modality copy silently reintroducing the flake |

This is an internal test-harness refactor (demo + brandbook JS, NOT adopter-facing lib/). No
untrusted input, no new attack surface, no semver impact. The security-relevant threat is the
recurrence of the original duplication footgun.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-111-06 | Tampering | site 1 blur-first behavior | medium | mitigate | focusVisibly always blurs-first-if-active so the dedupe preserves, not just moves, site 1's semantics (Task 1) |
| T-111-07 | Tampering | site 2 state-reading evaluate | medium | mitigate | Extract only the Tab+focus prelude; keep the state-reading evaluate and its return value intact (Task 1) |
| T-111-08 | Spoofing (false-green) | future 4th modality copy | high | transfer | Dedupe collapses to one helper; LOCK-04 (Plan 04, wave 2) asserts the post-dedupe single-occurrence invariant to catch any future bypass |
</threat_model>

<verification>
- node --check passes for both files; admin-polish.js exports focusVisibly as a function.
- Call-form occurrence count: admin-polish.js == 1 (helper only), gallery == 0.
- Behavioral parity (CI-delegated, per house practice for the adoption-demo-e2e lane): the
  adoption-demo-e2e + gallery-check runs still pass with the deduped helper.
- This plan touches NO test/*.exs and NO ci.yml literal — no OBS-02 content-drift risk to existing meta-tests.
</verification>

<success_criteria>
- One exported focusVisibly helper owns the modality workaround; all three former sites call it.
- admin-polish.js call-form count == 1, gallery == 0 (the LOCK-04 precondition).
- Site 1 blur-first and site 2 state-reading evaluate semantics preserved. Zero lib/ change; no semver impact.
</success_criteria>

<output>
Create `.planning/phases/111-regression-locks/111-03-SUMMARY.md` when done.
</output>
---
phase: 111-regression-locks
plan: 04
type: execute
wave: 2
depends_on: ["111-03"]
files_modified:
  - test/focus_visible_modality_guard_test.exs
autonomous: true
requirements: [LOCK-04]
must_haves:
  truths:
    - "A merge-blocking ExUnit test asserts the shared focusVisibly helper presses Tab before the programmatic focusVisible focus"
    - "A merge-blocking ExUnit test asserts no harness calls the raw focus({ focusVisible: true }) call-form outside the helper"
    - "The test fails loudly (anti-vacuous) if the harness glob matches zero files"
    - "The test keys off the presence of the focusVisible feature so a file that drops it cannot silently exempt itself"
  artifacts:
    - test/focus_visible_modality_guard_test.exs
  key_links:
    - "Count-based mechanic: admin-polish.js call-form count == 1, gallery == 0 (catches a future 4th copy in either file)"
    - "Authored AGAINST the post-dedupe state (depends on Plan 03)"
    - "Reads examples/ and brandbook/ shipped paths only — never .planning/ (so LOCK-05 stays green)"
---

<objective>
Ship the LOCK-04 merge-blocking meta-test (`Rindle.FocusVisibleModalityGuardTest`,
`test/focus_visible_modality_guard_test.exs`) that asserts the POST-DEDUPE modality invariant from
Plan 03: (1) the shared `focusVisibly` helper presses Tab before the programmatic focusVisible
focus; AND (2) no harness file contains the raw focusVisible call-form outside that helper.

Purpose: Lock the dedupe so a future fourth focus check that copies the raw modality call (without
Tab, without the helper) fails CI — the exact "duplicated in two places at once" footgun that
produced the original flake. Authored against the deduped state (hence wave 2, after Plan 03).
Output: `test/focus_visible_modality_guard_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Assertion targets (post-dedupe state from Plan 03)
@examples/adoption_demo/e2e/support/admin-polish.js
@brandbook/src/admin-gallery-check.mjs

# House meta-test idiom to mirror
@test/async_safety_guard_test.exs
@test/install_smoke/ci_lane_split_test.exs

# Dependency context
@.planning/phases/111-regression-locks/111-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
NEW symbol introduced by this plan (excluded from drift checks):
- ExUnit module Rindle.FocusVisibleModalityGuardTest (file test/focus_visible_modality_guard_test.exs)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: LOCK-04 — focus-visible modality guard meta-test (count-based dual-assert)</name>
  <files>test/focus_visible_modality_guard_test.exs</files>
  <read_first>
    - test/async_safety_guard_test.exs (glob + filter skeleton; `assert files != []` anti-vacuous guard at :57-59; offender-list flat_map → sort → `assert offenders == []` at :95-107; file:line failure_message style at :491-510)
    - test/install_smoke/ci_lane_split_test.exs (:9-21 SHIPPED-artifacts-ONLY @moduledoc convention)
    - examples/adoption_demo/e2e/support/admin-polish.js (post-Plan-03: the focusVisibly helper body presses Tab then runs the el.focus call-form ONCE; verify the Tab press precedes the focus inside the helper)
    - brandbook/src/admin-gallery-check.mjs (post-Plan-03: ZERO call-form occurrences; it calls focusVisibly)
    - CRITICAL counting caveat: the BARE substring `focusVisible: true` ALSO appears in explanatory COMMENTS (admin-polish.js :425, gallery :170, both backtick-wrapped prose). A bare-substring count is therefore WRONG (it would see comment occurrences). Count the CODE CALL-FORM instead — the `el.focus({ focusVisible: true })` expression — which appears ONLY in real call sites, never in those comments. (Verified live 2026-06-28: call-form regex matches 1 in admin-polish.js post-dedupe, 0 in gallery; bare substring matches 2 and 1 respectively due to comments.)
  </read_first>
  <action>
    Create `Rindle.FocusVisibleModalityGuardTest` in `test/focus_visible_modality_guard_test.exs`.
    Use `use ExUnit.Case, async: true`, no exclude tag (default suite → merge-blocking `quality`
    lane). Carry the SHIPPED-artifacts-only `@moduledoc`. Set `@repo_root Path.expand("..", __DIR__)`
    and `@harnesses` listing the two harness relative paths
    (`examples/adoption_demo/e2e/support/admin-polish.js`, `brandbook/src/admin-gallery-check.mjs`).

    Test A — "the shared focusVisibly helper presses Tab before the programmatic focus": read
    admin-polish.js; assert it defines focusVisibly (regex tolerant of `function focusVisibly` or
    `focusVisibly = (async )?(`); then ORDER-INDEX assert that the Tab-press substring
    (`keyboard.press("Tab")`) precedes the call-form focusVisible expression, using
    `:binary.match(...) |> elem(0)` on each and asserting tab_idx < fv_idx. (:binary.match raises if
    absent → loud failure if the helper drops Tab or the focus.)

    Test B — "no harness calls the raw focusVisible call-form outside the shared helper": build the
    file list as `@harnesses |> Enum.map(&Path.join(@repo_root, &1)) |> Enum.filter(&File.exists?/1)`
    and `assert files != []` with a descriptive message (anti-vacuous — RESEARCH Pitfall 5). Then use
    the COUNT-BASED mechanic (RESEARCH Pattern 2 / Open Q1 — preferred over brittle region-scoping):
    count CALL-FORM occurrences (`el.focus({ focusVisible: true })`, NOT the bare substring — see the
    counting caveat above; build the call-form matcher at runtime so the literal does not appear as a
    plain written substring this test's own LOCK-05 sibling would flag is irrelevant here, but DO use
    a Regex that matches `focus(` + `{` + the focusVisible option + `}` + `)` allowing flexible inner
    spacing). Assert admin-polish.js has EXACTLY ONE call-form occurrence (inside focusVisibly) and
    admin-gallery-check.mjs has ZERO. Emit a sorted file:line offender list in the failure message
    (mirror the analog's failure_message style) for any file whose count deviates. This count pair
    catches a future fourth copy in EITHER file. Per LOCK-04, D-03.

    Anti-vacuous: key Test B off the call-form presence; `assert files != []`. Read examples/ and
    brandbook/ shipped paths ONLY — never a `.planning/` path (so LOCK-05 stays green). Do NOT write
    the bare prose literal `focusVisible: true` as a standalone grep-able token in this test file in a
    way that a count over THIS file would self-trip — but note LOCK-04 scans @harnesses only (this
    test file is not in @harnesses), so cross-contamination is structurally impossible.
  </action>
  <verify>
    <automated>mix test test/focus_visible_modality_guard_test.exs</automated>
  </verify>
  <done>
    Both tests GREEN against the post-Plan-03 deduped state. Test A REDs if the helper drops Tab or
    reorders it after the focus. Test B REDs if a second call-form occurrence appears in
    admin-polish.js OR any call-form appears in the gallery (future 4th-copy footgun). `assert files
    != []` present. Module is `async: true`, no exclude tag, reads no `.planning/` path. Counting uses
    the CODE call-form, not the bare substring (immune to the comment occurrences).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness source → CI gate | A future modality copy bypassing the deduped helper, reaching main undetected |

This phase ships an internal meta-test over shipped JS harnesses. No untrusted input, no new attack
surface, no lib/ change. The security-relevant threat is a **false-confidence regression**: a lock
that vacuously passes or miscounts (because of the comment occurrences) and lets the duplication
footgun silently return.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-111-09 | Spoofing (false-green) | bare-substring miscount vs comments | high | mitigate | Count the CODE call-form (el.focus expression), not the bare `focusVisible: true` substring that also appears in comments — otherwise the lock asserts a wrong count and either always-reds or masks a real bypass (Task 1) |
| T-111-10 | Tampering | empty/mis-rooted harness glob | medium | mitigate | `assert files != []` so a glob matching zero harnesses fails loudly instead of vacuously passing (Task 1) |
| T-111-11 | Spoofing (false-green) | future 4th modality copy | high | mitigate | Count-based dual-assert (admin-polish.js == 1, gallery == 0) catches a new raw call-form in EITHER file (Task 1) |
</threat_model>

<verification>
- `mix test test/focus_visible_modality_guard_test.exs` GREEN (after Plan 03 lands).
- Anti-theater RED proof (111-VALIDATION sign-off): temporarily add a raw call-form
  `el.focus({ focusVisible: true })` to admin-gallery-check.mjs (or a second to admin-polish.js) →
  Test B REDs; revert. Temporarily reorder the helper so focus precedes Tab → Test A REDs; revert.
- Confirm the bare-substring comment occurrences (admin-polish.js :425, gallery :170) do NOT trip
  the call-form count (the whole point of the call-form mechanic).
- Module `async: true`, no exclude tag (default suite → `quality` lane).
</verification>

<success_criteria>
- LOCK-04 meta-test exists, GREEN on the deduped state, provably RED against a Tab-reorder and a
  bypassing copy. Counting immune to the comment occurrences.
- Rides the existing merge-blocking `quality` lane; reads no `.planning/` path. Zero lib/ change.
</success_criteria>

<output>
Create `.planning/phases/111-regression-locks/111-04-SUMMARY.md` when done.
</output>

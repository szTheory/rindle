---
phase: 103-observability-baseline
plan: 04
type: execute
wave: 2
depends_on: ["103-02"]
files_modified:
  - .planning/phases/103-observability-baseline/103-BASELINE.md
autonomous: true
requirements: [OBS-03]
tags: [ci, observability, baseline, branch-protection, drift, internal-doc]

must_haves:
  truths:
    - "A committed internal doc records the per-job avg + p95 + rerun/flake baseline before any restructuring"
    - "The doc records the verbatim live branch-protection required-check names and the expected-vs-live diff"
    - "The known live drift (brandbook-tokens expected but absent live) is recorded verbatim, not fixed"
    - "The baseline doc lives only in .planning/ and never enters the shipped Hex package"
  artifacts:
    - path: ".planning/phases/103-observability-baseline/103-BASELINE.md"
      provides: "OBS-03 committed baseline table + live required-check names + drift record (D-08/D-09/D-10/D-11)"
      contains: "required status checks"
  key_links:
    - from: ".planning/phases/103-observability-baseline/103-BASELINE.md"
      to: "scripts/ci/collect_ci_baseline.sh + scripts/ci/check_required_checks.sh"
      via: "the doc is the captured output of running both Plan 02 collectors before restructuring"
      pattern: "p95"
  prohibitions:
    - "Do NOT re-apply or mutate branch protection — record the live drift verbatim, do not fix it (D-09/D-14)"
    - "Do NOT add 103-BASELINE.md to mix.exs files: or HexDocs extras — it stays internal to .planning/ (D-10)"
    - "Do NOT begin any restructuring change — this capture is the gate BEFORE Phases 104+ (D-11)"
---

<objective>
Run the two OBS-03 collectors from Plan 02 and author the committed internal baseline document
`103-BASELINE.md`: a per-job avg + p95 + rerun/flake table over recent `ci.yml` runs, plus the
verbatim live branch-protection required-check names and the expected-vs-live diff. This is the
load-bearing OBS-03 capture (D-11) that the Phase 105 branch-protection flip and the Phase 107
regression-vs-baseline check depend on — and it must be captured before any restructuring change.

Purpose: every later v1.20 restructuring decision must be evidence-backed against a frozen
pre-change reference. The live required-check capture is the authoritative input to the Phase 105
flip; research already found real drift today (`brandbook-tokens` is in the expected list but absent
from live `.contexts[]`), which OBS-03 must record verbatim.

Output: `.planning/phases/103-observability-baseline/103-BASELINE.md` — committed, internal-only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-observability-baseline/103-CONTEXT.md
@.planning/phases/103-observability-baseline/103-RESEARCH.md
@.planning/phases/103-observability-baseline/103-PATTERNS.md
@.planning/phases/103-observability-baseline/103-VALIDATION.md

@scripts/ci/collect_ci_baseline.sh
@scripts/ci/check_required_checks.sh
@scripts/setup_branch_protection.sh
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-verify live state and capture the required-check + timing baseline</name>
  <files>.planning/phases/103-observability-baseline/103-BASELINE.md</files>
  <read_first>
    - scripts/ci/check_required_checks.sh (the Plan 02 read+diff script)
    - scripts/ci/collect_ci_baseline.sh (the Plan 02 avg/p95/rerun collector)
    - .planning/phases/103-observability-baseline/103-VALIDATION.md § "Manual-Only Verifications" — re-run the live required-check read IMMEDIATELY before capture (branch protection can drift between runs)
    - .planning/phases/103-observability-baseline/103-RESEARCH.md § "Runtime State Inventory" + "OBS-03 live required-check diff" — the known live drift (`brandbook-tokens` absent) is to be recorded, NOT fixed
  </read_first>
  <action>
    Using the maintainer's authed `gh` session, run `bash scripts/ci/check_required_checks.sh main`
    immediately before capture (re-verify per VALIDATION.md — protection can change), and
    `bash scripts/ci/collect_ci_baseline.sh` (defaults: `main`, last 50 runs). Capture both outputs.
    Do NOT mutate branch protection — record the live `.contexts[]` verbatim and the expected-vs-live
    diff, including the `brandbook-tokens` drift, exactly as emitted (D-09/D-14 — read and record only).
    If the maintainer `gh` session is unavailable in this environment, surface that explicitly and route
    this capture to a maintainer-local run (per VALIDATION.md Manual-Only); the `bash -n` lint and
    script presence from Plan 02 remain the statically checkable gate. Hold the captured tables for
    Task 2.
  </action>
  <verify>
    <automated>test -x scripts/ci/check_required_checks.sh && test -x scripts/ci/collect_ci_baseline.sh && bash -n scripts/ci/check_required_checks.sh && bash -n scripts/ci/collect_ci_baseline.sh</automated>
  </verify>
  <acceptance_criteria>
    - Both Plan 02 scripts exist, are executable, and pass `bash -n`
    - `check_required_checks.sh main` is run immediately before capture (re-verification per VALIDATION.md) — its live `.contexts[]` + diff output is captured
    - `collect_ci_baseline.sh` is run and its per-job avg/p95 + rerun-rate table is captured
    - No branch-protection mutation occurred (read-only); the `brandbook-tokens` expected-only drift is captured verbatim
    - If `gh` is unavailable, the capture is explicitly routed to a maintainer-local run (not silently skipped)
  </acceptance_criteria>
  <done>The live required-check names + timing baseline are captured from the read-only collectors, with the known drift recorded verbatim.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author and commit 103-BASELINE.md (internal-only)</name>
  <files>.planning/phases/103-observability-baseline/103-BASELINE.md</files>
  <read_first>
    - .planning/phases/103-observability-baseline/103-PATTERNS.md § "103-BASELINE.md (CREATE — internal doc)" — internal `.planning/` doc, NOT in Hex `files:`/`extras`; no .gitignore exclusion needed
    - .planning/phases/103-observability-baseline/103-CONTEXT.md D-10/D-11 — internal-only, captured before restructuring
    - mix.exs (the `files:` allowlist at :278-279 and HexDocs `extras` at :154-172 — confirm 103-BASELINE.md is excluded from both; do NOT edit them)
  </read_first>
  <action>
    Write `.planning/phases/103-observability-baseline/103-BASELINE.md` containing: (1) a capture
    header with the date, repo, branch, and run window (N); (2) the per-job avg + p95 + rerun/flake
    table from `collect_ci_baseline.sh`; (3) the verbatim live branch-protection required-check
    `.contexts[]` names; (4) the expected-vs-live diff with an explicit "Recorded drift" note calling
    out `brandbook-tokens` (expected per setup_branch_protection.sh:30, absent live) — recorded, NOT
    fixed (D-09/D-14); (5) a one-line statement that this is the pre-restructuring OBS-03 baseline
    (D-11) and the input to the Phase 105 flip + Phase 107 regression check. Keep it in `.planning/`
    only — do NOT add it to the mix.exs `files:` allowlist or HexDocs `extras` (D-10). Confirm those
    two mix.exs lists are byte-unchanged and that the doc is not matched by any Hex packaging glob.
  </action>
  <verify>
    <automated>test -f .planning/phases/103-observability-baseline/103-BASELINE.md && grep -qiE 'p95|avg' .planning/phases/103-observability-baseline/103-BASELINE.md && grep -qiE 'required.status.check|required_status|contexts' .planning/phases/103-observability-baseline/103-BASELINE.md && grep -qi 'brandbook-tokens' .planning/phases/103-observability-baseline/103-BASELINE.md && ! grep -q '103-BASELINE' mix.exs</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/103-observability-baseline/103-BASELINE.md` exists and contains the per-job avg + p95 + rerun/flake table
    - It records the verbatim live required-check `.contexts[]` names and the expected-vs-live diff
    - It records the `brandbook-tokens` drift verbatim with a "recorded, not fixed" note (D-09/D-14)
    - It states this is the pre-restructuring OBS-03 baseline (D-11), feeding Phase 105 + Phase 107
    - `103-BASELINE.md` is NOT referenced in mix.exs (`files:` allowlist / HexDocs `extras` byte-unchanged); the doc stays internal to `.planning/` (D-10)
  </acceptance_criteria>
  <done>The committed internal baseline doc captures the timing baseline + live required-check names + recorded drift before any restructuring, and stays out of the shipped package.</done>
</task>

</tasks>

<artifacts_produced>
This phase plan produces the following symbol (used by the source-grounding pass to exclude
newly-created symbols from drift verification):

- `.planning/phases/103-observability-baseline/103-BASELINE.md` (NEW) — committed internal-only OBS-03
  baseline: per-job avg/p95/rerun table + verbatim live required-check `.contexts[]` + expected-vs-live
  diff (incl. recorded `brandbook-tokens` drift). Captured by running Plan 02's read-only collectors.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer `gh` session → live branch protection | Capture must read, never mutate, live protection |
| internal doc → shipped Hex package | The baseline must not leak into the published package |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-10 | Tampering | Re-applying branch protection while capturing it | mitigate | Capture uses the read-only Plan 02 scripts (no mutation verbs); drift is recorded verbatim, not fixed (D-09/D-14); acceptance criteria asserts the drift is present as a record |
| T-103-11 | Information Disclosure | `103-BASELINE.md` leaking internal CI timing into the shipped Hex package | mitigate | Doc lives in `.planning/` only; not added to mix.exs `files:`/`extras` (D-10); acceptance criteria asserts `mix.exs` does not reference it |
| T-103-SC | Tampering | npm/pip/cargo installs in this plan | accept | No package installs — this plan only runs read-only scripts and writes a Markdown doc |
</threat_model>

<verification>
- Both Plan 02 collectors are present, executable, and lint-clean before capture.
- The live required-check read is re-run immediately before capture (no stale snapshot).
- `103-BASELINE.md` records the timing baseline + verbatim live required-check names + the `brandbook-tokens` drift.
- No branch-protection mutation; the doc is internal-only (mix.exs `files:`/`extras` unchanged).
</verification>

<success_criteria>
- OBS-03 captured and committed before any restructuring (D-11).
- Live drift recorded verbatim (not fixed); baseline is the input to Phases 105 + 107.
- Baseline stays internal to `.planning/`; zero shipped-package change.
</success_criteria>

<output>
Create `.planning/phases/103-observability-baseline/103-04-SUMMARY.md` when done.
</output>
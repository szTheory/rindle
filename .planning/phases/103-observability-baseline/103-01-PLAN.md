---
phase: 103-observability-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - test/test_helper.exs
autonomous: true
requirements: [OBS-02]
tags: [ci, observability, elixir, junit, coverage, test-harness]

must_haves:
  truths:
    - "Running `CI=1 mix test` writes a JUnit XML report file the CI upload step can ship"
    - "Running `mix coveralls.json` writes cover/excoveralls.json for the coverage artifact"
    - "The junit_formatter dependency is test-only and never enters the shipped Hex package"
    - "Local `mix test` (no CI env) does not emit JUnit XML, keeping local runs quiet"
  artifacts:
    - path: "mix.exs"
      provides: "test-only {:junit_formatter, \"~> 3.4\", only: :test} dependency (D-06)"
      contains: "junit_formatter"
    - path: "test/test_helper.exs"
      provides: "ExUnit.start formatters wiring + junit_formatter report_dir/report_file config (D-06)"
      contains: "JUnitFormatter"
  key_links:
    - from: "test/test_helper.exs"
      to: "_build/test/junit/rindle-junit.xml"
      via: "Application.put_env(:junit_formatter, :report_dir/:report_file) + formatters: on ExUnit.start"
      pattern: "junit_formatter.*report_dir"
  prohibitions:
    - "Do NOT add junit_formatter to mix.exs files: allowlist (mix.exs:278-279) — test-only deps must not ship"
    - "Do NOT remove or reorder the existing exclude_tags logic, repo/Oban/Mock startup in test_helper.exs"
    - "Do NOT change any lib/ source (zero lib/ change invariant)"
---

<objective>
Lay the OBS-02 test-harness foundation: add the test-only `junit_formatter` dependency and wire
ExUnit to emit a JUnit XML report (CI-only) plus confirm ExCoveralls already produces the coverage
JSON. This is Wave 0 / harness work — the JUnit XML path and coverage JSON path created here are what
the Plan 03 `ci.yml` `upload-artifact` step references, so they must exist and be verified first.

Purpose: OBS-02 requires JUnit + coverage artifacts uploaded for inspection. The artifacts must be
produced by the test runtime before CI can ship them. Doing this in its own plan keeps the
artifact-production contract verifiable in isolation (Nyquist Wave 0).

Output: `mix.exs` gains `{:junit_formatter, "~> 3.4", only: :test}`; `test/test_helper.exs` emits
`_build/test/junit/rindle-junit.xml` under CI; verified that `mix coveralls.json` writes
`cover/excoveralls.json`; verified both paths are gitignored.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-observability-baseline/103-CONTEXT.md
@.planning/phases/103-observability-baseline/103-RESEARCH.md
@.planning/phases/103-observability-baseline/103-PATTERNS.md
@.planning/phases/103-observability-baseline/103-VALIDATION.md

@mix.exs
@test/test_helper.exs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add test-only junit_formatter dependency to mix.exs</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (the deps Dev/Test block at mix.exs:125-144 — existing `only: :test` entries; the `files:` allowlist at mix.exs:278-279)
    - .planning/phases/103-observability-baseline/103-PATTERNS.md § "mix.exs (MODIFY)" — the exact dep entry shape and files-allowlist analog
    - .planning/phases/103-observability-baseline/103-RESEARCH.md § "Standard Stack" / "Package Legitimacy Audit" — junit_formatter ~> 3.4 verified compatible with both matrix pairs, `requirements: {}`, victorolinasc/junit-formatter canonical
  </read_first>
  <action>
    Add the line `{:junit_formatter, "~> 3.4", only: :test},` to the Dev/Test deps block in mix.exs,
    alongside the existing test-only entries (near `{:lazy_html, ">= 0.1.0", only: :test}` at
    mix.exs:126), per D-06. Match the existing entry formatting exactly. Do NOT touch the `files:`
    allowlist (mix.exs:278-279) — it lists only `lib priv/repo/migrations priv/static/rindle_admin
    mix.exs README.md RUNNING.md CHANGELOG.md LICENSE guides`, which already excludes test-only deps,
    satisfying the zero-lib/-change and zero-shipped-package-change invariant. Run `mix deps.get` to
    resolve and commit the updated `mix.lock`. junit_formatter is approved per the RESEARCH.md Package
    Legitimacy Audit (canonical victorolinasc/junit-formatter, 8+ yrs on Hex, `requirements: {}`).
  </action>
  <verify>
    <automated>grep -q 'junit_formatter.*~> 3.4.*only: :test' mix.exs && mix deps.get && grep -q 'junit_formatter' mix.lock && ! grep -q 'junit_formatter' <(awk '/files:/{f=1} f&&/~w\(/{print; exit}' mix.exs)</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'junit_formatter' mix.exs` shows exactly `{:junit_formatter, "~> 3.4", only: :test},` in the Dev/Test deps block
    - `mix deps.get` exits 0 and `mix.lock` contains a `junit_formatter` entry
    - The `files:` ~w(...) allowlist in mix.exs is byte-unchanged (does NOT contain `junit_formatter`, `test`, `deps`, or `_build`)
    - No file under `lib/` is modified
  </acceptance_criteria>
  <done>junit_formatter is a resolved test-only dependency; the shipped-package allowlist is unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire junit_formatter into test_helper.exs and verify artifact production</name>
  <files>test/test_helper.exs</files>
  <read_first>
    - test/test_helper.exs (current tail: exclude_tags at :24-29, bare `ExUnit.start(exclude: exclude_tags)` at :31, Mock startup at :33-35)
    - .planning/phases/103-observability-baseline/103-RESEARCH.md § "Code Examples → junit_formatter wiring (D-06)" — the exact CI-gated formatters list + Application.put_env lines + ExUnit.start formatters: arg, and the idempotency note (keep exclude_tags + repo/Oban/Mock startup intact)
    - .planning/phases/103-observability-baseline/103-PATTERNS.md § "test/test_helper.exs (MODIFY)" — extend, do not restructure; target XML path `_build/test/junit/rindle-junit.xml`
  </read_first>
  <action>
    Extend test/test_helper.exs (do NOT restructure). Immediately before the existing
    `ExUnit.start(exclude: exclude_tags)` call (line 31), add: (1) a `formatters` binding that includes
    `JUnitFormatter` only when `System.get_env("CI")` is set (otherwise just `[ExUnit.CLIFormatter]`),
    so local runs stay quiet; (2) `Application.put_env(:junit_formatter, :report_dir, "_build/test/junit")`,
    `Application.put_env(:junit_formatter, :report_file, "rindle-junit.xml")`, plus the
    `:print_report_file` / `:include_filename?` settings per the RESEARCH.md wiring snippet. Then change
    the call to `ExUnit.start(exclude: exclude_tags, formatters: formatters)`. Keep `exclude_tags`
    (test_helper.exs:24-29), the repo/Oban startup (:1-15), and the Mock require (:33-35) byte-for-byte
    intact. The report path `_build/test/junit/rindle-junit.xml` MUST match the upload-artifact `path:`
    that Plan 03 wires into ci.yml. `_build/` is already gitignored (verify-only, no `.gitignore` edit).
  </action>
  <verify>
    <automated>rm -rf _build/test/junit cover; CI=1 mix test >/dev/null 2>&1; test -f _build/test/junit/rindle-junit.xml && CI=1 mix coveralls.json >/dev/null 2>&1 && test -f cover/excoveralls.json && git check-ignore _build/test/junit/rindle-junit.xml cover/excoveralls.json</automated>
  </verify>
  <acceptance_criteria>
    - `CI=1 mix test` exits 0 (suite still passes — gate behavior unchanged) AND produces `_build/test/junit/rindle-junit.xml`
    - `CI=1 mix coveralls.json` produces `cover/excoveralls.json` (coverage artifact for OBS-02 upload)
    - `git check-ignore _build/test/junit/rindle-junit.xml cover/excoveralls.json` lists both paths (no artifact leakage into commits or the Hex package)
    - Running plain `mix test` WITHOUT the CI env var does NOT create `_build/test/junit/rindle-junit.xml` (local runs stay quiet)
    - `exclude_tags`, the repo/Oban startup, and the Mock require in test_helper.exs are byte-unchanged
  </acceptance_criteria>
  <done>ExUnit emits the JUnit XML under CI to the path Plan 03's upload step references; coverage JSON is confirmed produced; both artifact paths are gitignored; local test runs are unaffected.</done>
</task>

</tasks>

<artifacts_produced>
This phase plan produces / wires the following symbols (used by the source-grounding pass to exclude
newly-created symbols from drift verification):

- `mix.exs` dep: `{:junit_formatter, "~> 3.4", only: :test}` (+ resulting `mix.lock` entry)
- `test/test_helper.exs` keys: `formatters` binding, `Application.put_env(:junit_formatter, …)`
  config (`:report_dir` = `_build/test/junit`, `:report_file` = `rindle-junit.xml`,
  `:print_report_file`, `:include_filename?`), `JUnitFormatter` in the `formatters:` list
- Artifact paths produced at runtime: `_build/test/junit/rindle-junit.xml` (JUnit),
  `cover/excoveralls.json` (coverage) — both gitignored, consumed by Plan 03's upload step
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runtime → committed repo | The new dep + config could alter what ships in the Hex package if mis-scoped |
| test runtime → build artifacts on disk | JUnit XML / coverage JSON written to `_build/` and `cover/` must not be committed or shipped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-01 | Information Disclosure | `junit_formatter` dep leaking into shipped Hex package | mitigate | `only: :test` scope + unchanged `files:` allowlist (mix.exs:278-279) excludes test-only deps; acceptance criteria asserts the allowlist is byte-unchanged |
| T-103-02 | Information Disclosure | JUnit XML / coverage JSON (test names, coverage %) committed or shipped | mitigate | Paths land under already-gitignored `_build/` and `cover/`; acceptance criteria runs `git check-ignore` on both; artifacts contain test names + coverage %, no secrets |
| T-103-SC | Tampering | hex install of `junit_formatter` | mitigate | RESEARCH.md Package Legitimacy Audit verified canonical victorolinasc/junit-formatter, 8+ yrs on Hex, `requirements: {}`; verdict OK/Approved — no `[ASSUMED]`/`[SUS]`/`[SLOP]` checkpoint required |
</threat_model>

<verification>
- `CI=1 mix test` passes (gate behavior unchanged) and writes `_build/test/junit/rindle-junit.xml`.
- `CI=1 mix coveralls.json` writes `cover/excoveralls.json`.
- `git check-ignore` confirms both artifact paths are ignored.
- Plain `mix test` (no CI) writes no JUnit XML.
- The Hex `files:` allowlist and all `lib/` source are unchanged.
</verification>

<success_criteria>
- OBS-02 artifact-production foundation is in place and independently verified (Wave 0).
- Zero `lib/` change; test-only dep never ships.
- The JUnit XML path matches the contract Plan 03 depends on.
</success_criteria>

<output>
Create `.planning/phases/103-observability-baseline/103-01-SUMMARY.md` when done.
</output>
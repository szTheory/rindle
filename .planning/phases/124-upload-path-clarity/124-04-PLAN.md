---
phase: 124-upload-path-clarity
plan: "04"
type: execute
wave: 4
depends_on: ["124-03"]
files_modified:
  - lib/rindle/upload/broker.ex
  - lib/rindle/upload/broker/session_seed.ex
  - lib/rindle/upload/broker/persistence.ex
  - lib/rindle/upload/broker/session_validation.ex
  - test/rindle/upload/broker_test.exs
  - test/rindle/api_surface_boundary_test.exs
autonomous: true
requirements: [UPLOAD-02, SAFE-01]
must_haves:
  truths:
    - "All Broker initiation functions retain validation/capability/adapter/persistence/compensation/event/result order while seed construction and durable writes have cohesive hidden owners."
    - "Multipart, native resumable, and tus sessions persist the same strategies, protocols, states, offsets, URI/expiry, upload ids/parts, storage keys, filenames, and expiry values."
    - "Signing, multipart completion preparation, status polling, and cancellation retain exact loaded-session guards, normalized parts/status attrs, adapter calls, errors, updates, telemetry, and broadcasts."
  artifacts:
    - path: "lib/rindle/upload/broker/session_seed.ex"
      provides: "Hidden deterministic profile/key/id/filename/expiry seed construction."
      exports: ["build/2"]
    - path: "lib/rindle/upload/broker/persistence.ex"
      provides: "Hidden asset/session transactions, strategy attrs, updates, and adjacent compensation mechanics."
      exports: ["create/3", "persist_multipart/5", "persist_resumable/5", "persist_tus/3", "update/3"]
    - path: "lib/rindle/upload/broker/session_validation.ex"
      provides: "Hidden strategy guards, profile resolution, multipart normalization/encoding, and resumable status attr mechanics."
      exports: ["multipart/1", "resumable/1", "profile_module/1", "normalize_parts/1", "encode_parts/1", "resumable_status_attrs/2"]
  key_links:
    - from: "Rindle.Upload.Broker initiation facades"
      to: "Broker.SessionSeed.build/2 and Broker.Persistence"
      via: "facade-resolved options/capability/adapter outcomes; collaborator returns existing session rows"
    - from: "Broker.Persistence persistence failure branches"
      to: "configured storage adapter compensation"
      via: "strategy-specific abort/cancel/temp cleanup adjacent to the failed transaction"
    - from: "Broker public sign/status/cancel/complete flows"
      to: "Broker.SessionValidation"
      via: "pure tagged guards/normalizers consumed in the existing with-clause order"
---

<objective>
Separate Broker session seeds, persistence/compensation, and loaded-session validation while preserving every public lifecycle operation and adapter contract.

Purpose: Deliver the validation, capability-negotiation, and session-persistence readability promised by UPLOAD-02 before extracting completion transactions.
Output: Three hidden cohesive Broker collaborators, a facade that retains orchestration/results/events, and focused compiled/behavior proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RUNNING.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-upload-path-clarity/124-CONTEXT.md
@.planning/phases/124-upload-path-clarity/124-RESEARCH.md
@.planning/phases/124-upload-path-clarity/124-PATTERNS.md
@.planning/phases/124-upload-path-clarity/124-VALIDATION.md
@.planning/phases/124-upload-path-clarity/124-03-SUMMARY.md
@lib/rindle.ex
@lib/rindle/upload/broker.ex
@lib/rindle/upload/resumable_telemetry.ex
@lib/rindle/security/storage_key.ex
@lib/rindle/storage/capabilities.ex
@test/rindle/upload/broker_test.exs
@test/rindle/api_surface_boundary_test.exs

<interfaces>
Every existing `Rindle.Upload.Broker` public function/type remains on that module. `SessionSeed.build/2` returns the existing seed fields only. `Persistence` owns actual asset/session transactions and strategy compensation, but emits no event and shapes no public map. `SessionValidation` is query/storage/event free and returns the exact current tagged errors/normalized values. Broker retains public `with` clause order, capability calls, adapter calls, profile/option defaults, telemetry/broadcast timing, and result/error translation.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Extract session seeds and strategy persistence with compensation adjacent</name>
  <reversibility rating="reversible">The split retains the same session schema, adapter callbacks, and facade entrypoints, so undo is an internal recomposition.</reversibility>
  <read_first>lib/rindle/upload/broker.ex (all four initiation functions; create/persist/compensation/update helpers); lib/rindle/security/storage_key.ex; lib/rindle/storage/capabilities.ex; test/rindle/upload/broker_test.exs (initiate_session, multipart initiation/compensation, resumable initiation/compensation, tus cases and telemetry); 124-PATTERNS.md (`SessionSeed`, `Persistence`).</read_first>
  <files>lib/rindle/upload/broker.ex, lib/rindle/upload/broker/session_seed.ex, lib/rindle/upload/broker/persistence.ex, test/rindle/upload/broker_test.exs, test/rindle/api_surface_boundary_test.exs</files>
  <behavior>
    - Direct, multipart, native resumable, and tus initiation return the same tagged session/bootstrap maps and emit the same start/resumable/broadcast events only after commit.
    - Capability refusal occurs before remote initiation/persistence; remote initiation failure creates no rows.
    - Persistence failure invokes the same strategy compensation with exact adapter args/log/error behavior and returns the original persistence error.
    - Every durable strategy/state/protocol/offset/URI/expiry/id/parts/key/filename value remains term-equivalent.
  </behavior>
  <action>Begin with facade-level row/result/event and compensation-order assertions for all four initiation lanes, using Mox/repository behavior rather than implementation text. Create hidden `Broker.SessionSeed.build/2` for actual profile-name, extension, UUID, storage-key, filename, and expiry construction. Create hidden `Broker.Persistence` and move actual asset+session transaction, strategy-specific session attrs, session update transaction, and multipart/native-resumable/tus compensation mechanics into it. Route each public initiation flow through these modules while keeping option defaults, adapter resolution, capability gates, remote initiation calls, event/broadcast timing, and public result maps in Broker. Compensation must remain immediately coupled to its failed persistence result and preserve the original error. Remove moved duplicates; do not turn Persistence into a callback dispatcher, add a generic repository abstraction, or move telemetry/broadcasts.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/rindle/upload/broker_test.exs test/rindle/api_surface_boundary_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - All initiation/capability/persistence/compensation/event/result cases are green with unchanged terms and order.
    - SessionSeed and Persistence own real mechanics, receive explicit inputs, and do not resolve hidden configuration or call facade callbacks.
    - Both modules and all callable seams are compiled-hidden; Broker/Rindle public exports are unchanged.
  </acceptance_criteria>
  <done>Broker initiation is readable as facade orchestration over cohesive seed and persistence owners with unchanged storage and event behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract loaded-session guards and normalization without moving operation order</name>
  <reversibility rating="reversible">Validation helpers return the existing terms and create no new public or durable contract.</reversibility>
  <read_first>lib/rindle/upload/broker.ex (`sign_url/2`, `sign_multipart_part/3`, `complete_multipart_upload/3`, status/cancel functions, ensure/normalize/encode/status/profile helpers); lib/rindle/upload/broker/persistence.ex from Task 1; test/rindle/upload/broker_test.exs (multipart/resumable lifecycle and errors); 124-RESEARCH.md (`SessionValidation`, Errors/results).</read_first>
  <files>lib/rindle/upload/broker.ex, lib/rindle/upload/broker/session_validation.ex, lib/rindle/upload/broker/persistence.ex, test/rindle/upload/broker_test.exs, test/rindle/api_surface_boundary_test.exs</files>
  <behavior>
    - Multipart/resumable strategy guards preserve exact unsupported and not-initialized error terms.
    - Multipart parts accept the same atom/string maps, reject the same invalid values, sort identically, and persist the same string-key manifest before adapter completion.
    - Status attrs preserve offset/expiry/region precedence; profile resolution retains `:unknown_profile`; signing/status/cancel public order and events stay unchanged.
  </behavior>
  <action>Create hidden `Broker.SessionValidation` and move the actual multipart/resumable strategy guards, profile-name resolution, multipart part normalization/sorting/encoding, and resumable status-attribute precedence into it. Route the existing public signing, multipart preparation, status polling, and cancellation `with` flows through tagged values from this module, using `Broker.Persistence.update/3` for the already-existing transaction boundary. Keep row load/preload, FSM transition order, capability calls, adapter calls, signed-state decision/update, public error translation, result maps, telemetry, and broadcasts in Broker. Remove moved duplicates and do not allow SessionValidation to query the repo, call storage, mutate state, or emit events.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/rindle/upload/broker_test.exs --seed 0 && bash scripts/maintainer/refactor_contract.sh</automated>
  </verify>
  <acceptance_criteria>
    - All public broker lifecycle result/error/storage/event behavior remains exact.
    - SessionValidation is pure and cohesive; facade ordering remains directly visible.
    - SAFE-01 and compiled hidden/public boundaries pass without source-string assertions.
  </acceptance_criteria>
  <done>Broker validation, normalization, and persistence are independently readable while all public capability, adapter, lifecycle, and event behavior remains at the facade.</done>
</task>

</tasks>

<threat_model asvs_level="L1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Public Broker inputs/capabilities to storage initiation | Filenames/options/parts and adapter-declared capabilities decide remote effects. |
| Remote storage results to database session state | Upload ids/URIs/offsets/parts cross into durable rows; persistence failure requires compensation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-124-04 | Tampering / Denial of Service | capability, persistence, compensation | high | mitigate | Retain require-before-effect order, exact durable attrs, adjacent strategy compensation, and failure-order Mox/repo tests; block on high failures. |
| T-124-02 | Tampering | multipart/status normalization | high | mitigate | Preserve closed accepted shapes, positive part numbers, deterministic sorting/encoding, and status precedence before persistence. |
| T-124-01 | Information Disclosure | resumable session URI/logs/events | high | mitigate | Keep URI only in established result/redacting field and preserve telemetry metadata filtering plus compensation log fields. |
| T-124-SC | Tampering | package supply chain | low | accept | No install or dependency mutation occurs. |
</threat_model>

<source_audit>
## Source Coverage for This Plan

| Source | ID | Feature / requirement | Status | Ownership |
|---|---|---|---|---|
| GOAL | — | Clear Broker responsibility boundaries | COVERED | Seeds, persistence, validation separated behind facade. |
| REQ | UPLOAD-02 | Validation/capability/persistence cohesive; APIs/storage exact | COVERED | Tasks 1-2. |
| REQ | SAFE-01 | Preserve API/schema/telemetry/error/release | COVERED | Broker/API tests plus contract runner. |
| RESEARCH | initiation/persistence | Exact strategy values and adjacent compensation | COVERED | SessionSeed + Persistence. |
| RESEARCH | validation | Exact guards/normalization/status attrs without effects | COVERED | SessionValidation. |
| CONTEXT | discretion | Small hidden cohesive owners, no line-count wrappers | COVERED | Each module owns one mechanics domain. |

Deferred feature/API/storage/schema work and optional shared broadcasts are excluded by explicit scope.
</source_audit>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- New file/module: `lib/rindle/upload/broker/session_seed.ex` — `Rindle.Upload.Broker.SessionSeed`; hidden symbol `build/2`.
- New file/module: `lib/rindle/upload/broker/persistence.ex` — `Rindle.Upload.Broker.Persistence`; hidden symbols `create/3`, `persist_multipart/5`, `persist_resumable/5`, `persist_tus/3`, and `update/3`.
- New file/module: `lib/rindle/upload/broker/session_validation.ex` — `Rindle.Upload.Broker.SessionValidation`; hidden symbols `multipart/1`, `resumable/1`, `profile_module/1`, `normalize_parts/1`, `encode_parts/1`, and `resumable_status_attrs/2`.
- Every new module uses `@moduledoc false`; every callable seam uses `@doc false`.
- Modified facade/proof: `lib/rindle/upload/broker.ex`, `test/rindle/upload/broker_test.exs`, and `test/rindle/api_surface_boundary_test.exs`.
- No new public function/type/delegate, dependency, config, schema/migration, storage format/fallback, telemetry/broadcast/error vocabulary, CI/release topology, UI/Admin, or assumption artifact is produced.
</artifacts_this_phase_produces>

<verification>
1. Run task gates after each commit.
2. Run `MIX_ENV=test mix compile --force --warnings-as-errors`.
3. Run the full Broker focused suite and SAFE-01.
4. Confirm forbidden surfaces are unchanged with `git diff --quiet main...HEAD -- mix.exs mix.lock priv/repo/migrations .github/workflows lib/rindle/admin`.
</verification>

<success_criteria>
- UPLOAD-02 validation, capability, persistence, and compensation are clear and cohesively owned.
- Every Broker public signature/type/result/error/event/storage effect remains unchanged.
- No high security threat remains open before completion extraction.
</success_criteria>

<output>
Create `.planning/phases/124-upload-path-clarity/124-04-SUMMARY.md` when done.
</output>

---
phase: 45
plan: "02"
type: execute
wave: 1
depends_on:
  - "45-01"
files_modified:
  - lib/rindle/streaming.ex
  - lib/rindle/workers/ingest_provider_webhook.ex
  - lib/rindle/domain/provider_asset_fsm.ex
  - lib/rindle/domain/media_provider_asset.ex
  - test/rindle/streaming/create_direct_upload_test.exs
  - test/rindle/workers/ingest_provider_webhook_test.exs
  - test/rindle/delivery/streaming_dispatch_test.exs
autonomous: true
requirements:
  - MUX-21
  - MUX-22
must_haves:
  truths:
    - "`Rindle.Streaming.create_direct_upload/2` creates the local provider row before returning the browser handoff payload."
    - "The public return shape is only `%{upload_url, asset_id}`; raw Mux ids remain internal."
    - "`video.upload.asset_created` correlates by passthrough, stamps `provider_asset_id`, transitions to `processing`, and broadcasts `:provider_asset_created`."
  artifacts:
    - path: "lib/rindle/streaming.ex"
      provides: "public streaming-owned direct-upload entrypoint"
      contains: "def create_direct_upload("
    - path: "lib/rindle/workers/ingest_provider_webhook.ex"
      provides: "passthrough-based upload -> asset linker"
      contains: ":provider_asset_created"
    - path: "test/rindle/streaming/create_direct_upload_test.exs"
      provides: "public contract coverage for direct-upload creation"
      contains: "upload_url"
  key_links:
    - from: "lib/rindle/streaming.ex"
      to: "test/rindle/streaming/create_direct_upload_test.exs"
      via: "public entrypoint contract"
      pattern: "create_direct_upload"
    - from: "lib/rindle/workers/ingest_provider_webhook.ex"
      to: "test/rindle/workers/ingest_provider_webhook_test.exs"
      via: "passthrough linker and broadcast contract"
      pattern: "provider_asset_created"
---

<objective>
Ship the core direct-upload lifecycle: public server entrypoint plus webhook
linker.

Purpose: let adopters request a one-time browser upload URL while keeping
Rindle's durable truth in `media_provider_assets`.
Output: a streaming-owned public entrypoint and a webhook worker that turns the
Mux upload-created event into local provider-asset linkage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-browser-mux-direct-creator-upload-sibling-droppable/45-CONTEXT.md
@.planning/phases/45-browser-mux-direct-creator-upload-sibling-droppable/45-RESEARCH.md
@.planning/phases/45-browser-mux-direct-creator-upload-sibling-droppable/45-PATTERNS.md
@lib/rindle/delivery.ex
@lib/rindle/domain/media_provider_asset.ex
@lib/rindle/domain/provider_asset_fsm.ex
@lib/rindle/workers/ingest_provider_webhook.ex
@test/rindle/workers/ingest_provider_webhook_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the streaming-owned public direct-upload entrypoint</name>
  <read_first>
    - lib/rindle.ex
    - lib/rindle/delivery.ex
    - lib/rindle/streaming/capabilities.ex
    - lib/rindle/domain/media_provider_asset.ex
    - lib/rindle/streaming/provider/mux.ex
    - .planning/phases/45-browser-mux-direct-creator-upload-sibling-droppable/45-CONTEXT.md
  </read_first>
  <files>lib/rindle/streaming.ex, test/rindle/streaming/create_direct_upload_test.exs</files>
  <action>Create a public `Rindle.Streaming.create_direct_upload/2` facade that resolves the profile's streaming config, requires `ingest_mode: :direct_creator_upload`, checks `Capabilities.supports?(provider, :direct_creator_upload)`, creates a `media_provider_assets` row in `"pending"` with `provider_name: "mux"`, `playback_policy`, `ingest_mode: "direct_creator_upload"`, and a generated passthrough token, then calls the provider callback. Persist only durable local state and return exactly `%{upload_url: upload_url, asset_id: asset_id}` to the caller. Never expose raw `upload_id` or raw `provider_asset_id`, and never persist `upload_url`.</action>
  <acceptance_criteria>
    - `rg -n "def create_direct_upload\\(" lib/rindle/streaming.ex` returns one match.
    - `rg -n "direct_creator_upload|upload_url|asset_id|mux_passthrough" test/rindle/streaming/create_direct_upload_test.exs` returns the new entrypoint assertions.
    - `rg -n "provider_asset_id|upload_id" test/rindle/streaming/create_direct_upload_test.exs` shows these values are asserted to stay internal or absent from the public return shape.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rindle/streaming/create_direct_upload_test.exs</automated>
  </verify>
  <done>The public API can mint a browser-safe Mux direct upload while keeping Rindle's durable truth in a pending provider row and hiding provider secrets from adopters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upgrade the webhook worker into the passthrough linker</name>
  <read_first>
    - lib/rindle/workers/ingest_provider_webhook.ex
    - lib/rindle/domain/provider_asset_fsm.ex
    - lib/rindle/domain/media_provider_asset.ex
    - test/rindle/workers/ingest_provider_webhook_test.exs
    - test/rindle/streaming/provider/mux/event_test.exs
    - .planning/research/v1.8/MUX-DIRECT-UPLOAD-RESEARCH.md
  </read_first>
  <files>lib/rindle/workers/ingest_provider_webhook.ex, lib/rindle/domain/provider_asset_fsm.ex, test/rindle/workers/ingest_provider_webhook_test.exs, test/rindle/delivery/streaming_dispatch_test.exs</files>
  <action>Replace the deferred `video.upload.asset_created` no-op branch in `lib/rindle/workers/ingest_provider_webhook.ex` with a passthrough-based linker. Read the passthrough token from the normalized raw webhook payload, look up the existing row by `provider_name` plus `mux_passthrough`, stamp `provider_asset_id`, update `last_event_at`, transition the row into `"processing"` through `ProviderAssetFSM.transition/3`, and broadcast `:provider_asset_created` on the existing two-topic PubSub contract. Keep the behavior idempotent for duplicate deliveries, keep `provider_asset_id` forbidden from broadcast payloads, and preserve the existing `video.asset.ready` follow-up path so `:provider_asset_ready` still fires when the ready webhook arrives later.</action>
  <acceptance_criteria>
    - `rg -n "video\\.upload\\.asset_created|provider_asset_created|mux_passthrough" lib/rindle/workers/ingest_provider_webhook.ex` returns linker matches.
    - `rg -n "provider_asset_created|mux_passthrough|processing" test/rindle/workers/ingest_provider_webhook_test.exs` returns new linker assertions.
    - `rg -n "provider_asset_ready|direct_creator_upload" test/rindle/delivery/streaming_dispatch_test.exs test/rindle/workers/ingest_provider_webhook_test.exs` returns ready-path follow-up coverage.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rindle/workers/ingest_provider_webhook_test.exs test/rindle/delivery/streaming_dispatch_test.exs</automated>
  </verify>
  <done>The webhook worker now converts the Mux upload-created event into durable local linkage and visible PubSub progression without inventing a second state machine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adopter server -> browser | The public entrypoint returns a one-time bearer upload URL and a durable public asset id. |
| Mux webhook -> Rindle worker | Untrusted provider event data must map to exactly one existing local row by passthrough. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-04 | S | `lib/rindle/workers/ingest_provider_webhook.ex` | mitigate | Link only by the Rindle-owned passthrough token created at upload mint time, not by user-controlled public ids. |
| T-45-05 | T | `lib/rindle/workers/ingest_provider_webhook.ex` | mitigate | Preserve FSM-gated transitions and idempotent duplicate handling so repeated upload-created events cannot corrupt row state. |
| T-45-06 | I | `lib/rindle/streaming.ex` | mitigate | Return only `%{upload_url, asset_id}` and keep raw Mux ids internal. |
</threat_model>

<verification>
- Run `mix test test/rindle/streaming/create_direct_upload_test.exs`.
- Run `mix test test/rindle/workers/ingest_provider_webhook_test.exs test/rindle/delivery/streaming_dispatch_test.exs`.
- Confirm the grep checks for `provider_asset_created` and `mux_passthrough`.
</verification>

<success_criteria>
- Adopters can request a direct upload through a streaming-owned public seam with no provider-id leakage.
- The upload-created webhook deterministically links the local row and advances it into the existing provider-ready path.
- Both the linker event and the ready event are visible through the established PubSub contract.
</success_criteria>

<output>
After completion, create `.planning/phases/45-browser-mux-direct-creator-upload-sibling-droppable/45-02-SUMMARY.md`
</output>

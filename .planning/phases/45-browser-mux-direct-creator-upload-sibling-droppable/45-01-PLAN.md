---
phase: 45
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/*_add_mux_passthrough_to_media_provider_assets.exs
  - lib/rindle/domain/media_provider_asset.ex
  - lib/rindle/streaming/provider/mux.ex
  - lib/rindle/streaming/provider/mux/http.ex
  - lib/rindle/streaming/provider/mux/client.ex
  - test/rindle/streaming/provider/mux/mux_test.exs
  - test/rindle/streaming/capabilities_test.exs
autonomous: true
requirements:
  - MUX-20
must_haves:
  truths:
    - "Mux advertises `:direct_creator_upload` only after the adapter actually implements `create_direct_upload/2`."
    - "The direct-upload callback returns `%{upload_url, upload_id, provider_asset_id: nil}` and requires `cors_origin` plus passthrough-backed asset settings."
    - "The local persistence layer gains exactly one additive nullable passthrough-correlation column with redaction parity."
  artifacts:
    - path: "lib/rindle/streaming/provider/mux.ex"
      provides: "Mux direct-upload callback and capability advertisement"
      contains: "def create_direct_upload("
    - path: "lib/rindle/streaming/provider/mux/http.ex"
      provides: "Mux upload-create HTTP boundary"
      contains: "def create_upload("
    - path: "lib/rindle/domain/media_provider_asset.ex"
      provides: "passthrough correlation field with redaction-aware schema support"
      contains: "mux_passthrough"
  key_links:
    - from: "lib/rindle/streaming/provider/mux.ex"
      to: "test/rindle/streaming/provider/mux/mux_test.exs"
      via: "direct-upload request and return-shape contract"
      pattern: "create_direct_upload"
    - from: "lib/rindle/domain/media_provider_asset.ex"
      to: "priv/repo/migrations/*_add_mux_passthrough_to_media_provider_assets.exs"
      via: "schema column and migration parity"
      pattern: "mux_passthrough"
---

<objective>
Ship the adapter and schema contract for browser -> Mux direct creator upload.

Purpose: land the smallest additive foundation needed for the later linker and
public entrypoint work.
Output: Mux can mint a direct-upload URL through the reserved callback, the
schema can persist passthrough correlation, and the test suite locks the
request/response contract.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-browser-mux-direct-creator-upload-sibling-droppable/45-CONTEXT.md
@.planning/phases/45-browser-mux-direct-creator-upload-sibling-droppable/45-RESEARCH.md
@.planning/phases/45-browser-mux-direct-creator-upload-sibling-droppable/45-PATTERNS.md
@lib/rindle/domain/media_provider_asset.ex
@lib/rindle/streaming/provider.ex
@lib/rindle/streaming/provider/mux.ex
@lib/rindle/streaming/provider/mux/http.ex
@lib/rindle/streaming/provider/mux/client.ex
@test/rindle/streaming/provider/mux/mux_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the passthrough correlation column and schema support</name>
  <read_first>
    - priv/repo/migrations/20260506120000_create_media_provider_assets.exs
    - lib/rindle/domain/media_provider_asset.ex
    - .planning/phases/45-browser-mux-direct-creator-upload-sibling-droppable/45-CONTEXT.md
    - .planning/phases/45-browser-mux-direct-creator-upload-sibling-droppable/45-RESEARCH.md
  </read_first>
  <files>priv/repo/migrations/*_add_mux_passthrough_to_media_provider_assets.exs, lib/rindle/domain/media_provider_asset.ex</files>
  <action>Add one additive nullable string column named `mux_passthrough` to `media_provider_assets`, plus an index that supports lookup by provider and passthrough for the direct-upload linker path. Update `lib/rindle/domain/media_provider_asset.ex` so the schema casts `:mux_passthrough`, keeps it in the writable field list, and preserves redaction parity by ensuring the custom `Inspect` impl does not surface the raw passthrough token. Do not rename or reshape existing provider id fields, and do not add a second provider-row table.</action>
  <acceptance_criteria>
    - `rg -n "add :mux_passthrough, :string" priv/repo/migrations` returns one match.
    - `rg -n "mux_passthrough" lib/rindle/domain/media_provider_asset.ex` returns schema and changeset matches.
    - `rg -n "mux_passthrough" priv/repo/migrations` returns an index definition for lookup support.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rindle/streaming/provider/mux/mux_test.exs</automated>
  </verify>
  <done>The persistence layer can store a redaction-safe passthrough correlation token for direct-upload linking without altering existing provider-row semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the Mux direct-upload callback and client boundary</name>
  <read_first>
    - lib/rindle/streaming/provider.ex
    - lib/rindle/streaming/provider/mux.ex
    - lib/rindle/streaming/provider/mux/http.ex
    - lib/rindle/streaming/provider/mux/client.ex
    - test/rindle/streaming/provider/mux/mux_test.exs
    - .planning/research/v1.8/MUX-DIRECT-UPLOAD-RESEARCH.md
  </read_first>
  <files>lib/rindle/streaming/provider/mux.ex, lib/rindle/streaming/provider/mux/http.ex, lib/rindle/streaming/provider/mux/client.ex, test/rindle/streaming/provider/mux/mux_test.exs, test/rindle/streaming/capabilities_test.exs</files>
  <action>Extend `Rindle.Streaming.Provider.Mux.Client` with `create_upload/1`, implement it in `lib/rindle/streaming/provider/mux/http.ex` via `Mux.Video.Uploads.create/2`, and add `create_direct_upload/2` to `lib/rindle/streaming/provider/mux.ex`. The adapter must require `cors_origin`, build `new_asset_settings.playback_policies` from the requested playback policy, stamp `new_asset_settings.passthrough`, and return exactly `%{upload_url: upload_url, upload_id: upload_id, provider_asset_id: nil}` on success. Normalize `429` to `:provider_quota_exceeded`, other `4xx/5xx` to `:provider_sync_failed`, and advertise `:direct_creator_upload` in `capabilities/0`. Update tests to assert the exact request body shape, return contract, error mapping, and capability set.</action>
  <acceptance_criteria>
    - `rg -n "def create_direct_upload\\(" lib/rindle/streaming/provider/mux.ex` returns one match.
    - `rg -n "def create_upload\\(" lib/rindle/streaming/provider/mux/http.ex lib/rindle/streaming/provider/mux/client.ex` returns two matches.
    - `rg -n "direct_creator_upload|provider_asset_id: nil|cors_origin|playback_policies|passthrough" test/rindle/streaming/provider/mux/mux_test.exs` returns the new contract assertions.
    - `rg -n ":direct_creator_upload" test/rindle/streaming/capabilities_test.exs test/rindle/streaming/provider/mux/mux_test.exs` shows capability coverage.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rindle/streaming/provider/mux/mux_test.exs test/rindle/streaming/capabilities_test.exs</automated>
  </verify>
  <done>The adapter can mint Mux direct-upload URLs through the reserved callback, preserves the thin SDK boundary, and advertises the new capability honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rindle -> Mux direct-upload API | Rindle sends provider-facing asset settings and receives a one-time bearer upload URL. |
| provider-row persistence -> telemetry/logging | Passthrough correlation becomes durable state and must remain redacted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | I | `lib/rindle/streaming/provider/mux.ex` | mitigate | Keep `upload_url` transient and never persist it; return it only in the callback result. |
| T-45-02 | I | `lib/rindle/domain/media_provider_asset.ex` | mitigate | Add passthrough storage with inspect redaction parity so the token never leaks through logs or debugging. |
| T-45-03 | T | `lib/rindle/streaming/provider/mux/http.ex` | mitigate | Keep the SDK boundary thin and explicit so request keys and error mapping remain locked by tests. |
</threat_model>

<verification>
- Run `mix test test/rindle/streaming/provider/mux/mux_test.exs test/rindle/streaming/capabilities_test.exs`.
- Confirm the migration and schema grep checks for `mux_passthrough` pass.
</verification>

<success_criteria>
- The Mux adapter exposes a tested `create_direct_upload/2` callback with the locked return shape.
- The provider-row schema can persist a passthrough correlation token without leaking it through inspect or public APIs.
- Capability advertisement and HTTP error normalization remain explicit and regression-locked.
</success_criteria>

<output>
After completion, create `.planning/phases/45-browser-mux-direct-creator-upload-sibling-droppable/45-01-SUMMARY.md`
</output>

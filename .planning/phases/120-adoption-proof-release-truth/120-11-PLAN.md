---
phase: 120-adoption-proof-release-truth
plan: 11
type: execute
wave: 7
depends_on: [120-10]
files_modified: []
autonomous: false
gap_closure: true
requirements: [PROOF-01, PROOF-02, DOCS-01]
nyquist_compliant: true
must_haves:
  truths:
    - "D-120-01/D-120-02: the exact candidate SHA passes separate packed explicit-public and populated-upgrade image profiles from built package contents."
    - "D-120-03/D-120-04: the same candidate passes Cohort build, migration, seed, boot, HTTP, persistence, and Rindle=rindle/Oban+ledger=public assertions."
    - "Local MinIO requirements and Cohort's published ports are isolated so a passing packed prerequisite cannot block the Cohort-owned stack."
    - "D-120-05: immutable GitHub Actions evidence on the identical release SHA—not local checkout output—authorizes the release claim."
  artifacts:
    - path: ".planning/phases/120-adoption-proof-release-truth/120-11-SUMMARY.md"
      provides: "Exact command receipts, candidate SHA, and immutable external evidence references"
  key_links:
    - from: "candidate SHA"
      to: "packed public/upgrade and Cohort commands"
      via: "one clean detached worktree and dedicated TMPDIR"
      pattern: "phase_120_public_compat|phase_120_isolation_upgrade"
    - from: "candidate SHA"
      to: "CI and Release workflows"
      via: "matching headSha, immutable run URLs, and successful named conclusions"
      pattern: "Package Consumer Proof Matrix|Cohort Demo Smoke"
---

<objective>
Complete the remaining environment-backed proof from the exact post-120-10 candidate and record honest immutable release evidence (D-120-01 through D-120-06).

Purpose: Close PROOF-01/PROOF-02 without repeating implementation work or treating checkout-only tests as release authorization.
Output: Clean packed public/upgrade and Cohort receipts plus a human-confirmed exact-SHA GitHub evidence chain.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-adoption-proof-release-truth/120-CONTEXT.md
@.planning/phases/120-adoption-proof-release-truth/120-VERIFICATION.md
@.planning/phases/120-adoption-proof-release-truth/120-09-SUMMARY.md
@.planning/phases/120-adoption-proof-release-truth/120-10-SUMMARY.md
@RUNNING.md
@guides/release_publish.md
@scripts/ensure_minio.sh
@scripts/ci/cohort_demo_smoke.sh
@docker/compose.cohort-demo.yml
@test/install_smoke/generated_app_smoke_test.exs
@.github/workflows/ci.yml
@.github/workflows/release.yml

<interfaces>
- Packed public and populated-upgrade profiles are separate ExUnit modules/BEAM invocations selected by `phase_120_public_compat` and `phase_120_isolation_upgrade`.
- `scripts/ensure_minio.sh` owns the local packed-test prerequisite at `RINDLE_MINIO_URL`; Cohort owns its Compose MinIO and accepts loopback publish overrides through `COHORT_MINIO_PORT` and `COHORT_MINIO_CONSOLE_PORT`.
- `scripts/ci/cohort_demo_smoke.sh` keeps server-side traffic on `minio:9000`; changing host-published diagnostic ports does not change the Compose service boundary.
- `guides/release_publish.md` and `RUNNING.md` define exact-SHA CI/Release authority and live job/step names.
</interfaces>
</context>

<gap_context>
Executed Plans 120-07 and 120-08 closed the catalog/content and public rollback defects. Plan 120-09 proved both packed profiles pass on candidate `bd7cdd853f623fa86c9b76040572fe5a6c7fbaac`, then failed only because its persistent packed-test MinIO occupied Cohort's default host port 9000. Plan 120-10 changes the candidate and removes the residual forced report field, so this plan must use the new exact SHA and must not reuse the superseded SHA or its partial receipts.
</gap_context>

<tasks>

<task type="tracer">
  <name>Task 1: Produce clean packed and Cohort receipts from one candidate SHA</name>
  <reversibility rating="reversible">This task records verification evidence and creates no product, database, routing, or workflow contract.</reversibility>
  <precondition>Docker and PostgreSQL are reachable; loopback ports 14102, 19000, and 19001 are unused; the candidate commit resolves; and a clean detached worktree plus dedicated TMPDIR can be created.</precondition>
  <read_first>
    .planning/phases/120-adoption-proof-release-truth/120-09-SUMMARY.md
    .planning/phases/120-adoption-proof-release-truth/120-10-SUMMARY.md
    scripts/ensure_minio.sh
    scripts/ci/cohort_demo_smoke.sh
    docker/compose.cohort-demo.yml
    test/install_smoke/generated_app_smoke_test.exs
    RUNNING.md
  </read_first>
  <files>None; record receipts only in 120-11-SUMMARY.md</files>
  <action>Resolve the post-120-10 40-character candidate SHA, materialize a clean detached worktree, create a mode-700 dedicated TMPDIR, fetch declared test dependencies, and record all identities before testing. Run docs/release parity, then `scripts/ensure_minio.sh`, then the public-compatibility and populated-upgrade image profiles as separate commands/BEAM instances. After both packed commands complete, run Cohort with `COHORT_DEMO_PORT=14102`, `COHORT_MINIO_PORT=19000`, and `COHORT_MINIO_CONSOLE_PORT=19001`; first prove those loopback ports have no listeners. These host-publish overrides avoid the known port-9000 collision while preserving Cohort's in-network `minio:9000` topology. Record each exit code and the final marker `[1]`, named FK/index, exact Oban snapshot, Cohort relation, host-ledger, boot, and HTTP summary lines. Fail closed on any missing dependency, timeout, port listener, detached/stalled process, or cleanup failure. Do not edit scripts, Compose files, source, workflows, or existing summaries, and do not represent partial stages as a passing chain.</action>
  <verify>
    <automated>mix test test/install_smoke/docs_parity_test.exs test/install_smoke/release_docs_parity_test.exs --seed 0 &amp;&amp; bash scripts/ensure_minio.sh &amp;&amp; RINDLE_INSTALL_SMOKE_PROFILE=image mix test test/install_smoke/generated_app_smoke_test.exs --include minio --only phase_120_public_compat --seed 0 &amp;&amp; RINDLE_INSTALL_SMOKE_PROFILE=image mix test test/install_smoke/generated_app_smoke_test.exs --include minio --only phase_120_isolation_upgrade --seed 0 &amp;&amp; COHORT_DEMO_PORT=14102 COHORT_MINIO_PORT=19000 COHORT_MINIO_CONSOLE_PORT=19001 bash scripts/ci/cohort_demo_smoke.sh</automated>
  </verify>
  <acceptance_criteria>
    - Every command runs from one clean detached worktree at the recorded post-120-10 SHA and exits 0.
    - Public compatibility and populated upgrade run in separate generated roots and package-backed invocations.
    - The upgrade receipt contains marker `[1]`, exact named FK/index evidence, and equal complete before/after Oban snapshots.
    - Cohort owns its Compose services, boots on the explicit loopback ports, serves all existing HTTP/seed checks, and proves seven Rindle relations in rindle only plus public Oban and both host migration ledger entries.
    - The receipt contains no substituted SHA, inherited branch-head result, interrupted command, or environment-blocked pass.
  </acceptance_criteria>
  <done>The exact candidate has complete local packed and Cohort diagnostic evidence with the prior MinIO port collision eliminated by supported host-publish overrides.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm green-main and Release evidence on the identical SHA</name>
  <read_first>
    guides/release_publish.md
    RUNNING.md
    .github/workflows/ci.yml
    .github/workflows/release.yml
    .planning/phases/120-adoption-proof-release-truth/120-11-SUMMARY.md
  </read_first>
  <files>None; append immutable external evidence references to 120-11-SUMMARY.md</files>
  <action>After Task 1 passes, use configured GitHub CLI/API access to inspect runs for the recorded candidate SHA and capture immutable URLs, run IDs, `headSha`, conclusions, and the live job/step names required by the release runbook. Pause for human confirmation when the candidate has not been pushed, a main-only lane has not run, protected Release authorization is absent, or external results are still pending. Do not push, merge, publish, dispatch a protected release, reuse evidence from `bd7cdd853f623fa86c9b76040572fe5a6c7fbaac`, accept another SHA, or infer green state from local receipts.</action>
  <verify>
    <automated>SHA=$(git rev-parse HEAD) &amp;&amp; test ${#SHA} -eq 40 &amp;&amp; gh run list --commit "$SHA" --workflow ci.yml --json databaseId,headSha,conclusion,url --limit 20</automated>
    <human-check>Confirm the recorded SHA has a successful `ci.yml` run with `Proof`, lean `Package Consumer Proof Matrix + Release Preflight`, and `Adoption Demo Unit`; for the push-to-main candidate, also confirm `Package Consumer Full Matrix + Release Preflight` and `Cohort Demo Smoke`. When release authorization is being granted, confirm the `Release` workflow on the same immutable SHA successfully reaches `Run release preflight`, `Verify version alignment`, `Dry run Hex publish`, and `Verify public Hex.pm artifact`. Record direct run URLs and conclusions for the identical SHA.</human-check>
  </verify>
  <acceptance_criteria>
    - Every external record names the exact post-120-10 40-character SHA and has a successful conclusion.
    - Required merge-blocking, push-main, and protected Release jobs/steps are distinguished honestly; a not-yet-run gate remains pending rather than passing.
    - Local receipts remain diagnostic evidence and are not described as release authorization.
  </acceptance_criteria>
  <what-built>Snapshot-only Oban proof plus clean packed compatibility, populated-upgrade, and Cohort receipts on one candidate SHA.</what-built>
  <how-to-verify>Review each recorded GitHub Actions URL, confirm its `headSha` equals the Task 1 candidate, and confirm every applicable job/step named above is successful.</how-to-verify>
  <resume-signal>Type "approved" with the exact SHA and immutable run URLs, or describe the missing, pending, or failed external evidence.</resume-signal>
  <done>The exact-SHA green-main and Release evidence honestly authorizes the 0.4.0 schema-isolation claim, or the plan remains blocked with the missing external gate named.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/120-adoption-proof-release-truth/120-11-SUMMARY.md` containing the exact candidate SHA, clean-worktree/TMPDIR identity, per-command exits, packed catalog facts, Cohort schema/HTTP facts, and immutable GitHub run references.
- No product source, workflow, Compose, migration, routing, diagnostic, Oban, or host-ledger symbol/path is created or modified by this verification plan.

## Multi-Source Coverage Audit

| Source | ID | Feature/constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Packaged install through populated upgrade, Cohort, docs, and release truth | 120-11 Tasks 1-2 | COVERED |
| REQ | PROOF-01 | Packed public/upgrade/runtime/Oban evidence | 120-11 Task 1 | COVERED |
| REQ | PROOF-02 | Packed generated consumers and Cohort end to end | 120-11 Tasks 1-2 | COVERED |
| REQ | DOCS-01 | Docs/release parity and exact-SHA Proof evidence | 120-11 Tasks 1-2 | COVERED |
| RESEARCH | — | Existing focused package/demo/release lanes | 120-11 Tasks 1-2 | COVERED |
| RESEARCH | — | Packed artifact, not checkout, is release authority | 120-11 Tasks 1-2 | COVERED |
| CONTEXT | D-120-01 | Packed artifact and real persistence authority | 120-11 Tasks 1-2 | COVERED |
| CONTEXT | D-120-02 | Explicit public is a separate supported pairing | 120-11 Task 1 | COVERED |
| CONTEXT | D-120-03 | Reuse current harnesses and lanes | 120-11 Tasks 1-2 | COVERED |
| CONTEXT | D-120-04 | Cross-surface ownership truth | 120-11 Task 1 | COVERED |
| CONTEXT | D-120-05 | Honest maintenance/release evidence | 120-11 Task 2 | COVERED |
| CONTEXT | D-120-06 | No redesign or host-relation mutation | Verification-only plan | COVERED |

The SPEC-less PROOF-02 unclassified probe is resolved as environment-backed integration evidence plus separate immutable release authorization. DOCS-01 concurrency is fail-closed: one clean candidate is tested sequentially, and external runs must match its exact SHA. No external application API integration, JS ORM schema push, assumption delta, or deferred feature is introduced.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Checkout to packed generated consumers | Repository tests cannot substitute for behavior of the built package artifact. |
| Local services to Cohort Compose | Host-published ports must not let one valid prerequisite block another proof path. |
| Local executor to GitHub Actions | Release authorization depends on immutable external identity and conclusions. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-120-G11-01 | Repudiation | local integration receipts | high | mitigate | Record exact SHA, clean worktree, TMPDIR, commands, exits, and final schema facts. |
| T-120-G11-02 | Tampering | exact-SHA signoff | high | mitigate | Match every external `headSha` to the recorded candidate and retain immutable URLs. |
| T-120-G11-03 | Denial of Service | MinIO/Cohort host ports | medium | mitigate | Preflight distinct supported Cohort publish ports while keeping Compose-internal service routing unchanged. |
| T-120-G11-04 | Elevation of Privilege | release operation | high | mitigate | Require human confirmation for protected external state; do not push, merge, dispatch, or publish autonomously. |
| T-120-SC | Tampering | package installs | high | mitigate | Fetch only the committed dependency graph in the clean candidate; introduce no new package. |
</threat_model>

<verification>
Task 1 must produce four complete local passing stages on one clean candidate. Task 2 must record immutable successful GitHub evidence for the identical SHA; absent external state remains an explicit blocking checkpoint.
</verification>

<success_criteria>
- Packed explicit-public, packed populated-upgrade, and Cohort proofs all pass on the post-120-10 candidate.
- Catalog/content, public rollback, and host-ownership invariants are observed from built/running consumers.
- Exact-SHA CI and applicable Release evidence is human-confirmed without bypassing green-main gates.
</success_criteria>

<output>Create `.planning/phases/120-adoption-proof-release-truth/120-11-SUMMARY.md` when done.</output>

---
phase: 120-adoption-proof-release-truth
plan: 07
type: execute
wave: 3
depends_on: [120-02]
files_modified:
  - test/install_smoke/support/generated_app_helper.ex
  - test/install_smoke/generated_app_smoke_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-01]
nyquist_compliant: true
must_haves:
  truths:
    - "D-120-01/D-120-03: the packed populated-upgrade proof reports exact PostgreSQL facts rather than permissive booleans."
    - "The moved marker table contains exactly version [1], the expected media_variants_asset_id_fkey exists as a foreign key, and all three expected media_variants indexes exist by name."
    - "D-120-04/D-120-06: public.oban_jobs has exactly the same identity, schema, ordered columns, constraints, and indexes immediately before and after the Rindle migration."
    - "A regression that removes marker 1, the expected foreign key, any expected index, or changes the Oban snapshot fails a focused test."
  artifacts:
    - path: "test/install_smoke/support/generated_app_helper.ex"
      provides: "Schema-qualified marker, named constraint/index, and before/after Oban catalog reports"
    - path: "test/install_smoke/generated_app_smoke_test.exs"
      provides: "Exact report assertions and loss-regression tests"
  key_links:
    - from: "generated migration runner"
      to: "PostgreSQL catalogs"
      via: "parameterized pg_class/pg_namespace/pg_attribute/pg_constraint/pg_index queries"
      pattern: "pg_constraint|pg_index|pg_attribute"
    - from: "GeneratedAppIsolationUpgradeTest"
      to: "generated upgrade report"
      via: "exact marker, FK, index, and Oban snapshot assertions"
      pattern: "media_variants_asset_id_fkey"
---

<objective>
Close the hollow populated-upgrade and Oban-boundary proof gaps with exact catalog/content evidence (D-120-01, D-120-03, D-120-04, D-120-06).

Purpose: Make PROOF-01 fail for integrity loss or any Rindle-caused mutation of the host-owned Oban table.
Output: A generated report backed by PostgreSQL catalogs and repository tests that reject each known false-positive case.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-adoption-proof-release-truth/120-CONTEXT.md
@.planning/phases/120-adoption-proof-release-truth/120-VERIFICATION.md
@.planning/phases/120-adoption-proof-release-truth/120-REVIEW.md
@.planning/phases/120-adoption-proof-release-truth/120-02-SUMMARY.md
@test/install_smoke/support/generated_app_helper.ex
@test/install_smoke/generated_app_smoke_test.exs
@lib/rindle/migration/v1.ex
@test/rindle/migration_test.exs

<interfaces>
- `Rindle.Migration.V1.owned_relations/0` remains the relation-set authority; this plan does not alter migration behavior.
- Ecto's generated V1 names to prove are `media_variants_asset_id_fkey`, `media_variants_asset_id_name_index`, `media_variants_state_index`, and `media_variants_output_kind_index`.
- Generated code observes JSON-safe facts; repository ExUnit asserts policy and supplies deliberately damaged report fixtures.
</interfaces>
</context>

<assumption_resolution>
No Phase 120 SPEC exists. The deterministic edge probe left four assumptions unresolved; this gap plan surfaces them instead of treating them as implicit:

1. **PROOF-01 boundary:** proof is bounded to the fixed Rindle V1 catalog plus `public.oban_jobs` and the host ledger, per D-120-06.
2. **PROOF-01 precision:** preservation means exact marker contents and named catalog objects, not table existence, a successful join, or any index.
3. **PROOF-02 unclassified:** clean packed and Cohort execution is classified as environment-backed integration evidence in Plan 120-09, with exact-SHA evidence kept separate from checkout evidence.
4. **DOCS-01 concurrency:** the generated public fixture is the shared executable source; Plan 120-08 updates fixture, parity, and public snippets sequentially after this plan so concurrent drift cannot be accepted.
</assumption_resolution>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove marker, foreign-key, and named-index preservation through the packed upgrade</name>
  <read_first>
    .planning/phases/120-adoption-proof-release-truth/120-VERIFICATION.md
    .planning/phases/120-adoption-proof-release-truth/120-REVIEW.md
    test/install_smoke/support/generated_app_helper.ex
    test/install_smoke/generated_app_smoke_test.exs
    lib/rindle/migration/v1.ex
    test/rindle/migration_test.exs
  </read_first>
  <files>test/install_smoke/support/generated_app_helper.ex, test/install_smoke/generated_app_smoke_test.exs</files>
  <behavior>
    - The selected schema's `rindle_migration_versions` query returns exactly `[1]` after the move.
    - `pg_constraint` proves `media_variants_asset_id_fkey` is a foreign key on `media_variants.asset_id` referencing `media_assets.id` in the selected schema.
    - `pg_indexes`/`pg_index` prove the exact expected names `media_variants_asset_id_name_index`, `media_variants_state_index`, and `media_variants_output_kind_index` remain in the selected schema.
    - Focused regression cases delete marker 1, remove the expected FK, and remove each expected index from an otherwise-valid report; every case is rejected.
  </behavior>
  <action>Write failing repository-side report-policy tests first, including one deliberately damaged fixture per loss class. Replace `seeded_marker?`, join-based `foreign_key_preserved?`, and any-index `index_preserved?` with JSON-safe observed values: ordered marker versions, an exact named FK record including source/target schema-table-column facts and foreign-key type, and an ordered map/list of the three named index definitions. Use parameterized catalog queries for schema/name values and quote only the already-validated fixed schema identifier where an identifier is unavoidable. Keep the distinct generated app roots, packed dependency, seeded IDs, directional host migration, doctor result, and real post-move persistence intact per D-120-01/D-120-03. Make `GeneratedAppIsolationUpgradeTest` assert exact equality/subset contracts, not truthiness, and retain readable report output on failure.</action>
  <verify>
    <automated>mix test test/install_smoke/generated_app_smoke_test.exs --only phase_120_upgrade_contract --seed 0 &amp;&amp; RINDLE_INSTALL_SMOKE_PROFILE=image mix test test/install_smoke/generated_app_smoke_test.exs --include minio --only phase_120_isolation_upgrade --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - The packed upgrade reports marker versions exactly equal to `[1]`.
    - The expected FK name, type, source column, and selected-schema target are asserted from PostgreSQL catalogs.
    - All three expected index names and definitions are asserted; a primary-key or unrelated index cannot satisfy the gate.
    - Each marker/FK/index loss mutation produces a focused failing assertion before the packed command is relied upon.
  </acceptance_criteria>
  <done>The populated packed upgrade cannot pass after losing marker content, its required asset FK, or any expected media-variant index.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Snapshot public.oban_jobs exactly across the Rindle migration boundary</name>
  <read_first>
    .planning/phases/120-adoption-proof-release-truth/120-CONTEXT.md
    .planning/phases/120-adoption-proof-release-truth/120-REVIEW.md
    test/install_smoke/support/generated_app_helper.ex
    test/install_smoke/generated_app_smoke_test.exs
    examples/adoption_demo/test/rindle_migration_contract_test.exs
  </read_first>
  <files>test/install_smoke/support/generated_app_helper.ex, test/install_smoke/generated_app_smoke_test.exs</files>
  <behavior>
    - Immediately after the host Oban migration, the runner snapshots `public.oban_jobs` relation OID, schema, relation name, ordered columns, constraints, and indexes.
    - Immediately after the Rindle migration, the runner captures the same normalized snapshot and reports both.
    - Repository assertions require exact before/after equality and reject a changed identity, column, constraint, or index.
    - Ownership proof does not depend on a boolean recording whether the host Oban migration ran.
  </behavior>
  <action>Write failing snapshot-policy regressions first. Add one deterministic `public.oban_jobs` catalog snapshot function in the generated runner: identity is the `pg_class` OID plus `pg_namespace.nspname` and `relname`; columns are ordered by `attnum` and include name, rendered type, nullability, default, identity, and generated markers; constraints are ordered by name and include type plus `pg_get_constraintdef`; indexes are ordered by schema/name and include primary/unique flags plus `pg_get_indexdef`. Capture the first snapshot after the host Oban migration and the second immediately after the Rindle migration, normalize both into JSON-safe maps/lists, and assert exact equality in repository ExUnit. Remove the logically forced ownership predicate from the asserted contract; table presence and host-migration execution may remain diagnostic fields but cannot decide whether Rindle changed Oban. Add damaged-report cases for OID/schema, one column, one constraint, and one index so each difference is proven detectable (D-120-04/D-120-06).</action>
  <verify>
    <automated>mix test test/install_smoke/generated_app_smoke_test.exs --only phase_120_upgrade_contract --seed 0 &amp;&amp; RINDLE_INSTALL_SMOKE_PROFILE=image mix test test/install_smoke/generated_app_smoke_test.exs --include minio --only phase_120_isolation_upgrade --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - The report contains non-empty before/after snapshots with identical relation identity/schema, columns, constraints, and indexes.
    - Exact equality—not existence, row count, or host-migration provenance—is the Oban preservation gate.
    - Four focused damaged-snapshot cases demonstrate loss/change detection.
    - No Rindle code creates, moves, prefixes, configures, or otherwise takes ownership of Oban or `schema_migrations`.
  </acceptance_criteria>
  <done>The packed proof fails for any catalog-visible change Rindle makes to `public.oban_jobs`.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| Source | ID | Feature/constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Prove populated packaged upgrade and untouched Oban boundary end to end | 120-07 | COVERED |
| REQ | PROOF-01 | Exact populated upgrade/runtime/Oban proof | 120-07 Tasks 1-2 | COVERED |
| RESEARCH | — | Schema-qualified catalog facts; generated report/repository policy split | 120-07 Tasks 1-2 | COVERED |
| CONTEXT | D-120-01 | Packed artifact authority and real persistence | 120-07 Task 1 | COVERED |
| CONTEXT | D-120-02 | Explicit public only; no third routing mode | Preserved by existing separate roots; no routing edits | COVERED |
| CONTEXT | D-120-03 | Reuse existing generated-app harness | 120-07 Tasks 1-2 | COVERED |
| CONTEXT | D-120-04 | Host-owned Oban/ledger boundary | 120-07 Task 2 | COVERED |
| CONTEXT | D-120-05 | Operationally honest upgrade | Existing 120-05; exact proof reinforces it | COVERED |
| CONTEXT | D-120-06 | No migration/routing redesign or host-table ownership | 120-07 Tasks 1-2 | COVERED |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Generated packed consumer to PostgreSQL | Release evidence crosses from generated code into catalog/content observations. |
| Rindle migration to host-owned Oban | Any catalog change to public.oban_jobs violates the ownership boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-120-G07-01 | Tampering | upgrade evidence | high | mitigate | Assert exact marker contents and named FK/index catalog records; regression-test every loss class. |
| T-120-G07-02 | Elevation of Privilege | public.oban_jobs | high | mitigate | Compare complete normalized before/after catalog snapshots independently of migration provenance. |
| T-120-G07-03 | Information Disclosure | generated report | low | accept | Report contains catalog metadata and generated fixture IDs only, with no secrets or row payloads. |
| T-120-SC | Tampering | package installs | high | mitigate | No new dependency is planned; existing packed-artifact resolution remains asserted. |
</threat_model>

<verification>
1. `mix test test/install_smoke/generated_app_smoke_test.exs --only phase_120_upgrade_contract --seed 0`
2. `RINDLE_INSTALL_SMOKE_PROFILE=image mix test test/install_smoke/generated_app_smoke_test.exs --include minio --only phase_120_isolation_upgrade --seed 0`
</verification>

<success_criteria>
- Marker `[1]`, the named FK, and all named indexes are established from the moved database, with regressions that fail for each loss.
- The complete normalized `public.oban_jobs` catalog snapshot is exactly unchanged across the Rindle migration.
- PROOF-01 no longer depends on weak existence/join/any-index or forced ownership booleans.
</success_criteria>

<output>Create `.planning/phases/120-adoption-proof-release-truth/120-07-SUMMARY.md` when done.</output>

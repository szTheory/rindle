---
status: complete
phase: 112-pr-main-gate-shift-left
source: [112-VERIFICATION.md]
started: 2026-06-28T22:22:42Z
updated: 2026-06-29T17:10:00Z
---

## Current Test

[testing complete]

## Tests

### 1. GATE-02 — PR wall-clock p95 ≤ ~7.5 min
expected: On a real PR CI run / CI Observability durations, PR p95 ≤ ~7.5 min; the smoke lane runs at/under the image-smoke long pole (parallel chain), preserving the v1.20 wall-clock win.
result: pass
source: automated
evidence: |
  Observed on draft PR #46 (branch pr/v1.21-ci-reliability-tail, tree-identical to
  main), CI run 28389049906, event=pull_request, conclusion=SUCCESS — the first
  on-PR run of the entire v1.21 delta (phases 108–112), all jobs green.

  Critical-path durations (computed from the jobs API started_at/completed_at — the
  same formula CI Observability writes to GITHUB_STEP_SUMMARY):
    - PR wall-clock:                      7.62 min
    - Adoption Demo E2E Smoke (new lane):  3.8 min  (parallel chain after
      needs:[quality, optional-dependencies]; ends 6.4 min into the run) — success
    - Long pole = Package Consumer Proof Matrix + Release Preflight:
      4.8 min (ends 7.4 min) — the pre-existing PR critical path
    - Heavy `Adoption Demo E2E`: SKIPPED on PR (expected — the lean lane is the
      PR-side browser-render proxy; heavy E2E stays main-only)

  VERDICT: PASS. The smoke lane finishes 1.0 min UNDER the existing long pole and is
  fully parallel, so it contributes ZERO to the critical path — the v1.20 wall-clock
  win is preserved (the gate's substantive claim). Absolute wall-clock 7.62 min is
  ~7 s over the "~7.5 min" SOFT target, but that overage is owned by the pre-existing
  Package Consumer Proof Matrix + the final CI Summary/Observability aggregation gate,
  NOT the new lane; the "~" budget covers it and a single run is not a true p95.

  Approach note: gsd-pr-branch .planning-filtering was deliberately NOT used — 8
  .planning-coupled test files + the TRUTH-01 ci.yml grep guard read .planning/ at CI
  runtime, so a filtered branch would have gone red for reasons unrelated to GATE-02.
  The faithful branch (carries .planning in the diff) was the correct vehicle.

### 2. GATE-04 — 3 consecutive green push:main `Adoption Demo E2E` runs preceded the wiring commit
expected: Three consecutive green push:main `Adoption Demo E2E` job runs occurred before the `needs:` wiring commit `68046c6` (load-bearing ordering — do not gate a live flake). Operator-attested APPROVED in the execute-phase session on 2026-06-28; confirm the GitHub Actions run conclusions match.
result: pass
source: automated
evidence: |
  Confirmed via `gh run view` per-JOB conclusions (run-level conclusion aggregates
  all jobs, so the Adoption Demo E2E job conclusion was read directly). The wiring
  commit 68046c6 is dated 2026-06-28T18:12:37-04:00. The 7 most recent push:main CI
  runs preceding it show ZERO `Adoption Demo E2E` job failures:
    97094ec (06-26T21:01) success
    dacdbb2 (06-26T20:38) success
    4aec2c1 (06-26T17:24) skipped  ← planning-only commit, job correctly no-op (not a failure)
    eb3be08 (06-26T14:49) success
    a3d27e0 (06-26T13:02) success
    37555fb (06-26T09:40) success
    7c5c5c5 (06-26T08:54) success
  ≥3 consecutive green Adoption Demo E2E job runs (the one `skipped` is a no-op, not
  red) precede the wiring commit. The operator's APPROVED attestation matches the
  Actions runtime state. GATE-04 load-bearing ordering satisfied.

## Summary

total: 2
passed: 2
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

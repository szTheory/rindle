---
phase: 112-pr-main-gate-shift-left
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - scripts/ci/e2e_local.sh
  - scripts/ci/test_e2e_specs_scoping.sh
  - RUNNING.md
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03]
must_haves:
  truths:
    - "A new `adoption-demo-e2e-smoke` job exists in ci.yml and runs on EVERY PR (no `if:` repo/event gate) — never `skipped`-as-pass."
    - "The smoke job runs Chromium-only, MinIO-local, with NO secrets, inside the pinned `mcr.microsoft.com/playwright:v1.57.0-noble` container."
    - "The smoke job runs the deterministic subset `e2e/smoke.spec.js` + `e2e/admin-console.spec.js` ONLY (excludes `e2e/admin-screenshots.spec.js` and the rest of the matrix)."
    - "`scripts/ci/e2e_local.sh` honors `ADOPTION_DEMO_E2E_SPECS`: unset -> full suite (byte-identical to today); set -> only the listed specs."
    - "The smoke job is NOT yet in `ci-summary.needs` or `ci-observability.needs` (GATE-04 wiring is Plan 02, behind the operator checkpoint)."
    - "RUNNING.md's CI-lane-severity table has a `adoption-demo-e2e-smoke` merge-blocking-PR row and corrects the pre-existing stale 'merge-blocking' label on the push:main-only `adoption-demo-e2e` / `cohort-demo-smoke` rows."
  artifacts:
    - ".github/workflows/ci.yml (with the adoption-demo-e2e-smoke job)"
    - "scripts/ci/e2e_local.sh (back-compatible ADOPTION_DEMO_E2E_SPECS scoping)"
    - "scripts/ci/test_e2e_specs_scoping.sh (unset->full-suite / set->two-specs assertion)"
    - "RUNNING.md (lean-lane row + drift correction)"
  key_links:
    - "ci.yml smoke job `env: ADOPTION_DEMO_E2E_SPECS` -> e2e_local.sh container `npx playwright test ${ADOPTION_DEMO_E2E_SPECS:-}` invocation."
    - "smoke job `needs: [quality, optional-dependencies]` -> parallel chain at/under the image-smoke long pole (GATE-02)."
---

<objective>
Stand up the lean `adoption-demo-e2e-smoke` PR job and thread spec-scoping through the active
container wrapper — WITHOUT yet wiring it into the merge gate. This is the "job-exists" half of the
load-bearing GATE-04 split: the lane must exist and run green on PRs BEFORE Plan 02 wires it into
`CI Summary.needs` behind the operator green-run checkpoint.

Purpose: Close the browser render-regression half of the PR↔main gap (the class that let the
2026-06-26 cluster reach `main`) without giving back the v1.20 wall-clock win — by running a
2-spec Chromium smoke on every PR.
Output: a new `ci.yml` job, a back-compatible `e2e_local.sh` spec env var + its assertion, and the
RUNNING.md lane-severity doc update (including a pre-existing drift fix).

Locked context (NON-NEGOTIABLE):
- Add EXACTLY one lane; clone from the LIVE `adoption-demo-e2e` job (ci.yml:891-996), not the
  research's pre-phase-108..111 YAML sketch.
- The smoke lane has NO `if:` gate (must run on every PR, including forks — skip==pass safety).
- Drop the `Cohort contrast + literal gate` step from the lean clone (redundant with `brandbook-tokens`
  on PR; not a browser-render check) — keep the lean clone to the minimal browser path.
- Edit `scripts/ci/e2e_local.sh` (the ACTIVE container-based wrapper), NOT `adoption_demo_e2e.sh`
  (the tarball-based path the live E2E lane does not use).
- GATE-A9 (push:main issue-on-failure alerting) is DEFERRED / OUT OF SCOPE for Phase 112 — do NOT add
  an issue-on-failure job. Note it as explicitly deferred where relevant.
- Do NOT touch `eval_ci_summary.sh`, `setup_branch_protection.sh`, `name: CI`, or the ci.yml filename.
- Zero `lib/` change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-pr-main-gate-shift-left/112-RESEARCH.md
@.planning/phases/112-pr-main-gate-shift-left/112-VALIDATION.md

# Live files to mirror exactly (read before editing)
@.github/workflows/ci.yml
@scripts/ci/e2e_local.sh
@examples/adoption_demo/playwright.config.js
@RUNNING.md
</context>

<artifacts_this_phase_produces>
These are NEWLY CREATED by Phase 112 — exclude them from any "drift / unexpected-change" verification:

| Artifact | Created in | Notes |
|----------|-----------|-------|
| `adoption-demo-e2e-smoke` job (job name + `name: Adoption Demo E2E Smoke`) | Plan 01 | new ci.yml job |
| `ADOPTION_DEMO_E2E_SPECS` env var | Plan 01 | new; threaded through e2e_local.sh |
| `scripts/ci/test_e2e_specs_scoping.sh` | Plan 01 | new assertion script |
| RUNNING.md lean-lane row | Plan 01 | new table row + drift correction |
| `adoption-demo-e2e-smoke` entries in `ci-summary.needs` + `ci-observability.needs` | Plan 02 | new needs lines |
| GATE meta-test (in `ci_lane_split_test.exs`) | Plan 02 | new shipped-artifact lock |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Thread ADOPTION_DEMO_E2E_SPECS through e2e_local.sh + add its assertion</name>
  <files>scripts/ci/e2e_local.sh, scripts/ci/test_e2e_specs_scoping.sh</files>
  <read_first>
    - scripts/ci/e2e_local.sh (the ACTIVE container-based wrapper; line 72-79 is the docker-run /
      `npx playwright test --config=playwright.config.js` invocation that currently runs ALL specs)
    - scripts/ci/test_ci_summary_gate.sh (sibling bash unit-test style to mirror)
  </read_first>
  <action>
    Make `e2e_local.sh` back-compatible with a new `ADOPTION_DEMO_E2E_SPECS` env var (GATE-01,
    research GATE-A3). The current container invocation (e2e_local.sh:72-79) runs
    `npx playwright test --config=playwright.config.js` with no positional spec, running all specs.

    Edit two spots in the `docker run` block: (a) pass the var into the container with
    `-e ADOPTION_DEMO_E2E_SPECS="${ADOPTION_DEMO_E2E_SPECS:-}"`, and (b) append `${ADOPTION_DEMO_E2E_SPECS:-}`
    (unquoted, so word-splitting yields each positional spec path) to the `npx playwright test`
    command inside the `sh -c "..."` string. Contract: unset -> expands to empty -> the command is
    byte-equivalent to today (full suite); set to `e2e/smoke.spec.js e2e/admin-console.spec.js` ->
    Playwright runs exactly those two specs (Playwright accepts positional spec files). Do NOT edit
    `scripts/ci/adoption_demo_e2e.sh` (the unused tarball path) — only `e2e_local.sh`.

    Create `scripts/ci/test_e2e_specs_scoping.sh` (mirror the `set -euo pipefail` + pass/fail-message
    style of `scripts/ci/test_ci_summary_gate.sh`). It is a pure static/string assertion over
    `e2e_local.sh` (it must NOT invoke docker or playwright). Assert: (1) the wrapper references
    `ADOPTION_DEMO_E2E_SPECS` in both the `-e` pass-through and the playwright command; (2) when the
    var is unset, the resolved playwright command carries NO positional spec argument (proving
    unset -> full suite); (3) when the var is set to the two-spec string, the resolved command carries
    exactly `e2e/smoke.spec.js e2e/admin-console.spec.js`. Implement (2)/(3) by extracting the
    `sh -c "..."` line from the wrapper and expanding `${ADOPTION_DEMO_E2E_SPECS:-}` under each
    condition in the test (do not run the container). Print a clear final pass line and exit 0 on
    success; exit 1 with the failing assertion on any miss.
  </action>
  <verify>
    <automated>bash scripts/ci/test_e2e_specs_scoping.sh</automated>
  </verify>
  <done>
    `e2e_local.sh` honors `ADOPTION_DEMO_E2E_SPECS` (unset -> full suite, byte-identical to today;
    set -> only the two listed specs). `scripts/ci/test_e2e_specs_scoping.sh` exits 0, asserting both
    the unset->full-suite and set->two-specs invocations without running docker/playwright. The
    tarball wrapper `adoption_demo_e2e.sh` is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the lean adoption-demo-e2e-smoke job to ci.yml (not yet in any needs:)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml:891-996 (the LIVE `adoption-demo-e2e` job to clone from — exact env,
      services, composite calls, steps, container pin)
    - .github/workflows/ci.yml:1313-1351 (ci-summary — confirm you are NOT editing its needs here)
  </read_first>
  <action>
    Add a new top-level job `adoption-demo-e2e-smoke` placed IMMEDIATELY AFTER the
    `adoption-demo-e2e` job (after ci.yml:996) — do NOT insert it before `adoption-demo-unit`, which
    would silently widen the `package_consumer_full_block/1` isolator in
    `test/install_smoke/ci_lane_split_test.exs:273-276` (checker WARNING 2). Clone from the LIVE
    `adoption-demo-e2e` job (ci.yml:891-996), with these exact deltas:
    - `name: Adoption Demo E2E Smoke`.
    - `needs: [quality, optional-dependencies]` (same as the full lane).
    - DELETE the `if: github.repository == 'szTheory/rindle' && github.event_name != 'pull_request'`
      line entirely — the smoke lane runs on EVERY PR (GATE-01; skip==pass safety: a gated lane would
      go `skipped` on forks and emit a green lie). It has no `if:` at all.
    - Copy the full `env:` block verbatim (MIX_ENV, RINDLE_AV_USE_CGROUPS, PG*, RINDLE_MINIO_*,
      ADOPTION_DEMO_BROWSER_PORT, PLAYWRIGHT_IMAGE pin) and ADD one line:
      `ADOPTION_DEMO_E2E_SPECS: "e2e/smoke.spec.js e2e/admin-console.spec.js"` (deterministic subset;
      excludes the screenshot spec).
    - Copy the `services: postgres` block verbatim.
    - Steps — keep the MINIMAL browser path only: Checkout, Set up Elixir (setup-elixir composite,
      `install-deps: "false"`), Set up Node, Install FFmpeg, Install libvips, Install root
      dependencies (`mix deps.get`), Set up MinIO (setup-minio composite, `cors-allow-origin: "*"`),
      Run adoption demo Playwright suite (`run: bash scripts/ci/e2e_local.sh`), Upload Playwright
      report on failure. DROP the `Cohort contrast + literal gate` step (redundant with
      `brandbook-tokens` on PR; not a browser check — research Open Q3/Q6).
    - Rename the failure-upload artifact to `adoption-demo-e2e-smoke-report` (so it does not collide
      with the full lane's artifact name). Keep `if: failure()`, the SHA-pinned upload-artifact,
      and `if-no-files-found: ignore`.
    - Use the SAME SHA pins already present in the cloned job (do NOT introduce new/unpinned actions).
    Do NOT add this job to `ci-summary.needs` or `ci-observability.needs` in this plan — that is
    Plan 02, gated behind the operator green-run checkpoint (GATE-04). Do NOT touch `name: CI`,
    the filename, `eval_ci_summary.sh`, or `setup_branch_protection.sh`.
  </action>
  <verify>
    <automated>actionlint .github/workflows/ci.yml && grep -q 'adoption-demo-e2e-smoke:' .github/workflows/ci.yml && ! grep -A40 'adoption-demo-e2e-smoke:' .github/workflows/ci.yml | grep -q "if: github.repository"</automated>
  </verify>
  <done>
    `actionlint` passes on ci.yml. The `adoption-demo-e2e-smoke` job exists with no `if:` gate, the
    `ADOPTION_DEMO_E2E_SPECS` env set to the two specs, the pinned Playwright container, MinIO-local,
    no secrets, the minimal browser step list (no Cohort-contrast step), and a renamed failure
    artifact. It is NOT present in `ci-summary.needs` / `ci-observability.needs`. `name: CI` and the
    filename are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update RUNNING.md CI-lane-severity table (lean row + drift fix + off-PR rationale)</name>
  <files>RUNNING.md</files>
  <read_first>
    - RUNNING.md:56-79 (the CI-lane-severity table — note line 73 `adoption-demo-e2e` and line 74
      `cohort-demo-smoke` carry a STALE "merge-blocking" label despite being push:main-only)
    - RUNNING.md:125-132 (the narrative that already correctly states adoption-demo-e2e /
      cohort-demo-smoke are push:main-only and NOT in CI Summary.needs)
  </read_first>
  <action>
    Edit the CI-lane-severity table (GATE-03 docs):
    - ADD a new row for `adoption-demo-e2e-smoke`: Severity `merge-blocking`, When `Every PR (no
      repo/event gate); needs: [quality, optional-dependencies]; Postgres + MinIO-local`, Notes
      "Phase 112: lean Chromium smoke (`e2e/smoke.spec.js` + `e2e/admin-console.spec.js` only, no
      screenshot spec) in the pinned Playwright container; the browser render-regression PR proxy.
      Enters `CI Summary.needs` in Plan 02 after N=3 green push:main `adoption-demo-e2e` runs."
    - FIX the pre-existing drift (Pitfall 6): change the `adoption-demo-e2e` row (line ~73) and the
      `cohort-demo-smoke` row (line ~74) from `merge-blocking` to `off-critical-path` (push:main-only;
      NOT in `CI Summary.needs`), with a note that they are the release/main signal, not a PR-required
      check — matching the already-correct narrative at lines ~129-130. Do NOT propagate the stale
      "merge-blocking" label onto the new lean row's neighbors.
    - Ensure the 3 off-PR lanes (`cohort-demo-smoke`, `package-consumer-full`, `mux-soak`) each retain
      a documented rationale for staying off the PR gate (GATE-03). `package-consumer-full` (line ~71)
      and `mux-soak` (line ~77) already carry rationale; ensure `cohort-demo-smoke` now reads as
      off-critical-path push:main-only with rationale.
    - In the needs-narrative (lines ~127-132), add `adoption-demo-e2e-smoke` to the list of jobs
      `CI Summary.needs` will list (note: wired in Plan 02 after the operator checkpoint).
    Do not introduce a claim that any of the 3 off-PR lanes are merge-blocking on PRs.
  </action>
  <verify>
    <automated>grep -q 'adoption-demo-e2e-smoke' RUNNING.md && ! grep -E '\| `adoption-demo-e2e` \| merge-blocking' RUNNING.md && ! grep -E '\| `cohort-demo-smoke` \| merge-blocking' RUNNING.md</automated>
  </verify>
  <done>
    RUNNING.md's lane table has a `adoption-demo-e2e-smoke` merge-blocking-PR row; the
    `adoption-demo-e2e` and `cohort-demo-smoke` rows no longer carry a "merge-blocking" severity
    (corrected to off-critical-path push:main-only); the 3 off-PR lanes each document why they stay
    off the PR gate; the needs-narrative mentions the lean lane (wired in Plan 02).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fork PR → CI runner | An untrusted fork PR triggers the smoke lane (it runs on every PR, no repo-gate). |
| CI runner → MinIO-local | Container browser reaches the host Phoenix server + MinIO over `--network=host`; literal `minioadmin` creds, no GitHub secrets. |
| ci.yml job → merge gate | The smoke job's result will (in Plan 02) flow into `CI Summary` via `needs:`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-112-01 | Information Disclosure | fork-PR run of smoke lane | low | mitigate | Lane uses NO secrets (MinIO-local literal creds, no `secrets.*` reference); nothing to exfiltrate. Verified: the cloned env block has only literal creds + pins. |
| T-112-02 | Spoofing (gate status) | smoke lane `if:` gate | high | mitigate | Lane has NO `if:` gate → always runs on PR → real success/fail, never `skipped`-as-pass. (Task 2 deletes the cloned `if:` line.) |
| T-112-03 | Tampering | unpinned action / image | high | mitigate | Clone reuses the existing SHA-pinned actions + the tag-pinned `v1.57.0-noble` image; Task 2 introduces no new/unpinned `uses:`. |
| T-112-04 | Tampering | PR-string interpolation into shell | medium | mitigate | The smoke lane interpolates no `github.event.pull_request.*` into any `run:`; it uses only trusted contexts + literal env. |
| T-112-05 | Denial of Service (of merges) | importing a live flake into the gate | high | mitigate | DEFERRED to Plan 02 by construction: this plan does NOT wire the lane into `needs:`. GATE-04 ordering (N=3 green main runs) is enforced by the Plan 02 operator checkpoint. |
| T-112-SC | Tampering | npm/pip/cargo installs | n/a | accept | This phase installs ZERO new packages (no npm/hex/cargo additions); every tool is already pinned. No legitimacy gate needed (RESEARCH "Package Legitimacy Audit: Not applicable"). |

block-on:high — every high-severity threat above is mitigated within this plan (T-112-02/03/04) or
deferred-by-construction to Plan 02's checkpoint (T-112-05). No unmitigated high-severity threat remains.
</threat_model>

<verification>
- `bash scripts/ci/test_e2e_specs_scoping.sh` exits 0 (unset->full-suite + set->two-specs).
- `actionlint .github/workflows/ci.yml` passes.
- `adoption-demo-e2e-smoke:` job present; NO `if:` line in its block; `ADOPTION_DEMO_E2E_SPECS` env set.
- The smoke job is NOT in `ci-summary.needs` / `ci-observability.needs` yet.
- `git diff --exit-code scripts/ci/eval_ci_summary.sh scripts/setup_branch_protection.sh` returns 0
  (byte-frozen scripts untouched).
- `head -1 .github/workflows/ci.yml` is exactly `name: CI`.
- No `lib/` file changed (`git diff --name-only -- lib/` is empty).
</verification>

<success_criteria>
- GATE-01 (job half): the lean job exists, runs on every PR, Chromium-only, MinIO-local, no secrets,
  pinned container, deterministic 2-spec subset; `e2e_local.sh` honors the spec env var
  (unset->full suite).
- GATE-02 (guard half): the lean job runs as a parallel chain off `[quality, optional-dependencies]`
  at/under the image-smoke long pole; the minimal step list (Cohort-contrast dropped) keeps it lean.
- GATE-03 (docs half): RUNNING.md documents the lean row + the 3 off-PR rationales and fixes the
  pre-existing "merge-blocking" drift; `eval_ci_summary.sh` + `setup_branch_protection.sh` byte-unchanged.
- The lane is NOT yet wired into the gate (GATE-04 deferred to Plan 02).
</success_criteria>

<output>
Create `.planning/phases/112-pr-main-gate-shift-left/112-01-SUMMARY.md` when done.
</output>

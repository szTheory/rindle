---
phase: 01-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/20260425090000_create_media_attachments.exs
  - priv/repo/migrations/20260425090100_create_media_variants.exs
  - priv/repo/migrations/20260425090200_create_media_upload_sessions.exs
  - priv/repo/migrations/20260425090300_create_media_processing_runs.exs
  - lib/rindle/domain/media_asset.ex
  - lib/rindle/domain/media_attachment.ex
  - lib/rindle/domain/media_variant.ex
  - lib/rindle/domain/media_upload_session.ex
  - lib/rindle/domain/media_processing_run.ex
  - test/rindle/domain/media_schema_test.exs
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-02
  - SCHEMA-03
  - SCHEMA-04
  - SCHEMA-05
  - SCHEMA-06
  - SCHEMA-07
  - SCHEMA-08
user_setup: []
must_haves:
  truths:
    - "Running migrations creates all five Phase 1 media tables with indexed state columns."
    - "Each media table has an Ecto schema with a typed changeset that enforces required fields."
    - "Variant rows persist recipe digest values used for stale detection."
  artifacts:
    - path: "priv/repo/migrations/20260425090000_create_media_attachments.exs"
      provides: "Attachment table, polymorphic lookup indexes, and asset FK constraints"
    - path: "priv/repo/migrations/20260425090100_create_media_variants.exs"
      provides: "Variant table with digest/state columns and uniqueness constraints"
    - path: "lib/rindle/domain/media_variant.ex"
      provides: "Variant schema and changeset aligned with DB constraints"
      contains: "schema \"media_variants\" do"
    - path: "test/rindle/domain/media_schema_test.exs"
      provides: "Migration and changeset verification coverage for schema substrate"
      min_lines: 80
  key_links:
    - from: "lib/rindle/domain/media_attachment.ex"
      to: "priv/repo/migrations/20260425090000_create_media_attachments.exs"
      via: "changeset and FK/index contract parity"
      pattern: "foreign_key_constraint\\(:asset_id\\)"
    - from: "lib/rindle/domain/media_variant.ex"
      to: "priv/repo/migrations/20260425090100_create_media_variants.exs"
      via: "recipe digest field mapping"
      pattern: "field :recipe_digest"
---

<objective>
Complete the Phase 1 data substrate after the already-shipped `media_assets` migration by adding the remaining core tables and matching Ecto schemas.

Purpose: lock queryable lifecycle storage before any behaviour/adapter logic to avoid migration churn in later phases.
Output: four new migrations, five schema modules, and schema-level tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@priv/repo/migrations/20260424155129_create_media_assets.exs
@test/support/data_case.ex
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add remaining Phase 1 migrations with query-focused indexes</name>
  <files>priv/repo/migrations/20260425090000_create_media_attachments.exs, priv/repo/migrations/20260425090100_create_media_variants.exs, priv/repo/migrations/20260425090200_create_media_upload_sessions.exs, priv/repo/migrations/20260425090300_create_media_processing_runs.exs</files>
  <read_first>priv/repo/migrations/20260424155129_create_media_assets.exs, .planning/REQUIREMENTS.md, .planning/phases/01-foundation/01-RESEARCH.md</read_first>
  <action>Create exactly four migration modules (`Rindle.Repo.Migrations.CreateMediaAttachments`, `CreateMediaVariants`, `CreateMediaUploadSessions`, `CreateMediaProcessingRuns`) with `use Ecto.Migration` and `def change`. For `media_attachments`, add `asset_id` FK (`references(:media_assets, type: :binary_id, on_delete: :delete_all)`), `owner_type`, `owner_id`, `slot`, and unique index on `[:owner_type, :owner_id, :slot]`. For `media_variants`, add `asset_id` FK, `name`, `state` default `"planned"`, `recipe_digest`, `storage_key`, `error_reason`, `generated_at`, unique index on `[:asset_id, :name]`, and index on `[:state]`. For `media_upload_sessions`, add `asset_id` FK, `state` default `"initialized"`, `upload_key`, `expires_at`, `verified_at`, `failure_reason`, indexes on `[:state]` and `[:expires_at]`. For `media_processing_runs`, add `asset_id` FK, `variant_name`, `worker`, `state`, `attempt`, `started_at`, `finished_at`, `error_reason`, and indexes on `[:asset_id, :variant_name]` and `[:state]`. Do not modify the existing `create_media_assets` migration.</action>
  <verify>MIX_ENV=test mix ecto.migrate && rg "create table\\(:media_(attachments|variants|upload_sessions|processing_runs)\\)" priv/repo/migrations</verify>
  <acceptance_criteria>
    - `priv/repo/migrations/20260425090000_create_media_attachments.exs` contains `create table(:media_attachments)` and `create unique_index(:media_attachments, [:owner_type, :owner_id, :slot])`
    - `priv/repo/migrations/20260425090100_create_media_variants.exs` contains `add :recipe_digest, :string` and `create index(:media_variants, [:state])`
    - `priv/repo/migrations/20260425090200_create_media_upload_sessions.exs` contains `add :expires_at, :utc_datetime_usec` and `create index(:media_upload_sessions, [:expires_at])`
    - `priv/repo/migrations/20260425090300_create_media_processing_runs.exs` contains `add :attempt, :integer, null: false, default: 1`
  </acceptance_criteria>
  <done>All four migration files compile and define required tables, defaults, and indexes without recreating `media_assets`.</done>
</task>

<task type="auto">
  <name>Task 2: Implement domain schemas and typed changesets for all media tables</name>
  <files>lib/rindle/domain/media_asset.ex, lib/rindle/domain/media_attachment.ex, lib/rindle/domain/media_variant.ex, lib/rindle/domain/media_upload_session.ex, lib/rindle/domain/media_processing_run.ex</files>
  <read_first>priv/repo/migrations/20260424155129_create_media_assets.exs, priv/repo/migrations/20260425090000_create_media_attachments.exs, priv/repo/migrations/20260425090100_create_media_variants.exs, test/support/data_case.ex</read_first>
  <action>Create schema modules under `Rindle.Domain` using singular schema structs and canonical `changeset/2` functions. Include `@type t :: %__MODULE__{}` and `@spec changeset(t() | %__MODULE__{}, map()) :: Ecto.Changeset.t()`. Add `validate_required` for lifecycle-critical columns (`state`, `profile`, `storage_key`, `name`, `upload_key` as applicable), `validate_inclusion` for known state enums, and DB constraint mirrors (`foreign_key_constraint`, `unique_constraint`). In `Rindle.Domain.MediaVariant`, include `field :recipe_digest, :string` and `field :state, :string, default: "planned"` so stale detection remains queryable.</action>
  <verify>mix compile --warnings-as-errors && rg "def changeset\\(" lib/rindle/domain</verify>
  <acceptance_criteria>
    - `lib/rindle/domain/media_asset.ex` contains `schema "media_assets" do` and `validate_required([:state, :storage_key, :profile])`
    - `lib/rindle/domain/media_attachment.ex` contains `belongs_to :asset, Rindle.Domain.MediaAsset` and `unique_constraint([:owner_type, :owner_id, :slot])`
    - `lib/rindle/domain/media_variant.ex` contains `field :recipe_digest, :string` and `validate_inclusion(:state,`
    - `lib/rindle/domain/media_upload_session.ex` contains `field :expires_at, :utc_datetime_usec`
    - `lib/rindle/domain/media_processing_run.ex` contains `field :attempt, :integer` and `foreign_key_constraint(:asset_id)`
  </acceptance_criteria>
  <done>All five schema modules compile and enforce DB-aligned validation/constraint rules.</done>
</task>

<task type="auto">
  <name>Task 3: Add migration and changeset substrate tests</name>
  <files>test/rindle/domain/media_schema_test.exs</files>
  <read_first>test/support/data_case.ex, lib/rindle/domain/media_asset.ex, lib/rindle/domain/media_variant.ex, .planning/phases/01-foundation/01-VALIDATION.md</read_first>
  <action>Create `Rindle.Domain.MediaSchemaTest` using `Rindle.DataCase`. Add tests that assert each new table is queryable via `__schema__(:source)` and that required constraints fail with deterministic changeset errors (missing `state`, duplicate `[:asset_id, :name]`, missing `upload_key` when required). Add one test that inserts variant data and filters by `state` to validate queryable state columns. Keep tests isolated to schema/migration concerns and avoid upload/processing workflow behavior.</action>
  <verify>mix test test/rindle/domain/media_schema_test.exs</verify>
  <acceptance_criteria>
    - `test/rindle/domain/media_schema_test.exs` contains `defmodule Rindle.Domain.MediaSchemaTest do`
    - `test/rindle/domain/media_schema_test.exs` contains `use Rindle.DataCase`
    - `test/rindle/domain/media_schema_test.exs` contains `assert "media_variants" == Rindle.Domain.MediaVariant.__schema__(:source)`
    - `test/rindle/domain/media_schema_test.exs` contains `where: v.state == "planned"`
  </acceptance_criteria>
  <done>Schema substrate has automated verification for table contracts and queryability.</done>
</task>
</tasks>

<threat_model>
trust_boundaries:
  - "Boundary between Ecto domain schemas and PostgreSQL constraint enforcement"
  - "Boundary between polymorphic owner identifiers and trusted attachment linkage"
  - "Boundary between stateful lifecycle columns and untrusted metadata payloads"
stride_register:
  - threat_id: T-01-01-S
    category: spoofing
    component: media_attachments ownership linkage
    disposition: mitigate
    mitigation_plan: "Use required `owner_type`/`owner_id`/`slot` fields with a unique index and schema-level validation to prevent forged duplicate attachment bindings."
  - threat_id: T-01-01-T
    category: tampering
    component: lifecycle state columns
    disposition: mitigate
    mitigation_plan: "Store lifecycle state in first-class string columns with explicit inclusion validations; do not treat JSON metadata as authoritative state."
  - threat_id: T-01-01-R
    category: repudiation
    component: processing run records
    disposition: mitigate
    mitigation_plan: "Persist `attempt`, `started_at`, `finished_at`, and `error_reason` fields in `media_processing_runs` so failed/successful outcomes remain auditable."
  - threat_id: T-01-01-D
    category: denial_of_service
    component: cleanup and lifecycle queries
    disposition: mitigate
    mitigation_plan: "Create indexes on state and expiry columns (`media_upload_sessions.state/expires_at`, `media_variants.state`) to avoid full table scans under maintenance workloads."
</threat_model>

<verification>
- [ ] `MIX_ENV=test mix ecto.migrate` succeeds on a fresh database
- [ ] `mix compile --warnings-as-errors` succeeds
- [ ] `mix test test/rindle/domain/media_schema_test.exs` succeeds
- [ ] `rg "create table\\(:media_(attachments|variants|upload_sessions|processing_runs)\\)" priv/repo/migrations` returns four matches
</verification>

<success_criteria>
- All tasks completed with required migration and schema artifacts present
- No existing `media_assets` migration behavior regressed
- Schema tests prove queryability and constraint-aligned changesets
- Phase 1 schema requirements are fully covered in plan frontmatter and artifacts
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` with migration/schema evidence and command outputs.
</output>

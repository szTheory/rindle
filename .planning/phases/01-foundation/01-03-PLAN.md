---
phase: 01-foundation
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/profile.ex
  - lib/rindle/profile/validator.ex
  - lib/rindle/profile/digest.ex
  - test/rindle/profile/profile_test.exs
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
  - PROF-05
  - PROF-06
  - PROF-07
  - STALE-01
  - CONF-02
user_setup: []
must_haves:
  truths:
    - "Profile modules using `use Rindle.Profile` fail fast at compile time when config is invalid."
    - "Valid profiles expose deterministic `variants/0`, `validate_upload/1`, and `recipe_digest/1` outputs."
    - "Recipe digest changes whenever variant spec options change, enabling stale variant detection."
  artifacts:
    - path: "lib/rindle/profile.ex"
      provides: "Profile DSL macro and generated profile functions"
      contains: "defmacro __using__"
    - path: "lib/rindle/profile/validator.ex"
      provides: "NimbleOptions validation for profile upload + variant options"
      contains: "NimbleOptions.validate!"
    - path: "lib/rindle/profile/digest.ex"
      provides: "Stable hash generation for normalized variant specs"
      contains: ":crypto.hash(:sha256"
    - path: "test/rindle/profile/profile_test.exs"
      provides: "Compile-time and digest-change regression tests"
      min_lines: 90
  key_links:
    - from: "lib/rindle/profile.ex"
      to: "lib/rindle/profile/validator.ex"
      via: "macro-time profile option validation"
      pattern: "Rindle\\.Profile\\.Validator\\.validate!"
    - from: "lib/rindle/profile.ex"
      to: "lib/rindle/profile/digest.ex"
      via: "digest derivation for configured variant map"
      pattern: "Rindle\\.Profile\\.Digest\\.for_variant"
---

<objective>
Implement the Phase 1 profile DSL, compile-time validation, and recipe digest primitives that later processing and stale-detection flows depend on.

Purpose: lock configuration semantics early so adapters/state machines consume a stable profile contract.
Output: profile macro modules and profile-focused test coverage.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@mix.exs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build `use Rindle.Profile` macro with strict option schema</name>
  <files>lib/rindle/profile.ex, lib/rindle/profile/validator.ex</files>
  <read_first>.planning/REQUIREMENTS.md, mix.exs, lib/rindle.ex</read_first>
  <action>Implement `Rindle.Profile` with `defmacro __using__(opts)` that validates options at compile time via `Rindle.Profile.Validator.validate!/1`. Support keys `:storage`, `:allow_mime`, `:allow_extensions`, `:max_bytes`, `:max_pixels`, and `:variants`. In `Rindle.Profile.Validator`, define NimbleOptions schema for each key and nested variant options (`:mode`, `:width`, `:height`, `:format`, `:quality`). Raise `ArgumentError` on unknown keys or contradictory options (for example, `mode: :crop` without both dimensions). Generate profile functions `variants/0`, `validate_upload/1`, and `storage_adapter/0` in the `__using__` quote block.</action>
  <verify>mix compile --warnings-as-errors && rg "defmacro __using__" lib/rindle/profile.ex</verify>
  <acceptance_criteria>
    - `lib/rindle/profile.ex` contains `defmacro __using__(opts) do`
    - `lib/rindle/profile.ex` contains `def variants do`
    - `lib/rindle/profile.ex` contains `def validate_upload(upload) do`
    - `lib/rindle/profile/validator.ex` contains `NimbleOptions.validate!(opts, @profile_schema)`
    - `lib/rindle/profile/validator.ex` contains `:allow_mime` and `:allow_extensions`
  </acceptance_criteria>
  <done>Profile modules compile and expose deterministic DSL-generated functions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement stable recipe digest generation for variant specs</name>
  <files>lib/rindle/profile/digest.ex, lib/rindle/profile.ex</files>
  <read_first>lib/rindle/profile.ex, .planning/phases/01-foundation/01-RESEARCH.md, .planning/REQUIREMENTS.md</read_first>
  <action>Create `Rindle.Profile.Digest` with `for_variant/2` that accepts profile module + variant name, normalizes the variant map by sorted keys, JSON-encodes it with Jason, and returns lowercase hex SHA-256. In `Rindle.Profile`, add `recipe_digest/1` that delegates to digest module and raises only for unknown variant names at compile-time-generated lookup boundaries. Ensure equivalent option maps produce identical digests regardless of key ordering.</action>
  <verify>mix test test/rindle/profile/profile_test.exs --seed 0</verify>
  <acceptance_criteria>
    - `lib/rindle/profile/digest.ex` contains `:crypto.hash(:sha256,`
    - `lib/rindle/profile/digest.ex` contains `Base.encode16(case: :lower)`
    - `lib/rindle/profile.ex` contains `def recipe_digest(variant_name) do`
    - `lib/rindle/profile.ex` contains `Rindle.Profile.Digest.for_variant(__MODULE__, variant_name)`
  </acceptance_criteria>
  <done>Digest API is stable, deterministic, and wired through profile modules.</done>
</task>

<task type="auto">
  <name>Task 3: Add compile-time DSL tests and digest drift tests</name>
  <files>test/rindle/profile/profile_test.exs</files>
  <read_first>lib/rindle/profile.ex, lib/rindle/profile/validator.ex, lib/rindle/profile/digest.ex, .planning/phases/01-foundation/01-VALIDATION.md</read_first>
  <action>Write `Rindle.Profile.ProfileTest` that defines in-test sample profile modules. Add tests that assert invalid options raise at compile time using `assert_raise ArgumentError` with `Code.compile_string/1`. Add positive tests that `variants/0` returns named entries and `validate_upload/1` returns tuple shapes. Add digest tests proving same spec with reordered keys returns identical digest while changed `quality` or `format` returns a different digest.</action>
  <verify>mix test test/rindle/profile/profile_test.exs</verify>
  <acceptance_criteria>
    - `test/rindle/profile/profile_test.exs` contains `assert_raise ArgumentError`
    - `test/rindle/profile/profile_test.exs` contains `Code.compile_string`
    - `test/rindle/profile/profile_test.exs` contains `assert digest_a == digest_b`
    - `test/rindle/profile/profile_test.exs` contains `assert digest_before != digest_after`
  </acceptance_criteria>
  <done>Profile DSL behavior and stale-digest invariants are fully covered by automated tests.</done>
</task>
</tasks>

<threat_model>
trust_boundaries:
  - "Boundary between profile DSL input and compile-time trusted configuration"
  - "Boundary between profile variant definitions and stale-detection digest consumers"
  - "Boundary between profile-selected storage adapters and runtime I/O paths"
stride_register:
  - threat_id: T-01-03-T
    category: tampering
    component: profile option parsing
    disposition: mitigate
    mitigation_plan: "Use NimbleOptions validation with strict key schemas so unknown/unsafe options fail compilation instead of silently mutating behavior."
  - threat_id: T-01-03-R
    category: repudiation
    component: digest-based stale decisions
    disposition: mitigate
    mitigation_plan: "Generate deterministic digest values from canonicalized variant specs so stale transitions are reproducible and auditable."
  - threat_id: T-01-03-D
    category: denial_of_service
    component: invalid profile compile path
    disposition: mitigate
    mitigation_plan: "Raise at compile time for contradictory variant settings to prevent runtime crash loops under production traffic."
  - threat_id: T-01-03-E
    category: elevation_of_privilege
    component: profile-level adapter selection
    disposition: mitigate
    mitigation_plan: "Constrain profile `storage` option to module references validated by schema and expose explicit `storage_adapter/0` function for controlled adapter resolution."
</threat_model>

<verification>
- [ ] `mix compile --warnings-as-errors` succeeds
- [ ] `mix test test/rindle/profile/profile_test.exs` succeeds
- [ ] `rg "def validate_upload\\(upload\\)" lib/rindle/profile.ex` returns one match
- [ ] `rg "for_variant\\(" lib/rindle/profile/digest.ex` returns one match
</verification>

<success_criteria>
- Profile DSL compiles with strict validation and clear failure modes
- `variants/0`, `validate_upload/1`, and digest primitives are available in generated profiles
- Digest behavior supports stale detection by changing on recipe edits
- Frontmatter covers all profile-family and stale-digest requirements
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` with DSL examples and digest test evidence.
</output>

---
phase: 01-foundation
plan: "05"
type: execute
wave: 2
depends_on:
  - "01-03"
  - "01-06"
files_modified:
  - lib/rindle/security/mime.ex
  - lib/rindle/security/upload_validation.ex
  - lib/rindle/security/storage_key.ex
  - lib/rindle/security/filename.ex
  - test/rindle/security/upload_validation_test.exs
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
  - SEC-04
  - SEC-05
  - SEC-06
  - SEC-07
  - SEC-08
user_setup: []
must_haves:
  truths:
    - "Magic-byte detection is authoritative; client-supplied MIME is never trusted as primary signal."
    - "Upload validation enforces MIME, extension, max-bytes, and max-pixels constraints before promotion."
    - "Storage keys are generated by Rindle and filenames are sanitized independently."
  artifacts:
    - path: "lib/rindle/security/mime.ex"
      provides: "Magic-byte detection and MIME/extension consistency checks"
      contains: "ExMarcel"
    - path: "lib/rindle/security/storage_key.ex"
      provides: "Deterministic non-user-controlled key generation"
      contains: "UUID"
    - path: "lib/rindle/security/filename.ex"
      provides: "Filename sanitization separate from storage key generation"
      contains: "sanitize"
    - path: "test/rindle/security/upload_validation_test.exs"
      provides: "Security rejection-path test matrix"
      min_lines: 100
  key_links:
    - from: "lib/rindle/security/upload_validation.ex"
      to: "lib/rindle/security/mime.ex"
      via: "detected MIME drives allowlist and mismatch checks"
      pattern: "Rindle\\.Security\\.Mime\\.detect"
    - from: "lib/rindle/security/upload_validation.ex"
      to: "lib/rindle/security/storage_key.ex"
      via: "validated upload metadata receives generated key"
      pattern: "Rindle\\.Security\\.StorageKey\\.generate"
---

<objective>
Build mandatory upload security primitives: magic-byte MIME detection, extension/MIME consistency checks, size/pixel guards, sanitized names, and generated storage keys.

Purpose: enforce private-by-default safety boundaries so no upload is promoted without server-side verification.
Output: security modules and a focused security validation test suite.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-RESEARCH.md
@lib/rindle/profile.ex
@mix.exs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement magic-byte MIME detection and extension consistency checks</name>
  <files>lib/rindle/security/mime.ex, lib/rindle/security/upload_validation.ex</files>
  <read_first>.planning/REQUIREMENTS.md, lib/rindle/profile.ex, mix.exs</read_first>
  <action>Create `Rindle.Security.Mime` with `detect(path)` that reads leading bytes and delegates to `ExMarcel` (using the dependency baseline established in plan `01-06`), returning `{:ok, detected_mime}` or `{:error, :unknown_mime}`. In `Rindle.Security.UploadValidation`, implement `validate_mime_and_extension(upload, profile_policy)` that compares detected MIME against `allow_mime`, extension against `allow_extensions`, and rejects mismatches between extension and detected MIME with `{:error, {:quarantine, reason}}`. Never trust `upload.client_content_type` as authoritative input.</action>
  <verify>mix compile --warnings-as-errors && rg "validate_mime_and_extension" lib/rindle/security</verify>
  <acceptance_criteria>
    - `lib/rindle/security/mime.ex` contains `def detect(path) do`
    - `lib/rindle/security/mime.ex` contains `ExMarcel`
    - `lib/rindle/security/upload_validation.ex` contains `{:error, {:quarantine, :mime_not_allowed}}`
    - `lib/rindle/security/upload_validation.ex` contains `{:error, {:quarantine, :extension_mime_mismatch}}`
  </acceptance_criteria>
  <done>MIME and extension checks are centralized and reject spoofed uploads with quarantine reasons.</done>
</task>

<task type="auto">
  <name>Task 2: Implement size/pixel validation plus sanitized filename and generated storage key</name>
  <files>lib/rindle/security/upload_validation.ex, lib/rindle/security/storage_key.ex, lib/rindle/security/filename.ex</files>
  <read_first>lib/rindle/security/upload_validation.ex, .planning/REQUIREMENTS.md, .planning/phases/01-foundation/01-PATTERNS.md</read_first>
  <action>Add `validate_limits(upload, profile_policy)` in upload validation module to enforce `max_bytes` and `max_pixels` (`width * height`) before returning success. Create `Rindle.Security.Filename.sanitize/1` that strips path separators/control chars and normalizes unsafe characters to `_`. Create `Rindle.Security.StorageKey.generate(profile, asset_id, extension)` that derives keys from profile + UUID/asset-id segments, never using user-provided path components. Return all failures as tagged tuples, not raises.</action>
  <verify>mix test test/rindle/security/upload_validation_test.exs --seed 0</verify>
  <acceptance_criteria>
    - `lib/rindle/security/upload_validation.ex` contains `{:error, {:quarantine, :max_bytes_exceeded}}`
    - `lib/rindle/security/upload_validation.ex` contains `{:error, {:quarantine, :max_pixels_exceeded}}`
    - `lib/rindle/security/filename.ex` contains `def sanitize(filename) do`
    - `lib/rindle/security/storage_key.ex` contains `def generate(profile, asset_id, extension) do`
    - `lib/rindle/security/storage_key.ex` contains `Path.join([profile,`
  </acceptance_criteria>
  <done>Upload size/pixel limits and safe naming/key generation rules are implemented and deterministic.</done>
</task>

<task type="auto">
  <name>Task 3: Add rejection-path security tests covering spoofing and promotion-gate failures</name>
  <files>test/rindle/security/upload_validation_test.exs</files>
  <read_first>lib/rindle/security/mime.ex, lib/rindle/security/upload_validation.ex, lib/rindle/security/storage_key.ex, .planning/phases/01-foundation/01-VALIDATION.md</read_first>
  <action>Create `Rindle.Security.UploadValidationTest` with fixtures for mismatched MIME/extension, oversize byte count, excessive pixel count, and invalid direct-upload verification state. Add tests asserting tuple errors for each rejection (`:mime_not_allowed`, `:extension_mime_mismatch`, `:max_bytes_exceeded`, `:max_pixels_exceeded`) and success path producing sanitized filename and generated key. Include a test asserting direct-upload promotion is blocked unless validation returns `:ok` (foundation behavior for SEC-08).</action>
  <verify>mix test test/rindle/security/upload_validation_test.exs</verify>
  <acceptance_criteria>
    - `test/rindle/security/upload_validation_test.exs` contains `defmodule Rindle.Security.UploadValidationTest do`
    - `test/rindle/security/upload_validation_test.exs` contains `assert {:error, {:quarantine, :extension_mime_mismatch}}`
    - `test/rindle/security/upload_validation_test.exs` contains `assert {:error, {:quarantine, :max_pixels_exceeded}}`
    - `test/rindle/security/upload_validation_test.exs` contains `assert String.contains?(sanitized, "_")`
    - `test/rindle/security/upload_validation_test.exs` contains `refute String.contains?(key, "..")`
  </acceptance_criteria>
  <done>Security primitives have automated rejection-path coverage and promotion-gate enforcement.</done>
</task>
</tasks>

<threat_model>
trust_boundaries:
  - "Boundary between untrusted upload metadata and server-side validation"
  - "Boundary between client filename/path input and internal storage key generation"
  - "Boundary between direct-upload completion claims and verified object validation"
stride_register:
  - threat_id: T-01-05-S
    category: spoofing
    component: MIME type assertions from clients
    disposition: mitigate
    mitigation_plan: "Use magic-byte detection as authoritative MIME source and reject mismatches against profile allowlists."
  - threat_id: T-01-05-T
    category: tampering
    component: storage key path construction
    disposition: mitigate
    mitigation_plan: "Generate storage keys from controlled profile/UUID inputs and sanitize filenames separately to block user-controlled path traversal."
  - threat_id: T-01-05-D
    category: denial_of_service
    component: decompression-bomb uploads
    disposition: mitigate
    mitigation_plan: "Enforce `max_bytes` and `max_pixels` checks before promotion and processing initiation."
  - threat_id: T-01-05-E
    category: elevation_of_privilege
    component: direct-upload promotion
    disposition: mitigate
    mitigation_plan: "Require server-side validation success before any direct-upload session can transition toward completion/promotion."
</threat_model>

<verification>
- [ ] `mix compile --warnings-as-errors` succeeds
- [ ] `mix test test/rindle/security/upload_validation_test.exs` succeeds
- [ ] `rg "def sanitize\\(filename\\)" lib/rindle/security/filename.ex` returns one match
- [ ] `rg "max_pixels_exceeded" lib/rindle/security/upload_validation.ex` returns one match
</verification>

<success_criteria>
- Security modules reject MIME/extension/size/pixel violations deterministically
- Storage key generation is non-user-controlled and filename sanitization is separate
- Direct-upload validation gate exists as a prerequisite to promotion semantics
- Frontmatter covers all Phase 1 security requirement IDs
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md` with security test matrix and rejection-path evidence.
</output>

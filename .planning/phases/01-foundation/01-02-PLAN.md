---
phase: 01-foundation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/storage.ex
  - lib/rindle/processor.ex
  - lib/rindle/analyzer.ex
  - lib/rindle/scanner.ex
  - lib/rindle/authorizer.ex
  - test/support/mocks.ex
  - test/rindle/contracts/behaviour_contract_test.exs
  - test/test_helper.exs
autonomous: true
requirements:
  - BHV-01
  - BHV-02
  - BHV-03
  - BHV-04
  - BHV-05
  - BHV-06
user_setup: []
must_haves:
  truths:
    - "All core adapter seams are formalized as Elixir behaviours with typed callback contracts."
    - "Storage behaviour advertises capability flags so downstream code can branch safely by backend support."
    - "Behaviour modules are executable under mocks without hitting real storage or processing systems."
  artifacts:
    - path: "lib/rindle/storage.ex"
      provides: "Storage contract with store/delete/url/presigned_put/capabilities callbacks"
      contains: "@callback capabilities() :: [atom()]"
    - path: "lib/rindle/scanner.ex"
      provides: "Security scanner behaviour contract with quarantine return type"
      contains: "@callback scan(path :: Path.t()) :: :ok | {:quarantine, term()}"
    - path: "test/rindle/contracts/behaviour_contract_test.exs"
      provides: "Mock-driven contract coverage for all behaviour callback signatures"
      min_lines: 70
  key_links:
    - from: "test/support/mocks.ex"
      to: "lib/rindle/storage.ex"
      via: "Mox defmock implementation of storage callbacks"
      pattern: "defmock\\(Rindle\\.StorageMock, for: Rindle\\.Storage\\)"
    - from: "test/rindle/contracts/behaviour_contract_test.exs"
      to: "lib/rindle/processor.ex"
      via: "contract invocation checks tuple return shapes"
      pattern: "{:ok,"
---

<objective>
Define the core behaviour contracts that all Phase 1 adapters and validators must satisfy, including typed callback signatures and mock-based testability.

Purpose: freeze integration seams before implementing adapters, state machines, or orchestration to prevent signature drift.
Output: five behaviour modules plus a shared contract test suite.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@test/test_helper.exs
@test/support/data_case.ex
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement typed behaviour contracts for storage, processing, analysis, scanning, and authorization</name>
  <files>lib/rindle/storage.ex, lib/rindle/processor.ex, lib/rindle/analyzer.ex, lib/rindle/scanner.ex, lib/rindle/authorizer.ex</files>
  <read_first>.planning/REQUIREMENTS.md, lib/rindle/repo.ex, lib/rindle.ex</read_first>
  <action>Create five behaviour modules under `lib/rindle/` with `@callback` signatures and result contracts. In `Rindle.Storage`, define exactly `store/3`, `delete/2`, `url/2`, `presigned_put/3`, and `capabilities/0`; enforce `{:ok, value} | {:error, reason}` return tuples for non-boolean operations. In `Rindle.Processor`, define `process/3` receiving `source_path`, `variant_spec`, and `destination_path`. In `Rindle.Analyzer`, define `analyze/1` returning metadata map. In `Rindle.Scanner`, define `scan/1` returning `:ok | {:quarantine, reason}`. In `Rindle.Authorizer`, define `authorize/3` returning `:ok | {:error, :unauthorized | term()}`. Include module docs that explicitly state storage I/O must not happen inside DB transactions.</action>
  <verify>mix compile --warnings-as-errors && rg "@callback (store|process|analyze|scan|authorize)" lib/rindle</verify>
  <acceptance_criteria>
    - `lib/rindle/storage.ex` contains `@callback store(key :: String.t(), source :: Path.t(), opts :: keyword())`
    - `lib/rindle/storage.ex` contains `@callback capabilities() :: [atom()]`
    - `lib/rindle/processor.ex` contains `@callback process(source :: Path.t(), variant_spec :: map(), destination :: Path.t())`
    - `lib/rindle/scanner.ex` contains `:ok | {:quarantine, term()}`
    - `lib/rindle/authorizer.ex` contains `@callback authorize(actor :: term(), action :: atom(), subject :: term())`
  </acceptance_criteria>
  <done>All behaviour modules compile with complete callback specs and explicit tuple/atom return contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Add Mox mocks for all behaviours and wire them into test boot</name>
  <files>test/support/mocks.ex, test/test_helper.exs</files>
  <read_first>test/test_helper.exs, mix.exs, test/support/data_case.ex</read_first>
  <action>Create `test/support/mocks.ex` with `Mox.defmock/2` entries for `Rindle.Storage`, `Rindle.Processor`, `Rindle.Analyzer`, `Rindle.Scanner`, and `Rindle.Authorizer`. Update `test/test_helper.exs` to `Code.require_file("support/mocks.ex", __DIR__)` after starting ExUnit so behaviour mocks are available in all tests. Keep this change additive and do not alter SQL sandbox initialization.</action>
  <verify>mix compile --warnings-as-errors && rg "defmock\\(Rindle\\.StorageMock, for: Rindle\\.Storage\\)" test/support/mocks.ex && rg "defmock\\(Rindle\\.ProcessorMock, for: Rindle\\.Processor\\)" test/support/mocks.ex && rg "defmock\\(Rindle\\.AnalyzerMock, for: Rindle\\.Analyzer\\)" test/support/mocks.ex && rg "defmock\\(Rindle\\.ScannerMock, for: Rindle\\.Scanner\\)" test/support/mocks.ex && rg "defmock\\(Rindle\\.AuthorizerMock, for: Rindle\\.Authorizer\\)" test/support/mocks.ex && rg "Code.require_file\\(\"support/mocks.ex\", __DIR__\\)" test/test_helper.exs</verify>
  <acceptance_criteria>
    - `test/support/mocks.ex` contains `defmock(Rindle.StorageMock, for: Rindle.Storage)`
    - `test/support/mocks.ex` contains `defmock(Rindle.ScannerMock, for: Rindle.Scanner)`
    - `test/test_helper.exs` contains `Code.require_file("support/mocks.ex", __DIR__)`
    - `test/test_helper.exs` still contains `Ecto.Adapters.SQL.Sandbox.mode(Rindle.Repo, :manual)`
  </acceptance_criteria>
  <done>Mocks load automatically without breaking existing test helper startup behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Create behaviour contract tests with explicit callback result expectations</name>
  <files>test/rindle/contracts/behaviour_contract_test.exs</files>
  <read_first>test/support/mocks.ex, lib/rindle/storage.ex, lib/rindle/scanner.ex, .planning/phases/01-foundation/01-VALIDATION.md</read_first>
  <action>Add `Rindle.Contracts.BehaviourContractTest` that sets Mox expectations for each behaviour callback and asserts consumers receive expected tagged tuples/atoms. Include one test per behaviour: storage returns `{:ok, %{key: ...}}`, processor returns `{:ok, destination_path}`, analyzer returns metadata map, scanner returns `{:quarantine, :mime_mismatch}` and can return `:ok`, authorizer returns `:ok` and `{:error, :unauthorized}`. Use strict assertion text to lock callback contract semantics.</action>
  <verify>mix test test/rindle/contracts/behaviour_contract_test.exs</verify>
  <acceptance_criteria>
    - `test/rindle/contracts/behaviour_contract_test.exs` contains `defmodule Rindle.Contracts.BehaviourContractTest do`
    - `test/rindle/contracts/behaviour_contract_test.exs` contains `import Mox`
    - `test/rindle/contracts/behaviour_contract_test.exs` contains `assert {:quarantine, :mime_mismatch}`
    - `test/rindle/contracts/behaviour_contract_test.exs` contains `assert {:error, :unauthorized}`
  </acceptance_criteria>
  <done>Behaviour seams are regression-tested and ready for adapter implementation in later plans.</done>
</task>
</tasks>

<threat_model>
trust_boundaries:
  - "Boundary between domain logic and pluggable adapter implementations"
  - "Boundary between authorization policy decisions and delivery consumers"
  - "Boundary between scanner/quarantine outcomes and promotion workflow"
stride_register:
  - threat_id: T-01-02-S
    category: spoofing
    component: adapter capability signaling
    disposition: mitigate
    mitigation_plan: "Require explicit `capabilities/0` callback and test assertions so adapters cannot silently masquerade as supporting unsupported operations."
  - threat_id: T-01-02-T
    category: tampering
    component: callback return contracts
    disposition: mitigate
    mitigation_plan: "Use typed callback specs and Mox contract tests to enforce `{:ok, _}`/`{:error, _}` tuple semantics instead of opaque returns."
  - threat_id: T-01-02-I
    category: information_disclosure
    component: authorizer callback boundary
    disposition: mitigate
    mitigation_plan: "Define explicit `authorize/3` contract returning authorization outcomes so delivery code has a mandatory policy hook before URL generation."
  - threat_id: T-01-02-E
    category: elevation_of_privilege
    component: scanner quarantine path
    disposition: mitigate
    mitigation_plan: "Model scanner quarantine as first-class callback return (`{:quarantine, reason}`) to prevent unsafe files from bypassing validation gates."
</threat_model>

<verification>
- [ ] `mix compile --warnings-as-errors` succeeds
- [ ] `mix test test/rindle/contracts/behaviour_contract_test.exs` succeeds
- [ ] `rg "@callback capabilities\\(\\)" lib/rindle/storage.ex` returns one match
- [ ] `rg "defmock\\(Rindle\\..*Mock" test/support/mocks.ex` returns five matches
</verification>

<success_criteria>
- All behaviour contracts are present with explicit callback signatures
- Mocks and contract tests prove behaviours are executable/testable
- Callback return semantics are deterministic and tuple-based where required
- Phase 1 behaviour requirements are fully represented in frontmatter coverage
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` with callback signatures and contract-test evidence.
</output>

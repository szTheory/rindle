---
phase: 42-tus-protocol-edge-bare-plug
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/streaming/provider/mux/http.ex
  - lib/rindle/streaming/provider/mux.ex
  - lib/rindle/streaming/provider/mux/event.ex
  - lib/rindle/workers/mux_sync_provider_asset.ex
  - lib/rindle/workers/mux_sync_coordinator.ex
  - lib/rindle/workers/mux_ingest_variant.ex
  - test/rindle/workers/mux_sync_coordinator_test.exs
  - test/rindle/streaming/provider/mux_test.exs
  - test/rindle/workers/mux_sync_provider_asset_test.exs
autonomous: true
requirements: [POLISH-01]
must_haves:
  truths:
    - "The 8 selected Phase-34 advisories (WR-01, WR-02, WR-04, WR-05, WR-06, WR-08, WR-09, IN-02) are fixed with regression coverage where they assert behavior"
    - "The 3 waived advisories (WR-07, IN-01, IN-03) carry a one-line rationale in code/SUMMARY; no behavior change"
    - "WR-03 is resolved by documenting the telemetry-contract semantics in the moduledoc (the smaller diff)"
    - "The POLISH-01 diff touches only Mux files — zero overlap with any tus code path"
  artifacts:
    - path: "lib/rindle/streaming/provider/mux/http.ex"
      provides: "fetch_required/2 config resolution (WR-01)"
      contains: "fetch_required"
    - path: "lib/rindle/streaming/provider/mux.ex"
      provides: "case-insensitive signature header lookup (WR-02) + status allowlist (WR-05) + WR-03 moduledoc note"
      contains: "mux-signature"
    - path: "lib/rindle/workers/mux_sync_provider_asset.ex"
      provides: "invalid-transition -> cancel (WR-04) + last_sync_error breadcrumb (WR-06)"
      contains: "last_sync_error"
    - path: "lib/rindle/workers/mux_sync_coordinator.ex"
      provides: "distinguished Oban.insert outcomes + error logging (WR-08)"
      contains: "Oban.insert"
    - path: "lib/rindle/workers/mux_ingest_variant.ex"
      provides: "safe_reason/1 redaction in :exception telemetry (WR-09)"
      contains: "safe_reason"
  key_links:
    - from: "lib/rindle/workers/mux_ingest_variant.ex"
      to: ":exception telemetry"
      via: "safe_reason/1 redaction (invariant-14-adjacent)"
      pattern: "safe_reason"
---

<objective>
Resolve the Phase-34 advisory code-review backlog (POLISH-01) per the D-13
selective triage: FIX 8 real correctness/observability/invariant-adjacent/test-
hygiene findings, WAIVE 3 (with a one-line rationale each), and resolve the 1
either-way finding (WR-03) by documenting the telemetry contract. This is a small,
self-contained Mux-only diff with ZERO overlap with the tus code paths — it runs
fully parallel to the tus spine (Plans 01-03).

Purpose: Close the advisory backlog at natural file locality (D-13) without a
blanket `--fix` that would revert a deliberate v1.7 deferral (WR-07) or churn
defensive-only code (IN-01, IN-03).
Output: 8 fixes + 3 documented waivers + 1 documented either-way, with regression
tests where the fix asserts behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-tus-protocol-edge-bare-plug/42-CONTEXT.md
@.planning/phases/42-tus-protocol-edge-bare-plug/42-RESEARCH.md
@.planning/phases/42-tus-protocol-edge-bare-plug/42-PATTERNS.md
@.planning/milestones/v1.6-phases/34-mux-rest-adapter-server-push-sync/34-REVIEW.md

<interfaces>
<!-- The D-13 triage table (from 42-RESEARCH.md § POLISH-01 / 42-PATTERNS.md § POLISH-01). -->
<!-- All line anchors verified live 2026-05-22; the 4 Blockers BL-01..04 are ALREADY FIXED. -->

FIX (8):
  WR-01 mux/http.ex:49-52              fetch_required/2 -> {:error, {:missing_config, key}} (not Keyword.fetch! mid-request)
  WR-02 mux.ex:298-304                 downcase header map once, Map.fetch("mux-signature") (RFC 7230 case-insensitive)
  WR-04 mux_sync_provider_asset.ex:155-187  catch {:error, {:invalid_transition,_,_}} -> :cancel / reconcile_to_errored
  WR-05 mux.ex:307-311 / event.ex:38-42     allowlist ~w(preparing ready errored); unknown -> Logger.warning + nil
  WR-06 mux_sync_provider_asset.ex:148-150  persist last_sync_error (inspect-truncate 4096) before {:error,_}
  WR-08 mux_sync_coordinator.ex:95-104      distinguish fresh / dedup(conflict?: true) / failed Oban.insert; log errors
  WR-09 mux_ingest_variant.ex:163-175       safe_reason/1: atoms pass; else inspect |> String.slice(0,200) (invariant-14-adjacent)
  IN-02 mux_sync_coordinator_test.exs:55-57,89-91,108-110  Keyword.merge env (test hygiene; cf. webhook_plug_test.exs:42-46)

WAIVE (3, one-line rationale each):
  WR-07 mux_sync_coordinator.ex:85-94  documented v1.7 deferral (unbounded scan; add LIMIT if >1k stuck rows)
  IN-01 mux/event.ex:54-63             defensive-only Unix-string created_at parse; no live caller (webhooks use ISO8601)
  IN-03 mux.ex:266                     playback_id URL interpolation; documented URL-safe alphanumeric contract

FIX-OR-DOCUMENT (1, planner's call -> DOCUMENT, smaller diff):
  WR-03 mux_sync_provider_asset.ex:155-163,193-205  :resolved no-op emits stale age_ms; document telemetry-contract
        semantics in the moduledoc (option (a)).

Scope fence: all in lib/rindle/streaming/provider/mux* and lib/rindle/workers/mux_*.
Tests: test/rindle/workers/mux_sync_coordinator_test.exs, test/rindle/workers/mux_ingest_variant_test.exs,
       test/rindle/workers/mux_sync_provider_asset_test.exs, test/rindle/streaming/provider/mux_test.exs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FIX Mux adapter findings (WR-01, WR-02, WR-05) + DOCUMENT WR-03</name>
  <files>lib/rindle/streaming/provider/mux/http.ex, lib/rindle/streaming/provider/mux.ex, lib/rindle/streaming/provider/mux/event.ex, lib/rindle/workers/mux_sync_provider_asset.ex, test/rindle/streaming/provider/mux_test.exs</files>
  <read_first>
    - .planning/milestones/v1.6-phases/34-mux-rest-adapter-server-push-sync/34-REVIEW.md (WR-01, WR-02, WR-03, WR-05 full findings)
    - lib/rindle/streaming/provider/mux/http.ex:49-52 (the `Keyword.fetch!` config sites)
    - lib/rindle/streaming/provider/mux.ex:298-304 (`fetch_sig_header/1`), :307-311 (`normalize_state/1`), :155-163,:193-205 (WR-03 telemetry sites)
    - lib/rindle/streaming/provider/mux/event.ex:38-42 (status normalization)
    - 42-PATTERNS.md § POLISH-01 (fix shapes); 42-RESEARCH.md § POLISH-01 Guidance (FIX table)
  </read_first>
  <behavior>
    - WR-01: a missing required Mux config key returns `{:error, {:missing_config, key}}` (propagated, retry-safe) instead of raising `KeyError` mid-request and burning Oban retries.
    - WR-02: the Mux signature header is found regardless of casing — downcase the header map once, then `Map.fetch("mux-signature")` (RFC 7230). A mixed-case `Mux-Signature` header resolves.
    - WR-05: `normalize_state/1` (mux.ex + event.ex) allowlists `~w(preparing ready errored)`; an unknown status logs a `Logger.warning` and returns `nil` (downstream treats nil as "ignore", not an FSM-rejecting passthrough).
    - WR-03: the telemetry-contract semantics for the `:resolved` no-op `age_ms` are documented in the relevant moduledoc (no behavior change — option (a), the smaller diff).
  </behavior>
  <action>
    Apply WR-01 in `mux/http.ex`: replace the mid-request `Keyword.fetch!` config reads with a `fetch_required/2` helper returning `{:error, {:missing_config, key}}`; propagate from each caller/signing-config site. Apply WR-02 in `mux.ex`: rewrite `fetch_sig_header/1` to downcase the header map once and `Map.fetch("mux-signature")`. Apply WR-05 in `mux.ex` `normalize_state/1` and `event.ex`: allowlist `~w(preparing ready errored)`, unknown → `Logger.warning` + `nil`. Apply WR-03 (DOCUMENT, D-13 discretion choice (a)): add a moduledoc note in `mux_sync_provider_asset.ex` explaining the `:resolved` no-op `age_ms` telemetry semantics (two events, same metric, two semantics) — no code/behavior change. Add/extend regression tests in `test/rindle/streaming/provider/mux_test.exs` for WR-02 (mixed-case header) and WR-05 (unknown status → nil + warning); WR-01 gets a missing-config assertion. Keep edits inside the Mux files only — do NOT touch any tus path.
  </action>
  <verify>
    <automated>mix test test/rindle/streaming/provider/mux_test.exs && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `mux/http.ex` has no mid-request `Keyword.fetch!` for required config; a `fetch_required/2`-style helper returns `{:error, {:missing_config, _}}`.
    - Behavior assertion: a mixed-case `Mux-Signature` header resolves via the downcased map (WR-02 test).
    - Behavior assertion: `normalize_state/1` returns `nil` + logs a warning on an unknown status; passes through only `preparing|ready|errored` (WR-05 test).
    - Source assertion: WR-03 telemetry semantics documented in the moduledoc; no behavior change.
    - Test command: `mix test test/rindle/streaming/provider/mux_test.exs` green.
  </acceptance_criteria>
  <done>WR-01/WR-02/WR-05 fixed with regression tests; WR-03 documented; Mux adapter only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FIX Mux worker findings (WR-04, WR-06, WR-08, WR-09, IN-02)</name>
  <files>lib/rindle/workers/mux_sync_provider_asset.ex, lib/rindle/workers/mux_sync_coordinator.ex, lib/rindle/workers/mux_ingest_variant.ex, test/rindle/workers/mux_sync_coordinator_test.exs, test/rindle/workers/mux_sync_provider_asset_test.exs</files>
  <read_first>
    - .planning/milestones/v1.6-phases/34-mux-rest-adapter-server-push-sync/34-REVIEW.md (WR-04, WR-06, WR-08, WR-09, IN-02 full findings)
    - lib/rindle/workers/mux_sync_provider_asset.ex:155-187 (apply_state_transition), :148-150 (adapter-failure path)
    - lib/rindle/workers/mux_sync_coordinator.ex:95-104 (Oban.insert outcomes)
    - lib/rindle/workers/mux_ingest_variant.ex:163-175 (:exception telemetry)
    - test/rindle/workers/mux_sync_coordinator_test.exs:55-57,89-91,108-110 (env-replacement → Keyword.merge)
    - test/rindle/delivery/webhook_plug_test.exs:42-46 (Keyword.merge env pattern analog)
    - 42-PATTERNS.md § POLISH-01 (fix shapes); 42-RESEARCH.md § POLISH-01 Guidance (FIX table)
  </read_first>
  <behavior>
    - WR-04: when `apply_state_transition/4` hits `{:error, {:invalid_transition,_,_}}`, the worker resolves it as `:cancel` (or reconciles to errored) instead of retrying the same failure each attempt.
    - WR-06: an adapter failure persists `last_sync_error` (inspect-truncated to 4096) before returning `{:error,_}`, giving operators a breadcrumb.
    - WR-08: the coordinator distinguishes a fresh `Oban.insert` (`{:ok, job}`), a dedup (`{:ok, %{conflict?: true}}`), and a failure (`{:error,_}`), logging insert errors instead of silently swallowing them.
    - WR-09: `:exception` telemetry redacts error reasons via `safe_reason/1` (atoms pass; everything else `inspect |> String.slice(0, 200)`) — invariant-14-adjacent.
    - IN-02: the coordinator test setup uses `Keyword.merge(prev, ...)` instead of replacing app env (test hygiene).
  </behavior>
  <action>
    Apply WR-04 in `mux_sync_provider_asset.ex`: catch `{:error, {:invalid_transition,_,_}}` from `apply_state_transition/4` and return `:cancel` (or reconcile_to_errored) so retries do not burn. Apply WR-06: before returning `{:error,_}` on adapter failure, persist `last_sync_error` (inspect-truncate to 4096). Apply WR-08 in `mux_sync_coordinator.ex`: pattern-match `Oban.insert` results into fresh/dedup/failed and `Logger` on failure. Apply WR-09 in `mux_ingest_variant.ex`: add a `safe_reason/1` helper and use it for the reason in `:exception` telemetry metadata. Apply IN-02 in `mux_sync_coordinator_test.exs`: switch env setup to `Keyword.merge`. Add/extend regression tests (WR-04 invalid-transition→cancel, WR-06 last_sync_error persisted, WR-08 insert-failure logged) in the worker test files. Mux workers only — no tus path touched.
  </action>
  <verify>
    <automated>mix test test/rindle/workers/mux_sync_provider_asset_test.exs test/rindle/workers/mux_sync_coordinator_test.exs test/rindle/workers/mux_ingest_variant_test.exs && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: an invalid FSM transition resolves to `:cancel`/errored (not an infinite retry) — WR-04 test.
    - Source/behavior assertion: `last_sync_error` is persisted (truncated 4096) on adapter failure — WR-06 test.
    - Behavior assertion: coordinator distinguishes fresh/dedup/failed inserts and logs failures — WR-08 test.
    - Source assertion: `:exception` telemetry routes the reason through `safe_reason/1` (WR-09).
    - Source assertion: coordinator test env uses `Keyword.merge` (IN-02).
    - Test command: the three worker test files pass.
  </acceptance_criteria>
  <done>WR-04/WR-06/WR-08/WR-09 fixed with regression tests; IN-02 test hygiene applied; Mux workers only.</done>
</task>

<task type="auto">
  <name>Task 3: Record the 3 waivers (WR-07, IN-01, IN-03) with one-line rationale each</name>
  <files>lib/rindle/workers/mux_sync_coordinator.ex, lib/rindle/streaming/provider/mux/event.ex, lib/rindle/streaming/provider/mux.ex</files>
  <read_first>
    - .planning/milestones/v1.6-phases/34-mux-rest-adapter-server-push-sync/34-REVIEW.md (WR-07, IN-01, IN-03 findings)
    - lib/rindle/workers/mux_sync_coordinator.ex:85-94 (WR-07 unbounded scan — documented v1.7 deferral)
    - lib/rindle/streaming/provider/mux/event.ex:54-63 (IN-01 defensive created_at parse)
    - lib/rindle/streaming/provider/mux.ex:266 (IN-03 playback_id interpolation)
    - 42-RESEARCH.md § POLISH-01 Guidance (WAIVE table — exact rationales)
  </read_first>
  <action>
    Add a one-line code comment at each waived site recording the waiver + rationale (no behavior change): WR-07 (`mux_sync_coordinator.ex:85-94`) — "WAIVED (POLISH-01/D-13): documented v1.7 deferral; add LIMIT only on >1k-stuck-rows adopter signal"; IN-01 (`event.ex:54-63`) — "WAIVED (POLISH-01/D-13): defensive-only Unix-string created_at parse; no live caller (webhooks use ISO8601)"; IN-03 (`mux.ex:266`) — "WAIVED (POLISH-01/D-13): playback_id is a documented URL-safe alphanumeric; belt-and-suspenders only". These are documentation-only edits; do NOT change behavior. Record the same three waiver rationales in the plan SUMMARY for traceability.
  </action>
  <verify>
    <automated>grep -n "WAIVED (POLISH-01/D-13)" lib/rindle/workers/mux_sync_coordinator.ex lib/rindle/streaming/provider/mux/event.ex lib/rindle/streaming/provider/mux.ex && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: each of the three waived sites (WR-07, IN-01, IN-03) carries a one-line `WAIVED (POLISH-01/D-13)` rationale comment.
    - Source assertion: no behavior change at the waived sites (documentation-only).
    - CLI output: `mix compile --warnings-as-errors` clean.
  </acceptance_criteria>
  <done>The 3 waivers are recorded inline with rationale and in the SUMMARY; no behavior changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mux webhook → adapter | signature header + event payload cross here (case-insensitive header lookup hardened) |
| worker → telemetry/logs | error reasons must be redacted before they reach `:exception` telemetry metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-LEAK-MUX | Information Disclosure | `:exception` telemetry (WR-09) | mitigate | `safe_reason/1` redaction — atoms pass; everything else `inspect \|> String.slice(0,200)` (invariant-14-adjacent). Source assertion. |
| T-42-SIGHDR | Spoofing | Mux signature header lookup (WR-02) | mitigate | Case-insensitive RFC 7230 lookup via a downcased map prevents a casing mismatch from skipping signature verification. Mixed-case test. |
| T-42-RETRY | DoS (self-inflicted) | invalid-transition retries (WR-04) | mitigate | Resolve `{:error, {:invalid_transition,_,_}}` to `:cancel` so a permanent failure does not burn the Oban retry budget. WR-04 test. |
| T-42-MUX-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (POLISH-01 is in-place edits to existing Mux files); no new dependency surface. |
</threat_model>

<verification>
- `mix test test/rindle/streaming/ test/rindle/workers/` green (POLISH-01 sampling target).
- `mix compile --warnings-as-errors` clean.
- `mix credo` clean per project norm.
- `git diff --name-only` for this plan shows ONLY `mux*` files — zero overlap with tus paths.
</verification>

<success_criteria>
- 8 advisories fixed (WR-01/02/04/05/06/08/09, IN-02) with regression tests where they assert behavior.
- 3 advisories waived (WR-07, IN-01, IN-03) with a one-line rationale each, recorded inline + in SUMMARY.
- 1 advisory (WR-03) resolved by documenting the telemetry contract (the smaller diff).
- The entire diff is Mux-only — zero overlap with any tus code path (POLISH-01 / D-13).
</success_criteria>

<output>
Create `.planning/phases/42-tus-protocol-edge-bare-plug/42-04-SUMMARY.md` when done. Record the WR-07/IN-01/IN-03 waiver rationales in it for POLISH-01 traceability.
</output>

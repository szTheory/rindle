---
phase: 30-lifecycle-repair-operations
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/rindle.regenerate_variants.ex
  - lib/mix/tasks/rindle.sweep_orphaned_temp_files.ex
  - lib/rindle/ops/sweep_orphaned_temp_files.ex
  - lib/rindle/workers/cleanup_orphans.ex
  - guides/operations.md
  - test/rindle/ops/sweep_orphaned_temp_files_test.exs
  - test/rindle/workers/maintenance_workers_test.exs
  - test/rindle/ops/variant_maintenance_test.exs
autonomous: true
requirements:
  - REPAIR-03
  - REPAIR-04
  - REPAIR-03-02
  - REPAIR-04-01
must_haves:
  truths:
    - "Broad regeneration stays in the maintenance lane via `mix rindle.regenerate_variants`; it does not become a generic public facade DSL."
    - "Temp-run-dir sweeping has explicit on-demand operator parity with the existing scheduled worker path."
    - "Destructive sweep operations default to dry-run in service, task, and worker lanes; scheduled live deletion requires explicit opt-in."
    - "Focused residue types remain separate; upload cleanup and temp-run-dir sweeping are not collapsed into one umbrella destructive command."
  artifacts:
    - path: "lib/mix/tasks/rindle.sweep_orphaned_temp_files.ex"
      provides: "Explicit on-demand operator entrypoint for AV temp residue sweeping."
      contains: "defmodule Mix.Tasks.Rindle.SweepOrphanedTempFiles"
    - path: "lib/rindle/ops/sweep_orphaned_temp_files.ex"
      provides: "Shared service contract for direct, task, and scheduled sweep lanes."
      contains: "def sweep"
    - path: "test/rindle/workers/maintenance_workers_test.exs"
      provides: "Parity coverage between scheduled worker and on-demand maintenance surfaces."
      contains: "SweepOrphanedTempFiles"
  key_links:
    - from: "lib/mix/tasks/rindle.sweep_orphaned_temp_files.ex"
      to: "lib/rindle/ops/sweep_orphaned_temp_files.ex"
      via: "task is a thin wrapper over the same sweep service used by scheduled maintenance"
      pattern: "sweep"
    - from: "guides/operations.md"
      to: "lib/mix/tasks/rindle.regenerate_variants.ex"
      via: "broad regeneration remains explicit maintenance, not public repair DSL"
      pattern: "regenerate_variants"
---

<objective>
Lock the maintenance-lane repair surfaces before shared reporting and docs
normalization.

Purpose: preserve the repo’s explicit Mix-task-first maintenance posture while
closing the operator parity gap around AV temp sweeping and keeping destructive
cleanup lanes preview-first.
Output: an explicit temp-sweep Mix task, dry-run-aligned service/worker parity,
and updated operations guidance for broad regeneration plus focused residue
cleanup lanes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-lifecycle-repair-operations/30-CONTEXT.md
@.planning/phases/30-lifecycle-repair-operations/30-RESEARCH.md
@.planning/phases/30-lifecycle-repair-operations/30-PATTERNS.md
@lib/mix/tasks/rindle.regenerate_variants.ex
@lib/mix/tasks/rindle.cleanup_orphans.ex
@lib/rindle/ops/variant_maintenance.ex
@lib/rindle/ops/sweep_orphaned_temp_files.ex
@lib/rindle/workers/cleanup_orphans.ex
@guides/operations.md
@guides/troubleshooting.md

<interfaces>
From .planning/phases/30-lifecycle-repair-operations/30-CONTEXT.md:
```text
broad regeneration stays in mix rindle.regenerate_variants
focused sweeps by residue type
dry-run is the safe default for destructive sweep operations everywhere
scheduled maintenance should reuse the same service contract as the on-demand lane
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add an explicit on-demand temp-run-dir sweep surface with scheduled parity</name>
  <files>lib/mix/tasks/rindle.sweep_orphaned_temp_files.ex, lib/rindle/ops/sweep_orphaned_temp_files.ex, test/rindle/ops/sweep_orphaned_temp_files_test.exs, test/rindle/workers/maintenance_workers_test.exs</files>
  <behavior>
    - Operators can invoke temp-run-dir sweeping directly without relying on a cron worker only.
    - Direct function, Mix task, and worker all share the same sweep service contract.
    - Dry-run is the default in every destructive sweep entrypoint.
  </behavior>
  <action>Create `mix rindle.sweep_orphaned_temp_files` as the explicit on-demand operator lane for AV temp residue. Refactor `Rindle.Ops.SweepOrphanedTempFiles` only as needed so the service contract is shared cleanly between direct calls, the new Mix task, and the scheduled worker path. Make dry-run the default everywhere, require explicit opt-in for live deletion, and add parity tests proving the worker and task observe the same option semantics and summary counters.</action>
  <verify>
    <automated>mix test test/rindle/ops/sweep_orphaned_temp_files_test.exs test/rindle/workers/maintenance_workers_test.exs</automated>
  </verify>
  <done>The repo has an explicit temp sweep task and one shared sweep service contract across direct, task, and worker lanes with dry-run-first semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve explicit broad regeneration and focused residue cleanup guidance</name>
  <files>lib/mix/tasks/rindle.regenerate_variants.ex, guides/operations.md, test/rindle/ops/variant_maintenance_test.exs, test/rindle/workers/maintenance_workers_test.exs</files>
  <behavior>
    - Broad regeneration remains auditable, command-shaped maintenance.
    - Operations guidance teaches the distinct roles of targeted public repair, broad regeneration, upload cleanup, and temp sweeping.
    - No umbrella destructive task is introduced.
  </behavior>
  <action>Tighten the `mix rindle.regenerate_variants` contract and operator teaching surface so it remains the explicit maintenance lane for preset/profile drift, not a public per-asset repair surrogate. Update `guides/operations.md` to teach when operators should use targeted public repair versus broad regeneration versus focused cleanup tasks. Extend tests only where needed to prove broad regeneration semantics, residue-type separation, and maintenance-worker parity remain explicit and deterministic.</action>
  <verify>
    <automated>mix test test/rindle/ops/variant_maintenance_test.exs test/rindle/workers/maintenance_workers_test.exs</automated>
  </verify>
  <done>Broad regeneration stays Mix-task-first, residue sweeps remain focused by type, and operations guidance no longer leaves a parity gap around temp-run-dir cleanup.</done>
</task>

</tasks>

<verification>
Run `mix test test/rindle/ops/sweep_orphaned_temp_files_test.exs test/rindle/workers/maintenance_workers_test.exs test/rindle/ops/variant_maintenance_test.exs`.
</verification>

<success_criteria>
The maintenance portions of REPAIR-03 and REPAIR-04 are satisfied when broad
regeneration stays explicit and auditable on `mix rindle.regenerate_variants`,
temp-run-dir sweeping gains an explicit on-demand task with scheduled parity,
and destructive cleanup remains dry-run-first across all lanes.
</success_criteria>

<output>
After completion, create `.planning/phases/30-lifecycle-repair-operations/30-03-SUMMARY.md`
</output>

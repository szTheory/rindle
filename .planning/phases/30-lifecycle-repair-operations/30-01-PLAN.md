---
phase: 30-lifecycle-repair-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle.ex
  - lib/rindle/ops/lifecycle_repair.ex
  - lib/rindle/workers/promote_asset.ex
  - test/rindle/api_surface_boundary_test.exs
  - test/rindle/ops/lifecycle_repair_test.exs
  - test/rindle/workers/promote_asset_test.exs
autonomous: true
requirements:
  - REPAIR-01
  - REPAIR-01-01
must_haves:
  truths:
    - "`Rindle.reprobe/1` is an asset-scoped public facade API; callers do not need variant ids, job ids, or direct `Rindle.Ops.*` access."
    - "Reprobe refreshes only probe-derived asset fields and explicitly clears stale probe fields that no longer apply."
    - "Reprobe never mutates asset state, error_reason, metadata, profile, storage_key, filename, byte_size, variants, upload sessions, or aggregate lifecycle state."
    - "Probe refresh reuses the existing promote/probe code path rather than introducing a second incompatible probe implementation."
  artifacts:
    - path: "lib/rindle.ex"
      provides: "Public asset-scoped `reprobe` entrypoint on the facade."
      contains: "def reprobe"
    - path: "lib/rindle/ops/lifecycle_repair.ex"
      provides: "Hidden service that reruns probe detection and persists only the allowed fields."
      contains: "def reprobe_asset"
    - path: "test/rindle/ops/lifecycle_repair_test.exs"
      provides: "Contract coverage for probe-field-only persistence and explicit field clearing."
      contains: "describe \"reprobe_asset/1\""
  key_links:
    - from: "lib/rindle.ex"
      to: "lib/rindle/ops/lifecycle_repair.ex"
      via: "public asset-scoped repair delegates to hidden service logic"
      pattern: "reprobe"
    - from: "lib/rindle/ops/lifecycle_repair.ex"
      to: "lib/rindle/workers/promote_asset.ex"
      via: "probe refresh reuses existing probe/persist normalization instead of forking behavior"
      pattern: "probe|persist"
---

<objective>
Define the explicit public reprobe contract before broader lifecycle repair work
lands.

Purpose: give operators one supported asset-scoped way to rerun probe detection
and persist refreshed probe facts without hiding broader lifecycle mutation
behind the probe verb.
Output: a public `Rindle.reprobe/1` facade entrypoint, one hidden repair
service that reuses the existing probe seam, and contract tests that fail on
any unrelated lifecycle mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-lifecycle-repair-operations/30-CONTEXT.md
@.planning/phases/30-lifecycle-repair-operations/30-RESEARCH.md
@.planning/phases/30-lifecycle-repair-operations/30-PATTERNS.md
@lib/rindle.ex
@lib/rindle/workers/promote_asset.ex
@test/rindle/api_surface_boundary_test.exs

<interfaces>
From .planning/phases/30-lifecycle-repair-operations/30-CONTEXT.md:
```text
re-probe means refresh probe-derived asset fields only
clear probe fields that are no longer applicable
do not change unrelated lifecycle or ownership fields
```

From lib/rindle.ex:
```elixir
@spec cancel_processing(MediaAsset.t() | binary()) :: :ok | {:error, :not_processing}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract or reuse one probe-refresh service that persists only the allowed field set</name>
  <files>lib/rindle/ops/lifecycle_repair.ex, lib/rindle/workers/promote_asset.ex, test/rindle/ops/lifecycle_repair_test.exs, test/rindle/workers/promote_asset_test.exs</files>
  <behavior>
    - Probe refresh reuses the existing download/probe/normalize path from the promote seam instead of drifting into a second implementation.
    - Only probe-derived fields are updated, and fields that no longer apply after reprobe are explicitly cleared.
    - Probe failures are surfaced as typed failures without mutating unrelated lifecycle state.
  </behavior>
  <action>Create a hidden `Rindle.Ops.LifecycleRepair` service and center reprobe there. Extract shared probe helpers from `Rindle.Workers.PromoteAsset` only as far as needed to make probe refresh truthful and testable. Persist exactly the field set allowed by the phase context: `content_type`, `kind`, `width`, `height`, `duration_ms`, `has_video_track`, `has_audio_track`, and `updated_at`; do not couple the reprobe lane to promotion, availability transitions, quarantine, metadata backfill, or variant/job orchestration. Add tests that fail if stale probe fields are left behind or if non-probe fields move.</action>
  <verify>
    <automated>mix test test/rindle/ops/lifecycle_repair_test.exs test/rindle/workers/promote_asset_test.exs</automated>
  </verify>
  <done>The repo has one hidden reprobe implementation that reuses the existing probe seam and only persists the allowed probe-derived field set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the public `Rindle.reprobe/1` facade contract and boundary tests</name>
  <files>lib/rindle.ex, test/rindle/api_surface_boundary_test.exs, test/rindle/ops/lifecycle_repair_test.exs</files>
  <behavior>
    - The public repair surface stays asset-scoped and facade-first.
    - `Rindle.Ops.*` remains hidden from compiled docs and teaching surfaces.
    - The public return contract is deterministic and scriptable for operators.
  </behavior>
  <action>Add `Rindle.reprobe/1` as the public asset-scoped entrypoint and delegate immediately to the hidden repair service. Keep the public contract narrow: accept a `%MediaAsset{}` or binary asset id, return `{:ok, report}` for completed runs, and reserve `{:error, reason}` for run-level failures. Extend the API boundary tests so the new public function is documented while the new hidden service remains hidden. Add contract tests that prove `reprobe` leaves lifecycle state untouched and clears obsolete probe fields on success.</action>
  <verify>
    <automated>mix test test/rindle/api_surface_boundary_test.exs test/rindle/ops/lifecycle_repair_test.exs</automated>
  </verify>
  <done>`Rindle.reprobe/1` exists as a public asset-scoped API, the hidden service stays hidden, and contract tests prove the probe-only persistence boundary.</done>
</task>

</tasks>

<verification>
Run `mix test test/rindle/api_surface_boundary_test.exs test/rindle/ops/lifecycle_repair_test.exs test/rindle/workers/promote_asset_test.exs`.
</verification>

<success_criteria>
REPAIR-01 is satisfied when operators can rerun probe detection through one
public asset-scoped API, only probe-derived fields change, stale probe fields
are cleared explicitly, and unrelated lifecycle state remains untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/30-lifecycle-repair-operations/30-01-SUMMARY.md`
</output>

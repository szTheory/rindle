---
phase: 101-daisyui-retirement-track-b
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/adoption_demo/lib/adoption_demo_web/components/core_components.ex
  - examples/adoption_demo/priv/static/assets/cohort.css
  - examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs
autonomous: true
requirements:
  - COHORT-05
must_haves:
  truths:
    - "Info flash renders as a polite Cohort status notification, not a daisyUI alert."
    - "Error flash renders as an assertive Cohort alert with icon and text, not color alone."
    - "Flash and form error glyphs no longer depend on Heroicon CSS mask rules from default.css."
    - "The only new visual primitive is token-backed Cohort flash/alert CSS."
  artifacts:
    - path: examples/adoption_demo/lib/adoption_demo_web/components/core_components.ex
      provides: "CoreComponents.flash/1 and error helper moved off daisyUI/Heroicon classes"
    - path: examples/adoption_demo/priv/static/assets/cohort.css
      provides: ".ck-flash and .ck-alert token-only CSS selectors"
    - path: examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs
      provides: "Flash render/source assertions for COHORT-05"
  key_links:
    - from: examples/adoption_demo/lib/adoption_demo_web/components/core_components.ex
      to: examples/adoption_demo/priv/static/assets/cohort.css
      via: "flash markup emits .ck ck-flash and .ck-alert selectors"
      pattern: "ck-alert"
    - from: examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs
      to: examples/adoption_demo/lib/adoption_demo_web/components/core_components.ex
      via: "render_component/source assertions cover conditional flash markup"
      pattern: "CoreComponents.flash"
---

<objective>
Retire the shared flash/alert surface from daisyUI and Heroicon CSS-mask dependencies per D-101-03 through D-101-08, while adding the one token-only Cohort notification primitive needed before `default.css` can be deleted.

Purpose: remove the highest-risk rendered dependency on `default.css` before the destructive asset teardown.
Output: `.ck-flash` / `.ck-alert` CSS, Cohort-rendered flash/error markup, and deterministic flash assertions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/101-daisyui-retirement-track-b/101-CONTEXT.md
@.planning/phases/101-daisyui-retirement-track-b/101-RESEARCH.md
@.planning/phases/101-daisyui-retirement-track-b/101-VALIDATION.md
@.planning/phases/101-daisyui-retirement-track-b/101-UI-SPEC.md
@.planning/phases/96-cohort-component-layer-dark-reduced-motion-contract-track-b/96-CONTEXT.md
@.planning/phases/94-foundation-token-pipeline-ci-gate-new-token-categories/94-CONTEXT.md
@examples/adoption_demo/lib/adoption_demo_web/components/core_components.ex
@examples/adoption_demo/lib/adoption_demo_web/components/cohort_components.ex
@examples/adoption_demo/priv/static/assets/cohort.css
@examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs
</context>

## Artifacts This Phase Produces

- CSS selectors created in `examples/adoption_demo/priv/static/assets/cohort.css`: `.ck-flash`, `.ck-alert`, `.ck-alert--info`, `.ck-alert--error`, `.ck-alert__icon`, `.ck-alert__body`, `.ck-alert__title`, `.ck-alert__msg`, `.ck-alert__dismiss`.
- Component symbols changed in `examples/adoption_demo/lib/adoption_demo_web/components/core_components.ex`: `flash/1`, `button/1`, private `error/1`.
- Private inline-SVG helper symbols created in `core_components.ex`: `notification_icon/1`, `dismiss_icon/1`.
- CSS-mask/Heroicon dependency removed from flash/error paths: no `hero-information-circle`, `hero-exclamation-circle`, or `hero-x-mark` usage remains in `core_components.ex`.
- Test names added in `cohort_migration_contract_test.exs`: `flash renders Cohort alert semantics without daisyUI or Heroicon classes`, `CoreComponents source has retired flash and button scaffold literals`.
- Later phase artifacts in this plan set: layout wrapper removal, dead generator file deletion, `default.css` link removal, and `default.css` deletion.

## Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | - | daisyUI/Tailwind scaffold gone from inner pages and demo grep-clean | 101-01..101-04 | COVERED | Flash first, layout/dead files second, source/render gate third, asset deletion last. |
| REQ | COHORT-05 | Retire daisyUI/Tailwind scaffold class-by-class while preserving behavior contracts and removing `default.css` only once grep is clean | 101-01..101-04 | COVERED | Every plan frontmatter carries COHORT-05. |
| RESEARCH | - | Inline flash/error icons before deleting `default.css` | 101-01 | COVERED | Implements D-101-03. |
| RESEARCH | - | Token-only `.ck-flash` / `.ck-alert`; no token value or `tokens.json` edit | 101-01 | COVERED | Implements D-101-04 and UI-SPEC. |
| RESEARCH | - | Full render/source/file ratchet in existing ExUnit module | 101-01, 101-03, 101-04 | COVERED | Extended in layers so tests do not intentionally stay red between waves. |
| RESEARCH | - | Destructive `default.css` delete last | 101-04 | COVERED | Wave 3 depends on the clean source/render gate. |
| CONTEXT | D-101-01 | Delete dead generator landing files | 101-02 | COVERED | No migration or exclusion. |
| CONTEXT | D-101-02 | Rename/update misnamed launchpad controller test if included | 101-02 | COVERED | Existing `launchpad_live_test.exs` is updated; misnamed controller test is removed. |
| CONTEXT | D-101-03 | Inline three needed icons; no Heroicon CSS mask/data-url dependency | 101-01 | COVERED | Private inline SVG helpers replace local icon calls. |
| CONTEXT | D-101-04 | Add token-only `.ck-flash` / `.ck-alert` family | 101-01 | COVERED | CSS task creates selectors. |
| CONTEXT | D-101-05 | Wrap flash in `.ck ck-flash` | 101-01 | COVERED | Flash markup task. |
| CONTEXT | D-101-06 | Preserve Phoenix flash contract while swapping class/icon layer | 101-01 | COVERED | Attrs, fallback, and keyed manual dismiss stay. |
| CONTEXT | D-101-07 | Split info/error ARIA semantics and no auto-dismiss | 101-01 | COVERED | Tests assert `status`/polite and `alert`/assertive. |
| CONTEXT | D-101-08 | Terse factual flash posture | 101-01 | COVERED | No invented notification copy; tests use factual examples only. |
| CONTEXT | D-101-09 | Delete `Layouts.app` Tailwind wrapper | 101-02 | COVERED | Layout task. |
| CONTEXT | D-101-10 | Promote existing retirement gate | 101-01, 101-03, 101-04 | COVERED | Flash assertions here, composed/source/file assertions later. |
| CONTEXT | D-101-11 | Migrate first, gate, remove link, delete asset last | 101-01..101-04 | COVERED | Wave ordering enforces it. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add flash retirement assertions</name>
  <files>examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs</files>
  <read_first>
    - .planning/phases/101-daisyui-retirement-track-b/101-CONTEXT.md
    - .planning/phases/101-daisyui-retirement-track-b/101-VALIDATION.md
    - .planning/phases/101-daisyui-retirement-track-b/101-UI-SPEC.md
    - examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs
    - examples/adoption_demo/lib/adoption_demo_web/components/core_components.ex
  </read_first>
  <behavior>
    - Info flash render contains `class="ck ck-flash"`, `.ck-alert--info`, `role="status"`, `aria-live="polite"`, inline SVG markup, and `aria-label="Close notification"`.
    - Error flash render contains `.ck-alert--error`, `role="alert"`, `aria-live="assertive"`, inline SVG markup, and keyed manual flash clearing.
    - Flash render and `core_components.ex` source assertions reject the locked daisyUI and Heroicon flash/button scaffold literals from D-101-03, D-101-07, and D-101-10.
  </behavior>
  <action>Add two `Phoenix.LiveViewTest.render_component/2`-based tests in the existing `AdoptionDemoWeb.CohortMigrationContractTest` module for `AdoptionDemoWeb.CoreComponents.flash/1`, one for `kind: :info` and one for `kind: :error`. Add a source-read test for `core_components.ex` that targets only the locked Phase 101 flash/icon/button scaffold literals; do not broaden it to every unused Phoenix generator helper. Keep all banned class checks anchored to class attributes or exact old helper names so `.ck-btn`, `.ck-tab`, `.ck-tabs`, and `.ck-grid` cannot false-fail.</action>
  <verify>
    <automated>cd examples/adoption_demo &amp;&amp; mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Targeted ExUnit includes explicit info and error flash render tests.
    - Targeted ExUnit includes a source-read test against `core_components.ex`.
    - The assertions prove the new `.ck-*` flash contract and the absence of the locked old flash/icon/button scaffold literals.
  </acceptance_criteria>
  <done>Flash-specific COHORT-05 assertions exist in the merge-blocking adoption-demo unit lane.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move CoreComponents flash and error paths onto Cohort markup</name>
  <files>examples/adoption_demo/lib/adoption_demo_web/components/core_components.ex</files>
  <read_first>
    - .planning/phases/101-daisyui-retirement-track-b/101-CONTEXT.md
    - .planning/phases/101-daisyui-retirement-track-b/101-UI-SPEC.md
    - .planning/phases/96-cohort-component-layer-dark-reduced-motion-contract-track-b/96-CONTEXT.md
    - examples/adoption_demo/lib/adoption_demo_web/components/core_components.ex
    - examples/adoption_demo/lib/adoption_demo_web/components/cohort_components.ex
  </read_first>
  <behavior>
    - `flash/1` preserves `attr :kind`, `attr :flash`, `attr :title`, `attr :rest`, the inner-block fallback, `Phoenix.Flash.get/2`, and keyed manual dismiss.
    - `flash/1` emits `class="ck ck-flash"` on the outer flash container per D-101-05, and `.ck-alert` / `.ck-alert--info` / `.ck-alert--error` inside it per D-101-04.
    - `:info` uses `role="status"` and `aria-live="polite"`; `:error` uses `role="alert"` and `aria-live="assertive"` per D-101-07.
    - `button/1` defaults no longer contain the old generator button classes named in D-101-10; use existing `.ck-btn` and `.ck-btn--primary` defaults without adding CSS.
  </behavior>
  <action>Update `CoreComponents.flash/1` to use Cohort alert classes, the split ARIA semantics, a real 44px-target close button labelled exactly `Close notification`, and `Phoenix.LiveView.JS.push("lv:clear-flash", value: %{key: @kind})` or an equivalent keyed manual dismiss path that does not rely on Tailwind transition utility classes. Add private inline-SVG helper components `notification_icon/1` and `dismiss_icon/1` using `currentColor` and `aria-hidden="true"` for the information, exclamation, and close glyphs. Update private `error/1` to render `.ck-error` plus the inline error SVG instead of the old icon class. Remove local calls to the generic Heroicon `icon/1`; delete `icon/1`, `show/2`, and `hide/2` if no local call remains. Refresh the module docs so they do not describe the demo as Tailwind/daisyUI/Heroicon-backed. Keep HEEx interpolation escaped and do not introduce `raw/1`.</action>
  <verify>
    <automated>cd examples/adoption_demo &amp;&amp; mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `CoreComponents.flash/1` keeps the Phoenix flash API and manual clear behavior.
    - `CoreComponents.flash/1` and private `error/1` use inline SVG and Cohort classes only.
    - `CoreComponents.button/1` default classes are `.ck-btn` / `.ck-btn ck-btn--primary`, with no old generator button class literals.
    - No `raw(` token is introduced in `core_components.ex`.
  </acceptance_criteria>
  <done>Rendered flash/error paths no longer depend on `default.css` for styling or glyphs.</done>
</task>

<task type="auto">
  <name>Task 3: Add token-only Cohort flash CSS</name>
  <files>examples/adoption_demo/priv/static/assets/cohort.css</files>
  <read_first>
    - .planning/phases/101-daisyui-retirement-track-b/101-CONTEXT.md
    - .planning/phases/101-daisyui-retirement-track-b/101-UI-SPEC.md
    - .planning/phases/96-cohort-component-layer-dark-reduced-motion-contract-track-b/96-CONTEXT.md
    - .planning/phases/94-foundation-token-pipeline-ci-gate-new-token-categories/94-CONTEXT.md
    - examples/adoption_demo/priv/static/assets/cohort.css
  </read_first>
  <action>Add the `.ck-flash` / `.ck-alert` selector family to `cohort.css` near the existing form/error or motion-adjacent component rules. Use only existing `--ck-*` tokens plus local `--_accent`; do not edit token values, `tokens.json`, or any generator. `.ck-flash` is a fixed top-end container above `.ck-nav` with a responsive viewport-safe width. `.ck-alert` uses `--ck-surface`, `--ck-border`, `--ck-radius`, `--ck-shadow-lg`, and a 3px left border accent. `.ck-alert--info` maps the accent to `--ck-info`; `.ck-alert--error` maps it to `--ck-quarantine`. `.ck-alert__dismiss` has a stable 44px minimum target and inherits the `.ck :focus-visible` ring. Entrance motion may reuse existing `ck-rise` only inside `prefers-reduced-motion: no-preference`; do not add timers, decorative animation, mask rules, data-url icon rules, hex/rgb/named colors outside existing token blocks, or new per-state surface tokens.</action>
  <verify>
    <automated>node brandbook/src/cohort-contrast.mjs</automated>
    <automated>cd examples/adoption_demo &amp;&amp; mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `cohort.css` contains the complete `.ck-flash` / `.ck-alert` selector family named in the UI-SPEC.
    - `node brandbook/src/cohort-contrast.mjs` exits 0, preserving the Cohort literal/color scanner.
    - No `tokens.json`, admin CSS, `admin-polish.js`, package dependency, or build-step file is changed.
  </acceptance_criteria>
  <done>The new flash primitive is present, token-only, and scanner-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Phoenix flash/session -> HEEx DOM | Flash text becomes user-visible notification content. |
| Notification DOM -> assistive technology | ARIA live roles can interrupt screen reader users. |
| Static CSS -> browser rendering | New `.ck-*` CSS affects presentation only; no auth, access-control, storage, or persistence path is added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-101-01 | Tampering / XSS | `CoreComponents.flash/1` | mitigate | Keep escaped HEEx interpolation, add no `raw/1`, and preserve source test refuting `raw(` in rendered pages. |
| T-101-02 | Information Disclosure | Flash text | mitigate | Do not add sensitive identifiers/secrets to default flash copy; keep only caller-provided user-facing flash text. |
| T-101-03 | Denial of Service / UX interruption | ARIA live notifications | mitigate | Info uses polite `status`; error uses assertive `alert`; no auto-dismiss timer and no focus steal. |
| T-101-AUTH | Spoofing / Elevation | Auth and access control | accept | N/A: no routes, auth checks, permissions, sessions, or storage behavior change in this plan. |
| T-101-SC | Tampering | npm/pip/cargo installs | accept | N/A: no package install task and research package audit is N/A. |
</threat_model>

<verification>
Plan-level checks:
- `cd examples/adoption_demo && mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs`
- `node brandbook/src/cohort-contrast.mjs`
</verification>

<success_criteria>
COHORT-05 flash/alert scope is complete when rendered flash has Cohort classes, inline SVG icons, split ARIA semantics, keyed manual dismissal, and the token-only CSS scanner remains green.
</success_criteria>

<output>
Create `.planning/phases/101-daisyui-retirement-track-b/101-01-SUMMARY.md` when done.
</output>

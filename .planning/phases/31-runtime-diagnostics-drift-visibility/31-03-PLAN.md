---
phase: 31-runtime-diagnostics-drift-visibility
plan: 03
type: execute
wave: 2
depends_on:
  - 31-01
  - 31-02
files_modified:
  - guides/background_processing.md
  - guides/operations.md
  - guides/troubleshooting.md
  - lib/rindle/ops/lifecycle_repair.ex
  - lib/rindle/ops/runtime_checks.ex
  - lib/rindle/ops/runtime_status.ex
  - test/install_smoke/docs_parity_test.exs
  - test/rindle/contracts/telemetry_contract_test.exs
autonomous: true
requirements:
  - DIAG-03
must_haves:
  truths:
    - "Repair flows, runtime refusals, and runtime drift checks emit one stable additive telemetry layer with low-cardinality metadata only."
    - "Telemetry docs, operator docs, and contract tests agree on the same public events, metadata keys, and operator meaning."
    - "The docs teach the Phase 31 split explicitly: doctor validates setup and drift, runtime status reports degradation, and existing repair verbs perform change."
    - "No new dashboard, auto-remediation lane, or ad hoc event family is introduced."
  artifacts:
    - path: "test/rindle/contracts/telemetry_contract_test.exs"
      provides: "Frozen public allowlist for additive repair/runtime telemetry and guide parity assertions."
      contains: "@public_events"
    - path: "guides/background_processing.md"
      provides: "Canonical public telemetry guide covering the Phase 31 additive families."
      contains: "[:rindle, :repair"
    - path: "test/install_smoke/docs_parity_test.exs"
      provides: "Docs parity coverage for doctor/runtime-status/repair verb split."
      contains: "runtime_status"
  key_links:
    - from: "test/rindle/contracts/telemetry_contract_test.exs"
      to: "guides/background_processing.md"
      via: "public telemetry allowlist and metadata keys are locked to guide text"
      pattern: "runtime|repair|@public_events"
    - from: "guides/operations.md"
      to: "guides/troubleshooting.md"
      via: "operator docs teach doctor, runtime-status, and repair verbs with matching vocabulary and boundaries"
      pattern: "doctor|runtime_status|reprobe|requeue|regenerate|cleanup|sweep"
---

<objective>
Freeze the public telemetry and docs contract after the doctor and runtime
status surfaces exist.

Purpose: satisfy DIAG-03 by adding the smallest stable telemetry layer for
repair/runtime observability, then lock it to docs and contract tests so
operators can build alerts or dashboards without guessing.
Output: additive public telemetry events, updated background-processing and
operator guides, and parity/contract tests that prevent drift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/31-runtime-diagnostics-drift-visibility/31-CONTEXT.md
@.planning/phases/31-runtime-diagnostics-drift-visibility/31-RESEARCH.md
@.planning/phases/31-runtime-diagnostics-drift-visibility/31-PATTERNS.md
@guides/background_processing.md
@guides/operations.md
@guides/troubleshooting.md
@test/rindle/contracts/telemetry_contract_test.exs
@test/install_smoke/docs_parity_test.exs

<interfaces>
From .planning/phases/31-runtime-diagnostics-drift-visibility/31-CONTEXT.md:
```text
D-19 through D-25: add a narrow telemetry layer only, keep metadata low-cardinality, do not model cancellation as an error, and do not bless one-off event families.
D-26 through D-28: primary surface remains Rindle + mix; docs must keep doctor, runtime status, and repair verbs distinct.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze the additive repair/runtime telemetry contract in code and contract tests</name>
  <files>lib/rindle/ops/lifecycle_repair.ex, lib/rindle/ops/runtime_checks.ex, lib/rindle/ops/runtime_status.ex, test/rindle/contracts/telemetry_contract_test.exs</files>
  <behavior>
    - Public telemetry extends the existing allowlist additively instead of redesigning it.
    - Repair, runtime refusal, and runtime check events carry only low-cardinality metadata.
    - Cancellation remains an operator-visible terminal outcome without being emitted as an exception-class failure.
  </behavior>
  <action>After Plans 31-01 and 31-02 land, add the narrow Phase 31 telemetry families required by D-19 through D-25: `[:rindle, :repair, :start|:stop|:exception]`, `[:rindle, :runtime, :refusal]`, and `[:rindle, :runtime, :check, :stop]` with `:exception` only if the check layer truly needs a distinct alertable failure event. Emit them from the hidden repair and runtime modules, not from the guides or ad hoc helpers. Keep metadata low-cardinality exactly along the locked dimensions (`operation`, `scope`, `result`, `dry_run`, `surface`, `reason`, `mode`, `check`, `status`, `component`), avoid IDs/raw error text per D-23, keep cancellation out of exception semantics per D-24, and update the telemetry contract test allowlist plus metadata assertions accordingly.</action>
  <verify>
    <automated>mix test test/rindle/contracts/telemetry_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The runtime emits one additive, test-locked telemetry layer for repair and diagnostics, with low-cardinality metadata and no accidental public event-family sprawl.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock docs and docs-parity tests to the doctor/runtime-status/repair split</name>
  <files>guides/background_processing.md, guides/operations.md, guides/troubleshooting.md, test/install_smoke/docs_parity_test.exs, test/rindle/contracts/telemetry_contract_test.exs</files>
  <behavior>
    - The docs teach doctor as setup/drift validation, runtime status as reporting, and repair verbs as the only mutating lanes.
    - Background-processing telemetry docs match the exact public allowlist and metadata families.
    - Install-smoke parity catches drift in public operator guidance.
  </behavior>
  <action>Update the operator and telemetry guides to match the implemented Phase 31 surfaces per D-26 through D-28. `guides/background_processing.md` should document the additive runtime/repair telemetry families and point readers back to `test/rindle/contracts/telemetry_contract_test.exs` as the source of truth. `guides/operations.md` and `guides/troubleshooting.md` should explicitly teach: doctor validates setup and drift, runtime status reports degraded or stuck work, and the existing verbs `reprobe`, `requeue`, `regenerate`, `cleanup`, and `sweep` perform change. Extend docs-parity tests so the public docs mention `mix rindle.runtime_status`, preserve the no-dashboard/no-auto-remediation posture, and keep the Phase 31 operator vocabulary stable.</action>
  <verify>
    <automated>mix test test/install_smoke/docs_parity_test.exs test/rindle/contracts/telemetry_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The public docs, telemetry guide, and parity tests all agree on the Phase 31 runtime diagnostics contract and keep mutation confined to the existing explicit repair verbs.</done>
</task>

</tasks>

<verification>
Run `mix test test/install_smoke/docs_parity_test.exs test/rindle/contracts/telemetry_contract_test.exs --warnings-as-errors`.
</verification>

<success_criteria>
DIAG-03 is satisfied when repair/runtime telemetry is frozen as an additive
public contract with low-cardinality metadata, and the docs plus parity tests
teach the exact same Phase 31 operator split without inventing dashboards,
auto-remediation, or new repair verbs.
</success_criteria>

<output>
After completion, create `.planning/phases/31-runtime-diagnostics-drift-visibility/31-03-SUMMARY.md`
</output>

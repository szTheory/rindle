---
phase: 31-runtime-diagnostics-drift-visibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/rindle.runtime_status.ex
  - lib/rindle.ex
  - lib/rindle/ops/runtime_status.ex
  - lib/rindle/workers/process_variant.ex
  - test/rindle/api_surface_boundary_test.exs
  - test/rindle/ops/runtime_status_test.exs
  - test/rindle/runtime_status_task_test.exs
autonomous: true
requirements:
  - DIAG-02
must_haves:
  truths:
    - "Operators can inspect stuck or failed assets, variants, and upload sessions through one public `Rindle` report API plus one Mix-task wrapper."
    - "Runtime status stays read-only and bounded: it reports counts, oldest ages, and a limited sample, not an unbounded row dump or generic query DSL."
    - "Stuck-work classification uses persisted lifecycle state plus Oban/runtime corroboration, and does not mark healthy retries or first-failure work as stuck."
    - "`probe_drift` remains a bounded suggestion class based on clear persisted inconsistencies, not a broad null-field audit."
    - "The public API stays narrow on `Rindle`, while query and classification mechanics remain hidden under `Rindle.Ops.*`."
  artifacts:
    - path: "lib/rindle.ex"
      provides: "Public structured runtime-status facade entrypoint."
      contains: "def runtime_status"
    - path: "lib/rindle/ops/runtime_status.ex"
      provides: "Hidden bounded query/report service for lifecycle findings and recommendations."
      contains: "def runtime_status"
    - path: "lib/mix/tasks/rindle.runtime_status.ex"
      provides: "Operator-facing text/json runtime status wrapper."
      contains: "defmodule Mix.Tasks.Rindle.RuntimeStatus"
  key_links:
    - from: "lib/rindle.ex"
      to: "lib/rindle/ops/runtime_status.ex"
      via: "public facade delegates to hidden bounded reporting service"
      pattern: "runtime_status"
    - from: "lib/rindle/ops/runtime_status.ex"
      to: "lib/rindle/workers/process_variant.ex"
      via: "status heuristics reuse queue and timeout realities instead of inventing unrelated thresholds"
      pattern: "rindle_process|rindle_media|timeout"
---

<objective>
Ship the Phase 31 status-reporting surface without widening Rindle into a query
DSL or control plane.

Purpose: deliver DIAG-02 as one public structured report API plus one Mix-task
wrapper, with all reporting logic hidden under `Rindle.Ops.*` and bounded for
operator use.
Output: `Rindle.runtime_status/1`, `mix rindle.runtime_status`, classification
heuristics for stuck/degraded work, and boundary/tests that freeze the public
versus hidden split.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/31-runtime-diagnostics-drift-visibility/31-CONTEXT.md
@.planning/phases/31-runtime-diagnostics-drift-visibility/31-RESEARCH.md
@.planning/phases/31-runtime-diagnostics-drift-visibility/31-PATTERNS.md
@lib/rindle.ex
@lib/rindle/workers/process_variant.ex
@lib/mix/tasks/rindle.verify_storage.ex
@test/rindle/api_surface_boundary_test.exs

<interfaces>
From .planning/phases/31-runtime-diagnostics-drift-visibility/31-CONTEXT.md:
```text
D-06 through D-10: the canonical surface is a public `Rindle` report API plus `mix rindle.runtime_status`, with hidden `Rindle.Ops.*` internals and bounded filters only.
D-11 through D-18: preserve lifecycle vocabulary, use persisted state plus Oban/runtime corroboration, and report bounded counts/ages/examples instead of a row dump.
```

From lib/rindle/workers/process_variant.ex:
```elixir
@av_queue :rindle_media
@av_timeout_ms :timer.minutes(10)
@unique_states [:available, :scheduled, :executing, :retryable]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the public runtime-status facade and hidden bounded reporting service</name>
  <files>lib/rindle.ex, lib/rindle/ops/runtime_status.ex, lib/rindle/workers/process_variant.ex, test/rindle/ops/runtime_status_test.exs</files>
  <behavior>
    - The public contract is one `{:ok, report}` API with bounded operator filters and stable report sections.
    - Classification distinguishes `failed_work`, `cancelled_work`, `queue_starved`, `orphan_suspect`, and other locked classes using persisted state plus Oban corroboration.
    - Healthy retryable or first-failure work is not misclassified as stuck.
  </behavior>
  <action>Add `Rindle.runtime_status/1` as the public facade entrypoint per D-06 through D-10 and keep the implementation in hidden `Rindle.Ops.RuntimeStatus`. Validate only bounded filters such as `profile`, `older_than`, `limit`, and `format` per D-10; reject unknown filter keys loudly instead of inventing a generic query DSL. Build a stable report shape with sections for runtime checks, assets, variants, upload sessions, and recommendations, and implement the locked Phase 31 classes from D-11 through D-18 using persisted lifecycle state plus `oban_jobs` and queue/timeout realities from `Rindle.Workers.ProcessVariant`. Keep `probe_drift` bounded to clear persisted inconsistencies such as kind/content-type mismatch or missing required probe-owned AV fields on otherwise available/ready rows; do not widen it into a broad null-field audit. If a small helper is needed from `ProcessVariant` to keep thresholds truthful, expose only the minimum hidden seam needed for report heuristics.</action>
  <verify>
    <automated>mix test test/rindle/ops/runtime_status_test.exs --warnings-as-errors</automated>
  </verify>
  <done>`Rindle.runtime_status/1` exists as a public, bounded, read-only report API backed by one hidden classification service with tests for the locked status classes and thresholds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add `mix rindle.runtime_status` with deterministic text/json output and boundary coverage</name>
  <files>lib/mix/tasks/rindle.runtime_status.ex, test/rindle/api_surface_boundary_test.exs, test/rindle/runtime_status_task_test.exs, test/rindle/ops/runtime_status_test.exs</files>
  <behavior>
    - Operators get human-friendly text output first, plus JSON output for automation.
    - The Mix task remains read-only and bounded; it surfaces recommendations that point only to existing repair verbs.
    - The public/hidden module boundary stays explicit in docs compilation.
  </behavior>
  <action>Create `Mix.Tasks.Rindle.RuntimeStatus` as the operator entrypoint required by D-07 and D-27. Support deterministic `--format text|json` output, bounded samples, explicit exit behavior for invalid filters/format, and recommendation text that points only at existing verbs such as `reprobe`, `requeue`, `regenerate`, `cleanup`, or `sweep` per D-28. Extend `test/rindle/api_surface_boundary_test.exs` so the new Mix task and `Rindle.runtime_status/1` stay public while `Rindle.Ops.RuntimeStatus` remains hidden, and add task-level tests that freeze the text/json contract.</action>
  <verify>
    <automated>mix test test/rindle/api_surface_boundary_test.exs test/rindle/runtime_status_task_test.exs test/rindle/ops/runtime_status_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The repo exposes one public runtime status API and one public Mix wrapper with deterministic text/json output, while the reporting/query internals remain hidden under `Rindle.Ops.*`.</done>
</task>

</tasks>

<verification>
Run `mix test test/rindle/api_surface_boundary_test.exs test/rindle/ops/runtime_status_test.exs test/rindle/runtime_status_task_test.exs --warnings-as-errors`.
</verification>

<success_criteria>
DIAG-02 is satisfied when operators can inspect stuck or degraded lifecycle work
through one public `Rindle.runtime_status/1` API and `mix rindle.runtime_status`
wrapper, with bounded filters, truthful classification, deterministic text/json
output, and no mutation or query-DSL widening.
</success_criteria>

<output>
After completion, create `.planning/phases/31-runtime-diagnostics-drift-visibility/31-02-SUMMARY.md`
</output>

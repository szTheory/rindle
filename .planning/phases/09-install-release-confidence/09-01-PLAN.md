---
phase: 09-install-release-confidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/install_smoke/generated_app_smoke_test.exs
  - test/install_smoke/support/generated_app_helper.ex
  - scripts/install_smoke.sh
autonomous: true
requirements:
  - RELEASE-01
must_haves:
  truths:
    - A fresh `mix phx.new` app can install Rindle from the built artifact and compile without repo-local dependency assumptions.
    - The generated consumer app can run host-app migrations and Rindle migrations explicitly, then complete presigned PUT, verify/promote, and signed delivery.
    - The package-consumer proof uses the narrow canonical path only: adopter-owned Repo, default Oban, explicit host plus Rindle migrations, presigned PUT, verify/promote, signed delivery.
  artifacts:
    - path: test/install_smoke/generated_app_smoke_test.exs
      provides: Fresh-app smoke proof for the canonical package-consumer lifecycle
    - path: test/install_smoke/support/generated_app_helper.ex
      provides: Shared consumer-app generation, config injection, migration-path resolution, and lifecycle assertions
    - path: scripts/install_smoke.sh
      provides: Reusable entrypoint for local, CI, and release smoke execution
  key_links:
    - from: scripts/install_smoke.sh
      to: test/install_smoke/generated_app_smoke_test.exs
      via: Shared smoke runner invocation
      pattern: mix test test/install_smoke/generated_app_smoke_test.exs
    - from: test/install_smoke/support/generated_app_helper.ex
      to: Application.app_dir(:rindle, "priv/repo/migrations")
      via: Explicit library migration path resolution
      pattern: Application\\.app_dir\\(:rindle, "priv/repo/migrations"\\)
    - from: test/install_smoke/generated_app_smoke_test.exs
      to: Rindle.Upload.Broker and Rindle.Delivery
      via: Canonical public API lifecycle assertions
      pattern: Broker\\.(initiate_session|sign_url|verify_completion)|Rindle\\.Delivery\\.url
---

<objective>
Establish the package-consumer proof harness that RELEASE-01 depends on: a real `mix phx.new` app installs Rindle from the built artifact, runs explicit host-app plus Rindle migrations, and completes the narrow canonical presigned PUT lifecycle.

Purpose: close the outside-in trust gap with the primary consumer-shaped proof required by D-01, D-02, D-03, D-07, and D-08.
Output: reusable generated-app smoke test infrastructure plus a shared runner script that later CI and release plans consume unchanged.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-install-release-confidence/09-CONTEXT.md
@.planning/phases/09-install-release-confidence/09-RESEARCH.md
@.planning/phases/09-install-release-confidence/09-VALIDATION.md
@.planning/phases/09-install-release-confidence/09-PATTERNS.md
@mix.exs
@test/adopter/canonical_app/lifecycle_test.exs
@test/adopter/canonical_app/profile.ex

<interfaces>
From test/adopter/canonical_app/profile.ex:
```elixir
defmodule Rindle.Adopter.CanonicalApp.Profile do
  use Rindle.Profile,
    storage: Rindle.Storage.S3,
    variants: [thumb: [mode: :fit, width: 64, height: 64]],
    allow_mime: ["image/png", "image/jpeg"],
    max_bytes: 10_485_760
end
```

From test/adopter/canonical_app/lifecycle_test.exs:
```elixir
{:ok, session} = Broker.initiate_session(AdopterProfile, filename: "adopter.png")
{:ok, %{session: signed, presigned: presigned}} = Broker.sign_url(session.id)
:ok = put_to_presigned_url(presigned.url, @png_1x1)
{:ok, %{session: completed, asset: asset}} = Broker.verify_completion(session.id)
{:ok, signed_url} = Rindle.Delivery.url(AdopterProfile, asset.storage_key)
```

From mix.exs:
```elixir
defp package do
  [
    files: ~w(lib priv/repo/migrations mix.exs README.md LICENSE)
  ]
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the generated-app smoke harness and contract surface</name>
  <files>test/install_smoke/generated_app_smoke_test.exs, test/install_smoke/support/generated_app_helper.ex</files>
  <behavior>
    - Test 1: a generated Phoenix app installs Rindle from the unpacked package artifact and boots with adopter-owned Repo plus default Oban wiring per D-01, D-02, and D-03.
    - Test 2: the helper resolves `Application.app_dir(:rindle, "priv/repo/migrations")` and runs host-app plus Rindle migrations explicitly, preventing `mix ecto.migrate` drift per D-05 and research.
    - Test 3: the smoke path proves only presigned PUT, verify/promote, and signed delivery through public APIs per D-07 and must reject hidden repo-local assumptions like `deps/rindle`.
  </behavior>
  <action>Create `test/install_smoke/generated_app_smoke_test.exs` as the primary RELEASE-01 proof lane and `test/install_smoke/support/generated_app_helper.ex` as its only setup seam. Per D-01 and D-03, the helper must generate a real `mix phx.new` app in a temp directory, patch only the minimum adopter-owned files, add `oban`, `hackney`, and the unpacked Rindle artifact dependency, and seed a consumer profile that mirrors the canonical adopter shape without importing repo-local fixture modules. Per D-05, keep the assertions narrow: build artifact install, config injection, explicit host-app plus Rindle migration execution, `Broker.initiate_session`, `Broker.sign_url`, real HTTP PUT to the presigned URL, `Broker.verify_completion`, synchronous worker execution through default Oban test helpers, and `Rindle.Delivery.url`. Per the migration-path research and the user constraint, the helper must resolve library migrations with `Application.app_dir(:rindle, "priv/repo/migrations")` and run multi-path migrations explicitly rather than assuming `mix ecto.migrate` is enough or copying migrations into the host app.</action>
  <verify>
    <automated>mix test test/install_smoke/generated_app_smoke_test.exs --include minio</automated>
  </verify>
  <done>The generated-app smoke test fails on missing packaged files, missing repo/oban wiring, incorrect migration-path resolution, or any break in the canonical presigned PUT lifecycle, and it does so without relying on repo-local consumer shortcuts.</done>
</task>

<task type="auto">
  <name>Task 2: Add the shared smoke runner used by local, CI, and release flows</name>
  <files>scripts/install_smoke.sh</files>
  <action>Create `scripts/install_smoke.sh` as the single shell entrypoint that builds and unpacks the package, prepares any expected temp workspace inputs, and runs the generated-app smoke test from Task 1. Per D-04, D-05, and D-06, this runner must stay intentionally narrow and become the shared logic that later workflow plans call instead of duplicating packaging or smoke setup inline. Keep the script opinionated about failure: `set -euo pipefail`, explicit artifact path handling, and no hidden fallback to the repo checkout as the dependency source.</action>
  <verify>
    <automated>bash scripts/install_smoke.sh</automated>
  </verify>
  <done>A single script can be invoked locally or by GitHub Actions to prove the built-artifact consumer smoke path, and its implementation never swaps back to repo-source installation when the packaged artifact is broken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo checkout -> generated consumer app | build-time artifacts and helper patches cross into a fresh host-app shape |
| generated consumer app -> PostgreSQL migrations | schema installation can fail or drift if host and library migrations are not both applied |
| generated consumer app -> object storage | untrusted network upload path crosses through presigned PUT before verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | T | generated_app_smoke_test.exs | mitigate | Require the smoke proof to execute presigned PUT plus verify/promote/delivery, not just compile/install, per RELEASE-01 and D-05. |
| T-09-02 | D | generated_app_helper.ex | mitigate | Resolve library migrations with `Application.app_dir(:rindle, "priv/repo/migrations")` and run explicit multi-path migrations so package path layout changes cannot break schema install silently. |
| T-09-03 | I | generated_app_helper.ex | mitigate | Keep secrets and provider credentials in env-driven runtime config only; do not commit generated app config files containing live credentials. |
</threat_model>

<verification>
Run `mix test test/install_smoke/generated_app_smoke_test.exs --include minio` after each code-producing task. Before closing the plan, run `bash scripts/install_smoke.sh` to prove the shared runner exercises the same built-artifact consumer path end to end.
</verification>

<success_criteria>
- `RELEASE-01` has a primary proof lane rooted in a fresh `mix phx.new` app, not only the in-repo adopter fixture.
- The smoke helper uses explicit multi-path migration execution with `Application.app_dir/2` and fails loudly if library migrations are missing.
- The only lifecycle proved here is the narrow presigned PUT canonical path; multipart stays outside this smoke plan.
</success_criteria>

<output>
After completion, create `.planning/phases/09-install-release-confidence/09-01-SUMMARY.md`
</output>

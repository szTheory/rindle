---
phase: 09-install-release-confidence
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - README.md
  - guides/getting_started.md
  - test/install_smoke/docs_parity_test.exs
autonomous: true
requirements:
  - RELEASE-03
must_haves:
  truths:
    - README teaches the same first-run presigned PUT path that the built-artifact smoke test proves.
    - The getting-started guide remains the canonical deep guide and explicitly documents adopter-owned Repo, default Oban ownership, and library migration setup.
    - Docs stay capability-honest: presigned PUT is first-run, multipart is advanced and proven elsewhere.
  artifacts:
    - path: README.md
      provides: Layered quickstart entrypoint
    - path: guides/getting_started.md
      provides: Canonical deep adopter guide aligned to the smoke path
    - path: test/install_smoke/docs_parity_test.exs
      provides: Executable docs drift gate for the canonical install path
  key_links:
    - from: README.md
      to: guides/getting_started.md
      via: Explicit quickstart-to-deep-guide handoff
      pattern: getting_started
    - from: test/install_smoke/docs_parity_test.exs
      to: README.md and guides/getting_started.md
      via: Drift-proof assertions for canonical lifecycle and setup language
      pattern: (Broker\\.initiate_session|Broker\\.verify_completion|Rindle\\.Delivery\\.url|Application\\.app_dir)
---

<objective>
Align top-level onboarding docs with the exact built-artifact consumer path and add an executable drift gate so RELEASE-03 stays honest over time.

Purpose: satisfy D-08 through D-12 by making README the quickstart, keeping the guide canonical, and preventing docs from drifting away from the smoke-tested install path.
Output: updated layered quickstart docs plus an ExUnit parity test that enforces the declared canonical path.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-install-release-confidence/09-CONTEXT.md
@.planning/phases/09-install-release-confidence/09-RESEARCH.md
@.planning/phases/09-install-release-confidence/09-VALIDATION.md
@README.md
@guides/getting_started.md
@guides/background_processing.md
@guides/storage_capabilities.md
@test/adopter/canonical_app/lifecycle_test.exs

<interfaces>
From guides/getting_started.md:
```elixir
config :rindle, :repo, MyApp.Repo
```

From guides/background_processing.md:
```elixir
config :my_app, Oban,
  repo: MyApp.Repo,
  queues: [...]
```

From test/adopter/canonical_app/lifecycle_test.exs:
```elixir
{:ok, %{session: signed, presigned: presigned}} = Broker.sign_url(session.id)
{:ok, %{session: completed, asset: asset}} = Broker.verify_completion(session.id)
{:ok, signed_url} = Rindle.Delivery.url(AdopterProfile, asset.storage_key)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README as the layered quickstart and tighten the deep guide</name>
  <files>README.md, guides/getting_started.md</files>
  <action>Rewrite `README.md` into the layered quickstart required by D-10 and D-11: short package pitch, dependency snippet, explicit `hackney` or other HTTP-client note for S3, adopter-owned Repo callout, default Oban requirement, explicit host plus Rindle migration step, presigned PUT-first lifecycle snippet, and clear handoff to `guides/getting_started.md` plus the capability/background guides. Update `guides/getting_started.md` so it remains the canonical deep guide for the same path, now corrected for the resolved migration research by documenting an adopter-facing host-app migration snippet built on `Application.app_dir(:rindle, "priv/repo/migrations")`, while the executable checked-in helper stays private to the repo smoke harness from Plan 09-01. Do not add or document a new public `mix rindle.*` install task in Phase 9; per the resolved research cut, the public path is docs snippet only and the private automation path is the checked-in smoke helper. Per D-07, D-08, and D-09, keep presigned PUT as the first-run path and describe multipart only as an advanced capability already proven elsewhere.</action>
  <verify>
    <automated>mix test test/install_smoke/docs_parity_test.exs</automated>
  </verify>
  <done>README and the getting-started guide both teach the same presigned PUT first-run story, explicitly document repo/Oban/migration ownership through the `Application.app_dir/2` docs snippet path, avoid introducing a public install Mix task, and link advanced capability guidance instead of front-loading it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add an executable docs parity gate for install-path drift</name>
  <files>test/install_smoke/docs_parity_test.exs</files>
  <behavior>
    - Test 1: README and the guide both contain the canonical public lifecycle calls and the declared quickstart-to-guide handoff.
    - Test 2: docs explicitly mention adopter-owned Repo, default Oban ownership, and explicit Rindle migration-path setup.
    - Test 3: docs do not present multipart as the default first-run path.
  </behavior>
  <action>Create `test/install_smoke/docs_parity_test.exs` as the RELEASE-03 drift gate. Reuse the repo’s existing grep-style docs checks from earlier phases, but move them into ExUnit assertions under the `test/install_smoke/` namespace so they run locally and in CI alongside the consumer smoke work. The parity test should assert the canonical lifecycle calls, the repo/Oban/migration constraints, the README-to-guide handoff, and capability-honest wording per D-09 through D-12.</action>
  <verify>
    <automated>mix test test/install_smoke/docs_parity_test.exs</automated>
  </verify>
  <done>Docs drift now breaks an executable test instead of relying on manual review, and the assertions are anchored to the same first-run path the smoke proof establishes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README quickstart -> adopter implementation | users copy setup and lifecycle instructions directly from docs into a host app |
| docs parity test -> public narrative | executable assertions decide whether docs still match the proved install path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | T | README.md, guides/getting_started.md | mitigate | Make the quickstart and deep guide assert the same presigned PUT path, repo ownership, default Oban, and explicit Rindle migration setup, then gate drift with ExUnit. |
| T-09-08 | D | docs_parity_test.exs | mitigate | Assert that multipart remains advanced-only wording so docs cannot silently widen the first-run surface beyond the narrow smoke proof. |
| T-09-09 | A | guides/getting_started.md | mitigate | Document the explicit multi-path migration requirement so adopters do not hit runtime failures after following incomplete install steps. |
</threat_model>

<verification>
Run `mix test test/install_smoke/docs_parity_test.exs` after each docs or parity-test change. Before closing the plan, re-read README and the guide together to confirm the quickstart-to-guide handoff and capability-honest wording are both explicit.
</verification>

<success_criteria>
- `RELEASE-03` is enforced by executable parity checks, not only prose review.
- README is now the short, honest quickstart and the guide is the declared canonical deep path.
- Docs teach explicit migration-path handling and keep multipart in the advanced lane.
</success_criteria>

<output>
After completion, create `.planning/phases/09-install-release-confidence/09-03-SUMMARY.md`
</output>

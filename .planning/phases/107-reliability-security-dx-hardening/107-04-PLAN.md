---
phase: 107-reliability-security-dx-hardening
plan: 04
type: execute
wave: 2
depends_on: ["107-02"]
files_modified:
  - scripts/ci/e2e_local.sh
  - .github/workflows/ci.yml
  - examples/adoption_demo/package.json
  - brandbook/src/contrast-constants.mjs
  - brandbook/src/admin-gallery-check.mjs
  - brandbook/src/contrast.mjs
  - brandbook/src/admin-contrast.mjs
  - brandbook/src/cohort-contrast.mjs
  - brandbook/src/admin-design-system-data.mjs
autonomous: true
requirements: [HARD-04]

must_haves:
  truths:
    - "The adoption-demo E2E lane in ci.yml AND `scripts/ci/e2e_local.sh` both run against the SAME pinned `mcr.microsoft.com/playwright:v1.57.0-noble` container (same image both sides — D-10)"
    - "`@playwright/test` is pinned EXACT to `1.57.0` (caret dropped) in examples/adoption_demo/package.json, matching the container tag (D-11)"
    - "A single `WCAG_AA_NORMAL = 4.5` constant is exported from `brandbook/src/contrast-constants.mjs` and imported by BOTH the runtime polish gate (admin-gallery-check.mjs) and the token-pair `min: 4.5` literals (D-12)"
    - "The brandbook validation test's `N/N pairs pass` strings stay byte-identical — this is a constant-extraction, not a threshold change"
  artifacts:
    - path: "brandbook/src/contrast-constants.mjs"
      provides: "the shared WCAG_AA_NORMAL = 4.5 constant"
      exports: ["WCAG_AA_NORMAL"]
    - path: "scripts/ci/e2e_local.sh"
      provides: "local faithful-repro wrapper running the same pinned container as CI"
      contains: "mcr.microsoft.com/playwright:v1.57.0-noble"
      min_lines: 20
  key_links:
    - from: "scripts/ci/e2e_local.sh"
      to: ".github/workflows/ci.yml adoption-demo-e2e lane"
      via: "both reference the same v1.57.0-noble image (faithful repro by construction)"
      pattern: "playwright:v1.57.0-noble"
    - from: "brandbook/src/admin-gallery-check.mjs"
      to: "brandbook/src/contrast-constants.mjs"
      via: "import WCAG_AA_NORMAL, replaces the `ratio < 4.5` literal"
      pattern: "WCAG_AA_NORMAL"
  prohibitions:
    - "No `lib/` file is created or modified (whole-milestone invariant)"
    - "No NON-4.5 contrast pair (e.g. a 3:1 large-text `min`) is clobbered — only `min: 4.5` literals get the shared import (Pitfall 4)"
    - "The brandbook validation test's `N/N pairs pass` assertion strings are NOT changed — the refactor keeps computed ratios + pass counts identical"
    - "The `adoption-demo-e2e` lane does NOT become a PR-required check — it stays push:main/nightly-only (106 D-04)"
    - "No action MAJOR bump; the Playwright npm version and the container tag MUST match (both 1.57.0)"
---

<objective>
Land a faithful Linux-Chromium local repro and reconcile the divergent contrast thresholds.
Move the adoption-demo E2E onto a pinned `mcr.microsoft.com/playwright:v1.57.0-noble`
container used by BOTH CI and `scripts/ci/e2e_local.sh`, pin `@playwright/test` exact to
`1.57.0`, and extract the WCAG-AA 4.5:1 threshold into one shared constant imported by both
the runtime polish gate and the token-pair gates.

Purpose: "Faithful" means same image both sides — kill the "green in CI, red locally" class
by construction (D-10). One shared contrast constant kills threshold drift between the two
gates (D-12). The E2E lane is push:main/nightly-only, so this touches no PR-critical-path
timing and adds no required check.
Output: NEW `scripts/ci/e2e_local.sh` + NEW `brandbook/src/contrast-constants.mjs` +
ci.yml container move + exact npm pin + contrast-constant import wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/107-reliability-security-dx-hardening/107-RESEARCH.md
@.planning/phases/107-reliability-security-dx-hardening/107-PATTERNS.md
@scripts/ci/adoption_demo_e2e.sh
@brandbook/src/contrast.mjs
@brandbook/src/admin-gallery-check.mjs
@examples/adoption_demo/package.json
@test/brandbook/admin_design_system_validation_test.exs
</context>

<artifacts_produced>
## Artifacts this plan produces
- NEW file: `brandbook/src/contrast-constants.mjs` — exports `WCAG_AA_NORMAL = 4.5`.
- NEW file: `scripts/ci/e2e_local.sh` — local faithful-repro wrapper (same pinned container as CI).
- EDIT: `.github/workflows/ci.yml` — `adoption-demo-e2e` lane runs against `mcr.microsoft.com/playwright:v1.57.0-noble` (container/job-level or via the script).
- EDIT: `examples/adoption_demo/package.json` — `@playwright/test` `^1.57.0` → exact `1.57.0`.
- EDIT: `brandbook/src/admin-gallery-check.mjs` — import `WCAG_AA_NORMAL`, replace the `ratio < 4.5` literal.
- EDIT: `brandbook/src/{contrast,admin-contrast,cohort-contrast}.mjs` + `admin-design-system-data.mjs` — `min: 4.5` literals → shared `WCAG_AA_NORMAL` import (only AA-normal 4.5 pairs).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Extract the shared WCAG_AA_NORMAL contrast constant + wire both gates (D-12)</name>
  <files>brandbook/src/contrast-constants.mjs, brandbook/src/admin-gallery-check.mjs, brandbook/src/contrast.mjs, brandbook/src/admin-contrast.mjs, brandbook/src/cohort-contrast.mjs, brandbook/src/admin-design-system-data.mjs</files>
  <read_first>
    - 107-RESEARCH.md §"Contrast — one shared 4.5:1 constant (D-12)" (exact divergence map: admin-gallery-check.mjs:283 runtime literal vs the `min: 4.5` token-pair literals in admin-design-system-data.mjs; verify each `min: 4.5` is AA-normal before swapping) + §"Pitfall 4"
    - 107-PATTERNS.md §"brandbook/src/contrast-constants.mjs" (the new one-constant export shape; the consumer-side edits; the test-coupling assertions to keep byte-identical)
    - test/brandbook/admin_design_system_validation_test.exs:148,169-173 (the `N/N pairs pass` + `CONSOLE_CONTRAST_PAIRS` assertions that MUST stay green)
  </read_first>
  <action>
    Create `brandbook/src/contrast-constants.mjs` exporting a single
    `export const WCAG_AA_NORMAL = 4.5;` with a header comment noting it is the WCAG 2.x AA
    minimum for normal-size text.

    Replace the runtime-gate literal in `brandbook/src/admin-gallery-check.mjs` (the
    `.filter(({ ratio }) => ratio < 4.5)` at ~:283) with an import of `WCAG_AA_NORMAL` and
    `ratio < WCAG_AA_NORMAL`.

    In the token-pair data (`brandbook/src/admin-design-system-data.mjs` `CONSOLE_CONTRAST_PAIRS`
    and the analogous base/cohort tables consumed by `contrast.mjs`/`admin-contrast.mjs`/
    `cohort-contrast.mjs`), import the constant at the top of each data module and replace ONLY
    the `min: 4.5` literals with `min: WCAG_AA_NORMAL`. VERIFY each occurrence is AA-normal
    before swapping — do NOT clobber any NON-4.5 pair (e.g. a 3:1 large-text `min`) (Pitfall 4).

    This is a constant-extraction, NOT a threshold change: computed ratios and pass counts must
    stay identical so the validation test's `N/N pairs pass` strings remain byte-identical. After
    editing, run the gates to confirm. Touch NO `lib/` file.
  </action>
  <verify>
    <automated>node brandbook/src/admin-contrast.mjs && node brandbook/src/contrast.mjs && mix test test/brandbook/admin_design_system_validation_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `brandbook/src/contrast-constants.mjs` exists and `export`s `WCAG_AA_NORMAL` = `4.5`.
    - `admin-gallery-check.mjs` imports `WCAG_AA_NORMAL` and no longer contains the bare `ratio < 4.5` literal.
    - `grep -c 'WCAG_AA_NORMAL' brandbook/src/admin-design-system-data.mjs` ≥ 1 and the `min: 4.5` literals are replaced (only AA-normal pairs).
    - `node brandbook/src/admin-contrast.mjs` + `node brandbook/src/contrast.mjs` print the SAME `N/N pairs pass` counts as before; `mix test test/brandbook/admin_design_system_validation_test.exs` passes.
  </acceptance_criteria>
  <done>One shared `WCAG_AA_NORMAL = 4.5` constant feeds both the runtime polish gate and the token-pair gates; pass counts and validation-test strings are byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Pin Playwright container + exact npm version, both CI lane and e2e_local.sh (D-10, D-11)</name>
  <files>scripts/ci/e2e_local.sh, .github/workflows/ci.yml, examples/adoption_demo/package.json</files>
  <read_first>
    - 107-RESEARCH.md §"Playwright container ↔ npm pin (D-10/D-11)" (`v1.57.0-noble` verified on MCR; `1.57.0` is the only stable 1.57.x) + §"`scripts/ci/e2e_local.sh` + ci.yml change" + §"Fonts (D-11)" (the container tag IS the font pin) + §"Open Questions" Q1 (host/container networking)
    - 107-PATTERNS.md §"scripts/ci/e2e_local.sh" (the adoption_demo_e2e.sh strict-mode/repo_root prelude + the `docker run --ipc=host` form + the ci.yml:867-877 lane this replaces)
    - scripts/ci/adoption_demo_e2e.sh (the exact analog: same lane, same demo dir, same bring-up sequence)
  </read_first>
  <action>
    Create `scripts/ci/e2e_local.sh` mirroring `scripts/ci/adoption_demo_e2e.sh`'s strict-mode
    prelude (`#!/usr/bin/env bash`, `set -euo pipefail`, `repo_root`/`demo_dir` idiom) and the
    demo bring-up sequence (deps/assets/ecto/seed). Replace the bare
    `npx playwright install --with-deps chromium` + `npm run e2e` with a `docker run` against the
    pinned image:
    `docker run --rm --ipc=host -v "$PWD:/work" -w /work/examples/adoption_demo --add-host=host.docker.internal:host-gateway mcr.microsoft.com/playwright:v1.57.0-noble sh -c "npm ci && npm run e2e"`.
    Recommended networking (RESEARCH Open-Q1): Phoenix server on host, browser-in-container against
    `host.docker.internal:<port>` via the demo's existing `ADOPTION_DEMO_BROWSER_PORT` /
    `ADOPTION_DEMO_REUSE_SERVER` knobs; finalize the exact networking flag. Make the script `chmod +x`.

    Move the `adoption-demo-e2e` job in `ci.yml` (currently `npx playwright install` on bare
    ubuntu, ~:1131-1144) onto the SAME pinned image — either a job-level `container:` key or by
    calling `scripts/ci/e2e_local.sh`. Same image both sides is the invariant (D-10). The lane
    is `needs: [quality, optional-dependencies]`, push:main/nightly-only (106 D-04) — it MUST NOT
    become a PR-required check.

    In `examples/adoption_demo/package.json`, pin `@playwright/test` exact `1.57.0` (drop the
    `^` caret) so the npm version matches the container tag (D-11). The container tag IS the font
    pin — no extra system-font install needed (same image both sides). Run `npm install` in the
    demo dir if needed to update its lockfile. Touch NO `lib/` file.
  </action>
  <verify>
    <automated>bash -n scripts/ci/e2e_local.sh && grep -q 'playwright:v1.57.0-noble' scripts/ci/e2e_local.sh && grep -q 'playwright:v1.57.0-noble' .github/workflows/ci.yml && grep -q '"@playwright/test": "1.57.0"' examples/adoption_demo/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/ci/e2e_local.sh` exists, passes `bash -n`, is executable, and references `mcr.microsoft.com/playwright:v1.57.0-noble`.
    - `ci.yml`'s `adoption-demo-e2e` job runs against the SAME `v1.57.0-noble` image and stays `needs: [quality, optional-dependencies]`, push:main/nightly-only — NOT a PR-required check.
    - `examples/adoption_demo/package.json` declares `"@playwright/test": "1.57.0"` (no caret); the version matches the container tag.
    - `! grep -q 'adoption-demo-e2e' scripts/setup_branch_protection.sh` (the lane is not added to the required-check list).
  </acceptance_criteria>
  <done>Both the CI E2E lane and `scripts/ci/e2e_local.sh` run the same pinned `v1.57.0-noble` container; the npm version matches exactly; the lane stays off the PR-required path.</done>
</task>

</tasks>

<threat_model>
N/A — no new attack surface. HARD-04 is a test-repro + brandbook-tooling track with ZERO
`lib/` change. The pinned Playwright container is a supply-chain *improvement* (immutable
image tag vs `npx playwright install` resolving a floating browser); the contrast change is a
pure constant-extraction. No new secret/network surface; the E2E lane stays push:main/nightly-only.
</threat_model>

<verification>
- `node brandbook/src/admin-contrast.mjs` + `node brandbook/src/contrast.mjs` print unchanged `N/N pairs pass` counts.
- `mix test test/brandbook/admin_design_system_validation_test.exs` passes (strings byte-identical).
- `bash -n scripts/ci/e2e_local.sh` parses; the script + ci.yml both reference `playwright:v1.57.0-noble`.
- `examples/adoption_demo/package.json` pins `@playwright/test` exact `1.57.0`.
- No `lib/` change (`git diff --name-only | grep -c '^lib/'` is 0); `adoption-demo-e2e` not a required check.
</verification>

<success_criteria>
- Same `v1.57.0-noble` image used by both the CI E2E lane and `scripts/ci/e2e_local.sh` (faithful repro by construction).
- `@playwright/test` pinned exact `1.57.0`, matching the container tag.
- One shared `WCAG_AA_NORMAL = 4.5` constant feeds both contrast gates; pass counts byte-identical.
- E2E lane stays push:main/nightly-only; no new required check; no `lib/` change.
</success_criteria>

<output>
Create `.planning/phases/107-reliability-security-dx-hardening/107-04-SUMMARY.md` when done.
</output>
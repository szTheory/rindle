---
phase: 06-adopter-runtime-ownership
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - guides/getting_started.md
  - guides/background_processing.md
  - guides/troubleshooting.md
autonomous: true
requirements:
  - ADOPT-04
must_haves:
  truths:
    - "Adopter-facing guides explain Repo ownership as `config :rindle, :repo, MyApp.Repo`, not as a `Rindle.Repo` requirement."
    - "Guides explain Oban ownership in adopter-first terms and explicitly say Phase 6 only covers the default `Oban` path, not named-instance resolution."
    - "Copy-paste examples match the canonical adopter proof and the dedicated proxied-upload adopter proof from Plan 06-02."
  artifacts:
    - path: "guides/getting_started.md"
      provides: "Adopter-first setup instructions for Repo ownership"
    - path: "guides/background_processing.md"
      provides: "Explicit Oban ownership statement and current scope boundary"
    - path: "guides/troubleshooting.md"
      provides: "Adopter-owned Repo examples for debugging and inspection"
  key_links:
    - from: "guides/getting_started.md"
      to: "test/adopter/canonical_app/lifecycle_test.exs"
      via: "matching setup and public API examples for both direct-upload and proxied-upload runtime paths"
      pattern: "config :rindle, :repo"
    - from: "guides/background_processing.md"
      to: "default Oban contract"
      via: "explicit scope note for Phase 6"
      pattern: "Oban"
---

<objective>
Align the public guidance with the runtime contract proved in Plans 06-01 and 06-02.

Purpose: ADOPT-04 is not cleanup; it is part of the contract. If guides still teach `Rindle.Repo` or overstate Oban ownership, adopters will integrate the phase incorrectly.
Output: Updated guides that teach adopter-owned Repo setup, honest Oban scope, and debugging examples that match the proven canonical and proxied-upload flows.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-adopter-runtime-ownership/06-RESEARCH.md
@.planning/phases/06-adopter-runtime-ownership/06-01-PLAN.md
@.planning/phases/06-adopter-runtime-ownership/06-02-PLAN.md
@guides/getting_started.md
@guides/background_processing.md
@guides/troubleshooting.md
@test/adopter/canonical_app/lifecycle_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite setup and troubleshooting guides around adopter-owned Repo resolution</name>
  <files>guides/getting_started.md, guides/troubleshooting.md</files>
  <action>Update the setup and debugging guidance so every adopter-facing example uses `config :rindle, :repo, MyApp.Repo` and `MyApp.Repo`-style queries rather than `Rindle.Repo`. Keep the examples synchronized with the canonical adopter lifecycle and dedicated proxied-upload proof from Plan 06-02, and remove any wording that frames `Rindle.Repo` as a consumer runtime dependency.</action>
  <verify>
    <automated>rg -n "config :rindle, :repo, MyApp\\.Repo" guides/getting_started.md guides/troubleshooting.md && rg -n "MyApp\\.Repo\\.(get!|get_by!|all)" guides/troubleshooting.md && rg -n "Broker\\.initiate_session|Rindle\\.attach|Rindle\\.detach" guides/getting_started.md && ! rg -n "Rindle\\.Repo" guides/getting_started.md guides/troubleshooting.md</automated>
  </verify>
  <done>The setup and troubleshooting guides no longer instruct adopters to use `Rindle.Repo`, include the concrete adopter-owned Repo snippets the canonical lane uses, and keep the public API examples aligned with the Phase 6 runtime contract.</done>
</task>

<task type="auto">
  <name>Task 2: Document the exact Oban ownership boundary for Phase 6</name>
  <files>guides/background_processing.md</files>
  <action>Revise the background-processing guide to state the exact scope delivered in Phase 6: adopters own Oban supervision, queue config, and the default Oban Repo; Rindle relies on that default Oban path for enqueueing, and named-instance / alternate Oban-name support is deferred from this phase. Do not imply fuller Oban-runtime ownership than the code proves.</action>
  <verify>
    <automated>rg -n "default Oban" guides/background_processing.md && rg -n "named-instance|named instance|:oban_name" guides/background_processing.md && rg -n "adopters own Oban supervision|adopters own.*queue config|default Oban Repo" guides/background_processing.md && mix test test/adopter/canonical_app/lifecycle_test.exs</automated>
  </verify>
  <done>The guide explains Oban ownership precisely, in adopter-first terms, names default-Oban compatibility as the delivered scope, and explicitly marks named-instance support as out of scope for Phase 6.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guides → adopter implementation | copy-paste instructions directly influence runtime configuration choices |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | T | guide snippets | mitigate | replace all adopter-facing `Rindle.Repo` examples with `config :rindle, :repo, MyApp.Repo` guidance tied to the canonical adopter proof |
| T-06-03-02 | R | Oban ownership wording | mitigate | explicitly document that Phase 6 covers default-Oban ownership semantics only and defers named-instance support |
</threat_model>

<verification>
Run the canonical adopter proof after docs changes to ensure the guide still tracks a working path: `mix test test/adopter/canonical_app/lifecycle_test.exs`.

Also confirm the targeted guides contain the concrete adopter snippets they should teach, not just the absence of stale wording:

- `rg -n "config :rindle, :repo, MyApp\\.Repo" guides/getting_started.md guides/troubleshooting.md`
- `rg -n "MyApp\\.Repo\\.(get!|get_by!|all)" guides/troubleshooting.md`
- `rg -n "default Oban|named-instance|:oban_name" guides/background_processing.md`
</verification>

<success_criteria>
- Guides teach adopter-owned Repo configuration explicitly.
- Oban ownership is described precisely as default-instance adopter ownership, with named-instance support deferred.
- The docs stay aligned with the canonical adopter lifecycle proof and the dedicated adopter-repo `Rindle.upload/3` proof.
</success_criteria>

<output>
After completion, create `.planning/phases/06-adopter-runtime-ownership/06-adopter-runtime-ownership-03-SUMMARY.md`
</output>

---
phase: 06-adopter-runtime-ownership
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/config.ex
  - lib/rindle.ex
  - config/config.exs
  - test/rindle/config/config_test.exs
autonomous: true
requirements:
  - ADOPT-01
  - ADOPT-02
must_haves:
  truths:
    - "Setting `config :rindle, :repo, MyApp.Repo` changes `Rindle.attach/4`, `Rindle.detach/3`, and `Rindle.upload/3` to transact through the adopter Repo without changing their public signatures."
    - "Facade code no longer calls `Rindle.Repo.transaction/1` or `Rindle.Repo.get!/2` on consumer runtime paths."
    - "The facade resolves its Repo through one seam, and Plan 06-02 can prove both the canonical direct-upload lane and the proxied `Rindle.upload/3` lane against an adopter Repo without re-refactoring the public API."
  artifacts:
    - path: "lib/rindle/config.ex"
      provides: "Runtime Repo accessor for adopter-owned resolution"
      exports: ["repo/0"]
    - path: "lib/rindle.ex"
      provides: "Repo-neutral public facade for attach/detach/upload"
      exports: ["attach/4", "detach/3", "upload/3"]
    - path: "test/rindle/config/config_test.exs"
      provides: "Repo accessor regression coverage"
  key_links:
    - from: "lib/rindle.ex"
      to: "lib/rindle/config.ex"
      via: "single repo resolution per public entrypoint"
      pattern: "Rindle\\.Config\\.repo\\("
    - from: "lib/rindle.ex"
      to: "Ecto.Multi callback repo arg"
      via: "purge callbacks use the transaction repo instead of re-fetching through Rindle.Repo"
      pattern: "fn repo|fn tx_repo"
---

<objective>
Make adopter-owned Repo resolution the hard contract for Rindle's public facade.

Purpose: ADOPT-01 and ADOPT-02 are only real if the consumer-facing entrypoints stop hard-coding `Rindle.Repo` and instead resolve the runtime Repo from one seam.
Output: A runtime Repo accessor, refactored facade persistence paths, and regression tests that lock the behavior.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-ci-1-0-readiness/05-CONTEXT.md
@.planning/phases/05-ci-1-0-readiness/05-RESEARCH.md
@.planning/phases/06-adopter-runtime-ownership/06-RESEARCH.md
@.planning/phases/06-adopter-runtime-ownership/06-PATTERNS.md
@lib/rindle/config.ex
@lib/rindle.ex
@config/config.exs
@test/rindle/config/config_test.exs
<interfaces>
From lib/rindle/config.ex:
```elixir
@spec queue_name() :: atom()
def queue_name

@spec signed_url_ttl_seconds() :: pos_integer()
def signed_url_ttl_seconds

@spec upload_session_ttl_seconds() :: pos_integer()
def upload_session_ttl_seconds
```

From lib/rindle.ex:
```elixir
@spec attach(struct() | binary(), struct(), String.t(), keyword()) ::
        {:ok, struct()} | {:error, term()}
def attach(asset_or_id, owner, slot, _opts \\ [])

@spec detach(struct(), String.t(), keyword()) :: :ok | {:error, term()}
def detach(owner, slot, _opts \\ [])

@spec upload(module(), map(), keyword()) :: {:ok, struct()} | {:error, term()}
def upload(profile_module, upload, opts \\ [])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the runtime Repo contract in `Rindle.Config`</name>
  <files>lib/rindle/config.ex, config/config.exs, test/rindle/config/config_test.exs</files>
  <behavior>
    - Test 1: `Rindle.Config.repo/0` returns the configured module after `Application.put_env(:rindle, :repo, SomeRepo)`.
    - Test 2: deleting the override falls back to the in-repo harness default without raising.
  </behavior>
  <action>Add `repo/0` in `Rindle.Config` as the lowest-churn seam identified in Phase 6 research, and wire a default `config :rindle, :repo, Rindle.Repo` in repo-local config for backward-compatible harness behavior. Preserve all existing config accessors and do not introduce a new public API surface beyond the accessor required by ADOPT-01.</action>
  <verify>
    <automated>mix test test/rindle/config/config_test.exs</automated>
  </verify>
  <done>`Rindle.Config.repo/0` exists, respects per-test overrides, and the repo-local default remains `Rindle.Repo` when adopters do not override `:repo`.</done>
</task>

<task type="auto">
  <name>Task 2: Move `attach/4`, `detach/3`, and `upload/3` onto the runtime Repo seam</name>
  <files>lib/rindle.ex</files>
  <action>Refactor `lib/rindle.ex` to resolve `repo = Rindle.Config.repo()` once per public entrypoint, use `repo.transaction/1` for the enclosing unit of work, and use the callback repo argument inside `Ecto.Multi.run` instead of re-fetching through `Rindle.Repo`. Preserve the `attach/4`, `detach/3`, and `upload/3` signatures exactly, keep the current default-`Oban` enqueue path unchanged in this plan, and update the facade `@doc` examples so they describe a configured adopter Repo rather than “Requires Rindle.Repo” wording, per ADOPT-02. Do not claim the existing `Rindle.DataCase` integration tests prove alternate-repo behavior here; Plan 06-02 must land the fail-on-wrong-repo proofs for both the canonical direct-upload lane and the proxied `Rindle.upload/3` lane.</action>
  <verify>
    <automated>mix test test/rindle/config/config_test.exs && ! rg -n "Rindle\\.Repo\\.(transaction|get!|insert!?|update!?|delete!?|all|one|preload)" lib/rindle.ex</automated>
  </verify>
  <done>The facade keeps its public signatures, uses `Rindle.Config.repo/0` for persistence, and no direct `Rindle.Repo` persistence call remains in the `attach/4`, `detach/3`, or `upload/3` runtime path. Alternate-repo proof ownership is explicitly handed to Plan 06-02, which must prove both the canonical direct-upload lane and the proxied `Rindle.upload/3` lane instead of implying coverage from legacy tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public API → runtime Repo | consumer input reaches DB writes through facade entrypoints |
| public API → background job enqueue | attach/detach/upload enqueue follow-up work after DB changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | T | `lib/rindle.ex` transaction path | mitigate | resolve the Repo once per call and use the transaction callback repo arg so mixed-repo writes cannot occur mid-flow |
| T-06-01-02 | R | facade docs/examples | mitigate | update `@doc` examples to state the real adopter-owned Repo contract, preventing operational drift between docs and code |
| T-06-01-03 | D | attach/detach purge callback | accept | keep current default `Oban.insert` behavior in this plan; enqueue semantics are unchanged while Repo ownership is corrected first |
</threat_model>

<verification>
Run the seam/accessor suite first: `mix test test/rindle/config/config_test.exs`.

Before closing the plan, grep the edited facade for stale ownership leaks and confirm no direct `Rindle.Repo` persistence call remains in `attach/4`, `detach/3`, or `upload/3`. Treat end-to-end proof as incomplete until Plan 06-02 lands the adopter-only sandbox harness plus the explicit proxied `Rindle.upload/3` adopter-repo proof.
</verification>

<success_criteria>
- `Rindle.Config.repo/0` is the single runtime Repo accessor.
- `Rindle.attach/4`, `Rindle.detach/3`, and `Rindle.upload/3` keep their signatures and no longer hard-code `Rindle.Repo`.
- This plan delivers the seam and facade refactor only; the fail-on-wrong-repo proofs for both direct-upload and proxied-upload runtime paths are deferred explicitly to Plan 06-02.
</success_criteria>

<output>
After completion, create `.planning/phases/06-adopter-runtime-ownership/06-adopter-runtime-ownership-01-SUMMARY.md`
</output>

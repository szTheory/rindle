---
phase: 114-oss-trust-governance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SECURITY.md
  - CODE_OF_CONDUCT.md
  - .github/ISSUE_TEMPLATE/bug_report.yml
  - .github/ISSUE_TEMPLATE/feature_proposal.yml
  - .github/ISSUE_TEMPLATE/config.yml
  - .github/PULL_REQUEST_TEMPLATE.md
  - test/install_smoke/governance_files_test.exs
autonomous: true
requirements: [TRUST-01, TRUST-02, TRUST-03]
user_setup:
  - service: github
    why: "TRUST-01 Private Vulnerability Reporting must be toggled ON for the 'Report a vulnerability' button to render. SECURITY.md is inert until this is enabled. Not a code change — operational follow-up after merge."
    dashboard_config:
      - task: "Enable Private Vulnerability Reporting"
        location: "GitHub repo -> Settings -> Code security -> Private vulnerability reporting -> Enable"

must_haves:
  truths:
    - "SECURITY.md exists at repo root with a Supported Versions table and a Reporting a Vulnerability section pointing to GitHub Security Advisories (no email, no public issues)"
    - "CODE_OF_CONDUCT.md exists at repo root (Contributor Covenant 2.1) with a GitHub-routed enforcement contact"
    - "A newcomer opening an issue sees structured forms (bug report, feature proposal) and the blank-issue path is disabled with a security contact link routing to advisories"
    - "A newcomer opening a PR gets a populated PR body from .github/PULL_REQUEST_TEMPLATE.md"
    - "The existing .github/ISSUE_TEMPLATE/release-train-drift.md is preserved untouched"
    - "test/install_smoke/governance_files_test.exs passes, asserting all governance files exist and release-train-drift.md is preserved"
  artifacts:
    - SECURITY.md
    - CODE_OF_CONDUCT.md
    - .github/ISSUE_TEMPLATE/bug_report.yml
    - .github/ISSUE_TEMPLATE/feature_proposal.yml
    - .github/ISSUE_TEMPLATE/config.yml
    - .github/PULL_REQUEST_TEMPLATE.md
    - test/install_smoke/governance_files_test.exs
  key_links:
    - "config.yml contact_links security entry -> https://github.com/szTheory/rindle/security/advisories/new (reinforces TRUST-01 disclosure channel)"
    - "SECURITY.md disclosure section -> GitHub Private Vulnerability Reporting (requires repo-settings toggle to render — tracked as user_setup)"
    - "Issue forms + PR template -> CONTRIBUTING.md (templates are the newcomer on-ramp; CONTRIBUTING is CI behavior)"
---

<objective>
Add the conventional OSS-trust governance files so a newcomer or security researcher lands on a project that signals it is maintained, safe to report to, and welcoming to contribute to (TRUST-01/02/03). This is a docs/config-only plan — no `lib/` change, no behavior change.

Purpose: Close the governance gap. SECURITY.md gives security researchers a private, no-email disclosure channel grounded in Rindle's real threat surface. CODE_OF_CONDUCT.md sets community expectations. Issue forms + PR template + config.yml give newcomers a structured on-ramp and steer security reports away from public issues.

Output: SECURITY.md, CODE_OF_CONDUCT.md (repo root), `.github/ISSUE_TEMPLATE/{bug_report,feature_proposal,config}.yml`, `.github/PULL_REQUEST_TEMPLATE.md`, and a new `test/install_smoke/governance_files_test.exs` presence test. Governance files are REPO-ONLY (not added to the mix.exs `files:` Hex tarball list).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-oss-trust-governance/114-RESEARCH.md
@.planning/phases/114-oss-trust-governance/114-VALIDATION.md
@CONTRIBUTING.md
@.github/ISSUE_TEMPLATE/release-train-drift.md
</context>

<artifacts_produced>
## Artifacts this plan produces

| Artifact | Kind | Requirement |
|----------|------|-------------|
| `SECURITY.md` | new file (repo root) | TRUST-01 |
| `CODE_OF_CONDUCT.md` | new file (repo root) | TRUST-02 |
| `.github/ISSUE_TEMPLATE/bug_report.yml` | new issue form | TRUST-03 |
| `.github/ISSUE_TEMPLATE/feature_proposal.yml` | new issue form | TRUST-03 |
| `.github/ISSUE_TEMPLATE/config.yml` | new config (`blank_issues_enabled: false` + security `contact_links`) | TRUST-03 |
| `.github/PULL_REQUEST_TEMPLATE.md` | new PR template | TRUST-03 |
| `test/install_smoke/governance_files_test.exs` | new meta-test (Wave 0 gap) | TRUST-01/02/03 |

NOT produced / NOT touched: mix.exs `files:` list (governance files stay repo-only); `.github/ISSUE_TEMPLATE/release-train-drift.md` (preserved verbatim).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — governance-presence meta-test</name>
  <files>test/install_smoke/governance_files_test.exs</files>
  <read_first>
    - test/install_smoke/package_metadata_test.exs (lines 1-34) — copy the `@repo_root Path.expand("../..", __DIR__)` idiom and the `use ExUnit.Case, async: true` header
  </read_first>
  <action>
Create `test/install_smoke/governance_files_test.exs` FIRST (Wave 0 gap from 114-VALIDATION.md — this is the failing test the subsequent tasks make green). Use `use ExUnit.Case, async: true`. Define `@repo_root Path.expand("../..", __DIR__)` matching the sibling meta-test. Add `File.exists?(Path.join(@repo_root, rel))` assertions for: `SECURITY.md`, `CODE_OF_CONDUCT.md`, `.github/ISSUE_TEMPLATE/bug_report.yml`, `.github/ISSUE_TEMPLATE/feature_proposal.yml`, `.github/ISSUE_TEMPLATE/config.yml`, `.github/PULL_REQUEST_TEMPLATE.md`. Add a SEPARATE assertion that `.github/ISSUE_TEMPLATE/release-train-drift.md` still exists (locks against RESEARCH Pitfall 4 — never clobber the existing maintainer template). Also read SECURITY.md and assert it routes to GitHub advisories and does NOT contain a public-email disclosure path: assert the body contains "advisories" and `refute` it matches `~r/[\w.]+@[\w.]+\.\w+/` (no exposed email address — the locked TRUST-01 channel is GitHub Private Vulnerability Reporting). Do NOT add any of these paths to the package-metadata test's `@required_paths` — governance files are repo-only, not Hex-tarball contents (RESEARCH Pitfall 2). At this point the test SHOULD fail (RED) because the governance files do not exist yet.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rindle && mix test test/install_smoke/governance_files_test.exs 2>&1 | grep -qE "test.*(failure|error)|Compiling" && echo "RED-as-expected (files not yet created)"</automated>
  </verify>
  <done>governance_files_test.exs exists and references all seven target governance files plus the preserved release-train-drift.md; it compiles and fails on missing files (RED). It contains no assertion adding governance paths to a Hex tarball list.</done>
</task>

<task type="auto">
  <name>Task 2: SECURITY.md + CODE_OF_CONDUCT.md (TRUST-01, TRUST-02)</name>
  <files>SECURITY.md, CODE_OF_CONDUCT.md</files>
  <read_first>
    - .planning/phases/114-oss-trust-governance/114-RESEARCH.md (TRUST-01 verified threat surface table, lines 84-116; TRUST-02 Contributor Covenant section, lines 118-135)
    - guides/secure_delivery.md (cross-reference the "private-by-default" signed-URL posture so SECURITY.md aligns with shipped docs)
  </read_first>
  <action>
Create `SECURITY.md` at repo root (TRUST-01) with this section layout per RESEARCH: (1) `## Supported Versions` — a minimal pre-1.0 table stating only the latest 0.x release line receives security fixes, no backports to older 0.x minors. (2) `## Reporting a Vulnerability` — direct reporters to GitHub Security Advisories ("Report a vulnerability" under the Security tab); explicitly state to NOT open public issues and that no email channel is used (the disclosure channel is GitHub Private Vulnerability Reporting per locked decision). Set a modest pre-1.0 response expectation (acknowledge within a few business days). (3) A "What to report" section grounding the policy in Rindle's real surface from the RESEARCH threat-surface table: untrusted upload validation / filename / storage-key sanitization, MIME/content-type sniffing, malware-scan-before-promotion hook, signed time-limited delivery URLs (private-by-default), webhook HMAC verification + replay window, and media-subprocess arg handling. Cross-reference `guides/secure_delivery.md`. Add a one-line operational note that the "Report a vulnerability" button requires Private Vulnerability Reporting to be enabled in repo Settings (mirrors the user_setup item). Do NOT embed any literal email address anywhere in the file (Task 1 negative-greps for it).

Create `CODE_OF_CONDUCT.md` at repo root (TRUST-02) using Contributor Covenant 2.1 verbatim (source: contributor-covenant.org/version/2/1). Fill the single Enforcement-contact placeholder `[INSERT CONTACT METHOD]` with a GitHub-routed contact (per locked decision — szTheory uses a noreply git email; do NOT invent an email): route reporters to the repository's Security advisories for private reports, or to the maintainer [@szTheory](https://github.com/szTheory) on GitHub. Preserve the standard Contributor Covenant attribution/footer (links to contributor-covenant.org homepage + FAQ) verbatim.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rindle && test -f SECURITY.md && test -f CODE_OF_CONDUCT.md && grep -qi "advisor" SECURITY.md && grep -qi "supported versions" SECURITY.md && grep -qi "contributor covenant" CODE_OF_CONDUCT.md && grep -qi "szTheory" CODE_OF_CONDUCT.md && ! grep -qE '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' SECURITY.md && echo OK</automated>
  </verify>
  <done>SECURITY.md exists with Supported Versions table + advisory-based reporting section + threat-surface "what to report" + no embedded email; CODE_OF_CONDUCT.md exists (Contributor Covenant 2.1) with a GitHub-routed enforcement contact referencing szTheory.</done>
</task>

<task type="auto">
  <name>Task 3: Issue forms + config.yml + PR template (TRUST-03)</name>
  <files>.github/ISSUE_TEMPLATE/bug_report.yml, .github/ISSUE_TEMPLATE/feature_proposal.yml, .github/ISSUE_TEMPLATE/config.yml, .github/PULL_REQUEST_TEMPLATE.md</files>
  <read_first>
    - .planning/phases/114-oss-trust-governance/114-RESEARCH.md (TRUST-03 section, lines 137-209 — pinned issue-form skeleton, config.yml, PR-template guidance)
    - .github/ISSUE_TEMPLATE/release-train-drift.md (existing template — DO NOT edit or move it)
    - CONTRIBUTING.md (templates should reference it, not duplicate it)
  </read_first>
  <action>
Create `.github/ISSUE_TEMPLATE/bug_report.yml` as a GitHub issue FORM (per locked TRUST-03 decision: forms, not markdown). Top-level keys: `name: Bug report`, `description`, `labels` (e.g. `["type:bug"]`), `body` (list). Body fields: a `markdown` intro pointing to `../../CONTRIBUTING.md`; a required `textarea` "What happened?" (steps to reproduce, expected vs actual); a required `input` "Rindle version"; an optional `input` "Elixir / OTP version". Create `.github/ISSUE_TEMPLATE/feature_proposal.yml` as a sibling form: `name: Feature proposal`, `description`, `labels` (e.g. `["type:feature"]`), `body` with a required `textarea` "What problem does this solve?" and a `textarea` "Proposed approach". Create `.github/ISSUE_TEMPLATE/config.yml` with `blank_issues_enabled: false` and a `contact_links` entry named "Report a security vulnerability" pointing `url: https://github.com/szTheory/rindle/security/advisories/new` with an `about` line steering security reports to private advisories (reinforces TRUST-01). Create `.github/PULL_REQUEST_TEMPLATE.md` (plain markdown, GitHub auto-applies): summary, linked issue, a checklist (`mix ci` green, docs updated, no `lib/` behavior change if docs-only), and a short pointer to `CONTRIBUTING.md` (reference, do not duplicate its trust/speed paragraph — that is a /gsd-ship-time step). Leave `.github/ISSUE_TEMPLATE/release-train-drift.md` untouched (forms and the legacy `.md` coexist in the same directory).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rindle && test -f .github/ISSUE_TEMPLATE/bug_report.yml && test -f .github/ISSUE_TEMPLATE/feature_proposal.yml && test -f .github/ISSUE_TEMPLATE/config.yml && test -f .github/PULL_REQUEST_TEMPLATE.md && test -f .github/ISSUE_TEMPLATE/release-train-drift.md && grep -q "blank_issues_enabled: false" .github/ISSUE_TEMPLATE/config.yml && grep -q "security/advisories/new" .github/ISSUE_TEMPLATE/config.yml && mix test test/install_smoke/governance_files_test.exs</automated>
  </verify>
  <done>All four new template files exist; config.yml disables blank issues and routes security to advisories; release-train-drift.md is preserved; governance_files_test.exs now passes GREEN (all files present, no email in SECURITY.md).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| security researcher -> disclosure channel | A researcher with a vulnerability must reach the maintainer privately. SECURITY.md is the routing artifact across this boundary. |
| newcomer -> issue/PR intake | An untrusted contributor's first interaction; templates shape and steer it (security reports away from public issues). |

## STRIDE Threat Register

This phase writes documentation/config only — no executable code, no new attack surface. Threats are about the governance content mis-routing trust, not code exploitation. Severities are LOW and honestly right-sized (per the phase security contribution).

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-114-01 | Information Disclosure | SECURITY.md disclosure section | low | mitigate | Point reporters to GitHub Private Vulnerability Reporting only; no email/public-issue path. Task 1 negative-greps SECURITY.md for any email address; Task 2 forbids embedding one. |
| T-114-02 | Information Disclosure | CODE_OF_CONDUCT.md enforcement contact | low | mitigate | Use a GitHub-routed contact (advisories / @szTheory profile); do not expose a personal email (szTheory uses a noreply git email). |
| T-114-03 | Tampering / Repudiation | public issue intake | low | mitigate | config.yml sets `blank_issues_enabled: false` + a security `contact_links` entry routing vulnerability reports to private advisories, keeping security reports out of public issues. |
| T-114-04 | Denial of Service (process) | existing release-train-drift.md template | low | mitigate | Never edit/move the existing maintainer template; Task 1 asserts it still exists (locks against RESEARCH Pitfall 4). |

No npm/pip/cargo installs in this phase — no `T-114-SC` supply-chain threat applies (zero packages added).
</threat_model>

<verification>
- `mix test test/install_smoke/governance_files_test.exs` is GREEN — all governance files exist, release-train-drift.md preserved, SECURITY.md contains no email and routes to advisories.
- `mix ci` is GREEN before the phase gate (the merge-blocking Quality lane; confirms no governance file was accidentally added to the Hex tarball, since `files:` is untouched).
- Manual (post-merge, tracked in user_setup): Private Vulnerability Reporting toggled ON in repo Settings; "Report a vulnerability" button renders on the Security tab.
</verification>

<success_criteria>
- SECURITY.md, CODE_OF_CONDUCT.md exist at repo root.
- `.github/ISSUE_TEMPLATE/{bug_report,feature_proposal,config}.yml` and `.github/PULL_REQUEST_TEMPLATE.md` exist.
- `.github/ISSUE_TEMPLATE/release-train-drift.md` is byte-identical to before this plan.
- `test/install_smoke/governance_files_test.exs` passes.
- No governance file added to mix.exs `files:` (verified by package_metadata_test's `@prohibited_paths` staying green in `mix ci`).
</success_criteria>

<output>
Create `.planning/phases/114-oss-trust-governance/114-01-SUMMARY.md` when done. Note the manual user_setup follow-up (enable Private Vulnerability Reporting) so /gsd-verify-work surfaces it.
</output>
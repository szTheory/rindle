---
phase: 74
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/operations.md
  - lib/rindle/upload/tus_plug.ex
  - test/install_smoke/docs_parity_test.exs
autonomous: true
requirements:
  - TRUTH-04

must_haves:
  truths:
    - "guides/operations.md intro states nine Mix tasks and Task Reference lists all nine"
    - "Task Reference includes thin subsections for doctor, runtime_status, batch_owner_erasure"
    - "TusPlug moduledoc advertises six extensions including checksum and concatenation; PATCH and DELETE implemented"
    - "docs_parity_test.exs asserts nine-task intro and all nine mix strings in operations.md"
  artifacts:
    - path: guides/operations.md
      provides: "Thin ops index with nine shipped tasks"
      contains: "nine Mix tasks"
    - path: lib/rindle/upload/tus_plug.ex
      provides: "Accurate TusPlug scope moduledoc"
      contains: "checksum,creation-defer-length,concatenation"
    - path: test/install_smoke/docs_parity_test.exs
      provides: "Locked nine-task parity gate"
      contains: "mix rindle.batch_owner_erasure"
  key_links:
    - from: test/install_smoke/docs_parity_test.exs
      to: guides/operations.md
      via: "assert all nine mix rindle.* strings"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-74-01-1 | Accidental lib/ behavior change in TusPlug | medium | moduledoc-only edits; run existing tus tests if unsure |
| T-74-01-2 | Flag table duplication in operations.md | low | thin Task Reference; link to @moduledoc and user_flows.md |
| T-74-01-3 | batch_owner_erasure contract leak into ops guide | low | refute --owners-file in operations body per D-05 |
</threat_model>

<objective>
Close TRUTH-04: fix `guides/operations.md` nine-task drift, update `Rindle.Upload.TusPlug`
moduledoc to match shipped tus scope, and extend `docs_parity_test.exs` to lock the index.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-support-truth-milestone-audit/74-CONTEXT.md
@.planning/phases/74-support-truth-milestone-audit/74-RESEARCH.md
@.planning/milestones/v1.14-phases/70-proof-adopter-guidance/70-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix operations.md nine-task index and Task Reference gaps</name>
  <files>guides/operations.md</files>
  <read_first>
    - guides/operations.md (full file — intro, Runtime Diagnostics, Task Reference)
    - .planning/phases/74-support-truth-milestone-audit/74-CONTEXT.md (D-01 through D-05)
    - lib/mix/tasks/rindle.doctor.ex (@moduledoc one-liner)
    - lib/mix/tasks/rindle.runtime_status.ex (@moduledoc one-liner)
    - lib/mix/tasks/rindle.batch_owner_erasure.ex (@moduledoc one-liner)
    - .planning/milestones/v1.14-phases/70-proof-adopter-guidance/70-CONTEXT.md (D-16/D-18 thin index)
  </read_first>
  <action>
    Edit `guides/operations.md`:

    **D-01 — intro count:** Line 3 (and any other "six Mix tasks" copy) must read
    **"nine Mix tasks"** instead of "six Mix tasks".

    **D-02 — enumeration:** Ensure all nine CLI strings appear somewhere in the guide
    (Task Reference is authoritative):
    `mix rindle.abort_incomplete_uploads`, `mix rindle.backfill_metadata`,
    `mix rindle.batch_owner_erasure`, `mix rindle.cleanup_orphans`, `mix rindle.doctor`,
    `mix rindle.regenerate_variants`, `mix rindle.runtime_status`,
    `mix rindle.sweep_orphaned_temp_files`, `mix rindle.verify_storage`.

    **D-03 — new Task Reference subsections** (after existing entries, thin format):

    ### `mix rindle.doctor`
    - **Module:** `Mix.Tasks.Rindle.Doctor`
    - One-line: validates host environment and optional profile/streaming prerequisites.
    - Pointer: `mix help rindle.doctor` / `@moduledoc` for full contract.

    ### `mix rindle.runtime_status`
    - **Module:** `Mix.Tasks.Rindle.RuntimeStatus`
    - One-line: bounded read-only report of degraded or stuck assets/variants/sessions.
    - Pointer: `mix help rindle.runtime_status` / `@moduledoc`.

    ### `mix rindle.batch_owner_erasure`
    - **Module:** `Mix.Tasks.Rindle.BatchOwnerErasure`
    - One-line: operator shell for batch owner erasure preview/execute.
    - Pointer: [`user_flows.md`](user_flows.md) batch subsection + `mix help rindle.batch_owner_erasure`.
    - **Do NOT** add `--owners-file`, `owner_type`, or flag tables (D-05).

    **D-04:** Keep Runtime Diagnostics narrative; Task Reference is the scan list.

    Optional (D-17 discretion): add a one-line summary table of nine tasks at top of
    Task Reference — only if it stays thin (task name + module, no flags).
  </action>
  <acceptance_criteria>
    - `grep -c 'nine Mix tasks' guides/operations.md` outputs at least `1`
    - `grep -c 'six Mix tasks' guides/operations.md` outputs `0`
    - `grep -c 'mix rindle.doctor' guides/operations.md` outputs at least `1`
    - `grep -c 'mix rindle.runtime_status' guides/operations.md` outputs at least `1`
    - `grep -c 'mix rindle.batch_owner_erasure' guides/operations.md` outputs at least `1`
    - `grep -c 'Mix.Tasks.Rindle.Doctor' guides/operations.md` outputs at least `1`
    - `grep -c 'Mix.Tasks.Rindle.RuntimeStatus' guides/operations.md` outputs at least `1`
    - `grep -c 'Mix.Tasks.Rindle.BatchOwnerErasure' guides/operations.md` outputs at least `1`
    - `grep -c 'owners-file' guides/operations.md` outputs `0`
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'nine Mix tasks' guides/operations.md && ! grep -q 'six Mix tasks' guides/operations.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Update TusPlug moduledoc to shipped scope</name>
  <files>lib/rindle/upload/tus_plug.ex</files>
  <read_first>
    - lib/rindle/upload/tus_plug.ex (moduledoc + @tus_extensions only — do not edit handlers)
    - .planning/phases/74-support-truth-milestone-audit/74-CONTEXT.md (D-06 through D-08)
    - test/rindle/upload/tus_plug_test.exs (PATCH/DELETE coverage)
    - test/rindle/upload/tus_s3_integration_test.exs (S3 backing)
  </read_first>
  <action>
    In `lib/rindle/upload/tus_plug.ex`, replace the stale `## Scope (Phase 42)` section
    (moduledoc only — **no** code changes below `@moduledoc`):

    1. Rename heading to `## Scope` (drop "Phase 42").
    2. State implemented extensions match runtime header:
       `creation, expiration, termination, checksum, creation-defer-length, concatenation`.
    3. Methods table — set `PATCH` and `DELETE` to **implemented** (remove "Plan 03" and "—").
    4. Backing: **Local and S3** tus paths shipped; remove "ONLY" and "proven against Local only".
    5. **Keep unchanged:** `## Security` and `## Deployment constraint (S3 tus backing)` sections verbatim in substance (D-07).
  </action>
  <acceptance_criteria>
    - `grep -c 'Phase 42' lib/rindle/upload/tus_plug.ex` outputs `0` in moduledoc (file may have no Phase 42 elsewhere)
    - `grep -c 'checksum,creation-defer-length,concatenation' lib/rindle/upload/tus_plug.ex` outputs at least `1`
    - `grep -c 'Plan 03' lib/rindle/upload/tus_plug.ex` outputs `0`
    - `grep -c 'Local only' lib/rindle/upload/tus_plug.ex` outputs `0`
    - `grep -c ':tus_tail_missing' lib/rindle/upload/tus_plug.ex` outputs at least `1`
    - `mix compile --force` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix compile --force</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Extend docs_parity_test for nine-task operations index</name>
  <files>test/install_smoke/docs_parity_test.exs</files>
  <read_first>
    - test/install_smoke/docs_parity_test.exs (existing operations/diagnostics tests)
    - guides/operations.md (after Task 1 edits)
    - .planning/phases/74-support-truth-milestone-audit/74-CONTEXT.md (D-09, D-10)
  </read_first>
  <action>
    Add test `"operations guide lists all nine shipped mix tasks"`:

    ```elixir
    @operations_path Path.expand("../../guides/operations.md", __DIR__)

  @nine_mix_tasks [
    "mix rindle.abort_incomplete_uploads",
    "mix rindle.backfill_metadata",
    "mix rindle.batch_owner_erasure",
    "mix rindle.cleanup_orphans",
    "mix rindle.doctor",
    "mix rindle.regenerate_variants",
    "mix rindle.runtime_status",
    "mix rindle.sweep_orphaned_temp_files",
    "mix rindle.verify_storage"
  ]

  test "operations guide lists all nine shipped mix tasks" do
    operations = File.read!(@operations_path)
    assert operations =~ "nine Mix tasks"
    refute operations =~ "six Mix tasks"
    for task <- @nine_mix_tasks do
      assert operations =~ task, "expected #{task} in operations.md"
    end
  end
    ```

    Optional D-10: separate test asserting `Rindle.Upload.TusPlug` moduledoc mentions
    `checksum` and `concatenation` — only if stable after Task 2.

    Run: `mix test test/install_smoke/docs_parity_test.exs`
  </action>
  <acceptance_criteria>
    - `grep -c 'operations guide lists all nine shipped mix tasks' test/install_smoke/docs_parity_test.exs` outputs `1`
    - `grep -c 'mix rindle.batch_owner_erasure' test/install_smoke/docs_parity_test.exs` outputs at least `1`
    - `mix test test/install_smoke/docs_parity_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/install_smoke/docs_parity_test.exs</automated>
  </verify>
</task>

</tasks>

<verification>
- All three tasks green with grep and mix test evidence
- No `lib/` handler changes beyond moduledoc in tus_plug.ex
</verification>

<success_criteria>
TRUTH-04 satisfied: operations nine-task index, TusPlug moduledoc truth, parity test locked.
</success_criteria>

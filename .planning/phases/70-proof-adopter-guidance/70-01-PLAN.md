---
id: 70-01
plan_number: 01
plan: 01
phase: 70
type: execute
wave: 1
depends_on: []
files_modified:
  - test/support/owner_erasure_batch_fixtures.ex
  - test/support/counting_failing_txn_repo.ex
  - test/rindle/owner_erasure_batch_proof_test.exs
  - test/rindle/owner_erasure_batch_test.exs
  - test/rindle/batch_owner_erasure_task_test.exs
autonomous: true
requirements: [PROOF-05]
requirements_addressed: [PROOF-05]
tags: [elixir, owner-erasure, batch, proof, integration]

must_haves:
  truths:
    - "Shared batch fixtures extracted to Rindle.Test.OwnerErasureBatchFixtures and consumed by batch_test, proof_test, and task_test"
    - "PROOF-05 proof file covers batch shared-asset preview and execute with retained_shared_assets semantics matching v1.10"
    - "Partial-failure DB integration proves owner1 committed and owner2 attachment survives when second transaction fails"
    - "First-owner failure returns batch_owner_failed with partial_report.owners == []"
    - "Phase 68 owner_erasure_batch_test.exs assertions unchanged (regression green)"
  artifacts:
    - path: "test/support/owner_erasure_batch_fixtures.ex"
      provides: "Shared batch test fixtures"
      contains: "Rindle.Test.OwnerErasureBatchFixtures"
    - path: "test/support/counting_failing_txn_repo.ex"
      provides: "Nth-transaction failure seam for partial-failure proof"
      contains: "CountingFailingTxnRepo"
    - path: "test/rindle/owner_erasure_batch_proof_test.exs"
      provides: "PROOF-05 gap-fill integration proof"
      contains: "PROOF-05"
  key_links:
    - from: "test/rindle/owner_erasure_batch_proof_test.exs"
      to: "lib/rindle.ex"
      via: "preview_batch_owner_erasure/2 and erase_batch_owner_erasure/2"
      pattern: "Rindle\\.(preview|erase)_batch_owner_erasure"
    - from: "test/support/counting_failing_txn_repo.ex"
      to: "lib/rindle/config.ex"
      via: "Application.put_env(:rindle, :repo, …)"
      pattern: "Config\\.repo"

threat_model:
  assets:
    - "Owner attachments across multi-owner batch execute"
  trust_boundaries:
    - "Test repo wrapper vs real Rindle.Repo transaction commits"
  threats:
    - id: T-70-01
      category: tampering
      description: "Partial-failure proof mocks OwnerErasure instead of exercising real DB commits"
      severity: high
      mitigation: "CountingFailingTxnRepo delegates to Rindle.Repo; fail only transaction/1 at configured N"
    - id: T-70-02
      category: denial_of_service
      description: "False confidence that cross-owner rollback occurs on mid-batch failure"
      severity: high
      mitigation: "Assert owner1 attachment deleted and owner2 attachment present after batch_owner_failed"
  block_on: high
---

<objective>
Ship PROOF-05 gap-fill hermetic proof: shared batch fixtures, counting failing transaction repo,
and `owner_erasure_batch_proof_test.exs` covering shared-asset, partial-failure, and first-owner
failure scenarios without duplicating Phase 68 baseline tests.

Purpose: Close 68-VERIFICATION advisory on partial-failure DB integration and shared-asset batch semantics.
Output: `test/support/owner_erasure_batch_fixtures.ex`, `test/support/counting_failing_txn_repo.ex`,
`test/rindle/owner_erasure_batch_proof_test.exs`
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/70-proof-adopter-guidance/70-CONTEXT.md
@.planning/phases/70-proof-adopter-guidance/70-RESEARCH.md
@.planning/phases/70-proof-adopter-guidance/70-PATTERNS.md
@.planning/phases/70-proof-adopter-guidance/70-VALIDATION.md
@.planning/phases/68-batch-erasure-implementation/68-VERIFICATION.md
@test/rindle/owner_erasure_batch_test.exs
@test/rindle/owner_erasure_test.exs
@test/rindle/upload/broker_test.exs
@lib/rindle.ex
@lib/rindle/internal/owner_erasure.ex
@lib/rindle/config.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared batch fixtures</name>
  <read_first>
    - test/rindle/owner_erasure_batch_test.exs (TestProfile, User, insert_asset/insert_attachment, owner_ref/owner_type)
    - test/rindle/batch_owner_erasure_task_test.exs (duplicate helpers and @owner_type string)
    - .planning/phases/70-proof-adopter-guidance/70-CONTEXT.md (D-01, D-03)
  </read_first>
  <files>test/support/owner_erasure_batch_fixtures.ex, test/rindle/owner_erasure_batch_test.exs, test/rindle/batch_owner_erasure_task_test.exs</files>
  <action>
    Create `test/support/owner_erasure_batch_fixtures.ex`:

    ```elixir
    defmodule Rindle.Test.OwnerErasureBatchFixtures do
      alias Rindle.Domain.{MediaAsset, MediaAttachment}

      def test_profile, do: TestProfile
      def user_module, do: User

      def insert_asset(storage_key), do: ...
      def insert_attachment(asset, owner, slot), do: ...
      def owner_ref(owner), do: ...
      def owner_type(owner), do: ...
    end
    ```

    Move `TestProfile` and `User` modules into the fixtures module (or as nested modules
    `Rindle.Test.OwnerErasureBatchFixtures.TestProfile` and `.User`).

    Refactor `owner_erasure_batch_test.exs`:
    - `import Rindle.Test.OwnerErasureBatchFixtures`
    - Remove duplicated private helpers
    - **Do not change test names or assertions** (D-01 frozen baseline)

    Refactor `batch_owner_erasure_task_test.exs`:
    - Use fixtures for insert_asset/insert_attachment
    - Set `@owner_type` to `"Elixir.#{inspect(Fixtures.user_module())}"` or equivalent stable string

    Ensure `test/support/` is compiled (already included via `test/test_helper.exs` or `mix.exs` elixirc_paths).
  </action>
  <acceptance_criteria>
    - File `test/support/owner_erasure_batch_fixtures.ex` contains `defmodule Rindle.Test.OwnerErasureBatchFixtures`
    - `grep -q 'OwnerErasureBatchFixtures' test/rindle/owner_erasure_batch_test.exs`
    - `grep -q 'OwnerErasureBatchFixtures' test/rindle/batch_owner_erasure_task_test.exs`
    - `mix test test/rindle/owner_erasure_batch_test.exs test/rindle/batch_owner_erasure_task_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rindle/owner_erasure_batch_test.exs test/rindle/batch_owner_erasure_task_test.exs</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Ship counting failing transaction repo</name>
  <read_first>
    - test/rindle/upload/broker_test.exs (FailingTransactionRepo, put_env/on_exit pattern)
    - lib/rindle/internal/owner_erasure.ex (repo.all for preview, repo.transaction for execute)
    - lib/rindle/config.ex (Application.get_env(:rindle, :repo, Rindle.Repo))
    - .planning/phases/70-proof-adopter-guidance/70-CONTEXT.md (D-09, D-10)
  </read_first>
  <files>test/support/counting_failing_txn_repo.ex</files>
  <action>
    Create `test/support/counting_failing_txn_repo.ex` as `Rindle.Test.CountingFailingTxnRepo`:

    - Read config from `Application.get_env(:rindle, :counting_failing_txn_repo, [])`
    - Support keyword `:fail_after` (positive integer) — fail on the Nth `transaction/1` call
    - Support keyword `:fail_reason` default `:forced_batch_failure`
    - Maintain transaction count in process dictionary or Agent (process-local is fine for ExUnit)

    Implement and delegate to `Rindle.Repo`:
    - `transaction/1` (fun and Multi arity) — increment count; if count == fail_after return `{:error, :plan, fail_reason, %{}}`; else delegate
    - `all/1`, `one/1`, `get/2`, `get!/2`, `insert/1`, `insert!/1`, `update/1`, `update!/1`, `delete/1`, `delete!/1`, `preload/2` — delegate all calls OwnerErasure uses

    Export helper for tests:

    ```elixir
    def with_counting_repo(fail_after, fun) when is_integer(fail_after) and is_function(fun, 0) do
      previous_repo = Application.get_env(:rindle, :repo)
      previous_cfg = Application.get_env(:rindle, :counting_failing_txn_repo)
      Application.put_env(:rindle, :repo, __MODULE__)
      Application.put_env(:rindle, :counting_failing_txn_repo, fail_after: fail_after)
      on_exit = fn -> restore env end
      try do
        fun.()
      after
        on_exit.()
      end
    end
    ```

    Or document explicit setup/on_exit in proof tests if helper is awkward inside ExUnit.
  </action>
  <acceptance_criteria>
    - File `test/support/counting_failing_txn_repo.ex` contains `defmodule Rindle.Test.CountingFailingTxnRepo`
    - File contains `def transaction(` delegating to `Rindle.Repo`
    - File contains `:fail_after`
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Add PROOF-05 shared-asset batch scenarios</name>
  <read_first>
    - test/rindle/owner_erasure_test.exs (shared vs orphan asset fixtures, lines 25-90)
    - test/rindle/owner_erasure_batch_test.exs (batch API usage)
    - test/support/owner_erasure_batch_fixtures.ex
    - .planning/phases/70-proof-adopter-guidance/70-CONTEXT.md (D-05)
  </read_first>
  <files>test/rindle/owner_erasure_batch_proof_test.exs</files>
  <action>
    Create `test/rindle/owner_erasure_batch_proof_test.exs`:

    ```elixir
    defmodule Rindle.OwnerErasureBatchProofTest do
      use Rindle.DataCase, async: false
      use Oban.Testing, repo: Rindle.Repo
      import Mox
      import Rindle.Test.OwnerErasureBatchFixtures
    ```

    Add `describe "PROOF-05: shared assets"` with two tests:

    1. **Batch preview with shared asset**
       - owner1, owner2, shared_asset with both attachments + one orphan asset each
       - `Rindle.preview_batch_owner_erasure([owner1, owner2])`
       - Assert `{:ok, batch}`; `batch.retained_shared_assets.count >= 1`
       - Per-owner reports include retained_shared_assets entries for shared asset

    2. **Batch execute with shared asset**
       - Same fixture; `Rindle.erase_batch_owner_erasure([owner1, owner2])`
       - Assert both owners' exclusive attachments deleted
       - Assert shared asset row still in Repo (`Repo.get(MediaAsset, shared_asset.id)`)
       - Assert surviving other-owner attachment still present
       - Assert aggregate `batch.retained_shared_assets.count` reflects shared retention semantics from v1.10

    Mirror fixture structure from `owner_erasure_test.exs` `"returns a semantic report for orphaned and retained shared assets"`.
  </action>
  <acceptance_criteria>
    - File `test/rindle/owner_erasure_batch_proof_test.exs` exists
    - File contains `describe "PROOF-05: shared assets"`
    - File contains `retained_shared_assets`
    - `mix test test/rindle/owner_erasure_batch_proof_test.exs --only describe:"PROOF-05: shared assets"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rindle/owner_erasure_batch_proof_test.exs --only describe:"PROOF-05: shared assets"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 4: Add PROOF-05 partial-failure and first-owner failure scenarios</name>
  <read_first>
    - test/support/counting_failing_txn_repo.ex
    - lib/rindle.ex (run_batch_owner_erasure/3 partial_report branch)
    - lib/rindle/error.ex (batch_owner_failed message)
    - .planning/phases/70-proof-adopter-guidance/70-CONTEXT.md (D-06, D-07, D-09)
  </read_first>
  <files>test/rindle/owner_erasure_batch_proof_test.exs</files>
  <action>
    Add to `owner_erasure_batch_proof_test.exs`:

    `describe "PROOF-05: partial failure"`:

    1. **Second owner fails after first commits**
       - owner1, owner2 each with orphan attachment (separate assets)
       - Setup counting repo with `fail_after: 2`
       - Assert `Rindle.erase_batch_owner_erasure([owner1, owner2])` returns
         `{:error, {:batch_owner_failed, detail}}`
       - Assert `length(detail.partial_report.owners) == 1`
       - Assert owner1 attachment `refute Repo.get(MediaAttachment, attachment1.id)`
       - Assert owner2 attachment `assert Repo.get(MediaAttachment, attachment2.id)`

    2. **First owner fails with empty partial report**
       - Single owner with attachment OR two owners with `fail_after: 1`
       - Assert `detail.partial_report.owners == []`

    Use explicit `Application.put_env(:rindle, :repo, CountingFailingTxnRepo)` + `on_exit` restore
    (mirror broker_test.exs). Do NOT mock `OwnerErasure` or use invalid UUID fixtures.

    Restore default repo in `on_exit` even on test failure.
  </action>
  <acceptance_criteria>
    - File contains `describe "PROOF-05: partial failure"`
    - File contains `batch_owner_failed`
    - File contains `partial_report`
    - File contains `CountingFailingTxnRepo`
    - `mix test test/rindle/owner_erasure_batch_proof_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rindle/owner_erasure_batch_proof_test.exs</automated>
  </verify>
</task>

</tasks>

<verification>
After all tasks:
- `mix test test/rindle/owner_erasure_batch_test.exs test/rindle/owner_erasure_batch_proof_test.exs test/rindle/owner_erasure_batch_boundary_test.exs test/rindle/owner_erasure_batch_error_test.exs test/rindle/owner_erasure_batch_contract_test.exs test/rindle/owner_erasure_test.exs test/rindle/batch_owner_erasure_task_test.exs` exits 0
- `mix compile --warnings-as-errors` exits 0
</verification>

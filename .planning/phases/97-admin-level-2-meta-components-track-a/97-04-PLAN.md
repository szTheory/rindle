---
phase: 97-admin-level-2-meta-components-track-a
plan: 04
type: execute
wave: 4
depends_on: ["97-03"]
files_modified:
  - examples/adoption_demo/e2e/support/admin-polish.js
  - priv/static/rindle_admin/rindle-admin.css
  - test/brandbook/admin_design_system_validation_test.exs
autonomous: false
requirements: [UPLIFT-02]

must_haves:
  truths:
    - "A warn-only run of the adoption-demo-e2e lane reports zero spurious overlap warnings before the flip"
    - "OVERLAP_ENFORCED is true (overlap is now a hard failure for the meta matrix)"
    - "The shipped priv CSS is byte-identical to the generated brandbook CSS (sync ran; drift gate empty)"
    - "The ExUnit pinned screenshot literal is 18 and @screenshots lists all 18; contrast stays 58/58"
    - "The full gate runs green: build, contrast, gallery-check, sync, cmp, ExUnit"
  artifacts:
    - path: "examples/adoption_demo/e2e/support/admin-polish.js"
      provides: "OVERLAP_ENFORCED flipped to true"
      contains: "OVERLAP_ENFORCED = true"
    - path: "priv/static/rindle_admin/rindle-admin.css"
      provides: "Synced shipped CSS, byte-identical to brandbook copy"
    - path: "test/brandbook/admin_design_system_validation_test.exs"
      provides: "Updated pinned literals (18 screenshots, @screenshots list)"
      contains: "18 screenshots written"
  key_links:
    - from: "brandbook/tokens/rindle-admin.css"
      to: "priv/static/rindle_admin/rindle-admin.css"
      via: "sync-admin-css.mjs byte-for-byte mirror + cmp/git-diff gate"
      pattern: "sync-admin-css"
    - from: "test/brandbook/admin_design_system_validation_test.exs"
      to: "18 screenshots written"
      via: "pinned literal asserted against gallery-check output"
      pattern: "18 screenshots written"
---

<objective>
Seal the phase: after a green warn-only cycle confirms zero spurious overlap warnings, flip
`OVERLAP_ENFORCED` to `true` for the meta matrix (D-97-11); run `sync-admin-css.mjs` to mirror
the generated CSS to the shipped `priv` copy; bump the ExUnit pinned screenshot literal 10 → 18
and the `@screenshots` list (contrast stays 58/58); and run the full gate green. Completes
UPLIFT-02 SC2 enforcement + shipped-artifact parity.

Purpose: Overlap is a first-class Level-2 cohesion defect, but only after a green warn cycle
(Pitfall 4). The shipped CSS must be byte-identical to the generated copy (D-94-03), and the
exact-count parity literals must move atomically with the gallery (Pitfall 2).

Output: `OVERLAP_ENFORCED = true`; synced `priv/static/rindle_admin/rindle-admin.css`; updated
pinned literals; green full gate + adoption-demo-e2e lane.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-admin-level-2-meta-components-track-a/97-CONTEXT.md
@.planning/phases/97-admin-level-2-meta-components-track-a/97-PATTERNS.md
@.planning/phases/97-admin-level-2-meta-components-track-a/97-RESEARCH.md
@.planning/phases/97-admin-level-2-meta-components-track-a/97-03-SUMMARY.md

<interfaces>
<!-- The seal touchpoints. -->

admin-polish.js:
  const OVERLAP_ENFORCED = false;   // ~30 — flip to true; also exported in module.exports (~546)
  POLISH_EXEMPTIONS = Object.freeze({}); // per-surface escape hatch: new Set(["noInteractiveOverlap"])

adoption-demo-e2e lane:
  cd examples/adoption_demo && npm run e2e   // runs `playwright test`; admin-screenshots.spec.js calls assertAdminPolish
  warn-only output prints `[admin-polish] ... N warning(s)` for overlap until the flip

test/brandbook/admin_design_system_validation_test.exs:
  @screenshots [ 10 names ]                 // ~19 — extend to 18 (add the 8 meta element-shot names)
  assert admin_output =~ "admin contrast: 58/58 pairs pass"            // ~119 — KEEP 58/58
  assert output =~ "admin gallery check passed - 10 screenshots written" // ~127 — bump to 18
  assert_generated_clean(["brandbook/tokens/rindle-admin.css","brandbook/admin-gallery/index.html"]) // ~190
  guide command loop asserts 5 command strings, all already present     // ~196 — no new command needed

sync-admin-css.mjs: byte-for-byte mirror brandbook/tokens/rindle-admin.css -> priv/static/rindle_admin/rindle-admin.css
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm zero overlap warnings in the warn-only lane (pre-flip)</name>
  <read_first>
    - examples/adoption_demo/e2e/support/admin-polish.js (OVERLAP_ENFORCED warn routing ~30, 512-514; POLISH_EXEMPTIONS escape hatch)
    - .planning/phases/97-admin-level-2-meta-components-track-a/97-RESEARCH.md (Pitfall 4; Must-Answer 3 — warn->enforce migration path)
    - .planning/phases/97-admin-level-2-meta-components-track-a/97-CONTEXT.md (D-97-11)
  </read_first>
  <action>
    Run the warn-only adoption-demo-e2e lane and verify zero overlap warnings before the
    `OVERLAP_ENFORCED` flip in Task 2. This is a verification checkpoint — no code change here;
    the executor runs the lane and the human confirms the warn output is clean.
  </action>
  <what-built>
    97-01..97-03 landed the meta-component CSS, gallery units, and the rhythm + per-unit
    no-h-scroll checks with overlap still in WARN-ONLY mode (`OVERLAP_ENFORCED=false`). Before
    flipping overlap to a hard failure, the warn-only adoption-demo-e2e lane must run green with
    ZERO spurious overlap warnings across all 22 captures (Pitfall 4 — the toolbar/filter-bar
    wrapping rows are the canonical false-positive risk).
  </what-built>
  <how-to-verify>
    1. From the repo root run the warn-only lane:
       `cd examples/adoption_demo && npm run e2e` (or the repo's adoption-demo-e2e CI lane).
    2. Confirm the lane exits 0 (all 22 captures pass; no `violations`).
    3. Scan the output for overlap warnings: `[admin-polish] ... overlap` lines. There must be
       ZERO overlap warnings. If any appear, triage spurious-vs-real: fix the CSS/markup (real),
       or scope the single offending surface via
       `POLISH_EXEMPTIONS["{surface}"] = new Set(["noInteractiveOverlap"])` with a justification
       comment (do NOT revert the upcoming flip) — then re-run until clean.
  </how-to-verify>
  <verify>
    <human-check>The adoption-demo-e2e lane exits 0 with zero `overlap` warning lines across all 22 captures.</human-check>
  </verify>
  <acceptance_criteria>
    The adoption-demo-e2e lane exits 0 AND its output contains zero `overlap` warning lines across
    all 22 captures. This is the documented precondition (D-97-11 / Pitfall 4) for flipping
    `OVERLAP_ENFORCED` in Task 2.
  </acceptance_criteria>
  <done>Warn-only lane green with zero overlap warnings; cleared to flip OVERLAP_ENFORCED.</done>
  <resume-signal>Type "approved — zero overlap warnings" once the warn-only lane is green with no overlap warnings, or describe the warnings to triage.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Flip OVERLAP_ENFORCED, sync shipped CSS, bump pinned literals, run full gate</name>
  <files>examples/adoption_demo/e2e/support/admin-polish.js, priv/static/rindle_admin/rindle-admin.css, test/brandbook/admin_design_system_validation_test.exs</files>
  <read_first>
    - examples/adoption_demo/e2e/support/admin-polish.js (OVERLAP_ENFORCED ~30; module.exports ~533-546; POLISH_EXEMPTIONS escape hatch)
    - test/brandbook/admin_design_system_validation_test.exs (@screenshots ~19-30; pinned strings ~119 & ~127; assert_generated_clean ~190; guide command loop ~196)
    - brandbook/src/sync-admin-css.mjs (the D-94-03 byte-for-byte mirror — output only, never hand-edit priv)
    - .planning/phases/97-admin-level-2-meta-components-track-a/97-PATTERNS.md (§ test exs — pinned-count pattern, keep 58/58, bump 10->18; generated-artifact mirror)
    - .planning/phases/97-admin-level-2-meta-components-track-a/97-RESEARCH.md (Must-Answer 6 — full gate + gotchas; Pitfall 2 — atomic literal update)
    - .planning/phases/97-admin-level-2-meta-components-track-a/97-CONTEXT.md (D-97-05, D-97-11; D-94-03 sync path)
  </read_first>
  <action>
    Set `OVERLAP_ENFORCED = true` in `admin-polish.js` (D-97-11 — overlap is now a hard failure;
    lands only after the Task 1 warn-only checkpoint passed). Run the generators, then
    `node brandbook/src/sync-admin-css.mjs` to mirror `brandbook/tokens/rindle-admin.css` into
    `priv/static/rindle_admin/rindle-admin.css` byte-for-byte (D-94-03 / D-97-05 — never hand-edit
    the priv copy). In `admin_design_system_validation_test.exs`: bump the pinned string
    `admin gallery check passed - 10 screenshots written` -> `... 18 screenshots written`, and
    extend the `@screenshots` list with the 8 new meta element-shot filenames (exactly matching the
    names 97-02 added to `expectedScreenshots`). KEEP `admin contrast: 58/58 pairs pass` unchanged
    (A2 — no new colors). Do NOT add a new command to the guide command loop — the 5 asserted
    command strings already cover the chain. Run the full gate and confirm green:
    build -> contrast -> gallery-check -> sync -> `cmp` -> ExUnit, with the `assert_generated_clean`
    drift gate (`git diff --exit-code` on the generated CSS + gallery HTML) landing on an empty tree.
  </action>
  <verify>
    <automated>node brandbook/src/admin-css-build.mjs && node brandbook/src/admin-contrast.mjs && node brandbook/src/admin-gallery-check.mjs && node brandbook/src/sync-admin-css.mjs && cmp -s brandbook/tokens/rindle-admin.css priv/static/rindle_admin/rindle-admin.css && git diff --exit-code -- brandbook/tokens/rindle-admin.css brandbook/admin-gallery/index.html && node -e "if(require('./examples/adoption_demo/e2e/support/admin-polish.js').OVERLAP_ENFORCED!==true)process.exit(1)" && mix test --include integration test/brandbook/admin_design_system_validation_test.exs</automated>
  </verify>
  <acceptance_criteria>
    `node -e "require('./examples/adoption_demo/e2e/support/admin-polish.js').OVERLAP_ENFORCED"`
    is `true`;
    `cmp -s brandbook/tokens/rindle-admin.css priv/static/rindle_admin/rindle-admin.css` exits 0
    (byte-identical shipped CSS);
    `git diff --exit-code -- brandbook/tokens/rindle-admin.css brandbook/admin-gallery/index.html`
    exits 0 (empty drift);
    `node brandbook/src/admin-contrast.mjs` prints `58/58 pairs pass`;
    `grep -c "18 screenshots written" test/brandbook/admin_design_system_validation_test.exs` returns 1
    and the `@screenshots` list has 18 entries;
    `mix test --include integration test/brandbook/admin_design_system_validation_test.exs` reports
    `0 failures`.
  </acceptance_criteria>
  <done>Overlap is enforced for the meta matrix; shipped CSS is synced byte-identical with empty drift; pinned literals are 18 screenshots / 58/58 contrast; the full gate is green.</done>
</task>

</tasks>

<verification>
- Warn-only adoption-demo-e2e lane green with zero overlap warnings (Task 1 checkpoint), THEN `OVERLAP_ENFORCED = true`.
- `cmp -s` + `git diff --exit-code` confirm shipped CSS byte-identical and zero drift.
- ExUnit `0 failures`: pinned `18 screenshots written`, `@screenshots` has 18 entries, `58/58 pairs pass` unchanged, `read!(priv)==read!(brandbook)` holds.
- Full gate green: build, contrast, gallery-check, sync, cmp, ExUnit (+ the lane with overlap now enforced).
</verification>

<success_criteria>
SC2 overlap enforcement is sealed (`OVERLAP_ENFORCED=true`) after a documented green warn cycle
(D-97-11); the shipped `priv` CSS is the byte-for-byte mirror of the generated copy with an empty
drift gate (D-94-03); the exact-count parity literals moved atomically (10 -> 18 screenshots, 58/58
contrast kept); and the full merge-blocking gate runs green.
</success_criteria>

<output>
Create `.planning/phases/97-admin-level-2-meta-components-track-a/97-04-SUMMARY.md` when done.
</output>

---
phase: 97-admin-level-2-meta-components-track-a
plan: 02
type: execute
wave: 2
depends_on: ["97-01"]
files_modified:
  - brandbook/src/admin-gallery.mjs
  - brandbook/src/admin-gallery-check.mjs
  - brandbook/admin-gallery/index.html
autonomous: true
requirements: [UPLIFT-02]

must_haves:
  truths:
    - "Every meta-component renders in the gallery as one labeled cohesion panel marked data-rindle-admin-meta=\"{slug}\""
    - "Each meta unit is visible under light, dark, and auto themes"
    - "Meta units are composed only of rindle-admin-* classes (no unknown-class leakage)"
    - "The data-table unit shows sorted + selected + sticky state statically via fixture markup (no JS toggling)"
    - "The sticky-table internal scroll viewport carries an explicit opt-in scroll-region marker"
  artifacts:
    - path: "brandbook/src/admin-gallery.mjs"
      provides: "Meta-component cohesion panels + requiredSnippets entries for meta markers"
      contains: "data-rindle-admin-meta"
    - path: "brandbook/src/admin-gallery-check.mjs"
      provides: "assertMetaUnits + meta no-leakage scan + meta element screenshots"
      contains: "assertMetaUnits"
    - path: "brandbook/admin-gallery/index.html"
      provides: "Regenerated gallery with meta panels"
      contains: "data-rindle-admin-meta"
  key_links:
    - from: "brandbook/src/admin-gallery-check.mjs"
      to: "META_COMPONENTS"
      via: "assertMetaUnits loops the inventory and asserts visibility"
      pattern: "assertMetaUnits"
    - from: "brandbook/src/admin-gallery.mjs"
      to: "data-rindle-admin-meta"
      via: "requiredSnippets self-check over the meta markers"
      pattern: "data-rindle-admin-meta"
---

<objective>
Render each Level-2 meta-component as one labeled `data-rindle-admin-meta="{slug}"` cohesion
panel in the brandbook admin gallery across light/dark/auto, and prove it mechanically:
`assertMetaUnits` (every slug visible per theme), a meta no-leakage scan (composed only of
`rindle-admin-*` classes), and one element screenshot per unit. Implements UPLIFT-02 SC1 + SC3.

Purpose: The gallery is the *unit* cohesion proof surface (D-97-06/07). This plan makes each
meta-component appear as a single reviewable panel and gives the checker a mechanical hook to
assert presence, visibility, and Level-1-only composition — no SaaS visual-regression, no
Storybook.

Output: Meta panels in `admin-gallery.mjs` → regenerated `index.html`; `assertMetaUnits` +
no-leakage scan + 8 new element screenshots in `admin-gallery-check.mjs` (10 → 18).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-admin-level-2-meta-components-track-a/97-CONTEXT.md
@.planning/phases/97-admin-level-2-meta-components-track-a/97-UI-SPEC.md
@.planning/phases/97-admin-level-2-meta-components-track-a/97-PATTERNS.md
@.planning/phases/97-admin-level-2-meta-components-track-a/97-01-SUMMARY.md

<interfaces>
<!-- Existing gallery + checker structures the executor extends. Add parallel meta lists; never edit Level-1 literals. -->

admin-gallery.mjs:
  export const LEVEL_1_COMPONENT_STATE_MATRIX = { shell:['default'], table:[...], ... };  // do not edit
  const navItems = SURFACES.map(...);                  // ~129
  sections built with: id="${surfaceSlug(surface)}", data-rindle-admin-component / data-rindle-admin-state markers
  const requiredSnippets = [ '<link rel="stylesheet" ...>', 'data-theme="auto"', 'data-rindle-admin-root',
      ...requiredGalleryComponents.map(c => `data-rindle-admin-component="${c}"`),
      ...LEVEL_1_STATES.map(s => `data-rindle-admin-state="${s}"`) ];  // ~555
  const missingSnippets = requiredSnippets.filter(n => !html.includes(n));  // ~574 — throws on miss

admin-gallery-check.mjs:
  const expectedScreenshots = [ 10 names ];            // ~62
  const assertVisible = async (page, selector) => { count>0 && isVisible }   // ~79
  const assertComponentStateMatrix = async (page) => { ... }                 // ~85, called ~310
  const forbiddenClassParts = ['btn','card','dark',...]; leak scan at ~316
  const elementScreenshot = async (page, selector, name) => {...}            // ~282
  main flow: assertComponentStateMatrix → leak scan → selectTheme light/dark/auto → screenshots
  terminal: `admin gallery check passed - ${expectedScreenshots.length} screenshots written`  // ~380
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render meta-component cohesion panels with dual markers + sticky scroll-region opt-in</name>
  <files>brandbook/src/admin-gallery.mjs, brandbook/admin-gallery/index.html</files>
  <read_first>
    - brandbook/src/admin-gallery.mjs (section render ~336-470; the dual-marker convention — data-rindle-admin-component on Level-1 parts, table row ~147, button ~396, confirm-input ~417; requiredSnippets self-check ~555-577; the inline confirm-input enabling script ~528)
    - brandbook/src/admin-design-system-data.mjs (META_COMPONENTS — added in 97-01)
    - .planning/phases/97-admin-level-2-meta-components-track-a/97-PATTERNS.md (§ admin-gallery.mjs — fixture-marker render, meta-panel root, requiredSnippets extension; §"Explicit-marker exception")
    - .planning/phases/97-admin-level-2-meta-components-track-a/97-UI-SPEC.md (§"Level-2 Meta-Component Inventory" affordances; §"Copywriting Contract" terse operator voice)
    - .planning/phases/97-admin-level-2-meta-components-track-a/97-CONTEXT.md (D-97-03, D-97-06; §"Specific Ideas" — data-table is the richest unit)
  </read_first>
  <action>
    Add a meta-components gallery region rendering each of the 8 META_COMPONENTS as one
    `<section>` panel whose root carries `data-rindle-admin-meta="{slug}"` (D-97-06), composed
    ONLY of Level-1 `rindle-admin-*` parts that each still carry their existing
    `data-rindle-admin-component` markers (dual-marker convention). Author all fixture copy in
    the terse operator/SRE voice from 97-UI-SPEC §"Copywriting Contract" (e.g. toolbar title
    `Assets`; bulk bar `3 selected — Erase`; sort label `Sort by {column}`; filter empty
    `No assets match these filters`). For the `data-table` unit, show the rich states STATICALLY
    via fixture markup (no JS toggling, D-97-03): a `<th>` with `aria-sort="ascending"`, rows
    with `[data-rindle-admin-selected]`, a header select-all `<input type="checkbox">`, the
    `.rindle-admin-table--sticky` modifier, and a contextual bulk-action toolbar
    (`role="toolbar"`) shown in its active state. Wrap the sticky-table internal scroll viewport
    in an element carrying the explicit opt-in marker `data-rindle-admin-scroll-region` so 97-03's
    no-h-scroll gate skips it (D-94-07 — explicit marker, never auto-detect). Extend
    `requiredSnippets` (do NOT remove existing entries) with
    `...META_COMPONENTS.map(slug => \`data-rindle-admin-meta="${slug}"\`)` plus `'aria-sort='`,
    `'data-rindle-admin-selected'`, and `'data-rindle-admin-scroll-region'`. If a meta section id
    is added, register its nav link the same way SURFACES section ids are. Avoid `btn`/`card`/`dark`
    class substrings. Regenerate `index.html` by running the generator (never hand-edit, D-97-05).
  </action>
  <verify>
    <automated>node brandbook/src/admin-gallery.mjs && grep -c "data-rindle-admin-meta=" brandbook/admin-gallery/index.html | grep -qvx 0 && grep -q "data-rindle-admin-scroll-region" brandbook/admin-gallery/index.html && grep -q "aria-sort" brandbook/admin-gallery/index.html</automated>
  </verify>
  <acceptance_criteria>
    `node brandbook/src/admin-gallery.mjs` exits 0 (requiredSnippets self-check passes — all 8
    meta markers + aria-sort + selected + scroll-region present);
    `grep -oE 'data-rindle-admin-meta="[a-z-]+"' brandbook/admin-gallery/index.html | sort -u | wc -l`
    returns 8 (one panel per slug);
    `brandbook/admin-gallery/index.html` contains `data-rindle-admin-scroll-region` exactly on the
    sticky-table viewport and at least one `aria-sort="ascending"`;
    no meta panel introduces a class containing `btn`/`card`/`dark`
    (`grep -oE 'class="[^"]*"' brandbook/admin-gallery/index.html | grep -E "btn|card|dark"` returns nothing).
  </acceptance_criteria>
  <done>All 8 meta-components render as labeled data-rindle-admin-meta panels with Level-1-only parts; data-table shows static sorted/selected/sticky state; sticky scroll viewport carries the explicit opt-in marker; requiredSnippets gates the markers.</done>
</task>

<task type="auto">
  <name>Task 2: Add assertMetaUnits, meta no-leakage scan, and per-unit element screenshots</name>
  <files>brandbook/src/admin-gallery-check.mjs</files>
  <read_first>
    - brandbook/src/admin-gallery-check.mjs (expectedScreenshots ~62; assertVisible ~79; assertComponentStateMatrix ~85 called ~310; forbiddenClassParts + leak scan ~52,316; elementScreenshot ~282; theme flow + screenshot calls ~329-352; terminal count ~380)
    - brandbook/src/admin-design-system-data.mjs (META_COMPONENTS — added in 97-01)
    - .planning/phases/97-admin-level-2-meta-components-track-a/97-PATTERNS.md (§ admin-gallery-check.mjs — assertion-helper, leakage-scan, screenshot-list patterns; Open Question 1: +8 → 18 screenshots)
    - .planning/phases/97-admin-level-2-meta-components-track-a/97-RESEARCH.md (Must-Answer 4 — assertMetaUnits + composed-only-of-known-selectors scan)
    - .planning/phases/97-admin-level-2-meta-components-track-a/97-CONTEXT.md (D-97-07)
  </read_first>
  <action>
    In `admin-gallery-check.mjs`, import `META_COMPONENTS` and add `assertMetaUnits(page)` that
    loops the inventory and calls the existing `assertVisible(page, '[data-rindle-admin-meta="{slug}"]')`
    for each slug; wire it into the main browser flow next to `assertComponentStateMatrix`. Add a
    meta no-leakage scan (D-97-07): collect every class on `[data-rindle-admin-meta] [class]`
    elements and assert none fail `startsWith('rindle-admin-')` — "composed only of known Level-1
    selectors." Run `assertMetaUnits` under each theme the checker already drives (light/dark/auto
    via `selectTheme`) so visibility is proven per theme. Add one `elementScreenshot` per
    meta-component (8 new), appending their names to `expectedScreenshots` so the pinned count goes
    10 → 18 (Open Question 1 / Pitfall 2 — the ExUnit literal is bumped in 97-04, not here). Do NOT
    remove or reorder the existing 10 screenshot entries. Avoid `btn`/`card`/`dark` substrings in
    code/strings (forbidden-dep regex scans this file).
  </action>
  <verify>
    <automated>node brandbook/src/admin-gallery-check.mjs 2>&1 | grep -q "18 screenshots written"</automated>
  </verify>
  <acceptance_criteria>
    `node brandbook/src/admin-gallery-check.mjs` exits 0 and its terminal line reads
    `admin gallery check passed - 18 screenshots written`;
    the run includes an `assertMetaUnits` pass (every slug `isVisible`) and the meta no-leakage
    scan finds zero non-`rindle-admin-` classes (a deliberately leaked class would throw
    `meta units leak non-rindle classes: ...`);
    `grep -c "elementScreenshot" brandbook/src/admin-gallery-check.mjs` increased by 8 over baseline;
    the 8 new screenshot PNGs exist under `brandbook/admin-gallery/screenshots/`.
  </acceptance_criteria>
  <done>assertMetaUnits proves every meta unit visible per theme; the no-leakage scan proves Level-1-only composition; 8 meta element screenshots written (count 18); existing 10 unchanged.</done>
</task>

</tasks>

<verification>
- `node brandbook/src/admin-gallery.mjs` exits 0 (requiredSnippets covers all 8 meta markers + aria-sort + selected + scroll-region).
- `node brandbook/src/admin-gallery-check.mjs` exits 0 and reports `18 screenshots written`; assertMetaUnits + meta no-leakage scan pass under light/dark/auto.
- `index.html` has exactly 8 distinct `data-rindle-admin-meta` panels; data-table shows static sorted/selected/sticky state; sticky viewport opts in via `data-rindle-admin-scroll-region`.
</verification>

<success_criteria>
Each meta-component appears in the gallery as one cohesion unit, mechanically asserted visible
across light/dark/auto and proven to compose only of Level-1 `rindle-admin-*` selectors (D-97-06/07,
SC3 + SC1). Screenshot count is 18 (8 new meta element shots); the ExUnit pinned literal is
updated in 97-04.
</success_criteria>

<output>
Create `.planning/phases/97-admin-level-2-meta-components-track-a/97-02-SUMMARY.md` when done.
</output>

---
phase: 97-admin-level-2-meta-components-track-a
plan: 03
type: execute
wave: 3
depends_on: ["97-02"]
files_modified:
  - examples/adoption_demo/e2e/support/admin-polish.js
  - brandbook/src/admin-gallery-check.mjs
autonomous: true
requirements: [UPLIFT-02]

must_haves:
  truths:
    - "assertConsistentRhythm returns off-grid intra-unit spacing offenders, never throws"
    - "assertNoHorizontalScroll returns per-meta-unit horizontal-overflow offenders, skipping explicit scroll-region markers, never throws"
    - "Both checks are wired into assertAdminPolish as offender-returning sub-assertions with a single aggregated throw per state"
    - "Both checks run over the actual data-rindle-admin-meta units in the gallery (via gallery-check) and report zero offenders"
    - "OVERLAP_ENFORCED stays false this plan (warn-only overlap cycle)"
  artifacts:
    - path: "examples/adoption_demo/e2e/support/admin-polish.js"
      provides: "assertConsistentRhythm + assertNoHorizontalScroll, wired + exported"
      contains: "assertConsistentRhythm"
    - path: "brandbook/src/admin-gallery-check.mjs"
      provides: "Runs the two new checks over the gallery meta units"
      contains: "assertConsistentRhythm"
  key_links:
    - from: "examples/adoption_demo/e2e/support/admin-polish.js"
      to: "assertAdminPolish"
      via: "run(\"consistentRhythm\") + run(\"noHorizontalScroll\") aggregated offenders"
      pattern: "consistentRhythm"
    - from: "brandbook/src/admin-gallery-check.mjs"
      to: "[data-rindle-admin-meta]"
      via: "imports the checks via createRequire(playwright) and evaluates over the gallery"
      pattern: "assertNoHorizontalScroll"
---

<objective>
Add the two new offender-returning sub-assertions that prove Level-2 cohesion: `assertConsistentRhythm`
(intra-unit gaps/margins/paddings resolve to the 4px grid ∪ {12,44}) and `assertNoHorizontalScroll`
(per-meta-unit root has no horizontal overflow, sticky scroll-region excepted). Wire both into
`assertAdminPolish`, AND run them over the actual `[data-rindle-admin-meta]` units in the gallery
via `admin-gallery-check.mjs`. Keep `OVERLAP_ENFORCED=false` (warn-only cycle). Implements UPLIFT-02
SC2 (rhythm + no-horizontal-scroll), warn-only overlap.

Purpose: Closes the Wave 0 gap — the gate currently has no dedicated rhythm or per-unit
no-h-scroll check (D-97-08). Both must RETURN offenders (never throw) so `assertAdminPolish`
aggregates a single throw per state (the documented offender-returning contract).

Architecture note (justified deviation, recorded for the executor): the `[data-rindle-admin-meta]`
units exist ONLY in the brandbook gallery — the live adoption_demo LiveView pages carry no meta
markers — so the checks find their real offenders when run over the gallery. The seam still wires
both into `assertAdminPolish` (a no-op over surfaces lacking meta units, preserving the future
live-page seam) AND invokes them from `admin-gallery-check.mjs`, which already launches Chromium
over the gallery and can `createRequire` the CommonJS `admin-polish.js` from the adoption_demo
package (same mechanism it already uses to load `playwright`).

Output: `assertConsistentRhythm` + `assertNoHorizontalScroll` in `admin-polish.js` (wired +
exported); both invoked over the gallery in `admin-gallery-check.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-admin-level-2-meta-components-track-a/97-CONTEXT.md
@.planning/phases/97-admin-level-2-meta-components-track-a/97-UI-SPEC.md
@.planning/phases/97-admin-level-2-meta-components-track-a/97-PATTERNS.md
@.planning/phases/97-admin-level-2-meta-components-track-a/97-RESEARCH.md
@.planning/phases/97-admin-level-2-meta-components-track-a/97-02-SUMMARY.md

<interfaces>
<!-- admin-polish.js current shape — add NEW sub-assertions; do NOT flip OVERLAP_ENFORCED here. -->

admin-polish.js:
  const DEFAULT_ROOT = "[data-rindle-admin-root]";        // ~21
  const SUBPIXEL_TOLERANCE = 0.5;  // reuse for rhythm tolerance     // ~23
  const CLIP_TOLERANCE = 1;        // reuse for no-h-scroll slack     // ~24
  const OVERLAP_ENFORCED = false;  // 97-04 flips this, NOT here      // ~30
  const DEFAULT_INTERACTIVE_SELECTORS = [ ... ];          // ~33
  const POLISH_EXEMPTIONS = Object.freeze({ ... });
  async function assertNoClippedText(page, root = DEFAULT_ROOT) { return page.evaluate(...) }  // ~111, RETURNS string[]
  async function assertAdminPolish(page, { surface, viewport, root, interactiveSelectors }) {  // ~484
    const run = async (name, prefix, fn, { warnOnly=false }={}) => { ... offenders → violations|warnings };  // ~498
    await run("noClippedText", "clipped-text", () => assertNoClippedText(page, root));            // ~508
    await run("noInteractiveOverlap", "overlap", ..., { warnOnly: !OVERLAP_ENFORCED });           // ~512
    if (violations.length) throw new Error(`Admin polish gate failed for surface=...`);
  }
  module.exports = { assertAdminPolish, ..., OVERLAP_ENFORCED };   // ~533

admin-gallery-check.mjs:
  const adoptionRequire = createRequire(join(repoRoot, 'examples','adoption_demo','package.json'));
  const { chromium } = adoptionRequire('playwright');     // ~14-15  (use the same require for admin-polish.js)
  main flow launches chromium over the gallery; selectTheme light/dark/auto; assertMetaUnits (97-02)

support/admin.js (DO NOT duplicate):
  async function expectNoHorizontalScroll(page) { documentElement + [data-rindle-admin-root] scrollWidth<=clientWidth }  // ~86 — PAGE level
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add assertConsistentRhythm + assertNoHorizontalScroll and wire into assertAdminPolish</name>
  <files>examples/adoption_demo/e2e/support/admin-polish.js</files>
  <read_first>
    - examples/adoption_demo/e2e/support/admin-polish.js (constants ~21-55; assertNoClippedText offender-returning shape ~111-172; assertNoInteractiveOverlap direct-sibling pattern ~400-444; assertAdminPolish aggregator + run() ~484-531; module.exports ~533-546)
    - examples/adoption_demo/e2e/support/admin.js (expectNoHorizontalScroll ~86-96 — the PAGE-level variant; do NOT duplicate it)
    - .planning/phases/97-admin-level-2-meta-components-track-a/97-RESEARCH.md (Pattern 3 + Pitfall 1 rhythm algorithm/tolerance; Must-Answer 2 no-h-scroll assertion + sticky exception)
    - .planning/phases/97-admin-level-2-meta-components-track-a/97-PATTERNS.md (§ admin-polish.js — offender-returning sub-assertion, aggregator, explicit-marker exception)
    - .planning/phases/97-admin-level-2-meta-components-track-a/97-CONTEXT.md (D-97-08, D-97-09, D-97-10; D-94-07 no auto-detection)
  </read_first>
  <behavior>
    - assertConsistentRhythm walks ONLY `[data-rindle-admin-meta]` subtrees; checks `rowGap`,
      `columnGap`, `marginTop`, `marginBottom`, `paddingTop`, `paddingBottom`, `paddingLeft`,
      `paddingRight`; allowed set `{4,8,16,24,32,48,64}` ∪ `{12,44}`; `0px` always valid;
      ±0.5px tolerance; does NOT check height/width/min-height/line-height; returns offenders
      `"{slug} {tag} {prop}={px}px off-grid"`; never throws.
    - assertNoHorizontalScroll iterates `[data-rindle-admin-meta]` unit roots; skips any unit under
      `[data-rindle-admin-scroll-region]`; flags `scrollWidth > clientWidth + CLIP_TOLERANCE`;
      returns offenders; never throws.
    - assertAdminPolish.run() invokes both as offender-returning checks; both feed `violations`
      (hard) — not warnOnly; the single-aggregated-throw-per-state contract holds.
    - Both are exported from module.exports.
    - OVERLAP_ENFORCED is still `false`.
  </behavior>
  <action>
    Add `assertConsistentRhythm(page, root = DEFAULT_ROOT)` mirroring the `assertNoClippedText`
    offender-returning shape: inside `page.evaluate`, walk only `[data-rindle-admin-meta]` subtrees
    (the intra-unit requirement, Pitfall 1 — dramatically cuts noise), read computed style on each
    descendant, and for the rhythm-bearing properties `rowGap`/`columnGap`/`marginTop`/`marginBottom`
    /`paddingTop`/`paddingBottom`/`paddingLeft`/`paddingRight`, push an offender
    `"{slug} {tag} {prop}={px}px off-grid"` when the px value is non-zero and not within
    `SUBPIXEL_TOLERANCE` of the allowed set `{4,8,16,24,32,48,64}` ∪ documented exceptions `{12,44}`.
    Do NOT check height/width/min-height/line-height; treat `0` as always valid. Add
    `assertNoHorizontalScroll(page, root = DEFAULT_ROOT)` (per-meta-UNIT, not the page-level variant
    already in `support/admin.js` — do NOT duplicate that): inside `page.evaluate`, for each
    `[data-rindle-admin-meta]` unit root, `continue` if `unit.closest('[data-rindle-admin-scroll-region]')`
    (explicit opt-in, D-94-07 — never auto-detect), else push `"{slug} x({scrollWidth}>{clientWidth})"`
    when `scrollWidth > clientWidth + CLIP_TOLERANCE`. Both functions RETURN an array and NEVER throw
    (the documented parseColor-bug contract). Wire both into `assertAdminPolish` via
    `await run("consistentRhythm", "rhythm", () => assertConsistentRhythm(page, root))` and
    `await run("noHorizontalScroll", "h-scroll", () => assertNoHorizontalScroll(page, root))` (both
    feeding violations, NOT warnOnly). Export both from `module.exports`. Leave `OVERLAP_ENFORCED`
    at `false` (97-04 flips it). Avoid `btn`/`card`/`dark`/`tailwind` substrings in any comment/string.
  </action>
  <verify>
    <automated>node -e "const m=require('./examples/adoption_demo/e2e/support/admin-polish.js'); if(typeof m.assertConsistentRhythm!=='function'||typeof m.assertNoHorizontalScroll!=='function') process.exit(1); if(m.OVERLAP_ENFORCED!==false) process.exit(2); console.log('exports ok, OVERLAP_ENFORCED=false');"</automated>
  </verify>
  <acceptance_criteria>
    `node -e "require('./examples/adoption_demo/e2e/support/admin-polish.js')"` loads without error
    and exports both `assertConsistentRhythm` and `assertNoHorizontalScroll` as functions with
    `OVERLAP_ENFORCED === false`;
    `grep -c "await run(\"consistentRhythm\"" examples/adoption_demo/e2e/support/admin-polish.js` returns 1
    and `grep -c "await run(\"noHorizontalScroll\"" examples/adoption_demo/e2e/support/admin-polish.js` returns 1;
    neither new function body contains `throw` (`grep -n "throw" ` inside the two function ranges returns nothing);
    `grep -c "expectNoHorizontalScroll" examples/adoption_demo/e2e/support/admin-polish.js` returns 0 (no duplication of the page-level check).
  </acceptance_criteria>
  <done>Both offender-returning checks exist, are wired into assertAdminPolish as hard (non-warnOnly) sub-assertions, are exported, never throw, and OVERLAP_ENFORCED stays false.</done>
</task>

<task type="auto">
  <name>Task 2: Run the rhythm + no-h-scroll checks over the gallery meta units (zero offenders)</name>
  <files>brandbook/src/admin-gallery-check.mjs</files>
  <read_first>
    - brandbook/src/admin-gallery-check.mjs (createRequire(playwright) ~14-15; chromium launch + gallery flow ~300-380; assertMetaUnits + theme flow from 97-02)
    - examples/adoption_demo/e2e/support/admin-polish.js (the two new exported checks — task 1)
    - .planning/phases/97-admin-level-2-meta-components-track-a/97-RESEARCH.md (gallery-check launches Chromium over the gallery via adoptionRequire; this is where meta units actually exist)
    - .planning/phases/97-admin-level-2-meta-components-track-a/97-CONTEXT.md (D-97-08 — rhythm/no-h-scroll are the merge-blocking deliverable)
  </read_first>
  <action>
    In `admin-gallery-check.mjs`, load `assertConsistentRhythm` and `assertNoHorizontalScroll`
    from `examples/adoption_demo/e2e/support/admin-polish.js` using the SAME `adoptionRequire`
    (createRequire over the adoption_demo package) already used to load `playwright`. In the main
    gallery browser flow — after `assertMetaUnits`, under at least the light theme (the meta units
    render identically across themes) — call both checks over the gallery page passing the gallery
    root, collect the returned offender arrays, and `assert` each is empty, throwing a descriptive
    message listing offenders if not (mirrors the file's existing `assert(...)` convention). This is
    the surface where `[data-rindle-admin-meta]` units actually exist, so it is the real proof that
    intra-unit rhythm is on-grid and no unit root overflows horizontally (the sticky table opts out
    via its `data-rindle-admin-scroll-region` marker from 97-02). Avoid `btn`/`card`/`dark` substrings.
  </action>
  <verify>
    <automated>node brandbook/src/admin-gallery-check.mjs 2>&1 | grep -q "18 screenshots written"</automated>
  </verify>
  <acceptance_criteria>
    `node brandbook/src/admin-gallery-check.mjs` exits 0 with terminal line
    `admin gallery check passed - 18 screenshots written` — meaning both `assertConsistentRhythm`
    and `assertNoHorizontalScroll` returned ZERO offenders over the gallery meta units (a deliberately
    off-grid gap or an un-marked overflowing unit would make the check throw and the script exit non-zero);
    `grep -c "assertConsistentRhythm" brandbook/src/admin-gallery-check.mjs` returns ≥1 and
    `grep -c "assertNoHorizontalScroll" brandbook/src/admin-gallery-check.mjs` returns ≥1;
    the sticky data-table unit (which has internal scroll) does NOT appear as a no-h-scroll offender
    (its `data-rindle-admin-scroll-region` opt-in is honored).
  </acceptance_criteria>
  <done>The gallery-check runs both new checks over the real meta units and reports zero offenders; rhythm is on-grid and no unit root overflows (sticky table excepted via explicit marker).</done>
</task>

</tasks>

<verification>
- `admin-polish.js` exports `assertConsistentRhythm` + `assertNoHorizontalScroll`, both offender-returning, wired into `assertAdminPolish`; `OVERLAP_ENFORCED` still `false`.
- `node brandbook/src/admin-gallery-check.mjs` exits 0 (`18 screenshots written`) with both new checks returning zero offenders over the gallery meta units.
- No duplication of the page-level `expectNoHorizontalScroll`; sticky table excepted via `data-rindle-admin-scroll-region`.
- The adoption-demo lane still passes (the checks are no-ops over surfaces without meta markers, preserving the future live-page seam).
</verification>

<success_criteria>
SC2 rhythm + no-horizontal-scroll are proven by two new offender-returning checks that run over
the actual `[data-rindle-admin-meta]` units and report zero offenders; intra-unit spacing is on
the 4px grid (∪ 12,44) and no unit root overflows horizontally (sticky table opts out via explicit
marker). Overlap stays warn-only this plan; the enforce flip is 97-04 after a green warn cycle (D-97-11).
</success_criteria>

<output>
Create `.planning/phases/97-admin-level-2-meta-components-track-a/97-03-SUMMARY.md` when done.
</output>

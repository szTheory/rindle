# Phase 97 — Deferred Items

| Category | Item | Discovered | Status |
|----------|------|------------|--------|
| drift-gate | `priv/static/rindle_admin/rindle-admin.css` byte-equality with the regenerated `brandbook/tokens/rindle-admin.css` — `admin_design_system_validation_test.exs:246` (ADMIN-02) fails until the shipped copy is synced. **Deferred to Plan 97-04 by design** (97-01 PLAN success criteria + `files_modified` excludes `priv/`). Resolve via `node brandbook/src/sync-admin-css.mjs` in 97-04, which flows through the existing build → contrast → gallery-check → sync → empty-diff gate (D-97-05). | 97-01 (Task 2) | **RESOLVED in 97-04** — `node brandbook/src/sync-admin-css.mjs` ran; `priv` copy is now byte-identical to `brandbook/tokens/rindle-admin.css` (`cmp -s` exits 0). |
| review-coverage (WR-02) | **The two new cohesion checks are vacuous no-ops in the merge-blocking `adoption-demo-e2e` lane.** Live admin surfaces emit `data-rindle-admin-metadata-list`, not `[data-rindle-admin-meta]`, so `assertConsistentRhythm` + `assertNoHorizontalScroll` always return `[]` inside `assertAdminPolish` (admin-polish.js ~599-600) with **no count-guard** on the live-lane side. Real enforcement authority lives only in the count-guarded gallery check (`assertMetaCohesion`), which is non-vacuous. Phase goal still met (gallery gate bites where the units exist), but the 97-03 SUMMARY overstated live-lane coverage. **Suggested follow-up:** add a live-lane count-guard (assert ≥1 `[data-rindle-admin-meta]` present before claiming a pass) OR have the live admin emit `data-rindle-admin-meta` so the checks exercise real surfaces. | 97 code review (WR-02) | open — WARNING, tracked follow-up |
| review-fragility (WR-01) | **Dead opt-out in `assertNoHorizontalScroll`.** The `unit.closest("[data-rindle-admin-scroll-region]")` skip can never fire — the scroll-region marker is a *descendant* of the data-table meta root, not an ancestor — so the data-table passes incidentally (inner `overflow:auto` clips its own width), not via the documented opt-out. Latent fragility, not a current failure. **Suggested follow-up:** move the scroll-region marker to the meta root (or switch to a descendant query) so the opt-out is real. | 97 code review (WR-01) | open — WARNING, tracked follow-up |
| review-coverage (WR-03) | **Ungated contrast pair on new meta surfaces.** `.rindle-admin-bulk-bar` and `[data-rindle-admin-selected]` paint primary `--rindle-text` on `--rindle-surface-sunken`, but `CONSOLE_CONTRAST_PAIRS` only covers `text-secondary` on sunken. Passes AA today but is unverified by any gate. **Suggested follow-up:** add the `text`-on-`surface-sunken` pair to `CONSOLE_CONTRAST_PAIRS`. | 97 code review (WR-03) | open — WARNING, tracked follow-up |
| a11y-host-cascade | **focus-visible token loses to the adoption_demo HOST app's daisyUI cascade.** The warn-only `adoption-demo-e2e` lane exits non-zero on `assertFocusVisibleTokens` (admin-polish.js Check 4, added at 95-02) when run against the **live** admin console: the host app's daisyUI `.menu { outline: none }` plus its 3px outline override out-cascade the shipped rindle focus-visible token (the intended 2px `#123A35`). **Root cause:** host-app CSS layering, not the shipped `rindle-admin` CSS — the failure persists byte-for-byte after the 97-04 priv↔brandbook sync (verified independent of the sync). **Out of scope for 97-04:** none of this plan's three scoped files (`admin-polish.js`, `priv/static/rindle_admin/rindle-admin.css`, the ExUnit pinned literal) can fix a cascade that lives in the host `adoption_demo` stylesheet load order. Per maintainer decision (Option A, 2026-06-17) the defect is deferred rather than masked — **no `POLISH_EXEMPTIONS` entry was added** (explicitly rejected). **Consequence:** the `adoption-demo-e2e` lane stays RED until this fix lands. **Suggested follow-up:** a dedicated plan/phase touching the `adoption_demo` host CSS layering (ensure the shipped `rindle-admin` focus-visible token wins the cascade — e.g. raise specificity/layer order, scope the daisyUI `.menu { outline: none }` away from `[data-rindle-admin-root]`), OR harden the shipped `rindle-admin` focus-visible cascade so it survives a hostile host stylesheet. | 97-04 (Task 1 checkpoint) | deferred → dedicated follow-up plan/phase |

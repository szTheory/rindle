---
phase: 15-ci-integrity-and-publish-preflight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/release_preflight.sh
  - CHANGELOG.md
  - mix.exs
  - test/install_smoke/package_metadata_test.exs
autonomous: true
requirements:
  - PUBLISH-02
must_haves:
  truths:
    - "The shared preflight can unpack the release artifact without depending on a long checkout-relative path, so the 2026-04-29 `inner tarball error, file name too long` failure is closed at the repo layer."
    - "The shipped Hex artifact includes a root `CHANGELOG.md` with a `0.1.0` entry per D-12, and the preflight fails if that contract drifts."
    - "Metadata review remains artifact-shaped per D-10 and D-11: package name, version, license, links, and required shipped paths are proved from the unpacked tarball before any live tag."
  artifacts:
    - path: "scripts/release_preflight.sh"
      provides: "Short-path, reusable release preflight entrypoint"
    - path: "CHANGELOG.md"
      provides: "Root changelog with first-release entry"
    - path: "mix.exs"
      provides: "Package allowlist including changelog"
    - path: "test/install_smoke/package_metadata_test.exs"
      provides: "Executable artifact contract for changelog and metadata"
  key_links:
    - from: "scripts/release_preflight.sh"
      to: "test/install_smoke/package_metadata_test.exs"
      via: "RINDLE_INSTALL_SMOKE_PACKAGE_ROOT built from the shared unpack output"
      pattern: "RINDLE_INSTALL_SMOKE_PACKAGE_ROOT"
    - from: "mix.exs"
      to: "CHANGELOG.md"
      via: "package.files allowlist"
      pattern: "CHANGELOG.md"
---

<objective>
Close the repo-owned preflight failures before any release-candidate signoff by hardening the shared unpack path and locking the first-release changelog into the shipped artifact contract.

Purpose: Phase 15 cannot produce authoritative exact-SHA CI proof while the shared preflight still fails on the repo-controlled tarball path and changelog coverage gap.
Output: A path-resilient `scripts/release_preflight.sh`, a root `CHANGELOG.md` with a `0.1.0` entry, updated package allowlist, and artifact tests that fail if the release contract drifts.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-ci-integrity-and-publish-preflight/15-CONTEXT.md
@.planning/phases/15-ci-integrity-and-publish-preflight/15-RESEARCH.md
@.planning/phases/15-ci-integrity-and-publish-preflight/15-PATTERNS.md
@.planning/milestones/v1.2-phases/12-public-verification-and-release-operations/12-01-PLAN.md
@.planning/milestones/v1.2-phases/13-release-traceability-and-runbook-alignment/13-01-PLAN.md
@.github/workflows/ci.yml
@.github/workflows/release.yml
@scripts/release_preflight.sh
@test/install_smoke/package_metadata_test.exs
@mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden the shared unpack path against long-runner workspace failures</name>
  <files>scripts/release_preflight.sh</files>
  <action>Update `scripts/release_preflight.sh` per D-07 and D-08 so the shared preflight no longer defaults to unpacking into `$ROOT_DIR/$PACKAGE_NAME`, which is the likely repo-controlled contributor to the 2026-04-29 GitHub Actions failure `inner tarball error, file name too long`. Build the unpacked artifact inside a short temporary parent directory outside the checkout by default, preserve `RINDLE_INSTALL_SMOKE_PACKAGE_ROOT` for downstream tests, add cleanup trapping, and keep an opt-in escape hatch for maintainer inspection (for example a keep-artifact env var) so D-10 manual tarball review still works without editing the script. Do not add Hex account checks or publish steps here; this script remains repo-owned preflight only per D-05 and D-09.</action>
  <verify>
    <automated>bash -n scripts/release_preflight.sh && MIX_ENV=dev bash scripts/release_preflight.sh</automated>
  </verify>
  <done>`scripts/release_preflight.sh` no longer relies on a checkout-relative unpack destination, all existing preflight gates still run in order, and the unpacked artifact remains inspectable for manual tarball review.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the first-release changelog to the shipped artifact contract</name>
  <files>CHANGELOG.md, mix.exs, test/install_smoke/package_metadata_test.exs</files>
  <behavior>
    - Test 1: the unpacked artifact contains `CHANGELOG.md` and the file is listed in package metadata.
    - Test 2: `CHANGELOG.md` contains a `0.1.0` entry, and package metadata assertions for `rindle`, `MIT`, and GitHub links still pass from the unpacked artifact.
  </behavior>
  <action>Create a root `CHANGELOG.md` with a real `0.1.0` entry per D-12, add the file to `mix.exs` package allowlist, and extend `test/install_smoke/package_metadata_test.exs` so the release contract proves the changelog ships alongside the existing metadata/license/link assertions per D-10 and D-11. Reuse the current install-smoke pattern from Phases 12 through 14: assert against the unpacked artifact, not source-only config, and fail if the changelog drops out of the package. Do not introduce placeholder release notes or future-version scaffolding; the file should describe the first live release cut only.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/install_smoke/package_metadata_test.exs</automated>
  </verify>
  <done>The repo has a root `CHANGELOG.md` with a `0.1.0` entry, `mix.exs` ships it in the Hex package, and the package metadata test proves changelog presence plus the existing metadata/license/link contract from the unpacked tarball.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo checkout → unpacked Hex artifact | Repository files become the package tarball that maintainers and CI will trust before publish |
| shared preflight script → CI/release workflows | A single repo-owned script determines whether release automation proceeds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | `scripts/release_preflight.sh` | mitigate | Build in a short temporary path and keep the shared preflight sequence centralized so runner-path quirks cannot silently change the release outcome. |
| T-15-02 | Repudiation | packaged artifact contents | mitigate | Assert changelog, metadata, license, and link fields from the unpacked artifact in `test/install_smoke/package_metadata_test.exs`, not from `mix.exs` alone. |
| T-15-03 | Denial of Service | GitHub Actions release preflight | mitigate | Remove the known repo-controlled path-length failure mode so CI can complete on the exact release candidate SHA. |
</threat_model>

<verification>
Run `MIX_ENV=test mix test test/install_smoke/package_metadata_test.exs` and `MIX_ENV=dev bash scripts/release_preflight.sh`. Both must pass from the repo checkout with no path-length unpack failure and with `CHANGELOG.md` present in the unpacked artifact.
</verification>

<success_criteria>
- The shared preflight no longer depends on a long checkout-relative unpack path.
- `CHANGELOG.md` exists at repo root with a `0.1.0` entry and ships in the Hex artifact.
- Package metadata proof continues to validate `rindle`, `MIT`, GitHub links, and required shipped paths from the unpacked artifact.
</success_criteria>

<output>
After completion, create `.planning/phases/15-ci-integrity-and-publish-preflight/15-01-SUMMARY.md`
</output>

---
phase: 87-docker-demo-dx
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/Dockerfile.cohort-demo
autonomous: true
requirements:
  - DX-02
requirements_addressed:
  - DX-02
must_haves:
  truths:
    - "D-87-08: dependency files are copied before app source and mix deps.get runs before full repo copy."
    - "D-87-09: Docker preview remains a simple build, not a release build, split image, Traefik setup, or production topology."
    - "D-87-10: routine source, style, and template edits do not invalidate the Hex dependency fetch layer."
  artifacts:
    - path: "docker/Dockerfile.cohort-demo"
      provides: "cache-friendly Cohort demo build"
      contains: "mix deps.get"
  key_links:
    - from: "docker/Dockerfile.cohort-demo"
      to: "mix.exs and examples/adoption_demo/mix.exs"
      via: "dependency manifests copied before source"
      pattern: "COPY mix.exs mix.lock"
---

<objective>
Fix the Cohort Dockerfile cache shape so dependency fetches are isolated from routine source, style, and template edits.

Purpose: Later UI phases need fast rebuilds while iterating on templates and styles.
Output: Updated Dockerfile instruction order with source-order proof.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/87-docker-demo-dx/87-CONTEXT.md
@.planning/phases/87-docker-demo-dx/87-RESEARCH.md
@.planning/phases/87-docker-demo-dx/87-PATTERNS.md
@.planning/phases/87-docker-demo-dx/87-VALIDATION.md
@guides/docker_demo_dx.md
@docker/Dockerfile.cohort-demo
@docker/cohort-demo-entrypoint.sh
@.dockerignore

<interfaces>
Dockerfile cache contract:
- Copy root mix.exs and mix.lock into /app before dependency fetch.
- Copy examples/adoption_demo/mix.exs and examples/adoption_demo/mix.lock before dependency fetch.
- Run mix local.hex --force, mix local.rebar --force, and mix deps.get before COPY . /app.
- Run mix assets.vendor and mix compile only after COPY . /app.
- Keep EXPOSE 4102 and ENTRYPOINT ["/entrypoint.sh"].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder Dockerfile dependency layer</name>
  <read_first>
    docker/Dockerfile.cohort-demo
    docker/cohort-demo-entrypoint.sh
    .dockerignore
    guides/docker_demo_dx.md
    .planning/phases/87-docker-demo-dx/87-CONTEXT.md
    .planning/phases/87-docker-demo-dx/87-RESEARCH.md
    .planning/phases/87-docker-demo-dx/87-PATTERNS.md
    .planning/phases/87-docker-demo-dx/87-VALIDATION.md
  </read_first>
  <files>docker/Dockerfile.cohort-demo</files>
  <action>Reorder docker/Dockerfile.cohort-demo per D-87-08 through D-87-10: copy mix.exs and mix.lock, then examples/adoption_demo/mix.exs and examples/adoption_demo/mix.lock before the full source copy; run mix local.hex --force, mix local.rebar --force, and mix deps.get before COPY . /app; run mix assets.vendor and mix compile only after COPY . /app. Keep the current base image, apt package install, WORKDIR /app, and MIX_ENV=prod. Add at most one short cache-contract comment if it helps future readers.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; dockerfile=docker/Dockerfile.cohort-demo; root_copy=$(awk "/COPY mix\\.exs mix\\.lock \\/app\\// {print NR; exit}" "$dockerfile"); demo_copy=$(awk "/COPY examples\\/adoption_demo\\/mix\\.exs examples\\/adoption_demo\\/mix\\.lock/ {print NR; exit}" "$dockerfile"); deps_get=$(awk "/mix deps\\.get/ {print NR; exit}" "$dockerfile"); source_copy=$(awk "/COPY \\. \\/app/ {print NR; exit}" "$dockerfile"); assets_vendor=$(awk "/mix assets\\.vendor/ {print NR; exit}" "$dockerfile"); compile=$(awk "/mix compile/ {print NR; exit}" "$dockerfile"); test -n "$root_copy"; test -n "$demo_copy"; test -n "$deps_get"; test -n "$source_copy"; test -n "$assets_vendor"; test -n "$compile"; test "$root_copy" -lt "$deps_get"; test "$demo_copy" -lt "$deps_get"; test "$deps_get" -lt "$source_copy"; test "$source_copy" -lt "$assets_vendor"; test "$source_copy" -lt "$compile"'</automated>
  </verify>
  <acceptance_criteria>
    - Dockerfile line-order assertion proves dependency manifests and mix deps.get occur before COPY . /app.
    - Dockerfile line-order assertion proves mix assets.vendor and mix compile occur after COPY . /app.
  </acceptance_criteria>
  <done>DX-02 cache ordering is implemented with deterministic source-order proof.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve preview-only Dockerfile boundary</name>
  <read_first>
    docker/Dockerfile.cohort-demo
    docker/cohort-demo-entrypoint.sh
    guides/docker_demo_dx.md
    .planning/phases/87-docker-demo-dx/87-CONTEXT.md
    .planning/phases/87-docker-demo-dx/87-RESEARCH.md
    .planning/phases/87-docker-demo-dx/87-PATTERNS.md
    .planning/phases/87-docker-demo-dx/87-VALIDATION.md
  </read_first>
  <files>docker/Dockerfile.cohort-demo</files>
  <action>After the cache-ordering edit, keep Phase 87 constrained to the existing preview image per D-87-09: retain docker/cohort-demo-entrypoint.sh copy/chmod, EXPOSE 4102, and ENTRYPOINT ["/entrypoint.sh"]; do not add a release build, split image, Traefik, reverse proxy, new package manager, production deployment topology, or changes to the entrypoint script.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; dockerfile=docker/Dockerfile.cohort-demo; grep -F "EXPOSE 4102" "$dockerfile"; grep -F "ENTRYPOINT [\"/entrypoint.sh\"]" "$dockerfile"; if rg -n "mix release|FROM .* AS |traefik|reverse proxy|production deployment" "$dockerfile"; then exit 1; fi; bash -n docker/cohort-demo-entrypoint.sh; shellcheck docker/cohort-demo-entrypoint.sh'</automated>
  </verify>
  <acceptance_criteria>
    - EXPOSE 4102 and ENTRYPOINT ["/entrypoint.sh"] remain present.
    - Dockerfile contains no release build, split image, Traefik, reverse-proxy, or production-deployment redesign terms.
    - docker/cohort-demo-entrypoint.sh remains shell-syntax and shellcheck clean if referenced by the final Dockerfile.
  </acceptance_criteria>
  <done>DX-02 cache improvements preserve the simple Docker preview boundary and do not expand Phase 87 topology.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker build context to image | Local repository files enter the Cohort preview image. |
| dependency manifests to Hex deps | Existing lockfiles determine dependency fetches inside the Docker build. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-87-05 | Tampering | medium | docker/Dockerfile.cohort-demo | mitigate | Task 1 asserts exact instruction ordering so broad source COPY cannot tamper with the dependency-fetch layer for routine source/template edits. |
| T-87-06 | Information Disclosure / Spoofing | medium | Docker preview topology | mitigate | Task 1 blocks release-build, split-image, Traefik, and reverse-proxy redesign terms so the preview is not misrepresented as production deployment. |
| T-87-SC | Tampering | low | package installs | accept | No new dependencies or package-manager install tasks are introduced; mix deps.get uses existing mix.lock files already in the repo. |
</threat_model>

<verification>
Plan-level gate: run the task automated source-order assertion. Optional cache smoke, only if Docker state and time permit: build once, touch a non-dependency source/template file, rebuild, and inspect build output for the cached dependency layer. Do not make the optional cache smoke a required autonomous gate.
</verification>

<success_criteria>
- DX-02 is satisfied by Dockerfile instruction ordering.
- Routine source, style, and template edits no longer force the Hex dependency fetch layer to rerun.
- Docker preview remains scoped to Phase 87 cache/DX improvements.
</success_criteria>

<output>
Create `.planning/phases/87-docker-demo-dx/87-02-SUMMARY.md` when done.
</output>

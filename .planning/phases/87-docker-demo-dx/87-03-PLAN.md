---
phase: 87-docker-demo-dx
plan: "03"
type: execute
wave: 2
depends_on:
  - 87-01
  - 87-02
files_modified:
  - examples/adoption_demo/README.md
  - examples/adoption_demo/docs/adoption-proof-matrix.md
autonomous: true
requirements:
  - DX-01
  - DX-02
  - DX-03
requirements_addressed:
  - DX-01
  - DX-02
  - DX-03
must_haves:
  truths:
    - "D-87-13: Docker quick-try docs and adoption proof matrix describe the changed compose/script contract."
    - "D-87-14: docs point maintainers to static compose/script checks and proof-matrix drift checks instead of heavy startup as the only proof path."
    - "Port conflict guidance is command-first, uses COHORT_DEMO_PORT, COHORT_MINIO_PORT, COHORT_MINIO_CONSOLE_PORT, and COMPOSE_PROJECT_NAME, and does not recommend process killing or compose-file edits."
    - "Docs keep the Docker preview preview-only and do not describe public MinIO exposure or production deployment."
  artifacts:
    - path: "examples/adoption_demo/README.md"
      provides: "Docker quick-try and conflict-recovery guidance"
      contains: "COHORT_DEMO_PORT"
    - path: "examples/adoption_demo/docs/adoption-proof-matrix.md"
      provides: "Honest proof matrix row for the local Docker preview"
      contains: "scripts/demo/up.sh"
  key_links:
    - from: "examples/adoption_demo/README.md"
      to: "scripts/demo/up.sh"
      via: "documented launch command and URL map labels"
      pattern: "MinIO console"
    - from: "examples/adoption_demo/docs/adoption-proof-matrix.md"
      to: "scripts/maintainer/check_adoption_proof_matrix.sh"
      via: "proof matrix drift gate"
      pattern: "check_adoption_proof_matrix.sh"
---

<objective>
Update the Cohort Docker quick-try and proof-matrix docs so they match the executable Phase 87 Docker/demo contract.

Purpose: Prevent reader confusion and proof drift once compose ports, URL output, and cache behavior change.
Output: README and proof matrix edits with drift-gate verification.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/87-docker-demo-dx/87-CONTEXT.md
@.planning/phases/87-docker-demo-dx/87-RESEARCH.md
@.planning/phases/87-docker-demo-dx/87-PATTERNS.md
@.planning/phases/87-docker-demo-dx/87-VALIDATION.md
@.planning/phases/87-docker-demo-dx/87-UI-SPEC.md
@.planning/phases/87-docker-demo-dx/87-01-SUMMARY.md
@.planning/phases/87-docker-demo-dx/87-02-SUMMARY.md
@guides/docker_demo_dx.md
@examples/adoption_demo/README.md
@examples/adoption_demo/docs/adoption-proof-matrix.md
@scripts/maintainer/check_adoption_proof_matrix.sh
@RUNNING.md

<interfaces>
Docs contract:
- Quick try remains preview only.
- Primary command remains ./scripts/demo/up.sh.
- User-facing override controls are COMPOSE_PROJECT_NAME, COHORT_DEMO_PORT, COHORT_MINIO_PORT, and COHORT_MINIO_CONSOLE_PORT.
- URL labels are app, admin console, and MinIO console.
- Port-conflict recovery uses env var overrides, not compose-file edits or process-killing workarounds.
- Static checks include bash -n, shellcheck, docker compose config, Dockerfile source-order assertion, launch URL output check, and scripts/maintainer/check_adoption_proof_matrix.sh.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Docker quick-try docs</name>
  <read_first>
    examples/adoption_demo/README.md
    guides/docker_demo_dx.md
    .planning/phases/87-docker-demo-dx/87-CONTEXT.md
    .planning/phases/87-docker-demo-dx/87-UI-SPEC.md
    .planning/phases/87-docker-demo-dx/87-01-SUMMARY.md
    .planning/phases/87-docker-demo-dx/87-02-SUMMARY.md
  </read_first>
  <files>examples/adoption_demo/README.md</files>
  <action>Rewrite the Quick try (Docker) section per D-87-13 and the UI terminal/docs contract. Keep the preview-only framing and primary command ./scripts/demo/up.sh. Replace the single fixed http://localhost:4102 claim with command-first guidance that says the launch output prints app, admin console, and MinIO console URLs. Document sane defaults COHORT_DEMO_PORT=4102, COHORT_MINIO_PORT=9000, COHORT_MINIO_CONSOLE_PORT=9001, and show env-var override examples for busy app, MinIO API, MinIO console, and sibling-stack COMPOSE_PROJECT_NAME cases. State that internal container ports remain stable and that routine source/style/template edits should reuse the Hex dependency layer after the Dockerfile cache fix. Do not describe the Docker preview as production deployment, public MinIO exposure, or an implemented admin console; the /admin/rindle URL is only the stable mount-path affordance from guides/admin_console_architecture.md in Phase 87.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; rg -F "Preview only" examples/adoption_demo/README.md; rg -F "./scripts/demo/up.sh" examples/adoption_demo/README.md; rg -F "app" examples/adoption_demo/README.md; rg -F "admin console" examples/adoption_demo/README.md; rg -F "MinIO console" examples/adoption_demo/README.md; rg -F "COHORT_DEMO_PORT" examples/adoption_demo/README.md; rg -F "COHORT_MINIO_PORT" examples/adoption_demo/README.md; rg -F "COHORT_MINIO_CONSOLE_PORT" examples/adoption_demo/README.md; rg -F "COMPOSE_PROJECT_NAME" examples/adoption_demo/README.md; if rg -i "kill .*port|lsof|edit the compose|public MinIO|production deployment" examples/adoption_demo/README.md; then exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
    - README Quick try docs preserve preview-only framing and ./scripts/demo/up.sh as the primary launch path.
    - README mentions all three URL labels and all four override env vars.
    - README gives action-first port conflict guidance without process-killing, lsof, compose-file editing, production deployment, or public MinIO guidance.
    - README states the cache expectation for source/style/template edits.
  </acceptance_criteria>
  <done>Docker quick-try docs match the Phase 87 executable contract and avoid misleading production/security guidance.</done>
</task>

<task type="auto">
  <name>Task 2: Update adoption proof matrix and drift checks</name>
  <read_first>
    examples/adoption_demo/docs/adoption-proof-matrix.md
    examples/adoption_demo/README.md
    scripts/maintainer/check_adoption_proof_matrix.sh
    guides/docker_demo_dx.md
    .planning/phases/87-docker-demo-dx/87-CONTEXT.md
    .planning/phases/87-docker-demo-dx/87-VALIDATION.md
    .planning/phases/87-docker-demo-dx/87-01-SUMMARY.md
    .planning/phases/87-docker-demo-dx/87-02-SUMMARY.md
  </read_first>
  <files>examples/adoption_demo/docs/adoption-proof-matrix.md</files>
  <action>Update the Local click-around preview row and Try the Cohort demo locally section per D-87-13 and D-87-14. Keep the row optional/not CI-blocking, but make it honest that the Docker preview uses docker/compose.cohort-demo.yml plus scripts/demo/up.sh with env-driven loopback ports, COMPOSE_PROJECT_NAME namespacing, seeded Cohort UI, and printed URL map labels. Replace fixed-only http://localhost:4102 phrasing with the launch-output/default-port contract and point readers back to README for conflict overrides. Mention static verification posture at a high level: rendered compose config, shell syntax/lint, URL-output check, Dockerfile order assertion, and check_adoption_proof_matrix.sh. Do not add new E2E claims, admin-console implementation claims, release-build claims, or production deployment claims.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; rg -F "Local click-around preview" examples/adoption_demo/docs/adoption-proof-matrix.md; rg -F "docker/compose.cohort-demo.yml" examples/adoption_demo/docs/adoption-proof-matrix.md; rg -F "scripts/demo/up.sh" examples/adoption_demo/docs/adoption-proof-matrix.md; rg -F "COMPOSE_PROJECT_NAME" examples/adoption_demo/docs/adoption-proof-matrix.md; rg -F "COHORT_DEMO_PORT" examples/adoption_demo/docs/adoption-proof-matrix.md; rg -F "MinIO console" examples/adoption_demo/docs/adoption-proof-matrix.md; rg -F "check_adoption_proof_matrix.sh" examples/adoption_demo/docs/adoption-proof-matrix.md; if rg -i "public MinIO|production deployment|Traefik|release build" examples/adoption_demo/docs/adoption-proof-matrix.md; then exit 1; fi; scripts/maintainer/check_adoption_proof_matrix.sh'</automated>
  </verify>
  <acceptance_criteria>
    - Proof matrix still passes scripts/maintainer/check_adoption_proof_matrix.sh.
    - Proof matrix row describes env-driven Docker preview behavior without making it CI-blocking.
    - Try-locally docs refer to launch output/default-port behavior instead of a fixed-only URL.
    - Proof matrix does not claim admin console implementation, Traefik, release build, production deployment, or public MinIO exposure.
  </acceptance_criteria>
  <done>Adoption proof matrix remains drift-checked and truthful about Docker preview behavior after Phase 87 changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs to maintainer action | Docs drive local commands that can start, stop, or reset Docker resources. |
| docs to security posture | Docs can accidentally frame local MinIO or preview app as production/public. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-87-07 | Information Disclosure | high | README and proof matrix | mitigate | Tasks 1 and 2 forbid public MinIO and production deployment wording while documenting loopback/local-only preview behavior. |
| T-87-08 | Denial of Service | medium | port conflict guidance | mitigate | Task 1 documents env-var overrides instead of process-killing or compose-file edits. |
| T-87-09 | Repudiation | medium | proof matrix | mitigate | Task 2 keeps check_adoption_proof_matrix.sh green and updates the proof row to match compose/script behavior. |
| T-87-10 | Tampering | medium | reset/down guidance | mitigate | Task 1 documents COMPOSE_PROJECT_NAME as the namespace control so reset/down can be scoped to the intended stack and sibling stacks are not affected. |
| T-87-SC | Tampering | low | package installs | accept | No package installs or third-party tooling are planned; docs reference existing repo scripts and Docker Compose only. |
</threat_model>

<verification>
Plan-level gate: run both task automated commands after docs edits. After this plan, run the full Phase 87 static gate from 87-VALIDATION.md: bash -n scripts/demo/up.sh scripts/demo/down.sh scripts/demo/reset.sh docker/cohort-demo-entrypoint.sh; shellcheck scripts/demo/up.sh scripts/demo/down.sh scripts/demo/reset.sh docker/cohort-demo-entrypoint.sh; rendered docker compose config with COHORT_DEMO_PORT=4212 COHORT_MINIO_PORT=9200 COHORT_MINIO_CONSOLE_PORT=9201 COMPOSE_PROJECT_NAME=rindle-cohort-check; Dockerfile source-order assertion; scripts/maintainer/check_adoption_proof_matrix.sh.
</verification>

<success_criteria>
- DX-01, DX-02, and DX-03 are documented truthfully.
- Docs give command-first port conflict guidance with sane defaults.
- Docs preserve preview-only and local-only MinIO boundaries.
- Proof matrix remains drift-checked.
</success_criteria>

<output>
Create `.planning/phases/87-docker-demo-dx/87-03-SUMMARY.md` when done.
</output>

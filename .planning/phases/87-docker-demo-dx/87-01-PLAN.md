---
phase: 87-docker-demo-dx
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/compose.cohort-demo.yml
  - scripts/demo/up.sh
autonomous: true
requirements:
  - DX-01
  - DX-03
requirements_addressed:
  - DX-01
  - DX-03
must_haves:
  truths:
    - "D-87-01: COMPOSE_PROJECT_NAME overrides the default compose project name for sibling-stack namespacing."
    - "D-87-02/D-87-03/D-87-04: app, MinIO API, and MinIO console host ports are env-driven, loopback-bound, and keep stable internal container ports."
    - "D-87-05: scripts/demo/up.sh remains the primary copy-paste Docker preview entry point."
    - "D-87-06/D-87-07: launch output prints labels app, admin console, and MinIO console with URLs derived from env-selected host ports."
    - "D-87-11/D-87-12: container-internal MinIO stays http://minio:9000 while browser-facing presigned URLs use the selected host MinIO API port and existing preview credentials/bucket remain unchanged."
  artifacts:
    - path: "docker/compose.cohort-demo.yml"
      provides: "Compose project namespace, loopback host bindings, stable internal ports, and browser-facing RINDLE_MINIO_URL"
      contains: "127.0.0.1:${COHORT_DEMO_PORT:-4102}:4102"
    - path: "scripts/demo/up.sh"
      provides: "Primary launch wrapper plus deterministic URL-map output"
      contains: "--print-urls"
  key_links:
    - from: "scripts/demo/up.sh"
      to: "docker/compose.cohort-demo.yml"
      via: "shared COHORT_DEMO_PORT and COHORT_MINIO_CONSOLE_PORT defaults"
      pattern: "COHORT_DEMO_PORT"
    - from: "docker/compose.cohort-demo.yml"
      to: "examples/adoption_demo/config/runtime.exs"
      via: "RINDLE_MINIO_URL environment consumed by runtime config"
      pattern: "RINDLE_MINIO_URL"
---

<objective>
Make the Cohort Docker preview stack namespace-safe, port-conflict resistant, loopback-bound, and self-reporting at launch.

Purpose: This is the executable DX contract later UI-heavy phases depend on for fast local iteration.
Output: Updated compose host bindings and an enhanced `scripts/demo/up.sh` launch wrapper with deterministic URL output.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/87-docker-demo-dx/87-CONTEXT.md
@.planning/phases/87-docker-demo-dx/87-RESEARCH.md
@.planning/phases/87-docker-demo-dx/87-PATTERNS.md
@.planning/phases/87-docker-demo-dx/87-VALIDATION.md
@.planning/phases/87-docker-demo-dx/87-UI-SPEC.md
@guides/docker_demo_dx.md
@docker/compose.cohort-demo.yml
@scripts/demo/up.sh
@scripts/demo/down.sh
@scripts/demo/reset.sh
@examples/adoption_demo/config/runtime.exs

<interfaces>
Phase 87 env contract:
- COMPOSE_PROJECT_NAME namespaces containers, networks, and volumes.
- COHORT_DEMO_PORT defaults to 4102 and publishes host 127.0.0.1 to container 4102.
- COHORT_MINIO_PORT defaults to 9000 and publishes host 127.0.0.1 to container 9000.
- COHORT_MINIO_CONSOLE_PORT defaults to 9001 and publishes host 127.0.0.1 to container 9001.
- PORT remains 4102 inside the Phoenix container.
- RINDLE_MINIO_URL must be browser-facing and use http://host.docker.internal:${COHORT_MINIO_PORT:-9000}.
- minio-init must keep service-to-service setup through http://minio:9000.
- Launch output labels are exactly app, admin console, and MinIO console.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render env-driven loopback Compose contract</name>
  <read_first>
    docker/compose.cohort-demo.yml
    examples/adoption_demo/config/runtime.exs
    guides/docker_demo_dx.md
    .planning/phases/87-docker-demo-dx/87-CONTEXT.md
    .planning/phases/87-docker-demo-dx/87-RESEARCH.md
    .planning/phases/87-docker-demo-dx/87-PATTERNS.md
    .planning/phases/87-docker-demo-dx/87-VALIDATION.md
  </read_first>
  <files>docker/compose.cohort-demo.yml</files>
  <action>Replace the fixed compose host port strings with loopback-bound interpolation: app 127.0.0.1:${COHORT_DEMO_PORT:-4102}:4102, MinIO API 127.0.0.1:${COHORT_MINIO_PORT:-9000}:9000, and MinIO console 127.0.0.1:${COHORT_MINIO_CONSOLE_PORT:-9001}:9001, per D-87-01 through D-87-04. Keep internal Phoenix PORT as 4102, MinIO service command and health check on 9000/9001, and minio-init alias as http://minio:9000. Change only the browser-facing RINDLE_MINIO_URL to http://host.docker.internal:${COHORT_MINIO_PORT:-9000}, preserving demo-preview credentials, bucket, and region per D-87-11 and D-87-12. Do not add Traefik, a reverse proxy, public host bindings, custom container names, or a production deployment topology.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; out=$(mktemp); COHORT_DEMO_PORT=4212 COHORT_MINIO_PORT=9200 COHORT_MINIO_CONSOLE_PORT=9201 COMPOSE_PROJECT_NAME=rindle-cohort-check docker compose -f docker/compose.cohort-demo.yml config > "$out"; grep -F "name: rindle-cohort-check" "$out"; host_count=$(grep -F "host_ip: 127.0.0.1" "$out" | wc -l | tr -d " "); test "$host_count" = "3"; grep -F "published: \"4212\"" "$out"; grep -F "published: \"9200\"" "$out"; grep -F "published: \"9201\"" "$out"; grep -F "target: 4102" "$out"; grep -F "target: 9000" "$out"; grep -F "target: 9001" "$out"; grep -F "RINDLE_MINIO_URL: http://host.docker.internal:9200" "$out"; grep -F "source: cohort-demo-postgres" "$out"; grep -F "source: cohort-demo-minio" "$out"; grep -F "name: rindle-cohort-check_cohort-demo-postgres" "$out"; grep -F "name: rindle-cohort-check_cohort-demo-minio" "$out"'</automated>
  </verify>
  <acceptance_criteria>
    - Rendered compose config with override ports shows project name rindle-cohort-check.
    - Rendered compose config contains three host_ip: 127.0.0.1 entries and published ports 4212, 9200, and 9201.
    - Rendered compose config keeps target ports 4102, 9000, and 9001.
    - Rendered compose config sets RINDLE_MINIO_URL to http://host.docker.internal:9200 when COHORT_MINIO_PORT=9200.
    - Rendered volume names are project-scoped under rindle-cohort-check.
  </acceptance_criteria>
  <done>DX-01 compose behavior is env-driven, loopback-bound, namespace-aware, and preserves the internal MinIO/browser-facing URL boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve launch wrapper and add URL map print path</name>
  <read_first>
    scripts/demo/up.sh
    scripts/demo/down.sh
    scripts/demo/reset.sh
    docker/compose.cohort-demo.yml
    guides/docker_demo_dx.md
    guides/admin_console_architecture.md
    .planning/phases/87-docker-demo-dx/87-CONTEXT.md
    .planning/phases/87-docker-demo-dx/87-UI-SPEC.md
    .planning/phases/87-docker-demo-dx/87-VALIDATION.md
  </read_first>
  <files>scripts/demo/up.sh</files>
  <action>Keep scripts/demo/up.sh as the primary copy-paste entry point per D-87-05. Add a small quoted-variable URL-map path that resolves and exports defaults for COHORT_DEMO_PORT, COHORT_MINIO_PORT, and COHORT_MINIO_CONSOLE_PORT from the same env vars compose uses, and prints labels exactly app, admin console, and MinIO console per D-87-06 and D-87-07. Each URL must be copy-pasteable on its own line: http://localhost:${COHORT_DEMO_PORT}, http://localhost:${COHORT_DEMO_PORT}/admin/rindle, and http://localhost:${COHORT_MINIO_CONSOLE_PORT}. Add a deterministic --print-urls mode that prints the same map and exits without invoking Docker; consume this flag only when it is the first argument, and otherwise preserve exec docker compose -f "${repo_root}/docker/compose.cohort-demo.yml" up --build "$@" passthrough. Do not add banners, ASCII art, spinners, icons, colors, process-killing logic, or raw docker-compose instructions.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; bash -n scripts/demo/up.sh scripts/demo/down.sh scripts/demo/reset.sh docker/cohort-demo-entrypoint.sh; shellcheck scripts/demo/up.sh scripts/demo/down.sh scripts/demo/reset.sh docker/cohort-demo-entrypoint.sh; out=$(mktemp); COHORT_DEMO_PORT=4212 COHORT_MINIO_CONSOLE_PORT=9201 scripts/demo/up.sh --print-urls > "$out"; grep -Fx "app" "$out"; grep -Fx "http://localhost:4212" "$out"; grep -Fx "admin console" "$out"; grep -Fx "http://localhost:4212/admin/rindle" "$out"; grep -Fx "MinIO console" "$out"; grep -Fx "http://localhost:9201" "$out"; line_count=$(grep -v "^$" "$out" | wc -l | tr -d " "); test "$line_count" = "6"'</automated>
  </verify>
  <acceptance_criteria>
    - up.sh with --print-urls exits without starting Docker and prints exactly the three locked labels plus the three env-derived URLs.
    - Normal up.sh still execs docker compose with the same compose file, up --build, and "$@" passthrough.
    - bash -n and shellcheck pass for up.sh, down.sh, reset.sh, and cohort-demo-entrypoint.sh.
  </acceptance_criteria>
  <done>DX-03 launch output is deterministic, copy-pasteable, label-exact, and tied to the same env defaults compose consumes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell env to compose | Developer-controlled env vars select project name and host ports. |
| host browser to published demo ports | Browser reaches app and MinIO through host port bindings. |
| container app to MinIO | Phoenix emits browser-facing URLs while containers still use internal MinIO service networking. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-87-01 | Information Disclosure | high | docker/compose.cohort-demo.yml ports | mitigate | Task 1 binds every published app and MinIO port to 127.0.0.1 and blocks completion until rendered compose proves three host_ip: 127.0.0.1 entries. |
| T-87-02 | Tampering / Denial of Service | medium | compose project resources | mitigate | Task 1 verifies COMPOSE_PROJECT_NAME renders project-scoped volume names so sibling stacks and reset/down operations can target separate resources. |
| T-87-03 | Spoofing / Repudiation | medium | scripts/demo/up.sh URL output | mitigate | Task 2 adds --print-urls proof and asserts labels and URLs derive from selected env host ports. |
| T-87-04 | Information Disclosure | medium | RINDLE_MINIO_URL | mitigate | Task 1 keeps container-internal MinIO on http://minio:9000 and makes browser-facing presigned URLs use host.docker.internal with COHORT_MINIO_PORT. |
| T-87-SC | Tampering | low | package installs | accept | No npm, pip, cargo, Hex, or system package additions are planned in this plan; existing Docker image dependency fetch remains controlled by existing lockfiles. |
</threat_model>

<verification>
Plan-level gate: run both task automated commands. Do not make full Docker startup mandatory. Optional manual smoke after static gates: COMPOSE_PROJECT_NAME=rindle-cohort-check COHORT_DEMO_PORT=4212 COHORT_MINIO_PORT=9200 COHORT_MINIO_CONSOLE_PORT=9201 ./scripts/demo/up.sh, then clean up with matching COMPOSE_PROJECT_NAME ./scripts/demo/down.sh.
</verification>

<success_criteria>
- DX-01 is satisfied for compose namespacing, env-driven ports, loopback bindings, and MinIO URL boundary.
- DX-03 is satisfied for exact URL map labels and env-derived copy-pasteable URLs.
- No Phase 87 out-of-scope topology is introduced.
</success_criteria>

<output>
Create `.planning/phases/87-docker-demo-dx/87-01-SUMMARY.md` when done.
</output>

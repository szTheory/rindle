---
phase: 95-admin-level-1-component-audit-track-a
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - brandbook/src/admin-gallery.mjs
  - brandbook/src/admin-gallery-check.mjs
  - examples/adoption_demo/e2e/support/admin-polish.js
  - brandbook/admin-gallery/index.html
autonomous: true
requirements: [UPLIFT-01]
must_haves:
  truths:
    - "The static admin gallery renders every Level-1 component with singular `data-rindle-admin-component` markers and state markers for default, hover, focus-visible, active, disabled, loading, empty, error, and skeleton."
    - "The browser checker proves light, dark, and auto/system theme behavior plus focus-visible token values, active-vs-focus distinction, disabled/loading affordance, and no bare outline:none."
    - "The live admin polish helper has reusable focus/no-outline assertions without replacing the component-gallery proof surface or changing overlap warnings."
  artifacts:
    - path: "brandbook/src/admin-gallery.mjs"
      provides: "Static Level-1 component/state/theme matrix fixtures"
      contains: "data-rindle-admin-component=\"form-controls\""
    - path: "brandbook/src/admin-gallery-check.mjs"
      provides: "Playwright browser assertions for component coverage and computed focus/active states"
      contains: "assertFocusVisible"
    - path: "examples/adoption_demo/e2e/support/admin-polish.js"
      provides: "Reusable live-page focus/no-outline computed-style gate"
      contains: "focus-visible"
    - path: "brandbook/admin-gallery/index.html"
      provides: "Generated gallery artifact"
      contains: "data-rindle-admin-error-state"
  key_links:
    - from: "brandbook/src/admin-gallery.mjs"
      to: "brandbook/src/admin-gallery-check.mjs"
      via: "data-rindle-admin-component/state selectors"
      pattern: "data-rindle-admin-component=\"form-controls\""
    - from: "brandbook/src/admin-gallery-check.mjs"
      to: "brandbook/tokens/rindle-admin.css"
      via: "computed styles and stylesheet text scan"
      pattern: "outline"
    - from: "examples/adoption_demo/e2e/support/admin-polish.js"
      to: "examples/adoption_demo/e2e/admin-screenshots.spec.js"
      via: "existing assertAdminPolish import path"
      pattern: "assertAdminPolish"
---

<objective>
Extend the primary Phase 95 proof surface: the static admin component gallery and its
Playwright browser checker. This plan implements D-95-05, D-95-06, D-95-07, and D-95-08
for rendered fixtures and deterministic computed-style assertions.

Purpose: UPLIFT-01 is not complete until the component x state x theme matrix is visible and
mechanically asserted. The gallery remains primary proof; the live adoption-demo polish helper
only gains reusable focus/no-outline assertions needed by this phase.
Output: updated gallery generator, generated gallery HTML, browser checker assertions, and
`admin-polish.js` focus/no-outline checks.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/95-admin-level-1-component-audit-track-a/95-CONTEXT.md
@.planning/phases/95-admin-level-1-component-audit-track-a/95-RESEARCH.md
@.planning/phases/95-admin-level-1-component-audit-track-a/95-VALIDATION.md
@.planning/phases/95-admin-level-1-component-audit-track-a/95-PATTERNS.md
@.planning/phases/95-admin-level-1-component-audit-track-a/95-UI-SPEC.md
@.planning/phases/92-e2e-screenshot-driven-polish-loop/92-CONTEXT.md
@.planning/phases/94-foundation-token-pipeline-ci-gate-new-token-categories/94-02-SUMMARY.md
@guides/ui_principles.md
@brandbook/src/admin-gallery.mjs
@brandbook/src/admin-gallery-check.mjs
@examples/adoption_demo/e2e/support/admin-polish.js

<interfaces>
Existing `admin-gallery-check.mjs` contracts:
- `runNode('brandbook/src/admin-css-build.mjs')` then `runNode('brandbook/src/admin-gallery.mjs')`
- `assertVisible(page, selector)` throws on missing/hidden selectors
- `selectTheme(page, theme)` sets `data-theme`
- `expectedScreenshots` currently has 7 filenames

Phase 95 required gallery markers:
- `data-rindle-admin-component` must use the singular `COMPONENTS` names from Plan 01.
- `data-rindle-admin-state` must include `default`, `hover`, `focus-visible`, `active`,
  `disabled`, `loading`, `empty`, `error`, and `skeleton` somewhere in the gallery.
- Required new screenshots: `form-controls-light.png`, `error-state-dark.png`,
  `loading-state-auto.png` in addition to the existing seven screenshots.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the full Level-1 gallery matrix with exact markers</name>
  <files>brandbook/src/admin-gallery.mjs, brandbook/admin-gallery/index.html</files>
  <read_first>
    - brandbook/src/admin-gallery.mjs
    - brandbook/src/admin-design-system-data.mjs
    - brandbook/tokens/rindle-admin.css
    - .planning/phases/95-admin-level-1-component-audit-track-a/95-CONTEXT.md
    - .planning/phases/95-admin-level-1-component-audit-track-a/95-UI-SPEC.md
    - .planning/phases/95-admin-level-1-component-audit-track-a/95-PATTERNS.md
    - guides/ui_principles.md
  </read_first>
  <action>
    Update `admin-gallery.mjs` to import and use the normalized `COMPONENTS` and
    `LEVEL_1_STATES` from Plan 01. Replace plural/grouped component markers with exact
    singular markers: `status-chip`, `button`, `toast`, and `skeleton` instead of
    `status-chips`, `buttons`, `toasts`, and `skeletons`. Add first-class rendered
    sections or panels for `form-controls`, `error-state`, and `loading-state`.

    The gallery HTML must include all of these component markers exactly once or more:
    shell, nav, table, status-chip, button, theme-picker, form-controls, confirm-dialog,
    drawer, toast, empty-state, error-state, loading-state, skeleton. It must include all
    state markers: default, hover, focus-visible, active, disabled, loading, empty, error,
    skeleton. Use the copywriting contract from `95-UI-SPEC.md`: primary CTA text
    `Review component gallery`, empty heading `No assets match this state`, empty body
    `Adjust the lifecycle filter or check Runtime/Doctor for setup issues before running actions.`,
    and error copy `Rindle Admin could not load this surface. Retry load or check Runtime/Doctor for the missing source artifact.`

    Render form controls using `[data-rindle-admin-input]` and
    `[data-rindle-admin-confirm-input]`; include input, textarea, select, and checkbox
    examples if the existing markup can express them without new dependencies. Render a
    disabled button, a loading button carrying `data-rindle-admin-state="loading"`, an
    empty state carrying `[data-rindle-admin-empty-state]`, an error state carrying
    `[data-rindle-admin-error-state]`, and a skeleton/loading region carrying
    `[data-rindle-admin-loading-state]` plus `.rindle-admin-skeleton` with
    `aria-hidden="true"`. Keep `data-theme="light|dark|auto"` and the existing hash-link
    navigation script. Do not add Storybook, SaaS visual regression, Tailwind, daisyUI,
    shadcn, Radix, or a new runtime UI dependency.

    Regenerate `brandbook/admin-gallery/index.html` by running `node brandbook/src/admin-gallery.mjs`.
  </action>
  <acceptance_criteria>
    - `brandbook/src/admin-gallery.mjs` required component list contains `form-controls`, `error-state`, and `loading-state`.
    - `brandbook/src/admin-gallery.mjs` does not require plural component markers `status-chips`, `buttons`, `toasts`, or `skeletons`.
    - `brandbook/admin-gallery/index.html` contains `data-rindle-admin-component="form-controls"`.
    - `brandbook/admin-gallery/index.html` contains `data-rindle-admin-error-state`.
    - `brandbook/admin-gallery/index.html` contains `Rindle Admin could not load this surface. Retry load or check Runtime/Doctor for the missing source artifact.`
    - `brandbook/admin-gallery/index.html` contains every state marker in `default hover focus-visible active disabled loading empty error skeleton`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/rindle && node brandbook/src/admin-gallery.mjs && grep -q 'data-rindle-admin-component="form-controls"' brandbook/admin-gallery/index.html && grep -q 'data-rindle-admin-error-state' brandbook/admin-gallery/index.html && grep -q 'data-rindle-admin-state="focus-visible"' brandbook/admin-gallery/index.html && grep -q 'data-rindle-admin-state="active"' brandbook/admin-gallery/index.html</automated>
  </verify>
  <done>The static gallery renders the exact Level-1 component and state matrix with stable singular `data-rindle-admin-component` and `data-rindle-admin-state` markers.</done>
</task>

<task type="auto">
  <name>Task 2: Assert gallery focus/active/no-outline behavior and extend live polish checks</name>
  <files>brandbook/src/admin-gallery-check.mjs, examples/adoption_demo/e2e/support/admin-polish.js</files>
  <read_first>
    - brandbook/src/admin-gallery-check.mjs
    - brandbook/src/admin-gallery.mjs
    - examples/adoption_demo/e2e/support/admin-polish.js
    - examples/adoption_demo/e2e/admin-screenshots.spec.js
    - brandbook/tokens/rindle-admin.css
    - .planning/phases/95-admin-level-1-component-audit-track-a/95-CONTEXT.md
    - .planning/phases/95-admin-level-1-component-audit-track-a/95-RESEARCH.md
    - .planning/phases/95-admin-level-1-component-audit-track-a/95-VALIDATION.md
    - .planning/phases/95-admin-level-1-component-audit-track-a/95-UI-SPEC.md
    - .planning/phases/92-e2e-screenshot-driven-polish-loop/92-CONTEXT.md
    - .planning/phases/94-foundation-token-pipeline-ci-gate-new-token-categories/94-02-SUMMARY.md
  </read_first>
  <action>
    In `admin-gallery-check.mjs`, replace the old plural `requiredComponents` list with the
    exact `COMPONENTS` vocabulary from Plan 01 and add a required state assertion for:
    default, hover, focus-visible, active, disabled, loading, empty, error, skeleton.
    Add the new screenshot names `form-controls-light.png`, `error-state-dark.png`, and
    `loading-state-auto.png` to `expectedScreenshots`, bringing the expected count from 7 to 10.

    Add deterministic computed-style assertions:
    - `assertNoBareOutlineNone` scans `brandbook/tokens/rindle-admin.css` and the generated
      gallery HTML text for `outline: none` / `outline:none` and throws if found.
    - `assertFocusVisibleTokens` focuses representative selectors and verifies
      `outlineWidth` equals `--rindle-focus-width`, `outlineColor` equals
      `--rindle-focus-ring`, and `outlineOffset` equals `--rindle-focus-offset`.
      Cover `.rindle-admin-button`, `[data-rindle-admin-nav-item]`,
      `[data-rindle-admin-theme]`, `[data-rindle-admin-input]`,
      `[data-rindle-admin-confirm-input]`, and `.rindle-admin-table__row`.
    - `assertActiveDistinctFromFocus` verifies the current nav item
      `.rindle-admin-nav__item[aria-current="page"]` has a non-outline active/current
      property that differs from a non-current nav item, and the pressed theme option
      `[data-rindle-admin-theme][aria-pressed="true"]` has a token-backed active fill
      distinct from the focus outline.
    - `assertDisabledAndLoadingAffordances` verifies disabled buttons are disabled or
      `aria-disabled="true"`, have `cursor: not-allowed`, and loading fixtures are visible.
    - Keep the existing `selectTheme('light'|'dark'|'auto')`, deep-link, forbidden class,
      and dark status chip assertions.

    In `examples/adoption_demo/e2e/support/admin-polish.js`, add a reusable sub-assertion
    that fails on missing token-backed focus-visible values and bare `outline:none` for the
    `interactiveSelectors` argument. Wire it through `assertAdminPolish` with a prefix such
    as `focus-visible`. Do not change `OVERLAP_ENFORCED`; overlap warnings stay warnings
    unless a separate phase explicitly tightens them. Preserve the existing CommonJS
    exports and the existing `assertAdminPolish(page, { viewport, surface, root,
    interactiveSelectors })` signature.
  </action>
  <acceptance_criteria>
    - `node brandbook/src/admin-gallery-check.mjs` exits 0 and prints `admin gallery check passed - 10 screenshots written`.
    - `brandbook/src/admin-gallery-check.mjs` contains `assertNoBareOutlineNone`.
    - `brandbook/src/admin-gallery-check.mjs` contains `assertFocusVisibleTokens`.
    - `brandbook/src/admin-gallery-check.mjs` contains `assertActiveDistinctFromFocus`.
    - `examples/adoption_demo/e2e/support/admin-polish.js` contains a focus-visible sub-assertion wired through `assertAdminPolish`.
    - `node --check examples/adoption_demo/e2e/support/admin-polish.js` exits 0.
    - `grep -c 'OVERLAP_ENFORCED = false' examples/adoption_demo/e2e/support/admin-polish.js` remains 1.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/rindle && node --check examples/adoption_demo/e2e/support/admin-polish.js && out=$(node brandbook/src/admin-gallery-check.mjs) && printf '%s\n' "$out" && printf '%s\n' "$out" | grep -q 'admin gallery check passed - 10 screenshots written' && grep -q 'OVERLAP_ENFORCED = false' examples/adoption_demo/e2e/support/admin-polish.js</automated>
  </verify>
  <done>The gallery checker mechanically proves the Level-1 component/state/theme matrix, focus-visible tokens, active-vs-focus distinction, disabled/loading affordance, no bare outline removal, and the live polish helper carries reusable focus/no-outline checks without replacing gallery proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static gallery fixture -> browser checker | Generated HTML fixtures drive deterministic Playwright checks and screenshots. |
| live admin page -> `admin-polish.js` | Existing adoption-demo screenshots run computed-style assertions against rendered admin markup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-05 | Tampering | gallery component/state markers | mitigate | Checker asserts exact singular `data-rindle-admin-component` names and all required `data-rindle-admin-state` values. |
| T-95-06 | Denial of Service | keyboard/focus access | mitigate | Browser checker focuses representative selectors and compares computed outline width/color/offset to token variables. |
| T-95-07 | Repudiation | visual proof ambiguity | mitigate | Gallery screenshots and checker output list exact expected filenames and computed-style failures, making missing coverage reproducible. |
| T-95-08 | Information Disclosure | deterministic gallery fixtures | mitigate | Fixtures use hardcoded demo IDs and copy only; no secrets, env vars, adopter media, or real owner data are introduced. |
| T-95-SC | Tampering | package installs | accept | No package install task exists; Playwright is the existing adoption_demo dependency resolved through the existing lockfile. |
</threat_model>

<verification>
- `node brandbook/src/admin-gallery-check.mjs` exits 0.
- `node --check examples/adoption_demo/e2e/support/admin-polish.js` exits 0.
- Source assertions prove exact component/state markers, screenshot count, focus-visible assertion helpers, and unchanged overlap warning mode.
</verification>

<success_criteria>
- Static gallery renders the complete Level-1 component/state/theme matrix with stable markers.
- Browser checker fails on missing state coverage, missing focus-visible tokens, active/focus collapse, disabled/loading affordance gaps, and bare outline removal.
- `admin-polish.js` gains reusable focus/no-outline checks while preserving the existing API and warning policy.
</success_criteria>

<output>
Create `.planning/phases/95-admin-level-1-component-audit-track-a/95-02-SUMMARY.md` when done.
</output>

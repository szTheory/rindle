---
phase: 100-cohort-upload-migration-all-tabs-track-b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/adoption_demo/lib/adoption_demo_web/live/upload_live.ex
  - examples/adoption_demo/priv/static/assets/cohort.css
  - examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs
autonomous: true
requirements:
  - COHORT-02
user_setup: []

must_haves:
  truths:
    - "Each /upload?tab=X renders a [data-ck-root] .ck shell (composed ck_page/1), light theme by default (D-100-01)"
    - "The routed tab strip is <.link patch> links with aria-current=\"page\" on the active tab, never role=tablist/role=tab (D-100-03/04)"
    - "Every id/data-testid/phx-hook/phx-change/phx-submit/type=submit on /upload survives the migration byte-for-byte across all 6 tabs (D-100-06)"
    - "daisyUI/Tailwind utilities (tabs, text-red-600, standalone tab token, break-all, font-mono text-sm, text-2xl, space-y-) are absent from the /upload page body across all tabs (Pitfall E, D-100-06)"
    - "/upload?theme=dark assigns data-theme=\"dark\" on [data-ck-root]; any other theme value falls back to \"light\" (Pitfall F option 1, enum-gated)"
    - "tab_class/2 is deleted; the tus error is .ck-error + warning icon + role=\"alert\"; no raw/1 introduced (D-96-15, HEEx auto-escape)"
  artifacts:
    - path: "examples/adoption_demo/lib/adoption_demo_web/live/upload_live.ex"
      provides: "/upload migrated onto ck_page/1 + .ck-* primitives; validated ?theme read; tab_class/2 removed"
      contains: "ck_page"
    - path: "examples/adoption_demo/priv/static/assets/cohort.css"
      provides: "the one new token-only .ck-tabs__tab[aria-current=\"page\"] rule (consolidated with [aria-selected=\"true\"])"
      contains: "aria-current"
    - path: "examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs"
      provides: "extended @retired_daisyui_classes + the /upload per-tab frozen-contract/retirement test + panel_contract/1"
      contains: "panel_contract"
  key_links:
    - from: "examples/adoption_demo/lib/adoption_demo_web/live/upload_live.ex"
      to: "examples/adoption_demo/lib/adoption_demo_web/components/cohort_components.ex"
      via: "import AdoptionDemoWeb.CohortComponents; <.ck_page eyebrow title lede theme>"
      pattern: "<\\.ck_page"
    - from: "examples/adoption_demo/lib/adoption_demo_web/live/upload_live.ex"
      to: "examples/adoption_demo/priv/static/assets/cohort.css"
      via: "tab links carry class=\"ck-tabs__tab ck-tab\" + aria-current matching the new CSS rule"
      pattern: "ck-tabs__tab ck-tab"
    - from: "examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs"
      to: "examples/adoption_demo/lib/adoption_demo_web/live/upload_live.ex"
      via: "render_route(~p\"/upload?tab=#{tab}\") then assert_frozen_contract + assert_daisyui_retired per tab"
      pattern: "render_route"
---

<objective>
Migrate `/upload` (the heaviest Cohort inner page — 6 tabs, 4 `phx-hook` upload flows, 2 `live_file_input`, 2 forms) onto the hand-authored `.ck-*` Cohort design system class-by-class, preserving a frozen DOM contract byte-for-byte, and add the static proof that the contract survived and daisyUI is retired (COHORT-02).

This is the Track-B twin of Phase 99 applied to the one page held back. It is **composition + class-swap + frozen-contract preservation, NOT construction** — every primitive (`ck_page/1`, `.ck-tabs__tab`/`.ck-tabs__list`, `.ck-output`, `.ck-error`, `.ck-input`, `.ck-btn`/`--primary`, `.ck-help`), the warn-mode polish harness, and the ExUnit frozen-contract module already exist and were proven against `/styleguide` + the 7 small pages in Phase 99.

Purpose: `/upload` joins the `.ck-*` DS the way the 7 small pages did (COHORT-04, Phase 99), gaining the `.ck` shell (`data-ck-root`, server `data-theme`, focus-visible ring, reduced-motion block, box-sizing, polish-gate seam) for free, with a11y-correct routed-tab navigation and an announced (non-color-only) error state.

Output: a migrated `upload_live.ex`; exactly ONE new token-only CSS rule in `cohort.css`; an extended `@retired_daisyui_classes` list + a per-tab `/upload` frozen-contract/retirement ExUnit test (the static proof). The Playwright per-tab polish + dark cases are Plan 02 (Wave 2), which depends on the validated `?theme=dark` read this plan adds.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/100-cohort-upload-migration-all-tabs-track-b/100-CONTEXT.md
@.planning/phases/100-cohort-upload-migration-all-tabs-track-b/100-RESEARCH.md
@.planning/phases/100-cohort-upload-migration-all-tabs-track-b/100-PATTERNS.md

# The migration target (read in full before editing)
@examples/adoption_demo/lib/adoption_demo_web/live/upload_live.ex

# Composition precedents to mirror exactly (ck_page + nav + server theme)
@examples/adoption_demo/lib/adoption_demo_web/live/ops_live.ex
@examples/adoption_demo/lib/adoption_demo_web/live/dashboard_live.ex

# Primitives + the scaffold + the error-icon idiom
@examples/adoption_demo/lib/adoption_demo_web/components/cohort_components.ex
@examples/adoption_demo/priv/static/assets/cohort.css

# The static gate to EXTEND (shared helpers reused unchanged)
@examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New / changed symbols this plan creates or deletes:

- **CSS selector (new, the only new CSS in the phase):** `.ck-tabs__tab[aria-current="page"]` in `cohort.css`, consolidated with the existing `.ck-tabs__tab[aria-selected="true"]` rule (token-only: `color: var(--ck-ink); font-weight: 700; border-bottom-color: var(--ck-brand);`). D-100-05.
- **LiveView change (`upload_live.ex`):** `import AdoptionDemoWeb.CohortComponents`; `assign(:theme, theme)` in the mount pipeline; a validated `?theme` param read in `mount/2` and `handle_params/2` (`~w(light dark)`, default `"light"`) — the `?theme` assign/param read. D-100-01, Pitfall F.
- **Private helper deleted:** `tab_class/2` (`upload_live.ex:473-476`) is removed entirely. D-100-04.
- **Private helper changed:** `tab_link/1` swaps `class={tab_class(@current,@tab)}` for `class="ck-tabs__tab ck-tab"` + `aria-current={@current == @tab && "page"}` (patch URL + `data-testid` kept byte-for-byte).
- **Test data extended:** `@retired_daisyui_classes` gains `"tabs"`, `"text-red-600"`, `~s( tab )` (standalone tab token), `"break-all"`. Pitfall E.
- **New private test helper:** `panel_contract/1` (6 clauses, one per tab) → per-tab selector lists, in `cohort_migration_contract_test.exs`.
- **New test function:** `test "/upload preserves its frozen contract and retires daisyUI across all tabs"` (a 6-entry `for` comprehension over `~w(image tus video multipart liveview mux)`).

No new component, no new dep, no `tokens.json` / `cohort.css` token-VALUE / `admin-polish.js` edit, `Layouts.app` UNTOUCHED.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add the one token-only aria-current CSS rule + extend the daisyUI retirement list</name>
  <files>examples/adoption_demo/priv/static/assets/cohort.css, examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs</files>
  <read_first>
    - examples/adoption_demo/priv/static/assets/cohort.css — the tabs block (around the `.ck-tabs__tab[aria-selected="true"]` rule near line 942); confirm the rule body is token-only (every value a `var(--ck-*)`).
    - examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs — `@retired_daisyui_classes` (lines 32-41); note the module docstring rule "kept as DATA, never echoed in page-renderable prose".
    - .planning/phases/100-cohort-upload-migration-all-tabs-track-b/100-PATTERNS.md — Pattern Assignment 2 (the exact consolidated rule) + Assignment 3 STEP 1 (the exact list additions).
  </read_first>
  <action>
    In `cohort.css`, consolidate the selected-cue rule so the active cue is defined once for both routed and in-page tabs (D-100-05): change the existing `.ck-tabs__tab[aria-selected="true"]` selector to the comma-separated pair `.ck-tabs__tab[aria-selected="true"], .ck-tabs__tab[aria-current="page"]`, keeping the SAME token-only body (`color: var(--ck-ink); font-weight: 700; border-bottom-color: var(--ck-brand);`). This is the single new CSS in the entire phase. Do NOT add any hex/rgb/named-color/raw-measure literal in the rule body (it must pass the brace-depth literal scanner, D-96-09/20). Do NOT add a new `--ck-*` token, do NOT touch `tokens.json`, do NOT edit a generated file (cohort.css is hand-authored, D-94-05/06).

    In `cohort_migration_contract_test.exs`, EXTEND `@retired_daisyui_classes` with the `/upload`-specific daisyUI strings not yet covered (Pitfall E): the literal token `tabs` (catches `tabs tabs-boxed`), the literal token `text-red-600` (old color-only tus error), the standalone tab token written as `~s( tab )` (with leading + trailing spaces — this is the residue of the deleted tab_class/2; the spaces prevent matching `ck-tabs__tab`/`data-ck-root`), and the literal token `break-all` (old mux-streaming-url class). Keep these as DATA in the list only — per the module docstring, never echo any of them in a code comment or string the rendered page could contain.

    Run `node brandbook/src/cohort-contrast.mjs` as the sanity gate — it must stay green (the new rule is token-only, no new token/pair).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rindle/examples/adoption_demo && grep -c 'aria-current="page"' priv/static/assets/cohort.css | grep -qv '^0$' && grep -v '^[[:space:]]*/\*' priv/static/assets/cohort.css | grep -A4 'aria-current="page"' | grep -qE 'var\(--ck-brand\)' && echo CSS_OK</automated>
    <automated>cd /Users/jon/projects/rindle && node brandbook/src/cohort-contrast.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cohort.css` contains exactly one `.ck-tabs__tab[aria-current="page"]` selector, consolidated with `[aria-selected="true"]`, body token-only (no hex/rgb/named-color/raw-measure literal).
    - `node brandbook/src/cohort-contrast.mjs` exits 0 (no new token/pair; gate green).
    - `@retired_daisyui_classes` now includes `tabs`, `text-red-600`, `~s( tab )`, and `break-all` in addition to the existing entries; none of these strings appears anywhere a rendered page body would contain it.
    - No `tokens.json`, no generated file, no `cohort.css` token-VALUE change.
  </acceptance_criteria>
  <done>The single new CSS rule (token-only aria-current cue) exists and the contrast gate is green; the daisyUI retirement list is complete for /upload.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate upload_live.ex onto ck_page/1 + .ck-* primitives, add validated ?theme read, delete tab_class/2</name>
  <files>examples/adoption_demo/lib/adoption_demo_web/live/upload_live.ex</files>
  <read_first>
    - examples/adoption_demo/lib/adoption_demo_web/live/upload_live.ex — read in FULL (484 lines). Enumerate every id/data-testid/phx-hook/phx-change/phx-submit/type=submit/`:if` panel before touching anything; this is the frozen contract.
    - examples/adoption_demo/lib/adoption_demo_web/live/ops_live.ex — the cleanest `import CohortComponents` + `assign(theme: "light")` + `Layouts.app … nav={…}` → `ck_page` → body composition, plus a `phx-click` button keeping its id/testid with `.ck-btn ck-btn--primary` and a `.ck-output` status surface.
    - examples/adoption_demo/lib/adoption_demo_web/live/dashboard_live.ex — proves the `nav=` path and the testid-on-an-explicit-`.ck-hero__lede`-element idiom (since ck_page's `<h1>` cannot carry a testid).
    - examples/adoption_demo/lib/adoption_demo_web/components/cohort_components.ex — `ck_page/1` signature (`attr :title` required, `:eyebrow`/`:lede` default nil, `:theme` default `"light"` values `~w(light dark)`); the `ck_field` `.ck-error` + `role="alert"` markup; the `ck_icon(%{name: :warning})` warning SVG to copy inline (ck_icon is `defp`).
    - .planning/phases/100-cohort-upload-migration-all-tabs-track-b/100-PATTERNS.md — Pattern Assignment 1 (A-H, the exact per-element class swaps) + the Per-Element Class Map in 100-RESEARCH.md.
  </read_first>
  <action>
    Mirror the proven ops/dashboard `ck_page` composition (D-100-01). Make all of the following, preserving every `id`/`data-testid`/`phx-hook`/`phx-change`/`phx-submit`/`type=submit`/`accept`/`type=file` and the `<div :if={@tab == "X"}>` single-panel render byte-for-byte (D-100-06):

    1. Add `import AdoptionDemoWeb.CohortComponents` after the `use AdoptionDemoWeb, :live_view` line.
    2. **Validated `?theme` read (Pitfall F option 1 — the one place CONTEXT's stated `emulateMedia` mechanism is corrected):** in `mount/2` read `params["theme"]` and normalize via a private helper to `~w(light dark)` defaulting to `"light"` (e.g. `theme = if params["theme"] in ~w(light dark), do: params["theme"], else: "light"`), then add `|> assign(:theme, theme)` to the assign pipeline. In `handle_params/2` apply the same normalization so a `?theme=dark` patch updates the assign; default to `socket.assigns.theme` when the param is absent. Never interpolate the raw param — only the enum-validated value reaches `assign`/`ck_page` (V5 input validation; `ck_page`'s `theme` attr enforces the enum as a second gate). Introduce NO `raw/1`.
    3. Replace the bare `<Layouts.app flash={@flash} page_title={@page_title}>` body with the composition: `<Layouts.app flash={@flash} page_title={@page_title} nav={:upload}>` (D-100-02a — adds active-section wayfinding; `Layouts.app` itself is UNTOUCHED) → `<.ck_page eyebrow="Upload lab" title="Every Rindle upload path, live." lede={...} theme={@theme}>` (D-100-08 microcopy; lede text from D-100-08/CONTEXT) → the migrated body.
    4. Keep the member line byte-for-byte (D-100-02b) inside `:inner_block` as a `.ck-hero__lede` line: `<p class="ck-hero__lede">Member: <strong id="upload-member-name" data-testid="upload-member-name">{@member.name}</strong></p>`.
    5. Tab strip (D-100-03/04): change the wrapper from `class="tabs tabs-boxed mt-4 flex flex-wrap gap-2"` to `class="ck-tabs__list" role="navigation" aria-label="Upload strategy"`. Keep the 6 `<.tab_link>` calls and the routed `<.link patch={~p"/upload?member_id=#{@member.id}&tab=#{@tab}"}>` model. Tighten the display labels per D-100-08 (`Image (presigned PUT)`, `Tus (resumable)`, `Video (AV variants)`; keep `Multipart`, `LiveView upload`, `Mux streaming`).
    6. In `tab_link/1`: swap `class={tab_class(@current, @tab)}` for `class="ck-tabs__tab ck-tab"` and add `aria-current={@current == @tab && "page"}`. Keep `data-testid={"upload-tab-#{@tab}"}` and the patch URL byte-for-byte. Do NOT add `role=tab`/`role=tablist` and do NOT adopt `ck_tabs/1` (Pitfall B — it would break the `?tab=` patch + the `tus-resume.spec.js` `?tab=tus` deep link). Then **DELETE `defp tab_class/2` entirely** (lines ~473-476).
    7. Panel class swaps (per the Per-Element Class Map): each status `<p class="font-mono text-sm" id="X-upload-status">` → `class="ck-output"`; the tus error `<p class="text-red-600 …" id="tus-upload-error">` → `class="ck-error"` + `role="alert"` + an inline warning icon (copy the `ck_icon(:warning)` SVG markup from `cohort_components.ex`; ck_icon is private) wrapping the message in a `<span>`, keeping `:if={@tus_error}` + `id`/`data-testid`; each bare file `<input type="file" … phx-hook=...>` and each `<.live_file_input>` → add `class="ck-input"` (keep every attr/binding); the `multipart-upload-button` (`phx-hook="MultipartUpload"`) and the two `type="submit"` buttons → `class="ck-btn ck-btn--primary"` on the EXISTING `<button>` (NEVER `ck_button/1` — Pitfall A: it renders `<.link href>` and drops the hook/submit); the description `<p class="text-sm">` per panel and `image-upload-asset-id` → `class="ck-help"`; `mux-streaming-url` (`text-xs break-all`) → `class="ck-output"` (scroll-not-wrap is accepted per the Mux-URL caveat — do NOT add a new CSS rule); drop the `mt-6 space-y-3` panel-wrapper daisyUI utilities from the 6 `<div :if>` wrappers (keep the `:if={@tab == "X"}` + `id`/`data-testid`).

    Confirm it compiles cleanly: `cd examples/adoption_demo && MIX_ENV=test mix compile` (note: default-env `--warnings-as-errors` fails on pre-existing Mox warnings per Phase 96 deferred-items; use MIX_ENV=test for a clean signal).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rindle/examples/adoption_demo && MIX_ENV=test mix compile 2>&1 | tail -5</automated>
    <automated>cd /Users/jon/projects/rindle/examples/adoption_demo && grep -q 'import AdoptionDemoWeb.CohortComponents' lib/adoption_demo_web/live/upload_live.ex && grep -q '<.ck_page' lib/adoption_demo_web/live/upload_live.ex && grep -q 'ck-tabs__list' lib/adoption_demo_web/live/upload_live.ex && grep -q 'aria-current=' lib/adoption_demo_web/live/upload_live.ex && ! grep -q 'defp tab_class' lib/adoption_demo_web/live/upload_live.ex && ! grep -q 'raw(' lib/adoption_demo_web/live/upload_live.ex && echo MIGRATION_OK</automated>
    <automated>cd /Users/jon/projects/rindle/examples/adoption_demo && for h in PresignedPut PresignedVideoPut PresignedMuxPut MultipartUpload; do grep -q "phx-hook=\"$h\"" lib/adoption_demo_web/live/upload_live.ex || { echo "MISSING HOOK $h"; exit 1; }; done && for t in image tus video multipart liveview mux; do grep -q "upload-tab-#{@tab}" lib/adoption_demo_web/live/upload_live.ex || true; done && echo HOOKS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `upload_live.ex` imports CohortComponents, composes `<.ck_page … theme={@theme}>` inside `<Layouts.app … nav={:upload}>` (Layouts.app source UNCHANGED), and `MIX_ENV=test mix compile` succeeds.
    - The tab strip is `<.link patch>` links with `class="ck-tabs__tab ck-tab"` + `aria-current`; no `role=tab`/`role=tablist`/`ck_tabs`/`phx-hook="Tabs"` present; `defp tab_class/2` is gone.
    - Every `phx-hook` (`PresignedPut`/`PresignedVideoPut`/`PresignedMuxPut`/`MultipartUpload`), `phx-change`/`phx-submit`, `type="submit"`, file-input attr, the 2 `<.form>`, the 2 `<.live_file_input>`, every `id`/`data-testid`, and the 6 `<div :if={@tab == "X"}>` panels survive byte-for-byte.
    - The `?theme` read is enum-validated (`~w(light dark)`, default `"light"`) in both `mount/2` and `handle_params/2`; no `raw/1` introduced; the member line `<strong>` is preserved byte-for-byte.
    - The tus error is `.ck-error` + `role="alert"` + inline warning icon (no `text-red-600`); hook/submit buttons carry `.ck-btn ck-btn--primary` on the existing `<button>` (no `ck_button/1`).
  </acceptance_criteria>
  <done>/upload renders on the .ck-* DS class-by-class with the frozen DOM contract intact, a11y-correct routed tabs, a validated server theme read, and an announced error state; tab_class/2 deleted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author the /upload per-tab frozen-contract + daisyUI-retirement ExUnit test</name>
  <files>examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs</files>
  <read_first>
    - examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs — the shared helpers `assert_frozen_contract/2`, `assert_daisyui_retired/1`, `render_route/2`, `page_body/1` (reuse UNCHANGED); the existing single-page tests (e.g. the `/ops` test) for the exact shape to mirror; the now-extended `@retired_daisyui_classes`.
    - .planning/phases/100-cohort-upload-migration-all-tabs-track-b/100-PATTERNS.md — Pattern Assignment 3 STEP 2 (the exact `for`-comprehension shape + the 6 `panel_contract/1` clauses).
    - examples/adoption_demo/lib/adoption_demo_web/live/upload_live.ex — confirm the per-tab panel ids/testids/hooks/form selectors the test asserts actually exist (post-migration).
  </read_first>
  <behavior>
    - For each tab in `~w(image tus video multipart liveview mux)`, `render_route(conn, ~p"/upload?tab=#{tab}")` returns HTML containing `data-ck-root` (asserted inside `assert_frozen_contract`).
    - The always-present contract (member line id+testid + all 6 `upload-tab-X` testids) is present on every tab.
    - The active panel's selectors (id, status testid, and the tab's hook/form/submit) are present — only the `:if`-rendered panel for that tab.
    - `assert_daisyui_retired/1` passes per tab (no `tabs`/`text-red-600`/standalone ` tab `/`break-all`/`font-mono text-sm`/`text-2xl`/`space-y-` in the page body).
    - `:if`-only selectors that render only after a handler fires (`tus-upload-error`, `image-upload-asset-id`, `mux-streaming-url`) are NOT asserted statically — they are the behavior specs' backstop (Plan 02 / the 6 specs).
  </behavior>
  <action>
    Add ONE test to the module: `test "/upload preserves its frozen contract and retires daisyUI across all tabs", %{conn: conn}` wrapping a `for tab <- ~w(image tus video multipart liveview mux)` loop. Inside the loop: render `~p"/upload?tab=#{tab}"`, call `assert_frozen_contract(html, [...])` with the always-present selectors (member line `id`/`data-testid` + the 6 `data-testid="upload-tab-X"`), call `assert_frozen_contract(html, panel_contract(tab))`, and call `assert_daisyui_retired(html)`. Add a private `panel_contract/1` with one clause per tab returning that tab's id/status-testid/hook-or-form/submit selectors (use the exact selector lists from PATTERNS.md Assignment 3 STEP 2). Add two static a11y-shape assertions (one place per the Validation map): `assert html =~ ~s(aria-current="page")` and `refute html =~ ~s(role="tablist")` / `refute html =~ ~s(role="tab")` — proving routed-tab links not a tablist. Reuse all shared helpers UNCHANGED. Do NOT force any `:if`-only panel true in the static test.

    Run the test: `cd examples/adoption_demo && mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rindle/examples/adoption_demo && mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - The new `/upload` per-tab test exists, loops all 6 tabs, and passes (0 failures).
    - `panel_contract/1` has 6 clauses; each asserts that tab's panel id + status testid + the tab-specific hook (`PresignedPut`/`PresignedVideoPut`/`PresignedMuxPut`/`MultipartUpload`) or form (`tus-form`/`liveview-form` + `phx-submit` + submit id).
    - The test asserts `aria-current="page"` is present and refutes `role="tablist"`/`role="tab"`.
    - `assert_daisyui_retired/1` passes for every tab (the extended list catches the /upload daisyUI strings).
    - No `:if`-only selector (`tus-upload-error`/`image-upload-asset-id`/`mux-streaming-url`) is asserted statically.
  </acceptance_criteria>
  <done>The static frozen-contract + daisyUI-retirement gate proves /upload's contract survived and the swap is complete across all 6 tabs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → LiveView mount/handle_params | URL query params (`?tab=`, the new `?theme=`) cross from the (untrusted) URL into server assigns |
| server assign → HEEx render | assign values interpolated into the rendered DOM (member name, status strings, asset ids, URLs, theme) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01 | Tampering / Information Disclosure | the new `?theme=` param read in `upload_live.ex` `mount/2`/`handle_params/2` | mitigate | Enum-validate to `~w(light dark)` (default `"light"`) before `assign`; never interpolate the raw param into a class/attr. The `ck_page` `:theme` attr enforces the same enum as a second gate. Asserted by the `ck_page` attr + the ExUnit grep (`data-theme="dark"` only for the valid value). |
| T-100-02 | Tampering (XSS via auto-escape bypass) | HEEx interpolation across the migrated body | mitigate | Introduce NO `raw/1` — HEEx auto-escapes `{...}`; the migration interpolates the same values as today (member name, status, asset ids, URLs). Asserted by `assert_frozen_contract`'s `refute html =~ "raw("`. |
| T-100-SC | Tampering | npm/hex installs | accept | This phase installs ZERO packages (no `mix deps.get`, no `npm install`, no `tokens.json` edit). Per RESEARCH `## Package Legitimacy Audit` (N/A). No `[ASSUMED]`/`[SUS]` package → no legitimacy checkpoint needed. |

No high-severity threats expected: this is a presentational restyle of a demo app with no new write paths, auth changes, or data flows; the only new security surface is the optional `?theme=dark` read, enum-gated above (ASVS L1, block-on: high).
</threat_model>

<verification>
- `MIX_ENV=test mix compile` succeeds (no new warnings beyond the pre-existing Mox ones).
- `mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs` — 0 failures; the new per-tab `/upload` test green.
- `node brandbook/src/cohort-contrast.mjs` — exits 0 (token-pair/parity/literal gate green; the one new rule is token-only).
- `grep` confirms: `<.ck_page` present, `ck-tabs__list` + `ck-tabs__tab ck-tab` + `aria-current` present, no `defp tab_class`, no `raw(`, no `role="tab"`/`role="tablist"`/`ck_tabs`/`phx-hook="Tabs"`, all 4 upload `phx-hook`s present.
- `Layouts.app` (`layouts.ex`) is unchanged by this plan; `tokens.json` and `admin-polish.js` are unchanged.
</verification>

<success_criteria>
- `/upload` (every `?tab=X`) renders the `.ck` shell (`data-ck-root`) via composed `ck_page/1`, class-by-class onto `.ck-*` primitives (COHORT-02 SC1).
- Every `id`/`data-testid`/`phx-hook`/`phx-change`/`phx-submit`/`type=submit` survives byte-for-byte across all 6 tabs (COHORT-02 SC2 static half).
- The routed tab strip is links + `aria-current="page"`, not `role=tablist` (D-100-03/04).
- daisyUI/Tailwind utilities are retired from the `/upload` page body across all tabs (D-100-06).
- Exactly ONE new token-only CSS rule (`.ck-tabs__tab[aria-current="page"]`); `tab_class/2` deleted; the tus error is announced (`.ck-error` + icon + `role="alert"`, D-96-15).
- A validated `?theme=dark` read exists (enum-gated, default light) enabling the Plan 02 dark case; no `raw/1` introduced.
</success_criteria>

<output>
Create `.planning/phases/100-cohort-upload-migration-all-tabs-track-b/100-01-SUMMARY.md` when done.
</output>

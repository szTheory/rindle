---
phase: 113-evaluation-baseline-release-hygiene
plan: 04
type: execute
wave: 2
depends_on: ["113-01", "113-02", "113-03"]
files_modified:
  - .planning/PROJECT.md
  - .planning/MILESTONES.md
  - .planning/RETROSPECTIVE.md
  - .planning/RELEASE-TRAIN.md
autonomous: true
requirements: [HYGIENE-01]
user_setup:
  - service: github
    why: "Rotating the RELEASE_PLEASE_TOKEN repo secret is a repo-admin action Claude cannot perform; it is the single irreducible human gate for cutting 0.3.2."
    dashboard_config:
      - task: "Rotate RELEASE_PLEASE_TOKEN (prefer a GitHub App installation token; or a fine-grained PAT with contents:write + pull-requests:write [+ issues:write]) and update the repo secret"
        location: "GitHub repo Settings -> Secrets and variables -> Actions -> RELEASE_PLEASE_TOKEN"
must_haves:
  truths:
    - "Hex 0.3.2 is live (curl hex.pm/api/packages/rindle | jq -r .latest_stable_version == 0.3.2), carrying the merged-but-unreleased v1.21 :epipe / $callers lib/ fixes; mix.exs / .release-please-manifest.json / CHANGELOG all reflect 0.3.2 (written by release-please, not by hand)."
    - "PROJECT.md / MILESTONES.md / RETROSPECTIVE.md reconcile the aspirational 'ships as 0.3.2' prose with reality using the D-11 canonical sentence, committed ONLY AFTER publish is observed (D-12)."
    - "A dated 0.3.2-published Verification-Log row is appended to RELEASE-TRAIN.md after the publish run succeeds."
  artifacts: []
  key_links:
    - "Token rotation (human) -> release.yml re-trigger -> release-please cuts 0.3.2 -> automerge -> gate-ci-green -> publish -> public_verify -> THEN truth edits (D-12 sequencing)"
---

<objective>
Track B, Wave 2 — GATED on a human checkpoint. After the Track-A doc/guard commits land and push to main (re-triggering release.yml with a rotated token), observe release-please cut and publish 0.3.2 via the canonical pipeline, then commit the truth-reconciliation edits that say "0.3.2 is now live" (D-10/D-11) — and ONLY then (D-12), never before publish.

Purpose: deliver the merged-but-unreleased v1.21 fixes to adopters (HYGIENE-01) and reconcile the aspirational planning prose with reality without creating a NEW lie. The cut itself is operational (rotate a secret, observe the existing machine) — Option A, D-05; Options B (hand-author PR) and C (out-of-band mix hex.publish) are rejected.
Output: 0.3.2 live on Hex + reconciled live-truth docs + a dated "published" ledger row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Truth-edit targets + ledger:
@.planning/MILESTONES.md
@.planning/RETROSPECTIVE.md
@.planning/RELEASE-TRAIN.md

# Pipeline reference (operational — do NOT hand-edit versioned source):
@.github/workflows/release.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm 0.3.2 is live (human checkpoint ALREADY SATISFIED 2026-06-30)</name>
  <files></files>
  <read_first></read_first>
  <action>
    ALREADY DONE — do NOT pause, do NOT re-prompt the maintainer. The maintainer rotated the token and the cut was driven to completion during the 2026-06-30 planning session. This task now only RE-CONFIRMS the published state before Task 2 writes the truth edits.

    Confirmed outcome (evidence for Task 2's RELEASE-TRAIN row): Hex live == 0.3.2; tag `rindle-v0.3.2` on merge SHA `d228b67`; publish+public-verify run `28420598348` all green (Public Verify GREEN — the :epipe fix in 0.3.2 also resolved the D-08 junit smoke bug that failed 0.3.1's Public Verify).

    Unsticking 0.3.2 required THREE fixes in series (NOT the single token rotation D-04/D-05 assumed):
    1. Maintainer rotated the expired `RELEASE_PLEASE_TOKEN` fine-grained PAT (fixed the `Bad credentials` 401).
    2. Relabeled merged PR #40 `autorelease: pending` -> `autorelease: tagged` — D-04 wrongly called this "cosmetic"; it was a real 2nd blocker (release-please re-found #40 instead of opening 0.3.2).
    3. The new PAT lacked `Actions: write`, so the automerge job's publish-dispatch step 403'd (`Resource not accessible by personal access token`); completed via a manual `gh workflow run release.yml --field recovery_ref=d228b67…`. DURABLE FIX STILL PENDING: add `Actions: Read and write` to the PAT (or move to a GitHub App token) so future releases auto-publish.
  </action>
  <verify>
    <automated>test "$(curl -s https://hex.pm/api/packages/rindle | jq -r .latest_stable_version)" = "0.3.2" && echo OK</automated>
  </verify>
  <done>Hex live == 0.3.2 re-confirmed; the three-blocker reality + the pending Actions:write durable fix are recorded for Task 2's ledger row.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the truth-reconciliation edits + the published ledger row (D-10/D-11/D-12)</name>
  <files>.planning/PROJECT.md, .planning/MILESTONES.md, .planning/RETROSPECTIVE.md, .planning/RELEASE-TRAIN.md</files>
  <read_first>
    - 113-RESEARCH.md Pitfall 4 (the VERIFIED current truth-edit line numbers — RE-GREP before editing, they drift) and D-11 (the canonical reconciliation sentence to reuse verbatim)
    - 113-PATTERNS.md "Doc-Edit Surfaces" table (per-file anchors + which lines to LEAVE)
    - .planning/PROJECT.md (the live-truth surface), .planning/MILESTONES.md, .planning/RETROSPECTIVE.md
  </read_first>
  <action>
    ONLY after Task 1's publish is observed (D-12 — do NOT pre-write "now live"). RE-GREP each surface first (line numbers in D-10/Pitfall 4 have drifted): `grep -n '0\.3\.2' .planning/PROJECT.md .planning/MILESTONES.md .planning/RETROSPECTIVE.md`.

    Edit the LIVE planning truth only (D-10 + the Pitfall-4 corrected catalogue):
    - .planning/PROJECT.md: the Current-State 0.3.2 line (~19), the "Release-state correction" block (~43-48 — re-date/re-tense to past), and the aspirational hits at ~70, ~109, ~405, ~599. Per Open Question 3 / Pitfall 4, INCLUDE lines ~70 and ~109 (they say "cut the stuck"/"ships as 0.3.2", which become past-tense once live) — these are live-truth prose beyond D-10's literal list. LEAVE line ~814 (the v1.21-retro statement, accurate as-of-its-context — D-10/D-13).
    - .planning/MILESTONES.md: the v1.21-stats 0.3.2 line (~20).
    - .planning/RETROSPECTIVE.md: the 0.3.2 line (~9).

    Use the D-11 canonical reconciliation sentence verbatim where a full statement fits: "Hex 0.3.2 (the two adopter-invisible v1.21 `lib/` `fix:` patches) was merged to `main` in v1.21 but left unreleased — release-please never opened the 0.3.2 PR (Hex live was 0.3.1). v1.22 Phase 113 (HYGIENE-01) cut the stuck release; 0.3.2 is now live." Use the short form ("…-> Hex 0.3.2 (merged in v1.21, released in v1.22 Phase 113 — was merged-but-unreleased)") for inline bullet hits.

    DO NOT edit any `.planning/milestones/v1.21-*` archive, v1.21 phase artifacts, CHANGELOG `**104-03:**` false positives, or STATE.md/ROADMAP.md/SEED-005/REQUIREMENTS.md hits (already correct / immutable history — D-13). Do NOT hand-edit mix.exs/manifest/CHANGELOG (release-please wrote those — D-12).

    Append the published Verification-Log row to .planning/RELEASE-TRAIN.md (cols Date | Check | Result | Evidence) with the REAL evidence:
    | 2026-06-30 | 0.3.2 publish + public verify | Pass | run 28420598348 (merge SHA d228b67) — Publish + Public Verify GREEN; Hex live == 0.3.2. Required THREE fixes: rotate expired RELEASE_PLEASE_TOKEN (fine-grained PAT) + relabel #40 pending->tagged + manual publish-dispatch (PAT lacked Actions:write — durable fix pending). |

    Optionally append ONE dated append-only line to the user-memory file project_v1_22_quality_consolidation_arc.md ("0.3.2 cut in Phase 113 on <date>"); never rewrite it and never touch the v1.21 memory file.
  </action>
  <verify>
    <automated>test "$(curl -s https://hex.pm/api/packages/rindle | jq -r .latest_stable_version)" = "0.3.2" && grep -q 'released in v1.22 Phase 113' .planning/PROJECT.md && grep -q 'now live' .planning/PROJECT.md && grep -q '0.3.2' .planning/MILESTONES.md && grep -q '0.3.2' .planning/RETROSPECTIVE.md && ! git diff --name-only | grep -qE '\.planning/milestones/v1\.21-' && echo OK</automated>
    <human-check>Spot-check that no pre-publish "now live" text was committed and that the v1.21 archive is untouched.</human-check>
  </verify>
  <done>Hex live == 0.3.2; PROJECT.md / MILESTONES.md / RETROSPECTIVE.md carry the D-11 reconciliation (committed only after publish); a dated published row is in RELEASE-TRAIN.md; no v1.21 archive or versioned source (mix.exs/manifest/CHANGELOG) was hand-edited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo-admin secret store -> release pipeline | RELEASE_PLEASE_TOKEN rotation (human) crosses into the pipeline; least-privilege scope + preference for a GitHub App token bound the blast radius. |
| Hex registry publish | the canonical gate-ci-green + exact-SHA + idempotency guards (not bypassed — Option A) protect the irreversible Hex publish. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-113-09 | Elevation of Privilege | rotated RELEASE_PLEASE_TOKEN scope | high | mitigate | Least-privilege: fine-grained PAT (contents:write + pull-requests:write [+ issues:write]) or a GitHub App installation token (preferred, no long-lived PAT); recorded in the ledger. |
| T-113-10 | Tampering / irreversibility | out-of-band publish bypassing the green-on-SHA gate | high | mitigate | Option A only — rotate + let the pipeline run; reject hand-authored release PR (Option B) and out-of-band mix hex.publish (Option C, D-05) which bypass gate-ci-green. |
| T-113-11 | Repudiation (planning truth) | pre-writing "0.3.2 is now live" before publish | medium | mitigate | D-12 sequencing enforced: truth edits gated behind the observed publish; verify gate asserts Hex == 0.3.2 before accepting the edits. |
| T-113-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs; the cut is operational (existing pipeline). |
</threat_model>

<verification>
- `curl -s https://hex.pm/api/packages/rindle | jq -r .latest_stable_version` == `0.3.2` (machine-observable after the human rotation).
- D-11 reconciliation sentence present in PROJECT.md (and short-form in MILESTONES.md / RETROSPECTIVE.md); no pre-publish "now live" prose.
- `git diff` touches NO `.planning/milestones/v1.21-*` path and NO versioned source (mix.exs / manifest / CHANGELOG hand-edits).
- Dated published row appended to RELEASE-TRAIN.md.
</verification>

<success_criteria>
Hex 0.3.2 is live (delivering the v1.21 fixes); the three live-truth docs reconcile the aspirational claim using the D-11 sentence, committed only after publish; the v1.21 archive and versioned source are untouched.
</success_criteria>

<output>
Create `.planning/phases/113-evaluation-baseline-release-hygiene/113-04-SUMMARY.md` when done.
</output>

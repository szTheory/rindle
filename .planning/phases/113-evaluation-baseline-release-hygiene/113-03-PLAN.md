---
phase: 113-evaluation-baseline-release-hygiene
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/public_smoke.sh
  - test/test_helper.exs
  - guides/release_publish.md
  - .planning/RELEASE-TRAIN.md
autonomous: true
requirements: [HYGIENE-01]
must_haves:
  truths:
    - "An abnormal-exit (e.g. :epipe-induced) install-smoke suite under public_smoke.sh yields a clean failure, not a confusing `File.Error ... bad argument` from JUnitFormatter writing rindle-junit.xml during teardown (D-08)."
    - "The corrected root-cause causal chain is recorded as prose in guides/release_publish.md (a stuck-release runbook entry) and .planning/RELEASE-TRAIN.md (a dated Verification-Log row) — using the CORRECTED chain, not D-04's PR-#40-was-0.3.2 framing."
  artifacts:
    - guides/release_publish.md
    - .planning/RELEASE-TRAIN.md
  key_links:
    - "D-08 junit hardening is an INDEPENDENT Track-A item — NOT a precondition of cutting 0.3.2 (Pitfall 3)"
---

<objective>
Track A, Wave 1. Two independent hardening items: (1) D-08 — harden the public_smoke.sh / JUnitFormatter write path so an abnormal-exit suite surfaces a clean failure rather than an opaque `bad argument`; (2) D-07 — record the CORRECTED stuck-release root cause as durable prose in the release runbook and the maintainer ledger.

Purpose: the 0.3.1 Public Verify failed with a junit `bad argument` that was actually a teardown symptom of the very :epipe bug 0.3.2 fixes (Pitfall 3) — harden it so future crashes are legible. And the investigation must leave a recorded causal chain that uses the verified-correct sequence (PR #40 was the 0.3.1 PR; the fixes merged 2026-06-28 after the last good run; the first post-fix run 2026-06-29 401'd on the expired token), NOT D-04's superseded "PR #40 was 0.3.2" narrative (Pitfall 1).
Output: a robust junit write path + two recorded-prose surfaces.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# In-repo analogs:
@scripts/public_smoke.sh
@test/test_helper.exs
@guides/release_publish.md
@.planning/RELEASE-TRAIN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden the public_smoke.sh / JUnitFormatter write path (D-08)</name>
  <files>scripts/public_smoke.sh, test/test_helper.exs</files>
  <read_first>
    - scripts/public_smoke.sh (whole file; the run_install_smoke_profile shell-out around lines 39-44 that runs `mix test test/install_smoke/generated_app_smoke_test.exs` with CI=true)
    - test/test_helper.exs lines 31-47 (the CI-gated JUnitFormatter wiring: formatters list, report_dir, the `File.mkdir_p!` guard)
    - 113-RESEARCH.md Pitfall 3 (the exact failure chain: :epipe -> File.Error ... bad argument at JUnitFormatter.handle_suite_finished/1, and the 3 vetted fix options) and 113-PATTERNS.md item 5
  </read_first>
  <action>
    The failure (verified, run 28246413418): public_smoke.sh runs the parent install-smoke suite with CI=true, which flips on JUnitFormatter; when a child `:epipe` crash propagates, JUnitFormatter.handle_suite_finished/1 fails to write `_build/test/junit/rindle-junit.xml` during an abnormal GenServer teardown, surfacing `File.Error ... bad argument` instead of the real crash.

    Pick ONE of the three research-vetted fixes (Claude's discretion), goal: an abnormal-exit suite yields a CLEAN failure, not `bad argument`. Recommended minimal option (c): in scripts/public_smoke.sh, run the parent install-smoke `mix test` with `CI` UNSET (e.g. `env -u CI mix test ...` for that specific shell-out) so JUnitFormatter does not engage during the crash-prone clean-room run — keeping the child app's own JUnit behavior unaffected. If instead hardening test_helper.exs (option a/b), ensure `_build/test/junit` exists+writable regardless of crash, or gate JUnitFormatter off for the install-smoke shell-out child. Keep the change tightly scoped — do NOT disable JUnit for the normal CI suite, and do NOT alter unrelated public_smoke.sh logic (the exact-SHA gate, hex index wait, etc.).

    This is NOT a precondition of the 0.3.2 cut (D-08 / Pitfall 3) — 0.3.2's tarball already contains the :epipe fix; this hardening is so a FUTURE crash is legible.
  </action>
  <verify>
    <automated>bash -n scripts/public_smoke.sh && grep -qiE 'junit|CI' scripts/public_smoke.sh test/test_helper.exs && mix test test/install_smoke/package_metadata_test.exs 2>&1 | tail -5 | grep -qE '0 failures' && echo OK</automated>
  </verify>
  <done>public_smoke.sh (or test_helper.exs) is hardened so an abnormal-exit install-smoke suite produces a clean failure with no `File.Error ... bad argument`; the normal CI JUnit emission is unchanged; package_metadata_test stays green; bash syntax is valid.</done>
</task>

<task type="auto">
  <name>Task 2: Record the CORRECTED root cause in release_publish.md + RELEASE-TRAIN.md (D-07)</name>
  <files>guides/release_publish.md, .planning/RELEASE-TRAIN.md</files>
  <read_first>
    - guides/release_publish.md `## Recovery Workflow Contract` (line 118) and `## Post-Publish Follow-Up` (line 131) — append the runbook subsection between them
    - .planning/RELEASE-TRAIN.md `## Verification Log (maintainer)` table (header at line 24-25, append after the last row ~line 32; cols: Date | Check | Result | Evidence)
    - 113-RESEARCH.md Pitfall 1 (the CORRECTED chain) + the Code Examples runbook/ledger templates (lines 351-381). NOTE: use the corrected chain, NOT D-04's "PR #40 = the 0.3.2 PR" framing.
  </read_first>
  <action>
    Append to guides/release_publish.md, as a new `### Stuck release: expired RELEASE_PLEASE_TOKEN (the `|| github.token` footgun)` subsection under `## Recovery Workflow Contract` (before `## Post-Publish Follow-Up`):
    - Symptom: `release-please failed: Bad credentials` in the Release Please job; no `chore(main): release rindle X.Y.Z` PR appears despite releasable commits on main.
    - Root cause (CORRECTED chain per Pitfall 1): the :epipe/$callers fix commits merged 2026-06-28, AFTER the last successful release-please run (2026-06-26, which re-found the already-merged 0.3.1 PR #40 — PR #40 was the 0.3.1 release PR, NOT a 0.3.2 PR). The next push-to-main (2026-06-29, run 28399407429) — the first that would have opened a 0.3.2 PR — failed with `Bad credentials` because RELEASE_PLEASE_TOKEN had expired and `secrets.RELEASE_PLEASE_TOKEN || github.token` lets a present-but-invalid secret WIN the `||` so github.token never engages. So no 0.3.2 PR was ever opened. (Do NOT write "PR #40 was the 0.3.2 PR".)
    - Recovery: (1) rotate RELEASE_PLEASE_TOKEN (prefer a GitHub App installation token — no expiry surprise — or a fine-grained PAT: contents:write + pull-requests:write [+ issues:write]); update repo Settings -> Secrets -> Actions; (2) if a prior release PR is stuck on `autorelease: pending` but its version IS published, relabel truthfully via `gh pr edit <N> --remove-label "autorelease: pending" --add-label "autorelease: tagged"`; (3) push (or re-run the latest main push) to re-trigger release.yml; the canonical automerge -> dispatch -> gate-ci-green -> publish chain runs.
    - Prevention: the release-train-drift.yml guard self-files an issue when main has releasable commits with no open release PR; the token-validity step in the Release Please job fails loudly on a present-but-invalid token.

    Append a dated Verification-Log row to .planning/RELEASE-TRAIN.md (root-cause + guarded), cols Date | Check | Result | Evidence:
    | 2026-06-29 | Release-please stuck: 0.3.2 PR never opened | Root-caused + guarded | `Bad credentials` 401 in run 28399407429; RELEASE_PLEASE_TOKEN expired; `secrets.X || github.token` masks a bad token. Rotated token + added release-train-drift guard + token-validity guard. |

    (The matching "0.3.2 publish + public verify — Pass" ledger row is written in Plan 04, AFTER publish — D-12. Do NOT pre-write it here.)
  </action>
  <verify>
    <automated>grep -q 'Bad credentials' guides/release_publish.md && grep -q 'RELEASE_PLEASE_TOKEN' guides/release_publish.md && grep -q '0.3.1' guides/release_publish.md && grep -q 'Bad credentials' .planning/RELEASE-TRAIN.md && grep -q '28399407429' .planning/RELEASE-TRAIN.md && ! grep -q 'PR #40 was the 0.3.2' guides/release_publish.md && echo OK</automated>
  </verify>
  <done>release_publish.md has a stuck-release runbook subsection with the CORRECTED causal chain (PR #40 = 0.3.1; fixes merged 2026-06-28; first post-fix run 2026-06-29 401'd) and rotation/recovery/prevention steps; RELEASE-TRAIN.md has the dated root-cause Verification-Log row citing run 28399407429; no "PR #40 was the 0.3.2" framing appears; the post-publish "released" row is deliberately NOT written here (D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install-smoke clean-room -> junit file I/O | public_smoke.sh runs a child suite that, on abnormal exit, attempts a junit write; the hardening keeps that write from masking the real crash. No untrusted input or secret crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-113-07 | Denial of Service (release proof) | public_smoke.sh junit write under abnormal suite exit | medium | mitigate | D-08: harden the write path so a crash surfaces a clean failure, not `bad argument`, keeping the post-publish parity proof legible. |
| T-113-08 | Repudiation | recorded root-cause accuracy | low | mitigate | The runbook + ledger record the verified-correct chain (Pitfall 1); verify gate refutes the superseded "PR #40 was 0.3.2" framing. |
| T-113-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this plan. |
</threat_model>

<verification>
- `bash -n scripts/public_smoke.sh` valid; `mix test test/install_smoke/package_metadata_test.exs` green.
- release_publish.md + RELEASE-TRAIN.md contain the corrected root-cause prose; the superseded "PR #40 was 0.3.2" framing is absent.
- No post-publish "0.3.2 is now live" prose written in this plan (that is Plan 04, after publish — D-12).
</verification>

<success_criteria>
The junit write path is hardened so abnormal exits are legible; the corrected stuck-release root cause is recorded in both the runbook and the maintainer ledger.
</success_criteria>

<output>
Create `.planning/phases/113-evaluation-baseline-release-hygiene/113-03-SUMMARY.md` when done.
</output>
---
phase: 98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/admin/components.ex
  - brandbook/src/admin-css-build.mjs
  - brandbook/tokens/rindle-admin.css
  - priv/static/rindle_admin/rindle-admin.css
autonomous: true
requirements: [UPLIFT-03, UPLIFT-04, UPLIFT-05, UPLIFT-06]
tags: [admin, design-system, css, generated-css, scaffold, motion, responsive, focus-visible]

must_haves:
  truths:
    - "page/1 exists in components.ex with slots :summary/:filters/:work(required)/:aside/:actions and attr :state :ok|:empty|:error|:loading, rendered in canonical DOM order (D-98-01, §A)."
    - "Omitting the required :work slot is a compile-time error (slot :work, required: true)."
    - "node brandbook/src/admin-css-build.mjs emits 'parity OK' and exits 0 — the requiredTokenUses/requiredSelectors/requiredMetaSelectors self-checks pass with the new Phase-98 selectors and var(--rindle-shadow-card) (D-98-13)."
    - "[NON-INFERABLE / Playwright-backstop, asserted in P4] At ≥1024px the :work+:aside region computed grid-template-columns resolves to two tracks; below 1024px it is one track (§A two-pane, lives ONLY in the min-width:1024px block, D-98-15)."
    - "[NON-INFERABLE / Playwright-backstop, asserted in P4] At <760px the data <table>/<tr>/<td> computed display is block and each <td>::before content resolves to attr(data-label); at ≥760px they are table/table-row/table-cell with empty ::before content (§C)."
    - "[NON-INFERABLE / Playwright-backstop, asserted in P4] Under emulated prefers-reduced-motion:reduce every animated selector computes transition-duration 0s (read UN-FROZEN); under no-preference it equals the token duration (§B, RESEARCH Pitfall 6)."
    - "[NON-INFERABLE / Playwright-backstop, asserted in P4] :focus-visible (keyboard) yields outline 2px solid var(--rindle-focus-ring) + outline-offset 2px in both themes; pointer :focus yields no ring (§D)."
    - "Both rindle-admin.css copies (brandbook/tokens/ and priv/static/rindle_admin/) are byte-identical after sync-admin-css.mjs (ADMIN-02); git diff --exit-code is clean on the regenerated brandbook copy (DS-01)."
  artifacts:
    - path: "lib/rindle/admin/components.ex"
      provides: "page/1 Level-3 scaffold component"
      contains: "def page("
    - path: "brandbook/src/admin-css-build.mjs"
      provides: "All Phase-98 generated-CSS blocks (scaffold grid, two-pane, mobile-first responsive, motion, :focus-visible, skip-link, stacked-table ::before) + fail-closed guards"
      contains: "rindle-admin-page"
    - path: "brandbook/tokens/rindle-admin.css"
      provides: "Regenerated admin stylesheet (generator output, never hand-edited)"
      contains: ".rindle-admin-page"
    - path: "priv/static/rindle_admin/rindle-admin.css"
      provides: "Byte-identical shipped copy of the regenerated stylesheet"
      contains: ".rindle-admin-page"
  key_links:
    - from: "lib/rindle/admin/components.ex page/1"
      to: "brandbook/src/admin-css-build.mjs .rindle-admin-page* selectors"
      via: "page/1 renders class='rindle-admin-page' (+ slot subselectors) with data-rindle-admin-* seam; all grid/measure/two-pane lives in the generated CSS, never inline"
      pattern: "rindle-admin-page"
    - from: "brandbook/src/admin-css-build.mjs"
      to: "priv/static/rindle_admin/rindle-admin.css"
      via: "sync-admin-css.mjs byte-mirror after admin-css-build.mjs regenerates brandbook/tokens/rindle-admin.css"
      pattern: "sync-admin-css"
---

<objective>
Author the Level-3 `page/1` scaffold in `lib/rindle/admin/components.ex` AND land ALL Phase-98 generated-CSS (two-pane grid §A, motion §B, mobile-first responsive §C, `:focus-visible`/focus-ring/skip-link §D, stacked-table `::before` §C) through `brandbook/src/admin-css-build.mjs` → build → contrast → gallery-check → `sync-admin-css.mjs` → byte-identical drift gate.

Purpose: The single generated-CSS file + byte-identical drift gate forbids parallel "motion CSS" / "a11y CSS" plans (they edit one generator → drift-gate thrash, RESEARCH Pitfall 2). All CSS lands here in P1 so the six surface migrations (P2b) and IA/microcopy edits (P3) touch markup/wiring ONLY. Implements D-98-01, D-98-12, D-98-13, D-98-14, D-98-15.

Output: `page/1` existing-but-UNUSED (console still renders old markup); every Phase-98 selector present and self-check-guarded; both CSS copies byte-identical; ExUnit static gate green for the unconditional clauses; the four conditional/cascade clauses authored here are PROVEN later in P4 (Playwright).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-CONTEXT.md
@.planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-UI-SPEC.md
@.planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-PATTERNS.md
@.planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-RESEARCH.md
@.planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan (P1) creates these net-new symbols — the source-grounding/drift pass must EXCLUDE them from "missing/undefined" verification because they are newly authored here:
- `page/1` function component in `lib/rindle/admin/components.ex` (slots `:summary/:filters/:work(required)/:aside/:actions`, `attr :state`).
- New generated-CSS selector blocks in `admin-css-build.mjs`: `.rindle-admin-page` (scaffold root), `.rindle-admin-page__summary`, `.rindle-admin-page__filters`, `.rindle-admin-page__work`, `.rindle-admin-page__aside`, `.rindle-admin-page__actions`, the two-pane `.rindle-admin-page--two-pane` (or equivalent) region, stacked-table `td::before { content: attr(data-label) }` rule, the nav disclosure button selector, and any new `:focus-visible` members.
- New entries in `requiredSelectors`/`requiredMetaSelectors` and `var(--rindle-shadow-card)` in `requiredTokenUses` (D-98-13 guards).
NOTE: Each `<td data-label="...">` MARKUP is added in P2b (rides the surface migration, D-98-08); P1 authors only the CSS `::before` rule that consumes it.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author the page/1 Level-3 scaffold in components.ex</name>
  <read_first>
    - lib/rindle/admin/components.ex (READ FULLY — analog: shell/1 ~L23-57 for attr/slot/render_slot idiom; empty_state/1 ~L123 and error_state/1 ~L134 for :state fallbacks; table/1 ~L172 for the bare-wrapper convention page/1 supersedes; alias Phoenix.LiveView.JS at L6)
    - .planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-UI-SPEC.md §A (canonical slot order, slot/attr API, :state semantics)
    - .planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-PATTERNS.md (page/1 scaffold pattern: declare slots, render in DOM order, :state drives existing empty/error/loading primitives; grid is NOT inline)
  </read_first>
  <action>
    Add a new `page/1` function component to `components.ex` (after `table/1`, before the private helpers). Declare attrs/slots immediately above the def per the file's idiom: `slot :summary`, `slot :filters`, `slot :work, required: true`, `slot :aside`, `slot :actions`, `attr :state, :atom, default: :ok` (values `:ok | :empty | :error | :loading`). Render the slots in canonical DOM order (§A): `:summary` triage band → `:filters` → `:work` (required) → `:aside` inspector → `:actions` footer. Wrap in `<div class="rindle-admin-page" data-rindle-admin-root data-rindle-admin-state={@state}>` (the scaffold root MUST carry a `data-rindle-admin-*` seam so both test homes can target it). Give each slot a BEM subselector class: `rindle-admin-page__summary`, `__filters`, `__work`, `__aside`, `__actions`. The `:work + :aside` two-pane region needs a stable selector (e.g. wrap `:work` and `:aside` in a `.rindle-admin-page__panes` container, or class the root `.rindle-admin-page--two-pane` when `:aside` is present) so the generated CSS in Task 2 can target the two-pane grid — DECIDE the exact class in this task and use the SAME name in Task 2. The `:state` attr drives the existing fallbacks: when `@state` is `:empty`/`:error`/`:loading`, render the matching existing primitive (`empty_state/1` / `error_state/1` / a loading-skeleton wrapper) INSTEAD of (or above) the `:work` content — call the existing primitives, do NOT duplicate their markup. Put NO `display:grid`/`grid-template-columns`/measure styling inline — ALL layout is authored in `admin-css-build.mjs` (Task 2), per §A "structurally forbids page-local styling" and D-98-12.
    Do NOT migrate any surface onto `page/1` here — page/1 ends this plan existing-but-unused (D-98-01).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | grep -v "Mox\|test/support" ; mix test test/brandbook/admin_design_system_validation_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def page(" lib/rindle/admin/components.ex` returns a match.
    - `grep -n "slot(:work" lib/rindle/admin/components.ex` (or `slot :work`) shows `required: true`.
    - `grep -c "rindle-admin-page" lib/rindle/admin/components.ex` ≥ 1 (scaffold root class emitted).
    - The component compiles; `mix compile` succeeds (pre-existing test-only Mox warnings under MIX_ENV=test are allowed per Phase-96 note — verify with `MIX_ENV=test mix compile` if `--warnings-as-errors` trips on them).
    - page/1 is NOT referenced from any `*_live.ex` yet (`grep -rn "<.page" lib/rindle/admin/live/` returns nothing) — it ends unused this plan.
  </acceptance_criteria>
  <done>page/1 scaffold exists with the canonical slot order + required :work + :state-driven fallbacks + a data-rindle-admin-* seam, carries no inline grid/measure, and is not yet wired to any surface.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author all Phase-98 generated-CSS blocks + fail-closed guards in admin-css-build.mjs, regenerate, contrast, gallery-check, sync</name>
  <read_first>
    - brandbook/src/admin-css-build.mjs (READ the relevant ranges: the single growing `css` template string and `writeFileSync(adminCssPath, css)` ~L986; the co-located `@media (max-width:760px)` block ~L946; the reduced-motion block ~L970; the `:focus-visible` block ~L927-942; the `.rindle-admin-shell` grid ~L111-121; the `requiredSelectors` ~L989-1026, `requiredMetaSelectors` ~L1030-1043, `requiredTokenUses` ~L1046-1054 self-check arrays and the `for`/`missing`/`process.exit(1)` loop ~L1056-1060)
    - brandbook/src/sync-admin-css.mjs (the byte-mirror step)
    - brandbook/tokens/tokens.json (confirm tokens exist: motion transition/popover/toast/press + 3 easings, breakpoint sm480/md760/lg1024/xl1280, space_fluid gutter/section, radius card14/panel20, focus 2px/2px, shadow-card — NO tokens.json change this phase)
    - .planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-UI-SPEC.md §A/§B/§C/§D (grid/two-pane, motion catalog + GPU-only + reduced-motion, two-stop responsive, :focus-visible)
    - .planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-PATTERNS.md (generated-CSS blocks pattern: token-consuming selector idiom, media-query two-stop comment, fail-closed guards)
    - .planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-CONTEXT.md (D-98-12/13/14/15)
  </read_first>
  <action>
    Author EVERY new selector as literal template-string blocks INSIDE `admin-css-build.mjs` (co-located with the existing `@media` block ~L946 — this is the redirect target for every UI-SPEC "L1087" citation, D-98-14), consuming ONLY existing `var(--rindle-*)` tokens (never literals except media-query breakpoint pixel values, which CSS conditions cannot read from custom props). Author these blocks:
    1. SCAFFOLD GRID (§A): `.rindle-admin-page` page padding (`--rindle-space-fluid-gutter` horizontal, `--rindle-space-fluid-section` section gap, card gap `--rindle-space-5`, intra-card `--rindle-space-4`/`-3`); `.rindle-admin-page__work` single-column by default; `:summary` 1st-order hierarchy = `background: var(--rindle-surface-raised)` + `border-radius: var(--rindle-radius-panel)` (20px) + `box-shadow: var(--rindle-shadow-card)` — THIS is the first consumer of `--rindle-shadow-card` (D-98-13 guard-1); `:work` table 2nd-order = `var(--rindle-surface)` + `border-radius: var(--rindle-radius-card)` (14px); text-region measure cap ~68–78ch (tables exempt/full-width).
    2. TWO-PANE (§A, lives EXCLUSIVELY in the min-width:1024 block, D-98-15): the two-pane region selector (matching the class chosen in Task 1) gets `grid-template-columns: minmax(0,1fr) minmax(320px,380px)` ONLY inside `@media (min-width:1024px)`; single track below.
    3. MOBILE-FIRST RESPONSIVE (§C, D-98-14): convert the desktop-first `@media (max-width:760px)` shell rule to mobile-first. Author TWO never-conflated min-width blocks (D-98-15): `@media (min-width:760px)` owns the SHELL sidebar `grid-template-columns: minmax(220px,260px) minmax(0,1fr)` ONLY (with a CSS comment "/* 760 = shell sidebar switch ONLY */"); `@media (min-width:1024px)` owns the `:work + :aside` two-pane ONLY (CSS comment "/* 1024 = :aside two-pane ONLY */"). Add `@media (min-width:480px)` filter-fields-2-up and `@media (min-width:1280px)` main max-width cap. STACKED TABLE: at base/<760 flip `.rindle-admin-table`/`tr`/`td` to `display:block` and emit `.rindle-admin-table td::before { content: attr(data-label); }` (the `<td data-label>` markup is added in P2b — author only the rule here). EXCEPTION: `.rindle-admin-table--sticky` keeps `overflow-x:auto` at all widths (D-98-08). NO column gets `display:none` at any breakpoint (§C — operators must never silently lose State/Job columns). Nav disclosure button selector with `--rindle-admin-target-min` (44px) hit-area.
    4. MOTION (§B): for each catalog row author the animated selector with `transition` enumerating EXACT properties only — `transition: opacity var(--rindle-motion-*) <easing>, transform var(--rindle-motion-*) <easing>`. NEVER `transition: all`. Button press `:active { transform: translateY(1px) }` (press 120ms standard); popover/menu/theme-picker opacity+transform (popover 160ms); toast opacity+transform translateY (toast 200ms); drawer/dialog opacity+transform (transition 300ms); status-chip CONTENT cross-fade only (no color tween/pulse); skeleton→content opacity cross-fade (static skeleton). Elevation via static box-shadow token swap (instant). GPU-only: ONLY transform/opacity animate. Extend the SINGLE `@media (prefers-reduced-motion:reduce)` block to collapse every new animated selector to `transition-duration:0ms; animation:none; transform:none`.
    5. FOCUS-VISIBLE + SKIP-LINK (§D): append any NEW interactive selectors (nav disclosure button, sort-th control, stacked-row tap target) to the existing comma-list `:focus-visible` block (`outline: var(--rindle-focus-width) solid var(--rindle-focus-ring); outline-offset: var(--rindle-focus-offset)`). Author the skip-link selector (visually-hidden until focused) and a dialog `:focus`/permanent-border rule (a programmatically-focused dialog may not trigger `:focus-visible`, RESEARCH).
    6. FAIL-CLOSED GUARDS (D-98-13): add `'var(--rindle-shadow-card)'` to `requiredTokenUses`; add every new structural selector (`.rindle-admin-page`, the two-pane region selector, the stacked-table `::before` rule marker, the disclosure button) to `requiredSelectors`/`requiredMetaSelectors` so a dropped block fails the build.
    Then run the full pipeline in order: `node brandbook/src/admin-css-build.mjs` (regenerate brandbook/tokens/rindle-admin.css + self-check "parity OK") → `node brandbook/src/admin-contrast.mjs` (58/58, source-hex full-float — do NOT add browser contrast) → the admin gallery-check → `node brandbook/src/sync-admin-css.mjs` (byte-mirror to priv). NEVER hand-edit either rindle-admin.css (D-98-12). Add NO Set/Map/Date.now nondeterminism (keep the generator deterministic for byte-equality).
  </action>
  <verify>
    <automated>node brandbook/src/admin-css-build.mjs && node brandbook/src/admin-contrast.mjs && node brandbook/src/sync-admin-css.mjs && cmp -s brandbook/tokens/rindle-admin.css priv/static/rindle_admin/rindle-admin.css && mix test test/brandbook/admin_design_system_validation_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `node brandbook/src/admin-css-build.mjs` exits 0 and its output contains `parity OK` (self-check passed with the new selectors + `var(--rindle-shadow-card)`).
    - `node brandbook/src/admin-contrast.mjs` exits 0 (58/58 unchanged — no token value changed).
    - `cmp -s brandbook/tokens/rindle-admin.css priv/static/rindle_admin/rindle-admin.css` exits 0 (byte-identical, ADMIN-02).
    - `git -C /Users/jon/projects/rindle diff --exit-code brandbook/tokens/rindle-admin.css` is clean AFTER a fresh regenerate (DS-01 determinism) — i.e. running the generator twice produces no diff.
    - `grep -c 'transition: all\|transition:all' brandbook/tokens/rindle-admin.css` returns 0 (§B — no `transition:all` anywhere).
    - `grep -c 'rindle-admin-page' brandbook/tokens/rindle-admin.css` ≥ 1; the file contains the `min-width: 1024px` two-pane block and the `content: attr(data-label)` stacked-table rule.
    - `grep -n "var(--rindle-shadow-card)" brandbook/src/admin-css-build.mjs` shows it in BOTH a rule body and the `requiredTokenUses` array.
    - `mix test test/brandbook/admin_design_system_validation_test.exs` passes (DS-01 selector-presence + drift + ADMIN-02 byte-equality green).
  </acceptance_criteria>
  <done>Every Phase-98 selector (scaffold grid, two-pane @1024-only, mobile-first two-stop responsive, stacked-table ::before, motion catalog with enumerated transition properties + reduced-motion collapse, :focus-visible + skip-link, dialog border) is authored in admin-css-build.mjs, guarded by the fail-closed self-checks, regenerated, contrast-clean, and byte-mirrored to priv with a clean drift gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build pipeline → shipped asset | `admin-css-build.mjs` (and `sync-admin-css.mjs`) write `priv/static/rindle_admin/rindle-admin.css`, a shipped static asset served to operators |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01-01 | Tampering | Hand-edit of generated/shipped `rindle-admin.css` bypassing the generator | mitigate | DS-01 `git diff --exit-code` after regenerate + ADMIN-02 byte-equality (`cmp -s`) fail the build on any drift; D-98-12 forbids hand-edits |
| T-98-01-02 | Tampering | Dropped/renamed CSS block ships a console with broken layout/motion | mitigate | Fail-closed `requiredSelectors`/`requiredMetaSelectors`/`requiredTokenUses` self-check (`process.exit(1)` on miss) — D-98-13 |
| T-98-01-03 | Denial of Service | Indeterminate spinner / animate-everything degrades operator console | accept | §B bans indeterminate spinners + GPU-only `transform`/`opacity`; reduced-motion collapse — no runtime DoS surface, generated CSS only |
| T-98-01-SC | Tampering | npm/node package installs | mitigate | N/A — this phase installs ZERO new packages (D-98-12, RESEARCH Package Legitimacy Audit N/A); no `[ASSUMED]`/`[SUS]` packages |
</threat_model>

<verification>
- ExUnit static gate: `mix test test/brandbook/admin_design_system_validation_test.exs` green (DS-01 selector presence + drift, ADMIN-02 byte-equality, DS-03 contrast 58/58).
- Generator determinism: regenerate twice → `git diff --exit-code` clean.
- No `transition:all` anywhere in the generated CSS.
- page/1 compiles and is unused (no surface references it yet).
- The four cascade/viewport/theme/focus clauses authored here (two-pane track count, stacked-table display+::before, reduced-motion 0s, :focus-visible-vs-pointer) are PROVEN in P4 via Playwright — they are non-inferable from this plan's static gate by design (RESEARCH Pitfall 1/6, VALIDATION backstops).
</verification>

<success_criteria>
- page/1 scaffold exists with canonical slot order, required :work, :state-driven fallbacks, data-rindle-admin-* seam, zero inline grid/measure — and is UNUSED (console renders old markup).
- All Phase-98 generated-CSS lands in admin-css-build.mjs and nowhere else; both CSS copies byte-identical; fail-closed guards extended (incl var(--rindle-shadow-card)).
- ExUnit gate green; generator deterministic; no transition:all; contrast 58/58.
</success_criteria>

<output>
Create `.planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-01-SUMMARY.md` when done.
</output>

---
phase: 98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco
plan: 03
type: execute
wave: 4
depends_on: ["98-02b"]
files_modified:
  - lib/rindle/admin/components.ex
  - lib/rindle/admin/router.ex
  - lib/rindle/admin/queries.ex
  - lib/rindle/admin/live/home_live.ex
  - lib/rindle/admin/live/variants_jobs_live.ex
  - lib/rindle/admin/live/assets_live.ex
  - lib/rindle/admin/live/upload_sessions_live.ex
  - lib/rindle/admin/live/runtime_doctor_live.ex
  - lib/rindle/admin/live/actions_live.ex
autonomous: true
requirements: [UPLIFT-07, UPLIFT-08]
tags: [admin, ia, routing, microcopy, triage-home, deep-links, variants-jobs-show, gds]

must_haves:
  truths:
    - "Nav (@surfaces in shell) renders exactly six items in order [Overview, Assets, Upload sessions, Processing, Doctor, Maintenance] with relabeled text + correct hrefs; no item is literally named Actions/Home/Status/Variants/Jobs/Runtime/Doctor (§E, D-98-03)."
    - "Overview (home_live.ex) renders a GDS triage home off Queries.home_status/1 with DOM order: needs-attention band -> system-health chips -> recent activity -> vanity totals last; the inspect/1 anti-pattern (~L56) is gone (§E, D-98-10)."
    - "Each needs-attention entry is an <a href> carrying a documented query param (variants-jobs?state=failed, variants-jobs?class=stale, assets?state=quarantined, upload-sessions?state=expired, runtime-doctor) mapping to ALREADY-PARSED handle_params filters — NO new filter routes (D-98-10)."
    - "Overview all-clear renders the affirmative copy ('Nothing needs attention. Lifecycle events are flowing and all Doctor checks pass.'), not the generic filtered-no-match empty state (§E)."
    - "A new variants-jobs/:id :show route + handle_params(%{\"id\"=>id}) + a new Queries run-detail function render one run's error_reason/attempt/worker + its single primary action, with redaction parity matching the Assets/Upload :show pattern; the borrowed assets/:id link is replaced (D-98-09)."
    - "The Actions verb-bucket is distributed (regenerate->Processing stale-row, release/quarantine->asset detail, reconcile->Doctor); Maintenance keeps only contextless cross-cutting ops (owner/batch erasure); inline confirm panels are rewired to confirm_dialog/1 (D-98-10, D-98-11)."
    - "All §F microcopy replacements applied: the six off-voice strings no longer appear in source and their replacements do; status labels in frozen lexicon; action labels <=4 words; no '!' in confirmation/destructive bodies; confirmation headings match '{Verb} this {noun}?' (§F)."
  artifacts:
    - path: "lib/rindle/admin/router.ex"
      provides: "New variants-jobs/:id :show route"
      contains: "/variants-jobs/:id"
    - path: "lib/rindle/admin/queries.ex"
      provides: "New run-detail query function with redaction parity"
      contains: "def "
    - path: "lib/rindle/admin/live/home_live.ex"
      provides: "GDS task-first triage home (no inspect/1)"
      contains: "home_status"
  key_links:
    - from: "lib/rindle/admin/live/home_live.ex needs-attention <a href>"
      to: "existing surface handle_params filters"
      via: "admin_path(@admin_base_path, \"variants-jobs?state=failed\") etc. — pure <a>, deep-links to already-parsed filters (D-98-10)"
      pattern: "state=failed|state=quarantined|state=expired|class=stale"
    - from: "lib/rindle/admin/live/variants_jobs_live.ex handle_params(%{\"id\"=>id})"
      to: "lib/rindle/admin/queries.ex run-detail function"
      via: "the :show clause loads one run's detail via the new redaction-parity Queries function and renders the one-run detail"
      pattern: "handle_params"
    - from: "lib/rindle/admin/live/actions_live.ex confirm flows"
      to: "lib/rindle/admin/components.ex confirm_dialog/1"
      via: "inline confirm panels replaced by the P2a confirm_dialog primitive with trigger id + open/close events + server-assign-driven inert"
      pattern: "confirm_dialog"
---

<objective>
IA + routing + microcopy: relabel/reorder the nav, rebuild Overview as a gov.uk task-first triage home, wire deep-link query params, add the dedicated `variants-jobs/:id` `:show` route + run-detail query, distribute the "Actions" verb-bucket (rewiring confirms to `confirm_dialog/1`), and apply all §F microcopy replacements. Implements D-98-03, D-98-09, D-98-10, D-98-11 (per-surface wiring), §E + §F.

Purpose: All six surfaces now render through `page/1` (P2b) with the §D primitives + overlay available (P2a). This plan layers the task-first IA, the content/label edits, and the one new route — merged because IA and microcopy are atomic content edits to the same surfaces now on the scaffold, and the IA gate demands all six nav items change together (D-98-03). This is the ONLY plan that adds a route/query/handler (D-98-09); deep-links reuse already-parsed filters (D-98-10, NO new routes).

Output: a task-first console — relabeled nav, triage home, contextual deep-links, dedicated Processing detail route with redaction parity, distributed actions with accessible confirm dialogs, and operator/SRE-voice microcopy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-CONTEXT.md
@.planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-UI-SPEC.md
@.planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-PATTERNS.md
@.planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-RESEARCH.md
@.planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-02b-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (P3) creates these net-new symbols — exclude from drift/undefined verification:
- NEW route: `live(Path.join(path, "/variants-jobs/:id"), Rindle.Admin.Live.VariantsJobsLive, :show)` in `router.ex` (same module, :show action — NO auth-macro change).
- NEW `Queries` run-detail function (name at executor discretion, e.g. `variant_run_detail/1`) returning `{:ok, %{generated_at: ..., run: ...}}` / `{:error, :not_found}` with redaction parity to `upload_session_detail/1`/`asset_detail/1`.
- NEW `handle_params(%{"id" => id}, _uri, socket)` clause + a one-run detail `render/1` clause in `variants_jobs_live.ex`.
- MODIFIED: shell `@surfaces` nav labels/order; `home_live.ex` triage render (replaces inspect/1); deep-link `<a href>`s across surfaces; `actions_live.ex` distribution + confirm_dialog wiring; §F microcopy strings in components.ex defaults + surface modules.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add the variants-jobs/:id :show route + Queries run-detail (redaction parity) + the :show render clause</name>
  <read_first>
    - lib/rindle/admin/router.ex (READ FULLY — :show routes ~L95-109: /assets/:id L97, /upload-sessions/:id L100-104, /variants-jobs index L106; the auth-gated mount-validation macro ~L58-111 — DO NOT change it)
    - lib/rindle/admin/queries.ex (READ asset_detail/1 ~L78-94, upload_session_detail/1 ~L125-148 for the with-pipeline + UUID cast + {:ok,map}/{:error,:not_found} idiom; @redacted_session_uri/@redacted_provider_id ~L23-24 + redacted_provider_id helper ~L552; the existing variant-run query the index uses)
    - lib/rindle/admin/live/variants_jobs_live.ex (READ FULLY — its handle_params, its render clauses, the borrowed assets/#{...} detail link ~L104-110; analog: assets_live.ex handle_params(%{"id"=>id}) split + two render clauses)
    - lib/rindle/admin/live/assets_live.ex (the detail :show pattern: handle_params id-clause -> load_detail -> render(%{detail: ...}) head)
    - .planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-UI-SPEC.md §A (dedicated detail route below md) + §E (the "thing" = one run's error_reason/attempt/worker + single primary action)
    - .planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-CONTEXT.md (D-98-09 option A LOCKED; the security note: preserve auth-gated mount-validation, match Assets/Upload redaction parity — no error_reason/worker/PII leakage beyond those routes)
    - .planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-PATTERNS.md (queries run-detail pattern + router :show pattern + variants_jobs migration)
  </read_first>
  <action>
    Add the dedicated Processing detail (D-98-09, option A):
    1. ROUTE: in `router.ex`, immediately after the existing `variants-jobs` index (L106), add `live(Path.join(path, "/variants-jobs/:id"), Rindle.Admin.Live.VariantsJobsLive, :show)`. Same module, `:show` action — make NO change to the auth-gated mount-validation macro (L58-111); the new route inherits it (security: auth gate preserved).
    2. QUERY: add a new run-detail function to `queries.ex` (e.g. `variant_run_detail/1`) following the `upload_session_detail/1` idiom: `is_binary` guard, `Ecto.UUID.cast`, `with`-pipeline returning `{:ok, %{generated_at: DateTime.utc_now(), run: <redacted run row>}}` / `{:error, :not_found}` (nil and :error both map to :not_found). Return ONE run's `error_reason` / attempt / worker. REDACTION PARITY (security, D-98-09): apply the SAME redaction discipline as the Assets/Upload detail rows — reuse the existing `@redacted_*` attrs / `redacted_provider_id` helpers; do NOT expose any field beyond what those `:show` routes already expose (no extra PII, no raw provider ids, no unredacted worker/error internals beyond the existing pattern).
    3. LIVEVIEW: in `variants_jobs_live.ex` add `def handle_params(%{"id" => id}, _uri, socket)` that loads the run detail via the new query into a `detail` assign (mirror assets_live's `handle_params(%{"id"=>id})` + the list-clause that resets `detail: nil` and loads the list). Add a one-run detail `render/1` clause (head matching `%{detail: %{run: _}} = assigns`) showing the run's error_reason/attempt/worker + its single primary action, composed through `page/1` (`:work` = the one "thing"). Replace the borrowed `assets/#{...}` "View details" link (L104-110) with `admin_path(@admin_base_path, "variants-jobs/#{id}")`. Preserve the PubSub re-render + behavior contracts (D-98-16).
  </action>
  <verify>
    <automated>mix compile 2>&1 | grep -iv "Mox\|test/support" | grep -i "error" || true ; mix test test/rindle/admin 2>/dev/null || true ; mix test test/brandbook/admin_design_system_validation_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '/variants-jobs/:id' lib/rindle/admin/router.ex` ≥ 1 with `:show`.
    - The auth-gated mount-validation macro is byte-unchanged: `git -C /Users/jon/projects/rindle diff lib/rindle/admin/router.ex` shows only the added `:show` line within the existing live_session (no macro edits).
    - A new run-detail function exists in queries.ex returning `{:ok, %{generated_at` / `{:error, :not_found}` and uses the existing `@redacted_*`/redaction helpers (`grep -n "redacted" lib/rindle/admin/queries.ex` shows the new fn reusing them).
    - `grep -n 'handle_params(%{"id"' lib/rindle/admin/live/variants_jobs_live.ex` matches; a detail render clause exists.
    - The borrowed assets link is gone: `grep -c 'assets/#{' lib/rindle/admin/live/variants_jobs_live.ex` is reduced (the variant detail now points at `variants-jobs/#{`).
    - Compiles; admin tests + ExUnit static gate green.
  </acceptance_criteria>
  <done>variants-jobs/:id :show route added (auth gate preserved), a redaction-parity run-detail query + handle_params id-clause + one-run detail render exist, and the borrowed assets/:id link is replaced — satisfying §A's dedicated-detail-route and the 760-1023px band's real detail route.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Nav relabel/reorder + Overview triage-home rebuild + deep-link query params + Actions distribution</name>
  <read_first>
    - lib/rindle/admin/components.ex (shell @surfaces nav attr ~L8-15 / surface_links; admin_path/2 ~L186-194; the confirm_dialog/1 from P2a)
    - lib/rindle/admin/live/home_live.ex (READ FULLY — the inspect/1 anti-pattern ~L56; current section structure to replace)
    - lib/rindle/admin/queries.ex home_status/1 ~L27 (already returns recommendations + counts — NO query change for the triage home)
    - lib/rindle/admin/live/actions_live.ex (READ FULLY — the verb-bucket sections + inline confirm panels ~L609-779 to distribute + rewire)
    - lib/rindle/admin/live/{assets,upload_sessions,variants_jobs,runtime_doctor}_live.ex (handle_params filter parsing — confirm the deep-link params already parse: state/class/profile/kind)
    - .planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-UI-SPEC.md §E (triage-home ordered sections, nav labels/order, Actions distribution, empty/healthy IA) + §F where confirm copy intersects
    - .planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-CONTEXT.md (D-98-03, D-98-10, D-98-11) + 98-PATTERNS.md (home triage rebuild + actions distribution)
  </read_first>
  <action>
    1. NAV (shell `@surfaces`): relabel + reorder to exactly six items in order Overview (was Home/Status) · Assets · Upload sessions · Processing (was Variants/Jobs) · Doctor (was Runtime/Doctor) · Maintenance (was Actions), with correct hrefs via `admin_path/2`. Keep `aria-current="page"`. No item literally named "Actions"/"Home/Status"/"Variants/Jobs"/"Runtime/Doctor".
    2. OVERVIEW TRIAGE HOME (home_live.ex, render-only off `Queries.home_status/1` which already returns recommendations + counts — NO query change, D-98-10): replace the `inspect/1` anti-pattern (~L56) with a GDS task-list. DOM order: (1) "Needs attention" band — ONLY non-zero problem counts, each a deep-link `<a href>` (e.g. "12 failed processing runs →" → `admin_path(@admin_base_path, "variants-jobs?state=failed")`, "8 stale variants →" → `?class=stale`, "3 quarantined assets →" → `assets?state=quarantined`, "5 expired upload sessions →" → `upload-sessions?state=expired`, orphaned objects → `runtime-doctor`); (2) system-health: 3 chips (lifecycle-events-flowing · Doctor pass/fail · storage reachable); (3) recent lifecycle activity (last 5); (4) vanity totals LAST/below the fold. All-clear (no problems) renders the affirmative copy "Nothing needs attention. Lifecycle events are flowing and all Doctor checks pass." — NOT the generic filtered-no-match empty state. Deep-links are PURE `<a href>` mapping to ALREADY-PARSED handle_params filters — add NO new routes/handlers (D-98-10).
    3. ACTIONS DISTRIBUTION (actions_live.ex, D-98-10): distribute the verb-bucket — regenerate → onto the Processing stale-variant row; release/quarantine → onto asset detail (Assets); reconcile → onto Doctor. Maintenance keeps ONLY genuinely contextless cross-cutting ops (owner-erasure / batch-erasure, GDPR-driven). Rewire the inline confirm panels (~L609-779) to the P2a `confirm_dialog/1` primitive: stash the trigger id, wire open/close events, and the server-assign-driven inert toggle (D-98-11). Where you place a contextual action on another surface (Processing/Assets/Doctor), wire its confirm there too.
    Make NO CSS edits. Microcopy strings touched here should already use §F copy (final pass is Task 3) — prefer writing the final §F strings directly where you author new content to reduce churn.
  </action>
  <verify>
    <automated>mix compile 2>&1 | grep -iv "Mox\|test/support" | grep -i "error" || true ; mix test test/brandbook/admin_design_system_validation_test.exs ; mix test test/rindle/admin 2>/dev/null || true</automated>
  </verify>
  <acceptance_criteria>
    - shell nav source contains the six relabeled items in order (`grep -n "Overview\|Upload sessions\|Processing\|Doctor\|Maintenance" lib/rindle/admin/components.ex`) and NO literal "Variants/Jobs"/"Runtime/Doctor"/"Home/Status" (`grep -c 'Variants/Jobs\|Runtime/Doctor\|Home/Status' lib/rindle/admin/components.ex` returns 0).
    - `grep -c "inspect(" lib/rindle/admin/live/home_live.ex` returns 0 (anti-pattern removed).
    - `grep -nE 'state=failed|class=stale|state=quarantined|state=expired' lib/rindle/admin/live/home_live.ex` matches (deep-links present).
    - `grep -c "Nothing needs attention" lib/rindle/admin/live/home_live.ex` ≥ 1 (affirmative all-clear copy).
    - No new filter routes added: `git -C /Users/jon/projects/rindle diff lib/rindle/admin/router.ex` shows ONLY the Task-1 `:show` line.
    - `grep -c "confirm_dialog" lib/rindle/admin/live/actions_live.ex` ≥ 1 (confirms rewired off the inline panels).
    - Compiles; admin tests + ExUnit static gate green.
  </acceptance_criteria>
  <done>Nav has the six relabeled/reordered items; Overview is a GDS triage home off home_status/1 (no inspect/1, deep-links to existing filters, affirmative all-clear); the Actions verb-bucket is distributed with confirms rewired to confirm_dialog/1; no new filter routes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Apply all §F microcopy replacements across components.ex defaults + surface modules</name>
  <read_first>
    - lib/rindle/admin/components.ex (empty_state heading ~L116 "No records match this view"; error_state ~L137-140 doubled copy + "Retry load"; live_indicator copy)
    - lib/rindle/admin/live/actions_live.ex ("Regenerate Variants" ~L779 casing; "Confirm broad regeneration" ~L773)
    - lib/rindle/admin/live/variants_jobs_live.ex (long diagnostic sentence ~L121)
    - .planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-UI-SPEC.md §F (FULL — voice rubric R1-R7, frozen status lexicon, per-surface header/empty/healthy bank, confirmation pattern, toasts, live_indicator copy, the six off-voice replacements table)
  </read_first>
  <action>
    Apply the §F microcopy contract (final pass). Replace the six off-voice strings (UI-SPEC §F table) and apply the per-surface bank:
    - components.ex: empty_state generic heading "No records match this view" → keep ONLY as the filtered-no-match fallback; per-surface §14 headings used by surfaces (each surface passes its own heading). error_state doubled "Rindle Admin could not load this surface" → heading "This surface could not load." + body "Review the runtime checks, then retry after the source is available." "Retry load" → "Retry". live_indicator: "Waiting for lifecycle events" (idle) / "Live — lifecycle events streaming" (streaming).
    - actions_live.ex: "Regenerate Variants" → "Regenerate variants" (casing); "Confirm broad regeneration" → "Regenerate stale variants across all profiles" + the locked confirm ("Regenerate stale variants?" body, no "!").
    - variants_jobs_live.ex: the long diagnostic sentence ~L121 → "This is a diagnostic recommendation. No repair runs on this surface."
    - Across all surfaces: action labels verb-first ≤4 words; status chip labels EXACTLY from the frozen lexicon for their domain (Asset/Variant/Upload session sets in §F); NO R4 hype words; NO R5 vague standalone labels (Go/Submit/Process/Fix/Continue/Manage/OK); NO "!" in confirmation/destructive bodies; confirmation headings match "{Verb} this {noun}?". Apply the locked confirm strings ("Purge this asset?", "Regenerate stale variants?", etc.) and the past-tense toasts from §F.
    CONSTRAINT: this is content/label edits ONLY — no structural, routing, or CSS changes.
  </action>
  <verify>
    <automated>mix compile 2>&1 | grep -iv "Mox\|test/support" | grep -i "error" || true ; mix test test/brandbook/admin_design_system_validation_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The six off-voice strings are GONE: `grep -c "No records match this view" lib/rindle/admin/components.ex` may remain only as a labeled filtered-no-match fallback (not a per-surface default); `grep -c "Retry load" lib/rindle/admin/components.ex` returns 0; `grep -c "Regenerate Variants" lib/rindle/admin/live/actions_live.ex` returns 0 (lowercased) ; `grep -c "Confirm broad regeneration" lib/rindle/admin/live/actions_live.ex` returns 0.
    - Replacements present: `grep -c "This surface could not load." lib/rindle/admin/components.ex` ≥ 1; `grep -c "diagnostic recommendation" lib/rindle/admin/live/variants_jobs_live.ex` ≥ 1; `grep -c "Regenerate stale variants" lib/rindle/admin/live/actions_live.ex` ≥ 1.
    - No "!" in confirm/destructive bodies (manual grep over confirm copy shows none).
    - Compiles; ExUnit static gate green (the §F denylist/lexicon ExUnit clauses are AUTHORED in P4, but copy must be correct now so they pass there).
  </acceptance_criteria>
  <done>All §F off-voice strings replaced and the per-surface bank/lexicon/confirm/toast copy applied across components.ex and the surface modules; content-only edits, no structure/route/CSS change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client URL/`:id` param → LiveView mount/handle_params | The new `variants-jobs/:id` route accepts an untrusted `:id`; the run-detail query reads + redacts run internals (error_reason/worker) |
| client query string → handle_params filter | Deep-link params (`state=`, `class=`) flow into already-parsed `handle_params` filters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-03-01 | Information Disclosure | `variants-jobs/:id` run-detail leaks `error_reason`/worker/PII beyond the Assets/Upload `:show` parity | mitigate | Reuse existing `@redacted_*`/redaction helpers; expose no field beyond the existing `:show` detail pattern; redaction-parity acceptance criterion (D-98-09) |
| T-98-03-02 | Elevation of Privilege | New `:show` route bypasses the auth-gated mount-validation macro | mitigate | Route added INSIDE the existing `live_session`/macro scope; macro byte-unchanged (verified via `git diff`); same auth path as `/assets/:id` |
| T-98-03-03 | Tampering | Malformed/forged `:id` (non-UUID, non-existent) causes crash or info leak | mitigate | `Ecto.UUID.cast` + `{:error, :not_found}` mapping (nil and :error → not_found); no raw id echoed; mirrors `asset_detail/1` |
| T-98-03-04 | Tampering | Unsanitized deep-link query param opens a new injection sink | mitigate | Deep-links reuse ALREADY-PARSED `handle_params` filters (`take_filters` over an allowlist `~w(state class profile kind)`); NO new param sinks (D-98-10) — confirm no raw param interpolation |
| T-98-03-SC | Tampering | npm/node package installs | mitigate | N/A — zero new packages this phase |
</threat_model>

<verification>
- ExUnit static gate green; admin tests green (route + query + handle_params compile and load).
- §E IA primarily proven in ExUnit (nav order/labels/hrefs, triage DOM order, deep-link params, affirmative all-clear) — those CLAUSES are authored as tests in P4; this plan makes the markup correct so P4 passes.
- §F microcopy primarily proven in ExUnit (denylist + lexicon + off-voice replacements) — authored in P4; copy made correct here.
- Security: redaction parity + auth-gate preservation are acceptance criteria (grep + git diff), backed by P4 assertions over the new route.
</verification>

<success_criteria>
- Task-first IA: six relabeled/reordered nav items, GDS triage Overview off home_status/1, contextual deep-links to existing filters, distributed Actions with accessible confirm dialogs.
- Dedicated variants-jobs/:id :show route + redaction-parity run-detail + one-run detail render; auth gate preserved; no new filter routes/param sinks.
- All §F microcopy replacements applied (off-voice strings gone, replacements present, lexicon/confirm/toast copy correct).
- Compiles; ExUnit static gate + admin tests green.
</success_criteria>

<output>
Create `.planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-03-SUMMARY.md` when done.
</output>

---
phase: 93-truth-docs-milestone-audit
plan: 04
type: execute
wave: 2
depends_on: ["93-01", "93-02", "93-03"]
files_modified:
  - test/install_smoke/docs_parity_test.exs
  - .planning/v1.18-MILESTONE-AUDIT.md
  - .planning/milestones/v1.18-MILESTONE-AUDIT.md
  - .planning/phases/93-truth-docs-milestone-audit/93-VALIDATION.md
  - .planning/ROADMAP.md
  - .planning/MILESTONES.md
autonomous: false
requirements: [TRUTH-07]

must_haves:
  truths:
    - "The parity test (docs_parity_test.exs) CI-locks every corrected surface: facade no longer denies admin UI; operations/troubleshooting no longer say 'no dashboard'; user_flows no longer excludes admin UI; admin_console.md exists, is in extras, mentions the macro; README links it"
    - "The full parity suite passes green AND the two existing facade/operations/user_flows assertions were reworked (not left silently matching old intent)"
    - "v1.18-MILESTONE-AUDIT.md is regenerated from current phase artifacts using the canonical v1.15 audit structure (NOT the stale draft's conclusions)"
    - "The milestone-close status reflects HUMAN-UAT sign-off state: tech_debt with explicit follow-ups unless the maintainer confirms UAT sign-off at execute time"
    - "93-VALIDATION.md has nyquist_compliant: true after the parity assertions are green"
  artifacts:
    - path: "test/install_smoke/docs_parity_test.exs"
      provides: "Truth-parity assertions locking all corrected surfaces"
      contains: "admin_console"
    - path: ".planning/milestones/v1.18-MILESTONE-AUDIT.md"
      provides: "Canonical v1.18 milestone audit"
      contains: "milestone: v1.18"
    - path: ".planning/phases/93-truth-docs-milestone-audit/93-VALIDATION.md"
      provides: "Nyquist closure for Phase 93"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "test/install_smoke/docs_parity_test.exs"
      to: "lib/rindle.ex / guides/*.md / guides/admin_console.md / README.md"
      via: "Code.fetch_docs/1 + File.read! assert/refute on corrected phrases"
      pattern: "refute.*admin ui|assert.*rindle_admin"
---

<objective>
Lock all corrected surfaces with parity assertions, regenerate the v1.18 milestone audit from
current artifacts, determine the honest milestone-close status (via a maintainer checkpoint),
and flip 93-VALIDATION.md to nyquist_compliant. This plan runs LAST (Wave 2) so every corrected
surface from Plans 01–03 exists before assertions run.

Purpose: Truth in this repo is enforced mechanically — without the parity lock the false phrases
can silently regress (Pitfall 5). The milestone audit is the canonical closeout artifact, and the
stale draft must be regenerated (Pitfall 1), not trusted. The close status must not over-claim
`shipped` while HUMAN-UAT is open (Pitfall 4).
Output: Extended parity test (green), regenerated audit at the canonical milestones/ path, updated
ROADMAP/MILESTONES links, and a compliant 93-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/93-truth-docs-milestone-audit/93-RESEARCH.md
@.planning/phases/93-truth-docs-milestone-audit/93-VALIDATION.md
@.planning/milestones/v1.15-MILESTONE-AUDIT.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Existing parity-test assertions that MUST be reworked together with the truth edits -->
<!-- (Pitfall 5: removing/adding text near them can break or silently preserve old intent). -->

In test/install_smoke/docs_parity_test.exs (current state, pre-Plan-01 edits):
  - test "operations and troubleshooting ... doctor vs runtime_status split" asserts
    `operations =~ "no dashboard"` (line ~333). Plan 01 removed "intentionally has no dashboard";
    this assertion MUST be reworked to assert the truthful console mention + retained
    "no auto-remediation", and MUST NOT keep asserting a bare "no dashboard".
  - test "user flows guide freezes the canonical owner-erasure support truth" asserts the
    normalized list includes "admin ui" (line ~360). Plan 01 removed admin UI from user_flows;
    that snippet MUST be replaced with a truthful token (e.g. retain "force-delete") and MUST NOT
    keep asserting "admin ui" is present.

Compiled-doc read pattern already in the file: Code.fetch_docs/1 → moduledoc!/1 helper.

Phase status inputs for the audit (verified): phases 90/91/92 VERIFICATION status = human_needed;
91-HUMAN-UAT.md and 92-HUMAN-UAT.md exist. Phase 89 VERIFICATION passed. All v1.18 reqs satisfied
per Plan 02 traceability closure.

Canonical audit location convention: working copy .planning/v1.18-MILESTONE-AUDIT.md (overwrite
the stale untracked draft) AND archived copy .planning/milestones/v1.18-MILESTONE-AUDIT.md
(ROADMAP/MILESTONES link the milestones/ path at completion).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 [BLOCKING — Nyquist lock]: Extend docs_parity_test.exs to lock all corrected surfaces</name>
  <read_first>
    - test/install_smoke/docs_parity_test.exs (existing pattern: Code.fetch_docs/1, moduledoc!/1, setup_all doc reads; the two assertions to rework at lines ~333 and ~360)
    - lib/rindle.ex (corrected facade moduledoc from Plan 01)
    - guides/operations.md, guides/troubleshooting.md, guides/user_flows.md (corrected by Plan 01)
    - guides/admin_console.md (created by Plan 03)
    - README.md (linked by Plan 03)
    - mix.exs (extras wiring by Plan 03)
  </read_first>
  <action>
    Extend `test/install_smoke/docs_parity_test.exs` (do NOT hand-roll a separate script). Add an
    `@admin_console_path` read and new assertions, and REWORK the two existing assertions that
    referenced the now-removed phrases (Pitfall 5):
    (1) Facade: read the compiled `Rindle` moduledoc via the existing `moduledoc!/1`/Code.fetch_docs
    pattern; `refute` it matches `~r/admin ui/i` in a denial context AND `assert` it matches
    "rindle_admin"; keep `assert` for "force-delete" and "scheduler/cron erasure".
    (2) operations.md + troubleshooting.md: `refute` each matches "intentionally has no dashboard";
    `assert` each retains "no auto-remediation"; `assert` operations mentions the admin console
    ("admin_console" or "admin console"). Update the existing `operations =~ "no dashboard"`
    assertion so it no longer asserts the bare false phrase.
    (3) user_flows.md: in the existing owner-erasure freeze test, REMOVE "admin ui" from the
    required-snippets list and `refute` user_flows matches "an admin UI" and "Admin UI, force-delete";
    keep "force-delete" asserted.
    (4) admin_console guide: `assert File.exists?("guides/admin_console.md")`,
    `assert guide =~ "rindle_admin"`, and `assert File.read!("mix.exs") =~ "guides/admin_console.md"`.
    (5) README: `assert File.read!("README.md") =~ "admin_console.html"`.
    Run the file after each edit; iterate until green. This task MUST leave the suite green. No
    fenced code blocks belong in this action — write the assertions directly in the test file.
  </action>
  <acceptance_criteria>
    - `mix test test/install_smoke/docs_parity_test.exs` passes (0 failures).
    - New/updated assertions reference admin_console + the macro: `grep -q "admin_console" test/install_smoke/docs_parity_test.exs` and `grep -q "rindle_admin" test/install_smoke/docs_parity_test.exs`.
    - No assertion still requires the bare false phrase: `! grep -E '=~ "no dashboard"' test/install_smoke/docs_parity_test.exs` and the user_flows freeze test no longer lists "admin ui" as a required snippet.
    - Repo-wide false-phrase scan is clean: `grep -rni -E "an admin UI|intentionally has no dashboard" lib/rindle.ex README.md guides/operations.md guides/troubleshooting.md guides/user_flows.md | grep -v admin_console` returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/install_smoke/docs_parity_test.exs && grep -q "admin_console" test/install_smoke/docs_parity_test.exs && grep -q "rindle_admin" test/install_smoke/docs_parity_test.exs && (grep -rniE "an admin UI|intentionally has no dashboard" lib/rindle.ex README.md guides/operations.md guides/troubleshooting.md guides/user_flows.md | grep -v admin_console && echo FAIL || echo OK)</automated>
  </verify>
  <done>The parity suite is green and CI-locks every corrected surface; the two old assertions are reworked to assert truth, not the removed false phrases; no false phrase remains on any public surface.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Maintainer confirms HUMAN-UAT sign-off state for the milestone close status</name>
  <what-built>
    Plans 01–03 corrected all public surfaces; Task 1 locked them with passing parity tests.
    Phases 90/91/92 verified all must-haves but their VERIFICATION status is `human_needed`
    (91-HUMAN-UAT.md and 92-HUMAN-UAT.md exist). The milestone audit's close status depends on
    whether you have signed off human UAT.
  </what-built>
  <how-to-verify>
    1. Review `.planning/phases/91-cohort-demo-evolution/91-HUMAN-UAT.md` and
       `.planning/phases/92-e2e-screenshot-driven-polish-loop/92-HUMAN-UAT.md`.
    2. Decide: have you completed and signed off the human UAT items for phases 90–92?
       - If YES (signed off): the audit may record `status: shipped`.
       - If NO (still pending): the audit records `status: tech_debt` with explicit HUMAN-UAT
         follow-ups (the honest default — do NOT over-claim `shipped`).
    3. Tell the executor which status to write, and (if shipped) note where UAT sign-off is recorded.
  </how-to-verify>
  <resume-signal>Reply "shipped — UAT signed off" or "tech_debt — UAT pending" (or describe the follow-ups to record).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Regenerate the v1.18 milestone audit + 93-VALIDATION flip + ROADMAP/MILESTONES links</name>
  <read_first>
    - .planning/milestones/v1.15-MILESTONE-AUDIT.md (canonical structure: YAML frontmatter + 9 sections)
    - .planning/v1.18-MILESTONE-AUDIT.md (STALE untracked draft — template ONLY; its "orphaned/missing" conclusions are false)
    - .planning/REQUIREMENTS.md (post-closure traceability from Plan 02 — 19/19)
    - .planning/phases/90-console-ops-actions/90-VERIFICATION.md, .planning/phases/91-cohort-demo-evolution/91-VERIFICATION.md, .planning/phases/92-e2e-screenshot-driven-polish-loop/92-VERIFICATION.md (phase status + must-have evidence)
    - .planning/ROADMAP.md (Phase 93 success criteria 1–6; milestone link row), .planning/MILESTONES.md (v1.18 entry)
    - .planning/phases/93-truth-docs-milestone-audit/93-VALIDATION.md (frontmatter to flip)
  </read_first>
  <action>
    (1) Regenerate `.planning/v1.18-MILESTONE-AUDIT.md` AND copy it to
    `.planning/milestones/v1.18-MILESTONE-AUDIT.md`, mirroring the v1.15 audit's YAML frontmatter
    (milestone, name, audited ISO, status, scores {requirements, phases, integration, flows},
    nyquist {compliant_phases, partial_phases, missing_phases, overall}, gaps, tech_debt) and its
    9 sections (Result/Status, Scope table, Phase Verification Summary for ALL phases 86–93,
    Requirements Coverage 3-source cross-reference, Integration Report, Nyquist Coverage, Tech Debt
    by Phase, Verdict). Score honestly: 19/19 requirements satisfied, 8/8 phases verified.
    Set `status` to the value the maintainer chose in Task 2 (`tech_debt` with explicit HUMAN-UAT
    follow-ups for phases 90/91/92 if UAT is pending; `shipped` only if signed off). Include Phase
    93's own VALIDATION/Nyquist as compliant (set by step 2 below). Do NOT copy the stale draft's
    "orphaned/missing" rows. (2) Flip `.planning/phases/93-truth-docs-milestone-audit/93-VALIDATION.md`
    frontmatter `nyquist_compliant: false` → `true` and `wave_0_complete: false` → `true` (parity
    assertions are green as of Task 1). (3) In `.planning/ROADMAP.md` Phase 93 entry, update
    "Plans: 0 plans created" to the real count and link the archived audit
    `milestones/v1.18-MILESTONE-AUDIT.md`; in `.planning/MILESTONES.md` v1.18 entry, link the audit.
    Do not mark the v1.18 milestone fully shipped in ROADMAP/MILESTONES if status is `tech_debt`.
    No fenced code blocks in this action — author the audit Markdown directly in the file.
  </action>
  <acceptance_criteria>
    - `test -f .planning/milestones/v1.18-MILESTONE-AUDIT.md` and `grep -q "milestone: v1.18" .planning/milestones/v1.18-MILESTONE-AUDIT.md`.
    - The audit is NOT the stale draft's content: `! grep -q "orphaned" .planning/milestones/v1.18-MILESTONE-AUDIT.md`.
    - The audit `status:` is `tech_debt` or `shipped` matching the Task 2 decision (NOT `gaps_found`, given 19/19 + 8/8).
    - The audit references all phases 86–93 and 19/19 requirements coverage: `grep -q "TRUTH-07" .planning/milestones/v1.18-MILESTONE-AUDIT.md` and `grep -qE "Phase 9[0-3]" .planning/milestones/v1.18-MILESTONE-AUDIT.md`.
    - If status is `tech_debt`, explicit HUMAN-UAT follow-ups are listed: `grep -qi "uat" .planning/milestones/v1.18-MILESTONE-AUDIT.md` (skip if maintainer chose `shipped`).
    - `grep -q "nyquist_compliant: true" .planning/phases/93-truth-docs-milestone-audit/93-VALIDATION.md`.
    - `grep -q "v1.18-MILESTONE-AUDIT.md" .planning/ROADMAP.md`.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/milestones/v1.18-MILESTONE-AUDIT.md && grep -q "milestone: v1.18" .planning/milestones/v1.18-MILESTONE-AUDIT.md && (grep -q "orphaned" .planning/milestones/v1.18-MILESTONE-AUDIT.md && echo FAIL || echo OK) && grep -q "TRUTH-07" .planning/milestones/v1.18-MILESTONE-AUDIT.md && grep -qE "Phase 9[0-3]" .planning/milestones/v1.18-MILESTONE-AUDIT.md && grep -q "nyquist_compliant: true" .planning/phases/93-truth-docs-milestone-audit/93-VALIDATION.md && grep -q "v1.18-MILESTONE-AUDIT.md" .planning/ROADMAP.md</automated>
  </verify>
  <done>The milestone audit is regenerated at the canonical milestones/ path with honest 19/19 + 8/8 scoring and the maintainer-chosen status; 93-VALIDATION is nyquist_compliant; ROADMAP/MILESTONES link the audit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI / future contributor → parity test | The parity test is the mechanical truth gate; if it asserts the wrong thing, false claims can regress unnoticed. |

## STRIDE Threat Register

This plan edits an ExUnit test + planning Markdown. No new runtime, routes, auth, or input handling.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-05 | Tampering (silent regression) | docs_parity_test.exs assertions | mitigate | Rework the two existing assertions so they assert the corrected truth, not the removed false phrase; run the suite green; acceptance criteria explicitly check the bare false phrase is no longer a required positive assertion. |
| T-93-06 | Repudiation (over-claim) | milestone audit status | mitigate | Status is set from an evidence-backed maintainer checkpoint; default `tech_debt` with recorded HUMAN-UAT follow-ups unless sign-off is confirmed. |
| T-93-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. No legitimacy gate required. |
</threat_model>

<verification>
- `mix test test/install_smoke/docs_parity_test.exs` green (Nyquist lock).
- Audit exists at the canonical path and is not the stale draft: `test -f .planning/milestones/v1.18-MILESTONE-AUDIT.md && ! grep -q orphaned .planning/milestones/v1.18-MILESTONE-AUDIT.md`.
- 93-VALIDATION compliant: `grep -q "nyquist_compliant: true" .planning/phases/93-truth-docs-milestone-audit/93-VALIDATION.md`.
</verification>

<success_criteria>
All corrected surfaces are CI-locked green; the v1.18 milestone audit is regenerated honestly at
the canonical path with the maintainer-chosen close status; 93-VALIDATION is nyquist_compliant;
ROADMAP/MILESTONES link the audit.
</success_criteria>

<output>
Create `.planning/phases/93-truth-docs-milestone-audit/93-04-SUMMARY.md` when done.
</output>

---
phase: 93-truth-docs-milestone-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/admin_console.md
  - mix.exs
  - README.md
autonomous: true
requirements: [TRUTH-07]

must_haves:
  truths:
    - "guides/admin_console.md exists and accurately documents the mountable console (mount, host-auth + production refusal, 8 pages, ops actions, self-contained assets, optional-dep behavior)"
    - "guides/admin_console.md is wired into mix.exs docs/extras so HexDocs renders it under Guides"
    - "README links to the Admin Console guide so a hex.pm reader learns the console exists"
    - "The guide does not promote Rindle.Admin.Queries to the public facade and does not claim any new lifecycle semantics"
  artifacts:
    - path: "guides/admin_console.md"
      provides: "Adopter-facing admin console how-to"
      contains: "rindle_admin"
      min_lines: 60
    - path: "mix.exs"
      provides: "docs/extras entry for admin_console.md"
      contains: "guides/admin_console.md"
    - path: "README.md"
      provides: "Admin Console mention + admin_console.html link"
      contains: "admin_console.html"
  key_links:
    - from: "mix.exs docs/extras"
      to: "guides/admin_console.md"
      via: "extras list entry → groups_for_extras Guides regex"
      pattern: "guides/admin_console.md"
    - from: "README.md Next Reads"
      to: "guides/admin_console.md"
      via: "[Admin Console](admin_console.html) link"
      pattern: "admin_console.html"
---

<objective>
Author the adopter-facing `guides/admin_console.md`, wire it into HexDocs `extras`, and add an
honest README mention. This is the affirmative half of TRUTH-07 (README is an OMISSION, not a
falsehood — fixed by addition).

Purpose: HexDocs readers currently never learn the console exists. The guide is the canonical
adopter how-to; without the `extras` wiring it never renders (Pitfall 3).
Output: A new guide describing the console accurately, listed in `mix.exs` extras (auto-grouped
under Guides), and linked from README.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/93-truth-docs-milestone-audit/93-RESEARCH.md

<interfaces>
<!-- Console ground truth the guide MUST describe (verified from router.ex). Do NOT over-claim. -->

Public entry: Rindle.Admin.Router.rindle_admin(path, opts \\ []) — expands to direct LiveView
routes inside a live_session (LiveDashboard/Oban-Web style), guarded by Code.ensure_loaded?
on Phoenix.LiveView/Phoenix.Router/Plug.Static (compiles away without phoenix_live_view, ADMIN-06).

Mount/auth (ADMIN-01): host owns browser pipeline, auth pipeline, LiveView :on_mount, scope,
actor assigns. Production REFUSES unsafe mounts — requires non-empty :on_mount OR auth_guarded?: true.
allow_unauthenticated?: true is dev/test-only, rejected in :prod.
Options: :on_mount, :as (default :rindle_admin), :home_path ("/"), :live_socket_path ("/live"),
:transport ("websocket"), :csp_nonce_assign_key, auth_guarded?, allow_unauthenticated?.

Assets (ADMIN-02): StaticAssetsPlug serves priv/static/rindle_admin/ — allowlist
rindle-admin.css, rindle-admin.js, logo.svg, favicon.svg; tokens.json denied. Zero host Tailwind.

8 routes (relative to mount path): / (HomeLive), /assets (index), /assets/:id (show),
/upload-sessions, /upload-sessions/:id, /variants-jobs, /runtime-doctor, /actions.

Actions (ADMIN-04): owner erasure preview/execute (typed `ERASE type:id`), batch erasure (typed
`ERASE N OWNERS` + partial-failure receipts), lifecycle repair (Rindle.reprobe/1,
Rindle.requeue_variants/2), variant regeneration, read-only quarantine triage. NO new lifecycle semantics.

Live updates (ADMIN-05): reuses Rindle.PubSub + :asset/:variant/:upload_session topics; reads in
Rindle.Admin.Queries (INTERNAL — keep OUT of the public facade and OUT of public module groups).

Cohort demo (DEMO-03): scope "/admin" + rindle_admin("/rindle", allow_unauthenticated?: true) →
/admin/rindle (demo port 4102), walkthrough in examples/adoption_demo/README.md.

Format analog to mirror (voice + section rhythm): guides/streaming_providers.md, guides/storage_gcs.md.
Material source (wrong tense/audience — adapt to present-tense adopter how-to):
guides/admin_console_architecture.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author guides/admin_console.md (adopter how-to)</name>
  <read_first>
    - guides/streaming_providers.md (format/voice analog — env/setup → wiring → usage → troubleshooting rhythm)
    - guides/storage_gcs.md (second optional-feature guide analog)
    - guides/admin_console_architecture.md (material source — forward-looking/internal; convert to present-tense adopter framing)
    - lib/rindle/admin/router.ex (source of truth for the macro, options, routes, asset plug)
  </read_first>
  <action>
    Create `guides/admin_console.md` in present-tense, adopter how-to voice mirroring
    `guides/streaming_providers.md`. Cover, in order: (1) What it is — host-authenticated,
    library-owned mountable console; the only new public surface this milestone. (2) Requirements
    — `phoenix_live_view` optional dependency; host owns auth. (3) Mounting — the
    `Rindle.Admin.Router.rindle_admin/2` macro inside an authenticated `scope`, with a
    copy-pasteable router example adapted from `admin_console_architecture.md` (present-tense,
    adopter-framed). (4) Auth & the production refusal rule — `:on_mount` / `auth_guarded?` /
    dev-only `allow_unauthenticated?` (state plainly that production refuses unsafe mounts).
    (5) Pages — the 8-route table. (6) Actions — destructive UX with typed confirmation, reuse of
    existing facade verbs, no new lifecycle semantics. (7) Assets / CSP — self-contained assets,
    `:csp_nonce_assign_key`, `:live_socket_path`, `:transport`. (8) Optional-dependency behavior —
    compiles away without LiveView. (9) Try it — the Cohort demo at `/admin/rindle`. Do NOT
    document or promote `Rindle.Admin.Queries` as a public facade API. Router/config code
    examples in a Markdown guide are allowed (this is a guide file, not a plan <action>).
  </action>
  <acceptance_criteria>
    - `test -f guides/admin_console.md` (file exists).
    - `grep -q "rindle_admin" guides/admin_console.md` (mount macro documented).
    - `grep -qi "on_mount\|auth_guarded\|allow_unauthenticated" guides/admin_console.md` (auth model documented).
    - `grep -q "/admin/rindle" guides/admin_console.md` (Cohort try-it pointer).
    - `! grep -q "Rindle.Admin.Queries" guides/admin_console.md` (internal queries NOT promoted as public API; a passing mention that reads stay internal is fine but it must not be presented as a facade entrypoint — keep the assertion as: no public-API framing).
    - File is substantive (≥ 60 lines): `[ "$(wc -l < guides/admin_console.md)" -ge 60 ]`.
  </acceptance_criteria>
  <verify>
    <automated>test -f guides/admin_console.md && grep -q "rindle_admin" guides/admin_console.md && grep -q "/admin/rindle" guides/admin_console.md && [ "$(wc -l < guides/admin_console.md)" -ge 60 ]</automated>
  </verify>
  <done>guides/admin_console.md exists, documents the macro/auth/8-pages/actions/assets/optional-dep, points to the Cohort demo, and does not present Rindle.Admin.Queries as public API.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the guide into mix.exs extras and add the README mention</name>
  <read_first>
    - mix.exs (docs/0 — extras list lines ~129–146; groups_for_extras lines ~147–150; groups_for_modules lines ~151–198)
    - README.md (guide list / "Next Reads" section ~265–272)
    - guides/admin_console.md (created in Task 1 — the file being wired)
  </read_first>
  <action>
    In `mix.exs` `docs/0` `extras:` list, add `"guides/admin_console.md"` (it auto-falls into the
    `Guides:` group via the existing `~r/guides\/(?!release_publish).*\.md$/` regex — no
    groups_for_extras change needed). Optionally add a "Admin Console" entry to
    `groups_for_modules` for `Rindle.Admin.Router` only (KEEP `Rindle.Admin.Queries` OUT of any
    public module group — it is internal). In `README.md`, add a short honest mention of the
    mountable admin console and a `[Admin Console](admin_console.html)` link in the "Next Reads"
    guide list (alongside the existing User Flows / Getting Started links). Do not remove or
    reorder existing README content; add only.
  </action>
  <acceptance_criteria>
    - `grep -q "guides/admin_console.md" mix.exs` (extras wired).
    - `grep -q "admin_console.html" README.md` (README links the guide).
    - `grep -qi "admin console" README.md` (honest mention present).
    - `! grep -q "Rindle.Admin.Queries" mix.exs` (internal queries not added to public module groups).
    - `mix docs` runs without error and produces `doc/admin_console.html`.
  </acceptance_criteria>
  <verify>
    <automated>test -f guides/admin_console.md && grep -q "guides/admin_console.md" mix.exs && grep -q "admin_console.html" README.md && (grep -q "Rindle.Admin.Queries" mix.exs && echo FAIL || echo OK) && mix docs >/dev/null && test -f doc/admin_console.html</automated>
  </verify>
  <done>admin_console.md is in mix.exs extras (renders under Guides), README links it, Rindle.Admin.Queries stays out of public module groups, and `mix docs` generates the guide page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HexDocs reader → admin console guide | The guide is how adopters learn to mount a privileged console. |

## STRIDE Threat Register

This plan adds a documentation guide + docs config + README link. No new runtime, routes,
auth, or input handling.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-03 | Information disclosure / misconfiguration | admin_console.md mount section | mitigate | Guide MUST state plainly that the host owns auth and production refuses unsafe mounts (non-empty :on_mount OR auth_guarded?: true), and that allow_unauthenticated? is dev/test-only — so adopters do not deploy an unauthenticated privileged console. |
| T-93-04 | Information disclosure | Rindle.Admin.Queries | mitigate | Keep Rindle.Admin.Queries out of the guide's public-API framing and out of mix.exs public module groups; it is internal read composition, not an adopter entrypoint. |
| T-93-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. No legitimacy gate required. |
</threat_model>

<verification>
- Guide renders in HexDocs: `mix docs` succeeds and `doc/admin_console.html` exists.
- Guide wired: `test -f guides/admin_console.md && grep -q "admin_console.md" mix.exs`.
- README mentions console: `grep -q "admin_console.html" README.md`.
</verification>

<success_criteria>
The adopter-facing admin console guide exists, is HexDocs-discoverable (extras + Guides group),
README links it truthfully, and the internal query module is not promoted.
</success_criteria>

<output>
Create `.planning/phases/93-truth-docs-milestone-audit/93-03-SUMMARY.md` when done.
</output>

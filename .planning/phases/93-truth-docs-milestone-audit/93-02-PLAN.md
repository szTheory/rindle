---
phase: 93-truth-docs-milestone-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/JTBD-MAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [TRUTH-07]

must_haves:
  truths:
    - "JTBD-MAP no longer lists 'admin UI' in job row 36 or the T4 frontier row"
    - "JTBD-MAP records the admin console as a SHIPPED job (✅, v1.18) via a new row, framed as a charter-recorded scope reversal"
    - "JTBD-MAP anchor line is refreshed to milestone v1.18 / hex 0.3.0 / current sha, with a dated regeneration-history entry appended"
    - "REQUIREMENTS.md traceability shows no active v1.18 requirement stuck at 'Planned'"
    - "TRUTH-07 checkbox is checked and its traceability Status reflects this phase closing it"
  artifacts:
    - path: ".planning/JTBD-MAP.md"
      provides: "T4 reversal across three edit points + refreshed anchor"
      contains: "v1.18"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Closed v1.18 traceability table"
      contains: "TRUTH-07"
  key_links:
    - from: ".planning/JTBD-MAP.md new shipped row"
      to: "Rindle.Admin.Router.rindle_admin/2"
      via: "shipped-job entry citing the public macro"
      pattern: "rindle_admin"
---

<objective>
Reverse the JTBD T4 "admin UI" exclusion idempotently and close v1.18 requirements
traceability. Two planning surfaces: `.planning/JTBD-MAP.md` (F6 job row 36 + F7 T4 frontier
row + a NEW shipped row + anchor refresh) and `.planning/REQUIREMENTS.md` (flip stale
"Planned" rows to "Complete", check TRUTH-07).

Purpose: Closes the strategy-doc and traceability portions of TRUTH-07. The internal job map
still excludes admin UI "by design"; the requirements table still marks shipped reqs as
"Planned". Both must reflect reality at milestone close.
Output: An idempotently-updated JTBD-MAP (anchor → v1.18) and a closed REQUIREMENTS traceability
table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/93-truth-docs-milestone-audit/93-RESEARCH.md

<interfaces>
<!-- The reversal is a deliberate, charter-recorded scope change (ROADMAP charter 2026-06-10), -->
<!-- NOT a T4 capitulation. The OTHER T4 items (HLS/DASH, DRM, AI/GPU, PDF/Office, CDN) stay excluded. -->
<!-- Public surface to cite in the new shipped row: Rindle.Admin.Router.rindle_admin/2 -->

Traceability rows currently stale (current → should-be), per research cross-reference:
  ADMIN-03 Planned→Complete (Phase 89 VERIFICATION passed)
  ADMIN-04 Planned→Complete (Phase 90 VERIFICATION 8/8)
  ADMIN-05 Planned→Complete (Phase 89 VERIFICATION passed)
  DEMO-01  Planned→Complete (Phase 91 VERIFICATION 6/6)
  DEMO-02  Planned→Complete (Phase 91 VERIFICATION)
  DEMO-03  Planned→Complete (Phase 91; Cohort mounts /admin/rindle)
  E2E-01   Planned→Complete (Phase 92 SUMMARY requirements-completed)
  DX-01    Planned→Complete (Phase 87)
  DX-02    Planned→Complete (Phase 87)
  DX-03    Planned→Complete (Phase 87 passed; Phase 91 mounts console)
  TRUTH-07 Planned→Complete (closed by THIS phase)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reverse the T4 admin-UI exclusion in JTBD-MAP (F6, F7 + new shipped row + anchor)</name>
  <read_first>
    - .planning/JTBD-MAP.md (anchor line ~3; "Update protocol" lines ~17–30; job row 36 line ~91; T4 frontier row line ~106; regeneration-history list at the bottom ~185+)
    - .planning/ROADMAP.md (charter at top of Phase 86–93 section — the recorded scope reversal)
    - lib/rindle/admin/router.ex (public macro to cite in the new shipped row)
  </read_first>
  <action>
    Follow the in-file "Update protocol" anchor steps idempotently — do NOT recreate the file.
    (1) Read the anchor line (`Against: milestone v1.16 … hex 0.1.5 … git fbd09de`).
    (2) Job row 36 (line ~91): remove "admin UI, " from the non-goals list — leave "Full
    HLS/DASH platform, DRM, AI/GPU processing, PDF/Office, ... CDN replacement" intact.
    (3) T4 frontier row (line ~106): remove "admin UI, " from the T4 row's capability list —
    the other T4 items stay; leave the "⛔ Excluded by design" disposition for the remaining
    items. (4) ADD a new shipped JTBD inventory row for the admin console: status ✅, milestone
    v1.18, citing `Rindle.Admin.Router.rindle_admin/2`, phrased as a charter-recorded maintainer-
    pull scope reversal (not a frontier capitulation). (5) Refresh the anchor line: date
    2026-06-13, milestone v1.18, hex 0.3.0, and the current git sha (`git rev-parse --short HEAD`).
    (6) Append a dated regeneration-history entry at the bottom describing the v1.18 admin-UI
    reversal and the new shipped row. Re-rank gap analysis only as needed to reflect the shipped
    console; do not touch unrelated rows.
  </action>
  <acceptance_criteria>
    - The T4 frontier row and job row 36 no longer contain "admin UI": `grep -nE "admin UI" .planning/JTBD-MAP.md` returns NO lines within the T4 table / row-36 region (a citation inside the new shipped/history entry referencing the reversal is acceptable but must not be an exclusion).
    - `grep -E "Against:.*v1\.18" .planning/JTBD-MAP.md` succeeds (anchor refreshed).
    - `grep -q "0.3.0" .planning/JTBD-MAP.md` succeeds (hex version refreshed in anchor).
    - `grep -q "rindle_admin" .planning/JTBD-MAP.md` succeeds (new shipped row cites the macro).
    - A new dated regeneration-history entry mentioning the admin UI reversal exists.
  </acceptance_criteria>
  <verify>
    <automated>grep -E "Against:.*v1\.18" .planning/JTBD-MAP.md && grep -q "rindle_admin" .planning/JTBD-MAP.md && grep -q "0.3.0" .planning/JTBD-MAP.md</automated>
  </verify>
  <done>JTBD-MAP shows admin UI removed from the two exclusion points, a new ✅ v1.18 shipped row citing rindle_admin/2, a refreshed v1.18/0.3.0 anchor, and a dated history entry.</done>
</task>

<task type="auto">
  <name>Task 2: Close v1.18 requirements traceability (REQUIREMENTS.md)</name>
  <read_first>
    - .planning/REQUIREMENTS.md (TRUTH-07 checkbox line ~69; Traceability table lines ~148–175; Coverage summary lines ~177–181)
    - .planning/phases/90-console-ops-actions/90-VERIFICATION.md (ADMIN-04 evidence)
    - .planning/phases/91-cohort-demo-evolution/91-VERIFICATION.md (DEMO-01..03 evidence)
    - .planning/phases/92-e2e-screenshot-driven-polish-loop/92-VERIFICATION.md (E2E-01 evidence)
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md`: (1) Check the TRUTH-07 checkbox (line ~69) from `[ ]` to
    `[x]`. (2) In the Traceability table, flip these Status cells from "Planned" to "Complete":
    ADMIN-03, ADMIN-04, ADMIN-05, DEMO-01, DEMO-02, DEMO-03, E2E-01, DX-01, DX-02, DX-03; set
    TRUTH-07 Status to "Complete". Leave already-correct rows (ADMIN-01, ADMIN-02, ADMIN-06,
    DS-01..03, E2E-02, PRIN-01, PAUSE-*, LIFE-06-*/STREAM-10-*/TRANS-01/PRIV-01) untouched —
    do NOT flip any Deferred row. (3) Update the Coverage summary line ~178 from "8 satisfied"
    to "19 satisfied" (19/19 v1.18 requirements). Do not invent new requirements or alter the
    Out-of-Scope / Future tables.
  </action>
  <acceptance_criteria>
    - `! grep -E "ADMIN-0[345].*Planned" .planning/REQUIREMENTS.md` (no ADMIN-03/04/05 stuck Planned).
    - `! grep -E "DX-0[123].*Planned" .planning/REQUIREMENTS.md` and `! grep -E "DEMO-0[123].*Planned" .planning/REQUIREMENTS.md` and `! grep -E "E2E-01.*Planned" .planning/REQUIREMENTS.md`.
    - `grep -E "TRUTH-07.*Complete" .planning/REQUIREMENTS.md` succeeds.
    - `grep -q "\[x\] \*\*TRUTH-07\*\*" .planning/REQUIREMENTS.md` succeeds (checkbox checked).
    - `grep -E "LIFE-06.*Deferred" .planning/REQUIREMENTS.md` still succeeds (deferrals untouched).
  </acceptance_criteria>
  <verify>
    <automated>(grep -E "ADMIN-0[345].*Planned|DX-0[123].*Planned|DEMO-0[123].*Planned|E2E-01.*Planned" .planning/REQUIREMENTS.md && echo FAIL || echo OK) && grep -E "TRUTH-07.*Complete" .planning/REQUIREMENTS.md && grep -E "LIFE-06.*Deferred" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>No active v1.18 requirement reads "Planned"; TRUTH-07 is checked and Complete; coverage says 19/19; deferred rows unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Internal planning artifacts only; not shipped to adopters or HexDocs. |

## STRIDE Threat Register

This plan edits internal planning Markdown only. No runtime, no routes, no auth, no input
handling. No new attack surface.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-02 | Tampering (planning truth) | REQUIREMENTS traceability | mitigate | Status flips must be evidence-backed against phase VERIFICATION artifacts (cited in read_first); no row flipped without verification evidence. |
| T-93-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. No legitimacy gate required. |
</threat_model>

<verification>
- JTBD-MAP anchor refreshed: `grep -E "Against:.*v1\.18" .planning/JTBD-MAP.md`.
- No active req stuck Planned: `grep -E "(ADMIN-0[345]|DX-0[123]|DEMO-0[123]|E2E-01|TRUTH-07).*Planned" .planning/REQUIREMENTS.md` returns nothing.
</verification>

<success_criteria>
F6/F7 reversed with a new shipped row + refreshed anchor; traceability closed with TRUTH-07
checked and all active v1.18 reqs Complete; deferred items preserved.
</success_criteria>

<output>
Create `.planning/phases/93-truth-docs-milestone-audit/93-02-SUMMARY.md` when done.
</output>

---
phase: 121-truthful-quality-signals-mechanical-hygiene
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/gsd_cleanup.sh
  - test/install_smoke/repository_residue_test.exs
autonomous: true
requirements: [SIGNAL-04]
must_haves:
  truths:
    - "Known exact-name root residue is removed by a tracked-safe cleanup command; existing general OS/crash ignores stay intact and recurrence-prone GSD/Credo root outputs remain narrowly ignored."
    - "Canonical milestone audit evidence, especially v1.8, remains present and duplicate root copies cannot be mistaken for the canonical archive."
    - "Tracked, unique, debug, audit, historical, and maintainer evidence is never deleted by cleanup matching alone."
  artifacts:
    - path: "scripts/gsd_cleanup.sh"
      provides: "Tracked-safe exact-file cleanup for known transient root outputs"
    - path: "test/install_smoke/repository_residue_test.exs"
      provides: "Shipped-tree residue, narrow-ignore, and cleanup-safety regression lock"
  key_links:
    - from: "scripts/gsd_cleanup.sh"
      to: ".gitignore"
      via: "exact GSD/Credo root outputs plus the existing general `.DS_Store` and `erl_crash.dump` rules"
    - from: "test/install_smoke/repository_residue_test.exs"
      to: ".gitignore"
      via: "root-anchored assertions only for GSD/Credo artifacts; general-rule assertions for OS/crash names"
---

<objective>
Turn the already evidence-backed repository cleanup into a repeatable, tracked-safe hygiene contract.

Purpose: SIGNAL-04 must keep mechanical residue out without creating a broad deletion rule that can erase durable evidence.
Output: an expanded safe cleanup allowlist and a shipped-artifact regression test for residue, ignores, and canonical audit preservation.

Planner assumption: commit `9da7a6f` already reconciled the unique v1.8 evidence into `.planning/milestones/v1.8-MILESTONE-AUDIT.md` before deleting duplicate root audit/lint files. Treat that canonical file as immutable evidence and plan only recurrence prevention; do not reopen or normalize historical archives.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-CONTEXT.md
@.gitignore
@scripts/gsd_cleanup.sh
@.planning/milestones/v1.8-MILESTONE-AUDIT.md
@test/planning_path_hygiene_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Extend tracked-safe cleanup to the proven root residue set</name>
  <read_first>scripts/gsd_cleanup.sh, .gitignore, .planning/milestones/v1.8-MILESTONE-AUDIT.md</read_first>
  <files>scripts/gsd_cleanup.sh</files>
  <behavior>
    - Cleanup removes only the named untracked `.DS_Store`, `erl_crash.dump`, GSD root JSON/text outputs, and root Credo JSON/text outputs when present.
    - A path matching the list but tracked by Git is reported and preserved.
    - No wildcard or recursive removal reaches `rindle-0.1.0-dev/`, other `rindle-*` directories, `.planning/milestones`, debug directories, or maintainer evidence.
  </behavior>
  <action>Add only the live proven root residue filenames to `TRANSIENT_FILES`; retain `is_tracked` as the mandatory deletion guard. Remove `TRANSIENT_GLOBS`, the generic `rindle-*` loop, and recursive deletion from the cleanup path. Delete exact files with a non-recursive file operation after checking they are untracked. Do not delete or inspect `rindle-0.1.0-dev/` during this phase; it is ignored local package evidence, not validated disposable residue. Do not add archive/debug/audit patterns or generic extension patterns. Run the cleanup only for the exact named files. Commit intent is `chore(121-05)`.</action>
  <acceptance_criteria>The cleanup removes the enumerated untracked root files, skips a tracked fixture in a temporary Git repository, leaves `rindle-0.1.0-dev/` untouched, contains no broad root glob, and never targets the canonical v1.8 milestone audit.</acceptance_criteria>
  <verify>
    <automated>bash scripts/gsd_cleanup.sh</automated>
  </verify>
  <done>Mechanically proven root residue is cleanable without risking tracked evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock residue recurrence and canonical audit preservation</name>
  <read_first>scripts/gsd_cleanup.sh, .gitignore, .planning/milestones/v1.8-MILESTONE-AUDIT.md, test/planning_path_hygiene_test.exs</read_first>
  <files>test/install_smoke/repository_residue_test.exs</files>
  <behavior>
    - The test fails if known generated root names become tracked, if GSD/Credo artifacts lose exact root-anchored rules, or if `.DS_Store`/`erl_crash.dump` lose their existing general ignore coverage.
    - The test rejects obsolete root audit copies without reading planning directories.
    - The test exercises the cleanup in an isolated temporary Git repo to prove tracked matches are preserved, exact untracked files are removed, and an ignored `rindle-0.1.0-dev/` directory remains byte-present.
  </behavior>
  <action>Create a shipped-artifact ExUnit test that checks `git ls-files` for the finite root residue list, requires exact root-anchored ignore entries only for the GSD/Credo root artifacts, and preserves/tests the repository's existing general `.DS_Store` and `erl_crash.dump` rules without rewriting them as root-only entries. Reject obsolete root audit filenames and broad cleanup globs/recursive removal, and behaviorally prove tracked-skip/exact-untracked-remove behavior in an isolated temporary Git repository. Include a sentinel file beneath `rindle-0.1.0-dev/` and assert cleanup leaves it untouched. Do not edit `.gitignore` unless execution discovers an actually missing exact GSD/Credo root-artifact rule; no OS/crash-rule churn is authorized. Keep the test free of planning-path reads so it survives PR-branch filtering. Verify canonical v1.8 evidence separately in the task command and leave the file byte-unchanged.</action>
  <acceptance_criteria>The test proves absence, narrow ignore coverage, and cleanup safety; task verification separately proves canonical audit evidence remains present and unchanged.</acceptance_criteria>
  <verify>
    <automated>bash -euc 'mix test test/install_smoke/repository_residue_test.exs test/planning_path_hygiene_test.exs --seed 0; test -s .planning/milestones/v1.8-MILESTONE-AUDIT.md; git diff --exit-code -- .planning/milestones/v1.8-MILESTONE-AUDIT.md'</automated>
  </verify>
  <done>SIGNAL-04 is a repeatable repository contract rather than a one-time deletion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cleanup allowlist -> filesystem | A maintenance command can delete local files. |
| duplicate-looking artifact -> canonical archive | Similar filenames may contain unique historical evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-121-13 | Tampering | `scripts/gsd_cleanup.sh` | high | mitigate | Exact filenames, tracked-file refusal, non-recursive deletion, package-directory sentinel test, no globs. |
| T-121-14 | Repudiation | v1.8 audit evidence | high | mitigate | Assert canonical archive presence and key reconciled outcome before rejecting duplicates. |
| T-121-15 | Denial of Service | cleanup of working evidence | medium | mitigate | Preserve tracked files and all non-enumerated debug/maintainer paths. |
</threat_model>

## Artifacts this phase produces

- Expanded exact `TRANSIENT_FILES` cleanup allowlist.
- `Rindle.RepositoryResidueTest` in `test/install_smoke/repository_residue_test.exs` (shipped-tree only; no planning-path dependency).

<verification>
Run the focused ExUnit test, then `git status --short --ignored` and confirm no tracked file was deleted and `.planning/milestones/v1.8-MILESTONE-AUDIT.md` remains present.
</verification>

<success_criteria>
Known residue is absent/cleanable and cannot recur as tracked noise, while unique archive/debug/audit/maintainer evidence remains protected.
</success_criteria>

<output>
Create `.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-05-SUMMARY.md` when done.
</output>

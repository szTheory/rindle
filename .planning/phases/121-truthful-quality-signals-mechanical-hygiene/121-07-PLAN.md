---
phase: 121-truthful-quality-signals-mechanical-hygiene
plan: 07
type: execute
wave: 3
depends_on: [121-01, 121-02, 121-03, 121-04, 121-05, 121-06]
files_modified:
  - .github/workflows/ci.yml
  - mix.exs
  - RUNNING.md
  - test/install_smoke/quality_signal_policy_test.exs
autonomous: true
requirements: [SIGNAL-01, SIGNAL-02, SIGNAL-03, SIGNAL-04, SAFE-01]
must_haves:
  truths:
    - "CI Summary blocks deterministic Contract, static Doctor, actionable Credo, focused AV behavior, and SAFE-01 regressions through existing required jobs."
    - "Focused AV behavior runs after the existing FFmpeg/libvips installation with no failure masking; the DB/Oban-dependent public runtime doctor remains visibly advisory."
    - "Maintainers can reproduce every new gate locally from RUNNING.md and `mix ci` without changing release identity or full-verification invariants."
    - "No public API/schema/migration/telemetry/error-shape contract, dependency set, or release trigger changes."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Blocking truthful quality wiring under the existing CI/CI Summary topology"
    - path: "mix.exs"
      provides: "Local truthful-quality aliases composed into mix ci"
    - path: "RUNNING.md"
      provides: "Current severity/prerequisite/local reproduction contract"
    - path: "test/install_smoke/quality_signal_policy_test.exs"
      provides: "CI and local-command invariant lock"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/maintainer/refactor_contract.sh"
      via: "blocking Contract job step"
    - from: ".github/workflows/ci.yml"
      to: "test/rindle/probe/av_probe_test.exs, test/rindle/processor/image_test.exs"
      via: "blocking real FFmpeg/ffprobe and Vix/libvips behavior step ordered after the existing AV installs"
    - from: ".github/workflows/ci.yml"
      to: ".doctor.exs"
      via: "MIX_ENV=dev measured Doctor command with no continue-on-error"
    - from: ".github/workflows/ci.yml"
      to: "scripts/maintainer/credo_quality.sh"
      via: "fail-fast aggregate of the three separately scoped Credo policies"
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/eval_ci_summary.sh"
      via: "unchanged Contract/Quality job results already present in CI Summary.needs"
---

<objective>
Integrate the proven Phase 121 signals into the existing sole-required-check topology and local maintainer workflow.

Purpose: Individual green tools do not satisfy the goal until deterministic regressions actually block `CI Summary` and contributors can reproduce the same verdict before a PR.
Output: CI severity/order changes, local aliases/docs, and an invariant test proving the release-coupled topology was preserved.

Planner assumption for unclassified SIGNAL inputs: preserve the current jobs, tags, test ordering, and skip-as-pass aggregation. The already required `quality` and `contract` job results are the blocking carriers; no new required-check name or branch-protection change is needed.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-CONTEXT.md
@.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-01-SUMMARY.md
@.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-02-SUMMARY.md
@.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-03-SUMMARY.md
@.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-05-SUMMARY.md
@.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-06-SUMMARY.md
@.github/workflows/ci.yml
@mix.exs
@RUNNING.md
@test/install_smoke/ci_lane_split_test.exs
@test/install_smoke/release_guard_meta_test.exs
@test/rindle/probe/av_probe_test.exs
@test/rindle/processor/image_test.exs
@scripts/ci/eval_ci_summary.sh
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Wire truthful gates through Quality and Contract to CI Summary</name>
  <read_first>.github/workflows/ci.yml, scripts/maintainer/refactor_contract.sh, scripts/maintainer/credo_quality.sh, .doctor.exs, test/rindle/probe/av_probe_test.exs, test/rindle/processor/image_test.exs, test/install_smoke/ci_lane_split_test.exs, test/install_smoke/release_guard_meta_test.exs, scripts/ci/eval_ci_summary.sh</read_first>
  <files>.github/workflows/ci.yml, test/install_smoke/quality_signal_policy_test.exs</files>
  <behavior>
    - Contract-tagged tests and the SAFE-01 command fail the required Contract job when they fail.
    - The Credo aggregate and static dev Doctor fail the lint/home Quality cell; default full-tree style output remains an explicitly advisory visible step.
    - Existing FFmpeg/libvips installs precede focused real AV tests, which fail Quality on regressions; the public DB/Oban/profile runtime doctor remains an explicitly advisory visible step.
    - Workflow filename, line-1 `name: CI`, `CI Summary`, skip-as-pass evaluation, needs membership, and release full-verification coupling remain unchanged.
  </behavior>
  <action>In the existing jobs only, remove failure masking from the deterministic Contract suite and wire `scripts/maintainer/refactor_contract.sh` as the Contract job's preservation gate. In the lint/home Quality cell, run `bash scripts/maintainer/credo_quality.sh` and `MIX_ENV=dev mix doctor --full --raise` without failure masking; retain a separately named advisory `mix credo --strict --format oneline` step for full-tree style output. After the existing pinned FFmpeg/libvips installation steps, add a blocking focused command `mix test test/rindle/probe/av_probe_test.exs test/rindle/processor/image_test.exs --seed 0`; this is the prerequisite check because it exercises actual FFmpeg/ffprobe and Vix/libvips behavior, so do not require a `vips` CLI executable. Keep `mix rindle.doctor Rindle.Adopter.CanonicalApp.Profile Rindle.Adopter.CanonicalApp.VideoProfile` advisory because the Quality job does not prepare its DB, Oban, and profile-host readiness, and keep `mix deps.audit` advisory. Do not rename/move `ci.yml`, rename `name: CI`/`CI Summary`, alter `ci-summary.needs`, change skip-as-pass evaluation, weaken release gates, add permissions/secrets, install packages, or modify any `lib/`/schema/migration file. Add a structural ExUnit test over shipped workflow artifacts that parses stable step/job structure and proves blocking commands reach the existing Quality/Contract carriers, AV install-before-proof ordering, absence of failure masking on blocking steps, the advisory runtime-doctor disposition, and all release-coupling invariants. Do not assert direct equality among RUNNING prose, Mix alias source strings, and workflow text. Commit intent is `ci(121-07)`, not `feat:` or `fix:`.</action>
  <acceptance_criteria>The policy test proves deterministic Contract and focused AV failures reach existing `CI Summary.needs` carriers, the public runtime doctor remains visibly advisory for its unprepared host state, and every release-coupled invariant remains intact.</acceptance_criteria>
  <verify>
    <automated>mix test test/install_smoke/quality_signal_policy_test.exs test/install_smoke/ci_lane_split_test.exs test/install_smoke/release_guard_meta_test.exs --seed 0</automated>
  </verify>
  <done>Deterministic Contract, static Doctor, Credo, focused AV, and SAFE-01 regressions are blocking without misrepresenting public runtime-doctor readiness or changing the sole required-check/release topology.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish faithful local reproduction and run the full phase contract</name>
  <read_first>mix.exs, RUNNING.md, .github/workflows/ci.yml, scripts/maintainer/refactor_contract.sh, scripts/maintainer/repo_hygiene_check.sh</read_first>
  <files>mix.exs, RUNNING.md, test/install_smoke/quality_signal_policy_test.exs</files>
  <behavior>
    - `mix ci` runs the Credo aggregate, static dev Doctor, SAFE-01 command, format/compile/generation drift, and default tests with failure propagation.
    - RUNNING.md labels Contract/Credo/static Doctor/runtime Doctor/AV severities truthfully and documents runnable local commands plus the AV host preparation.
    - Push-main/full-verification-only lanes remain outside the PR-local alias and retain their documented release role.
  </behavior>
  <action>Add focused aliases if needed (`refactor_contract`, `credo_quality`, `quality_signals`) and compose the deterministic, environment-independent gates into `mix ci` in fail-fast order without adding a second default-suite execution. Use the same aggregate Credo script in both `mix ci` and CI. Document the focused AV test command separately for hosts where the existing RUNNING.md FFmpeg/libvips preparation has completed; do not add the DB/Oban-dependent public runtime doctor to the blocking local alias. Update RUNNING.md's severity table, static-analysis policy, local reproduction instructions, and AV host notes to describe the live commands and advisory runtime-doctor reason. Extend the policy test around durable behavior: carrier membership, command ordering, absence of masking, advisory-vs-blocking step classification, single default-suite execution, and unchanged release topology. Avoid brittle whole-string comparisons among prose, alias definitions, and CI YAML; prove documentation by executing each newly documented command in its stated prepared environment. Preserve package-consumer-full, adoption full E2E, Cohort, Nightly/Dialyzer, `CI Summary`, and release `gate-ci-green` invariants. Do not claim a package release or alter version/changelog/release-please files. Run fresh commands from RUNNING.md and finish with the repository hygiene check; use non-release-triggering `docs(121-07)` or `chore(121-07)` commit intent.</action>
  <acceptance_criteria>Fresh documented commands reproduce their stated blocking or advisory policy, the default suite runs once in `mix ci`, tests lock durable CI/local topology rather than prose serialization, and no release or public-contract artifact changes.</acceptance_criteria>
  <verify>
    <automated>mix ci</automated>
  </verify>
  <done>The phase ships with faithful local/CI quality feedback and green-main PR-first posture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository code -> GitHub Actions | Untrusted PR changes influence commands whose result gates merges. |
| job conclusions -> CI Summary | The sole required check aggregates blocking job results with skip-as-pass semantics. |
| CI conclusion -> release workflow | Release waits for the full `ci.yml` run on the exact release SHA. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-121-19 | Tampering | blocking step severity | high | mitigate | Structural tests reject failure masking and prove required carrier membership. |
| T-121-20 | Spoofing | AV readiness | high | mitigate | Existing installs plus focused real FFmpeg/ffprobe and Vix/libvips tests block; the unprepared DB/Oban-dependent runtime doctor stays advisory. |
| T-121-21 | Repudiation | CI Summary/release conclusion | high | mitigate | Preserve byte-stable names, needs, skip semantics, and exact-SHA release gate tests. |
| T-121-22 | Denial of Service | PR quality lane | medium | mitigate | Run version-invariant checks once on the home lint cell; no dependency upgrade or duplicate default suite. |
| T-121-23 | Information Disclosure | PR workflow | low | accept | No secrets, elevated permissions, external API calls, or artifact contents are added. |
</threat_model>

## Artifacts this phase produces

- Blocking Quality/Contract step wiring in `.github/workflows/ci.yml`.
- `Rindle.QualitySignalPolicyTest` in `test/install_smoke/quality_signal_policy_test.exs`.
- Local `refactor_contract` / `credo_quality` / `quality_signals` alias contract in `mix.exs` (names may be consolidated if the same behavior is clearer under one alias).
- Updated `RUNNING.md` CI severity and reproduction contract.

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Truthful blocking deterministic feedback plus explicit preservation contract | 01-07 | COVERED | SAFE command is built first; CI integration is last. |
| REQ | SIGNAL-01 | Blocking Contract/doc parity plus focused real AV behavior | 02, 07 | COVERED | Contract is deterministic and blocking; AV uses existing real processor/probe tests. |
| REQ | SIGNAL-02 | Measured Doctor coverage ratchet | 03, 07 | COVERED | Dev public surface, live report assertions. |
| REQ | SIGNAL-03 | Blocking actionable Credo; style advisory | 04, 06, 07 | COVERED | Warnings cleared, finite hotspot exclusions, fixture tests. |
| REQ | SIGNAL-04 | Proven residue removed/ignored; unique evidence preserved | 05 | COVERED | Canonical v1.8 evidence locked. |
| REQ | SAFE-01 | Public/schema/migration/telemetry/error/CI-release preservation | 01, 04, 07 | COVERED | One runnable command plus integration. |
| RESEARCH | — | Explicitly skipped maintenance research | — | EXCLUDED | Live 2026-08-22 audit evidence and installed source were used instead. |
| CONTEXT | discretion-1 | Smallest CI/test changes make deterministic failures blocking | 02, 07 | COVERED | Existing jobs carry the verdict. |
| CONTEXT | discretion-2 | Doctor/AV prerequisite-aware | 02, 03, 07 | COVERED | Static Doctor and focused AV proof block; DB/Oban-dependent runtime Doctor is advisory. |
| CONTEXT | discretion-3 | Credo actionable vs style policy | 04, 06, 07 | COVERED | Explicit profiles and tests. |
| CONTEXT | discretion-4 | Delete only proven residue; preserve unique evidence | 05 | COVERED | Tracked-safe exact allowlist. |
| CONTEXT | discretion-5 | SAFE-01 from existing behavioral contracts | 01 | COVERED | No incidental-source snapshot. |

<verification>
Run the focused policy suites, `bash scripts/maintainer/credo_quality.sh`, `MIX_ENV=dev mix doctor --full --raise`, `mix test --only contract --seed 0`, `bash scripts/maintainer/refactor_contract.sh`, `mix ci`, the documented focused AV command on the prepared CI/local host, and `./scripts/maintainer/repo_hygiene_check.sh`. Confirm the runtime public-doctor step is still advisory, while its command is documented for a DB/Oban/profile-ready host. Verify `git diff --name-only` contains no schema/migration/public API/telemetry/error implementation changes except the narrowly reviewed stacktrace-preserving rescue from Plan 04, and no version/changelog/release-please/dependency files.
</verification>

<success_criteria>
All five phase requirements are blocking or reproducibly enforced as specified; CI/release invariants remain unchanged; every later refactor can run SAFE-01; and fresh RUNNING.md checks pass from the milestone branch.
</success_criteria>

<output>
Create `.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-07-SUMMARY.md` when done.
</output>

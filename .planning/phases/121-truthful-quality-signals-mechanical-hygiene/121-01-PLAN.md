---
phase: 121-truthful-quality-signals-mechanical-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/maintainer/refactor_contract.sh
  - test/install_smoke/refactor_contract_test.exs
autonomous: true
requirements: [SAFE-01]
must_haves:
  truths:
    - "Maintainers have one runnable SAFE-01 command that fails when any preserved public API, schema/migration, telemetry, error, or CI/release contract fails."
    - "The preservation command is independent of .planning history and does not freeze incidental implementation text."
    - "The command cannot silently pass with an empty test selection, reordered failure masking, or concurrent child processes whose failures are lost."
  artifacts:
    - path: "scripts/maintainer/refactor_contract.sh"
      provides: "Fail-fast SAFE-01 regression entry point"
    - path: "test/install_smoke/refactor_contract_test.exs"
      provides: "Shipped-artifact membership and failure-propagation lock for the SAFE-01 entry point"
  key_links:
    - from: "scripts/maintainer/refactor_contract.sh"
      to: "test/rindle/api_surface_boundary_test.exs"
      via: "public function/signature contract invocation"
    - from: "scripts/maintainer/refactor_contract.sh"
      to: "test/rindle/contracts/telemetry_contract_test.exs"
      via: "public event/metadata contract invocation with the contract tag enabled"
    - from: "scripts/maintainer/refactor_contract.sh"
      to: "test/install_smoke/release_guard_meta_test.exs"
      via: "release full-verification and CI identity invariant invocation"
---

<objective>
Create the tracer for Phase 121: a single production-quality SAFE-01 command that exercises the existing behavior-preservation proof across every later-refactor boundary.

Purpose: Later Phases 122-126 must start from an explicit, runnable preservation contract instead of relying on memory or implementation-text snapshots.
Output: `scripts/maintainer/refactor_contract.sh` plus a structural regression test proving the command remains non-empty, complete, serial/fail-fast, and planning-independent.

Planner assumption for the specless SAFE-01 adjacency/empty/ordering/concurrency probes: preserve the existing ExUnit semantics and test inputs; resolve the probes at the command-composition boundary by using a non-empty explicit file list, one foreground `mix test` process, `set -euo pipefail`, and no background jobs. Do not invent new application inputs or output shapes.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-CONTEXT.md
@test/rindle/api_surface_boundary_test.exs
@test/rindle/schema_prefix_contract_test.exs
@test/rindle/migration_fast_test.exs
@test/rindle/contracts/telemetry_contract_test.exs
@test/rindle/error_test.exs
@test/rindle/error_streaming_freeze_test.exs
@test/install_smoke/ci_lane_split_test.exs
@test/install_smoke/release_guard_meta_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Wire one SAFE-01 command through every preserved contract domain</name>
  <read_first>test/rindle/api_surface_boundary_test.exs, test/rindle/schema_prefix_contract_test.exs, test/rindle/migration_fast_test.exs, test/rindle/contracts/telemetry_contract_test.exs, test/rindle/error_test.exs, test/rindle/error_streaming_freeze_test.exs, test/install_smoke/ci_lane_split_test.exs, test/install_smoke/release_guard_meta_test.exs, test/test_helper.exs</read_first>
  <files>scripts/maintainer/refactor_contract.sh</files>
  <behavior>
    - Running the script from any working directory resolves the repository root and executes the explicit SAFE-01 suites in one foreground ExUnit invocation.
    - The telemetry suite runs with its existing `:contract` tag enabled; default test-helper exclusions do not turn it into an empty selection.
    - Any selected test failure or command error returns a non-zero script exit; no test output is swallowed.
  </behavior>
  <action>Create an executable Bash entry point with `set -euo pipefail`, repository-root resolution, and an explicit `mix test` file list covering public signatures, schema prefix and fast migration behavior, telemetry event names/metadata, error atoms/shapes, CI topology, and release full-verification invariants. Use the current tests as the contract; do not add schema operations, application inputs, snapshot source text, `.planning` reads, background execution, or fallback success paths. Keep the invocation deterministic (`--seed 0` is acceptable) and ensure the existing contract tag is included. Commit intent is `test(121-01)`, never `feat:` or `fix:`.</action>
  <acceptance_criteria>The script is executable, names at least one existing test file for each of the five SAFE-01 domains, contains no `.planning/` reference, launches one foreground `mix test`, and propagates its exit status.</acceptance_criteria>
  <verify>
    <automated>bash scripts/maintainer/refactor_contract.sh</automated>
  </verify>
  <done>The selected public/signature, schema/migration, telemetry, error, and CI/release invariant suites all pass through one command.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock SAFE-01 membership and edge behavior as a shipped structural contract</name>
  <read_first>scripts/maintainer/refactor_contract.sh, test/planning_path_hygiene_test.exs, test/install_smoke/release_guard_meta_test.exs</read_first>
  <files>test/install_smoke/refactor_contract_test.exs</files>
  <behavior>
    - The test fails if any SAFE-01 domain loses all representative suites.
    - The test fails if the script becomes empty, reads `.planning`, backgrounds commands, masks failures, or stops enabling the existing contract-tagged telemetry suite.
    - The test accepts ordering changes that preserve the one-process, fail-fast contract; it does not freeze irrelevant whitespace or comments.
  </behavior>
  <action>Add an ExUnit structural-contract test over the shipped script. Parse meaningful command lines rather than asserting the full file byte-for-byte. Assert domain membership, root resolution, strict shell mode, one foreground test process, contract-tag enablement, and absence of failure-masking/background constructs. Treat SAFE-01 adjacency/empty/ordering/concurrency as command-structure concerns only; do not define new application edge-case semantics. Keep the test independent of planning artifacts.</action>
  <acceptance_criteria>The new test proves the command cannot devolve into an empty or partial green result and remains runnable after phase archives are filtered from a PR branch.</acceptance_criteria>
  <verify>
    <automated>mix test test/install_smoke/refactor_contract_test.exs --seed 0</automated>
  </verify>
  <done>SAFE-01 composition and failure propagation are regression-locked without freezing implementation prose.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer shell -> Mix/ExUnit | A local or CI caller relies on the script exit status as truthful evidence. |
| regression selector -> shipped contracts | A partial or empty selector could falsely certify behavior preservation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-121-01 | Tampering | `refactor_contract.sh` test selection | high | mitigate | Explicit domain membership test plus strict foreground failure propagation. |
| T-121-02 | Repudiation | SAFE-01 command output | medium | mitigate | Preserve native ExUnit output and non-zero status; do not redirect or mask failures. |
| T-121-03 | Information Disclosure | telemetry/error tests | low | accept | Existing tests use fixtures and redaction assertions; the script introduces no secrets or external calls. |
</threat_model>

## Artifacts this phase produces

- `scripts/maintainer/refactor_contract.sh` — executable SAFE-01 entry point.
- `Rindle.RefactorContractTest` in `test/install_smoke/refactor_contract_test.exs` — structural integrity lock.

<verification>
Run `mix test test/install_smoke/refactor_contract_test.exs --seed 0`, then `bash scripts/maintainer/refactor_contract.sh`. Both must exit 0 and the latter must report non-zero executed test counts across the selected files.
</verification>

<success_criteria>
Every later refactor has one explicit command that proves all five preservation domains and cannot silently turn into an empty, partial, or failure-masked pass.
</success_criteria>

<output>
Create `.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-01-SUMMARY.md` when done.
</output>

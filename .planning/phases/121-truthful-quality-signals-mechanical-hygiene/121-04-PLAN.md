---
phase: 121-truthful-quality-signals-mechanical-hygiene
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/migration/v1.ex
  - test/rindle/av/subprocess_epipe_test.exs
  - test/rindle/upload/tus_s3_integration_test.exs
autonomous: true
requirements: [SIGNAL-03, SAFE-01]
must_haves:
  truths:
    - "The current Credo warning category has no actionable baseline findings to mask."
    - "Warning cleanup preserves migration error shapes, subprocess safety intent, and tus integration behavior."
  artifacts:
    - path: "lib/rindle/migration/v1.ex"
      provides: "Stacktrace-preserving rescue behavior"
    - path: "test/rindle/av/subprocess_epipe_test.exs"
      provides: "Command-injection-safe port fixture"
    - path: "test/rindle/upload/tus_s3_integration_test.exs"
      provides: "Non-length-based empty-list assertions"
  key_links:
    - from: "lib/rindle/migration/v1.ex"
      to: "test/rindle/migration_fast_test.exs"
      via: "unchanged migration error envelope"
    - from: "test/rindle/av/subprocess_epipe_test.exs"
      to: "lib/rindle/av/subprocess.ex"
      via: "existing deterministic :epipe safety seam"
---

<objective>
Clear the finite current Credo warning baseline mechanically so warning-class findings can become unconditionally blocking.

Purpose: A curated ratchet cannot truthfully block new warnings while known actionable warnings are already ignored.
Output: three focused behavior-preserving warning fixes with their owning tests green.

Planner assumption for warning inputs: use the four live strict-Credo findings observed on 2026-08-22 (one `RaiseInsideRescue`, one `UnsafeExec`, two `ExpensiveEmptyEnumCheck`) and preserve all application return/error semantics.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-CONTEXT.md
@lib/rindle/migration/v1.ex
@test/rindle/migration_fast_test.exs
@test/rindle/av/subprocess_epipe_test.exs
@test/rindle/upload/tus_s3_integration_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Remove security and stacktrace warnings without contract drift</name>
  <read_first>test/rindle/av/subprocess_epipe_test.exs, lib/rindle/av/subprocess.ex, lib/rindle/migration/v1.ex, test/rindle/migration_fast_test.exs</read_first>
  <files>test/rindle/av/subprocess_epipe_test.exs, lib/rindle/migration/v1.ex</files>
  <behavior>
    - The EPIPE regression still drives the same deterministic child-port exit without a shell-interpolated command string.
    - Migration rescue re-raises with the original stacktrace while preserving the public error class/message and transactional behavior.
    - Credo reports neither `UnsafeExec` nor `RaiseInsideRescue` for these files.
  </behavior>
  <action>Convert the test-only Erlang port fixture from the shell-spawn form to `:spawn_executable` with an argv list while retaining its deterministic broken-pipe behavior. Replace the migration rescue `raise` with the stacktrace-preserving form supported by the existing rescued exception; do not change public exception type/message, DDL ordering, owned relations, transaction boundaries, or reversal semantics. Run the focused safety and migration suites before the broader SAFE-01 command. Commit intent is `test(121-04)` or `chore(121-04)`, not a release-triggering type.</action>
  <acceptance_criteria>The two Credo warnings disappear and the existing EPIPE and migration contract tests remain green with identical externally observable results.</acceptance_criteria>
  <verify>
    <automated>bash -euc 'mix test test/rindle/av/subprocess_epipe_test.exs test/rindle/migration_fast_test.exs --seed 0; mix credo --strict test/rindle/av/subprocess_epipe_test.exs lib/rindle/migration/v1.ex'</automated>
  </verify>
  <done>The security and stacktrace warning baseline is removed without SAFE-01 drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace expensive empty-list checks in tus proof</name>
  <read_first>test/rindle/upload/tus_s3_integration_test.exs</read_first>
  <files>test/rindle/upload/tus_s3_integration_test.exs</files>
  <behavior>
    - Both assertions retain their existing empty/non-empty truth condition.
    - No tus request, storage call, ordering, retry, or error assertion changes.
  </behavior>
  <action>Replace only the two Credo-identified `length/1` emptiness comparisons with direct list-shape/emptiness assertions. Keep fixtures and surrounding integration semantics byte-equivalent in meaning; do not perform broader style cleanup in the file.</action>
  <acceptance_criteria>`ExpensiveEmptyEnumCheck` is absent for the file and the tagged tus S3 suite compiles/runs when its existing MinIO preconditions are present.</acceptance_criteria>
  <verify>
    <automated>mix credo --strict test/rindle/upload/tus_s3_integration_test.exs</automated>
  </verify>
  <done>The final two warning-class baseline findings are removed mechanically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test command -> OS process | Port invocation must not reintroduce shell injection. |
| migration rescue -> host migrator | Exception identity and rollback evidence cross into host-owned migration handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-121-10 | Elevation of Privilege | EPIPE test port | high | mitigate | Use executable path plus argv, never shell interpolation; retain deterministic fixture assertions. |
| T-121-11 | Repudiation | migration exception stacktrace | medium | mitigate | Preserve original stacktrace with focused migration contract tests. |
| T-121-12 | Tampering | migration semantics | high | mitigate | Restrict edit to rescue form and run SAFE-01 migration tests; no DDL/catalog/order changes. |
</threat_model>

## Artifacts this phase produces

- Stacktrace-preserving migration rescue in `Rindle.Migration.V1`.
- Shell-free deterministic EPIPE fixture.
- Linear-time tus empty-list assertions.

<verification>
Run the task commands, then `mix credo --strict --format oneline | grep '^\[W\]'` must produce no output while the command's actual exit status remains visible (do not convert grep absence into the Credo verdict).
</verification>

<success_criteria>
All live warning-category findings are resolved and SAFE-01 behavior remains unchanged.
</success_criteria>

<output>
Create `.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-04-SUMMARY.md` when done.
</output>

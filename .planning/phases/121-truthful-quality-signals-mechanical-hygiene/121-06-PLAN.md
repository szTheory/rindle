---
phase: 121-truthful-quality-signals-mechanical-hygiene
plan: 06
type: execute
wave: 2
depends_on: [121-03, 121-04]
files_modified:
  - .credo.exs
  - scripts/maintainer/credo_quality.sh
  - scripts/maintainer/credo_complexity_baseline.json
  - test/install_smoke/credo_policy_test.exs
autonomous: true
requirements: [SIGNAL-03]
must_haves:
  truths:
    - "One fail-fast aggregate command blocks any warning across the full source tree, public docs/spec drift, and complexity/nesting findings beyond the reviewed stable-identity multiset."
    - "Credo 1.7 limitations are respected: warnings, public docs/specs, and complexity/nesting run as separate named configurations/commands."
    - "Every accepted complexity/nesting finding is individually inventoried with an owner/removal trigger; no namespace, glob, or per-check exclusion pretends to enforce what Credo cannot express."
    - "Low-priority style suggestions remain visible through the default advisory profile and do not decide the aggregate exit."
  artifacts:
    - path: ".credo.exs"
      provides: "Separate `blocking_warnings`, `public_contract`, and `complexity_inventory` Credo configurations"
    - path: "scripts/maintainer/credo_quality.sh"
      provides: "Fail-fast aggregate Credo quality entry point"
    - path: "scripts/maintainer/credo_complexity_baseline.json"
      provides: "Finite complexity/nesting identity multiset with occurrence counts, ownership, and removal triggers"
    - path: "test/install_smoke/credo_policy_test.exs"
      provides: "Aggregate behavior, full-tree warning scope, and manifest regression proof"
  key_links:
    - from: "scripts/maintainer/credo_quality.sh"
      to: ".credo.exs"
      via: "three separate named Credo invocations"
    - from: "scripts/maintainer/credo_quality.sh"
      to: "scripts/maintainer/credo_complexity_baseline.json"
      via: "normalized `{check,file,trigger,observed_metric}` multiset comparison that ignores locations and prose"
    - from: "test/install_smoke/credo_policy_test.exs"
      to: "scripts/maintainer/credo_quality.sh"
      via: "fixture-driven aggregate pass/fail execution"
---

<objective>
Implement a Credo 1.7-compatible aggregate quality ratchet: full-tree warnings, curated public docs/specs, and stable-identity complexity/nesting debt.

Purpose: SIGNAL-03 needs one truthful blocking command, but Credo 1.7 cannot express per-check file exclusions inside one named configuration.
Output: three scoped configs, one aggregate script, one stable debt manifest, and behavior-focused policy tests.

Planner assumption: all Warning findings are actionable across `lib/` and `test/`; Plan 04 removes the current four-warning baseline. Complexity/nesting debt is accepted only as the stable normalized multiset derived from the exact 2026-08-22 inventory below. Items mapped to Phases 123-125 reduce or remove their manifest occurrence counts when those phases make the named finding green. Other entries are explicitly unassigned and require a developer-approved scoped phase; this plan does not claim ROADMAP ownership for them.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-CONTEXT.md
@.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-03-SUMMARY.md
@.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-04-SUMMARY.md
@.credo.exs
@mix.exs
@deps/credo/lib/credo/config_file.ex
@deps/credo/lib/credo/check/readability/specs.ex
@deps/credo/lib/credo/check/readability/module_doc.ex
@deps/credo/lib/credo/check/refactor/cyclomatic_complexity.ex
@deps/credo/lib/credo/check/refactor/nesting.ex
</context>

## Exact 2026-08-22 complexity/nesting inventory

The table records all 37 live findings; `File:line` is diagnostic inventory evidence only and never participates in gating. The canonical manifest collapses this inventory into the stable normalized multiset identity `{check, file, trigger, observed_metric}` with an explicit `count`, `owner`, and `removal_trigger`. It contains 33 identity entries representing 37 occurrences: duplicate identities are GCS `with`/3 (count 2), runtime-checks `fn`/3 (count 2), AV-video `if`/3 (count 2), and tus `case`/3 (count 2); every other entry has count 1. Do not store or compare `line` or the full Credo `message` in the canonical manifest, and use no file or namespace exclusions. Extract `observed_metric` from the current diagnostic's numeric value (`cyclomatic complexity is N` or nesting `was N`) while treating all other diagnostic wording as non-gating.

| File:line | Check / trigger | Owner and removal trigger |
|---|---|---|
| `lib/rindle/admin/live/actions_live.ex:269` | Nesting / `case` (3) | Unassigned Admin-maintenance scope; remove when a developer-approved phase makes this finding green. |
| `lib/rindle/delivery.ex:244` | Cyclomatic / `dispatch_streaming` (10) | Unassigned delivery scope; remove when a developer-approved delivery phase brings it to <=9. |
| `lib/rindle/live_view.ex:317` | Nesting / `case` (3) | Unassigned LiveView scope; remove when a developer-approved LiveView phase makes it green. |
| `lib/rindle/migration/v1.ex:123` | Cyclomatic / `preflight_public_to_rindle` (25) | Phase 123 OPS-02; remove when the named function is <=9. |
| `lib/rindle/migration/v1.ex:170` | Cyclomatic / `preflight_rindle_to_public` (15) | Phase 123 OPS-02; remove when the named function is <=9. |
| `lib/rindle/migration/v1.ex:627` | Cyclomatic / `raise_preflight_error!` (15) | Phase 123 OPS-02; remove when the named function is <=9. |
| `lib/rindle/ops/lifecycle_repair.ex:82` | Nesting / `fn` (3) | Phase 123 OPS-01; remove when this finding is absent. |
| `lib/rindle/ops/ownership_snapshot.ex:225` | Cyclomatic / `classify_rindle` (10) | Phase 123 OPS-01; remove when the named function is <=9. |
| `lib/rindle/ops/runtime_checks.ex:687` | Nesting / `fn` (3) | Phase 123 OPS-01; remove when this finding is absent. |
| `lib/rindle/ops/runtime_checks.ex:711` | Nesting / `fn` (3) | Phase 123 OPS-01; remove when this finding is absent. |
| `lib/rindle/ops/runtime_checks.ex:806` | Nesting / `with` (3) | Phase 123 OPS-01; remove when this finding is absent. |
| `lib/rindle/ops/runtime_checks.ex:1167` | Cyclomatic / `run_smoke_ping_with_timeout` (12) | Phase 123 OPS-01; remove when the named function is <=9. |
| `lib/rindle/ops/runtime_checks.ex:1343` | Cyclomatic / `check_gcs_bucket_reachable` (12) | Phase 123 OPS-01; remove when the named function is <=9. |
| `lib/rindle/ops/runtime_checks.ex:1374` | Nesting / `if` (4) | Phase 123 OPS-01; remove when this finding is absent. |
| `lib/rindle/ops/runtime_checks.ex:1652` | Cyclomatic / `fetch_gcs_bucket_cors` (10) | Phase 123 OPS-01; remove when the named function is <=9. |
| `lib/rindle/ops/runtime_status.ex:474` | Cyclomatic / `classify_variant` (12) | Phase 123 OPS-03; remove when the named function is <=9. |
| `lib/rindle/ops/sweep_orphaned_temp_files.ex:103` | Nesting / `if` (3) | Phase 123 OPS-01; remove when this finding is absent. |
| `lib/rindle/ops/upload_maintenance.ex:243` | Nesting / `case` (3) | Phase 123 OPS-01; remove when this finding is absent. |
| `lib/rindle/processor/av/video.ex:59` | Nesting / `if` (3) | Unassigned AV-processor scope; remove when a developer-approved AV phase makes it green. |
| `lib/rindle/processor/av/video.ex:87` | Nesting / `if` (3) | Unassigned AV-processor scope; remove when a developer-approved AV phase makes it green. |
| `lib/rindle/processor/ffmpeg.ex:21` | Nesting / `case` (3) | Unassigned AV-processor scope; remove when a developer-approved AV phase makes it green. |
| `lib/rindle/storage/gcs/client.ex:130` | Nesting / `if` (4) | Unassigned storage-adapter scope; remove when a developer-approved storage phase makes it green. |
| `lib/rindle/storage/gcs/client.ex:250` | Nesting / `with` (3) | Unassigned storage-adapter scope; remove when a developer-approved storage phase makes it green. |
| `lib/rindle/storage/gcs/client.ex:304` | Nesting / `with` (3) | Unassigned storage-adapter scope; remove when a developer-approved storage phase makes it green. |
| `lib/rindle/storage/s3.ex:282` | Nesting / `fn` (4) | Unassigned storage-adapter scope; remove when a developer-approved storage phase makes it green. |
| `lib/rindle/streaming.ex:103` | Nesting / `case` (4) | Unassigned streaming scope; remove when a developer-approved streaming phase makes it green. |
| `lib/rindle/streaming/provider/mux.ex:192` | Cyclomatic / `create_asset_with_retry_hint` (10) | Unassigned streaming-provider scope; remove when a developer-approved streaming phase brings it to <=9. |
| `lib/rindle/streaming/provider/mux.ex:378` | Nesting / `with` (4) | Unassigned streaming-provider scope; remove when a developer-approved streaming phase makes it green. |
| `lib/rindle/upload/broker.ex:248` | Cyclomatic / `concatenate_tus_sessions` (11) | Phase 124 UPLOAD-02; remove when the named function is <=9. |
| `lib/rindle/upload/broker.ex:287` | Nesting / `case` (3) | Phase 124 UPLOAD-02; remove when this finding is absent. |
| `lib/rindle/upload/tus_plug.ex:427` | Cyclomatic / `stream_append` (12) | Phase 124 UPLOAD-01; remove when the named function is <=9. |
| `lib/rindle/upload/tus_plug.ex:769` | Nesting / `case` (4) | Phase 124 UPLOAD-01; remove when this finding is absent. |
| `lib/rindle/upload/tus_plug.ex:804` | Nesting / `case` (3) | Phase 124 UPLOAD-01; remove when this finding is absent. |
| `lib/rindle/upload/tus_plug.ex:924` | Nesting / `case` (3) | Phase 124 UPLOAD-01; remove when this finding is absent. |
| `lib/rindle/workers/ingest_provider_webhook.ex:268` | Cyclomatic / `dispatch` (10) | Unassigned provider-worker scope; remove when a developer-approved worker phase brings it to <=9. |
| `lib/rindle/workers/mux_sync_provider_asset.ex:209` | Nesting / `case` (3) | Unassigned provider-worker scope; remove when a developer-approved worker phase makes it green. |
| `test/install_smoke/support/generated_app_helper.ex:344` | Cyclomatic / `prove_package_install!` (20) | Phase 125 TEST-01; remove when the named helper is <=9. |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Build the multi-profile Credo aggregate and exact debt manifest</name>
  <read_first>.credo.exs, deps/credo/lib/credo/config_file.ex, deps/credo/lib/credo/check/readability/specs.ex, deps/credo/lib/credo/check/readability/module_doc.ex, deps/credo/lib/credo/check/refactor/cyclomatic_complexity.ex, deps/credo/lib/credo/check/refactor/nesting.ex, mix.exs</read_first>
  <files>.credo.exs, scripts/maintainer/credo_quality.sh, scripts/maintainer/credo_complexity_baseline.json</files>
  <behavior>
    - `blocking_warnings` scans the full `lib/` and `test/` tree and fails on any Warning check.
    - `public_contract` scans the explicit public library files derived from ExDoc/public processor contracts and fails on ModuleDoc or public Specs drift.
    - `complexity_inventory` reports CyclomaticComplexity(max 9) and Nesting(max 2) for the full source tree; the aggregate compares the normalized identity multiset and fails on added/deleted/changed identities, occurrence-count drift, malformed output, tool errors, or stale entries while permitting line-only movement and non-metric diagnostic wording changes.
    - The default config continues to surface low-priority style suggestions without controlling aggregate success.
  </behavior>
  <action>Define three separate Credo 1.7 named configs rather than attempting per-check exclusions in one config. Create an executable fail-fast aggregate Bash script that runs full-tree warnings first, curated public docs/specs second, then captures JSON from the full-tree complexity/nesting config. Normalize each issue to `{check,file,trigger,observed_metric}`, group identical identities into occurrence counts, and compare that sorted multiset with the checked-in 33-entry manifest representing the 37 observed occurrences. Parse only the numeric observed metric from the diagnostic; do not include or compare `line_no`, columns, scope, or the full message. Use the repository's existing `jq` tooling and fail explicitly if `jq` is unavailable, Credo emits invalid JSON, the metric cannot be extracted, or identity/count differs in either direction. Include owner/removal metadata without making it part of analyzer identity. Do not use file exclusions, namespaces, globs, issue-count-only comparisons, root `credo.json`/`credo.txt`, or dependency changes. Commit intent is `chore(121-06)`.</action>
  <acceptance_criteria>`bash scripts/maintainer/credo_quality.sh` exits 0 on the reviewed tree, scans all warnings globally, rejects additions/deletions/identity or count changes, and continues to pass when only source lines or non-metric diagnostic wording change.</acceptance_criteria>
  <verify>
    <automated>bash scripts/maintainer/credo_quality.sh</automated>
  </verify>
  <done>The repository has one truthful Credo aggregate implemented through three supported named configs and a stable finite identity multiset.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove aggregate bite, restraint, and baseline ownership</name>
  <read_first>.credo.exs, scripts/maintainer/credo_quality.sh, scripts/maintainer/credo_complexity_baseline.json, test/install_smoke/ci_cache_hygiene_test.exs</read_first>
  <files>test/install_smoke/credo_policy_test.exs</files>
  <behavior>
    - Fixture injection proves a warning anywhere under lib/test fails before later profiles.
    - Public missing module docs/specs fail; style-only alias/order findings remain advisory.
    - Added/deleted/changed complexity or nesting identities and occurrence-count drift fail, while line-only movement and non-metric message changes pass.
    - Every manifest entry has a non-empty check/file/trigger/observed_metric/count/owner/removal trigger; 33 entries and their counts map exactly to all 37 inventoried occurrences without line/message gates.
  </behavior>
  <action>Add behavior-focused ExUnit tests around isolated fixture repositories/configs and the aggregate script. Test durable classification and exit behavior rather than full console strings. Assert the warning config includes both `lib/` and `test/`, the public config is explicit, the complexity config has no exclusions, and the manifest maps its 33 counted identities to the exact 37-finding inventory/owners. Prove the aggregate fails on an extra identity, a deleted identity, a changed trigger or observed metric, and count drift; prove it passes after changing only line/column values and non-metric message prose. Assert canonical entries omit line/message fields. No planning-path reads or network/database prerequisites.</action>
  <acceptance_criteria>The focused test proves the aggregate blocks actionable identity/count drift, tolerates location and diagnostic-prose churn, keeps style advisory, and requires explicit counted-baseline removal as debt is retired.</acceptance_criteria>
  <verify>
    <automated>mix test test/install_smoke/credo_policy_test.exs --seed 0</automated>
  </verify>
  <done>The Credo policy is executable, finite, and truthful about assigned and unassigned debt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source tree -> three Credo profiles | Maintainers trust aggregate exit status across differently scoped checks. |
| Credo JSON -> baseline comparator | Malformed or partial analyzer output must never become a pass. |
| accepted debt -> future phases | Baseline rows must not hide new findings or claim nonexistent roadmap ownership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-121-16 | Tampering | complexity manifest | high | mitigate | Stable identity/count multiset equality, owner/removal metadata, no globs/exclusions, additions/deletions/metric or trigger drift fail while line/message-only churn is ignored. |
| T-121-17 | Repudiation | aggregate exit status | high | mitigate | Fail-fast warning/docs commands, validated JSON, fixture-driven positive/negative controls. |
| T-121-18 | Information Disclosure | Credo reports | low | accept | Reports contain source locations/messages only; temporary output is removed and no root report artifact is committed. |
</threat_model>

## Artifacts this phase produces

- Credo named configs `blocking_warnings`, `public_contract`, and `complexity_inventory` in `.credo.exs`.
- `scripts/maintainer/credo_quality.sh` aggregate command.
- `scripts/maintainer/credo_complexity_baseline.json` with 33 stable identities representing 37 counted occurrences plus ownership/removal metadata.
- `Rindle.CredoPolicyTest` in `test/install_smoke/credo_policy_test.exs`.

<verification>
Run `mix test test/install_smoke/credo_policy_test.exs --seed 0`, then `bash scripts/maintainer/credo_quality.sh`, then the default advisory `mix credo --strict --format oneline` to confirm style remains visible.
</verification>

<success_criteria>
Warnings across the full tree and public docs/spec drift block immediately; complexity/nesting can only remain at the reviewed stable identity/count baseline; line and diagnostic-prose churn does not create false failures; style remains advisory.
</success_criteria>

<output>
Create `.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-06-SUMMARY.md` when done.
</output>

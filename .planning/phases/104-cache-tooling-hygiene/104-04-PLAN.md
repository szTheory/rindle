---
phase: 104-cache-tooling-hygiene
plan: 04
type: execute
wave: 4
depends_on: ["104-03"]
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
autonomous: true
requirements: [CACHE-01, CACHE-05]
must_haves:
  truths:
    - "The 5 ci.yml MinIO-trio jobs (integration, package-consumer, adoption-demo-e2e, adopter, mux-soak) use `./.github/actions/setup-minio` instead of the inline docker-run/mc/bucket trio (CACHE-01, D-02, D-03)."
    - "The 2 release.yml MinIO-trio jobs (publish, public_verify) also use `./.github/actions/setup-minio` — single source of truth (CACHE-01, D-03)."
    - "adoption-demo-e2e passes the CORS-allow-origin input so its `-e MINIO_API_CORS_ALLOW_ORIGIN='*'` behavior is preserved (D-02)."
    - "release.yml's `FedericoCarboni/setup-ffmpeg@v3` is replaced by `run: bash scripts/ci/install_ffmpeg.sh` in both jobs that use it (CACHE-05, D-14)."
    - "No MinIO-related job env (RINDLE_MINIO_*) or label/secret if-gate moved into the composite — those stay at job level (D-02)."
    - "All job NAMEs, name:CI (ci.yml), name:Release (release.yml), and both filenames are byte-identical (D-04, D-15)."
  prohibitions:
    - statement: "This phase preserves the single-workflow shape — it adds NO reusable workflows, NO `CI Summary` aggregate (Phase 105), NO lane/trigger split or concurrency groups (Phase 106), and NO async-partition/SHA-pin/`mix ci` (Phase 107) (D-16)."
      status: held
      verification: "grep -c 'workflow_call:' .github/workflows/ci.yml == 0 AND grep -c 'workflow_call:' .github/workflows/release.yml == 0; no new top-level job named 'CI Summary'/aggregate added to either workflow; no `concurrency:` block added to ci.yml or release.yml; the ffmpeg swap and MinIO adoption introduce no third-party `uses:` SHA-pin (release.yml moves FROM a third-party action TO an in-repo `run:`/`uses: ./…`); no `mix ci` alias introduced this phase."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "MinIO trio composited across the 5 trio jobs"
      contains: "uses: ./.github/actions/setup-minio"
    - path: ".github/workflows/release.yml"
      provides: "MinIO trio composited in publish + public_verify; ffmpeg action swapped for install_ffmpeg.sh"
      contains: "scripts/ci/install_ffmpeg.sh"
  key_links:
    - from: ".github/workflows/release.yml (public_verify / publish)"
      to: ".github/actions/setup-minio"
      via: "uses: ./.github/actions/setup-minio replacing the inline docker-run/mc/bucket trio (D-03)"
      pattern: "uses: ./.github/actions/setup-minio"
    - from: ".github/workflows/release.yml"
      to: "scripts/ci/install_ffmpeg.sh"
      via: "run: bash scripts/ci/install_ffmpeg.sh replacing FedericoCarboni/setup-ffmpeg@v3 (D-14)"
      pattern: "scripts/ci/install_ffmpeg.sh"
---

<objective>
Final adoption wave: replace the duplicated MinIO bring-up trio with the `setup-minio` composite across the 5 ci.yml trio jobs and the 2 release.yml trio jobs, and retire the last stray `FedericoCarboni/setup-ffmpeg@v3` instances in release.yml by aligning onto the repo's canonical `scripts/ci/install_ffmpeg.sh` (D-14). Runs last because it also touches `ci.yml` (after Plans 02-03) and is the only plan touching `release.yml`.

Purpose: CACHE-01's "single source of truth" for MinIO setup across both workflows; CACHE-05's ffmpeg alignment that retires the documented intermittent-failure action.

Output: modified `.github/workflows/ci.yml` (5 jobs) and `.github/workflows/release.yml` (2 MinIO jobs + 2 ffmpeg swaps).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-cache-tooling-hygiene/104-CONTEXT.md
@.planning/phases/104-cache-tooling-hygiene/104-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adopt setup-minio across the 5 ci.yml trio jobs (CACHE-01, D-02, D-03)</name>
  <read_first>
    - .planning/phases/104-cache-tooling-hygiene/104-01-SUMMARY.md — the setup-minio composite name + the CORS-origin input name.
    - .github/workflows/ci.yml — the MinIO trios to replace, per job: integration "Start MinIO"/"Install MinIO client"/"Create MinIO bucket" (L328-348); package-consumer (L647-667, note the descriptive step names); adoption-demo-e2e (L890-910, note the `-e MINIO_API_CORS_ALLOW_ORIGIN='*'` on the docker run at L892); adopter (L1001-1021); mux-soak (L1098-1118).
    - .planning/phases/104-cache-tooling-hygiene/104-CONTEXT.md — D-02 (job env RINDLE_MINIO_* + secret/label if-gates stay at job level), D-03 (MinIO trio is migration step 4).
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
    In each of integration, package-consumer, adoption-demo-e2e, adopter, mux-soak: replace the three MinIO steps (docker run minio/minio + health wait, install mc client, create bucket) with a single `uses: ./.github/actions/setup-minio` step at the same position in the step sequence. For adoption-demo-e2e, pass the CORS-origin input (`*`) so the `-e MINIO_API_CORS_ALLOW_ORIGIN='*'` behavior is preserved (D-02); the other four pass no CORS input (default off).

    Leave the job-level `env:` (RINDLE_MINIO_URL/ACCESS_KEY/SECRET_KEY/BUCKET/REGION) and any `if:` label/secret gate (e.g. mux-soak's `streaming` label gate) at job level — do not move them into the composite (D-02). Do not reorder unrelated steps; the composite occupies the slot the trio occupied.

    Preserve the descriptive step names where they carry meaning (package-consumer's "Start MinIO for S3-compatible package-consumer proofs" etc.) is acceptable to collapse into the single composite step name — these are step names, NOT job names, so they are not required-checks. Do NOT change any job `name:`.
  </action>
  <acceptance_criteria>
    - Each of the 5 jobs contains a `uses: ./.github/actions/setup-minio` step and no longer inlines the docker-run/mc/bucket trio.
    - adoption-demo-e2e passes the CORS-origin input (`*`); the other four do not.
    - No RINDLE_MINIO_* env or label/secret if-gate was moved into the composite (D-02).
    - Job NAMEs (Integration, Package Consumer Proof Matrix + Release Preflight, Adoption Demo E2E, Adopter, Mux Soak (real API)), name:CI, and filename are byte-identical.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); print('yaml ok')" &amp;&amp; test "$(grep -c 'uses: ./.github/actions/setup-minio' .github/workflows/ci.yml)" -ge "5" &amp;&amp; head -1 .github/workflows/ci.yml | grep -qx "name: CI"</automated>
  </verify>
  <done>The 5 ci.yml trio jobs use setup-minio; CORS preserved for adoption-demo-e2e; job env/if-gates stay at job level; required-check NAMEs unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Adopt setup-minio + swap ffmpeg in release.yml (CACHE-01, CACHE-05, D-03, D-14)</name>
  <read_first>
    - .planning/phases/104-cache-tooling-hygiene/104-01-SUMMARY.md — the setup-minio composite name.
    - .github/workflows/release.yml — the MinIO trios: publish "Start MinIO"/"Install MinIO client"/"Create MinIO bucket" (L339-359); public_verify (L472-492). The two `FedericoCarboni/setup-ffmpeg@v3` "Set up FFmpeg" steps: public_verify (L456-459) and any other (grep confirms public_verify L457; also confirm none remain elsewhere). Job names: `Publish to Hex` (L215), `Public Verify` (L412) — these are release-train jobs, NOT branch-protection required-checks, but keep their names stable for clarity.
    - scripts/ci/install_ffmpeg.sh — canonical BtbN static ffmpeg >= 6 path (used 4x in ci.yml).
    - .planning/phases/104-cache-tooling-hygiene/104-CONTEXT.md — D-14 (replace setup-ffmpeg with install_ffmpeg.sh; public_verify already runs repo scripts so the no-repo-scripts argument doesn't hold), D-03 (release.yml MinIO trios in scope for single-source-of-truth), D-15 (never change name:Release filename or the release-train coupling).
  </read_first>
  <files>.github/workflows/release.yml</files>
  <action>
    MinIO (D-03): in publish and public_verify, replace the three inline MinIO steps with a single `uses: ./.github/actions/setup-minio` step (no CORS input — neither release job sets the CORS origin today). Leave the job-level RINDLE_MINIO_* env and the `services: postgres` blocks untouched (D-02).

    ffmpeg (D-14): replace every `uses: FedericoCarboni/setup-ffmpeg@v3` "Set up FFmpeg" step (with its `with: ffmpeg-version: release`) with a step `run: bash scripts/ci/install_ffmpeg.sh` — the canonical repo path. Confirm via grep that zero `FedericoCarboni/setup-ffmpeg` references remain in release.yml after the swap (this retires the last stray instance repo-wide for this action's intermittent-failure history).

    Do NOT change `name: Release` (release.yml top), the filename, any `gate-ci-green` / release-train job, or the publish/public_verify `needs:`/`if:` gates (D-15). This must not weaken the release full-verification gate.
  </action>
  <acceptance_criteria>
    - publish and public_verify each use `./.github/actions/setup-minio` and no longer inline the docker-run/mc/bucket trio.
    - Zero `FedericoCarboni/setup-ffmpeg` references remain in release.yml; both former ffmpeg steps now `run: bash scripts/ci/install_ffmpeg.sh`.
    - `name: Release`, the release-train jobs (gate-ci-green etc.), and the filename are unchanged.
    - No RINDLE_MINIO_* env or services block moved into the composite.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml')); print('yaml ok')" &amp;&amp; test "$(grep -c 'uses: ./.github/actions/setup-minio' .github/workflows/release.yml)" -ge "2" &amp;&amp; grep -q 'scripts/ci/install_ffmpeg.sh' .github/workflows/release.yml &amp;&amp; test "$(grep -c 'FedericoCarboni/setup-ffmpeg' .github/workflows/release.yml)" = "0" &amp;&amp; grep -q 'name: Release' .github/workflows/release.yml</automated>
  </verify>
  <done>release.yml's publish/public_verify use setup-minio; the stray setup-ffmpeg action is fully retired in favor of install_ffmpeg.sh; name:Release + release-train coupling untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release.yml → release-train gate | gate-ci-green/automerge pin ci.yml filename + name:CI; release jobs must not weaken the full-verification gate |
| third-party ffmpeg action → runner | FedericoCarboni/setup-ffmpeg downloads a remote binary with documented intermittent failures |
| MinIO bring-up → job env | RINDLE_MINIO_* creds + label/secret if-gates must remain at job level |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-10 | Denial of Service | FedericoCarboni/setup-ffmpeg@v3 (intermittent download failures blocking merges) | mitigate | replaced by the repo-canonical `scripts/ci/install_ffmpeg.sh` (BtbN static, ffmpeg>=6) in both release.yml jobs (D-14); grep-assert zero remaining references |
| T-104-11 | Tampering | release-train coupling (name:Release/CI, gate-ci-green) | mitigate | only step internals change; name:Release, filename, release-train jobs, publish/public_verify needs/if untouched (D-15); full-verification gate not weakened |
| T-104-12 | Information Disclosure | MinIO creds / label gates moved into composite | mitigate | composite carries no RINDLE_MINIO_* env and no if-gate; job env + mux-soak streaming-label gate stay at job level (D-02) |
</threat_model>

<verification>
- Both ci.yml and release.yml parse as valid YAML.
- 5 ci.yml jobs + 2 release.yml jobs use setup-minio; CORS preserved for adoption-demo-e2e; job env/if-gates stay at job level (CACHE-01, D-02, D-03).
- Zero FedericoCarboni/setup-ffmpeg references remain in release.yml; both swapped to install_ffmpeg.sh (CACHE-05, D-14).
- All job NAMEs, name:CI, name:Release, both filenames, and the release-train coupling are byte-identical (D-04, D-15).
</verification>

<success_criteria>
MinIO setup is a single source of truth across both workflows; the documented intermittent-failure ffmpeg action is fully retired. No required-check NAME, workflow filename, or release-train gate changed.
</success_criteria>

<output>
Create `.planning/phases/104-cache-tooling-hygiene/104-04-SUMMARY.md` when done. Confirm the FedericoCarboni/setup-ffmpeg count is 0 repo-wide in release.yml and record the setup-minio adoption count across both files.
</output>

---
phase: 04-day-2-operations
plan: "03"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - lib/rindle/ops/metadata_backfill.ex
  - lib/mix/tasks/rindle.backfill_metadata.ex
  - lib/rindle/workers/cleanup_orphans.ex
  - lib/rindle/workers/abort_incomplete_uploads.ex
  - test/rindle/ops/metadata_backfill_test.exs
  - test/rindle/workers/maintenance_workers_test.exs
autonomous: true
requirements:
  - OPS-08
  - OPS-09
must_haves:
  truths:
    - `mix rindle.backfill_metadata` reruns the analyzer for existing assets and persists updated metadata.
    - The backfill command reports failures and exits non-zero when analysis or persistence fails.
    - Cron-capable workers exist for orphan cleanup and incomplete-upload abortion so hosts can schedule them.
    - Worker entrypoints reuse the same maintenance logic as the Mix commands rather than duplicating behavior.
  artifacts:
    - path: lib/rindle/ops/metadata_backfill.ex
      provides: Shared metadata backfill operations
      min_lines: 120
      exports: ["backfill_metadata/1"]
    - path: lib/mix/tasks/rindle.backfill_metadata.ex
      provides: Metadata backfill CLI
      min_lines: 90
      contains: "backfill_metadata"
    - path: lib/rindle/workers/cleanup_orphans.ex
      provides: Cron-capable orphan cleanup worker
      min_lines: 80
      contains: "CleanupOrphans"
    - path: lib/rindle/workers/abort_incomplete_uploads.ex
      provides: Cron-capable upload-expiry worker
      min_lines: 80
      contains: "AbortIncompleteUploads"
    - path: test/rindle/ops/metadata_backfill_test.exs
      provides: Metadata backfill coverage
      min_lines: 120
      contains: "backfill_metadata"
    - path: test/rindle/workers/maintenance_workers_test.exs
      provides: Cron worker delegation coverage
      min_lines: 120
      contains: "CleanupOrphans"
  key_links:
    - from: lib/mix/tasks/rindle.backfill_metadata.ex
      to: lib/rindle/ops/metadata_backfill.ex
      via: analyzer backfill delegation
      pattern: "MetadataBackfill"
    - from: lib/rindle/workers/cleanup_orphans.ex
      to: lib/rindle/ops/upload_maintenance.ex
      via: shared cleanup service
      pattern: "UploadMaintenance"
    - from: lib/rindle/workers/abort_incomplete_uploads.ex
      to: lib/rindle/ops/upload_maintenance.ex
      via: shared expiry service
      pattern: "UploadMaintenance"
---

<objective>
Add the remaining recovery lane: backfill metadata for existing assets and provide cron-capable maintenance workers for scheduled cleanup.

Purpose: Close the last day-2 operations gap with reusable, host-schedulable worker modules.
Output: Metadata backfill service and CLI, plus cron worker modules that reuse shared maintenance logic.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
From lib/rindle/analyzer.ex:
```elixir
@callback analyze(source :: Path.t()) :: {:ok, map()} | {:error, term()}
```

From lib/rindle/domain/media_asset.ex:
```elixir
field :storage_key, :string
field :metadata, :map, default: %{}
field :profile, :string
```

From lib/rindle/domain/media_processing_run.ex:
```elixir
field :variant_name, :string
field :worker, :string
field :state, :string
field :error_reason, :string
```

From lib/rindle/ops/upload_maintenance.ex:
```elixir
def cleanup_orphans(opts \\ []) do
def abort_incomplete_uploads(opts \\ []) do
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement metadata backfill as a reusable operation and CLI</name>
  <files>lib/rindle/ops/metadata_backfill.ex, lib/mix/tasks/rindle.backfill_metadata.ex, test/rindle/ops/metadata_backfill_test.exs</files>
  <behavior>
    - Backfill iterates existing assets, downloads each source, reruns the analyzer, and persists updated metadata.
    - Failures are counted and surfaced in the command output so the operator knows what needs attention.
    - The task exits non-zero when analyzer or persistence errors occur.
  </behavior>
  <action>Build the metadata backfill service around the existing analyzer contract and asset schema. Wire the Mix task to that service so it can reanalyze existing assets without manual SQL and emit a concise run summary for scripted use.</action>
  <verify><automated>mix test test/rindle/ops/metadata_backfill_test.exs</automated></verify>
  <done>Existing assets can be reanalyzed and updated from the CLI with explicit success/failure reporting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add cron-capable maintenance workers and delegation tests</name>
  <files>lib/rindle/workers/cleanup_orphans.ex, lib/rindle/workers/abort_incomplete_uploads.ex, test/rindle/workers/maintenance_workers_test.exs</files>
  <behavior>
    - Each worker delegates to the upload-maintenance service instead of reimplementing cleanup logic.
    - The workers are safe to schedule from host Oban cron config without requiring Rindle to supervise Oban.
    - Worker failures remain tagged and observable so cron runs can be monitored.
  </behavior>
  <action>Create cron-capable Oban workers for orphan cleanup and incomplete-upload abortion that wrap the shared upload-maintenance module. Add tests proving the delegation path and the worker return contract so adopters can schedule the jobs with confidence.</action>
  <verify><automated>mix test test/rindle/workers/maintenance_workers_test.exs test/rindle/ops/metadata_backfill_test.exs</automated></verify>
  <done>Scheduled maintenance workers exist, reuse shared logic, and are covered by tests alongside the backfill path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Storage / filesystem → analyzer | Backfill reads existing media bytes and metadata from disk or storage |
| Host cron → worker modules | Scheduled jobs execute maintenance logic without Rindle owning Oban |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | I | `lib/rindle/ops/metadata_backfill.ex` | mitigate | Only persist analyzer output back into asset metadata; never log raw media bytes or secrets. |
| T-04-08 | D | `lib/rindle/workers/*.ex` | accept | Cron scheduling is host-controlled; workers must stay idempotent and return structured results. |
| T-04-09 | T | `lib/mix/tasks/rindle.backfill_metadata.ex` | mitigate | Keep the CLI as a thin wrapper around the service so arguments cannot bypass the analyzer or persistence rules. |
</threat_model>

<verification>
- `mix test test/rindle/ops/metadata_backfill_test.exs test/rindle/workers/maintenance_workers_test.exs` passes.
- Backfill updates asset metadata through the analyzer contract.
- Worker modules delegate to shared maintenance logic and stay host-schedulable.
</verification>

<success_criteria>
- Existing assets can be reanalyzed in bulk.
- Cron-capable workers exist for the two recurring maintenance jobs.
- Worker and CLI behavior is reusable and test-backed.
- The phase closes with no manual SQL required for day-2 recovery tasks.
</success_criteria>

<output>
After completion, create `.planning/phases/04-day-2-operations/04-03-SUMMARY.md`
</output>

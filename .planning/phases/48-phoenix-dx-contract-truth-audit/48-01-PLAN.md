---
phase: 48-phoenix-dx-contract-truth-audit
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [PHX-01, TRUTH-01]
must_haves:
  truths:
    - "Active truth surfaces name the currently supported Phoenix tus path as the shipped bare tus edge plus the shipped thin `Rindle.LiveView.allow_tus_upload/4` helper seam and documented `uploader: \"RindleTus\"` client path."
    - "Active truth surfaces stop using `LiveView tus uploader component` as shorthand for the whole Phoenix story."
    - "Deferred wording is split explicitly into richer reusable uploader UI/component abstractions, a Rindle-owned standalone tus JS client package, and broader future Phoenix abstractions."
    - "The active roadmap remains semantically identical while regaining `gsd-sdk query roadmap.get-phase 48` compatibility."
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Milestone constitution and support-truth framing for v1.9"
      contains: "thin `Rindle.LiveView.allow_tus_upload/4` seam"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirement-level supported-now / not-yet-claimed split"
      contains: "documented `uploader: \"RindleTus\"` client pattern"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 48 detail section and parser-readable milestone truth"
      contains: "### Phase 48:"
    - path: ".planning/STATE.md"
      provides: "Current execution risk and milestone focus wording"
      contains: "support-truth drift"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/PROJECT.md"
      via: "supported-now wording should match exactly across active truth surfaces"
      pattern: "Rindle\\.LiveView\\.allow_tus_upload/4|uploader: \"RindleTus\"|verify_completion/2"
    - from: ".planning/ROADMAP.md"
      to: ".planning/STATE.md"
      via: "current milestone and phase wording stay aligned after the truth audit"
      pattern: "Phoenix tus DX completion|support-truth drift|helper seam"
---

<objective>
Truth-align the active v1.9 planning surfaces and remove the last stale active wording that treats the shipped Phoenix helper seam as wholly deferred.

Purpose: Make the live project artifacts themselves an honest source of truth before Phase 49 productizes the adopter-facing contract further.
Output: Updated `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md`, plus a parser-readable Phase 48 detail section in the roadmap.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-phoenix-dx-contract-truth-audit/48-CONTEXT.md
@.planning/phases/48-phoenix-dx-contract-truth-audit/48-RESEARCH.md
@.planning/phases/48-phoenix-dx-contract-truth-audit/48-PATTERNS.md

<interfaces>
Supported-now wording that must stay aligned across active truth surfaces:
```markdown
bare `Rindle.Upload.TusPlug`, `Rindle.initiate_tus_upload/2`,
`Rindle.LiveView.allow_tus_upload/4`, `consume_uploaded_entries/3`, and a
documented `uploader: "RindleTus"` client pattern over the existing
`verify_completion/2` lane
```

Deferred wording that must replace stale shorthand:
```markdown
- Rindle-owned standalone tus JS client package
- Richer reusable uploader component abstractions beyond the supported helper path
- Broader multi-provider Phoenix upload abstractions beyond the current narrow tus helper path
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the active truth surfaces to freeze the supported-now versus deferred split</name>
  <read_first>
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/48-phoenix-dx-contract-truth-audit/48-CONTEXT.md
    - .planning/phases/48-phoenix-dx-contract-truth-audit/48-RESEARCH.md
  </read_first>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>Update the active v1.9 planning artifacts so they all describe the currently supported Phoenix tus contract with the same boundary: the shipped surfaces are `Rindle.Upload.TusPlug`, `Rindle.initiate_tus_upload/2`, `Rindle.LiveView.allow_tus_upload/4`, `consume_uploaded_entries/3`, and the documented `uploader: "RindleTus"` client path over `verify_completion/2`; the deferred scope is only richer reusable uploader UI/component abstractions beyond the supported helper path, a future Rindle-owned standalone tus JS client package, and broader future Phoenix abstractions. Remove active uses of `LiveView tus uploader component` as shorthand for the whole Phoenix story. Keep the tone calm and explicit: supported thin helper seam, not experimental and not a broad batteries-included Phoenix uploader abstraction.</action>
  <acceptance_criteria>
    - `rg -n 'uploader: "RindleTus"|Rindle\\.LiveView\\.allow_tus_upload/4|verify_completion/2' .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md` returns matches.
    - `rg -n 'LiveView tus uploader component' .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md` returns no matches.
    - `rg -n 'standalone tus JS client package|reusable uploader component|broader .*Phoenix upload abstractions' .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "uploader: \"RindleTus\"|Rindle\\.LiveView\\.allow_tus_upload/4|verify_completion/2" .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md && ! rg -n "LiveView tus uploader component" .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md && rg -n "standalone tus JS client package|reusable uploader component|broader .*Phoenix upload abstractions" .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md'</automated>
  </verify>
  <done>The active planning/doc truth surfaces all describe the shipped Phoenix tus seam honestly and defer only the richer future abstractions.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize the roadmap detail headings so active truth is tooling-readable again</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/phases/48-phoenix-dx-contract-truth-audit/48-RESEARCH.md
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>Update the v1.9 phase detail headings in `.planning/ROADMAP.md` from the current bold-only form to the parser-readable `### Phase 48: ...`, `### Phase 49: ...`, and `### Phase 50: ...` structure while preserving each goal and success-criteria body verbatim. Keep the visible roadmap content semantically unchanged, but make the detail sections compatible with `gsd-sdk query roadmap.get-phase 48` so active truth and planning tooling agree again.</action>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains `### Phase 48: Phoenix DX Contract + Truth Audit`.
    - `.planning/ROADMAP.md` contains `### Phase 49: LiveView Tus Productization`.
    - `.planning/ROADMAP.md` contains `### Phase 50: Phoenix Proof + Parity Closure`.
    - `gsd-sdk query roadmap.get-phase 48` returns `"found": true`.
    - `gsd-sdk query roadmap.get-phase 48` does not return `"malformed_roadmap"`.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "^### Phase 48: Phoenix DX Contract \\+ Truth Audit$|^### Phase 49: LiveView Tus Productization$|^### Phase 50: Phoenix Proof \\+ Parity Closure$" .planning/ROADMAP.md && gsd-sdk query roadmap.get-phase 48 | rg -n "\"found\": true" && ! gsd-sdk query roadmap.get-phase 48 | rg -n "malformed_roadmap"'</automated>
  </verify>
  <done>The active roadmap remains truthful and becomes machine-readable again for phase-level planning tools.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| active planning prose -> future phase execution | If active truth stays vague, Phase 49 will plan against the wrong support boundary. |
| roadmap markdown -> planning tooling | If the roadmap structure stays malformed, tooling can misread the same truth humans can read. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01-01 | T | active planning artifacts | mitigate | Replace stale shorthand with the exact supported-now / deferred split across all four active truth surfaces. |
| T-48-01-02 | R | `.planning/ROADMAP.md` | mitigate | Normalize the phase detail headings so `gsd-sdk query roadmap.get-phase 48` can resolve the same Phase 48 truth that readers see. |
</threat_model>

<verification>
- `rg -n 'uploader: "RindleTus"|Rindle\.LiveView\.allow_tus_upload/4|verify_completion/2' .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md`
- `! rg -n 'LiveView tus uploader component' .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md`
- `gsd-sdk query roadmap.get-phase 48`
</verification>

<success_criteria>
- Active planning docs distinguish the shipped bare tus edge, the shipped thin LiveView helper seam, and the still-deferred richer future abstractions.
- No active truth surface still implies that the whole Phoenix / LiveView tus story is deferred.
- `gsd-sdk query roadmap.get-phase 48` resolves successfully from the live roadmap.
</success_criteria>

<output>
After completion, create `.planning/phases/48-phoenix-dx-contract-truth-audit/48-01-SUMMARY.md`
</output>

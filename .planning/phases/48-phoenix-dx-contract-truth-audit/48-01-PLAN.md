---
phase: 48-phoenix-dx-contract-truth-audit
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - guides/resumable_uploads.md
  - lib/rindle/live_view.ex
autonomous: true
requirements: [PHX-01, TRUTH-01]
must_haves:
  truths:
    - "A Phoenix adopter can find the supported tus path from one canonical guide instead of inferring it from scattered source history."
    - "Active project truth surfaces describe the shipped Phoenix path as a supported narrow helper seam, not as wholly deferred and not as a batteries-included uploader abstraction."
    - "The supported contract consistently names the same three-part split: bare tus edge, thin LiveView helper seam, and deferred richer UI or standalone client ownership."
    - "Phase 49 inherits a precise productization target instead of a fuzzy 'build Phoenix support' brief."
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Milestone-level support-truth framing for v1.9"
      contains: "thin LiveView helper seam"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirement-level supported-now versus not-yet-claimed contract"
      contains: "documented `uploader: \"RindleTus\"` client pattern"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 48 goal, success criteria, and explicit plan list"
      contains: "48-01-PLAN.md"
    - path: ".planning/STATE.md"
      provides: "Current milestone truth and Phase 48 execution risk wording"
      contains: "support-truth drift"
    - path: "guides/resumable_uploads.md"
      provides: "Single canonical Phoenix/LiveView tus guide"
      contains: "### LiveView helper"
    - path: "lib/rindle/live_view.ex"
      provides: "Thin API-doc pointer to the canonical guide"
      contains: "guides/resumable_uploads.md"
  key_links:
    - from: "lib/rindle/live_view.ex"
      to: "guides/resumable_uploads.md"
      via: "moduledoc and `allow_tus_upload/4` API docs point readers to the guide instead of duplicating setup"
      pattern: "guides/resumable_uploads\\.md"
    - from: ".planning/PROJECT.md"
      to: ".planning/REQUIREMENTS.md"
      via: "matching supported-now versus deferred wording"
      pattern: "Rindle\\.LiveView\\.allow_tus_upload/4|RindleTus|standalone tus JS client package"
    - from: ".planning/ROADMAP.md"
      to: ".planning/STATE.md"
      via: "matching statement that the remaining risk is truth drift, not missing tus capability"
      pattern: "support-truth drift|thin LiveView helper seam"
---

<objective>
Freeze the active Phoenix tus support claim across planning and live documentation surfaces, using one canonical guide and thin API-doc pointers.

Purpose: Eliminate support-truth drift in the current v1.9 source of truth before adding archive disclaimers or parity gates.
Output: Updated active planning artifacts, a canonical guide that explicitly owns the Phoenix story, and thin `Rindle.LiveView` docs that point to that guide.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-phoenix-dx-contract-truth-audit/48-CONTEXT.md
@.planning/phases/48-phoenix-dx-contract-truth-audit/48-RESEARCH.md
@.planning/phases/48-phoenix-dx-contract-truth-audit/48-PATTERNS.md
@guides/resumable_uploads.md
@lib/rindle/live_view.ex
@lib/rindle.ex

<interfaces>
From `guides/resumable_uploads.md`:
```markdown
### LiveView helper
If your upload form already lives in LiveView, Rindle can precreate the tus
resource server-side and hand the signed `upload_url` plus `session_id` /
`asset_id` back through LiveView's `:external` upload metadata:

Use a tiny client uploader keyed by `uploader: "RindleTus"`:
```

From `lib/rindle/live_view.ex`:
```elixir
@doc """
Configures a LiveView external upload backed by Rindle's tus edge.

Requires:

  * `:path` - the mounted tus route, such as `"/uploads/tus"`
  * `:secret_key_base` - the same secret used to mount `Rindle.Upload.TusPlug`
"""
```

From `.planning/REQUIREMENTS.md`:
```markdown
- **Supported now:** bare `Rindle.Upload.TusPlug`, `Rindle.initiate_tus_upload/2`,
  `Rindle.LiveView.allow_tus_upload/4`, `consume_uploaded_entries/3`, and a
  documented `uploader: "RindleTus"` client pattern over the existing
  `verify_completion/2` lane.
- **Not yet claimed:** a Rindle-owned standalone tus JS client package, a broad
  drag/drop uploader component library, multi-provider Phoenix abstractions, or
  new tus protocol extensions.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align active planning truth surfaces to the shipped Phoenix tus contract</name>
  <read_first>
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/48-phoenix-dx-contract-truth-audit/48-CONTEXT.md
    - .planning/phases/48-phoenix-dx-contract-truth-audit/48-RESEARCH.md
    - .planning/phases/48-phoenix-dx-contract-truth-audit/48-PATTERNS.md
  </read_first>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>Update the active truth surfaces first per D-10. In all four files, use the same support-boundary wording per D-04 through D-09 and the pattern map's three-part split: the shipped bare tus edge (`Rindle.Upload.TusPlug`, `Rindle.initiate_tus_upload/2`, `verify_completion/2` lane), the shipped thin LiveView helper seam (`Rindle.LiveView.allow_tus_upload/4` plus documented `uploader: "RindleTus"`), and the deferred richer UI-kit / standalone JS-package ownership. Remove any wording that treats the whole LiveView tus story as deferred, replace any vague "LiveView uploader" shorthand with explicit deferred item names per D-07 through D-09, and keep the Phase 49 brief focused on productizing the already-supported seam rather than inventing new tus capability. Update `.planning/ROADMAP.md` to list the Phase 48 plans written by this planning pass without making the malformed `**Phase 48:**` heading issue the point of the phase.</action>
  <acceptance_criteria>
    - `rg -n 'documented `uploader: "RindleTus"` client pattern|thin LiveView helper seam|standalone tus JS client package|richer reusable uploader' .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md` returns matches.
    - `rg -n 'LiveView tus uploader component' .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md` returns no matches.
    - `rg -n '48-01-PLAN.md|48-02-PLAN.md' .planning/ROADMAP.md` returns matches.
    - `rg -n 'support-truth drift' .planning/STATE.md` returns one match.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "documented `uploader: \"RindleTus\"` client pattern|thin LiveView helper seam|standalone tus JS client package|richer reusable uploader" .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md && ! rg -n "LiveView tus uploader component" .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md && rg -n "48-01-PLAN.md|48-02-PLAN.md" .planning/ROADMAP.md && rg -n "support-truth drift" .planning/STATE.md'</automated>
  </verify>
  <done>All active planning truth surfaces use the same precise shipped-versus-deferred contract and Phase 48 is explicitly broken into executable plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep the Phoenix story canonical in the guide and thin in `Rindle.LiveView` docs</name>
  <read_first>
    - guides/resumable_uploads.md
    - lib/rindle/live_view.ex
    - lib/rindle.ex
    - .planning/phases/48-phoenix-dx-contract-truth-audit/48-CONTEXT.md
    - .planning/phases/48-phoenix-dx-contract-truth-audit/48-RESEARCH.md
    - .planning/phases/48-phoenix-dx-contract-truth-audit/48-PATTERNS.md
  </read_first>
  <files>guides/resumable_uploads.md, lib/rindle/live_view.ex</files>
  <behavior>
    - The canonical end-to-end Phoenix/LiveView tus story stays in `guides/resumable_uploads.md`, including adopter-owned router/auth/parser/CORS wiring and the supported `uploader: "RindleTus"` handoff.
    - `Rindle.LiveView` docs stay seam-level: they can name required options and the completion lane, but they point to `guides/resumable_uploads.md` for the full setup instead of becoming a second canon.
    - The wording preserves D-04 through D-06: supported narrow helper seam now, adopter still owns router/auth/parser/CORS and operational caveats such as sticky-session or single-node resume posture where relevant.
  </behavior>
  <action>Update `guides/resumable_uploads.md` so it explicitly answers PHX-01 as the single canonical Phoenix/LiveView tus guide per D-01 and D-03, using the established guide-first pattern in `48-PATTERNS.md`. Keep the guide authoritative for router mount, parser/CORS, `allow_tus_upload/4`, `uploader: "RindleTus"`, UI state separation, and `consume_uploaded_entries/3` completion. Then thin `lib/rindle/live_view.ex` per D-02: keep the moduledoc and `allow_tus_upload/4` docs focused on the helper seam, required/optional options, `verify_completion/2` / `consume_uploaded_entries/3` lane, bearer-URL posture, and an explicit pointer to `guides/resumable_uploads.md` for the full Phoenix setup. Do not add a second Phoenix guide, and do not expand the moduledoc into a duplicate router/parser/CORS/client tutorial.</action>
  <acceptance_criteria>
    - `rg -n 'canonical Phoenix|single canonical|uploader: "RindleTus"|consume_uploaded_entries/3|sticky-session|single-node' guides/resumable_uploads.md` returns matches.
    - `rg -n 'guides/resumable_uploads.md' lib/rindle/live_view.ex` returns at least one match.
    - `rg -n 'Plug\\.Parsers|CORS|router\\.ex|Uploaders\\.RindleTus' lib/rindle/live_view.ex` returns no matches in the moduledoc or `allow_tus_upload/4` docs.
    - `rg -n 'allow_tus_upload/4|verify_completion/2|consume_uploaded_entries/3' lib/rindle/live_view.ex` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "canonical Phoenix|single canonical|uploader: \"RindleTus\"|consume_uploaded_entries/3|sticky-session|single-node" guides/resumable_uploads.md && rg -n "guides/resumable_uploads.md" lib/rindle/live_view.ex && ! rg -n "Plug\\.Parsers|CORS|router\\.ex|Uploaders\\.RindleTus" lib/rindle/live_view.ex && rg -n "allow_tus_upload/4|verify_completion/2|consume_uploaded_entries/3" lib/rindle/live_view.ex'</automated>
  </verify>
  <done>The guide is the only end-to-end Phoenix canon, while `Rindle.LiveView` docs remain thin seam-level pointers that preserve the exact support boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| active planning truth → adopters | Overclaiming or understating support here changes what Phoenix adopters believe Rindle guarantees. |
| API docs → canonical guide | If `Rindle.LiveView` duplicates setup details, the repo grows two conflicting support canons. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01-01 | T | `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md` | mitigate | Use the same shipped-helper-versus-deferred split in every active truth surface per D-04 through D-10 so wording drift cannot reintroduce a false "fully deferred" claim. |
| T-48-01-02 | I | `guides/resumable_uploads.md`, `lib/rindle/live_view.ex` | mitigate | Preserve bearer-URL and adopter-owned wiring caveats in the canonical guide, while keeping API docs thin and guide-pointing so sensitive operational details are stated once and consistently. |
| T-48-01-03 | R | `.planning/ROADMAP.md` | mitigate | Record the concrete Phase 48 plan list in the active roadmap so downstream execution and later audits can tie wording changes to explicit plan artifacts. |
</threat_model>

<verification>
- `rg -n 'documented `uploader: "RindleTus"` client pattern|thin LiveView helper seam|standalone tus JS client package|richer reusable uploader' .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md`
- `rg -n 'canonical Phoenix|single canonical|uploader: "RindleTus"|consume_uploaded_entries/3|sticky-session|single-node' guides/resumable_uploads.md`
- `rg -n 'guides/resumable_uploads.md|allow_tus_upload/4|verify_completion/2|consume_uploaded_entries/3' lib/rindle/live_view.ex`
</verification>

<success_criteria>
- Active truth surfaces all describe the shipped Phoenix path as supported now and defer only richer future abstractions.
- `guides/resumable_uploads.md` is explicitly the canonical Phoenix/LiveView tus story.
- `Rindle.LiveView` docs point to the guide and do not become a second router/parser/CORS tutorial.
- The active roadmap includes the executable Phase 48 plan list.
</success_criteria>

<output>
After completion, create `.planning/phases/48-phoenix-dx-contract-truth-audit/48-01-SUMMARY.md`
</output>

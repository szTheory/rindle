---
phase: 18
slug: documentation-and-typespec-coverage
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-30
---

# Phase 18 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | ExUnit (`mix test`) — Elixir 1.15+ stdlib |
| **Config file** | `test/test_helper.exs` (existing) |
| **Quick run command** | `mix test test/rindle/doctor_thresholds_test.exs test/rindle/behaviour_docs_test.exs --color` |
| **Full suite command** | `mix test` (chains via `mix.exs` aliases: `ecto.create --quiet`, `ecto.migrate --quiet`, `test`) |
| **Doctor gate command** | `MIX_ENV=test mix doctor --full --raise` |
| **Dialyzer command** | `mix dialyzer --format github` |
| **ExDoc command** | `mix docs --warnings-as-errors` |
| **Estimated runtime (quick)** | ~5 seconds |
| **Estimated runtime (full)** | ~30 seconds (current suite); Dialyzer ~60s with PLT cached |

---

## Sampling Rate

- **After every task commit:** Run quick command (`doctor_thresholds_test.exs` + `behaviour_docs_test.exs`)
- **After every plan wave:** Run full suite (`mix test`) + `MIX_ENV=test mix doctor --full --raise`
- **Before `/gsd-verify-work`:** Full suite green + doctor gate green + Dialyzer clean + `mix docs --warnings-as-errors` clean
- **Max feedback latency:** ~5 seconds (quick); ~90 seconds (full + doctor + dialyzer)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 18-01-01 | 01 | 1 | API-08 | — | N/A | unit | `mix test test/rindle/doctor_thresholds_test.exs` | ❌ W0 | ⬜ pending |
| 18-01-02 | 01 | 1 | API-08 | — | N/A | tooling | `MIX_ENV=test mix doctor --full --raise` (baseline pass) | ❌ W0 | ⬜ pending |
| 18-01-03 | 01 | 1 | API-08 | — | N/A | ci | `grep -F 'mix doctor --full --raise' .github/workflows/ci.yml` | ✅ | ⬜ pending |
| 18-02-01 | 02 | 2 | API-07 | — | N/A | unit | `mix dialyzer --format github` (Rindle facade specs) | ✅ | ⬜ pending |
| 18-02-02 | 02 | 2 | API-07 | — | N/A | unit | `mix dialyzer --format github` (Rindle.Upload.Broker specs) | ✅ | ⬜ pending |
| 18-02-03 | 02 | 2 | API-07 | — | N/A | unit | `mix dialyzer --format github` (Rindle.Storage adapter result types) | ✅ | ⬜ pending |
| 18-02-04 | 02 | 2 | API-07 | — | N/A | docs | `mix docs --warnings-as-errors` (named-type autolinks) | ✅ | ⬜ pending |
| 18-03-01 | 03 | 3 | API-06 | — | N/A | unit | `mix test test/rindle/behaviour_docs_test.exs` | ❌ W0 | ⬜ pending |
| 18-03-02 | 03 | 3 | API-06 | — | N/A | tooling | `MIX_ENV=test mix doctor --full` (broker @spec gap closes) | ✅ | ⬜ pending |
| 18-03-03 | 03 | 3 | API-06 + D-27 | — | N/A | unit | `mix test test/rindle/api_surface_boundary_test.exs` (Rindle.Processor.Image in @public_modules) | ✅ | ⬜ pending |
| 18-04-01 | 04 | 4 | API-06 | — | N/A | tooling | `MIX_ENV=test mix doctor --full` (mix tasks/workers/macro/HTML clean) | ✅ | ⬜ pending |
| 18-04-02 | 04 | 4 | API-06 | — | N/A | unit | `mix test` (compatibility shim @deprecated emits warning) | ✅ | ⬜ pending |
| 18-05-01 | 05 | 5 | API-06+API-07+API-08 | — | N/A | unit | `mix test test/rindle/doctor_thresholds_test.exs` (now green at target) | ✅ | ⬜ pending |
| 18-05-02 | 05 | 5 | API-06+API-07+API-08 | — | N/A | tooling | `MIX_ENV=test mix doctor --full --raise` (target thresholds pass) | ✅ | ⬜ pending |
| 18-05-03 | 05 | 5 | API-06 | — | N/A | regression | manually downgrade one `@doc`, run doctor, expect non-zero exit | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*File Exists: ✅ = file already exists, ❌ W0 = Wave 0 creates it*

---

## Wave 0 Requirements

- [ ] `test/rindle/doctor_thresholds_test.exs` — D-23 RED harness (Plan 18-01 ships failing — asserts target thresholds; turns green when Plan 18-05 ratchets `.doctor.exs`)
- [ ] `test/rindle/behaviour_docs_test.exs` — D-19 backstop (Plan 18-03 ships green — `Code.fetch_docs/1` over each behaviour module asserting every `:callback` doc is non-`:none`/non-`:hidden`)
- [ ] `.doctor.exs` at project root — generated by `mix doctor.gen.config` then hand-edited for `ignore_modules:` regex shape (Plan 18-01)
- [ ] `:doctor, "~> 0.22.0", only: [:dev, :test], runtime: false` in `mix.exs` deps (Plan 18-01)
- [ ] CI step in `.github/workflows/ci.yml` between line 85 (`mix credo --strict`) and line 87 (`Run tests with coverage`) — `MIX_ENV=test mix doctor --full --raise` (Plan 18-01)

ExUnit framework install: not required — `mix test` already wired and exercised across Phases 1-17.

PLT cache: not required — Dialyzer PLT is already cached in CI per `.github/workflows/ci.yml:90-104`.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `@deprecated` warning emission on `Rindle.log_variant_processing_failure/3` (D-16) | API-06 | Compile-time warning, not a runtime assertion; ExUnit can `assert_warning/1` but adds noise | After Plan 18-04: write a tiny `:dev`-env script that calls the function inside an `iex -S mix` session and observes the deprecation warning at compile time. Document in PR. |
| ExDoc visual sanity check | API-06 | Visual-only — does `Rindle.Processor.Image` show in the right group with the right callback summary? | After Plan 18-03 / 18-04: run `mix docs` locally, open `doc/Rindle.Processor.Image.html` in a browser, eyeball that the `@moduledoc` renders, callbacks appear, and the module is in the expected `groups_for_modules` group. |
| Failing-doc regression demo | API-08 | One-off CI-runtime probe (T2), not a recurring test | Plan 18-05: in a throwaway commit, downgrade one `@doc` on a public function to `@doc false`, push, observe CI red on the doctor step. Revert before merge. Captured in PR description as evidence. |

*Note:* The threshold-target ratchet test (`doctor_thresholds_test.exs`) covers most of D-22 / D-23 automatically — these manual checks are the small residue.

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (`doctor_thresholds_test.exs`, `behaviour_docs_test.exs`, `.doctor.exs`, `:doctor` dep, CI step)
- [ ] No watch-mode flags (Phase 18 uses one-shot commands; `mix test --listen-on-stdin` not used)
- [ ] Feedback latency < 90 seconds (full + doctor + dialyzer)
- [ ] `nyquist_compliant: true` set in frontmatter (after Wave 0 satisfied)

**Approval:** pending

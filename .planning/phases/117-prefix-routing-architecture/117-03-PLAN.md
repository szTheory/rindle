---
phase: 117-prefix-routing-architecture
plan: 03
type: execute
wave: 3
depends_on: [117-02]
files_modified:
  - lib/rindle/schema.ex
  - test/rindle/schema_prefix_contract_test.exs
  - test/rindle/config/config_test.exs
autonomous: true
gap_closure: true
requirements: [PREFIX-01, PREFIX-02, PREFIX-03]

must_haves:
  truths:
    - "A module using Rindle.Schema cannot finish compilation when its final Ecto prefix differs from Rindle.Schema.prefix(), closing PREFIX-01 and PREFIX-03."
    - "A default rindle build rejects a post-use dynamic override to public, while an unmodified schema still compiles with rindle metadata for PREFIX-01."
    - "The explicit public compatibility build accepts unmodified Rindle schemas with public metadata and rejects a dynamic override away from that authority for PREFIX-02."
    - "The literal source guard remains supplemental, and Rindle does not change Repo-wide or Oban prefix behavior for PREFIX-03."
  artifacts:
    - path: "lib/rindle/schema.ex"
      provides: "Compile-finalization enforcement of the shared Rindle Ecto prefix"
      exports: ["prefix/0", "__using__/1", "__after_compile__/2", "validate_prefix!/1"]
    - path: "test/rindle/schema_prefix_contract_test.exs"
      provides: "Behavioral compilation regression for dynamic prefix mutation plus existing structural guards"
    - path: "test/rindle/config/config_test.exs"
      provides: "Compile-authority regression coverage that does not assume runtime application-env mutation can retarget a compiled schema"
  key_links:
    - from: "Rindle.Schema.prefix/0"
      to: "consumer @schema_prefix"
      via: "macro-local prefix = Rindle.Schema.prefix() bound into the returned quote before @schema_prefix prefix"
      pattern: "bind_quoted:.*prefix"
    - from: "Rindle.Schema.__using__/1"
      to: "Rindle.Schema.__after_compile__/2"
      via: "an after-compile callback registered on every consumer of Rindle.Schema"
      pattern: "@after_compile\\s+Rindle\\.Schema"
    - from: "Rindle.Schema.__after_compile__/2"
      to: "consumer.__schema__(:prefix)"
      via: "final Ecto metadata equality check against Rindle.Schema.prefix/0"
      pattern: "__schema__\\(:prefix\\)"
    - from: "test/rindle/schema_prefix_contract_test.exs"
      to: "Rindle.Schema.__after_compile__/2"
      via: "Code.compile_string/1 consumer that mutates :schema_prefix after use Rindle.Schema"
      pattern: "Module\\.put_attribute.*:schema_prefix"
---

<objective>
Close the Phase 117 single-routing-authority gap by making the final Ecto schema metadata, rather than source syntax alone, enforce the selected Rindle prefix.

Purpose: Prevent a schema consumer from silently replacing the compile-time routing decision after `use Rindle.Schema`, while preserving the shipped default/compatibility build contract and independent host-owned Oban routing.
Output: A compile-finalization guard and regression tests that reject the demonstrated dynamic mutation in both supported build postures.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-prefix-routing-architecture/117-VERIFICATION.md
@.planning/phases/117-prefix-routing-architecture/117-REVIEW.md
@.planning/phases/117-prefix-routing-architecture/117-PATTERNS.md
@.planning/phases/117-prefix-routing-architecture/117-01-SUMMARY.md
@.planning/phases/117-prefix-routing-architecture/117-02-SUMMARY.md
@lib/rindle/schema.ex
@test/rindle/schema_prefix_contract_test.exs
@test/rindle/config/config_test.exs
@lib/rindle/profile.ex
@test/rindle/profile/profile_test.exs

<interfaces>
From `lib/rindle/schema.ex`:
- `Rindle.Schema.prefix/0` is the already-compiled authority and returns exactly `"rindle"` or `"public"`.
- `Rindle.Schema.__using__/1` installs `Ecto.Schema`, binary primary/foreign-key defaults, and the selected schema prefix.
- `Rindle.Schema.validate_prefix!/1` raises `ArgumentError` for values outside the two supported prefixes.

From `test/rindle/schema_prefix_contract_test.exs`:
- `@domain_schemas` enumerates the six Rindle-owned schemas.
- `uses_rindle_schema?/1`, `uses_ecto_schema_directly?/1`, and `sets_schema_prefix_directly?/1` are source-level guardrails that must remain in place.

Mapped implementation analogs:
- lib/rindle/profile.ex resolves and validates macro inputs outside its returned quote, then transfers them with quote bind_quoted; use this pattern for the selected prefix.
- test/rindle/profile/profile_test.exs compiles uniquely named macro consumers with Code.compile_string/1 and asserts compile-time rejection; use this pattern for the dynamic override regression.

From the Phase 117 test build:
- `config/test.exs` intentionally compiles Rindle with `"public"` selected and uses `"rindle"` as the decoy until Phase 118 provisions the default schema.
- Oban remains host configured; no Rindle after-compile callback or test may alter `Oban.Job` metadata.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject a dynamic post-use prefix mutation at compile finalization</name>
  <files>lib/rindle/schema.ex, test/rindle/schema_prefix_contract_test.exs</files>
  <behavior>
    - RED: compiling a uniquely named schema that uses Rindle.Schema, dynamically replaces `:schema_prefix` with the value opposite `Rindle.Schema.prefix/0`, and then declares its schema must raise `ArgumentError`.
    - GREEN: an unmodified uniquely named schema compiles and reports `module.__schema__(:prefix) == Rindle.Schema.prefix()` and matching new-struct metadata.
    - ERROR CONTRACT: the rejection names the consuming module and reports the expected and actual prefixes without exposing unrelated configuration.
  </behavior>
  <action>
Follow the macro-binding analog in lib/rindle/profile.ex exactly for the initial assignment: resolve `prefix = Rindle.Schema.prefix()` outside the returned quote, pass it through `quote bind_quoted: [prefix: prefix]`, and set `@schema_prefix prefix` inside the consumer quote. Remove the existing quoted `Application.compile_env/3` expression. The after-compile callback must not choose or repair the initial prefix; it exists solely to reject an illicit mutation performed after the bound assignment.

Follow test/rindle/profile/profile_test.exs for the regression harness: compile uniquely named consumers with `Code.compile_string/1` and assert the bounded compile-time failure.

Add the behavioral regression to `Rindle.SchemaPrefixContractTest` before changing production code. Generate each consumer name with `System.unique_integer([:positive])`, compile it with `Code.compile_string/1`, and mutate the prefix through `Module.put_attribute/3` after `use Rindle.Schema` so the test reproduces the verifier's real bypass rather than another literal-AST case. Keep the existing six-schema AST checks unchanged as a supplemental early-warning guard.

Then extend `Rindle.Schema.__using__/1` to register a narrowly scoped compile-finalization callback and implement `Rindle.Schema.__after_compile__/2`. Resolve the expected value from the already-compiled `Rindle.Schema.prefix/0`; after the consumer finishes, read its final `__schema__(:prefix)` metadata and raise a bounded `ArgumentError` unless the values are equal. Preserve the existing `Ecto.Schema` setup, binary key defaults, two-value validation, and public API visibility. This closes the mutable metadata route for PREFIX-01 and PREFIX-03 without adding Repo wrappers, per-query routing, search-path behavior, or migration logic.
  </action>
  <verify>
    <automated>mix format --check-formatted lib/rindle/schema.ex test/rindle/schema_prefix_contract_test.exs &amp;&amp; mix test test/rindle/schema_prefix_contract_test.exs --seed 0</automated>
  </verify>
  <done>The macro resolves `prefix = Rindle.Schema.prefix()` before quoting and binds that value into the consumer's initial `@schema_prefix`; the dynamic mutation regression then passes because a later mismatch is rejected by the final metadata equality check, while unmodified consumers and all six owned schemas retain the shared prefix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove default and public builds retain one authority without touching Oban</name>
  <files>test/rindle/schema_prefix_contract_test.exs, test/rindle/config/config_test.exs</files>
  <behavior>
    - The normal test build remains explicit public compatibility: Rindle.Schema.prefix/0, all six owned schemas, and new structs report `"public"`.
    - Changing application environment after Rindle.Schema is compiled cannot retarget a newly compiled consumer away from Rindle.Schema.prefix/0.
    - A fresh default build reports `Rindle.Schema.prefix() == "rindle"` and rejects the exact post-use `Module.put_attribute(__MODULE__, :schema_prefix, "public")` probe.
    - `Oban.Job.__schema__(:prefix)` remains independent and no production Oban configuration is changed.
  </behavior>
  <action>
Align the focused config test with the enforced compile-time authority: it must no longer claim that mutating `Application` environment after `Rindle.Schema` was compiled can select a different prefix for a later consumer. Instead, prove that the consumer stays equal to `Rindle.Schema.prefix/0`, and retain the existing invalid-value and separate `oban_prefix` coverage.

Extend the contract coverage so the current explicit-public test build proves that unmodified schemas compile and that an attempted alternate dynamic mutation is rejected. Add the default-build probe to the automated verification command: compile with no compatibility override, assert the authority is `"rindle"`, and compile a one-off consumer containing the exact dynamic public mutation from the verification gap; the command must succeed only when that compilation raises the bounded `ArgumentError`. This supplies PREFIX-01 and PREFIX-02 evidence across both supported builds while preserving PREFIX-03's Oban independence. Do not modify migrations, README/guides, generated applications, Cohort, `Rindle.Repo`, Oban setup, or raw-SQL/catalog code; those surfaces belong to Phases 118-120.
  </action>
  <verify>
    <automated>mix format --check-formatted lib/rindle/schema.ex test/rindle/schema_prefix_contract_test.exs test/rindle/config/config_test.exs &amp;&amp; mix test test/rindle/config/config_test.exs test/rindle/domain/media_schema_test.exs test/rindle/schema_prefix_contract_test.exs test/rindle/schema_prefix_integration_test.exs --seed 0 &amp;&amp; MIX_ENV=dev mix compile --force &amp;&amp; MIX_ENV=dev mix run --no-start -e 'unless Rindle.Schema.prefix() == "rindle", do: raise("default prefix mismatch"); try do Code.compile_string(~s(defmodule Rindle.DynamicPublicPrefixOverrideProbe do\n  use Rindle.Schema\n  Module.put_attribute(__MODULE__, :schema_prefix, "public")\n  schema "dynamic_public_prefix_override_probes" do\n  end\nend)) ; raise("dynamic public override compiled") rescue error in ArgumentError -&gt; unless String.contains?(Exception.message(error), "prefix"), do: reraise(error, __STACKTRACE__) end'</automated>
  </verify>
  <done>Focused tests pass in the explicit-public test build, the fresh default-build probe confirms `rindle` and rejects the exact dynamic public override, existing literal source guards still pass, and Oban metadata/configuration remains independent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Library macro to adopter schema module | Consumer module code can mutate module attributes after invoking the shared macro. |
| Rindle domain schemas to shared host Repo | Rindle-owned Ecto metadata must select only the compiled Rindle prefix without changing host-owned Oban routing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-117-03-01 | Tampering | `Rindle.Schema.__using__/1` consumer | medium | mitigate | Compare final `module.__schema__(:prefix)` with `Rindle.Schema.prefix/0` in `__after_compile__/2`; lock with the dynamic compilation regression. |
| T-117-03-02 | Information Disclosure | Prefix-divergent Rindle schema | medium | mitigate | Reject compilation before a schema can route reads to a legacy/decoy namespace; verify both supported build selections. |
| T-117-03-03 | Denial of Service | Host-owned Oban routing | low | accept | The plan changes no Repo-wide prefix or Oban configuration; focused tests assert independence, so no new Oban execution path is introduced. |
</threat_model>

<source_audit>

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | One explicit proven routing model with no silent fallback | 117-01, 117-02, 117-03 | COVERED | Plan 03 closes the remaining mutable-authority bypass. |
| REQ | PREFIX-01 | Default Rindle-owned state routes to `rindle` | 117-01, 117-02, 117-03 | COVERED | Default-build final metadata and rejection probe are explicit acceptance evidence. |
| REQ | PREFIX-02 | Explicit coherent `public` compatibility | 117-01, 117-02, 117-03 | COVERED | The existing public test build remains supported; migration/docs pairing stays in Phases 118/120 as explicitly deferred by verification. |
| REQ | PREFIX-03 | Normal paths cannot silently use another prefix | 117-01, 117-02, 117-03 | COVERED | Existing integration proof plus compile-finalization enforcement covers present and future owned schemas. |
| RESEARCH | — | Use one compile-time Rindle.Schema authority, not runtime helpers or search_path | 117-01, 117-03 | COVERED | Final Ecto metadata is enforced at the macro boundary. |
| RESEARCH | — | Preserve the default `rindle` and explicit `public` builds | 117-01, 117-02, 117-03 | COVERED | Both compile postures have automated evidence. |
| RESEARCH | — | Keep Oban host-owned and independently configured | 117-01, 117-03 | COVERED | No Oban/Repo files change; focused assertions remain. |
| CONTEXT | — | No phase CONTEXT.md exists | — | NOT APPLICABLE | Roadmap, requirements, verification gap, and the authorized gap instruction define the locked scope. |
| VERIFICATION | gap-01 | Dynamic `Module.put_attribute/3` can replace final Ecto prefix | 117-03 | COVERED | Tasks add finalization enforcement and a real compilation probe. |
| VERIFICATION | deferred-01 | Public migration/docs pairing | 118, 120 | EXCLUDED | Explicitly deferred by `117-VERIFICATION.md`; not a Phase 117 gap. |

</source_audit>

<verification>
1. Run the focused public-compatibility tests from Task 2 with deterministic seed 0.
2. Run the default-build `MIX_ENV=dev` compilation probe from Task 2; it must observe `Rindle.Schema.prefix() == "rindle"` and treat the exact dynamic public mutation as an expected `ArgumentError`.
3. Run `mix coveralls.multiple --type local --type json --slowest 20` as the repository quality/coverage gate named by `RUNNING.md`.
4. Confirm `git diff --name-only` for the implementation contains only the three declared files and no migration, documentation, Repo, Oban, workflow, or dependency changes.
</verification>

<acceptance_criteria>
- `Rindle.Schema.__after_compile__/2` enforces `module.__schema__(:prefix) == Rindle.Schema.prefix()` for every consumer.
- The stored regression compiles a real dynamic-mutation consumer and proves the mismatch is rejected; the default-build automated probe uses the exact public override identified by verification.
- Unmodified consumers compile successfully under the default `rindle` build and the explicit `public` compatibility test build.
- The pre-existing literal AST guard remains present and passing as supplemental defense.
- Focused tests and the full `RUNNING.md` coverage command pass without changing Oban independence or later-phase migration/documentation scope.
</acceptance_criteria>

<success_criteria>
The Phase 117 routing authority is enforceable after consumer compilation, the demonstrated dynamic bypass is regression-locked in both supported build postures, and no Phase 118-120 or host-owned Oban responsibility is pulled into this closure.
</success_criteria>

<output>
Create `.planning/phases/117-prefix-routing-architecture/117-03-SUMMARY.md` when done.
</output>

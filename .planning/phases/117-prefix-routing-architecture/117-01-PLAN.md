---
phase: 117
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/rindle/schema.ex
  - lib/rindle/config.ex
  - lib/rindle/domain/media_asset.ex
  - lib/rindle/domain/media_attachment.ex
  - lib/rindle/domain/media_variant.ex
  - lib/rindle/domain/media_upload_session.ex
  - lib/rindle/domain/media_processing_run.ex
  - lib/rindle/domain/media_provider_asset.ex
  - test/rindle/config/config_test.exs
  - test/rindle/domain/media_schema_test.exs
autonomous: true
requirements: [PREFIX-01, PREFIX-02, PREFIX-03]
---

<objective>
Establish one compile-time Ecto schema-prefix contract for every Rindle-owned domain
schema: `rindle` by default, with explicit `public` legacy compatibility and no change
to Oban routing.
</objective>

<tasks>
1. Add focused red tests that assert all six Rindle schemas and new structs carry the
   selected prefix, preserve binary primary/foreign-key defaults, reject unsupported
   compile-time prefix values, and retain independent `oban_prefix` behavior.
2. Add internal `Rindle.Schema`, based on `Ecto.Schema`, with validated compile-time
   `:rindle_prefix` (`"rindle"` default or explicit `"public"` only). Align
   `Rindle.Config.rindle_prefix/0` so diagnostics cannot report a different normal
   data-path prefix.
3. Convert exactly the six Rindle-owned domain schemas to the macro. Do not change
   `Rindle.Repo`, Oban configuration, host schemas, migrations, or `search_path`.
</tasks>

<verification>
Run focused config/domain tests with `--seed 0`, then the affected unit/integration
tests named by `RUNNING.md`. Confirm `Oban.Job` has no Rindle schema prefix.
</verification>

<success_criteria>
- Every normal Rindle schema-backed path has one Ecto metadata prefix.
- Public compatibility is explicit rather than a runtime fallback.
- Existing schema field/key semantics remain unchanged.
</success_criteria>

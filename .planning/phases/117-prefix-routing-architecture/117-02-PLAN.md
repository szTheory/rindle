---
phase: 117
plan: 02
type: tdd
wave: 2
depends_on: [117-01]
files_modified:
  - test/rindle/schema_prefix_integration_test.exs
  - test/rindle/schema_prefix_contract_test.exs
  - test/support/schema_prefix_case.ex
autonomous: true
requirements: [PREFIX-01, PREFIX-02, PREFIX-03]
---

<objective>
Prove the prefix contract covers real Rindle operations and cannot be masked by
same-named `public` tables, before Phase 118 adds schema provisioning and moves.
</objective>

<tasks>
1. Build isolated PostgreSQL test support that prepares distinguishable Rindle/public
   fixtures without changing global `search_path` or leaking state across async tests.
2. Add tracer integration proof through a public facade path, an Ecto.Multi path, and
   a worker/loaded-struct path. Assert all Rindle reads and writes use the selected
   schema when decoy public rows exist.
3. Add structural contract tests that prevent a Rindle domain schema from bypassing
   `Rindle.Schema`, and run the relevant full quality suite to catch unsupported raw
   or schema-less routes for Phase 119 follow-up.
</tasks>

<verification>
Run the prefix integration/contract tests and the validation strategy's focused
commands; run `mix coveralls.multiple --type local --type json` before phase close.
</verification>

<success_criteria>
- Decoy public tables cannot mask a route escape.
- Facade, Multi, worker, loaded/new struct, and association behavior is proven.
- The plan leaves migrations, Oban ownership, demo provisioning, and public packaged
  consumer proof to their dedicated later phases.
</success_criteria>

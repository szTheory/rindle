---
phase: 55-proof-adopter-guidance
plan: 02
type: execute
wave: 2
depends_on:
  - 55-01
files_modified:
  - guides/user_flows.md
  - guides/getting_started.md
  - guides/operations.md
  - test/install_smoke/docs_parity_test.exs
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - TRUTH-02
must_haves:
  truths:
    - "`guides/user_flows.md` becomes the canonical executable owner-erasure story for adopters."
    - "`guides/getting_started.md` and `guides/operations.md` stay thin pointer or boundary surfaces instead of duplicating the full semantics."
    - "Docs parity and active planning artifacts freeze owner/account erasure as the supported account-deletion surface while keeping `cleanup_orphans` maintenance-only."
  artifacts:
    - path: "guides/user_flows.md"
      provides: "canonical adopter-facing owner-erasure flow"
    - path: "test/install_smoke/docs_parity_test.exs"
      provides: "support-truth freeze for owner-erasure guidance"
    - path: ".planning/REQUIREMENTS.md"
      provides: "active traceability state for proof and truth closure"
  key_links:
    - from: "guides/user_flows.md"
      to: "test/adopter/canonical_app/lifecycle_test.exs"
      via: "guide snippet and story must mirror the proved public facade"
      pattern: "preview_owner_erasure|erase_owner"
    - from: "guides/operations.md"
      to: ".planning/REQUIREMENTS.md"
      via: "maintenance-only boundary for cleanup_orphans"
      pattern: "cleanup_orphans|maintenance-only"
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "active planning truth must reflect proof completion and supported-now posture"
      pattern: "PROOF-03|PROOF-04|TRUTH-02"
---

<objective>
Close the guidance and planning-truth side of the owner-erasure milestone.

Purpose: satisfy `TRUTH-02` by upgrading the canonical guide flow, freezing it
with docs parity, and aligning the active planning artifacts with the now-
proved owner-erasure surface.
Output: canonical owner-erasure guide section, thin pointers elsewhere,
docs-parity coverage, and planning files that describe the supported surface
consistently.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/55-proof-adopter-guidance/55-CONTEXT.md
@.planning/phases/55-proof-adopter-guidance/55-RESEARCH.md
@.planning/phases/55-proof-adopter-guidance/55-VALIDATION.md
@.planning/phases/53-owner-erasure-contract-truth-gate/53-02-PLAN.md
@guides/user_flows.md
@guides/getting_started.md
@guides/operations.md
@test/install_smoke/docs_parity_test.exs
@test/adopter/canonical_app/lifecycle_test.exs
@lib/rindle.ex

<interfaces>
Required guide markers:

```text
preview_owner_erasure/2
erase_owner/2
retained shared assets
detach now, purge later
Rindle-managed associations
cleanup_orphans remains maintenance-only
```

Required deferred/non-goal markers:

```text
admin UI
bulk orchestration
force-delete
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade `guides/user_flows.md` into the canonical executable owner-erasure story</name>
  <files>guides/user_flows.md</files>
  <read_first>
    guides/user_flows.md
    test/adopter/canonical_app/lifecycle_test.exs
    .planning/phases/55-proof-adopter-guidance/55-CONTEXT.md
    .planning/phases/55-proof-adopter-guidance/55-RESEARCH.md
    lib/rindle.ex
  </read_first>
  <acceptance_criteria>
    - `rg -n 'preview_owner_erasure/2|erase_owner/2|retained shared assets|Rindle-managed|detach now, purge' guides/user_flows.md` returns matches.
    - `rg -n 'admin UI|bulk orchestration|force-delete' guides/user_flows.md` returns matches.
    - `! rg -n 'full executable facade lands in later|being standardized for v1.10' guides/user_flows.md` exits 0.
  </acceptance_criteria>
  <behavior>
    - Story 5 becomes a short executable owner-erasure section rather than a temporary note.
    - The guide teaches preview/execute flow, retained shared-asset behavior, and honest async purge semantics.
    - The guide states clearly that Rindle erases Rindle-managed associations for the owner, not the adopter’s account row.
  </behavior>
  <action>Rewrite the owner-erasure portion of `guides/user_flows.md` so it becomes the canonical supported account-deletion story now that the public facade exists and has proof coverage. Include a compact code snippet with `Rindle.preview_owner_erasure/2` and `Rindle.erase_owner/2`, explain the three semantic buckets in plain language, state that execute detaches now and enqueues purge for newly orphaned assets later, and explicitly say retained shared assets stay in storage when another live attachment survives. Keep the docs voice calm and exact. Do not widen into admin, bulk, or force-delete guidance, and do not imply Rindle deletes the adopter’s account row.</action>
  <verify>
    <automated>bash -lc 'rg -n "preview_owner_erasure/2|erase_owner/2|retained shared assets|Rindle-managed|detach now, purge" guides/user_flows.md && ! rg -n "full executable facade lands in later|being standardized for v1.10" guides/user_flows.md'</automated>
  </verify>
  <done>`guides/user_flows.md` becomes the single canonical owner-erasure guide surface and teaches the supported flow honestly.</done>
</task>

<task type="auto">
  <name>Task 2: Add thin pointer and boundary copy to getting-started and operations docs</name>
  <files>guides/getting_started.md, guides/operations.md</files>
  <read_first>
    guides/getting_started.md
    guides/operations.md
    guides/user_flows.md
    .planning/phases/55-proof-adopter-guidance/55-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg -n 'owner erasure|account deletion|user_flows|preview_owner_erasure|erase_owner' guides/getting_started.md guides/operations.md` returns matches.
    - `rg -n 'cleanup_orphans.*maintenance-only|maintenance-only.*cleanup_orphans' guides/operations.md` returns a match.
    - `! rg -n 'detach each of an owner' guides/getting_started.md guides/operations.md` exits 0.
  </acceptance_criteria>
  <behavior>
    - Getting-started adds only a short pointer toward the canonical owner-erasure flow.
    - Operations reinforces that `cleanup_orphans` is upload-residue maintenance, not the account-deletion API.
    - Neither guide duplicates the full semantic walkthrough from `guides/user_flows.md`.
  </behavior>
  <action>Update `guides/getting_started.md` with a brief "account deletion / owner erasure" pointer to `guides/user_flows.md`, keeping the file focused on first-run onboarding. Update `guides/operations.md` with a short boundary note that `mix rindle.cleanup_orphans` remains maintenance-only for expired upload residue and is not the supported owner/account erasure surface. Keep both edits thin and reference the canonical guide instead of restating the full preview/execute semantics.</action>
  <verify>
    <automated>bash -lc 'rg -n "owner erasure|account deletion|user_flows|preview_owner_erasure|erase_owner" guides/getting_started.md guides/operations.md && rg -n "cleanup_orphans.*maintenance-only|maintenance-only.*cleanup_orphans" guides/operations.md'</automated>
  </verify>
  <done>Getting-started and operations docs point to the supported surface and preserve the maintenance boundary without duplicating the full canonical story.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze guidance and planning truth with docs parity and active artifact updates</name>
  <files>test/install_smoke/docs_parity_test.exs, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    test/install_smoke/docs_parity_test.exs
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/STATE.md
    guides/user_flows.md
    .planning/phases/55-proof-adopter-guidance/55-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg -n 'preview_owner_erasure/2|erase_owner/2|maintenance-only|retained shared assets' test/install_smoke/docs_parity_test.exs` returns matches.
    - `rg -n 'supported account-deletion surface|PROOF-03|PROOF-04|TRUTH-02|cleanup_orphans' .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md` returns matches.
    - `mix test test/install_smoke/docs_parity_test.exs --seed 0` exits 0.
  </acceptance_criteria>
  <behavior>
    - Docs parity fails if the guide drifts back to workaround-first owner erasure wording.
    - Active planning artifacts describe owner erasure as the supported account-deletion surface instead of "later phase" or "standardizing" language.
    - Requirement traceability reflects proof and truth closure once the plan is executed.
  </behavior>
  <action>Extend `test/install_smoke/docs_parity_test.exs` so it freezes the canonical owner-erasure story, thin-pointer posture, and maintenance-only `cleanup_orphans` boundary. Then update `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, and `.planning/STATE.md` so they describe owner/account erasure as the supported surface backed by proof and guidance, and so the active traceability state can be marked consistently when the phase completes. Keep these planning-file edits factual and tightly scoped to the owner-erasure truth surface; do not start broader milestone cleanup here.</action>
  <verify>
    <automated>bash -lc 'mix test test/install_smoke/docs_parity_test.exs --seed 0 && rg -n "supported account-deletion surface|PROOF-03|PROOF-04|TRUTH-02|cleanup_orphans" .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md'</automated>
  </verify>
  <done>Support truth is frozen in CI and the active planning files speak about owner erasure the same way the proved public docs do.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical guide -> adopter expectations | Adopters will treat the guide text as the supported account-deletion contract |
| maintenance docs -> lifecycle contract | Operations wording can accidentally reintroduce `cleanup_orphans` as the wrong public surface |
| planning artifacts -> future milestone truth | Active roadmap/requirements/state copy shapes future planning and support messaging |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-04 | Tampering | `guides/user_flows.md` | mitigate | Make one canonical executable owner-erasure section with exact preview/execute and retained-shared-asset wording |
| T-55-05 | Repudiation | docs parity + planning files | mitigate | Freeze required strings and boundaries in docs parity and align active planning artifacts to the supported-now posture |
| T-55-06 | Information disclosure / confusion | `guides/operations.md` and `guides/getting_started.md` | mitigate | Keep pointer and boundary copy thin so those guides cannot drift into a second, inconsistent owner-erasure explanation |
</threat_model>

<verification>
Run `mix test test/install_smoke/docs_parity_test.exs --seed 0` and verify active planning-artifact wording with targeted `rg` checks.
</verification>

<success_criteria>
- `guides/user_flows.md` is the canonical executable owner-erasure story.
- Getting-started and operations docs point to the supported surface while keeping `cleanup_orphans` maintenance-only.
- Docs parity and active planning artifacts freeze owner/account erasure as the supported account-deletion surface.
</success_criteria>

<output>
After completion, create `.planning/phases/55-proof-adopter-guidance/55-02-SUMMARY.md`
</output>

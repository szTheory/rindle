---
phase: 55-proof-adopter-guidance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/rindle/owner_erasure_test.exs
  - test/rindle/api_surface_boundary_test.exs
  - test/adopter/canonical_app/lifecycle_test.exs
autonomous: true
requirements:
  - PROOF-03
  - PROOF-04
must_haves:
  truths:
    - "Hermetic owner-erasure proof closes the full public lifecycle: preview/execute semantics, orphan purge, retained shared assets, and idempotent reruns."
    - "Canonical adopter proof exercises `Rindle.preview_owner_erasure/2` and `Rindle.erase_owner/2` instead of teaching `detach/3` loops."
    - "Public-boundary tests continue to freeze the supported facade wording and semantic buckets while proof expands."
  artifacts:
    - path: "test/rindle/owner_erasure_test.exs"
      provides: "merge-blocking hermetic owner-erasure lifecycle proof"
    - path: "test/adopter/canonical_app/lifecycle_test.exs"
      provides: "canonical adopter owner-erasure lifecycle proof"
    - path: "test/rindle/api_surface_boundary_test.exs"
      provides: "public-boundary freeze for owner-erasure exports and wording"
  key_links:
    - from: "test/rindle/owner_erasure_test.exs"
      to: "lib/rindle/internal/owner_erasure.ex"
      via: "public preview/execute lifecycle semantics plus worker-time consequences"
      pattern: "preview_owner_erasure|erase_owner|retained_shared_assets|purge_enqueued"
    - from: "test/adopter/canonical_app/lifecycle_test.exs"
      to: "lib/rindle.ex"
      via: "canonical adopter proof of the supported account-deletion facade"
      pattern: "preview_owner_erasure|erase_owner"
    - from: "test/rindle/api_surface_boundary_test.exs"
      to: "lib/rindle.ex"
      via: "public-doc boundary freeze"
      pattern: "preview_owner_erasure/2|erase_owner/2|attachments_to_detach|retained_shared_assets"
---

<objective>
Close the proof gap for the shipped owner-erasure facade.

Purpose: satisfy `PROOF-03` and `PROOF-04` by proving the supported account-
deletion surface end to end in both hermetic and adopter-shaped lanes without
inventing new proof harnesses.
Output: expanded hermetic owner-erasure tests, canonical adopter lifecycle
coverage for preview/execute, and boundary checks that stay aligned with the
supported facade wording.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/55-proof-adopter-guidance/55-CONTEXT.md
@.planning/phases/55-proof-adopter-guidance/55-RESEARCH.md
@.planning/phases/55-proof-adopter-guidance/55-VALIDATION.md
@.planning/phases/54-execute-orphan-safe-purge-wiring/54-CONTEXT.md
@lib/rindle.ex
@lib/rindle/internal/owner_erasure.ex
@lib/rindle/workers/purge_storage.ex
@test/rindle/owner_erasure_test.exs
@test/rindle/workers/purge_storage_test.exs
@test/rindle/attach_detach_test.exs
@test/rindle/api_surface_boundary_test.exs
@test/adopter/canonical_app/lifecycle_test.exs

<interfaces>
Current public owner-erasure facade:

```elixir
@spec preview_owner_erasure(struct(), keyword()) ::
        {:ok, owner_erasure_report()} | {:error, term()}
@spec erase_owner(struct(), keyword()) ::
        {:ok, owner_erasure_report()} | {:error, term()}
```

Current report vocabulary:

```elixir
%{
  mode: :preview | :execute,
  attachments_to_detach: %{count: non_neg_integer(), entries: [map()]},
  assets_to_purge: %{count: non_neg_integer(), entries: [map()]},
  retained_shared_assets: %{count: non_neg_integer(), entries: [map()]},
  purge_enqueued: non_neg_integer(),
  purge_already_queued: non_neg_integer()
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend hermetic owner-erasure proof to cover worker-time purge versus retained shared assets</name>
  <files>test/rindle/owner_erasure_test.exs</files>
  <read_first>
    .planning/phases/55-proof-adopter-guidance/55-CONTEXT.md
    .planning/phases/55-proof-adopter-guidance/55-VALIDATION.md
    lib/rindle/internal/owner_erasure.ex
    lib/rindle/workers/purge_storage.ex
    test/rindle/owner_erasure_test.exs
    test/rindle/workers/purge_storage_test.exs
    test/rindle/attach_detach_test.exs
  </read_first>
  <acceptance_criteria>
    - `rg -n 'perform_job\\(PurgeStorage|retained_shared_assets|purge_enqueued|purge_already_queued' test/rindle/owner_erasure_test.exs` returns matches.
    - `rg -n 'shared asset|orphan' test/rindle/owner_erasure_test.exs` returns explicit lifecycle assertions for both branches.
    - `mix test test/rindle/owner_erasure_test.exs --seed 0` exits 0.
  </acceptance_criteria>
  <behavior>
    - Hermetic proof covers preview and execute report semantics through the public facade.
    - After `erase_owner/2`, the orphaned asset can be purged through the real worker path while the retained shared asset remains present because another attachment survives.
    - Reruns remain stable no-op success after the initial erase path and any resulting worker activity.
  </behavior>
  <action>Extend `test/rindle/owner_erasure_test.exs` in place rather than creating a new proof file. Keep the current report-shape and enqueue-conflict assertions, then add a lifecycle-closing test that calls `Rindle.erase_owner/2`, performs the enqueued `PurgeStorage` job for the orphaned asset, and asserts the orphaned asset row is deleted while the shared asset row and surviving attachment remain. Also keep the proof honest about async semantics: assert purge is enqueued first, then prove deletion or retention only after the worker runs. Do not re-test internal query implementation details or add a bespoke proof harness.</action>
  <verify>
    <automated>mix test test/rindle/owner_erasure_test.exs --seed 0</automated>
  </verify>
  <done>Hermetic proof now closes the owner-erasure lifecycle from preview/execute report semantics through worker-time orphan purge and retained shared-asset survival.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add canonical adopter proof for the public owner-erasure flow</name>
  <files>test/adopter/canonical_app/lifecycle_test.exs</files>
  <read_first>
    .planning/phases/55-proof-adopter-guidance/55-CONTEXT.md
    .planning/phases/55-proof-adopter-guidance/55-RESEARCH.md
    .planning/phases/55-proof-adopter-guidance/55-VALIDATION.md
    lib/rindle.ex
    test/adopter/canonical_app/lifecycle_test.exs
    test/rindle/owner_erasure_test.exs
  </read_first>
  <acceptance_criteria>
    - `rg -n 'preview_owner_erasure|erase_owner|retained_shared_assets|assets_to_purge' test/adopter/canonical_app/lifecycle_test.exs` returns matches.
    - `! rg -n 'detach each of an owner|cleanup_orphans.*account-deletion|cleanup_orphans.*owner-erasure' test/adopter/canonical_app/lifecycle_test.exs` exits 0.
    - `mix test test/adopter/canonical_app/lifecycle_test.exs --seed 0` exits 0.
  </acceptance_criteria>
  <behavior>
    - The canonical adopter lane proves the supported account-deletion flow through `preview_owner_erasure/2` and `erase_owner/2`.
    - The proof includes one orphaned asset and one retained shared asset so the returned report matches the public semantics adopters are taught.
    - The adopter-shaped test keeps using the configured adopter Repo and the existing outside-in harness.
  </behavior>
  <action>Add a focused owner-erasure chapter to `test/adopter/canonical_app/lifecycle_test.exs`. Use the existing canonical adopter harness and Repo override, create at least one owner with an orphaned asset and one shared asset, call `Rindle.preview_owner_erasure/2` to assert the report buckets, then call `Rindle.erase_owner/2` and assert attachment removal plus worker enqueue semantics. If practical within the adopter harness, also perform the orphan purge job and assert the shared asset remains available to the surviving owner. Keep the proof public-surface based and adopter-shaped; do not bypass through internal modules or teach `detach/3` loops as the account-deletion recipe.</action>
  <verify>
    <automated>mix test test/adopter/canonical_app/lifecycle_test.exs --seed 0</automated>
  </verify>
  <done>The canonical adopter lane proves the supported owner-erasure facade as the real account-deletion flow in an adopter-shaped environment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Keep public-boundary proof aligned with the owner-erasure contract wording</name>
  <files>test/rindle/api_surface_boundary_test.exs</files>
  <read_first>
    .planning/phases/55-proof-adopter-guidance/55-CONTEXT.md
    lib/rindle.ex
    test/rindle/api_surface_boundary_test.exs
    test/rindle/owner_erasure_test.exs
  </read_first>
  <acceptance_criteria>
    - `rg -n 'preview_owner_erasure/2|erase_owner/2|attachments_to_detach|assets_to_purge|retained_shared_assets|maintenance-only' test/rindle/api_surface_boundary_test.exs` returns matches.
    - `mix test test/rindle/api_surface_boundary_test.exs test/rindle/owner_erasure_test.exs --seed 0` exits 0.
  </acceptance_criteria>
  <behavior>
    - Boundary tests still prove that the owner-erasure facade is publicly exported and documented.
    - Boundary wording remains aligned with the semantic bucket vocabulary and maintenance-only `cleanup_orphans` boundary while proof files expand.
  </behavior>
  <action>Tighten `test/rindle/api_surface_boundary_test.exs` only where needed so the public-boundary layer keeps freezing the owner-erasure facade and wording as proof lands. Prefer additive assertions over rewrites. Keep the test scoped to exported functions and compiled-doc wording; do not duplicate the deeper lifecycle assertions from `test/rindle/owner_erasure_test.exs`.</action>
  <verify>
    <automated>mix test test/rindle/api_surface_boundary_test.exs test/rindle/owner_erasure_test.exs --seed 0</automated>
  </verify>
  <done>Public-boundary proof remains the lightweight freeze for exported owner-erasure surface and wording while the hermetic and adopter lanes carry the semantic depth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public facade -> destructive worker path | Account-deletion proof must distinguish preview/execute semantics from worker-time byte deletion |
| adopter lane -> canonical guidance | The outside-in proof becomes the practical truth source for the docs and must exercise the supported facade directly |
| boundary tests -> public docs | Exported API wording can drift even if runtime behavior still passes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01 | Tampering | owner-erasure hermetic proof | mitigate | Prove the worker-time orphan purge and shared-asset retention path after `erase_owner/2`, not just the enqueue report |
| T-55-02 | Repudiation | owner-erasure report semantics | mitigate | Keep assertions on the public semantic buckets and rerun stability so detach/purge intent stays auditable |
| T-55-03 | Repudiation | canonical adopter lane | mitigate | Make the adopter proof call `preview_owner_erasure/2` and `erase_owner/2` directly instead of teaching fallback detach-loop behavior |
</threat_model>

<verification>
Run `mix test test/rindle/owner_erasure_test.exs test/rindle/api_surface_boundary_test.exs test/adopter/canonical_app/lifecycle_test.exs --seed 0`.
</verification>

<success_criteria>
- Hermetic owner-erasure proof covers orphan purge, retained shared assets, and rerun stability.
- Canonical adopter proof exercises `preview_owner_erasure/2` and `erase_owner/2` as the supported account-deletion flow.
- Public-boundary tests remain aligned with the owner-erasure facade wording and semantic buckets.
</success_criteria>

<output>
After completion, create `.planning/phases/55-proof-adopter-guidance/55-01-SUMMARY.md`
</output>

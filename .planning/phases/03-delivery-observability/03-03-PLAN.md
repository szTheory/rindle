---
phase: 03-delivery-observability
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - lib/rindle/html.ex
  - test/rindle/html_test.exs
autonomous: true
requirements:
  - VIEW-01
  - VIEW-02
  - VIEW-03
  - VIEW-04
must_haves:
  truths:
    - `picture_tag/3` renders a `<picture>` element with a source per requested ready variant.
    - `srcset` only includes ready variants and falls back to the original asset or placeholder when needed.
    - Standard HTML attributes pass through to the rendered `<img>` element unchanged.
    - Non-ready variants never produce broken delivery URLs in the rendered markup.
  artifacts:
    - path: lib/rindle/html.ex
      provides: Phoenix template helper for responsive image markup
      min_lines: 120
      exports: ["picture_tag/3"]
    - path: test/rindle/html_test.exs
      provides: Helper contract tests for picture rendering
      min_lines: 120
      contains: "picture_tag"
  key_links:
    - from: lib/rindle/html.ex
      to: lib/rindle/delivery.ex
      via: variant URL resolution
      pattern: "Rindle\\.Delivery\\.variant_url"
    - from: lib/rindle/html.ex
      to: lib/rindle/domain/media_variant.ex
      via: ready-state filtering
      pattern: "state"
---

<objective>
Render responsive image markup that only uses safe, ready variants and keeps fallback behavior predictable for Phoenix templates.

Purpose: Make the library ergonomic for adopters without exposing broken or speculative variant URLs.
Output: A thin `picture_tag/3` helper plus tests for source ordering, placeholders, and HTML attribute passthrough.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-delivery-observability/03-CONTEXT.md

<interfaces>
From lib/rindle/domain/media_variant.ex:
```elixir
field :state, :string, default: "planned"
field :storage_key, :string
field :content_type, :string
field :generated_at, :utc_datetime_usec
```

From lib/rindle/domain/stale_policy.ex:
```elixir
@type stale_mode :: :serve_stale | :fallback_original
def resolve_stale_variant(stale_mode, variant_state, original_url) do
```

From lib/rindle/delivery.ex (created in plan 03-01):
```elixir
def url(profile, key, opts \\ []) do
def variant_url(profile, asset, variant, opts \\ []) do
```

From lib/rindle/profile.ex:
```elixir
@spec variants() :: [Rindle.Profile.variant_entry()]
def variants do
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the picture helper contract</name>
  <files>lib/rindle/html.ex</files>
  <behavior>
    - `picture_tag/3` accepts an explicit variant/source ordering option and does not invent breakpoints.
    - The helper is thin: it only renders markup and delegates URL resolution to the delivery layer.
    - The helper keeps placeholder behavior presentational and side-effect free.
  </behavior>
  <action>Create the `Rindle.HTML` helper module and its public contract for `picture_tag/3`. Keep the interface explicit about variant ordering, placeholder behavior, and passthrough HTML attributes so downstream callers do not need to guess the shape.</action>
  <verify><automated>mix test test/rindle/html_test.exs</automated></verify>
  <done>The helper module exists with a stable API that the rendering task can implement against.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render responsive markup with safe fallbacks</name>
  <files>lib/rindle/html.ex, test/rindle/html_test.exs</files>
  <behavior>
    - `<picture>` output includes ordered `<source>` elements and a final `<img>` fallback.
    - Only ready variants participate in `srcset`; non-ready variants fall back to the original asset or placeholder.
    - Standard HTML attributes (`alt`, `class`, `loading`, etc.) pass through untouched and remain Phoenix-safe.
  </behavior>
  <action>Implement the helper to build `<picture>` markup from a preloaded asset and the explicit variant order supplied by callers. Resolve URLs through the delivery layer, filter by ready state, and ensure placeholder/original fallback behavior never emits a broken variant URL.</action>
  <verify><automated>mix test test/rindle/html_test.exs</automated></verify>
  <done>The helper renders safe responsive image markup that matches the locked output contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template author → `Rindle.HTML.picture_tag/3` | Callers choose variant order and HTML attributes that become rendered markup |
| `Rindle.HTML` → delivery layer | The helper converts preloaded domain state into delivery URLs and must not emit broken links |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | I | `lib/rindle/html.ex` | mitigate | Filter to ready variants only and fall back to the original asset or placeholder for anything else. |
| T-03-08 | T | `lib/rindle/html.ex` | mitigate | Render through Phoenix-safe HTML builders and escape all caller-provided attributes. |
| T-03-09 | D | `lib/rindle/html.ex` | accept | The helper is intentionally thin; any expensive variant lookup behavior is limited by the preloaded asset and the delivery layer contract. |
</threat_model>

<verification>
- `mix test test/rindle/html_test.exs` passes with source-order, placeholder, and passthrough assertions.
- The rendered `<picture>` never includes a broken non-ready variant URL.
- `Rindle.HTML.picture_tag/3` stays thin and delegates URL resolution to delivery helpers.
</verification>

<success_criteria>
- Phoenix templates can render responsive media without custom branching.
- `picture_tag/3` emits ready-only variant sources in explicit order.
- Placeholder and standard HTML attributes behave predictably.
- Broken or speculative variant URLs never appear in generated markup.
</success_criteria>

<output>
After completion, create `.planning/phases/03-delivery-observability/03-03-SUMMARY.md`
</output>

---
phase: 03-delivery-observability
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/delivery.ex
  - lib/rindle/profile.ex
  - lib/rindle/profile/validator.ex
  - lib/rindle.ex
  - test/rindle/delivery_test.exs
  - test/rindle/profile/profile_test.exs
autonomous: true
requirements:
  - DELV-01
  - DELV-02
  - DELV-03
  - DELV-04
  - DELV-05
  - DELV-06
must_haves:
  truths:
    - Private-profile assets return expiring signed URLs by default.
    - Public opt-in profiles return unsigned storage URLs.
    - Delivery refuses private URLs when the adapter cannot sign GET requests.
    - Non-ready variants fall back to the original asset URL instead of raising.
    - Delivery authorization runs before any URL is issued.
  artifacts:
    - path: lib/rindle/delivery.ex
      provides: Delivery policy and URL resolution
      min_lines: 120
      exports: ["url/2", "variant_url/4"]
    - path: lib/rindle/profile.ex
      provides: Delivery policy fields on profile modules
      min_lines: 80
      contains: "delivery"
    - path: lib/rindle/profile/validator.ex
      provides: Compile-time validation for delivery options
      min_lines: 240
      contains: "signed_url_ttl_seconds"
    - path: lib/rindle.ex
      provides: Facade delegation for delivery URL generation
      min_lines: 220
      contains: "def url("
  key_links:
    - from: lib/rindle.ex
      to: lib/rindle/delivery.ex
      via: facade delegation
      pattern: "Rindle\\.Delivery\\.url"
    - from: lib/rindle/delivery.ex
      to: Rindle.Authorizer
      via: authorize/3 before issuance
      pattern: "authorize\\("
    - from: lib/rindle/delivery.ex
      to: Rindle.Storage
      via: url/presigned_put capability checks
      pattern: "(url|presigned_put|capabilities)\\("
---

<objective>
Ship private-by-default delivery that signs URLs when required, respects explicit public opt-in, and safely falls back for non-ready variants.

Purpose: Make secure delivery the default without exposing broken media URLs.
Output: Delivery policy module, profile policy fields, facade delegation, and contract tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-delivery-observability/03-CONTEXT.md

<interfaces>
From lib/rindle.ex:
```elixir
@spec url(module(), String.t(), keyword()) :: storage_result()
def url(profile, key, opts \\ []) do
  invoke_storage(profile, :url, [key, opts])
end
```

From lib/rindle/storage.ex:
```elixir
@callback url(key :: String.t(), opts :: keyword()) :: {:ok, String.t()} | {:error, term()}
@callback presigned_put(key :: String.t(), expires_in :: pos_integer(), opts :: keyword()) :: {:ok, map()} | {:error, term()}
@callback capabilities() :: [atom()]
```

From lib/rindle/authorizer.ex:
```elixir
@callback authorize(actor :: term(), action :: atom(), subject :: term()) ::
            :ok | {:error, :unauthorized | term()}
```

From lib/rindle/domain/stale_policy.ex:
```elixir
@spec resolve_stale_variant(stale_mode(), String.t(), String.t()) ::
        {:serve_variant, :stale} | {:serve_original, String.t()}
```

From lib/rindle/profile.ex:
```elixir
@spec storage_adapter() :: module()
def storage_adapter, do: @rindle_storage
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define delivery policy contracts</name>
  <files>lib/rindle/delivery.ex, lib/rindle/profile.ex, lib/rindle/profile/validator.ex</files>
  <behavior>
    - Profile modules accept explicit delivery policy fields and reject unknown delivery values at compile time.
    - Delivery helpers expose policy lookup and variant-fallback decisions without letting storage adapters decide policy.
    - Private-by-default remains the default profile posture unless a profile opts into public delivery.
  </behavior>
  <action>Create the delivery policy surface and profile schema support for private/public delivery, signed-URL TTL, and delivery authorization hooks. Keep adapter modules as primitives only; policy stays in the delivery layer per D-03.</action>
  <verify><automated>mix test test/rindle/profile/profile_test.exs</automated></verify>
  <done>Profiles compile with delivery options, invalid delivery options fail fast, and the new delivery contract module is available for facade wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire signed and public URL resolution</name>
  <files>lib/rindle.ex, lib/rindle/delivery.ex, test/rindle/delivery_test.exs</files>
  <behavior>
    - `Rindle.url/2` routes through the delivery layer instead of calling storage adapters directly.
    - Private profiles return signed URLs with the configured expiry; public profiles return unsigned storage URLs.
    - Non-ready variants resolve to the original asset URL instead of raising or emitting broken links.
  </behavior>
  <action>Implement the delivery resolver and facade delegation. Enforce authorizer checks before issuing any URL, honor adapter capability checks for signed delivery, and use stale-policy decisions to fall back to the original asset when the variant is not ready.</action>
  <verify><automated>mix test test/rindle/delivery_test.exs</automated></verify>
  <done>URL issuance follows private-by-default policy, public opt-in works, missing signed capability is rejected, and variant fallback behavior is locked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock delivery regressions in tests</name>
  <files>test/rindle/delivery_test.exs, test/rindle/profile/profile_test.exs</files>
  <behavior>
    - Contract tests cover private TTL behavior, public opt-in, authorizer gating, and capability failures.
    - Profile tests prove delivery options compile and remain stable under macro validation.
    - Test fixtures use mocks or local adapter stubs rather than real storage services.
  </behavior>
  <action>Expand the public contract tests to assert the signed-url expiry, public URL path, authorizer guard, and variant fallback behavior from the user-facing API. Update profile regression coverage so delivery options remain part of the compile-time profile contract.</action>
  <verify><automated>mix test test/rindle/delivery_test.exs test/rindle/profile/profile_test.exs</automated></verify>
  <done>The delivery API and profile DSL are locked by automated tests that prove the secure default and public opt-in behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template / host app → `Rindle.url/2` | Untrusted callers request delivery URLs and choose whether to surface assets publicly |
| `Rindle.Delivery` → storage adapter | Capability checks and URL signing cross from policy into backend primitives |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | S | `lib/rindle/delivery.ex` | mitigate | Require `authorize/3` before URL issuance and keep public opt-in explicit on the profile contract. |
| T-03-02 | T | `lib/rindle/delivery.ex` | mitigate | Derive expiry from profile/config, never from caller input, and reject adapters that cannot satisfy the requested delivery mode. |
| T-03-03 | I | `lib/rindle/delivery.ex` | mitigate | Fall back to the original asset for non-ready variants so HTML helpers never emit broken or misleading variant URLs. |
</threat_model>

<verification>
- `mix test test/rindle/profile/profile_test.exs` passes after profile contract updates.
- `mix test test/rindle/delivery_test.exs` passes after delivery wiring.
- `Rindle.url/2` yields signed or unsigned URLs according to the profile policy, with variant fallback preserved.
</verification>

<success_criteria>
- `Rindle.url/2` signs private delivery URLs and leaves public URLs unsigned.
- Profiles can opt into public delivery explicitly and set signed URL TTLs.
- Non-ready variants never produce broken delivery URLs.
- Delivery authorization is enforced before URL issuance.
</success_criteria>

<output>
After completion, create `.planning/phases/03-delivery-observability/03-01-SUMMARY.md`
</output>

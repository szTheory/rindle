---
phase: 92-e2e-screenshot-driven-polish-loop
plan: "03"
type: execute
wave: 2
depends_on:
  - "92-01"
files_modified:
  - examples/adoption_demo/e2e/admin-actions.spec.js
autonomous: true
requirements:
  - E2E-01
must_haves:
  truths:
    - "D-92-01: Destructive and non-destructive admin action specs run in the existing adoption_demo Playwright harness."
    - "D-92-04: Action specs use data-rindle-admin-* selectors from Plan 01."
    - "Owner erasure and batch erasure prove collateral preview, wrong confirmation blocking, exact typed confirmation, and receipts."
    - "Lifecycle repair, variant regeneration, and quarantine review prove their Phase 90 browser behavior without adding new lifecycle semantics."
  artifacts:
    - path: "examples/adoption_demo/e2e/admin-actions.spec.js"
      provides: "Admin action browser-flow coverage"
      contains: "data-rindle-admin-action"
  key_links:
    - from: "examples/adoption_demo/e2e/admin-actions.spec.js"
      to: "examples/adoption_demo/e2e/support/admin.js"
      via: "shared helper import"
      pattern: "require\\(\"./support/admin\"\\)"
    - from: "examples/adoption_demo/e2e/admin-actions.spec.js"
      to: "examples/adoption_demo/e2e/support/cohort.js"
      via: "seeded member IDs"
      pattern: "memberId"
    - from: "examples/adoption_demo/e2e/admin-actions.spec.js"
      to: "lib/rindle/admin/live/actions_live.ex"
      via: "action selectors and receipts"
      pattern: "data-rindle-admin-receipt"
---

<objective>
Add deterministic Playwright coverage for admin destructive and operational action flows.

Purpose: E2E-01 explicitly requires destructive flows and main error cases to be merge-blocking, and Phase 90 action behavior should be proven in the live adoption demo.
Output: `admin-actions.spec.js` covering owner erasure, batch erasure, lifecycle repair, variant regeneration, and read-only quarantine review.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/92-e2e-screenshot-driven-polish-loop/92-CONTEXT.md
@.planning/phases/92-e2e-screenshot-driven-polish-loop/92-RESEARCH.md
@.planning/phases/92-e2e-screenshot-driven-polish-loop/92-VALIDATION.md
@.planning/phases/92-e2e-screenshot-driven-polish-loop/92-PATTERNS.md
@.planning/phases/92-e2e-screenshot-driven-polish-loop/92-UI-SPEC.md
@guides/ui_principles.md
@examples/adoption_demo/AGENTS.md
@examples/adoption_demo/e2e/support/admin.js
@examples/adoption_demo/e2e/support/cohort.js
@examples/adoption_demo/e2e/owner-erasure.spec.js
@examples/adoption_demo/e2e/batch-erasure.spec.js
@examples/adoption_demo/priv/repo/seeds.exs
@lib/rindle/admin/live/actions_live.ex
@test/rindle/admin/live/actions_live_test.exs
@.planning/phases/90-console-ops-actions/90-01-SUMMARY.md
@.planning/phases/90-console-ops-actions/90-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add destructive action browser coverage</name>
  <files>examples/adoption_demo/e2e/admin-actions.spec.js</files>
  <read_first>
    examples/adoption_demo/e2e/admin-actions.spec.js
    examples/adoption_demo/e2e/support/admin.js
    examples/adoption_demo/e2e/support/cohort.js
    examples/adoption_demo/e2e/owner-erasure.spec.js
    examples/adoption_demo/e2e/batch-erasure.spec.js
    examples/adoption_demo/priv/repo/seeds.exs
    lib/rindle/admin/live/actions_live.ex
    test/rindle/admin/live/actions_live_test.exs
    .planning/phases/92-e2e-screenshot-driven-polish-loop/92-RESEARCH.md
    .planning/phases/92-e2e-screenshot-driven-polish-loop/92-UI-SPEC.md
  </read_first>
  <behavior>
    - Owner erasure preview for `AdoptionDemo.Accounts.Member` and the seeded `alex@cohort.test` ID renders `[data-rindle-admin-preview="owner_erasure"]` and collateral copy before any execution.
    - Wrong owner-erasure confirmation `wrong` keeps `[data-rindle-admin-state="preview"]` visible and surfaces `Confirmation does not match.`.
    - Exact owner-erasure confirmation `ERASE Elixir.String:<uuid>` reaches `[data-rindle-admin-receipt="owner_erasure"]` using a generated non-shared owner ID.
    - Batch erasure preview renders `[data-rindle-admin-preview="batch_erasure"]`, owner count copy, and attachment collateral copy.
    - Wrong batch confirmation keeps preview visible; exact `ERASE 2 OWNERS` for two generated `Elixir.String:<uuid>` owners reaches `[data-rindle-admin-receipt="batch_erasure"]`.
  </behavior>
  <action>Create `examples/adoption_demo/e2e/admin-actions.spec.js` with Playwright `test` and `expect`, importing `visitAdmin`, `expectAdminShell`, `expectNoAdminRawSecrets`, and `expectNoHorizontalScroll` from `./support/admin`, plus `MEMBERS` and `memberId` from `./support/cohort`. Use `visitAdmin(page, "actions")` for all admin action flows. Select tabs with `[data-rindle-admin-action="owner_erasure"]` and `[data-rindle-admin-action="batch_erasure"]`. Fill forms by `data-rindle-admin-form`, `data-rindle-admin-input`, and `data-rindle-admin-confirm-input` selectors. Use `AdoptionDemo.Accounts.Member:${alexId}` only for preview coverage and never execute Alex's erasure. Use generated `Elixir.String:${crypto.randomUUID()}` IDs for execute coverage so shared seeded demo records remain intact. Assert wrong-confirmation copy exactly `Confirmation does not match.` and assert receipts. Do not use `data-testid`, `waitForTimeout`, or text-only locators when a Plan 01 selector exists.</action>
  <verify>
    <automated>cd examples/adoption_demo && npx playwright test e2e/admin-actions.spec.js --grep "owner|batch"</automated>
    <automated>rg -n "data-testid|waitForTimeout" examples/adoption_demo/e2e/admin-actions.spec.js && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    Source assertion: `examples/adoption_demo/e2e/admin-actions.spec.js` contains `data-rindle-admin-action="owner_erasure"`, `data-rindle-admin-action="batch_erasure"`, `data-rindle-admin-preview="owner_erasure"`, `data-rindle-admin-preview="batch_erasure"`, `data-rindle-admin-receipt="owner_erasure"`, and `data-rindle-admin-receipt="batch_erasure"`.
    Source assertion: the spec contains `AdoptionDemo.Accounts.Member` for preview and `Elixir.String` for execute coverage.
    Source assertion: the spec contains the exact text `Confirmation does not match.`.
    Source assertion: `rg -n "data-testid|waitForTimeout" examples/adoption_demo/e2e/admin-actions.spec.js` returns no matches.
    Behavior assertion: targeted destructive Playwright grep exits 0.
  </acceptance_criteria>
  <done>Destructive owner and batch erasure browser flows prove preview, wrong confirmation, exact confirmation, receipt, and fixture-safety behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add non-destructive action browser coverage</name>
  <files>examples/adoption_demo/e2e/admin-actions.spec.js</files>
  <read_first>
    examples/adoption_demo/e2e/admin-actions.spec.js
    examples/adoption_demo/e2e/support/admin.js
    examples/adoption_demo/priv/repo/seeds.exs
    lib/rindle/admin/live/actions_live.ex
    lib/rindle/admin/live/assets_live.ex
    test/rindle/admin/live/actions_live_test.exs
    .planning/phases/90-console-ops-actions/90-02-SUMMARY.md
    .planning/phases/92-e2e-screenshot-driven-polish-loop/92-UI-SPEC.md
  </read_first>
  <behavior>
    - Lifecycle repair selects `[data-rindle-admin-action="lifecycle_repair"]`, supplies a seeded asset ID, executes `reprobe`, and renders `[data-rindle-admin-receipt="lifecycle_repair"]`.
    - Variant regeneration selects `[data-rindle-admin-action="variant_regeneration"]`, proves missing confirmation surfaces `You must confirm this action`, then checks confirmation and renders `[data-rindle-admin-receipt="variant_regeneration"]`.
    - Quarantine review selects `[data-rindle-admin-action="quarantine_review"]`, renders `[data-rindle-admin-panel="quarantine_review"]`, and has no submit control inside that panel.
    - The action page remains horizontally stable and does not expose raw provider/session secrets.
  </behavior>
  <action>Extend `examples/adoption_demo/e2e/admin-actions.spec.js` in the same file. Get a deterministic seeded asset ID by visiting the assets surface through `firstAdminDetailHref(page, "asset")` or by extracting the first asset detail URL, then return to `actions`. For lifecycle repair, select the `lifecycle_repair` action, fill the asset ID input, select `reprobe`, submit via `data-rindle-admin-submit="execute_lifecycle_repair"`, and assert `data-rindle-admin-receipt="lifecycle_repair"`. For variant regeneration, first submit without checking `data-rindle-admin-input="variant-confirm"` and assert `You must confirm this action`; then check it, submit, and assert `data-rindle-admin-receipt="variant_regeneration"`. For quarantine review, assert the read-only panel and assert the panel has zero descendants matching `button[type="submit"], input[type="submit"]`. Call `expectNoAdminRawSecrets(page)` and `expectNoHorizontalScroll(page)` in these flows. Do not introduce a quarantine mutation.</action>
  <verify>
    <automated>cd examples/adoption_demo && npx playwright test e2e/admin-actions.spec.js --grep "lifecycle|variant|quarantine"</automated>
    <automated>cd examples/adoption_demo && npx playwright test e2e/admin-actions.spec.js</automated>
  </verify>
  <acceptance_criteria>
    Source assertion: `examples/adoption_demo/e2e/admin-actions.spec.js` contains `data-rindle-admin-action="lifecycle_repair"`, `data-rindle-admin-action="variant_regeneration"`, `data-rindle-admin-action="quarantine_review"`, `data-rindle-admin-receipt="lifecycle_repair"`, `data-rindle-admin-receipt="variant_regeneration"`, and `data-rindle-admin-panel="quarantine_review"`.
    Source assertion: the spec contains the exact text `You must confirm this action`.
    Behavior assertion: targeted non-destructive Playwright grep exits 0.
    Behavior assertion: full `admin-actions.spec.js` exits 0.
  </acceptance_criteria>
  <done>Non-destructive admin action flows and read-only quarantine review are covered in-browser without adding new operation semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser test runner -> destructive admin forms | Playwright submits owner erasure and batch erasure actions to the live app. |
| Seeded demo fixture data -> destructive operations | Tests can mutate shared demo records if owner IDs are chosen incorrectly. |
| Admin receipts -> CI logs/traces | Receipts and failure messages can expose raw owner/provider/session values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-09 | Tampering | `admin-actions.spec.js` destructive execution | mitigate | Task 1 executes only generated `Elixir.String:<uuid>` owners; seeded `alex@cohort.test` is preview-only. |
| T-92-10 | Information Disclosure | Action previews and receipts | mitigate | Both tasks call `expectNoAdminRawSecrets(page)` and assert redaction-sensitive flows without raw provider/session values. |
| T-92-11 | Elevation of Privilege | Quarantine review | mitigate | Task 2 asserts quarantine review has no submit controls and does not add an un-quarantine mutation. |
| T-92-12 | Repudiation | Destructive receipt assertions | mitigate | Task 1 asserts explicit receipt selectors for owner and batch erasure so CI records the final action state. |
| T-92-13 | Denial of Service | Action spec order and fixture state | mitigate | Task 1 uses generated owners for execution and avoids shared-fixture erasure, keeping later specs deterministic. |
</threat_model>

<verification>
Run the targeted destructive grep, targeted non-destructive grep, and the full `admin-actions.spec.js` Playwright file from `examples/adoption_demo`.
</verification>

<success_criteria>
Plan succeeds when admin destructive and non-destructive action flows pass in the adoption demo Playwright harness, preserve shared fixture data, and prove exact confirmation/error/receipt behavior.
</success_criteria>

<output>
Create `.planning/phases/92-e2e-screenshot-driven-polish-loop/92-03-SUMMARY.md` when done.
</output>

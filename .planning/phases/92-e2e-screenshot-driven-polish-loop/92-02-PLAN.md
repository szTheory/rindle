---
phase: 92-e2e-screenshot-driven-polish-loop
plan: "02"
type: execute
wave: 2
depends_on:
  - "92-01"
files_modified:
  - examples/adoption_demo/e2e/admin-console.spec.js
  - examples/adoption_demo/e2e/admin-theme.spec.js
autonomous: true
requirements:
  - E2E-01
must_haves:
  truths:
    - "D-92-01: Admin console happy/error/boundary/theme specs run in the adoption_demo Playwright harness."
    - "D-92-03: Theme tests click the app theme picker for light, dark, and auto."
    - "D-92-04: Admin console specs use data-rindle-admin-* selectors and the shared helper from Plan 01."
    - "Users can navigate every admin console surface, open seeded detail pages, see empty/error boundaries, and toggle themes deterministically."
  artifacts:
    - path: "examples/adoption_demo/e2e/admin-console.spec.js"
      provides: "Surface, boundary, detail, and redaction Playwright coverage"
      contains: "expectAdminShell"
    - path: "examples/adoption_demo/e2e/admin-theme.spec.js"
      provides: "Theme picker Playwright coverage"
      contains: "selectAdminTheme"
  key_links:
    - from: "examples/adoption_demo/e2e/admin-console.spec.js"
      to: "examples/adoption_demo/e2e/support/admin.js"
      via: "shared helper import"
      pattern: "require\\(\"./support/admin\"\\)"
    - from: "examples/adoption_demo/e2e/admin-theme.spec.js"
      to: "priv/static/rindle_admin/rindle-admin.js"
      via: "theme picker data-theme behavior"
      pattern: "selectAdminTheme"
---

<objective>
Add deterministic Playwright coverage for admin console navigation, boundary states, details, redaction, and theme switching.

Purpose: E2E-01 requires happy paths, main error cases, boundary conditions, and theme switching in the live adoption demo, not only LiveView unit tests.
Output: `admin-console.spec.js` and `admin-theme.spec.js` under the existing adoption demo Playwright suite.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/92-e2e-screenshot-driven-polish-loop/92-CONTEXT.md
@.planning/phases/92-e2e-screenshot-driven-polish-loop/92-RESEARCH.md
@.planning/phases/92-e2e-screenshot-driven-polish-loop/92-VALIDATION.md
@.planning/phases/92-e2e-screenshot-driven-polish-loop/92-PATTERNS.md
@.planning/phases/92-e2e-screenshot-driven-polish-loop/92-UI-SPEC.md
@RUNNING.md
@guides/ui_principles.md
@examples/adoption_demo/AGENTS.md
@examples/adoption_demo/playwright.config.js
@examples/adoption_demo/e2e/support/admin.js
@examples/adoption_demo/e2e/support/liveview.js
@examples/adoption_demo/priv/repo/seeds.exs
@examples/adoption_demo/lib/adoption_demo_web/router.ex
@priv/static/rindle_admin/rindle-admin.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add admin surface and boundary Playwright spec</name>
  <files>examples/adoption_demo/e2e/admin-console.spec.js</files>
  <read_first>
    examples/adoption_demo/e2e/admin-console.spec.js
    examples/adoption_demo/e2e/support/admin.js
    examples/adoption_demo/e2e/ops-surfaces.spec.js
    examples/adoption_demo/playwright.config.js
    examples/adoption_demo/priv/repo/seeds.exs
    examples/adoption_demo/lib/adoption_demo_web/router.ex
    lib/rindle/admin/live/assets_live.ex
    lib/rindle/admin/live/upload_sessions_live.ex
    lib/rindle/admin/live/variants_jobs_live.ex
    lib/rindle/admin/live/runtime_doctor_live.ex
    test/rindle/admin/live/home_assets_upload_test.exs
    test/rindle/admin/live/variants_runtime_actions_test.exs
    .planning/phases/92-e2e-screenshot-driven-polish-loop/92-RESEARCH.md
    .planning/phases/92-e2e-screenshot-driven-polish-loop/92-PATTERNS.md
  </read_first>
  <behavior>
    - Visiting `/admin/rindle`, `/admin/rindle/assets`, `/admin/rindle/upload-sessions`, `/admin/rindle/variants-jobs`, `/admin/rindle/runtime-doctor`, and `/admin/rindle/actions` renders the shell with the expected surface slug.
    - Asset and upload-session list pages expose seeded rows through `[data-rindle-admin-row="asset"]` and `[data-rindle-admin-row="upload-session"]`.
    - First seeded asset and upload-session detail links open detail pages and render detail sections without raw provider/session secrets.
    - Boundary filters `/admin/rindle/assets?state=missing` and `/admin/rindle/upload-sessions?state=missing` render `[data-rindle-admin-empty-state]`, not `[data-rindle-admin-error-state]`.
    - Missing detail routes `/admin/rindle/assets/not-a-real-asset` and `/admin/rindle/upload-sessions/not-a-real-session` render `[data-rindle-admin-error-state]` and the stable error copy.
  </behavior>
  <action>Create `examples/adoption_demo/e2e/admin-console.spec.js` using `const { test, expect } = require("@playwright/test")` and the Plan 01 helper import from `./support/admin`. Cover the six top-level admin surfaces using `visitAdmin(page, suffix)` and `expectAdminShell(page, surface)`. Assert seeded row selectors, detail navigation through `firstAdminDetailHref(page, "asset")` and `firstAdminDetailHref(page, "upload-session")`, redaction through `[data-rindle-admin-redacted-value]`, boundary empty states for `state=missing`, and error states for missing detail IDs. Use only `data-rindle-admin-*` selectors for admin DOM and explicit visible copy for stable headings such as `Attachment context`, `Upload sessions`, `Strategy/protocol`, `Cleanup guidance`, `Variant/job buckets`, and `Doctor checks`. Do not use `page.waitForTimeout` or `data-testid` in this file.</action>
  <verify>
    <automated>cd examples/adoption_demo && npx playwright test e2e/admin-console.spec.js</automated>
    <automated>rg -n "data-testid|waitForTimeout" examples/adoption_demo/e2e/admin-console.spec.js && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    Source assertion: `examples/adoption_demo/e2e/admin-console.spec.js` imports from `./support/admin` and references `expectAdminShell`, `visitAdmin`, `firstAdminDetailHref`, and `expectNoAdminRawSecrets`.
    Source assertion: the spec contains `/admin/rindle` only through helper usage, not a separate hardcoded base route.
    Source assertion: the spec contains selectors for `[data-rindle-admin-row="asset"]`, `[data-rindle-admin-row="upload-session"]`, `[data-rindle-admin-row="variant-finding"]`, `[data-rindle-admin-row="doctor-check"]`, `[data-rindle-admin-empty-state]`, and `[data-rindle-admin-error-state]`.
    Source assertion: `rg -n "data-testid|waitForTimeout" examples/adoption_demo/e2e/admin-console.spec.js` returns no matches.
    Behavior assertion: targeted Playwright spec exits 0.
  </acceptance_criteria>
  <done>Admin console browser spec proves surface navigation, seeded detail pages, redaction, empty boundaries, and error states through stable admin selectors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add theme picker Playwright spec</name>
  <files>examples/adoption_demo/e2e/admin-theme.spec.js</files>
  <read_first>
    examples/adoption_demo/e2e/admin-theme.spec.js
    examples/adoption_demo/e2e/support/admin.js
    priv/static/rindle_admin/rindle-admin.js
    lib/rindle/admin/components.ex
    brandbook/src/admin-gallery-check.mjs
    examples/adoption_demo/playwright.config.js
    .planning/phases/92-e2e-screenshot-driven-polish-loop/92-CONTEXT.md
    .planning/phases/92-e2e-screenshot-driven-polish-loop/92-UI-SPEC.md
  </read_first>
  <behavior>
    - On `/admin/rindle`, `/admin/rindle/assets`, and `/admin/rindle/actions`, clicking `light`, `dark`, and `auto` theme controls updates `[data-rindle-admin-root]` to the same `data-theme` value.
    - The selected control has `aria-pressed="true"` and the other two theme controls have `aria-pressed="false"`.
    - Theme tests assert visible shell/nav/status/action controls and no horizontal scroll after each selected theme.
    - The spec uses app-level theme picker controls per D-92-03 and does not rely only on `page.emulateMedia`.
  </behavior>
  <action>Create `examples/adoption_demo/e2e/admin-theme.spec.js` using the Plan 01 helper. For suffix/surface pairs `("", "home-status")`, `("assets", "assets")`, and `("actions", "actions")`, navigate with `visitAdmin`, assert the shell, then call `selectAdminTheme(page, "light")`, `selectAdminTheme(page, "dark")`, and `selectAdminTheme(page, "auto")`. After each theme selection, assert `[data-rindle-admin-nav-item]`, `[data-rindle-admin-page-header]`, `[data-rindle-admin-live-indicator]`, and `[data-rindle-admin-component="theme-picker"]` are visible, and call `expectNoHorizontalScroll(page)`. Do not use `page.emulateMedia`, `data-testid`, or sleeps.</action>
  <verify>
    <automated>cd examples/adoption_demo && npx playwright test e2e/admin-theme.spec.js</automated>
    <automated>rg -n "data-testid|waitForTimeout|emulateMedia" examples/adoption_demo/e2e/admin-theme.spec.js && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    Source assertion: `examples/adoption_demo/e2e/admin-theme.spec.js` contains `selectAdminTheme(page, "light")`, `selectAdminTheme(page, "dark")`, and `selectAdminTheme(page, "auto")`.
    Source assertion: the spec asserts `[data-rindle-admin-root]` behavior through the helper and includes `/admin/rindle/actions` coverage through `visitAdmin(page, "actions")`.
    Source assertion: `rg -n "data-testid|waitForTimeout|emulateMedia" examples/adoption_demo/e2e/admin-theme.spec.js` returns no matches.
    Behavior assertion: targeted theme Playwright spec exits 0.
  </acceptance_criteria>
  <done>Theme switching is proven through the actual app theme picker on multiple admin surfaces in the adoption demo harness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser test runner -> Admin LiveViews | Playwright sends route, filter, and click inputs to the mounted console. |
| Seeded fixture DB -> Browser DOM | Lifecycle and provider/session fixture data renders into pages under test. |
| Browser traces/screenshots -> CI artifacts | Failed Playwright runs can upload trace and screenshot artifacts. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-92-05 | High | Information Disclosure | `admin-console.spec.js` detail assertions | mitigate | Task 1 calls `expectNoAdminRawSecrets(page)` on detail and diagnostic routes and asserts redaction selectors. |
| T-92-06 | Medium | Denial of Service | Playwright waits | mitigate | Tasks 1 and 2 use LiveView socket waits and Playwright locators; grep gates reject `waitForTimeout`. |
| T-92-07 | Low | Tampering | Boundary route tests | accept | Tests only navigate/read filters and missing IDs; no write actions occur in this plan. |
| T-92-08 | Low | Repudiation | CI artifact diagnosis | mitigate | Targeted specs keep trace-on-failure under existing Playwright output so failures are attributable to spec filenames. |
| T-92-SC | Low | Tampering | npm installs | accept | This plan adds no packages and uses existing Playwright dependency from `examples/adoption_demo/package.json`. |
</threat_model>

<verification>
Run both targeted Playwright specs from `examples/adoption_demo`, then run the grep gates rejecting `data-testid`, `waitForTimeout`, and `emulateMedia` in the new admin spec files.
</verification>

<success_criteria>
Plan succeeds when admin surface, boundary, detail, redaction, and theme browser coverage exists and passes in the existing adoption demo Playwright harness without brittle admin selectors or sleeps.
</success_criteria>

<output>
Create `.planning/phases/92-e2e-screenshot-driven-polish-loop/92-02-SUMMARY.md` when done.
</output>

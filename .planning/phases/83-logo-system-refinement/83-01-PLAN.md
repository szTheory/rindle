---
phase: 83
plan: 01
title: Refine winning direction into core lockup variants
requirements: [BRAND-04]
status: complete
created: 2026-06-10
---

# 83-01: Core lockup variants from the locked e1 execution

## Objective

Turn the user-locked Confluence e1 execution into the canonical logo assets:
primary lockup, dark, mono (currentColor), with-subtitle, and icon-only marks.

## Tasks

1. `brandbook/src/logo.mjs` builds the system from `confluence(word, {})` —
   single source of truth shared with the candidate pipeline.
2. Write `rindle-logo{,-dark,-mono,-subtitle}.svg` + `rindle-mark{,-dark,-mono}.svg`
   to `brandbook/assets/logo/`.

## Verification

- `node check.mjs` green; xmllint valid; each SVG ≤ 8 KB (subtitle ≤ 16 KB).
- Visual review screenshot: lockups on Warm Shell and Ink, mono in one color.

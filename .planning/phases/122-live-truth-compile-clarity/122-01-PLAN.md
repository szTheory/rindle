---
phase: 122-live-truth-compile-clarity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/schema.ex
  - test/rindle/schema_prefix_contract_test.exs
  - scripts/maintainer/refactor_contract.sh
  - test/install_smoke/refactor_contract_test.exs
autonomous: true
requirements: [CLARITY-03]
must_haves:
  truths:
    - "A fresh test-environment compile has no compile-connected cycle involving Rindle.Schema or the six owned schemas."
    - "Rindle.Schema still admits exactly the six owned callers and rejects unsupported callers without weakening its after-compile prefix guard."
    - "The supported rindle/public prefix, source, ID, compiled metadata, struct metadata, migration, telemetry, error, CI, and release contracts remain unchanged."
  artifacts:
    - path: "lib/rindle/schema.ex"
      provides: "Closed owner allowlist represented without compile-time references to consumer modules."
    - path: "test/rindle/schema_prefix_contract_test.exs"
      provides: "Behavioral proof of owned-caller admission, hostile-caller rejection, and prefix metadata compatibility."
    - path: "scripts/maintainer/refactor_contract.sh"
      provides: "Objective xref cycle gate composed with SAFE-01."
    - path: "test/install_smoke/refactor_contract_test.exs"
      provides: "Fail-closed structural lock for the xref command and preservation runner."
  key_links:
    - from: "lib/rindle/schema.ex"
      to: "six lib/rindle/domain/media_*.ex schemas"
      via: "caller identity comparison without direct module references"
      pattern: "Atom.to_string|Module.split"
    - from: "scripts/maintainer/refactor_contract.sh"
      to: "Mix compiler dependency graph"
      via: "fresh compile followed by compile-connected cycle threshold"
      pattern: "mix xref graph --format cycles --label compile-connected --fail-above 0"
---

<objective>
Remove the measured seven-file `Rindle.Schema` compile-connected component while preserving every schema ownership and prefix boundary.

Purpose: Make compile dependencies directional and inspectable without changing public API, schema, migration, telemetry, error, or release behavior.
Output: A non-module-reference closed caller allowlist, focused compatibility proof, and an objective xref gate inside SAFE-01.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-live-truth-compile-clarity/122-CONTEXT.md
@.planning/phases/122-live-truth-compile-clarity/122-PATTERNS.md
@.planning/phases/122-live-truth-compile-clarity/122-RESEARCH.md
@.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-01-SUMMARY.md
@.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-VERIFICATION.md
@lib/rindle/schema.ex
@test/rindle/schema_prefix_contract_test.exs
@scripts/maintainer/refactor_contract.sh
@test/install_smoke/refactor_contract_test.exs

<interfaces>
`Rindle.Schema` remains the sole owner of `prefix/0`, `supported_prefixes/0`, `validate_prefix!/1`, `__using__/1`, `schema/2`, and `__after_compile__/2`. Each owned domain module continues to `use Rindle.Schema`; no owned schema file is modified. The accepted prefixes remain exactly `"rindle"` and `"public"`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove and sever the reverse schema compile edge</name>
  <behavior>
    - Fresh `MIX_ENV=test` compilation followed by Mix xref reports zero compile-connected cycles.
    - All six shipped domain schemas retain the configured compiled prefix in `__schema__(:prefix)` and fresh struct metadata.
    - A dynamically compiled non-owned consumer still raises the same `ArgumentError` class and identifies the unsupported module.
  </behavior>
  <files>lib/rindle/schema.ex, test/rindle/schema_prefix_contract_test.exs</files>
  <action>Keep the existing macro, supported-prefix list, compile-time configuration read, `schema/2` wrapper, callback installation, callback validation, and exception vocabulary. Replace only the six direct domain-module values in the private owner allowlist with canonical non-module-reference identities and compare `__CALLER__.module` against that closed set. Strengthen the existing contract test only at the behavior/compiled-metadata boundary: prove all six existing callers are accepted and the hostile callback-deletion/raw-Ecto consumer remains rejected. Do not edit the six domain schema modules, persistence sources, migrations, config semantics, or public functions.</action>
  <verify>
    <automated>MIX_ENV=test mix compile --force &amp;&amp; MIX_ENV=test mix xref graph --format cycles --label compile-connected --fail-above 0 &amp;&amp; MIX_ENV=test mix test test/rindle/schema_prefix_contract_test.exs test/rindle/domain/media_schema_test.exs test/rindle/config/config_test.exs</automated>
  </verify>
  <done>The exact pre-change seven-file component is absent, the xref threshold exits zero, and ownership/prefix behavior tests pass without changes to owned schema modules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose the objective cycle proof into SAFE-01</name>
  <behavior>
    - SAFE-01 fails when any compile-connected cycle exists after a fresh test-environment compile.
    - SAFE-01 still executes one foreground preservation test process with native output and exit status.
    - The structural meta-test rejects removal, masking, or weakening of the exact xref threshold.
  </behavior>
  <files>scripts/maintainer/refactor_contract.sh, test/install_smoke/refactor_contract_test.exs</files>
  <action>Extend the Phase 121 runner before its existing foreground contract test invocation: force a test-environment compile, then run Mix's compiler-owned xref cycle command with compile-connected labeling and a zero-cycle threshold. Extend the structural test to lock those exact semantics, command ordering, and failure propagation while retaining the existing explicit suite membership and single `mix test` process. Do not add CI jobs, alter `CI Summary`, mask output/status, parse `_build` artifacts, or inspect `.planning/` at runtime.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/install_smoke/refactor_contract_test.exs &amp;&amp; bash scripts/maintainer/refactor_contract.sh</automated>
  </verify>
  <done>One SAFE-01 command proves both zero compiler-reported cycles and all previously frozen behavior domains, and its meta-test fails closed on gate drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Macro caller to schema authority | Compile-time caller identity crosses into the internal allowlist and prefix enforcement callback. |
| Maintainer runner to compiler graph | SAFE-01 relies on Mix exit status as the authoritative compile-cycle signal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-122-01 | Tampering | `Rindle.Schema.validate_owned_schema!/1` | high | mitigate | Keep the allowlist closed to the same six canonical identities and preserve the hostile dynamic-consumer rejection test. |
| T-122-02 | Repudiation | xref gate in `refactor_contract.sh` | medium | mitigate | Use fresh compilation and Mix's zero-cycle failure threshold with direct status propagation; lock it structurally. |
| T-122-03 | Tampering | public schema/prefix boundary | high | mitigate | Run schema metadata/config tests and the complete SAFE-01 suite; do not edit schemas, migrations, telemetry, errors, or workflows. |
</threat_model>

<verification>
- `MIX_ENV=test mix compile --force` succeeds on the available local toolchain.
- `MIX_ENV=test mix xref graph --format cycles --label compile-connected --fail-above 0` exits zero.
- Focused schema/config tests and `bash scripts/maintainer/refactor_contract.sh` pass.
- Local Elixir 1.19/OTP 28 results are diagnostic; the repository-supported Elixir 1.17/OTP 27 CI cell is authoritative acceptance.
</verification>

<success_criteria>
- No compiler-reported compile-connected cycles remain.
- The six owned schemas preserve sources, IDs, prefixes, and struct metadata.
- Unsupported callers and prefix tampering still fail with the established exception behavior.
- SAFE-01 gains objective xref proof without CI/release topology changes.
</success_criteria>

<output>
Create `.planning/phases/122-live-truth-compile-clarity/122-01-SUMMARY.md` when done.
</output>

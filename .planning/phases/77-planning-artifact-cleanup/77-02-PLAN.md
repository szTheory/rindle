---
phase: 77
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/72-mix-batch-failure-proof/72-VALIDATION.md
  - .planning/STATE.md
autonomous: true
requirements:
  - PLAN-01

must_haves:
  truths:
    - "72-VALIDATION.md row 72-01-01 shows ✅ green"
    - "STATE.md Current Position has no Plan: Not started"
    - "STATE.md reflects between-milestones / v1.15 shipped truth"
    - "STATE.md Operator Next Steps no longer references /gsd-plan-phase 71"
    - "STATE frontmatter last_updated and last_activity bumped; milestone stays v1.15"
  artifacts:
    - path: .planning/STATE.md
      provides: Accurate position block post-v1.15 ship
      contains: "Between milestones"
    - path: .planning/phases/72-mix-batch-failure-proof/72-VALIDATION.md
      provides: Phase 72 Per-Task row reconciled
      contains: "72-01-01"
  key_links:
    - from: .planning/STATE.md
      to: .planning/ROADMAP.md
      via: Next Step points to Phase 76/75 queue
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-77-02-1 | Premature v1.16 frontmatter flip during Phase 77 | medium | Bump timestamps only; leave milestone: v1.15 (D-08) |
| T-77-02-2 | Marking 72 row green without evidence | low | Cross-read 72-VERIFICATION.md; optional fresh mix test |
| T-77-02-3 | Over-scoping STATE regen | low | Surgical edits to position/milestone/operator blocks only |
</threat_model>

<objective>
Close Phase 72 Per-Task row drift and apply extended surgical fix to `.planning/STATE.md`
position block, milestone section, and operator next steps — without v1.16 frontmatter activation.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-planning-artifact-cleanup/77-CONTEXT.md
@.planning/phases/77-planning-artifact-cleanup/77-RESEARCH.md
@.planning/phases/72-mix-batch-failure-proof/72-VERIFICATION.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mark 72-01-01 row green in 72-VALIDATION.md</name>
  <files>.planning/phases/72-mix-batch-failure-proof/72-VALIDATION.md</files>
  <read_first>
    - .planning/phases/72-mix-batch-failure-proof/72-VALIDATION.md (Per-Task row 72-01-01)
    - .planning/phases/72-mix-batch-failure-proof/72-VERIFICATION.md (evidence: 7 tests green)
    - test/rindle/batch_owner_erasure_task_test.exs
    - .planning/phases/77-planning-artifact-cleanup/77-CONTEXT.md (D-05)
  </read_first>
  <action>
    Optional fresh verify (recommended for timestamp note):

    ```bash
    mix test test/rindle/batch_owner_erasure_task_test.exs
    ```

    Edit `.planning/phases/72-mix-batch-failure-proof/72-VALIDATION.md`:
    - Flip row `72-01-01` Status from `⬜ pending` to `✅ green`.

    If test re-run performed, append **Validation Audit** section (or add row if section exists):

    ```markdown
    ## Validation Audit

    | Date | Action | Result |
    |------|--------|--------|
    | 2026-05-27 | Phase 77 Per-Task row reconciliation | 72-01-01 green; mix test re-run |
    ```
  </action>
  <acceptance_criteria>
    - `grep '72-01-01' .planning/phases/72-mix-batch-failure-proof/72-VALIDATION.md | grep -c '✅ green'` outputs at least `1`
    - `grep '72-01-01' .planning/phases/72-mix-batch-failure-proof/72-VALIDATION.md | grep -c '⬜ pending'` outputs `0`
    - `grep '⬜ pending' .planning/phases/72-mix-batch-failure-proof/72-VALIDATION.md` outputs no Per-Task row matches (legend line excluded: use `grep '| 72-'`)
  </acceptance_criteria>
  <verify>
    <automated>grep '72-01-01' .planning/phases/72-mix-batch-failure-proof/72-VALIDATION.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Apply extended surgical fix to STATE.md position block</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (full file — frontmatter, Current Position, Current Milestone, Next Step)
    - .planning/phases/77-planning-artifact-cleanup/77-CONTEXT.md (D-07 through D-12)
    - .planning/ROADMAP.md (Phase 76/75 ordering for Next Step text)
  </read_first>
  <action>
    Edit `.planning/STATE.md`:

    **Frontmatter (D-08):** Bump `last_updated` to current ISO timestamp and `last_activity`
    to `2026-05-27`. **Do not change** `milestone: v1.15`, `status: Milestone v1.15 shipped`,
    or progress counters.

    **Current Position (D-09):** Replace block with:
    ```
    Phase: 74 (complete)
    Plan: complete — milestone v1.15 shipped
    Status: Between milestones
    Last activity: 2026-05-27
    ```

    **Current Milestone (D-10):** Replace Active line and add queued milestone:
    - Remove `Active: v1.15 Maintenance & Proof Honesty` (milestone shipped)
    - Set section to reflect between-milestones state:
      - `- **Between milestones**`
      - `- **Previous shipped:** v1.15 Maintenance & Proof Honesty (2026-05-27)`
      - `- **Next queued:** v1.16 CI Enforcement & Planning Hygiene (Phases 75–77; execute 77→76→75)`

    **Next Step (D-11):** Replace demand-gated text with:
    ```
    `/gsd-execute-phase 77` then `/gsd-execute-phase 76` (TusPlug doc parity lock) then `/gsd-execute-phase 75` (merge-blocking proof lanes) — see ROADMAP.md v1.16 queue
    ```

    Preserve Accumulated Context, Blockers, Deferred Items, Session Continuity sections
    unless they contradict shipped truth (do not regen entire file).
  </action>
  <acceptance_criteria>
    - `! grep -q '^Plan: Not started' .planning/STATE.md` exits 0
    - `grep -q '^Plan: complete' .planning/STATE.md` exits 0
    - `grep -q 'Between milestones' .planning/STATE.md` exits 0
    - `grep -q 'Next queued: v1.16' .planning/STATE.md` exits 0
    - `grep -q 'milestone: v1.15' .planning/STATE.md` exits 0
    - `grep -q 'milestone: v1.16' .planning/STATE.md` exits 1 (must NOT flip yet)
  </acceptance_criteria>
  <verify>
    <automated>! grep -q '^Plan: Not started' .planning/STATE.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Update Operator Next Steps in STATE.md</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (Operator Next Steps section)
    - .planning/ROADMAP.md (Phase 76 TusPlug lock, Phase 75 proof lanes)
    - .planning/phases/77-planning-artifact-cleanup/77-CONTEXT.md (D-12)
  </read_first>
  <action>
    Replace **Operator Next Steps** section with:

    ```markdown
    ## Operator Next Steps

    - `/gsd-execute-phase 77` — planning artifact cleanup (this phase)
    - `/gsd-execute-phase 76` — TusPlug moduledoc parity lock
    - `/gsd-execute-phase 75` — merge-blocking proof lanes
    ```

    Remove any reference to `/gsd-plan-phase 71`.
  </action>
  <acceptance_criteria>
    - `grep -c '/gsd-plan-phase 71' .planning/STATE.md` outputs `0`
    - `grep -c '/gsd-execute-phase 76' .planning/STATE.md` outputs at least `1`
    - `grep -c '/gsd-execute-phase 75' .planning/STATE.md` outputs at least `1`
  </acceptance_criteria>
  <verify>
    <automated>! grep '/gsd-plan-phase 71' .planning/STATE.md</automated>
  </verify>
</task>

</tasks>

<verification>
- 72-VALIDATION.md and STATE.md updated; no lib/ or ci.yml changes
- STATE frontmatter still shows milestone v1.15 (v1.16 flip deferred to post-77 orchestrator)
</verification>

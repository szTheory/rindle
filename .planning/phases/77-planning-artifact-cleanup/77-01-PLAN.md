---
phase: 77
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/71-ci-proof-honesty/71-VALIDATION.md
autonomous: true
requirements:
  - PLAN-01

must_haves:
  truths:
    - "71-VALIDATION.md row 71-02-02 criterion reads ≥ 6 (not ≥ 8)"
    - "All four Phase 71 Per-Task rows show ✅ green"
    - "71-VALIDATION.md frontmatter status: complete and nyquist_compliant: true"
    - "Validation Sign-Off complete with Approval: approved 2026-05-27"
    - "Validation Audit table appended"
  artifacts:
    - path: .planning/phases/71-ci-proof-honesty/71-VALIDATION.md
      provides: Phase 71 Nyquist-compliant validation artifact
      contains: "nyquist_compliant: true"
  key_links:
    - from: .planning/phases/71-ci-proof-honesty/71-VALIDATION.md
      to: .planning/phases/71-ci-proof-honesty/71-VERIFICATION.md
      via: evidence cross-reference before row flips
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-77-01-1 | Blind metadata flip without running verify commands | high | All four commands must exit 0 before row flips |
| T-77-01-2 | Stale ≥ 8 criterion causes false red on verify | medium | Fix criterion to ≥ 6 before any verify run |
| T-77-01-3 | Conflating Nyquist closure with CI enforcement depth | low | Do not edit ci.yml; Phase 75 owns merge-blocking proof |
</threat_model>

<objective>
Close Phase 71 Nyquist metadata drift: fix stale `71-02-02` criterion, re-run all four
verify commands fresh, reconcile `71-VALIDATION.md` to green with Validation Audit trail.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-planning-artifact-cleanup/77-CONTEXT.md
@.planning/phases/77-planning-artifact-cleanup/77-RESEARCH.md
@.planning/phases/71-ci-proof-honesty/71-VERIFICATION.md
@.planning/phases/73-nyquist-validation-closure/73-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 71-02-02 criterion from ≥ 8 to ≥ 6</name>
  <files>.planning/phases/71-ci-proof-honesty/71-VALIDATION.md</files>
  <read_first>
    - .planning/phases/71-ci-proof-honesty/71-VALIDATION.md (Per-Task row 71-02-02)
    - .planning/phases/71-ci-proof-honesty/71-VERIFICATION.md (evidence: 6 comment blocks)
    - .github/workflows/ci.yml (confirm 6 "Phase 71 (CI proof honesty)" comments)
    - .planning/phases/77-planning-artifact-cleanup/77-CONTEXT.md (D-02)
  </read_first>
  <action>
    In `.planning/phases/71-ci-proof-honesty/71-VALIDATION.md`, edit Per-Task row
    `71-02-02` Automated Command acceptance text:

    **From:** `rg 'Phase 71 \(CI proof honesty\)' .github/workflows/ci.yml | wc -l` ≥ 8

    **To:** `test "$(rg 'Phase 71 \(CI proof honesty\)' .github/workflows/ci.yml | wc -l | tr -d ' ')" -ge 6`

    Update the Automated Command column to match the verify command in CONTEXT D-03.
    Do not change any other rows yet.
  </action>
  <acceptance_criteria>
    - `grep -c '≥ 6' .planning/phases/71-ci-proof-honesty/71-VALIDATION.md` outputs at least `1`
    - `grep -c '≥ 8' .planning/phases/71-ci-proof-honesty/71-VALIDATION.md` outputs `0`
    - `test "$(rg 'Phase 71 \(CI proof honesty\)' .github/workflows/ci.yml | wc -l | tr -d ' ')" -ge 6` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep '≥ 6' .planning/phases/71-ci-proof-honesty/71-VALIDATION.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Run all four Phase 71 verify commands fresh</name>
  <files>.planning/phases/71-ci-proof-honesty/71-VALIDATION.md</files>
  <read_first>
    - .planning/phases/71-ci-proof-honesty/71-VALIDATION.md (Per-Task map — all four commands)
    - RUNNING.md (CI lane severity section)
    - .github/workflows/ci.yml (package-consumer job)
    - test/install_smoke/docs_parity_test.exs
    - .planning/phases/77-planning-artifact-cleanup/77-CONTEXT.md (D-03)
  </read_first>
  <action>
    Run from repo root in order (all must exit 0):

    ```bash
    rg '## CI lane severity' RUNNING.md
    mix test test/install_smoke/docs_parity_test.exs
    ! rg -A2 'package-consumer:' .github/workflows/ci.yml | rg 'continue-on-error'
    test "$(rg 'Phase 71 \(CI proof honesty\)' .github/workflows/ci.yml | wc -l | tr -d ' ')" -ge 6
    ```

    Record pass timestamps in commit message or SUMMARY — do not flip rows until all four pass.
  </action>
  <acceptance_criteria>
    - `rg '## CI lane severity' RUNNING.md` exits 0
    - `mix test test/install_smoke/docs_parity_test.exs` exits 0
    - `! rg -A2 'package-consumer:' .github/workflows/ci.yml | rg 'continue-on-error'` exits 0
    - `test "$(rg 'Phase 71 \(CI proof honesty\)' .github/workflows/ci.yml | wc -l | tr -d ' ')" -ge 6` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/install_smoke/docs_parity_test.exs</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Reconcile 71-VALIDATION.md to Nyquist-complete state</name>
  <files>.planning/phases/71-ci-proof-honesty/71-VALIDATION.md</files>
  <read_first>
    - .planning/phases/71-ci-proof-honesty/71-VALIDATION.md (current draft state)
    - .planning/phases/73-nyquist-validation-closure/73-01-PLAN.md (Validation Audit format)
    - .planning/phases/73-nyquist-validation-closure/73-VALIDATION.md (sign-off pattern)
    - .planning/phases/77-planning-artifact-cleanup/77-CONTEXT.md (D-04)
  </read_first>
  <action>
    Edit `.planning/phases/71-ci-proof-honesty/71-VALIDATION.md`:

    **Frontmatter:**
    - `status: complete`
    - `nyquist_compliant: true`

    **Per-Task map:** Flip all four rows (`71-01-01`, `71-01-02`, `71-02-01`, `71-02-02`)
    Status column from `⬜ pending` to `✅ green`.

    **Validation Sign-Off:** Flip all `- [ ]` to `- [x]`.

    **Approval:** Set `**Approval:** approved 2026-05-27`.

    **Validation Audit:** Append section if missing:

    ```markdown
    ## Validation Audit

    | Date | Action | Result |
    |------|--------|--------|
    | 2026-05-27 | Phase 77 metadata reconciliation | nyquist_compliant: true |
    ```
  </action>
  <acceptance_criteria>
    - `grep 'status: complete' .planning/phases/71-ci-proof-honesty/71-VALIDATION.md` exits 0
    - `grep 'nyquist_compliant: true' .planning/phases/71-ci-proof-honesty/71-VALIDATION.md` exits 0
    - `grep -c '⬜ pending' .planning/phases/71-ci-proof-honesty/71-VALIDATION.md` outputs `0`
    - `grep -cE '^- \[ \]' .planning/phases/71-ci-proof-honesty/71-VALIDATION.md` outputs `0`
    - `grep 'Approval: approved 2026-05-27' .planning/phases/71-ci-proof-honesty/71-VALIDATION.md` exits 0
    - `grep 'Validation Audit' .planning/phases/71-ci-proof-honesty/71-VALIDATION.md` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep 'nyquist_compliant: true' .planning/phases/71-ci-proof-honesty/71-VALIDATION.md</automated>
  </verify>
</task>

</tasks>

<verification>
- All three tasks complete with grep and mix test evidence
- 71-VALIDATION.md is the only file modified in this plan
- No ci.yml or lib/ changes
</verification>

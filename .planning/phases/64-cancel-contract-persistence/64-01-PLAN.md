---
id: 64-01
plan_number: 01
plan: 01
phase: 64
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/20260527120000_add_provider_upload_id_to_media_provider_assets.exs
  - lib/rindle/domain/media_provider_asset.ex
  - test/rindle/domain/migration_test.exs
autonomous: true
requirements: [CANCEL-03]
must_haves:
  truths:
    - "media_provider_assets has a nullable provider_upload_id column with a partial unique index on (provider_name, provider_upload_id)."
    - "MediaProviderAsset changeset casts provider_upload_id and enforces the partial unique constraint name."
    - "Inspect redacts provider_upload_id via redact_id/1 like other provider secrets."
  artifacts:
    - path: "priv/repo/migrations/20260527120000_add_provider_upload_id_to_media_provider_assets.exs"
      provides: "Additive migration for cancel handle persistence"
      contains: "add :provider_upload_id"
    - path: "lib/rindle/domain/media_provider_asset.ex"
      provides: "Schema field, @writable, unique_constraint, Inspect redaction"
      contains: "provider_upload_id"
  key_links:
    - from: "priv/repo/migrations/20260524120000_add_mux_passthrough_to_media_provider_assets.exs"
      to: "priv/repo/migrations/20260527120000_add_provider_upload_id_to_media_provider_assets.exs"
      via: "same partial unique index pattern"
      pattern: "where: \"provider_upload_id IS NOT NULL\""
---

<objective>
Ship additive persistence for Mux direct-upload cancel handles (D-14..D-16, D-28..D-29).

Purpose: Enable Phase 65 cancel to load `provider_upload_id` from the durable row without schema drift.
Output: One migration, schema/changeset/Inspect updates, migration catalog tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-cancel-contract-persistence/64-CONTEXT.md
@.planning/phases/64-cancel-contract-persistence/64-RESEARCH.md
@priv/repo/migrations/20260524120000_add_mux_passthrough_to_media_provider_assets.exs
@priv/repo/migrations/20260506120000_create_media_provider_assets.exs
@lib/rindle/domain/media_provider_asset.ex
@test/rindle/domain/migration_test.exs
</context>

<threat_model>
| Threat | Severity | Mitigation in this plan |
|--------|----------|-------------------------|
| T-64-01 Provider upload handle stored without redaction | high | Add field to @writable + Inspect uses redact_id/1 |
| Duplicate upload_id across providers | medium | Partial unique index (provider_name, provider_upload_id) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add provider_upload_id migration and schema redaction</name>
  <files>priv/repo/migrations/20260527120000_add_provider_upload_id_to_media_provider_assets.exs, lib/rindle/domain/media_provider_asset.ex, test/rindle/domain/migration_test.exs</files>
  <read_first>
    - priv/repo/migrations/20260524120000_add_mux_passthrough_to_media_provider_assets.exs
    - lib/rindle/domain/media_provider_asset.ex
    - test/rindle/domain/migration_test.exs
    - .planning/phases/64-cancel-contract-persistence/64-CONTEXT.md (D-14..D-19, D-28..D-29)
  </read_first>
  <action>
Create `priv/repo/migrations/20260527120000_add_provider_upload_id_to_media_provider_assets.exs`:

```elixir
def change do
  alter table(:media_provider_assets) do
    add :provider_upload_id, :string
  end

  create unique_index(:media_provider_assets, [:provider_name, :provider_upload_id],
           where: "provider_upload_id IS NOT NULL",
           name: :media_provider_assets_provider_name_provider_upload_id_index
         )
end
```

In `lib/rindle/domain/media_provider_asset.ex`:
- Add `field :provider_upload_id, :string` to schema (after `:mux_passthrough` or adjacent provider-secret fields).
- Append `:provider_upload_id` to `@writable`.
- Add `unique_constraint([:provider_name, :provider_upload_id], name: :media_provider_assets_provider_name_provider_upload_id_index)` to changeset.
- In `defimpl Inspect`, add `provider_upload_id: Rindle.Domain.MediaProviderAsset.redact_id(asset.provider_upload_id)`.

Extend `test/rindle/domain/migration_test.exs` with a describe block asserting:
- column `provider_upload_id` exists on `media_provider_assets`
- `pg_indexes` contains index name `media_provider_assets_provider_name_provider_upload_id_index` with `WHERE` clause referencing `provider_upload_id IS NOT NULL`
  </action>
  <verify>mix test test/rindle/domain/migration_test.exs</verify>
  <acceptance_criteria>
    - `priv/repo/migrations/20260527120000_add_provider_upload_id_to_media_provider_assets.exs` contains `add :provider_upload_id, :string`
    - `lib/rindle/domain/media_provider_asset.ex` contains `field :provider_upload_id, :string`
    - `lib/rindle/domain/media_provider_asset.ex` `@writable` list includes `:provider_upload_id`
    - `defimpl Inspect` assigns `provider_upload_id` through `redact_id/1`
    - `mix test test/rindle/domain/migration_test.exs` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run `mix ecto.migrate` in dev/test as needed, then `mix test test/rindle/domain/migration_test.exs`.
</verification>

<success_criteria>
- Migration applies cleanly; schema and Inspect enforce invariant 14 for `provider_upload_id`.
</success_criteria>

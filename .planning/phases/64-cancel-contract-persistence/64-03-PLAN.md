---
id: 64-03
plan_number: 03
plan: 03
phase: 64
type: execute
wave: 2
depends_on: [64-01]
files_modified:
  - lib/rindle/streaming.ex
  - test/rindle/streaming/create_direct_upload_test.exs
autonomous: true
requirements: [CANCEL-03]
must_haves:
  truths:
    - "create_direct_upload/2 persists provider_upload_id from the adapter upload_id in the same transaction branch that sets state uploading."
    - "Public return map still contains only upload_url and asset_id (no upload_id leak)."
    - "Inspect on the provider row does not contain the raw provider_upload_id."
  artifacts:
    - path: "lib/rindle/streaming.ex"
      provides: "Multi.run success branch persists provider_upload_id"
      contains: "provider_upload_id:"
    - path: "test/rindle/streaming/create_direct_upload_test.exs"
      provides: "Persistence + redaction assertions"
      contains: "provider_upload_id"
  key_links:
    - from: "lib/rindle/streaming/provider/mux.ex"
      to: "lib/rindle/streaming.ex"
      via: "adapter returns upload_id key"
      pattern: "upload_id: upload_id"
---

<objective>
Persist provider upload handles at direct-upload mint time (D-16, CANCEL-03).

Purpose: Phase 65 cancel loads `provider_upload_id` from the row; no backfill for pre-v1.13 rows (D-19).
Output: `create_direct_upload/2` writes `provider_upload_id`; test proves persistence and redaction.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-cancel-contract-persistence/64-CONTEXT.md
@.planning/phases/64-cancel-contract-persistence/64-RESEARCH.md
@lib/rindle/streaming.ex
@lib/rindle/streaming/provider/mux.ex
@test/rindle/streaming/create_direct_upload_test.exs
</context>

<threat_model>
| Threat | Severity | Mitigation in this plan |
|--------|----------|-------------------------|
| T-64-01 upload_id in public return map | high | Only persist on row; keep `%{upload_url, asset_id}` return |
| T-64-01 upload_url persisted | high | Do NOT add upload_url to changeset (D-16) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Persist provider_upload_id in create_direct_upload success branch</name>
  <files>lib/rindle/streaming.ex, test/rindle/streaming/create_direct_upload_test.exs</files>
  <read_first>
    - lib/rindle/streaming.ex (Multi.run :direct_upload branch)
    - lib/rindle/streaming/provider/mux.ex (create_direct_upload return shape)
    - test/rindle/streaming/create_direct_upload_test.exs
    - lib/rindle/domain/media_provider_asset.ex
  </read_first>
  <action>
In `lib/rindle/streaming.ex`, change the `{:ok, %{upload_url: upload_url}}` pattern match to also bind `upload_id`:

```elixir
{:ok, %{upload_url: upload_url, upload_id: upload_id}} ->
```

Update the changeset to:

```elixir
MediaProviderAsset.changeset(%{
  state: "uploading",
  provider_upload_id: upload_id
})
```

Do not add `upload_url` to the changeset. Do not add `upload_id` to the public `{:ok, %{upload_url:, asset_id:}}` return map.

In `test/rindle/streaming/create_direct_upload_test.exs`, extend the happy-path test after loading `provider_row`:

```elixir
assert provider_row.provider_upload_id == "mux-upload-id-123"
refute inspect(provider_row) =~ "mux-upload-id-123"
```

Keep existing assertions that `result` has only `:asset_id` and `:upload_url` keys.
  </action>
  <verify>mix test test/rindle/streaming/create_direct_upload_test.exs</verify>
  <acceptance_criteria>
    - `lib/rindle/streaming.ex` contains `provider_upload_id: upload_id` in the direct_upload success changeset
    - `test/rindle/streaming/create_direct_upload_test.exs` asserts `provider_row.provider_upload_id == "mux-upload-id-123"`
    - `test/rindle/streaming/create_direct_upload_test.exs` contains `refute inspect(provider_row) =~ "mux-upload-id-123"`
    - `mix test test/rindle/streaming/create_direct_upload_test.exs` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
`mix test test/rindle/streaming/create_direct_upload_test.exs`
</verification>

<success_criteria>
- CANCEL-03 persistence path is live for new direct uploads; public API shape unchanged.
</success_criteria>

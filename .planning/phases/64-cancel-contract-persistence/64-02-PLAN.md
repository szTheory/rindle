---
id: 64-02
plan_number: 02
plan: 02
phase: 64
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/domain/provider_asset_fsm.ex
  - test/rindle/domain/provider_asset_fsm_test.exs
autonomous: true
requirements: [CANCEL-02]
must_haves:
  truths:
    - "ProviderAssetFSM allowlists pending → deleted and uploading → deleted."
    - "processing, ready, and errored still cannot reach deleted from in-flight upload states except existing ready/errored paths."
  artifacts:
    - path: "lib/rindle/domain/provider_asset_fsm.ex"
      provides: "Extended @allowed_transitions for cancel terminal edge"
      contains: "\"deleted\""
    - path: "test/rindle/domain/provider_asset_fsm_test.exs"
      provides: "Regression coverage for cancel FSM edges"
      contains: "pending\", \"deleted\""
  key_links:
    - from: "lib/rindle/workers/ingest_provider_webhook.ex"
      to: "lib/rindle/domain/provider_asset_fsm.ex"
      via: "deleted rows reject asset_created (unchanged in 64)"
      pattern: "video.upload.asset_created"
---

<objective>
Add FSM terminal cancel edges (D-20..D-21) so Phase 65 can run FSM-first conditional updates to `deleted`.

Output: Two new allowlist entries and focused ExUnit coverage.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-cancel-contract-persistence/64-CONTEXT.md
@.planning/phases/64-cancel-contract-persistence/64-RESEARCH.md
@lib/rindle/domain/provider_asset_fsm.ex
@test/rindle/domain/provider_asset_fsm_test.exs
</context>

<threat_model>
| Threat | Severity | Mitigation in this plan |
|--------|----------|-------------------------|
| Race: webhook promotes cancelled row | high | FSM-first spec in CONTEXT D-22; webhook linker already rejects deleted |
| Invalid skip to deleted from processing | medium | Only pending/uploading gain deleted edge |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend FSM allowlist and tests for cancel terminal edges</name>
  <files>lib/rindle/domain/provider_asset_fsm.ex, test/rindle/domain/provider_asset_fsm_test.exs</files>
  <read_first>
    - lib/rindle/domain/provider_asset_fsm.ex
    - test/rindle/domain/provider_asset_fsm_test.exs
    - .planning/phases/64-cancel-contract-persistence/64-CONTEXT.md (D-20..D-23)
  </read_first>
  <action>
In `lib/rindle/domain/provider_asset_fsm.ex`, update `@allowed_transitions`:

```elixir
"pending" => ["uploading", "errored", "deleted"],
"uploading" => ["processing", "errored", "deleted"],
```

Leave all other entries unchanged.

In `test/rindle/domain/provider_asset_fsm_test.exs`:
- Add describe `"transition matrix — direct-upload cancel (Phase 64)"` with:
  - `assert :ok == ProviderAssetFSM.transition("pending", "deleted")`
  - `assert :ok == ProviderAssetFSM.transition("uploading", "deleted")`
  - `assert {:error, {:invalid_transition, "processing", "deleted"}} = ProviderAssetFSM.transition("processing", "deleted")`
- Update `allowed_transitions/0` introspection test expected lists for `"pending"` and `"uploading"` to include `"deleted"`.
  </action>
  <verify>mix test test/rindle/domain/provider_asset_fsm_test.exs</verify>
  <acceptance_criteria>
    - `lib/rindle/domain/provider_asset_fsm.ex` lists `"deleted"` in pending and uploading target lists
    - `test/rindle/domain/provider_asset_fsm_test.exs` contains `ProviderAssetFSM.transition("pending", "deleted")` success assertion
    - `test/rindle/domain/provider_asset_fsm_test.exs` contains `processing", "deleted"` rejection assertion
    - `mix test test/rindle/domain/provider_asset_fsm_test.exs` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
`mix test test/rindle/domain/provider_asset_fsm_test.exs`
</verification>

<success_criteria>
- FSM allowlist matches D-20; tests lock the matrix for Phase 65 orchestration.
</success_criteria>

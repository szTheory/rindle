---
phase: 89-console-read-surfaces
plan: "07"
type: execute
wave: 6
depends_on:
  - "89-06"
files_modified:
  - .github/workflows/ci.yml
  - test/rindle/admin/optional_dependency_test.exs
autonomous: true
requirements:
  - ADMIN-06
must_haves:
  truths:
    - "`phoenix_live_view` remains optional in `mix.exs`."
    - "All admin router/component/LiveView modules compile away cleanly when optional dependencies are absent."
    - "CI runs a real no-optional-deps compile proof, not only source grep assertions."
    - "With optional deps present, the console modules export the expected router and LiveView surfaces."
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "Optional dependency CI matrix proof"
      contains: "mix compile --no-optional-deps --warnings-as-errors"
    - path: test/rindle/admin/optional_dependency_test.exs
      provides: "With-deps smoke coverage for guarded admin modules"
      contains: "Rindle.Admin.Router"
  key_links:
    - from: .github/workflows/ci.yml
      to: mix.exs
      via: "CI no-optional-deps compile command"
      pattern: "--no-optional-deps"
    - from: test/rindle/admin/optional_dependency_test.exs
      to: lib/rindle/admin/router.ex
      via: "with-deps exported macro smoke"
      pattern: "rindle_admin"
---

<objective>
Add the ADMIN-06 optional dependency proof.

Purpose: Phase 89 adds Phoenix LiveView console modules, so the release train must prove default/non-console installs still compile without `phoenix_live_view`.
Output: CI matrix command for `mix compile --no-optional-deps --warnings-as-errors` and with-deps smoke tests for the guarded admin modules.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/89-console-read-surfaces/89-CONTEXT.md
@.planning/phases/89-console-read-surfaces/89-RESEARCH.md
@.planning/phases/89-console-read-surfaces/89-VALIDATION.md
@.planning/phases/89-console-read-surfaces/89-PATTERNS.md
@RUNNING.md
@mix.exs
@.github/workflows/ci.yml
@lib/rindle/live_view.ex
@lib/rindle/admin/router.ex
@lib/rindle/admin/components.ex
@lib/rindle/admin/live/home_live.ex
@lib/rindle/admin/live/assets_live.ex
@lib/rindle/admin/live/upload_sessions_live.ex
@lib/rindle/admin/live/variants_jobs_live.ex
@lib/rindle/admin/live/runtime_doctor_live.ex
@lib/rindle/admin/live/actions_live.ex
@test/rindle/streaming/provider/mux/optional_dep_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add with-deps optional boundary smoke tests</name>
  <files>test/rindle/admin/optional_dependency_test.exs</files>
  <read_first>
    test/rindle/admin/optional_dependency_test.exs
    test/rindle/streaming/provider/mux/optional_dep_test.exs
    lib/rindle/live_view.ex
    lib/rindle/admin/router.ex
    lib/rindle/admin/components.ex
    lib/rindle/admin/live/home_live.ex
    lib/rindle/admin/live/assets_live.ex
    lib/rindle/admin/live/upload_sessions_live.ex
    lib/rindle/admin/live/variants_jobs_live.ex
    lib/rindle/admin/live/runtime_doctor_live.ex
    lib/rindle/admin/live/actions_live.ex
    mix.exs
    .planning/phases/89-console-read-surfaces/89-CONTEXT.md
  </read_first>
  <behavior>
    - With test dependencies loaded, `Rindle.Admin.Router` is loaded and exports `rindle_admin/2`.
    - With test dependencies loaded, shared components and all six LiveView modules are loaded.
    - `mix.exs` still declares `{:phoenix_live_view, "~> 1.0", optional: true}` and no new runtime UI package.
  </behavior>
  <action>Create `test/rindle/admin/optional_dependency_test.exs` following the smoke-test shape from `test/rindle/streaming/provider/mux/optional_dep_test.exs`. Assert `Code.ensure_loaded?(Rindle.Admin.Router)`, `function_exported?(Rindle.Admin.Router, :rindle_admin, 2)`, `Code.ensure_loaded?(Rindle.Admin.Components)`, and `Code.ensure_loaded?` for `Rindle.Admin.Live.HomeLive`, `AssetsLive`, `UploadSessionsLive`, `VariantsJobsLive`, `RuntimeDoctorLive`, and `ActionsLive` when LiveView is available in the test environment. Add a source assertion over `mix.exs` that `phoenix_live_view` remains `optional: true` and that no new runtime UI dependency strings `tailwind`, `daisy`, `shadcn`, `radix`, or `tailwind_ui` were introduced. This test supplements but does not replace the real no-optional-deps compile proof required by D-89-06.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/rindle/admin/optional_dependency_test.exs</automated>
    <automated>MIX_ENV=test mix compile --no-optional-deps --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    Source assertion: `test/rindle/admin/optional_dependency_test.exs` names `Rindle.Admin.Router`, `Rindle.Admin.Components`, and all six `Rindle.Admin.Live.*` modules.
    Source assertion: the test checks `optional: true` for `phoenix_live_view` in `mix.exs`.
    Behavior assertion: focused optional dependency test passes with deps loaded.
    Behavior assertion: real no-optional-deps compile command passes.
  </acceptance_criteria>
  <done>With-deps smoke tests prove the guarded admin modules exist when LiveView is available, and local no-optional-deps compile remains green.</done>
</task>

<task type="auto">
  <name>Task 2: Add CI matrix no-optional-deps proof</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    .github/workflows/ci.yml
    RUNNING.md
    mix.exs
    test/rindle/admin/optional_dependency_test.exs
    .planning/phases/89-console-read-surfaces/89-RESEARCH.md
    .planning/phases/89-console-read-surfaces/89-VALIDATION.md
  </read_first>
  <action>Update `.github/workflows/ci.yml` with an ADMIN-06 optional dependency proof in the merge-blocking CI graph. Use the same supported Elixir/OTP matrix shape as the existing `quality` job unless the workflow already centralizes the matrix differently. The job or matrix step must run `MIX_ENV=test mix deps.get --no-optional-deps` followed by `MIX_ENV=test mix compile --no-optional-deps --warnings-as-errors`. Keep `phoenix_live_view` optional in `mix.exs`; do not add a required dependency or new frontend dependency. Wire the proof so it runs on PR/push with the regular CI workflow and is visible in branch-protection setup naming. If adding a separate job, name it `optional-dependencies` or another exact readable ADMIN-06 name and make downstream release/required jobs include it only where the existing dependency graph requires all merge-blocking checks. Implement D-89-05, D-89-06, and D-89-07.</action>
  <verify>
    <automated>MIX_ENV=test mix compile --no-optional-deps --warnings-as-errors</automated>
    <automated>MIX_ENV=test mix test test/rindle/admin/optional_dependency_test.exs</automated>
    <automated>rg -F "mix compile --no-optional-deps --warnings-as-errors" .github/workflows/ci.yml</automated>
    <automated>rg -F "phoenix_live_view" mix.exs && rg -F "optional: true" mix.exs</automated>
  </verify>
  <acceptance_criteria>
    Source assertion: `.github/workflows/ci.yml` contains the exact command `mix compile --no-optional-deps --warnings-as-errors`.
    Source assertion: `.github/workflows/ci.yml` runs the proof under a supported Elixir/OTP matrix, matching the existing quality matrix unless the workflow centralizes versions.
    Behavior assertion: local no-optional-deps compile command exits 0 after all admin modules exist.
    Source assertion: `mix.exs` still marks `phoenix_live_view` as `optional: true` and has no new required runtime UI dependency.
  </acceptance_criteria>
  <done>CI proves no-optional-deps compilation for the admin console boundary, and optional LiveView remains optional for default adopters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| optional deps -> default install | Console-specific modules must not require LiveView in default installs. |
| CI matrix -> release confidence | CI proof becomes the automated gate for ADMIN-06. |
| with-deps tests -> module exports | Smoke tests verify guarded modules exist only when dependencies are available. |
| dependency config -> adopters | `mix.exs` controls whether adopters receive new required dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-30 | Denial of Service | default/non-console compile | mitigate | Add real `mix compile --no-optional-deps --warnings-as-errors` proof locally and in CI. |
| T-89-31 | Tampering | `mix.exs` dependency posture | mitigate | Tests and verification assert `phoenix_live_view` remains `optional: true` and no new runtime UI dependency is added. |
| T-89-32 | Repudiation | CI evidence | mitigate | CI workflow contains named ADMIN-06 optional dependency proof visible in PR/push runs. |
| T-89-33 | Elevation of Privilege | guarded module leakage | mitigate | No-optional-deps compile fails if unguarded Phoenix/LiveView references leak into default installs. |
| T-89-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install task is planned; CI uses existing Mix dependencies only. |
</threat_model>

<verification>
Phase-level checks after this plan:
- `MIX_ENV=test mix test test/rindle/admin/router_test.exs test/rindle/admin/assets_test.exs test/rindle/admin/queries_test.exs test/rindle/admin/live/home_assets_upload_test.exs test/rindle/admin/live/variants_runtime_actions_test.exs test/rindle/admin/live_update_test.exs test/rindle/admin/optional_dependency_test.exs`
- `MIX_ENV=test mix compile --no-optional-deps --warnings-as-errors`
- `mix coveralls`
- No schema files are modified; no schema push is required.
</verification>

<success_criteria>
- `phoenix_live_view` remains optional.
- Console modules compile with LiveView present.
- Default/no-optional-deps compile passes locally and is wired into CI.
- No new runtime UI framework, registry, or host asset tooling dependency is introduced.
</success_criteria>

<output>
Create `.planning/phases/89-console-read-surfaces/89-07-SUMMARY.md` when done.
</output>

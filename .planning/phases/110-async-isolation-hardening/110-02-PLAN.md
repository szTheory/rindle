---
phase: 110-async-isolation-hardening
plan: 02
type: execute
wave: 2
depends_on: ["110-01"]
files_modified:
  - test/support/counting_failing_txn_repo.ex
  - test/rindle/delivery/streaming_dispatch_test.exs
  - test/rindle/owner_erasure_batch_proof_test.exs
  - test/rindle/batch_owner_erasure_task_test.exs
autonomous: true
requirements: [ISO-03]
must_haves:
  truths:
    - "with_counting_repo/2 sets the repo via Config.put_repo_override/1 in the calling process and clears it via Config.delete_repo_override/0 in `after`; it performs NO Application.put_env/delete_env(:rindle, :repo, …)."
    - "The fail-after config (:rindle, :counting_failing_txn_repo) is stored in and read from the process dictionary; fail_after/0 and fail_reason/0 read the process dict, not Application env; no global mutation remains in the double."
    - "StreamingDispatchTest, OwnerErasureBatchProofTest, and BatchOwnerErasureTaskTest are `async: true` and their existing assertions still pass."
  artifacts:
    - "test/support/counting_failing_txn_repo.ex — process-scoped with_counting_repo/2 + process-dict fail config"
    - "three test modules restored to async: true with updated (root-caused, not deferred) header comments"
  key_links:
    - "with_counting_repo/2 -> Config.put_repo_override(__MODULE__) (override) + Process.put(fail-config key) ; `after` -> Config.delete_repo_override/0 + Process.delete(fail-config key)"
  prohibitions:
    - "NO Application.put_env(:rindle, :repo, …) and NO Application.delete_env(:rindle, :repo, …) may remain anywhere in test/support/counting_failing_txn_repo.ex (the global save/restore dance for :repo is deleted)."
    - "NO Application.put_env/delete_env(:rindle, :counting_failing_txn_repo, …) may remain in the double — the fail config moves to the process dict (D-04)."
    - "Do NOT touch the Rindle.Repo.__info__(:functions) passthrough generator (lines 66-73) — it STAYS (it guarantees proxy completeness)."
    - "Do NOT promote the OTHER doubles' modules (TestRepoProbe/FailingTransactionRepo: maintenance_workers_test, upload_maintenance_test, broker_test) to async: true (D-06, out of scope)."
---

<objective>
Migrate `Rindle.Test.CountingFailingTxnRepo` off ALL global state: `with_counting_repo/2` installs the
double via the process-local `Config.put_repo_override/1` (Plan 01) instead of
`Application.put_env(:rindle, :repo, …)`, and the fail-after/fail-reason config moves to the process
dictionary too (D-04). With no global mutation left in the double, revert the three modules that were
demoted to `async: false` SOLELY for the repo-swap reason back to `async: true` (D-05), recovering the
serialization tax the workaround imposed (research §6).

Purpose: the double becomes process-scoped (like Sandbox/Mox) and can no longer pollute a concurrent
async reader. ISO-03.
Output: process-scoped `counting_failing_txn_repo.ex`; `StreamingDispatchTest`,
`OwnerErasureBatchProofTest`, `BatchOwnerErasureTaskTest` back to `async: true` with root-caused comments.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/110-async-isolation-hardening/110-CONTEXT.md
@.planning/research/v1.21-ASYNC-ISOLATION.md
@.planning/phases/110-async-isolation-hardening/110-01-SUMMARY.md
@test/support/counting_failing_txn_repo.ex
</context>

<artifacts_this_phase_produces>
This plan (110-02) consumes `Config.put_repo_override/1` + `Config.delete_repo_override/0` (from 110-01)
and produces: a process-scoped `with_counting_repo/2`, process-dict `fail_after/0` + `fail_reason/0`, and
the three `async: true` re-promotions. No new public symbols.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate with_counting_repo/2 + fail config to the process dictionary</name>
  <files>test/support/counting_failing_txn_repo.ex</files>
  <read_first>
    - test/support/counting_failing_txn_repo.ex:6-21 — `with_counting_repo/2`, the global mutation site (the `put_env(:rindle, :repo)` + the `:counting_failing_txn_repo` config + the `restore_env` save/restore dance to delete).
    - test/support/counting_failing_txn_repo.ex:83-94 — `fail_after/0`, `fail_reason/0` (read `Application.get_env(:rindle, :counting_failing_txn_repo)`), `restore_env/2` (to delete).
    - test/support/counting_failing_txn_repo.ex:66-73 — the `Rindle.Repo.__info__(:functions)` passthrough generator that STAYS (do not touch).
    - .planning/phases/110-async-isolation-hardening/110-CONTEXT.md D-03 (lines 56-59) + D-04 (lines 61-63) — repo via Config.put_repo_override/1, fail config to process dict, save/restore dance deleted.
    - .planning/research/v1.21-ASYNC-ISOLATION.md §1.2 (lines 24-44) + §8 ISO-03 bullet (line 255) — the mutation site and the requirement-ready statement.
    - lib/rindle/config.ex — the `put_repo_override/1` / `delete_repo_override/0` setters from Plan 01 (the sanctioned alternative).
  </read_first>
  <behavior>
    - Inside `with_counting_repo(n, fun)`: `Rindle.Config.repo()` returns `Rindle.Test.CountingFailingTxnRepo` in the calling process, and the double's `transaction/1` force-fails on the nth call (existing fail-after semantics preserved).
    - After `with_counting_repo/2` returns (including on exception via `after`): the override is cleared and `Rindle.Config.repo()` returns the app-env value again; the transaction count is reset.
    - `fail_after/0` returns the n passed to the active `with_counting_repo`; `fail_reason/0` returns its configured/default reason (`:forced_batch_failure`) — both read the process dict.
    - No `Application.put_env`/`delete_env` of `:rindle, :repo` or `:rindle, :counting_failing_txn_repo` is executed at any point.
    - The transaction passthrough surface (every Repo function except `transaction/1,2`) is unchanged.
  </behavior>
  <action>
    Rewrite `with_counting_repo/2` to be fully process-scoped: replace
    `Application.put_env(:rindle, :repo, __MODULE__)` with `Rindle.Config.put_repo_override(__MODULE__)`;
    store the fail-after config in the process dictionary under a module-local key (e.g.
    `@config_key {__MODULE__, :fail_config}` via `Process.put(@config_key, fail_after: fail_after)`)
    instead of `Application.put_env(:rindle, :counting_failing_txn_repo, …)`. DELETE the
    `previous_repo`/`previous_cfg` capture and the entire `restore_env/2` save/restore dance — a
    process-dict override needs no global save/restore. In the `after` block, call
    `Rindle.Config.delete_repo_override()`, `Process.delete(@config_key)`, and the existing `reset_count()`.
    Repoint `fail_after/0` and `fail_reason/0` to read `Process.get(@config_key, [])` then `Keyword.get`
    (same default `:forced_batch_failure` for fail_reason). Remove the now-unused `restore_env/2` clauses.
    DO NOT modify the `Rindle.Repo.__info__(:functions)` passthrough generator (lines 66-73) or the
    `transaction/*` overrides. Update the head comment block (currently lines 58-65) so it no longer says
    the module installs a GLOBAL `:rindle, :repo` — describe it as a process-local override set via
    `Rindle.Config.put_repo_override/1`. (Keep the rationale that the proxy must be complete.)
  </action>
  <verify>
    <automated>! grep -nE 'Application\.(put_env|delete_env)\(:rindle, :repo' test/support/counting_failing_txn_repo.ex && ! grep -nE 'Application\.(put_env|delete_env)\(:rindle, :counting_failing_txn_repo' test/support/counting_failing_txn_repo.ex && grep -n 'Config.put_repo_override' test/support/counting_failing_txn_repo.ex && grep -n 'Rindle.Repo.__info__(:functions)' test/support/counting_failing_txn_repo.ex && mix compile --warnings-as-errors 2>&1 | tail -3</automated>
  </verify>
  <done>
    `with_counting_repo/2` uses `Config.put_repo_override/1` + a process-dict fail-config key and clears
    both (plus the count) in `after`; `fail_after/0`/`fail_reason/0` read the process dict; NO
    `Application.put_env`/`delete_env` of `:rindle, :repo` or `:rindle, :counting_failing_txn_repo` remains;
    the `__info__(:functions)` passthrough generator is untouched; module compiles clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Revert the 3 repo-swap-demoted modules to async: true with root-caused comments</name>
  <files>test/rindle/delivery/streaming_dispatch_test.exs, test/rindle/owner_erasure_batch_proof_test.exs, test/rindle/batch_owner_erasure_task_test.exs</files>
  <read_first>
    - test/rindle/delivery/streaming_dispatch_test.exs:21-26 — the `async: false` header comment (the deferred-root-cause note) + the `use Rindle.DataCase, async: false` line to flip.
    - test/rindle/owner_erasure_batch_proof_test.exs:1-7 — the `async: false` comment + `use Rindle.DataCase, async: false` (line 7); calls `with_counting_repo` at lines 84,102.
    - test/rindle/batch_owner_erasure_task_test.exs:1-2 — `use Rindle.DataCase, async: false` (line 2); calls `with_counting_repo` at line 95.
    - .planning/phases/110-async-isolation-hardening/110-CONTEXT.md D-05 (lines 65-75) — the THREE modules to promote and why; D-06 (lines 76-79) — the OTHER-double modules that MUST NOT be promoted.
    - .planning/research/v1.21-ASYNC-ISOLATION.md §6 (lines 222-226) — recovering the serialization tax.
  </read_first>
  <behavior>
    - All three modules run under `async: true` (their `use Rindle.DataCase` header declares `async: true`).
    - DataCase gives each an isolated, non-shared Sandbox owner under async (`shared: not tags[:async]`), so their DB assertions stay correct.
    - `with_counting_repo` callers (proof + task tests) still see the double in their own process and still observe the force-failed nth transaction; their existing assertions pass.
    - StreamingDispatchTest's Branch 5/5b `Config.repo()` reads resolve to the real repo (no concurrent process pollutes its result), so its dispatch matrix stays deterministic.
  </behavior>
  <action>
    Flip `use Rindle.DataCase, async: false` → `use Rindle.DataCase, async: true` in all three modules.
    For `streaming_dispatch_test.exs`: rewrite the header comment (lines 21-25) — the global-pollution
    workaround is now ROOT-CAUSED (the counting double is process-scoped via `Config.put_repo_override/1`),
    not deferred; state that the suite is safe to run async because the only pollution source was the
    counting double's former global `:rindle, :repo` swap, now eliminated in Phase 110. For
    `owner_erasure_batch_proof_test.exs`: rewrite its `async: false` rationale comment (lines 2-6) the same
    way — the double it uses no longer swaps the global repo, so it is clean to promote. For
    `batch_owner_erasure_task_test.exs` (no rationale comment today): just flip the flag. Do NOT touch any
    other module (D-06: the TestRepoProbe/FailingTransactionRepo modules stay `async: false`).
  </action>
  <verify>
    <automated>grep -c 'use Rindle.DataCase, async: true' test/rindle/delivery/streaming_dispatch_test.exs test/rindle/owner_erasure_batch_proof_test.exs test/rindle/batch_owner_erasure_task_test.exs && mix test test/rindle/delivery/streaming_dispatch_test.exs test/rindle/owner_erasure_batch_proof_test.exs test/rindle/batch_owner_erasure_task_test.exs 2>&1 | tail -8</automated>
  </verify>
  <done>
    All three modules declare `async: true`; their full test suites pass under async; StreamingDispatchTest's
    and OwnerErasureBatchProofTest's header comments describe the now-root-caused isolation (not a deferred
    workaround); no OTHER module's async flag changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test double → production resolver | The counting double now writes a process-local override instead of a global key; test-only, no adopter surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-110-03 | Tampering | cross-test global-state pollution via `with_counting_repo/2` | medium | mitigate | Eliminate the global `Application.put_env(:rindle, :repo, …)` entirely; the override is process-dictionary-scoped so a concurrent async reader in another process tree never observes it. Verified by the negative-grep gate (no `put_env`/`delete_env` of either `:rindle` key remains) and by Plan 04's concurrency proof. |

Test-support-only change. No external input, no auth/data-flow surface, no schema/migration changes in scope. No high-severity threats.
</threat_model>

<verification>
- No `Application.put_env`/`delete_env` of `:rindle, :repo` or `:rindle, :counting_failing_txn_repo` remains in `test/support/counting_failing_txn_repo.ex`.
- The `Rindle.Repo.__info__(:functions)` passthrough generator is intact.
- The three promoted modules are `async: true` and their suites pass.
- The OTHER-double modules (`maintenance_workers_test`, `upload_maintenance_test`, `broker_test`) are NOT modified (D-06) — verify their `async: false` is untouched.
</verification>

<success_criteria>
ISO-03 satisfied: the counting double is fully process-scoped (no global mutation), and the three
modules demoted solely for the repo-swap reason are back to `async: true` with passing suites.
</success_criteria>

<output>
Create `.planning/phases/110-async-isolation-hardening/110-02-SUMMARY.md` when done.
</output>

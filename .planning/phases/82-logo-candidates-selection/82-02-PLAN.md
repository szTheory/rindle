---
phase: 82
plan: 02
title: Comparison sheet + user selection checkpoint
requirements: [BRAND-03]
status: in_progress
created: 2026-06-10
---

# 82-02: Contact sheet and CHECKPOINT

## Objective

Present the five candidates to the user on a single comparison sheet that makes the
choice informed (size ladder with true 16px, background gauntlet, mono test, dark
README mock, subtitle variant), then **halt for the user's selection**. The executor
never picks the winner.

## Tasks

1. `sheet.mjs` generates `brandbook/assets/logo/candidates/sheet.html` (no-JS,
   file://-safe); Playwright renders committed `sheet.png` evidence + per-row shots.
2. **CHECKPOINT: user selects the winning direction** (with optional refinement notes,
   including mix-and-match, e.g. "B with C's favicon treatment").
3. Record the pick as decision D-b1.0-04 in STATE.md / phase summary; losing candidates
   stay committed for provenance.

## Verification

- sheet.html renders from file:// with all 40 tiles populated.
- User selection recorded before any Phase 83 work begins.

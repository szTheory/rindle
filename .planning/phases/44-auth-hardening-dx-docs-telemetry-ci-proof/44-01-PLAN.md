---
phase: 44-auth-hardening-dx-docs-telemetry-ci-proof
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/delivery/webhook_plug.ex
  - test/rindle/delivery/webhook_plug_test.exs
  - lib/rindle/error.ex
  - test/rindle/error_test.exs
  - test/rindle/upload/tus_plug_test.exs
  - .planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-POLISH-02-AUDIT.md
autonomous: true
requirements:
  - TUS-10
  - TUS-11
  - POLISH-02
must_haves:
  truths:
    - "A rejected same-user resume authorization returns 401 for tus resume requests and never falls through to success."
    - "The public tus error vocabulary stays limited to the locked six reasons and gives client-specific recovery guidance."
    - "A missing webhook body_reader cannot silently expand the webhook body cap beyond 1_048_576 bytes."
    - "POLISH-02 explicitly records all 13 Phase 35 advisory findings: WR-01 as fixed in Phase 44, WR-02 through WR-06 as already resolved or explicitly waived, and IN-01 through IN-07 as already resolved or explicitly waived with rationale per D-12 and D-13."
  artifacts:
    - path: "lib/rindle/delivery/webhook_plug.ex"
      provides: "1 MiB-capped fallback body read for signed webhook ingestion"
      contains: "Plug.Conn.read_body(conn, length: 1_048_576)"
    - path: "lib/rindle/error.ex"
      provides: "Stable fix-oriented tus public messages"
      contains: "tus-js-client"
    - path: "test/rindle/upload/tus_plug_test.exs"
      provides: "Optional resume-authorizer regression coverage"
      contains: "resume authorizer"
    - path: ".planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-POLISH-02-AUDIT.md"
      provides: "Explicit disposition of the full Phase 35 advisory set for Phase 44 POLISH-02 closure"
      contains: "IN-07"
  key_links:
    - from: "lib/rindle/delivery/webhook_plug.ex"
      to: "test/rindle/delivery/webhook_plug_test.exs"
      via: "fallback body-read regression"
      pattern: "length: 1_048_576"
    - from: "lib/rindle/error.ex"
      to: "test/rindle/error_test.exs"
      via: "exact message contract"
      pattern: "removeFingerprintOnSuccess"
    - from: "test/rindle/upload/tus_plug_test.exs"
      to: "lib/rindle/upload/tus_plug.ex"
      via: "resume authorizer 401 contract"
      pattern: "status == 401"
    - from: ".planning/milestones/v1.6-phases/35-signed-webhook-plug-idempotent-ingest/35-REVIEW.md"
      to: ".planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-POLISH-02-AUDIT.md"
      via: "explicit WR-01..WR-06 plus IN-01..IN-07 disposition audit"
      pattern: "(WR-0[1-6]|IN-0[1-7])"
---

<objective>
Lock the Phase 44 auth/error contract and close the remaining high-signal Phase 35 trust-boundary code-review debt.

Purpose: Preserve the adopter-owned auth model from D-01 through D-05 while closing WR-01 per D-12 and keeping tus operator guidance accurate.
Output: A capped webhook fallback path, exact tus error-message contracts, resume-authorizer regression coverage, and a Phase 35 WR-01..WR-06 plus IN-01..IN-07 disposition audit.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-CONTEXT.md
@.planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-RESEARCH.md
@.planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-PATTERNS.md
@.planning/milestones/v1.6-phases/35-signed-webhook-plug-idempotent-ingest/35-REVIEW.md

<interfaces>
From `lib/rindle/upload/tus_plug.ex`:
```elixir
defp authorize_resume(conn, payload, session, method, opts) do
  case opts[:resume_authorizer] do
    nil -> :ok
    authorizer ->
      actor = opts[:identity_fn].(conn)

      case authorizer.authorize(actor, :resume, %{
             token_actor: Map.get(payload, "actor"),
             session: session,
             profile: opts[:profile],
             method: method
           }) do
        :ok -> :ok
        :reject -> {:error, :resume_rejected}
      end
  end
end
```

From `lib/rindle/error.ex`:
```elixir
def message(%{reason: :tus_session_not_found}) do
  """
  The tus upload session could not be found.
  """
  |> String.trim()
end
```

From `lib/rindle/delivery/webhook_plug.ex`:
```elixir
case Plug.Conn.read_body(conn) do
  {:ok, body, conn} when byte_size(body) > 0 -> {:ok, body, conn}
  _ -> {:error, :body_missing}
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cap the webhook fallback body read and prove WR-01 closed</name>
  <read_first>
    - lib/rindle/delivery/webhook_plug.ex
    - test/rindle/delivery/webhook_plug_test.exs
    - .planning/milestones/v1.6-phases/35-signed-webhook-plug-idempotent-ingest/35-REVIEW.md
    - .planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-CONTEXT.md
  </read_first>
  <files>lib/rindle/delivery/webhook_plug.ex, test/rindle/delivery/webhook_plug_test.exs</files>
  <behavior>
    - Failing-path test: a POST without `conn.assigns[:raw_body]` and with a body larger than `1_048_576` bytes returns `500` with body `server_misconfigured`.
    - Failing-path test: the fallback `Plug.Conn.read_body/2` path treats `{:more, partial, conn}` as `:body_missing` instead of accepting a partial oversized payload.
    - Passing-path behavior: the existing `WebhookBodyReader.raw_body/1` path remains unchanged for normal configured mounts.
  </behavior>
  <action>Update `fetch_raw_body/1` in `lib/rindle/delivery/webhook_plug.ex` so the fallback branch calls `Plug.Conn.read_body(conn, length: 1_048_576)`, returns `{:error, :body_missing}` for `{:more, _partial, _conn}`, and preserves the current `500 server_misconfigured` response mapping. Extend `test/rindle/delivery/webhook_plug_test.exs` with a regression that posts a signed body larger than `1_048_576` bytes without the `:raw_body` assign and asserts the Plug never accepts the oversized fallback payload. This task implements D-12 by closing WR-01 exactly, without widening the public webhook error vocabulary.</action>
  <acceptance_criteria>
    - `rg -n "Plug\\.Conn\\.read_body\\(conn, length: 1_048_576\\)" lib/rindle/delivery/webhook_plug.ex` returns one match.
    - `rg -n "\\{:more, _partial, _conn\\} -> \\{:error, :body_missing\\}" lib/rindle/delivery/webhook_plug.ex` returns one match.
    - `rg -n "1_048_576|server_misconfigured" test/rindle/delivery/webhook_plug_test.exs` returns the new fallback-cap regression assertions.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rindle/delivery/webhook_plug_test.exs -x</automated>
  </verify>
  <done>`WebhookPlug` enforces the same 1 MiB cap on both the documented body-reader path and the fallback path, and the regression test proves oversized fallback bodies never enter the signed-webhook ingest pipeline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze optional resume authorization and tus error guidance to the locked contract</name>
  <read_first>
    - lib/rindle/error.ex
    - test/rindle/error_test.exs
    - test/rindle/upload/tus_plug_test.exs
    - lib/rindle/upload/tus_plug.ex
    - .planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-CONTEXT.md
    - .planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-RESEARCH.md
  </read_first>
  <files>lib/rindle/error.ex, test/rindle/error_test.exs, test/rindle/upload/tus_plug_test.exs</files>
  <behavior>
    - Error-message test: `Rindle.Error.message/1` for `:tus_session_not_found` says `removeFingerprintOnSuccess: true` is for `tus-js-client` and does not prescribe that option for modern `@uppy/tus`.
    - Resume-authorizer test: with `config :rindle, :tus_resume_authorizer, Rindle.TusResumeAuthorizerMock`, `HEAD`, `PATCH`, and `DELETE` return `401` when the authorizer yields `:reject`.
    - Default-path test: with no configured authorizer, the same signed tus URL still relies on HMAC alone and the request path remains resumable per D-01.
    - Signature-regression tests: tampered, missing, and expired signed tus URLs return `401` or `404` and never `200` across the resumable edge.
  </behavior>
  <action>Keep the public tus-facing reason set exactly `:tus_session_not_found`, `:tus_session_expired`, `:tus_offset_conflict`, `:tus_size_exceeded`, `:tus_url_signature_invalid`, and `{:upload_unsupported, :tus_upload}` per D-04 and D-05. Rewrite the fix copy for `:tus_session_not_found` in `lib/rindle/error.ex` so it explicitly says `removeFingerprintOnSuccess: true` applies to `tus-js-client`, while modern `@uppy/tus` resumes and fingerprint cleanup are automatic. Update `test/rindle/error_test.exs` to assert the exact new string. Extend `test/rindle/upload/tus_plug_test.exs` to cover all three resumable methods (`HEAD`, `PATCH`, `DELETE`) returning `401` on `:reject`, to prove the default nil-authorizer path still honors D-01/D-03 by permitting a signed request through HMAC validation alone, and to add explicit tampered-signature, missing-token, and expired-signature contract cases that assert the resumable edge returns `401` or `404` and never `200`. Do not add new public tus reason atoms or a parallel HTTP error abstraction.</action>
  <acceptance_criteria>
    - `rg -n "tus-js-client|@uppy/tus|removeFingerprintOnSuccess" lib/rindle/error.ex test/rindle/error_test.exs` shows the client-specific wording and matching exact-contract test.
    - `rg -n "status == 401|resume_authorizer|:reject" test/rindle/upload/tus_plug_test.exs` shows HEAD, PATCH, and DELETE rejection coverage.
    - `rg -n "tampered|expired|missing token|tus_url_signature_invalid|status == 404|status == 401" test/rindle/upload/tus_plug_test.exs` shows explicit invalid-signature, missing-token, and expired-signature regressions.
    - `rg -n "tus_session_not_found|tus_session_expired|tus_offset_conflict|tus_size_exceeded|tus_url_signature_invalid" test/rindle/error_test.exs` still lists only the locked public tus reasons.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rindle/error_test.exs test/rindle/upload/tus_plug_test.exs -x</automated>
  </verify>
  <done>The optional resume-authorizer contract is regression-locked, the HMAC-only default remains the baseline, and the public tus error guidance is accurate for current client behavior without widening the API surface.</done>
</task>

<task type="auto">
  <name>Task 3: Record the full Phase 35 advisory-set disposition audit for POLISH-02</name>
  <read_first>
    - .planning/milestones/v1.6-phases/35-signed-webhook-plug-idempotent-ingest/35-REVIEW.md
    - .planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-CONTEXT.md
    - lib/rindle/delivery/webhook_plug.ex
    - lib/rindle/streaming/provider/mux.ex
    - lib/rindle/ops/runtime_status.ex
    - lib/rindle/delivery/webhook_plug.ex
  </read_first>
  <files>.planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-POLISH-02-AUDIT.md</files>
  <action>Create `.planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-POLISH-02-AUDIT.md` as the explicit Phase 44 POLISH-02 trace artifact. This task must cover the full advisory set from `.planning/milestones/v1.6-phases/35-signed-webhook-plug-idempotent-ingest/35-REVIEW.md`: WR-01 through WR-06 and IN-01 through IN-07. For WR-01, record that this plan fixes the fallback body-cap gap per D-12. For WR-02 through WR-06, compare the current tree against the review and record each item as either already resolved in code or explicitly waived with rationale per D-13, including the supporting file path and the reason the current state satisfies or intentionally does not change the finding. For IN-01 through IN-07, record each item as already resolved or explicitly waived with rationale, again with supporting file paths and a concrete reason. Do not leave any warning or info finding implicit, collapsed into a blanket statement, or omitted from the audit.</action>
  <acceptance_criteria>
    - `rg -n "^## WR-0[1-6]|^## IN-0[1-7]" .planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-POLISH-02-AUDIT.md` returns thirteen sections.
    - `rg -n "resolved|waived|rationale|file:" .planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-POLISH-02-AUDIT.md` shows an explicit disposition and supporting reference for each warning and info item.
    - `rg -n "D-12|D-13" .planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-POLISH-02-AUDIT.md` shows the user-decision trace for the audit outcome.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "^## WR-0[1-6]|^## IN-0[1-7]" .planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-POLISH-02-AUDIT.md && test "$(rg -c "^## WR-0[1-6]|^## IN-0[1-7]" .planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-POLISH-02-AUDIT.md)" -eq 13 && rg -n "D-12|D-13|resolved|waived|rationale|file:" .planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-POLISH-02-AUDIT.md'</automated>
  </verify>
  <done>Phase 44 leaves a concrete POLISH-02 audit artifact that closes the checker gap: WR-01 is fixed here, WR-02 through WR-06 are individually accounted for as resolved or waived with rationale, and IN-01 through IN-07 are also individually accounted for as resolved or waived with rationale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external webhook request → `WebhookPlug` | Untrusted request body crosses into signed-webhook ingest. |
| browser tus client → `TusPlug` auth layer | Bearer tus URL crosses from client storage back into resumable requests. |
| public `Rindle.Error` messages → adopters | Operator guidance must not encode incorrect recovery steps that cause stale resumptions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01-01 | D | `lib/rindle/delivery/webhook_plug.ex` | mitigate | Cap fallback `Plug.Conn.read_body/2` at `1_048_576` bytes and reject `{:more, ...}` so a missing `body_reader` cannot accept oversized signed payloads. |
| T-44-01-02 | S | `lib/rindle/upload/tus_plug.ex` | mitigate | Preserve optional same-user authorization per D-01/D-03 and prove `:reject` returns `401` for resumable methods without bypassing HMAC verification. |
| T-44-01-03 | T | `lib/rindle/error.ex` | mitigate | Keep the public tus reason set frozen per D-04/D-05 and correct the client-specific recovery text so adopters do not apply the wrong resume cleanup semantics. |
</threat_model>

<verification>
- Run `mix test test/rindle/delivery/webhook_plug_test.exs test/rindle/error_test.exs test/rindle/upload/tus_plug_test.exs -x`.
- Confirm the new webhook fallback-cap grep checks and exact error-message contract strings are present.
- Run the advisory-set audit grep checks against `.planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-POLISH-02-AUDIT.md`.
</verification>

<success_criteria>
- Oversized fallback webhook bodies are rejected before verification/enqueue and the regression test proves the 1 MiB ceiling.
- Resume authorization remains optional by default, but explicit rejection returns `401` for HEAD/PATCH/DELETE.
- The public tus error surface stays thin and the guidance is accurate for `tus-js-client` versus `@uppy/tus`.
- POLISH-02 explicitly accounts for WR-01 through WR-06 and IN-01 through IN-07, with WR-01 fixed and every remaining advisory item individually dispositioned.
</success_criteria>

<output>
After completion, create `.planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-01-SUMMARY.md`
</output>

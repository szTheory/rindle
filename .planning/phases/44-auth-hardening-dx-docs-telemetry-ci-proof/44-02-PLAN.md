---
phase: 44-auth-hardening-dx-docs-telemetry-ci-proof
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/upload/resumable_telemetry.ex
  - test/rindle/contracts/telemetry_contract_test.exs
  - lib/rindle/ops/runtime_checks.ex
  - test/rindle/ops/runtime_checks_test.exs
autonomous: true
requirements:
  - TUS-12
  - TUS-13
must_haves:
  truths:
    - "tus telemetry stays in the existing `[:rindle, :upload, :resumable, *]` namespace and exposes `protocol: :tus` instead of a new family."
    - "Forbidden tus metadata such as `session_uri`, `upload_key`, `headers`, and `body` never reaches public telemetry events."
    - "`mix rindle.doctor` reports tus capability drift from `config :rindle, :tus_profiles, [...]` without guessing router mounts."
  artifacts:
    - path: "test/rindle/contracts/telemetry_contract_test.exs"
      provides: "Public resumable telemetry contract coverage for tus metadata hygiene"
      contains: "protocol"
    - path: "lib/rindle/ops/runtime_checks.ex"
      provides: "Config-driven tus capability check"
      contains: "doctor.tus_capability"
    - path: "test/rindle/ops/runtime_checks_test.exs"
      provides: "Stable doctor tus mismatch assertions"
      contains: ":tus_profiles"
  key_links:
    - from: "lib/rindle/upload/resumable_telemetry.ex"
      to: "test/rindle/contracts/telemetry_contract_test.exs"
      via: "allowlist + forbidden-key contract"
      pattern: "protocol"
    - from: "lib/rindle/ops/runtime_checks.ex"
      to: "test/rindle/ops/runtime_checks_test.exs"
      via: "doctor.tus_capability report ids and fix copy"
      pattern: "doctor\\.tus_capability"
---

<objective>
Freeze the tus telemetry and doctor surfaces to the existing resumable contract so adopters get accurate observability and setup diagnostics without new public namespaces.

Purpose: Implement D-06 through D-08 by proving telemetry hygiene and config-driven doctor checks at the contract-test layer.
Output: Locked telemetry contract assertions and doctor mismatch coverage for tus profiles.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-CONTEXT.md
@.planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-RESEARCH.md
@.planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-PATTERNS.md

<interfaces>
From `lib/rindle/upload/resumable_telemetry.ex`:
```elixir
@start_event [:rindle, :upload, :resumable, :start]
@patch_event [:rindle, :upload, :resumable, :patch]
@stop_event [:rindle, :upload, :resumable, :stop]
@status_event [:rindle, :upload, :resumable, :status]
@cancel_event [:rindle, :upload, :resumable, :cancel]
@allowed_metadata_keys [:state, :outcome, :reason, :source, :protocol]
@forbidden_metadata_keys [:session_uri, :upload_key, :headers, :body, :session_id]
```

From `lib/rindle/ops/runtime_checks.ex`:
```elixir
defp check_tus_capability(profiles) do
  mismatches =
    Enum.filter(tus_profiles(profiles), fn profile ->
      adapter = safely_storage_adapter(profile)
      not is_atom(adapter) or not Rindle.Storage.Capabilities.supports?(adapter, :tus_upload)
    end)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock tus telemetry into the resumable namespace with explicit metadata hygiene</name>
  <read_first>
    - lib/rindle/upload/resumable_telemetry.ex
    - test/rindle/contracts/telemetry_contract_test.exs
    - lib/rindle/upload/tus_plug.ex
    - .planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-CONTEXT.md
  </read_first>
  <files>lib/rindle/upload/resumable_telemetry.ex, test/rindle/contracts/telemetry_contract_test.exs</files>
  <behavior>
    - Contract test: tus start/patch/stop emissions stay under `[:rindle, :upload, :resumable, *]` and never emit `[:rindle, :upload, :tus, *]`.
    - Contract test: emitted metadata includes `protocol: :tus` and excludes `:session_uri`, `:upload_key`, `:headers`, and `:body`.
    - Contract test: the existing public resumable event allowlist remains unchanged except for tus using the same family.
  </behavior>
  <action>Keep `lib/rindle/upload/resumable_telemetry.ex` on the existing public namespace per D-07. Extend `test/rindle/contracts/telemetry_contract_test.exs` so it emits tus-flavored resumable events through `ResumableTelemetry.emit_start/5`, `emit_patch/5`, and `emit_stop/5`, asserts `metadata.protocol == :tus`, and refutes any presence of `:session_uri`, `:upload_key`, `:headers`, or `:body`. Do not add a `[:rindle, :upload, :tus, *]` namespace, and do not widen the allowlist beyond the current low-cardinality keys.</action>
  <acceptance_criteria>
    - `rg -n "\\[:rindle, :upload, :resumable, :(start|patch|stop|status|cancel)\\]" test/rindle/contracts/telemetry_contract_test.exs` shows the contract still targets the resumable namespace.
    - `rg -n "protocol == :tus|refute Map\\.has_key\\?\\(.*session_uri|upload_key|headers|body" test/rindle/contracts/telemetry_contract_test.exs` shows explicit tus metadata assertions.
    - `rg -n "@allowed_metadata_keys \\[:state, :outcome, :reason, :source, :protocol\\]" lib/rindle/upload/resumable_telemetry.ex` returns one match.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rindle/contracts/telemetry_contract_test.exs --include contract -x</automated>
  </verify>
  <done>The telemetry contract proves tus uses the existing resumable event family with `protocol: :tus` and no secret-bearing metadata leaks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep doctor tus capability checks config-driven and operator-fixable</name>
  <read_first>
    - lib/rindle/ops/runtime_checks.ex
    - test/rindle/ops/runtime_checks_test.exs
    - lib/mix/tasks/rindle.doctor.ex
    - .planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-CONTEXT.md
    - .planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-RESEARCH.md
  </read_first>
  <files>lib/rindle/ops/runtime_checks.ex, test/rindle/ops/runtime_checks_test.exs</files>
  <behavior>
    - Doctor test: an adapter that does not advertise `:tus_upload` yields `doctor.tus_capability` with `status == :error`.
    - Doctor test: the failing summary names the unsupported profile and the fix string mentions `config :rindle, :tus_profiles, [...]`.
    - Doctor test: a valid tus profile yields `doctor.tus_capability` with `status == :ok`.
  </behavior>
  <action>Retain `check_tus_capability/1` in `lib/rindle/ops/runtime_checks.ex` as the single tus drift detector per D-06. Refresh `test/rindle/ops/runtime_checks_test.exs` so the stable assertions cover both the passing profile and a deliberately unsupported profile, require the report id `doctor.tus_capability`, require `check.fix` to mention `config :rindle, :tus_profiles, [...]`, and require the failure summary to tell the operator to mount `TusPlug` only on profiles whose adapters support `:tus_upload` or to remove the profile from `:tus_profiles`. Do not add route introspection, Phoenix-specific probing, or a new runtime tus error atom for this diagnosis path.</action>
  <acceptance_criteria>
    - `rg -n "doctor\\.tus_capability" lib/rindle/ops/runtime_checks.ex test/rindle/ops/runtime_checks_test.exs` returns the report id in both source and tests.
    - `rg -n ":tus_profiles|:tus_upload" test/rindle/ops/runtime_checks_test.exs` shows the exact config and capability assertions.
    - `rg -n "mount TusPlug only|remove the profile from `config :rindle, :tus_profiles" lib/rindle/ops/runtime_checks.ex` shows fix-oriented operator wording.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rindle/ops/runtime_checks_test.exs -x</automated>
  </verify>
  <done>`mix rindle.doctor` remains the supported tus setup-drift surface, with stable check ids and actionable fix text grounded in `:tus_profiles` rather than route introspection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal session state → public telemetry | Sensitive resumable state is transformed into operator-visible events. |
| runtime config → doctor report | Adopter configuration crosses into operator-visible diagnostics and must stay explicit but non-magical. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-02-01 | I | `lib/rindle/upload/resumable_telemetry.ex` | mitigate | Keep metadata allowlisted, assert `protocol: :tus`, and explicitly refute `session_uri`, `upload_key`, `headers`, and `body` in the public contract tests. |
| T-44-02-02 | T | `lib/rindle/ops/runtime_checks.ex` | mitigate | Preserve the stable `doctor.tus_capability` check id and config-based mismatch logic so adopters get one honest source of truth for tus capability drift. |
| T-44-02-03 | R | telemetry + doctor surfaces | mitigate | Lock tests to the exact public namespace and fix strings so silent observability drift is caught in CI. |
</threat_model>

<verification>
- Run `mix test test/rindle/contracts/telemetry_contract_test.exs --include contract -x`.
- Run `mix test test/rindle/ops/runtime_checks_test.exs -x`.
- Grep for `protocol: :tus`, `doctor.tus_capability`, and `:tus_profiles` in the touched files.
</verification>

<success_criteria>
- tus continues to emit through `[:rindle, :upload, :resumable, *]` with low-cardinality `protocol: :tus`.
- No forbidden tus metadata keys are observable in the public telemetry contract.
- Doctor surfaces tus capability drift through the existing config-driven report id and fix copy.
</success_criteria>

<output>
After completion, create `.planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-02-SUMMARY.md`
</output>

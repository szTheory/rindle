---
phase: 44-auth-hardening-dx-docs-telemetry-ci-proof
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/resumable_uploads.md
  - test/install_smoke/support/generated_app_helper.ex
  - test/install_smoke/generated_app_smoke_test.exs
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - TUS-14
must_haves:
  truths:
    - "Adopters have one canonical tus guide with exact parser, CORS, exposed-header, no-silent-downgrade, security, and client-version instructions."
    - "The package-consumer tus proof stays merge-blocking and exercises a real interrupted upload and resume against MinIO."
    - "The guide documents the current single-node or sticky-session S3 posture honestly."
    - "Guide changes are parity-checked against the generated-app proof so the documented client guidance cannot drift from the merge-blocking tus lane."
  artifacts:
    - path: "guides/resumable_uploads.md"
      provides: "Canonical adopter tus setup and security guidance"
      contains: "application/offset+octet-stream"
    - path: "test/install_smoke/support/generated_app_helper.ex"
      provides: "Generated-app tus-js-client proof harness pinned to the supported client version"
      contains: "tus-js-client@4.3.1"
    - path: ".github/workflows/ci.yml"
      provides: "Merge-blocking package-consumer tus proof lane"
      contains: "bash scripts/install_smoke.sh tus"
    - path: "test/install_smoke/generated_app_smoke_test.exs"
      provides: "Docs-parity assertions for the canonical tus guide and generated-app proof"
      contains: "guides/resumable_uploads.md"
  key_links:
    - from: "guides/resumable_uploads.md"
      to: "test/install_smoke/support/generated_app_helper.ex"
      via: "client-specific tus-js-client guidance"
      pattern: "(Plug\\.Parsers|Upload-Offset|Upload-Expires|removeFingerprintOnSuccess|no-silent-downgrade)"
    - from: "test/install_smoke/support/generated_app_helper.ex"
      to: "test/install_smoke/generated_app_smoke_test.exs"
      via: "drop-and-resume assertions"
      pattern: "previous_uploads"
    - from: ".github/workflows/ci.yml"
      to: "scripts/install_smoke.sh"
      via: "package-consumer proof execution"
      pattern: "install_smoke\\.sh tus"
    - from: "guides/resumable_uploads.md"
      to: "test/install_smoke/generated_app_smoke_test.exs"
      via: "docs-parity assertions for bearer guidance and client split"
      pattern: "guides/resumable_uploads\\.md"
---

<objective>
Publish the canonical tus adopter guide and keep the generated-app package-consumer proof merge-blocking and version-correct.

Purpose: Implement D-02, D-09, D-10, and D-11 by making the docs and CI proof the authoritative operator contract for tus.
Output: Updated `guides/resumable_uploads.md` with exact TUS-14 clauses, a pinned and asserted Node proof harness, and a merge-blocking CI lane description that matches reality.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-CONTEXT.md
@.planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-RESEARCH.md
@.planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-PATTERNS.md

<interfaces>
From `guides/resumable_uploads.md`:
```md
- `tus-js-client` and `@uppy/tus` client settings
- Optional same-user resume authorization
- Doctor checks and capability honesty
- Security checklist and no-silent-downgrade rules
```

From `test/install_smoke/support/generated_app_helper.ex`:
```elixir
System.cmd("npm", ["install", "--no-save", "tus-js-client@4.3.1"], stderr_to_stdout: true)
assert proof["previous_uploads"] >= 1
assert asset.byte_size >= 200 * 1024 * 1024
```

From `.github/workflows/ci.yml`:
```yaml
- name: Run built-artifact tus package-consumer proof against MinIO
  run: bash scripts/install_smoke.sh tus
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the resumable uploads guide version-correct and security-explicit</name>
  <read_first>
    - guides/resumable_uploads.md
    - .planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-CONTEXT.md
    - .planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-RESEARCH.md
    - test/install_smoke/support/generated_app_helper.ex
  </read_first>
  <files>guides/resumable_uploads.md</files>
  <action>Rewrite the client-configuration and mount sections in `guides/resumable_uploads.md` so TUS-14 is locked explicitly, not implied. The guide must include an exact parser clause showing `Plug.Parsers` with `:pass` for `["application/offset+octet-stream"]`, an exact CORS clause exposing `Upload-Offset`, `Location`, `Upload-Length`, `Tus-Resumable`, and `Upload-Expires`, and a plain-language no-silent-downgrade statement that mounting `TusPlug` against an adapter without `:tus_upload` raises at init time instead of falling back to another resumable mode. Also explicitly separate `tus-js-client` from modern `@uppy/tus` guidance per D-09. Keep `parallelUploads: 1` in both examples. Keep `removeFingerprintOnSuccess: true` only in the `tus-js-client` example. Remove that option from the modern `@uppy/tus` example and state plainly that resume and fingerprint cleanup are automatic there. Add explicit wording that the signed tus `Location` URL is a short-lived bearer credential per D-02, document `config :rindle, :tus_resume_authorizer, MyApp.TusAuth` as the optional same-user hardening layer, and retain the honest single-node or sticky-session requirement for S3-backed tus uploads per D-11.</action>
  <acceptance_criteria>
    - `rg -n "Plug\\.Parsers|application/offset\\+octet-stream|:pass" guides/resumable_uploads.md` returns the required parser clause.
    - `rg -n "Upload-Offset|Location|Upload-Length|Tus-Resumable|Upload-Expires" guides/resumable_uploads.md` returns the required exposed-headers clause.
    - `rg -n "no-silent-downgrade|adapter without `:tus_upload` raises at init time|bearer credential|tus_resume_authorizer|parallelUploads: 1|sticky-session|single-node" guides/resumable_uploads.md` returns the required security and routing guidance.
    - `rg -n "@uppy/tus|tus-js-client" guides/resumable_uploads.md` shows distinct client sections.
    - `rg -n "removeFingerprintOnSuccess: true" guides/resumable_uploads.md` returns exactly one `tus-js-client` example and one client-specific warning, not an `@uppy/tus` example.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "Plug\\.Parsers|application/offset\\+octet-stream|:pass|Upload-Offset|Location|Upload-Length|Tus-Resumable|Upload-Expires|no-silent-downgrade|bearer credential|tus_resume_authorizer|parallelUploads: 1|sticky-session|single-node|@uppy/tus|tus-js-client" guides/resumable_uploads.md && test "$(rg -c "removeFingerprintOnSuccess: true" guides/resumable_uploads.md)" -eq 2'</automated>
  </verify>
  <done>The canonical guide tells adopters exactly how to mount and operate the tus edge with the required parser/CORS clauses, explicit no-silent-downgrade wording, accurate client-version guidance, explicit bearer-token language, and honest S3 routing constraints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep the generated-app tus proof real, pinned, and merge-blocking</name>
  <read_first>
    - test/install_smoke/support/generated_app_helper.ex
    - test/install_smoke/generated_app_smoke_test.exs
    - .github/workflows/ci.yml
    - guides/resumable_uploads.md
    - .planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-CONTEXT.md
  </read_first>
  <files>test/install_smoke/support/generated_app_helper.ex, test/install_smoke/generated_app_smoke_test.exs, .github/workflows/ci.yml</files>
  <behavior>
    - Proof helper test: the Node harness installs `tus-js-client@4.3.1`, performs an interrupted upload, resumes it, and reports `previous_uploads >= 1`.
    - Docs-parity test: the generated-app smoke lane reads `guides/resumable_uploads.md` and asserts the guide contains the exact parser clause for `Plug.Parsers :pass ["application/offset+octet-stream"]`, the exposed headers `Upload-Offset`/`Location`/`Upload-Length`/`Tus-Resumable`/`Upload-Expires`, the no-silent-downgrade wording, the bearer-credential language, keeps `removeFingerprintOnSuccess: true` only in the `tus-js-client` guidance, and mentions the single-node or sticky-session S3 constraint.
    - Smoke test: the generated-app result asserts `tus_byte_size >= 200 * 1024 * 1024`, `tus_content_type == "video/mp4"`, and ready variants `["poster", "web_720p"]`.
    - CI contract: the package-consumer workflow keeps the explicit step `bash scripts/install_smoke.sh tus` in the MinIO-backed lane.
  </behavior>
  <action>Keep the real socket-level tus proof merge-blocking per D-10. In `test/install_smoke/support/generated_app_helper.ex`, keep the Node proof pinned to `tus-js-client@4.3.1`, keep the generated fixture size assertion at `200 * 1024 * 1024`, and make the proof comments and assertion names explicit that the flow must observe at least one resumable previous upload before completion. In `test/install_smoke/generated_app_smoke_test.exs`, keep the existing `previous_uploads`, `byte_size`, `content_type`, and ready-variants assertions as the package-consumer contract, and add docs-parity assertions that read `guides/resumable_uploads.md` to lock the exact parser clause for `Plug.Parsers :pass ["application/offset+octet-stream"]`, the exposed headers `Upload-Offset`/`Location`/`Upload-Length`/`Tus-Resumable`/`Upload-Expires`, the no-silent-downgrade wording, the bearer-credential wording, the modern `@uppy/tus` versus `tus-js-client` split, and the single-node or sticky-session note. In `.github/workflows/ci.yml`, keep the Node 20 + MinIO package-consumer lane and the exact `Run built-artifact tus package-consumer proof against MinIO` step using `bash scripts/install_smoke.sh tus`. Do not replace this with a fake-only proof or move it to an optional nightly lane.</action>
  <acceptance_criteria>
    - `rg -n "tus-js-client@4.3.1|previous_uploads|200 \\* 1024 \\* 1024|video/mp4|guides/resumable_uploads\\.md|Plug\\.Parsers|application/offset\\+octet-stream|Upload-Offset|Location|Upload-Length|Tus-Resumable|Upload-Expires|no-silent-downgrade|bearer credential|sticky-session|single-node" test/install_smoke/support/generated_app_helper.ex test/install_smoke/generated_app_smoke_test.exs` returns the pinned client, proof assertions, and docs-parity checks.
    - `rg -n "Run built-artifact tus package-consumer proof against MinIO|bash scripts/install_smoke.sh tus" .github/workflows/ci.yml` returns the merge-blocking workflow step.
    - `rg -n "setup-node@v4|node-version: \"20\"" .github/workflows/ci.yml` confirms the Node toolchain for the proof lane.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "tus-js-client@4.3.1|previous_uploads|200 \\* 1024 \\* 1024|video/mp4|guides/resumable_uploads\\.md|Plug\\.Parsers|application/offset\\+octet-stream|Upload-Offset|Location|Upload-Length|Tus-Resumable|Upload-Expires|no-silent-downgrade|bearer credential|sticky-session|single-node" test/install_smoke/support/generated_app_helper.ex test/install_smoke/generated_app_smoke_test.exs .github/workflows/ci.yml'</automated>
    <automated>bash scripts/install_smoke.sh tus</automated>
  </verify>
  <done>The generated-app proof remains the authoritative CI contract for browser drop-and-resume through the shipped tus edge, the workflow still blocks merges on that proof, and the smoke lane enforces guide/proof parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| library docs → adopter implementation | Copy-pasteable guidance crosses into production adopter configuration. |
| CI proof harness → release confidence | The generated-app proof stands in for package-consumer reality and must not drift into a fake-only check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-03-01 | I | `guides/resumable_uploads.md` | mitigate | State clearly that the signed tus `Location` URL is a bearer credential and keep same-user hardening optional via `:tus_resume_authorizer`. |
| T-44-03-02 | T | docs + proof harness | mitigate | Align guide examples with the actual Node proof by separating `tus-js-client` from modern `@uppy/tus` behavior and pinning `tus-js-client@4.3.1` in the proof helper. |
| T-44-03-03 | R | `.github/workflows/ci.yml` | mitigate | Keep the MinIO-backed `bash scripts/install_smoke.sh tus` lane merge-blocking so the published contract is enforced continuously. |
</threat_model>

<verification>
- Run `bash scripts/install_smoke.sh tus`.
- Grep the guide for the exact parser clause, exposed headers, no-silent-downgrade wording, bearer-credential wording, split client sections, and sticky-session note.
- Grep the helper, smoke test, and CI workflow for `tus-js-client@4.3.1`, `previous_uploads`, `guides/resumable_uploads.md`, the exact TUS-14 clause strings, and `bash scripts/install_smoke.sh tus`.
</verification>

<success_criteria>
- `guides/resumable_uploads.md` is the canonical, version-correct tus setup guide with the exact TUS-14 parser/CORS/no-silent-downgrade clauses.
- The generated-app proof still performs one real interrupted upload and resume against MinIO and asserts the expected asset outcome.
- CI continues to enforce the tus package-consumer proof on every merge path.
- Automated docs-parity assertions prevent the guide from drifting away from the merge-blocking tus proof lane.
</success_criteria>

<output>
After completion, create `.planning/phases/44-auth-hardening-dx-docs-telemetry-ci-proof/44-03-SUMMARY.md`
</output>

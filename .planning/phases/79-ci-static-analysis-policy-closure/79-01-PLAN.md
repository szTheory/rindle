---
id: 79-01
plan_number: 01
plan: 01
phase: 79
type: execute
wave: 1
depends_on: []
files_modified:
  - RUNNING.md
  - .github/workflows/ci.yml
autonomous: true
requirements: [CI-04]
requirements_addressed: [CI-04]
tags: [ci-policy, running-md, ci-yml, static-analysis, docs]

must_haves:
  truths:
    - "RUNNING.md records explicit Credo and Dialyzer advisory decision under Static analysis policy (CI-04)"
    - "RUNNING.md rationale covers signal value, fork latency, and green-main honesty"
    - "ci.yml quality-job comments reference CI-04 policy in RUNNING.md without changing step wiring"
    - "Credo and Dialyzer steps retain continue-on-error: true in ci.yml"
  artifacts:
    - path: "RUNNING.md"
      provides: "Canonical CI-04 static-analysis policy record"
      contains: "Static analysis policy (CI-04)"
    - path: ".github/workflows/ci.yml"
      provides: "Comment alignment with RUNNING.md CI-04 policy"
      contains: "CI-04"
  key_links:
    - from: "RUNNING.md"
      to: ".github/workflows/ci.yml"
      via: "Advisory severity matches continue-on-error wiring"
      pattern: "continue-on-error"
    - from: ".github/workflows/ci.yml"
      to: "RUNNING.md"
      via: "Comment pointer to CI-04 subsection"
      pattern: "Static analysis policy \\(CI-04\\)"
---

<objective>
Record the locked CI-04 decision in RUNNING.md and align the quality-job comment
block in ci.yml — comments only, no workflow wiring changes.

Purpose: Maintainer-facing source of truth for Credo/Dialyzer advisory policy with
documented rationale.
Output: RUNNING.md CI-04 subsection + updated ci.yml L94–96 comments.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/79-ci-static-analysis-policy-closure/79-CONTEXT.md
@.planning/phases/79-ci-static-analysis-policy-closure/79-RESEARCH.md
@.planning/phases/79-ci-static-analysis-policy-closure/79-VALIDATION.md
@.planning/phases/79-ci-static-analysis-policy-closure/79-PATTERNS.md
@.github/workflows/ci.yml
@RUNNING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RUNNING.md Static analysis policy (CI-04) subsection</name>
  <read_first>
    - RUNNING.md (## CI lane severity matrix L14–36; ### Release train L38)
    - .planning/phases/79-ci-static-analysis-policy-closure/79-CONTEXT.md (D-02 locked decision)
    - .planning/phases/79-ci-static-analysis-policy-closure/79-RESEARCH.md (§2 RUNNING.md insertion)
    - .github/workflows/ci.yml (L97–99 Credo, L131–133 Dialyzer)
  </read_first>
  <files>RUNNING.md</files>
  <action>
    In `RUNNING.md`, insert a new subsection **`### Static analysis policy (CI-04)`**
    immediately after the CI matrix table (after the `gcs-soak` / `package-consumer-gcs-live`
    row, before `### Release train`).

    Include this content (paragraph structure may vary; all elements required):

    ```markdown
    ### Static analysis policy (CI-04)

    **Decision (v1.17):** Credo (strict) and Dialyzer remain **advisory** in the `quality`
    job. Wiring uses step-level `continue-on-error: true` in
    [`.github/workflows/ci.yml`](.github/workflows/ci.yml) (Credo L97–99, Dialyzer L131–133).
    Making either tool merge-blocking is explicitly rejected for this milestone.

    **Rationale:**

    - **Signal value:** Static analysis catches style and typespec drift; failures remain
      visible in CI logs for maintainers without blocking adopter-critical merge lanes.
    - **Fork latency:** Dialyzer PLT build is slow; merge-blocking would raise contributor
      and fork PR cost disproportionate to adopter impact.
    - **Green-main honesty:** Adopter-critical lanes are already merge-blocking (`mix coveralls`,
      `proof`, `package-consumer`, `adopter`, `integration`, contract AV hygiene). Static
      analysis is maintainer hygiene, not adopter contract.

    Doctor and AV doctor steps remain advisory without a separate CI-04 decision record
    (CI-04 names Credo and Dialyzer only). See matrix rows above.
    ```

    Optionally add to the Credo and Dialyzer matrix Notes column: `(see CI-04 below)` —
    only if it fits without widening the table awkwardly.
  </action>
  <acceptance_criteria>
    - `rg 'Static analysis policy \(CI-04\)' RUNNING.md` exits 0
    - `rg -i 'signal value' RUNNING.md` exits 0
    - `rg -i 'fork latency' RUNNING.md` exits 0
    - `rg -i 'green-main honesty' RUNNING.md` exits 0
    - `rg 'Credo and Dialyzer remain \*\*advisory\*\*|remain \*\*advisory\*\*' RUNNING.md` exits 0
    - `rg 'Doctor and AV doctor' RUNNING.md` exits 0 (scope guard D-07)
  </acceptance_criteria>
  <verify>
    <automated>rg 'Static analysis policy \(CI-04\)' RUNNING.md && rg -i 'signal value|fork latency|green-main honesty' RUNNING.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Update ci.yml quality-job comment block (L94–96)</name>
  <read_first>
    - .github/workflows/ci.yml (L94–133 quality job steps)
    - RUNNING.md (new ### Static analysis policy (CI-04) subsection)
    - .planning/phases/79-ci-static-analysis-policy-closure/79-CONTEXT.md (D-01, D-03)
    - .planning/phases/79-ci-static-analysis-policy-closure/79-RESEARCH.md (§3 ci.yml comment block)
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
    In `.github/workflows/ci.yml`, replace **only** the three-line comment block at L94–96.
    Do **not** modify any step `run:` lines, `continue-on-error` values, or job structure.

    **Find (exact):**
    ```yaml
          # Phase 71 (CI proof honesty): Credo, Doctor, AV doctor, and Dialyzer stay advisory.
          # Default unit suite (mix coveralls) is merge-blocking (post-v1.16 assessment).
          # See RUNNING.md `## CI lane severity` for the full matrix.
    ```

    **Replace with (exact):**
    ```yaml
          # CI-04 (v1.17): Credo and Dialyzer remain advisory (continue-on-error).
          # Doctor and AV doctor also advisory — not part of CI-04 decision record.
          # Default unit suite (mix coveralls) is merge-blocking. See RUNNING.md
          # `### Static analysis policy (CI-04)` and `## CI lane severity`.
    ```

    Verify Credo step (L97–99) and Dialyzer step (L131–133) still have
    `continue-on-error: true` unchanged after edit.
  </action>
  <acceptance_criteria>
    - `rg 'CI-04' .github/workflows/ci.yml` exits 0
    - `rg 'Phase 71 \(CI proof honesty\)' .github/workflows/ci.yml` exits 1 (old comment removed)
    - `rg 'Static analysis policy \(CI-04\)' .github/workflows/ci.yml` exits 0
    - `rg -A2 'name: Credo \(strict\)' .github/workflows/ci.yml | rg 'continue-on-error: true'` exits 0
    - `rg -A2 'name: Dialyzer' .github/workflows/ci.yml | rg 'continue-on-error: true'` exits 0
    - `rg -A2 'name: Run tests with coverage' .github/workflows/ci.yml | rg 'continue-on-error' ; test $? -eq 1` (coveralls still merge-blocking)
  </acceptance_criteria>
  <verify>
    <automated>rg 'CI-04' .github/workflows/ci.yml && rg -A2 'name: Credo \(strict\)' .github/workflows/ci.yml | rg 'continue-on-error: true'</automated>
  </verify>
</task>

</tasks>

<verification_criteria>
Wave 1 gate before Plan 02:

```bash
rg 'Static analysis policy \(CI-04\)' RUNNING.md .github/workflows/ci.yml
rg -i 'signal value|fork latency|green-main honesty' RUNNING.md
rg 'Decision deferred' RUNNING.md ; test $? -eq 1
```
</verification_criteria>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Accidental removal of continue-on-error on Credo/Dialyzer | high | Task 2 acceptance criteria verify wiring unchanged; D-01 scope guard |
| Policy subsection contradicts matrix rows | medium | Manual read checklist in Plan 02; matrix rows remain advisory |
| ci.yml step wiring changed | high | Task 2 edits comments only; grep verifies coveralls has no continue-on-error |
</threat_model>

## PLANNING COMPLETE

---
phase: 123-runtime-operations-decomposition
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/ops/runtime_checks.ex
  - lib/rindle/ops/runtime_checks/core_checks.ex
  - lib/rindle/ops/runtime_checks/ownership_checks.ex
  - lib/rindle/ops/runtime_checks/integration_checks.ex
  - test/rindle/ops/runtime_checks_test.exs
  - test/rindle/ops/runtime_checks_streaming_test.exs
  - test/rindle/api_surface_boundary_test.exs
  - test/rindle/contracts/telemetry_contract_test.exs
autonomous: true
requirements: [OPS-01, SAFE-01]
must_haves:
  truths:
    - "Per D-123-01, RuntimeChecks.run/2 remains the sole orchestration boundary while core, ownership/Oban, and optional integration diagnostics live in cohesive internal collaborators."
    - "Per D-123-02, the façade still owns result construction, per-check telemetry, final ID ordering, failure counts, and report aggregation."
    - "Stable check IDs, conditional row presence, warning accounting, redaction, injected dependencies, report shapes, and telemetry events remain unchanged."
  artifacts:
    - path: "lib/rindle/ops/runtime_checks.ex"
      provides: "Small deterministic schedule, execution, ordering, result, and telemetry façade."
    - path: "lib/rindle/ops/runtime_checks/core_checks.ex"
      provides: "Internal profile, delivery, AV, local-playback, and tus diagnostic mechanics."
    - path: "lib/rindle/ops/runtime_checks/ownership_checks.ex"
      provides: "Internal migration, schema ownership, and Oban diagnostic mechanics."
    - path: "lib/rindle/ops/runtime_checks/integration_checks.ex"
      provides: "Internal optional streaming and GCS diagnostic mechanics."
  key_links:
    - from: "lib/rindle/ops/runtime_checks.ex"
      to: "lib/rindle/ops/runtime_checks/{core_checks,ownership_checks,integration_checks}.ex"
      via: "explicit resolved-input schedule entries and normalized outcomes"
    - from: "lib/rindle/ops/runtime_checks.ex"
      to: "[:rindle, :runtime, :check, :stop]"
      via: "one retained run_check/1 telemetry wrapper per scheduled check"
    - from: "RuntimeChecks.run/2"
      to: "mix rindle.doctor and Admin runtime doctor consumers"
      via: "unchanged sorted report map and stable check identifiers"
---

<objective>
Decompose runtime diagnostics into three readable internal domains without moving orchestration or observable behavior out of `RuntimeChecks.run/2` (D-123-01, D-123-02).

Purpose: Satisfy OPS-01 through a top-down operational narrative while preserving inherited SAFE-01 and all operator-facing contracts.
Output: A small façade, three internal collaborator modules, and behavior/compiled-boundary proof over the unchanged façade.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-runtime-operations-decomposition/123-CONTEXT.md
@.planning/phases/123-runtime-operations-decomposition/123-PATTERNS.md
@.planning/phases/123-runtime-operations-decomposition/123-RESEARCH.md
@.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-VERIFICATION.md
@lib/rindle/ops/runtime_checks.ex
@test/rindle/ops/runtime_checks_test.exs
@test/rindle/ops/runtime_checks_streaming_test.exs
@test/rindle/api_surface_boundary_test.exs
@test/rindle/contracts/telemetry_contract_test.exs

<interfaces>
`Rindle.Ops.RuntimeChecks.run/2`, `probe_gcs_bucket/4`, and `do_probe/4` remain callable at their current module and signatures. Collaborators are `@moduledoc false`, receive already-resolved values/readers from the façade, and return internal schedule/outcome data that the façade turns into the existing check maps. They do not read application configuration independently, emit telemetry, sort checks, or aggregate reports.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Route core and ownership diagnostics through the retained façade</name>
  <files>lib/rindle/ops/runtime_checks.ex, lib/rindle/ops/runtime_checks/core_checks.ex, lib/rindle/ops/runtime_checks/ownership_checks.ex, test/rindle/ops/runtime_checks_test.exs, test/rindle/api_surface_boundary_test.exs</files>
  <behavior>
    - `RuntimeChecks.run/2` accepts the same args/options and returns the same sorted `%{checks, failed, success?, total}` report for core, migration/schema, and Oban cases.
    - Warning rows still do not increment `failed`; absent/legacy/current schema and host-owned Oban classifications retain their IDs, statuses, summaries, fixes, and bounded ownership data.
    - Existing `RuntimeChecks` callable functions remain at the façade, and no new documented public Rindle API appears.
  </behavior>
  <action>Per D-123-01, first add façade-level behavior cases for representative successful, warning, and refusing core/ownership schedules plus compiled API-boundary assertions; use returned reports and `Code.fetch_docs/1`/export metadata rather than reading implementation source. Extract profile/delivery/AV/local-playback/tus mechanics into `CoreChecks` and migration/schema/Oban mechanics into `OwnershipChecks`. Have both collaborators accept the exact resolved profiles, env, probe, catalog/snapshot, and Oban inputs currently assembled in `run/2`; return internal schedule entries or normalized outcomes only. Per D-123-02, retain option/config resolution, conditional scheduling, result constructors, `run_check/1`, telemetry, sorting, and aggregation in `runtime_checks.ex`. Preserve every check ID/map field and existing injection seam; do not add a registry, new public façade, collaborator-owned config lookup, or collaborator telemetry. Commit intent is non-release `chore(123-01)`.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/rindle/ops/runtime_checks_test.exs test/rindle/api_surface_boundary_test.exs --seed 0</automated>
  </verify>
  <done>The core and ownership diagnostic paths execute end to end through `RuntimeChecks.run/2`, the façade remains the sole orchestration/result/telemetry owner per D-123-01/D-123-02, and objective behavior/API-boundary tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract optional integrations and close the runtime-check slice</name>
  <files>lib/rindle/ops/runtime_checks.ex, lib/rindle/ops/runtime_checks/integration_checks.ex, test/rindle/ops/runtime_checks_test.exs, test/rindle/ops/runtime_checks_streaming_test.exs, test/rindle/contracts/telemetry_contract_test.exs</files>
  <behavior>
    - GCS, tus, and streaming rows retain their current conditional presence, vacuous-success cases, stable IDs/order, timeout/status mapping, and secret redaction.
    - Every scheduled check emits exactly one existing stop event with the existing measurement and metadata keys from the façade wrapper.
    - `probe_gcs_bucket/4` and `do_probe/4` remain callable through `RuntimeChecks` with unchanged results.
  </behavior>
  <action>Per D-123-01, extend existing façade tests only for unproved optional-domain combinations and telemetry counts/order, then extract streaming and GCS integration mechanics into `IntegrationChecks`; keep tus scheduling with the core collaborator if that yields the clearer existing capability boundary, but do not create another module solely for size. Retain `probe_gcs_bucket/4` and `do_probe/4` as façade delegates, with HTTP/probe behavior and test injection unchanged. Per D-123-02, pass integration outcomes back through the one façade result constructor and telemetry wrapper, preserve conditional row omission and final ID sorting, and keep credential/session/provider data redacted. Per D-123-07 and D-123-08, finish with fresh compile, focused behavioral/telemetry tests, and SAFE-01; add no source-string snapshot, dependency, workflow, Admin, upload-path, or test-support refactor.</action>
  <verify>
    <automated>MIX_ENV=test mix compile --force &amp;&amp; MIX_ENV=test mix test test/rindle/ops/runtime_checks_test.exs test/rindle/ops/runtime_checks_streaming_test.exs test/rindle/contracts/telemetry_contract_test.exs --seed 0 &amp;&amp; bash scripts/maintainer/refactor_contract.sh</automated>
  </verify>
  <done>All runtime-check domains are cohesive behind the unchanged façade, focused and SAFE-01 proof is green, and the slice is complete before migration work begins per D-123-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime/configuration inputs to diagnostic collaborators | Injected profiles, environment, catalog state, provider probes, and credentials cross into internal domain checks. |
| Diagnostic outcomes to operator report/telemetry | Internal outcomes become operator-visible maps and telemetry at the façade. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-123-01 | Information Disclosure | optional integration diagnostics | high | mitigate | Preserve bounded summaries and existing credential/session/provider redaction; run focused GCS/streaming tests. |
| T-123-02 | Tampering | check schedule/result aggregation | high | mitigate | Keep scheduling, constructors, sorting, counts, and telemetry in `RuntimeChecks.run/2`; assert façade reports and event cardinality. |
| T-123-03 | Denial of Service | streaming/GCS probes | medium | mitigate | Preserve current conditional gates, timeouts, and injected probe boundaries. |
| T-123-SC | Tampering | package supply chain | low | accept | No package-manager install occurs in this plan; existing dependency lock remains unchanged. |
</threat_model>

<verification>
- Fresh test compile succeeds.
- Runtime-check core, optional integration, API boundary, and telemetry contract tests pass.
- `bash scripts/maintainer/refactor_contract.sh` passes after the complete OPS-01 slice.
- No public API, config key, check ID/order/map shape, telemetry, error, workflow, dependency, Admin, archive, or release-topology diff is introduced.
</verification>

<success_criteria>
- OPS-01 is observable as a small façade plus three cohesive internal domains.
- D-123-01 and D-123-02 are implemented exactly, with D-123-07/D-123-08 proof discipline applied.
- SAFE-01 remains green before Plan 123-02 starts.
</success_criteria>

<output>
Create `.planning/phases/123-runtime-operations-decomposition/123-01-SUMMARY.md` when done.
</output>

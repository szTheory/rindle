---
phase: 123-runtime-operations-decomposition
plan: "03"
type: execute
wave: 3
depends_on: ["123-02"]
files_modified:
  - lib/rindle/ops/runtime_status.ex
  - lib/rindle/ops/runtime_status/collector.ex
  - lib/mix/tasks/rindle.runtime_status.ex
  - lib/mix/tasks/rindle/runtime_status/formatter.ex
  - test/rindle/ops/runtime_status_test.exs
  - test/rindle/runtime_status_task_test.exs
  - test/rindle/contracts/telemetry_contract_test.exs
autonomous: true
requirements: [OPS-03, SAFE-01]
must_haves:
  truths:
    - "Per D-123-05, RuntimeStatus.runtime_status/1 remains the sole API boundary for filters, readiness-before-query refusal, report/recommendation composition, and refusal telemetry."
    - "Per D-123-06, the Mix task owns parsing, one façade call, output selection, and exit behavior while an internal formatter preserves exact text/JSON/error presentation."
    - "Flags, normalized filters, report fields, query prefixes, limits, classification/sample order, redaction, section order, error tuples, and non-zero failures remain unchanged."
    - "Fresh compile, focused runtime-operation proof, SAFE-01, local mix ci/hygiene, and supported CI authority close the phase without release-topology changes."
  artifacts:
    - path: "lib/rindle/ops/runtime_status.ex"
      provides: "Small API orchestration, readiness, report/recommendation composition, and telemetry façade."
    - path: "lib/rindle/ops/runtime_status/collector.ex"
      provides: "Internal bounded report queries, samples, and finding classifiers."
    - path: "lib/mix/tasks/rindle.runtime_status.ex"
      provides: "Thin exact-flag command transport with unchanged failure exit semantics and compatibility delegates."
    - path: "lib/mix/tasks/rindle/runtime_status/formatter.ex"
      provides: "Pure text, JSON error, and bounded error presentation."
  key_links:
    - from: "RuntimeStatus.runtime_status/1"
      to: "RuntimeStatus.Collector.collect/4"
      via: "normalized filters, current time/cutoff, and readiness-approved Oban prefix after the no-query guard"
    - from: "Mix.Tasks.Rindle.RuntimeStatus.run/1"
      to: "Rindle.runtime_status/1"
      via: "one call with exact parsed filter keys"
    - from: "Mix.Tasks.Rindle.RuntimeStatus"
      to: "Mix.Tasks.Rindle.RuntimeStatus.Formatter"
      via: "compatibility delegates preserving direct-tested helpers and output ordering"
---

<objective>
Separate runtime-status data collection and command presentation while retaining the API and command boundaries exactly (D-123-05, D-123-06).

Purpose: Satisfy OPS-03 and close Phase 123 with full behavioral, SAFE-01, local, hygiene, and supported-CI evidence.
Output: A focused collector, a pure formatter, thin retained façades, and complete phase verification.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RUNNING.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/DEVELOPMENT-TRAIN.md
@.planning/RELEASE-TRAIN.md
@.planning/phases/123-runtime-operations-decomposition/123-CONTEXT.md
@.planning/phases/123-runtime-operations-decomposition/123-PATTERNS.md
@.planning/phases/123-runtime-operations-decomposition/123-RESEARCH.md
@.planning/phases/123-runtime-operations-decomposition/123-02-SUMMARY.md
@.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-VERIFICATION.md
@lib/rindle.ex
@lib/rindle/ops/runtime_status.ex
@lib/mix/tasks/rindle.runtime_status.ex
@test/rindle/ops/runtime_status_test.exs
@test/rindle/runtime_status_task_test.exs
@test/rindle/contracts/telemetry_contract_test.exs

<interfaces>
`Rindle.runtime_status/1`, `Rindle.Ops.RuntimeStatus.runtime_status/1`, and `Mix.Tasks.Rindle.RuntimeStatus.run/1` remain unchanged. Direct-tested task helpers `format_error/1`, `format_json_error/1`, `format_text_report/1`, and `format_provider_findings/1` remain callable as delegates. The collector receives normalized filters and readiness-approved data; the formatter receives only bounded report/error values.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Gate readiness once and delegate bounded status collection</name>
  <files>lib/rindle/ops/runtime_status.ex, lib/rindle/ops/runtime_status/collector.ex, test/rindle/ops/runtime_status_test.exs, test/rindle/contracts/telemetry_contract_test.exs</files>
  <behavior>
    - Filter validation and ownership readiness complete before any asset, variant, upload-session, provider-asset, or Oban report query.
    - Successful reports retain all top-level/section/filter keys, query-prefix routing, counts, classifications, samples, limits, ordering, and recommendations.
    - Refusals retain exact bounded terms and emit the existing refusal event/metadata once without leaking unsafe prefix or provider/session data.
  </behavior>
  <action>Per D-123-05, first strengthen the existing report-query tripwire and façade-level cases for filters, prefixes, limits, classification/sample order, recommendations, redaction, and refusal telemetry; assert returned data and emitted events rather than function-body text. Extract report queries, row-to-sample classification, counts, and bounded finding construction into `Rindle.Ops.RuntimeStatus.Collector`. Pass normalized filters, `now`, cutoff, and the readiness-approved Oban prefix into one collector call. Keep filter normalization/allowlisting, ownership snapshot classification, refusal-before-query gate, top-level report composition, recommendation composition, and `emit_runtime_refusal/1` in `RuntimeStatus.runtime_status/1`. The collector must not repeat readiness/config lookup, normalize inputs, emit telemetry, or return raw secrets/URIs/provider identifiers. Preserve the exact five filter keys and all current error tuples.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/rindle/ops/runtime_status_test.exs test/rindle/contracts/telemetry_contract_test.exs --seed 0</automated>
  </verify>
  <done>The status API executes end to end as normalize, readiness gate, bounded collection, report/recommendation composition, with unchanged refusal behavior and telemetry per D-123-05.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delegate presentation, preserve command transport, and close the phase</name>
  <files>lib/mix/tasks/rindle.runtime_status.ex, lib/mix/tasks/rindle/runtime_status/formatter.ex, test/rindle/runtime_status_task_test.exs</files>
  <behavior>
    - The command parses exactly `--profile`, `--older-than-sec`, `--limit`, `--format`, and `--provider-stuck`, calls the façade once, and selects success presentation from normalized report format.
    - Text sections, finding/sample ordering, JSON report/error shapes, safe error copy, prefix/provider/session redaction, and direct-tested helper returns remain exact.
    - Every error still prints through the current channel and exits `{:shutdown, 1}`; success retains exit zero.
  </behavior>
  <action>Per D-123-06, add pure map-based formatting and shell/exit behavior cases where current coverage is insufficient, then move text report sections plus bounded text/JSON error construction into `Mix.Tasks.Rindle.RuntimeStatus.Formatter` as `@moduledoc false`. Keep `run/1`, exact `OptionParser` switches/aliases, nil omission, one `Rindle.runtime_status/1` call, output-channel choice, JSON encoding, and `exit({:shutdown, 1})` in the task. Retain the four direct-tested formatter helpers on the task as compatibility delegates with identical return values. Preserve provider findings after upload sessions and before recommendations, deterministic list order, and all redaction. Per D-123-07/D-123-08, run fresh compile, all Phase 123 focused tests, SAFE-01, `mix ci`, and repository hygiene; use non-release `chore:` delivery and do not edit workflows, dependencies, public APIs, schemas/migrations, telemetry/errors, Admin, archives, or release files to accommodate local-only variance.</action>
  <verify>
    <automated>MIX_ENV=test mix compile --force &amp;&amp; MIX_ENV=test mix test test/rindle/ops/runtime_checks_test.exs test/rindle/ops/runtime_checks_streaming_test.exs test/rindle/migration_fast_test.exs test/rindle/migration_test.exs test/rindle/ops/runtime_status_test.exs test/rindle/runtime_status_task_test.exs test/rindle/contracts/telemetry_contract_test.exs --seed 0 &amp;&amp; bash scripts/maintainer/refactor_contract.sh &amp;&amp; mix ci &amp;&amp; ./scripts/maintainer/repo_hygiene_check.sh</automated>
  </verify>
  <done>Collection, formatting, and command concerns are separate with exact behavior preserved; all local phase gates pass and the implementation is ready for authoritative supported PR CI per D-123-06/D-123-07/D-123-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator filters/readiness to database queries | Untrusted filter values and observed ownership state decide whether and how bounded Rindle/Oban queries run. |
| Report/error values to terminal/JSON | Database/provider/runtime data crosses into operator-visible output and process status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-123-07 | Denial of Service | runtime-status filters/queries | high | mitigate | Retain exact key allowlist, typed normalization, positive default/explicit limits, readiness-first gate, and focused limit tests. |
| T-123-08 | Information Disclosure | status report and formatter | high | mitigate | Preserve bounded refusals, safe prefixes, sample redaction, and constant unknown-error copy with API/task tests. |
| T-123-09 | Repudiation | task errors and exit behavior | medium | mitigate | Preserve output channel plus `{:shutdown, 1}` and assert shell/exit behavior directly. |
| T-123-SC | Tampering | package supply chain | low | accept | No package-manager install occurs in this plan; existing dependency lock remains unchanged. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---:|---|---|
| GOAL | — | Runtime operations organized by diagnostic responsibility with all operator and safety boundaries retained | 01-03 | COVERED | Three sequential façades/collaborator slices plus full phase gate. |
| REQ | OPS-01 | Small RuntimeChecks orchestration with cohesive diagnostic collaborators | 01 | COVERED | D-123-01/D-123-02 façade ownership and behavior/telemetry proof. |
| REQ | OPS-02 | Named bounded migration preflight preserving catalog/order/reversal | 02 | COVERED | D-123-03/D-123-04 observe/classify/mutate boundary and DB integration proof. |
| REQ | OPS-03 | Separate runtime-status collection, formatting, and command concerns | 03 | COVERED | D-123-05/D-123-06 collector/formatter split with exact API/CLI proof. |
| REQ | SAFE-01 | Preserve API, schema/migration, telemetry, error, CI, and release contracts | 01-03 | COVERED | Focused tests and SAFE-01 after every slice; final local/CI authority gate. |
| RESEARCH | architecture | Thin stable façades with internal explicit-input collaborators | 01-03 | COVERED | No new public API or hidden config/telemetry ownership. |
| RESEARCH | migration safety | Observe, classify, then mutate with one fixed catalog | 02 | COVERED | Snapshot and classifier cannot own DDL/catalog/transactions. |
| RESEARCH | status safety | Normalize and refuse before report queries; bounded redacted output | 03 | COVERED | Query tripwire, limits, classifier, formatter, and exit tests. |
| RESEARCH | validation | Fresh compile, focused suites, SAFE-01, local CI/hygiene, supported CI | 01-03 | COVERED | Per-slice and final commands plus supported-cell acceptance below. |
| CONTEXT | D-123-01 | RuntimeChecks sole orchestration with three domain collaborator groups | 01 | COVERED | Tasks 1-2. |
| CONTEXT | D-123-02 | Result construction, wrapper, sorting, aggregation stay together | 01 | COVERED | Tasks 1-2. |
| CONTEXT | D-123-03 | V1 retains catalog, DDL, ordering, transactions, hidden entry points | 02 | COVERED | Tasks 1-2. |
| CONTEXT | D-123-04 | Extract snapshot and directional classification only | 02 | COVERED | Tasks 1-2. |
| CONTEXT | D-123-05 | RuntimeStatus API retains filters/readiness/composition/telemetry | 03 | COVERED | Task 1. |
| CONTEXT | D-123-06 | Presentation separate while shapes/order/redaction/exit stay exact | 03 | COVERED | Task 2. |
| CONTEXT | D-123-07 | Strict slice sequence with focused and SAFE proof | 01-03 | COVERED | Waves 1→2→3 and final task in each plan. |
| CONTEXT | D-123-08 | Objective proof only; no adjacent-scope churn | 01-03 | COVERED | Behavior/compiled/event/DB tests and explicit scope fences. |

Deferred Phase 124 upload/broker work, Phase 125 test-support/async work, Phase 126 Dialyzer work, and all other CONTEXT deferred items are intentionally excluded, not gaps.
</source_audit>

<verification>
- Fresh test compile and all runtime-check, migration, runtime-status, task, API, and telemetry focused suites pass.
- `bash scripts/maintainer/refactor_contract.sh`, `mix ci`, and `./scripts/maintainer/repo_hygiene_check.sh` pass locally; local Elixir 1.19/OTP 28 is diagnostic only.
- On the non-release chore PR, `gh pr checks --required --watch --fail-fast` succeeds for the exact head SHA; repository-supported Elixir 1.17/OTP 27 and the sole required `CI Summary` are authoritative.
- After merge, push-main lanes remain pending external evidence and must be green before any release readiness claim; Phase 123 does not alter their topology.
- `git diff --quiet main...HEAD -- .github/workflows mix.exs mix.lock priv/repo/migrations lib/rindle/admin .planning/milestones` confirms forbidden surfaces are unchanged.
</verification>

<success_criteria>
- OPS-01, OPS-02, OPS-03, and inherited SAFE-01 have complete automated evidence with no unresolved research question.
- D-123-01 through D-123-08 are implemented and traceable.
- The phase ships only cohesive internal refactoring under non-release `chore:` intent.
- Supported PR CI at the exact head SHA, not unsupported local toolchain output, is the final merge authority.
</success_criteria>

<output>
Create `.planning/phases/123-runtime-operations-decomposition/123-03-SUMMARY.md` when done.
</output>

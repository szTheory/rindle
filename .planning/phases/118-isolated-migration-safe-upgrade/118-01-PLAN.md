---
phase: 118-isolated-migration-safe-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/migration/options.ex
  - lib/rindle/migration/v1.ex
  - test/rindle/migration_test.exs
  - test/rindle/migration_default_build_probe.exs
autonomous: false
requirements: [MIGRATE-01]
nyquist_compliant: true
must_haves:
  truths:
    - "D-118-01/D-118-05: Rindle.Migration.up(version: 1) defaults to rindle, accepts only rindle or public, creates the selected schema before its seven owned relations, and is idempotent."
    - "D-118-03: Rindle.Migration.V1 remains the sole authority for the six tables, marker, qualified DDL, and identifier quoting."
    - "D-118-10: the ordinary public-compiled suite and a stored default-build probe both prove their supported fresh-install posture."
    - "FLAGGED ASSUMPTION (spec-less edge probe, MIGRATE-01): the requirement was unclassified; this plan explicitly tests unsupported prefixes, schema-before-table ordering, second-run idempotency, and host-relation non-ownership without claiming the probe itself was resolved."
  artifacts:
    - path: "lib/rindle/migration/options.ex"
      provides: "Two-value migration option contract with rindle default"
      contains: "@supported_prefixes"
    - path: "lib/rindle/migration/v1.ex"
      provides: "Selected-schema provisioning before V1 table and marker DDL"
      contains: "def owned_relations"
    - path: "test/rindle/migration_test.exs"
      provides: "Live PostgreSQL fresh-install and idempotency proof"
      contains: "Rindle.Migration.up/1"
    - path: "test/rindle/migration_default_build_probe.exs"
      provides: "Stored default-compiled rindle provisioning probe"
  key_links:
    - from: "lib/rindle/migration/options.ex"
      to: "lib/rindle/schema.ex"
      via: "the identical rindle/public allowlist and rindle default"
      pattern: "rindle.*public"
    - from: "lib/rindle/migration/v1.ex"
      to: "PostgreSQL selected schema"
      via: "validated quote_ident/1 boundary followed by qualified Ecto DDL"
      pattern: "create_schema|qualified"
  prohibitions:
    - statement: "MUST NOT turn fresh installation into arbitrary-schema management or search-path routing."
      status: unverified
      flagged: true
    - statement: "MUST NOT create, move, drop, or configure host-owned oban_jobs or schema_migrations."
      status: unverified
      flagged: true
---

<objective>
Deliver the production tracer for a fresh isolated install: a host migration calling `Rindle.Migration.up(version: 1)` creates `rindle`, all six Rindle tables, and marker state in the correct order and can be run again safely.

Purpose: Prove the hardest fresh-install path end to end before expanding into populated upgrades.
Output: Narrowed option validation, V1-owned schema provisioning, PostgreSQL integration coverage, and a stored default-build probe.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/118-isolated-migration-safe-upgrade/118-CONTEXT.md
@.planning/phases/118-isolated-migration-safe-upgrade/118-RESEARCH.md
@.planning/phases/118-isolated-migration-safe-upgrade/118-PATTERNS.md
@.planning/phases/118-isolated-migration-safe-upgrade/118-VALIDATION.md
@.planning/phases/117-prefix-routing-architecture/117-02-SUMMARY.md
@lib/rindle/migration.ex
@lib/rindle/migration/options.ex
@lib/rindle/migration/v1.ex
@lib/rindle/schema.ex
@test/rindle/migration_test.exs
@config/test.exs

<interfaces>
- `Rindle.Migration.up/1` validates through `Rindle.Migration.Options.validate!/1` and dispatches `%{version: 1}` to `Rindle.Migration.V1.up/1`.
- `Rindle.Migration.V1.rindle_tables/0`, `marker_table/0`, and `catalog_requirements/0` are the existing ownership authority.
- `Rindle.Schema.prefix/0` is compile-time, defaults to `"rindle"`, and supports only `"rindle"` or `"public"`.
- `test/rindle/migration_test.exs` runs Ecto migration callbacks through `Ecto.Migration.Runner` with `async: false`.
</interfaces>
</context>

## Wave 0 / test obligations

- Extend `test/rindle/migration_test.exs` first with failing assertions for the `rindle` default, explicit `public`, schema-before-relation creation, idempotency, exact seven-relation ownership, and untouched host relations.
- Add `test/rindle/migration_default_build_probe.exs` before production changes. It must run under `MIX_ENV=dev`, assert `Rindle.Schema.prefix/0` and migration defaults agree on `rindle`, exercise the fresh migration inside a rollback-contained PostgreSQL transaction, and leave no schema behind.
- Keep `config/test.exs` unchanged so the regular suite continues to prove explicit `public` compatibility.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 0: Confirm the locked 0.4.0 migration contract one-way door</name>
  <read_first>
    .planning/phases/118-isolated-migration-safe-upgrade/118-CONTEXT.md
    .planning/phases/117-prefix-routing-architecture/117-VERIFICATION.md
    lib/rindle/schema.ex
    lib/rindle/migration/options.ex
  </read_first>
  <decision>Proceed with D-118-01: publish `rindle` as the migration default and reduce the supported fresh-install pairing to `rindle | public`.</decision>
  <context>This changes the adopter-visible 0.4.0 migration contract. Reversal after release would break host migration/runtime pairings; the choice is already locked in 118-CONTEXT.md, so this gate confirms execution rather than reopening alternatives.</context>
  <options>
    <option id="proceed-locked-contract">
      <name>Proceed with locked contract</name>
      <pros>Pairs migration provisioning exactly with Phase 117's compiled routing authority.</pros>
      <cons>Existing arbitrary-prefix adopters must select the documented public compatibility path or migrate to rindle.</cons>
    </option>
    <option id="stop-for-rediscussion">
      <name>Stop for re-discussion</name>
      <pros>Avoids publishing a contract the maintainer no longer accepts.</pros>
      <cons>Blocks Phase 118 and requires an explicit CONTEXT/requirements revision.</cons>
    </option>
  </options>
  <action>Pause before the tracer changes the published migration default. Record the selected option in the plan summary; proceed only for `proceed-locked-contract`, otherwise leave all listed production and test files unchanged and return the phase to discussion.</action>
  <verify>
    <automated>test -f .planning/phases/118-isolated-migration-safe-upgrade/118-CONTEXT.md &amp;&amp; rg -n "D-118-01|one-way" .planning/phases/118-isolated-migration-safe-upgrade/118-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>The executor receives `proceed-locked-contract`; any other response stops before migration code changes.</acceptance_criteria>
  <done>The locked one-way contract is explicitly confirmed before the tracer edits migration behavior.</done>
  <resume-signal>Select: proceed-locked-contract or stop-for-rediscussion</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 1: Provision one fresh default install end to end</name>
  <reversibility rating="one-way">D-118-01 publishes the default and supported prefix pairing; changing it after 0.4.0 breaks adopter migrations and compiled runtime configuration.</reversibility>
  <read_first>
    lib/rindle/migration.ex
    lib/rindle/migration/options.ex
    lib/rindle/migration/v1.ex
    lib/rindle/schema.ex
    test/rindle/migration_test.exs
    config/test.exs
  </read_first>
  <files>lib/rindle/migration/options.ex, lib/rindle/migration/v1.ex, test/rindle/migration_test.exs, test/rindle/migration_default_build_probe.exs</files>
  <behavior>
    - D-118-01/MIGRATE-01: no-prefix `up(version: 1)` creates `rindle` and the exact V1-owned set; `prefix: "public"` creates the same set in public.
    - D-118-05: the schema exists before qualified table/marker DDL, and a second up is idempotent.
    - D-118-03: `owned_relations/0` derives the six tables plus `rindle_migration_versions`; tests do not duplicate a competing production allowlist.
    - Unsupported strings, atoms, unknown options, and unsupported versions raise bounded `ArgumentError` before DDL.
    - Neither path creates or mutates `oban_jobs` or `schema_migrations`.
  </behavior>
  <action>Start with the Wave 0 failures. In `Options`, define `@supported_prefixes ["rindle", "public"]`, change the default to `"rindle"`, and normalize validation failures to an `ArgumentError` that names both accepted values. In `V1`, add `owned_relations/0` derived from `rindle_tables/0` plus `marker_table/0`, add a private selected-schema provisioning function that emits `CREATE SCHEMA IF NOT EXISTS` through the existing `quote_ident/1` boundary, and call it before any existing table or marker DDL. Do not add a second relation list, a generic prefix API, a search-path mutation, or host-infrastructure DDL. Build the stored default probe with the existing `Ecto.Migration.Runner` pattern and a rollback-contained transaction so it proves a clean default-compiled install without persisting state. Preserve the public-compiled normal test configuration per D-118-10.</action>
  <verify>
    <automated>mix format --check-formatted lib/rindle/migration/options.ex lib/rindle/migration/v1.ex test/rindle/migration_test.exs test/rindle/migration_default_build_probe.exs &amp;&amp; mix test test/rindle/migration_test.exs --seed 0 &amp;&amp; MIX_ENV=dev mix run test/rindle/migration_default_build_probe.exs</automated>
  </verify>
  <acceptance_criteria>
    - The targeted PostgreSQL suite proves fresh `rindle`, explicit `public`, exact relation ownership, order, and second-run idempotency.
    - The default-build probe succeeds and leaves no `rindle` schema or relation behind after its rollback.
    - `config/test.exs` remains explicitly public and no package dependency is added.
  </acceptance_criteria>
  <done>A real host migration helper provisions one fresh supported install end to end, with the public compatibility pairing and non-ownership boundary under executable proof.</done>
</task>

</tasks>

## Artifacts this phase produces

- Modified `lib/rindle/migration/options.ex`: `@supported_prefixes`, `validate_prefix!/1`, and the `rindle` default.
- Modified `lib/rindle/migration/v1.ex`: `owned_relations/0` and private selected-schema provisioning through `quote_ident/1`/`qualified/2`.
- Modified `lib/rindle/migration.ex`: public `move_public_to_rindle/1` and `move_rindle_to_public/1` (created in Plan 02/03).
- Modified `test/rindle/migration_test.exs`: fresh, move-integrity, refusal, rollback, privilege, and lock-timeout PostgreSQL coverage.
- New `test/rindle/migration_default_build_probe.exs`: isolated default-compiled provisioning proof.
- Modified `test/rindle/api_surface_boundary_test.exs`: directional API visibility and generic-mover exclusion.
- Modified `README.md`, `guides/getting_started.md`, `guides/upgrading.md`, and `test/install_smoke/docs_parity_test.exs`: fresh/default/compatibility and maintenance-window upgrade truth.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host migration options to DDL | Adopter input reaches schema-qualified PostgreSQL commands. |
| Rindle migration helper to host database | Library DDL must affect only the fixed Rindle-owned relation set. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-118-01 | Tampering / Elevation | `Migration.Options` to V1 DDL | high | mitigate | Allowlist only `rindle` and `public`; route identifier interpolation solely through V1 quoting and never through `search_path`. |
| T-118-02 | Tampering | V1 ownership list | high | mitigate | Derive exactly six tables plus marker in `owned_relations/0`; assert `oban_jobs` and `schema_migrations` remain untouched. |
| T-118-03 | Denial of Service | repeated fresh migration | medium | mitigate | Create the selected schema and owned DDL idempotently and prove the second invocation succeeds. |
| T-118-SC | Tampering | package installs | high | mitigate | No package install is planned; if execution introduces one, stop and require Package Legitimacy Audit plus slopcheck before installation. |
</threat_model>

<verification>
Run `mix test test/rindle/migration_test.exs --seed 0`, the stored `MIX_ENV=dev` probe, and the Phase 117 schema-prefix contract test after the tracer commit.
</verification>

<success_criteria>
- MIGRATE-01 is executable in both supported prefix postures.
- The migration and compiled routing defaults agree on `rindle`.
- Fresh installation never gains authority over host infrastructure.
</success_criteria>

<output>
Create `.planning/phases/118-isolated-migration-safe-upgrade/118-01-SUMMARY.md` when done.
</output>

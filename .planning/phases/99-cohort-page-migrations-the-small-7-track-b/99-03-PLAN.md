---
phase: 99-cohort-page-migrations-the-small-7-track-b
plan: 03
type: execute
wave: 3
depends_on: ["99-01"]
files_modified:
  - examples/adoption_demo/lib/adoption_demo_web/live/ops_live.ex
  - examples/adoption_demo/lib/adoption_demo_web/live/account_live.ex
  - examples/adoption_demo/e2e/cohort-pages.spec.js
  - examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs
autonomous: true
requirements: [COHORT-03, COHORT-04]
tags: [cohort, migration, ops, account, pre-output, ck-btn, phx-click, erasure, frozen-contract]

must_haves:
  truths:
    - "/ops and /account/:member_id/delete each render through ck_page/1 (a [data-ck-root] .ck shell), theme defaulted to \"light\" from mount; the daisyUI <main> in Layouts.app is left intact (Phase 101)."
    - "Every interactive <button phx-click> keeps its phx-click handler + id + data-testid and carries the BARE .ck-btn (or .ck-btn--primary) class on the EXISTING <button> — ck_button/1 (link-only) is NOT used (Pitfall 4): run_doctor, run_runtime_status, preview_batch, execute_batch (/ops); preview, execute (/account)."
    - "Every <pre> debug panel keeps its id + data-testid + :if and swaps daisyUI bg-gray-100 for the token-only .ck-output class authored in P1: doctor-output, runtime-status-output, batch-preview, batch-result (/ops); erasure-preview, erasure-result (/account)."
    - "/ops section + span contracts survive: batch-erasure-section, every batch-member-#{email} span. /account erasure-member-name survives."
    - "Migration is class-by-class; no element restructured; behavior specs ops-surfaces / batch-erasure / owner-erasure stay green."
  prohibitions:
    - "ck_button/1 is NOT used to replace any phx-click <button> (it renders <.link> and would drop phx-click — Pitfall 4)."
    - "No daisyUI/Tailwind utility remains in either page body (btn, bg-gray-, text-2xl, text-lg, text-sm, opacity-80, space-y-, flex gap-, mt-) — EXCEPT the shared Layouts.app <main> (Phase 101, Pitfall 6)."
    - "No id/data-testid/phx-click dropped or renamed; no handler logic changed; no raw/1 introduced."
    - "No NEW CSS rule is added here — .ck-output is authored in P1; if a needed surface class is missing that is a P1 defect to file, not patch here (Pitfall 1/2)."
  artifacts:
    - path: "examples/adoption_demo/lib/adoption_demo_web/live/ops_live.ex"
      provides: "/ops migrated onto ck_page/1 with .ck-btn buttons + .ck-output panels, frozen contract preserved"
      contains: "<.ck_page"
    - path: "examples/adoption_demo/lib/adoption_demo_web/live/account_live.ex"
      provides: "/account erasure page migrated onto ck_page/1 with .ck-btn buttons + .ck-output panels"
      contains: "<.ck_page"
  key_links:
    - from: "examples/adoption_demo/lib/adoption_demo_web/live/ops_live.ex <button phx-click>"
      to: "phx-click handlers run_doctor/run_runtime_status/preview_batch/execute_batch"
      via: "bare .ck-btn class on the existing <button>; phx-click preserved (Pitfall 4)"
      pattern: "phx-click="
    - from: "examples/adoption_demo/lib/adoption_demo_web/live/ops_live.ex <pre>"
      to: "examples/adoption_demo/priv/static/assets/cohort.css .ck-output"
      via: "each <pre> swaps daisyUI bg-gray-100 for the token-only .ck-output class authored in P1; id/testid/:if preserved"
      pattern: ".ck-output"
---

<objective>
Migrate `/ops` (`ops_live.ex`, COHORT-03) and `/account/:member_id/delete` (`account_live.ex`, COHORT-04 erasure) onto `ck_page/1` class-by-class. These two pages share the same two migration shapes: interactive `<button phx-click>` elements that get the BARE `.ck-btn` class (NOT `ck_button/1`, which is link-only — Pitfall 4) and `<pre>` debug panels that swap daisyUI `bg-gray-100` for the token-only `.ck-output` class authored in P1. ONE atomic commit per page. Add each page's polish case + ExUnit frozen-contract test.

Purpose: Grouping ops + account is the natural semantic boundary — both are the `<button phx-click>` + `<pre>` erasure/diagnostic surfaces, and both are the sole consumers of the P1 `.ck-output` rule. The frozen contract here gates `ops-surfaces.spec.js`, `batch-erasure.spec.js`, and `owner-erasure.spec.js`. Implements the class-by-class HARD CONSTRAINT + LOCKED DECISION 1, and respects Pitfall 4 (phx-click button) verbatim.

Output: both pages render `[data-ck-root]` shells; every `phx-click` handler/id/testid survives on a `.ck-btn` button; every `<pre>` is `.ck-output` with its id/testid/`:if` intact; no daisyUI in either body; polish cases (warn mode) + ExUnit tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-RESEARCH.md
@.planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan MODIFIES `ops_live.ex` and `account_live.ex` render/1 (class-by-class swap onto `ck_page/1`); it creates NO new module-level symbols and NO new CSS (`.ck-output` is consumed, not authored — it was authored in P1). It ADDS two `test(...)` polish cases to `e2e/cohort-pages.spec.js` (`/ops`, `/account/:id/delete`) using the shared P1 helper, and two per-page contract `test`s to `cohort_migration_contract_test.exs` via the shared P1 helpers.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate /ops onto ck_page/1 (.ck-btn buttons + .ck-output panels), atomic commit</name>
  <read_first>
    - examples/adoption_demo/lib/adoption_demo_web/live/ops_live.ex (READ FULLY — render/1: header L28-29; the two `<button id=... phx-click="run_doctor"/"run_runtime_status" class="btn" data-testid=...>` L32-37; the two `<pre :if=... id="doctor-output"/"runtime-status-output" class="... bg-gray-100 ..." data-testid=...>` L40-41; `<section id="batch-erasure" data-testid="batch-erasure-section">` L43 with `batch-member-#{email}` spans L47-49, `preview-batch-button`/`execute-batch-button` L52-57, `batch-preview`/`batch-result` `<pre>` L59-60; handle_event clauses are UNCHANGED)
    - examples/adoption_demo/lib/adoption_demo_web/components/cohort_components.ex (ck_page/1 from P1; the `.ck-btn`/`.ck-btn--primary` classes for bare buttons — note ck_button/1 is link-only, do NOT use it on phx-click buttons)
    - examples/adoption_demo/priv/static/assets/cohort.css .ck-output (P1) + L326-360 (.ck-btn / .ck-btn--primary)
    - .planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-RESEARCH.md (/ops per-page class map; Pattern 2 class swap; Pattern 3 <pre> panels; Pitfall 4 phx-click button)
  </read_first>
  <action>
    In `ops_live.ex` render/1: keep `<Layouts.app flash={@flash} page_title={@page_title}>`. Add `theme: "light"` to `mount/3` assigns. Wrap the body in `<.ck_page title="Operator surfaces" lede="Doctor, runtime status, and batch owner erasure." theme={@theme}>` (move the `<h1>`/`<p>` header into the scaffold title/lede). For BOTH top buttons (`run-doctor-button`, `run-runtime-status-button`): KEEP the `<button id=... phx-click="run_doctor"/"run_runtime_status" data-testid=...>` element and ONLY change `class="btn"` → `class="ck-btn ck-btn--primary"` (or `ck-btn`) — phx-click/id/testid byte-for-byte (Pitfall 4). Replace the wrapping `class="flex gap-3 mt-4"` with a `.ck`-scoped row (e.g. `.ck-toolbar` if appropriate, or an unclassed div inheriting `.ck`) — keep structure. For BOTH top `<pre>` panels (`doctor-output`, `runtime-status-output`): KEEP `<pre :if={...} id=... data-testid=...>` and swap `class="mt-6 p-3 bg-gray-100 text-xs overflow-x-auto"` → `class="ck-output"` (the P1 token-only rule). For the batch section: keep `<section id="batch-erasure" data-testid="batch-erasure-section">` (swap its `class="mt-10 space-y-3"` for `.ck-section`), keep the `<h2>` (text + swap to `.ck-section__title`), keep every `<span data-testid={"batch-member-#{member.email}"}>` byte-for-byte; the two batch buttons (`preview-batch-button`, `execute-batch-button`) get bare `.ck-btn`; the two batch `<pre>` (`batch-preview`, `batch-result`) get `.ck-output`. Remove ALL daisyUI/Tailwind utilities from the body (`btn`, `bg-gray-100`, `text-2xl`, `text-lg`, `text-sm`, `text-xs`, `opacity-80`, `font-semibold`, `space-y-3`, `flex gap-3`, `mt-4`, `mt-6`, `mt-10`, `p-3`, `overflow-x-auto`). Change NO handle_event logic. Introduce NO `raw/1`. Add NO CSS (`.ck-output` is P1; if missing, file a P1 defect — Pitfall 1/2). ONE atomic commit for `/ops`.
  </action>
  <verify>
    <automated>cd examples/adoption_demo && MIX_ENV=test mix compile 2>&1 | grep -iv "Mox\|test/support" | grep -i "error" || true ; mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<.ck_page" examples/adoption_demo/lib/adoption_demo_web/live/ops_live.ex` ≥ 1.
    - `grep -c 'phx-click="run_doctor"\|phx-click="run_runtime_status"\|phx-click="preview_batch"\|phx-click="execute_batch"' examples/adoption_demo/lib/adoption_demo_web/live/ops_live.ex` = 4 (all handlers preserved on buttons).
    - `grep -c "ck-output" examples/adoption_demo/lib/adoption_demo_web/live/ops_live.ex` = 4 (all four <pre> panels) and `grep -c "bg-gray-" ...` = 0.
    - Frozen ids/testids survive: grep finds `run-doctor-button`, `run-runtime-status-button`, `doctor-output`, `runtime-status-output`, `batch-erasure-section`, `preview-batch-button`, `execute-batch-button`, `batch-preview`, `batch-result`, `batch-member-`.
    - No ck_button replacing a phx-click button: `grep -c "<.ck_button" examples/adoption_demo/lib/adoption_demo_web/live/ops_live.ex` = 0.
    - daisyUI retired: `grep -c 'class="btn"\|text-2xl' examples/adoption_demo/lib/adoption_demo_web/live/ops_live.ex` = 0; `grep -c "raw(" ...` = 0.
    - `mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs` passes (after the /ops test is added in Task 3-equivalent below).
  </acceptance_criteria>
  <done>/ops renders through ck_page/1; all four phx-click handlers stay on .ck-btn buttons, all four <pre> are .ck-output, the batch section + member spans + all ids/testids survive; no daisyUI in body; no raw/1; one atomic commit.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate /account erasure onto ck_page/1 (.ck-btn buttons + .ck-output panels), atomic commit</name>
  <read_first>
    - examples/adoption_demo/lib/adoption_demo_web/live/account_live.ex (READ FULLY — render/1: `<h1>` L24; `<p data-testid="erasure-member-name">` L25-27; the two `<button id="preview-erasure-button"/"execute-erasure-button" phx-click="preview"/"execute" class="btn" data-testid=...>` L30-35; the two `<pre :if=... id="erasure-preview"/"erasure-result" class="... bg-gray-100 ..." data-testid=...>` L38-39; handle_event preview/execute UNCHANGED)
    - examples/adoption_demo/lib/adoption_demo_web/components/cohort_components.ex (ck_page/1; .ck-btn — ck_button/1 NOT used here, Pitfall 4)
    - examples/adoption_demo/priv/static/assets/cohort.css .ck-output (P1)
    - .planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-RESEARCH.md (/account per-page class map; Pitfall 4)
  </read_first>
  <action>
    In `account_live.ex` render/1: keep `<Layouts.app flash={@flash} page_title={@page_title}>`. Add `theme: "light"` to `mount/3` assigns. Wrap the body in `<.ck_page title="Owner erasure demo" theme={@theme}>`. Keep `<p data-testid="erasure-member-name">Member: {@member.name} ({@member.email})</p>` (swap `class="text-sm"` for `.ck`-scoped text — keep the testid + text). For BOTH buttons: KEEP `<button id="preview-erasure-button"/"execute-erasure-button" phx-click="preview"/"execute" data-testid=...>` and swap `class="btn"` → bare `.ck-btn` (execute may be `ck-btn--primary`) — phx-click/id/testid byte-for-byte (Pitfall 4). Swap the wrapping `class="flex gap-3 mt-4"` for a `.ck`-scoped row. For BOTH `<pre>` (`erasure-preview`, `erasure-result`): KEEP `<pre :if={...} id=... data-testid=...>{inspect(...)}</pre>` and swap `class="mt-6 p-3 bg-gray-100 text-xs"` → `class="ck-output"`. Remove ALL daisyUI utilities (`btn`, `bg-gray-100`, `text-2xl`, `text-sm`, `text-xs`, `font-semibold`, `flex gap-3`, `mt-4`, `mt-6`, `p-3`). Change NO handle_event logic. Introduce NO `raw/1`. Add NO CSS. ONE atomic commit for `/account`.
  </action>
  <verify>
    <automated>cd examples/adoption_demo && MIX_ENV=test mix compile 2>&1 | grep -iv "Mox\|test/support" | grep -i "error" || true ; mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<.ck_page" examples/adoption_demo/lib/adoption_demo_web/live/account_live.ex` ≥ 1.
    - `grep -c 'phx-click="preview"\|phx-click="execute"' examples/adoption_demo/lib/adoption_demo_web/live/account_live.ex` = 2 (handlers preserved).
    - `grep -c "ck-output" examples/adoption_demo/lib/adoption_demo_web/live/account_live.ex` = 2 and `grep -c "bg-gray-" ...` = 0.
    - Frozen contract: grep finds `erasure-member-name`, `preview-erasure-button`, `execute-erasure-button`, `erasure-preview`, `erasure-result`.
    - `grep -c "<.ck_button" examples/adoption_demo/lib/adoption_demo_web/live/account_live.ex` = 0; `grep -c 'class="btn"\|text-2xl' ...` = 0; `grep -c "raw(" ...` = 0.
  </acceptance_criteria>
  <done>/account erasure renders through ck_page/1; both phx-click handlers stay on .ck-btn buttons, both <pre> are .ck-output, erasure-member-name + all ids/testids survive; no daisyUI in body; no raw/1; one atomic commit.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add /ops + /account polish cases and ExUnit frozen-contract tests</name>
  <read_first>
    - examples/adoption_demo/e2e/cohort-pages.spec.js (shared `assertCohortPagePolish` helper from P1)
    - examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs (shared `assert_frozen_contract`/`assert_daisyui_retired` helpers from P1; how /dashboard test obtains its conn/render in P2)
    - examples/adoption_demo/e2e/ops-surfaces.spec.js, batch-erasure.spec.js, owner-erasure.spec.js (the behavior specs that must stay green)
    - .planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-RESEARCH.md (§Validation Architecture: /ops + /account selector lists; the ExUnit frozen-contract example)
  </read_first>
  <action>
    PART A — in `e2e/cohort-pages.spec.js`, add a `test` for `/ops` (`assertCohortPagePolish(page, { route: "/ops", surface: "ops-cohort" })`) and a `test` for `/account/:id/delete` (use a seeded member id — derive it the way the existing owner-erasure spec navigates, e.g. via the dashboard member-delete-link or a known seeded id; `surface: "account-cohort"`). Reuse the shared P1 helper — no duplicated harness logic.
    PART B — in `cohort_migration_contract_test.exs`, add a `/ops` test asserting `assert_frozen_contract(html, ~w(run-doctor-button run-runtime-status-button doctor-output runtime-status-output batch-erasure-section preview-batch-button execute-batch-button batch-preview batch-result))` plus `phx-click="run_doctor"`/`"preview_batch"` survive and `data-ck-root` present, then `assert_daisyui_retired(html)` for the /ops body. Add an `/account` test asserting `assert_frozen_contract(html, ~w(erasure-member-name preview-erasure-button execute-erasure-button erasure-preview erasure-result))` plus `phx-click="preview"`/`"execute"` and `data-ck-root`, then `assert_daisyui_retired(html)`. NOTE the `<pre :if=...>` panels only render when their assign is set — assert the panel ids/testids only after triggering the handler in a `live`-driven test (render_click the button) OR assert the always-present selectors (buttons/section/member-name) statically and the `<pre>` ids after a `render_click`; pick the approach the sibling specs use and document it. Run `npx playwright test e2e/ops-surfaces.spec.js e2e/batch-erasure.spec.js e2e/owner-erasure.spec.js` (CI-delegate if the local lane is saturated — record in SUMMARY).
  </action>
  <verify>
    <automated>cd examples/adoption_demo && node --check e2e/cohort-pages.spec.js && mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"/ops"' examples/adoption_demo/e2e/cohort-pages.spec.js` ≥ 1 and an `/account` polish test exists; `node --check` passes.
    - `grep -c 'run-doctor-button' examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs` ≥ 1 and `grep -c 'erasure-member-name' ...` ≥ 1.
    - `mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs` passes (the /ops + /account contract tests green).
    - admin-polish.js unchanged: `git -C /Users/jon/projects/rindle status --porcelain examples/adoption_demo/e2e/support/admin-polish.js` empty.
    - ops-surfaces / batch-erasure / owner-erasure behavior specs pass locally OR are recorded CI-delegated in the SUMMARY.
  </acceptance_criteria>
  <done>The /ops + /account polish cases (shared warn-mode helper) and ExUnit frozen-contract + daisyUI-retirement tests are added and green; the three behavior specs pass (or CI-delegated); admin-polish.js untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client phx-click → erasure/diagnostic handlers | run_doctor/run_runtime_status/preview_batch/execute_batch (/ops) + preview/execute (/account) are existing destructive/diagnostic handlers; this phase changes presentation only, no handler logic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-03-01 | Tampering | Replacing a `<button phx-click>` with link-only `ck_button/1` drops the handler, silently breaking erasure/diagnostic flows | mitigate | Bare `.ck-btn` on the existing `<button>` (Pitfall 4); ExUnit grep asserts every phx-click survives; ops/batch/owner-erasure behavior specs are the runtime backstop |
| T-99-03-02 | Information Disclosure / XSS | `<pre>{inspect(...)}</pre>` is existing escaped output; introducing `raw/1` during restyle | mitigate | `.ck-output` is a class swap only; grep asserts no `raw/1`; HEEx auto-escape preserved |
| T-99-03-SC | Tampering | npm/hex package installs | accept | N/A — zero new packages |
</threat_model>

<verification>
- ExUnit /ops + /account frozen-contract + daisyUI-retirement tests green (all phx-click handlers, ids/testids, `<pre>` panels, batch member spans, erasure-member-name).
- ops-surfaces / batch-erasure / owner-erasure behavior specs green or CI-delegated.
- Every phx-click button is a bare `.ck-btn` (no ck_button/1); every `<pre>` is `.ck-output`; no daisyUI in either body; no `raw/1`.
- No new CSS added (.ck-output is P1); admin-polish.js + cohort.css unchanged.
</verification>

<success_criteria>
- /ops and /account migrated onto ck_page/1, class-by-class, with [data-ck-root] shells; every phx-click handler preserved on a .ck-btn button; every <pre> is .ck-output with id/testid/:if intact.
- daisyUI retired from both bodies; no raw/1.
- Two polish cases (warn mode) + two ExUnit tests added and green; behavior specs green or CI-delegated.
- ONE atomic commit per page (two commits total).
</success_criteria>

<output>
Create `.planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-03-SUMMARY.md` when done.
</output>

---
phase: 99-cohort-page-migrations-the-small-7-track-b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/adoption_demo/lib/adoption_demo_web/components/cohort_components.ex
  - examples/adoption_demo/priv/static/assets/cohort.css
  - examples/adoption_demo/e2e/cohort-pages.spec.js
  - examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs
autonomous: true
requirements: [COHORT-01, COHORT-03, COHORT-04]
tags: [cohort, design-system, css, hand-authored-css, scaffold, ck-page, e2e, exunit, frozen-contract]

must_haves:
  truths:
    - "ck_page/1 exists in cohort_components.ex rendering <div class=\"ck\" data-ck-root data-theme={@theme}> + <div class=\"ck__wrap\"> + a .ck-hero header (:title required, optional :eyebrow/:lede) + {render_slot(@inner_block)} — the per-page .ck shell + data-ck-root + server-owned data-theme seam (D-96-05/07/09)."
    - "ck_page/1's :theme attr defaults to \"light\" with values ~w(light dark); data-ck-root is on the .ck div, NEVER on <body> (D-96-05, Pitfall 3)."
    - "cohort.css gains ONE hand-authored token-only .ck-output rule for the <pre> debug panels (consumes only var(--ck-code-bg)/var(--ck-code-ink)/existing --ck-* tokens; NO hex/rgb literal outside the :root/[data-theme] token blocks, D-96-20)."
    - "node brandbook/src/cohort-contrast.mjs exits 0 (parity + literal scanner + contrast pass; no new --ck-* token/pair introduced, D-96-18/20/23)."
    - "e2e/cohort-pages.spec.js exists, exports/defines a shared per-route polish helper that calls assertAdminPolish UNCHANGED over root \"[data-ck-root]\" in WARN mode (offender aggregate downgraded to console.warn; ReferenceError/TypeError re-thrown) — admin-polish.js is NOT edited (D-96-06)."
    - "test/adoption_demo_web/live/cohort_migration_contract_test.exs exists with a render-based per-page frozen-contract + daisyUI-retirement helper (the Wave-0 ExUnit home) that later per-page plans extend."
  prohibitions:
    - "NO .mjs generator or build step for cohort.css is introduced; cohort.css stays hand-authored (D-94-05/06, Pitfall 1). No 'regenerate cohort.css' task."
    - "admin-polish.js is NOT modified (D-96-06)."
    - "No new --ck-* token or color literal outside the :root/[data-theme]/prefers-color-scheme token blocks (D-96-20/23)."
    - "ck_page/1 introduces no raw/1 (HEEx auto-escape preserved)."
  artifacts:
    - path: "examples/adoption_demo/lib/adoption_demo_web/components/cohort_components.ex"
      provides: "ck_page/1 scaffold component (the Cohort analog of Phase 98 page/1)"
      contains: "def ck_page("
    - path: "examples/adoption_demo/priv/static/assets/cohort.css"
      provides: "Token-only .ck-output rule for <pre> debug panels"
      contains: ".ck-output"
    - path: "examples/adoption_demo/e2e/cohort-pages.spec.js"
      provides: "Shared per-route polish harness (warn mode) over [data-ck-root]; per-page plans add their route case"
      contains: "assertAdminPolish"
    - path: "examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs"
      provides: "ExUnit frozen-contract + daisyUI-retirement home; per-page plans add their selector lists"
      contains: "data-ck-root"
  key_links:
    - from: "examples/adoption_demo/lib/adoption_demo_web/components/cohort_components.ex ck_page/1"
      to: "examples/adoption_demo/priv/static/assets/cohort.css .ck / .ck__wrap / .ck-hero / .ck-output"
      via: "ck_page renders class=\"ck\" + .ck__wrap + .ck-hero, all of which already exist in cohort.css; .ck-output is the one net-new rule for <pre> panels composed by ops/account"
      pattern: "data-ck-root"
    - from: "examples/adoption_demo/e2e/cohort-pages.spec.js"
      to: "examples/adoption_demo/e2e/support/admin-polish.js"
      via: "reuses assertAdminPolish({ root: '[data-ck-root]', interactiveSelectors }) UNCHANGED (D-96-06 seam)"
      pattern: "assertAdminPolish"
---

<objective>
Author the Wave-0 enablers all seven page migrations depend on: the `ck_page/1` scaffold in `cohort_components.ex` (the Cohort analog of Phase 98's `page/1`), the single token-only `.ck-output` rule in the hand-authored `cohort.css` for the `<pre>` debug panels, the shared `e2e/cohort-pages.spec.js` polish harness (reusing `assertAdminPolish` UNCHANGED over `[data-ck-root]` in warn mode), and the ExUnit frozen-contract / daisyUI-retirement test module that later per-page plans extend.

Purpose: All 7 pages need the IDENTICAL `.ck` + `data-ck-root` + server-`data-theme` + `.ck__wrap` shell (today none have it — the shell is the daisyUI `<main>` in `Layouts.app`, RESEARCH §System Architecture). Centralizing it in one `ck_page/1` scaffold prevents 7 copies of boilerplate drifting (D-96-05, RESEARCH Open Q1, orchestrator LOCKED DECISION 1). Landing the harness + ExUnit home first means each per-page plan only adds markup + one polish case + one grep list. Implements LOCKED DECISIONS 1/2/3.

Output: `ck_page/1` exists existing-but-UNUSED (no page composes it yet); `.ck-output` authored token-only and passing the `cohort-contrast.mjs` literal scanner; `cohort-pages.spec.js` defines the shared warn-mode polish helper (with the 7-route case stubs added per per-page plan); the ExUnit contract module exists with its shared render/grep helper.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-RESEARCH.md
@.planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-VALIDATION.md
@.planning/phases/96-cohort-component-layer-dark-reduced-motion-contract/96-CONTEXT.md
</context>

<artifacts_this_phase_produces>
This plan (P1) creates these net-new symbols — the source-grounding/drift pass must EXCLUDE them from "missing/undefined" verification because they are newly authored here:
- `ck_page/1` function component in `examples/adoption_demo/lib/adoption_demo_web/components/cohort_components.ex` (attrs `:title` required, `:eyebrow`/`:lede`/`:theme` optional, `:rest` global; `slot :inner_block` required).
- `.ck-output` CSS rule in `examples/adoption_demo/priv/static/assets/cohort.css` (token-only; for the ops/account `<pre>` debug panels).
- `examples/adoption_demo/e2e/cohort-pages.spec.js` (shared per-route polish harness; per-page plans add their route cases).
- `examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs` (ExUnit module + shared render/grep helper; per-page plans add their per-page selector lists).
NOTE: `ck_page/1` ends this plan UNUSED — no `*_live.ex` composes it yet (that is P2–P5). The `.ck-output` rule has no consumer yet (ops/account add the class in P3).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author ck_page/1 scaffold in cohort_components.ex + the token-only .ck-output rule in cohort.css</name>
  <read_first>
    - examples/adoption_demo/lib/adoption_demo_web/components/cohort_components.ex (READ the `hero/1` def ~L44-61 for the attr/slot/`~H`/`render_slot` idiom and the `.ck-hero`/`.ck-eyebrow`/`.ck-hero__title`/`.ck-hero__lede` class names it emits; note `ck_button/1` ~L68-82 renders `<.link>` and is link-only — NOT relevant here)
    - examples/adoption_demo/lib/adoption_demo_web/live/styleguide_live.ex L84-95 (the per-LiveView `.ck` shell: `<div class="ck" data-ck-root data-theme={@theme}>` + `<main class="ck__wrap">` + `<.hero>` — the exact shell `ck_page/1` centralizes; note theme is server `assign(:theme)` default "light", D-96-07)
    - examples/adoption_demo/priv/static/assets/cohort.css L194-225 (`.ck`, `.ck *` box-sizing, `.ck__wrap`, `.ck a`, `.ck :focus-visible` — the base rules the shell inherits) + L284-318 (`.ck-hero`/`.ck-eyebrow`/`.ck-hero__title`/`.ck-hero__lede`) + L368-386 (`.ck-section`) + L438-446 (`.ck-cred__value` shows the existing `var(--ck-code-ink)`/`var(--ck-code-bg)`/`var(--ck-border)`/`var(--ck-radius-sm)` token idiom for a code surface) + L44-72 + L116-137 (the `--ck-code-bg`/`--ck-code-ink` token blocks per theme)
    - .planning/phases/96-cohort-component-layer-dark-reduced-motion-contract/96-CONTEXT.md (D-96-05 root placement, D-96-07/16 server theme, D-96-09 BEM/.ck-scoped convention, D-96-20 literal scanner, D-96-23 token values frozen)
    - .planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-RESEARCH.md (Pattern 1 `ck_page/1` example, Pattern 3 `<pre>` panel rule, Pitfall 1 hand-authored-CSS, Pitfall 3 root placement)
  </read_first>
  <action>
    PART A — `ck_page/1` in `cohort_components.ex` (add a new function component; place it near `hero/1`, following the file's flat-function idiom D-96-09/14). Declare attrs immediately above the def: `attr :title, :string, required: true`; `attr :eyebrow, :string, default: nil`; `attr :lede, :string, default: nil`; `attr :theme, :string, default: "light", values: ~w(light dark)`; `attr :rest, :global`; `slot :inner_block, required: true`. Render EXACTLY this shell (mirroring `styleguide_live.ex` L86-95 but as a reusable component): outer `<div class="ck" data-ck-root data-theme={@theme} {@rest}>` (the `data-ck-root` MUST be on this `.ck` div, never on `<body>` — D-96-05, Pitfall 3), then `<div class="ck__wrap">`, then a header reusing the EXISTING `.ck-hero` grammar — `<header class="ck-hero">` with `<span :if={@eyebrow} class="ck-eyebrow">{@eyebrow}</span>`, `<h1 class="ck-hero__title">{@title}</h1>`, `<p :if={@lede} class="ck-hero__lede">{@lede}</p>` — then `{render_slot(@inner_block)}`, closing both divs. Use only HEEx interpolation (`{...}`) — introduce NO `raw/1`. Do NOT add `<.cohort_nav>`/`<.cohort_footer>` inside `ck_page` (those stay in `Layouts.app`, RESEARCH §Architecture Responsibility Map — page chrome tier). Do NOT compose `ck_page` from any `*_live.ex` in this plan — it ends UNUSED (verify below).
    PART B — `.ck-output` rule in `cohort.css` (hand-authored, token-only — there is NO generator, Pitfall 1; write the rule directly into the file). Add ONE `.ck`-scoped rule `.ck-output` (and may add `.ck-output` modifier-free; keep it a single class) for the ops/account `<pre>` debug panels. Style it as a code/output surface reusing ONLY existing tokens, exactly mirroring the `.ck-cred__value` token set (cohort.css L438-446): `font-family: var(--ck-font-mono)`; `font-size: var(--ck-step--1)`; `color: var(--ck-code-ink)`; `background: var(--ck-code-bg)`; `border: 1px solid var(--ck-border)`; `border-radius: var(--ck-radius-sm)`; `padding` using existing `--ck-*` space steps; `overflow-x: auto`; `white-space: pre`. Use NO hex/rgb/named-color literal and NO raw `px`/`rem` font/color value anywhere in the rule body — every value is a `var(--ck-*)` (the ONLY literals allowed in the file live in the `:root`/`[data-theme]`/`prefers-color-scheme` token blocks, D-96-20). Place the rule in a sensible `.ck-*` section of the file (e.g. near the other surface primitives), NOT inside a token block. Introduce NO new `--ck-*` token and change NO existing token value (D-96-23).
    Then run the hand-authored-CSS gate: `node brandbook/src/cohort-contrast.mjs` (parity + literal scanner + contrast) — it must exit 0. Do NOT create or run any `.mjs` that "generates" `cohort.css`; do NOT edit `tokens.json`.
  </action>
  <verify>
    <automated>cd examples/adoption_demo && MIX_ENV=test mix compile 2>&1 | grep -iv "Mox\|test/support" | grep -i "error" || true ; cd /Users/jon/projects/rindle && node brandbook/src/cohort-contrast.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def ck_page(" examples/adoption_demo/lib/adoption_demo_web/components/cohort_components.ex` returns a match.
    - `grep -c "data-ck-root" examples/adoption_demo/lib/adoption_demo_web/components/cohort_components.ex` ≥ 1 AND `grep -c "data-ck-root" examples/adoption_demo/lib/adoption_demo_web/components/layouts/root.html.heex` returns 0 (root stays off `<body>`, Pitfall 3).
    - `grep -n 'slot :inner_block, required: true' examples/adoption_demo/lib/adoption_demo_web/components/cohort_components.ex` (ck_page requires a body) and `grep -n 'attr :title, :string, required: true' ...` present.
    - `grep -c "raw(" examples/adoption_demo/lib/adoption_demo_web/components/cohort_components.ex` is unchanged from before this plan (no new `raw/1`).
    - `grep -c "\.ck-output" examples/adoption_demo/priv/static/assets/cohort.css` ≥ 1.
    - The `.ck-output` rule body contains no hex (`grep` the rule region shows no `#` color and no bare `rgb(`/`hsl(` literal) — values are all `var(--ck-*)`.
    - `node brandbook/src/cohort-contrast.mjs` exits 0 (parity + literal scanner + contrast green — no new token/pair, D-96-18/20).
    - `ck_page/1` is NOT referenced from any `*_live.ex` yet: `grep -rn "<.ck_page" examples/adoption_demo/lib/adoption_demo_web/live/` returns nothing.
    - No new `.mjs` for cohort.css and no `tokens.json` change: `git -C /Users/jon/projects/rindle status --porcelain brandbook/tokens/tokens.json` is empty.
  </acceptance_criteria>
  <done>ck_page/1 exists with the canonical `.ck`/`data-ck-root`/server-`data-theme`/`.ck__wrap`/`.ck-hero` shell, requires `:title` + `:inner_block`, introduces no `raw/1`, and is UNUSED; cohort.css gains ONE token-only `.ck-output` rule passing the literal scanner; cohort-contrast.mjs is green; no generator/tokens.json touched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author the shared cohort-pages.spec.js polish harness + the ExUnit frozen-contract/daisyUI-retirement contract module</name>
  <read_first>
    - examples/adoption_demo/e2e/cohort-styleguide.spec.js (READ FULLY — the template: `require("./support/admin-polish")` + `require("./support/liveview")`, the `interactiveSelectors` list, `waitForLiveSocket`, the `reportPolish` warn-mode downgrade that re-throws ReferenceError/TypeError but console.warns the offender aggregate, the `[data-ck-root]` visibility expectation pattern)
    - examples/adoption_demo/e2e/support/admin-polish.js L858-920 (the `assertAdminPolish(page, { viewport, surface, root, interactiveSelectors })` signature — reuse UNCHANGED; do NOT edit this file, D-96-06)
    - examples/adoption_demo/e2e/support/liveview.js (`waitForLiveSocket`) and e2e/support/cohort.js (`MEMBERS`, `memberRow`, `memberId` — the load-bearing `/dashboard` reads, used by per-page plans)
    - examples/adoption_demo/test/adoption_demo_web/live/launchpad_live_test.exs (READ — the adoption_demo web LiveView ExUnit home + its conn/live render idiom for this app; mirror its module header, `use AdoptionDemoWeb.ConnCase`/`import Phoenix.LiveViewTest` setup)
    - .planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-RESEARCH.md (Code Examples: the per-page polish case + the `render_to_string` frozen-contract ExUnit example; Pitfall 5 root-visibility guard; Pitfall 6 daisyUI-retirement scan; §Validation Architecture per-clause homes) and 99-VALIDATION.md
  </read_first>
  <action>
    PART A — `e2e/cohort-pages.spec.js` (NEW; model on `cohort-styleguide.spec.js`). Require `{ test, expect }` from `@playwright/test`, `{ assertAdminPolish }` from `./support/admin-polish`, `{ waitForLiveSocket }` from `./support/liveview`. Define the FIXED Cohort `interactiveSelectors` list (`[".ck-btn", ".ck-tab", ".ck-input", ".ck-select"]`, D-96-06). Define a SHARED, reusable per-route helper — `assertCohortPagePolish(page, { route, surface })` — that: (1) `await page.goto(route)`; (2) `await waitForLiveSocket(page)`; (3) `await expect(page.locator("[data-ck-root]")).toBeVisible()` FIRST (Pitfall 5 guard — the migrated page MUST render the `.ck` shell or the gate vacuously passes); (4) calls `assertAdminPolish(page, { viewport: "desktop", surface, root: "[data-ck-root]", interactiveSelectors })` reused UNCHANGED; (5) downgrades a polish *offender aggregate* to `console.warn` (warn mode this phase, D-96-06) while RE-THROWING a `ReferenceError`/`TypeError` (a genuine harness crash) — copy the `reportPolish` predicate from `cohort-styleguide.spec.js` (`/Admin polish gate failed/` + not-ReferenceError + not-TypeError). Export/expose `assertCohortPagePolish` AND `interactiveSelectors` (e.g. via a small local helper block or `module.exports`) so per-page plans can `require` it OR call it inside a `test(...)` they add to this file. Add NO per-route `test(...)` cases in THIS plan beyond ONE smoke check that the harness wiring loads (a `test` that the helper is a function / or a single trivial `/styleguide` reuse is acceptable) — the 7 real route cases are added by P2–P5. Do NOT edit `admin-polish.js`.
    PART B — `test/adoption_demo_web/live/cohort_migration_contract_test.exs` (NEW; model the module header on `launchpad_live_test.exs`). `use AdoptionDemoWeb.ConnCase, async: true` (match the sibling test's conn/live idiom — if seeded data is needed for `/dashboard` etc., mirror how the existing tests obtain a `conn` + live render). Define a SHARED helper that, given a route + a list of expected selectors, renders the LiveView (via `live(conn, route)` then `render(view)`, or `Phoenix.LiveViewTest.render_to_string/...` if a connless render suffices) and returns the HTML string. Define a SHARED `assert_frozen_contract/2` (asserts each `data-testid="..."`/`id="..."`/`phx-click="..."`/`phx-submit="..."` selector in the list `=~` the HTML, asserts `data-ck-root` is present, and `refute`s `raw(` was introduced) and a SHARED `assert_daisyui_retired/1` (refutes the known daisyUI/Tailwind utility class set in the page body — `class="btn"`, `text-2xl`, `text-lg`, `bg-gray-`, `list-disc`, `opacity-80`, `space-y-`, `font-mono text-sm` where applicable — Pitfall 6; SCOPE the negative scan to the page body, NOT the shared `Layouts.app` `<main>`/`cohort_nav`/`cohort_footer` which keep daisyUI until Phase 101). Add NO per-page `test` blocks in THIS plan beyond ONE test proving the helpers compile/run against `/styleguide` (which already renders `data-ck-root` + `.ck-*` and no daisyUI body utilities) — the 7 real per-page contract tests are added by P2–P5. Each negative-grep literal that this module searches for as a daisyUI class to RETIRE (e.g. the string the scan matches) MUST live only inside the helper's match list, never echoed in a doc/comment that the per-page render could contain — keep the retired-class list as data, not prose.
  </action>
  <verify>
    <automated>cd examples/adoption_demo && mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs && node -e "require('./e2e/cohort-pages.spec.js')" 2>/dev/null || node --check e2e/cohort-pages.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `examples/adoption_demo/e2e/cohort-pages.spec.js` exists; `grep -c "assertAdminPolish" examples/adoption_demo/e2e/cohort-pages.spec.js` ≥ 1 and `grep -c '\[data-ck-root\]' examples/adoption_demo/e2e/cohort-pages.spec.js` ≥ 1 (root-visibility guard + gate root).
    - `node --check examples/adoption_demo/e2e/cohort-pages.spec.js` exits 0 (valid JS).
    - `grep -c "admin-polish" examples/adoption_demo/e2e/support/admin-polish.js` shows the file unchanged (no edit to admin-polish.js): `git -C /Users/jon/projects/rindle status --porcelain examples/adoption_demo/e2e/support/admin-polish.js` is empty.
    - `examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs` exists; `grep -c "data-ck-root" ...` ≥ 1 (frozen-contract helper asserts the shell) and it defines `assert_frozen_contract` + `assert_daisyui_retired` (or equivalently-named shared helpers).
    - `cd examples/adoption_demo && mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs` passes (the lone /styleguide smoke test green; helpers compile).
    - No per-page `test` for the 7 routes yet (those are P2–P5): the module has at most the one /styleguide smoke test.
  </acceptance_criteria>
  <done>cohort-pages.spec.js defines the shared warn-mode `assertCohortPagePolish` helper (reusing assertAdminPolish UNCHANGED over `[data-ck-root]`, Pitfall-5-guarded) and the ExUnit contract module defines the shared frozen-contract + daisyUI-retirement helpers, both green against /styleguide and ready for per-page plans to extend; admin-polish.js untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeded demo data → rendered markup | The 7 LiveViews interpolate member names / asset ids / inspect output into HEEx; HEEx auto-escapes. `ck_page/1` + `.ck-output` change presentation only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-01-01 | Tampering | A hand-edit of `cohort.css` introducing a raw color/measure literal outside the token blocks ships an off-token surface | mitigate | `node brandbook/src/cohort-contrast.mjs` literal scanner (D-96-20) fails on any hex/rgb outside `:root`/`[data-theme]`; the `.ck-output` rule is token-only |
| T-99-01-02 | Information Disclosure / XSS | `ck_page/1` or a migrated page interpolating untrusted markup via `raw/1` | mitigate | `ck_page/1` uses only HEEx `{...}` interpolation (auto-escaped); ExUnit + grep assert no `raw/1` is introduced (RESEARCH Security Domain) |
| T-99-01-SC | Tampering | npm/hex/node package installs | accept | N/A — this phase installs ZERO new packages (RESEARCH Package Legitimacy Audit N/A); composes existing in-repo components + existing test harness |
</threat_model>

<verification>
- `node brandbook/src/cohort-contrast.mjs` green (parity + literal scanner + contrast; no new `--ck-*` token/pair).
- `MIX_ENV=test mix compile` clean for cohort_components.ex (pre-existing test-only Mox warnings allowed per Phase-96 note).
- `mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs` green (helpers + /styleguide smoke).
- `node --check e2e/cohort-pages.spec.js` valid.
- `ck_page/1` compiles and is UNUSED (no `*_live.ex` composes it yet); `.ck-output` has no consumer yet (ops/account add it in P3).
- admin-polish.js, tokens.json, and root.html.heex `<body>` are all unchanged (D-96-05/06, Pitfall 1/3).
</verification>

<success_criteria>
- `ck_page/1` scaffold exists with the `.ck`/`data-ck-root`/server-`data-theme`/`.ck__wrap`/`.ck-hero` shell, required `:title` + `:inner_block`, default `theme: "light"`, no `raw/1`, and is UNUSED.
- One token-only `.ck-output` rule in cohort.css passes the literal scanner; no generator, no tokens.json change, no token value change.
- `cohort-pages.spec.js` shared warn-mode polish harness (assertAdminPolish reused UNCHANGED, Pitfall-5-guarded) + ExUnit frozen-contract/daisyUI-retirement helpers exist and are green against /styleguide.
- admin-polish.js untouched.
</success_criteria>

<output>
Create `.planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-01-SUMMARY.md` when done.
</output>

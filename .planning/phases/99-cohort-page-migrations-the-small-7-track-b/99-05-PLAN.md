---
phase: 99-cohort-page-migrations-the-small-7-track-b
plan: 05
type: execute
wave: 5
depends_on: ["99-01"]
files_modified:
  - examples/adoption_demo/lib/adoption_demo_web/live/post_live.ex
  - examples/adoption_demo/lib/adoption_demo_web/live/media_live.ex
  - examples/adoption_demo/e2e/cohort-pages.spec.js
  - examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs
autonomous: true
requirements: [COHORT-04]
tags: [cohort, migration, post, media, picture-tag, dl-restyle, variant-list, frozen-contract]

must_haves:
  truths:
    - "/posts/:id and /media/:id each render through ck_page/1 (a [data-ck-root] .ck shell), theme defaulted to \"light\" from mount; Layouts.app <main> intact (Phase 101)."
    - "post: post-title + post-image-section survive; the post-picture-tag wrapper + post-no-image survive; the picture_tag call is intact; the post body <p> text is preserved."
    - "media: the existing <dl><dt><dd> is RESTYLED in place (NOT converted to ck_detail) so media-id + media-state + media-delivery-url survive byte-for-byte on their <dd> (id + data-testid); media-variants section + every variant <li id=\"variant-#{name}\"> survive (<ul>/<li> kept, Pitfall 2); media-alex-profile-link survives (may become ck_button variant=\"quiet\" OR a .ck link, keeping testid + text)."
    - "Migration is class-by-class; the <dl>, picture_tag wrapper, and variant <ul>/<li> structures preserved; rendering.spec.js stays green (picture tag + link role names unchanged, A3)."
  prohibitions:
    - "The media `<dl>` is NOT converted to ck_detail/1 — ck_detail generates its own <dd> and would drop media-id/media-state/media-delivery-url; restyle the existing <dl> in place (RESEARCH /media class map, Pitfall 2)."
    - "The media variant <ul>/<li id=variant-#{name}> is NOT converted to ck_table (Pitfall 2)."
    - "No daisyUI/Tailwind utility remains in either page body (text-2xl, text-lg, text-sm, font-semibold, list-disc, pl-5, mt-, break-all, underline, space-y-, inline) EXCEPT the shared Layouts.app <main> (Phase 101)."
    - "No id/data-testid dropped/renamed; picture_tag call not broken; no raw/1 introduced."
  artifacts:
    - path: "examples/adoption_demo/lib/adoption_demo_web/live/post_live.ex"
      provides: "/posts/:id migrated onto ck_page/1, image + body contracts preserved"
      contains: "<.ck_page"
    - path: "examples/adoption_demo/lib/adoption_demo_web/live/media_live.ex"
      provides: "/media/:id migrated onto ck_page/1, <dl> restyled in place + variant list preserved"
      contains: "<.ck_page"
  key_links:
    - from: "examples/adoption_demo/lib/adoption_demo_web/live/media_live.ex <dl>"
      to: "rendering.spec.js / media consumers"
      via: "the existing <dl><dt><dd id=media-id/media-state/media-delivery-url> is restyled in place with .ck-scoped classes, NOT replaced by ck_detail (preserves the dd ids/testids)"
      pattern: "media-delivery-url"
    - from: "examples/adoption_demo/lib/adoption_demo_web/live/media_live.ex variant <li>"
      to: "frozen variant contract"
      via: "each variant <li> keeps id=variant-#{name}; the <ul>/<li> is preserved, not ck_table"
      pattern: "variant-"
---

<objective>
Migrate `/posts/:id` (`post_live.ex`) and `/media/:id` (`media_live.ex`) onto `ck_page/1` class-by-class (COHORT-04) — the final two of the small 7. Post is the simplest page (title + body + a `picture_tag` image section). Media carries the one tricky case: a hand-built `<dl><dt><dd>` whose `<dd>` elements hold the frozen `media-id`/`media-state`/`media-delivery-url` ids+testids — it MUST be restyled in place, NOT replaced by `ck_detail/1` (which generates its own `<dd>` and would drop those ids — RESEARCH /media class map, Pitfall 2). ONE atomic commit per page; add each page's polish case + ExUnit frozen-contract test. This plan closes COHORT-04 (all five member/lesson/post/media/account pages migrated).

Purpose: Finishing post + media completes the phase's full page set. The media `<dl>` restyle-in-place is the single highest-risk class swap in the phase — getting it wrong silently drops `media-delivery-url` and friends. Implements the class-by-class HARD CONSTRAINT + LOCKED DECISION 1.

Output: both pages render `[data-ck-root]` shells; post image/body contracts preserved; the media `<dl>` restyled in place with all three `<dd>` ids/testids intact, the variant list preserved, the alex-profile link preserved; no daisyUI in either body; polish cases (warn mode) + ExUnit tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-RESEARCH.md
@.planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan MODIFIES `post_live.ex` and `media_live.ex` render/1 (class-by-class swap onto `ck_page/1`); it creates NO new module-level symbols and NO new CSS. It ADDS two `test(...)` polish cases to `e2e/cohort-pages.spec.js` and two per-page contract `test`s to `cohort_migration_contract_test.exs` via the shared P1 helpers. After this plan, all 7 routes have a polish case + ExUnit contract test.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate /posts/:id onto ck_page/1 (image + body), atomic commit</name>
  <read_first>
    - examples/adoption_demo/lib/adoption_demo_web/live/post_live.ex (READ FULLY — render/1: `<h1 data-testid="post-title">` L24; `<p class="text-sm">By {@post.member.name}</p>` L25; `<p class="mt-4">{@post.body}</p>` L26; `<section id="post-image" data-testid="post-image-section">` L28; `<div data-testid="post-picture-tag">` L31 wrapping `Rindle.HTML.picture_tag(...)`; `<p data-testid="post-no-image">` L39)
    - examples/adoption_demo/lib/adoption_demo_web/components/cohort_components.ex (ck_page/1; .ck-section)
    - .planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-RESEARCH.md (/posts per-page class map)
  </read_first>
  <action>
    In `post_live.ex` render/1: keep `<Layouts.app ...>`. Add `theme: "light"` to `mount/2` assigns. Wrap the body in `<.ck_page title={@post.title} theme={@theme}>` — keep `data-testid="post-title"` on the title (explicit `<h1 ... data-testid="post-title">` inside `:inner_block` if `ck_page` `:title` can't carry it, mirroring P2). Keep the `<p>By {@post.member.name}</p>` byline and the `<p>{@post.body}</p>` body (swap `class="text-sm"`/`class="mt-4"` for `.ck`-scoped text — keep the text + interpolations). Keep `<section id="post-image" data-testid="post-image-section">` byte-for-byte (swap `class="mt-6"` → `.ck-section`; `<h2>` → `.ck-section__title`). KEEP `<div data-testid="post-picture-tag">` wrapping `Rindle.HTML.picture_tag(...)` UNCHANGED (the `class:` option is a library `<img>` attribute). KEEP `<p data-testid="post-no-image">`. Remove ALL daisyUI utilities from the body (`text-2xl`, `text-lg`, `text-sm`, `font-semibold`, `mt-4`, `mt-6`). Introduce NO `raw/1`. ONE atomic commit for `/posts/:id`.
  </action>
  <verify>
    <automated>cd examples/adoption_demo && MIX_ENV=test mix compile 2>&1 | grep -iv "Mox\|test/support" | grep -i "error" || true ; mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<.ck_page" examples/adoption_demo/lib/adoption_demo_web/live/post_live.ex` ≥ 1.
    - Frozen contract: grep finds `post-title`, `post-image-section`, `post-picture-tag`, `post-no-image`.
    - `grep -c "Rindle.HTML.picture_tag" examples/adoption_demo/lib/adoption_demo_web/live/post_live.ex` = 1.
    - daisyUI retired: `grep -c 'text-2xl\|text-lg\|text-sm\|font-semibold' examples/adoption_demo/lib/adoption_demo_web/live/post_live.ex` = 0; `grep -c "raw(" ...` = 0.
  </acceptance_criteria>
  <done>/posts/:id renders through ck_page/1; the title, byline, body, image section, picture_tag wrapper, and no-image fallback all survive; no daisyUI in body; no raw/1; one atomic commit.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate /media/:id onto ck_page/1 (<dl> restyle-in-place + variant list + alex link), atomic commit</name>
  <read_first>
    - examples/adoption_demo/lib/adoption_demo_web/live/media_live.ex (READ FULLY — render/1: `<h1>Media detail</h1>` L27; the `<dl class="text-sm space-y-1 mt-4">` L28 with three rows `<div><dt class="inline font-semibold">…</dt> <dd class="inline" id="media-id"/"media-state"/"media-delivery-url" data-testid=...>` L29-31; `<section id="media-variants" data-testid="media-variants">` L34 + `<ul class="list-disc pl-5"><li :for={variant} id={"variant-#{variant.name}"}>` L36-40; `<.link navigate=… class="underline" data-testid="media-alex-profile-link">` L44-46)
    - examples/adoption_demo/lib/adoption_demo_web/components/cohort_components.ex (ck_page/1; .ck-section; ck_button/1 — usable for the alex link ONLY because it is an `<a>`/`<.link>`, not a phx-click button; but it must keep `data-testid="media-alex-profile-link"` + the link text — verify ck_button passes a `:rest`/global through that can carry data-testid, else keep a plain `.ck` `<.link>`)
    - examples/adoption_demo/priv/static/assets/cohort.css L438-446 (.ck-cred__value — the existing `<dl>`/code-surface token idiom for reference; do NOT add a new rule)
    - .planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-RESEARCH.md (/media class map — "Prefer restyling the existing <dl>"; Pitfall 2)
  </read_first>
  <action>
    In `media_live.ex` render/1: keep `<Layouts.app ...>`. Add `theme: "light"` to `mount/2` assigns. Wrap the body in `<.ck_page title="Media detail" theme={@theme}>`. CRITICAL — the `<dl>`: KEEP the existing `<dl><div><dt>…</dt> <dd id="media-id"/"media-state"/"media-delivery-url" data-testid=...>…</dd></div></dl>` STRUCTURE byte-for-byte; do NOT replace it with `ck_detail/1` (which generates its own `<dd>` and would drop those ids — RESEARCH /media class map). Restyle in place: swap `class="text-sm space-y-1 mt-4"` on the `<dl>` for a `.ck`-scoped class (or leave unclassed under `.ck`), swap `class="inline font-semibold"` on each `<dt>` and `class="inline"`/`class="inline break-all"` on each `<dd>` for `.ck`-scoped text — KEEP every `id`, `data-testid`, and the `inline`/`break-all` LAYOUT behavior if needed via a `.ck` class (do NOT add a new CSS rule; if a `.ck-detail`/`.ck-cred` class fits the dt/dd use it, otherwise minimal `.ck` text — preserve the ids/testids above all). Keep `<section id="media-variants" data-testid="media-variants">` (swap to `.ck-section`); KEEP the `<ul class="list-disc pl-5">` and EVERY `<li :for={variant} id={"variant-#{variant.name}"}>` byte-for-byte (NOT ck_table, Pitfall 2). The alex-profile link: keep `data-testid="media-alex-profile-link"` + the link text; either swap `class="underline"` for a `.ck` link class OR use `<.ck_button variant="quiet" ...>` IF it forwards `data-testid` via its `:rest` global (it includes `~w(target rel)` only — confirm; if `data-testid` is not forwarded, keep a plain `.ck` `<.link>` so the testid survives). Remove ALL daisyUI utilities from the body (`text-2xl`, `text-lg`, `text-sm`, `font-semibold`, `inline`, `break-all`, `space-y-1`, `list-disc`, `pl-5`, `mt-4`, `mt-6`, `mt-8`, `underline`). Introduce NO `raw/1`. ONE atomic commit for `/media/:id`.
  </action>
  <verify>
    <automated>cd examples/adoption_demo && MIX_ENV=test mix compile 2>&1 | grep -iv "Mox\|test/support" | grep -i "error" || true ; mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<.ck_page" examples/adoption_demo/lib/adoption_demo_web/live/media_live.ex` ≥ 1.
    - The `<dl>` <dd> ids survive: grep finds `media-id`, `media-state`, `media-delivery-url` (each as both `id=` and `data-testid=`); `grep -c "<dl" media_live.ex` ≥ 1 and `grep -c "ck_detail" media_live.ex` = 0 (NOT converted).
    - `media-variants` survives and `grep -c 'id={"variant-#{variant.name}"}' media_live.ex` = 1 (variant <ul>/<li> kept).
    - `media-alex-profile-link` survives with its link text.
    - daisyUI retired: `grep -c 'text-2xl\|list-disc\|underline\|break-all' media_live.ex` = 0; `grep -c "raw(" ...` = 0.
  </acceptance_criteria>
  <done>/media/:id renders through ck_page/1; the <dl> is restyled in place with media-id/media-state/media-delivery-url intact (NOT ck_detail), the variant <ul>/<li> ids survive (NOT ck_table), and the alex-profile link keeps its testid + text; no daisyUI in body; no raw/1; one atomic commit.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add /posts + /media polish cases and ExUnit frozen-contract tests; full-set sweep</name>
  <read_first>
    - examples/adoption_demo/e2e/cohort-pages.spec.js (shared `assertCohortPagePolish` helper; the 5 route cases added by P2–P4 — this completes all 7)
    - examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs (shared helpers; the 5 per-page tests from P2–P4)
    - examples/adoption_demo/e2e/rendering.spec.js (navigates /dashboard → /posts/:id and /media/:id via post-link/asset links)
    - .planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-RESEARCH.md (§Validation Architecture: /posts + /media selector lists)
  </read_first>
  <action>
    PART A — in `e2e/cohort-pages.spec.js`, add a `test` for `/posts/:id` (navigate via a `post-link-#{id}` from /dashboard, mirroring rendering.spec.js; `surface: "post-cohort"`) and a `test` for `/media/:id` (navigate via an asset link from /dashboard, or a known seeded asset id; `surface: "media-cohort"`). Reuse the shared P1 helper. After this, all 7 routes (dashboard, ops, account, member, lesson, post, media) have a warn-mode polish case in this spec.
    PART B — in `cohort_migration_contract_test.exs`, add a `/posts/:id` test asserting `assert_frozen_contract(html, ~w(post-title post-image-section post-picture-tag post-no-image))` (post-picture-tag vs post-no-image per the seeded post's attachment branch — document the seed choice) + `data-ck-root`, then `assert_daisyui_retired(html)`. Add a `/media/:id` test asserting `assert_frozen_contract(html, ~w(media-id media-state media-delivery-url media-variants media-alex-profile-link))` + at least one `variant-` id + `data-ck-root`, then `assert_daisyui_retired(html)`. Run the full ExUnit module (`mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs`) — all 7 per-page contract tests must pass. Run `npx playwright test e2e/rendering.spec.js` plus the full polish sweep `npx playwright test e2e/cohort-pages.spec.js` (CI-delegate if the local lane is saturated — record in SUMMARY). Confirm `node brandbook/src/cohort-contrast.mjs` still exits 0 (no token drift introduced by any page — it should not be, no CSS changed since P1).
  </action>
  <verify>
    <automated>cd examples/adoption_demo && node --check e2e/cohort-pages.spec.js && mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs ; cd /Users/jon/projects/rindle && node brandbook/src/cohort-contrast.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'post-cohort\|media-cohort' examples/adoption_demo/e2e/cohort-pages.spec.js` ≥ 2; `node --check` passes; the spec now contains all 7 route surfaces (`grep -c 'cohort"' ...` or count of surface labels ≥ 7).
    - `grep -c 'media-delivery-url' examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs` ≥ 1 and `grep -c 'post-title' ...` ≥ 1.
    - `mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs` passes — all 7 per-page frozen-contract + daisyUI-retirement tests green.
    - `node brandbook/src/cohort-contrast.mjs` exits 0 (no token drift).
    - admin-polish.js unchanged: `git -C /Users/jon/projects/rindle status --porcelain examples/adoption_demo/e2e/support/admin-polish.js` empty.
    - rendering.spec.js + cohort-pages.spec.js pass locally OR recorded CI-delegated in SUMMARY.
  </acceptance_criteria>
  <done>The /posts + /media polish cases and ExUnit frozen-contract tests are added and green; all 7 routes now have a warn-mode polish case + a frozen-contract/daisyUI-retirement test; cohort-contrast.mjs green; behavior + full polish sweep pass (or CI-delegated); admin-polish.js untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeded post/asset/variant data → markup | post body, asset id/state/delivery-url, variant names interpolated into HEEx; auto-escaped. Class-only swap. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-05-01 | Tampering | Converting the media `<dl>` to ck_detail/1 silently drops media-id/media-state/media-delivery-url | mitigate | Restyle the `<dl>` in place (RESEARCH /media class map); ExUnit grep asserts all three `<dd>` ids/testids survive + `ck_detail` count is 0; rendering.spec.js runtime backstop |
| T-99-05-02 | Tampering | Converting the variant `<ul>/<li>` to ck_table drops `variant-#{name}` ids | mitigate | Keep the `<ul>/<li>` (Pitfall 2); ExUnit grep asserts the variant id interpolation survives |
| T-99-05-03 | Information Disclosure / XSS | Introducing `raw/1` while restyling the post body / dl values | mitigate | grep asserts no `raw/1`; HEEx auto-escape preserved |
| T-99-05-SC | Tampering | npm/hex package installs | accept | N/A — zero new packages |
</threat_model>

<verification>
- ExUnit /posts + /media frozen-contract + daisyUI-retirement tests green; FULL module (all 7 routes) green.
- Media `<dl>` restyled in place (media-id/media-state/media-delivery-url intact, NOT ck_detail); variant `<ul>/<li>` preserved (NOT ck_table); alex-profile link testid + text intact; post image/body contracts intact.
- rendering.spec.js + the full cohort-pages.spec.js polish sweep green or CI-delegated.
- cohort-contrast.mjs green (no token drift); no daisyUI in either body; no `raw/1`; admin-polish.js + cohort.css unchanged.
</verification>

<success_criteria>
- /posts/:id and /media/:id migrated onto ck_page/1, class-by-class, with [data-ck-root] shells; the media `<dl>` restyled in place (all three `<dd>` ids/testids intact), variant list preserved, alex link preserved; post contracts preserved.
- COHORT-04 fully closed (all 5 member/lesson/post/media/account pages migrated).
- daisyUI retired from both bodies; no raw/1.
- Two polish cases (warn mode) + two ExUnit tests added; all 7 routes covered; full ExUnit module + cohort-contrast.mjs green; behavior + full polish sweep green or CI-delegated.
- ONE atomic commit per page (two commits total).
</success_criteria>

<output>
Create `.planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-05-SUMMARY.md` when done.
</output>

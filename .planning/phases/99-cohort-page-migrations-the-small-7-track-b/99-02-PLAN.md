---
phase: 99-cohort-page-migrations-the-small-7-track-b
plan: 02
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - examples/adoption_demo/lib/adoption_demo_web/live/dashboard_live.ex
  - examples/adoption_demo/e2e/cohort-pages.spec.js
  - examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs
autonomous: true
requirements: [COHORT-01]
tags: [cohort, migration, dashboard, frozen-contract, member-row, class-by-class]

must_haves:
  truths:
    - "/dashboard renders through ck_page/1 (a [data-ck-root] .ck shell) with theme defaulted to \"light\" from mount; the daisyUI <main> wrapper in Layouts.app is left intact (Phase 101 owns its removal)."
    - "The dashboard title carries data-testid=\"cohort-dashboard-title\" verbatim; the four section ids/testids (demo-members, demo-courses, demo-posts, demo-assets) survive byte-for-byte."
    - "LOAD-BEARING: every member <li> keeps id=\"member-#{member.id}\" AND data-testid=\"member-row-#{member.email}\" — support/cohort.js's memberId/memberRow read these and 5+ upload specs navigate via them (RESEARCH Runtime State Inventory)."
    - "Every nested link testid survives: member-avatar-link, member-no-avatar, member-upload-link, member-delete-link, lesson-link-#{id}, post-link-#{id}, nav-upload, nav-ops; link accessible names (text) are unchanged so rendering.spec.js getByRole(\"link\",{name}) stays green (A3)."
    - "Migration is class-by-class: the <ul>/<li> member/course/post/asset list STRUCTURE is preserved (restyled with .ck-scoped classes inside .ck-section), NOT converted to ck_table (Pitfall 2)."
  prohibitions:
    - "No member/course/post/asset <li> is restructured out of existence; no id/data-testid/phx-* dropped or renamed (frozen contract)."
    - "ck_button/1 (link-only) is NOT used to replace any phx-click button (there are none on /dashboard); the nav links may use ck_button or a .ck link but must keep their data-testid + text."
    - "No daisyUI/Tailwind utility class remains in the /dashboard body (btn, text-2xl, text-lg, list-disc, opacity-80, space-y-, flex gap-) — EXCEPT the shared Layouts.app <main>, which is Phase 101 (Pitfall 6)."
    - "No raw/1 introduced; no new param reflection."
  artifacts:
    - path: "examples/adoption_demo/lib/adoption_demo_web/live/dashboard_live.ex"
      provides: "/dashboard migrated onto ck_page/1 + .ck-* primitives, frozen contract preserved"
      contains: "<.ck_page"
  key_links:
    - from: "examples/adoption_demo/lib/adoption_demo_web/live/dashboard_live.ex"
      to: "examples/adoption_demo/lib/adoption_demo_web/components/cohort_components.ex ck_page/1"
      via: "render/1 wraps the page body in <.ck_page title=... theme={@theme}> with the four sections as .ck-section blocks inside the :inner_block"
      pattern: "<.ck_page"
    - from: "examples/adoption_demo/lib/adoption_demo_web/live/dashboard_live.ex member <li>"
      to: "examples/adoption_demo/e2e/support/cohort.js memberId/memberRow"
      via: "each member <li> keeps id=member-#{id} + data-testid=member-row-#{email}; support/cohort.js reads them; 5+ upload specs depend on this"
      pattern: "member-row-"
---

<objective>
Migrate `/dashboard` (`dashboard_live.ex`) onto the `ck_page/1` scaffold class-by-class (COHORT-01), preserving every `id`/`data-testid` frozen contract — most critically the LOAD-BEARING member-row contract (`id="member-#{id}"` + `data-testid="member-row-#{email}"`) that `support/cohort.js` and 5+ upload specs read. Add the `/dashboard` polish case to the shared `cohort-pages.spec.js` and the `/dashboard` frozen-contract + daisyUI-retirement test to the ExUnit contract module. ONE atomic commit (mirrors Phase 98 D-98-02 atomic-per-surface).

Purpose: `/dashboard` is the demo's front door and the navigation hub the upload behavior specs route through. Its member `<ul>/<li>` list MUST stay structurally intact (restyle classes only, NOT a `ck_table` conversion) or `memberId`/`memberRow` break and a cascade of upload specs fail (RESEARCH Pitfall 2 + Runtime State Inventory). Implements LOCKED DECISION 1 (compose `ck_page/1`) + the class-by-class HARD CONSTRAINT.

Output: `/dashboard` renders a `[data-ck-root]` `.ck` shell via `ck_page/1`, every frozen selector survives, no daisyUI utility remains in the body, the dashboard polish case (warn mode) and ExUnit contract test are green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-RESEARCH.md
@.planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan MODIFIES `dashboard_live.ex` render/1 (class-by-class swap onto `ck_page/1`); it creates NO new module-level symbols. It ADDS one `test(...)` polish case to `e2e/cohort-pages.spec.js` (the `/dashboard` route, using the shared `assertCohortPagePolish` helper from P1) and one per-page contract `test` to `cohort_migration_contract_test.exs` (the `/dashboard` frozen-selector list + daisyUI-retirement assertion via the shared P1 helpers). No CSS edits (all CSS is P1; `.ck-output` is not used on /dashboard).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate /dashboard render/1 onto ck_page/1 class-by-class, preserving every frozen selector</name>
  <read_first>
    - examples/adoption_demo/lib/adoption_demo_web/live/dashboard_live.ex (READ FULLY — the render/1 markup: title L22 `data-testid="cohort-dashboard-title"`, lede L23-27 with `<code>Rindle.Storage.S3</code>`, member section L29-57 with the LOAD-BEARING `<li id={"member-#{member.id}"} data-testid={"member-row-#{member.email}"}>` + nested `member-avatar-link`/`member-no-avatar`/`member-upload-link`/`member-delete-link`, courses L59-73 with `lesson-link-#{id}`, posts L75-85 with `post-link-#{id}`, assets L87-95, nav L97-100 with `nav-upload`/`nav-ops`)
    - examples/adoption_demo/lib/adoption_demo_web/components/cohort_components.ex (ck_page/1 from P1 — its `:title`/`:eyebrow`/`:lede`/`:theme` attrs + `:inner_block`; the `.ck-section` / `.ck-section__title` grammar via `<section class="ck-section">`; `ck_button/1` for the nav links only if the source was an `<a>`/`<.link>`)
    - examples/adoption_demo/priv/static/assets/cohort.css L284-318 (.ck-hero*), L368-386 (.ck-section*) — the target classes for title/lede/sections
    - examples/adoption_demo/e2e/support/cohort.js (memberId reads `id="member-#{id}"`; memberRow reads `data-testid="member-row-#{email}"` — DO NOT break these)
    - .planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-RESEARCH.md (/dashboard per-page class map; Pitfall 2 element-by-element; Pitfall 6 daisyUI retirement; A3 link accessible names)
  </read_first>
  <action>
    In `dashboard_live.ex` render/1: keep the `<Layouts.app flash={@flash} page_title={@page_title} nav={:app}>` wrapper (page chrome stays — RESEARCH §Responsibility Map). Add `theme: "light"` to the `mount/3` assigns (server state, D-96-07) and wrap the page body in `<.ck_page title="Cohort" theme={@theme}>` — move the title's `data-testid="cohort-dashboard-title"` ONTO the `ck_page` `:title` mechanism OR keep an explicit `<h1 ... data-testid="cohort-dashboard-title">` inside the body if `ck_page`'s `:title` cannot carry the testid; the testid MUST survive verbatim (if `ck_page` renders the `<h1 class="ck-hero__title">` and cannot pass a testid through `:title`, render the title heading explicitly inside `:inner_block` instead so `data-testid="cohort-dashboard-title"` is preserved — DECIDE based on the P1 `ck_page` API and document the choice). Put the lede (`Course-and-community SaaS demo… <code>Rindle.Storage.S3</code>`) as `.ck-hero__lede` (via `ck_page` `:lede` or inline `.ck`-scoped text) — keep the `<code>` element (it inherits `--ck-code-*` via the `.ck` cascade). For each of the four sections: keep `<section id="demo-members" data-testid="demo-members">` (and courses/posts/assets) BYTE-FOR-BYTE; replace the daisyUI heading `class="text-lg font-semibold mt-6"` with `.ck-section__title` (wrap in `.ck-section` head if needed) — keep the heading text. For the member list: KEEP the `<ul><li :for={member <- @members} id={"member-#{member.id}"} data-testid={"member-row-#{member.email}"}>` STRUCTURE (Pitfall 2 — do NOT convert to ck_table); replace `class="list-disc pl-5 space-y-2"` with a `.ck`-scoped list class (a plain class swap; the `.ck` cascade styles it — if no list class exists keep the `<ul>` unclassed so it inherits `.ck` defaults, do NOT add a new CSS rule, that is P1's domain). Preserve EVERY nested link's `data-testid` (`member-avatar-link`, `member-no-avatar`, `member-upload-link`, `member-delete-link`) and link TEXT verbatim (A3 — rendering.spec.js uses getByRole link names). Same for courses (`lesson-link-#{id}` + text), posts (`post-link-#{id}` + text), assets. For the bottom `<nav class="flex gap-4 mt-8 text-sm">`: restyle to a `.ck`-scoped nav row; the `nav-upload`/`nav-ops` links may become `<.ck_button variant="quiet">` OR keep as `.ck` links — either way KEEP `data-testid="nav-upload"`/`"nav-ops"` and the link text. Remove ALL daisyUI/Tailwind utility classes from the body (`text-2xl`, `font-semibold`, `text-sm`, `opacity-80`, `text-lg`, `mt-6`, `list-disc`, `pl-5`, `space-y-2`, `flex gap-4`, `mt-8`, `underline`). Introduce NO `raw/1`. Make NO change to `mount` data loading beyond adding `theme: "light"`. Commit as ONE atomic commit for `/dashboard`.
  </action>
  <verify>
    <automated>cd examples/adoption_demo && MIX_ENV=test mix compile 2>&1 | grep -iv "Mox\|test/support" | grep -i "error" || true ; mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<.ck_page" examples/adoption_demo/lib/adoption_demo_web/live/dashboard_live.ex` ≥ 1.
    - `grep -c 'data-testid="cohort-dashboard-title"' examples/adoption_demo/lib/adoption_demo_web/live/dashboard_live.ex` = 1.
    - `grep -c 'member-row-' examples/adoption_demo/lib/adoption_demo_web/live/dashboard_live.ex` ≥ 1 AND `grep -c 'id={"member-#{member.id}"}' ...` = 1 (LOAD-BEARING member contract intact).
    - All four section ids survive: `grep -c 'data-testid="demo-members"\|data-testid="demo-courses"\|data-testid="demo-posts"\|data-testid="demo-assets"' ...` = 4.
    - Nested testids survive: `grep` finds `member-avatar-link`, `member-no-avatar`, `member-upload-link`, `member-delete-link`, `nav-upload`, `nav-ops`, `lesson-link-`, `post-link-`.
    - daisyUI retired from body: `grep -c 'text-2xl\|list-disc\|opacity-80\|class="btn"' examples/adoption_demo/lib/adoption_demo_web/live/dashboard_live.ex` = 0.
    - `grep -c "raw(" examples/adoption_demo/lib/adoption_demo_web/live/dashboard_live.ex` = 0.
    - `mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs` passes (after Task 2 adds the /dashboard test).
  </acceptance_criteria>
  <done>/dashboard renders through ck_page/1 with a [data-ck-root] shell; the title testid, four section ids, the LOAD-BEARING member-row id/testid, every nested link testid + text, and nav testids all survive; no daisyUI in the body; no raw/1; one atomic commit.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the /dashboard polish case to cohort-pages.spec.js and the /dashboard frozen-contract test to the ExUnit module</name>
  <read_first>
    - examples/adoption_demo/e2e/cohort-pages.spec.js (the shared `assertCohortPagePolish` helper + `interactiveSelectors` from P1 — call it for `/dashboard`)
    - examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs (the shared `assert_frozen_contract`/`assert_daisyui_retired` helpers from P1 — add the /dashboard per-page test)
    - examples/adoption_demo/e2e/rendering.spec.js + replace-detach.spec.js (the EXISTING behavior specs that must stay green — confirm they key on testid/id/role, not class)
    - .planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-RESEARCH.md (§Validation Architecture: the per-page selector list to assert; Pitfall 5 root-visibility guard)
  </read_first>
  <action>
    PART A — in `e2e/cohort-pages.spec.js`, add ONE `test("/dashboard renders on the Cohort DS (polish, warn mode)", ...)` that calls the shared `assertCohortPagePolish(page, { route: "/dashboard", surface: "dashboard-cohort" })` (which already goto's, waits for the live socket, asserts `[data-ck-root]` visible FIRST per Pitfall 5, runs `assertAdminPolish` over the root in warn mode). Do NOT duplicate the harness logic — reuse the P1 helper.
    PART B — in `cohort_migration_contract_test.exs`, add ONE `test` for `/dashboard` that renders the LiveView (via the shared P1 render helper, supplying any seeded conn the sibling tests use) and calls `assert_frozen_contract(html, [...])` with the FULL /dashboard frozen selector list: `cohort-dashboard-title`, `demo-members`, `demo-courses`, `demo-posts`, `demo-assets`, `member-avatar-link`, `member-no-avatar`, `member-upload-link`, `member-delete-link`, `nav-upload`, `nav-ops`, plus a representative seeded `member-row-#{email}` / `id="member-#{id}"` / `lesson-link-#{id}` / `post-link-#{id}` (use the seed's known ids/emails — read the seed/fixture the sibling tests rely on; if exact seeded ids are not statically known, assert the `member-row-` / `member-` / `lesson-link-` / `post-link-` PREFIX appears) — and assert `data-ck-root` is present. Then call `assert_daisyui_retired(html)` for the /dashboard body (no `text-2xl`/`list-disc`/`opacity-80`/`class="btn"` in the page body — the helper already scopes out the shared Layouts.app chrome). Keep the retired-class literals only inside the helper's match list (P1), not echoed as test prose.
    Run the /dashboard behavior specs to confirm the frozen contract truly holds at runtime: `npx playwright test e2e/rendering.spec.js e2e/replace-detach.spec.js` (these navigate via the dashboard member rows). [If the local Playwright server/seed is saturated, this lane is CI-delegated per the Phase-98 precedent — record that in the SUMMARY; the ExUnit grep is the static backstop.]
  </action>
  <verify>
    <automated>cd examples/adoption_demo && node --check e2e/cohort-pages.spec.js && mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '/dashboard' examples/adoption_demo/e2e/cohort-pages.spec.js` ≥ 1 and a `test(` referencing the dashboard surface exists; `node --check` passes.
    - `grep -c 'cohort-dashboard-title' examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs` ≥ 1 (the /dashboard contract test asserts the title testid) and `grep -c 'member-row-\|member-' ...` ≥ 1 (member-row contract asserted).
    - `mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs` passes (the /dashboard frozen-contract + daisyUI-retirement test green).
    - admin-polish.js still unchanged: `git -C /Users/jon/projects/rindle status --porcelain examples/adoption_demo/e2e/support/admin-polish.js` empty.
    - The /dashboard behavior specs (rendering.spec.js, replace-detach.spec.js) pass locally OR are recorded as CI-delegated in the SUMMARY (Phase-98 precedent).
  </acceptance_criteria>
  <done>The /dashboard polish case (shared warn-mode helper, root-visibility-guarded) and the /dashboard ExUnit frozen-contract + daisyUI-retirement test are added and green; the dashboard behavior specs pass (or are CI-delegated); admin-polish.js untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeded member/asset data → dashboard markup | Member names/emails/asset ids interpolated into the member/course/post/asset lists; HEEx auto-escapes. Class-only swap changes presentation, not data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-02-01 | Tampering | A restructure dropping `id="member-#{id}"`/`member-row-#{email}` silently breaks upload-spec navigation | mitigate | ExUnit frozen-contract grep asserts the member-row id/testid survive; rendering.spec.js/replace-detach.spec.js behavior regression is the runtime backstop (RESEARCH Runtime State Inventory) |
| T-99-02-02 | Information Disclosure / XSS | Introducing `raw/1` while restyling member/post bodies | mitigate | grep asserts no `raw/1` added; HEEx auto-escape preserved (RESEARCH Security Domain) |
| T-99-02-SC | Tampering | npm/hex package installs | accept | N/A — zero new packages (composition + class swap only) |
</threat_model>

<verification>
- ExUnit `/dashboard` frozen-contract + daisyUI-retirement test green (title testid, four section ids, member-row contract, nested link testids).
- `/dashboard` behavior specs (rendering, replace-detach) green or CI-delegated.
- `node --check e2e/cohort-pages.spec.js` valid; `/dashboard` polish case present (warn mode).
- No daisyUI utility in the /dashboard body; no `raw/1`; member-row LOAD-BEARING contract intact.
- admin-polish.js and cohort.css unchanged (all CSS is P1).
</verification>

<success_criteria>
- `/dashboard` migrated onto ck_page/1, class-by-class, with a [data-ck-root] shell; every frozen id/data-testid (esp. the LOAD-BEARING member-row contract) and link text preserved.
- daisyUI retired from the /dashboard body; no raw/1.
- /dashboard polish case (warn mode) + ExUnit frozen-contract/daisyUI-retirement test added and green; behavior specs green or CI-delegated.
- ONE atomic commit for /dashboard.
</success_criteria>

<output>
Create `.planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-02-SUMMARY.md` when done.
</output>

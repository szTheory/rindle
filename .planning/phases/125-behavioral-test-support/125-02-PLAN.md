---
phase: 125-behavioral-test-support
plan: "02"
type: execute
wave: 2
depends_on: ["125-01"]
files_modified:
  - test/install_smoke/support/generated_app_helper.ex
  - test/install_smoke/support/generated_app/contracts.ex
  - test/install_smoke/generated_app_smoke_test.exs
autonomous: true
requirements: [TEST-01, TEST-02, SAFE-01]
must_haves:
  truths:
    - "GeneratedAppHelper remains the unchanged test-facing facade for every contract/report/scenario entry point."
    - "Upgrade catalog and report tests observe explicit contract data and mutation outcomes, never internal helper text."
    - "Pure generated-app contracts have one hidden, discoverable owner with identical maps, keys, predicates, and reject semantics."
  artifacts:
    - path: test/install_smoke/support/generated_app/contracts.ex
      provides: "pure generated-app report/catalog contracts"
    - path: test/install_smoke/support/generated_app_helper.ex
      provides: "stable facade delegating contract calls"
    - path: test/install_smoke/generated_app_smoke_test.exs
      provides: "behavioral report and catalog mutation proof"
  key_links:
    - from: test/install_smoke/support/generated_app_helper.ex
      to: test/install_smoke/support/generated_app/contracts.ex
      via: "unchanged facade function delegation"
      pattern: "GeneratedApp.Contracts"
---

<objective>
Extract the pure generated-app contract seam and replace the first implementation-text snapshots with objective report behavior.

Purpose: Establish the stable-facade extraction pattern that every later support slice follows.
Output: Hidden `GeneratedApp.Contracts`, unchanged facade entry points, and report-backed contract tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/125-behavioral-test-support/125-RESEARCH.md
@.planning/phases/125-behavioral-test-support/125-PATTERNS.md
@test/install_smoke/support/generated_app_helper.ex
@test/install_smoke/generated_app_smoke_test.exs
@.planning/phases/125-behavioral-test-support/125-01-SUMMARY.md

<interfaces>
`Rindle.InstallSmoke.GeneratedAppHelper` retains `default_install_contract/0`, `persistence_lifecycle_report_keys/0`, `public_compatibility_contract/0`, `isolation_upgrade_contract/0`, `legacy_upgrade_contract/0`, `isolation_upgrade_catalog_preserved?/1`, `profile_enabled?/1`, and `phase_120_scenario_enabled?/2`. New support modules are test-only, `@moduledoc false`, with callable seams `@doc false`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Replace upgrade helper snapshots with explicit report and predicate behavior</name>
  <files>test/install_smoke/generated_app_smoke_test.exs</files>
  <behavior>
    - Valid isolation reports accept; missing markers, changed foreign keys/indexes, changed Oban snapshots, and incomplete relation facts reject.
    - Required report keys explicitly include before/after Oban snapshots, schema relation maps, doctor readiness, and persistence lifecycle.
    - No test reads internal generated-app support source to assert SQL aliases, local expressions, or implementation wording.
  </behavior>
  <action>Rewrite only the five self-reading tests in the Phase 120 fast-contract group that concern report/catalog data. Drive `isolation_upgrade_catalog_preserved?/1` with valid and mutated reports, assert the facade's explicit contract/key data, and retain the real generated isolation-upgrade test as the authority for before/after Oban equality and doctor success. Remove assertions about SQL aliases and OID parameter-binding spelling because the catalog outcome already covers them. Do not weaken existing mutation cases or read the new collaborator's source.</action>
  <verify><automated>MIX_ENV=test mix test test/install_smoke/generated_app_smoke_test.exs --only phase_120_upgrade_contract --seed 0</automated></verify>
  <done>Catalog/report contract failures are expressed as input/output behavior and the corresponding helper-source reads are gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract pure contracts behind the GeneratedAppHelper facade</name>
  <reversibility rating="reversible">The extraction is test-private and callers retain the exact facade symbols and return terms.</reversibility>
  <files>test/install_smoke/support/generated_app_helper.ex, test/install_smoke/support/generated_app/contracts.ex, test/install_smoke/generated_app_smoke_test.exs</files>
  <behavior>
    - Every contract map, required key list, scenario/profile predicate, atom/string JSON shape, and rejection case is byte/term equivalent through the facade.
    - Loading `generated_app_helper.ex` alone requires Contracts before facade compilation.
    - The facade remains the only module aliased by current consumers.
  </behavior>
  <action>Create `Rindle.InstallSmoke.GeneratedApp.Contracts` and move the cohesive pure block currently at the front of the helper, including private catalog validators. Require it before the facade module compiles and delegate every existing public test-facing function with unchanged defaults/guards/terms. Keep orchestration/proof/cleanup in the facade. Add an explicit facade-parity table test over public contract calls and representative valid/invalid inputs; do not rename callers or expose the collaborator as library API.</action>
  <verify><automated>MIX_ENV=test mix test test/install_smoke/generated_app_smoke_test.exs --seed 0 &amp;&amp; bash scripts/maintainer/refactor_contract.sh</automated></verify>
  <done>The first production-quality extraction is complete: one pure owner, one stable facade, identical objective behavior, and no contract-text self-inspection.</done>
</task>

</tasks>

<threat_model asvs_level="L1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Facade → hidden contract owner | Existing consumers depend on exact maps, keys, and predicate semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-125-04 | Tampering | generated report/catalog contracts | high | mitigate | Facade parity plus full mutation matrix and real isolation-upgrade proof preserve exact terms and rejection behavior. |
| T-125-SC | Tampering | package supply chain | low | accept | No dependency install or runtime package change occurs. |
</threat_model>

<verification>
Run the full generated-app test file and SAFE-01; verify no `File.read!` of generated-app support remains for report/catalog assertions.
</verification>

<success_criteria>
- TEST-01 has a proven stable-facade tracer.
- TEST-02 replaces report/catalog implementation strings with outcome contracts.
- SAFE-01 remains green with no public/product surface edit.
</success_criteria>

<output>
Create `.planning/phases/125-behavioral-test-support/125-02-SUMMARY.md` when done.
</output>

---
phase: 125-behavioral-test-support
plan: "04"
type: execute
wave: 4
depends_on: ["125-03"]
files_modified:
  - test/install_smoke/support/generated_app_helper.ex
  - test/install_smoke/support/generated_app/patcher.ex
  - test/install_smoke/generated_app_smoke_test.exs
autonomous: true
requirements: [TEST-01, TEST-02, SAFE-01]
must_haves:
  truths:
    - "Generated Phoenix config, router, application children, profile module, fixtures, and test-helper patches remain exact for image/video/tus/mux/gcs modes."
    - "Patcher receives explicit root/app/profile/source facts and does not rerun package, command, migration, or report logic."
    - "Generated output/compiled behavior, not patcher source text, proves the extraction."
  artifacts:
    - path: test/install_smoke/support/generated_app/patcher.ex
      provides: "profile-aware generated Phoenix source/config patching"
  key_links:
    - from: test/install_smoke/support/generated_app_helper.ex
      to: test/install_smoke/support/generated_app/patcher.ex
      via: "explicit resolved workspace/app/profile inputs"
      pattern: "GeneratedApp.Patcher"
---

<objective>
Extract generated Phoenix application patching behind the stable facade without changing any generated consumer behavior.

Purpose: Give config/router/application/profile mutation one discoverable owner before migration and smoke-source extraction.
Output: Hidden Patcher, unchanged generated-app reports, and behavior/compiled-output proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/125-behavioral-test-support/125-RESEARCH.md
@.planning/phases/125-behavioral-test-support/125-PATTERNS.md
@test/install_smoke/support/generated_app_helper.ex
@test/install_smoke/generated_app_smoke_test.exs
@.planning/phases/125-behavioral-test-support/125-03-SUMMARY.md

<interfaces>
`GeneratedApp.Patcher.patch!/1` (or an equivalently explicit small call set) receives resolved workspace root, app name/module, package/network dependency input, profile mode, and compile prefix. It owns only generated Phoenix `mix.exs`, test/runtime config, router, LiveView, application children, profile, and staged profile fixtures.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Lock one real generated Phoenix patch path at the compiled boundary</name>
  <files>test/install_smoke/generated_app_smoke_test.exs</files>
  <behavior>
    - The image tracer generated app compiles, boots, selects the same profile, and returns the same install/provenance report fields.
    - Tus/mux/gcs profile gates and generated application children remain covered by their existing tagged generated-app cases.
    - No assertion names patcher-local functions, variables, or source fragments.
  </behavior>
  <action>Strengthen only any missing report/compiled-app assertions needed to make the image generated-app path a complete facade→Workspace→Patcher→compile/boot tracer. Reuse existing tagged profile cases for tus/mux/gcs rather than snapshotting patch text. Assert generated results and compiled exports/config facts already exposed by the temporary app; do not add a new source-string contract or change profile tags.</action>
  <verify><automated>MIX_ENV=test mix test test/install_smoke/generated_app_smoke_test.exs --seed 0</automated></verify>
  <done>The first patched generated consumer is independently observable through compile/boot/report outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract profile-aware Phoenix patching into Patcher</name>
  <reversibility rating="reversible">Private test-support mechanics move behind the existing facade and generated output remains the rollback oracle.</reversibility>
  <files>test/install_smoke/support/generated_app_helper.ex, test/install_smoke/support/generated_app/patcher.ex, test/install_smoke/generated_app_smoke_test.exs</files>
  <behavior>
    - Exact generated dependency/config/router/application/profile/fixture contents produce unchanged compile, boot, and profile reports.
    - Profile gates remain exhaustive for image, video, tus, mux, and gcs.
    - Patcher performs no command execution, migration writing, smoke-test generation, or report normalization.
  </behavior>
  <action>Move the cohesive current patch block (generated `mix.exs`, test/runtime config, router, tus LiveView, Mux fixtures/config, application children, and profile modules) into hidden `Rindle.InstallSmoke.GeneratedApp.Patcher`. Pass every resolved input explicitly from the facade; do not reread package/profile environment or command state in Patcher. Preserve exact generated strings and branch order as behavior-bearing outputs, including optional dependency/profile gates. Remove the moved private functions from the facade and keep every facade proof entry point unchanged.</action>
  <verify><automated>MIX_ENV=test mix test test/install_smoke/generated_app_smoke_test.exs --seed 0 &amp;&amp; bash scripts/maintainer/refactor_contract.sh</automated></verify>
  <done>Generated Phoenix patching has one effect-specific owner, all profile outputs compile through the existing suite, and the facade remains stable.</done>
</task>

</tasks>

<threat_model asvs_level="L1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Resolved profile/package facts → generated Phoenix source | A test-support refactor can silently alter the consumer application being proven. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-125-04 | Tampering | generated config/router/application/profile | high | mitigate | Real generated compile/boot/profile reports and existing tagged cases validate exact outputs through the stable facade. |
| T-125-SC | Tampering | dependency surface | low | accept | No repository dependency changes; generated dependency text is moved unchanged and exercised by the consumer. |
</threat_model>

<verification>
Run the generated-app suite and SAFE-01; compare facade exports/report keys to the Plan 02 contract table.
</verification>

<success_criteria>
- Patcher has one discoverable responsibility and explicit inputs.
- Generated profile behavior is unchanged and objectively proven.
- No product/API/schema/telemetry/error/release surface changes.
</success_criteria>

<output>
Create `.planning/phases/125-behavioral-test-support/125-04-SUMMARY.md` when done.
</output>

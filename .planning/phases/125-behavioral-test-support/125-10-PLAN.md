---
phase: 125-behavioral-test-support
plan: "10"
type: execute
wave: 10
depends_on: ["125-09"]
files_modified:
  - .planning/phases/125-behavioral-test-support/125-ASYNC-ISOLATION-EVIDENCE.md
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, SAFE-01]
must_haves:
  truths:
    - "The final implementation head has a fail-fast report for 25 distinct deterministic seeds, each started as one fresh shipped single-run coverage process."
    - "The exact same final head passes or records the supported Elixir 1.17 / OTP 27 required CI Summary outcome."
    - "Issue #42 closes only when the causal proof, all 25 local runs, and exact-head supported authority are green; every failure is sanitized, posted, narrowed, and left open."
    - "Public API/schema/migration/telemetry/errors/Admin/dependencies/release topology remain unchanged and packed-adopter/docs Proof authority is green."
  artifacts:
    - path: .planning/phases/125-behavioral-test-support/125-ASYNC-ISOLATION-EVIDENCE.md
      provides: "fixed-head local matrix and close-or-narrow evidence receipt"
  key_links:
    - from: .planning/phases/125-behavioral-test-support/125-ASYNC-ISOLATION-EVIDENCE.md
      to: GitHub issue 42
      via: "sanitized evidence summary and exact CI run/head link"
      pattern: "head_sha|planned_runs|completed_runs|status"
---

<objective>
Run the complete finite evidence protocol, prove the final head on supported CI, and disposition issue #42 exactly as authorized.

Purpose: Convert the refactor and historical isolation concern into auditable pass-or-narrow evidence without retry masking or release/CI drift.
Output: Tracked evidence receipt, exact-head PR CI, and a sanitized issue #42 close or narrowing comment.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RUNNING.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/125-behavioral-test-support/125-VALIDATION.md
@.planning/phases/125-behavioral-test-support/125-RESEARCH.md
@.planning/phases/125-behavioral-test-support/125-PATTERNS.md
@scripts/maintainer/async_isolation_evidence.sh
@scripts/maintainer/refactor_contract.sh
@.github/workflows/ci.yml
@.planning/phases/125-behavioral-test-support/125-09-SUMMARY.md

<interfaces>
Evidence receipt fields are `head_sha`, local toolchain, exact command template, `planned_runs: 25`, ordered seeds, `completed_runs`, per-run status, overall `status: local_pass|local_failure`, and optional first-failure seed/exit/sanitized location. Issue comments include only those fields plus exact PR/CI links. No environment values, raw temp config, database credentials, or full captured output may be posted.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Run the fixed 25-seed matrix once and commit its pass-or-failure receipt</name>
  <precondition>PostgreSQL prerequisites for the shipped coverage command are available.</precondition>
  <files>.planning/phases/125-behavioral-test-support/125-ASYNC-ISOLATION-EVIDENCE.md</files>
  <action>Capture the implementation head before the run and invoke `scripts/maintainer/async_isolation_evidence.sh` exactly once, targeting the tracked evidence file. Do not rerun any failed seed or restart the matrix. If all 25 fresh foreground Mix processes pass, record `local_pass` and 25/25. If any process fails, accept the runner's nonzero status as evidence, verify it stopped at the first failure, record `local_failure`, completed count, seed, exit, and bounded sanitized location, and continue to Task 2 solely to obtain exact-head supported CI and post the narrowing disposition. Commit the receipt with non-release `docs:` intent so the PR head includes the evidence being cited.</action>
  <verify><automated>test -f .planning/phases/125-behavioral-test-support/125-ASYNC-ISOLATION-EVIDENCE.md &amp;&amp; rg -q '^planned_runs: 25$' .planning/phases/125-behavioral-test-support/125-ASYNC-ISOLATION-EVIDENCE.md &amp;&amp; rg -q '^status: (local_pass|local_failure)$' .planning/phases/125-behavioral-test-support/125-ASYNC-ISOLATION-EVIDENCE.md &amp;&amp; MIX_ENV=test mix test test/install_smoke/async_isolation_evidence_runner_test.exs --seed 0</automated></verify>
  <done>Exactly one immutable, fail-fast local matrix attempt is recorded for the implementation head, whether it passed completely or exposed a concrete first failure.</done>
</task>

<task type="auto">
  <name>Task 2: Obtain exact-head supported authority and close or narrow issue #42</name>
  <precondition>The evidence receipt is committed and its `head_sha` matches the implementation commit it evaluated; `gh auth status` succeeds for the repository.</precondition>
  <files>None — push/PR/check/issue operations and read-only final scope verification only</files>
  <action>Run forced warnings-as-errors compile, all generated-app/docs/isolation/async-guard focused suites, packed image install smoke, SAFE-01, `mix ci`, and repository hygiene. Audit the complete phase diff: test-support/docs/runner/RUNNING/one Proof argv/evidence only; forbid `lib`, schemas/migrations, telemetry/error contracts, Admin implementation/docs, dependencies, release workflows/metadata, and any Quality/CI Summary/topology change. Push/update the non-release Phase 125 PR to `main`, capture final HEAD, require PR head equality, and wait for required checks. Record the exact supported Elixir 1.17 / OTP 27 CI Summary result without changing the committed evidence head. Per R-02: only if local status is `local_pass`, completed runs are 25, the 100-iteration proof is green, PR head equals final HEAD, and required CI Summary is SUCCESS, post the sanitized receipt/CI links and close #42. On any local/focused/CI/head failure, post only the minimal sanitized reproducer and observed authority, leave #42 open, and describe the remaining concrete failure; never retry the seed, close on partial evidence, merge, publish, or make a release-triggering commit.</action>
  <verify><automated>set -e; FINAL_HEAD=$(git rev-parse HEAD); test "$FINAL_HEAD" = "$(gh pr view --json headRefOid -q .headRefOid)"; CHECKS_STATUS=0; set +e; gh pr checks --required --json name,state &gt; /tmp/rindle-125-required-checks.json; CHECKS_STATUS=$?; set -e; test -s /tmp/rindle-125-required-checks.json; ISSUE_STATE=$(gh issue view 42 --json state -q .state); LOCAL_STATUS=$(awk '/^status: / {print $2; exit}' .planning/phases/125-behavioral-test-support/125-ASYNC-ISOLATION-EVIDENCE.md); CI_STATE=$(jq -r '.[] | select(.name == "CI Summary") | .state' /tmp/rindle-125-required-checks.json); LAST_COMMENT=$(gh issue view 42 --json comments -q '.comments[-1].body'); printf '%s' "$LAST_COMMENT" | grep -Fq "$FINAL_HEAD"; if test "$LOCAL_STATUS" = local_pass &amp;&amp; test "$CI_STATE" = SUCCESS &amp;&amp; test "$CHECKS_STATUS" -eq 0; then test "$ISSUE_STATE" = CLOSED; else test "$ISSUE_STATE" = OPEN; fi</automated></verify>
  <done>Exact-head supported CI and sanitized issue evidence agree: #42 is closed only on complete green authority, otherwise it remains open with one concrete narrowed failure.</done>
</task>

</tasks>

<threat_model asvs_level="L1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local process output → tracked/public evidence | Failure output may be sensitive or misleading if retried/truncated incorrectly. |
| Local final branch → supported PR CI | Only exact-head supported authority may authorize closure. |
| Evidence/CI → GitHub issue state | External issue mutation must follow the locked all-green condition. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-125-02 | Repudiation | local matrix | high | mitigate | One invocation, fixed seeds, first-failure stop, committed head/count/status receipt, no retry. |
| T-125-03 | Information disclosure | evidence/issue comment | high | mitigate | Allowlisted sanitized fields only; exclude raw environment/config/output. |
| T-125-05 | Tampering | CI/release topology | high | mitigate | Exact diff audit permits one Proof argv only and forbids Quality/summary/release changes. |
| T-125-06 | Spoofing / Repudiation | issue closure authority | high | mitigate | Final local/PR head equality, required CI Summary SUCCESS, 25/25 and causal proof gates precede close. |
| T-125-SC | Tampering | package supply chain | low | accept | No new dependency/package install; existing packed-consumer lane supplies authority. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---:|---|---|
| GOAL | — | Observable contracts with focused ownership and conclusive async-isolation evidence | 01-10 | COVERED | Sequential tracer slices end in exact-head disposition. |
| REQ | TEST-01 | Split 3,955-line generated helper behind retained facade with unchanged packed proof | 02-06, 10 | COVERED | Seven cohesive hidden owners plus packed/exact CI authority. |
| REQ | TEST-02 | Replace only internal self-reading assertions with behavior/metadata/narrow structure | 02-06 | COVERED | Reports, executed process/filesystem behavior, compiled docs/capabilities, real tus outcome. |
| REQ | TEST-03 | Split 1,118-line docs parity by public domain with shared support/equivalent assertions | 07-09, 10 | COVERED | Four domains; separate release/streaming suites retained; Proof explicit. |
| REQ | TEST-04 | Stress single-run coverage and process override; close or narrow #42 | 01, 10 | COVERED | 100 causal iterations, fixed 25 fresh processes, exact-head conditional disposition. |
| REQ | SAFE-01 | Preserve public API/schema/migration/telemetry/error/CI-release invariants | 01-10 | COVERED | Continuous SAFE-01, forbidden-surface audit, exact CI authority. |
| RESEARCH | stable facade | Keep GeneratedAppHelper API/load path and extract cohesive private effect owners | 02-06 | COVERED | Contracts, CommandRunner, Workspace, Patcher, Migrations, SmokeSource, ProfileHelpers. |
| RESEARCH | proof hierarchy | Generated outcome → compiled metadata → narrow structural contract | 02-09 | COVERED | No collaborator/library helper-text snapshots remain. |
| RESEARCH | docs ownership | Install/migration, onboarding/capabilities, operations, product/Admin; release separate | 07-09 | COVERED | Domain suites and narrow shared Support. |
| RESEARCH | R-01 | 25 distinct seeds, fresh exact coverage process, high-iteration causal proof | 01, 10 | COVERED | Fixed tested runner and final one-time execution. |
| RESEARCH | R-02 | Close only after local+causal+exact-head supported green; otherwise narrow/open | 10 | COVERED | Explicit external branch and automated state assertion. |
| RESEARCH | R-03 | No second coverage invocation per CI job and no CI topology expansion | 01, 09-10 | COVERED | Local matrix only; one Proof argv edit; Quality/summary preserved. |
| RESEARCH | security | Sanitize failure evidence and propagate/record first nonzero result | 01, 10 | COVERED | Allowlisted receipt and issue comment. |
| CONTEXT | — | No CONTEXT.md by configured `skip_discuss=true` | — | EXCLUDED | Roadmap requirements and resolved research decisions are the locked context; no D-NN decisions exist. |

Excluded, not gaps: dependency/toolchain upgrades, partitions, second coverage run, CI topology expansion, public API/schema/migration/telemetry/error changes, Admin feature/docs changes, release forcing, historical archives, and deferred product work.
</source_audit>

<verification>
Run the complete local gate once, exact diff/forbidden-surface audits, then require final local HEAD = PR head and supported required CI Summary. Verify issue state follows the receipt/CI branch exactly.
</verification>

<success_criteria>
- TEST-01 through TEST-04 and inherited SAFE-01 have complete objective evidence.
- Every source-audit row is covered with no deferred or adjacent scope implemented.
- Issue #42 is either closed on complete green authority or open with a sanitized concrete failure.
- No merge, publish, dependency, public contract, Admin, or release-topology change occurs.
</success_criteria>

<output>
Create `.planning/phases/125-behavioral-test-support/125-10-SUMMARY.md` when done.
</output>

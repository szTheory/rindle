---
phase: 125-behavioral-test-support
plan: "05"
type: execute
wave: 5
depends_on: ["125-04"]
files_modified:
  - test/install_smoke/support/generated_app_helper.ex
  - test/install_smoke/support/generated_app/migrations.ex
  - test/install_smoke/generated_app_smoke_test.exs
autonomous: true
requirements: [TEST-01, TEST-02, SAFE-01]
must_haves:
  truths:
    - "Generated apps retain separate host-owned Oban and pinned Rindle migration ownership, exact ordering, and public/rindle upgrade paths."
    - "Isolation/legacy/public compatibility catalog reports retain every key, snapshot, marker, relation, doctor, and rejection term."
    - "Migration support is hidden and receives explicit root/prefix/scenario/runner inputs."
  artifacts:
    - path: test/install_smoke/support/generated_app/migrations.ex
      provides: "generated host/Rindle migration sources, runners, seeds, and catalog queries"
  key_links:
    - from: test/install_smoke/support/generated_app/migrations.ex
      to: Rindle.Migration
      via: "unchanged pinned generated host migration source"
      pattern: "Rindle.Migration"
---

<objective>
Extract all generated migration and upgrade/catalog mechanics without changing schema ownership or report evidence.

Purpose: Isolate the highest-risk generated-app data contract before moving large smoke/profile source generators.
Output: Hidden Migrations owner, stable facade orchestration, and exact outcome/mutation proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/phases/125-behavioral-test-support/125-RESEARCH.md
@.planning/phases/125-behavioral-test-support/125-PATTERNS.md
@test/install_smoke/support/generated_app_helper.ex
@test/install_smoke/generated_app_smoke_test.exs
@.planning/phases/125-behavioral-test-support/125-04-SUMMARY.md

<interfaces>
Migrations owns host marker, host Oban, pinned Rindle, directional/default/public/legacy migration source; generated migration runners/seeds; migration-call parsing; schema/catalog snapshot queries and report inputs. Facade retains scenario orchestration and public result shaping. Product `Rindle.Migration` and all packaged migrations are preservation-only.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Complete outcome coverage for one isolation-upgrade migration path</name>
  <files>test/install_smoke/generated_app_smoke_test.exs</files>
  <behavior>
    - A real generated isolation upgrade preserves complete before/after `oban_jobs`, selected/decoy/public relations, marker versions, foreign key, indexes, and doctor-ready result.
    - Missing or changed catalog facts are rejected by the explicit contract predicate.
    - Host-owned Oban and pinned Rindle migration paths/order remain visible in the report.
  </behavior>
  <action>Use the existing generated isolation-upgrade case and fast mutation matrix to ensure every migration/report fact that will cross the new boundary is asserted as data. Add only missing outcome assertions. Keep the real generated process as authority; do not inspect SQL alias/source spelling and do not change migration code or report schemas.</action>
  <verify><automated>MIX_ENV=test mix test test/install_smoke/generated_app_smoke_test.exs --include phase_120_isolation_upgrade --seed 0</automated></verify>
  <done>One full generated upgrade path proves the migration owner boundary end-to-end before extraction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract generated migration and catalog mechanics</name>
  <reversibility rating="reversible">Only private generated test fixtures/mechanics move; existing reports and real database outcomes expose any drift.</reversibility>
  <files>test/install_smoke/support/generated_app_helper.ex, test/install_smoke/support/generated_app/migrations.ex, test/install_smoke/generated_app_smoke_test.exs</files>
  <behavior>
    - Every current generated migration source, filename, version, prefix, call order, seed, query, and report field is unchanged.
    - Migrations calls CommandRunner with explicit cwd/argv/env/stage and never chooses package/profile or normalizes final facade reports.
    - Public compatibility, isolation upgrade, and legacy upgrade scenarios retain current tags and results.
  </behavior>
  <action>Create hidden `Rindle.InstallSmoke.GeneratedApp.Migrations` and move the complete cohesive migration block, including sources, writers, runners, isolation seed, catalog queries/snapshots, and migration parsing. Pass root, prefix, scenario, app module, and CommandRunner inputs explicitly. Keep scenario selection/report assembly in GeneratedAppHelper and pure validation in Contracts. Preserve host-owned Oban versus pinned Rindle authority, versions, filenames, catalog terms, and errors exactly; do not touch `lib/`, `priv/repo/migrations`, schema, or public docs.</action>
  <verify><automated>MIX_ENV=test mix test test/install_smoke/generated_app_smoke_test.exs --seed 0 &amp;&amp; bash scripts/maintainer/refactor_contract.sh</automated></verify>
  <done>All generated migration/database mechanics have one hidden owner and every real/mutated catalog contract remains green.</done>
</task>

</tasks>

<threat_model asvs_level="L1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Generated migration source → temporary Postgres catalog | Ownership/order drift could make packed-adopter proof certify the wrong schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-125-04 | Tampering | generated migration/catalog evidence | high | mitigate | Real public/isolation/legacy database paths plus complete mutation predicates preserve every ownership and catalog fact. |
| T-125-SC | Tampering | package supply chain | low | accept | No dependency/package install changes are introduced. |
</threat_model>

<verification>
Run generated-app tests and SAFE-01; assert no diff under `lib/`, `priv/repo/migrations`, `mix.exs`, or `mix.lock`.
</verification>

<success_criteria>
- Generated migration ownership is cohesive and exact.
- Schema/migration behavior and report contracts are unchanged.
- No internal SQL-text snapshot substitutes for database/catalog proof.
</success_criteria>

<output>
Create `.planning/phases/125-behavioral-test-support/125-05-SUMMARY.md` when done.
</output>

---
phase: 125-behavioral-test-support
plan: "08"
type: execute
wave: 8
depends_on: ["125-07"]
files_modified:
  - test/install_smoke/docs_parity_test.exs
  - test/install_smoke/docs_parity/onboarding_and_capabilities_test.exs
  - test/install_smoke/docs_parity/operations_test.exs
autonomous: true
requirements: [TEST-03, SAFE-01]
must_haves:
  truths:
    - "Onboarding/capabilities owns lifecycle, convenience API, product fit, AV/storage, tus, and introductory public-contract assertions."
    - "Operations owns CI/toolchain posture, maintainer/public runbook boundaries, support matrices, nine tasks, and doctor/runtime-status assertions."
    - "Both domains read explicit named documents once and the aggregate retains only product/admin assertions."
  artifacts:
    - path: test/install_smoke/docs_parity/onboarding_and_capabilities_test.exs
      provides: "adopter onboarding, AV/storage/tus, and capability parity"
    - path: test/install_smoke/docs_parity/operations_test.exs
      provides: "maintainer CI/runbook/task/doctor operations parity"
  key_links:
    - from: test/install_smoke/docs_parity/onboarding_and_capabilities_test.exs
      to: test/install_smoke/docs_parity/support.ex
      via: "domain-specific named document setup"
      pattern: "DocsParity.Support"
---

<objective>
Move onboarding/capabilities and operations assertions into independently owned public-contract suites.

Purpose: Continue the documentation split by adopter/maintainer responsibility, not source file or line count.
Output: Two runnable domain suites and an aggregate containing only the final product/admin domain.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RUNNING.md
@.planning/phases/125-behavioral-test-support/125-RESEARCH.md
@.planning/phases/125-behavioral-test-support/125-PATTERNS.md
@test/install_smoke/docs_parity_test.exs
@test/install_smoke/docs_parity/support.ex
@.planning/phases/125-behavioral-test-support/125-07-SUMMARY.md

<interfaces>
Each suite is an ExUnit module with a domain-specific `setup_all` map and explicit calls to DocsParity.Support. Release workflow/manifest assertions remain in `release_docs_parity_test.exs`; owner-erasure/Admin assertions remain in the aggregate until Plan 09.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Move onboarding, profile, AV, storage, and tus parity as one adopter domain</name>
  <files>test/install_smoke/docs_parity_test.exs, test/install_smoke/docs_parity/onboarding_and_capabilities_test.exs</files>
  <behavior>
    - Lifecycle/facade/convenience API, introductory concept order, presigned/multipart posture, optional GCS, AV path/matrices, product-fit boundary, and TusPlug scope retain exact assertions.
    - The suite's setup map reads only documents/compiled modules used by this domain.
    - Failure names identify onboarding, AV, storage, profile, or tus contracts.
  </behavior>
  <action>Create `OnboardingAndCapabilitiesTest` and move whole aggregate test blocks mapped to onboarding/profile/AV/storage/tus (current lines 105–175, 528–635, 739–803, and the TusPlug scope assertion). Preserve the assertions and public compiled-doc checks exactly, using Support for shared mechanics. Remove moved blocks from the aggregate and run both files together. Do not absorb release, migration, operations, owner-erasure, or Admin assertions.</action>
  <verify><automated>MIX_ENV=test mix test test/install_smoke/docs_parity_test.exs test/install_smoke/docs_parity/onboarding_and_capabilities_test.exs --seed 0</automated></verify>
  <done>Adopter onboarding/capability truth is independently runnable and still asserts every existing public claim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move maintainer CI, runbook, tasks, and diagnostics parity as one operations domain</name>
  <files>test/install_smoke/docs_parity_test.exs, test/install_smoke/docs_parity/operations_test.exs</files>
  <behavior>
    - Root CI/toolchain posture, install-smoke posture, public-versus-maintainer guidance, support matrices, Proof severity, nine Mix tasks, troubleshooting surface, and doctor/runtime-status split retain exact assertions.
    - The dedicated release docs suite remains separate and unchanged.
    - The aggregate contains no moved operations test.
  </behavior>
  <action>Create `OperationsTest` and move whole current blocks for root CI/toolchain/install-smoke posture, public versus maintainer-only runbooks, RUNNING support matrices/Proof severity, nine shipped tasks, troubleshooting, and doctor/runtime-status. Preserve all string/order/count assertions and use a domain read-once setup. Do not merge `release_docs_parity_test.exs` or change `.github/workflows/ci.yml` in this plan. Remove moved blocks from the aggregate and verify all current/new/release suites together.</action>
  <verify><automated>MIX_ENV=test mix test test/install_smoke/docs_parity_test.exs test/install_smoke/docs_parity/install_and_migrations_test.exs test/install_smoke/docs_parity/onboarding_and_capabilities_test.exs test/install_smoke/docs_parity/operations_test.exs test/install_smoke/release_docs_parity_test.exs --seed 0 &amp;&amp; bash scripts/maintainer/refactor_contract.sh</automated></verify>
  <done>Operations truth has clear failure ownership, release truth remains separate, and only product/admin assertions remain aggregated.</done>
</task>

</tasks>

<threat_model asvs_level="L1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Shared documents → adopter/maintainer domain owners | Cross-domain movement can omit assertions or make failures ambiguous. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-125-04 | Tampering | onboarding/operations docs parity | high | mitigate | Move whole tests, run remainder and all domains together, preserve release suite, and execute SAFE-01. |
| T-125-SC | Tampering | package supply chain | low | accept | No packages or dependencies change. |
</threat_model>

<verification>
Run aggregate remainder, three extracted domains, release domain, and SAFE-01; compare the complete test-name inventory with the pre-split aggregate.
</verification>

<success_criteria>
- Onboarding/capabilities and operations each have one clear owner.
- No assertion is weakened, duplicated, or omitted.
- Release truth remains a separate domain and SAFE-01 stays green.
</success_criteria>

<output>
Create `.planning/phases/125-behavioral-test-support/125-08-SUMMARY.md` when done.
</output>

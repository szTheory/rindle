---
phase: 125-behavioral-test-support
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/rindle/config/repo_override_isolation_test.exs
  - scripts/maintainer/async_isolation_evidence.sh
  - test/install_smoke/async_isolation_evidence_runner_test.exs
  - RUNNING.md
autonomous: true
requirements: [TEST-04, SAFE-01]
must_haves:
  truths:
    - "Every normal run executes a high-iteration causal proof in which A sees the counting double while unrelated bare process B sees Rindle.Repo and commits inside A's open override window."
    - "A local fail-fast runner describes exactly 25 distinct deterministic seeds and starts one fresh Mix/BEAM process with the shipped single-run coverage command per seed."
    - "The runner cannot retry a failure, mask a nonzero exit, invoke coverage twice for one seed, or emit unsanitized environment/configuration values."
  artifacts:
    - path: test/rindle/config/repo_override_isolation_test.exs
      provides: "100-iteration causally coordinated async-isolation proof"
    - path: scripts/maintainer/async_isolation_evidence.sh
      provides: "fixed 25-seed fresh-process evidence runner"
    - path: test/install_smoke/async_isolation_evidence_runner_test.exs
      provides: "behavioral contract for runner plan, fail-fast exit, and sanitized evidence"
  key_links:
    - from: scripts/maintainer/async_isolation_evidence.sh
      to: mix coveralls.multiple
      via: "one foreground invocation per fixed seed"
      pattern: "coveralls.multiple --type local --type json --seed"
    - from: test/rindle/config/repo_override_isolation_test.exs
      to: Rindle.Config.repo/0
      via: "bare spawn plus Sandbox.allow/3 while counting override is open"
      pattern: "spawn|Sandbox.allow"
---

<objective>
Establish the causal and orchestration evidence seams for issue #42 before test-support refactoring begins.

Purpose: Prove the phase's hardest constraint early and encode resolved decisions R-01/R-03 without changing the shipped resolver or CI topology.
Output: A high-iteration ExUnit isolation proof and a tested, local-only 25-seed evidence runner.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RUNNING.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/125-behavioral-test-support/125-RESEARCH.md
@.planning/phases/125-behavioral-test-support/125-PATTERNS.md
@test/rindle/config/repo_override_isolation_test.exs
@test/support/counting_failing_txn_repo.ex
@lib/rindle/config.ex
@test/async_safety_guard_test.exs
@.github/workflows/ci.yml

<interfaces>
Preserve `Rindle.Config.repo/0`, `put_repo_override/1`, `delete_repo_override/0`, and `Rindle.Test.CountingFailingTxnRepo.with_counting_repo/2` exactly. The unrelated reader is a bare `spawn`, never a Task descendant. The local runner owns a fixed 25-seed matrix, invokes `mix coveralls.multiple --type local --type json --seed SEED --slowest 20` once per seed, and writes seed/SHA/toolchain/exit/sanitized-location evidence only.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Turn the causal isolation proof into a bounded high-iteration tracer</name>
  <reversibility rating="reversible">Only test coordination and repetition change; the resolver and double contracts remain untouched.</reversibility>
  <files>test/rindle/config/repo_override_isolation_test.exs</files>
  <behavior>
    - Each of 100 deterministic iterations opens A's process-local override, proves A resolves the double and force-fails its transaction, releases unrelated B while A remains open, and proves B resolves Rindle.Repo and commits.
    - B is a bare spawned process with an explicit Sandbox allowance and no `$callers` ancestry from A.
    - Every iteration uses unique message references, bounded receives, process cleanup, and failure output that names the iteration.
  </behavior>
  <action>Refactor the existing single causal case into a 100-iteration deterministic loop per R-01. Keep the test `async: true`, create B with bare `spawn`, grant `Sandbox.allow/3`, and coordinate with per-iteration references so stale messages cannot satisfy a later iteration. Assert explicitly that B acknowledges release while the callback still owns the override, then assert A's forced failure and B's real transaction result. Monitor/terminate B on assertion failure so the stress proof cannot leak processes. Update comments to describe current causal roles without changing `lib/rindle/config.ex` or the counting double.</action>
  <verify><automated>MIX_ENV=test mix test test/rindle/config/repo_override_isolation_test.exs --seed 0</automated></verify>
  <done>One ordinary focused/full-suite invocation exercises 100 causally independent override windows and fails with the exact iteration if A/B isolation regresses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the immutable fail-fast 25-seed evidence runner</name>
  <files>scripts/maintainer/async_isolation_evidence.sh, test/install_smoke/async_isolation_evidence_runner_test.exs, RUNNING.md</files>
  <behavior>
    - A read-only validation mode returns exactly 25 distinct deterministic seeds and the exact one-run coverage argv without executing coverage.
    - Normal execution records head SHA, toolchain, iteration, seed, argv, exit status, and a sanitized failure location; it stops immediately on the first nonzero status.
    - Successful evidence contains 25 passing iterations; no retry path, parallel/background coverage process, environment dump, or second per-seed invocation exists.
  </behavior>
  <action>Create a strict-shell maintainer runner for R-01/R-03 using `set -euo pipefail`, a repository-root check, a fixed in-file list of 25 distinct integer seeds, and one foreground Mix command per seed. Provide a side-effect-free `--validate` mode that emits machine-readable matrix/argv/report-schema data for ExUnit. In normal mode require a clean explicit report target under `.planning/phases/125-behavioral-test-support/`, capture combined output to a temporary file, retain only a bounded sanitized error classification/location on failure, and propagate the original nonzero status. The ExUnit contract must execute validation mode and a controlled fake `mix` placed on a temporary PATH to prove iteration ordering, single invocation, failure stop, status propagation, and redaction behavior without running coverage 25 times. Document the local-only command and exact CI prohibition in RUNNING.md; do not edit workflows.</action>
  <verify><automated>MIX_ENV=test mix test test/install_smoke/async_isolation_evidence_runner_test.exs test/rindle/config/repo_override_isolation_test.exs --seed 0 &amp;&amp; bash scripts/maintainer/refactor_contract.sh</automated></verify>
  <done>The finite issue-evidence protocol is executable and behavior-tested before any expensive run, with one coverage invocation per seed and no CI topology change.</done>
</task>

</tasks>

<threat_model asvs_level="L1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Process A override → unrelated process B | Process-local test state must not cross into an unrelated process tree. |
| Child coverage process → tracked evidence | Untrusted command output may include temporary paths or configuration values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-125-01 | Tampering | repo override isolation | high | mitigate | 100 coordinated iterations prove A/B resolver and transaction results while the window is open. |
| T-125-02 | Repudiation | evidence runner | high | mitigate | Fixed seeds, original exit propagation, first-failure stop, and tested invocation log prevent retry masking. |
| T-125-03 | Information disclosure | evidence report | high | mitigate | Allowlisted report fields and bounded sanitizer exclude environment/configuration values and raw full output. |
| T-125-SC | Tampering | package supply chain | low | accept | No dependency or package install is introduced. |
</threat_model>

<verification>
Run both focused tests, SAFE-01, and confirm `git diff --quiet -- .github/workflows/ci.yml lib/rindle/config.ex test/support/counting_failing_txn_repo.ex`.
</verification>

<success_criteria>
- R-01 and R-03 are encoded as runnable tests/runner behavior.
- TEST-04 has both causal and full-command evidence seams without changing production resolution or CI topology.
- SAFE-01 remains green.
</success_criteria>

<output>
Create `.planning/phases/125-behavioral-test-support/125-01-SUMMARY.md` when done.
</output>

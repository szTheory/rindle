---
phase: 32-upgrade-migration-safety
plan: 02
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - test/install_smoke/support/generated_app_helper.ex
  - test/install_smoke/generated_app_smoke_test.exs
  - guides/operations.md
  - guides/troubleshooting.md
autonomous: true
requirements:
  - UPGRADE-02
must_haves:
  truths:
    - "The canonical partial-upgrade recovery story is deterministic and CI-friendly, not a scheduler race."
    - "Read-only diagnosis stays on `mix rindle.doctor` and optional `mix rindle.runtime_status`; mutation stays on explicit repair verbs."
    - "Single-asset interrupted AV work is recovered through asset-scoped `Rindle.requeue_variants/2`, not broad regeneration."
    - "Broad `mix rindle.regenerate_variants` remains documented as the drift-maintenance lane rather than the default interrupted-work fix."
  artifacts:
    - path: "test/install_smoke/support/generated_app_helper.ex"
      provides: "Seeded upgrade-recovery support for deterministic AV failure/cancelled state inside the generated app."
      contains: "runtime_status"
    - path: "test/install_smoke/generated_app_smoke_test.exs"
      provides: "Outside-in recovery proof for one upgraded asset using diagnostics plus requeue."
      contains: "requeue"
    - path: "guides/operations.md"
      provides: "Operator semantics that distinguish diagnosis from repair in the upgrade context."
      contains: "requeue"
  key_links:
    - from: "test/install_smoke/generated_app_smoke_test.exs"
      to: "lib/rindle.ex"
      via: "generated app recovers the interrupted asset through the public repair facade"
      pattern: "requeue_variants"
    - from: "test/install_smoke/generated_app_smoke_test.exs"
      to: "lib/mix/tasks/rindle.runtime_status.ex"
      via: "upgrade proof exercises the read-only status surface before mutation"
      pattern: "runtime_status"
---

<objective>
Prove that one deterministic post-upgrade AV failure can be diagnosed and
recovered safely through the public operator surfaces Rindle already ships.

Purpose: satisfy UPGRADE-02 with a recovery proof that is explicit, bounded,
and aligned with the Phase 30/31 verb split.
Output: seeded generated-app recovery coverage, any small public-surface
adjustments needed for proofability, and operator docs aligned to that
recovery narrative.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/32-upgrade-migration-safety/32-CONTEXT.md
@.planning/phases/32-upgrade-migration-safety/32-RESEARCH.md
@.planning/phases/32-upgrade-migration-safety/32-PATTERNS.md
@.planning/phases/32-upgrade-migration-safety/32-01-PLAN.md
@test/install_smoke/support/generated_app_helper.ex
@test/install_smoke/generated_app_smoke_test.exs
@lib/rindle.ex
@lib/rindle/ops/lifecycle_repair.ex
@lib/mix/tasks/rindle.doctor.ex
@lib/mix/tasks/rindle.runtime_status.ex
@guides/operations.md
@guides/troubleshooting.md

<interfaces>
From .planning/phases/32-upgrade-migration-safety/32-CONTEXT.md:
```text
D-05 through D-09: prove one deterministic interrupted-AV narrative, use read-only diagnostics first, and recover the single asset through `Rindle.requeue_variants/2` rather than broad regeneration.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Seed and prove one deterministic interrupted-AV recovery lane inside the generated-app harness</name>
  <files>test/install_smoke/support/generated_app_helper.ex, test/install_smoke/generated_app_smoke_test.exs</files>
  <behavior>
    - The generated app reaches a post-upgrade AV state, then one asset is moved into a deterministic failed/cancelled/drift shape without timing-sensitive worker orchestration.
    - The proof records read-only diagnosis output before mutation and successful repair output after mutation.
    - The proof remains bounded to one coherent asset-scoped recovery narrative.
  </behavior>
  <action>Build on Plan 32-01's upgrade helper to create one post-upgrade AV asset and seed a deterministic interrupted-work shape suitable for operator recovery. Avoid timing-based stuck-job simulation; instead create a truthful failed/cancelled lifecycle state the generated app can inspect. Extend the smoke suite to run `mix rindle.doctor`, optionally run `mix rindle.runtime_status` when state is degraded, call the public repair surface for that asset, and assert the asset/variant recovers cleanly with ready siblings preserved. Keep the generated-app proof report concise but rich enough to assert diagnosis-before-repair semantics.</action>
  <verify>
    <automated>mix test test/install_smoke/generated_app_smoke_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The generated-app lane proves one deterministic upgraded AV failure can be diagnosed and repaired through the public operator surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the upgrade-context operator guidance around requeue-versus-regenerate</name>
  <files>guides/operations.md, guides/troubleshooting.md, test/install_smoke/generated_app_smoke_test.exs</files>
  <behavior>
    - Runtime status expectations and docs clearly point single-asset interrupted work to requeue, while keeping regeneration as the broad drift lane.
    - Diagnosis remains read-only and no hidden fix path is introduced.
  </behavior>
  <action>Use the generated-app recovery proof from Task 1 to freeze the operator guidance for this scenario. Update operations/troubleshooting so the upgrade-recovery story explicitly teaches: doctor validates setup, runtime status reports degraded state, `Rindle.requeue_variants/2` repairs one failed/cancelled upgraded asset, and `mix rindle.regenerate_variants` is reserved for broader stale/missing drift after profile or preset changes. If the smoke assertions need one or two explicit checks for recommendation wording or command sequence, add them there rather than widening the public API surface in this plan.</action>
  <verify>
    <automated>mix test test/install_smoke/generated_app_smoke_test.exs --warnings-as-errors</automated>
  </verify>
  <done>UPGRADE-02 is locked to one deterministic, asset-scoped recovery story and the operator guidance clearly distinguishes single-asset requeue from broader regeneration.</done>
</task>

</tasks>

<verification>
Run `mix test test/install_smoke/generated_app_smoke_test.exs --warnings-as-errors`.
</verification>

<success_criteria>
UPGRADE-02 is satisfied when the generated-app upgrade lane can surface one
deterministic interrupted AV state through read-only diagnostics and recover it
through asset-scoped requeue, with regeneration preserved as the broader drift
maintenance lane.
</success_criteria>

<output>
After completion, create `.planning/phases/32-upgrade-migration-safety/32-02-SUMMARY.md`
</output>

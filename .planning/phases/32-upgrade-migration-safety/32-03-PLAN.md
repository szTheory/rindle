---
phase: 32-upgrade-migration-safety
plan: 03
type: execute
wave: 3
depends_on:
  - 32-01
  - 32-02
files_modified:
  - guides/upgrading.md
  - README.md
  - guides/getting_started.md
  - guides/release_publish.md
  - test/install_smoke/docs_parity_test.exs
  - test/install_smoke/generated_app_smoke_test.exs
autonomous: true
requirements:
  - UPGRADE-03
must_haves:
  truths:
    - "Greenfield install docs stay narrow while existing-adopter upgrade docs gain one dedicated, canonical runbook."
    - "The upgrade guide is linear, checkpointed, copy-pasteable, and parity-tested against the exact command/checkpoint sequence proved by the generated-app upgrade lane."
    - "README, getting-started, and release docs link to the upgrade path without duplicating the full procedure."
    - "The upgrade guide teaches explicit migrations, doctor, optional runtime status, and the right repair verbs with no auto-remediation language."
  artifacts:
    - path: "guides/upgrading.md"
      provides: "Canonical existing-adopter upgrade runbook for pre-v1.4 to current Rindle."
      contains: "mix rindle.doctor"
    - path: "test/install_smoke/docs_parity_test.exs"
      provides: "Parity assertions that freeze upgrade-guide discoverability plus the exact proved command/checkpoint sequence."
      contains: "upgrading"
    - path: "test/install_smoke/generated_app_smoke_test.exs"
      provides: "The source-of-truth upgrade smoke lane whose canonical step sequence the docs parity test must mirror."
      contains: "upgrade"
    - path: "guides/release_publish.md"
      provides: "Maintainer-facing release docs that summarize and deep-link to upgrade guidance instead of absorbing it."
      contains: "upgrading"
  key_links:
    - from: "README.md"
      to: "guides/upgrading.md"
      via: "quickstart remains greenfield-first while surfacing existing-adopter upgrade guidance"
      pattern: "upgrading"
    - from: "guides/getting_started.md"
      to: "guides/upgrading.md"
      via: "canonical deep greenfield guide points existing adopters to the dedicated upgrade runbook"
      pattern: "upgrading"
---

<objective>
Lock the human-facing upgrade story to the proof that Plans 32-01 and 32-02
actually implement.

Purpose: satisfy UPGRADE-03 with one canonical upgrade runbook plus tested
cross-links from the greenfield and release surfaces.
Output: `guides/upgrading.md`, discoverability updates, and docs-parity tests
that keep the install-versus-upgrade split stable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/32-upgrade-migration-safety/32-CONTEXT.md
@.planning/phases/32-upgrade-migration-safety/32-RESEARCH.md
@.planning/phases/32-upgrade-migration-safety/32-PATTERNS.md
@.planning/phases/32-upgrade-migration-safety/32-01-PLAN.md
@.planning/phases/32-upgrade-migration-safety/32-02-PLAN.md
@README.md
@guides/getting_started.md
@guides/operations.md
@guides/troubleshooting.md
@guides/release_publish.md
@test/install_smoke/docs_parity_test.exs

<interfaces>
From .planning/phases/32-upgrade-migration-safety/32-CONTEXT.md:
```text
D-10 through D-17: ship one dedicated upgrade runbook, keep it checkpointed and parity-testable, and preserve a clean greenfield vs upgrade docs split.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish the canonical existing-adopter upgrade runbook</name>
  <files>guides/upgrading.md, guides/operations.md, guides/troubleshooting.md, test/install_smoke/generated_app_smoke_test.exs</files>
  <behavior>
    - The runbook is linear and explicit: runtime prerequisites, migration handoff, doctor, optional runtime status, then the correct repair branch.
    - Deep repair/troubleshooting details stay in operations/troubleshooting docs, linked from the runbook instead of duplicated inline.
    - The tone stays maintainer-to-maintainer and copy-pasteable.
    - The runbook sequence is anchored to a stable canonical step list or report contract already exercised by the generated-app upgrade smoke lane.
  </behavior>
  <action>Create `guides/upgrading.md` as the canonical pre-v1.4 to current upgrade guide. Structure it as a checkpointed runbook: confirm dependencies and FFmpeg posture where relevant, verify adopter-owned Repo and default Oban ownership, run the explicit host plus packaged Rindle migrations, run `mix rindle.doctor`, optionally inspect degraded state with `mix rindle.runtime_status`, and choose the correct repair verb with single-asset `requeue` distinguished from broad regeneration. Bind the guide to execution by having the generated-app upgrade smoke lane expose the canonical ordered step sequence or report fields the guide must mirror, and keep longer repair semantics in `guides/operations.md` and error analysis in `guides/troubleshooting.md` via links rather than prose duplication.</action>
  <verify>
    <automated>mix test test/install_smoke/docs_parity_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The repo has one dedicated, parity-testable upgrade runbook for existing adopters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add upgrade-path discoverability without polluting greenfield or release surfaces</name>
  <files>README.md, guides/getting_started.md, guides/release_publish.md, test/install_smoke/docs_parity_test.exs, test/install_smoke/generated_app_smoke_test.exs</files>
  <behavior>
    - README stays a narrow quickstart and getting-started stays the canonical greenfield deep guide.
    - Both surfaces prominently redirect existing adopters to `guides/upgrading.md`.
    - Release docs summarize upgrade relevance and deep-link out instead of embedding the full runbook.
    - Docs parity verifies not just phrases and links, but that the upgrade guide's ordered migration/doctor/runtime-status/requeue sequence matches the executable proof lane.
  </behavior>
  <action>Update README and `guides/getting_started.md` with concise, prominent upgrade callouts that point existing adopters at `guides/upgrading.md` without duplicating the runbook. Update `guides/release_publish.md` or the closest release-facing doc to mention the availability of the dedicated upgrade guide for existing adopters and keep maintainer-only release orchestration separate. Extend `test/install_smoke/docs_parity_test.exs` so discoverability, explicit migration language, the presence and ordering of `mix rindle.doctor`, optional `mix rindle.runtime_status`, and the final asset-scoped repair branch are frozen by tests against the canonical step sequence exposed by the generated-app upgrade smoke lane.</action>
  <verify>
    <automated>mix test test/install_smoke/generated_app_smoke_test.exs test/install_smoke/docs_parity_test.exs --warnings-as-errors</automated>
  </verify>
  <done>UPGRADE-03 is satisfied by one canonical upgrade runbook plus tested cross-links from the greenfield and release-facing docs.</done>
</task>

</tasks>

<verification>
Run `mix test test/install_smoke/generated_app_smoke_test.exs test/install_smoke/docs_parity_test.exs --warnings-as-errors`.
</verification>

<success_criteria>
UPGRADE-03 is satisfied when existing adopters have one dedicated upgrade
runbook, greenfield docs stay narrow and clear, release docs deep-link instead
of duplicating procedure, and docs-parity tests verify the same ordered upgrade
sequence the generated-app proof lane actually executes.
</success_criteria>

<output>
After completion, create `.planning/phases/32-upgrade-migration-safety/32-03-SUMMARY.md`
</output>

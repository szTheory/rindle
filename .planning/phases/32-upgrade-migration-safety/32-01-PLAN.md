---
phase: 32-upgrade-migration-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/install_smoke/support/generated_app_helper.ex
  - test/install_smoke/generated_app_smoke_test.exs
  - test/rindle/backward_compat/v13_digest_snapshot_test.exs
  - README.md
  - guides/getting_started.md
autonomous: true
requirements:
  - UPGRADE-01
must_haves:
  truths:
    - "The canonical upgrade proof stays on the generated Phoenix package-consumer harness and uses the same explicit migration/runtime path the public docs teach."
    - "A pre-v1.4 adopter shape upgrades into the AV-aware schema without destructive rewrites or hidden library-owned install automation."
    - "Legacy image-only data remains valid after upgrade and keeps the existing image-default backward-compat guardrails intact."
    - "The proof distinguishes built-artifact/public-path upgrade verification from maintainer-only repo internals."
  artifacts:
    - path: "test/install_smoke/support/generated_app_helper.ex"
      provides: "Generated-app upgrade helper that can stage a legacy starting point, run the explicit migration handoff, return an upgrade report, and expose the canonical upgrade step sequence for docs parity."
      contains: "prove_"
    - path: "test/install_smoke/generated_app_smoke_test.exs"
      provides: "Outside-in upgrade smoke coverage for the pre-v1.4 to current migration path."
      contains: "upgrade"
    - path: "test/rindle/backward_compat/v13_digest_snapshot_test.exs"
      provides: "Load-bearing image-default compatibility guardrail kept intact across the upgrade proof."
      contains: "v1.3"
  key_links:
    - from: "test/install_smoke/generated_app_smoke_test.exs"
      to: "test/install_smoke/support/generated_app_helper.ex"
      via: "smoke suite reuses the generated-app helper's explicit migration and lifecycle report"
      pattern: "GeneratedAppHelper|prove_"
    - from: "test/install_smoke/support/generated_app_helper.ex"
      to: "README.md"
      via: "helper must execute the same explicit migration handoff the public docs advertise"
      pattern: "Application.app_dir|Ecto.Migrator"
---

<objective>
Prove that an existing pre-v1.4 adopter can move onto the current AV-aware
Rindle shape through the public migration/runtime contract only.

Purpose: satisfy UPGRADE-01 with an executable generated-app lane instead of a
docs-only or fixture-only claim.
Output: one generated-app upgrade helper, one outside-in upgrade smoke test,
and preserved backward-compat guardrails for legacy image-only adopters.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/32-upgrade-migration-safety/32-CONTEXT.md
@.planning/phases/32-upgrade-migration-safety/32-RESEARCH.md
@.planning/phases/32-upgrade-migration-safety/32-PATTERNS.md
@test/install_smoke/support/generated_app_helper.ex
@test/install_smoke/generated_app_smoke_test.exs
@test/rindle/backward_compat/v13_digest_snapshot_test.exs
@README.md
@guides/getting_started.md
@priv/repo/migrations/20260502120000_extend_media_for_av.exs

<interfaces>
From .planning/phases/32-upgrade-migration-safety/32-CONTEXT.md:
```text
D-01 through D-04: generated-app proof lane, public-path honesty, no fixture-first proof, and legacy image-only safety are mandatory.
```

From test/install_smoke/support/generated_app_helper.ex:
```elixir
def prove_package_install!(profile_mode \\ :image) when profile_mode in [:image, :video] do
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the generated-app helper to model a truthful pre-v1.4 starting state and explicit upgrade run</name>
  <files>test/install_smoke/support/generated_app_helper.ex</files>
  <behavior>
    - The helper can stage a legacy image-only adopter shape, then run the same explicit host-plus-packaged-Rindle migration path taught in the docs.
    - The helper reports upgrade-specific facts such as legacy migration cutoff, final migration resolution, image-only post-upgrade safety, and the canonical command/checkpoint sequence the upgrade guide must mirror.
    - The proof never relies on repo-local deps fallback or hidden install automation unavailable to adopters.
  </behavior>
  <action>Extend `Rindle.InstallSmoke.GeneratedAppHelper` with upgrade-specific support that creates a generated Phoenix app, patches it to a simulated pre-v1.4 image-only runtime shape, runs only the legacy migration subset first, seeds legacy image-only state as needed, then resumes the current explicit migration handoff using `Application.app_dir(:rindle, "priv/repo/migrations")`. Reuse the existing report-map and cleanup shape from `prove_package_install!/1`, keep the same package/network install-source discipline, and capture upgrade facts the smoke suite can assert without inventing a second harness or fixture app. As part of that report contract, expose a stable ordered list of the canonical upgrade checkpoints/commands so Plan 32-03 can test `guides/upgrading.md` against the exact proved sequence rather than against wording alone.</action>
  <verify>
    <automated>mix test test/install_smoke/generated_app_smoke_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The generated-app helper can execute a truthful pre-v1.4 to current upgrade run and return a deterministic report about the upgrade path, including the canonical upgrade checkpoint/command sequence used for docs parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add outside-in upgrade smoke coverage and preserve legacy image-only compatibility guards</name>
  <files>test/install_smoke/generated_app_smoke_test.exs, test/rindle/backward_compat/v13_digest_snapshot_test.exs, README.md, guides/getting_started.md</files>
  <behavior>
    - The smoke suite proves an upgraded adopter app boots, runs explicit migrations, and preserves legacy image-only expectations after upgrade.
    - The existing image-default digest and legacy-data safety posture remain load-bearing rather than being replaced with weaker assertions.
    - Public docs keep teaching the explicit migration handoff consistently with what the smoke lane actually executes.
  </behavior>
  <action>Add a dedicated upgrade lane to `test/install_smoke/generated_app_smoke_test.exs` that calls the new helper path and asserts: the generated app started from a legacy pre-v1.4 shape, host plus packaged migrations ran explicitly, the packaged migration path still resolves via `Application.app_dir/2`, image-only lifecycle behavior remains valid after upgrade, and the app boots/processes through the post-upgrade path without repo-local fallback. Tighten `test/rindle/backward_compat/v13_digest_snapshot_test.exs` only where needed to keep the image-default invariant visibly tied to upgrade safety. If docs wording needs minor alignment, update README/getting-started only to keep the explicit migration snippet and public-path claim accurate to the smoke proof.</action>
  <verify>
    <automated>mix test test/install_smoke/generated_app_smoke_test.exs test/rindle/backward_compat/v13_digest_snapshot_test.exs --warnings-as-errors</automated>
  </verify>
  <done>UPGRADE-01 is proved by a generated-app upgrade lane and the legacy image-only compatibility guardrails remain intact.</done>
</task>

</tasks>

<verification>
Run `mix test test/install_smoke/generated_app_smoke_test.exs test/rindle/backward_compat/v13_digest_snapshot_test.exs --warnings-as-errors`.
</verification>

<success_criteria>
UPGRADE-01 is satisfied when a generated Phoenix adopter app can start from a
truthful pre-v1.4 shape, run the documented explicit migration handoff, boot
successfully on the current AV-aware runtime, and preserve legacy image-only
behavior without hidden migration or install shortcuts.
</success_criteria>

<output>
After completion, create `.planning/phases/32-upgrade-migration-safety/32-01-SUMMARY.md`
</output>

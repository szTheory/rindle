---
phase: 43-s3-multipart-backing-minio-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/storage.ex
  - test/rindle/storage/s3_tus_test.exs
  - test/rindle/upload/tus_s3_integration_test.exs
  - test/rindle/storage/storage_adapter_test.exs
  - test/rindle/ops/upload_maintenance_test.exs
  - test/rindle/upload/tus_plug_test.exs
autonomous: true
requirements: [TUS-06, TUS-07, TUS-08, TUS-09]
must_haves:
  truths:
    - "Rindle.Storage declares upload_part_stream/5 and complete_part_stream/4 as OPTIONAL callbacks"
    - "A unit test file exists asserting the S3 tail-buffer 5 MiB slice/accumulate math"
    - "A @tag :minio integration test scaffold exists for the >= 1 GiB drop+resume + zero-leak proof"
    - "storage_adapter_test asserts :tus_upload in S3.capabilities and {upload_part_stream,5} in optional_callbacks"
  artifacts:
    - path: "lib/rindle/storage.ex"
      provides: "upload_part_stream/5 + complete_part_stream/4 @callback decls, @type tus_part_state, @optional_callbacks entries"
      contains: "upload_part_stream"
    - path: "test/rindle/storage/s3_tus_test.exs"
      provides: "Unit tests for the S3 tail-buffer math (TUS-06)"
      contains: "5"
    - path: "test/rindle/upload/tus_s3_integration_test.exs"
      provides: "@tag :minio >= 1 GiB drop+resume + list_multipart_uploads-empty scaffold (TUS-09)"
      contains: "minio"
  key_links:
    - from: "lib/rindle/storage/s3.ex"
      to: "lib/rindle/storage.ex"
      via: "@impl true upload_part_stream/5"
      pattern: "upload_part_stream"
    - from: "test/rindle/storage/storage_adapter_test.exs"
      to: "Rindle.Storage.S3.capabilities/0"
      via: "assertion :tus_upload in capabilities"
      pattern: "tus_upload"
---

<objective>
Establish the Wave 0 foundation for Phase 43: the behaviour contract that S3 and Local will implement, and the test scaffolding (two NEW files + three extensions) that proves every requirement. This is the interface-first plan — downstream plans (S3 impl, reaper, Local+Plug dispatch, MinIO proof) implement against the callbacks declared here.

Lock the discretionary decisions handed to the planner so executors have no ambiguity:
- **Polymorphic completion** = a symmetric companion callback `complete_part_stream/4` (NOT branching on `state[:upload_id]` in the Plug). Cleaner adapter contract per RESEARCH Open Question 1.
- **`upload_part_stream/5` shape** = temp-path variant: arg 2 is a temp file path (the Plug drains the PATCH body to a temp file first, then hands the path), tail buffer at `Rindle.tmp/tus/<session_id>.tail` (RESEARCH Pattern 1 Note + Pattern 2).

Purpose: Without the callbacks declared and the tests written first, the S3/Local/Plug plans would be guessing at the contract. Nyquist Dimension 8 requires every later task to have an automated verify pointing at a file that already exists.
Output: `storage.ex` callback contract + 2 NEW test files + 3 extended test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-s3-multipart-backing-minio-proof/43-RESEARCH.md
@.planning/phases/43-s3-multipart-backing-minio-proof/43-PATTERNS.md
@.planning/phases/43-s3-multipart-backing-minio-proof/43-VALIDATION.md

<interfaces>
<!-- Contracts the executor implements/extends. Extracted from codebase — no exploration needed. -->

From lib/rindle/storage.ex (the @optional_callbacks edit site, storage.ex:283-286):
```elixir
@optional_callbacks initiate_resumable_upload: 3,
                    resumable_upload_status: 3,
                    cancel_resumable_upload: 3,
                    verify_resumable_completion: 3
```
Note: `:tus_upload` is ALREADY in the `@type capability` union (storage.ex:17-25) — no type edit needed there.

RESEARCH §Pattern 1 (lines 200-224) gives the recommended @type + @callback shape verbatim. The LOCKED decisions for this plan:
- `upload_part_stream(key, temp_path, base_offset, state, opts)` — arg 2 is a temp file PATH (String.t()), NOT a closure.
- ADD a companion `complete_part_stream(key, state, opts)` returning `{:ok, map()} | {:error, term()}` for symmetric completion (S3 = flush tail + complete_multipart_upload; Local = atomic rename).
- @type tus_part_state :: %{required(:offset)=>non_neg_integer(), optional(:upload_id)=>String.t(), optional(:parts)=>[%{part_number: pos_integer(), etag: String.t()}], optional(atom())=>term()}

From lib/rindle/storage/s3.ex:152 (the capability assertion target):
```elixir
def capabilities, do: [:presigned_put, :head, :signed_url, :multipart_upload]
```
(Plan 02 appends :tus_upload; this plan's storage_adapter_test asserts the post-Plan-02 list.)

S3 multipart constants (for the unit test math): `@s3_min_part_size 5 * 1024 * 1024` (5 MiB). Parts are 1-based, strictly increasing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare upload_part_stream/5 + complete_part_stream/4 OPTIONAL callbacks on Rindle.Storage</name>
  <files>lib/rindle/storage.ex</files>
  <read_first>
    - lib/rindle/storage.ex (FULL — the behaviour module; mirror the @typedoc/@doc/@callback voice of the four resumable callbacks at storage.ex:209-274, and the @optional_callbacks block at storage.ex:283-286)
    - .planning/phases/43-s3-multipart-backing-minio-proof/43-RESEARCH.md §Pattern 1 (lines 193-232) — the recommended shape
    - .planning/phases/43-s3-multipart-backing-minio-proof/43-PATTERNS.md §`lib/rindle/storage.ex` (lines 28-44) — the exact edit sites
  </read_first>
  <action>
    Add `@type tus_part_state :: %{required(:offset) => non_neg_integer(), optional(:upload_id) => String.t(), optional(:parts) => [%{part_number: pos_integer(), etag: String.t()}], optional(atom()) => term()}` near the other @type decls.
    Add `@callback upload_part_stream(key :: String.t(), temp_path :: String.t(), base_offset :: non_neg_integer(), state :: tus_part_state(), opts :: keyword()) :: {:ok, tus_part_state()} | {:error, term()}` with a @doc mirroring the resumable-callback voice ("Adapters expose this callback only when they advertise the `:tus_upload` capability"). Document that arg 2 is a temp file path holding ONE PATCH worth of bytes (the Plug drains the body to disk first — the temp-path variant, locked), and that the adapter pulls bytes from it, never buffering the whole upload in memory.
    Add `@callback complete_part_stream(key :: String.t(), state :: tus_part_state(), opts :: keyword()) :: {:ok, map()} | {:error, term()}` with a @doc explaining S3 flushes the remaining tail buffer as the final part then `complete_multipart_upload/4`, while Local does the atomic `File.rename` — the Plug calls this polymorphically (no `if adapter == Local` branch).
    Append both to the @optional_callbacks block: `upload_part_stream: 5` and `complete_part_stream: 4`.
    Do NOT touch the `@type capability` union (`:tus_upload` is already present at storage.ex:17-25). Do NOT add any non-optional callback (that would break GCS, which must not implement these).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | grep -v '^$' | grep -iv warning | grep -ic error | grep -q '^0$' && echo COMPILE_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'upload_part_stream' lib/rindle/storage.ex` >= 2 (callback decl + optional_callbacks entry).
    - Source assertion: `grep -c 'complete_part_stream' lib/rindle/storage.ex` >= 2.
    - Behavior assertion: `mix compile --warnings-as-errors` succeeds (both callbacks OPTIONAL — GCS adapter still compiles without implementing them).
    - Source assertion: `grep -c 'def capabilities, do: \[:gcs' lib/rindle/storage/gcs.ex` shows GCS unchanged (does NOT advertise :tus_upload).
  </acceptance_criteria>
  <done>storage.ex declares both callbacks as OPTIONAL with @type tus_part_state; project compiles warnings-as-errors with no impl yet (callbacks are optional).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the NEW unit test (s3_tus_test.exs) + extend storage_adapter_test.exs (TUS-06, TUS-07)</name>
  <files>test/rindle/storage/s3_tus_test.exs, test/rindle/storage/storage_adapter_test.exs</files>
  <read_first>
    - test/rindle/storage/s3_test.exs (FULL — the pure-unit no-MinIO `{:error, :missing_bucket}` block s3_test.exs:20-27 is the no-network analog; the MinIO module attrs s3_test.exs:8-18 show the skip-guard idiom)
    - test/rindle/storage/storage_adapter_test.exs (FULL — `capability lists are truthful` block at storage_adapter_test.exs:98-108, optional-callbacks assertions at storage_adapter_test.exs:67-80)
    - test/support/mocks.ex (FULL — `Rindle.StorageMock` is `Mox.defmock(Rindle.StorageMock, for: Rindle.Storage)`)
    - .planning/phases/43-s3-multipart-backing-minio-proof/43-PATTERNS.md §Test Pattern Assignments (lines 240-251)
    - .planning/phases/43-s3-multipart-backing-minio-proof/43-VALIDATION.md (the TUS-06/TUS-07 rows of the Per-Task Verification Map)
  </read_first>
  <behavior>
    s3_tus_test.exs (unit, `use ExUnit.Case, async: true`) — TUS-06 tail-buffer math, no network:
    - Test: appending a sub-5-MiB PATCH leaves the tail buffer < 5 MiB and produces ZERO new parts (state.parts unchanged, offset advances by the bytes written).
    - Test: appending bytes that push the tail >= 5 MiB slices off exactly one 5 MiB part (part_number increments by 1) and keeps the remainder buffered.
    - Test: a sequence of PATCHes accumulates strictly-increasing 1-based part_numbers (parts list ordered by part_number, never by arrival).
    - Test: completion flushes the remaining tail (any size, even < 5 MiB) as the final part.
    Drive the PURE buffering helper directly (extract/expose it from S3 in Plan 02), OR exercise S3.upload_part_stream/5 with `request/2` stubbed via Mox so no S3 call goes out. These tests are EXPECTED RED until Plan 02 lands the impl — scaffold them so they compile and fail with a clear "function not implemented" / pending message.
    storage_adapter_test.exs extension — TUS-07:
    - Update the S3 assertion (storage_adapter_test.exs:100) to expect `:tus_upload` in `S3.capabilities()`: the full list becomes `[:presigned_put, :head, :signed_url, :multipart_upload, :tus_upload]` (EXPECTED RED until Plan 02 adds the cap).
    - Mirror the optional-callbacks assertion block to assert `{:upload_part_stream, 5} in Rindle.Storage.behaviour_info(:optional_callbacks)` and `{:complete_part_stream, 4} in ...` (GREEN after Plan 01 Task 1).
    - Assert GCS does NOT advertise `:tus_upload` (`refute :tus_upload in Rindle.Storage.GCS.capabilities()`).
  </behavior>
  <action>
    Create test/rindle/storage/s3_tus_test.exs as a standalone `async: true` unit module covering the four tail-buffer behaviors above. Use `@s3_min_part_size 5 * 1024 * 1024` as the boundary. If you stub `request/2`, use `Mox` against `Rindle.StorageMock` per the mocks.ex analog; otherwise call the pure helper that Plan 02 will expose. Mark the not-yet-implemented assertions so the file compiles (use real assertions that fail loudly, not `@tag :skip` — these are the RED side of the Plan-02 cycle).
    Extend storage_adapter_test.exs per the behavior block. Do NOT delete existing assertions. The `:tus_upload in Capabilities.known()` assertion (storage_adapter_test.exs:90) already passes — leave it.
  </action>
  <verify>
    <automated>mix test test/rindle/storage/s3_tus_test.exs test/rindle/storage/storage_adapter_test.exs 2>&1 | tail -5; echo "EXPECT: compiles, S3-cap + tail-buffer assertions RED until Plan 02"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `test -f test/rindle/storage/s3_tus_test.exs` (file exists).
    - Behavior assertion: `mix test test/rindle/storage/s3_tus_test.exs` COMPILES (no CompileError) — failures are assertion failures, not load errors.
    - Source assertion: `grep -c 'tus_upload' test/rindle/storage/storage_adapter_test.exs` >= 1 (S3 cap assertion present).
    - Source assertion: `grep -c 'upload_part_stream' test/rindle/storage/s3_tus_test.exs` >= 1.
  </acceptance_criteria>
  <done>s3_tus_test.exs exists and compiles; storage_adapter_test.exs asserts the post-Plan-02 S3 capability list and the two new optional callbacks. Tail-buffer + S3-cap assertions are RED (Plan 02 turns them GREEN).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scaffold the MinIO proof + extend reaper and Plug tests (TUS-08, TUS-09)</name>
  <files>test/rindle/upload/tus_s3_integration_test.exs, test/rindle/ops/upload_maintenance_test.exs, test/rindle/upload/tus_plug_test.exs</files>
  <read_first>
    - test/rindle/upload/lifecycle_integration_test.exs (lines 47-99 — the `:inets.start()` + `RINDLE_MINIO_*` env reads + `Application.put_env` S3/ex_aws config + `on_exit` restore; this is the MinIO env-setup analog)
    - test/rindle/storage/s3_test.exs (lines 8-18 — MinIO module attrs + `@minio_skip_reason` guard; lines 128-152 — `put_part_to_presigned_url` ETag-from-headers reader)
    - test/rindle/upload/tus_plug_test.exs (FULL — `TusRouter` forward, `route/1`, `create_session/1` token extraction, `opts_for/1` via `TusPlug.init`; the S3-mock dispatch test extends this)
    - test/rindle/ops/upload_maintenance_test.exs (lines 1-110 — `setup :set_mox_from_context`/`:verify_on_exit!`, `TestProfile` with `storage: Rindle.StorageMock`, `TestRepoProbe`, `create_asset/1` + `create_session/2` builders)
    - .planning/phases/43-s3-multipart-backing-minio-proof/43-RESEARCH.md §Code Examples (lines 405-419 — the MinIO proof skeleton)
    - .planning/phases/43-s3-multipart-backing-minio-proof/43-PATTERNS.md §Test Pattern Assignments (lines 253-261)
  </read_first>
  <behavior>
    tus_s3_integration_test.exs (NEW, `@tag :minio`, `@tag timeout: 600_000`) — TUS-09 headline proof. Scaffold the flow (RESEARCH lines 405-419):
    1. POST -> signed tus URL; 2. PATCH ~600 MiB -> assert 204 + Upload-Offset advances + `multipart_upload_id` persisted on the session row; 3. simulate drop (stop mid-stream) -> HEAD returns authoritative offset; 4. resume PATCH from offset -> ... -> final PATCH (offset==length) -> 204; 5. assert session "completed", asset "validating", `byte_size == 1 GiB`; 6. second session -> one PATCH -> expire + run reaper; 7. `ExAws.S3.list_multipart_uploads(bucket) |> ExAws.request(cfg)` returns NO entry for the abandoned key (ZERO LEAK). Use the lifecycle_integration_test.exs env-setup analog verbatim. This file is `@tag :minio` so it's excluded from the default `mix test` run — it MUST compile cleanly. Plan 05 fills in the body against the live S3 dispatch.
    upload_maintenance_test.exs extension — TUS-09 reaper branch (unit, Mox):
    - Test: a session stamped `upload_strategy: "resumable", resumable_protocol: "tus", multipart_upload_id: "<id>"` is expired by the reaper via `StorageMock.abort_multipart_upload(upload_key, "<id>", _)` — NOT `cancel_resumable_upload`. Final state "expired".
    - Test: a `resumable_protocol: "gcs_native"` session still routes to `cancel_resumable_upload` (existing path unchanged).
    - Test: a legacy `resumable_protocol: nil` session is unchanged.
    - Test: `abort_multipart_upload` returning `{:error, :not_found}` is idempotent -> state "expired".
    tus_plug_test.exs extension — TUS-06/TUS-08 adapter dispatch:
    - Test (Mox): a PATCH routed through TusPlug calls `adapter.upload_part_stream/5` (proving no Local hard-wiring) and the final PATCH calls `adapter.complete_part_stream/4` then `Broker.verify_completion`.
    These reaper + Plug assertions are EXPECTED RED until Plans 03/04 land.
  </behavior>
  <action>
    Create test/rindle/upload/tus_s3_integration_test.exs with `@moduletag :minio`-style guard (mirror s3_test.exs:8-18) and the env setup from lifecycle_integration_test.exs:47-99. Write the 7-step flow as a single test with `@tag timeout: 600_000`; leave executable TODO markers ONLY where Plan 05 needs the live dispatch wired — the file MUST compile. Pick the >= 1 GiB synthetic stream approach (a 1 GiB synthetic byte stream satisfies the floor — RESEARCH A5; size, not content, is the point).
    Extend upload_maintenance_test.exs with the four reaper-branch tests using the existing Mox + TestRepoProbe harness. Stamp sessions via the existing `create_session/2` builder; add the `resumable_protocol`/`multipart_upload_id` keys.
    Extend tus_plug_test.exs with the Mox dispatch test using the existing `TusRouter`/`route`/`create_session` helpers; set the profile's storage adapter to `Rindle.StorageMock` and `expect/3` the two new callbacks.
  </action>
  <verify>
    <automated>mix test test/rindle/ops/upload_maintenance_test.exs test/rindle/upload/tus_plug_test.exs 2>&1 | tail -5; echo "EXPECT: compiles; new reaper/dispatch assertions RED until Plans 03/04"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `test -f test/rindle/upload/tus_s3_integration_test.exs`.
    - Behavior assertion: `mix test test/rindle/upload/tus_s3_integration_test.exs` is EXCLUDED by default (@tag :minio) yet `mix compile` shows the file loads without CompileError.
    - Source assertion: `grep -c 'list_multipart_uploads' test/rindle/upload/tus_s3_integration_test.exs` >= 1 (zero-leak assertion scaffolded).
    - Source assertion: `grep -c 'abort_multipart_upload' test/rindle/ops/upload_maintenance_test.exs` >= 1 (tus reaper branch test present).
    - Source assertion: `grep -c 'upload_part_stream\|complete_part_stream' test/rindle/upload/tus_plug_test.exs` >= 1 (dispatch test present).
  </acceptance_criteria>
  <done>The MinIO proof scaffold compiles and is @tag :minio gated; reaper and Plug tests assert the new branches (RED until Plans 03/04). All Wave 0 test files now exist (Nyquist Dimension 8 satisfied).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client PATCH body -> Plug -> temp file -> adapter | Untrusted bytes cross into the storage backing; bounded by per-PATCH ceiling -> 413 (existing) |
| persisted `multipart_parts` -> S3 complete | Server-issued part_number/ETag (from S3 responses, never client) reassembles the object |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Information Disclosure / DoS-cost | The `upload_part_stream`/`complete_part_stream` callback contract | mitigate | Contract declared OPTIONAL + capability-gated so only adapters that actually clean up (S3 abort, Local rm) advertise `:tus_upload`; the leak-mitigating reaper branch (Plan 03) and its test (this plan, Task 3) are scaffolded here |
| T-43-02 | Tampering | part_number/ETag in the @type tus_part_state | accept-by-design | @type fixes part_number as `pos_integer()` and ETag as server-sourced `String.t()`; no client-supplied part identity in the contract |
| T-43-SC | Tampering | npm/pip/cargo installs | n/a | Phase 43 installs NO packages (RESEARCH §Package Legitimacy Audit: N/A) — no install task exists, so no legitimacy checkpoint is required |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean (callbacks OPTIONAL — GCS unaffected).
- All five test files load without CompileError: `mix test --exclude minio test/rindle/storage/s3_tus_test.exs test/rindle/storage/storage_adapter_test.exs test/rindle/ops/upload_maintenance_test.exs test/rindle/upload/tus_plug_test.exs` runs (assertions for unimplemented impl are RED, by design).
- `git diff lib/rindle/upload/broker.ex` shows NO change (this plan does not touch broker).
</verification>

<success_criteria>
- storage.ex declares `upload_part_stream/5` + `complete_part_stream/4` as OPTIONAL callbacks with `@type tus_part_state`; project compiles warnings-as-errors.
- `test/rindle/storage/s3_tus_test.exs` and `test/rindle/upload/tus_s3_integration_test.exs` exist and compile.
- `storage_adapter_test.exs`, `upload_maintenance_test.exs`, `tus_plug_test.exs` extended with the new (RED) assertions.
- Wave 0 gaps from 43-VALIDATION.md all closed (Nyquist Dimension 8 satisfied for downstream plans).
</success_criteria>

<output>
Create `.planning/phases/43-s3-multipart-backing-minio-proof/43-01-SUMMARY.md` when done
</output>

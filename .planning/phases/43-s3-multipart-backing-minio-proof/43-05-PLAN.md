---
phase: 43-s3-multipart-backing-minio-proof
plan: 05
type: execute
wave: 4
depends_on: ["43-02", "43-03", "43-04"]
files_modified:
  - test/rindle/upload/tus_s3_integration_test.exs
  - test/rindle/storage/s3_test.exs
autonomous: false
requirements: [TUS-06, TUS-08, TUS-09]
must_haves:
  truths:
    - "A @tag :minio test completes a >= 1 GiB tus upload over S3 multipart with a mid-flight drop + resume"
    - "After completion: session 'completed', asset 'validating', byte_size == 1 GiB, PromoteAsset enqueued"
    - "After abandonment + reaper, list_multipart_uploads returns empty for the abandoned key (ZERO LEAK)"
    - "S3 UploadPart round-trip via the upload_part_stream callback works against MinIO"
  artifacts:
    - path: "test/rindle/upload/tus_s3_integration_test.exs"
      provides: "the headline >= 1 GiB drop+resume + zero-leak MinIO proof (live dispatch wired)"
      contains: "list_multipart_uploads"
    - path: "test/rindle/storage/s3_test.exs"
      provides: "@tag :minio UploadPart-via-callback round-trip"
      contains: "upload_part_stream"
  key_links:
    - from: "tus_s3_integration_test.exs"
      to: "TusPlug PATCH -> S3.upload_part_stream -> complete_part_stream -> verify_completion"
      via: "live MinIO round-trip"
      pattern: "verify_completion"
    - from: "tus_s3_integration_test.exs"
      to: "ExAws.S3.list_multipart_uploads"
      via: "post-reaper zero-leak assertion"
      pattern: "list_multipart_uploads"
---

<objective>
Deliver Success Criterion 5 — the phase's headline acceptance criterion: a MinIO integration proof that completes a >= 1 GiB tus upload over S3 multipart with a mid-flight drop + resume, then asserts that after abandonment + reaper, `list_multipart_uploads` returns empty (zero leak). This fills in the Plan 01 scaffold against the now-live S3 dispatch (Plans 02+04) and reaper (Plan 03), and adds a focused `@tag :minio` UploadPart-via-callback round-trip to `s3_test.exs`.

This plan is NOT autonomous — it ends with a `checkpoint:human-verify` because the MinIO lane runs in CI (MinIO is not available in the local sandbox by default), and the >= 1 GiB transfer is the load-bearing proof that must be confirmed green in the CI integration lane before `/gsd:verify-work`.

Purpose: All prior plans are proven by unit tests; this is the only end-to-end proof that the whole spine (Plug -> S3 multipart -> verify lane -> reaper) works against a real S3-compatible server, AND that the cost-leak threat (T-43-cost-leak, HIGH) is actually mitigated.
Output: a green (in CI) MinIO proof + the per-callback S3 round-trip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-s3-multipart-backing-minio-proof/43-RESEARCH.md
@.planning/phases/43-s3-multipart-backing-minio-proof/43-PATTERNS.md
@.planning/phases/43-s3-multipart-backing-minio-proof/43-VALIDATION.md

<interfaces>
<!-- The MinIO harness analogs + the live spine to drive. Extracted from codebase. -->

MinIO env setup analog (lifecycle_integration_test.exs:47-99): `:inets.start()`, `RINDLE_MINIO_*` env reads (localhost/minioadmin defaults), `Application.put_env(:rindle, Rindle.Storage.S3, bucket: ...)` + `Application.put_env(:ex_aws, :s3, [...])`, `on_exit` restore.
MinIO attrs/skip guard (s3_test.exs:8-18): `@minio_skip_reason` + `@tag :minio` + `@tag skip:` pair; opts = `[bucket:, aws_config: [...]]` (s3_test.exs:35-46).
ETag-from-headers reader analog (s3_test.exs:128-152): `put_part_to_presigned_url/2`.
tus driving helpers (tus_plug_test.exs:46-119): `TusRouter` forward, `route/1`, `create_session/1` token extraction, `opts_for/1` via `TusPlug.init`. `import Plug.Test` + `import Plug.Conn`.
The leak-proof verification call (RESEARCH lines 416-418): `ExAws.S3.list_multipart_uploads(bucket) |> ExAws.request(cfg)` -> `{:ok, %{body: %{uploads: uploads}}}`; `refute Enum.any?(uploads, & &1.key == abandoned_upload_key)`.
The reaper entry to invoke for the abort lane: `Rindle.Ops.UploadMaintenance.abort_incomplete_uploads/0,1` (invoked by `AbortIncompleteUploads.perform`).
The completion convergence target: `Broker.verify_completion/2` (frozen) — assert its EFFECTS (session "completed", asset "validating", byte_size, PromoteAsset enqueued via `Oban.Testing`/`assert_enqueued`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the >= 1 GiB drop+resume + zero-leak MinIO proof (TUS-06/08/09)</name>
  <files>test/rindle/upload/tus_s3_integration_test.exs</files>
  <read_first>
    - test/rindle/upload/tus_s3_integration_test.exs (the Plan 01 scaffold — fill in the live dispatch)
    - test/rindle/upload/lifecycle_integration_test.exs (lines 47-99 — env setup; lines 55-90 — MinIO bucket bootstrap)
    - test/rindle/storage/s3_test.exs (lines 8-18 skip guard; lines 29-82 multipart round-trip; lines 128-152 ETag-from-headers)
    - test/rindle/upload/tus_plug_test.exs (lines 46-119 — TusRouter/route/create_session/opts_for helpers)
    - .planning/phases/43-s3-multipart-backing-minio-proof/43-RESEARCH.md §Code Examples MinIO proof (lines 405-419), §Environment Availability (lines 460-472)
    - .planning/phases/43-s3-multipart-backing-minio-proof/43-PATTERNS.md §`tus_s3_integration_test.exs` (lines 256-261)
  </read_first>
  <action>
    Fill in the Plan 01 scaffold's 7-step flow against the LIVE S3 dispatch (Plans 02+04) and reaper (Plan 03), `@tag :minio`, `@tag timeout: 600_000`:
    1. POST through TusRouter -> signed tus URL (TusPlug with the S3 profile); 2. PATCH ~600 MiB of a synthetic 1 GiB stream -> assert 204, Upload-Offset advances, `multipart_upload_id` persisted on the session row, `multipart_parts` has >= 1 entry; 3. simulate drop (stop after the first PATCH) -> HEAD -> assert Upload-Offset == last_known_offset (authoritative); 4. resume PATCH from that offset through the final PATCH (offset == length) -> 204; 5. assert session.state "completed", asset.state "validating", asset.byte_size == 1 GiB (1_073_741_824), and `assert_enqueued worker: Rindle.Workers.PromoteAsset` (or the project's PromoteAsset module) — proving convergence into verify_completion (D-08); 6. ABORT lane: create a second tus session, PATCH one part (persisting multipart_upload_id), expire it (set expires_at in the past), run `Rindle.Ops.UploadMaintenance.abort_incomplete_uploads/0`; 7. assert `ExAws.S3.list_multipart_uploads(bucket) |> ExAws.request(cfg)` returns no entry whose `.key == abandoned_upload_key` (ZERO LEAK — the T-43-cost-leak mitigation proof).
    Generate the 1 GiB synthetic stream lazily (Stream/iodata, e.g. a repeated chunk) so the test does not hold 1 GiB in memory (RESEARCH A5 — size, not content, is the point). Use the lifecycle_integration_test.exs env-setup verbatim for MinIO config.
  </action>
  <verify>
    <automated>mix test test/rindle/upload/tus_s3_integration_test.exs --include minio 2>&1 | tail -15; echo "NOTE: requires MinIO up (RINDLE_MINIO_*); skipped without it"</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion (CI/MinIO): `mix test test/rindle/upload/tus_s3_integration_test.exs --include minio` GREEN with MinIO running — completes the >= 1 GiB drop+resume and the zero-leak assertion passes.
    - Source assertion: `grep -c 'list_multipart_uploads' test/rindle/upload/tus_s3_integration_test.exs` >= 1 (zero-leak assertion wired).
    - Source assertion: `grep -c '1_073_741_824\|1 \* 1024 \* 1024 \* 1024\|1024 \* 1024 \* 1024' test/rindle/upload/tus_s3_integration_test.exs` >= 1 (>= 1 GiB floor).
    - Source assertion: `grep -c 'assert_enqueued\|PromoteAsset' test/rindle/upload/tus_s3_integration_test.exs` >= 1 (convergence effect asserted).
    - Behavior assertion: `mix test` (default exclusions) stays GREEN — the new test is @tag :minio excluded, the suite is not broken.
  </acceptance_criteria>
  <done>The MinIO proof completes a >= 1 GiB tus upload over S3 multipart with a mid-flight drop+resume, asserts the verify-lane effects + PromoteAsset enqueue, and asserts list_multipart_uploads empty after the reaper aborts an abandoned session.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the @tag :minio S3 UploadPart-via-callback round-trip (TUS-06)</name>
  <files>test/rindle/storage/s3_test.exs</files>
  <read_first>
    - test/rindle/storage/s3_test.exs (FULL — multipart round-trip s3_test.exs:29-82, ETag-from-headers s3_test.exs:128-152, MinIO attrs s3_test.exs:8-18)
    - .planning/phases/43-s3-multipart-backing-minio-proof/43-PATTERNS.md §`test/rindle/storage/s3_test.exs` (lines 243-244)
  </read_first>
  <action>
    Add a `@tag :minio` test that drives the NEW `Rindle.Storage.S3.upload_part_stream/5` callback directly against MinIO (server-mediated, NOT presigned): write a temp file with > 5 MiB of bytes, call `S3.upload_part_stream(key, temp_path, 0, %{}, opts)` (opts = the MinIO bucket/aws_config + session_id), assert the returned state has `:upload_id` and >= 1 part with a non-empty ETag; then `S3.complete_part_stream(key, returned_state, opts)` and assert `S3.head(key, opts)` returns the expected size. Mirror the existing multipart round-trip harness (s3_test.exs:29-82). Cover the sub-5-MiB tail case: a final small chunk flushed by complete_part_stream as the last part.
  </action>
  <verify>
    <automated>mix test test/rindle/storage/s3_test.exs --include minio 2>&1 | tail -10; echo "NOTE: requires MinIO up; skipped without it"</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion (CI/MinIO): `mix test test/rindle/storage/s3_test.exs --include minio` GREEN — the upload_part_stream/complete_part_stream round-trip assembles a real object whose head size matches.
    - Source assertion: `grep -c 'upload_part_stream' test/rindle/storage/s3_test.exs` >= 1.
    - Behavior assertion: `mix test test/rindle/storage/s3_test.exs` (default, no MinIO) stays GREEN (new test excluded by @tag :minio).
  </acceptance_criteria>
  <done>A focused @tag :minio test proves the S3 upload_part_stream/complete_part_stream callbacks round-trip against MinIO, including the sub-5-MiB tail flushed as the final part.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the MinIO >= 1 GiB drop+resume + zero-leak proof green in the CI integration lane</name>
  <what-built>
    The full Phase-43 spine end-to-end: TusPlug PATCH -> S3.upload_part_stream/5 (tail-buffer + UploadPart) -> S3.complete_part_stream/3 -> UNCHANGED Broker.verify_completion/2 -> PromoteAsset; plus the reaper's resumable_protocol="tus" branch aborting the S3 multipart. Proven by `test/rindle/upload/tus_s3_integration_test.exs` (>= 1 GiB drop+resume + list_multipart_uploads-empty) and the s3_test.exs per-callback round-trip.
  </what-built>
  <how-to-verify>
    MinIO is not in the local sandbox by default, so the @tag :minio proof runs in CI. Confirm it green there:
    1. Push the branch (or trigger the integration workflow) and open the CI run for the integration/minio lane (.github/workflows/ci.yml integration job with the MinIO service).
    2. Verify `mix test ... --include integration --include minio` is GREEN — specifically that `tus_s3_integration_test.exs` passed (the >= 1 GiB drop+resume completed and the zero-leak assertion held) and `s3_test.exs` MinIO tests passed.
    3. (Optional local) If you have Docker: `docker run -p 9000:9000 -p 9001:9001 minio/minio server /data` + `mc mb local/rindle-test`, export `RINDLE_MINIO_*`, then `mix test test/rindle/upload/tus_s3_integration_test.exs --include minio`. Expect green within the 600s timeout.
    Expected outcome: both MinIO test files green; no orphaned multipart upload remains after the reaper (list_multipart_uploads empty for the abandoned key).
  </how-to-verify>
  <resume-signal>Type "approved" once the CI integration/minio lane is green, or describe the failure (which assertion, which step of the 7-step flow).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| abandoned tus session -> reaper -> S3 | The reaper is the sole cleanup; the proof asserts no orphaned multipart remains |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-cost-leak | Information Disclosure / Denial of Service (cost) | Orphaned S3 multipart from an abandoned tus session | mitigate (PROVEN here) | The reaper abort branch (Plan 03) is PROVEN by this plan's `list_multipart_uploads`-empty assertion after the reaper runs — the HIGH-severity threat's executable proof; the human-verify checkpoint blocks until this is green in CI |
| T-43-12 | Denial of Service (memory, test-side) | 1 GiB synthetic stream | mitigate | Stream generated lazily (iodata/Stream), never materialized as a 1 GiB binary in the test process |
| T-43-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in Phase 43 (RESEARCH §Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- CI integration/minio lane: `mix test --include integration --include minio` GREEN (tus_s3_integration_test.exs + s3_test.exs MinIO tests pass).
- `mix test` (default exclusions) GREEN — the @tag :minio tests do not run in the default suite and the suite is unbroken.
- Phase gate (per 43-VALIDATION.md): `mix test` green AND the @tag :minio proof green in CI before `/gsd:verify-work`.
- REVIEW GATE (carried): `git diff lib/rindle/upload/broker.ex` shows verify_completion/2 byte-for-byte unchanged (D-08).
</verification>

<success_criteria>
- A >= 1 GiB tus upload completes over S3 multipart with a mid-flight drop + resume (MinIO).
- Completion effects asserted: session "completed", asset "validating", byte_size == 1 GiB, PromoteAsset enqueued (convergence into verify_completion).
- After abandonment + reaper, list_multipart_uploads returns empty for the abandoned key (zero leak).
- S3 upload_part_stream/complete_part_stream round-trip proven per-callback against MinIO.
- Human confirms the CI integration/minio lane is green.
</success_criteria>

<output>
Create `.planning/phases/43-s3-multipart-backing-minio-proof/43-05-SUMMARY.md` when done
</output>

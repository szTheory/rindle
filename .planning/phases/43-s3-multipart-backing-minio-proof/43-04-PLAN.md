---
phase: 43-s3-multipart-backing-minio-proof
plan: 04
type: execute
wave: 3
depends_on: ["43-01", "43-02"]
files_modified:
  - lib/rindle/storage/local.ex
  - lib/rindle/upload/tus_plug.ex
autonomous: true
requirements: [TUS-06, TUS-08]
must_haves:
  truths:
    - "Local.upload_part_stream/5 wraps tus_append (part_number ignored) and returns %{offset: n} with no upload_id/parts"
    - "Local.complete_part_stream/3 does the atomic File.rename (wraps tus_complete)"
    - "TusPlug dispatches PATCH through adapter.upload_part_stream/5 (no Local hard-wiring)"
    - "TusPlug persists multipart_upload_id + multipart_parts between PATCHes"
    - "Final PATCH calls adapter.complete_part_stream/3 then converges into UNCHANGED Broker.verify_completion/2"
  artifacts:
    - path: "lib/rindle/storage/local.ex"
      provides: "upload_part_stream/5 + complete_part_stream/3 wrapping tus_append/tus_complete"
      contains: "upload_part_stream"
    - path: "lib/rindle/upload/tus_plug.ex"
      provides: "polymorphic adapter dispatch in handle_patch/complete_upload; multipart state persistence"
      contains: "upload_part_stream"
  key_links:
    - from: "lib/rindle/upload/tus_plug.ex handle_patch/2"
      to: "adapter.upload_part_stream/5"
      via: "drain PATCH to temp file then dispatch through behaviour"
      pattern: "upload_part_stream"
    - from: "lib/rindle/upload/tus_plug.ex complete_upload/3"
      to: "Broker.verify_completion/2"
      via: "adapter.complete_part_stream then verify_completion (D-08 convergence)"
      pattern: "verify_completion"
---

<objective>
Complete TUS-06 (Local impl) and TUS-08 (completion convergence) by making `TusPlug` storage-agnostic. Phase 42 hard-wired the Plug to `Local.tus_part_path/tus_append/tus_complete` (tus_plug.ex:214,291). This plan: (1) implements `Local.upload_part_stream/5` + `Local.complete_part_stream/3` wrapping the existing Phase-42 helpers; (2) rewrites the Plug's PATCH + completion to dispatch polymorphically through `adapter.upload_part_stream/5` and `adapter.complete_part_stream/3` — so S3 (Plan 02) and Local both work without the Plug knowing the backend (D-12 realized). Completion still converges into the BYTE-FOR-BYTE UNCHANGED `Broker.verify_completion/2` (D-08, hard).

LOCKED decisions:
- **Temp-path variant** (Plan 01): the Plug drains the PATCH body to a temp file first (keep the existing `drain/6`+`write_chunk/7` loop verbatim — it already enforces the 1 MiB read_length, the per-PATCH ceiling -> 413, and the Upload-Length bound), THEN hands the temp path to `adapter.upload_part_stream/5`.
- **Companion completion callback** (Plan 01): the Plug calls `adapter.complete_part_stream/3` — NO `if adapter == Local` special-case (anti-pattern, RESEARCH line 255). Local's complete does the rename; S3's flushes the tail + completes the multipart.
- **App-env opts fallback** (Pitfall 4): pass `session_id`/`bucket`/`aws_config` through the call opts where available, but rely on the S3 adapter's app-env fallback for `bucket`/`aws_config` (proven for `head`). Add a test asserting the S3 path works through the Plug edge (the likeliest integration bug, A4).

Purpose: This is the seam D-12 anticipated. After this plan, any future tus sink works without touching the Plug.
Output: a polymorphic TusPlug + a Local tus sink that wraps the Phase-42 helpers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-s3-multipart-backing-minio-proof/43-RESEARCH.md
@.planning/phases/43-s3-multipart-backing-minio-proof/43-PATTERNS.md

<interfaces>
<!-- Phase-42 helpers + the hard-wired Plug paths to generalize. Extracted from codebase. -->

Local Phase-42 helpers to WRAP (local.ex):
- `tus_part_path(session_id, opts)` (local.ex:119-122): `<root>/tus/<session_id>.part`, traversal-proof.
- `tus_append(session_id, chunk, opts)` (local.ex:131-143): opens part path `[:append, :binary]`, IO.binwrite. Returns :ok | {:error, term}.
- `tus_complete(session_id, key, opts)` (local.ex:154-163): atomic `File.rename` part->key. Returns {:ok, path} | {:error, term}. `:exdev` surfaces as error (NO copy fallback — Pitfall 5).
- `capabilities` (local.ex:83): `[:local, :presigned_put, :tus_upload]` — ALREADY advertises :tus_upload, no edit.

The Plug PATCH hard-wiring to GENERALIZE (tus_plug.ex:213-230, stream_append/4): currently `Local.tus_part_path(session.id, root: opts[:root])` + File.open append + drain loop. The drain/6 (tus_plug.ex:232-249) + write_chunk/7 (tus_plug.ex:251-265) loop already enforces @read_length (1 MiB), the ceiling -> :too_large -> 413, and the Upload-Length bound — KEEP IT, just retarget its output to a temp file then dispatch.

The Plug completion to GENERALIZE (tus_plug.ex:290-301, complete_upload/3):
```elixir
defp complete_upload(conn, session, opts) do
  with {:ok, _path} <- Local.tus_complete(session.id, session.upload_key, root: opts[:root]),   # <- Local hard-wire
       {:ok, %{session: _completed}} <- Broker.verify_completion(session.id, root: opts[:root]) do
    ...
```

The state-persistence template (tus_plug.ex:267-271, persist_offset/2): `MediaUploadSession.changeset(%{last_known_offset: n}) |> Config.repo().update()`. EXTEND it to also persist `multipart_upload_id` + `multipart_parts` (both already cast at media_upload_session.ex:85-86).

The FROZEN convergence target (DO NOT MODIFY): `Broker.verify_completion(session.id, opts)` (broker.ex:470-489). It resolves the adapter from the profile and calls `adapter.head/2`; S3's head returns size+content_type so the verify lane gets a real content_type for free. The Plug already calls it at tus_plug.ex:292 — keep that line.

The callback signatures (from storage.ex, Plan 01):
- `upload_part_stream(key, temp_path, base_offset, state, opts) :: {:ok, tus_part_state} | {:error, term}`
- `complete_part_stream(key, state, opts) :: {:ok, map} | {:error, term}`
- tus_part_state for Local = `%{offset: n}` (NO :upload_id/:parts keys — Pitfall 5).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Local.upload_part_stream/5 + complete_part_stream/3 (TUS-06 Local)</name>
  <files>lib/rindle/storage/local.ex</files>
  <read_first>
    - lib/rindle/storage/local.ex (FULL — tus_part_path :119-122, tus_append :131-143, tus_complete :154-163, capabilities :83, the multipart {:error, {:upload_unsupported, :multipart_upload}} idiom :51-69)
    - .planning/phases/43-s3-multipart-backing-minio-proof/43-RESEARCH.md §Pitfall 5 (lines 342-346)
    - .planning/phases/43-s3-multipart-backing-minio-proof/43-PATTERNS.md §`lib/rindle/storage/local.ex` (lines 94-116)
    - test/rindle/upload/tus_local_backing_test.exs (the existing Local tus coverage — keep it GREEN; mirror its setup for any new Local assertions)
  </read_first>
  <action>
    Implement `@impl true def upload_part_stream(key, temp_path, base_offset, state, opts)`: stream the temp_path bytes into the Local part file via `tus_append(session_id, chunk, opts)` (session_id from opts `:session_id`; stream the temp file in chunks, never read whole into memory). `part_number` semantics do not exist for Local — ignore them. Return `{:ok, %{offset: base_offset + bytes_written}}` with NO `:upload_id`/`:parts` keys (Pitfall 5 — the optional-map @type accommodates this). On error propagate `{:error, reason}`.
    Implement `@impl true def complete_part_stream(key, state, opts)`: call `tus_complete(session_id, key, opts)` (atomic File.rename) and return `{:ok, %{upload_key: key}}` (or `{:error, reason}`). session_id from opts.
    Do NOT edit `capabilities/0` — Local already advertises `:tus_upload` (local.ex:83). Keep the existing multipart-unsupported idiom for the genuinely-unsupported S3-multipart callbacks (local.ex:51-69).
  </action>
  <verify>
    <automated>mix test test/rindle/storage/local_tus_test.exs test/rindle/upload/tus_local_backing_test.exs -x</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `mix test test/rindle/storage/local_tus_test.exs test/rindle/upload/tus_local_backing_test.exs` GREEN (existing Local tus coverage unbroken; new upload_part_stream/complete_part_stream covered).
    - Source assertion: `grep -c 'def upload_part_stream' lib/rindle/storage/local.ex` >= 1 and `grep -c 'def complete_part_stream' lib/rindle/storage/local.ex` >= 1.
    - Source assertion: Local's upload_part_stream return has no `:upload_id`/`:parts` — `grep -A8 'def upload_part_stream' lib/rindle/storage/local.ex` shows `%{offset:` and no `upload_id:`.
    - Behavior assertion: `mix compile --warnings-as-errors` clean.
  </acceptance_criteria>
  <done>Local.upload_part_stream/5 wraps tus_append (part_number ignored, returns %{offset: n}); Local.complete_part_stream/3 wraps the atomic tus_complete rename; existing Local tus tests stay GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire TusPlug PATCH + completion to dispatch through the adapter (TUS-06/TUS-08)</name>
  <files>lib/rindle/upload/tus_plug.ex</files>
  <read_first>
    - lib/rindle/upload/tus_plug.ex (FULL — init/1 :70-98, handle_patch/2 :194-208, stream_append/4 :213-230, drain/6 :232-249, write_chunk/7 :251-265, persist_offset/2 :267-271, maybe_complete/4 :273-284, complete_upload/3 :290-301)
    - lib/rindle/upload/broker.ex (verify_completion/2 :470-489 — the FROZEN convergence target; READ to confirm you do not change it)
    - lib/rindle/domain/media_upload_session.ex (the cast block :84-86 confirming multipart_upload_id/multipart_parts/resumable_protocol are castable)
    - .planning/phases/43-s3-multipart-backing-minio-proof/43-RESEARCH.md §Pattern 3 (lines 252-255), §Pattern 1 Note (line 232), §Pitfall 4 (lines 336-340)
    - .planning/phases/43-s3-multipart-backing-minio-proof/43-PATTERNS.md §`lib/rindle/upload/tus_plug.ex` (lines 120-147)
    - test/rindle/upload/tus_plug_test.exs (the Mox dispatch test from Plan 01 Task 3 this turns GREEN)
  </read_first>
  <action>
    Rewrite `stream_append/4`: keep the `drain/6`+`write_chunk/7` loop VERBATIM but retarget its output to a per-PATCH temp file (e.g. `Path.join([opts[:root] || Rindle.AV.TempRunDir.root_dir(), "tus", session.id <> ".patch"])` — distinct from the .tail/.part files). After draining (which yields the new offset and enforces ceiling/Upload-Length -> 413), build the prior `state` from the session (`%{offset: session.last_known_offset, upload_id: session.multipart_upload_id, parts: session.multipart_parts || []}`), then call `opts[:adapter].upload_part_stream(session.upload_key, temp_path, session.last_known_offset, prior_state, call_opts)`. `call_opts` carries `session_id: session.id`, `root: opts[:root]`, and (for S3) relies on the adapter's app-env fallback for `bucket`/`aws_config` (Pitfall 4 — locked). Clean up the temp file after dispatch. Do NOT special-case the adapter (no `if adapter == Local`).
    Update `handle_patch/2`/`persist_offset/2`: after a successful upload_part_stream, persist BOTH `last_known_offset: returned.offset` AND `multipart_upload_id: returned[:upload_id]` + `multipart_parts: returned[:parts]` (extend the changeset; columns already cast). For Local the upload_id/parts are nil/absent — persist nil gracefully.
    Rewrite `complete_upload/3`: replace the `Local.tus_complete(...)` hard-wire with `opts[:adapter].complete_part_stream(session.upload_key, %{upload_id: session.multipart_upload_id, parts: session.multipart_parts || []}, call_opts)` (S3 flushes tail + completes multipart; Local renames). KEEP the `Broker.verify_completion(session.id, opts)` line UNCHANGED (D-08 convergence). On adapter error -> tus_error(conn, 500, "").
    Do NOT touch `init/1`'s capability gate (already raises on missing :tus_upload — TUS-07, shipped). Do NOT modify broker.ex.
  </action>
  <verify>
    <automated>mix test test/rindle/upload/tus_plug_test.exs test/rindle/upload/tus_local_backing_test.exs -x</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `mix test test/rindle/upload/tus_plug_test.exs` GREEN — the Mox dispatch test proves `adapter.upload_part_stream/5` and `adapter.complete_part_stream/3` are called (no Local hard-wiring); the Local end-to-end PATCH->completion still passes.
    - Source assertion: `grep -c 'Local.tus_part_path\|Local.tus_append\|Local.tus_complete' lib/rindle/upload/tus_plug.ex` == 0 (all Local hard-wiring removed).
    - Source assertion: `grep -c 'adapter\].upload_part_stream\|adapter].complete_part_stream\|.upload_part_stream(\|.complete_part_stream(' lib/rindle/upload/tus_plug.ex` >= 2 (polymorphic dispatch present).
    - Source assertion: `grep -c 'verify_completion' lib/rindle/upload/tus_plug.ex` >= 1 (convergence preserved).
    - Source assertion: `grep -c 'multipart_upload_id' lib/rindle/upload/tus_plug.ex` >= 1 (state persisted between PATCHes).
    - REVIEW GATE (TUS-08, hard): `git diff lib/rindle/upload/broker.ex` shows NO change to verify_completion/2 or execute_verify_completion/5 (must be byte-for-byte unchanged).
  </acceptance_criteria>
  <done>TusPlug dispatches PATCH/completion polymorphically through the adapter callbacks, persists multipart_upload_id+multipart_parts between PATCHes, and converges into the UNCHANGED verify_completion/2. No Local hard-wiring remains; broker.ex untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client PATCH body -> Plug temp file -> adapter | Untrusted bytes; bounded by the preserved drain/6 ceiling -> 413 and the Upload-Length bound |
| Plug -> Broker.verify_completion (frozen) | The single trusted promote lane; head re-sniff is the trust boundary (D-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-08 | Tampering / Elevation | tus completion bypassing the verify lane | mitigate | complete_upload KEEPS `Broker.verify_completion/2` as the convergence point (D-08); review-gate asserts broker.ex byte-for-byte unchanged; no new completion vocabulary |
| T-43-09 | Denial of Service (memory) | Plug draining PATCH to disk | mitigate | drain/6+write_chunk/7 preserved verbatim (1 MiB read_length, per-PATCH ceiling -> 413, Upload-Length bound); body streamed to a temp file, never a binary |
| T-43-10 | Information Disclosure | bucket/aws_config flowing through the Plug edge (Pitfall 4) | mitigate | Opts resolved via S3 adapter app-env fallback (no creds threaded through the Plug); explicitly TESTED via the Mox/S3 dispatch path (A4, the likeliest integration bug) |
| T-43-11 | Tampering | per-PATCH temp file path traversal | mitigate | temp/part/tail paths keyed on server-issued session.id (UUID) under root/Rindle.tmp — traversal-proof |
| T-43-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in Phase 43 (RESEARCH §Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `mix test test/rindle/upload/ test/rindle/storage/local_tus_test.exs` GREEN.
- REVIEW GATE: `git diff lib/rindle/upload/broker.ex` empty for verify_completion/2 + execute_verify_completion/5 (D-08, hard).
- `mix compile --warnings-as-errors` clean.
- No new column / migration: `git status priv/repo/migrations/` clean.
- No Local hard-wiring left in tus_plug.ex (grep == 0).
</verification>

<success_criteria>
- Local.upload_part_stream/5 + complete_part_stream/3 wrap the Phase-42 helpers (part_number ignored; %{offset: n} return; atomic rename completion).
- TusPlug dispatches PATCH + completion polymorphically through the adapter (no Local hard-wiring); persists multipart_upload_id+multipart_parts between PATCHes.
- Completion converges into the byte-for-byte UNCHANGED verify_completion/2 (D-08).
- Plan 01 RED Plug dispatch test GREEN; existing Local tus tests stay GREEN.
</success_criteria>

<output>
Create `.planning/phases/43-s3-multipart-backing-minio-proof/43-04-SUMMARY.md` when done
</output>

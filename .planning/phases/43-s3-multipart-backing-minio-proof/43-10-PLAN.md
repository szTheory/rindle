---
phase: 43-s3-multipart-backing-minio-proof
plan: 10
type: execute
wave: 4
depends_on: ["43-09", "43-08"]
files_modified:
  - test/rindle/upload/tus_s3_integration_test.exs
autonomous: true
gap_closure: true
requirements: [TUS-09]

must_haves:
  truths:
    - "Against MinIO, a tus DELETE on an S3-backed session leaves ZERO orphaned multipart uploads (list_multipart_uploads returns no entry for the deleted key)"
    - "After a reap, the on-disk tus/*.tail file for the abandoned session is gone (proving CR-02 + CR-03 cleanup actually fires end-to-end)"
  artifacts:
    - path: "test/rindle/upload/tus_s3_integration_test.exs"
      provides: "MinIO @tag :minio cases for the DELETE-then-empty path and the post-reap tail-gone assertion"
      contains: "list_multipart_uploads"
  key_links:
    - from: "test/rindle/upload/tus_s3_integration_test.exs DELETE case"
      to: "ExAws.S3.list_multipart_uploads"
      via: "assert no upload entry remains for the DELETEd key"
      pattern: "list_multipart_uploads"
    - from: "test/rindle/upload/tus_s3_integration_test.exs post-reap case"
      to: "S3.tus_tail_path"
      via: "assert the tail file is gone after reaping"
      pattern: "tus_tail_path|\\.tail"
---

<objective>
Close **SC5 / IN-04**: the existing MinIO integration proof
(`tus_s3_integration_test.exs`) covers only the timeout-expiry reaper path on a
single node, giving false confidence that the cost leak is closed. Extend it with
the two cases the gap fixes make passable:

- **DELETE-then-zero-leak (CR-01 proof):** create + PATCH a tus S3 session against
  MinIO, DELETE it, then assert `list_multipart_uploads` returns NO entry for that
  upload key — proving the DELETE path now aborts the S3 multipart (not just the
  timeout path).
- **Post-reap tail-gone (CR-02 + CR-03 proof):** after an abandoned session is
  reaped, assert the on-disk `tus/<encoded id>.tail` file (at `S3.tus_tail_path/2`)
  no longer exists — proving the reaper's tail cleanup actually targets and removes
  the real file end-to-end.

Keep both as `@tag :minio` (CI-only, excluded from the default suite). The new
cases MUST compile cleanly even without MinIO configured.

Purpose: Depends on 43-09 (the DELETE abort must work) and 43-08 (the encoding-
correct tail removal). This is the headline SC5 zero-leak proof that now covers the
DELETE path the verification flagged as a BLOCKER.

Output: Two new MinIO integration cases proving zero-leak on the DELETE path and
real tail-file removal after a reap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-s3-multipart-backing-minio-proof/43-VERIFICATION.md
@.planning/phases/43-s3-multipart-backing-minio-proof/43-VALIDATION.md
@.planning/phases/43-s3-multipart-backing-minio-proof/43-REVIEW.md

<interfaces>
<!-- Existing harness in tus_s3_integration_test.exs to reuse, not rebuild. -->
  @moduletag :minio
  @secret_key_base "tus-s3-integration-secret-key-base-0123456789abcdef"
  defmodule MinioTusProfile (storage: Rindle.Storage.S3, max_bytes: 2_147_483_648)
  setup -> {:ok, bucket: bucket}   # configures :ex_aws :s3 from RINDLE_MINIO_* env
  defp aws_config() -> Application.get_env(:ex_aws, :s3, [])
  defp opts_for() -> TusPlug.init(profile: MinioTusProfile, secret_key_base:, max_size:)
  defp create(opts, length) -> {token, sid}
  defp patch(opts, token, offset, body) -> conn
  defp head(opts, token) -> conn
  defp synthetic_bytes(size) -> lazy stream (no whole-body binary in test memory)
  defp repo() -> Rindle.Config.repo()
  # Existing zero-leak pattern (timeout path) at line ~189-208:
  #   {:ok, %{body: %{uploads: uploads}}} = ExAws.S3.list_multipart_uploads(bucket) |> ExAws.request(aws_config())
  #   refute Enum.any?(uploads, fn u -> u.key == abandoned_key end)

<!-- DELETE through the Plug (mirror the create/patch helpers' conn shape). -->
From lib/rindle/upload/tus_plug.ex: DELETE is conn(:delete, "/uploads/tus/" <> token) |> TusPlug.call(opts)
From lib/rindle/storage/s3.ex (43-06): S3.tus_tail_path(session_id_or_key, opts) -> on-disk tail path
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MinIO DELETE-then-zero-leak case (CR-01 SC5 proof)</name>
  <files>test/rindle/upload/tus_s3_integration_test.exs</files>
  <read_first>
    - test/rindle/upload/tus_s3_integration_test.exs (full file — the @moduletag :minio harness, create/patch/head helpers line ~106-137, the existing timeout-path zero-leak assertion line ~189-208 to mirror, aws_config/0 line ~93-96, repo/0 line ~214)
    - lib/rindle/upload/tus_plug.ex (handle_delete/2 as fixed by 43-09 — confirm DELETE returns 204 and aborts the multipart so the list assertion will be empty)
    - .planning/phases/43-s3-multipart-backing-minio-proof/43-VALIDATION.md (TUS-09 integration row line ~50 — the ≥1 GiB / list_multipart_uploads-empty contract; the MinIO proof command line ~24)
  </read_first>
  <action>
    Add a new `@tag :minio` test "a tus DELETE on an S3-backed session leaves zero
    multipart leak". Steps, reusing the existing helpers: (1) opts = opts_for(); (2)
    create a session and PATCH at least one part (use a body >= 5 MiB via
    synthetic_bytes so a real UploadPart crosses the wire and multipart_upload_id is
    persisted — assert it is a binary on the row); (3) capture the upload key from
    the session row; (4) DELETE via conn(:delete, "/uploads/tus/" <> token) |>
    TusPlug.call(opts) and assert status 204 and session state == "aborted"; (5)
    assert ExAws.S3.list_multipart_uploads(bucket) |> ExAws.request(aws_config())
    returns NO entry for that key (refute Enum.any?, mirroring the timeout-path
    assertion). Do NOT need >= 1 GiB for this case — a single >= 5 MiB part is enough
    to prove a live multipart exists then is aborted by DELETE (keep the runtime
    bounded; the ≥1 GiB drop+resume is already covered by the existing test). Reuse
    the existing setup/teardown; do not duplicate the MinIO env wiring.
  </action>
  <verify>
    <automated>mix test test/rindle/upload/tus_s3_integration_test.exs --include minio</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/rindle/upload/tus_s3_integration_test.exs` (WITHOUT --include minio) still excludes the file and exits 0 (tag exclusion intact)
    - `mix compile --warnings-as-errors` succeeds (the new case compiles even without MinIO)
    - When run with `--include minio` against a configured MinIO, the DELETE case asserts list_multipart_uploads has no entry for the deleted key (refute Enum.any?)
    - The case DELETEs through TusPlug.call (the real handler), not a direct abort_multipart_upload call — proving the DELETE PATH itself is leak-free
  </acceptance_criteria>
  <done>The MinIO proof now covers the DELETE termination path with a zero-leak (list_multipart_uploads empty) assertion — the SC5 gap for the DELETE path is closed.</done>
</task>

<task type="auto">
  <name>Task 2: MinIO post-reap tail-gone case (CR-02 + CR-03 SC5 proof)</name>
  <files>test/rindle/upload/tus_s3_integration_test.exs</files>
  <read_first>
    - test/rindle/upload/tus_s3_integration_test.exs (the abandoned-session + reaper flow line ~189-208 to extend; synthetic_bytes/1; repo/0)
    - lib/rindle/storage/s3.ex (tus_tail_path/2 from 43-06 — to compute the expected on-disk tail location for the assertion)
    - lib/rindle/ops/upload_maintenance.ex (remove_tus_tail/1 as fixed by 43-08 — the reaper now deletes the real tail path; this test proves it end-to-end)
  </read_first>
  <action>
    Add a new `@tag :minio` test "after reaping an abandoned tus session, the on-disk
    tail file is removed". Steps: (1) create + PATCH a session with a body that
    leaves a sub-5-MiB remainder buffered in the tail (e.g. a PATCH whose size mod
    5 MiB is non-zero, or any < 5 MiB final remainder) so a tail file actually
    exists on disk; (2) compute the expected tail path via S3.tus_tail_path(<key or
    session_id>, root: <root used by the Plug/adapter>) and assert File.exists? is
    true before reaping (sanity: the tail was written); (3) force the session past
    its TTL and run UploadMaintenance.abort_incomplete_uploads([]) (mirror the
    existing timeout-reaper invocation); (4) assert File.exists? on the tail path is
    now false (CR-02 path-encoding fix made it deletable, CR-03 backstop covers
    residue). Determine the root the adapter used for the tail: the Plug threads
    opts[:root]; if opts_for() leaves root nil, the adapter falls back to
    Rindle.AV.TempRunDir.root_dir() — compute tus_tail_path with the SAME root the
    PATCH used so the assertion targets the real file (read the Plug's call_opts/root
    handling to get this right; do NOT hardcode a divergent path — use
    S3.tus_tail_path as the single source of truth).
  </action>
  <verify>
    <automated>mix test test/rindle/upload/tus_s3_integration_test.exs --include minio</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "tus_tail_path\|\\.tail" test/rindle/upload/tus_s3_integration_test.exs` shows the assertion computes the tail path via S3.tus_tail_path (source of truth), not a hardcoded raw-UUID path
    - The test asserts File.exists?(tail_path) is true BEFORE the reap and false AFTER (proving the reaper actually removed the real file)
    - `mix test test/rindle/upload/tus_s3_integration_test.exs` without --include minio still exits 0 (excluded)
    - `mix compile --warnings-as-errors` succeeds
  </acceptance_criteria>
  <done>The MinIO proof asserts the abandoned session's tail file is gone after reaping, proving CR-02 (correct path) + CR-03 (cleanup) work end-to-end against real storage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → MinIO | the integration proof exercises the real S3 multipart + list APIs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-10-01 | Information disclosure (cost leak) | DELETE-path multipart leak | mitigate | The new MinIO case asserts list_multipart_uploads is empty after a DELETE, providing the executable proof that the cost leak is closed on the DELETE path (not just timeout). |
| T-43-10-02 | Denial of service | residual tail-file accumulation | mitigate | The post-reap tail-gone case proves the on-disk tail is actually removed end-to-end (CR-02 + CR-03), closing the disk-fill leak in a real-storage proof. |
| T-43-10-03 | Tampering | MinIO test credentials in CI | accept | The harness reads RINDLE_MINIO_* env (existing convention); no new secret surface is introduced; @tag :minio keeps these out of the default suite. |
</threat_model>

<verification>
- `mix test test/rindle/upload/tus_s3_integration_test.exs` (default) excludes the file → exits 0.
- `mix compile --warnings-as-errors` succeeds.
- With `--include minio` against MinIO: the DELETE case asserts zero multipart leak; the reap case asserts the tail file is gone.
- Both assertions compute paths/keys via the adapter's own helpers (no duplicated/divergent path math).
</verification>

<success_criteria>
- SC5 now covers the DELETE termination path (CR-01) with a list_multipart_uploads-empty assertion.
- The post-reap tail-gone case proves CR-02 + CR-03 end-to-end against real storage.
- Both cases stay @tag :minio (CI-only) and compile without MinIO.
- The existing ≥1 GiB drop+resume + timeout-path zero-leak test is untouched.
</success_criteria>

<output>
Create `.planning/phases/43-s3-multipart-backing-minio-proof/43-10-SUMMARY.md` when done
</output>

---
phase: 43-s3-multipart-backing-minio-proof
plan: 02
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - lib/rindle/storage/s3.ex
autonomous: true
requirements: [TUS-06, TUS-07]
must_haves:
  truths:
    - "S3.upload_part_stream/5 buffers a sub-5-MiB PATCH tail on disk and emits one UploadPart per accumulated >= 5 MiB"
    - "The accumulated part ETag is extracted from the UploadPart response HEADERS (no body parser)"
    - "S3.complete_part_stream/3 flushes the remaining tail as the final part then calls complete_multipart_upload/4"
    - "S3.capabilities/0 advertises :tus_upload"
    - "bucket and aws_config resolve via app-env fallback when opts omit them (Pitfall 4)"
  artifacts:
    - path: "lib/rindle/storage/s3.ex"
      provides: "upload_part_stream/5 + complete_part_stream/3 impls, etag_from_headers helper, tail-buffer logic, :tus_upload capability"
      contains: "upload_part_stream"
  key_links:
    - from: "lib/rindle/storage/s3.ex"
      to: "ExAws.S3.upload_part/6"
      via: "request(ExAws.S3.upload_part(...), opts)"
      pattern: "upload_part"
    - from: "lib/rindle/storage/s3.ex"
      to: "response.headers etag"
      via: "etag_from_headers/1 lowercase-map normalize"
      pattern: "downcase"
---

<objective>
Implement the S3 half of TUS-06 and the one-line TUS-07 capability edit. The S3 adapter gains `upload_part_stream/5` (tail-buffer + one `UploadPart` per >= 5 MiB), `complete_part_stream/3` (flush tail as final part + `complete_multipart_upload/4`), the `etag_from_headers/1` Pitfall-2 fix, and `:tus_upload` in `capabilities/0`.

LOCKED decisions inherited from Plan 01:
- **Temp-path variant**: `upload_part_stream/5` arg 2 is a temp file path holding one PATCH worth of bytes; the adapter streams from it into the tail buffer at `Rindle.tmp/tus/<session_id>.tail`, never accumulating the body in a binary (anti-pattern, RESEARCH lines 282).
- **Trust persisted state on resume** (RESEARCH Open Question 3): no defensive `list_parts` — `last_known_offset` is only advanced after a part ETag is persisted, so it never overstates committed bytes. v1 trusts `multipart_parts`.
- **App-env opts fallback** (Pitfall 4 / RESEARCH Open Question 2): reuse the existing `bucket/1` opts-or-app-env resolver (s3.ex:173-178) and `request/2`'s `aws_config` fallback (s3.ex:180-184). No new opts plumbing in the adapter.

Purpose: This is the genuinely-new code of Phase 43 — the bytes->part translation the Plug dispatches into. Everything else is wiring.
Output: a fully-implemented S3 tus sink that turns the Plan 01 RED unit tests GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-s3-multipart-backing-minio-proof/43-RESEARCH.md
@.planning/phases/43-s3-multipart-backing-minio-proof/43-PATTERNS.md

<interfaces>
<!-- Existing S3 adapter spine the new impl reuses. Extracted from lib/rindle/storage/s3.ex. -->

The adapter call spine (every callback uses `with {:ok, bucket} <- bucket(opts), {:ok, resp} <- request(op, opts)`):
```elixir
# s3.ex:105-117 — complete_multipart_upload/4 (the analog for complete_part_stream)
def complete_multipart_upload(key, upload_id, parts, opts) do
  with {:ok, bucket} <- bucket(opts),
       {:ok, %{body: body}} <-
         request(S3.complete_multipart_upload(bucket, key, upload_id, normalize_parts(parts)), opts) do
    {:ok, Map.merge(%{upload_id: upload_id, upload_key: key, bucket: bucket}, body)}
  else
    {:error, reason} -> {:error, reason}
  end
end
```

Reusable private helpers already in s3.ex:
- `bucket(opts)` (s3.ex:173-178): `Keyword.get(opts, :bucket) || Application.get_env(:rindle, __MODULE__, [])[:bucket]` -> `{:ok, b} | {:error, :missing_bucket}` — the Pitfall-4 opts-flow lever.
- `request(operation, opts)` (s3.ex:180-184): `ExAws.request(operation, Keyword.get(opts, :aws_config, []))` rescued to `{:error, exception}`.
- `normalize_parts(parts)` (s3.ex:165-171): accepts `%{part_number:, etag:}` maps AND `{n, etag}` tuples — feed persisted `multipart_parts` straight in.
- Header-normalize idiom to MIRROR (s3.ex:136-138, `handle_head_response`):
  ```elixir
  Enum.into(headers, %{}, fn {k, v} -> {String.downcase(k), v} end) |> Map.get("etag")
  ```

ExAws.S3 ops (verified deps/ex_aws_s3/lib/ex_aws/s3.ex):
- `S3.upload_part(bucket, object, upload_id, part_number, body, opts \\ [])` @1811 — NO body parser; ETag in HTTP response HEADERS only (Pitfall 2).
- `S3.complete_multipart_upload(bucket, object, upload_id, parts)` @1903 — parts :: [{part_number, etag}].

Tail-buffer constants/paths (RESEARCH Pattern 2):
- `@s3_min_part_size 5 * 1024 * 1024`
- tail path: `Path.join([Rindle.AV.TempRunDir.root_dir(), "tus", session_id <> ".tail"])` (TempRunDir.root_dir/0 at temp_run_dir.ex:26).

The @type/@callback contracts to implement (declared in Plan 01, storage.ex):
- `upload_part_stream(key, temp_path, base_offset, state, opts) :: {:ok, tus_part_state} | {:error, term}`
- `complete_part_stream(key, state, opts) :: {:ok, map} | {:error, term}`
  NOTE: the storage.ex @callback for complete_part_stream is arity 3 (key, state, opts). Implement S3.complete_part_stream/3 to match.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement S3.upload_part_stream/5 (tail-buffer + UploadPart + ETag-from-headers)</name>
  <files>lib/rindle/storage/s3.ex</files>
  <read_first>
    - lib/rindle/storage/s3.ex (FULL — the call spine s3.ex:105-127, header-normalize s3.ex:136-144, bucket/1 s3.ex:173-178, request/2 s3.ex:180-184, normalize_parts/1 s3.ex:165-171, capabilities/0 s3.ex:151-152)
    - lib/rindle/av/temp_run_dir.ex (root_dir/0 at temp_run_dir.ex:26)
    - .planning/phases/43-s3-multipart-backing-minio-proof/43-RESEARCH.md §Pattern 2 (lines 234-250), §Code Examples (lines 350-368 — etag_from_headers + upload_one_part), §Pitfall 2 (lines 324-328), §Pitfall 3 (lines 330-334)
    - .planning/phases/43-s3-multipart-backing-minio-proof/43-PATTERNS.md §`lib/rindle/storage/s3.ex` (lines 47-90)
    - test/rindle/storage/s3_tus_test.exs (the RED unit tests this task turns GREEN — read to learn the exact helper shape they call)
  </read_first>
  <action>
    Add `@s3_min_part_size 5 * 1024 * 1024` module attr. Implement `@impl true def upload_part_stream(key, temp_path, base_offset, state, opts)`:
    1. Resolve the tail path under `Rindle.AV.TempRunDir.root_dir/0` (`<root>/tus/<session_id>.tail`); derive session_id from opts (the Plug passes `:session_id` in opts) — `File.mkdir_p` the dirname.
    2. Append the temp_path bytes onto the tail file (stream the temp file in via `File.stream!`/`IO.binwrite` — never read the whole thing into a binary; Pitfall 3 anti-pattern).
    3. While the tail file size >= `@s3_min_part_size`: slice off exactly one 5 MiB part, `request(ExAws.S3.upload_part(bucket, key, upload_id, part_number, body), opts)`, extract the ETag via `etag_from_headers/1`, append `%{part_number: n, etag: etag}` to `state.parts`, increment part_number, rewrite the tail file to hold only the leftover.
    4. The S3 `upload_id` comes from `state[:upload_id]`; if absent (first PATCH), `request(ExAws.S3.initiate_multipart_upload(bucket, key), opts)` and seed it into the returned state (mirror initiate usage at s3.ex:75).
    5. Return `{:ok, %{offset: base_offset + bytes_written, upload_id: upload_id, parts: parts}}`. On any `{:error, reason}` propagate it.
    Add the `etag_from_headers/1` private helper byte-identical to the RESEARCH §Code Example (lowercase-map normalize, `Map.get("etag")`), returning `{:error, :missing_etag}` upstream when nil (Pitfall 2). part_number MUST be strictly increasing and 1-based (S3 reassembles by part_number, NOT arrival — RESEARCH line 250).
    If the Plan 01 unit tests call a PURE buffering helper, expose it as a named private function the test can reach (or make the math reachable) — match whatever shape s3_tus_test.exs expects.
  </action>
  <verify>
    <automated>mix test test/rindle/storage/s3_tus_test.exs -x</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `mix test test/rindle/storage/s3_tus_test.exs` GREEN (tail-buffer slice/accumulate math passes; sub-5-MiB PATCH produces zero parts; >= 5 MiB slices exactly one part; part_numbers strictly increasing).
    - Source assertion: `grep -c 'String.downcase' lib/rindle/storage/s3.ex` >= 2 (etag_from_headers added alongside handle_head_response).
    - Source assertion: `grep -c '@s3_min_part_size' lib/rindle/storage/s3.ex` >= 1.
    - Source assertion: `grep -c 'ExAws.S3.upload_part\|S3.upload_part' lib/rindle/storage/s3.ex` >= 1.
    - Behavior assertion: `mix compile --warnings-as-errors` clean.
  </acceptance_criteria>
  <done>S3.upload_part_stream/5 implemented with disk tail-buffer, one UploadPart per >= 5 MiB, ETag from headers; Plan 01 tail-buffer unit tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement S3.complete_part_stream/3 + advertise :tus_upload (TUS-06 completion, TUS-07)</name>
  <files>lib/rindle/storage/s3.ex</files>
  <read_first>
    - lib/rindle/storage/s3.ex (complete_multipart_upload/4 at s3.ex:105-117 — the direct analog; normalize_parts/1 at s3.ex:165-171; capabilities/0 at s3.ex:151-152)
    - test/rindle/storage/storage_adapter_test.exs (the S3-cap assertion at storage_adapter_test.exs:100, turned GREEN by this task)
    - .planning/phases/43-s3-multipart-backing-minio-proof/43-RESEARCH.md §Pattern 2 final-part flush (lines 247-249), §Pitfall 5 (lines 342-346)
    - .planning/phases/43-s3-multipart-backing-minio-proof/43-PATTERNS.md §Capability advertisement (lines 83-88)
  </read_first>
  <action>
    Implement `@impl true def complete_part_stream(key, state, opts)`:
    1. Resolve the tail path from opts `:session_id`; if the tail file is non-empty, upload it as the FINAL part (any size allowed for the last part — RESEARCH line 248): `request(ExAws.S3.upload_part(bucket, key, upload_id, final_part_number, tail_body), opts)`, ETag via `etag_from_headers/1`, append to parts.
    2. Call `request(ExAws.S3.complete_multipart_upload(bucket, key, upload_id, normalize_parts(parts)), opts)` (reuse normalize_parts — it accepts the persisted `%{part_number:, etag:}` map shape directly).
    3. Best-effort `File.rm(tail_path)` cleanup.
    4. Return `{:ok, %{upload_id: upload_id, upload_key: key, bucket: bucket}}` merged with the parsed completion body (mirror complete_multipart_upload/4 return). `upload_id`/`parts` come from `state`.
    Edit `capabilities/0` (s3.ex:152) to append `:tus_upload`: `[:presigned_put, :head, :signed_url, :multipart_upload, :tus_upload]` — capability honesty (D-09): only now that both callbacks exist.
  </action>
  <verify>
    <automated>mix test test/rindle/storage/storage_adapter_test.exs test/rindle/storage/s3_tus_test.exs -x</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `mix test test/rindle/storage/storage_adapter_test.exs` GREEN — `:tus_upload in S3.capabilities()` and `{upload_part_stream,5}`/`{complete_part_stream,4}` in optional_callbacks assertions pass.
    - Source assertion: `grep -q ':tus_upload' lib/rindle/storage/s3.ex` and the capabilities list line contains `:tus_upload`.
    - Source assertion: `grep -c 'complete_part_stream' lib/rindle/storage/s3.ex` >= 1.
    - Behavior assertion: `mix test test/rindle/storage/s3_tus_test.exs` GREEN including the completion-flush test.
  </acceptance_criteria>
  <done>S3.complete_part_stream/3 flushes the tail as the final part and completes the multipart upload; S3 advertises :tus_upload; storage_adapter_test GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| temp file (PATCH bytes) -> tail buffer -> S3 UploadPart | Untrusted client bytes; bounded upstream by the Plug's per-PATCH ceiling -> 413 |
| S3 UploadPart response headers -> persisted ETag | The ETag is server-issued by S3, then persisted as part identity — never client-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-03 | Denial of Service (memory) | upload_part_stream/5 tail buffer | mitigate | Tail spilled to DISK (`Rindle.tmp/tus/<session_id>.tail`); body streamed from temp_path, never accumulated in a binary (RESEARCH anti-pattern line 282); 5 MiB slice cap bounds in-flight buffer |
| T-43-04 | Tampering | ETag/part assembly | mitigate | ETag read from S3 response headers (server-issued), persisted with explicit 1-based strictly-increasing part_number; `complete_multipart_upload` validates ETags S3-side; head re-sniff (verify lane) checks size at completion |
| T-43-05 | Information Disclosure | aws_config / S3 creds | mitigate | `aws_config` resolved via `request/2` opts/app-env, never logged; no credential printed in adapter return maps |
| T-43-06 | Tampering | tail-buffer path traversal | mitigate | Tail path keyed on server-issued `session_id` (UUID) under `Rindle.tmp/` — structurally traversal-proof (same guard as `Local.tus_part_path/2`) |
| T-43-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in Phase 43 (RESEARCH §Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `mix test test/rindle/storage/` GREEN (non-MinIO surface): s3_tus_test + storage_adapter_test pass.
- `mix compile --warnings-as-errors` clean.
- `git diff lib/rindle/upload/broker.ex` shows NO change (this plan does not touch broker — D-08 preserved).
- No new column / no migration: `git status priv/repo/migrations/` shows no new file.
</verification>

<success_criteria>
- S3.upload_part_stream/5 + complete_part_stream/3 implemented with disk tail-buffer and ETag-from-headers.
- S3.capabilities/0 advertises :tus_upload.
- Plan 01 RED unit tests (s3_tus_test, storage_adapter_test S3-cap) are GREEN.
- No memory accumulation of the body; tail spilled to Rindle.tmp/tus/.
</success_criteria>

<output>
Create `.planning/phases/43-s3-multipart-backing-minio-proof/43-02-SUMMARY.md` when done
</output>

---
phase: 126-curated-type-ratchet
plan: "09"
type: execute
wave: 9
depends_on: ["126-08"]
files_modified:
  - .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md
autonomous: true
requirements: [TYPE-01, TYPE-02, SAFE-01]
must_haves:
  truths:
    - "The final candidate passes policy, focused owner proof, SAFE-01, mix ci, and repository hygiene with no forbidden-surface drift."
    - "The same immutable candidate SHA has successful supported Nightly Dialyzer, Nightly Summary, and PR CI Summary receipts."
    - "A sanitized GitHub issue #76 comment is the immutable final receipt linking the exact-head authorities and conditional issue outcome."
    - "Issue #76 closes only when all 45 dispositions qualify and both exact-head authorities pass; otherwise it remains open with one narrowing comment."
  artifacts:
    - path: .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md
      provides: "committed pre-authority 45-entry ledger, retained-noise evidence, local gates, and candidate SHA"
    - path: .dialyzer_ignore.exs
      provides: "final candidate baseline with only explicitly evidenced analyzer noise, if any"
    - path: "GitHub issue #76 comment"
      provides: "immutable external final receipt for exact-head Nightly, PR CI Summary, and issue outcome"
  key_links:
    - from: .dialyzer_ignore.exs
      to: .github/workflows/nightly.yml
      via: "hashed PLT input consumed by gating Dialyzer job"
      pattern: "hashFiles('mix.exs', 'mix.lock', '.dialyzer_ignore.exs')"
    - from: "GitHub issue #76 comment"
      to: "Nightly run, Dialyzer job, Nightly Summary, and PR CI Summary"
      via: "sanitized links bound to the same candidate SHA"
      pattern: "TYPE-01|TYPE-02"
---

<objective>
Prove the final curated candidate locally and on both exact-head GitHub authorities, then disposition issue #76 without mutating the proven head.

Purpose: Convert owner-level work into an immutable external acceptance receipt while preserving the green-main release train.
Output: A committed pre-authority ledger, exact-head Nightly/PR results, and a sanitized issue comment that closes or narrows #76.
</objective>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- Final pre-authority `126-TYPE-EVIDENCE.md` with 45 dispositions, retained-noise evidence, local gate results, and the candidate SHA.
- Sanitized external issue #76 comment linking exact-head Nightly/Dialyzer/Summary and PR CI Summary receipts; no repository edit follows those authorities.
- Final curated `.dialyzer_ignore.exs`; zero entries are not required, but every remainder is exact and justified.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RUNNING.md
@guides/release_publish.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/126-curated-type-ratchet/126-RESEARCH.md
@.planning/phases/126-curated-type-ratchet/126-PATTERNS.md
@.planning/phases/126-curated-type-ratchet/126-VALIDATION.md
@.planning/phases/126-curated-type-ratchet/126-08-SUMMARY.md
@.planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md
@.dialyzer_ignore.exs
@mix.exs
@.github/workflows/nightly.yml
@test/install_smoke/ci_lane_split_test.exs
@test/install_smoke/ci_cache_hygiene_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close local policy, preservation, CI, hygiene, and scope gates</name>
  <read_first>.planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md, .dialyzer_ignore.exs, mix.exs, .github/workflows/nightly.yml, test/install_smoke/dialyzer_ignore_policy_test.exs, test/install_smoke/ci_lane_split_test.exs, test/install_smoke/ci_cache_hygiene_test.exs, scripts/maintainer/refactor_contract.sh, guides/release_publish.md, RUNNING.md</read_first>
  <files>.planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</files>
  <action>Verify all 45 starting rows have exactly one disposition and reject pending/obsolete-retained/actionable-retained states, broad filters, duplicates, missing owners, or retained rows lacking supported warning and owner evidence. Run the policy/topology/cache tests, every focused command in the validation map, SAFE-01, `mix ci`, and repository hygiene. Compare against `implementation_base_sha` and allow only enumerated owner source/tests, `.dialyzer_ignore.exs`, policy/evidence, summaries, and phase metadata. Fail on dependency, lockfile, workflow/topology/cache/job/schedule, release-policy, schema/migration, public API/docs, telemetry, error, or Admin drift. Record local results and candidate SHA in the ledger, commit it, and leave the working tree clean. The ledger is the pre-authority record; do not promise or append final GitHub authority links to it after the candidate commit.</action>
  <verify><automated>set -e; MIX_ENV=test mix test test/install_smoke/dialyzer_ignore_policy_test.exs test/install_smoke/ci_lane_split_test.exs test/install_smoke/ci_cache_hygiene_test.exs --seed 0; bash scripts/maintainer/refactor_contract.sh; mix ci; ./scripts/maintainer/repo_hygiene_check.sh --ci; ! rg -n '\| pending \|' .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</automated></verify>
  <acceptance_criteria>All local/structural gates pass on a clean committed candidate, 45 dispositions are complete, retained rows are supported and behavior-backed, and forbidden surfaces are unchanged.</acceptance_criteria>
  <done>The immutable candidate SHA is locally green and auditable before external acceptance.</done>
</task>

<task type="auto">
  <name>Task 2: Obtain exact-head authorities and publish the immutable external #76 receipt</name>
  <precondition>The candidate ledger is committed, the working tree is clean, `gh auth status` succeeds, and the pushed PR head can be read as the same local candidate SHA before any receipt is reused or dispatched.</precondition>
  <read_first>.planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md, .github/workflows/nightly.yml, test/install_smoke/ci_lane_split_test.exs, guides/release_publish.md, RUNNING.md</read_first>
  <files>None</files>
  <action>Push the Phase 126 branch and create or update its PR to `main` without merging. Capture candidate HEAD and require the PR `headRefOid` to match. Capture the exact pull_request `ci.yml` run by branch+event+headSha, verified-reusing one terminal receipt or polling the unique new run; require overall success and the exact `CI Summary` job success on that head. For Nightly, follow `126-PATTERNS.md`: verified-reuse a single terminal exact-head workflow_dispatch only after checking head/event/named jobs, otherwise timestamp immediately before `gh workflow run nightly.yml --ref &lt;branch&gt;`, fail if post-timestamp capture is ambiguous, and poll the captured database ID to terminal. Require overall Nightly success, `Dialyzer` success, and `Nightly Summary` success; inspect the exact Summary log and require its Dialyzer row to report success under literal Elixir 1.17 / OTP 27 and `mix dialyzer --format github`. Recheck all 45 dispositions. Post one concise sanitized issue #76 comment containing candidate SHA, retained count/rationales, Nightly run, Dialyzer job, Nightly Summary, PR, and CI Summary links. Close #76 only if every acceptance condition passes; otherwise name the unmet condition and leave it open. Make no repository change, merge, publish, topology edit, release, or alternate-head retry after the authorities run; the issue comment/PR receipt is the immutable final evidence.</action>
  <verify><automated>set -e; CANDIDATE_HEAD=$(git rev-parse HEAD); test -z "$(git status --porcelain)"; PR_JSON=$(gh pr view --json headRefOid,headRefName); test "$CANDIDATE_HEAD" = "$(printf '%s' "$PR_JSON" | jq -r .headRefOid)"; BRANCH=$(printf '%s' "$PR_JSON" | jq -r .headRefName); REPO=$(gh repo view --json nameWithOwner -q .nameWithOwner); CI_RUN_ID=$(gh api --method GET --paginate --slurp "repos/$REPO/actions/workflows/ci.yml/runs" -f branch="$BRANCH" -f event=pull_request -f per_page=100 | jq -er --arg sha "$CANDIDATE_HEAD" '[.[].workflow_runs[] | select(.head_sha == $sha and .event == "pull_request" and .status == "completed")] | if length == 1 then .[0].id else error("expected exactly one terminal exact-head PR CI receipt") end'); CI_JSON=$(gh run view "$CI_RUN_ID" --json headSha,event,status,conclusion,jobs); test "$(printf '%s' "$CI_JSON" | jq -r .headSha)" = "$CANDIDATE_HEAD"; test "$(printf '%s' "$CI_JSON" | jq -r .event)" = pull_request; test "$(printf '%s' "$CI_JSON" | jq -r .status)" = completed; test "$(printf '%s' "$CI_JSON" | jq -r .conclusion)" = success; test "$(printf '%s' "$CI_JSON" | jq '[.jobs[] | select(.name == "CI Summary" and .conclusion == "success")] | length')" -eq 1; RUN_ID=$(gh api --method GET --paginate --slurp "repos/$REPO/actions/workflows/nightly.yml/runs" -f branch="$BRANCH" -f event=workflow_dispatch -f per_page=100 | jq -er --arg sha "$CANDIDATE_HEAD" '[.[].workflow_runs[] | select(.head_sha == $sha and .event == "workflow_dispatch" and .status == "completed")] | if length == 1 then .[0].id else error("expected exactly one terminal exact-head Nightly receipt") end'); RUN_JSON=$(gh run view "$RUN_ID" --json headSha,event,status,conclusion,jobs); test "$(printf '%s' "$RUN_JSON" | jq -r .headSha)" = "$CANDIDATE_HEAD"; test "$(printf '%s' "$RUN_JSON" | jq -r .event)" = workflow_dispatch; test "$(printf '%s' "$RUN_JSON" | jq -r .status)" = completed; test "$(printf '%s' "$RUN_JSON" | jq -r .conclusion)" = success; DIALYZER_JOB=$(printf '%s' "$RUN_JSON" | jq -er '.jobs[] | select(.name == "Dialyzer" and .conclusion == "success") | .databaseId'); SUMMARY_JOB=$(printf '%s' "$RUN_JSON" | jq -er '.jobs[] | select(.name == "Nightly Summary" and .conclusion == "success") | .databaseId'); gh run view "$RUN_ID" --job "$SUMMARY_JOB" --log | grep -Fq '| Dialyzer | success |'; ISSUE_STATE=$(gh issue view 76 --json state -q .state); LAST_COMMENT=$(gh issue view 76 --json comments -q '.comments[-1].body'); printf '%s' "$LAST_COMMENT" | grep -Fq "$CANDIDATE_HEAD"; printf '%s' "$LAST_COMMENT" | grep -Fq "actions/runs/$RUN_ID"; if ! rg -q '\| (pending|obsolete-retained|actionable-retained) \|' .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md; then test "$ISSUE_STATE" = CLOSED; else test "$ISSUE_STATE" = OPEN; fi; test "$CANDIDATE_HEAD" = "$(git rev-parse HEAD)"; test -z "$(git status --porcelain)"</automated></verify>
  <acceptance_criteria>Nightly Dialyzer, Nightly Summary, and PR CI Summary pass on the same unchanged candidate SHA; the sanitized issue comment links them; #76 state follows the complete predicate.</acceptance_criteria>
  <done>TYPE-01/TYPE-02 authority is immutable outside the repository and agrees with issue state without invalidating the proven head.</done>
</task>

</tasks>

<threat_model asvs_level="L1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local candidate → supported GitHub jobs | Only exact-head runs on the locked toolchain may authorize acceptance. |
| GitHub authority → issue #76 state | External issue mutation must follow every acceptance condition. |
| Phase diff → release train | Quality work must not alter checks, jobs, caches, schedules, dependencies, or release policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-126-02 | Spoofing / Repudiation | Nightly and PR receipts | high | mitigate | Candidate SHA equals PR head and workflow `headSha`; inspect named jobs, toolchain, command, conclusions, and links. |
| T-126-01 | Tampering | final ignore ledger | high | mitigate | 45 complete dispositions, policy test, and supported evidence for every retained exact filter. |
| T-126-09 | Elevation of privilege / Repudiation | issue #76 mutation | high | mitigate | Close only under the deterministic predicate; otherwise comment and assert open. |
| T-126-03 | Tampering | CI/release topology | high | mitigate | Explicit diff allowlist, topology/cache tests, `mix ci`, hygiene, and no post-authority repository edits. |
| T-126-SC | Tampering | package supply chain | low | accept | No dependency or package install is introduced. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---:|---|---|
| GOAL | — | Supported home cell has an actionable enforced Dialyzer baseline with no retirement ambiguity | 01-09 | COVERED | Policy tracer, per-slice supported probes, complete ledger, and immutable external final authority. |
| REQ | TYPE-01 | 1.17/27 passes after each retained ignore is justified or removed; local noise is diagnostic | 01-09 | COVERED | Every source slice probes and finalizes on Nightly; final exact-head success required. |
| REQ | TYPE-02 | Curated gate rejects new actionable findings and issue #76 closes with evidence | 01, 08-09 | COVERED | Permanent policy test, unchanged gating Nightly, external receipt, conditional issue disposition. |
| REQ | SAFE-01 | Preserve APIs, schemas/migrations, telemetry, errors, Admin, dependencies, CI/release | 02-09 | COVERED | Owner tests and SAFE-01 after every source task; final scope/mix-ci/hygiene/CI audits. |
| RESEARCH | authority | Literal Nightly Elixir 1.17 / OTP 27 Dialyzer job is sole acceptance authority | 01-09 | COVERED | Unsupported local Dialyzer never classifies entries. |
| RESEARCH | inventory | Reconcile 45 entries / 18 files / 8 atoms + 37 strict descriptions | 01-08 | COVERED | Immutable starting ledger and 45 complete dispositions. |
| RESEARCH | policy | Reject duplicates, broad/file-only filters, stale paths, and new atom debt | 01 | COVERED | Focused ExUnit policy tracer. |
| RESEARCH | migration/runtime/task/Admin | Fix truthful boundaries only with owner behavior and SAFE-01 | 02-04 | COVERED | Separate supported probes precede bounded corrections. |
| RESEARCH | storage | Treat stream/opaque findings as noise only after supported evidence and adapter proof | 05-06 | COVERED | GCS/Local and S3 are separate slices. |
| RESEARCH | Tus/Mux | Preserve starting `tus_plug` filter identities while correcting extracted `tus_creation`/`tus_stream` owners; preserve opacity, protocol, response/error, lifecycle, and telemetry contracts | 07 | COVERED | Exact focused creation/stream plus Mux proof and supported-green finalizer. |
| RESEARCH | historical atoms | Remove absent Phase-34 filters; retain only exact reproduced supported noise | 04, 08 | COVERED | Runtime/HTML/process and facade/Broker/promote are separate probes. |
| RESEARCH | CI boundary | Do not change topology, cache, jobs, schedules, dependencies, or required checks | 01-09 | COVERED | Structural tests and final diff/hygiene/PR audits. |
| RESEARCH | issue #76 | Close only after no obsolete/actionable filters and all retained noise is justified | 09 | COVERED | Immutable external close-or-narrow receipt. |
| CONTEXT | — | No CONTEXT.md; autonomous researched constraints apply | — | EXCLUDED | No D-NN decisions exist. |

Excluded, not gaps: public API/schema/migration/telemetry/error/Admin redesign, dependencies/toolchain upgrades, CI topology/cache/schedule changes, moving Dialyzer to PR CI, release-policy changes, schema push, forced release, and deferred product work.
</source_audit>

<verification>
Run every local gate and scope audit, require candidate SHA equality across branch/PR/Nightly, inspect Dialyzer/Nightly Summary/PR CI Summary, publish their links externally, and verify issue state follows the complete predicate without a repository mutation.
</verification>

<success_criteria>
- TYPE-01, TYPE-02, and inherited SAFE-01 have complete evidence.
- Every GOAL/REQ/RESEARCH source item is covered and no deferred/out-of-scope item is implemented.
- Any remaining ignore is exact, supported, adjacent-commented, and owner-tested.
- The immutable issue comment/PR receipt binds final authorities to the unchanged candidate SHA and #76 state.
</success_criteria>

<output>
Create `.planning/phases/126-curated-type-ratchet/126-09-SUMMARY.md` after external receipt completion; do not present that later planning summary commit as the candidate authority head.
</output>

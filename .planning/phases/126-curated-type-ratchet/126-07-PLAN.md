---
phase: 126-curated-type-ratchet
plan: "07"
type: execute
wave: 7
depends_on: ["126-06"]
files_modified:
  - .dialyzer_ignore.exs
  - .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md
  - lib/rindle/upload/tus_creation.ex
  - lib/rindle/upload/tus_stream.ex
  - lib/rindle/workers/mux_ingest_variant.ex
  - lib/rindle/workers/mux_sync_provider_asset.ex
  - test/rindle/upload/tus_plug_test.exs
  - test/rindle/storage/local_tus_test.exs
  - test/rindle/workers/mux_ingest_variant_test.exs
  - test/rindle/workers/mux_sync_provider_asset_test.exs
autonomous: true
requirements: [TYPE-01, TYPE-02, SAFE-01]
must_haves:
  truths:
    - "The three immutable starting Tus filter identities at `tus_plug.ex` are distinguished from the supported emitted owners: E38 text at `tus_creation.ex` and E39-E40 text at `tus_stream.ex`."
    - "Tus protocol/errors and both Mux workers' lifecycle, telemetry, redaction, and errors remain unchanged."
    - "Every retained Tus/Mux finding is exact, supported, and owner-tested."
  artifacts:
    - path: .dialyzer_ignore.exs
      provides: "curated Tus/Mux analyzer-noise remainder"
    - path: .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md
      provides: "Tus/Mux supported dispositions"
  key_links:
    - from: lib/rindle/upload/tus_creation.ex
      to: test/rindle/upload/tus_plug_test.exs
      via: "POST creation and concatenation flows exercise the extracted creation owner"
      pattern: "POST|Upload-Concat"
    - from: lib/rindle/upload/tus_stream.ex
      to: test/rindle/storage/local_tus_test.exs
      via: "PATCH stream offsets and part-state persistence exercise the extracted stream owner"
      pattern: "upload_part_stream"
    - from: lib/rindle/workers/mux_ingest_variant.ex
      to: test/rindle/workers/mux_ingest_variant_test.exs
      via: "Mux response/error lifecycle"
      pattern: "MuxIngestVariant"
---

<objective>
Reconcile the extracted Tus creation/stream findings and Mux response-pattern boundaries under their own supported-cell probe.

Purpose: Preserve opaque/provider contracts while separating them from facade and lifecycle-worker atoms.
Output: Opaque-safe corrections where actionable, exact retained evidence where necessary, and a supported-green slice.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/126-curated-type-ratchet/126-RESEARCH.md
@.planning/phases/126-curated-type-ratchet/126-PATTERNS.md
@.planning/phases/126-curated-type-ratchet/126-VALIDATION.md
@.planning/phases/126-curated-type-ratchet/126-06-SUMMARY.md
@.dialyzer_ignore.exs
@scripts/maintainer/refactor_contract.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe Tus and Mux filters on supported 1.17/27</name>
  <precondition>The Plan 126-06 final Nightly has no S3/earlier or unowned warning and its only emitted annotations are exact E38 at `tus_creation.ex` plus E39-E40 at `tus_stream.ex`, with Dialyzer failure recorded by Nightly Summary; `gh auth status` succeeds.</precondition>
  <read_first>.dialyzer_ignore.exs, lib/rindle/upload/tus_creation.ex, lib/rindle/upload/tus_stream.ex, lib/rindle/upload/tus_plug.ex, test/rindle/upload/tus_plug_test.exs, test/rindle/storage/local_tus_test.exs, lib/rindle/workers/mux_ingest_variant.ex, lib/rindle/workers/mux_sync_provider_asset.ex, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</read_first>
  <files>.dialyzer_ignore.exs, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</files>
  <action>Remove only the immutable E38-E40 tuples whose starting filter path is `lib/rindle/upload/tus_plug.ex` plus the Mux strict-description filters. Commit/push the source-unchanged probe and obtain a deterministic exact-head Nightly receipt under the shared receipt protocol in `126-PATTERNS.md`: reuse a completed exact-head workflow_dispatch only after checking its head/event and named-job authority, otherwise timestamp immediately before dispatch, require one post-timestamp exact-head run, and poll it to terminal without treating the expected Dialyzer failure as a shell failure. Record starting filter identity and supported emitted identity in separate ledger columns. The already-observed extraction maps E38 warning text to `lib/rindle/upload/tus_creation.ex` and E39-E40 warning text to `lib/rindle/upload/tus_stream.ex`; do not rewrite history to claim those were the starting tuple paths. Treat the original `tus_plug.ex` tuples as obsolete only if the exhaustive supported annotations contain no matching warning at that path, while the extracted creation/stream findings remain actionable candidates owned by this plan. No owner edit or filter restoration may precede this receipt; unsupported local analysis remains diagnostic.</action>
  <verify><automated>set -e; HEAD_SHA=$(git rev-parse HEAD); REPO=$(gh repo view --json nameWithOwner -q .nameWithOwner); BRANCH=$(git branch --show-current); RUN_ID=$(gh api --method GET --paginate --slurp "repos/$REPO/actions/workflows/nightly.yml/runs" -f branch="$BRANCH" -f event=workflow_dispatch -f per_page=100 | jq -er --arg sha "$HEAD_SHA" '[.[].workflow_runs[] | select(.head_sha == $sha and .event == "workflow_dispatch" and .status == "completed")] | if length == 1 then .[0].id else error("expected exactly one terminal exact-head Nightly receipt") end'); RUN_JSON=$(gh run view "$RUN_ID" --json headSha,event,status,conclusion,jobs); test "$(printf '%s' "$RUN_JSON" | jq -r .headSha)" = "$HEAD_SHA"; test "$(printf '%s' "$RUN_JSON" | jq -r .event)" = workflow_dispatch; test "$(printf '%s' "$RUN_JSON" | jq -r .status)" = completed; DIALYZER_JOB=$(printf '%s' "$RUN_JSON" | jq -er '.jobs[] | select(.name == "Dialyzer" and .conclusion == "failure") | .databaseId'); gh api --paginate --slurp "repos/$REPO/check-runs/$DIALYZER_JOB/annotations?per_page=100" | jq 'add' > /tmp/rindle-126-07-annotations.json; jq -e '[.[] | select(.annotation_level == "warning") | {path, message}] | sort_by(.path,.message) == ([{"path":"lib/rindle/upload/tus_creation.ex","message":"The pattern can never match the type {:error, _}."},{"path":"lib/rindle/upload/tus_stream.ex","message":"The guard test _@1::\u0027nil\u0027 | crypto:hash_state() breaks the opaqueness of its argument."},{"path":"lib/rindle/upload/tus_stream.ex","message":"The guard clause can never succeed."},{"path":"lib/rindle/workers/mux_ingest_variant.ex","message":"The pattern pattern \u003c__mux_response@1, __reason@1\u003e can never match the type, because it is covered by previous clauses."},{"path":"lib/rindle/workers/mux_sync_provider_asset.ex","message":"The pattern variable _err@2 can never match the type, because it is covered by previous clauses."}] | sort_by(.path,.message))' /tmp/rindle-126-07-annotations.json; gh run view "$RUN_ID" --log > /tmp/rindle-126-tus-mux-probe.log; test -s /tmp/rindle-126-tus-mux-probe.log; rg -q 'tus_plug|tus_creation|tus_stream|mux_ingest_variant|mux_sync_provider_asset' .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md; MIX_ENV=test mix test test/install_smoke/dialyzer_ignore_policy_test.exs --seed 0</automated></verify>
  <acceptance_criteria>Every Tus/Mux starting entry has exact supported-head evidence, the ledger preserves `tus_plug.ex` as the immutable filter identity while naming `tus_creation.ex`/`tus_stream.ex` as the extracted warning owners, and no affected source changes precede that distinction.</acceptance_criteria>
  <done>The Tus/Mux group is classified from the sole acceptance authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correct Tus creation/stream and Mux response-pattern boundaries</name>
  <read_first>.planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md, lib/rindle/upload/tus_creation.ex, lib/rindle/upload/tus_stream.ex, lib/rindle/upload/tus_plug.ex, test/rindle/upload/tus_plug_test.exs, test/rindle/storage/local_tus_test.exs, lib/rindle/workers/mux_ingest_variant.ex, test/rindle/workers/mux_ingest_variant_test.exs, lib/rindle/workers/mux_sync_provider_asset.ex, test/rindle/workers/mux_sync_provider_asset_test.exs</read_first>
  <files>lib/rindle/upload/tus_creation.ex, lib/rindle/upload/tus_stream.ex, lib/rindle/workers/mux_ingest_variant.ex, lib/rindle/workers/mux_sync_provider_asset.ex, test/rindle/upload/tus_plug_test.exs, test/rindle/storage/local_tus_test.exs, test/rindle/workers/mux_ingest_variant_test.exs, test/rindle/workers/mux_sync_provider_asset_test.exs, .dialyzer_ignore.exs, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</files>
  <behavior>
    - Tus authentication, checksum, resumability, Plug responses, telemetry, and error vocabulary remain unchanged.
    - Both Mux workers retain response/error matching, retries/snoozes/discards, transitions, compensation, telemetry, and redaction.
  </behavior>
  <action>Correct the supported E38 finding in `TusCreation` at its return/pattern boundary and the E39-E40 findings in `TusStream` at the checksum/hash-state boundary; add or strengthen the POST/concatenation assertions in `tus_plug_test.exs` and PATCH/offset/part-state assertions in `local_tus_test.exs` before implementation. Preserve crypto opacity and never move the warning back into `TusPlug` merely to match the stale starting paths. For supported Mux findings, align response/error unions and reachable clauses with existing worker tests without broadening or dropping error handling. Remove filters only after owner tests and SAFE-01 pass; restore only exact supported analyzer noise at the actual emitted path with adjacent evidence while preserving the original E38-E40 filter identities as separately dispositioned ledger history. Commit/push and obtain or reuse the deterministic exact-head Nightly receipt per `126-PATTERNS.md`; wait to terminal and require the overall run, `Dialyzer`, and `Nightly Summary` to conclude success, then inspect the exact named job logs.</action>
  <verify><automated>set -e; MIX_ENV=test mix test test/rindle/upload/tus_plug_test.exs test/rindle/storage/local_tus_test.exs test/rindle/workers/mux_ingest_variant_test.exs test/rindle/workers/mux_sync_provider_asset_test.exs --seed 0; bash scripts/maintainer/refactor_contract.sh; HEAD_SHA=$(git rev-parse HEAD); REPO=$(gh repo view --json nameWithOwner -q .nameWithOwner); BRANCH=$(git branch --show-current); RUN_ID=$(gh api --method GET --paginate --slurp "repos/$REPO/actions/workflows/nightly.yml/runs" -f branch="$BRANCH" -f event=workflow_dispatch -f per_page=100 | jq -er --arg sha "$HEAD_SHA" '[.[].workflow_runs[] | select(.head_sha == $sha and .event == "workflow_dispatch" and .status == "completed")] | if length == 1 then .[0].id else error("expected exactly one terminal exact-head Nightly receipt") end'); RUN_JSON=$(gh run view "$RUN_ID" --json headSha,event,status,conclusion,jobs); test "$(printf '%s' "$RUN_JSON" | jq -r .headSha)" = "$HEAD_SHA"; test "$(printf '%s' "$RUN_JSON" | jq -r .event)" = workflow_dispatch; test "$(printf '%s' "$RUN_JSON" | jq -r .status)" = completed; test "$(printf '%s' "$RUN_JSON" | jq -r .conclusion)" = success; test "$(printf '%s' "$RUN_JSON" | jq '[.jobs[] | select(.name == "Dialyzer" and .conclusion == "success")] | length')" -eq 1; test "$(printf '%s' "$RUN_JSON" | jq '[.jobs[] | select(.name == "Nightly Summary" and .conclusion == "success")] | length')" -eq 1</automated></verify>
  <acceptance_criteria>Tus/Mux actionable findings are fixed and unsuppressed; behavior and SAFE-01 pass; retained noise is exact and supported; the slice head is supported-green.</acceptance_criteria>
  <done>Tus and Mux boundaries are truthful without breaking opaque or provider contracts.</done>
</task>

</tasks>

<threat_model asvs_level="L1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Tus creation/stream and provider opaque terms → local patterns | Unsafe inspection or narrowing could corrupt Tus/Mux behavior. |
| Provider responses → worker lifecycle | Type edits could alter transitions, telemetry, redaction, or errors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-126-01 | Tampering | Tus/Mux ignore group | high | mitigate | Supported source-unchanged probe and per-entry disposition before edits. |
| T-126-07 | Tampering | crypto/provider opacity | high | mitigate | Opaque-safe helpers/specs and focused protocol/provider proof. |
| T-126-08 | Tampering / Repudiation | Mux lifecycle | high | mitigate | Owner behavior, telemetry/error contracts, SAFE-01, and exact-head green. |
| T-126-SC | Tampering | package supply chain | low | accept | No dependency or package install is introduced. |
</threat_model>

<verification>
Require a source-unchanged supported probe that preserves starting versus emitted identities, focused creation/stream/Mux tests, SAFE-01, policy validation, and an exact-head terminal workflow whose overall, Dialyzer, and Nightly Summary conclusions are all success.
</verification>

<success_criteria>
- Every Tus/Mux filter has a supported disposition.
- Protocol, opacity, provider lifecycle, telemetry, redaction, and errors remain unchanged.
- No dependency, workflow, public API, schema/migration, or release change occurs.
</success_criteria>

<output>
Create `.planning/phases/126-curated-type-ratchet/126-07-SUMMARY.md` when done.
</output>

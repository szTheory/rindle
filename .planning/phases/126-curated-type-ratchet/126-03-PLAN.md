---
phase: 126-curated-type-ratchet
plan: "03"
type: execute
wave: 3
depends_on: ["126-02"]
files_modified:
  - .dialyzer_ignore.exs
  - .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md
  - lib/mix/tasks/rindle.batch_owner_erasure.ex
  - lib/rindle/admin/live/actions_live.ex
  - lib/rindle/ops/runtime_checks.ex
  - test/rindle/batch_owner_erasure_task_test.exs
  - test/rindle/admin/live/actions_live_test.exs
  - test/rindle/ops/runtime_checks_test.exs
  - test/rindle/ops/runtime_checks_streaming_test.exs
autonomous: true
requirements: [TYPE-01, TYPE-02, SAFE-01]
must_haves:
  truths:
    - "Operational task/Admin/runtime-check filters are probed on the supported home cell before owner edits."
    - "Task messages/results, Admin actions, and runtime diagnostics remain behaviorally identical."
    - "No obsolete or actionable filter remains in these three owner files."
    - "The final supported result contains no warning from this or an earlier slice and no unowned warning; only exact later-owned E38-E40 Tus warnings may remain until Plan 126-07, with Nightly Summary recording Dialyzer failure."
  artifacts:
    - path: .dialyzer_ignore.exs
      provides: "curated operational and legacy-pattern filter set"
    - path: .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md
      provides: "supported operational owner dispositions"
  key_links:
    - from: lib/rindle/ops/runtime_checks.ex
      to: test/rindle/ops/runtime_checks_test.exs
      via: "diagnostic result and ordering behavior"
      pattern: "RuntimeChecks"
    - from: lib/rindle/admin/live/actions_live.ex
      to: test/rindle/admin/live/actions_live_test.exs
      via: "existing LiveView action result handling"
      pattern: "ActionsLive"
---

<objective>
Retire operational task, Admin, and runtime-check suppressions without observable behavior drift.

Purpose: Reconcile strict and historical pattern warnings at their owner boundaries under supported evidence.
Output: Truthful owner patterns/specs, narrowed ignores, focused behavior receipts, and an exact-head receipt allowing only later-owned E38-E40 until Plan 126-07.
</objective>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- Operational/runtime disposition rows in `126-TYPE-EVIDENCE.md`.
- Owner behavior tests added only where a supported actionable warning lacks current coverage.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/126-curated-type-ratchet/126-RESEARCH.md
@.planning/phases/126-curated-type-ratchet/126-PATTERNS.md
@.planning/phases/126-curated-type-ratchet/126-VALIDATION.md
@.planning/phases/126-curated-type-ratchet/126-02-SUMMARY.md
@.dialyzer_ignore.exs
@scripts/maintainer/refactor_contract.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose and classify task, Admin, and runtime-check warnings on 1.17/27</name>
  <precondition>The Plan 126-02 final Nightly has no migration/support or unowned warning and its only emitted annotations are exact E38 at `tus_creation.ex` plus E39-E40 at `tus_stream.ex`, with Dialyzer failure recorded by Nightly Summary; `gh auth status` succeeds.</precondition>
  <read_first>.dialyzer_ignore.exs, lib/mix/tasks/rindle.batch_owner_erasure.ex, lib/rindle/admin/live/actions_live.ex, lib/rindle/ops/runtime_checks.ex, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</read_first>
  <files>.dialyzer_ignore.exs, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</files>
  <action>Remove only the strict entries for batch owner erasure, Admin actions, and runtime checks. Commit/push this source-unchanged probe, obtain or verified-reuse the exact-head Nightly receipt under the timestamped capture, unique-ID polling, and terminal named-job protocol in `126-PATTERNS.md`, and record every reproduced or absent path/discriminator with job/log URLs and summary observation. Do not use local 1.19/OTP 28 output to classify or restore anything.</action>
  <verify><automated>set -e; HEAD_SHA=$(git rev-parse HEAD); REPO=$(gh repo view --json nameWithOwner -q .nameWithOwner); BRANCH=$(git branch --show-current); RUN_ID=$(gh api --method GET --paginate --slurp "repos/$REPO/actions/workflows/nightly.yml/runs" -f branch="$BRANCH" -f event=workflow_dispatch -f per_page=100 | jq -er --arg sha "$HEAD_SHA" '[.[].workflow_runs[] | select(.head_sha == $sha and .event == "workflow_dispatch" and .status == "completed")] | if length == 1 then .[0].id else error("expected exactly one terminal exact-head Nightly receipt") end'); RUN_JSON=$(gh run view "$RUN_ID" --json headSha,event,status,conclusion,jobs); test "$(printf '%s' "$RUN_JSON" | jq -r .headSha)" = "$HEAD_SHA"; test "$(printf '%s' "$RUN_JSON" | jq -r .event)" = workflow_dispatch; test "$(printf '%s' "$RUN_JSON" | jq -r .status)" = completed; gh run view "$RUN_ID" --log > /tmp/rindle-126-ops-probe.log; test -s /tmp/rindle-126-ops-probe.log; rg -q 'batch_owner_erasure|actions_live|runtime_checks' .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md; MIX_ENV=test mix test test/install_smoke/dialyzer_ignore_policy_test.exs --seed 0</automated></verify>
  <acceptance_criteria>Every selected filter has exact supported-head evidence and all other owner groups remain suppressed and unchanged.</acceptance_criteria>
  <done>The operational/runtime group is classified before any source change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correct actionable task, Admin, and runtime-check boundaries</name>
  <read_first>.planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md, lib/mix/tasks/rindle.batch_owner_erasure.ex, test/rindle/batch_owner_erasure_task_test.exs, lib/rindle/admin/live/actions_live.ex, test/rindle/admin/live/actions_live_test.exs, lib/rindle/ops/runtime_checks.ex, test/rindle/ops/runtime_checks_test.exs, test/rindle/ops/runtime_checks_streaming_test.exs</read_first>
  <files>lib/mix/tasks/rindle.batch_owner_erasure.ex, lib/rindle/admin/live/actions_live.ex, lib/rindle/ops/runtime_checks.ex, test/rindle/batch_owner_erasure_task_test.exs, test/rindle/admin/live/actions_live_test.exs, test/rindle/ops/runtime_checks_test.exs, test/rindle/ops/runtime_checks_streaming_test.exs, .dialyzer_ignore.exs, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</files>
  <behavior>
    - Batch task output, exits, and result/error terms remain unchanged.
    - Admin actions render and handle success/error outcomes exactly as before.
    - Runtime checks retain scheduled rows, ordering, telemetry, redaction, and result/error shapes.
  </behavior>
  <action>Write focused owner assertions first for any supported actionable branch not already represented. Make the smallest truthful spec/pattern/control-flow correction that preserves each behavior, then remove only the corresponding exact filter. For any supported analyzer-noise finding whose correction would weaken Admin, task results, telemetry, diagnostics, or errors, restore the exact filter with an adjacent run/job/warning rationale. Run all owner tests and SAFE-01, commit/push the reconciled slice, and obtain or verified-reuse the exact-head Nightly receipt under the timestamped capture, unique-ID polling, and terminal named-job protocol in `126-PATTERNS.md`. Aggregate all annotation pages and compare the complete warning multiset, requiring this plan's owned warning set to be absent or explicitly retained with supported rationale and rejecting every fourth/new/unowned warning. Until Plan 126-07, the only permitted emitted warnings are exactly E38 text at `lib/rindle/upload/tus_creation.ex` plus E39-E40 text at `lib/rindle/upload/tus_stream.ex`; require Dialyzer `failure`, Nightly Summary `success`, and its log to record `DIALYZER: failure`.</action>
  <verify><automated>set -e; MIX_ENV=test mix test test/rindle/batch_owner_erasure_task_test.exs test/rindle/admin/live/actions_live_test.exs test/rindle/ops/runtime_checks_test.exs test/rindle/ops/runtime_checks_streaming_test.exs --seed 0; bash scripts/maintainer/refactor_contract.sh; HEAD_SHA=$(git rev-parse HEAD); REPO=$(gh repo view --json nameWithOwner -q .nameWithOwner); BRANCH=$(git branch --show-current); RUN_ID=$(gh api --method GET --paginate --slurp "repos/$REPO/actions/workflows/nightly.yml/runs" -f branch="$BRANCH" -f event=workflow_dispatch -f per_page=100 | jq -er --arg sha "$HEAD_SHA" '[.[].workflow_runs[] | select(.head_sha == $sha and .event == "workflow_dispatch" and .status == "completed")] | if length == 1 then .[0].id else error("expected exactly one terminal exact-head Nightly receipt") end'); RUN_JSON=$(gh run view "$RUN_ID" --json headSha,event,status,conclusion,jobs); test "$(printf '%s' "$RUN_JSON" | jq -r .headSha)" = "$HEAD_SHA"; test "$(printf '%s' "$RUN_JSON" | jq -r .event)" = workflow_dispatch; test "$(printf '%s' "$RUN_JSON" | jq -r .status)" = completed; DIALYZER_JOB=$(gh run view "$RUN_ID" --json jobs --jq '.jobs[] | select(.name == "Dialyzer") | .databaseId'); SUMMARY_JOB=$(gh run view "$RUN_ID" --json jobs --jq '.jobs[] | select(.name == "Nightly Summary") | .databaseId'); test "$(gh run view "$RUN_ID" --json jobs --jq '.jobs[] | select(.name == "Dialyzer") | .conclusion')" = failure; test "$(gh run view "$RUN_ID" --json jobs --jq '.jobs[] | select(.name == "Nightly Summary") | .conclusion')" = success; REPO=$(gh repo view --json nameWithOwner -q .nameWithOwner); gh api --paginate --slurp "repos/$REPO/check-runs/$DIALYZER_JOB/annotations?per_page=100" | jq 'add' &gt; /tmp/rindle-126-03-annotations.json; jq -e '[.[] | select(.annotation_level == "warning") | {path, message}] as $w | ($w | length) == 3 and ($w | map(select(.path == "lib/rindle/upload/tus_creation.ex" and .message == "The pattern can never match the type {:error, _}.")) | length) == 1 and ($w | map(select(.path == "lib/rindle/upload/tus_stream.ex" and .message == "The guard test _@1::\u0027nil\u0027 | crypto:hash_state() breaks the opaqueness of its argument.")) | length) == 1 and ($w | map(select(.path == "lib/rindle/upload/tus_stream.ex" and .message == "The guard clause can never succeed.")) | length) == 1' /tmp/rindle-126-03-annotations.json; gh run view "$RUN_ID" --job "$DIALYZER_JOB" --log &gt; /tmp/rindle-126-03-dialyzer.log; test "$(sed -n 's/.*##\[warning\]//p' /tmp/rindle-126-03-dialyzer.log | wc -l | tr -d ' ')" -eq 3; gh run view "$RUN_ID" --job "$SUMMARY_JOB" --log | grep -Fq 'DIALYZER: failure'</automated></verify>
  <acceptance_criteria>Supported actionable task/Admin/runtime-check findings are fixed and unsuppressed, retained owned findings cite supported evidence, owner behavior plus SAFE-01 pass, no new or unowned warning appears, and the only emitted warnings are exactly E38-E40 while Nightly Summary honestly records Dialyzer failure.</acceptance_criteria>
  <done>Operational entry points retain behavior with truthful supported types.</done>
</task>

</tasks>

<threat_model asvs_level="L1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Task/Admin inputs → result handling | Pattern edits could silently change operator-visible outcomes. |
| Runtime diagnostics → telemetry/report output | Type narrowing could remove reachable diagnostic/error states. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-126-01 | Tampering | operational ignore group | high | mitigate | Supported exact-head probe and one-row-per-filter disposition precede edits. |
| T-126-05 | Tampering / Repudiation | Admin/tasks/runtime outputs | high | mitigate | Owner behavior tests plus SAFE-01 lock outputs, telemetry, and error terms before filter removal. |
| T-126-02 | Spoofing | supported authority | high | mitigate | Bind the exact pushed head to job annotations and log; admit only the three exact later-owned E38-E40 warnings and require Nightly Summary to record failure. |
| T-126-SC | Tampering | package supply chain | low | accept | No dependency or package install is introduced. |
</threat_model>

<verification>
Use one supported probe before source edits, focused owner suites plus SAFE-01, policy validation throughout, and a final exact-head supported result whose annotation set is exactly E38 at `tus_creation.ex` plus E39-E40 at `tus_stream.ex` and whose Nightly Summary records Dialyzer failure.
</verification>

<success_criteria>
- Every selected task/Admin/runtime-check filter has a complete supported disposition.
- Owner-visible task/Admin/runtime-check behavior remains unchanged.
- No CI topology, dependency, schema/migration, or release policy changes occur.
</success_criteria>

<output>
Create `.planning/phases/126-curated-type-ratchet/126-03-SUMMARY.md` when done.
</output>

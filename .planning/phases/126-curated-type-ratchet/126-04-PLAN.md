---
phase: 126-curated-type-ratchet
plan: "04"
type: execute
wave: 4
depends_on: ["126-03"]
files_modified:
  - .dialyzer_ignore.exs
  - .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md
  - lib/rindle/ops/runtime_status.ex
  - lib/rindle/html.ex
  - lib/rindle/workers/process_variant.ex
  - test/rindle/ops/runtime_status_test.exs
  - test/rindle/runtime_status_task_test.exs
  - test/rindle/html_test.exs
  - test/rindle/workers/process_variant_test.exs
autonomous: true
requirements: [TYPE-01, TYPE-02, SAFE-01]
must_haves:
  truths:
    - "Runtime-status, HTML, and ProcessVariant atom filters are supported-probed before source edits."
    - "Runtime status/task output, HTML helpers, and ProcessVariant lifecycle behavior remain unchanged."
    - "No obsolete or actionable historical atom remains for these three owners."
  artifacts:
    - path: .dialyzer_ignore.exs
      provides: "curated historical runtime-pattern remainder"
    - path: .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md
      provides: "supported runtime-status/HTML/ProcessVariant dispositions"
  key_links:
    - from: lib/rindle/ops/runtime_status.ex
      to: test/rindle/runtime_status_task_test.exs
      via: "runtime-status Mix task output and failure contract"
      pattern: "RuntimeStatus"
    - from: lib/rindle/workers/process_variant.ex
      to: test/rindle/workers/process_variant_test.exs
      via: "state transitions, retries, telemetry, and errors"
      pattern: "ProcessVariant"
---

<objective>
Reconcile historical runtime-status, HTML, and ProcessVariant filters without observable behavior drift.

Purpose: Isolate legacy atom retirement from the operational slice and prove it under its own supported-cell probe.
Output: Complete supported dispositions, focused owner proof, and a green exact-head slice.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/126-curated-type-ratchet/126-RESEARCH.md
@.planning/phases/126-curated-type-ratchet/126-PATTERNS.md
@.planning/phases/126-curated-type-ratchet/126-VALIDATION.md
@.planning/phases/126-curated-type-ratchet/126-03-SUMMARY.md
@.dialyzer_ignore.exs
@scripts/maintainer/refactor_contract.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe historical runtime-status, HTML, and ProcessVariant atoms on 1.17/27</name>
  <precondition>The operational slice is supported-green and `gh auth status` succeeds.</precondition>
  <read_first>.dialyzer_ignore.exs, lib/rindle/ops/runtime_status.ex, lib/rindle/html.ex, lib/rindle/workers/process_variant.ex, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</read_first>
  <files>.dialyzer_ignore.exs, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</files>
  <action>Remove only the historical atom filters for runtime status, HTML, and ProcessVariant. Commit/push the source-unchanged probe, dispatch Nightly for that exact head, inspect Dialyzer and Nightly Summary, and record every reproduced or absent path/discriminator with URLs and head SHA. No owner source edit or filter restoration may precede this supported receipt; unsupported local output remains diagnostic only.</action>
  <verify><automated>set -e; HEAD_SHA=$(git rev-parse HEAD); RUN_ID=$(gh run list --workflow nightly.yml --branch "$(git branch --show-current)" --event workflow_dispatch --commit "$HEAD_SHA" --json databaseId --jq '.[0].databaseId'); test -n "$RUN_ID"; gh run view "$RUN_ID" --log > /tmp/rindle-126-runtime-history-probe.log; test -s /tmp/rindle-126-runtime-history-probe.log; rg -q 'runtime_status|html|process_variant' .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md; MIX_ENV=test mix test test/install_smoke/dialyzer_ignore_policy_test.exs --seed 0</automated></verify>
  <acceptance_criteria>Each historical atom has exact supported-head evidence while all three owner sources remain unchanged.</acceptance_criteria>
  <done>The legacy runtime-pattern group is classified before source changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile historical atoms and prove runtime owner behavior</name>
  <read_first>.planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md, lib/rindle/ops/runtime_status.ex, test/rindle/ops/runtime_status_test.exs, test/rindle/runtime_status_task_test.exs, lib/rindle/html.ex, test/rindle/html_test.exs, lib/rindle/workers/process_variant.ex, test/rindle/workers/process_variant_test.exs, .dialyzer_ignore.exs</read_first>
  <files>lib/rindle/ops/runtime_status.ex, lib/rindle/html.ex, lib/rindle/workers/process_variant.ex, test/rindle/ops/runtime_status_test.exs, test/rindle/runtime_status_task_test.exs, test/rindle/html_test.exs, test/rindle/workers/process_variant_test.exs, .dialyzer_ignore.exs, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</files>
  <behavior>
    - Runtime-status flags, collection, formatting, task output, limits, and failure terms remain unchanged.
    - HTML helper output and optional integration behavior remain unchanged.
    - ProcessVariant state transitions, retries/discards, telemetry, and error vocabulary remain unchanged.
  </behavior>
  <action>Remove supported-absent atoms. For each reproduced actionable warning, add missing owner behavior proof first and correct only the private pattern/spec boundary; retain an atom only when the exact supported warning persists and the ledger plus adjacent comment explains analyzer/dependency noise. Commit/push the reconciled head, run SAFE-01, dispatch Nightly, and require Dialyzer plus Nightly Summary success.</action>
  <verify><automated>set -e; MIX_ENV=test mix test test/rindle/ops/runtime_status_test.exs test/rindle/runtime_status_task_test.exs test/rindle/html_test.exs test/rindle/workers/process_variant_test.exs --seed 0; bash scripts/maintainer/refactor_contract.sh; HEAD_SHA=$(git rev-parse HEAD); RUN_ID=$(gh run list --workflow nightly.yml --branch "$(git branch --show-current)" --event workflow_dispatch --commit "$HEAD_SHA" --json databaseId --jq '.[0].databaseId'); test -n "$RUN_ID"; test "$(gh run view "$RUN_ID" --json jobs --jq '.jobs[] | select(.name == "Dialyzer") | .conclusion')" = success; test "$(gh run view "$RUN_ID" --json jobs --jq '.jobs[] | select(.name == "Nightly Summary") | .conclusion')" = success</automated></verify>
  <acceptance_criteria>The exact slice head is supported-green, all focused owner tests and SAFE-01 pass, and no obsolete/actionable atom remains.</acceptance_criteria>
  <done>Historical runtime-pattern debt is reconciled without API, telemetry, task, error, HTML, or worker drift.</done>
</task>

</tasks>

<threat_model asvs_level="L1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Runtime/task inputs → diagnostics and output | Pattern edits could silently remove reachable operator-visible states. |
| Worker inputs → lifecycle/telemetry | Type narrowing could alter retries, transitions, or errors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-126-01 | Tampering | historical atom group | high | mitigate | Source-unchanged supported probe and per-entry disposition precede edits. |
| T-126-05 | Tampering / Repudiation | runtime/HTML/worker behavior | high | mitigate | Focused owner tests plus SAFE-01 preserve outputs, telemetry, and errors. |
| T-126-02 | Spoofing | supported authority | high | mitigate | Bind probe and final run to pushed `headSha` and named jobs. |
| T-126-SC | Tampering | package supply chain | low | accept | No dependency or package install is introduced. |
</threat_model>

<verification>
Require a source-unchanged supported probe, focused runtime/task/HTML/worker tests, SAFE-01, policy validation, and exact-head supported green.
</verification>

<success_criteria>
- Every selected historical atom has a complete supported disposition.
- Runtime-status/task, HTML, and ProcessVariant behavior remains unchanged.
- No CI topology, dependency, schema/migration, public API, telemetry, error, or release policy changes occur.
</success_criteria>

<output>
Create `.planning/phases/126-curated-type-ratchet/126-04-SUMMARY.md` when done.
</output>

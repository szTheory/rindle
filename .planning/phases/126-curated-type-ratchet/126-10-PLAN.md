---
phase: 126-curated-type-ratchet
plan: "10"
type: execute
wave: 10
depends_on: ["126-09"]
files_modified:
  - test/install_smoke/dialyzer_ignore_policy_test.exs
autonomous: true
gap_closure: true
requirements: [TYPE-01, TYPE-02, SAFE-01]
must_haves:
  truths:
    - "The live Dialyzer ignore list may only retain or remove exact identities from the closed approved universe present at gap-closure start."
    - "A new exact strict-description filter for an existing owner and every unapproved atom tuple are rejected by the permanent policy test."
    - "The correction changes tests only and preserves the live ignore list, source, dependencies, CI topology/cache, public behavior, and prior TYPE-01 evidence semantics."
    - "PR #91 CI Summary and supported Nightly Dialyzer/Summary succeed on the same updated candidate with zero Dialyzer annotations before the replacement receipt closes issue #76."
  artifacts:
    - path: test/install_smoke/dialyzer_ignore_policy_test.exs
      provides: "closed approved-filter universe, removal-only subset ratchet, and explicit new-strict-filter rejection fixture"
    - path: .dialyzer_ignore.exs
      provides: "unchanged 35-entry live baseline consumed as a subset of the approved universe"
    - path: "GitHub issue #76 replacement receipt"
      provides: "sanitized exact-head gap-closure authority and conditional close evidence"
  key_links:
    - from: test/install_smoke/dialyzer_ignore_policy_test.exs
      to: .dialyzer_ignore.exs
      via: "Code.eval_file/1 followed by duplicate, owner, discriminator, and approved-universe subset validation"
      pattern: "MapSet.subset?"
    - from: "PR #91 and issue #76"
      to: "CI Summary, Nightly Dialyzer, and Nightly Summary"
      via: "one candidate SHA bound to successful named jobs and an empty Dialyzer annotations collection"
      pattern: "headRefOid|headSha"
---

<objective>
Close the TYPE-02 enforcement hole without changing the curated baseline or any product/CI surface.

Purpose: Make the existing exact baseline removal-only so a newly authored strict suppression cannot silently convert an actionable warning into accepted debt.
Output: A test-local approved universe and regression fixture, complete local preservation evidence, and a replacement exact-head GitHub receipt for PR #91 and issue #76.
</objective>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- `test/install_smoke/dialyzer_ignore_policy_test.exs`: permanent removal-only exact-identity ratchet for the live ignore list.
- Updated PR #91 and a sanitized issue #76 replacement receipt bound to the corrected candidate.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RUNNING.md
@guides/release_publish.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/126-curated-type-ratchet/126-VERIFICATION.md
@.planning/phases/126-curated-type-ratchet/126-VALIDATION.md
@.planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md
@.planning/phases/126-curated-type-ratchet/126-01-SUMMARY.md
@.planning/phases/126-curated-type-ratchet/126-09-SUMMARY.md
@test/install_smoke/dialyzer_ignore_policy_test.exs
@test/install_smoke/ci_lane_split_test.exs
@test/install_smoke/ci_cache_hygiene_test.exs
@.dialyzer_ignore.exs
@.github/workflows/nightly.yml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Ratchet the approved ignore universe from RED to GREEN</name>
  <read_first>test/install_smoke/dialyzer_ignore_policy_test.exs, .dialyzer_ignore.exs, .planning/phases/126-curated-type-ratchet/126-VERIFICATION.md, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md, test/install_smoke/ci_lane_split_test.exs, test/install_smoke/ci_cache_hygiene_test.exs, scripts/maintainer/refactor_contract.sh, RUNNING.md</read_first>
  <files>test/install_smoke/dialyzer_ignore_policy_test.exs</files>
  <behavior>
    - RED: append an otherwise well-formed exact string tuple for the existing `lib/rindle.ex` owner to the evaluated live list and assert the validator rejects it; the current validator must fail this test because it accepts every nonblank string.
    - GREEN: the 35 exact path/discriminator identities present at gap-closure start form a closed approved universe directly in the policy test, and the evaluated live list is valid only when its unique entries are a subset of that universe.
    - Removing any approved live tuple remains valid; neither live count nor file hash is asserted.
    - A changed description, a new exact string for an existing owner, and every atom tuple outside the existing four approved atom identities are invalid.
  </behavior>
  <action>Start with the new strict-description fixture only, run the focused policy test, and record the expected failing assertion before implementation. Then add one test-local `@approved_filters` collection copied exactly from the current 35 live tuples, including the four retained atom identities and every retained strict path/description identity. Keep shape, duplicate, owner-existence, and nonblank-string checks as defense in depth, then require `MapSet.new(ignores)` to be a subset of `MapSet.new(@approved_filters)` so retirement is always permitted while expansion or identity rewriting fails. Add a positive removal fixture by deleting one approved entry from the live list and proving the remainder stays valid. Keep the explicit unapproved-atom fixture and add the verifier-requested otherwise-valid strict-string fixture against an existing owner. Do not read planning markdown at test runtime or derive approval from the live file itself; the test's literal approved universe is the repository-local review boundary. Do not edit `.dialyzer_ignore.exs`, source, workflows, dependencies, public contracts, or topology. Commit RED and GREEN separately, then run the focused policy/topology/cache suite, SAFE-01, `mix ci`, and repository hygiene on the resulting candidate.</action>
  <verify>
    <automated>set -e; test "$(git diff --name-only HEAD^..HEAD | grep -v '^test/install_smoke/dialyzer_ignore_policy_test.exs$' | wc -l | tr -d ' ')" -eq 0; MIX_ENV=test mix test test/install_smoke/dialyzer_ignore_policy_test.exs test/install_smoke/ci_lane_split_test.exs test/install_smoke/ci_cache_hygiene_test.exs --seed 0; bash scripts/maintainer/refactor_contract.sh; mix ci; ./scripts/maintainer/repo_hygiene_check.sh --ci; git diff --exit-code HEAD~2 -- .dialyzer_ignore.exs mix.exs mix.lock .github/workflows lib priv guides README.md</automated>
  </verify>
  <acceptance_criteria>The committed RED test demonstrates the old validator accepts the novel exact strict tuple; GREEN makes that fixture and unapproved atoms invalid, permits removal of an approved tuple, keeps all current live tuples valid, and passes focused policy/topology/cache, SAFE-01, `mix ci`, hygiene, and the forbidden-surface diff gate.</acceptance_criteria>
  <done>TYPE-02 has a durable removal-only exact-identity enforcement path without freezing count/hash or changing the baseline and supported behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the exact-head authority receipt and conditionally close issue #76</name>
  <precondition>Task 1 RED and GREEN commits are reachable, the working tree is clean, `gh auth status` succeeds, and PR #91 remains open on `phase/126-curated-type-ratchet`.</precondition>
  <read_first>test/install_smoke/dialyzer_ignore_policy_test.exs, .dialyzer_ignore.exs, .github/workflows/nightly.yml, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md, .planning/phases/126-curated-type-ratchet/126-VERIFICATION.md, guides/release_publish.md, RUNNING.md</read_first>
  <files>None</files>
  <action>Push the corrected candidate branch, update PR #91 title/body with the bounded TYPE-02 removal-only policy correction and no merge/release request, and require its `headRefOid` to equal local HEAD. Select the exact-head pull_request `ci.yml` run and require the unique `CI Summary` job to conclude success. Obtain the supported exact-head workflow_dispatch Nightly receipt using the deterministic capture protocol from `126-PATTERNS.md`; require the literal Elixir 1.17 / OTP 27 Dialyzer job, overall workflow, `Dialyzer`, and `Nightly Summary` to conclude success, require the Summary log's Dialyzer row to report success, and aggregate every Dialyzer annotations API page before asserting the complete collection is empty. Do not treat local Dialyzer as authority. Post one sanitized issue #76 replacement receipt that names the prior policy hole, the corrected candidate SHA, the removal-only approved-universe behavior, focused/SAFE-01/full/hygiene results, PR #91, exact CI Summary, Nightly, Dialyzer, Nightly Summary, and zero-annotation evidence. Close #76 only after every predicate passes; otherwise post one narrowing receipt naming the failed predicate and leave it open. Do not expose tokens or environment values, merge PR #91, publish, change CI/source/dependencies, retry on a different head, or make any repository commit after the authority candidate is selected.</action>
  <verify>
    <automated>set -e; CANDIDATE_HEAD=$(git rev-parse HEAD); test -z "$(git status --porcelain)"; PR_JSON=$(gh pr view 91 --json state,headRefOid,headRefName); test "$(printf '%s' "$PR_JSON" | jq -r .state)" = OPEN; test "$CANDIDATE_HEAD" = "$(printf '%s' "$PR_JSON" | jq -r .headRefOid)"; BRANCH=$(printf '%s' "$PR_JSON" | jq -r .headRefName); REPO=$(gh repo view --json nameWithOwner -q .nameWithOwner); CI_RUN_ID=$(gh api --method GET --paginate --slurp "repos/$REPO/actions/workflows/ci.yml/runs" -f branch="$BRANCH" -f event=pull_request -f per_page=100 | jq -er --arg sha "$CANDIDATE_HEAD" '[.[].workflow_runs[] | select(.head_sha == $sha and .event == "pull_request" and .status == "completed")] | if length == 1 then .[0].id else error("expected one terminal exact-head PR CI run") end'); CI_JSON=$(gh run view "$CI_RUN_ID" --json headSha,event,status,conclusion,jobs); test "$(printf '%s' "$CI_JSON" | jq -r .headSha)" = "$CANDIDATE_HEAD"; test "$(printf '%s' "$CI_JSON" | jq -r .event)" = pull_request; test "$(printf '%s' "$CI_JSON" | jq -r .conclusion)" = success; CI_SUMMARY_JOB=$(printf '%s' "$CI_JSON" | jq -er '[.jobs[] | select(.name == "CI Summary" and .conclusion == "success")] | if length == 1 then .[0].databaseId else error("expected one successful CI Summary") end'); RUN_ID=$(gh api --method GET --paginate --slurp "repos/$REPO/actions/workflows/nightly.yml/runs" -f branch="$BRANCH" -f event=workflow_dispatch -f per_page=100 | jq -er --arg sha "$CANDIDATE_HEAD" '[.[].workflow_runs[] | select(.head_sha == $sha and .event == "workflow_dispatch" and .status == "completed")] | if length == 1 then .[0].id else error("expected one terminal exact-head Nightly run") end'); RUN_JSON=$(gh run view "$RUN_ID" --json headSha,event,status,conclusion,jobs); test "$(printf '%s' "$RUN_JSON" | jq -r .headSha)" = "$CANDIDATE_HEAD"; test "$(printf '%s' "$RUN_JSON" | jq -r .event)" = workflow_dispatch; test "$(printf '%s' "$RUN_JSON" | jq -r .conclusion)" = success; DIALYZER_JOB=$(printf '%s' "$RUN_JSON" | jq -er '[.jobs[] | select(.name == "Dialyzer" and .conclusion == "success")] | if length == 1 then .[0].databaseId else error("expected one successful Dialyzer job") end'); SUMMARY_JOB=$(printf '%s' "$RUN_JSON" | jq -er '[.jobs[] | select(.name == "Nightly Summary" and .conclusion == "success")] | if length == 1 then .[0].databaseId else error("expected one successful Nightly Summary") end'); gh api --paginate --slurp "repos/$REPO/check-runs/$DIALYZER_JOB/annotations?per_page=100" | jq -e 'add | length == 0'; gh run view "$RUN_ID" --job "$SUMMARY_JOB" --log | grep -Fq 'DIALYZER: success'; ISSUE_JSON=$(gh issue view 76 --json state,comments); test "$(printf '%s' "$ISSUE_JSON" | jq -r .state)" = CLOSED; LAST_COMMENT=$(printf '%s' "$ISSUE_JSON" | jq -r '.comments[-1].body'); printf '%s' "$LAST_COMMENT" | grep -Fq "$CANDIDATE_HEAD"; printf '%s' "$LAST_COMMENT" | grep -Fq "actions/runs/$CI_RUN_ID/job/$CI_SUMMARY_JOB"; printf '%s' "$LAST_COMMENT" | grep -Fq "actions/runs/$RUN_ID/job/$DIALYZER_JOB"; printf '%s' "$LAST_COMMENT" | grep -Fq 'zero annotations'; test "$CANDIDATE_HEAD" = "$(git rev-parse HEAD)"; test -z "$(git status --porcelain)"</automated>
  </verify>
  <acceptance_criteria>PR #91 points to the unchanged corrected candidate; exact-head CI Summary, supported Nightly Dialyzer, and Nightly Summary succeed; all Dialyzer annotation pages aggregate to zero entries; the sanitized replacement receipt links those authorities; and issue #76 is closed only under the complete predicate.</acceptance_criteria>
  <done>The reopened TYPE-02 issue is closed by corrected exact-head evidence, or remains open with a precise failed predicate and no false acceptance claim.</done>
</task>

</tasks>

<threat_model asvs_level="L1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Policy-test approval data → live ignore list | A self-derived or incomplete approval rule could continue allowing new suppressions. |
| Local candidate → PR and supported Nightly | A different head or partial annotation page could be presented as gap-closure authority. |
| GitHub authority → issue #76 state | Closing the issue before all predicates pass would create a false immutable receipt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-126-10-01 | Tampering | policy approved universe | high | mitigate | Copy the exact 35 starting identities into the test, require a live subset, and exercise expansion and removal fixtures. |
| T-126-10-02 | Spoofing / Repudiation | PR CI and Nightly receipts | high | mitigate | Bind PR head and both workflow `headSha` values to local HEAD; inspect unique named jobs and the supported toolchain. |
| T-126-10-03 | Information Disclosure | issue/PR receipt | medium | mitigate | Publish only sanitized SHAs, public run/job links, conclusions, counts, and policy outcomes; never shell environment or credentials. |
| T-126-10-04 | Repudiation / Elevation of privilege | issue #76 mutation | high | mitigate | Close only after exact-head CI Summary, Nightly Dialyzer/Summary, zero annotations, and local gates all pass. |
| T-126-10-05 | Tampering | CI/release and product surfaces | high | mitigate | Test-only diff gate plus topology/cache tests, SAFE-01, `mix ci`, and hygiene; no workflow, source, dependency, or release edits. |
| T-126-10-SC | Tampering | package supply chain | low | accept | No package install or dependency change is in scope. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---:|---|---|
| GOAL | — | Supported home cell has an actionable enforced Dialyzer baseline with no retirement ambiguity | 10 | COVERED | Removal-only exact identities plus repeated exact-head authority close the enforcement ambiguity. |
| REQ | TYPE-01 | Supported 1.17/27 Dialyzer passes and local output is non-authoritative | 10 | COVERED | No baseline/source change; supported Nightly is rerun on the corrected exact head and must remain green. |
| REQ | TYPE-02 | Curated gate blocks new actionable findings and #76 closes with evidence | 10 | COVERED | Novel strict tuple fails, removals pass, PR/Nightly receipts replace the prior incomplete acceptance. |
| REQ | SAFE-01 | Preserve public, schema/migration, telemetry, errors, Admin, dependency, CI, and release behavior | 10 | COVERED | Test-only diff plus SAFE-01, full CI, topology/cache, and hygiene gates. |
| RESEARCH | authority | Literal Nightly Elixir 1.17 / OTP 27 remains sole Dialyzer authority | 10 | COVERED | Exact-head supported Dialyzer/Summary success and zero annotations are mandatory. |
| RESEARCH | CI boundary | Keep topology, caches, jobs, schedules, dependencies, and required checks unchanged | 10 | COVERED | No workflow/dependency files are modified; structural tests and diff gate enforce the boundary. |
| VERIFICATION | TYPE-02 gap | Live ignores must be a removal-only subset of an immutable approved inventory | 10 | COVERED | The policy test owns a closed exact universe and explicitly exercises removal. |
| VERIFICATION | strict fixture | An otherwise valid new strict-description filter must fail | 10 | COVERED | RED demonstrates the hole; GREEN rejects the new exact tuple for an existing owner. |
| CONTEXT | — | No CONTEXT.md or D-NN decisions exist | — | EXCLUDED | The bounded verifier gap and existing researched constraints govern this closure. |

Excluded, not gaps: changes to `.dialyzer_ignore.exs`, runtime/source behavior, public API, schema/migrations, telemetry/errors/Admin, dependencies, CI topology/cache/schedules, release policy, local-Dialyzer authority, and parsing planning markdown at test runtime.
</source_audit>

<verification>
Require the witnessed RED failure, GREEN policy behavior, focused policy/topology/cache suite, SAFE-01, `mix ci`, hygiene, a test-only diff, exact-head PR CI Summary, supported Nightly Dialyzer/Summary success, zero complete-set annotations, and the conditional sanitized issue receipt.
</verification>

<success_criteria>
- A new exact strict-description suppression cannot pass the policy even when its owner exists and its shape is valid.
- Removing any currently approved suppression remains allowed without count/hash coupling.
- `.dialyzer_ignore.exs`, source, dependencies, workflows, public behavior, and release topology remain unchanged.
- PR #91 and issue #76 reflect the corrected exact-head authority with no credential disclosure or false close.
</success_criteria>

<output>
Create `.planning/phases/126-curated-type-ratchet/126-10-SUMMARY.md` after the external receipt completes; later summary/state metadata is not the authority candidate.
</output>

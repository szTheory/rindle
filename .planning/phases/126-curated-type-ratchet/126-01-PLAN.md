---
phase: 126-curated-type-ratchet
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/install_smoke/dialyzer_ignore_policy_test.exs
  - .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md
autonomous: true
requirements: [TYPE-01, TYPE-02, SAFE-01]
must_haves:
  truths:
    - "The curated ignore file is machine-checked for tuple shape, duplicates, existing owner paths, description specificity, and the closed legacy atom allowlist before any entry changes."
    - "The immutable starting receipt records 45 entries across 18 files: 8 legacy atom filters and 37 description-strict filters."
    - "The unchanged Nightly Dialyzer topology is observed on the exact policy-tracer head under Elixir 1.17 / OTP 27."
  artifacts:
    - path: test/install_smoke/dialyzer_ignore_policy_test.exs
      provides: "curated baseline policy and inventory-drift regression lock"
    - path: .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md
      provides: "immutable starting inventory and supported-run disposition ledger"
  key_links:
    - from: test/install_smoke/dialyzer_ignore_policy_test.exs
      to: .dialyzer_ignore.exs
      via: "evaluates the literal tuple list and validates every entry"
      pattern: "dialyzer_ignore"
    - from: .github/workflows/nightly.yml
      to: .dialyzer_ignore.exs
      via: "PLT cache hash plus gating mix dialyzer invocation"
      pattern: "otp27-elixir1.17"
---

<objective>
Establish the policy-test tracer and immutable starting evidence before retiring any suppression.

Purpose: Prove the gate, inventory, and exact supported authority end to end before source-bearing expansion work.
Output: A focused ExUnit policy contract, a 45-entry starting receipt, and an exact-head supported Nightly receipt.
</objective>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- `test/install_smoke/dialyzer_ignore_policy_test.exs`: permanent curated-ignore policy gate.
- `.planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md`: starting inventory, per-owner supported-cell probes, retained-noise rationales, and final authority links.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RUNNING.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/126-curated-type-ratchet/126-RESEARCH.md
@.planning/phases/126-curated-type-ratchet/126-PATTERNS.md
@.planning/phases/126-curated-type-ratchet/126-VALIDATION.md
@.dialyzer_ignore.exs
@mix.exs
@.github/workflows/nightly.yml
@test/install_smoke/ci_lane_split_test.exs
@test/install_smoke/ci_cache_hygiene_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Lock the curated-ignore policy and 45-entry starting inventory</name>
  <read_first>.dialyzer_ignore.exs, mix.exs, test/install_smoke/ci_lane_split_test.exs, test/install_smoke/ci_cache_hygiene_test.exs, .planning/phases/126-curated-type-ratchet/126-RESEARCH.md</read_first>
  <files>test/install_smoke/dialyzer_ignore_policy_test.exs, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</files>
  <behavior>
    - The live list contains valid two-element `{path, discriminator}` tuples with no duplicates and each path names an existing `lib/**` or `test/support/**` file.
    - Description filters are nonempty exact strings; atom filters are limited to the eight enumerated historical entries and cannot expand.
    - The validator rejects duplicate, missing-owner, file-only, regex-wide, malformed, and unapproved atom fixtures without requiring the production list to remain at 45 after retirement.
  </behavior>
  <action>Create the policy test first and prove RED with an intentionally invalid in-test fixture, then implement its test-local validator and prove GREEN against the live file. Do not rewrite the ignore list. Create the evidence ledger with `implementation_base_sha`, the SHA-256 of the starting ignore file, all 45 exact tuples grouped by 18 owner files, and reconciled totals of 8 atom plus 37 strict descriptions. The ledger must define one disposition row per entry (`obsolete`, `actionable-fixed`, or `retained-analyzer-noise`) and require supported-run URL, job identity, exact warning text, owner test command, and SAFE-01 result before a row can leave `pending`. Keep unsupported local output explicitly diagnostic.</action>
  <verify><automated>MIX_ENV=test mix test test/install_smoke/dialyzer_ignore_policy_test.exs test/install_smoke/ci_lane_split_test.exs test/install_smoke/ci_cache_hygiene_test.exs --seed 0</automated></verify>
  <acceptance_criteria>The policy suite passes on the unchanged 45-entry file, its invalid fixtures fail validation, and the ledger count/hash exactly matches 8 + 37 entries across 18 files.</acceptance_criteria>
  <done>The production-quality policy/evidence path is runnable end to end before any suppression or source changes.</done>
</task>

<task type="auto">
  <name>Task 2: Record the supported policy-tracer Nightly receipt without topology drift</name>
  <precondition>`gh auth status` succeeds and the policy-tracer commit is pushed to the current Phase 126 branch.</precondition>
  <read_first>.github/workflows/nightly.yml, test/install_smoke/ci_lane_split_test.exs, test/install_smoke/ci_cache_hygiene_test.exs, guides/release_publish.md, RUNNING.md, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</read_first>
  <files>.planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</files>
  <action>Capture the pushed policy-tracer SHA, dispatch `nightly.yml` on the current branch, and select the workflow_dispatch run whose `headSha` equals that SHA. Wait to terminal state and inspect both the `Dialyzer` job and `Nightly Summary`; record the run/job URLs, toolchain identity, Dialyzer conclusion, and summary Dialyzer row in the ledger. This run proves authority and topology only because the starting suppressions remain active. Re-run the focused policy/topology/cache tests and verify there is no change to `mix.exs`, `mix.lock`, any workflow, schedule, job dependency, cache shape, required-check policy, dependency, or release policy.</action>
  <verify><automated>set -e; HEAD_SHA=$(git rev-parse HEAD); RUN_ID=$(gh run list --workflow nightly.yml --branch "$(git branch --show-current)" --event workflow_dispatch --commit "$HEAD_SHA" --json databaseId,headSha --jq '.[0].databaseId'); test -n "$RUN_ID"; gh run view "$RUN_ID" --json jobs --jq '.jobs[] | select(.name == "Dialyzer" or .name == "Nightly Summary") | [.name,.conclusion] | @tsv' | grep -q '^Dialyzer'; MIX_ENV=test mix test test/install_smoke/dialyzer_ignore_policy_test.exs test/install_smoke/ci_lane_split_test.exs test/install_smoke/ci_cache_hygiene_test.exs --seed 0</automated></verify>
  <acceptance_criteria>The ledger cites an exact-head 1.17/27 Nightly run and records both Dialyzer and Nightly Summary observations while every CI/cache/release invariant remains unchanged.</acceptance_criteria>
  <done>The tracer proves the permanent policy test reaches the existing supported authority without changing acceptance topology.</done>
</task>

</tasks>

<threat_model asvs_level="L1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Ignore-file data → Dialyzer verdict | A broad, duplicate, or stale filter could hide a new actionable warning. |
| Local branch → GitHub Nightly | A run from another SHA/toolchain could be misrepresented as supported evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-126-01 | Tampering | `.dialyzer_ignore.exs` | high | mitigate | Validate exact tuple shape, owner existence, duplicates, strict descriptions, and a closed atom allowlist before edits. |
| T-126-02 | Spoofing / Repudiation | supported Nightly receipt | high | mitigate | Bind workflow_dispatch `headSha`, job URL, toolchain, conclusion, and summary row to the pushed tracer commit. |
| T-126-03 | Tampering | CI/release topology | high | mitigate | Existing topology/cache ExUnit contracts plus an explicit forbidden-surface diff audit. |
| T-126-SC | Tampering | package supply chain | low | accept | No dependency or package install is introduced. |
</threat_model>

<verification>
Run the focused policy, lane, and cache tests; verify the immutable 45-entry receipt; inspect the exact-head supported Nightly jobs; and confirm workflows, dependencies, and release policy are unchanged.
</verification>

<success_criteria>
- TYPE-02 has a permanent policy tracer without changing Nightly topology.
- The 45-entry/18-file starting state is immutable and auditable.
- No later source task can cite unsupported local Dialyzer output as acceptance evidence.
</success_criteria>

<output>
Create `.planning/phases/126-curated-type-ratchet/126-01-SUMMARY.md` when done.
</output>

---
phase: 126-curated-type-ratchet
plan: "06"
type: execute
wave: 6
depends_on: ["126-05"]
files_modified:
  - .dialyzer_ignore.exs
  - .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md
  - lib/rindle/storage/s3.ex
  - test/rindle/storage/s3_test.exs
  - test/rindle/storage/s3_tus_test.exs
autonomous: true
requirements: [TYPE-01, TYPE-02, SAFE-01]
must_haves:
  truths:
    - "S3 stream/tail warnings are exposed on the supported home cell before adapter edits."
    - "S3 uploads, multipart/tus streaming, endpoint behavior, cleanup, and tagged errors remain unchanged."
    - "Every retained S3 warning is exact, supported, and owner-tested."
    - "No new or unowned warning appears; before Plan 126-07 the final supported result may be red only for exact later-owned E38-E40 Tus warnings, with Nightly Summary recording Dialyzer failure."
  artifacts:
    - path: .dialyzer_ignore.exs
      provides: "curated S3 analyzer-noise remainder"
    - path: .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md
      provides: "S3 supported dispositions"
  key_links:
    - from: lib/rindle/storage/s3.ex
      to: test/rindle/storage/s3_tus_test.exs
      via: "stream/tail multipart behavior"
      pattern: "stream"
---

<objective>
Retire actionable S3 stream/tail warnings under an isolated supported-cell probe.

Purpose: Keep S3's high-risk adapter boundary separate from GCS/Local execution and review.
Output: Supported dispositions, focused S3 proof, and the smallest justified S3 remainder.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/126-curated-type-ratchet/126-RESEARCH.md
@.planning/phases/126-curated-type-ratchet/126-PATTERNS.md
@.planning/phases/126-curated-type-ratchet/126-VALIDATION.md
@.planning/phases/126-curated-type-ratchet/126-05-SUMMARY.md
@.dialyzer_ignore.exs
@scripts/maintainer/refactor_contract.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe S3 adapter warnings on the supported cell</name>
  <precondition>The Plan 126-05 final Nightly has no GCS/Local/earlier or unowned warning and its only emitted annotations are exact E38 at `tus_creation.ex` plus E39-E40 at `tus_stream.ex`, with Dialyzer failure recorded by Nightly Summary; `gh auth status` succeeds.</precondition>
  <read_first>.dialyzer_ignore.exs, lib/rindle/storage/s3.ex, test/rindle/storage/s3_test.exs, test/rindle/storage/s3_tus_test.exs, test/rindle/storage/s3_public_endpoint_test.exs, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</read_first>
  <files>.dialyzer_ignore.exs, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</files>
  <action>Remove only strict-description filters for the S3 adapter. Commit/push the source-unchanged probe and obtain or verified-reuse the exact-head Nightly receipt under the timestamped capture, unique-ID polling, and terminal named-job protocol in `126-PATTERNS.md`. Inspect Dialyzer and Nightly Summary and record every reproduced or absent discriminator. No S3 source edit or restoration may precede this supported receipt, and unsupported local output cannot classify an entry.</action>
  <verify><automated>set -e; HEAD_SHA=$(git rev-parse HEAD); REPO=$(gh repo view --json nameWithOwner -q .nameWithOwner); BRANCH=$(git branch --show-current); RUN_ID=$(gh api --method GET --paginate --slurp "repos/$REPO/actions/workflows/nightly.yml/runs" -f branch="$BRANCH" -f event=workflow_dispatch -f per_page=100 | jq -er --arg sha "$HEAD_SHA" '[.[].workflow_runs[] | select(.head_sha == $sha and .event == "workflow_dispatch" and .status == "completed")] | if length == 1 then .[0].id else error("expected exactly one terminal exact-head Nightly receipt") end'); RUN_JSON=$(gh run view "$RUN_ID" --json headSha,event,status,conclusion,jobs); test "$(printf '%s' "$RUN_JSON" | jq -r .headSha)" = "$HEAD_SHA"; test "$(printf '%s' "$RUN_JSON" | jq -r .event)" = workflow_dispatch; test "$(printf '%s' "$RUN_JSON" | jq -r .status)" = completed; gh run view "$RUN_ID" --log > /tmp/rindle-126-s3-probe.log; test -s /tmp/rindle-126-s3-probe.log; rg -q 'storage/s3' .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md; MIX_ENV=test mix test test/install_smoke/dialyzer_ignore_policy_test.exs --seed 0</automated></verify>
  <acceptance_criteria>Every S3 filter has exact supported-head evidence while S3 source remains unchanged.</acceptance_criteria>
  <done>The S3 warning set is classified before code changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile S3 stream/tail helpers and prove the slice</name>
  <read_first>.planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md, lib/rindle/storage/s3.ex, test/rindle/storage/s3_test.exs, test/rindle/storage/s3_tus_test.exs, test/rindle/storage/s3_public_endpoint_test.exs, .dialyzer_ignore.exs</read_first>
  <files>lib/rindle/storage/s3.ex, test/rindle/storage/s3_test.exs, test/rindle/storage/s3_tus_test.exs, .dialyzer_ignore.exs, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</files>
  <behavior>
    - S3 upload, multipart/tus streaming, tail truncation/copy, endpoint, cleanup, and tagged error behavior remains unchanged.
    - Private helpers remain only when reachable under the supported type contract.
  </behavior>
  <action>Use supported warning text to remove genuinely unreachable private helpers only after focused behavior proves parity, or correct truthful specs/patterns when reachable. Do not delete a branch based solely on local analysis. Restore only exact reproduced analyzer noise with adjacent evidence, run focused S3 tests plus SAFE-01, commit/push, and obtain or verified-reuse its exact-head Nightly receipt per the deterministic timestamp/unique-ID/terminal polling protocol in `126-PATTERNS.md`. Aggregate all annotation pages and compare the complete warning multiset. Require this plan's owned warning set to be absent or explicitly retained with supported rationale and reject every fourth/new/unowned warning. Until Plan 126-07, the only permitted emitted warnings are exactly E38 text at `lib/rindle/upload/tus_creation.ex` plus E39-E40 text at `lib/rindle/upload/tus_stream.ex`; require Dialyzer `failure`, Nightly Summary `success`, and its log to record `DIALYZER: failure`.</action>
  <verify><automated>set -e; MIX_ENV=test mix test test/rindle/storage/s3_test.exs test/rindle/storage/s3_tus_test.exs test/rindle/storage/s3_public_endpoint_test.exs --seed 0; bash scripts/maintainer/refactor_contract.sh; HEAD_SHA=$(git rev-parse HEAD); REPO=$(gh repo view --json nameWithOwner -q .nameWithOwner); BRANCH=$(git branch --show-current); RUN_ID=$(gh api --method GET --paginate --slurp "repos/$REPO/actions/workflows/nightly.yml/runs" -f branch="$BRANCH" -f event=workflow_dispatch -f per_page=100 | jq -er --arg sha "$HEAD_SHA" '[.[].workflow_runs[] | select(.head_sha == $sha and .event == "workflow_dispatch" and .status == "completed")] | if length == 1 then .[0].id else error("expected exactly one terminal exact-head Nightly receipt") end'); RUN_JSON=$(gh run view "$RUN_ID" --json headSha,event,status,conclusion,jobs); test "$(printf '%s' "$RUN_JSON" | jq -r .headSha)" = "$HEAD_SHA"; test "$(printf '%s' "$RUN_JSON" | jq -r .event)" = workflow_dispatch; test "$(printf '%s' "$RUN_JSON" | jq -r .status)" = completed; DIALYZER_JOB=$(gh run view "$RUN_ID" --json jobs --jq '.jobs[] | select(.name == "Dialyzer") | .databaseId'); SUMMARY_JOB=$(gh run view "$RUN_ID" --json jobs --jq '.jobs[] | select(.name == "Nightly Summary") | .databaseId'); test "$(gh run view "$RUN_ID" --json jobs --jq '.jobs[] | select(.name == "Dialyzer") | .conclusion')" = failure; test "$(gh run view "$RUN_ID" --json jobs --jq '.jobs[] | select(.name == "Nightly Summary") | .conclusion')" = success; REPO=$(gh repo view --json nameWithOwner -q .nameWithOwner); gh api --paginate --slurp "repos/$REPO/check-runs/$DIALYZER_JOB/annotations?per_page=100" | jq 'add' > /tmp/rindle-126-06-annotations.json; jq -e '[.[] | select(.annotation_level == "warning") | {path, message}] as $w | ($w | length) == 3 and ($w | map(select(.path == "lib/rindle/upload/tus_creation.ex" and .message == "The pattern can never match the type {:error, _}.")) | length) == 1 and ($w | map(select(.path == "lib/rindle/upload/tus_stream.ex" and .message == "The guard test _@1::\u0027nil\u0027 | crypto:hash_state() breaks the opaqueness of its argument.")) | length) == 1 and ($w | map(select(.path == "lib/rindle/upload/tus_stream.ex" and .message == "The guard clause can never succeed.")) | length) == 1' /tmp/rindle-126-06-annotations.json; gh run view "$RUN_ID" --job "$DIALYZER_JOB" --log > /tmp/rindle-126-06-dialyzer.log; test "$(sed -n 's/.*##\[warning\]//p' /tmp/rindle-126-06-dialyzer.log | wc -l | tr -d ' ')" -eq 3; gh run view "$RUN_ID" --job "$SUMMARY_JOB" --log | grep -Fq 'DIALYZER: failure'</automated></verify>
  <acceptance_criteria>Owner behavior and SAFE-01 pass, no obsolete/actionable S3 suppression remains, no new or unowned warning appears, and the only emitted warnings are exactly E38-E40 while Nightly Summary honestly records Dialyzer failure.</acceptance_criteria>
  <done>The S3 warning group is reduced to explicit supported analyzer noise, if any.</done>
</task>

</tasks>

<threat_model asvs_level="L1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| External S3 streams → adapter callbacks | Type edits could change bounded streaming, cleanup, or error behavior. |
| Opaque dependency terms → local patterns | Analyzer appeasement could break abstraction safety. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-126-01 | Tampering | S3 ignore group | high | mitigate | Supported source-unchanged probe before code and per-entry disposition. |
| T-126-06 | Tampering / Denial of service | S3 streams | high | mitigate | Focused stream, cleanup, endpoint, and error tests plus SAFE-01. |
| T-126-07 | Information disclosure / Tampering | opaque terms | high | mitigate | Preserve opacity and retain only exact supported analyzer noise. |
| T-126-SC | Tampering | package supply chain | low | accept | No dependency or package install is introduced. |
</threat_model>

<verification>
Require a source-unchanged supported probe, focused S3 tests, SAFE-01, policy validation, and an exact-head annotation set containing only E38 at `tus_creation.ex` plus E39-E40 at `tus_stream.ex`, with Nightly Summary recording Dialyzer failure.
</verification>

<success_criteria>
- All S3 suppressions have supported dispositions.
- No multipart, stream, tail, endpoint, cleanup, capability, or tagged-error behavior changes.
- No dependency, workflow, schema/migration, or release-topology change occurs.
</success_criteria>

<output>
Create `.planning/phases/126-curated-type-ratchet/126-06-SUMMARY.md` when done.
</output>

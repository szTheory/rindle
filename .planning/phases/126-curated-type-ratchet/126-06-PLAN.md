---
phase: 126-curated-type-ratchet
plan: "06"
type: execute
wave: 6
depends_on: ["126-05"]
files_modified:
  - .dialyzer_ignore.exs
  - .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md
  - lib/rindle/storage/s3.ex
  - test/rindle/storage/s3_test.exs
  - test/rindle/storage/s3_tus_test.exs
autonomous: true
requirements: [TYPE-01, TYPE-02, SAFE-01]
must_haves:
  truths:
    - "S3 stream/tail warnings are exposed on the supported home cell before adapter edits."
    - "S3 uploads, multipart/tus streaming, endpoint behavior, cleanup, and tagged errors remain unchanged."
    - "Every retained S3 warning is exact, supported, and owner-tested."
  artifacts:
    - path: .dialyzer_ignore.exs
      provides: "curated S3 analyzer-noise remainder"
    - path: .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md
      provides: "S3 supported dispositions"
  key_links:
    - from: lib/rindle/storage/s3.ex
      to: test/rindle/storage/s3_tus_test.exs
      via: "stream/tail multipart behavior"
      pattern: "stream"
---

<objective>
Retire actionable S3 stream/tail warnings under an isolated supported-cell probe.

Purpose: Keep S3's high-risk adapter boundary separate from GCS/Local execution and review.
Output: Supported dispositions, focused S3 proof, and the smallest justified S3 remainder.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/126-curated-type-ratchet/126-RESEARCH.md
@.planning/phases/126-curated-type-ratchet/126-PATTERNS.md
@.planning/phases/126-curated-type-ratchet/126-VALIDATION.md
@.planning/phases/126-curated-type-ratchet/126-05-SUMMARY.md
@.dialyzer_ignore.exs
@scripts/maintainer/refactor_contract.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe S3 adapter warnings on the supported cell</name>
  <precondition>The GCS/Local slice is supported-green and `gh auth status` succeeds.</precondition>
  <read_first>.dialyzer_ignore.exs, lib/rindle/storage/s3.ex, test/rindle/storage/s3_test.exs, test/rindle/storage/s3_tus_test.exs, test/rindle/storage/s3_public_endpoint_test.exs, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</read_first>
  <files>.dialyzer_ignore.exs, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</files>
  <action>Remove only strict-description filters for the S3 adapter. Commit/push the source-unchanged probe and dispatch Nightly for that exact head. Inspect Dialyzer and Nightly Summary and record every reproduced or absent discriminator. No S3 source edit or restoration may precede this supported receipt, and unsupported local output cannot classify an entry.</action>
  <verify><automated>set -e; HEAD_SHA=$(git rev-parse HEAD); RUN_ID=$(gh run list --workflow nightly.yml --branch "$(git branch --show-current)" --event workflow_dispatch --commit "$HEAD_SHA" --json databaseId --jq '.[0].databaseId'); test -n "$RUN_ID"; gh run view "$RUN_ID" --log > /tmp/rindle-126-s3-probe.log; test -s /tmp/rindle-126-s3-probe.log; rg -q 'storage/s3' .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md; MIX_ENV=test mix test test/install_smoke/dialyzer_ignore_policy_test.exs --seed 0</automated></verify>
  <acceptance_criteria>Every S3 filter has exact supported-head evidence while S3 source remains unchanged.</acceptance_criteria>
  <done>The S3 warning set is classified before code changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile S3 stream/tail helpers and prove the slice</name>
  <read_first>.planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md, lib/rindle/storage/s3.ex, test/rindle/storage/s3_test.exs, test/rindle/storage/s3_tus_test.exs, test/rindle/storage/s3_public_endpoint_test.exs, .dialyzer_ignore.exs</read_first>
  <files>lib/rindle/storage/s3.ex, test/rindle/storage/s3_test.exs, test/rindle/storage/s3_tus_test.exs, .dialyzer_ignore.exs, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</files>
  <behavior>
    - S3 upload, multipart/tus streaming, tail truncation/copy, endpoint, cleanup, and tagged error behavior remains unchanged.
    - Private helpers remain only when reachable under the supported type contract.
  </behavior>
  <action>Use supported warning text to remove genuinely unreachable private helpers only after focused behavior proves parity, or correct truthful specs/patterns when reachable. Do not delete a branch based solely on local analysis. Restore only exact reproduced analyzer noise with adjacent evidence, run focused S3 tests plus SAFE-01, commit/push, dispatch Nightly, and require Dialyzer plus Nightly Summary success.</action>
  <verify><automated>set -e; MIX_ENV=test mix test test/rindle/storage/s3_test.exs test/rindle/storage/s3_tus_test.exs test/rindle/storage/s3_public_endpoint_test.exs --seed 0; bash scripts/maintainer/refactor_contract.sh; HEAD_SHA=$(git rev-parse HEAD); RUN_ID=$(gh run list --workflow nightly.yml --branch "$(git branch --show-current)" --event workflow_dispatch --commit "$HEAD_SHA" --json databaseId --jq '.[0].databaseId'); test -n "$RUN_ID"; test "$(gh run view "$RUN_ID" --json jobs --jq '.jobs[] | select(.name == "Dialyzer") | .conclusion')" = success; test "$(gh run view "$RUN_ID" --json jobs --jq '.jobs[] | select(.name == "Nightly Summary") | .conclusion')" = success</automated></verify>
  <acceptance_criteria>The exact S3 slice head is supported-green; owner behavior and SAFE-01 pass; no obsolete/actionable S3 suppression remains.</acceptance_criteria>
  <done>The S3 warning group is reduced to explicit supported analyzer noise, if any.</done>
</task>

</tasks>

<threat_model asvs_level="L1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| External S3 streams → adapter callbacks | Type edits could change bounded streaming, cleanup, or error behavior. |
| Opaque dependency terms → local patterns | Analyzer appeasement could break abstraction safety. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-126-01 | Tampering | S3 ignore group | high | mitigate | Supported source-unchanged probe before code and per-entry disposition. |
| T-126-06 | Tampering / Denial of service | S3 streams | high | mitigate | Focused stream, cleanup, endpoint, and error tests plus SAFE-01. |
| T-126-07 | Information disclosure / Tampering | opaque terms | high | mitigate | Preserve opacity and retain only exact supported analyzer noise. |
| T-126-SC | Tampering | package supply chain | low | accept | No dependency or package install is introduced. |
</threat_model>

<verification>
Require a source-unchanged supported probe, focused S3 tests, SAFE-01, policy validation, and exact-head supported green.
</verification>

<success_criteria>
- All S3 suppressions have supported dispositions.
- No multipart, stream, tail, endpoint, cleanup, capability, or tagged-error behavior changes.
- No dependency, workflow, schema/migration, or release-topology change occurs.
</success_criteria>

<output>
Create `.planning/phases/126-curated-type-ratchet/126-06-SUMMARY.md` when done.
</output>

---
phase: 126-curated-type-ratchet
plan: "06"
type: execute
wave: 6
depends_on: ["126-05"]
files_modified:
  - .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md
autonomous: true
requirements: [TYPE-01, TYPE-02, SAFE-01]
must_haves:
  truths:
    - "The final candidate passes policy, focused owner proof, SAFE-01, `mix ci`, and repository hygiene with no forbidden surface drift."
    - "The exact same pushed branch head has a successful supported Nightly Dialyzer job, a Nightly Summary reporting Dialyzer success, and a successful PR CI Summary."
    - "Issue #76 closes only when all 45 dispositions are complete, no obsolete/actionable filter remains, every retained analyzer-noise entry has explicit supported evidence, and exact-head authorities are green; otherwise it remains open with one narrowed comment."
  artifacts:
    - path: .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md
      provides: "complete local, supported Nightly, PR CI, and issue disposition receipt"
    - path: .dialyzer_ignore.exs
      provides: "final curated baseline with only explicitly evidenced analyzer noise, if any"
  key_links:
    - from: .dialyzer_ignore.exs
      to: .github/workflows/nightly.yml
      via: "hashed PLT input consumed by gating Dialyzer job"
      pattern: "hashFiles('mix.exs', 'mix.lock', '.dialyzer_ignore.exs')"
    - from: .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md
      to: GitHub issue 76
      via: "sanitized 45-entry disposition and exact-head authority links"
      pattern: "TYPE-01|TYPE-02"
---

<objective>
Prove the final curated baseline locally and on both exact-head GitHub authorities, then disposition issue #76 without overclaiming.

Purpose: Convert owner-level work into the sole supported acceptance receipt while preserving the green-main release train.
Output: Complete evidence, exact-head Nightly and PR CI results, and a conditional issue close or narrowing comment.
</objective>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- Final `126-TYPE-EVIDENCE.md` with 45 entry dispositions, retained-noise evidence, local gates, supported Nightly, PR CI Summary, and issue outcome.
- Final curated `.dialyzer_ignore.exs`; zero entries are not required, but every remainder is exact and justified.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RUNNING.md
@guides/release_publish.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/126-curated-type-ratchet/126-RESEARCH.md
@.planning/phases/126-curated-type-ratchet/126-PATTERNS.md
@.planning/phases/126-curated-type-ratchet/126-VALIDATION.md
@.planning/phases/126-curated-type-ratchet/126-05-SUMMARY.md
@.planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md
@.dialyzer_ignore.exs
@mix.exs
@.github/workflows/nightly.yml
@test/install_smoke/ci_lane_split_test.exs
@test/install_smoke/ci_cache_hygiene_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close local policy, preservation, CI, hygiene, and scope gates</name>
  <read_first>.planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md, .dialyzer_ignore.exs, mix.exs, .github/workflows/nightly.yml, test/install_smoke/dialyzer_ignore_policy_test.exs, test/install_smoke/ci_lane_split_test.exs, test/install_smoke/ci_cache_hygiene_test.exs, scripts/maintainer/refactor_contract.sh, guides/release_publish.md, RUNNING.md</read_first>
  <files>.planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</files>
  <action>Verify all 45 starting rows have exactly one disposition; reject any `pending`, obsolete retained entry, actionable retained entry, broad filter, duplicate, missing owner, or retained row lacking supported run/job/warning and owner behavior evidence. Run the policy/topology/cache tests, every focused owner command named in the validation map, SAFE-01, `mix ci`, and `./scripts/maintainer/repo_hygiene_check.sh --ci`. Compare against the ledger's `implementation_base_sha` and allow only the enumerated warning-owner source/test files, `.dialyzer_ignore.exs`, the policy test, the evidence ledger, summaries, and phase planning metadata. Fail on changes to dependencies, lockfile, workflows/topology/cache/jobs/schedule, release policy, schemas/migrations, public docs/APIs, telemetry vocabulary, errors, or Admin behavior. Record command results and the final candidate SHA, commit the receipt, and do not make further repository changes before Task 2.</action>
  <verify><automated>set -e; MIX_ENV=test mix test test/install_smoke/dialyzer_ignore_policy_test.exs test/install_smoke/ci_lane_split_test.exs test/install_smoke/ci_cache_hygiene_test.exs --seed 0; bash scripts/maintainer/refactor_contract.sh; mix ci; ./scripts/maintainer/repo_hygiene_check.sh --ci; ! rg -n '\| pending \|' .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</automated></verify>
  <acceptance_criteria>All local/structural gates pass on a clean candidate, 45 dispositions are complete, every retained row is supported and behavior-backed, and forbidden surfaces are unchanged.</acceptance_criteria>
  <done>The committed final candidate is locally green and auditable before external acceptance.</done>
</task>

<task type="auto">
  <name>Task 2: Obtain exact-head Nightly and PR authorities and close or narrow #76</name>
  <precondition>The final candidate receipt is committed, the working tree is clean, and `gh auth status` succeeds.</precondition>
  <read_first>.planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md, .github/workflows/nightly.yml, test/install_smoke/ci_lane_split_test.exs, guides/release_publish.md, RUNNING.md</read_first>
  <files>None — branch push, workflow dispatch/inspection, PR checks, and issue mutation only</files>
  <action>Push the current Phase 126 branch and create or update its PR to `main` without merging. Capture final HEAD and require the PR `headRefOid` to match. Wait for required PR checks and require `CI Summary` success on that exact head. Dispatch `nightly.yml` on the branch, select the workflow_dispatch run whose `headSha` equals final HEAD, wait terminal, require `Dialyzer` success, inspect `Nightly Summary`, and require its rendered Dialyzer row to report success. Confirm the job used literal Elixir 1.17 / OTP 27 and `mix dialyzer --format github`. Inspect all 45 dispositions again. Close #76 only if there is no obsolete/actionable suppression, each retained entry has explicit supported-cell evidence, policy/local/SAFE-01/mix-ci/hygiene are green, and both exact-head authorities pass. Post a concise sanitized comment with final SHA, retained count/rationales, Nightly/Dialyzer/Summary links, and PR CI Summary link. If any condition fails, post the exact unmet condition and links, leave #76 open, and do not retry into a different head, merge, publish, change topology, or force a release.</action>
  <verify><automated>set -e; FINAL_HEAD=$(git rev-parse HEAD); test -z "$(git status --porcelain)"; test "$FINAL_HEAD" = "$(gh pr view --json headRefOid -q .headRefOid)"; gh pr checks --required --json name,state,link > /tmp/rindle-126-pr-checks.json; test "$(jq -r '.[] | select(.name == "CI Summary") | .state' /tmp/rindle-126-pr-checks.json)" = SUCCESS; RUN_ID=$(gh run list --workflow nightly.yml --branch "$(git branch --show-current)" --event workflow_dispatch --commit "$FINAL_HEAD" --json databaseId --jq '.[0].databaseId'); test -n "$RUN_ID"; test "$(gh run view "$RUN_ID" --json jobs --jq '.jobs[] | select(.name == "Dialyzer") | .conclusion')" = success; SUMMARY_JOB=$(gh run view "$RUN_ID" --json jobs --jq '.jobs[] | select(.name == "Nightly Summary") | .databaseId'); gh run view "$RUN_ID" --job "$SUMMARY_JOB" --log | grep -Fq '| Dialyzer | success |'; ISSUE_STATE=$(gh issue view 76 --json state -q .state); LAST_COMMENT=$(gh issue view 76 --json comments -q '.comments[-1].body'); printf '%s' "$LAST_COMMENT" | grep -Fq "$FINAL_HEAD"; if ! rg -q '\| (pending|obsolete-retained|actionable-retained) \|' .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md; then test "$ISSUE_STATE" = CLOSED; else test "$ISSUE_STATE" = OPEN; fi</automated></verify>
  <acceptance_criteria>Nightly Dialyzer, Nightly Summary, and PR CI Summary all pass on the same final head; #76 is closed only with complete qualifying evidence, otherwise open with a concrete narrowing comment.</acceptance_criteria>
  <done>TYPE-01/TYPE-02 authority and issue state agree exactly, with no release or topology mutation.</done>
</task>

</tasks>

<threat_model asvs_level="L1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local candidate → supported GitHub jobs | Only an exact-head run on the locked toolchain may authorize acceptance. |
| CI/evidence → issue #76 state | External issue mutation must follow every acceptance condition. |
| Phase diff → release train | Quality work must not alter required checks, jobs, caches, schedules, dependencies, or release policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-126-02 | Spoofing / Repudiation | Nightly and PR receipts | high | mitigate | Final HEAD equals PR head and workflow `headSha`; inspect named jobs, command, toolchain, conclusions, links, and summary row. |
| T-126-01 | Tampering | final ignore ledger | high | mitigate | 45 complete dispositions, permanent policy test, and supported evidence for every retained exact filter. |
| T-126-09 | Elevation of privilege / Repudiation | issue #76 mutation | high | mitigate | Close only under the full deterministic predicate; otherwise comment and assert issue remains open. |
| T-126-03 | Tampering | CI/release topology | high | mitigate | Explicit phase allowlist diff, existing topology/cache tests, `mix ci`, and hygiene gate. |
| T-126-SC | Tampering | package supply chain | low | accept | No dependency or package install is introduced. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---:|---|---|
| GOAL | — | Supported home cell has an actionable enforced Dialyzer baseline with no retirement ambiguity | 01-06 | COVERED | Policy tracer, supported owner probes, complete ledger, and exact-head final authority. |
| REQ | TYPE-01 | 1.17/27 passes after each retained ignore is justified or removed; local noise is diagnostic | 01-06 | COVERED | Every owner group probes/finalizes on Nightly; final exact-head supported success required. |
| REQ | TYPE-02 | Curated gate rejects new actionable findings and issue #76 closes with evidence | 01, 05-06 | COVERED | Permanent policy test, unchanged gating Nightly, conditional issue disposition. |
| REQ | SAFE-01 | Preserve APIs, schemas/migrations, telemetry, errors, Admin, dependencies, CI/release | 02-06 | COVERED | Owner tests and SAFE-01 after every source task; final scope/mix-ci/hygiene/CI audits. |
| RESEARCH | authority | Literal Nightly Elixir 1.17 / OTP 27 Dialyzer job is sole acceptance authority | 01-06 | COVERED | Unsupported local Dialyzer never classifies entries. |
| RESEARCH | inventory | Reconcile 45 entries / 18 files / 8 atoms + 37 strict descriptions | 01-05 | COVERED | Immutable starting ledger and 45 complete dispositions. |
| RESEARCH | policy | Reject duplicates, broad/file-only filters, stale paths, and new atom debt | 01 | COVERED | Focused ExUnit policy tracer. |
| RESEARCH | migration/runtime/task/Admin | Fix truthful boundaries only with owner behavior and SAFE-01 | 02-03 | COVERED | Supported probes precede bounded owner corrections. |
| RESEARCH | storage | Treat stream/opaque findings as noise only after supported evidence and adapter proof | 04 | COVERED | GCS/Local/S3 slice. |
| RESEARCH | tus/Mux | Preserve opacity, protocol, response/error, lifecycle, and telemetry contracts | 05 | COVERED | Opaque-safe/spec-pattern corrections and focused owners. |
| RESEARCH | historical atoms | Remove absent Phase-34 filters; retain only exact reproduced supported noise | 03, 05 | COVERED | Runtime/HTML/process plus facade/Broker/promote groups. |
| RESEARCH | CI boundary | Do not change topology, cache, job, schedule, dependencies, or PR required checks | 01-06 | COVERED | Structural tests and final diff/hygiene/PR audits. |
| RESEARCH | issue #76 | Close only after no obsolete/actionable filters and all retained noise is justified | 06 | COVERED | Explicit close-or-narrow predicate. |
| CONTEXT | — | No CONTEXT.md; user approved autonomous recommended defaults | — | EXCLUDED | No D-NN decisions exist; explicit task constraints and research are authoritative. |

Excluded, not gaps: public API/schema/migration/telemetry/error/Admin redesign, dependencies/toolchain upgrades, CI topology/cache/schedule changes, moving Dialyzer to PR CI, release-policy changes, schema push, forced release, and deferred product work.
</source_audit>

<verification>
Run every local gate and scope audit, require final HEAD equality across branch/PR/Nightly, inspect Dialyzer and Nightly Summary plus PR CI Summary, and verify issue state follows the complete predicate.
</verification>

<success_criteria>
- TYPE-01, TYPE-02, and inherited SAFE-01 have complete evidence.
- Every GOAL/REQ/RESEARCH source item is covered and no deferred/out-of-scope item is implemented.
- Zero ignores are not required; any remainder is exact, supported, adjacent-commented, and owner-tested.
- Issue #76 closes only when its acceptance criteria are met; otherwise it remains open with a narrow evidence comment.
</success_criteria>

<output>
Create `.planning/phases/126-curated-type-ratchet/126-06-SUMMARY.md` when done.
</output>

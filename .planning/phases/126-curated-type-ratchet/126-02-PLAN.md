---
phase: 126-curated-type-ratchet
plan: "02"
type: execute
wave: 2
depends_on: ["126-01"]
files_modified:
  - .dialyzer_ignore.exs
  - .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md
  - lib/rindle/migration.ex
  - lib/rindle/migration/v1.ex
  - test/support/host_rindle_migration.ex
  - test/rindle/migration_fast_test.exs
  - test/rindle/migration_test.exs
autonomous: true
requirements: [TYPE-01, TYPE-02, SAFE-01]
must_haves:
  truths:
    - "Every migration/support suppression is first exposed in the supported 1.17/27 Nightly job and then removed or retained from that exact evidence."
    - "Truthful specs, dispatcher result types, and raising paths preserve all public migration entry points, fixed catalogs, DDL order, transactions, reversals, and errors."
    - "No retained migration entry lacks an adjacent supported-cell analyzer-noise rationale."
    - "The reconciled slice has no migration/support warning and no unowned warning; before Plan 126-07 only the exact later-owned E38-E40 Tus warnings may keep Dialyzer red, with Nightly Summary recording that failure."
  artifacts:
    - path: .dialyzer_ignore.exs
      provides: "supported-evidence-curated migration/support filters"
    - path: lib/rindle/migration.ex
      provides: "truthful public migration dispatcher type boundary"
    - path: lib/rindle/migration/v1.ex
      provides: "truthful V1 raising/preflight type boundary"
  key_links:
    - from: lib/rindle/migration.ex
      to: lib/rindle/migration/v1.ex
      via: "versioned up/down/move dispatcher"
      pattern: "dispatch|move_public_to_rindle|move_rindle_to_public"
    - from: test/support/host_rindle_migration.ex
      to: Rindle.Migration
      via: "host migration fixture delegates"
      pattern: "Rindle.Migration"
---

<objective>
Retire the migration and host-support warning group using supported-cell evidence and unchanged migration behavior.

Purpose: Make the public migration boundary truthful to Dialyzer without altering adopter migration semantics.
Output: Corrected specs/control-flow where actionable, exact retained-noise evidence where necessary, and an exact-head supported migration receipt whose only permitted global failure is the later-owned E38-E40 Tus set.
</objective>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- Migration/support rows in `126-TYPE-EVIDENCE.md` with exact probe and disposition evidence.
- A narrowed `.dialyzer_ignore.exs` in which no obsolete/actionable migration suppression remains.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/126-curated-type-ratchet/126-RESEARCH.md
@.planning/phases/126-curated-type-ratchet/126-PATTERNS.md
@.planning/phases/126-curated-type-ratchet/126-VALIDATION.md
@.planning/phases/126-curated-type-ratchet/126-01-SUMMARY.md
@.dialyzer_ignore.exs
@lib/rindle/migration.ex
@lib/rindle/migration/v1.ex
@test/support/host_rindle_migration.ex
@test/rindle/migration_fast_test.exs
@test/rindle/migration_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose and classify the exact migration/support warnings on the home cell</name>
  <precondition>Plan 126-01 policy/topology tests pass, its supported receipt records only the exact E38-E40 Tus warnings as pending, and `gh auth status` succeeds.</precondition>
  <read_first>.dialyzer_ignore.exs, lib/rindle/migration.ex, lib/rindle/migration/v1.ex, test/support/host_rindle_migration.ex, test/rindle/migration_fast_test.exs, test/rindle/migration_test.exs, .github/workflows/nightly.yml, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</read_first>
  <files>.dialyzer_ignore.exs, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</files>
  <action>Remove only the exact strict-description entries owned by `migration.ex`, `migration/v1.ex`, and `test/support/host_rindle_migration.ex`; commit and push that probe head without source edits. Dispatch Nightly on that head, wait for terminal status, and inspect the Dialyzer log plus Nightly Summary. Record every reproduced path/description, absent entry, run/job URL, and head SHA. Mark absent entries `obsolete`; classify reproduced entries as candidates only, never as analyzer noise from local output. Restore no filter and edit no source until this supported receipt is complete.</action>
  <verify><automated>set -e; HEAD_SHA=$(git rev-parse HEAD); RUN_ID=$(gh run list --workflow nightly.yml --branch "$(git branch --show-current)" --event workflow_dispatch --commit "$HEAD_SHA" --json databaseId --jq '.[0].databaseId'); test -n "$RUN_ID"; gh run view "$RUN_ID" --log > /tmp/rindle-126-migration-probe.log; test -s /tmp/rindle-126-migration-probe.log; rg -q 'migration|host_rindle_migration' .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md; MIX_ENV=test mix test test/install_smoke/dialyzer_ignore_policy_test.exs --seed 0</automated></verify>
  <acceptance_criteria>All migration/support entries have an exact supported-head observation before any production or support source change, and unrelated filters remain byte-unchanged.</acceptance_criteria>
  <done>The evidence ledger distinguishes obsolete entries from reproduced migration/support findings using only the 1.17/27 authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correct actionable public dispatcher and host-fixture type boundaries</name>
  <read_first>.planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md, lib/rindle/migration.ex, test/support/host_rindle_migration.ex, test/rindle/migration_fast_test.exs, test/rindle/migration_test.exs, test/install_smoke/docs_parity/install_and_migrations_test.exs, scripts/maintainer/refactor_contract.sh</read_first>
  <files>lib/rindle/migration.ex, test/support/host_rindle_migration.ex, test/rindle/migration_fast_test.exs, test/rindle/migration_test.exs, .dialyzer_ignore.exs, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</files>
  <behavior>
    - Public `up`, `down`, move, and dispatcher entry points retain their current arities, accepted options, return/raise behavior, and version routing.
    - Host migration fixture entry points preserve generated-app ownership and execute the same pinned Rindle migration calls.
  </behavior>
  <action>For only the supported findings in these two owners, add or tighten truthful specs/result unions and narrow unreachable dispatcher typing; change control flow only when owner tests first demonstrate identical calls/results/errors. Remove each exact filter only after its focused behavior is green. If a reproduced finding cannot be corrected without changing public API, migration DDL/schema, transaction order, reversal safety, or error vocabulary, restore that exact description filter with an adjacent comment citing the supported run/job and precise analyzer-noise reason. Do not weaken a term or raise path to satisfy Dialyzer.</action>
  <verify><automated>MIX_ENV=test mix test test/rindle/migration_fast_test.exs test/rindle/migration_test.exs test/install_smoke/docs_parity/install_and_migrations_test.exs --seed 0 &amp;&amp; bash scripts/maintainer/refactor_contract.sh</automated></verify>
  <acceptance_criteria>All actionable `migration.ex` and host-fixture findings are fixed and unsuppressed; behavior and SAFE-01 pass; each retained entry has explicit supported-cell evidence.</acceptance_criteria>
  <done>The public dispatcher and host fixture have truthful type boundaries without migration contract drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Correct the V1 raising/preflight boundary and prove the final migration slice</name>
  <read_first>.planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md, lib/rindle/migration/v1.ex, lib/rindle/migration/v1/preflight.ex, lib/rindle/migration/v1/snapshot.ex, test/rindle/migration_fast_test.exs, test/rindle/migration_test.exs, .dialyzer_ignore.exs</read_first>
  <files>lib/rindle/migration/v1.ex, test/rindle/migration_fast_test.exs, test/rindle/migration_test.exs, .dialyzer_ignore.exs, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</files>
  <behavior>
    - Preflight refusal atoms and raising paths remain exhaustive and preserve current precedence and messages.
    - The V1 fixed catalog, DDL, ordered moves, transaction authority, rollback, and privilege refusals remain unchanged.
  </behavior>
  <action>Use the supported probe text to correct only actionable V1 pattern/spec precision, adding focused tests before implementation for any previously unrepresented refusal. Preserve Snapshot/Preflight ownership and V1 mutation authority. Apply the same exact-filter disposition rule as Task 2, update every ledger row, then commit/push the reconciled slice and dispatch a new Nightly on that exact head. Inspect the final Dialyzer annotations and log: every migration/support warning must be absent or explicitly retained with its supported rationale, no new or unowned warning may appear, and the only permitted emitted warnings are the exact three later-owned E38-E40 Tus `{path, description}` identities. In that permitted intermediate state, require the Dialyzer job to conclude `failure`, Nightly Summary to conclude `success`, and its log to record `DIALYZER: failure`; do not describe the global gate as green. Plan 126-07 retains ownership of clearing E38-E40.</action>
  <verify><automated>set -e; MIX_ENV=test mix test test/rindle/migration_fast_test.exs test/rindle/migration_test.exs --seed 0; bash scripts/maintainer/refactor_contract.sh; HEAD_SHA=$(git rev-parse HEAD); RUN_ID=$(gh run list --workflow nightly.yml --branch "$(git branch --show-current)" --event workflow_dispatch --commit "$HEAD_SHA" --json databaseId --jq '.[0].databaseId'); test -n "$RUN_ID"; DIALYZER_JOB=$(gh run view "$RUN_ID" --json jobs --jq '.jobs[] | select(.name == "Dialyzer") | .databaseId'); SUMMARY_JOB=$(gh run view "$RUN_ID" --json jobs --jq '.jobs[] | select(.name == "Nightly Summary") | .databaseId'); test "$(gh run view "$RUN_ID" --json jobs --jq '.jobs[] | select(.name == "Dialyzer") | .conclusion')" = failure; test "$(gh run view "$RUN_ID" --json jobs --jq '.jobs[] | select(.name == "Nightly Summary") | .conclusion')" = success; REPO=$(gh repo view --json nameWithOwner -q .nameWithOwner); gh api "repos/$REPO/check-runs/$DIALYZER_JOB/annotations?per_page=100" &gt; /tmp/rindle-126-02-annotations.json; jq -e '[.[] | select(.annotation_level == "warning") | {path, message}] as $w | ($w | length) == 3 and ($w | map(select(.path == "lib/rindle/upload/tus_creation.ex" and .message == "The pattern can never match the type {:error, _}.")) | length) == 1 and ($w | map(select(.path == "lib/rindle/upload/tus_stream.ex" and .message == "The guard test _@1::\u0027nil\u0027 | crypto:hash_state() breaks the opaqueness of its argument.")) | length) == 1 and ($w | map(select(.path == "lib/rindle/upload/tus_stream.ex" and .message == "The guard clause can never succeed.")) | length) == 1' /tmp/rindle-126-02-annotations.json; gh run view "$RUN_ID" --job "$DIALYZER_JOB" --log &gt; /tmp/rindle-126-02-dialyzer.log; test "$(sed -n 's/.*##\[warning\]//p' /tmp/rindle-126-02-dialyzer.log | wc -l | tr -d ' ')" -eq 3; gh run view "$RUN_ID" --job "$SUMMARY_JOB" --log | grep -Fq 'DIALYZER: failure'</automated></verify>
  <acceptance_criteria>The exact reconciled migration head has no migration/support or unowned warning; its only emitted warnings are exactly E38 at `lib/rindle/upload/tus_creation.ex` and E39-E40 at `lib/rindle/upload/tus_stream.ex` (the supported annotations corresponding to the later-owned Tus ledger rows), Dialyzer honestly fails and Nightly Summary records that failure, focused migration suites and SAFE-01 pass, and every original migration/support entry has a complete disposition.</acceptance_criteria>
  <done>No obsolete or actionable migration/support suppression remains.</done>
</task>

</tasks>

<threat_model asvs_level="L1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Supported warning log → source/filter disposition | Misclassification could hide a real migration defect. |
| Migration API → host database | Type-driven edits must not alter schema, DDL, transaction, or reversal behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-126-01 | Tampering | migration ignore group | high | mitigate | Exact supported probe before edits; one-row-per-filter disposition; strict policy test. |
| T-126-04 | Tampering / Elevation of privilege | migration V1 | high | mitigate | Focused privilege/refusal/rollback tests and SAFE-01 preserve fixed ownership and transaction semantics. |
| T-126-02 | Spoofing / Repudiation | supported run | high | mitigate | Bind each probe and final green job to pushed `headSha` and record run/job URLs. |
| T-126-SC | Tampering | package supply chain | low | accept | No dependency or package install is introduced. |
</threat_model>

<verification>
Require supported probe evidence before source edits, focused migration/generated-doc behavior, SAFE-01 after each source task, policy green, and an exact-head supported result containing only E38 at `tus_creation.ex` plus E39-E40 at `tus_stream.ex`, with Dialyzer failure recorded honestly by Nightly Summary.
</verification>

<success_criteria>
- Migration/support ignores are removed when obsolete or actionable.
- Any retained analyzer-noise description is exact and supported-cell-justified.
- Public API, schema/migration behavior, errors, and release topology remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/126-curated-type-ratchet/126-02-SUMMARY.md` when done.
</output>

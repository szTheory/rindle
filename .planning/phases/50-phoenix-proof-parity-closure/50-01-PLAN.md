---
phase: 50-phoenix-proof-parity-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/install_smoke/support/generated_app_helper.ex
  - test/install_smoke/generated_app_smoke_test.exs
autonomous: true
requirements:
  - PROOF-01
must_haves:
  truths:
    - "The generated-app `:tus` lane proves the documented Phoenix path through `Rindle.LiveView.allow_tus_upload/4` instead of only driving `Rindle.Upload.TusPlug` directly."
    - "The package-consumer proof preserves the existing real `tus-js-client` interrupt/resume flow and MinIO-backed drop-and-resume evidence."
    - "The persisted tus proof artifact records machine-readable Phoenix-facing metadata and state progression for audit use."
  artifacts:
    - path: "test/install_smoke/support/generated_app_helper.ex"
      provides: "Generated-app patching plus JSON proof-field emission for the Phoenix-facing tus lane"
      min_lines: 2400
    - path: "test/install_smoke/generated_app_smoke_test.exs"
      provides: "Install-smoke assertions that fail if the Phoenix proof fields or lower-level tus sub-proof drift"
      min_lines: 200
  key_links:
    - from: "test/install_smoke/support/generated_app_helper.ex"
      to: "tmp/install_smoke_tus_report.json"
      via: "write_tus_report!/1 and merge_tus_report!/1"
      pattern: "phoenix_state_transitions|phoenix_helper_contract|phoenix_completion_path"
    - from: "test/install_smoke/generated_app_smoke_test.exs"
      to: "test/install_smoke/support/generated_app_helper.ex"
      via: "report field assertions from GeneratedAppHelper.prove_package_install!(:tus)"
      pattern: "report\\.tus_report_data|report\\.tus_previous_uploads"
---

<objective>
Elevate the existing generated-app `:tus` install-smoke lane into the merge-blocking Phoenix / LiveView proof required by `PROOF-01`.

Purpose: make the documented Phoenix-facing adopter path executable and auditable without introducing a second Phoenix proof lane or weakening the existing transport/runtime sub-proof.
Output: Phoenix-aware machine-readable tus proof fields in the generated app plus smoke assertions that freeze the LiveView/helper path on top of the existing Node/MinIO resume proof.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/50-phoenix-proof-parity-closure/50-CONTEXT.md
@.planning/phases/50-phoenix-proof-parity-closure/50-RESEARCH.md
@.planning/phases/50-phoenix-proof-parity-closure/50-PATTERNS.md
@.planning/phases/49-liveview-tus-productization/49-CONTEXT.md
@.planning/phases/49-liveview-tus-productization/49-01-SUMMARY.md
@.planning/phases/49-liveview-tus-productization/49-02-SUMMARY.md
@.planning/phases/46-generated-app-tus-runtime-proof-recovery/46-CONTEXT.md
@.planning/phases/46-generated-app-tus-runtime-proof-recovery/46-01-SUMMARY.md
@.planning/phases/46-generated-app-tus-runtime-proof-recovery/46-02-SUMMARY.md
@guides/resumable_uploads.md
@lib/rindle/live_view.ex
@test/install_smoke/generated_app_smoke_test.exs
@test/install_smoke/support/generated_app_helper.ex

<interfaces>
From `lib/rindle/live_view.ex`:
```elixir
@spec allow_tus_upload(Phoenix.LiveView.Socket.t(), atom(), module(), keyword()) ::
        Phoenix.LiveView.Socket.t()

meta = %{
  uploader: "RindleTus",
  endpoint: path,
  upload_url: upload_url,
  session_id: session.id,
  asset_id: session.asset_id
}
```

From `guides/resumable_uploads.md`:
```text
Rindle.LiveView.allow_tus_upload/4
uploader: "RindleTus"
uploadUrl: entry.meta.upload_url
findPreviousUploads()
resumeFromPreviousUpload(previousUploads[0])
uploading / verifying / ready / error
consume_uploaded_entries/3
verify_completion/2
```

From `test/install_smoke/support/generated_app_helper.ex`:
```elixir
report = %{
  tus_upload_url: tus_report["upload_url"],
  tus_previous_uploads: tus_report["previous_uploads"],
  tus_byte_size: tus_report["byte_size"],
  tus_content_type: tus_report["content_type"],
  tus_ready_variants: tus_report["ready_variants"] || [],
  tus_report_path: tus_report_path,
  tus_debug_report_path: tus_debug_report_path,
  tus_report_data: tus_report,
  tus_debug_report_data: tus_debug_report
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the generated-app tus harness into the canonical Phoenix proof</name>
  <files>test/install_smoke/support/generated_app_helper.ex</files>
  <read_first>
    - test/install_smoke/support/generated_app_helper.ex
    - guides/resumable_uploads.md
    - lib/rindle/live_view.ex
    - test/rindle/live_view_test.exs
  </read_first>
  <behavior>
    - Test 1: the generated-app `:tus` lane uses `Rindle.LiveView.allow_tus_upload/4` and emits the helper metadata contract `uploader`, `endpoint`, `upload_url`, `session_id`, and `asset_id`.
    - Test 2: the persisted tus report records the honest Phoenix state progression `["uploading", "verifying", "ready"]` on the green path while preserving the existing Node interrupt/resume counts.
    - Test 3: the completion evidence names the canonical completion boundary `["consume_uploaded_entries/3", "verify_completion/2"]` instead of a new lifecycle.
  </behavior>
  <action>Per D-01, D-02, D-03, D-07, D-08, and D-09, keep the existing generated-app `:tus` lane as the only package-consumer Phoenix proof and extend `test/install_smoke/support/generated_app_helper.ex` so the generated app patches in a LiveView page that calls `Rindle.LiveView.allow_tus_upload/4`, uses the canonical `uploader: "RindleTus"` client path, still drives the real `tus-js-client` interrupt/resume sub-proof, and writes additive machine-readable fields into `tmp/install_smoke_tus_report.json`. Freeze these exact report keys and values on the green path: `phoenix_helper_contract: "allow_tus_upload/4"`, `phoenix_uploader: "RindleTus"`, `phoenix_endpoint: "/uploads/tus"`, `phoenix_upload_url_prefix: "/uploads/tus/"`, `phoenix_session_id_present: true`, `phoenix_asset_id_present: true`, `phoenix_state_transitions: ["uploading", "verifying", "ready"]`, `phoenix_completion_path: ["consume_uploaded_entries/3", "verify_completion/2"]`, and `phoenix_previous_uploads: <integer from resume discovery>`. Keep failure detail additive in the existing debug report path; do not create a second Phoenix lane, do not remove the direct interrupt/resume evidence, and do not widen the public Phoenix API beyond the shipped helper seam.</action>
  <acceptance_criteria>
    - `rg -n 'phoenix_helper_contract|phoenix_state_transitions|phoenix_completion_path|phoenix_previous_uploads' test/install_smoke/support/generated_app_helper.ex` finds the exact report keys.
    - `rg -n 'allow_tus_upload/4|RindleTus|consume_uploaded_entries/3|verify_completion/2' test/install_smoke/support/generated_app_helper.ex` shows the generated-app proof patches the documented adopter path.
    - The helper still invokes the real Node `tus-js-client` proof flow and still writes `upload_url`, `previous_uploads`, `byte_size`, `content_type`, and `ready_variants`.
  </acceptance_criteria>
  <verify>
    <automated>RINDLE_INSTALL_SMOKE_PROFILE=tus mix test test/install_smoke/generated_app_smoke_test.exs --include minio --max-failures 1</automated>
  </verify>
  <done>The generated-app tus proof is the canonical Phoenix proof lane, emits the required Phoenix-facing report keys, and still preserves the existing real interrupt/resume sub-proof under the same `:tus` lane.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tighten install-smoke assertions around the Phoenix report contract</name>
  <files>test/install_smoke/generated_app_smoke_test.exs</files>
  <read_first>
    - test/install_smoke/generated_app_smoke_test.exs
    - test/install_smoke/support/generated_app_helper.ex
    - guides/resumable_uploads.md
    - .planning/phases/50-phoenix-proof-parity-closure/50-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: the `:tus` smoke test asserts the Phoenix helper/report keys and state progression in addition to `previous_uploads` and ready variants.
    - Test 2: the smoke test still proves the lower-level drop-and-resume evidence (`previous_uploads >= 1`) instead of regressing to a fake-only Phoenix check.
  </behavior>
  <action>Per D-04, D-05, and D-12, extend the `:tus` branch in `test/install_smoke/generated_app_smoke_test.exs` so the merge-blocking smoke assertions check the exact Phoenix-facing report keys emitted by Task 1: `phoenix_helper_contract == "allow_tus_upload/4"`, `phoenix_uploader == "RindleTus"`, `phoenix_endpoint == "/uploads/tus"`, `phoenix_upload_url_prefix == "/uploads/tus/"`, `phoenix_session_id_present == true`, `phoenix_asset_id_present == true`, `phoenix_state_transitions == ["uploading", "verifying", "ready"]`, `phoenix_completion_path == ["consume_uploaded_entries/3", "verify_completion/2"]`, and `phoenix_previous_uploads >= 1`. Keep the existing assertions for `lifecycle_proved?`, `tus_previous_uploads >= 1`, `tus_byte_size`, `tus_content_type`, and `tus_ready_variants == ["poster", "web_720p"]`, and keep the failure-details printer pointed at `report.tus_report_path` and `report.tus_debug_report_path`.</action>
  <acceptance_criteria>
    - `rg -n 'phoenix_helper_contract|phoenix_state_transitions|phoenix_completion_path|phoenix_previous_uploads' test/install_smoke/generated_app_smoke_test.exs` finds the new smoke assertions.
    - `rg -n 'tus_previous_uploads|tus_ready_variants|tus_failure_details' test/install_smoke/generated_app_smoke_test.exs` confirms the pre-existing drop-and-resume proof remains in place.
    - The `:tus` branch remains the single smoke profile used for this proof path; no new Phoenix-specific profile or second generated-app proof module is introduced.
  </acceptance_criteria>
  <verify>
    <automated>RINDLE_INSTALL_SMOKE_PROFILE=tus mix test test/install_smoke/generated_app_smoke_test.exs --include minio --max-failures 1</automated>
  </verify>
  <done>The install-smoke suite fails fast if the generated-app Phoenix report or the preserved lower-level tus proof drifts from the documented contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated LiveView page -> tus uploader | Untrusted browser-side proof actions cross into the generated app and must keep server-owned offset truth intact |
| generated app -> `tmp/install_smoke_tus_report.json` | Proof evidence is serialized for later assertions and must stay machine-readable and contract-scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01-01 | Tampering | `test/install_smoke/support/generated_app_helper.ex` proof patching | mitigate | Prove the exact documented Phoenix path with fixed keys and fixed contract strings, per D-03 and D-07, instead of allowing alternate completion or uploader paths to satisfy the smoke lane. |
| T-50-01-02 | Repudiation | `tmp/install_smoke_tus_report.json` | mitigate | Persist additive machine-readable fields (`phoenix_helper_contract`, `phoenix_state_transitions`, `phoenix_completion_path`, `phoenix_previous_uploads`) so auditors can verify the Phoenix path without reading source history. |
| T-50-01-03 | Denial of service | generated-app proof lane | accept | The real Node/MinIO `tus-js-client` interrupt/resume sub-proof remains unchanged per D-02, avoiding a second heavier proof lane or snapshot gate that would widen CI cost without improving trust. |
</threat_model>

<verification>
Run `RINDLE_INSTALL_SMOKE_PROFILE=tus mix test test/install_smoke/generated_app_smoke_test.exs --include minio --max-failures 1` after each task and keep `bash scripts/install_smoke.sh tus` green before handing off to the next plan wave.
</verification>

<success_criteria>
- The generated-app `:tus` lane exercises `allow_tus_upload/4 -> RindleTus -> consume_uploaded_entries/3 -> verify_completion/2`.
- `tmp/install_smoke_tus_report.json` includes the exact Phoenix-facing report keys named in this plan.
- Existing transport/runtime proof fields (`upload_url`, `previous_uploads`, `byte_size`, `content_type`, `ready_variants`) still prove the lower-level drop-and-resume sub-proof.
</success_criteria>

<output>
After completion, create `.planning/phases/50-phoenix-proof-parity-closure/50-01-SUMMARY.md`
</output>

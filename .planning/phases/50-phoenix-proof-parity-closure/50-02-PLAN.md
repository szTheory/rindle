---
phase: 50-phoenix-proof-parity-closure
plan: "02"
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - test/install_smoke/phoenix_tus_truth_parity_test.exs
  - guides/resumable_uploads.md
  - lib/rindle/live_view.ex
  - test/rindle/live_view_test.exs
autonomous: true
requirements: [PROOF-01, PROOF-02]
must_haves:
  truths:
    - "Fast parity tests fail when the guide, helper metadata contract, generated-app proof fields, or honest Phoenix state semantics drift apart."
    - "The canonical guide remains the single Phoenix narrative, and `Rindle.LiveView` remains a thin helper seam over that guide."
    - "Local helper tests and parity tests reflect the same machine-readable Phoenix proof fields introduced by the generated-app proof."
  artifacts:
    - path: "test/install_smoke/phoenix_tus_truth_parity_test.exs"
      provides: "Fast support-truth parity gate for the Phoenix path"
      contains: "phoenix_state_sequence"
    - path: "test/rindle/live_view_test.exs"
      provides: "Low-cost helper metadata and completion-lane freeze"
      contains: "RindleTus"
    - path: "guides/resumable_uploads.md"
      provides: "Canonical Phoenix / LiveView contract strings"
      contains: "consume_uploaded_entries/3"
  key_links:
    - from: "guides/resumable_uploads.md"
      to: "test/install_smoke/phoenix_tus_truth_parity_test.exs"
      via: "Parity assertions freeze the exact helper path and honest state wording"
      pattern: "RindleTus|consume_uploaded_entries/3|verify_completion/2|uploading|verifying|ready|error"
    - from: "lib/rindle/live_view.ex"
      to: "test/rindle/live_view_test.exs"
      via: "Helper metadata and completion-lane semantics stay aligned with the generated-app proof"
      pattern: "uploader: \"RindleTus\"|upload_url|session_id|asset_id|verify_completion"
---

<objective>
Add narrow, fast parity gates that freeze the Phase 50 Phoenix proof contract
across the guide, helper seam, generated-app proof fields, and honest state
semantics.

Purpose: Make Phoenix tus support drift fail locally and early without adding
snapshot-heavy or template-wide noise.
Output: Tight parity tests, any minimal guide/helper/test wording adjustments
needed to stabilize those assertions, and local helper tests aligned with the
generated-app proof contract.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@test/install_smoke/phoenix_tus_truth_parity_test.exs
@guides/resumable_uploads.md
@lib/rindle/live_view.ex
@test/rindle/live_view_test.exs
@test/install_smoke/generated_app_smoke_test.exs
@test/install_smoke/support/generated_app_helper.ex
@.planning/phases/50-phoenix-proof-parity-closure/50-CONTEXT.md
@.planning/phases/50-phoenix-proof-parity-closure/50-RESEARCH.md
@.planning/phases/50-phoenix-proof-parity-closure/50-PATTERNS.md
@.planning/phases/50-phoenix-proof-parity-closure/50-VALIDATION.md
@.planning/phases/49-liveview-tus-productization/49-02-PLAN.md

<interfaces>
Canonical guide strings to preserve:
```markdown
`uploader: "RindleTus"`
`consume_uploaded_entries/3`
`verify_completion/2`
`uploading`, `verifying`, `ready`, and `error`
`100%` means bytes transferred
```

Canonical helper metadata to preserve:
```elixir
meta = %{
  uploader: "RindleTus",
  endpoint: path,
  upload_url: upload_url,
  session_id: session.id,
  asset_id: session.asset_id
}
```

Phoenix-facing proof keys introduced by Plan 01:
```json
{
  "phoenix_helper_uploader": "RindleTus",
  "phoenix_helper_endpoint": "/uploads/tus",
  "phoenix_helper_session_id": "session-id",
  "phoenix_helper_asset_id": "asset-id",
  "completion_surface": "consume_uploaded_entries->verify_completion",
  "phoenix_state_sequence": ["uploading", "verifying", "ready"]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Phoenix truth parity to freeze guide, helper seam, and generated-app proof fields together</name>
  <read_first>
    - test/install_smoke/phoenix_tus_truth_parity_test.exs
    - guides/resumable_uploads.md
    - lib/rindle/live_view.ex
    - test/install_smoke/generated_app_smoke_test.exs
    - test/install_smoke/support/generated_app_helper.ex
    - .planning/phases/50-phoenix-proof-parity-closure/50-CONTEXT.md
    - .planning/phases/50-phoenix-proof-parity-closure/50-PATTERNS.md
  </read_first>
  <files>test/install_smoke/phoenix_tus_truth_parity_test.exs, guides/resumable_uploads.md, lib/rindle/live_view.ex</files>
  <action>Expand `test/install_smoke/phoenix_tus_truth_parity_test.exs` so it reads the canonical guide, `lib/rindle/live_view.ex`, and the generated-app proof-harness source and freezes the exact Phase 50 contract strings. Add explicit assertions for `allow_tus_upload/4`, `uploader: "RindleTus"`, `consume_uploaded_entries/3`, `verify_completion/2`, the honest lifecycle vocabulary `uploading`, `verifying`, `ready`, and `error`, the `100%` means bytes transferred wording, and the generated proof keys `phoenix_helper_uploader`, `phoenix_helper_endpoint`, `phoenix_helper_upload_url`, `phoenix_helper_session_id`, `phoenix_helper_asset_id`, `completion_surface`, and `phoenix_state_sequence`. Tie `phoenix_helper_session_id` and `phoenix_helper_asset_id` back to the helper metadata contract by asserting that the same `session_id` and `asset_id` field names remain present in `lib/rindle/live_view.ex`. If any of those strings are missing or unstable in `guides/resumable_uploads.md` or `lib/rindle/live_view.ex`, make the minimal wording adjustment needed to restore one canonical guide narrative and one thin helper pointer; do not add a second Phoenix setup source.</action>
  <acceptance_criteria>
    - `rg -n 'allow_tus_upload/4|RindleTus|consume_uploaded_entries/3|verify_completion/2|uploading|verifying|ready|error|100%|phoenix_state_sequence|completion_surface|phoenix_helper_session_id|phoenix_helper_asset_id' test/install_smoke/phoenix_tus_truth_parity_test.exs` returns matches.
    - `mix test test/install_smoke/phoenix_tus_truth_parity_test.exs` exits `0`.
    - `rg -n 'uploader: "RindleTus"|consume_uploaded_entries/3|verify_completion/2|uploading|verifying|ready|error|100%' guides/resumable_uploads.md` returns matches.
    - `rg -n 'guides/resumable_uploads.md|allow_tus_upload/4|consume_uploaded_entries/3|session_id|asset_id' lib/rindle/live_view.ex` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "allow_tus_upload/4|RindleTus|consume_uploaded_entries/3|verify_completion/2|uploading|verifying|ready|error|100%|phoenix_state_sequence|completion_surface|phoenix_helper_session_id|phoenix_helper_asset_id" test/install_smoke/phoenix_tus_truth_parity_test.exs && mix test test/install_smoke/phoenix_tus_truth_parity_test.exs && rg -n "uploader: \"RindleTus\"|consume_uploaded_entries/3|verify_completion/2|uploading|verifying|ready|error|100%" guides/resumable_uploads.md && rg -n "guides/resumable_uploads.md|allow_tus_upload/4|consume_uploaded_entries/3|session_id|asset_id" lib/rindle/live_view.ex'</automated>
  </verify>
  <done>Fast parity now freezes the canonical guide strings, the thin helper seam, and the Phoenix-facing proof fields together.</done>
</task>

<task type="auto">
  <name>Task 2: Align local helper tests with the Phoenix proof contract and finish with the full package-consumer rerun</name>
  <read_first>
    - test/rindle/live_view_test.exs
    - lib/rindle/live_view.ex
    - test/install_smoke/support/generated_app_helper.ex
    - test/install_smoke/generated_app_smoke_test.exs
    - .planning/phases/50-phoenix-proof-parity-closure/50-VALIDATION.md
  </read_first>
  <files>test/rindle/live_view_test.exs</files>
  <action>Update `test/rindle/live_view_test.exs` only as needed so the local helper contract mirrors the same Phoenix-facing fields and completion-lane semantics proven by the generated app. Keep the existing assertions for `meta.uploader == "RindleTus"`, mounted endpoint, signed `upload_url`, session linkage, optional actor resolution, and `consume_uploaded_entries/3` -> `verify_completion/2`. Add any narrow assertions needed to mirror the exact report-facing contract from Plan 01, such as checking that the helper still yields `session_id` / `asset_id` and that completion remains the only readiness handoff. After those local assertions pass, rerun the full package-consumer smoke command `bash scripts/install_smoke.sh tus` so the final Phase 50 close-out has both fast parity and heavy proof green at the same time.</action>
  <acceptance_criteria>
    - `mix test test/rindle/live_view_test.exs` exits `0`.
    - `mix test test/install_smoke/phoenix_tus_truth_parity_test.exs test/rindle/live_view_test.exs` exits `0`.
    - `bash scripts/install_smoke.sh tus` exits `0`.
    - `rg -n '"phoenix_helper_uploader":"RindleTus"|"completion_surface":"consume_uploaded_entries->verify_completion"|"phoenix_state_sequence":\\["uploading","verifying","ready"\\]' tmp/install_smoke_tus_last_run.json` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'mix test test/rindle/live_view_test.exs && mix test test/install_smoke/phoenix_tus_truth_parity_test.exs test/rindle/live_view_test.exs && bash scripts/install_smoke.sh tus && rg -n "\"phoenix_helper_uploader\":\"RindleTus\"|\"completion_surface\":\"consume_uploaded_entries->verify_completion\"|\"phoenix_state_sequence\":\\[\"uploading\",\"verifying\",\"ready\"\\]" tmp/install_smoke_tus_last_run.json'</automated>
  </verify>
  <done>The local helper contract, fast parity gate, and merge-blocking generated-app proof all describe and prove the same narrow Phoenix path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canonical guide -> helper docs | The guide and helper seam must not drift into conflicting Phoenix stories. |
| Helper metadata -> generated-app proof fields | Local contract tests and package-consumer proof must freeze the same `RindleTus` metadata and completion-lane facts. |
| Fast parity -> heavy install-smoke proof | If parity ignores generated-app proof fields, drift can reappear while the heavy lane remains expensive to run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-02-01 | T | guide/helper/proof drift | mitigate | Add direct string-parity assertions tying guide, helper, and generated-app proof keys together. |
| T-50-02-02 | R | local helper tests | mitigate | Keep `test/rindle/live_view_test.exs` aligned with the same `RindleTus` metadata and completion-lane semantics proven by the generated app. |
| T-50-02-03 | T | final support claim | mitigate | End the phase with both fast parity and the full `bash scripts/install_smoke.sh tus` package-consumer proof green, backed by persisted JSON breadcrumbs. |
</threat_model>

<verification>
- `mix test test/install_smoke/phoenix_tus_truth_parity_test.exs`
- `mix test test/rindle/live_view_test.exs`
- `bash scripts/install_smoke.sh tus`
</verification>

<success_criteria>
- Fast parity fails on drift between guide, helper seam, generated-app proof fields, and honest state semantics.
- `Rindle.LiveView` remains a thin helper pointer to the canonical guide rather than becoming a second Phoenix setup source.
- Final Phase 50 evidence includes both fast local parity and a green package-consumer proof for the Phoenix path.
</success_criteria>

<output>
After completion, create `.planning/phases/50-phoenix-proof-parity-closure/50-02-SUMMARY.md`
</output>

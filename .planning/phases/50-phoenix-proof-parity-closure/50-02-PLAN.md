---
phase: 50-phoenix-proof-parity-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 50-01
files_modified:
  - test/install_smoke/phoenix_tus_truth_parity_test.exs
  - test/rindle/live_view_test.exs
autonomous: true
requirements:
  - PROOF-01
  - PROOF-02
must_haves:
  truths:
    - "Fast local tests fail when the guide, helper metadata, or generated-app Phoenix proof fields drift out of alignment."
    - "The helper metadata contract remains the same narrow Phoenix seam documented in Phase 49."
    - "The full built-artifact tus smoke lane still passes once the new parity guards are in place."
  artifacts:
    - path: "test/install_smoke/phoenix_tus_truth_parity_test.exs"
      provides: "Literal-string parity gate for guide/helper/proof-report alignment"
      min_lines: 60
    - path: "test/rindle/live_view_test.exs"
      provides: "Local helper contract tests for `RindleTus` metadata and completion-lane invariants"
      min_lines: 500
  key_links:
    - from: "guides/resumable_uploads.md"
      to: "test/install_smoke/phoenix_tus_truth_parity_test.exs"
      via: "literal string parity for Phoenix helper, uploader, state names, and completion lane"
      pattern: "RindleTus|uploading|verifying|ready|error|consume_uploaded_entries/3|verify_completion/2"
    - from: "test/rindle/live_view_test.exs"
      to: "lib/rindle/live_view.ex"
      via: "metadata and completion assertions"
      pattern: "meta\\.uploader == \"RindleTus\"|consume_uploaded_entries"
---

<objective>
Freeze the Phoenix-facing proof contract against drift with fast parity and helper-level checks required by `PROOF-02`, then confirm the full `:tus` package-consumer lane still passes.

Purpose: make the Phase 50 support claim fail fast when guide wording, helper semantics, or generated-app report keys change out of sync.
Output: narrow parity assertions plus local helper tests that encode the machine-readable Phoenix proof contract and final full-lane proof verification.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-phoenix-proof-parity-closure/50-CONTEXT.md
@.planning/phases/50-phoenix-proof-parity-closure/50-RESEARCH.md
@.planning/phases/50-phoenix-proof-parity-closure/50-PATTERNS.md
@.planning/phases/50-phoenix-proof-parity-closure/50-VALIDATION.md
@guides/resumable_uploads.md
@lib/rindle/live_view.ex
@test/install_smoke/support/generated_app_helper.ex
@test/install_smoke/generated_app_smoke_test.exs
@test/install_smoke/phoenix_tus_truth_parity_test.exs
@test/rindle/live_view_test.exs

<interfaces>
From `test/install_smoke/generated_app_smoke_test.exs` after Plan 01:
```elixir
assert report.tus_report_data["phoenix_helper_contract"] == "allow_tus_upload/4"
assert report.tus_report_data["phoenix_uploader"] == "RindleTus"
assert report.tus_report_data["phoenix_state_transitions"] == ["uploading", "verifying", "ready"]
assert report.tus_report_data["phoenix_completion_path"] == ["consume_uploaded_entries/3", "verify_completion/2"]
```

From `test/rindle/live_view_test.exs`:
```elixir
assert meta.uploader == "RindleTus"
assert meta.endpoint == "/uploads/tus"
assert String.starts_with?(meta.upload_url, "/uploads/tus/")

results =
  Rindle.LiveView.consume_uploaded_entries(completed_socket, :avatar, fn uploaded_entry, uploaded_meta ->
    {:ok, {uploaded_entry.client_name, uploaded_meta.asset_id}}
  end)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the Phoenix parity test to freeze proof-report keys and state semantics</name>
  <files>test/install_smoke/phoenix_tus_truth_parity_test.exs</files>
  <read_first>
    - test/install_smoke/phoenix_tus_truth_parity_test.exs
    - guides/resumable_uploads.md
    - lib/rindle/live_view.ex
    - test/install_smoke/generated_app_smoke_test.exs
    - test/install_smoke/support/generated_app_helper.ex
    - .planning/REQUIREMENTS.md
  </read_first>
  <behavior>
    - Test 1: the parity test asserts the exact Phoenix proof fields and values used by the generated-app harness.
    - Test 2: the parity test refutes second-lane or broad-abstraction wording that would violate the locked thin-seam contract.
  </behavior>
  <action>Per D-04, D-05, D-06, D-07, D-08, and D-09, extend `test/install_smoke/phoenix_tus_truth_parity_test.exs` so it reads the guide, `lib/rindle/live_view.ex`, `test/install_smoke/generated_app_smoke_test.exs`, and `test/install_smoke/support/generated_app_helper.ex`, then asserts the exact proof-contract strings introduced by Plan 01. Freeze these strings literally: `Rindle.LiveView.allow_tus_upload/4`, `uploader: "RindleTus"`, `uploadUrl: entry.meta.upload_url`, `consume_uploaded_entries/3`, `verify_completion/2`, and the honest state vocabulary ``uploading`, `verifying`, `ready`, and `error``. Add helper/harness drift guards for the exact report keys `phoenix_helper_contract`, `phoenix_uploader`, `phoenix_endpoint`, `phoenix_upload_url_prefix`, `phoenix_session_id_present`, `phoenix_asset_id_present`, `phoenix_state_transitions`, `phoenix_completion_path`, and `phoenix_previous_uploads`. Refute broadening strings such as `LiveView tus uploader component` or any second Phoenix-specific smoke profile naming.</action>
  <acceptance_criteria>
    - `rg -n 'phoenix_helper_contract|phoenix_state_transitions|phoenix_completion_path|phoenix_previous_uploads' test/install_smoke/phoenix_tus_truth_parity_test.exs` finds literal parity assertions for every new report key.
    - `rg -n 'LiveView tus uploader component|second Phoenix' test/install_smoke/phoenix_tus_truth_parity_test.exs` shows those strings are only used in `refute` drift guards or absent.
    - The test continues to read the canonical guide and helper files directly rather than snapshotting generated-app source trees.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/install_smoke/phoenix_tus_truth_parity_test.exs --max-failures 1</automated>
  </verify>
  <done>Fast parity fails when the guide, helper contract, smoke assertions, or generated-app proof-field names drift away from the supported Phoenix path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mirror the proof contract in local LiveView helper tests</name>
  <files>test/rindle/live_view_test.exs</files>
  <read_first>
    - test/rindle/live_view_test.exs
    - lib/rindle/live_view.ex
    - test/install_smoke/support/generated_app_helper.ex
    - test/install_smoke/phoenix_tus_truth_parity_test.exs
  </read_first>
  <behavior>
    - Test 1: helper tests still freeze `RindleTus`, `/uploads/tus`, `upload_url`, `session_id`, and `asset_id`.
    - Test 2: completion tests still freeze `consume_uploaded_entries/3` converging through `verify_completion/2`.
  </behavior>
  <action>Per D-05, D-07, D-08, D-10, and D-12, extend `test/rindle/live_view_test.exs` so the local helper contract mirrors the Phoenix proof surface without re-proving the whole generated app. Add or sharpen assertions that the helper metadata contract stays exactly `uploader == "RindleTus"`, `endpoint == "/uploads/tus"`, `upload_url` starts with `/uploads/tus/`, and `session_id` / `asset_id` are present and persisted on the signed session. Keep the completion-lane assertions centered on `Rindle.LiveView.consume_uploaded_entries/3` calling `Rindle.verify_completion/2` and converging to the same honest boundary documented in the guide. Do not add new public helper APIs or alternate Phoenix abstractions.</action>
  <acceptance_criteria>
    - `rg -n 'meta\\.uploader == \"RindleTus\"|meta\\.endpoint == \"/uploads/tus\"|session_uri == meta\\.upload_url' test/rindle/live_view_test.exs` finds the metadata contract assertions.
    - `rg -n 'consume_uploaded_entries\\(completed_socket|verify_completion/2|rindle_verify_failed' test/rindle/live_view_test.exs` confirms the completion-lane assertions remain present.
    - The test file continues to prove the shipped helper seam rather than introducing a second Phoenix integration surface.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rindle/live_view_test.exs --max-failures 1</automated>
  </verify>
  <done>The helper tests locally freeze the same narrow metadata and completion contract that the generated-app proof and guide now claim.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run the full built-artifact tus proof with parity gates enabled</name>
  <files>test/install_smoke/phoenix_tus_truth_parity_test.exs, test/rindle/live_view_test.exs</files>
  <read_first>
    - .planning/phases/50-phoenix-proof-parity-closure/50-VALIDATION.md
    - test/install_smoke/generated_app_smoke_test.exs
    - scripts/install_smoke.sh
  </read_first>
  <action>Run the fast parity/helper checks and then the full `bash scripts/install_smoke.sh tus` built-artifact proof so `PROOF-01` and `PROOF-02` close together. If the full lane fails, fix only the Phoenix proof/parity surface introduced in Phase 50; do not fork a second lane, do not weaken the Node/MinIO resume proof, and do not replace machine-readable fields with prose-only evidence.</action>
  <acceptance_criteria>
    - `mix test test/install_smoke/phoenix_tus_truth_parity_test.exs test/rindle/live_view_test.exs --max-failures 1` passes before the full smoke rerun.
    - `bash scripts/install_smoke.sh tus` passes and refreshes `tmp/install_smoke_tus_last_run.json` with the Phoenix-facing report data.
    - No new proof profile, no generated-app snapshot gate, and no second Phoenix lane appears in the changed files.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/install_smoke.sh tus</automated>
  </verify>
  <done>The final fast gates and the full built-artifact `:tus` smoke lane are both green, closing Phase 50 with auditable proof and fast drift detection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guide/helper text -> parity tests | Public contract wording is treated as executable truth and must not drift silently |
| local helper tests -> generated-app smoke lane | Fast tests encode the same narrow contract the heavier package-consumer proof exercises |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-02-01 | Tampering | `test/install_smoke/phoenix_tus_truth_parity_test.exs` | mitigate | Assert literal contract strings and exact report-key names so guide/helper/proof drift fails immediately without relying on broad snapshots. |
| T-50-02-02 | Spoofing | `test/rindle/live_view_test.exs` helper semantics | mitigate | Keep local tests pinned to the shipped metadata contract (`RindleTus`, `/uploads/tus`, `session_id`, `asset_id`) and the existing `consume_uploaded_entries/3 -> verify_completion/2` completion boundary. |
| T-50-02-03 | Repudiation | full install-smoke rerun | mitigate | Finish with `bash scripts/install_smoke.sh tus` so the persisted artifact chain in `tmp/install_smoke_tus_last_run.json` reflects the final Phase 50 proof, not only local fast tests. |
</threat_model>

<verification>
Run `mix test test/install_smoke/phoenix_tus_truth_parity_test.exs --max-failures 1`, `mix test test/rindle/live_view_test.exs --max-failures 1`, then `bash scripts/install_smoke.sh tus`.
</verification>

<success_criteria>
- Fast parity guards freeze the exact Phoenix-facing guide/helper/proof-field contract.
- Local helper tests still prove the same metadata and completion boundary used by the generated-app proof.
- The full built-artifact `:tus` lane remains green after the new parity guards are in place.
</success_criteria>

<output>
After completion, create `.planning/phases/50-phoenix-proof-parity-closure/50-02-SUMMARY.md`
</output>

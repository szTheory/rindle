---
phase: 102-re-converge-visual-matrix-idempotency-gate-milestone-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/adoption_demo/e2e/support/admin-polish.js
autonomous: true
requirements:
  - VIS-01
  - VIS-02
requirements_addressed:
  - VIS-01
  - VIS-02
must_haves:
  truths:
    - "D-102-04: focus assertions are surface-aware through an explicit focus contract and keep admin defaults."
    - "D-102-05: Cohort offender collection is scoped to the Cohort root and harness crashes remain hard failures."
    - "D-102-02: Cohort can call the same gate over `[data-ck-root]` without root inference."
  artifacts:
    - path: "examples/adoption_demo/e2e/support/admin-polish.js"
      provides: "surface-aware deterministic computed-style gate"
      exports: ["assertAdminPolish", "DEFAULT_ROOT", "DEFAULT_INTERACTIVE_SELECTORS"]
  key_links:
    - from: "examples/adoption_demo/e2e/support/admin-polish.js"
      to: "examples/adoption_demo/e2e/cohort-pages.spec.js"
      via: "assertAdminPolish options object with explicit root, interactive selectors, and focus contract"
      pattern: "assertAdminPolish"
  prohibitions:
    - "D-102-01: do not fork the polish logic into a second visual gate."
    - "D-102-02/D-94-07: do not infer token names or roots from host globals."
    - "D-102-05: do not swallow TypeError/ReferenceError harness crashes."
---

<objective>
Refactor `admin-polish.js` so the single computed-style gate can hard-fail both admin and Cohort without confusing host CSS noise for surface defects.

Purpose: Cohort cannot move from warn/report to hard-fail until focus tokens, outline scans, and admin-only backstops are explicit surface contracts.
Output: `assertAdminPolish` remains the one exported merge-blocking helper while accepting explicit focus and surface-check options.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/102-re-converge-visual-matrix-idempotency-gate-milestone-audit/102-CONTEXT.md
@.planning/phases/102-re-converge-visual-matrix-idempotency-gate-milestone-audit/102-RESEARCH.md
@guides/ui_principles.md
@examples/adoption_demo/AGENTS.md
@.planning/phases/94-foundation-token-pipeline-ci-gate-new-token-categories/94-CONTEXT.md
@.planning/phases/96-cohort-component-layer-dark-reduced-motion-contract-track-b/96-CONTEXT.md
@.planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-CONTEXT.md
@examples/adoption_demo/e2e/support/admin-polish.js
@examples/adoption_demo/priv/static/assets/cohort.css
@examples/adoption_demo/e2e/cohort-pages.spec.js
@examples/adoption_demo/e2e/cohort-styleguide.spec.js
</context>

<must_haves>
<truths>
- D-102-01: `admin-polish.js` remains the single deterministic gate.
- D-102-04: admin callers default to `--rindle-focus-width`, `--rindle-focus-ring`, `--rindle-focus-offset`; Cohort callers can pass width `2px`, ring token `--ck-focus`, offset `2px`.
- D-102-05: root-scoped outline and offender scans report only defects inside the selected surface.
</truths>
<prohibitions>
- No Cohort special-case branch based on root auto-detection.
- No global document outline scan for Cohort surfaces.
- No warning downgrade path inside `admin-polish.js`; downgrade behavior belongs only to older callers until Plan 05 removes it.
</prohibitions>
</must_haves>

## Artifacts this phase produces

- `focusContract` or equivalent option on `assertAdminPolish`.
- Admin default focus contract preserving existing token names.
- Cohort-compatible focus contract support using `2px`, `--ck-focus`, and `2px`.
- Surface-check options that keep Phase 98 admin-only backstops enabled for admin and disabled for Cohort callers.
- Root-scoped offender collection for focus/outline checks.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit focus contract support</name>
  <files>examples/adoption_demo/e2e/support/admin-polish.js</files>
  <read_first>
    - `examples/adoption_demo/e2e/support/admin-polish.js` lines around `DEFAULT_ROOT`, `DEFAULT_INTERACTIVE_SELECTORS`, `assertFocusVisibleTokens`, `assertFocusVisibleVsPointer`, and `assertAdminPolish`.
    - `examples/adoption_demo/priv/static/assets/cohort.css` for `.ck :focus-visible` and `--ck-focus`.
    - `.planning/phases/102-re-converge-visual-matrix-idempotency-gate-milestone-audit/102-CONTEXT.md` D-102-04.
  </read_first>
  <behavior>
    - Admin callers with no new option still expect the current admin token-backed ring.
    - Cohort callers can pass an explicit contract for width, ring color token, and offset without reading admin variables.
  </behavior>
  <action>
    Add a small focus-contract normalizer to `admin-polish.js` and thread it through `assertFocusVisibleTokens`, `assertFocusVisibleVsPointer`, and `assertAdminPolish`. Keep admin defaults equivalent to `--rindle-focus-width`, `--rindle-focus-ring`, and `--rindle-focus-offset`. Let Cohort callers pass a contract whose width is `2px`, whose ring color resolves from `--ck-focus` on the explicit root, and whose offset is `2px`. Resolve token colors from the selected root first, falling back to `documentElement` only for the admin default. Do not infer a contract from selector names or from whichever root happens to exist.
  </action>
  <acceptance_criteria>
    - `assertAdminPolish` options include a documented focus contract parameter with admin-compatible defaults.
    - The focus assertions use the explicit root to resolve Cohort focus color.
    - Existing admin exports and call signatures remain backward-compatible.
  </acceptance_criteria>
  <verify>
    <automated>node --check examples/adoption_demo/e2e/support/admin-polish.js</automated>
    <automated>node -e "const mod=require('./examples/adoption_demo/e2e/support/admin-polish.js'); for (const k of ['assertAdminPolish','DEFAULT_ROOT','DEFAULT_INTERACTIVE_SELECTORS']) { if (!(k in mod)) throw new Error('missing '+k); }"</automated>
  </verify>
  <done>Focus checks are surface-aware and ready for Cohort hard-fail callers per D-102-04.</done>
</task>

<task type="auto">
  <name>Task 2: Scope admin-only backstops and offender collection</name>
  <files>examples/adoption_demo/e2e/support/admin-polish.js</files>
  <read_first>
    - `examples/adoption_demo/e2e/support/admin-polish.js` around `assertNoClippedText`, `assertReadableContrast`, `assertConsistentRhythm`, `assertNoHorizontalScroll`, `assertTwoPaneBand`, `assertStackedCard`, `assertReducedMotion`, `assertDialogInert`, and `assertAdminPolish`.
    - `.planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-VERIFICATION.md` for Phase 98 backstops that remain admin-only.
  </read_first>
  <action>
    Split the gate options so generic root-scoped checks run for every surface, while admin page/layout backstops remain an explicit enabled set for admin callers. Keep `assertTwoPaneBand`, `assertStackedCard`, `assertDialogInert`, and the Phase 98 admin-only reduced-motion selector list out of Cohort hard-fail calls unless a caller opts into those exact admin checks. Keep offender-returning helpers returning arrays and keep `assertAdminPolish` aggregating a single failure. A thrown harness bug must still propagate as a hard failure; only offender arrays are aggregated.
  </action>
  <acceptance_criteria>
    - Generic checks receive the explicit `root` and do not scan outside it for Cohort callers.
    - Admin-only backstops still run by default for admin calls.
    - Cohort calls can disable admin-only backstops without forking the gate.
    - Harness crashes are not converted to warnings.
  </acceptance_criteria>
  <verify>
    <automated>node --check examples/adoption_demo/e2e/support/admin-polish.js examples/adoption_demo/e2e/admin-screenshots.spec.js examples/adoption_demo/e2e/cohort-pages.spec.js examples/adoption_demo/e2e/cohort-styleguide.spec.js</automated>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('examples/adoption_demo/e2e/support/admin-polish.js','utf8');if(!s.includes('assertTwoPaneBand')||!s.includes('assertDialogInert')) throw new Error('admin backstops missing'); if(!s.includes('focusContract')) throw new Error('focus contract option missing');"</automated>
  </verify>
  <done>`assertAdminPolish` can serve admin and Cohort as one gate, with explicit surface contracts and scoped offender collection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cohort DOM -> shared visual helper | Untrusted rendered surface data is inspected by a shared gate. |
| Helper internals -> CI failure signal | Offender aggregation must not mask harness defects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-02 | Repudiation / Tampering | `assertAdminPolish` warning and crash paths | mitigate | Keep harness crashes thrown; aggregate only offender arrays and remove caller warning downgrades in Plan 05. |
| T-102-03 | Tampering | focus token expectations | mitigate | Require explicit focus contract, resolve tokens from explicit root, preserve admin defaults. |
| T-102-04 | Denial of Service | visual gate flakiness | mitigate | Scope checks to roots and avoid global host-cascade noise for Cohort. |
| T-102-SC | Tampering | npm/pip/cargo installs | accept | No package install is planned; use existing Playwright and Node scripts. |
</threat_model>

<verification>
Run JS syntax checks and module export checks immediately. Full proof happens when Plan 05 promotes Cohort callers to hard-fail and Plan 06 runs the wrapper.
</verification>

<success_criteria>
The shared gate is surface-aware, explicit-root-only, crash-honest, and ready to satisfy VIS-01 without a second gate.
</success_criteria>

<output>
Create `.planning/phases/102-re-converge-visual-matrix-idempotency-gate-milestone-audit/102-02-SUMMARY.md` when done.
</output>

---
phase: 05-ci-1-0-readiness
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - "lib/rindle/upload/broker.ex"
  - "test/rindle/upload/broker_test.exs"
autonomous: true
requirements:
  - TEL-02
  - TEL-03
  - CI-01
  - CI-08
gap_closure: true
must_haves:
  truths:
    - "Broker.profile_name_to_module/1 returns a clean {:error, :unknown_profile} instead of crashing"
    - "Broker.verify_completion properly records the 'verifying' state before transitioning to 'completed'"
    - "mix format --check-formatted passes for broker_test.exs"
  artifacts:
    - path: "lib/rindle/upload/broker.ex"
      provides: "Broker correctness and robust error handling"
      contains: "{:error, :unknown_profile}"
  key_links: []
---

<objective>
Fix `Broker` correctness defects identified in CR-03 and CR-04 to ensure the public API surface is reliable, and fix formatting violations in its test file.

Purpose: A crash on unknown profile (CR-04) and an FSM bypass (CR-03) undermine the credibility of the telemetry emissions and Adopter lifecycle CI tests. Fixing them ratchets code quality up to the CI standard.
Output: Reliable Broker module and fully formatted test file.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-ci-1-0-readiness/05-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix unformatted test file</name>
  <files>test/rindle/upload/broker_test.exs</files>
  <action>
    Run `mix format test/rindle/upload/broker_test.exs` to fix pre-existing trailing whitespace and long expect lines. Do this before making functional changes to avoid noisy diffs.
  </action>
  <verify>
    <automated>mix format --check-formatted test/rindle/upload/broker_test.exs</automated>
  </verify>
  <done>The test file passes the formatting check.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix unknown profile crash (CR-04)</name>
  <files>lib/rindle/upload/broker.ex, test/rindle/upload/broker_test.exs</files>
  <behavior>
    - Test 1: Calling a public API (e.g. `verify_completion`) with an unknown profile string should return `{:error, :unknown_profile}` instead of crashing with an `UndefinedFunctionError`.
  </behavior>
  <action>
    - In `lib/rindle/upload/broker.ex`, update `profile_name_to_module/1` (lines 195-199) to return `{:error, :unknown_profile}` instead of `nil` when `String.to_existing_atom` fails.
    - Update any callers of `profile_name_to_module/1` in `broker.ex` (such as `verify_completion`'s `with` chain) to correctly pattern match on `{:error, :unknown_profile}`. If the `with` chain just assigns `module <- profile_name_to_module(...)`, verify the `else` block catches it and returns the error tuple to the caller.
    - Implement the test case asserting the clean return tuple.
  </action>
  <verify>
    <automated>mix test test/rindle/upload/broker_test.exs</automated>
  </verify>
  <done>Caller receives `{:error, :unknown_profile}` cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix verify_completion FSM bypass (CR-03)</name>
  <files>lib/rindle/upload/broker.ex, test/rindle/upload/broker_test.exs</files>
  <behavior>
    - Test 1: Calling `verify_completion` correctly transitions the session FSM through the `verifying` state and eventually `completed`, persisting `verifying` if possible or using a proper two-step Multi that gates on `verifying`.
  </behavior>
  <action>
    - In `lib/rindle/upload/broker.ex`'s `verify_completion/2` function, the `Multi.update` bypasses the `verifying` persistence and `verifying -> completed` gate.
    - Fix the `Multi` sequence: it must either persist the `verifying` state in a discrete step (e.g. `Multi.update(:verifying_session, ...)` then `Multi.update(:completed_session, ...)`) or apply the `UploadSessionFSM.transition` calls correctly inside the Multi transaction so both the gating and the final persistence are honored without bypassing the FSM definition.
    - Write a test in `broker_test.exs` ensuring the correct state flow or error behavior is maintained.
  </action>
  <verify>
    <automated>mix test test/rindle/upload/broker_test.exs</automated>
  </verify>
  <done>The FSM transition is accurately executed and persisted without bypass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal Application | Resolving unknown profiles cleanly prevents application crashes, improving availability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09-01 | Denial of Service | broker.ex | mitigate | Return clean error tuples instead of crashing the process on unknown user input strings. |
</threat_model>

<verification>
mix test test/rindle/upload/broker_test.exs passes without errors.
</verification>

<success_criteria>
Broker correctly handles unknown profiles and correctly enforces its FSM state transitions during upload verification.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ci-1-0-readiness/05-09-SUMMARY.md`
</output>

---
phase: 05-ci-1-0-readiness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - coveralls.json
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - CI-01
  - CI-02
  - CI-03
  - CI-04
  - CI-05
must_haves:
  truths:
    - "`mix coveralls` exits non-zero when line coverage is below 80% (D-13; CI-03)"
    - "`coveralls.json` declares `coverage_options.minimum_coverage: 80` and skips test/support, test/adopter, lib/rindle/repo.ex, lib/rindle/application.ex, priv/repo/migrations (D-13)"
    - "`mix.exs` deps includes `{:excoveralls, \"~> 0.18\", only: [:test, :dev], runtime: false}` (D-13)"
    - "`mix.exs` project/0 has `test_coverage: [tool: ExCoveralls]` and `preferred_cli_env:` mappings for coveralls tasks (Pitfall 3 prevention)"
    - "`.github/workflows/ci.yml` quality job runs `apt-get install -y libvips-dev` BEFORE `mix deps.get` (D-14)"
    - "`.github/workflows/ci.yml` quality job replaces the `mix test` step with `mix coveralls` (D-13; CI-03)"
    - "`.github/workflows/ci.yml` quality job preserves all existing CI-01/02/04/05 gates: format check, warnings-as-errors compile, credo --strict, dialyzer (CI-01, CI-02, CI-04, CI-05)"
    - "`.github/workflows/ci.yml` quality job MATRIX is preserved (1.15/26 + 1.17/27); the integration job is byte-for-byte unchanged (Blocker 4 + Blocker 7)"
    - "`mix coveralls` reports coverage with the four exclusions from coveralls.json applied"
  artifacts:
    - path: "coveralls.json"
      provides: "Repo-root excoveralls config: `coverage_options.minimum_coverage: 80` + `skip_files` allowlist of test/support, test/adopter, repo.ex, application.ex, priv/repo/migrations"
      contains: "minimum_coverage"
    - path: "mix.exs"
      provides: "Deps adds excoveralls; project/0 adds test_coverage + preferred_cli_env; ex_doc bumped to ~> 0.40"
      contains: "excoveralls"
    - path: ".github/workflows/ci.yml"
      provides: "Quality job: libvips install before deps.get, mix coveralls replacing mix test, all other gates preserved; matrix preserved; integration job unchanged"
      contains: "libvips-dev"
  key_links:
    - from: ".github/workflows/ci.yml (quality job)"
      to: "coveralls.json"
      via: "`mix coveralls` reads coveralls.json from repo root automatically"
      pattern: "mix coveralls"
    - from: "mix.exs"
      to: "excoveralls dep"
      via: "test_coverage: [tool: ExCoveralls] dispatches mix test to ExCoveralls runner"
      pattern: "test_coverage:"
---

<objective>
Harden the CI quality lane to satisfy CI-01..05 with explicit gates: `mix format --check-formatted`, `mix compile --warnings-as-errors`, `mix coveralls` (≥ 80% line coverage), `mix credo --strict`, `mix dialyzer --format github`. Add the `:excoveralls` dependency, configure `coveralls.json` with conventional skip_files, and ensure libvips is available so the `:image`-backed code paths are covered. Also bump `ex_doc` to `~> 0.40` to enable `groups_for_extras:` and `before_closing_head_tag:` (used by Plan 06).

Purpose: The quality lane is the floor under every PR. Coverage must be a hard gate (CI fails when below 80%) — `mix test` alone bypasses this, so per Pitfall 3 we replace `mix test` with `mix coveralls` AND wire `test_coverage: [tool: ExCoveralls]` in mix.exs.

Output: New `coveralls.json`, modified `mix.exs`, modified `.github/workflows/ci.yml` quality job. Independent of Plan 01 (no telemetry-source-file overlap) — runs in parallel as Wave 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ci-1-0-readiness/05-CONTEXT.md
@.planning/phases/05-ci-1-0-readiness/05-RESEARCH.md
@.planning/phases/05-ci-1-0-readiness/05-PATTERNS.md
@mix.exs
@.github/workflows/ci.yml

<interfaces>
<!-- excoveralls public API used by this plan. -->

excoveralls 0.18 — Mix tasks installed:
- `mix coveralls` — runs the test suite with coverage; exits 1 if below `minimum_coverage` in coveralls.json
- `mix coveralls.detail` — per-file coverage breakdown
- `mix coveralls.html` — HTML report under `cover/`
- `mix coveralls.json` — JSON output for CI integration

Required `mix.exs` wiring:
```elixir
# In project/0:
test_coverage: [tool: ExCoveralls],
preferred_cli_env: [
  coveralls: :test,
  "coveralls.detail": :test,
  "coveralls.html": :test,
  "coveralls.json": :test
]

# In deps/0:
{:excoveralls, "~> 0.18", only: [:test, :dev], runtime: false}
```

`coveralls.json` schema (verified RESEARCH.md Pattern 1):
```json
{
  "coverage_options": {
    "minimum_coverage": <integer 0-100>
  },
  "skip_files": [<string path prefix or regex>]
}
```

Existing `.github/workflows/ci.yml` quality job step ordering (lines 48-104):
1. Checkout (line 49)
2. Set up Elixir (line 53)
3. Restore deps cache (line 59)
4. Restore build cache (line 66)
5. Install dependencies — `mix deps.get` (line 73)  ← INSERT libvips install BEFORE this
6. Compile (warnings as errors) (line 76)
7. Check formatting (line 79)
8. Credo (strict) (line 82)
9. Run tests — `mix test` (line 85)  ← REPLACE with `mix coveralls`
10. Restore PLT cache (line 88)
11. Create PLT directory (line 96)
12. Build PLT (line 100)
13. Dialyzer (line 104)

NOTE: matrix has TWO entries (Elixir 1.15/OTP 26 and Elixir 1.17/OTP 27 — lines 22-26). All changes apply to both matrix variants automatically since they share the same step list.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add excoveralls dep, mix.exs project config, and coveralls.json</name>
  <files>
    - mix.exs
    - coveralls.json (new)
  </files>

  <read_first>
    - mix.exs (full file — already read)
    - .planning/phases/05-ci-1-0-readiness/05-RESEARCH.md (Pattern 1 — coveralls.json Threshold Gate, lines 213-256; Pitfall 3 — mix coveralls Threshold Not Triggering, lines 573-578; Code Examples — excoveralls Full mix.exs Configuration lines 604-627)
    - .planning/phases/05-ci-1-0-readiness/05-PATTERNS.md (mix.exs section lines 558-648; coveralls.json section lines 538-557)
    - .planning/phases/05-ci-1-0-readiness/05-CONTEXT.md (D-13 minimum_coverage 80; D-14 libvips; Claude's Discretion: "Coverage exclusion patterns (e.g., test support, Mix tasks) — pick conventional defaults")
  </read_first>

  <action>
    Step 1 — Modify `mix.exs`. Three edits:

    Edit 1.1 — Add `test_coverage` and `preferred_cli_env` to `project/0` (lines 7-25). Insert AFTER the existing `dialyzer:` block at line 23 (i.e., before the closing `]`):

    ```elixir
        ],
        test_coverage: [tool: ExCoveralls],
        preferred_cli_env: [
          coveralls: :test,
          "coveralls.detail": :test,
          "coveralls.html": :test,
          "coveralls.json": :test
        ]
      ]
    end
    ```

    Edit 1.2 — Add `{:excoveralls, "~> 0.18", only: [:test, :dev], runtime: false}` to `deps/0`. Insert as a new line in the Dev/Test block (currently lines 67-73). Place it after the existing `{:dialyxir, ...}` line (line 72) and before `{:ex_doc, ...}` (line 73).

    Edit 1.3 — Bump the `ex_doc` constraint from `"~> 0.34"` to `"~> 0.40"` (line 73). This unlocks `groups_for_extras:` and `before_closing_head_tag:` used by Plan 06. Final dep entry:
    ```elixir
    {:ex_doc, "~> 0.40", only: :dev, runtime: false}
    ```

    Step 2 — Create `coveralls.json` at the repo root (`/Users/jon/projects/rindle/coveralls.json`):

    ```json
    {
      "coverage_options": {
        "minimum_coverage": 80,
        "treat_no_relevant_lines_as_covered": true
      },
      "skip_files": [
        "test/support",
        "test/adopter",
        "lib/rindle/repo.ex",
        "lib/rindle/application.ex",
        "priv/repo/migrations",
        "lib/mix/tasks"
      ]
    }
    ```

    JUSTIFICATION for added `lib/mix/tasks` exclusion (Claude's Discretion per CONTEXT.md): Mix tasks are exercised via integration / adopter lanes, not unit tests; they have detailed `@moduledoc` blocks (per D-18) but their bodies do shell I/O that is impractical to unit test. Excluding them keeps the 80% threshold achievable for production library code. Documented in this file's comment block — but coveralls.json does NOT support comments, so the rationale lives ONLY in the SUMMARY.

    JUSTIFICATION for `treat_no_relevant_lines_as_covered: true`: prevents files containing only `@moduledoc` / `@type` / `defstruct` from being penalized as 0% coverage when they have no executable lines.

    Step 3 — Run `mix deps.get` to install excoveralls. Then run `mix coveralls` locally to confirm the threshold gate triggers (it should currently FAIL the 80% gate — that is expected at this stage; we only need to confirm `mix coveralls` runs and the gate behavior is wired).

    If `mix coveralls` reports below 80% currently, this is OK — the CI will fail until coverage improves OR the threshold is calibrated. The acceptance criteria below check that the gate IS WIRED, not that 80% is currently met. If coverage is materially below 80% (e.g., < 70%), DOC the actual current value in the SUMMARY and note for Plan 04 (adopter lane) that real lifecycle coverage will boost the number.
  </action>

  <verify>
    <automated>mix deps.get && mix coveralls 2>&1 | tee /tmp/coveralls-output.txt; grep -E "(\[TOTAL\]|FAILED minimum coverage)" /tmp/coveralls-output.txt</automated>
  </verify>

  <acceptance_criteria>
    - `grep "test_coverage: \[tool: ExCoveralls\]" mix.exs` returns 1 match
    - `grep "preferred_cli_env:" mix.exs` returns 1 match
    - `grep -c "coveralls: :test" mix.exs` returns 1
    - `grep "{:excoveralls" mix.exs` returns 1 match
    - `grep "ex_doc, \"~> 0.40\"" mix.exs` returns 1 match (NOT 0.34)
    - `test -f coveralls.json` exits 0
    - `python3 -c "import json; d=json.load(open('coveralls.json')); assert d['coverage_options']['minimum_coverage']==80"` exits 0
    - `python3 -c "import json; d=json.load(open('coveralls.json')); assert 'test/support' in d['skip_files']"` exits 0
    - `python3 -c "import json; d=json.load(open('coveralls.json')); assert 'lib/rindle/repo.ex' in d['skip_files']"` exits 0
    - `mix deps.get 2>&1 | grep -i excoveralls` returns at least 1 line (dep is fetched)
    - `mix coveralls 2>&1 | grep -E "(\[TOTAL\]|coverage)"` returns at least 1 match (coveralls is wired and runs — the test exits non-zero is OK at this phase if coverage < 80%)
  </acceptance_criteria>

  <done>
    excoveralls is in deps, mix.exs project/0 has the test_coverage tool wiring (Pitfall 3 prevention), coveralls.json sets the 80% threshold with conventional exclusions, ex_doc is bumped to 0.40 (unblocks Plan 06).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire libvips install + replace `mix test` with `mix coveralls` in CI quality job</name>
  <files>
    - .github/workflows/ci.yml
  </files>

  <read_first>
    - .github/workflows/ci.yml (full file — already read; quality job lines 16-104; integration job lines 106-189; matrix block lines 22-26)
    - .planning/phases/05-ci-1-0-readiness/05-RESEARCH.md (Pattern 7 — libvips in CI lines 526-532; Pattern 1 — Coveralls CI command line 249-255; Code Examples — CI Quality Job Extension lines 670-682)
    - .planning/phases/05-ci-1-0-readiness/05-PATTERNS.md (`.github/workflows/ci.yml` section lines 432-487 — the libvips + coveralls insertions)
    - .planning/phases/05-ci-1-0-readiness/05-CONTEXT.md (D-14 — `apt-get install -y libvips-dev` BEFORE `mix deps.get`; A2 in RESEARCH Assumptions Log — libvips is precaution-only for default precompiled mode but document the rationale)
  </read_first>

  <action>
    Step 1 — Modify `.github/workflows/ci.yml`. Two edits to the `quality` job:

    Edit 2.1 — Insert a new step BEFORE `Install dependencies` (line 72-73). The new step uses `sudo apt-get install -y libvips-dev`:

    ```yaml
          - name: Install libvips
            run: sudo apt-get install -y libvips-dev

          - name: Install dependencies
            run: mix deps.get
    ```

    Justification per A2 in 05-RESEARCH.md: the `:image` dep defaults to precompiled NIFs, but extended format coverage paths (HEIF, JPEG XL) require platform-provided libvips. Installing libvips-dev does not hurt the default mode and unlocks coverage for `lib/rindle/processor/image.ex` extended-format branches. Also resolves the STATE.md pending todo "Add libvips system dependency note to CI config and getting started guide".

    Edit 2.2 — Replace the `Run tests` step (lines 84-85) with `Run tests with coverage`:

    Current:
    ```yaml
          - name: Run tests
            run: mix test
    ```

    Replace with:
    ```yaml
          - name: Run tests with coverage
            run: mix coveralls
    ```

    Why `mix coveralls` and NOT `mix coveralls.github`: per RESEARCH.md Anti-Patterns line 536 and Alternatives Considered line 138, `coveralls.github` posts to coveralls.io (external service, requires token). For a local threshold gate, `mix coveralls` reads `coveralls.json` and exits 1 if below `minimum_coverage`. No external service needed.

    Step 2 — Confirm the rest of the quality job is UNCHANGED. Specifically these steps must remain in the existing order with their existing commands:
    - `Compile (warnings as errors)` (currently line 75-76) — `mix compile --warnings-as-errors` (CI-02)
    - `Check formatting` (line 78-79) — `mix format --check-formatted` (CI-01)
    - `Credo (strict)` (line 81-82) — `mix credo --strict` (CI-04)
    - PLT cache restore (line 87-93) and Build PLT (line 99-101) and `Dialyzer` (line 103-104) — `mix dialyzer --format github` (CI-05)

    The matrix block (lines 22-26: 1.15/26 + 1.17/27) MUST remain intact. Both matrix variants execute every step including the new libvips install + coveralls — neither matrix entry is modified or removed.

    Step 3 — Validate YAML: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0.

    Step 4 — Confirm the `integration` job (lines 106-189) is COMPLETELY UNCHANGED. Plan 02 will add a `contract` job; Plan 04 will add an `adopter` job; this plan only modifies the `quality` job. The integration job's MinIO + Postgres services and its `mix test test/rindle/upload/lifecycle_integration_test.exs --include integration` command must remain intact — Blocker 7 enforces this with explicit grep checks below.
  </action>

  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && grep -B1 "mix deps.get" .github/workflows/ci.yml | grep -c "libvips"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "libvips-dev" .github/workflows/ci.yml` returns at least 1
    - `grep -B1 "mix deps.get" .github/workflows/ci.yml | grep -c "libvips"` returns at least 1 (libvips step IMMEDIATELY precedes deps.get)
    - `grep -c "mix coveralls" .github/workflows/ci.yml` returns at least 1
    - `grep -c "^          - name: Run tests$" .github/workflows/ci.yml` returns 0 (the bare `mix test` step is gone)
    - `grep -c "name: Run tests with coverage" .github/workflows/ci.yml` returns at least 1
    - `grep -c "mix format --check-formatted" .github/workflows/ci.yml` returns at least 1 (CI-01 preserved)
    - `grep -c "mix compile --warnings-as-errors" .github/workflows/ci.yml` returns at least 1 (CI-02 preserved)
    - `grep -c "mix credo --strict" .github/workflows/ci.yml` returns at least 1 (CI-04 preserved)
    - `grep -c "mix dialyzer --format github" .github/workflows/ci.yml` returns at least 1 (CI-05 preserved)
    - `grep -A 20 "^  quality:" .github/workflows/ci.yml | grep -c "matrix:"` returns 1 (Blocker 4: quality matrix preserved — both 1.15/26 and 1.17/27 entries remain; downstream `needs: quality` jobs wait for ALL matrix entries to complete)
    - `grep -c "name: Integration" .github/workflows/ci.yml` returns 1 (Blocker 7: integration job preserved)
    - `grep -c "mix test test/rindle/upload/lifecycle_integration_test.exs --include integration" .github/workflows/ci.yml` returns at least 1 (Blocker 7: integration test command preserved)
    - `grep -c "services:" .github/workflows/ci.yml` returns at least 1 (Blocker 7: integration's MinIO + Postgres services preserved; will become at least 2 after Plan 04 adds adopter job services)
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0
  </acceptance_criteria>

  <done>
    Quality job installs libvips before fetching deps, runs `mix coveralls` instead of `mix test`, and preserves CI-01/02/04/05 gates. Integration job is untouched (Blocker 7 verified by grep on its test command and `services:` block). Quality matrix is preserved (Blocker 4). YAML is valid.

    **Coverage window note (Warning 6):** The Quality lane WILL be RED on first run. Plan 01 (telemetry emission tests) adds new test files that contribute to coverage; Plan 03 enables the 80% threshold. Plan 01 and Plan 03 must merge in the same PR / wave window so coverage measurement reflects the post-Plan-01 state. If the measured coverage is materially below 80% (e.g., < 70%) AFTER Plan 01 + Plan 03 land together, the threshold may need to be temporarily lowered (e.g., to the actual measured value rounded down to the nearest 5%) so subsequent plans (Plan 04 adopter lane) can ratchet it back up. Document the calibration decision in the SUMMARY: actual coverage measured, whether the threshold was held at 80% or temporarily lowered, and the path back to 80%.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → apt repository (libvips-dev) | apt fetches packages over HTTPS from Ubuntu mirrors; supply-chain trust delegated to Ubuntu |
| GitHub Actions runner → hex.pm (excoveralls dep) | mix deps.get fetches over HTTPS; checksums verified via mix.lock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | Compromised libvips package altering image processing behavior | accept | Standard ubuntu-latest apt repo; same trust model as the rest of the GitHub-hosted runner toolchain. No additional mitigation possible at the project level. |
| T-05-03-02 | Information Disclosure | excoveralls report contents leaking source code or test names | accept | Coverage report stays on the runner unless explicitly uploaded. `mix coveralls` (not `coveralls.github`) does NOT post to external services. |

Security disposition: LOW. This plan only adds developer-time gates; no production attack surface introduced.
</threat_model>

<verification>
- `mix coveralls` runs locally and reports a coverage percentage; the threshold gate exits 1 below 80%.
- `mix.exs` deps fetches excoveralls without error; `ex_doc` 0.40 is the new floor.
- CI quality job: libvips installs before deps fetch; coverage replaces bare test; format/compile/credo/dialyzer remain in place.
- YAML is parseable; quality matrix preserved (Blocker 4); integration job is byte-for-byte unchanged (Blocker 7 with explicit grep proofs).
- coveralls.json is valid JSON with the 80% threshold and the documented skip_files allowlist.
</verification>

<success_criteria>
- CI-01..05 are all gated in the quality job
- CI-03 specifically: `mix coveralls` enforces the 80% threshold (Pitfall 3 prevention via test_coverage in mix.exs)
- libvips is available so `lib/rindle/processor/image.ex` extended paths can contribute to coverage
- ex_doc 0.40 is the project floor (Plan 06 dependency unblocked)
- coveralls.json conventional exclusions documented (Claude's Discretion area)
- Quality matrix preserved (Blocker 4) and integration job preserved (Blocker 7) — both verified by automated grep checks
- Coverage window calibration documented in SUMMARY (Warning 6)
</success_criteria>

<output>
After completion, create `.planning/phases/05-ci-1-0-readiness/05-03-SUMMARY.md`. Note the actual coverage percentage observed during local `mix coveralls` so Plan 04 can plan to bridge the gap if needed. Per Warning 6 — explicitly document whether the 80% threshold was held or temporarily lowered, and the path back to 80% if lowered.
</output>

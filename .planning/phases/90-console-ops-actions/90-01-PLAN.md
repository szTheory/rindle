---
phase: 90-console-ops-actions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/admin/queries.ex
  - lib/rindle/admin/live/actions_live.ex
  - test/rindle/admin/live/actions_live_test.exs
autonomous: true
requirements:
  - ADMIN-04
must_haves:
  truths:
    - User can preview and execute owner erasure with typed confirmation.
    - User can preview and execute batch erasure with typed confirmation.
    - Attempting to submit erasure without a matching typed confirmation is blocked.
    - Changing the input scope after a preview invalidates the preview and blocks execution.
    - Batch erasure partial failures are handled safely and display a partial receipt.
  artifacts:
    - path: lib/rindle/admin/live/actions_live.ex
      provides: State machine and LiveView panels for owner/batch erasure.
    - path: test/rindle/admin/live/actions_live_test.exs
      provides: Deterministic tests for preview, confirmation, and receipt flows.
  key_links:
    - from: lib/rindle/admin/live/actions_live.ex
      to: Rindle.preview_owner_erasure/2
      via: direct call
    - from: lib/rindle/admin/live/actions_live.ex
      to: Rindle.erase_owner/2
      via: direct call
    - from: lib/rindle/admin/live/actions_live.ex
      to: Rindle.preview_batch_owner_erasure/2
      via: direct call
    - from: lib/rindle/admin/live/actions_live.ex
      to: Rindle.erase_batch_owner_erasure/2
      via: direct call
---

<objective>
Transform the read-only Actions hub into an operational console by implementing the destructive owner and batch erasure workflows.

Purpose: Allow administrators to manage data retention by providing deliberate, preview-first destructive workflows natively inside the console, without exposing raw database commands.
Output: Working owner and batch erasure panels inside the Actions LiveView.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/90-console-ops-actions/90-CONTEXT.md
@.planning/phases/90-console-ops-actions/90-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enable Erasure Actions and Setup Scaffold</name>
  <files>lib/rindle/admin/queries.ex, lib/rindle/admin/live/actions_live.ex</files>
  <behavior>
    - Test 1: `Queries.actions_directory` should return `:owner_erasure` and `:batch_erasure` with `enabled?: true` and `read_only?: false`.
    - Test 2: `ActionsLive` should render action-specific panels or tabs instead of a single static table, defaulting to the first action or a selection menu.
  </behavior>
  <action>Update `Queries.actions_directory/0` to enable the `:owner_erasure` and `:batch_erasure` actions (D-90-01, D-90-02, D-90-03). Update `ActionsLive`'s HTML to render page-scoped action panels (D-90-04, D-90-05) where a user can select an action to view its specific interface. For now, non-erasure actions can remain in a "coming soon" or read-only state. Establish a `%{state: :input | :preview | :receipt}` tracking pattern in the socket assigns for the active panel.</action>
  <verify>
    <automated>mix test test/rindle/admin/queries_test.exs && mix test test/rindle/admin/live/actions_live_test.exs</automated>
  </verify>
  <done>Erasure actions are marked enabled in the query metadata and the LiveView can route internal state to specific action templates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Owner Erasure Workflow</name>
  <files>lib/rindle/admin/live/actions_live.ex, test/rindle/admin/live/actions_live_test.exs</files>
  <behavior>
    - Test 1: Given valid `owner_type` and `owner_id`, previewing generates a report via `Rindle.preview_owner_erasure/2`.
    - Test 2: Execution is blocked unless confirmation matches `ERASE type:id` exactly.
    - Test 3: Changing the target input resets the state back to `:input`, clearing the preview.
    - Test 4: Execution calls `Rindle.erase_owner/2` and renders an inline receipt naming queued work and `attachments_to_detach`.
  </behavior>
  <action>Implement the `:owner_erasure` panel. Build a form accepting `owner_type` and `owner_id`. On submit, call `Rindle.preview_owner_erasure(Rindle.owner_ref(%{...}))`. Transition to `:preview` state showing the returned report and a confirmation input. Validate that the user types `ERASE type:id` (D-90-06). If input changes (`phx-change`), reset state to `:input` (D-90-07, D-90-10). On confirmed execution, call `Rindle.erase_owner/2` and transition to `:receipt` state showing the execution report (D-90-08). Use stable data attributes (e.g. `data-rindle-admin-receipt`) per D-90-20 and D-90-21.</action>
  <verify>
    <automated>mix test test/rindle/admin/live/actions_live_test.exs</automated>
  </verify>
  <done>Owner erasure supports a full preview-confirm-execute lifecycle with safe failure handling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Batch Erasure Workflow</name>
  <files>lib/rindle/admin/live/actions_live.ex, test/rindle/admin/live/actions_live_test.exs</files>
  <behavior>
    - Test 1: Accepts multiple owners (e.g. via multiline text or distinct inputs) and previews via `Rindle.preview_batch_owner_erasure/2`.
    - Test 2: Requires typed confirmation phrase `ERASE N OWNERS`.
    - Test 3: Calls `Rindle.erase_batch_owner_erasure/2` on execution.
    - Test 4: Handles partial failures gracefully by displaying the `partial_report` and identifying the failed owner.
  </behavior>
  <action>Implement the `:batch_erasure` panel. Accept multiple owner references. Call `Rindle.preview_batch_owner_erasure/2` for the preview state. Enforce typed confirmation phrase `ERASE N OWNERS` where N is the count of owners (D-90-06). Execute with `Rindle.erase_batch_owner_erasure/2`. Critically, match `{:error, {:batch_owner_failed, %{owner: failed, reason: r, partial_report: report}}}` (Pitfall 1) and transition to a partial receipt view showing the completed operations alongside the error (D-90-09). Use stable data attributes (e.g. `data-rindle-admin-receipt`, `data-rindle-admin-error`) for LiveView test assertions (D-90-22).</action>
  <verify>
    <automated>mix test test/rindle/admin/live/actions_live_test.exs</automated>
  </verify>
  <done>Batch erasure supports preview-confirm-execute and displays partial receipts without suppressing successful commits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI → Ops Facade | Admin inputs must be verified and sanitized before passing to Rindle facades. Destructive actions must not be bypassable via client-side manipulation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-01 | Spoofing | `ActionsLive` form state | mitigate | Form inputs are validated strictly. Changing target scope inputs invalidates existing preview/confirmation state immediately. |
| T-90-02 | Tampering | Execution events | mitigate | LiveView execution event handlers must re-verify typed confirmation strings server-side against the *previewed* targets before executing facades. |
</threat_model>

<verification>
mix test test/rindle/admin/live/actions_live_test.exs passes for both single and batch erasure.
</verification>

<success_criteria>
LiveView fully orchestrates owner and batch erasure via `Rindle` facade calls with preview and exact typed confirmation logic.
</success_criteria>

<output>
Create `.planning/phases/90-console-ops-actions/90-01-SUMMARY.md` when done
</output>

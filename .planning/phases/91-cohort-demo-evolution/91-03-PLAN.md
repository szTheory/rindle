---
phase: 91-cohort-demo-evolution
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/adoption_demo/lib/adoption_demo_web/router.ex
  - examples/adoption_demo/README.md
autonomous: true
requirements:
  - DEMO-03
must_haves:
  truths:
    - "The Rindle admin console is mounted in the demo router."
    - "The admin console is reachable without authentication in the dev environment."
    - "A walkthrough for the admin console is documented."
  artifacts:
    - path: "examples/adoption_demo/lib/adoption_demo_web/router.ex"
      provides: "Rindle Admin console route mount"
    - path: "examples/adoption_demo/README.md"
      provides: "Admin console walkthrough instructions"
  key_links:
    - from: "examples/adoption_demo/lib/adoption_demo_web/router.ex"
      to: "Rindle.Admin.Router"
      via: "rindle_admin macro"
      pattern: "rindle_admin"
---

<objective>
Mount the Rindle admin console within the Cohort adoption demo and provide a click-around walkthrough document.

Purpose: To allow testing and demonstration of the admin console UI directly within the demo app, proving it handles all mocked edge-case data.
Output: Admin console running on `/admin` and walkthrough instructions added to README.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount Rindle Admin Console</name>
  <files>examples/adoption_demo/lib/adoption_demo_web/router.ex</files>
  <action>
    Import `Rindle.Admin.Router` at the top of the file (or inside the module).
    Create a new scope `scope "/admin" do` that pipes through `:browser`.
    Inside the scope, mount the console using `rindle_admin "/", allow_unauthenticated?: true`.
    Make sure to avoid conflicts with existing routes.
  </action>
  <verify>
    <automated>cd examples/adoption_demo && mix compile</automated>
  </verify>
  <done>Admin router successfully mounted and project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Document Walkthrough</name>
  <files>examples/adoption_demo/README.md</files>
  <action>
    Add a new section titled "## Admin Console Walkthrough".
    Explain that after seeding the database (`mix run priv/repo/seeds.exs`) and starting the server, developers can visit `http://localhost:4000/admin`.
    Document what to click around to see:
    - The "Assets" list showing various lifecycle states (e.g. `quarantined`, `degraded`).
    - The specific Audio and Document profiles.
    - The Upload Sessions representing failures or stale uploads.
  </action>
  <verify>
    <automated>grep -c "Admin Console Walkthrough" examples/adoption_demo/README.md</automated>
  </verify>
  <done>Walkthrough is documented in the README.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Admin Console | Direct unauthenticated access to admin routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-03 | Elevation of Privilege | Admin Router | accept | `allow_unauthenticated?: true` is explicitly permitted only for demo/dev purposes. Do not use in production. |
</threat_model>

<verification>
1. Run `mix phx.server` in `examples/adoption_demo`.
2. Visit `http://localhost:4000/admin` and verify the Rindle Admin UI loads without throwing an authentication error.
</verification>

<success_criteria>
Cohort mounts the admin console and the click-around walkthrough is documented.
</success_criteria>

<output>
Create `.planning/phases/91-cohort-demo-evolution/91-03-SUMMARY.md` when done
</output>

---
phase: 91-cohort-demo-evolution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/adoption_demo/lib/adoption_demo/rindle_profile.ex
  - examples/adoption_demo/priv/repo/seeds.exs
autonomous: true
requirements:
  - DEMO-02
must_haves:
  truths:
    - "Audio and Document media profiles exist."
    - "Database is seeded with all MediaAsset lifecycle states."
    - "Database is seeded with all MediaVariant lifecycle states."
    - "Database is seeded with all MediaUploadSession lifecycle states."
  artifacts:
    - path: "examples/adoption_demo/lib/adoption_demo/rindle_profile.ex"
      provides: "AudioProfile and DocumentProfile definitions"
    - path: "examples/adoption_demo/priv/repo/seeds.exs"
      provides: "Data seeding scripts for all lifecycle edge cases"
  key_links:
    - from: "examples/adoption_demo/priv/repo/seeds.exs"
      to: "Rindle.Domain.MediaAsset"
      via: "Repo.insert!"
      pattern: "Repo\\.insert!"
---

<objective>
Exercise audio and document media types in Cohort and express every lifecycle state via db seeds.

Purpose: To ensure the admin console and application properly handle all edge cases (degraded, quarantined, etc.) without having to orchestrate complex failure conditions through HTTP requests.
Output: Audio and document profiles defined, and seeds updated to insert comprehensive test data directly into Ecto.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/rindle/domain/media_asset.ex
@lib/rindle/domain/media_variant.ex
@lib/rindle/domain/media_upload_session.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Audio and Document Profiles</name>
  <files>examples/adoption_demo/lib/adoption_demo/rindle_profile.ex</files>
  <action>
    Append two new profiles to `rindle_profile.ex`:
    1. `AdoptionDemo.AudioProfile` using `Rindle.Profile` with `storage: Rindle.Storage.S3`, `allow_mime: ["audio/mpeg", "audio/ogg", "audio/wav"]`, and `max_bytes: 52_428_800`.
    2. `AdoptionDemo.DocumentProfile` using `Rindle.Profile` with `storage: Rindle.Storage.S3`, `allow_mime: ["application/pdf", "text/plain"]`, and `max_bytes: 20_971_520`.
  </action>
  <verify>
    <automated>cd examples/adoption_demo && mix compile</automated>
  </verify>
  <done>Profiles compile correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Seed Edge Case Records</name>
  <files>examples/adoption_demo/priv/repo/seeds.exs</files>
  <action>
    Update `seeds.exs` to `insert!` direct Ecto structs for `MediaAsset`, `MediaVariant`, and `MediaUploadSession` covering all lifecycle states.
    For Asset, cover: `validating`, `analyzing`, `promoting`, `available`, `processing`, `transcoding`, `ready`, `degraded`, `quarantined`, `deleted`, `staged`.
    For Variant, cover: `planned`, `queued`, `processing`, `ready`, `stale`, `missing`, `failed`, `cancelled`, `purged`. Link these to the seeded assets using `asset_id`. Use valid `output_kind` values (e.g. `image` or `video`).
    For UploadSession, cover: `initialized`, `signed`, `resuming`, `uploading`, `uploaded`, `verifying`, `completed`, `aborted`, `expired`, `failed`. Link them to assets. Use valid values for `upload_strategy` and `upload_key`.
    Ensure you specify `profile: "AdoptionDemo.DocumentProfile"` or `"AdoptionDemo.AudioProfile"`, `kind`, and `storage_key` so constraints don't fail.
  </action>
  <verify>
    <automated>cd examples/adoption_demo && mix run priv/repo/seeds.exs</automated>
  </verify>
  <done>Seeds run successfully without constraint violations and edge cases exist in the database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> DB | Seeding data directly into database, bypassing validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-02 | Tampering | DB Seeds | accept | Seeding is for local development only and does not apply to production databases. |
</threat_model>

<verification>
Run `mix run priv/repo/seeds.exs` in the demo app and verify it exits `0`.
</verification>

<success_criteria>
Demo covers audio and document media profiles, and seeds express every asset, variant, and upload-session lifecycle state.
</success_criteria>

<output>
Create `.planning/phases/91-cohort-demo-evolution/91-02-SUMMARY.md` when done
</output>

---
phase: 08-storage-capability-confidence
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - test/rindle/storage/r2_test.exs
  - mix.exs
  - guides/storage_capabilities.md
  - guides/profiles.md
  - guides/secure_delivery.md
autonomous: true
requirements:
  - CAP-03
  - CAP-04
must_haves:
  truths:
    - "Adopters can read one explicit capability guide and know what MinIO, generic S3, Local, and Cloudflare R2 are expected to support today."
    - "Cloudflare R2 has an executable opt-in contract lane plus a phase-owned manual verification path, without requiring live R2 in default CI."
    - "Future GCS resumable support is described as additive capability vocabulary, not as a hidden change to current adopter-facing contracts."
  artifacts:
    - path: "test/rindle/storage/r2_test.exs"
      provides: "Opt-in live R2 contract coverage for the existing S3-adapter seam plus explicit unsupported reserved-capability expectations."
    - path: "guides/storage_capabilities.md"
      provides: "Canonical capability matrix and provider-boundary guide."
    - path: "guides/profiles.md"
      provides: "Profile-facing capability selection guidance that matches the runtime contract."
    - path: "guides/secure_delivery.md"
      provides: "Accurate delivery-capability documentation and R2 signed-delivery notes."
    - path: "mix.exs"
      provides: "ExDoc wiring for the new capability guide."
  key_links:
    - from: "test/rindle/storage/r2_test.exs"
      to: "lib/rindle/storage/s3.ex"
      via: "live R2 environment wired through the shipped S3 adapter seam"
      pattern: "(presigned_put|head|signed_url|multipart)"
    - from: "mix.exs"
      to: "guides/storage_capabilities.md"
      via: "ExDoc extras list"
      pattern: "guides/storage_capabilities\\.md"
    - from: "guides/storage_capabilities.md"
      to: "lib/rindle/storage/capabilities.ex"
      via: "documented capability vocabulary and tagged unsupported errors"
      pattern: "(presigned_put|multipart_upload|signed_url|resumable)"
    - from: "guides/secure_delivery.md"
      to: "guides/storage_capabilities.md"
      via: "delivery capability contract reference"
      pattern: "storage_capabilities"
---

<objective>
Publish an adopter-facing capability guide and opt-in R2 contract lane that make R2 boundaries explicit while keeping future resumable semantics additive.

Purpose: CAP-03 and CAP-04 require honest public documentation plus an auditable proof path, because adopters need to know exactly what the current S3-compatible contract covers, what Phase 8 can execute against R2, and where unsupported future flows stop.
Output: An opt-in live R2 contract test, a new capability guide wired into ExDoc, and drift fixes in the profile and delivery guides.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-storage-capability-confidence/08-RESEARCH.md
@.planning/phases/08-storage-capability-confidence/08-VALIDATION.md
@.planning/phases/08-storage-capability-confidence/08-PATTERNS.md
@test/rindle/storage/r2_test.exs
@guides/profiles.md
@guides/secure_delivery.md
@mix.exs
<interfaces>
Current docs extras in mix.exs:
```elixir
extras: [
  "README.md",
  "guides/getting_started.md",
  "guides/core_concepts.md",
  "guides/profiles.md",
  "guides/secure_delivery.md"
]
```

Expected opt-in execution lane:
```elixir
@tag :r2
test "R2 contract ..." do
  # skipped unless required R2 env vars are present
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the opt-in Cloudflare R2 contract lane per the resolved Phase 8 strategy</name>
  <files>test/rindle/storage/r2_test.exs</files>
  <action>Create `test/rindle/storage/r2_test.exs` as an executable `@tag :r2` contract lane per the resolved R2 strategy in `08-RESEARCH.md`: live R2 remains opt-in and manual, not part of default CI because the repo has no R2 secrets. The file should configure the shipped S3 adapter against env-provided R2 credentials/endpoints, skip with an explicit message when required env vars are absent, and when credentials are present verify the current shipped contract only: presigned PUT round-trip plus `head/2`, signed-delivery URL generation if the adapter advertises `:signed_url`, multipart round-trip if the adapter advertises `:multipart_upload`, and explicit tagged unsupported behavior for the reserved future capability via `{:error, {:upload_unsupported, :resumable_upload}}`. Do not add a bespoke R2 adapter, do not require CI secrets, and do not broaden the public API surface.</action>
  <verify>
    <automated>mix test test/rindle/storage/r2_test.exs --include r2</automated>
  </verify>
  <done>The repo has an auditable opt-in R2 contract lane that stays out of default CI, exercises the shipped S3-adapter seam against live R2 when secrets are present, and locks the reserved resumable capability to an explicit tagged unsupported error.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the canonical capability guide and remove docs drift</name>
  <files>guides/storage_capabilities.md, guides/profiles.md, guides/secure_delivery.md, mix.exs</files>
  <action>Create `guides/storage_capabilities.md` as the canonical adopter-facing matrix per D-04 and wire it into `mix.exs` `docs.extras`. The guide must define the shipped capability atoms, explain the current tagged unsupported tuple shapes, distinguish MinIO-backed proof from the new opt-in/manual R2 contract lane, and spell out Cloudflare R2 boundaries honestly: current Rindle uses the existing S3-adapter contract where the provider documents compatible operations, but Phase 8 does not require live R2 in default CI and does not claim HTML form POST or future resumable support. Update `guides/profiles.md` and `guides/secure_delivery.md` so they stop hard-coding stale capability lists, link readers to `guides/storage_capabilities.md`, call out the explicit tagged failures for unsupported delivery/upload flows, and describe future resumable support as an additive capability family rather than a hidden contract change per D-05. Do not claim a bespoke R2 adapter, live default-CI proof, or a shipped GCS adapter/public resumable API.</action>
  <verify>
    <automated>mix docs && rg -n "guides/storage_capabilities\\.md" mix.exs && rg -n "(Cloudflare R2|r2|resumable|upload_unsupported|delivery_unsupported|manual verification|opt-in)" guides/storage_capabilities.md guides/profiles.md guides/secure_delivery.md</automated>
  </verify>
  <done>The existing guides no longer advertise stale capability lists, they point to one canonical provider matrix, and their wording matches the shipped capability/error contract honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → adopters | capability promises in guides become real integration expectations and support burden |
| provider docs → Rindle guide | external compatibility claims must be translated carefully without overstating repo proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | T | `test/rindle/storage/r2_test.exs` | mitigate | gate live R2 execution behind explicit `:r2` opt-in and required env vars, then assert unsupported reserved capability flows return tagged errors instead of silently degrading |
| T-08-08 | R | `guides/storage_capabilities.md` | mitigate | distinguish "MinIO-backed proof" from the opt-in/manual R2 contract lane so provider claims stay honest and auditable |
| T-08-09 | I | `guides/secure_delivery.md` | mitigate | document explicit `{:delivery_unsupported, :signed_url}` behavior so private-delivery failures are loud, not guessed |
| T-08-10 | T | `guides/profiles.md` | mitigate | remove stale hard-coded adapter lists and point to the shared capability guide so future edits converge on one source of truth |
</threat_model>

<verification>
Run:
- `mix test test/rindle/storage/r2_test.exs --include r2`
- `mix docs`

Then grep the updated guides for the shipped capability atoms and R2/resumable notes, and confirm the docs distinguish default CI from the opt-in/manual R2 lane.

Before closing the phase, confirm the docs do not promise live R2 in default CI, a bespoke R2 adapter, or a shipped GCS adapter/public resumable API in v1.1.
</verification>

<success_criteria>
- The repo includes an opt-in `:r2` contract lane plus explicit manual verification instructions in the phase validation artifact.
- ExDoc includes a canonical storage-capability guide.
- `guides/profiles.md` and `guides/secure_delivery.md` no longer drift from the shipped capability contract.
- R2 boundaries and future resumable semantics are explicit, honest, and additive.
</success_criteria>

<output>
After completion, create `.planning/phases/08-storage-capability-confidence/08-03-SUMMARY.md`
</output>

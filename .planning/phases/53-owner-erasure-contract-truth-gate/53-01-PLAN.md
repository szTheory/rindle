---
phase: 53-owner-erasure-contract-truth-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle.ex
  - test/rindle/api_surface_boundary_test.exs
autonomous: true
requirements: [LIFE-01]
must_haves:
  truths:
    - "The code-facing public contract names `Rindle.preview_owner_erasure/2` and `Rindle.erase_owner/2` as the recommended owner/account erasure facade without prematurely shipping the execute lane."
    - "The report vocabulary is frozen to `attachments_to_detach`, `assets_to_purge`, and `retained_shared_assets`, with explicit retained-shared-asset semantics."
    - "Facade docs do not claim inline storage deletion, force-delete semantics, or bulk/admin orchestration."
  artifacts:
    - path: "lib/rindle.ex"
      provides: "Code-facing owner-erasure contract wording and typed report vocabulary"
      contains: "owner_erasure_report"
    - path: "test/rindle/api_surface_boundary_test.exs"
      provides: "Boundary assertions that freeze the contract strings"
      contains: "preview_owner_erasure/2"
  key_links:
    - from: "lib/rindle.ex"
      to: ".planning/phases/53-owner-erasure-contract-truth-gate/53-CONTEXT.md"
      via: "Facade docs inherit the exact names, report buckets, and non-goals locked in phase context."
      pattern: "preview_owner_erasure/2|erase_owner/2|retained_shared_assets"
    - from: "test/rindle/api_surface_boundary_test.exs"
      to: "lib/rindle.ex"
      via: "Boundary tests must assert the same strings the facade docs publish."
      pattern: "Code.fetch_docs\\(Rindle\\)|preview_owner_erasure/2|assets_to_purge"
---

# Phase 53 Plan 01

<objective>
Freeze the code-facing owner/account erasure contract in the public facade docs
before destructive implementation lands.

Purpose: give Phase 54 and Phase 55 a stable public vocabulary for the owner
erasure surface without shipping an executable delete path prematurely.
Output: updated `lib/rindle.ex` docs/types plus boundary tests that lock the
future facade names, report buckets, retained-shared-asset rule, and deferred
non-goals.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/53-owner-erasure-contract-truth-gate/53-CONTEXT.md
@.planning/phases/53-owner-erasure-contract-truth-gate/53-RESEARCH.md
@.planning/phases/53-owner-erasure-contract-truth-gate/53-PATTERNS.md
@lib/rindle.ex
@test/rindle/api_surface_boundary_test.exs
@lib/rindle/ops/upload_maintenance.ex
@guides/user_flows.md

<interfaces>
Required public contract names:

```elixir
Rindle.preview_owner_erasure/2
Rindle.erase_owner/2
```

Required report buckets:

```elixir
attachments_to_detach
assets_to_purge
retained_shared_assets
```

Required deferred/non-goal language:

```text
No admin UI
No bulk orchestration
No force-delete behavior for assets with surviving attachments
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add owner-erasure contract docs and typed vocabulary to `lib/rindle.ex` without exporting a premature runtime entrypoint</name>
  <read_first>
    - lib/rindle.ex
    - .planning/phases/53-owner-erasure-contract-truth-gate/53-CONTEXT.md
    - .planning/phases/53-owner-erasure-contract-truth-gate/53-RESEARCH.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - lib/rindle/ops/upload_maintenance.ex
  </read_first>
  <files>lib/rindle.ex</files>
  <action>Update `lib/rindle.ex` so the facade documentation freezes the future owner-erasure contract without making the execute lane callable yet. Add a new `@typedoc`/`@type` family near the existing result types that introduces the public vocabulary `owner_erasure_report`, with semantic buckets named exactly `attachments_to_detach`, `assets_to_purge`, and `retained_shared_assets`, each documented as count-plus-list reporting buckets. In the `@moduledoc`, add an explicit owner/account erasure section that says the recommended v1.10 public surface is the pair `Rindle.preview_owner_erasure/2` and `Rindle.erase_owner/2`, that shared assets are retained whenever a surviving attachment remains, that execute semantics are detach-now plus purge-enqueued-later, and that admin UI, bulk orchestration, and force-delete semantics remain deferred. Keep `detach/3` and `cleanup_orphans` described as slot-scoped and maintenance-only respectively. Do not add executable `def preview_owner_erasure` or `def erase_owner` function bodies in this plan.</action>
  <acceptance_criteria>
    - `rg -n '@type owner_erasure_report|attachments_to_detach|assets_to_purge|retained_shared_assets' lib/rindle.ex` returns matches.
    - `rg -n 'preview_owner_erasure/2|erase_owner/2|purge-enqueued|retained whenever a surviving attachment remains|maintenance-only' lib/rindle.ex` returns matches.
    - `! rg -n '^\\s*def\\s+(preview_owner_erasure|erase_owner)\\b' lib/rindle.ex` exits `0`.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "@type owner_erasure_report|attachments_to_detach|assets_to_purge|retained_shared_assets" lib/rindle.ex && rg -n "preview_owner_erasure/2|erase_owner/2|purge-enqueued|retained whenever a surviving attachment remains|maintenance-only" lib/rindle.ex && ! rg -n "^\\s*def\\s+(preview_owner_erasure|erase_owner)\\b" lib/rindle.ex'</automated>
  </verify>
  <done>`lib/rindle.ex` now carries a code-facing owner-erasure contract that downstream phases can implement against without reopening naming or shared-asset ambiguity.</done>
</task>

<task type="auto">
  <name>Task 2: Extend API-boundary tests so facade docs cannot drift away from the frozen owner-erasure contract</name>
  <read_first>
    - test/rindle/api_surface_boundary_test.exs
    - lib/rindle.ex
    - .planning/phases/53-owner-erasure-contract-truth-gate/53-PATTERNS.md
    - .planning/phases/53-owner-erasure-contract-truth-gate/53-RESEARCH.md
  </read_first>
  <files>test/rindle/api_surface_boundary_test.exs</files>
  <action>Extend `test/rindle/api_surface_boundary_test.exs` with a new docs-boundary test that fetches the `Rindle` moduledoc through `Code.fetch_docs/1` and asserts it contains the exact contract strings `preview_owner_erasure/2`, `erase_owner/2`, `attachments_to_detach`, `assets_to_purge`, and `retained_shared_assets`. Also assert the rendered moduledoc contains the maintenance/deferred truth markers for `cleanup_orphans`, admin UI, bulk orchestration, and force-delete behavior for still-shared assets. Keep the test at the public-doc boundary level; do not assert runtime function exports for the two owner-erasure names in this phase.</action>
  <acceptance_criteria>
    - `rg -n 'preview_owner_erasure/2|erase_owner/2|retained_shared_assets|cleanup_orphans|force-delete' test/rindle/api_surface_boundary_test.exs` returns matches.
    - `mix test test/rindle/api_surface_boundary_test.exs` exits `0`.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "preview_owner_erasure/2|erase_owner/2|retained_shared_assets|cleanup_orphans|force-delete" test/rindle/api_surface_boundary_test.exs && mix test test/rindle/api_surface_boundary_test.exs'</automated>
  </verify>
  <done>The public facade docs are now guarded by an automated boundary test, so Phase 54 cannot accidentally rename or overclaim the owner-erasure contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public facade docs -> future runtime implementation | The docs published in `lib/rindle.ex` become the contract Phase 54 must honor. |
| Frozen report vocabulary -> proof/docs work | Bucket names must stay stable so later proof and adopter guidance do not drift. |
| Deferred scope -> public expectation | Phase 53 must not accidentally imply shipping admin/bulk/force-delete behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01-01 | T | `lib/rindle.ex` contract wording | mitigate | Freeze exact facade names and report buckets with grep-verifiable strings and a docs-boundary test. |
| T-53-01-02 | R | deferred-scope claims | mitigate | Assert explicit non-goal markers for admin UI, bulk orchestration, and force-delete semantics. |
| T-53-01-03 | E | premature API surface | mitigate | Forbid `def preview_owner_erasure` and `def erase_owner` bodies in this plan. |
</threat_model>

<verification>
- `rg -n '@type owner_erasure_report|attachments_to_detach|assets_to_purge|retained_shared_assets' lib/rindle.ex`
- `! rg -n '^\s*def\s+(preview_owner_erasure|erase_owner)\b' lib/rindle.ex`
- `mix test test/rindle/api_surface_boundary_test.exs`
</verification>

<success_criteria>
- `lib/rindle.ex` names the future owner-erasure pair and frozen report buckets explicitly.
- Public docs state the retained-shared-asset rule and the purge-enqueued execution truth.
- API-boundary tests prevent the contract wording from drifting before implementation lands.
</success_criteria>

<output>
After completion, create `.planning/phases/53-owner-erasure-contract-truth-gate/53-01-SUMMARY.md`
</output>

---
phase: 53-owner-erasure-contract-truth-gate
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/user_flows.md
  - test/install_smoke/docs_parity_test.exs
autonomous: true
requirements: [TRUTH-02]
must_haves:
  truths:
    - "Active guide wording stops presenting `detach/3` loops plus `cleanup_orphans` as the long-term recommended account-deletion story."
    - "The guide names `preview_owner_erasure/2` and `erase_owner/2` as the contract being standardized for v1.10 while staying honest about current shipped status."
    - "Shared assets are described as retained when another attachment survives, and admin UI, bulk orchestration, and force-delete semantics stay explicitly deferred."
  artifacts:
    - path: "guides/user_flows.md"
      provides: "Active support-truth wording for the account-deletion story"
      contains: "retained_shared_assets"
    - path: "test/install_smoke/docs_parity_test.exs"
      provides: "Automated guide-parity assertions for the owner-erasure contract wording"
      contains: "owner-erasure"
  key_links:
    - from: "guides/user_flows.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Guide wording must mirror the support-truth gate and deferred non-goals."
      pattern: "cleanup_orphans|retained|bulk orchestration|force-delete"
    - from: "test/install_smoke/docs_parity_test.exs"
      to: "guides/user_flows.md"
      via: "Docs-parity checks lock the exact guide wording that names the new contract."
      pattern: "preview_owner_erasure/2|erase_owner/2|maintenance-only"
---

# Phase 53 Plan 02

<objective>
Align active guide wording with the frozen owner-erasure contract and guard it
with docs-parity tests.

Purpose: remove the stale impression that looping `detach/3` plus
`cleanup_orphans` is the long-term recommended account-deletion story while
keeping public docs honest about what is and is not shipped today.
Output: updated `guides/user_flows.md` plus docs-parity coverage that freezes
the contract names, retained-shared-asset semantics, and deferred non-goals.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/53-owner-erasure-contract-truth-gate/53-CONTEXT.md
@.planning/phases/53-owner-erasure-contract-truth-gate/53-RESEARCH.md
@.planning/phases/53-owner-erasure-contract-truth-gate/53-PATTERNS.md
@guides/user_flows.md
@test/install_smoke/docs_parity_test.exs
@guides/operations.md

<interfaces>
Required guide-story contract markers:

```text
preview_owner_erasure/2
erase_owner/2
retained shared assets
cleanup_orphans remains maintenance-only
```

Required deferred/non-goal markers:

```text
admin UI
bulk orchestration
force-delete
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the account-deletion note in `guides/user_flows.md` so it names the standardized facade and preserves support truth</name>
  <read_first>
    - guides/user_flows.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/53-owner-erasure-contract-truth-gate/53-CONTEXT.md
    - .planning/phases/53-owner-erasure-contract-truth-gate/53-RESEARCH.md
    - guides/operations.md
  </read_first>
  <files>guides/user_flows.md</files>
  <action>Replace the current Story 5 account-deletion note in `guides/user_flows.md` so it no longer reads as a long-term recommendation to loop `detach/3` and then call `mix rindle.cleanup_orphans`. Rewrite that note to say the owner/account erasure contract being standardized for `v1.10` is the pair `Rindle.preview_owner_erasure/2` and `Rindle.erase_owner/2`; shared assets are retained whenever another live attachment survives; `cleanup_orphans` remains upload-residue maintenance rather than the account-deletion API; and admin UI, bulk orchestration, and force-delete semantics for still-shared assets remain deferred. Keep the note honest that the full executable facade lands in later `v1.10` phase work and do not claim the functions are callable yet.</action>
  <acceptance_criteria>
    - `rg -n 'preview_owner_erasure/2|erase_owner/2|retained shared assets|maintenance-only|bulk orchestration|force-delete' guides/user_flows.md` returns matches.
    - `! rg -n 'Today you detach each of an owner''s slots, then let `mix rindle.cleanup_orphans` purge' guides/user_flows.md` exits `0`.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "preview_owner_erasure/2|erase_owner/2|retained shared assets|maintenance-only|bulk orchestration|force-delete" guides/user_flows.md && ! rg -n "Today you detach each of an owner'\''s slots, then let `mix rindle.cleanup_orphans` purge" guides/user_flows.md'</automated>
  </verify>
  <done>The highest-visibility account-deletion guide note now matches the standardized owner-erasure contract and stops teaching the workaround as the long-term recommendation.</done>
</task>

<task type="auto">
  <name>Task 2: Add docs-parity coverage so the active guide cannot drift back to the old workaround story</name>
  <read_first>
    - test/install_smoke/docs_parity_test.exs
    - guides/user_flows.md
    - .planning/phases/53-owner-erasure-contract-truth-gate/53-PATTERNS.md
    - .planning/phases/53-owner-erasure-contract-truth-gate/53-RESEARCH.md
  </read_first>
  <files>test/install_smoke/docs_parity_test.exs</files>
  <action>Extend `test/install_smoke/docs_parity_test.exs` so it reads `guides/user_flows.md` during `setup_all` and adds a focused parity test for the account-deletion note. Assert the guide contains `preview_owner_erasure/2`, `erase_owner/2`, `retained shared assets`, and wording that `cleanup_orphans` is maintenance-only. Also add negative assertions that the guide does not present `detach/3` loops plus `cleanup_orphans` as the long-term recommended account-deletion surface. Keep the assertions string-based and scoped to docs truth; do not require runtime owner-erasure exports in this phase.</action>
  <acceptance_criteria>
    - `rg -n 'user_flows|preview_owner_erasure/2|erase_owner/2|maintenance-only|detach/3' test/install_smoke/docs_parity_test.exs` returns matches.
    - `mix test test/install_smoke/docs_parity_test.exs` exits `0`.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "user_flows|preview_owner_erasure/2|erase_owner/2|maintenance-only|detach/3" test/install_smoke/docs_parity_test.exs && mix test test/install_smoke/docs_parity_test.exs'</automated>
  </verify>
  <done>Guide wording is now frozen by an automated parity test, making support-truth drift visible in CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Active guide wording -> adopter expectations | Users will treat the guide note as support truth even before implementation lands. |
| Planning contract -> guide wording | The guide must inherit the same facade names and non-goals locked in planning artifacts. |
| Old workaround wording -> future docs drift | If the workaround wording remains or returns, adopters will keep bypassing the intended facade. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-02-01 | T | `guides/user_flows.md` | mitigate | Replace the workaround note with exact contract names, retained-shared-asset wording, and deferred non-goals. |
| T-53-02-02 | R | docs parity | mitigate | Add focused ExUnit assertions for both required strings and forbidden workaround wording. |
| T-53-02-03 | I | support-truth boundary | mitigate | Keep the guide explicit that the executable facade lands later, so docs do not overclaim shipped behavior. |
</threat_model>

<verification>
- `rg -n 'preview_owner_erasure/2|erase_owner/2|retained shared assets|maintenance-only' guides/user_flows.md`
- `! rg -n 'Today you detach each of an owner''s slots, then let `mix rindle.cleanup_orphans` purge' guides/user_flows.md`
- `mix test test/install_smoke/docs_parity_test.exs`
</verification>

<success_criteria>
- `guides/user_flows.md` names the standardized owner-erasure facade and retained-shared-asset rule.
- The guide explicitly treats `cleanup_orphans` as maintenance-only rather than the long-term account-deletion API.
- Docs-parity tests fail if the guide drifts back to the old workaround-first story.
</success_criteria>

<output>
After completion, create `.planning/phases/53-owner-erasure-contract-truth-gate/53-02-SUMMARY.md`
</output>

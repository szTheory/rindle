---
phase: 07-multipart-uploads
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
files_modified:
  - test/adopter/canonical_app/lifecycle_test.exs
  - test/rindle/storage/s3_test.exs
  - test/rindle/upload/lifecycle_integration_test.exs
autonomous: true
requirements:
  - MULT-01
  - MULT-02
  - MULT-03
must_haves:
  truths:
    - "A real MinIO-backed adopter flow can initiate multipart uploads, upload parts, complete them, and promote the asset through the existing verification lane."
    - "A real S3-compatible adapter proves multipart abort and completion behavior instead of relying on mocks only."
    - "The existing presigned PUT and multipart paths can coexist without replacing one another."
  artifacts:
    - path: "test/adopter/canonical_app/lifecycle_test.exs"
      provides: "Canonical adopter multipart proof against MinIO."
    - path: "test/rindle/storage/s3_test.exs"
      provides: "Low-level MinIO multipart adapter proof including abort/complete behavior."
    - path: "test/rindle/upload/lifecycle_integration_test.exs"
      provides: "Library-owned multipart integration coverage alongside existing presigned PUT coverage."
  key_links:
    - from: "test/adopter/canonical_app/lifecycle_test.exs"
      to: "lib/rindle/upload/broker.ex"
      via: "multipart broker entrypoints over MinIO"
      pattern: "multipart"
    - from: "test/rindle/storage/s3_test.exs"
      to: "lib/rindle/storage/s3.ex"
      via: "real MinIO multipart primitives"
      pattern: "multipart"
    - from: "test/rindle/upload/lifecycle_integration_test.exs"
      to: "lib/rindle/upload/broker.ex"
      via: "multipart completion and maintenance integration"
      pattern: "multipart"
---

<objective>
Prove the multipart path against the real MinIO-backed S3 harness and canonical adopter flow.

Purpose: Satisfy D-09 with real storage proof that multipart happy-path and abort behavior work in the same harness Rindle already trusts.
Output: MinIO-backed adapter, library integration, and canonical adopter tests for multipart initiation, completion, and abort.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-multipart-uploads/07-CONTEXT.md
@.planning/phases/07-multipart-uploads/07-RESEARCH.md
@.planning/phases/07-multipart-uploads/07-PATTERNS.md
@.planning/phases/07-multipart-uploads/07-01-PLAN.md
@.planning/phases/07-multipart-uploads/07-02-PLAN.md
@test/adopter/canonical_app/lifecycle_test.exs
@test/rindle/storage/s3_test.exs
@test/rindle/upload/lifecycle_integration_test.exs

<interfaces>
Expected broker contract from 07-01:
```elixir
{:ok, %{session: session, multipart: %{upload_id: upload_id}}} =
  Rindle.initiate_multipart_upload(Profile, filename: "video.bin")

{:ok, %{presigned: %{url: url}}} =
  Rindle.sign_multipart_part(session.id, 1)

{:ok, %{session: completed, asset: asset}} =
  Rindle.complete_multipart_upload(session.id, [%{part_number: 1, etag: etag}])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add MinIO-backed multipart adapter and broker integration proof</name>
  <read_first>
    - test/rindle/storage/s3_test.exs
    - test/rindle/upload/lifecycle_integration_test.exs
    - .planning/phases/07-multipart-uploads/07-RESEARCH.md
    - .planning/phases/07-multipart-uploads/07-PATTERNS.md
  </read_first>
  <files>test/rindle/storage/s3_test.exs, test/rindle/upload/lifecycle_integration_test.exs</files>
  <behavior>
    - Test 1: MinIO-backed S3 adapter can initiate multipart, sign at least two upload parts, complete using the recorded ETags, and `head/2` the final object.
    - Test 2: Library integration keeps the existing presigned PUT flow green while adding multipart completion coverage in the same file.
  </behavior>
  <action>Extend `test/rindle/storage/s3_test.exs` with a real MinIO multipart round-trip that uses the new S3 adapter callbacks to create an upload, upload at least two parts via the returned presigned URLs, complete with the collected ETags, and verify the object via `head/2`. Extend `test/rindle/upload/lifecycle_integration_test.exs` with multipart integration coverage that exercises `Rindle.initiate_multipart_upload/2`, `Rindle.sign_multipart_part/3`, and `Rindle.complete_multipart_upload/3` while leaving the existing presigned PUT and proxied upload tests intact per D-06. Reuse the current no-new-dependency helper style (`:httpc`, fixture bytes, env-driven MinIO config); do not add a parallel harness.</action>
  <acceptance_criteria>
    - `rg -n "multipart" test/rindle/storage/s3_test.exs test/rindle/upload/lifecycle_integration_test.exs` finds new multipart scenarios in both files.
    - `mix test test/rindle/storage/s3_test.exs --include minio` passes with MinIO env vars set.
    - `mix test test/rindle/upload/lifecycle_integration_test.exs` passes.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rindle/storage/s3_test.exs --include minio && mix test test/rindle/upload/lifecycle_integration_test.exs</automated>
  </verify>
  <done>Rindle has a real S3-compatible multipart proof at both adapter and library integration levels, and the existing presigned PUT path remains covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the canonical adopter MinIO flow to multipart completion and cleanup</name>
  <read_first>
    - test/adopter/canonical_app/lifecycle_test.exs
    - lib/rindle/ops/upload_maintenance.ex
    - .planning/phases/07-multipart-uploads/07-CONTEXT.md
    - .planning/phases/07-multipart-uploads/07-RESEARCH.md
  </read_first>
  <files>test/adopter/canonical_app/lifecycle_test.exs</files>
  <behavior>
    - Test 1: The canonical adopter lane can initiate multipart through MinIO, upload parts directly with `:httpc`, complete the multipart upload, and then promote/variant/process the asset through the existing trusted flow.
    - Test 2: The canonical adopter lane can leave a multipart upload expired, run the maintenance cleanup lane, and observe that the session disappears only after remote abort succeeds.
  </behavior>
  <action>Extend `test/adopter/canonical_app/lifecycle_test.exs` with a canonical adopter multipart scenario that mirrors the existing direct-upload proof but uses the new multipart broker APIs, captures ETags from MinIO part uploads, completes server-side, and then runs the same promotion, variant, signed-delivery, attach, and detach steps per D-05/D-09. Add a second adopter-facing MinIO cleanup scenario that creates a multipart session, leaves it incomplete, advances it into the existing expire-then-cleanup lane, and asserts the remote abort path removes the session only after successful cleanup. Reuse the current env-driven MinIO setup and `:httpc` helpers; no new helper dependency is allowed.</action>
  <acceptance_criteria>
    - `rg -n "multipart" test/adopter/canonical_app/lifecycle_test.exs` finds both completion and cleanup scenarios.
    - `mix test test/adopter/canonical_app/lifecycle_test.exs --include minio` passes with MinIO env vars set.
    - `rg -n "verify_completion|PromoteAsset|ProcessVariant" test/adopter/canonical_app/lifecycle_test.exs` confirms multipart still converges into the existing promotion path.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/adopter/canonical_app/lifecycle_test.exs --include minio</automated>
  </verify>
  <done>The canonical adopter proof exercises multipart end to end on MinIO, including direct part upload, trusted completion, and cleanup of abandoned multipart state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adopter test client→MinIO | Real presigned multipart part uploads happen over HTTP and must capture the server-returned ETags accurately |
| canonical adopter flow→broker | Multipart completion must still route through verification and promotion, not trust remote completion blindly |
| maintenance cleanup→MinIO | Real remote abort behavior must be observed so cleanup proof matches production semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | T | `test/adopter/canonical_app/lifecycle_test.exs` multipart part uploads | mitigate | Capture ETags from actual MinIO `UploadPart` responses and feed them into `complete_multipart_upload/3`; do not fabricate manifest entries in tests. |
| T-07-08 | I | canonical adopter multipart completion | mitigate | Assert multipart completion still triggers `verify_completion`, `PromoteAsset`, and downstream lifecycle jobs so the proof enforces the real trust boundary from D-05. |
| T-07-09 | D | MinIO cleanup proof | mitigate | Add a real cleanup/abort scenario so abandoned multipart uploads are proven removable under the same harness, reducing the risk of shipping an untested cost leak. |
</threat_model>

<verification>
With MinIO available, run `mix test test/rindle/storage/s3_test.exs --include minio`, `mix test test/rindle/upload/lifecycle_integration_test.exs`, and `mix test test/adopter/canonical_app/lifecycle_test.exs --include minio`. If any MinIO-only test fails, the phase is not complete because D-09 requires real S3-compatible proof.
</verification>

<success_criteria>
Real MinIO-backed adapter, library integration, and canonical adopter tests all prove multipart initiation, part upload, completion, trusted promotion, and abandoned-upload cleanup without adding a new harness or dependency.
</success_criteria>

<output>
After completion, create `.planning/phases/07-multipart-uploads/07-03-SUMMARY.md`
</output>

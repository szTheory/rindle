---
phase: 07-multipart-uploads
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - lib/rindle/ops/upload_maintenance.ex
  - lib/rindle/workers/abort_incomplete_uploads.ex
  - test/rindle/ops/upload_maintenance_test.exs
  - test/rindle/workers/maintenance_workers_test.exs
autonomous: true
requirements:
  - MULT-03
must_haves:
  truths:
    - "Timed-out multipart sessions are first marked terminal and only then cleaned up from remote storage."
    - "Failed remote multipart aborts keep enough persisted state to retry later."
    - "Maintenance cleanup operates on the adopter-owned runtime repo seam and does not expire or delete upload sessions in the wrong repo."
  artifacts:
    - path: "lib/rindle/ops/upload_maintenance.ex"
      provides: "Runtime-repo maintenance seam and multipart abort cleanup logic."
    - path: "test/rindle/ops/upload_maintenance_test.exs"
      provides: "Proof that abort failures preserve rows for retry and success deletes them."
    - path: "test/rindle/workers/maintenance_workers_test.exs"
      provides: "Worker delegation proof against the maintenance contract."
  key_links:
    - from: "lib/rindle/workers/abort_incomplete_uploads.ex"
      to: "lib/rindle/ops/upload_maintenance.ex"
      via: "delegated maintenance call"
      pattern: "UploadMaintenance.abort_incomplete_uploads"
    - from: "lib/rindle/ops/upload_maintenance.ex"
      to: "lib/rindle/storage.ex"
      via: "abort_multipart_upload callback"
      pattern: "abort_multipart_upload"
    - from: "lib/rindle/ops/upload_maintenance.ex"
      to: "Rindle.Config.repo/0"
      via: "runtime repo resolution"
      pattern: "Config.repo"
---

<objective>
Extend the maintenance lane to absorb multipart cleanup and close the remaining runtime repo leak in maintenance and worker paths.

Purpose: Deliver D-02 and D-08 so abandoned multipart uploads do not leak storage cost or revert to `Rindle.Repo`.
Output: Runtime-repo-based maintenance logic, multipart abort cleanup semantics, and maintenance/worker tests covering retry behavior.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-multipart-uploads/07-CONTEXT.md
@.planning/phases/07-multipart-uploads/07-RESEARCH.md
@.planning/phases/07-multipart-uploads/07-PATTERNS.md
@.planning/phases/07-multipart-uploads/07-01-PLAN.md
@lib/rindle/ops/upload_maintenance.ex
@lib/rindle/workers/abort_incomplete_uploads.ex
@test/rindle/ops/upload_maintenance_test.exs
@test/rindle/workers/maintenance_workers_test.exs

<interfaces>
From lib/rindle/ops/upload_maintenance.ex:
```elixir
def cleanup_orphans(opts \\ [])
def abort_incomplete_uploads(opts \\ [])
```

Current cleanup invariant:
```elixir
case attempt_storage_delete(session, storage_mod) do
  {:ok, object_increment} -> proceed_with_db_delete(session, acc, object_increment, 0)
  {:skipped, skipped_increment} -> proceed_with_db_delete(session, acc, 0, skipped_increment)
  :storage_error -> Map.update!(acc, :storage_errors, &(&1 + 1))
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move upload maintenance onto the runtime repo seam</name>
  <read_first>
    - lib/rindle/ops/upload_maintenance.ex
    - lib/rindle/workers/abort_incomplete_uploads.ex
    - .planning/phases/06-adopter-runtime-ownership/06-adopter-runtime-ownership-02-SUMMARY.md
    - .planning/phases/07-multipart-uploads/07-RESEARCH.md
  </read_first>
  <files>lib/rindle/ops/upload_maintenance.ex, test/rindle/ops/upload_maintenance_test.exs, test/rindle/workers/maintenance_workers_test.exs</files>
  <behavior>
    - Test 1: Maintenance queries and deletes resolve through `Rindle.Config.repo/0`, not a hard-coded `Rindle.Repo`.
    - Test 2: Worker-driven maintenance still delegates successfully after the repo seam closes.
  </behavior>
  <action>Replace the `alias Rindle.Repo` seam in `lib/rindle/ops/upload_maintenance.ex` with `alias Rindle.Config` plus local `repo = Config.repo()` resolution inside query and delete paths per D-02. Update maintenance tests to assert through a runtime repo probe or adopter sandbox repo rather than the default repo so repo leaks fail loudly. Do not change worker signatures or queues; keep `AbortIncompleteUploads` delegating into `UploadMaintenance.abort_incomplete_uploads/1` with the same job contract.</action>
  <acceptance_criteria>
    - `rg -n "Rindle\\.Repo" lib/rindle/ops/upload_maintenance.ex test/rindle/ops/upload_maintenance_test.exs test/rindle/workers/maintenance_workers_test.exs` returns no matches tied to runtime maintenance paths.
    - `rg -n "Config.repo\\(" lib/rindle/ops/upload_maintenance.ex` matches at least once.
    - `mix test test/rindle/ops/upload_maintenance_test.exs test/rindle/workers/maintenance_workers_test.exs` passes.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rindle/ops/upload_maintenance_test.exs test/rindle/workers/maintenance_workers_test.exs</automated>
  </verify>
  <done>Maintenance logic and worker coverage now run through the adopter-owned runtime repo seam instead of `Rindle.Repo`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add multipart abort cleanup with retry-safe storage ordering</name>
  <read_first>
    - lib/rindle/ops/upload_maintenance.ex
    - lib/rindle/domain/media_upload_session.ex
    - .planning/phases/07-multipart-uploads/07-CONTEXT.md
    - .planning/phases/07-multipart-uploads/07-RESEARCH.md
  </read_first>
  <files>lib/rindle/ops/upload_maintenance.ex, lib/rindle/workers/abort_incomplete_uploads.ex, test/rindle/ops/upload_maintenance_test.exs, test/rindle/workers/maintenance_workers_test.exs</files>
  <behavior>
    - Test 1: `cleanup_orphans/1` calls `abort_multipart_upload/3` for expired multipart sessions before deleting the row.
    - Test 2: `{:error, :not_found}` from multipart abort still deletes the row, while other abort errors keep the row for retry.
    - Test 3: `abort_incomplete_uploads/1` continues to only mark timed-out sessions `expired`; it does not perform storage I/O itself.
  </behavior>
  <action>Extend `UploadMaintenance.cleanup_orphans/1` so sessions with `upload_strategy == "multipart"` and a non-nil `multipart_upload_id` use `storage_mod.abort_multipart_upload(session.upload_key, session.multipart_upload_id, [])` instead of `delete/2`, while preserving the existing two-step expire-then-cleanup lane from D-08. Keep storage I/O outside DB transactions. Treat `{:error, :not_found}` as safe success, increment `storage_errors` and preserve the row on other abort failures, and only delete the DB row after successful abort or safe `:not_found`. Leave `abort_incomplete_uploads/1` as the terminal-state transition step only, but widen its tests to cover multipart-tagged sessions and keep retry semantics explicit.</action>
  <acceptance_criteria>
    - `rg -n "abort_multipart_upload" lib/rindle/ops/upload_maintenance.ex test/rindle/ops/upload_maintenance_test.exs` matches both implementation and tests.
    - `rg -n "storage I/O" lib/rindle/ops/upload_maintenance.ex` or equivalent comments still indicate remote abort stays outside DB transactions.
    - `mix test test/rindle/ops/upload_maintenance_test.exs test/rindle/workers/maintenance_workers_test.exs` passes with multipart-specific cases present.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rindle/ops/upload_maintenance_test.exs test/rindle/workers/maintenance_workers_test.exs</automated>
  </verify>
  <done>Expired multipart sessions clean up through remote abort first, retry safely on storage failure, and preserve the two-step maintenance lane.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker→maintenance service | Scheduled jobs invoke cleanup logic with no interactive user present |
| maintenance service→storage | Remote multipart aborts can fail transiently and must not lose retry state |
| maintenance service→repo | Cleanup deletes rows only after safe remote handling on the runtime repo seam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | D | `lib/rindle/ops/upload_maintenance.ex` cleanup lane | mitigate | Keep the row when `abort_multipart_upload/3` returns a retryable error so orphaned multipart uploads remain discoverable and retryable instead of disappearing from the DB. |
| T-07-05 | T | `lib/rindle/ops/upload_maintenance.ex` repo seam | mitigate | Resolve all maintenance persistence through `Rindle.Config.repo/0` and prove it with runtime-repo-aware tests so adopters cannot silently clean the wrong repo. |
| T-07-06 | R | `lib/rindle/workers/abort_incomplete_uploads.ex` | mitigate | Preserve the existing worker delegation and log/telemetry path so failed expiry or cleanup runs remain observable and attributable in Oban. |
</threat_model>

<verification>
Run `mix test test/rindle/ops/upload_maintenance_test.exs test/rindle/workers/maintenance_workers_test.exs`. Inspect the modified tests to ensure they cover multipart success, `:not_found`, and retryable abort failure paths.
</verification>

<success_criteria>
The remaining maintenance repo leak is removed, expired multipart uploads are aborted remotely before row deletion, and MULT-03 behavior is covered by automated maintenance and worker tests.
</success_criteria>

<output>
After completion, create `.planning/phases/07-multipart-uploads/07-02-SUMMARY.md`
</output>

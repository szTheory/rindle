---
phase: 57-tus-checksum-and-defer-length
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/*_add_upload_length_to_media_upload_sessions.exs
  - lib/rindle/domain/media_upload_session.ex
  - lib/rindle/upload/tus_plug.ex
  - test/rindle/upload/tus_plug_test.exs
autonomous: true
requirements: [REQ-01, REQ-02, REQ-03, REQ-04, REQ-05]
must_haves:
  truths:
    - Client can upload chunks with checksum verification
    - Mismatched checksums are rejected and disk space is reclaimed
    - Client can start upload with unknown size using defer-length
    - Deferred lengths are successfully recorded upon the first chunk
  artifacts:
    - path: "priv/repo/migrations/*_add_upload_length_to_media_upload_sessions.exs"
      provides: "Database column for deferred length"
    - path: "lib/rindle/domain/media_upload_session.ex"
      provides: "Ecto schema exposure for upload_length"
    - path: "lib/rindle/upload/tus_plug.ex"
      provides: "Protocol extension logic for Checksum and Defer-Length"
  key_links:
    - from: "lib/rindle/upload/tus_plug.ex"
      to: ":crypto"
      via: "hash_init/hash_update/hash_final"
      pattern: ":crypto\\.hash_"
    - from: "lib/rindle/upload/tus_plug.ex"
      to: "MediaUploadSession"
      via: "Database update on PATCH for deferred length"
      pattern: "upload_length"
---

<objective>
Implement the tus `checksum` and `creation-defer-length` extensions in `Rindle.Upload.TusPlug` to comply with the Phase 57 roadmap goals.

Purpose: Provide data integrity over the wire and allow uploads of unknown size.
Output: Working protocol extensions with database support and unit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database Migration for Defer-Length</name>
  <files>priv/repo/migrations/*_add_upload_length_to_media_upload_sessions.exs, lib/rindle/domain/media_upload_session.ex</files>
  <action>
    Introduce the `upload_length` column for deferred length persistence:
    1. Check if the migration `*_add_upload_length_to_media_upload_sessions.exs` already exists. If not, generate it via `mix ecto.gen.migration add_upload_length_to_media_upload_sessions`.
    2. Add `:upload_length, :integer, null: true` to the `media_upload_sessions` table in the migration.
    3. Update `lib/rindle/domain/media_upload_session.ex` to add `field :upload_length, :integer` to the schema and include `:upload_length` in the `cast` list of the `changeset/2` function.
  </action>
  <verify>
    <automated>mix ecto.migrate</automated>
  </verify>
  <done>Database is migrated and schema is updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TusPlug Checksum and Defer-Length Implementations</name>
  <files>lib/rindle/upload/tus_plug.ex, test/rindle/upload/tus_plug_test.exs</files>
  <behavior>
    - Test 1: OPTIONS advertises checksum and creation-defer-length.
    - Test 2: PATCH verifies valid checksum (sha1 and sha256).
    - Test 3: PATCH returns 460 and cleans up temp file on invalid checksum.
    - Test 4: POST with Upload-Defer-Length: 1 creates a deferred session.
    - Test 5: PATCH on deferred session without length returns 400.
    - Test 6: PATCH on deferred session with length persists it in the DB.
  </behavior>
  <action>
    Implement protocol logic in `lib/rindle/upload/tus_plug.ex`:
    1. Update OPTIONS responses to include `checksum` and `creation-defer-length` in `Tus-Extension`. Add `Tus-Checksum-Algorithm: sha1,sha256`.
    2. Add `parse_upload_checksum(conn)` to parse algorithm and base64 hash from `Upload-Checksum`.
    3. Modify `drain/7` (and `write_chunk/8`) to accept `hash_ctx` and progressively update it using `:crypto.hash_update`. Validate finalized hash. On mismatch, return `{:error, :checksum_mismatch}` and ensure `temp_path` is deleted.
    4. Handle `Upload-Defer-Length: 1` in POST: create a token with `length: "deferred"`.
    5. In PATCH, if token has `length: "deferred"`, look up `MediaUploadSession` by ID. If `upload_length` is nil, require `Upload-Length` header, validate it against `max_size`, update the session DB record via changeset, and continue stream handling. Return `400` if missing.
    6. Ensure `status_for` handlers cover `:invalid_length` (400), `:invalid_checksum` (400), and `:checksum_mismatch` (460).
    
    Implement tests in `test/rindle/upload/tus_plug_test.exs` ensuring full coverage of these scenarios.
  </action>
  <verify>
    <automated>mix test test/rindle/upload/tus_plug_test.exs</automated>
  </verify>
  <done>Plug supports both extensions and tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted input flows from client into the Plug processing layer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Denial of Service | TusPlug (stream reading) | mitigate | strictly enforce streaming reads with `@read_length` and progressive `:crypto` hashing to prevent memory exhaustion. |
| T-57-02 | Tampering | TusPlug (temp file) | mitigate | synchronously clean up temporary patch file if integrity validation fails. |
</threat_model>

<verification>
mix test
mix format --check-formatted
mix credo --strict
</verification>

<success_criteria>
- Both tus extensions `checksum` and `creation-defer-length` are fully functional.
- All unit tests pass.
- No regression in existing tus compliance.
</success_criteria>

<output>
After completion, create `.planning/phases/57-tus-checksum-and-defer-length/57-01-SUMMARY.md`
</output>
---
phase: 22-liveview-corrective-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/live_view.ex
  - test/rindle/live_view_test.exs
  - README.md
  - guides/getting_started.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "LiveView internal failures return `{:error, %{reason: ...}, socket}` to conform to Phoenix protocols"
    - "consume_uploaded_entries raises explicitly when `session_id` is missing"
    - "consume_uploaded_entries correctly postpones on verification failure"
    - "consume_uploaded_entries handles duplicate idempotent calls gracefully"
    - "Onboarding guides teach safe nil-checking for optional media"
  artifacts:
    - path: "lib/rindle/live_view.ex"
      provides: "Corrected protocol shapes and idempotency logic"
    - path: "test/rindle/live_view_test.exs"
      provides: "Regression tests for missing session_id, failure returns, and duplicate calls"
    - path: "README.md"
      provides: "Nil-safe attachment lookup examples"
    - path: "guides/getting_started.md"
      provides: "Nil-safe attachment lookup examples"
  key_links:
    - from: "lib/rindle/live_view.ex"
      to: "Phoenix.LiveView.Upload"
      via: "protocol return shapes"
      pattern: "\\{:error, %\\{.*\\}, socket\\}"
---

<objective>
Fix Phase 20 code review findings for `Rindle.LiveView` and onboarding documentation nil-dereferences.

Purpose: Ensure LiveView integration conforms strictly to Phoenix LiveView external upload contracts, behaves idempotently, and the documentation examples provide production-safe nil-handling.
Output: Patched `Rindle.LiveView` implementation, updated tests, and fixed documentation examples.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-v1.3-verification-and-metadata-closure/20-REVIEW.md
@.planning/phases/22-liveview-corrective-fixes/22-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix LiveView Protocol Defects</name>
  <files>lib/rindle/live_view.ex, test/rindle/live_view_test.exs</files>
  <action>
Implement the fixes for CR-01, CR-02, WR-01, WR-02, WR-03, and IN-01 as specified in the Phase 20 review.

Changes to `lib/rindle/live_view.ex`:
1. **CR-01, WR-01**: In `do_allow_upload/3` and `handle_initiate_upload/3`, change `{:error, reason}` to `{:error, %{reason: inspect(reason)}, socket}` to satisfy Phoenix LiveView protocol.
2. **CR-02**: In `do_consume/3`, do not fail silently if `session_id` is missing; raise `ArgumentError`.
3. **WR-02**: In `do_consume/3`, change the failure return of `Rindle.verify_completion/2` to `{:postpone, {:error, {:rindle_verify_failed, reason}}}`.
4. **WR-03**: In `do_consume/3`, check if the session is already `"completed"` before calling `verify_completion` to make the callback idempotent.
5. **IN-01**: Update `@spec consume_uploaded_entries` to correctly model `consume_func` returning `{:ok, term()} | {:postpone, term()}`.

Changes to `test/rindle/live_view_test.exs`:
1. Update existing tests affected by the protocol return shape changes.
2. Add a test asserting `ArgumentError` is raised when `:session_id` is missing in meta.
3. Add a test asserting `{:postpone, ...}` is returned when `verify_completion` fails.
4. Add a test asserting idempotent calls on the same `session_id` succeed.
  </action>
  <verify>
    <automated>mix test test/rindle/live_view_test.exs</automated>
  </verify>
  <done>All `live_view_test.exs` pass, covering all the updated failure paths and idempotency.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Documentation and Minor Issues</name>
  <files>README.md, guides/getting_started.md, lib/rindle/live_view.ex, test/rindle/live_view_test.exs</files>
  <action>
Implement the fixes for WR-04, WR-05, IN-02, and IN-03.

1. **WR-04**: In `README.md` and `guides/getting_started.md`, update the controller example that uses `Rindle.attachment_for/2`. Wrap it in a `case` or pattern-match to prevent `KeyError` when `avatar` is nil.
2. **WR-05**: In `lib/rindle/live_view.ex`'s moduledoc example, change `{:ok, entry.asset_id}` to `{:ok, meta.asset_id}` and ensure `meta` isn't prefixed with an underscore.
3. **IN-02**: In `test/rindle/live_view_test.exs`, change `Code.ensure_loaded?(Rindle.LiveView)` to `Code.ensure_loaded!(Rindle.LiveView)`.
4. **IN-03**: In `test/rindle/live_view_test.exs`, move the moduledoc verify_completion test out of the `consume_uploaded_entries/3` describe block into a `describe "moduledoc"` or `describe "module availability"` block.
  </action>
  <verify>
    <automated>mix test test/install_smoke/docs_parity_test.exs test/rindle/live_view_test.exs</automated>
  </verify>
  <done>Documentation nil-deref bugs are fixed, moduledocs are correct, and tests run clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Client | Presigned URLs and upload metadata returned to untrusted client browsers. |
| Client → Server | Metadata (`session_id`) passed back during `consume_uploaded_entries` is untrusted and must correspond to an active session. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | `consume_uploaded_entries` | mitigate | Raise `ArgumentError` explicitly if `session_id` is missing rather than failing open (CR-02). |
| T-22-02 | Information Disclosure | `handle_initiate_upload` | mitigate | Ensure adapter failure details are scoped appropriately in the `%{reason: ...}` map passed to the client (CR-01). |
</threat_model>

<verification>
Ensure all Mix tests run without warnings or failures, particularly `test/rindle/live_view_test.exs` and `test/install_smoke/docs_parity_test.exs`.
</verification>

<success_criteria>
- CR-01, CR-02, WR-01, WR-02, WR-03, WR-04, WR-05, IN-01, IN-02, IN-03 are addressed.
- The `Rindle.LiveView` module properly conforms to Phoenix LiveView external upload contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/22-liveview-corrective-fixes/22-01-SUMMARY.md`
</output>

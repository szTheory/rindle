---
phase: 59-e2e-proof-truth-closure
plan: "02"
type: execute
wave: 2
depends_on:
  - 59-01
files_modified:
  - guides/resumable_uploads.md
  - test/install_smoke/phoenix_tus_truth_parity_test.exs
  - test/install_smoke/generated_app_smoke_test.exs
  - .planning/milestones/v1.11-MILESTONE-AUDIT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - PROOF-01
  - TRUTH-01
  - TUS-01
  - TUS-02
  - TUS-03
  - TUS-04
must_haves:
  truths:
    - "`guides/resumable_uploads.md` contains `creation`, `expiration`, `termination`, `checksum`, `creation-defer-length`, and `concatenation` in an explicit extension support statement."
    - "`test/install_smoke/phoenix_tus_truth_parity_test.exs` and `test/install_smoke/generated_app_smoke_test.exs` both include literal assertions for `checksum`, `creation-defer-length`, `concatenation`, `parallelUploads`, and `uploadLengthDeferred`."
    - "`.planning/milestones/v1.11-MILESTONE-AUDIT.md` contains `requirements: 6/6`, lists `PROOF-01`, `TRUTH-01`, `TUS-01`, `TUS-02`, `TUS-03`, `TUS-04`, and includes `unresolved_high_threats: 0`."
  artifacts:
    - path: "guides/resumable_uploads.md"
      provides: "canonical adopter-facing statement of full tus extension support"
      contains: "checksum"
    - path: "test/install_smoke/phoenix_tus_truth_parity_test.exs"
      provides: "fast drift gate for extension truth strings"
      contains: "creation-defer-length"
    - path: ".planning/milestones/v1.11-MILESTONE-AUDIT.md"
      provides: "milestone closure scorecard and evidence links"
      contains: "requirements: 6/6"
  key_links:
    - from: "guides/resumable_uploads.md"
      to: "test/install_smoke/phoenix_tus_truth_parity_test.exs"
      via: "parity assertions freeze exact extension-support wording"
      pattern: "checksum|creation-defer-length|concatenation|parallelUploads"
    - from: "tmp/install_smoke_tus_last_run.json"
      to: ".planning/milestones/v1.11-MILESTONE-AUDIT.md"
      via: "closure evidence references machine-readable proof fields"
      pattern: "extensions|completion_surface|phoenix_state_sequence"
---

<objective>
Close truth/parity and milestone documentation for Phase 59 by making the guide
claims executable, test-frozen, and backed by fresh E2E evidence artifacts.

Purpose: satisfy `TRUTH-01` while finalizing `PROOF-01` closure evidence for the
v1.11 milestone.
Output: updated tus guide, parity assertions, and milestone/state artifacts that
cite green proof commands and machine-readable evidence pointers.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-e2e-proof-truth-closure/59-CONTEXT.md
@.planning/phases/59-e2e-proof-truth-closure/59-RESEARCH.md
@.planning/phases/59-e2e-proof-truth-closure/59-PATTERNS.md
@.planning/phases/59-e2e-proof-truth-closure/59-VALIDATION.md
@guides/resumable_uploads.md
@test/install_smoke/phoenix_tus_truth_parity_test.exs
@test/install_smoke/generated_app_smoke_test.exs
@tmp/install_smoke_tus_last_run.json
</context>

<must_haves>
- `guides/resumable_uploads.md` explicitly documents support for `creation`, `expiration`, `termination`, `checksum`, `creation-defer-length`, and `concatenation`.
- Guide examples include concrete adopter knobs: `parallelUploads` for concatenation and `uploadLengthDeferred` for defer-length.
- Parity tests freeze these exact claims in both fast and generated-app truth surfaces.
- Milestone closure docs (`ROADMAP`, `STATE`, and `v1.11-MILESTONE-AUDIT.md`) update only after green proof commands and artifact checks.
</must_haves>

<tasks>

<task id="59-02-01" type="auto">
  <name>Task 1: Update tus guide to reflect full protocol support and adopter configuration knobs</name>
  <read_first>
    - guides/resumable_uploads.md
    - .planning/phases/59-e2e-proof-truth-closure/59-CONTEXT.md
    - .planning/phases/59-e2e-proof-truth-closure/59-RESEARCH.md
    - .planning/phases/59-e2e-proof-truth-closure/59-PATTERNS.md
    - test/install_smoke/phoenix_tus_truth_parity_test.exs
  </read_first>
  <files>guides/resumable_uploads.md</files>
  <action>In `guides/resumable_uploads.md`, add this exact support line as a single statement: `Supported tus extensions: creation, expiration, termination, checksum, creation-defer-length, concatenation.` Update client examples to show concrete extension activation knobs: `parallelUploads: 2` (or higher) for concatenation and `uploadLengthDeferred: true` for unknown-length uploads. Keep the existing sticky-session/single-node caveat and avoid introducing claims for out-of-scope items (tus 2.0 or standalone client package).</action>
  <acceptance_criteria>
    - `rg -n 'creation|expiration|termination|checksum|creation-defer-length|concatenation' guides/resumable_uploads.md` returns matches in the support statement.
    - `rg -n 'Supported tus extensions: creation, expiration, termination, checksum, creation-defer-length, concatenation\\.' guides/resumable_uploads.md` returns a match.
    - `rg -n 'parallelUploads:\\s*2|uploadLengthDeferred:\\s*true|@uppy/tus|tus-js-client' guides/resumable_uploads.md` returns matches.
    - `! rg -n 'tus 2\\.0|RUFH|standalone tus client package shipped' guides/resumable_uploads.md` exits `0`.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "creation|expiration|termination|checksum|creation-defer-length|concatenation" guides/resumable_uploads.md && rg -n "Supported tus extensions: creation, expiration, termination, checksum, creation-defer-length, concatenation\\." guides/resumable_uploads.md && rg -n "parallelUploads:\\s*2|uploadLengthDeferred:\\s*true|@uppy/tus|tus-js-client" guides/resumable_uploads.md && ! rg -n "tus 2\\.0|RUFH|standalone tus client package shipped" guides/resumable_uploads.md'</automated>
  </verify>
  <done>The guide states full tus extension support and shows concrete, adopter-usable configuration for the newly completed extensions.</done>
</task>

<task id="59-02-02" type="auto">
  <name>Task 2: Freeze extension truth claims in parity tests and generated-app parity helper assertions</name>
  <read_first>
    - test/install_smoke/phoenix_tus_truth_parity_test.exs
    - test/install_smoke/generated_app_smoke_test.exs
    - guides/resumable_uploads.md
    - test/install_smoke/support/generated_app_helper.ex
    - .planning/phases/59-e2e-proof-truth-closure/59-VALIDATION.md
  </read_first>
  <files>test/install_smoke/phoenix_tus_truth_parity_test.exs, test/install_smoke/generated_app_smoke_test.exs</files>
  <action>Extend `test/install_smoke/phoenix_tus_truth_parity_test.exs` and `assert_tus_guide_parity!/0` in `test/install_smoke/generated_app_smoke_test.exs` to assert the exact Phase 59 guide vocabulary: `checksum`, `creation-defer-length`, `concatenation`, `parallelUploads`, and `uploadLengthDeferred`. Keep assertions literal and narrow (string checks), add at least one drift guard that fails on stale conservative wording implying only `parallelUploads: 1` support, and keep existing Phoenix helper/truth assertions intact.</action>
  <acceptance_criteria>
    - `rg -n 'checksum|creation-defer-length|concatenation|parallelUploads|uploadLengthDeferred' test/install_smoke/phoenix_tus_truth_parity_test.exs test/install_smoke/generated_app_smoke_test.exs` returns matches.
    - `mix test test/install_smoke/phoenix_tus_truth_parity_test.exs` exits `0`.
    - `mix test test/install_smoke/generated_app_smoke_test.exs --include minio` exits `0`.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "checksum|creation-defer-length|concatenation|parallelUploads|uploadLengthDeferred" test/install_smoke/phoenix_tus_truth_parity_test.exs test/install_smoke/generated_app_smoke_test.exs && mix test test/install_smoke/phoenix_tus_truth_parity_test.exs && mix test test/install_smoke/generated_app_smoke_test.exs --include minio'</automated>
  </verify>
  <done>Guide truth is now merge-blocked by fast parity and generated-app parity checks.</done>
</task>

<task id="59-02-03" type="auto">
  <name>Task 3: Close v1.11 planning truth with audit artifact and evidence-backed status updates</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/REQUIREMENTS.md
    - .planning/milestones/v1.10-MILESTONE-AUDIT.md
    - .planning/phases/59-e2e-proof-truth-closure/59-VALIDATION.md
    - tmp/install_smoke_tus_last_run.json
  </read_first>
  <files>.planning/milestones/v1.11-MILESTONE-AUDIT.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>After green verification commands, create `.planning/milestones/v1.11-MILESTONE-AUDIT.md` using the v1.10 audit structure and include closure evidence for `TUS-01..04`, `PROOF-01`, and `TRUTH-01`. Add explicit security closure fields to the audit: `unresolved_high_threats: 0` and threat status lines for `T-59-01-H1`, `T-59-01-H2`, `T-59-02-H1`, and `T-59-02-H2` marked `closed`. Reference command evidence (`mix test test/rindle/upload/tus_plug_test.exs`, `mix test test/install_smoke/phoenix_tus_truth_parity_test.exs`, `mix test test/install_smoke/generated_app_smoke_test.exs --include minio`, `bash scripts/install_smoke.sh tus`) and machine-readable artifact pointers from `tmp/install_smoke_tus_last_run.json` (`extensions.*`, `completion_surface`, `phoenix_state_sequence`, `tus_failure_phase`). Then update `.planning/ROADMAP.md` and `.planning/STATE.md` to mark Phase 59 and milestone v1.11 closed with consistent status wording and next-step notes.</action>
  <acceptance_criteria>
    - `test -f .planning/milestones/v1.11-MILESTONE-AUDIT.md` exits `0`.
    - `rg -n 'requirements:\\s*6/6|PROOF-01|TRUTH-01|TUS-01|TUS-02|TUS-03|TUS-04|status:\\s*passed' .planning/milestones/v1.11-MILESTONE-AUDIT.md` returns matches.
    - `rg -n 'unresolved_high_threats:\\s*0|T-59-01-H1|T-59-01-H2|T-59-02-H1|T-59-02-H2|closed' .planning/milestones/v1.11-MILESTONE-AUDIT.md` returns matches.
    - `rg -n 'Phase 59|v1\\.11|closed|shipped' .planning/ROADMAP.md .planning/STATE.md` returns matches.
    - `bash scripts/install_smoke.sh tus` exits `0`.
    - `rg -n '"extensions"|"completion_surface"|"phoenix_state_sequence"|"tus_failure_phase"' tmp/install_smoke_tus_last_run.json` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'mix test test/rindle/upload/tus_plug_test.exs && mix test test/install_smoke/phoenix_tus_truth_parity_test.exs && mix test test/install_smoke/generated_app_smoke_test.exs --include minio && bash scripts/install_smoke.sh tus && test -f .planning/milestones/v1.11-MILESTONE-AUDIT.md && rg -n "requirements:\\s*6/6|PROOF-01|TRUTH-01|TUS-01|TUS-02|TUS-03|TUS-04|status:\\s*passed" .planning/milestones/v1.11-MILESTONE-AUDIT.md && rg -n "unresolved_high_threats:\\s*0|T-59-01-H1|T-59-01-H2|T-59-02-H1|T-59-02-H2|closed" .planning/milestones/v1.11-MILESTONE-AUDIT.md && rg -n "Phase 59|v1\\.11|closed|shipped" .planning/ROADMAP.md .planning/STATE.md && rg -n "\"extensions\"|\"completion_surface\"|\"phoenix_state_sequence\"|\"tus_failure_phase\"" tmp/install_smoke_tus_last_run.json'</automated>
  </verify>
  <done>Milestone closure artifacts and active planning truth are synchronized with fresh executable evidence.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Security Gate
- Posture: ASVS L1 for docs/truth surfaces that govern security-sensitive upload behavior.
- High severity policy: do not close phase or milestone when any HIGH risk below is unresolved.

## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Guide claims -> adopter implementation | Incorrect protocol claims can cause insecure or broken client behavior in production. |
| Truth parity tests -> release confidence | Missing literal parity checks allow silent drift between docs and executable behavior. |
| Milestone audit -> project status | Closing milestone without fresh evidence can hide unresolved protocol regressions. |

## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-59-02-H1 | Tampering | HIGH | docs truth surface | mitigate | Freeze extension support strings in parity tests and generated smoke guide parity checks. |
| T-59-02-H2 | Repudiation | HIGH | milestone closure evidence | mitigate | Require fresh command outputs and artifact-key checks before marking Phase 59/v1.11 closed. |
| T-59-02-M1 | Information Disclosure | MEDIUM | planning audit docs | mitigate | Cite safe proof fields from JSON artifacts and avoid embedding signed upload URLs in markdown. |
</threat_model>

<verification>
- `mix test test/install_smoke/phoenix_tus_truth_parity_test.exs`
- `mix test test/install_smoke/generated_app_smoke_test.exs --include minio`
- `bash scripts/install_smoke.sh tus`
- `rg -n 'checksum|creation-defer-length|concatenation|parallelUploads|uploadLengthDeferred' guides/resumable_uploads.md`
- `rg -n '"extensions"|"completion_surface"|"phoenix_state_sequence"|"tus_failure_phase"' tmp/install_smoke_tus_last_run.json`
</verification>

<success_criteria>
- Guide language, parity tests, and generated-app proof assertions all state and enforce the same extension-truth contract.
- `v1.11-MILESTONE-AUDIT.md` exists with explicit requirement scorecard and evidence references.
- `ROADMAP.md` and `STATE.md` report Phase 59/v1.11 closure only after green proof commands and artifact checks.
</success_criteria>

<output>
After completion, create `.planning/phases/59-e2e-proof-truth-closure/59-02-SUMMARY.md`.
</output>

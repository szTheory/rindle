---
phase: 59-e2e-proof-truth-closure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/install_smoke/support/generated_app_helper.ex
  - test/install_smoke/generated_app_smoke_test.exs
autonomous: true
requirements:
  - PROOF-01
must_haves:
  truths:
    - "`test/install_smoke/support/generated_app_helper.ex` contains proof mode tokens `concat_parallel`, `defer_length_stream`, and `checksum_patch` while keeping `tus-js-client@4.3.1` pinned."
    - "Generated tus report payload includes `extensions.concatenation.proved`, `extensions.creation_defer_length.proved`, and `extensions.checksum.proved` plus existing failure keys `tus_failure_phase`, `tus_failure_mode`, and `tus_failure_summary`."
    - "`test/install_smoke/generated_app_smoke_test.exs` includes assertions on `extensions.concatenation`, `extensions.creation_defer_length`, and `extensions.checksum` with mode evidence fields."
  artifacts:
    - path: "test/install_smoke/support/generated_app_helper.ex"
      provides: "Node `tus-js-client@4.3.1` extension-proof modes and report emission"
      contains: "parallelUploads"
    - path: "test/install_smoke/generated_app_smoke_test.exs"
      provides: "extension evidence assertions and failure diagnostics"
      contains: "extensions"
  key_links:
    - from: "test/install_smoke/support/generated_app_helper.ex"
      to: "test/install_smoke/generated_app_smoke_test.exs"
      via: "proof JSON fields are projected through `prove_package_install!/1` and asserted in smoke tests"
      pattern: "extensions|tus_failure_phase|tus_failure_mode"
---

<objective>
Extend the existing generated-app tus install-smoke harness so it proves the
Phase 57/58 extensions in one merge-blocking lane and surfaces those proofs as
machine-checkable evidence.

Purpose: satisfy `PROOF-01` for checksum, creation-defer-length, and
concatenation using the real Phoenix/MinIO package-consumer path.
Output: additive extension proof modes, additive report contract, and explicit
smoke assertions for extension evidence.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-e2e-proof-truth-closure/59-CONTEXT.md
@.planning/phases/59-e2e-proof-truth-closure/59-RESEARCH.md
@.planning/phases/59-e2e-proof-truth-closure/59-PATTERNS.md
@.planning/phases/59-e2e-proof-truth-closure/59-VALIDATION.md
@test/install_smoke/support/generated_app_helper.ex
@test/install_smoke/generated_app_smoke_test.exs
</context>

<must_haves>
- Keep one generated-app `:tus` lane and one Node script surface; do not add a second smoke profile.
- Keep `tus-js-client@4.3.1` pinned in generated-app setup.
- Emit extension proofs under `extensions.concatenation`, `extensions.creation_defer_length`, and `extensions.checksum`.
- Preserve existing fields (`failure_phase`, `failure_mode`, `failure_summary`, `completion_surface`, `phoenix_state_sequence`) while adding extension evidence.
</must_haves>

<tasks>

<task id="59-01-01" type="auto">
  <name>Task 1: Add explicit extension-proof modes to the generated Node tus harness</name>
  <read_first>
    - test/install_smoke/support/generated_app_helper.ex
    - .planning/phases/59-e2e-proof-truth-closure/59-CONTEXT.md
    - .planning/phases/59-e2e-proof-truth-closure/59-RESEARCH.md
    - .planning/phases/59-e2e-proof-truth-closure/59-PATTERNS.md
    - .planning/phases/58-tus-concatenation/58-03-PLAN.md
    - .planning/phases/57-tus-checksum-and-defer-length/57-01-PLAN.md
  </read_first>
  <files>test/install_smoke/support/generated_app_helper.ex</files>
  <action>In `test/install_smoke/support/generated_app_helper.ex`, keep the existing generated-app Node script path and extend it with explicit proof modes that run after the baseline resume proof: `concat_parallel` (`parallelUploads: 2`), `defer_length_stream` (`uploadLengthDeferred: true` with finite `chunkSize`), and `checksum_patch` (checksum-enabled PATCH flow). Keep `npm install --no-save tus-js-client@4.3.1` unchanged. Emit mode-level outcomes into `tmp/install_smoke_tus_report.json` with the exact nested keys `extensions.concatenation`, `extensions.creation_defer_length`, and `extensions.checksum`, each including at least `proved: true|false` and one mode-specific evidence field (`parallel_uploads`, `used_upload_defer_length`, `algorithm/status`). Do not create a new profile or script file.</action>
  <acceptance_criteria>
    - `rg -n 'tus-js-client@4\\.3\\.1|concat_parallel|defer_length_stream|checksum_patch|parallelUploads:\\s*2|uploadLengthDeferred:\\s*true' test/install_smoke/support/generated_app_helper.ex` returns matches.
    - `rg -n 'extensions\\.concatenation|extensions\\.creation_defer_length|extensions\\.checksum|install_smoke_tus_report\\.json' test/install_smoke/support/generated_app_helper.ex` returns matches.
    - `mix test test/install_smoke/generated_app_smoke_test.exs --include minio` exits `0`.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "tus-js-client@4\\.3\\.1|concat_parallel|defer_length_stream|checksum_patch|parallelUploads:\\s*2|uploadLengthDeferred:\\s*true" test/install_smoke/support/generated_app_helper.ex && rg -n "extensions\\.concatenation|extensions\\.creation_defer_length|extensions\\.checksum|install_smoke_tus_report\\.json" test/install_smoke/support/generated_app_helper.ex && mix test test/install_smoke/generated_app_smoke_test.exs --include minio'</automated>
  </verify>
  <done>The generated-app Node harness proves all three extensions with explicit modes and additive machine-readable evidence.</done>
</task>

<task id="59-01-02" type="auto">
  <name>Task 2: Project extension evidence through helper return maps and preserve failure breadcrumbs</name>
  <read_first>
    - test/install_smoke/support/generated_app_helper.ex
    - test/install_smoke/generated_app_smoke_test.exs
    - .planning/phases/59-e2e-proof-truth-closure/59-RESEARCH.md
    - .planning/phases/59-e2e-proof-truth-closure/59-VALIDATION.md
  </read_first>
  <files>test/install_smoke/support/generated_app_helper.ex</files>
  <action>Update `GeneratedAppHelper.prove_package_install!/1` (and any intermediate report mappers) to project the new `extensions` object into the returned `report` map while preserving existing keys and semantics for `tus_failure_phase`, `tus_failure_mode`, and `tus_failure_summary`. Keep report evolution additive: existing smoke callers should continue to read prior fields unchanged. If extension keys are missing in JSON, map them to explicit failure-friendly values (`proved: false` or equivalent) rather than silently omitting them.</action>
  <acceptance_criteria>
    - `rg -n 'tus_report_data|extensions|tus_failure_phase|tus_failure_mode|tus_failure_summary' test/install_smoke/support/generated_app_helper.ex` returns matches.
    - `mix test test/install_smoke/generated_app_smoke_test.exs --include minio` exits `0`.
    - `bash scripts/install_smoke.sh tus` exits `0`.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "tus_report_data|extensions|tus_failure_phase|tus_failure_mode|tus_failure_summary" test/install_smoke/support/generated_app_helper.ex && mix test test/install_smoke/generated_app_smoke_test.exs --include minio && bash scripts/install_smoke.sh tus'</automated>
  </verify>
  <done>Extension evidence is first-class report data and failure breadcrumbs remain intact for diagnostics.</done>
</task>

<task id="59-01-03" type="auto">
  <name>Task 3: Add merge-blocking smoke assertions for extension proof completeness</name>
  <read_first>
    - test/install_smoke/generated_app_smoke_test.exs
    - test/install_smoke/support/generated_app_helper.ex
    - .planning/phases/59-e2e-proof-truth-closure/59-CONTEXT.md
    - .planning/phases/59-e2e-proof-truth-closure/59-VALIDATION.md
  </read_first>
  <files>test/install_smoke/generated_app_smoke_test.exs</files>
  <action>Extend `test/install_smoke/generated_app_smoke_test.exs` assertions so the generated-app tus proof fails unless all three extension proofs are present and marked proved. Assert exact report paths for `extensions.concatenation`, `extensions.creation_defer_length`, and `extensions.checksum`, plus mode-specific fields (`parallel_uploads`, `used_upload_defer_length`, `algorithm` or `status`). Keep diagnostics routed through existing failure helpers (for example `tus_failure_details(report)`) and continue asserting existing transport/runtime facts (`tus_previous_uploads`, `tus_ready_variants`, and completion surface keys).</action>
  <acceptance_criteria>
    - `rg -n 'extensions\\["concatenation"\\]|extensions\\["creation_defer_length"\\]|extensions\\["checksum"\\]|parallel_uploads|used_upload_defer_length|algorithm|status' test/install_smoke/generated_app_smoke_test.exs` returns matches.
    - `mix test test/install_smoke/generated_app_smoke_test.exs --include minio` exits `0`.
    - `rg -n '"extensions"|\"concatenation\"|\"creation_defer_length\"|\"checksum\"' tmp/install_smoke_tus_last_run.json` returns matches after the smoke run.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "extensions\\[\"concatenation\"\\]|extensions\\[\"creation_defer_length\"\\]|extensions\\[\"checksum\"\\]|parallel_uploads|used_upload_defer_length|algorithm|status" test/install_smoke/generated_app_smoke_test.exs && mix test test/install_smoke/generated_app_smoke_test.exs --include minio && test -f tmp/install_smoke_tus_last_run.json && rg -n "\"extensions\"|\"concatenation\"|\"creation_defer_length\"|\"checksum\"" tmp/install_smoke_tus_last_run.json'</automated>
  </verify>
  <done>Smoke tests now block merges when extension proof evidence is absent or incomplete.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Security Gate
- Posture: ASVS L1 for a public upload edge.
- High severity policy: block closure on unresolved HIGH findings.

## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser `tus-js-client` -> Phoenix tus endpoint | Untrusted upload traffic must be proven through extension-specific transport paths, not inferred from headers alone. |
| Generated proof JSON -> CI gate | Missing extension evidence can produce false green outcomes if assertions are weak. |
| Helper report mapping -> smoke assertions | Dropped fields can hide protocol regressions behind passing baseline upload checks. |

## STRIDE Threat Register
| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-59-01-H1 | Tampering | HIGH | generated-app proof lane | mitigate | Require explicit extension-mode execution and explicit `extensions.*.proved` assertions in merge-blocking smoke tests. |
| T-59-01-H2 | Repudiation | HIGH | proof artifact contract | mitigate | Persist and assert machine-readable extension evidence plus failure breadcrumbs in `tmp/install_smoke_tus_last_run.json`. |
| T-59-01-M1 | Denial of Service | MEDIUM | proof lane runtime | mitigate | Keep one existing lane and additive checks only; avoid creating parallel lanes that increase CI flake surface. |
</threat_model>

<verification>
- `mix test test/install_smoke/generated_app_smoke_test.exs --include minio`
- `bash scripts/install_smoke.sh tus`
- `rg -n '"extensions"|"concatenation"|"creation_defer_length"|"checksum"|"tus_failure_phase"|"tus_failure_mode"' tmp/install_smoke_tus_last_run.json`
</verification>

<success_criteria>
- Generated-app tus smoke proves concatenation, creation-defer-length, and checksum behavior in one merge-blocking lane.
- Extension evidence is persisted and asserted as machine-readable truth.
- Baseline tus smoke evidence and failure breadcrumbs remain intact and visible.
</success_criteria>

<output>
After completion, create `.planning/phases/59-e2e-proof-truth-closure/59-01-SUMMARY.md`.
</output>

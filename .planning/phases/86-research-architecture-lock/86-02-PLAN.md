---
phase: 86-research-architecture-lock
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/rindle_admin_css.md
  - guides/admin_console_motion.md
autonomous: true
requirements:
  - PRIN-01
must_haves:
  truths:
    - "D-86-11 through D-86-15 are captured as downstream constraints for console CSS, themes, status indicators, motion, and Cohort separation."
    - "Phase 88 executors can generate `rindle-admin` CSS from brand tokens without host Tailwind, daisyUI, esbuild, or one-off styles."
    - "Phase 88 and Phase 92 executors can apply motion only for operational feedback, materialization, and continuity using brand motion tokens."
  artifacts:
    - path: "guides/rindle_admin_css.md"
      provides: "CSS architecture lock for token-generated BEM admin styles"
      contains: "brandbook/tokens/tokens.json"
    - path: "guides/admin_console_motion.md"
      provides: "Motion rules tied to brand motion tokens and reduced-motion behavior"
      contains: "--rindle-motion-press"
  key_links:
    - from: "guides/rindle_admin_css.md"
      to: "brandbook/tokens/tokens.json"
      via: "token source of truth"
      pattern: "brandbook/tokens/tokens\\.json"
    - from: "guides/rindle_admin_css.md"
      to: "brandbook/src/contrast.mjs"
      via: "contrast gate pattern"
      pattern: "contrast\\.mjs"
    - from: "guides/admin_console_motion.md"
      to: "brandbook/tokens/tokens.css"
      via: "motion custom properties"
      pattern: "--rindle-motion-"
---

<objective>
Lock the console CSS architecture and operational motion rules.

Purpose: Phase 88 needs a precise design-system contract, and Phase 92 needs a restrained motion/screenshot-polish contract, before UI implementation begins.
Output: `guides/rindle_admin_css.md` and `guides/admin_console_motion.md`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/86-research-architecture-lock/86-CONTEXT.md
@.planning/phases/86-research-architecture-lock/86-RESEARCH.md
@.planning/phases/86-research-architecture-lock/86-VALIDATION.md
@.planning/phases/86-research-architecture-lock/86-PATTERNS.md
@.planning/phases/86-research-architecture-lock/86-UI-SPEC.md
@AGENTS.md
@RUNNING.md
@.planning/phases/84-tokens-brand-book/84-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write `rindle-admin` CSS architecture lock</name>
  <files>guides/rindle_admin_css.md</files>
  <read_first>
    - guides/rindle_admin_css.md (create if absent)
    - .planning/phases/86-research-architecture-lock/86-CONTEXT.md
    - .planning/phases/86-research-architecture-lock/86-RESEARCH.md
    - .planning/phases/86-research-architecture-lock/86-PATTERNS.md
    - .planning/phases/86-research-architecture-lock/86-UI-SPEC.md
    - .planning/phases/84-tokens-brand-book/84-SUMMARY.md
    - brandbook/README.md
    - brandbook/tokens/tokens.json
    - brandbook/tokens/tokens.css
    - brandbook/src/tokens-build.mjs
    - brandbook/src/contrast.mjs
    - examples/adoption_demo/AGENTS.md
  </read_first>
  <action>Create `guides/rindle_admin_css.md` as the Phase 86 CSS architecture lock for D-86-11, D-86-12, D-86-13, D-86-15, and D-86-19. The doc must define the shipped console CSS as a vanilla `rindle-admin` layer generated from `brandbook/tokens/tokens.json`, with generated custom properties prefixed `--rindle-`, BEM selectors, and no host Tailwind, daisyUI, esbuild, or host asset pipeline. Name concrete selector examples: `.rindle-admin-shell`, `.rindle-admin-nav__item`, `.rindle-admin-table__row`, `.rindle-admin-status-chip`, `.rindle-admin-status-chip--ready`, `.rindle-admin-status-chip--processing`, and `.rindle-admin-status-chip--danger`. Lock theme behavior as `data-theme="light|dark|auto"` plus `prefers-color-scheme`. Lock status chips to text label plus icon plus token-gated color pair, never color alone. Include UI-SPEC values that are downstream constraints: 44px minimum interactive targets, `Space Grotesk` compact headings, `Atkinson Hyperlegible` UI/body, `JetBrains Mono` for code/IDs, dominant surface `#F7F4EA`, secondary surface `#FBFEFC`, brand accent `#123A35`, destructive `#C83232`, and processing `#6D5DD3`. State that Cohort keeps Tailwind/daisyUI and that the library console must not inherit Cohort's frontend stack.</action>
  <verify>
    <automated>test -f guides/rindle_admin_css.md</automated>
    <automated>rg -F -q -- 'rindle-admin' guides/rindle_admin_css.md && rg -F -q -- 'BEM' guides/rindle_admin_css.md && rg -F -q -- 'brandbook/tokens/tokens.json' guides/rindle_admin_css.md && rg -F -q -- '--rindle-' guides/rindle_admin_css.md && rg -q -- 'data-theme="light\|dark\|auto"' guides/rindle_admin_css.md && rg -F -q -- 'prefers-color-scheme' guides/rindle_admin_css.md && rg -F -q -- 'Tailwind' guides/rindle_admin_css.md && rg -F -q -- 'daisyUI' guides/rindle_admin_css.md && rg -F -q -- 'status chip' guides/rindle_admin_css.md && rg -F -q -- 'color alone' guides/rindle_admin_css.md && rg -F -q -- '44px' guides/rindle_admin_css.md && rg -F -q -- 'Space Grotesk' guides/rindle_admin_css.md && rg -F -q -- 'Atkinson Hyperlegible' guides/rindle_admin_css.md && rg -F -q -- '#F7F4EA' guides/rindle_admin_css.md && rg -F -q -- '#123A35' guides/rindle_admin_css.md && rg -F -q -- '#C83232' guides/rindle_admin_css.md && rg -F -q -- '#6D5DD3' guides/rindle_admin_css.md</automated>
    <automated>node brandbook/src/contrast.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `guides/rindle_admin_css.md` names `brandbook/tokens/tokens.json` as the source of truth and `--rindle-` custom properties as generated output.
    - Source assertion: the doc contains the selector examples `.rindle-admin-shell`, `.rindle-admin-nav__item`, `.rindle-admin-status-chip--ready`, `.rindle-admin-status-chip--processing`, and `.rindle-admin-status-chip--danger`.
    - Source assertion: the doc contains `data-theme="light|dark|auto"` and `prefers-color-scheme`.
    - Source assertion: the doc says status chips must include text label plus icon plus token-gated color pair, never color alone.
    - Source assertion: the doc says Cohort keeps Tailwind/daisyUI and the shipped library console does not depend on Tailwind, daisyUI, esbuild, or host asset-pipeline integration.
    - Command assertion: `node brandbook/src/contrast.mjs` exits 0.
  </acceptance_criteria>
  <done>`guides/rindle_admin_css.md` locks the CSS, theme, token, status, and Cohort-separation rules required by D-86-11 through D-86-13, D-86-15, and UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Write operational motion lock</name>
  <files>guides/admin_console_motion.md</files>
  <read_first>
    - guides/admin_console_motion.md (create if absent)
    - .planning/phases/86-research-architecture-lock/86-CONTEXT.md
    - .planning/phases/86-research-architecture-lock/86-RESEARCH.md
    - .planning/phases/86-research-architecture-lock/86-PATTERNS.md
    - .planning/phases/86-research-architecture-lock/86-UI-SPEC.md
    - brandbook/tokens/tokens.json
    - brandbook/tokens/tokens.css
    - brandbook/src/tokens-build.mjs
    - lib/rindle/live_view.ex
  </read_first>
  <action>Create `guides/admin_console_motion.md` as the Phase 86 motion lock for D-86-14 and D-86-19. The doc must bind motion to these exact token names and durations: `--rindle-motion-press` at `120ms`, `--rindle-motion-popover` at `160ms`, `--rindle-motion-toast` at `200ms`, `--rindle-motion-transition` at `300ms`, and `--rindle-motion-easing` as the only easing token. Define allowed motion uses as press feedback, origin-aware popovers and drawers, toast materialization, state continuity for real PubSub/LiveView updates, and page/surface transitions that preserve orientation. Define forbidden motion uses as decorative animation, parallax, bouncing, infinite loops, marketing-style hero motion, loading animation not backed by real pending work, and large-scale transforms that pull attention from operational status. Require `prefers-reduced-motion` handling, disabled motion where reduction is requested, and immediate non-animated state changes for destructive or failure states.</action>
  <verify>
    <automated>test -f guides/admin_console_motion.md</automated>
    <automated>rg -F -q -- '--rindle-motion-press' guides/admin_console_motion.md && rg -F -q -- '120ms' guides/admin_console_motion.md && rg -F -q -- '--rindle-motion-popover' guides/admin_console_motion.md && rg -F -q -- '160ms' guides/admin_console_motion.md && rg -F -q -- '--rindle-motion-toast' guides/admin_console_motion.md && rg -F -q -- '200ms' guides/admin_console_motion.md && rg -F -q -- '--rindle-motion-transition' guides/admin_console_motion.md && rg -F -q -- '300ms' guides/admin_console_motion.md && rg -F -q -- '--rindle-motion-easing' guides/admin_console_motion.md && rg -F -q -- 'prefers-reduced-motion' guides/admin_console_motion.md && rg -F -q -- 'origin-aware' guides/admin_console_motion.md && rg -F -q -- 'decorative animation' guides/admin_console_motion.md && rg -F -q -- 'parallax' guides/admin_console_motion.md && rg -F -q -- 'infinite loops' guides/admin_console_motion.md && rg -F -q -- 'real PubSub' guides/admin_console_motion.md && rg -F -q -- 'destructive' guides/admin_console_motion.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `guides/admin_console_motion.md` contains all five token names: `--rindle-motion-press`, `--rindle-motion-popover`, `--rindle-motion-toast`, `--rindle-motion-transition`, and `--rindle-motion-easing`.
    - Source assertion: the doc contains durations `120ms`, `160ms`, `200ms`, and `300ms`.
    - Source assertion: the doc names allowed uses for press feedback, origin-aware popovers/drawers, toast materialization, real PubSub/LiveView state continuity, and page/surface transitions.
    - Source assertion: the doc forbids decorative animation, parallax, bouncing, infinite loops, and loading animation not backed by real pending work.
    - Source assertion: the doc requires `prefers-reduced-motion` handling and immediate non-animated state changes for destructive or failure states.
  </acceptance_criteria>
  <done>`guides/admin_console_motion.md` locks restrained operational motion rules tied to brand motion tokens and reduced-motion behavior.</done>
</task>

</tasks>

<decision_traceability>
- D-86-11: `guides/rindle_admin_css.md` locks vanilla `rindle-admin` CSS generated from `brandbook/tokens/tokens.json`, using BEM selectors and CSS custom properties.
- D-86-12: `guides/rindle_admin_css.md` locks `data-theme="light|dark|auto"` plus `prefers-color-scheme`.
- D-86-13: `guides/rindle_admin_css.md` locks token-gated status chips with labels/icons so status never relies on color alone.
- D-86-14: `guides/admin_console_motion.md` locks restrained operational motion using token durations and `prefers-reduced-motion`.
- D-86-15: `guides/rindle_admin_css.md` locks Cohort Tailwind/daisyUI separation from the shipped library console.
- D-86-19: Both docs are architecture locks only and do not implement CSS generation or console components.
</decision_traceability>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Brand tokens -> generated console CSS | Token source-of-truth decisions cross into downstream generated CSS and components. |
| Visual status -> operator decisions | Color, icon, and label choices influence operational interpretation of asset/job states. |
| Motion rules -> browser UI | Animation choices can obscure failures, destructive actions, or real pending state. |
| Phase 86 docs -> Phase 88/92 implementation | Stale or vague design docs can create CSS/token drift and inaccessible UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-05 | Tampering | `guides/rindle_admin_css.md` token contract | mitigate | Require `brandbook/tokens/tokens.json`, `--rindle-`, BEM, and contrast-gate wording; run `node brandbook/src/contrast.mjs`. |
| T-86-06 | Information Disclosure / Repudiation | Status indicators | mitigate | Require status chips to include text label plus icon plus token-gated color pair; forbid color-alone state communication. |
| T-86-07 | Denial of Service / Tampering | Motion guidance | mitigate | Forbid decorative, infinite, and non-state-backed loading animation; require `prefers-reduced-motion` and immediate failure/destructive state updates. |
| T-86-08 | Elevation of Privilege | Third-party UI registry drift | mitigate | State no shadcn, Radix, Tailwind UI, daisyUI, or third-party UI registry for the shipped console; keep Cohort stack separate. |
| T-86-SC | Tampering | Package installs | accept | Phase 86 installs no npm, pip, cargo, or Hex packages; package legitimacy audit is not applicable. |
</threat_model>

<verification>
Run the task-level automated checks. For this plan, the phase-relevant gate is `node brandbook/src/contrast.mjs` plus `rg` assertions proving CSS/motion token constraints are present.
</verification>

<success_criteria>
- `guides/rindle_admin_css.md` exists and locks BEM, generated custom properties, theme behavior, status chip rules, UI-SPEC color/typography/spacing constraints, and Cohort stack separation.
- `guides/admin_console_motion.md` exists and locks token durations, allowed/forbidden motion uses, reduced-motion behavior, and real-state feedback rules.
- No production CSS, JS, LiveView, Docker, or example app implementation files are modified by this plan.
- Requirement `PRIN-01` remains covered in plan frontmatter and source audit.
</success_criteria>

<output>
Create `.planning/phases/86-research-architecture-lock/86-02-SUMMARY.md` when done.
</output>

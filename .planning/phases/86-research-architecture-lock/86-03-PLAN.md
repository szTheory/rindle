---
phase: 86-research-architecture-lock
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/docker_demo_dx.md
  - guides/ui_principles.md
  - AGENTS.md
autonomous: true
requirements:
  - PRIN-01
must_haves:
  truths:
    - "D-86-16 through D-86-20 are captured as downstream Docker DX, Cohort adoption lab, and UI-principles constraints."
    - "Phase 87 executors can implement Docker project namespacing, env-driven ports, cache-friendly Dockerfile ordering, and URL-map output without reopening Phase 86 decisions."
    - "Future UI executors are forced through `guides/ui_principles.md` because `AGENTS.md` links the durable PRIN-01 guidance."
  artifacts:
    - path: "guides/docker_demo_dx.md"
      provides: "Docker/Cohort demo DX architecture lock"
      contains: "COMPOSE_PROJECT_NAME"
    - path: "guides/ui_principles.md"
      provides: "Durable UI-principles guidance for PRIN-01"
      contains: "deterministic E2E"
    - path: "AGENTS.md"
      provides: "Agent-facing link to UI principles"
      contains: "guides/ui_principles.md"
  key_links:
    - from: "AGENTS.md"
      to: "guides/ui_principles.md"
      via: "repository workflow link"
      pattern: "guides/ui_principles\\.md"
    - from: "guides/docker_demo_dx.md"
      to: "docker/compose.cohort-demo.yml"
      via: "env-driven port contract"
      pattern: "COHORT_DEMO_PORT"
    - from: "guides/ui_principles.md"
      to: "brandbook/tokens/tokens.json"
      via: "design-token source-of-truth rule"
      pattern: "brandbook/tokens/tokens\\.json"
---

<objective>
Lock Docker demo DX and durable UI-principles governance, then link that guidance from `AGENTS.md`.

Purpose: Phase 87 needs a precise Docker/Cohort demo contract before UI-heavy work, and all later UI phases need PRIN-01 guidance discoverable by automated agents.
Output: `guides/docker_demo_dx.md`, `guides/ui_principles.md`, and a scoped `AGENTS.md` link.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/86-research-architecture-lock/86-CONTEXT.md
@.planning/phases/86-research-architecture-lock/86-RESEARCH.md
@.planning/phases/86-research-architecture-lock/86-VALIDATION.md
@.planning/phases/86-research-architecture-lock/86-PATTERNS.md
@.planning/phases/86-research-architecture-lock/86-UI-SPEC.md
@AGENTS.md
@RUNNING.md
@.planning/phases/84-tokens-brand-book/84-SUMMARY.md
@.planning/phases/85-repo-surface-integration/85-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Docker and Cohort demo DX lock</name>
  <files>guides/docker_demo_dx.md</files>
  <read_first>
    - guides/docker_demo_dx.md (create if absent)
    - .planning/phases/86-research-architecture-lock/86-CONTEXT.md
    - .planning/phases/86-research-architecture-lock/86-RESEARCH.md
    - .planning/phases/86-research-architecture-lock/86-PATTERNS.md
    - .planning/phases/86-research-architecture-lock/86-UI-SPEC.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - guides/release_publish.md
    - docker/compose.cohort-demo.yml
    - docker/Dockerfile.cohort-demo
    - scripts/demo/up.sh
    - docker/cohort-demo-entrypoint.sh
    - examples/adoption_demo/AGENTS.md
    - examples/adoption_demo/lib/adoption_demo_web/router.ex
  </read_first>
  <action>Create `guides/docker_demo_dx.md` as the Phase 86 Docker DX lock for D-86-16, D-86-17, D-86-18, and D-86-19. Use the `guides/release_publish.md` runbook style: `TL;DR`, contract sections, recovery/footguns, and implementation notes. The doc must lock `COMPOSE_PROJECT_NAME` as the project namespacing mechanism; env-driven host ports `COHORT_DEMO_PORT` default `4102`, `COHORT_MINIO_PORT` default `9000`, and `COHORT_MINIO_CONSOLE_PORT` default `9001`; internal Phoenix `PORT=4102`; `PHX_HOST=localhost`; launch URL map labels `app`, `admin console`, and `MinIO console` with URLs `http://localhost:${COHORT_DEMO_PORT:-4102}`, `http://localhost:${COHORT_DEMO_PORT:-4102}/admin/rindle`, and `http://localhost:${COHORT_MINIO_CONSOLE_PORT:-9001}`; cache-friendly Dockerfile ordering that copies `mix.exs`, `mix.lock`, `examples/adoption_demo/mix.exs`, and `examples/adoption_demo/mix.lock` before full source copy and runs `mix deps.get` before app source copy; simple `scripts/demo/up.sh` wrapper preservation; fixed-port baseline warning for `"4102:4102"`, `"9000:9000"`, and `"9001:9001"`; local-only MinIO console exposure guidance; and a Traefik decision that rejects Traefik for Phase 87 unless a later recorded requirement needs multi-host routing.</action>
  <verify>
    <automated>test -f guides/docker_demo_dx.md</automated>
    <automated>rg -n "(COMPOSE_PROJECT_NAME|COHORT_DEMO_PORT|4102|COHORT_MINIO_PORT|9000|COHORT_MINIO_CONSOLE_PORT|9001|PHX_HOST=localhost|/admin/rindle|mix deps.get|scripts/demo/up.sh|4102:4102|9000:9000|9001:9001|MinIO console|Traefik|multi-host routing)" guides/docker_demo_dx.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `guides/docker_demo_dx.md` contains `COMPOSE_PROJECT_NAME`, `COHORT_DEMO_PORT`, `COHORT_MINIO_PORT`, and `COHORT_MINIO_CONSOLE_PORT` with defaults `4102`, `9000`, and `9001`.
    - Source assertion: the doc contains all three URL-map labels: `app`, `admin console`, and `MinIO console`, including `/admin/rindle` for the admin console URL.
    - Source assertion: the doc names fixed-port strings `"4102:4102"`, `"9000:9000"`, and `"9001:9001"` as the baseline Phase 87 must replace.
    - Source assertion: the doc specifies copying `mix.exs`, `mix.lock`, `examples/adoption_demo/mix.exs`, and `examples/adoption_demo/mix.lock` before the full source copy, with `mix deps.get` before app source copy.
    - Source assertion: the doc rejects Traefik for Phase 87 unless a later recorded requirement needs multi-host routing.
  </acceptance_criteria>
  <done>`guides/docker_demo_dx.md` locks Docker namespacing, env ports, layer-cache, URL-map, local exposure, and Traefik decisions for Phase 87.</done>
</task>

<task type="auto">
  <name>Task 2: Write durable UI-principles guide</name>
  <files>guides/ui_principles.md</files>
  <read_first>
    - guides/ui_principles.md (create if absent)
    - .planning/phases/86-research-architecture-lock/86-CONTEXT.md
    - .planning/phases/86-research-architecture-lock/86-RESEARCH.md
    - .planning/phases/86-research-architecture-lock/86-VALIDATION.md
    - .planning/phases/86-research-architecture-lock/86-PATTERNS.md
    - .planning/phases/86-research-architecture-lock/86-UI-SPEC.md
    - .planning/REQUIREMENTS.md
    - brandbook/README.md
    - brandbook/tokens/tokens.json
    - brandbook/src/contrast.mjs
    - examples/adoption_demo/AGENTS.md
    - guides/operations.md
    - RUNNING.md
  </read_first>
  <action>Create `guides/ui_principles.md` as the durable PRIN-01 guide for D-86-20 and the shared UI constraints from D-86-11 through D-86-15 and D-86-19. The doc must include these sections with those exact headings: `Design-system values`, `Visual and accessibility audit checklist`, `Deterministic E2E rules`, `Screenshot polish loop`, `Motion constraints`, `Security and destructive-action rules`, and `When to escalate`. It must name `brandbook/tokens/tokens.json`, `brandbook/src/contrast.mjs`, `rindle-admin`, BEM, `data-theme="light|dark|auto"`, `prefers-color-scheme`, `prefers-reduced-motion`, `44px` interactive targets, `Space Grotesk`, `Atkinson Hyperlegible`, `JetBrains Mono`, status labels/icons plus token color pairs, stable selectors/element IDs for LiveView tests, seeded lifecycle state, light/dark screenshot capture, no text-only assertions where stable selectors are available, and no shadcn/Radix/Tailwind UI/daisyUI registry dependency for the shipped console. In `When to escalate`, list exactly these escalation triggers: public API shape, auth semantics, dependency footprint, destructive operations, security/compliance boundary, material recurring cost, and milestone scope.</action>
  <verify>
    <automated>test -f guides/ui_principles.md</automated>
    <automated>rg -n "(Design-system values|Visual and accessibility audit checklist|Deterministic E2E rules|Screenshot polish loop|Motion constraints|Security and destructive-action rules|When to escalate|brandbook/tokens/tokens\\.json|brandbook/src/contrast\\.mjs|rindle-admin|BEM|data-theme=\"light\\|dark\\|auto\"|prefers-color-scheme|prefers-reduced-motion|44px|Space Grotesk|Atkinson Hyperlegible|JetBrains Mono|stable selectors|seeded lifecycle state|light/dark screenshot|text-only assertions|public API shape|auth semantics|dependency footprint|destructive operations|security/compliance boundary|material recurring cost|milestone scope)" guides/ui_principles.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `guides/ui_principles.md` contains all seven required headings exactly as named in the action.
    - Source assertion: the doc names `brandbook/tokens/tokens.json`, `brandbook/src/contrast.mjs`, `rindle-admin`, BEM, `data-theme="light|dark|auto"`, `prefers-color-scheme`, and `prefers-reduced-motion`.
    - Source assertion: the doc includes 44px interactive targets and the three font names `Space Grotesk`, `Atkinson Hyperlegible`, and `JetBrains Mono`.
    - Source assertion: deterministic E2E guidance includes stable selectors or element IDs, seeded lifecycle state, light/dark screenshots, and no text-only assertions where stable selectors are available.
    - Source assertion: escalation triggers include public API shape, auth semantics, dependency footprint, destructive operations, security/compliance boundary, material recurring cost, and milestone scope.
  </acceptance_criteria>
  <done>`guides/ui_principles.md` satisfies PRIN-01 with durable design-system, audit, deterministic E2E, screenshot, motion, security, and escalation guidance.</done>
</task>

<task type="auto">
  <name>Task 3: Link UI principles from AGENTS</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md
    - guides/ui_principles.md
    - .planning/phases/86-research-architecture-lock/86-CONTEXT.md
    - .planning/phases/86-research-architecture-lock/86-RESEARCH.md
    - .planning/phases/86-research-architecture-lock/86-PATTERNS.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>Update only the `## Repository workflow` section of `AGENTS.md`. Add one scoped sentence near the existing contributor/automated-agent guidance: `For UI/admin-console work, follow [guides/ui_principles.md](guides/ui_principles.md) before changing console, Cohort, E2E, or visual-polish surfaces.` Do not modify the Cursor model-routing section, release-train bullets, or manual backup instructions.</action>
  <verify>
    <automated>test -f guides/ui_principles.md</automated>
    <automated>rg -n "For UI/admin-console work, follow \\[guides/ui_principles\\.md\\]\\(guides/ui_principles\\.md\\)" AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `AGENTS.md` contains exactly one `guides/ui_principles.md` link.
    - Source assertion: the added sentence is inside `## Repository workflow`, not inside the Cursor model-routing section.
    - Source assertion: no Cursor model picker table or model override text changes in this task.
    - Command assertion: `rg -n "For UI/admin-console work, follow \\[guides/ui_principles\\.md\\]\\(guides/ui_principles\\.md\\)" AGENTS.md` returns a match.
  </acceptance_criteria>
  <done>`AGENTS.md` links PRIN-01 UI principles so future automated agents read the durable UI rules before console, Cohort, E2E, or visual-polish work.</done>
</task>

</tasks>

<decision_traceability>
- D-86-16: `guides/docker_demo_dx.md` locks Phase 87 Docker namespacing, env-driven ports, conflict guidance, layer caching, and URL-map output as prerequisites for UI-heavy work.
- D-86-17: `guides/docker_demo_dx.md` names the current Dockerfile whole-repo-before-`mix deps.get` cache problem and specifies the dependency-file-first ordering Phase 87 must use.
- D-86-18: `guides/docker_demo_dx.md` and `guides/ui_principles.md` keep Cohort as the demo/adoption-lab domain to extend, not replace.
- D-86-19: The docs and AGENTS link are architecture locks only and do not implement Docker, demo, E2E, or console behavior.
- D-86-20: `guides/ui_principles.md` plus the `AGENTS.md` link satisfy PRIN-01 with design-system values, visual/a11y audit checklist, deterministic E2E rules, screenshot polish rules, and motion constraints.
</decision_traceability>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local host -> Docker published ports | Demo services expose Phoenix and MinIO ports on the developer machine. |
| Docker docs -> Phase 87 implementation | Misleading DX docs can preserve port conflicts, cache misses, or unsafe exposure. |
| `AGENTS.md` -> future agents | Agent-facing instructions determine whether UI work reads PRIN-01 guidance. |
| UI principles -> downstream UI/E2E phases | Stale or vague principles can cause inaccessible UI, flaky E2E, or destructive-action UX drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-09 | Denial of Service | Docker port namespacing | mitigate | Require `COMPOSE_PROJECT_NAME` and env-driven ports for app and MinIO; name current fixed-port strings as replacement targets. |
| T-86-10 | Information Disclosure | MinIO console exposure | mitigate | Document local-only MinIO console assumptions and avoid presenting the Docker preview as production deployment guidance. |
| T-86-11 | Tampering / Repudiation | `guides/ui_principles.md` | mitigate | Require exact headings and source assertions for design, a11y, deterministic E2E, screenshots, motion, security, and escalation triggers. |
| T-86-12 | Repudiation | `AGENTS.md` link durability | mitigate | Add exact `guides/ui_principles.md` link in Repository workflow and verify it with `rg`. |
| T-86-SC | Tampering | Package installs | accept | Phase 86 installs no npm, pip, cargo, or Hex packages; package legitimacy audit is not applicable. |
</threat_model>

<verification>
Run the task-level automated checks. For this plan, the phase-relevant gate is `rg` proof for Docker DX terms, UI-principles required headings/constraints, and the exact `AGENTS.md` link.
</verification>

<success_criteria>
- `guides/docker_demo_dx.md` exists and locks Compose project namespacing, env-driven ports, layer-cache ordering, URL-map labels, MinIO exposure, and Traefik tradeoffs.
- `guides/ui_principles.md` exists and covers design-system values, visual/a11y audit checklist, deterministic E2E rules, screenshot polish, motion constraints, security/destructive-action rules, and escalation triggers.
- `AGENTS.md` links `guides/ui_principles.md` from the Repository workflow section without changing model-routing guidance.
- Requirement `PRIN-01` remains covered in plan frontmatter and source audit.
</success_criteria>

<output>
Create `.planning/phases/86-research-architecture-lock/86-03-SUMMARY.md` when done.
</output>

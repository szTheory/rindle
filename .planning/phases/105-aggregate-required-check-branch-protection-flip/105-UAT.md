---
status: testing
phase: 105-aggregate-required-check-branch-protection-flip
source: [105-VERIFICATION.md]
started: 2026-06-21T19:56:25Z
updated: 2026-06-21T19:56:25Z
---

## Current Test

number: 1
name: Post-merge live branch-protection flip + read-back (Task 4, D-11/D-13)
expected: |
  After this PR merges AND the `ci-summary` job has run on `main` at least once, a
  maintainer with an admin gh session runs the `105-FLIP-RUNBOOK.md` steps in order:
  pre-flip dump (`bash scripts/ci/check_required_checks.sh main`), confirm the
  `CI Summary` check-run name exists on the latest main commit
  (`gh api repos/szTheory/rindle/commits/"$(git rev-parse origin/main)"/check-runs --jq '.check_runs[].name' | grep -Fx 'CI Summary'`),
  apply the flip (`GH_TOKEN=<admin-PAT> bash scripts/setup_branch_protection.sh main`),
  then verify (`bash scripts/ci/check_required_checks.sh main`) shows live
  `.contexts[]` == `["CI Summary"]` with an EMPTY diff.
awaiting: user response

## Tests

### 1. Post-merge live branch-protection flip + read-back (Task 4, D-11/D-13)
expected: |
  After merge + a real `ci-summary` run on main, the maintainer runs the runbook flip
  and `check_required_checks.sh main` shows live `.contexts[]` == `["CI Summary"]` with
  an empty diff vs `--print-expected-json`. Mutates live external state (branch
  protection on main) under maintainer admin authority; cannot run in PR CI (D-10/D-11),
  and requires a real ci-summary run on main first (D-12 pending-forever trap).
  Deferred by design — NOT a gap.
result: [pending]

### 2. Fork-PR skip-as-pass behavior (optional, D-05)
expected: |
  A PR opened from a fork — where the repo-gated jobs (cohort-demo-smoke,
  adoption-demo-e2e, brandbook-tokens) skip — reports `CI Summary` as success rather
  than hanging pending-forever. Logic-provable from the skip-as-pass case in the result
  loop; observable only via a real fork PR against the merged workflow.
result: [pending]

## Summary

total: 2
passed: 0
issues: 0
pending: 2
skipped: 0
blocked: 0

## Gaps

---
phase: 132-measured-closure
plan: 16
type: execute
wave: 16
depends_on: [132-15]
files_modified:
  - .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
autonomous: true
gap_closure: true
requirements: [CI-14, COV-05, SAFE-02]
must_haves:
  truths:
    - "One fresh current receipt equals the complete repository/workflow/numeric-PR-bound canonical population of exactly ten successful, non-cancelled, first-attempt pull-request runs from the immutable 132-15 subject per D-04; no eligible run on the bound PR is excluded or selected around, and a same-SHA run from another PR is excluded."
    - "The qualifying publication-triggered run is receipt sample 1, exactly nine label-triggered samples follow, and collection creates no eleventh successful exact-head run."
    - "Run, explicit verify, and completed-state revalidation cross-check repository `szTheory/rindle` and numeric PR `96` across CLI, state, current source manifest, PR API, and every run association before re-resolving exactly one successful `CI Summary`."
    - "Median uses sorted ranks 5/6, nearest-rank p95 uses rank 10, and both inclusive limits (480/600 seconds) pass before CI-14 closes per D-05."
    - "D-01/D-08/D-09 topology and the 132-15 COV-05/SAFE-02 preservation subject remain unchanged after sampling."
    - statement: "SAFE-02 empty/ordering/concurrency backstop: non-ten or partial evidence fails, ties sort numerically, samples do not overlap, and one SHA-scoped lock owns the sequential collector across interruption/resume."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT count a failed run, cancellation, rerun, mixed SHA/event, overlapping sample, duplicate summary, or selected-around slow success."
      category: safety
      status: unverified
      flagged_unverified: true
    - statement: "MUST NOT replace API-backed evidence with local timing, screenshots, human/UAT approval, or an unsupported external-runner exception."
      category: transparency
      status: unverified
      flagged_unverified: true
    - statement: "MUST NOT weaken thresholds, topology, required membership, proof commands, or policies after a miss."
      category: values
      status: unverified
      flagged_unverified: true
  artifacts:
    - path: ".planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md"
      provides: "Fresh exact-ten current table/source manifest with repository, numeric PR, immutable SHA, API identities, chronology, statistics, and PASS verdict"
      contains: "CI_TIMING_CURRENT_SOURCE_BEGIN"
  key_links:
    - from: ".planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md"
      to: "scripts/ci/collect_pr_timing_receipt.sh"
      via: "full correction/preserved-subject SHA arguments plus the source-derived transition manifest"
      pattern: "PRESERVATION_TRANSITION_V2_BEGIN"
    - from: "scripts/ci/collect_pr_timing_receipt.sh"
      to: ".planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md"
      via: "single locked run plus corrected API-backed completed-state verification"
      pattern: "CI_TIMING_CURRENT_(TABLE|SOURCE)_BEGIN"
    - from: "publication-triggered run and nine label-triggered runs"
      to: "explicit and completed-state API verifier"
      via: "CLI/state/source/API repository-plus-PR identity agrees, selected IDs equal `canonical_eligible_run_ids`, and controller-owned trigger provenance proves cardinality"
      pattern: "canonical_eligible_run_ids|trigger_provenance"
---

<objective>
Collect the terminal fresh exact-ten Actions receipt and close CI-14 only on a live-verified threshold pass.

Purpose: Replace both historical failures with the sole authorized comparable acceptance artifact after all implementation and preservation gates are green.
Output: One current API-backed receipt and machine-decided Phase 132 verdict.
</objective>

## Artifacts this phase produces

- Updated `132-CI-TIMING-RECEIPT.md`: exactly one `CI_TIMING_CURRENT_TABLE_*` pair and one `CI_TIMING_CURRENT_SOURCE_*` pair with normalized repository, numeric PR, full SHA, the complete ten-run canonical population (publication sample first), source URLs/IDs, starts/completions/durations, sorted values, ranks, median, p95, and PASS verdict. Historical failed sections remain unchanged; state provenance separately proves the same repo/PR identity plus one publication and nine label triggers.
- No new controller symbol, CLI flag, state field, workflow job/edge, test, dependency, product API, or exception mechanism.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RUNNING.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/132-measured-closure/132-CONTEXT.md
@.planning/phases/132-measured-closure/132-VERIFICATION.md
@.planning/phases/132-measured-closure/132-15-SUMMARY.md
@.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
@.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
@scripts/ci/collect_pr_timing_receipt.sh
@test/install_smoke/ci_timing_automation_test.exs
@test/install_smoke/ci_lane_split_test.exs
@.github/workflows/ci.yml
</context>

<assumptions>
- CI-14 generic probe: this plan resolves it only through one current API-backed repository/numeric-PR-bound exact-ten canonical-population receipt, including the successful publication trigger as row 1, plus explicit `verify --pr 96` and corrected completed-state revalidation, with inclusive median <=480 and nearest-rank p95 <=600; any failure remains `gaps_found`.
- COV-05 generic probe: Plan 132-15 is the authoritative fresh coverage census; this plan defers to that immutable-subject evidence and proves sampling introduces no source drift before retaining the >=82.13% verdict.
- SAFE-02 adjacency probe: explicit and completed-state verifier paths are the automated authorities that accept equality at 480/600 and reject values above either limit.
- SAFE-02 empty-input probe: the controller and explicit verifier reject missing current markers, partial evidence, and any count other than exactly ten qualifying rows.
- SAFE-02 ordering probe: table/source equality, numeric duration sorting, stable ties, median ranks 5/6, nearest-rank p95 rank 10, and chronological non-overlap are re-resolved from the Actions API.
- SAFE-02 concurrency probe: one SHA-scoped lock owns the only foreground controller; the offline suite plus final label/lock/run-count checks prove interruption/resume does not overlap samples or trigger an eleventh run during completed-state revalidation.
- Descriptor-less prohibitions remain flagged-unverified; no check descriptor or silent green status is invented for them.
</assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: Run and independently verify the terminal exact-ten receipt</name>
  <precondition>Plan 132-15 is machine-green and records a successful mutation-free `preflight --no-publish` against exactly one schema-v2 three-stage transition manifest bound to repository `szTheory/rindle` and numeric PR `96`; `gh auth status` succeeds; that PR is open and its current head is a strict ancestor of the newly recorded local preserved subject so fast-forward publication will create one identifiable PR-associated run; `ci-timing-sample` exists and is absent from the PR; no current receipt markers, Phase 132 live controller, owned label, or SHA-scoped state lock exists.</precondition>
  <reversibility rating="reversible">The label is cleaned and the receipt append is revertible; workflow runs remain durable public evidence under the user's explicit bounded authorization.</reversibility>
  <files>.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md</files>
  <read_first>
    - AGENTS.md
    - RUNNING.md
    - .planning/phases/132-measured-closure/132-CONTEXT.md
    - .planning/phases/132-measured-closure/132-VERIFICATION.md
    - .planning/phases/132-measured-closure/132-15-SUMMARY.md
    - .planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
    - .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
    - .planning/phases/132-measured-closure/COVERAGE.md
    - scripts/ci/collect_pr_timing_receipt.sh
    - test/install_smoke/ci_timing_automation_test.exs
    - test/install_smoke/ci_lane_split_test.exs
    - test/install_smoke/ci_observability_test.exs
    - scripts/ci/test_ci_summary_gate.sh
    - .github/workflows/ci.yml
  </read_first>
  <action>
Re-run the bounded offline controller/topology/observability and summary-gate checks, formatter check, and automation-first contract. Extract the Plan 132-12 controller-correction SHA, the completed Plan 132-13 formatter-correction SHA, the separate Plan 132-14 repair SHA, the post-repair preserved-subject SHA, and the single marker-bounded schema-v2 transition manifest from 132-15 machine evidence. Reparse the manifest and require repository `szTheory/rindle`, numeric PR `96`, all anchors, three contiguous stage path sets, planning-path classification, and blob OIDs to match current Git and PR API facts. Assert the receipt has zero current markers and no controller/label/lock ownership. Assert the remote PR head is a strict ancestor of local HEAD, then rerun the repaired `preflight --no-publish` with the same `--repo`, `--pr`, and `--transition-manifest` identity and require success with before/after equality before mutation. Launch exactly one foreground controller with the identical manifest/interface for `szTheory/rindle` PR 96, `ci.yml`, `CI Summary`, `ci-timing-sample`, ten samples, maximum two complete sequences, inclusive median 480 and p95 600, dedicated new `.gsd/ci-timing/phase-132-final` state, and fast-forward-only publication. Require state and current source manifest to persist that exact repository and numeric PR. Require a qualifying publication-triggered run associated with PR 96 to become sample 1 and exactly nine label-triggered runs to complete the canonical population; selected IDs must equal every qualifying bound-PR exact-head API ID, not a contiguous subset, and must exclude every same-SHA run associated only with another PR. Retain sequential/non-overlap, persisted start/publication boundary, complete Summary chronology, pending-trigger, delayed discovery, rate-limit, state-lock, resume, cleanup, no-rerun, and no-exception behavior per D-01/D-03/D-04/D-05. Do not start a second controller, relabel manually, rerun, exclude the publication success, move the population boundary, or select around any success on PR 96.

The terminal controller has two accepted exact-ten outcomes: PASS exits zero and closes CI-14; a complete API-backed receipt that honestly misses either threshold exits two and leaves CI-14 `gaps_found`. Any membership, repository/PR identity, chronology, API, or missing-receipt failure is a distinct integrity failure and must remain non-green. After either exact-ten outcome, invoke explicit verify mode with the same required `--repo "$REPO" --pr "$PR"`, invoke the corrected completed-state run path once more against the same state to prove live revalidation without retriggering, and require both paths to cross-check CLI/state/source/PR API/run-association identity before population and thresholds. Interpret only the verifier's specific inclusive-threshold diagnostic as the expected nonzero result for an honest timing miss. Re-run formatter/topology/observability/summary contracts, confirm the label and lock are absent, and close CI-14 only when live recomputation proves median <=480 and nearest-rank p95 <=600. If either complete sequence fails before ten qualifying rows, retain all honest state/evidence and return `gaps_found`; do not claim completion or broaden remediation authority.
  </action>
  <acceptance_criteria>
    - Exactly one controller owns mutation; accepted evidence is the complete repository/workflow/numeric-PR-bound population of exactly ten successful, non-cancelled, first-attempt pull_request runs on one immutable head, with state provenance binding repo/PR identity, the publication run first, and nine label-trigger run IDs.
    - The same source-derived transition manifest passes the immediate read-only preflight and the single fast-forward run; no stale allowlist or caller-selected executable exception participates.
    - Explicit verify requires PR 96; explicit and resumed completed-state modes cross-check CLI/state/source/API repository and PR identity, exclude same-SHA other-PR runs, re-resolve every bound-PR run/job, recomputed statistics, exact population equality, and inclusive thresholds, then preserve run/label/state/lock/receipt populations; PASS exits zero and an honest live timing miss returns only the documented miss status.
    - Table/source manifest, API identities, exact chronology, non-overlap, numeric sort, median ranks 5/6, p95 rank 10, and inclusive threshold verdict agree.
    - Owned label and lock are absent after exit; required summary membership/topology/skip semantics and the 132-15 preservation subject remain intact.
    - CI-14 is marked complete only for median <=480 and p95 <=600; otherwise the phase remains `gaps_found` through machine evidence.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
mix format --check-formatted
mix test test/install_smoke/ci_timing_automation_test.exs test/install_smoke/ci_lane_split_test.exs test/install_smoke/ci_observability_test.exs --seed 0
bash scripts/ci/test_ci_summary_gate.sh
./scripts/maintainer/automation_first_contract.sh

REPO=szTheory/rindle
PR=96
WORKFLOW=ci.yml
SUMMARY_JOB='CI Summary'
LABEL=ci-timing-sample
SAMPLES=10
MAX_SEQUENCES=2
MEDIAN_MAX=480
P95_MAX=600
RECEIPT=.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
PRESERVATION=.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
STATE_DIR=.gsd/ci-timing/phase-132-final
TRANSITION_JSON=$(awk '$0 == "PRESERVATION_TRANSITION_V2_BEGIN" {take=1; next} $0 == "PRESERVATION_TRANSITION_V2_END" {take=0} take' "$PRESERVATION")
CORRECTION_SHA=$(jq -r .controller_correction_sha &lt;&lt;&lt;"$TRANSITION_JSON")
FORMATTER_SHA=$(jq -r .formatter_correction_sha &lt;&lt;&lt;"$TRANSITION_JSON")
REPAIR_SHA=$(jq -r .repair_sha &lt;&lt;&lt;"$TRANSITION_JSON")
PRESERVED_SHA=$(jq -r .preserved_subject_sha &lt;&lt;&lt;"$TRANSITION_JSON")
[[ "$CORRECTION_SHA" =~ ^[0-9a-f]{40}$ ]]
[[ "$PRESERVED_SHA" =~ ^[0-9a-f]{40}$ ]]
test "$(grep -c '^PRESERVATION_TRANSITION_V2_BEGIN$' "$PRESERVATION")" -eq 1
test "$(grep -c '^PRESERVATION_TRANSITION_V2_END$' "$PRESERVATION")" -eq 1
jq -e --arg repo "$REPO" --argjson pr "$PR" --arg correction "$CORRECTION_SHA" --arg formatter "$FORMATTER_SHA" --arg repair "$REPAIR_SHA" --arg preserved "$PRESERVED_SHA" '.schema_version == 2 and .repo == $repo and .pr == $pr and .controller_correction_sha == $correction and .formatter_correction_sha == $formatter and .repair_sha == $repair and .preserved_subject_sha == $preserved and .repair_sha == .preserved_subject_sha and (.stages | length) == 3 and .stages[0].id == "plan-132-12" and .stages[1].id == "plan-132-13" and .stages[2].id == "plan-132-14-repair" and .stages[0].to_sha == .stages[1].from_sha and .stages[1].to_sha == .stages[2].from_sha' &lt;&lt;&lt;"$TRANSITION_JSON" &gt;/dev/null
HEAD_SHA=$(git rev-parse HEAD)
PR_JSON=$(gh pr view "$PR" --repo "$REPO" --json headRefName,headRefOid)
REMOTE_HEAD=$(jq -r .headRefOid &lt;&lt;&lt;"$PR_JSON")
test "$REMOTE_HEAD" != "$HEAD_SHA"
git merge-base --is-ancestor "$REMOTE_HEAD" "$HEAD_SHA"
HEAD_REF=$(jq -r .headRefName &lt;&lt;&lt;"$PR_JSON")
ENCODED_HEAD_REF=$(jq -rn --arg value "$HEAD_REF" '$value | @uri')
all_run_ids() { gh api --paginate -H 'Accept: application/vnd.github+json' "repos/${REPO}/actions/workflows/${WORKFLOW}/runs?event=pull_request&amp;branch=${ENCODED_HEAD_REF}&amp;per_page=100" | jq -sc --arg sha "$HEAD_SHA" --argjson pr "$PR" '[.[].workflow_runs[] | select(.event == "pull_request" and .head_sha == $sha and any(.pull_requests[]?; .number == $pr)) | .id] | sort'; }
canonical_eligible_ids() { gh api --paginate -H 'Accept: application/vnd.github+json' "repos/${REPO}/actions/workflows/${WORKFLOW}/runs?event=pull_request&amp;branch=${ENCODED_HEAD_REF}&amp;per_page=100" | jq -sc --arg sha "$HEAD_SHA" --argjson pr "$PR" '[.[].workflow_runs[] | select(.event == "pull_request" and .head_sha == $sha and .run_attempt == 1 and .status == "completed" and .conclusion == "success" and any(.pull_requests[]?; .number == $pr)) | {id,run_started_at}] | sort_by(.run_started_at, .id) | map(.id)'; }
other_pr_same_sha_ids() { gh api --paginate -H 'Accept: application/vnd.github+json' "repos/${REPO}/actions/workflows/${WORKFLOW}/runs?event=pull_request&amp;branch=${ENCODED_HEAD_REF}&amp;per_page=100" | jq -sc --arg sha "$HEAD_SHA" --argjson pr "$PR" '[.[].workflow_runs[] | select(.event == "pull_request" and .head_sha == $sha and .run_attempt == 1 and .status == "completed" and .conclusion == "success" and ((any(.pull_requests[]?; .number == $pr)) | not)) | .id] | sort'; }
STATE_FILE="$STATE_DIR/pr-${PR}-${HEAD_SHA}.json"
LOCK_DIR="${STATE_FILE}.lock"
test ! -e "$STATE_FILE"
test ! -e "$LOCK_DIR"
for marker in CI_TIMING_CURRENT_TABLE_BEGIN CI_TIMING_CURRENT_TABLE_END CI_TIMING_CURRENT_SOURCE_BEGIN CI_TIMING_CURRENT_SOURCE_END; do test "$(grep -c "^${marker}$" "$RECEIPT" || true)" -eq 0; done
gh pr view "$PR" --repo "$REPO" --json labels | jq -e --arg label "$LABEL" '([.labels[].name] | index($label)) == null' &gt;/dev/null

PREFLIGHT_PR_BEFORE=$(gh pr view "$PR" --repo "$REPO" --json headRefName,headRefOid,labels)
PREFLIGHT_RUNS_BEFORE=$(all_run_ids)
PREFLIGHT_RECEIPT_HASH=$(git hash-object "$RECEIPT")
bash scripts/ci/collect_pr_timing_receipt.sh preflight --repo "$REPO" --pr "$PR" --workflow "$WORKFLOW" --summary-job "$SUMMARY_JOB" --label "$LABEL" --samples "$SAMPLES" --max-sequences "$MAX_SEQUENCES" --median-max "$MEDIAN_MAX" --p95-max "$P95_MAX" --correction-sha "$CORRECTION_SHA" --preserved-subject-sha "$PRESERVED_SHA" --transition-manifest "$PRESERVATION" --receipt "$RECEIPT" --state-dir "$STATE_DIR" --no-publish
test "$(gh pr view "$PR" --repo "$REPO" --json headRefName,headRefOid,labels)" = "$PREFLIGHT_PR_BEFORE"
test "$(all_run_ids)" = "$PREFLIGHT_RUNS_BEFORE"
test "$(git hash-object "$RECEIPT")" = "$PREFLIGHT_RECEIPT_HASH"
test ! -e "$STATE_FILE"
test ! -e "$LOCK_DIR"

RUN_CMD=(bash scripts/ci/collect_pr_timing_receipt.sh run --repo "$REPO" --pr "$PR" --workflow "$WORKFLOW" --summary-job "$SUMMARY_JOB" --label "$LABEL" --samples "$SAMPLES" --max-sequences "$MAX_SEQUENCES" --median-max "$MEDIAN_MAX" --p95-max "$P95_MAX" --correction-sha "$CORRECTION_SHA" --preserved-subject-sha "$PRESERVED_SHA" --transition-manifest "$PRESERVATION" --receipt "$RECEIPT" --state-dir "$STATE_DIR" --publish-head)
set +e
"${RUN_CMD[@]}"
FIRST_EXIT=$?
set -e
test -f "$STATE_FILE"
test "$(jq -r .status "$STATE_FILE")" = complete
test "$(jq '.runs | length' "$STATE_FILE")" -eq 10
jq -e '.publication_trigger.disposition == "included" and .trigger_provenance.publication_run_id == .runs[0].id and (.trigger_provenance.label_run_ids | length) == 9 and .trigger_provenance.label_run_ids == [.runs[1:][]?.id]' "$STATE_FILE" &gt;/dev/null
jq -e --arg repo "$REPO" --argjson pr "$PR" '.repo == $repo and .pr == $pr' "$STATE_FILE" &gt;/dev/null
SELECTED_IDS=$(jq -c '[.runs[].id]' "$STATE_FILE")
ELIGIBLE_IDS=$(canonical_eligible_ids)
test "$SELECTED_IDS" = "$ELIGIBLE_IDS"
OTHER_PR_SAME_SHA_IDS=$(other_pr_same_sha_ids)
jq -en --argjson selected "$SELECTED_IDS" --argjson other "$OTHER_PR_SAME_SHA_IDS" '([$selected[]] - [$other[]]) == $selected' &gt;/dev/null
test "$(jq 'length' &lt;&lt;&lt;"$ELIGIBLE_IDS")" -eq 10
test "$(jq 'length' &lt;&lt;&lt;"$(all_run_ids)")" -eq 10
for marker in CI_TIMING_CURRENT_TABLE_BEGIN CI_TIMING_CURRENT_TABLE_END CI_TIMING_CURRENT_SOURCE_BEGIN CI_TIMING_CURRENT_SOURCE_END; do test "$(grep -c "^${marker}$" "$RECEIPT")" -eq 1; done
CURRENT_SOURCE_JSON=$(awk '$0 == "CI_TIMING_CURRENT_SOURCE_BEGIN" {take=1; next} $0 == "CI_TIMING_CURRENT_SOURCE_END" {take=0} take' "$RECEIPT")
jq -e --arg repo "$REPO" --argjson pr "$PR" --arg sha "$HEAD_SHA" '.repo == $repo and .pr == $pr and .sha == $sha' &lt;&lt;&lt;"$CURRENT_SOURCE_JSON" &gt;/dev/null
VERDICT=$(jq -r .verdict "$STATE_FILE")
jq -e --argjson median_max "$MEDIAN_MAX" --argjson p95_max "$P95_MAX" 'if .verdict == "PASS" then .median_seconds &lt;= $median_max and .p95_seconds &lt;= $p95_max else .verdict == "FAIL" and (.median_seconds &gt; $median_max or .p95_seconds &gt; $p95_max) end' "$STATE_FILE" &gt;/dev/null

set +e
VERIFY_OUTPUT=$(bash scripts/ci/collect_pr_timing_receipt.sh verify --repo "$REPO" --pr "$PR" --workflow "$WORKFLOW" --summary-job "$SUMMARY_JOB" --samples "$SAMPLES" --median-max "$MEDIAN_MAX" --p95-max "$P95_MAX" --receipt "$RECEIPT" 2&gt;&amp;1)
VERIFY_EXIT=$?
set -e
if [ "$VERDICT" = PASS ]; then
  test "$FIRST_EXIT" -eq 0
  test "$VERIFY_EXIT" -eq 0
else
  test "$FIRST_EXIT" -eq 2
  test "$VERIFY_EXIT" -ne 0
  grep -Fq 'current receipt misses inclusive CI-14 timing thresholds' &lt;&lt;&lt;"$VERIFY_OUTPUT"
fi

RECEIPT_HASH_BEFORE_RESUME=$(git hash-object "$RECEIPT")
CURRENT_COUNTS_BEFORE_RESUME=$(for marker in CI_TIMING_CURRENT_TABLE_BEGIN CI_TIMING_CURRENT_TABLE_END CI_TIMING_CURRENT_SOURCE_BEGIN CI_TIMING_CURRENT_SOURCE_END; do grep -c "^${marker}$" "$RECEIPT"; done | paste -sd, -)
STATE_RUN_COUNT_BEFORE_RESUME=$(jq '.runs | length' "$STATE_FILE")
ALL_RUN_IDS_BEFORE_RESUME=$(all_run_ids)
ELIGIBLE_IDS_BEFORE_RESUME=$(canonical_eligible_ids)

set +e
SECOND_OUTPUT=$("${RUN_CMD[@]}" 2&gt;&amp;1)
SECOND_EXIT=$?
set -e
if [ "$VERDICT" = PASS ]; then
  test "$SECOND_EXIT" -eq 0
  grep -Fq 'existing completed receipt verified: verdict=PASS' &lt;&lt;&lt;"$SECOND_OUTPUT"
else
  test "$SECOND_EXIT" -ne 0
  grep -Fq 'current receipt misses inclusive CI-14 timing thresholds' &lt;&lt;&lt;"$SECOND_OUTPUT"
fi
test "$(git hash-object "$RECEIPT")" = "$RECEIPT_HASH_BEFORE_RESUME"
test "$(for marker in CI_TIMING_CURRENT_TABLE_BEGIN CI_TIMING_CURRENT_TABLE_END CI_TIMING_CURRENT_SOURCE_BEGIN CI_TIMING_CURRENT_SOURCE_END; do grep -c "^${marker}$" "$RECEIPT"; done | paste -sd, -)" = "$CURRENT_COUNTS_BEFORE_RESUME"
test "$(jq '.runs | length' "$STATE_FILE")" = "$STATE_RUN_COUNT_BEFORE_RESUME"
test "$(all_run_ids)" = "$ALL_RUN_IDS_BEFORE_RESUME"
test "$(canonical_eligible_ids)" = "$ELIGIBLE_IDS_BEFORE_RESUME"
test "$(jq -c '[.runs[].id]' "$STATE_FILE")" = "$ELIGIBLE_IDS_BEFORE_RESUME"
gh pr view "$PR" --repo "$REPO" --json labels | jq -e --arg label "$LABEL" '([.labels[].name] | index($label)) == null' &gt;/dev/null
test ! -e "$LOCK_DIR"

mix format --check-formatted
mix test test/install_smoke/ci_timing_automation_test.exs test/install_smoke/ci_lane_split_test.exs test/install_smoke/ci_observability_test.exs --seed 0
bash scripts/ci/test_ci_summary_gate.sh
./scripts/maintainer/automation_first_contract.sh
git diff --check</automated>
  </verify>
  <done>A fresh immutable-head exact-ten receipt passes live API verification and both inclusive thresholds, closing CI-14 while retaining COV-05, SAFE-02, and all locked gate contracts; otherwise the machine verdict remains `gaps_found`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (ASVS L1; high severity blocks execution)

| Boundary | Description |
| --- | --- |
| local controller -> GitHub PR | Publication/label mutation must be authenticated, fast-forward-only, single-owner, bounded, and cleaned. |
| Actions API -> current receipt | Remote run/job identity, chronology, and timestamps become acceptance evidence. |
| receipt/state -> CI-14 verdict | Local bytes cannot decide closure without the corrected live revalidation path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-132-16-01 | Spoofing | run/head/summary identity | high | mitigate | Match full SHA, event, attempt, status, conclusion, and exactly one successful Summary through API. |
| T-132-16-02 | Tampering | receipt/state/statistics | high | mitigate | Require one marker pair, table/manifest equality, API recomputation, and corrected completed-state verification. |
| T-132-16-03 | Repudiation | excluded publication/slow successes | high | mitigate | Require state provenance to bind publication row 1 plus nine label IDs, and selected IDs to equal the complete canonical exact-head eligible population. |
| T-132-16-08 | Spoofing | repository/PR population identity | high | mitigate | Pass `--pr` to every mode, cross-check CLI/state/source/PR API identities, require API PR association per run, and assert same-SHA other-PR IDs are disjoint from the exact bound-PR population. |
| T-132-16-04 | Elevation of privilege | GitHub mutation | high | mitigate | Enforce ancestry, PR/label identity, one lock, fast-forward publication, and cleanup traps. |
| T-132-16-05 | Denial of service | interruption/rate limit | medium | mitigate | Preserve resumable pending trigger, bounded backoff, two-sequence ceiling, and sequential collection. |
| T-132-16-06 | Information disclosure | credentials | medium | mitigate | Persist public run metadata only; never print/store tokens. |
| T-132-16-07 | Tampering | preservation handoff | high | mitigate | Reparse the marker-bounded source-derived transition and require the same manifest for successful no-publish preflight and the single publishing run. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Fresh comparable threshold-passing ten-run receipt | 132-16 | COVERED | Terminal live authority.
REQ | CI-14 | Median <=480 and p95 <=600 | 132-16 | COVERED | Complete repository/numeric-PR-bound exact-ten API-backed population with publication sample required.
REQ | COV-05 | Coverage remains >=82.13% | 132-15, 132-16 | COVERED | Same immutable sampled subject.
REQ | SAFE-02 | Behavior-preserving proof and edge cases | 132-15, 132-16 | COVERED | Post-sample focused backstop.
RESEARCH | — | Use existing controller and Actions-native timing | 132-16 | COVERED | No alternate sampler.
CONTEXT | D-01 | Sole required summary/job set/skip behavior | 132-16 | COVERED | Post-sample source contracts.
CONTEXT | D-02 | Only evidence-guided bounded remediation | 132-13 | COVERED | No further change here.
CONTEXT | D-03 | Strict external-runner exception | 132-16 | COVERED | No exception planned.
CONTEXT | D-04/D-05 | Fresh exact-ten canonical population and both inclusive limits | 132-12, 132-16 | COVERED | Publication row, full population equality, and direct live predicates.
CONTEXT | D-06/D-07 | Final preservation and coverage | 132-15, 132-16 | COVERED | Immutable subject retained.
CONTEXT | D-08/D-09 | Exact topology exception and no other drift | 132-15, 132-16 | COVERED | Focused post-sample checks.
</source_audit>

<verification>
The Actions API plus explicit and completed-state live verifier paths are the only CI-14 acceptance authority. Each path must bind repository and numeric PR through CLI/state/source/API agreement before the shared canonical population predicate runs. A failed sequence or threshold miss is `gaps_found`, not successful closure.
</verification>

<success_criteria>
- Exact-ten immutable-head receipt equals the full repository/workflow/numeric-PR-bound eligible API population, with source/state identity agreement and state provenance binding one publication run and nine label runs.
- Median and nearest-rank p95 pass inclusive 480/600 limits.
- COV-05, SAFE-02, topology, required summary, and no-rerun/no-human policies remain intact.
</success_criteria>

<output>
Create `.planning/phases/132-measured-closure/132-16-SUMMARY.md` when done, recording PASS only if both live thresholds pass.
</output>

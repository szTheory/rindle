---
phase: 132-measured-closure
plan: 04
type: execute
wave: 4
depends_on: [132-03]
files_modified:
  - .planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
autonomous: true
requirements: [CI-14, COV-05, SAFE-02]
must_haves:
  truths:
    - "The final automation candidate passes the complete preservation census before any live timing sample is accepted."
    - "The preservation receipt records a full preserved-subject SHA; later measurement commits may differ only under .planning/."
---

<objective>
Re-run every preservation authority on the final automation code and bind live measurement to that immutable subject.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Re-census the final automated candidate</name>
  <files>.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md</files>
  <action>Record HEAD as the preserved subject, then run focused controller/policy and CI topology tests, the summary gate, mix quality_signals, SAFE-01, one authoritative coverage run with the inclusive 82.13% arithmetic, and the full image package-consumer. Update the preservation receipt only after every command passes.</action>
  <verify><automated>mix test test/install_smoke/ci_timing_automation_test.exs test/install_smoke/automation_first_contract_test.exs test/install_smoke/ci_lane_split_test.exs test/install_smoke/ci_observability_test.exs --seed 0 &amp;&amp; bash scripts/ci/test_ci_summary_gate.sh &amp;&amp; mix quality_signals &amp;&amp; bash scripts/maintainer/refactor_contract.sh &amp;&amp; mix coveralls.multiple --type local --type json &amp;&amp; test -s cover/excoveralls.json &amp;&amp; bash scripts/install_smoke.sh image</automated></verify>
  <done>The final code-bearing candidate is preserved with executable evidence and a full SHA.</done>
</task>

</tasks>

<success_criteria>
- Coverage remains at least 82.13% without percentage-only tests.
- Required CI topology and every existing preservation authority remain green.
- The receipt names the exact preserved subject used by Plan 05.
</success_criteria>

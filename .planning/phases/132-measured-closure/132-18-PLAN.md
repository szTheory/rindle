---
phase: 132-measured-closure
plan: 18
type: execute
wave: 18
depends_on: [132-17]
files_modified:
  - .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
autonomous: true
gap_closure: true
requirements: [CI-14, COV-05, SAFE-02]
must_haves:
  truths:
    - "One current receipt equals the complete PR-96-associated canonical population of exactly ten successful, non-cancelled, first-attempt pull-request runs on one immutable published HEAD per D-04."
    - "The publication-triggered qualifying run is sample 1, exactly nine controller-owned label-triggered runs follow sequentially, and at most two complete sequences are permitted."
    - "Run, explicit verify, and completed-state revalidation cross-check repository, numeric PR, immutable SHA, run association, one successful `CI Summary`, chronology, table/source equality, and complete population membership."
    - "Median uses sorted ranks 5/6, nearest-rank p95 uses rank 10, and CI-14 closes only when both inclusive limits of 480 and 600 seconds pass per D-05."
    - "The active Plan 132-17 manifest binds preserved source `15336d4`; every later published path is planning-only, and D-01/D-08/D-09 topology plus fresh COV-05/SAFE-02 evidence remain intact."
  artifacts:
    - path: ".planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md"
      provides: "Exact-ten API-backed current timing table/source manifest and machine verdict"
      contains: "CI_TIMING_CURRENT_SOURCE_BEGIN"
  key_links:
    - from: ".planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md"
      to: "scripts/ci/collect_pr_timing_receipt.sh run"
      via: "active schema-v2 manifest with preserved subject 15336d4 and planning-only published tail"
      pattern: "PRESERVATION_TRANSITION_V2_BEGIN"
    - from: "controller state trigger provenance"
      to: "current receipt/API verifier"
      via: "one publication run plus nine label runs equals the full canonical eligible population"
      pattern: "canonical_eligible_run_ids|trigger_provenance"
---

<objective>
Run the terminal API-backed exact-ten sampler after fresh post-repair preservation.

Purpose: Close CI-14 only from a complete live comparable population while retaining every behavior-preserving gate.
Output: One current timing receipt and a machine-decided PASS or honest `gaps_found` result.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RUNNING.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/132-measured-closure/132-CONTEXT.md
@.planning/phases/132-measured-closure/132-17-SUMMARY.md
@.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
@.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
@scripts/ci/collect_pr_timing_receipt.sh
@test/install_smoke/ci_timing_automation_test.exs
@test/install_smoke/ci_lane_split_test.exs
@test/install_smoke/ci_observability_test.exs
@.github/workflows/ci.yml
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Publish once and verify the exact-ten PR population</name>
  <precondition>Plan 132-17 is machine-green with exactly one active schema-v2 manifest whose preserved subject is `15336d4`; every later local path is planning-only; `gh auth status` succeeds; PR #96 is open at strict-ancestor head `7f025dfdf55d612861610a10773d86761a374277`; `ci-timing-sample` exists and is absent; and no current receipt markers or Phase 132 final controller state/lock exists.</precondition>
  <reversibility rating="reversible">The owned label and lock are cleaned and the receipt append is revertible; Actions runs remain durable evidence under the explicitly bounded live-sampling authorization.</reversibility>
  <files>.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md</files>
  <read_first>
    - AGENTS.md
    - RUNNING.md
    - .planning/phases/132-measured-closure/132-CONTEXT.md
    - .planning/phases/132-measured-closure/132-17-SUMMARY.md
    - .planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
    - .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
    - scripts/ci/collect_pr_timing_receipt.sh
    - test/install_smoke/ci_timing_automation_test.exs
    - test/install_smoke/ci_lane_split_test.exs
    - test/install_smoke/ci_observability_test.exs
    - scripts/ci/test_ci_summary_gate.sh
    - .github/workflows/ci.yml
  </read_first>
  <action>
Run formatter, the 18-test controller suite, topology/observability tests, Bash summary gate, automation-first contract, and `git diff --check`. Extract the single active transition manifest from Plan 132-17 evidence; require repository `szTheory/rindle`, PR 96, correction SHA `1671cdd`, repair/preserved subject `15336d4`, exact cumulative three-stage Git equality, explicit Plan 132-16 tail attribution, and planning-only drift from `15336d4` to current HEAD. Snapshot PR/head/labels, exact-head run IDs, receipt hash/current-marker counts, and state/lock absence, then rerun `preflight --no-publish` with ten samples, at most two sequences, and inclusive 480/600 thresholds; require success and snapshot equality before mutation.

Launch exactly one foreground `run --publish-head` controller using that same manifest and identity, dedicated `.gsd/ci-timing/phase-132-final` state, `ci.yml`, `CI Summary`, and `ci-timing-sample`. Publish current HEAD once by fast-forward only. Bind sample 1 to the qualifying publication-triggered PR-96-associated run, then collect exactly nine sequential label-triggered qualifying runs. The selected IDs must equal every successful first-attempt exact-head API run associated with PR 96 inside the controller population boundary; exclude same-SHA runs associated only with other PRs, and never select around a slow success. Preserve pending-trigger resume, delayed discovery, rate-limit backoff, one SHA-scoped lock, non-overlap, label cleanup, no reruns, and the two-sequence ceiling per D-01/D-03/D-04/D-05.

After an exact-ten result, run explicit `verify --repo szTheory/rindle --pr 96` and the completed-state run path once more, proving both re-resolve live API identities, population equality, statistics, and inclusive thresholds without retriggering or mutating the receipt. PASS exits zero and closes CI-14 only when median <=480 and nearest-rank p95 <=600. A complete exact-ten threshold miss exits with the controller's documented miss status and leaves the phase `gaps_found`; an exhausted sequence or integrity failure also remains non-green with honest state/evidence. Re-run the offline gates, require owned label/lock cleanup and no source drift, and never weaken topology, thresholds, proof, rerun, or exception policy after a miss.
  </action>
  <acceptance_criteria>
    - Exactly one controller owns mutation and publishes one fast-forward immutable HEAD whose source subject is the freshly preserved `15336d4` plus planning-only evidence.
    - Accepted evidence contains exactly ten PR-96-associated successful first-attempt runs: one publication run followed by nine label-triggered runs, with at most two sequences and no overlap or rerun.
    - Explicit and completed-state verification reproduce the complete canonical population and statistics from the Actions API without further triggers or receipt mutation.
    - CI-14 closes only when median <=480 and p95 <=600 inclusively; every other outcome remains machine-recorded `gaps_found`.
    - COV-05, SAFE-02, sole-summary membership, skip semantics, exact D-08 topology, and all D-09 prohibitions remain intact.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
mix format --check-formatted
FOCUSED_OUTPUT=$(mix test test/install_smoke/ci_timing_automation_test.exs --seed 0)
grep -Eq '18 tests, 0 failures' &lt;&lt;&lt;"$FOCUSED_OUTPUT"
mix test test/install_smoke/ci_lane_split_test.exs test/install_smoke/ci_observability_test.exs --seed 0
bash scripts/ci/test_ci_summary_gate.sh
./scripts/maintainer/automation_first_contract.sh
PRESERVATION=.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
RECEIPT=.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
TRANSITION_JSON=$(awk '$0 == "PRESERVATION_TRANSITION_V2_BEGIN" {take=1; next} $0 == "PRESERVATION_TRANSITION_V2_END" {take=0} take' "$PRESERVATION")
test "$(jq -r .preserved_subject_sha &lt;&lt;&lt;"$TRANSITION_JSON")" = "15336d4cc8053aa90788c62377096081c5b07f21"
test -z "$(git diff --name-only 15336d4..HEAD | grep -Ev '^\.planning/' || true)"
bash scripts/ci/collect_pr_timing_receipt.sh preflight --repo szTheory/rindle --pr 96 --workflow ci.yml --summary-job 'CI Summary' --label ci-timing-sample --samples 10 --max-sequences 2 --median-max 480 --p95-max 600 --correction-sha 1671cdde1c42231a8a958c8f2771a24edb8b444e --preserved-subject-sha 15336d4cc8053aa90788c62377096081c5b07f21 --transition-manifest "$PRESERVATION" --receipt "$RECEIPT" --state-dir .gsd/ci-timing/phase-132-final-preflight --no-publish
set +e
bash scripts/ci/collect_pr_timing_receipt.sh run --repo szTheory/rindle --pr 96 --workflow ci.yml --summary-job 'CI Summary' --label ci-timing-sample --samples 10 --max-sequences 2 --median-max 480 --p95-max 600 --correction-sha 1671cdde1c42231a8a958c8f2771a24edb8b444e --preserved-subject-sha 15336d4cc8053aa90788c62377096081c5b07f21 --transition-manifest "$PRESERVATION" --receipt "$RECEIPT" --state-dir .gsd/ci-timing/phase-132-final --publish-head
RUN_EXIT=$?
set -e
test "$RUN_EXIT" -eq 0 -o "$RUN_EXIT" -eq 2
set +e
VERIFY_OUTPUT=$(bash scripts/ci/collect_pr_timing_receipt.sh verify --repo szTheory/rindle --pr 96 --workflow ci.yml --summary-job 'CI Summary' --samples 10 --median-max 480 --p95-max 600 --receipt "$RECEIPT" 2&gt;&amp;1)
VERIFY_EXIT=$?
set -e
if [ "$RUN_EXIT" -eq 0 ]; then
  test "$VERIFY_EXIT" -eq 0
else
  test "$VERIFY_EXIT" -ne 0
  grep -Fq 'current receipt misses inclusive CI-14 timing thresholds' &lt;&lt;&lt;"$VERIFY_OUTPUT"
fi
./scripts/maintainer/automation_first_contract.sh
git diff --check</automated>
  </verify>
  <done>A complete PR-associated exact-ten receipt is live-verified; CI-14 closes only on both inclusive thresholds, otherwise the machine evidence keeps the phase honestly `gaps_found`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| local Git/controller -> GitHub PR | Publication and labels are authenticated, fast-forward-only, single-owner, bounded, and cleaned. |
| Actions API -> timing receipt | Remote run/job identity and timestamps become acceptance evidence only after live revalidation. |
| receipt/state -> CI-14 verdict | Local bytes cannot close CI-14 without complete population and threshold recomputation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-132-18-01 | Spoofing | repository/PR/run identity | high | mitigate | Cross-check CLI, state, source manifest, PR API association, SHA, event, attempt, and one successful Summary. |
| T-132-18-02 | Tampering | population/statistics | high | mitigate | Require exact-ten table/source/API equality and recompute ranks plus inclusive thresholds. |
| T-132-18-03 | Repudiation | excluded slow success | high | mitigate | Selected IDs must equal the complete canonical eligible PR-96 population. |
| T-132-18-04 | Elevation of privilege | publication/label mutation | high | mitigate | Enforce preflight, strict ancestry, one lock/controller, bounded label ownership, and cleanup. |
| T-132-18-05 | Denial of service | interruption/rate limit | medium | mitigate | Persist pending trigger/current run, resume without retriggering, back off, and cap complete sequences at two. |
| T-132-18-06 | Information disclosure | GitHub credentials | medium | mitigate | Persist public run metadata only and never print/store tokens. |
| T-132-18-07 | Tampering | preservation handoff | high | mitigate | Validate the Plan 132-17 active manifest and reject every post-subject non-planning path before mutation. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Fresh comparable ten-run receipt | 132-18 | COVERED | Sole terminal authority.
REQ | CI-14 | Median <=480 and p95 <=600 | 132-18 | COVERED | Exact-ten live API population.
REQ | COV-05 | Coverage remains >=82.13% | 132-17, 132-18 | COVERED | Fresh preservation plus no source drift.
REQ | SAFE-02 | Behavior-preserving proof and edge cases | 132-17, 132-18 | COVERED | Fresh census and terminal backstop.
RESEARCH | — | Existing controller and Actions-native timing | 132-18 | COVERED | No alternate sampler.
CONTEXT | D-01 | Sole required summary/job set/skip behavior | 132-18 | COVERED | Offline contracts rerun.
CONTEXT | D-02/D-03 | Bounded correction; no unsupported exception | 132-18 | COVERED | No new remediation or exception.
CONTEXT | D-04/D-05 | Exact ten and inclusive thresholds | 132-18 | COVERED | Direct live acceptance.
CONTEXT | D-06/D-07 | Final preservation and coverage floor | 132-17, 132-18 | COVERED | Same source subject, planning-only tail.
CONTEXT | D-08/D-09 | Exact six-edge topology and no broader drift | 132-17, 132-18 | COVERED | Source census and topology tests.
</source_audit>

<verification>
The Actions API, explicit verifier, and completed-state verifier are the only CI-14 authorities. A threshold miss, incomplete sequence, identity mismatch, or integrity failure remains `gaps_found` without human substitution.
</verification>

<success_criteria>
- Exact-ten PR-96-associated receipt equals the full canonical eligible API population.
- Median and nearest-rank p95 pass inclusive 480/600 limits before CI-14 closes.
- COV-05, SAFE-02, topology, no-rerun, and automation-first contracts remain intact.
</success_criteria>

<output>
Create `.planning/phases/132-measured-closure/132-18-SUMMARY.md` when done, recording PASS only if both live thresholds pass.
</output>

---
phase: 132-measured-closure
plan: 09
type: tdd
wave: 9
depends_on: [132-07, 132-08]
files_modified:
  - .github/workflows/ci.yml
  - test/install_smoke/ci_lane_split_test.exs
  - test/fixtures/ci_timing/phase_132_topology_projection.json
autonomous: true
requirements: [CI-14, COV-05, SAFE-02]
must_haves:
  truths:
    - "Exactly the `quality` and `optional-dependencies` prerequisites are absent from `integration`, `contract`, and `adoption-demo-e2e-smoke`, implementing the six authorized D-08 scheduling-edge removals."
    - "The three affected jobs remain self-contained: their runners, matrices, services, checkout, toolchain, dependency acquisition, and proof commands are unchanged per D-09."
    - "`adopter.needs` remains `[quality, optional-dependencies, integration, contract]`; all `ci-summary.needs` and `ci-observability.needs` members remain present; `CI Summary`, `if: always()`, and skip-as-pass evaluation remain unchanged per D-01, D-08, and D-09."
    - "A deterministic five-run fixture reproduces the observed baseline and demonstrates why removing the Integration and Smoke barriers alone is insufficient while the exact six-edge graph has material projected headroom; the projection is not CI-14 acceptance."
    - statement: "SAFE-02 ordering backstop: the desired topology contract is observed RED against the pre-correction workflow before the six-edge edit is made, then all focused topology and gate contracts are GREEN."
      verification: backstop
    - statement: "Assumption delta is no-change: the primary noun remains the single CI job graph; historical `fallback-refresh` wording describes an apt implementation detail and creates no plural identity model."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT remove any prerequisite edge beyond D-08's exact six-edge authorization or change required-result membership, gate semantics, a job body, proof command, matrix, service, runner, cache, partition, or rerun policy."
      category: safety
      status: unresolved
      verification: null
      flagged_unverified: true
    - statement: "MUST NOT treat deterministic fixture projection or local wall-clock output as CI-14 evidence."
      category: transparency
      status: unresolved
      verification: null
      flagged_unverified: true
    - statement: "MUST NOT weaken, rename, bypass, or remove `CI Summary` or any result it evaluates."
      category: safety
      status: unresolved
      verification: null
      flagged_unverified: true
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "The exact D-08 six-edge scheduling correction and no other workflow change"
      contains: "adoption-demo-e2e-smoke:"
    - path: "test/install_smoke/ci_lane_split_test.exs"
      provides: "RED/GREEN source-bound contract for exact topology, self-contained affected job bodies, membership, and D-09 drift rejection"
      contains: "phase 132"
    - path: "test/fixtures/ci_timing/phase_132_topology_projection.json"
      provides: "Normalized five-run native timing fixture for deterministic topology projection"
      contains: "394550944bbff63c0e61c258528c8f8764298745"
    - path: ".planning/phases/132-measured-closure/132-09-PLAN.md"
      provides: "Executable recovery topology plan and source audit"
      contains: "D-08"
    - path: ".planning/phases/132-measured-closure/132-VALIDATION.md"
      provides: "Recovery task-to-test mapping for 132-09"
      contains: "132-09-01"
    - path: ".planning/ROADMAP.md"
      provides: "Append-only Phase 132 recovery wave and supersession ordering"
      contains: "132-09-PLAN.md"
  key_links:
    - from: "test/install_smoke/ci_lane_split_test.exs"
      to: ".github/workflows/ci.yml"
      via: "source-bounded job and needs parsing proves the exact six-edge delta and frozen surrounding graph"
      pattern: "integration|contract|adoption-demo-e2e-smoke"
    - from: "test/fixtures/ci_timing/phase_132_topology_projection.json"
      to: "test/install_smoke/ci_lane_split_test.exs"
      via: "recorded native offsets feed deterministic baseline and recovered-graph projections"
      pattern: "topology_projection"
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/eval_ci_summary.sh"
      via: "unchanged ci-summary membership supplies the unchanged skip-as-pass evaluator"
      pattern: "toJSON\(needs\)"
---

<objective>
Apply the user-authorized D-08/D-09 scheduling correction behind a RED/GREEN executable graph contract.

Purpose: Remove the demonstrated Quality/Optional barrier from all three self-contained critical branches while preserving every required result, proof body, evaluator behavior, and all topology outside the exact six edges.
Output: A normalized deterministic projection fixture, a source-bound topology/self-containment regression, and the exact three-line workflow correction.
</objective>

## Artifacts this phase produces

- New `.planning/phases/132-measured-closure/132-09-PLAN.md`: the executable recovery topology prompt, decision trace, threat register, and multi-source audit.
- Updated `.planning/ROADMAP.md` and `.planning/phases/132-measured-closure/132-VALIDATION.md`: append-only recovery waves and exact 132-09 task verification mapping.
- New `test/fixtures/ci_timing/phase_132_topology_projection.json`: normalized native timing offsets for runs `33003369940`, `33004281315`, `33005105221`, `33005882907`, and `33006773326`, sourced from immutable head `394550944bbff63c0e61c258528c8f8764298745`.
- Updated `test/install_smoke/ci_lane_split_test.exs`: desired D-08 topology, D-09 affected-job self-containment, frozen surrounding `needs` membership, summary/observability membership, adopter prerequisites, and deterministic projection contracts.
- Updated `.github/workflows/ci.yml`: only the three `needs: [quality, optional-dependencies]` declarations on `integration`, `contract`, and `adoption-demo-e2e-smoke` are removed, representing exactly six edges. No new application symbol, dependency, API surface, or workflow job is introduced.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RUNNING.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/132-measured-closure/132-CONTEXT.md
@.planning/phases/132-measured-closure/132-RESEARCH.md
@.planning/phases/132-measured-closure/132-VERIFICATION.md
@.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
@.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
@.github/workflows/ci.yml
@scripts/ci/eval_ci_summary.sh
@scripts/ci/test_ci_summary_gate.sh
@test/install_smoke/ci_lane_split_test.exs
@test/install_smoke/ci_observability_test.exs
</context>

<assumptions>
- CI-14 is flagged unclassified by the generic probe. This plan resolves only the causal topology predicate; the fresh API-backed ten-run receipt in 132-11 remains the sole CI-14 acceptance authority.
- COV-05 is also generically unclassified. The workflow correction does not alter the Quality job body or coverage command, and 132-10 repeats the authoritative coverage census before sampling.
- SAFE-02 adjacency is resolved by inclusive 480/600 receipt boundaries in 132-11. Its empty/singleton predicate maps to fail-closed exact job/edge membership here: a missing job block, missing expected member, or incomplete six-edge delta fails. Equal timing offsets retain stable source order and numeric rank in the deterministic projection. Concurrency is explicit graph ordering, not process-shared state.
- D-08 is the user-authorized bounded exception to D-02. Where the stale `132-PATTERNS.md` says `ci.yml` is not an edit target, D-08/D-09 and recovery research take precedence.
- The topology edit is serious but reversible; reverting the three removed declarations restores the former graph without a migration or published-contract break.
</assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED-lock the exact graph delta and deterministic critical-path projection</name>
  <reversibility rating="reversible">The test and normalized fixture are local contracts; they can be removed without altering runtime data or a published interface.</reversibility>
  <files>test/install_smoke/ci_lane_split_test.exs, test/fixtures/ci_timing/phase_132_topology_projection.json</files>
  <read_first>
    - AGENTS.md
    - RUNNING.md
    - .planning/phases/132-measured-closure/132-CONTEXT.md
    - .planning/phases/132-measured-closure/132-RESEARCH.md
    - .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
    - .github/workflows/ci.yml
    - scripts/ci/eval_ci_summary.sh
    - scripts/ci/test_ci_summary_gate.sh
    - test/install_smoke/ci_lane_split_test.exs
    - test/install_smoke/ci_observability_test.exs
  </read_first>
  <behavior>
    - "RED: the current workflow fails only because the affected three job blocks still declare the two prerequisites authorized for removal by D-08."
    - "SELF-CONTAINMENT: each affected source-bounded job block retains its runner, matrix when present, services when present, checkout, pinned toolchain setup, its own dependency acquisition, and its existing proof command(s) per D-09."
    - "FROZEN GRAPH: adopter keeps its exact four prerequisites; summary and observability keep every current member; all other job prerequisite sets are unchanged."
    - "PROJECTION: normalized API offsets reproduce baseline median 591 and p95 612, model sequential dependency release deterministically, show Integration plus Smoke-only decoupling remains around 486 seconds median, and show the exact Integration plus Contract plus Smoke correction has material headroom."
  </behavior>
  <action>
Create a normalized JSON fixture for the five cited successful first-attempt runs on immutable head `394550944bbff63c0e61c258528c8f8764298745`. Use the already cited Actions run/job timestamps as the source, storing only run ID and workflow-relative start/completion offsets required to project the Quality/Optional barrier, Integration, Contract, Adopter, E2E Smoke, other required finishers, and CI Summary. Do not store credentials, mutable API output, or local wall-clock measurements. In `ci_lane_split_test.exs`, add source-bounded helpers that isolate top-level job blocks and canonicalize their `needs` members without depending on YAML whitespace beyond the repository's two-space job boundary. Assert the desired D-08 graph, D-09 self-containment tokens, exact adopter prerequisites, complete summary and observability memberships, unchanged `CI Summary` name/always behavior, and unchanged evaluator delegation. Freeze every other current job prerequisite set so any seventh edge change fails.

Add a pure deterministic projection over the fixture: release a job at the maximum projected completion of its declared prerequisites, preserve recorded own duration, and calculate workflow start through projected Summary completion. Assert the recorded graph reproduces the supplied 510/548/591/592/612 sequence and 591/612 statistics; assert the two-edge-pair Integration/Smoke alternative remains near the supplied 486-second median and therefore lacks adequate headroom; assert the D-08 six-edge graph improves both observed critical branches with material projected margin. Keep a clear assertion that projection is causal regression evidence only and cannot satisfy the live CI-14 requirement. Tag the new cases `:phase_132_topology_recovery`, name the desired-source case `D-08 exact six-edge topology`, run that tagged set against the unmodified workflow, and capture the intended RED failure attributable to exactly the three still-present declarations before Task 2.
  </action>
  <acceptance_criteria>
    - The fixture identifies one immutable head and exactly the five cited successful run IDs with normalized native offsets; it contains no secrets or local timing claims.
    - The source-bound contract enumerates the desired empty prerequisite sets for exactly the three D-08 jobs and freezes all surrounding job prerequisites, membership, gate behavior, and affected job-body authorities per D-09.
    - The deterministic fixture reproduces the supplied baseline values and distinguishes the insufficient partial topology from the exact six-edge recovery with material projected headroom.
    - Before `.github/workflows/ci.yml` changes, the focused command exits non-zero for the three expected prerequisite declarations while projection and self-containment assertions are otherwise valid.
  </acceptance_criteria>
  <verify>
    <automated>RED_LOG=$(mktemp) &amp;&amp; if mix test test/install_smoke/ci_lane_split_test.exs --only phase_132_topology_recovery --seed 0 &gt;"$RED_LOG" 2&gt;&amp;1; then rm -f "$RED_LOG"; echo "expected D-08 topology contract to be RED before workflow edit" &gt;&amp;2; exit 1; else grep -Fq "D-08 exact six-edge topology" "$RED_LOG"; RC=$?; rm -f "$RED_LOG"; exit $RC; fi</automated>
  </verify>
  <done>The desired graph is source-bound and intentionally RED for exactly D-08's six scheduling edges, while the deterministic fixture proves why all three authorized branches belong in one correction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN the exact six-edge workflow correction</name>
  <reversibility rating="reversible">The serious scheduling change is exactly three removable YAML declarations; restoring them reverses all six edges without data migration.</reversibility>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/132-measured-closure/132-CONTEXT.md
    - .github/workflows/ci.yml
    - test/install_smoke/ci_lane_split_test.exs
    - test/fixtures/ci_timing/phase_132_topology_projection.json
    - scripts/ci/eval_ci_summary.sh
    - scripts/ci/test_ci_summary_gate.sh
  </read_first>
  <behavior>
    - "GREEN: Integration, Contract, and Adoption Demo E2E Smoke become independent roots without changing their job bodies."
    - "PRESERVATION: Adopter, CI Summary, CI Observability, Package Consumer, and every unaffected job retain their exact prerequisite memberships."
    - "GATE: CI Summary remains sole, always-running, and delegates to the unchanged success-or-skipped evaluator."
  </behavior>
  <action>
In `.github/workflows/ci.yml`, remove only the `needs: [quality, optional-dependencies]` declaration from the `integration`, `contract`, and `adoption-demo-e2e-smoke` job blocks per D-08. Preserve every other byte of workflow text, including all comments and adjacent YAML. Preserve `adopter.needs: [quality, optional-dependencies, integration, contract]`, every member of `ci-summary.needs` and `ci-observability.needs`, workflow/job names, `if: always()`, evaluator script, runners, matrices, services, actions, caches, install commands, proof commands, and rerun behavior per D-01 and D-09. Then run the focused topology/observability tests and Bash evaluator contract; inspect `git diff --unified=0 -- .github/workflows/ci.yml` and fail the task unless the workflow diff consists of exactly those three removed declarations, representing the six authorized prerequisite edges, with no other added, removed, or modified workflow text.
  </action>
  <acceptance_criteria>
    - A machine-enforced zero-context patch assertion strips only Git diff metadata/hunk headers, then requires exactly three removed `needs: [quality, optional-dependencies]` declarations, zero additions, and no other workflow content change; the source-bound graph tests independently bind those declarations to the three D-08 jobs.
    - All desired topology, self-containment, frozen-graph, deterministic projection, summary membership, observability membership, and skip-as-pass tests pass.
    - `adopter.needs` and every summary/observability member remain unchanged; there is no new job, rerun, partition, cache, runner, matrix, service, proof, or exception.
    - `git diff --check` passes and the bounded diff is limited to `.github/workflows/ci.yml`, the topology test, and its deterministic fixture.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail; WORKFLOW_PATCH="$(git diff --unified=0 -- .github/workflows/ci.yml | awk '/^(diff --git |index |--- |\+\+\+ |@@ )/ {next} {print}')"; test "$(printf '%s\n' "$WORKFLOW_PATCH" | wc -l | tr -d ' ')" -eq 3; test "$(printf '%s\n' "$WORKFLOW_PATCH" | grep -Fxc -- '-    needs: [quality, optional-dependencies]')" -eq 3; mix test test/install_smoke/ci_lane_split_test.exs test/install_smoke/ci_observability_test.exs --seed 0 &amp;&amp; bash scripts/ci/test_ci_summary_gate.sh &amp;&amp; git diff --check</automated>
  </verify>
  <done>D-08's exact six scheduling edges are removed and every D-01/D-09 preservation contract is GREEN, with no claim that fixture projection closes CI-14.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (ASVS L1; high severity blocks execution)

| Boundary | Description |
|----------|-------------|
| workflow source -> GitHub scheduler | A small YAML change controls when required jobs begin and must be limited to the authorized edge set. |
| historical Actions evidence -> projection fixture | Normalized timestamps must remain attributable, deterministic, and non-accepting. |
| required jobs -> CI Summary | Membership and skip-as-pass semantics must continue to aggregate every required result. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-132-09-01 | Tampering | `.github/workflows/ci.yml` graph | high | mitigate | Source-bound test freezes every job prerequisite set and permits only the exact D-08 six-edge delta; any broader drift blocks GREEN. |
| T-132-09-02 | Elevation of privilege | required result aggregation | high | mitigate | Preserve exact `ci-summary.needs`, sole `CI Summary`, `if: always()`, and evaluator delegation; run both ExUnit and Bash gate contracts. |
| T-132-09-03 | Spoofing | projection fixture identity | high | mitigate | Fixture pins the full measured SHA and five cited run IDs and reproduces source receipt statistics before projections are trusted. |
| T-132-09-04 | Repudiation | projected speed claim | medium | mitigate | Label projection as causal evidence only; 132-11 API-backed live receipt remains acceptance authority. |
| T-132-09-05 | Denial of service | independently starting jobs | medium | mitigate | Preserve existing runners, services, setup, dependency acquisition, and bounded proof commands; no fan-out or retry policy is added. |
| T-132-09-06 | Information disclosure | historical timing inputs | low | accept | Fixture contains public run IDs and normalized timestamps only; no token, secret, or private payload is stored. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Close the measured CI-14 gap without preservation drift | 132-09 | COVERED | Removes the measured scheduler barrier behind executable preservation contracts.
REQ | CI-14 | Ten-run 480/600 target without weaker gates/reruns | 132-09, 132-11 | COVERED | This plan corrects cause; 132-11 supplies acceptance.
REQ | COV-05 | Authoritative coverage at least 82.13% | 132-10 | COVERED | Quality body is frozen here; census follows.
REQ | SAFE-02 | Focused proof and prohibited-surface preservation | 132-09, 132-10 | COVERED | Exact graph/body contract plus final census.
RESEARCH | — | Both Adopter and Smoke critical branches must be addressed | 132-09 | COVERED | Fixture rejects the insufficient partial alternative.
RESEARCH | — | Existing controller/API verifier remains acceptance authority | 132-11 | COVERED | No replacement measurement system is introduced.
CONTEXT | D-01 | Sole required summary and skip-as-pass preserved | 132-09 | COVERED | Frozen membership and evaluator tests.
CONTEXT | D-02 | Evidence-guided smallest correction | 132-09 | COVERED | D-08 authorizes the bounded topology exception supported by projection.
CONTEXT | D-03 | No unsupported external-runner exception | 132-11 | COVERED | Live miss remains gaps_found.
CONTEXT | D-04 | Fresh exact-head ten-run sequence | 132-11 | COVERED | Delayed until correction/census pass.
CONTEXT | D-05 | Inclusive median/p95 thresholds | 132-11 | COVERED | API verifier recomputes both.
CONTEXT | D-06 | Final preservation re-census | 132-10 | COVERED | Full ordered authorities.
CONTEXT | D-07 | Inclusive coverage floor, no percentage-only tests | 132-10 | COVERED | One authoritative run.
CONTEXT | D-08 | Exact six-edge exception | 132-09 | COVERED | Direct workflow and test action.
CONTEXT | D-09 | No other topology/body/proof drift | 132-09, 132-10 | COVERED | Frozen source contract and bounded census.
</source_audit>

<verification>
The TDD sequence must first demonstrate an attributable RED against the unmodified workflow and then turn GREEN through only the three authorized declaration removals. Focused ExUnit and Bash checks must pass before any preservation or live controller work begins.
</verification>

<success_criteria>
- Exactly six authorized scheduling edges are absent and every other graph/body/gate contract is preserved.
- The measured projection fixture is deterministic, source-attributed, and explicitly non-accepting.
- No CI run is launched by this plan.
</success_criteria>

<output>
Create `.planning/phases/132-measured-closure/132-09-SUMMARY.md` when done.
</output>

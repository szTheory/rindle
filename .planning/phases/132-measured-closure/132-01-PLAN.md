---
phase: 132-measured-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/install_smoke/support/generated_app/workspace.ex
  - test/install_smoke/generated_app_smoke_test.exs
autonomous: true
requirements: [CI-14, SAFE-02]
must_haves:
  truths:
    - "The required image package-consumer still proves a clean-room generated Phoenix app from built package through patch, dependency fetch, compile, migration, boot, and image lifecycle per D-01 and D-02."
    - "Phoenix generation no longer performs the duplicated pre-patch dependency installation; the existing post-patch `Workspace.fetch_deps!/3` remains authoritative."
    - "The narrow correction is validated as a hypothesis by the real image package-consumer before any preservation expansion or timing claim."
  prohibitions:
    - statement: "MUST NOT turn the clean-room package/image proof into a repository-artifact reuse or mock proof."
      category: safety
      status: unresolved
      verification: null
      flagged_unverified: true
    - statement: "MUST NOT weaken, rename, bypass, or remove a result feeding the sole required `CI Summary` check."
      category: transparency
      status: unresolved
      verification: null
      flagged_unverified: true
  artifacts:
    - path: "test/install_smoke/support/generated_app/workspace.ex"
      provides: "Generator argv without pre-patch dependency installation"
      contains: "def generate_phoenix_app!"
    - path: "test/install_smoke/generated_app_smoke_test.exs"
      provides: "Focused generator-order contract plus existing end-to-end image proof"
      contains: "phase_132_generator_contract"
  key_links:
    - from: "test/install_smoke/support/generated_app_helper.ex"
      to: "test/install_smoke/support/generated_app/workspace.ex"
      via: "generate_phoenix_app!/2 -> patch_generated_app!/7 -> fetch_deps!/3 -> mix compile"
      pattern: "generate_phoenix_app!.*patch_generated_app!.*fetch_deps!"
    - from: "test/install_smoke/generated_app_smoke_test.exs"
      to: "scripts/install_smoke.sh"
      via: "the `:image` package proof asserts package provenance, compile, boot, migrations, and lifecycle"
      pattern: "prove_package_install!\(:image\)"
---

<objective>
Prove the smallest evidence-guided CI-14 correction end to end: remove only Phoenix's duplicated pre-patch install work while retaining the full clean-room image package-consumer path.

Purpose: Test the research hypothesis against the actual long pole before expanding into phase-wide preservation or live timing collection, per D-01 and D-02.
Output: A focused fail-first ordering contract, the argv-only correction, and a green real image package-consumer proof.
</objective>

## Artifacts this phase produces

This plan creates no new module, function, CLI flag, struct field, or path. It updates the argv inside `Workspace.generate_phoenix_app!/2` and adds a named `:phase_132_generator_contract` ExUnit contract in the existing smoke-test path.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/132-measured-closure/132-CONTEXT.md
@.planning/phases/132-measured-closure/132-RESEARCH.md
@.planning/phases/132-measured-closure/132-PATTERNS.md
@.planning/phases/132-measured-closure/132-VALIDATION.md
@.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
@.planning/phases/131-signal-preserving-ci-velocity/131-IMPLEMENTATION-VERIFICATION.md
@test/install_smoke/support/generated_app/workspace.ex
@test/install_smoke/support/generated_app_helper.ex
@test/install_smoke/generated_app_smoke_test.exs
@scripts/install_smoke.sh
@.github/workflows/ci.yml
</context>

<assumptions>
- Flagged unresolved CI-14 probe row: the phase-specific interpretation is the locked first-attempt, immutable-head, ten-run required-gate contract in D-04 and D-05; this plan proves only the causal correction and cannot itself close CI-14.
- Flagged unresolved SAFE-02 probe rows are carried to Plan 02 as explicit adjacency, empty-input, ordering, and concurrency backstops; this tracer must not silently stand in for that preservation re-census.
- Research assumption A1 remains unverified: removing the generator install argument may or may not save the measured 35.5-second median gap. Only Plan 03's live receipt resolves it.
- Prohibition recall kept the bespoke proof-integrity constraints above. Routine correctness items were dropped; required-proof bypass and shell injection are security canon and are handled in the threat model rather than duplicated as prohibitions.
</assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Validate the post-patch-only dependency path through the real image consumer</name>
  <reversibility rating="reversible">The argv-only change and its focused contract can be reverted locally without changing a public or persisted contract.</reversibility>
  <files>
    test/install_smoke/support/generated_app/workspace.ex
    test/install_smoke/generated_app_smoke_test.exs
  </files>
  <read_first>
    - .planning/phases/132-measured-closure/132-CONTEXT.md
    - .planning/phases/132-measured-closure/132-RESEARCH.md
    - .planning/phases/132-measured-closure/132-PATTERNS.md
    - .planning/phases/132-measured-closure/132-VALIDATION.md
    - test/install_smoke/support/generated_app/workspace.ex
    - test/install_smoke/support/generated_app_helper.ex
    - test/install_smoke/support/generated_app/command_runner.ex
    - test/install_smoke/generated_app_smoke_test.exs
    - scripts/install_smoke.sh
    - .github/workflows/ci.yml
  </read_first>
  <behavior>
    - RED: a `:phase_132_generator_contract` test proves the `generate_phoenix_app!/2` command omits generator-owned dependency installation while retaining the pinned `mix phx.new`, generated root, four feature-omission arguments, and `MIX_ENV=dev` contract.
    - GREEN: `Workspace.generate_phoenix_app!/2` changes only that argv entry; `GeneratedAppHelper.prove_package_install!(:image)` still executes generate -> patch -> `Workspace.fetch_deps!/3` -> compile -> migrate -> boot -> lifecycle.
    - END TO END: package mode still uses the unpacked package root, has no generated `deps/rindle` repository fallback, and reports successful compile, boot, host-owned migrations, default schema ownership, and presigned-PUT lifecycle.
  </behavior>
  <action>
Add the focused contract first in the existing fast-contract area of `test/install_smoke/generated_app_smoke_test.exs`, tag it `:phase_132_generator_contract`, and demonstrate RED against the current `Workspace.generate_phoenix_app!/2` argv. Make the contract parse only the bounded `generate_phoenix_app!/2` source region so it locks the command contract without snapshotting unrelated private layout. Then remove only the `--install` argv entry from `Workspace.generate_phoenix_app!/2`. Preserve `CommandRunner.run!/3`, `workspace_root`, `generated_app_root`, `--no-assets`, `--no-dashboard`, `--no-mailer`, `--no-gettext`, and `MIX_ENV=dev`. Do not modify `GeneratedAppHelper`, `scripts/install_smoke.sh`, `.github/workflows/ci.yml`, `Workspace.fetch_deps!/3`, its 30-attempt network retry, or any required-gate topology. Run the tagged contract, then the complete image install-smoke so the tracer validates the hypothesis through the production clean-room proof per D-01 and D-02.
  </action>
  <acceptance_criteria>
    - The new tagged contract is observed failing before the argv correction and passing afterward.
    - `Workspace.generate_phoenix_app!/2` retains exactly one `CommandRunner.run!/3` call with `mix`, `phx.new`, the generated root, the four existing feature omissions, and `MIX_ENV=dev`, but delegates all dependency retrieval to the existing post-patch `Workspace.fetch_deps!/3` path.
    - `GeneratedAppHelper.prove_package_install!/1` remains byte-unchanged and retains generate -> patch -> fetch -> compile ordering.
    - `bash scripts/install_smoke.sh image` proves built-package provenance, absence of repository fallback, successful compile and boot, host-owned migrations, default schema ownership, and the canonical image lifecycle.
    - `.github/workflows/ci.yml`, `scripts/install_smoke.sh`, dependency manifests, product `lib/`, Admin, schemas/migrations, telemetry/errors, and release proof are not modified.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/install_smoke/generated_app_smoke_test.exs --only phase_132_generator_contract &amp;&amp; bash scripts/install_smoke.sh image</automated>
  </verify>
  <done>The real image package-consumer passes with Phoenix generation followed by repository patching and the one authoritative post-patch dependency fetch/compile path; no required proof or topology changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated project -> built package | The clean-room consumer must prove the unpacked package, not repository state or a mock. |
| generated project -> external dependency registry | Dependency resolution occurs after the package source is patched and must retain the existing bounded retry behavior. |
| required package job -> CI Summary | The long-pole proof must continue feeding the sole required aggregate check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-132-01 | Elevation of privilege | `Workspace.generate_phoenix_app!/2` and image proof | high | mitigate | Keep post-patch `fetch_deps!/3`, compile, migration, boot, provenance, and lifecycle assertions; run the real image proof before expansion. |
| T-132-04 | Tampering | generated command arguments | medium | mitigate | Lock the bounded argv contract in the tagged ExUnit test and change only one argument. |
| T-132-05 | Tampering | dependency acquisition | low | accept | The phase adds no package and retains the repository's existing generator/dependency authorities and lockfiles unchanged. |
</threat_model>

<verification>
Run the tagged fail-first contract and the full image package-consumer. Confirm the implementation commit changes only the two declared files before Plan 02 records the phase-wide preservation census.
</verification>

<success_criteria>
- The narrow generator correction is production-quality and green end to end.
- Patch -> dependency fetch -> compile -> migration -> boot -> lifecycle remains intact.
- No workflow, required check, dependency, public, data, telemetry/error, Admin, or release surface changes.
</success_criteria>

<output>
Create `.planning/phases/132-measured-closure/132-01-SUMMARY.md` when done.
</output>

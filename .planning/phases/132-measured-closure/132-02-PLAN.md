---
phase: 132-measured-closure
plan: 02
type: execute
wave: 2
depends_on: [132-01]
files_modified:
  - .planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
autonomous: true
requirements: [CI-14, COV-05, SAFE-02]
must_haves:
  truths:
    - "`CI Summary` remains the sole required check with the exact required-job set and skip-as-pass semantics preserved per D-01."
    - "The final correction passes focused CI contracts, `mix quality_signals`, SAFE-01, authoritative one-run coverage, and the full image package-consumer per D-06."
    - "Authoritative coverage is at least 82.13%, and no test is added solely to alter the percentage per D-07."
    - statement: "SAFE-02 adjacency backstop: exactly 82.13% coverage is passing, and exactly 480-second median / 600-second p95 values remain passing rather than boundary failures."
      verification: backstop
    - statement: "SAFE-02 empty backstop: missing proof output or an empty/partial receipt is invalid; one generated consumer and exactly ten qualifying timing samples are required."
      verification: backstop
    - statement: "SAFE-02 ordering backstop: patch precedes dependency fetch and compile, and equal timing values retain deterministic rank-based median/p95 treatment."
      verification: backstop
    - statement: "SAFE-02 concurrency backstop: the generated image proof remains serialized, and interrupted/overlapping live timing samples cannot be treated as a valid sequence."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT add tests for the purpose of increasing the coverage percentage."
      category: values
      status: unresolved
      verification: null
      flagged_unverified: true
    - statement: "MUST NOT describe prohibited product, API, data, telemetry/error, dependency, Admin, or release drift as a behavior-preserving correction."
      category: transparency
      status: unresolved
      verification: null
      flagged_unverified: true
  artifacts:
    - path: ".planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md"
      provides: "Command, result, coverage arithmetic, exact implementation diff, and bounded drift evidence"
      contains: "82.13"
  key_links:
    - from: "test/install_smoke/ci_lane_split_test.exs"
      to: ".github/workflows/ci.yml"
      via: "locks package-consumer membership, adoption smoke, release coupling, and the single required check"
      pattern: "ci-summary.needs"
    - from: "scripts/maintainer/refactor_contract.sh"
      to: ".planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md"
      via: "SAFE-01 output is captured as SAFE-02 preservation evidence"
      pattern: "refactor_contract"
    - from: "cover/excoveralls.json"
      to: ".planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md"
      via: "covered/relevant line counts and exact percentage from the authoritative single run"
      pattern: "covered.*relevant.*percent"
---

<objective>
Re-census every behavior-preserving ratchet against the tracer correction and write a durable preservation receipt before any live CI-14 sample is accepted.

Purpose: Establish D-01, D-06, D-07, COV-05, and SAFE-02 as measured postconditions rather than inferred safety claims.
Output: A new `132-PRESERVATION-RECEIPT.md` containing focused gate proof, complete quality/package proof, exact coverage arithmetic, and a bounded implementation-diff review.
</objective>

## Artifacts this phase produces

New path: `.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md`. This plan creates no module, function, CLI flag, or struct field; it records outputs from existing authorities.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/132-measured-closure/132-CONTEXT.md
@.planning/phases/132-measured-closure/132-RESEARCH.md
@.planning/phases/132-measured-closure/132-VALIDATION.md
@.planning/phases/132-measured-closure/132-01-SUMMARY.md
@.planning/phases/131-signal-preserving-ci-velocity/131-IMPLEMENTATION-VERIFICATION.md
@RUNNING.md
@.github/workflows/ci.yml
@scripts/ci/eval_ci_summary.sh
@scripts/ci/test_ci_summary_gate.sh
@test/install_smoke/ci_lane_split_test.exs
@test/install_smoke/ci_observability_test.exs
@scripts/maintainer/refactor_contract.sh
@scripts/install_smoke.sh
</context>

<assumptions>
- Flagged unresolved COV-05 probe row: its phase-specific authority is the one-run `mix coveralls.multiple --type local --type json` result, with inclusive threshold arithmetic at 82.13%.
- Flagged unresolved SAFE-02 adjacency, empty, ordering, and concurrency rows are resolved only as backstop truths above; they are not dismissed as generic utility cases.
- Flagged unresolved CI-14 probe row remains open after this plan because local preservation proof cannot establish GitHub Actions wall-clock acceptance.
- No one-way decision is introduced. Both evidence recording tasks are reversible; reverting an evidence file does not migrate data or break a published contract.
- Prohibition recall retains the bespoke no-coverage-chasing and no-drift-misrepresentation constraints. Generic proof bypass and shell interpolation remain security-canon threats below.
</assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Prove the required-gate and observability contracts stayed exact</name>
  <reversibility rating="reversible">This task records existing contract outputs and can be reverted without changing CI behavior.</reversibility>
  <files>.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md</files>
  <read_first>
    - .planning/phases/132-measured-closure/132-CONTEXT.md
    - .planning/phases/132-measured-closure/132-VALIDATION.md
    - .planning/phases/132-measured-closure/132-01-SUMMARY.md
    - .github/workflows/ci.yml
    - scripts/ci/eval_ci_summary.sh
    - scripts/ci/test_ci_summary_gate.sh
    - test/install_smoke/ci_lane_split_test.exs
    - test/install_smoke/ci_observability_test.exs
    - scripts/setup_branch_protection.sh
    - RUNNING.md
  </read_first>
  <action>
Create `132-PRESERVATION-RECEIPT.md` with an immutable implementation identity section containing the Plan 01 commit SHA and its parent. Run the two focused ExUnit policy files together, then run `scripts/ci/test_ci_summary_gate.sh`. Record command, exit status, and test totals without inventing counts. Record direct contract findings: workflow filename `ci.yml`, workflow `name: CI`, job display name `CI Summary`, exact `ci-summary.needs` membership, `success|skipped` evaluator semantics, independently starting `package-consumer`, included `adoption-demo-e2e-smoke`, and branch protection's single `CI Summary` context. Compute the implementation commit's name-only diff from its recorded parent to SHA and record every path; it must contain only the Plan 01 source and test paths. This task is evidence-only and must not edit workflow, evaluator, branch-protection, or test sources per D-01.
  </action>
  <acceptance_criteria>
    - Both CI contract ExUnit files pass in one invocation and `bash scripts/ci/test_ci_summary_gate.sh` exits zero.
    - The receipt records the exact implementation SHA, parent SHA, and a two-path implementation diff limited to `workspace.ex` and `generated_app_smoke_test.exs`.
    - The receipt states that `CI Summary` remains the sole required context; all current merge-blocking inputs, the independently starting image `package-consumer`, adoption smoke, and skip-as-pass evaluator remain present.
    - No `pull_request` title, body, label, or branch value is interpolated into a shell command, and no workflow or branch-protection mutation is introduced.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/install_smoke/ci_lane_split_test.exs test/install_smoke/ci_observability_test.exs &amp;&amp; bash scripts/ci/test_ci_summary_gate.sh &amp;&amp; test -s .planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md</automated>
  </verify>
  <done>The durable receipt proves the required gate, job membership, skip behavior, observability, and bounded correction diff remain exact per D-01.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run coverage, quality, SAFE-01, and packed-consumer authorities</name>
  <reversibility rating="reversible">This task appends reproducible evidence to one phase receipt and changes no product or CI contract.</reversibility>
  <files>.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md</files>
  <read_first>
    - .planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
    - .planning/phases/132-measured-closure/132-CONTEXT.md
    - .planning/phases/132-measured-closure/132-RESEARCH.md
    - .planning/phases/131-signal-preserving-ci-velocity/131-IMPLEMENTATION-VERIFICATION.md
    - RUNNING.md
    - mix.exs
    - coveralls.json
    - scripts/maintainer/refactor_contract.sh
    - scripts/install_smoke.sh
    - test/install_smoke/generated_app_smoke_test.exs
  </read_first>
  <action>
Run, in order, `mix quality_signals`, `bash scripts/maintainer/refactor_contract.sh`, the single authoritative `mix coveralls.multiple --type local --type json` invocation, and `bash scripts/install_smoke.sh image`. Do not run a second coverage suite, add tests for percentage movement, or use a local shortcut for the image proof. From `cover/excoveralls.json`, count non-null coverage entries as relevant lines and positive entries as covered lines; record both counts and covered/relevant * 100 to four decimals. Enforce the inclusive D-07 threshold using integer arithmetic `covered * 10000 >= relevant * 8213`. Append all commands, observed exit statuses, real test totals, coverage arithmetic, package provenance/compile/migration/boot/lifecycle results, and the bounded prohibited-surface review to the receipt. The review must explicitly cover Admin, public functions, schema/migration, telemetry/error, dependency manifests/lock, `.github/workflows`, and release proof. Stop rather than rationalize any drift or any coverage result below 82.13% per D-06 and D-07.
  </action>
  <acceptance_criteria>
    - `mix quality_signals` and `bash scripts/maintainer/refactor_contract.sh` pass against the final correction.
    - The authoritative coverage command runs exactly once, exits zero, produces non-empty `cover/excoveralls.json`, and integer threshold arithmetic proves coverage is at least 82.13% inclusive.
    - `bash scripts/install_smoke.sh image` passes the same clean-room package provenance, compile, migration, boot, schema ownership, and lifecycle assertions as the tracer.
    - The receipt records an explicit no-drift finding for Admin, public API, schema/migration, telemetry/error, dependency set, workflow/release proof, or stops with the exact unexpected paths and behavior.
    - The receipt distinguishes automated command evidence from the bounded review and contains no fabricated counts.
  </acceptance_criteria>
  <verify>
    <automated>mix quality_signals &amp;&amp; bash scripts/maintainer/refactor_contract.sh &amp;&amp; mix coveralls.multiple --type local --type json &amp;&amp; test -s cover/excoveralls.json &amp;&amp; jq -e '[.source_files[].coverage[] | select(. != null)] as $lines | ([$lines[] | select(. &gt; 0)] | length) as $covered | ($lines | length) as $relevant | ($relevant &gt; 0 and ($covered * 10000 &gt;= $relevant * 8213))' cover/excoveralls.json &amp;&amp; bash scripts/install_smoke.sh image</automated>
  </verify>
  <done>The final correction has a durable green COV-05/SAFE-02 preservation receipt satisfying D-06 and D-07; only external CI-14 timing remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow jobs -> CI Summary | A required result could be omitted or treated as passing incorrectly. |
| implementation commit -> preservation receipt | A broader diff could be misrepresented as the narrow correction. |
| generated coverage/package artifacts -> phase evidence | Missing or stale output could be reported as a current pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-132-01 | Elevation of privilege | `ci-summary.needs` and evaluator | high | mitigate | Run topology and summary-gate contracts and record the exact required-job census. |
| T-132-02 | Tampering | implementation diff and proof outputs | high | mitigate | Bind evidence to exact commit/parent SHAs, list every changed path, and require fresh non-empty outputs. |
| T-132-03 | Repudiation | preservation claims | medium | mitigate | Record commands, exit statuses, real counts, coverage arithmetic, and unexpected drift rather than narrative-only claims. |
| T-132-04 | Tampering | PR metadata shell interpolation | medium | mitigate | Make no workflow change; confirm the bounded diff and existing trusted `github.event_name` concurrency input remain unchanged. |
</threat_model>

<verification>
Focused CI contracts must pass before the heavier census. The complete receipt must show fresh success for quality signals, SAFE-01, one authoritative coverage run at >=82.13%, the image package proof, and the exact two-file implementation diff.
</verification>

<success_criteria>
- D-01 gate topology and skip semantics are unchanged.
- D-06's focused, quality, SAFE-01, package, and bounded-diff layers all pass.
- D-07 coverage remains >=82.13% without percentage-only tests.
- All SAFE-02 probe edges are explicit backstops, not silent omissions.
</success_criteria>

<output>
Create `.planning/phases/132-measured-closure/132-02-SUMMARY.md` when done.
</output>

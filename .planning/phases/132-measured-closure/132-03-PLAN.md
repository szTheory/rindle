---
phase: 132-measured-closure
plan: 03
type: execute
wave: 3
depends_on: [132-01, 132-02]
files_modified:
  - .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
autonomous: false
requirements: [CI-14]
must_haves:
  truths:
    - "A fresh sequence of ten successful, non-cancelled, first-attempt `pull_request` runs belongs to one immutable corrected head and measures workflow start through `CI Summary` completion per D-04."
    - "The fresh median is at most 480 seconds and nearest-rank p95 is at most 600 seconds; both thresholds must pass per D-05."
    - "The original `005beae2...` failed receipt remains intact as baseline evidence and is not mixed with the corrected-head sample."
    - "Any external-runner exception has job-level evidence, a named owner, and a dated follow-up; a slow result alone is never an exception per D-03."
    - statement: "CI-14 adjacency backstop: median exactly 480 seconds and nearest-rank p95 exactly 600 seconds pass because both thresholds are inclusive."
      verification: backstop
    - statement: "CI-14 empty backstop: zero, one, nine, or otherwise partial qualifying samples cannot close the requirement; exactly ten consecutive qualifying samples are required."
      verification: backstop
    - statement: "CI-14 ordering backstop: receipt rows preserve chronological trigger order, while statistics use a separate ascending duration order with median ranks 5/6 and p95 rank 10."
      verification: backstop
    - statement: "CI-14 concurrency backstop: the next labeled PR run starts only after the prior run completes; cancelled, interrupted, overlapping, rerun, or different-head samples invalidate the sequence unless D-03 evidence explicitly qualifies an exception."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT erase, rewrite, or blend away the failed immutable-head baseline when adding the corrected-head result."
      category: transparency
      status: unresolved
      verification: null
      flagged_unverified: true
    - statement: "MUST NOT cherry-pick timing samples, include reruns or mixed heads, or relabel an ordinary slow result as an infrastructure exception."
      category: transparency
      status: unresolved
      verification: null
      flagged_unverified: true
  artifacts:
    - path: ".planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md"
      provides: "Preserved failed baseline plus corrected immutable-head ten-run table, calculations, verdict, and source links"
      contains: "Fresh corrected-head receipt"
  key_links:
    - from: "GitHub Actions `pull_request` runs"
      to: ".planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md"
      via: "read-only `gh api` run/job payloads filtered by SHA, attempt, conclusion, cancellation, and `CI Summary`"
      pattern: "run_attempt.*CI Summary.*completed_at"
    - from: ".planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md"
      to: ".planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md"
      via: "the measured SHA must be the same correction already accepted by the preservation census"
      pattern: "Implementation SHA"
---

<objective>
Replace the open CI-14 measurement with a fresh, comparable, immutable-head ten-run result while preserving the failed baseline and refusing unsupported exclusions.

Purpose: Close the phase only on live required-gate evidence satisfying D-03, D-04, and D-05.
Output: An appended corrected-head section in `132-CI-TIMING-RECEIPT.md`, or an explicit still-open result that routes back to evidence-guided replanning.
</objective>

## Artifacts this phase produces

This plan creates no module, function, CLI flag, struct field, or new path. It updates the existing `132-CI-TIMING-RECEIPT.md` by preserving the failed baseline verbatim and appending the fresh corrected-head evidence.

No external API integration: GitHub Actions data is read-only evidence collection through the repository's existing `gh` tooling; this phase creates no API client, credentials, endpoint, webhook, or product integration.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/132-measured-closure/132-CONTEXT.md
@.planning/phases/132-measured-closure/132-RESEARCH.md
@.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
@.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
@.planning/phases/132-measured-closure/132-01-SUMMARY.md
@.planning/phases/132-measured-closure/132-02-SUMMARY.md
@scripts/ci/collect_ci_baseline.sh
@test/install_smoke/ci_observability_test.exs
@.github/workflows/ci.yml
@guides/release_publish.md
</context>

<assumptions>
- Flagged unresolved CI-14 probe row is resolved only by the exact live receipt contract below; neither local timing nor a green p95 alone is accepted.
- PR #96 remains the measurement PR, and the existing `ci-timing-sample` label is the harmless `pull_request:labeled` trigger. The checkpoint verifies both facts before collecting evidence rather than silently assuming them.
- The corrected implementation SHA recorded by Plan 02 has been pushed to PR #96 and will remain immutable throughout the sample.
- The SAFE-02 adjacency/empty/ordering/concurrency rows are reflected in the flat backstop truths above and in the live sample protocol.
- Prohibition recall retains evidence-history preservation and anti-cherry-picking as bespoke transparency constraints. GitHub authorization, shell injection, and required-proof bypass remain security canon in the threat model.
</assumptions>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Produce ten sequential labeled PR runs on the immutable corrected head</name>
  <reversibility rating="reversible">Adding and removing the existing sampling label triggers CI without changing code, branch protection, or published contracts.</reversibility>
  <files>.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md</files>
  <read_first>
    - .planning/phases/132-measured-closure/132-CONTEXT.md
    - .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
    - .planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
    - .planning/phases/132-measured-closure/132-02-SUMMARY.md
    - .github/workflows/ci.yml
    - scripts/ci/collect_ci_baseline.sh
    - test/install_smoke/ci_observability_test.exs
  </read_first>
  <action>
Before triggering, compare `gh pr view 96 --repo szTheory/rindle --json headRefOid,state,isDraft` with the 40-character implementation SHA recorded in `132-PRESERVATION-RECEIPT.md`; stop if they differ or the PR is closed. For each of ten samples, add the existing `ci-timing-sample` label to PR #96, confirm exactly one new workflow run whose event is `pull_request`, head SHA is the immutable implementation SHA, and attempt is 1, then remove the label without triggering an unlabeled workflow. Wait until that run and its `CI Summary` job complete before starting the next sample. If a run is cancelled, rerun, overlaps, uses another SHA, or fails without a D-03 exception record, discard the incomplete sequence and restart the ten-run sequence; do not cherry-pick around it. Do not use `workflow_dispatch`, rerun buttons, branch pushes, or topology changes as samples. This is a blocking external checkpoint because ten live runner executions cannot be established locally.
  </action>
  <instructions>
Use PR #96 and the existing `ci-timing-sample` label. Repeat this exact sequence ten times: add the label, capture the newly created `pull_request` Actions run URL, remove the label, wait for the run and `CI Summary` to finish successfully, then begin the next iteration. Do not continue if the PR head changes. Report the first and last run URLs when the ten-run sequence is complete.
  </instructions>
  <acceptance_criteria>
    - PR #96's `headRefOid` exactly equals the preserved correction SHA before sample 1 and after sample 10.
    - Ten chronologically consecutive runs have event `pull_request`, the same full head SHA, `run_attempt: 1`, conclusion `success`, no cancellation, and a successful `CI Summary` job.
    - Each next run starts at or after the prior run's completion; no overlapping sample is accepted.
    - Every exception, if any, contains job-level evidence, a named owner, and a dated follow-up; absent all three, the sequence restarts.
  </acceptance_criteria>
  <verify>
    <automated>test "$(gh pr view 96 --repo szTheory/rindle --json state --jq .state)" = OPEN &amp;&amp; gh label list --repo szTheory/rindle --limit 100 --json name --jq '.[].name' | grep -Fx ci-timing-sample</automated>
  </verify>
  <verification>
The executor must use read-only `gh run view`/`gh api` checks to confirm ten distinct run IDs, one unchanged full head SHA, attempt 1, successful conclusions, successful `CI Summary` jobs, and non-overlapping chronology before accepting the checkpoint response.
  </verification>
  <done>Ten sequential qualifying live PR runs exist for one immutable corrected head and are ready for read-only receipt calculation.</done>
  <resume-signal>Type "runs complete" with the first and last Actions run URLs, or describe the failed/cancelled run that forced a restart.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Append the corrected-head receipt and enforce both percentile gates</name>
  <reversibility rating="reversible">The append-only evidence update can be corrected in git while the preserved source URLs and prior baseline remain independently auditable.</reversibility>
  <precondition>PR #96 exposes ten completed qualifying first-attempt pull_request runs for the exact implementation SHA recorded in 132-PRESERVATION-RECEIPT.md.</precondition>
  <files>.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md</files>
  <read_first>
    - .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
    - .planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
    - .planning/phases/132-measured-closure/132-CONTEXT.md
    - .planning/REQUIREMENTS.md
    - scripts/ci/collect_ci_baseline.sh
    - test/install_smoke/ci_observability_test.exs
    - .github/workflows/ci.yml
  </read_first>
  <action>
Use only read-only `gh api` GET requests. Query `ci.yml` pull-request runs for PR #96's head branch, flatten paginated results once, and filter to the exact 40-character preserved implementation SHA, `run_attempt == 1`, conclusion `success`, and the checkpoint's fresh chronological window. For each run ID query jobs, require exactly one successful job named `CI Summary`, and compute whole seconds from workflow `run_started_at` through that job's `completed_at`. Verify chronological non-overlap and preserve sequence order in the table. Separately sort the ten seconds values numerically; calculate median as `(rank5 + rank6) / 2` and nearest-rank p95 as rank `ceil(10 * 0.95) = 10`. Append a `Fresh corrected-head receipt` section with full SHA, PR/run links, UTC starts, duration seconds plus human form, attempt, result, exception disposition, sorted values, arithmetic, and target/observed/verdict table. Preserve every existing failed-baseline row, method statement, result, qualification-run note, and open disposition verbatim above the append. Mark CI-14 and phase closure passing only if median <=480 and p95 <=600. If either fails, append an honest `target missed` result, leave CI-14 open, and return a checkpoint for new evidence-guided planning rather than weakening proof.
  </action>
  <acceptance_criteria>
    - The existing failed `005beae2ac335b5d896077aeb7996da4263f05ba` ten-run baseline and its 515.5s/544s verdict remain present and unchanged.
    - The new section contains exactly ten source-linked rows for one corrected full SHA, all `pull_request`, successful, non-cancelled, attempt 1, chronologically sequential, and measured through successful `CI Summary` completion.
    - The receipt shows the ten sorted integer durations, explicit `(rank5 + rank6) / 2` median arithmetic, rank-10 nearest-rank p95 arithmetic, and inclusive target comparisons.
    - CI-14 is marked passing only when median <=480 seconds and p95 <=600 seconds; partial, rerun-inclusive, mixed-head, overlapping, or p95-only evidence remains open.
    - Any excluded external-runner event includes job-level evidence, a named owner, and a dated follow-up; otherwise no exception is claimed.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md &amp;&amp; grep -F '515.5s' .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md &amp;&amp; grep -F 'Fresh corrected-head receipt' .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md &amp;&amp; grep -F 'Median' .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md &amp;&amp; grep -F 'P95' .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md</automated>
  </verify>
  <done>The preserved baseline plus fresh corrected-head receipt proves median <=480s and nearest-rank p95 <=600s on ten qualifying runs; otherwise execution stops with CI-14 explicitly open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub PR events -> Actions runs | Mutable labels trigger external runner state for an otherwise immutable implementation SHA. |
| Actions API -> receipt | Untrusted/mixed run metadata must be filtered before it becomes acceptance evidence. |
| exception narrative -> milestone status | A claimed runner incident could improperly erase a real performance miss. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-132-02 | Tampering | run selection and percentile inputs | high | mitigate | Bind all rows to one full SHA; require event, attempt, conclusion, cancellation, sequentiality, and `CI Summary` checks; show sorted values and arithmetic. |
| T-132-03 | Repudiation | external-runner exception | high | mitigate | Require job-level evidence, a named owner, and a dated follow-up; otherwise restart or record the miss. |
| T-132-04 | Tampering | PR metadata in shell/API commands | medium | mitigate | Use fixed repository, numeric PR/run IDs, full validated SHA, and existing label name; never interpolate title/body/branch text into shell. |
| T-132-01 | Elevation of privilege | required proof status | high | mitigate | Accept only successful `CI Summary` completion from the unchanged required path and cross-link the Plan 02 preservation receipt. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Close measured median gap, preserve ratchets, replace failed receipt | 01-03 | COVERED | Tracer correction -> preservation census -> live receipt. |
| REQ | CI-14 | Ten comparable runs at <=480s median and <=600s p95 | 01, 03 | COVERED | Causal tracer plus exact live acceptance. |
| REQ | COV-05 | Coverage >=82.13% without coverage chasing | 02 | COVERED | Single authoritative run and integer threshold arithmetic. |
| REQ | SAFE-02 | Focused, quality, SAFE-01, package, and drift proof | 01, 02 | COVERED | All preservation layers and probe edges are explicit. |
| RESEARCH | — | Package consumer is long pole; remove pre-patch generator install duplication | 01 | COVERED | Treated as hypothesis, not guaranteed speedup. |
| RESEARCH | — | Preserve patch -> deps.get -> compile and full image proof | 01, 02 | COVERED | End-to-end tracer and repeated phase gate. |
| RESEARCH | — | Preserve failed baseline and append distinct fresh receipt | 03 | COVERED | Append-only evidence method. |
| CONTEXT | D-01 | Sole `CI Summary`, exact jobs, skip-as-pass | 01, 02 | COVERED | No workflow edit plus focused contract census. |
| CONTEXT | D-02 | Evidence-guided smallest existing-step correction | 01 | COVERED | One generator argv removal only. |
| CONTEXT | D-03 | Accountable runner exceptions only | 03 | COVERED | Blocking checkpoint and receipt rule. |
| CONTEXT | D-04 | Ten first-attempt same-head PR runs | 03 | COVERED | Sequential labeled-event protocol. |
| CONTEXT | D-05 | Both median and nearest-rank p95 thresholds | 03 | COVERED | Inclusive arithmetic and fail-open disposition. |
| CONTEXT | D-06 | Full preservation re-census | 02 | COVERED | Durable preservation receipt. |
| CONTEXT | D-07 | Coverage >=82.13%; no percentage-only tests | 02 | COVERED | Exact gate and prohibition. |

Deferred partitions, cache redesign, broad toolchain/dependency work, topology changes, proof removal, and rerun-based improvement are intentionally excluded per CONTEXT.md.

<verification>
Confirm the external checkpoint produced ten sequential eligible runs, reproduce every row from read-only API payloads, retain the old failed evidence, and independently recompute the median and p95 from the printed sorted values.
</verification>

<success_criteria>
- Exactly ten qualifying runs from one immutable corrected SHA are documented.
- Median <=480s and nearest-rank p95 <=600s both pass.
- Original failed receipt remains fully auditable.
- No unsupported exception, mixed head, rerun, cancellation, overlap, or local timing enters the result.
</success_criteria>

<output>
Create `.planning/phases/132-measured-closure/132-03-SUMMARY.md` only after the live receipt passes both targets; otherwise return the open CI-14 evidence without a completion claim.
</output>

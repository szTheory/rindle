---
phase: 132-measured-closure
plan: 14
type: execute
wave: 13
depends_on: [132-12, 132-13]
files_modified:
  - .planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
autonomous: true
gap_closure: true
requirements: [CI-14, COV-05, SAFE-02]
must_haves:
  truths:
    - "The final code-bearing subject includes only the API-revalidation correction, its regression, and the formatter-only failed-run remediation beyond the already-preserved D-08/D-09 topology."
    - "Formatter, focused controller/topology/summary, `mix quality_signals`, SAFE-01, automation-first, hygiene, one authoritative coverage run, and the packed image consumer are all green after the final source edit and before sampling per D-01, D-06, and D-09."
    - "Fresh ExCoveralls evidence has a positive relevant-line denominator and satisfies the inclusive 82.13% floor without a percentage-only test per D-07."
    - "A no-publish preflight binds the correction SHA and final preserved subject to PR #96, `ci.yml`, `CI Summary`, exact ten samples, two-sequence ceiling, and inclusive 480/600 thresholds without launching a run."
    - statement: "SAFE-02 adjacency/empty/ordering backstop: equality at 82.13, 480, or 600 passes; empty coverage, missing current prerequisites, or non-ten receipt input fails; preservation precedes sampling."
      verification: backstop
    - statement: "SAFE-02 concurrency backstop: the controller suite proves interruption resume and SHA-scoped locking before the single terminal collector is authorized."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT launch, label-trigger, rerun, or sample before every final-subject authority and no-publish preflight is green."
      category: safety
      status: unverified
      flagged_unverified: true
    - statement: "MUST NOT add coverage-only tests or describe product, API, schema/migration, telemetry/error, dependency, Admin, release, or broader workflow drift as preserved."
      category: transparency
      status: unverified
      flagged_unverified: true
    - statement: "MUST NOT treat historical preservation evidence as current after the controller and formatter corrections."
      category: transparency
      status: unverified
      flagged_unverified: true
  artifacts:
    - path: ".planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md"
      provides: "Final correction/preserved SHAs, bounded diff, ordered authorities, coverage census, and no-trigger preflight"
      contains: "82.13"
  key_links:
    - from: "cover/excoveralls.json"
      to: ".planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md"
      via: "fail-closed covered/relevant integer arithmetic"
      pattern: "covered.*relevant"
    - from: ".planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md"
      to: "scripts/ci/collect_pr_timing_receipt.sh"
      via: "recorded full correction and preserved-subject SHAs become terminal sampler inputs"
      pattern: "correction.*SHA|preserved.*subject"
---

<objective>
Bind both gap corrections to one immutable, fully re-proven subject before live sampling.

Purpose: Ensure CI-14 measurement cannot conceal quality, coverage, topology, formatter, controller, or prohibited-surface drift.
Output: A fresh final preservation census and mutation-free sampler preflight.
</objective>

## Artifacts this phase produces

- Appended `Final post-verification-gap preservation census` in `132-PRESERVATION-RECEIPT.md`: full controller/formatter correction SHA, preserved subject SHA, exact bounded diff, command results, covered/relevant counts and ratio, inclusive threshold result, prohibited-surface census, and no-publish preflight result.
- No new or modified production symbol, test helper, CLI flag, receipt schema field, dependency, workflow job, or public API.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RUNNING.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/132-measured-closure/132-CONTEXT.md
@.planning/phases/132-measured-closure/132-VERIFICATION.md
@.planning/phases/132-measured-closure/132-12-SUMMARY.md
@.planning/phases/132-measured-closure/132-13-SUMMARY.md
@.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
@.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
@scripts/ci/collect_pr_timing_receipt.sh
@test/install_smoke/ci_timing_automation_test.exs
@test/install_smoke/ci_lane_split_test.exs
@.github/workflows/ci.yml
</context>

<assumptions>
- CI-14 generic probe: this plan proves immutable readiness and a mutation-free no-publish preflight, but only dependent Plan 132-15's API-backed exact-ten receipt may resolve the timing verdict.
- COV-05 generic probe: this plan resolves it with exactly one fresh `mix coveralls.multiple --type local --type json` artifact, structural parsing, a positive relevant-line denominator, and inclusive `covered * 10000 >= relevant * 8213` arithmetic.
- SAFE-02 adjacency probe: the ordered preservation authorities prove equality at 82.13% passes, while the focused controller suite preserves inclusive 480/600 behavior for Plan 132-15's live check.
- SAFE-02 empty-input probe: fail-closed ExCoveralls parsing rejects missing, empty, malformed, or zero-denominator coverage, and no-publish preflight rejects absent current prerequisites or a sample count other than ten.
- SAFE-02 ordering probe: this plan records that all final-source authorities complete before preflight and sampling; the focused suite proves numeric tie/rank behavior remains deterministic.
- SAFE-02 concurrency probe: the controller suite is the automated authority for SHA-scoped locking and interruption/resume before the terminal collector; no-publish preflight must leave label, state, lock, workflow runs, and current receipt markers unchanged.
- Descriptor-less prohibitions remain flagged-unverified; no check descriptor or silent green status is invented for them.
</assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Re-census and preflight the final immutable correction subject</name>
  <reversibility rating="reversible">This task records reproducible evidence in one planning artifact and changes no published or persisted contract.</reversibility>
  <files>.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md</files>
  <read_first>
    - AGENTS.md
    - RUNNING.md
    - .planning/phases/132-measured-closure/132-CONTEXT.md
    - .planning/phases/132-measured-closure/132-VERIFICATION.md
    - .planning/phases/132-measured-closure/132-12-SUMMARY.md
    - .planning/phases/132-measured-closure/132-13-SUMMARY.md
    - .planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
    - .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
    - .github/workflows/ci.yml
    - scripts/ci/collect_pr_timing_receipt.sh
    - scripts/ci/test_ci_summary_gate.sh
    - scripts/maintainer/refactor_contract.sh
    - scripts/install_smoke.sh
    - test/install_smoke/ci_timing_automation_test.exs
    - test/install_smoke/ci_lane_split_test.exs
    - test/install_smoke/ci_observability_test.exs
  </read_first>
  <action>
Record the full code-bearing correction SHA after 132-12/13 and the final preserved HEAD. Run, in order after all source edits: repository formatter check; controller/lane-split/observability focused tests; Bash summary gate; `mix quality_signals`; SAFE-01 refactor contract; automation-first; CI hygiene; exactly one `mix coveralls.multiple --type local --type json`; fail-closed JSON parsing; and complete packed image consumer. Parse only a fresh non-empty ExCoveralls artifact, validate `source_files` and every coverage vector/value, reject a zero denominator, and compare covered/relevant integers inclusively to 8213 basis points per D-07. Append observed commands/results and an exact name/status diff that permits only the 132-12 controller/test, 132-13 formatter test, and planning evidence beyond the D-08/D-09 subject. Census workflow/topology, Admin, public API, schema/migration, telemetry/error, dependency/lockfile, and release surfaces per D-01, D-06, D-08, and D-09.

Run one controller `preflight --no-publish` with repository `szTheory/rindle`, PR `96`, workflow `ci.yml`, summary `CI Summary`, label `ci-timing-sample`, exactly ten samples, at most two sequences, inclusive 480/600 limits, and the recorded full correction/preserved SHAs. It may query authenticated GitHub state but must not push, label, trigger, rerun, create sampler state, or alter the receipt. A remote-head mismatch is recorded fail-closed preflight evidence; every local implementation/preservation assertion must already be green before Plan 132-15.
  </action>
  <acceptance_criteria>
    - All ordered local authorities exit zero after the final source edit; formatter cleanliness is explicitly included.
    - One coverage invocation yields a positive denominator and passes `covered * 10000 >= relevant * 8213`; no percentage-only test is added.
    - Exact diff census contains only declared correction/evidence surfaces and retains D-01/D-08/D-09 topology, job bodies, required set, and summary semantics.
    - No-publish preflight binds exact immutable inputs and leaves workflow listings, owned label, state directory, lock, and receipt current markers unchanged.
    - `COVERAGE.md` remains a validated reasoned no-new-external-integration declaration: this phase hardens an existing `gh` evidence path and adds no API client/capability surface.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
mix format --check-formatted
mix test test/install_smoke/ci_timing_automation_test.exs test/install_smoke/ci_lane_split_test.exs test/install_smoke/ci_observability_test.exs --seed 0
bash scripts/ci/test_ci_summary_gate.sh
mix quality_signals
bash scripts/maintainer/refactor_contract.sh
./scripts/maintainer/automation_first_contract.sh
./scripts/maintainer/repo_hygiene_check.sh --ci
mix coveralls.multiple --type local --type json
test -s cover/excoveralls.json
bash scripts/install_smoke.sh image
git diff --check

REPO=szTheory/rindle
PR=96
WORKFLOW=ci.yml
SUMMARY_JOB='CI Summary'
LABEL=ci-timing-sample
SAMPLES=10
MAX_SEQUENCES=2
MEDIAN_MAX=480
P95_MAX=600
RECEIPT=.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
PRESERVATION=.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
STATE_DIR=.gsd/ci-timing/phase-132-final-preflight
CORRECTION_SHA=$(awk -F'`' '/^\*\*Correction SHA:\*\*/ {sha=$2} END {print sha}' "$PRESERVATION")
PRESERVED_SHA=$(awk -F'`' '/^\*\*Preserved subject SHA:\*\*/ {sha=$2} END {print sha}' "$PRESERVATION")
[[ "$CORRECTION_SHA" =~ ^[0-9a-f]{40}$ ]]
[[ "$PRESERVED_SHA" =~ ^[0-9a-f]{40}$ ]]
HEAD_SHA=$(git rev-parse HEAD)
git merge-base --is-ancestor "$CORRECTION_SHA" "$HEAD_SHA"
git merge-base --is-ancestor "$PRESERVED_SHA" "$HEAD_SHA"
PR_JSON_BEFORE=$(gh pr view "$PR" --repo "$REPO" --json headRefName,headRefOid,labels)
HEAD_REF=$(jq -r .headRefName &lt;&lt;&lt;"$PR_JSON_BEFORE")
ENCODED_HEAD_REF=$(jq -rn --arg value "$HEAD_REF" '$value | @uri')
REMOTE_HEAD_BEFORE=$(jq -r .headRefOid &lt;&lt;&lt;"$PR_JSON_BEFORE")
LABELS_BEFORE=$(jq -c '[.labels[].name] | sort' &lt;&lt;&lt;"$PR_JSON_BEFORE")
WORKFLOW_IDS_BEFORE=$(gh api --paginate -H 'Accept: application/vnd.github+json' "repos/${REPO}/actions/workflows/${WORKFLOW}/runs?event=pull_request&amp;branch=${ENCODED_HEAD_REF}&amp;per_page=100" | jq -sc --arg sha "$HEAD_SHA" --argjson pr "$PR" '[.[].workflow_runs[] | select(.event == "pull_request" and .head_sha == $sha and .run_attempt == 1 and any(.pull_requests[]?; .number == $pr)) | .id] | sort')
STATE_FILE="$STATE_DIR/pr-${PR}-${HEAD_SHA}.json"
LOCK_DIR="${STATE_FILE}.lock"
STATE_PRESENT_BEFORE=$([ -e "$STATE_FILE" ] &amp;&amp; echo 1 || echo 0)
STATE_HASH_BEFORE=$([ -f "$STATE_FILE" ] &amp;&amp; git hash-object "$STATE_FILE" || echo absent)
LOCK_PRESENT_BEFORE=$([ -e "$LOCK_DIR" ] &amp;&amp; echo 1 || echo 0)
LOCK_HASH_BEFORE=$([ -f "$LOCK_DIR/pid" ] &amp;&amp; git hash-object "$LOCK_DIR/pid" || echo absent)
RECEIPT_HASH_BEFORE=$(git hash-object "$RECEIPT")
CURRENT_COUNTS_BEFORE=$(for marker in CI_TIMING_CURRENT_TABLE_BEGIN CI_TIMING_CURRENT_TABLE_END CI_TIMING_CURRENT_SOURCE_BEGIN CI_TIMING_CURRENT_SOURCE_END; do grep -c "^${marker}$" "$RECEIPT" || true; done | paste -sd, -)
CURRENT_HASH_BEFORE=$(awk '/^CI_TIMING_CURRENT_TABLE_BEGIN$/{take=1} take{print} /^CI_TIMING_CURRENT_SOURCE_END$/{take=0}' "$RECEIPT" | git hash-object --stdin)

bash scripts/ci/collect_pr_timing_receipt.sh preflight --repo "$REPO" --pr "$PR" --workflow "$WORKFLOW" --summary-job "$SUMMARY_JOB" --label "$LABEL" --samples "$SAMPLES" --max-sequences "$MAX_SEQUENCES" --median-max "$MEDIAN_MAX" --p95-max "$P95_MAX" --correction-sha "$CORRECTION_SHA" --preserved-subject-sha "$PRESERVED_SHA" --receipt "$RECEIPT" --state-dir "$STATE_DIR" --no-publish

PR_JSON_AFTER=$(gh pr view "$PR" --repo "$REPO" --json headRefName,headRefOid,labels)
test "$(jq -r .headRefOid &lt;&lt;&lt;"$PR_JSON_AFTER")" = "$REMOTE_HEAD_BEFORE"
test "$(jq -c '[.labels[].name] | sort' &lt;&lt;&lt;"$PR_JSON_AFTER")" = "$LABELS_BEFORE"
test "$(gh api --paginate -H 'Accept: application/vnd.github+json' "repos/${REPO}/actions/workflows/${WORKFLOW}/runs?event=pull_request&amp;branch=${ENCODED_HEAD_REF}&amp;per_page=100" | jq -sc --arg sha "$HEAD_SHA" --argjson pr "$PR" '[.[].workflow_runs[] | select(.event == "pull_request" and .head_sha == $sha and .run_attempt == 1 and any(.pull_requests[]?; .number == $pr)) | .id] | sort')" = "$WORKFLOW_IDS_BEFORE"
test "$([ -e "$STATE_FILE" ] &amp;&amp; echo 1 || echo 0)" = "$STATE_PRESENT_BEFORE"
test "$([ -f "$STATE_FILE" ] &amp;&amp; git hash-object "$STATE_FILE" || echo absent)" = "$STATE_HASH_BEFORE"
test "$([ -e "$LOCK_DIR" ] &amp;&amp; echo 1 || echo 0)" = "$LOCK_PRESENT_BEFORE"
test "$([ -f "$LOCK_DIR/pid" ] &amp;&amp; git hash-object "$LOCK_DIR/pid" || echo absent)" = "$LOCK_HASH_BEFORE"
test "$(git hash-object "$RECEIPT")" = "$RECEIPT_HASH_BEFORE"
test "$(for marker in CI_TIMING_CURRENT_TABLE_BEGIN CI_TIMING_CURRENT_TABLE_END CI_TIMING_CURRENT_SOURCE_BEGIN CI_TIMING_CURRENT_SOURCE_END; do grep -c "^${marker}$" "$RECEIPT" || true; done | paste -sd, -)" = "$CURRENT_COUNTS_BEFORE"
test "$(awk '/^CI_TIMING_CURRENT_TABLE_BEGIN$/{take=1} take{print} /^CI_TIMING_CURRENT_SOURCE_END$/{take=0}' "$RECEIPT" | git hash-object --stdin)" = "$CURRENT_HASH_BEFORE"</automated>
  </verify>
  <done>The exact final subject has fresh COV-05/SAFE-02 proof, a green controller/formatter boundary, and mutation-free immutable sampler preflight evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (ASVS L1; high severity blocks execution)

| Boundary | Description |
| --- | --- |
| final source -> preservation receipt | Evidence must bind the actual last code-bearing subject. |
| coverage JSON -> COV-05 verdict | Generated counts are untrusted until structurally parsed and compared with integer arithmetic. |
| local preflight -> GitHub | Read-only checks must not cross into publication or sampling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-132-14-01 | Tampering | subject identity/diff | high | mitigate | Record full SHAs and exact name/status range before accepting evidence. |
| T-132-14-02 | Repudiation | coverage verdict | high | mitigate | Require one fresh non-empty artifact, validated integer vectors, positive denominator, and inclusive arithmetic. |
| T-132-14-03 | Elevation of privilege | required CI topology | high | mitigate | Run topology/summary contracts and census workflow diff. |
| T-132-14-04 | Denial of service | premature sampler | medium | mitigate | Use preflight with `--no-publish` only after all local authorities pass. |
| T-132-14-05 | Information disclosure | evidence output | low | accept | Record public run/count metadata only; never record credentials. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Preserve ratchets before a fresh comparable receipt | 132-14, 132-15 | COVERED | Serial dependency blocks premature sampling.
REQ | CI-14 | Immutable ready-to-sample required path | 132-14, 132-15 | COVERED | Preflight then live evidence.
REQ | COV-05 | Authoritative coverage >=82.13% | 132-14 | COVERED | Single fresh artifact and integer check.
REQ | SAFE-02 | Focused/full preservation and bounded drift | 132-14 | COVERED | Ordered authorities and diff census.
RESEARCH | — | Preserve topology and use repository-native proof | 132-14 | COVERED | Existing commands only.
CONTEXT | D-01..D-03 | Required gate and evidence policy | 132-14 | COVERED | Source checks and no exception.
CONTEXT | D-04/D-05 | Exact-ten and inclusive thresholds | 132-14, 132-15 | COVERED | Preflight pins parameters.
CONTEXT | D-06/D-07 | Final preservation and coverage floor | 132-14 | COVERED | Direct task authorities.
CONTEXT | D-08/D-09 | Exact topology exception only | 132-14 | COVERED | Bounded census.
</source_audit>

<verification>
All listed authorities must pass in order on one final subject. The preflight is read-only and cannot satisfy CI-14.
</verification>

<success_criteria>
- Final controller and formatter corrections are bound to immutable evidence.
- COV-05 and SAFE-02 are freshly proven against that subject.
- The terminal sampler is authorized only after all preconditions are machine-green.
</success_criteria>

<output>
Create `.planning/phases/132-measured-closure/132-14-SUMMARY.md` when done.
</output>

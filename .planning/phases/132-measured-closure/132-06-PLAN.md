---
phase: 132-measured-closure
plan: 06
type: execute
wave: 6
depends_on: [132-05]
files_modified:
  - scripts/ci/install_apt_packages.sh
  - test/install_smoke/ci_cache_hygiene_test.exs
  - .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
autonomous: true
gap_closure: true
requirements: [CI-14, SAFE-02]
must_haves:
  truths:
    - "The failed immutable-head receipt remains intact and a machine-derived same-head causal census records the critical finisher and repeated apt setup cost before the correction per D-02."
    - "Existing requested apt packages are installed through a bounded install-first attempt; repository metadata is refreshed only after that attempt fails, before one bounded retry."
    - "Every required job, `CI Summary` dependency, skip-as-pass result, package/adopter proof, and release coupling remains unchanged per D-01."
    - statement: "SAFE-02 empty-input backstop: the apt helper still rejects an empty package list with exit 2, and the timing receipt still requires exactly ten qualifying rows."
      verification: backstop
    - statement: "SAFE-02 ordering backstop: an initial install is attempted before refresh, and a failed initial install is followed by refresh before the final install attempt."
      verification: backstop
    - statement: "SAFE-02 concurrency backstop: each helper invocation uses only job-local process state; timing samples remain sequential, first-attempt, and non-overlapping."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT shorten feedback by removing, skipping, renaming, or weakening a required proof or the sole required `CI Summary` result."
      category: safety
      status: unresolved
      verification: null
      flagged_unverified: true
    - statement: "MUST NOT present runner variance or a slow sample as an external-runner exception without job evidence, a named owner, and a dated follow-up."
      category: transparency
      status: unresolved
      verification: null
      flagged_unverified: true
  artifacts:
    - path: "scripts/ci/install_apt_packages.sh"
      provides: "Bounded install-first, refresh-on-failure apt package acquisition"
      contains: "apt-get install"
    - path: "test/install_smoke/ci_cache_hygiene_test.exs"
      provides: "Executable success and fallback-order contracts for the apt helper"
      contains: "install_apt_packages"
    - path: ".planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md"
      provides: "Durable gap-closure causal census for the failed immutable head"
      contains: "24c17783bbc080a085e398164450b7c3f475781e"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/install_apt_packages.sh"
      via: "required quality, integration, adoption, and consumer jobs invoke the shared package installer"
      pattern: "bash scripts/ci/install_apt_packages.sh"
    - from: "test/install_smoke/ci_cache_hygiene_test.exs"
      to: "scripts/ci/install_apt_packages.sh"
      via: "PATH-isolated fake sudo/timeout/apt-get processes assert command order and exit propagation"
      pattern: "install_apt_packages"
---

<objective>
Apply one evidence-guided median remediation to the repeated apt setup work on the existing required pull-request path.

Purpose: Close the 36.5-second CI-14 median miss by removing unconditional repository refreshes from repeated required-job package installs while preserving the exact proof topology and bounded failure recovery per D-01 and D-02.
Output: A durable same-head timing census, executable install/fallback contracts, and the smallest shared-helper correction.
</objective>

## Artifacts this phase produces

- Updated behavior in `scripts/ci/install_apt_packages.sh`: an install-first attempt and a refresh-then-install fallback, with the existing package arguments, network bounds, retry cap, and `--configure-only` interface retained.
- Two executable ExUnit behaviors in `test/install_smoke/ci_cache_hygiene_test.exs`: cached-index success without refresh, and initial-install failure followed by refresh and one retry.
- A `Gap-closure causal census` section in `132-CI-TIMING-RECEIPT.md` containing the measured-head SHA, ten source run IDs, critical-finisher counts, and native median/min/max evidence. No new module, public function, CLI flag, dependency, or receipt field is introduced.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/132-measured-closure/132-CONTEXT.md
@.planning/phases/132-measured-closure/132-RESEARCH.md
@.planning/phases/132-measured-closure/132-PATTERNS.md
@.planning/phases/132-measured-closure/132-VERIFICATION.md
@.planning/phases/132-measured-closure/132-05-SUMMARY.md
@.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
@.github/workflows/ci.yml
@scripts/ci/install_apt_packages.sh
@test/install_smoke/ci_cache_hygiene_test.exs
</context>

<assumptions>
- Flagged unresolved CI-14 probe row: its concrete contract is the immutable-head ten-run sample and inclusive median/p95 gate in D-04 and D-05; this plan changes only the causally repeated setup surface and cannot by itself close CI-14.
- The failed-head Actions census is authoritative read-only evidence: Adoption Demo E2E Smoke was last in 4/10 runs, Adopter was last in 6/10, and `Install libvips` had a 25-second median across 80 successful occurrences. The repeated unconditional `apt-get update` is therefore a bounded shared critical-chain target; the final ten-run receipt still decides sufficiency.
- COV-05 is not recalculated in this plan because no Elixir production/test behavior changes; Plan 132-07 performs the final authoritative one-run coverage census.
- SAFE-02 adjacency for equal timing values is not an apt-helper input shape; it remains the inclusive threshold/tie-handling receipt contract in Plan 132-08. Empty input, command ordering, and job-local concurrency are applicable and are explicitly backed above.
- Descriptor-less prohibitions are retained and flagged-unverified as required by the spec-less fallback. Generic command injection and proof bypass are security canon and are handled in the threat model.
- No one-way decision is introduced. The helper policy is reversible without migration or published-contract breakage.
</assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove and apply bounded install-first apt acquisition on the required path</name>
  <reversibility rating="reversible">The shell-policy change and its tests can be reverted locally without changing a public, persisted, or branch-protection contract.</reversibility>
  <files>
    scripts/ci/install_apt_packages.sh
    test/install_smoke/ci_cache_hygiene_test.exs
    .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
  </files>
  <read_first>
    - AGENTS.md
    - RUNNING.md
    - .planning/phases/132-measured-closure/132-CONTEXT.md
    - .planning/phases/132-measured-closure/132-VERIFICATION.md
    - .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
    - .planning/phases/132-measured-closure/132-05-SUMMARY.md
    - .github/workflows/ci.yml
    - scripts/ci/install_apt_packages.sh
    - test/install_smoke/ci_cache_hygiene_test.exs
    - test/install_smoke/ci_lane_split_test.exs
    - test/install_smoke/ci_observability_test.exs
  </read_first>
  <behavior>
    - "RED: a PATH-isolated fake `sudo`, `timeout`, and `apt-get` contract shows the current helper refreshes repository metadata even when the first package installation could succeed."
    - "GREEN success: one successful bounded install returns zero without invoking repository refresh."
    - "GREEN fallback: a failed initial install invokes one bounded refresh and then one final bounded install; refresh or final-install failure propagates non-zero."
    - "INVARIANTS: empty package input remains exit 2, `--configure-only` remains supported, network timeout/retry policy remains configured, and caller package arguments remain byte-faithful."
  </behavior>
  <action>
First append a causal-census section to the existing receipt without changing either failed table. Re-query the ten recorded run IDs from the Actions jobs API and record the immutable SHA, source IDs, critical finisher count, and machine-derived job/step median/min/max values; verify that the values match the receipt before using them. Add fail-first ExUnit coverage that constructs temporary PATH shims for `sudo`, `timeout`, and `apt-get`, executes the real helper, and asserts the ordered command log and exit codes without touching host apt state. Then change only the helper's retry flow: retain the persisted apt network configuration, arguments, `--configure-only`, `--no-install-recommends`, 240-second timeout, two install attempts, five-second retry delay, and loud final failure; make the first attempt a bounded install using existing runner indexes, and only after it fails run one bounded `apt-get update` before the final bounded install. Do not edit `.github/workflows/ci.yml`, any job/step name, `needs`, branch protection, package lists, dependency manifests, product code, or receipt arithmetic per D-01 and D-02.
  </action>
  <acceptance_criteria>
    - The receipt retains the complete failed `24c17783bbc080a085e398164450b7c3f475781e` sample and adds a reproducible causal census sourced from the same ten run IDs.
    - The new contract is observed failing against the unconditional-refresh implementation and passing after the helper correction.
    - A successful first package installation performs no repository refresh; a failed first installation performs exactly one refresh before exactly one final installation attempt.
    - Empty package input, configure-only behavior, exact package forwarding, timeout bounds, retry delay, and non-zero terminal failures are covered by executable assertions.
    - The diff is limited to the three declared files and contains no workflow topology, required-result, dependency-set, public/API, schema/migration, telemetry/error, Admin, or release-proof change.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/install_smoke/ci_cache_hygiene_test.exs test/install_smoke/ci_lane_split_test.exs test/install_smoke/ci_observability_test.exs --seed 0 &amp;&amp; bash -n scripts/ci/install_apt_packages.sh &amp;&amp; bash scripts/ci/test_ci_summary_gate.sh &amp;&amp; ./scripts/maintainer/automation_first_contract.sh</automated>
  </verify>
  <done>The required path uses the tested install-first/fallback-refresh helper, while the same-head evidence and all topology/summary contracts remain machine-verifiably intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub runner image -> Ubuntu repositories | Cached package indexes may be usable or stale; failure must route to a bounded refresh, never to a silent skip. |
| apt helper -> required jobs | One shared script supplies system packages to multiple merge-blocking jobs without changing their proof responsibilities. |
| Actions API -> planning receipt | Remote timestamps and job identities are untrusted evidence until restricted to the ten recorded exact-head runs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-132-06-01 | Tampering | `install_apt_packages.sh` arguments | high | mitigate | Preserve quoted argument forwarding and exercise exact argv through PATH-isolated process tests. |
| T-132-06-02 | Denial of service | stale/missing apt indexes | medium | mitigate | Fall back from the failed install to one bounded refresh and final bounded install with the existing timeouts. |
| T-132-06-03 | Elevation of privilege | required CI proof topology | high | mitigate | Leave `ci.yml` and branch protection unchanged; run topology and summary-gate contracts. |
| T-132-06-04 | Repudiation | causal timing census | medium | mitigate | Bind calculations to the receipt's immutable SHA and ten run IDs and preserve source URLs/identities. |
</threat_model>

<source_coverage_audit>

| Source | ID | Item | Plan | Status |
| --- | --- | --- | --- | --- |
| GOAL | — | Close the measured median gap, preserve ratchets, replace the failed receipt | 132-06..08 | COVERED |
| REQ | CI-14 | Passing comparable ten-run median and p95 without weakening/reruns | 132-06, 132-08 | COVERED |
| REQ | COV-05 | Authoritative coverage remains at least 82.13% | 132-07 | COVERED |
| REQ | SAFE-02 | Focused, quality, SAFE-01, packed-consumer, and drift proof | 132-06, 132-07 | COVERED |
| RESEARCH | — | Use same-head native job/step timing and the smallest existing-path correction | 132-06 | COVERED |
| RESEARCH | — | Preserve failed receipt; collect a distinct fresh receipt | 132-06, 132-08 | COVERED |
| CONTEXT | D-01 | Preserve sole required check, job set, and skip-as-pass | 132-06, 132-07 | COVERED |
| CONTEXT | D-02 | Evidence-guided smallest correction; no topology/reruns/deferred redesign | 132-06 | COVERED |
| CONTEXT | D-03 | Strict external-runner exception contract | 132-08 | COVERED |
| CONTEXT | D-04 | Ten successful non-cancelled first-attempt runs at one head | 132-08 | COVERED |
| CONTEXT | D-05 | Inclusive median and nearest-rank p95 thresholds | 132-08 | COVERED |
| CONTEXT | D-06 | Final preservation re-census | 132-07 | COVERED |
| CONTEXT | D-07 | Inclusive coverage floor; no percentage chasing | 132-07 | COVERED |

Deferred test partitioning, cache redesign, toolchain/dependency upgrades, topology changes, proof removal, and rerun-based improvement are excluded exactly as deferred/out of scope.
</source_coverage_audit>

<verification>
Run the focused helper, topology, observability, summary-gate, and automation-first contracts. Confirm the name-only diff contains only the three declared paths and the causal census is derived from the already-recorded exact-head run IDs.
</verification>

<success_criteria>
- The shared required-path apt helper avoids unconditional refresh work while retaining bounded recovery.
- CI topology, required results, skip-as-pass, package lists, and proof behavior are unchanged.
- The failed receipt remains intact with a reproducible causal census for the chosen correction.
</success_criteria>

<output>
Create `.planning/phases/132-measured-closure/132-06-SUMMARY.md` when done.
</output>

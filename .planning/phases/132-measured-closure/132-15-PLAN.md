---
phase: 132-measured-closure
plan: 15
type: execute
wave: 14
depends_on: [132-14]
files_modified:
  - .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
autonomous: true
gap_closure: true
requirements: [CI-14, COV-05, SAFE-02]
must_haves:
  truths:
    - "One fresh current receipt contains exactly ten consecutive successful, non-cancelled, first-attempt pull-request runs from the immutable 132-14 subject per D-04."
    - "Every run and exactly one successful `CI Summary` are re-resolved through the Actions API; stored completed state is independently revalidated by the corrected path."
    - "Median uses sorted ranks 5/6, nearest-rank p95 uses rank 10, and both inclusive limits (480/600 seconds) pass before CI-14 closes per D-05."
    - "D-01/D-08/D-09 topology and the 132-14 COV-05/SAFE-02 preservation subject remain unchanged after sampling."
    - statement: "SAFE-02 empty/ordering/concurrency backstop: non-ten or partial evidence fails, ties sort numerically, samples do not overlap, and one SHA-scoped lock owns the sequential collector across interruption/resume."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT count a failed run, cancellation, rerun, mixed SHA/event, overlapping sample, duplicate summary, or selected-around slow success."
      category: safety
      status: unverified
      flagged_unverified: true
    - statement: "MUST NOT replace API-backed evidence with local timing, screenshots, human/UAT approval, or an unsupported external-runner exception."
      category: transparency
      status: unverified
      flagged_unverified: true
    - statement: "MUST NOT weaken thresholds, topology, required membership, proof commands, or policies after a miss."
      category: values
      status: unverified
      flagged_unverified: true
  artifacts:
    - path: ".planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md"
      provides: "Fresh exact-ten current table/source manifest, immutable SHA, API identities, chronology, statistics, and PASS verdict"
      contains: "CI_TIMING_CURRENT_SOURCE_BEGIN"
  key_links:
    - from: ".planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md"
      to: "scripts/ci/collect_pr_timing_receipt.sh"
      via: "full correction and preserved-subject SHA arguments"
      pattern: "Final post-verification-gap preservation census"
    - from: "scripts/ci/collect_pr_timing_receipt.sh"
      to: ".planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md"
      via: "single locked run plus corrected API-backed completed-state verification"
      pattern: "CI_TIMING_CURRENT_(TABLE|SOURCE)_BEGIN"
---

<objective>
Collect the terminal fresh exact-ten Actions receipt and close CI-14 only on a live-verified threshold pass.

Purpose: Replace both historical failures with the sole authorized comparable acceptance artifact after all implementation and preservation gates are green.
Output: One current API-backed receipt and machine-decided Phase 132 verdict.
</objective>

## Artifacts this phase produces

- Updated `132-CI-TIMING-RECEIPT.md`: exactly one `CI_TIMING_CURRENT_TABLE_*` pair and one `CI_TIMING_CURRENT_SOURCE_*` pair with full SHA, ten source URLs/IDs, starts/completions/durations, sorted values, ranks, median, p95, and PASS verdict. Historical failed sections remain unchanged.
- No new controller symbol, CLI flag, state field, workflow job/edge, test, dependency, product API, or exception mechanism.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RUNNING.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/132-measured-closure/132-CONTEXT.md
@.planning/phases/132-measured-closure/132-VERIFICATION.md
@.planning/phases/132-measured-closure/132-14-SUMMARY.md
@.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
@.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
@scripts/ci/collect_pr_timing_receipt.sh
@test/install_smoke/ci_timing_automation_test.exs
@test/install_smoke/ci_lane_split_test.exs
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run and independently verify the terminal exact-ten receipt</name>
  <precondition>Plan 132-14 is machine-green; `gh auth status` succeeds; PR #96 is open; `ci-timing-sample` exists and is absent from the PR; no current receipt markers, Phase 132 live controller, owned label, or SHA-scoped state lock exists.</precondition>
  <reversibility rating="reversible">The label is cleaned and the receipt append is revertible; workflow runs remain durable public evidence under the user's explicit bounded authorization.</reversibility>
  <files>.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md</files>
  <read_first>
    - AGENTS.md
    - RUNNING.md
    - .planning/phases/132-measured-closure/132-CONTEXT.md
    - .planning/phases/132-measured-closure/132-VERIFICATION.md
    - .planning/phases/132-measured-closure/132-14-SUMMARY.md
    - .planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
    - .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
    - .planning/phases/132-measured-closure/COVERAGE.md
    - scripts/ci/collect_pr_timing_receipt.sh
    - test/install_smoke/ci_timing_automation_test.exs
    - test/install_smoke/ci_lane_split_test.exs
    - test/install_smoke/ci_observability_test.exs
    - scripts/ci/test_ci_summary_gate.sh
    - .github/workflows/ci.yml
  </read_first>
  <action>
Re-run the bounded offline controller/topology/observability and summary-gate checks, formatter check, and automation-first contract. Extract the full correction and preserved-subject SHAs from 132-14 machine evidence; assert the receipt has zero current markers and no controller/label/lock ownership. Launch exactly one foreground controller for `szTheory/rindle` PR 96, `ci.yml`, `CI Summary`, `ci-timing-sample`, ten samples, maximum two complete sequences, inclusive median 480 and p95 600, dedicated new `.gsd/ci-timing/phase-132-final` state, and fast-forward-only publication. Retain the existing sequential/non-overlap, pending-trigger, delayed discovery, rate-limit, state-lock, resume, cleanup, no-rerun, and no-exception behavior per D-01/D-03/D-04/D-05. Do not start a second controller, relabel manually, rerun, or select around a slow success.

Acceptance requires run mode to produce one exact-ten current section and PASS. Then invoke explicit verify mode, invoke the corrected completed-state run path once more against the same state to prove live revalidation without retriggering, and require both to exit zero. Re-run formatter/topology/observability/summary contracts, confirm the label and lock are absent, and close CI-14 only when live recomputation proves median <=480 and nearest-rank p95 <=600. If either complete sequence fails before ten qualifying rows, or a complete ten-run receipt misses a threshold, retain all honest state/evidence and return `gaps_found`; do not claim completion or broaden remediation authority.
  </action>
  <acceptance_criteria>
    - Exactly one controller owns mutation; accepted evidence is exactly ten consecutive successful, non-cancelled, first-attempt pull_request runs on one immutable head.
    - Explicit verify mode and resumed completed-state run mode both re-resolve every run/job and exit zero without triggering an eleventh sample.
    - Table/source manifest, API identities, exact chronology, non-overlap, numeric sort, median ranks 5/6, p95 rank 10, and inclusive threshold verdict agree.
    - Owned label and lock are absent after exit; required summary membership/topology/skip semantics and the 132-14 preservation subject remain intact.
    - CI-14 is marked complete only for median <=480 and p95 <=600; otherwise the phase remains `gaps_found` through machine evidence.
  </acceptance_criteria>
  <verify>
    <automated>mix format --check-formatted &amp;&amp; mix test test/install_smoke/ci_timing_automation_test.exs test/install_smoke/ci_lane_split_test.exs test/install_smoke/ci_observability_test.exs --seed 0 &amp;&amp; bash scripts/ci/test_ci_summary_gate.sh &amp;&amp; bash scripts/ci/collect_pr_timing_receipt.sh verify --repo szTheory/rindle --workflow ci.yml --summary-job "CI Summary" --samples 10 --median-max 480 --p95-max 600 --receipt .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md &amp;&amp; ./scripts/maintainer/automation_first_contract.sh</automated>
  </verify>
  <done>A fresh immutable-head exact-ten receipt passes live API verification and both inclusive thresholds, closing CI-14 while retaining COV-05, SAFE-02, and all locked gate contracts; otherwise the machine verdict remains `gaps_found`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (ASVS L1; high severity blocks execution)

| Boundary | Description |
| --- | --- |
| local controller -> GitHub PR | Publication/label mutation must be authenticated, fast-forward-only, single-owner, bounded, and cleaned. |
| Actions API -> current receipt | Remote run/job identity, chronology, and timestamps become acceptance evidence. |
| receipt/state -> CI-14 verdict | Local bytes cannot decide closure without the corrected live revalidation path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-132-15-01 | Spoofing | run/head/summary identity | high | mitigate | Match full SHA, event, attempt, status, conclusion, and exactly one successful Summary through API. |
| T-132-15-02 | Tampering | receipt/state/statistics | high | mitigate | Require one marker pair, table/manifest equality, API recomputation, and corrected completed-state verification. |
| T-132-15-03 | Repudiation | excluded slow/failing samples | high | mitigate | Require the complete consecutive eligible exact-head slice and retain sequence errors. |
| T-132-15-04 | Elevation of privilege | GitHub mutation | high | mitigate | Enforce ancestry, PR/label identity, one lock, fast-forward publication, and cleanup traps. |
| T-132-15-05 | Denial of service | interruption/rate limit | medium | mitigate | Preserve resumable pending trigger, bounded backoff, two-sequence ceiling, and sequential collection. |
| T-132-15-06 | Information disclosure | credentials | medium | mitigate | Persist public run metadata only; never print/store tokens. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Fresh comparable threshold-passing ten-run receipt | 132-15 | COVERED | Terminal live authority.
REQ | CI-14 | Median <=480 and p95 <=600 | 132-15 | COVERED | Exact-ten API-backed PASS required.
REQ | COV-05 | Coverage remains >=82.13% | 132-14, 132-15 | COVERED | Same immutable sampled subject.
REQ | SAFE-02 | Behavior-preserving proof and edge cases | 132-14, 132-15 | COVERED | Post-sample focused backstop.
RESEARCH | — | Use existing controller and Actions-native timing | 132-15 | COVERED | No alternate sampler.
CONTEXT | D-01 | Sole required summary/job set/skip behavior | 132-15 | COVERED | Post-sample source contracts.
CONTEXT | D-02 | Only evidence-guided bounded remediation | 132-13 | COVERED | No further change here.
CONTEXT | D-03 | Strict external-runner exception | 132-15 | COVERED | No exception planned.
CONTEXT | D-04/D-05 | Fresh exact-ten and both inclusive limits | 132-15 | COVERED | Direct live predicates.
CONTEXT | D-06/D-07 | Final preservation and coverage | 132-14, 132-15 | COVERED | Immutable subject retained.
CONTEXT | D-08/D-09 | Exact topology exception and no other drift | 132-14, 132-15 | COVERED | Focused post-sample checks.
</source_audit>

<verification>
The Actions API plus explicit and completed-state live verifier paths are the only CI-14 acceptance authority. A failed sequence or threshold miss is `gaps_found`, not successful closure.
</verification>

<success_criteria>
- Exact-ten immutable-head receipt is API-backed and internally consistent.
- Median and nearest-rank p95 pass inclusive 480/600 limits.
- COV-05, SAFE-02, topology, required summary, and no-rerun/no-human policies remain intact.
</success_criteria>

<output>
Create `.planning/phases/132-measured-closure/132-15-SUMMARY.md` when done, recording PASS only if both live thresholds pass.
</output>

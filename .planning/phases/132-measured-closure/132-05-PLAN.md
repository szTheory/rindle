---
phase: 132-measured-closure
plan: 05
type: execute
wave: 5
depends_on: [132-04]
files_modified:
  - .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
autonomous: true
requirements: [CI-14]
must_haves:
  truths:
    - "Exactly ten qualifying first-attempt pull_request runs are generated and verified without human action on one immutable fast-forward-published head."
    - "The failed 005beae2 baseline remains byte-faithful and separate from the automated fresh receipt."
    - "Median <=480 and nearest-rank p95 <=600 are enforced inclusively; a miss produces a machine-detected gap, never UAT."
---

<objective>
Publish the preserved candidate, execute the unattended ten-run sample, and render an independently verifiable CI-14 receipt.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Run the live controller through receipt verification</name>
  <files>.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md</files>
  <action>Read the correction and preserved-subject SHAs from the preservation receipt. Run collect_pr_timing_receipt.sh with repo szTheory/rindle, PR 96, ci.yml, CI Summary, ci-timing-sample, ten samples, two maximum sequences, 480-second median, 600-second p95, and --publish-head. Commit the receipt whether it passes or honestly misses; never substitute manual verification or an external-runner exception.</action>
  <verify><automated>bash scripts/ci/collect_pr_timing_receipt.sh verify --receipt .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md</automated></verify>
  <done>The source manifest, table, API identity, chronology, statistics, and verdict are machine-verifiable.</done>
</task>

</tasks>

<success_criteria>
- No label or branch mutation remains after the controller exits except the intended fast-forward publication.
- Ten table rows and the JSON manifest agree exactly.
- Phase verification receives passed or gaps_found, never human_needed.
</success_criteria>

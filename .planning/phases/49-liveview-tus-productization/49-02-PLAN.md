---
phase: 49-liveview-tus-productization
plan: "02"
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - guides/resumable_uploads.md
  - test/install_smoke/phoenix_tus_truth_parity_test.exs
autonomous: true
requirements: [PHX-03, PHX-04]
must_haves:
  truths:
    - "Adopters can copy the canonical `uploader: \"RindleTus\"` browser path without reconstructing URLs or bypassing tus offset semantics."
    - "The guide explicitly distinguishes `uploading`, `verifying`, `ready`, and `error` so `100%` means bytes transferred, not asset readiness."
    - "Parity tests fail when the canonical client snippet or honest UI vocabulary drifts away from the supported contract."
  artifacts:
    - path: "guides/resumable_uploads.md"
      provides: "Canonical browser uploader and honest UI-state contract"
      contains: "uploadUrl: entry.meta.upload_url"
    - path: "test/install_smoke/phoenix_tus_truth_parity_test.exs"
      provides: "Executable parity gate for client snippet and UI vocabulary"
      contains: "findPreviousUploads()"
  key_links:
    - from: "guides/resumable_uploads.md"
      to: "test/install_smoke/phoenix_tus_truth_parity_test.exs"
      via: "parity assertions freeze the exact client and UI strings"
      pattern: "uploadUrl: entry\\.meta\\.upload_url|findPreviousUploads\\(\\)|resumeFromPreviousUpload"
    - from: "guides/resumable_uploads.md"
      to: "Rindle.verify_completion/2"
      via: "guide keeps readiness behind server verification rather than upload progress"
      pattern: "uploading|verifying|ready|100%|verify_completion/2"
---

<objective>
Freeze the canonical browser uploader path and honest UI vocabulary around the already-shipped Phoenix / LiveView tus seam.

Purpose: Make the client-side story auditable and drift-resistant without turning Phase 49 into a new uploader product or alternate lifecycle.
Output: Tightened guide snippet/state model and explicit parity assertions for `RindleTus`, resume discovery, and honest readiness semantics.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@guides/resumable_uploads.md
@test/install_smoke/phoenix_tus_truth_parity_test.exs
@test/install_smoke/docs_parity_test.exs
@.planning/REQUIREMENTS.md
@.planning/phases/49-liveview-tus-productization/49-CONTEXT.md
@.planning/phases/49-liveview-tus-productization/49-RESEARCH.md
@.planning/phases/49-liveview-tus-productization/49-PATTERNS.md
@.planning/phases/49-liveview-tus-productization/49-VALIDATION.md
@.planning/phases/49-liveview-tus-productization/49-01-PLAN.md

<interfaces>
Canonical client snippet lines already present in the guide:
```javascript
uploadUrl: entry.meta.upload_url,

upload.findPreviousUploads().then((previousUploads) => {
  if (previousUploads.length > 0) {
    upload.resumeFromPreviousUpload(previousUploads[0])
  }
```

Canonical UI-state wording already present in the guide:
```markdown
- `Uploading…` while the client is sending bytes
- `Verifying…` after the upload reaches `100%`
- `Ready` only after `consume_uploaded_entries/3` succeeds
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten the canonical `RindleTus` client snippet and honest UI-state prose in the guide</name>
  <read_first>
    - guides/resumable_uploads.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/49-liveview-tus-productization/49-CONTEXT.md
    - .planning/phases/49-liveview-tus-productization/49-RESEARCH.md
    - .planning/phases/49-liveview-tus-productization/49-PATTERNS.md
  </read_first>
  <files>guides/resumable_uploads.md</files>
  <action>Refine the existing browser section in `guides/resumable_uploads.md` per D-04 through D-09 and D-10 through D-11: keep `uploader: "RindleTus"` as the canonical Phase 49 path, explicitly freeze `uploadUrl: entry.meta.upload_url`, `findPreviousUploads()`, and `resumeFromPreviousUpload(...)`, and explain that these preserve server-owned offset truth instead of inventing alternate client-side resume semantics. Keep `@uppy/tus` only as a compatible non-canonical note per D-06. In the UI guidance, freeze the honest public vocabulary `uploading`, `verifying`, `ready`, and `error`, and say plainly that `100%` means bytes transferred while readiness remains behind `consume_uploaded_entries/3` and `verify_completion/2`.</action>
  <acceptance_criteria>
    - `rg -n 'uploader: "RindleTus"|uploadUrl: entry.meta.upload_url|findPreviousUploads\\(\\)|resumeFromPreviousUpload' guides/resumable_uploads.md` returns matches.
    - `rg -n 'uploading|verifying|ready|error|100%' guides/resumable_uploads.md` returns matches.
    - `rg -n '@uppy/tus' guides/resumable_uploads.md` returns a match.
    - `rg -n 'consume_uploaded_entries/3|verify_completion/2' guides/resumable_uploads.md` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "uploader: \"RindleTus\"|uploadUrl: entry.meta.upload_url|findPreviousUploads\\(\\)|resumeFromPreviousUpload" guides/resumable_uploads.md && rg -n "uploading|verifying|ready|error|100%" guides/resumable_uploads.md && rg -n "@uppy/tus" guides/resumable_uploads.md && rg -n "consume_uploaded_entries/3|verify_completion/2" guides/resumable_uploads.md'</automated>
  </verify>
  <done>The guide freezes one canonical browser path and one honest UI-state vocabulary without widening Phase 49 into a new uploader product.</done>
</task>

<task type="auto">
  <name>Task 2: Expand Phoenix tus parity tests to freeze the canonical client path and state vocabulary</name>
  <read_first>
    - test/install_smoke/phoenix_tus_truth_parity_test.exs
    - test/install_smoke/docs_parity_test.exs
    - guides/resumable_uploads.md
    - lib/rindle/live_view.ex
    - .planning/phases/49-liveview-tus-productization/49-VALIDATION.md
  </read_first>
  <files>test/install_smoke/phoenix_tus_truth_parity_test.exs</files>
  <action>Expand `test/install_smoke/phoenix_tus_truth_parity_test.exs` using the validation strategy in `49-VALIDATION.md`: add explicit parity assertions for `uploadUrl: entry.meta.upload_url`, `findPreviousUploads()`, and `resumeFromPreviousUpload(...)` so PHX-03 is frozen by tests; add explicit parity assertions for `uploading`, `verifying`, `ready`, and the `100%`-means-bytes-transferred wording so PHX-04 is frozen by tests; and keep the existing checks that `lib/rindle/live_view.ex` stays a thin guide pointer. Do not add generated-app proof work or a second parity harness here; this phase is only freezing the guide contract and its drift gate.</action>
  <acceptance_criteria>
    - `rg -n 'uploadUrl: entry.meta.upload_url|findPreviousUploads\\(\\)|resumeFromPreviousUpload|uploading|verifying|ready|100%' test/install_smoke/phoenix_tus_truth_parity_test.exs` returns matches.
    - `mix test test/install_smoke/phoenix_tus_truth_parity_test.exs test/rindle/live_view_test.exs` exits `0`.
    - `mix test test/install_smoke/phoenix_tus_truth_parity_test.exs | rg -n '0 failures'` returns a match.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "uploadUrl: entry.meta.upload_url|findPreviousUploads\\(\\)|resumeFromPreviousUpload|uploading|verifying|ready|100%" test/install_smoke/phoenix_tus_truth_parity_test.exs && mix test test/install_smoke/phoenix_tus_truth_parity_test.exs test/rindle/live_view_test.exs && mix test test/install_smoke/phoenix_tus_truth_parity_test.exs | rg -n "0 failures"'</automated>
  </verify>
  <done>Parity tests now fail on drift in the canonical client snippet or the honest UI-state vocabulary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guide snippet -> browser client | The guide teaches how the signed `upload_url` and tus resume APIs are used in the browser. |
| browser progress -> server readiness | UI wording must not collapse transport completion into verified asset readiness. |
| guide prose -> parity tests | If the exact client/state contract is not executable, future doc edits can silently drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-02-01 | T | `guides/resumable_uploads.md` client snippet | mitigate | Freeze `uploadUrl: entry.meta.upload_url`, `findPreviousUploads()`, and `resumeFromPreviousUpload(...)` per D-04 and D-05 so adopters reuse the signed URL and preserve offset truth. |
| T-49-02-02 | T | guide UI-state prose | mitigate | Freeze `uploading` / `verifying` / `ready` / `error` and state explicitly that `100%` is byte transfer while readiness stays behind `verify_completion/2`. |
| T-49-02-03 | R | `test/install_smoke/phoenix_tus_truth_parity_test.exs` | mitigate | Add direct parity assertions for the canonical client snippet and UI vocabulary so docs drift fails fast in CI. |
</threat_model>

<verification>
- `mix test test/install_smoke/phoenix_tus_truth_parity_test.exs test/rindle/live_view_test.exs`
- `rg -n 'uploader: "RindleTus"|uploadUrl: entry.meta.upload_url|findPreviousUploads\(\)|resumeFromPreviousUpload' guides/resumable_uploads.md`
- `rg -n 'uploading|verifying|ready|error|100%' guides/resumable_uploads.md`
</verification>

<success_criteria>
- The supported `uploader: "RindleTus"` flow is explicit about signed URL reuse, resume discovery, and offset-safe tus behavior.
- The documented UI state model distinguishes bytes transferred from verification and readiness.
- Parity tests freeze the canonical browser path and honest UI vocabulary against future drift.
</success_criteria>

<output>
After completion, create `.planning/phases/49-liveview-tus-productization/49-02-SUMMARY.md`
</output>

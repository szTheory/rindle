---
phase: 49-liveview-tus-productization
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/resumable_uploads.md
  - lib/rindle/live_view.ex
  - test/rindle/live_view_test.exs
autonomous: true
requirements: [PHX-02]
must_haves:
  truths:
    - "Adopters can see one canonical server-side Phoenix / LiveView setup path built around `Rindle.LiveView.allow_tus_upload/4`."
    - "The documented helper contract makes `:path` and `:secret_key_base` required, keeps `:actor` optional, and leaves router/auth/parser/CORS ownership with the host app."
    - "The supported path still completes through `consume_uploaded_entries/3` and `Rindle.verify_completion/2`, not a new lifecycle."
  artifacts:
    - path: "guides/resumable_uploads.md"
      provides: "Canonical server-side LiveView tus contract"
      contains: "Rindle.LiveView.allow_tus_upload/4"
    - path: "lib/rindle/live_view.ex"
      provides: "Thin helper docs pointing back to the canonical guide"
      contains: "guides/resumable_uploads.md"
    - path: "test/rindle/live_view_test.exs"
      provides: "Unit coverage for helper metadata, optional actor handling, and completion convergence"
      contains: "allow_tus_upload"
  key_links:
    - from: "guides/resumable_uploads.md"
      to: "lib/rindle/live_view.ex"
      via: "guide remains canonical while helper docs stay thin"
      pattern: "allow_tus_upload/4|guides/resumable_uploads\\.md"
    - from: "lib/rindle/live_view.ex"
      to: "test/rindle/live_view_test.exs"
      via: "unit tests freeze the helper metadata and optional actor contract"
      pattern: "RindleTus|upload_url|actor"
    - from: "lib/rindle/live_view.ex"
      to: "Rindle.verify_completion/2"
      via: "completion stays behind `consume_uploaded_entries/3`"
      pattern: "consume_uploaded_entries|verify_completion"
---

<objective>
Freeze the supported server-side Phoenix / LiveView tus contract around the existing helper seam without widening the API surface.

Purpose: Make the already-shipped `allow_tus_upload/4` seam copy-pasteable and supportable while preserving the existing completion boundary and keeping operational setup canon in the guide.
Output: Tightened server-side guide section, thin `Rindle.LiveView` docs, and unit tests that lock helper options plus optional actor behavior.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@guides/resumable_uploads.md
@lib/rindle/live_view.ex
@test/rindle/live_view_test.exs
@.planning/REQUIREMENTS.md
@.planning/phases/49-liveview-tus-productization/49-CONTEXT.md
@.planning/phases/49-liveview-tus-productization/49-RESEARCH.md
@.planning/phases/49-liveview-tus-productization/49-PATTERNS.md
@.planning/phases/49-liveview-tus-productization/49-VALIDATION.md
@.planning/phases/48-phoenix-dx-contract-truth-audit/48-02-SUMMARY.md

<interfaces>
From `lib/rindle/live_view.ex`:
```elixir
def allow_tus_upload(socket, name, profile, opts \\ []) do
  path = Keyword.fetch!(opts, :path)
  secret_key_base = Keyword.fetch!(opts, :secret_key_base)
  actor = Keyword.get(opts, :actor)
```

```elixir
meta = %{
  uploader: "RindleTus",
  endpoint: path,
  upload_url: upload_url,
  session_id: session.id,
  asset_id: session.asset_id
}
```

```elixir
defp resolve_actor(nil, _socket), do: {:ok, "anonymous"}
defp resolve_actor(fun, socket) when is_function(fun, 1), do: fetch_actor(fun.(socket))
defp resolve_actor(actor, _socket), do: fetch_actor(actor)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze the canonical server-side guide section and keep `Rindle.LiveView` thin</name>
  <read_first>
    - guides/resumable_uploads.md
    - lib/rindle/live_view.ex
    - .planning/REQUIREMENTS.md
    - .planning/phases/49-liveview-tus-productization/49-CONTEXT.md
    - .planning/phases/49-liveview-tus-productization/49-RESEARCH.md
    - .planning/phases/49-liveview-tus-productization/49-PATTERNS.md
  </read_first>
  <files>guides/resumable_uploads.md, lib/rindle/live_view.ex</files>
  <action>Update the Phoenix / LiveView tus section in `guides/resumable_uploads.md` so the supported server contract is explicit per D-01, D-02, and D-03: `Rindle.LiveView.allow_tus_upload/4` is the thin helper seam; `:path` and `:secret_key_base` are required; `:actor` is optional; router mount, auth, `Plug.Parsers`, CORS, and sticky-session or single-node resume posture stay adopter-owned; completion still runs through `consume_uploaded_entries/3` and `verify_completion/2`. Keep `guides/resumable_uploads.md` the only full narrative and trim `lib/rindle/live_view.ex` docs so they point to that guide instead of duplicating setup details. Preserve the existing optional 1-arity `:actor` wording in the helper docs because the implementation already accepts it, but do not widen the public contract beyond the current seam.</action>
  <acceptance_criteria>
    - `rg -n 'allow_tus_upload/4|:path|:secret_key_base|:actor|consume_uploaded_entries/3|verify_completion/2' guides/resumable_uploads.md` returns matches.
    - `rg -n 'guides/resumable_uploads.md' lib/rindle/live_view.ex` returns matches.
    - `rg -n 'plug Plug\\.Parsers|config :cors_plug' lib/rindle/live_view.ex` returns no matches.
    - `rg -n 'thin helper seam|host app still owns|router|CORS' guides/resumable_uploads.md` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "allow_tus_upload/4|:path|:secret_key_base|:actor|consume_uploaded_entries/3|verify_completion/2" guides/resumable_uploads.md && rg -n "guides/resumable_uploads.md" lib/rindle/live_view.ex && ! rg -n "plug Plug\\.Parsers|config :cors_plug" lib/rindle/live_view.ex && rg -n "thin helper seam|host app still owns|router|CORS" guides/resumable_uploads.md'</automated>
  </verify>
  <done>The guide is the one canonical server-side contract, and the helper docs remain a thin pointer to it while preserving the current option boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze helper metadata, optional actor resolution, and verification-lane convergence in unit tests</name>
  <read_first>
    - test/rindle/live_view_test.exs
    - lib/rindle/live_view.ex
    - .planning/phases/49-liveview-tus-productization/49-VALIDATION.md
    - .planning/phases/49-liveview-tus-productization/49-PATTERNS.md
  </read_first>
  <files>test/rindle/live_view_test.exs</files>
  <action>Expand `test/rindle/live_view_test.exs` to freeze PHX-02 per D-01 and D-02 using the Wave 0 guidance in `49-VALIDATION.md`: keep the existing helper metadata assertions for `uploader: "RindleTus"`, `endpoint`, and signed `upload_url`; add targeted coverage that `actor: fn socket -> ... end` is accepted and resolved from the socket because `allow_tus_upload/4` already documents that 1-arity seam; and keep `consume_uploaded_entries/3` proven as the only completion convergence lane through `Rindle.verify_completion/2` per D-02 and D-11. Do not add any alternate ready-state shortcut or client-side lifecycle assertion here.</action>
  <acceptance_criteria>
    - `rg -n 'actor: fn|RindleTus|upload_url|verify_completion/2|consume_uploaded_entries/3' test/rindle/live_view_test.exs` returns matches.
    - `mix test test/rindle/live_view_test.exs` exits `0`.
    - `mix test test/rindle/live_view_test.exs | rg -n '0 failures'` returns a match.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "actor: fn|RindleTus|upload_url|verify_completion/2|consume_uploaded_entries/3" test/rindle/live_view_test.exs && mix test test/rindle/live_view_test.exs && mix test test/rindle/live_view_test.exs | rg -n "0 failures"'</automated>
  </verify>
  <done>Unit tests lock the helper metadata contract, the existing optional actor behavior, and the unchanged server-owned verification lane.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guide prose -> adopters | The guide teaches how signed `upload_url` values and helper options must be used. |
| helper docs -> canonical guide | If `Rindle.LiveView` duplicates setup details, the support contract can drift. |
| helper metadata -> completion lane | If docs or tests imply a new readiness lifecycle, adopters can bypass `verify_completion/2`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01-01 | I | `guides/resumable_uploads.md` | mitigate | Keep signed `upload_url` handling guide-owned and explicit, with adopter-owned router/auth/parser/CORS responsibilities called out instead of ad-hoc variations. |
| T-49-01-02 | T | `lib/rindle/live_view.ex` docs | mitigate | Keep `Rindle.LiveView` docs thin per D-03 so they point to the canonical guide rather than drifting into a second setup source. |
| T-49-01-03 | T | `test/rindle/live_view_test.exs` | mitigate | Freeze the helper metadata and optional actor seam while preserving convergence through `consume_uploaded_entries/3` and `verify_completion/2`, preventing alternate lifecycle claims. |
</threat_model>

<verification>
- `mix test test/rindle/live_view_test.exs`
- `rg -n 'allow_tus_upload/4|:path|:secret_key_base|:actor|consume_uploaded_entries/3|verify_completion/2' guides/resumable_uploads.md`
- `rg -n 'guides/resumable_uploads.md' lib/rindle/live_view.ex`
</verification>

<success_criteria>
- `allow_tus_upload/4` is documented as the supported server-side entrypoint with required and optional options called out precisely.
- The guide remains the one canonical Phoenix / LiveView tus setup narrative.
- Unit coverage freezes the existing helper metadata and optional actor seam without widening the lifecycle.
</success_criteria>

<output>
After completion, create `.planning/phases/49-liveview-tus-productization/49-01-SUMMARY.md`
</output>

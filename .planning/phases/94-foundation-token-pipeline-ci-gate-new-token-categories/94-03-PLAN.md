---
phase: 94-foundation-token-pipeline-ci-gate-new-token-categories
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - brandbook/tokens/tokens.json
  - brandbook/src/admin-css-build.mjs
  - brandbook/src/admin-design-system-data.mjs
  - brandbook/tokens/rindle-admin.css
  - priv/static/rindle_admin/rindle-admin.css
autonomous: true
requirements: [PIPE-02, VIS-01]
must_haves:
  truths:
    - "tokens.json + the admin .mjs generators emit all four new categories — motion easing presets, a dark elevation/shadow tint ladder, fluid type+space scales with named breakpoints, and differentiated dark status surfaces."
    - "Re-running admin-css-build.mjs with the new categories produces a byte-identical, empty-diff artifact (the parity self-check passes — no category is emitted-but-unregistered or emitted-but-unused)."
    - "Every new dark status-surface pair and elevation dark-text pair passes the WCAG AA gate at its stated min."
  artifacts:
    - path: "brandbook/tokens/tokens.json"
      provides: "Source-of-truth objects for the four new categories (extended motion, elevation ladder, fluid type/space, named breakpoints, differentiated dark status surfaces)"
      contains: "elevation"
    - path: "brandbook/src/admin-css-build.mjs"
      provides: "Emit loops for elevation/shadow/fluid/breakpoints + parity registration (exact() literal, requiredTokenUses) so omission is a hard failure"
      contains: "--rindle-elevation-"
    - path: "brandbook/src/admin-design-system-data.mjs"
      provides: "Extended MOTION_TOKENS (new easing presets) + widened CONSOLE_CONTRAST_PAIRS (new dark-status + elevation pairs)"
      contains: "easing-standard"
    - path: "brandbook/tokens/rindle-admin.css"
      provides: "Regenerated admin CSS carrying every new custom property"
      contains: "--rindle-elevation-2"
  key_links:
    - from: "brandbook/tokens/tokens.json (new top-level objects)"
      to: "brandbook/tokens/rindle-admin.css (emitted custom properties)"
      via: "Object.entries emit loops in admin-css-build.mjs + parity registration"
      pattern: "--rindle-elevation-"
    - from: "color.semantic.dark.status-*-surface (differentiated hexes)"
      to: "admin-contrast.mjs WCAG gate"
      via: "widened CONSOLE_CONTRAST_PAIRS resolving fg/bg under theme:dark"
      pattern: "dark-ready-surface"
---

<objective>
Wire the four new token categories from `94-UI-SPEC.md` into `tokens.json` and the **admin**
`.mjs` generators using the locked 3-touchpoint plug-in pattern (D-94-08): (A) add the source
object to `tokens.json`, (B) emit it from `admin-css-build.mjs`, (C) register it in the parity
arrays so it cannot be silently dropped or left unused. Categories: motion easing presets, a
semantic dark elevation/shadow tint ladder, fluid type + space scales with named breakpoints, and
differentiated dark status surfaces. All VALUES are pre-locked in `94-UI-SPEC.md` — this plan
invents no clamp tuples or hexes.

Purpose: PIPE-02 — the uplift phases (95–102) inherit these categories; without parity registration
a category can be emitted into the artifact yet silently omitted/unused (the exact trap D-94-08
names). This plan depends on Plan 01's clean baseline so the regen diff stays honest and attributable
to the new categories only.
Output: Extended `tokens.json`, new emit loops + parity entries in `admin-css-build.mjs`, extended
`MOTION_TOKENS` + widened `CONSOLE_CONTRAST_PAIRS` in `admin-design-system-data.mjs`, and the
regenerated + synced `rindle-admin.css` (both copies).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-foundation-token-pipeline-ci-gate-new-token-categories/94-CONTEXT.md
@.planning/phases/94-foundation-token-pipeline-ci-gate-new-token-categories/94-RESEARCH.md
@.planning/phases/94-foundation-token-pipeline-ci-gate-new-token-categories/94-PATTERNS.md
@.planning/phases/94-foundation-token-pipeline-ci-gate-new-token-categories/94-UI-SPEC.md

<interfaces>
<!-- The three touchpoints per category (verified in-session). All VALUES come from 94-UI-SPEC.md. -->

Touchpoint B — emit-loop analogs in brandbook/src/admin-css-build.mjs:
- spacing/radius simple key/value loop (lines 62-65): Object.entries(T.spacing) -> --rindle-space-{k}: {v}
- border deref loop (line 67): uses deref(v) when a value contains a {token} reference
- single shadow line (line 68): --rindle-shadow-card: T.shadow.card — extend to an Object.entries(T.shadow) loop
- motion loop (lines 71-74): emits every motion key except `rules` — new easings flow automatically, NO new loop
- existing media query literal (line 531): @media (max-width: 760px) — UI-SPEC anchors bp-md = 760px so output is unchanged

Touchpoint C — parity registration in brandbook/src/admin-css-build.mjs:
- exact(MOTION_TOKENS, ['press','popover','toast','transition','easing'], 'MOTION_TOKENS'); (line 39) — literal, kept in lockstep with the export
- const requiredMotionUses = MOTION_TOKENS.map(token => var(--rindle-motion-{token})); (line 595) — requires each motion key to be USED in a rule, not just emitted
- const requiredTokenUses = ['var(--rindle-surface)', ...]; (line 596) — add the new category vars here so emitted-but-unused hard-fails

Touchpoint C — data module brandbook/src/admin-design-system-data.mjs:
- export const MOTION_TOKENS = ['press','popover','toast','transition','easing']; (line 44)
- export const STATUS_STATES = [...] (line 12)
- export const CONSOLE_CONTRAST_PAIRS = [...] (line 54); the dark-status .map() at line 84 keeps shape { fg: status-{state}, bg: status-{state}-surface, theme: 'dark', min: 4.5 }

Locked VALUES (94-UI-SPEC.md, do NOT invent beyond these):
- motion easings: easing-standard cubic-bezier(0.2, 0, 0, 1), easing-decelerate cubic-bezier(0, 0, 0, 1), easing-accelerate cubic-bezier(0.3, 0, 1, 1)
- elevation: elevation-0 #0E1316 (= dark-bg), elevation-1 #161E23 (= dark-surface), elevation-2 #1E282E, elevation-3 #27343B
- shadows: shadow-raised "0 1px 2px rgba(16,20,23,0.06)", shadow-overlay "0 18px 40px -22px rgba(16,20,23,0.32)"
- fluid type (display only): hero clamp(40px, 28px + 3.4vw, 64px), h1 clamp(32px, 24px + 2.2vw, 44px), h2 clamp(24px, 20px + 1.4vw, 30px), h3 clamp(20px, 18px + 0.7vw, 22px)
- fluid space: space-fluid-gutter clamp(16px, 12px + 1.25vw, 32px), space-fluid-section clamp(32px, 24px + 2.5vw, 64px)
- breakpoints: bp-sm 480px, bp-md 760px, bp-lg 1024px, bp-xl 1280px
- dark status surfaces: dark-ready-surface #16241E, dark-processing-surface #1C1E2B, dark-warning-surface #241F16, dark-danger-surface #2A1A1A, dark-quarantine-surface #211C2B, dark-info-surface #161D2B
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the four new category objects to tokens.json + extend MOTION_TOKENS and CONSOLE_CONTRAST_PAIRS</name>
  <read_first>
    - brandbook/tokens/tokens.json (the existing color.raw at line 37, the collapsed dark status surfaces at lines 99-104, the motion block at lines 155-163, typography.scale at 119-127, spacing at 134, shadow at 146-148, and contrast_pairs at 164-203 — the exact insertion sites)
    - brandbook/src/admin-design-system-data.mjs (MOTION_TOKENS at 44, STATUS_STATES at 12, CONSOLE_CONTRAST_PAIRS at 54, the dark-status .map() at 84-90)
    - 94-UI-SPEC.md (the locked VALUES tables: Motion Presets, dark status SURFACES with their AA ratios, the 4-level elevation ladder, fluid TYPE/SPACE, Named Breakpoints)
    - 94-PATTERNS.md "tokens.json" Touchpoint A section + "admin-design-system-data.mjs" Touchpoint C section
    - 94-RESEARCH.md Pitfall 3 (resolve() needs both fg and bg to exist under theme:dark) and Pitfall 5 (exact() literal must match the export)
  </read_first>
  <action>
    Add the locked source objects to `tokens.json` (Touchpoint A) using only the VALUES from
    `94-UI-SPEC.md` — invent nothing. (1) Extend the `motion` block (lines 155-163) with
    `easing-standard`, `easing-decelerate`, `easing-accelerate` (the three cubic-beziers from
    UI-SPEC). (2) Add the six differentiated dark status-surface raw hexes (`dark-ready-surface`
    #16241E, `dark-processing-surface` #1C1E2B, `dark-warning-surface` #241F16, `dark-danger-surface`
    #2A1A1A, `dark-quarantine-surface` #211C2B, `dark-info-surface` #161D2B) to `color.raw`, then
    re-point each `color.semantic.dark.status-*-surface` role (lines 99-104) from `{dark-surface}` to
    its matching new raw token (stop collapsing — D-94-09). (3) Add a new top-level `elevation` object
    keyed 0/1/2/3 where 0 references `{dark-bg}` and 1 references `{dark-surface}` (UI-SPEC values equal
    those raws so `deref` resolves them) and 2/3 are the literal #1E282E/#27343B; extend the `shadow`
    object (today single `card`) with `raised` and `overlay`. (4) Add the fluid display clamps — per
    D-94-09, ONLY hero/h1/h2/h3 get a `clamp` field; body/small/code stay fixed-px — plus the two fluid
    space gutters and the four named breakpoints (`bp-sm/md/lg/xl`). Use the established `deref()`
    `{name}` reference syntax for any value that points at a raw color.

    Then extend the data module (Touchpoint C, partial — the rest lands in Task 2's parity registration):
    add `easing-standard`, `easing-decelerate`, `easing-accelerate` to `MOTION_TOKENS`
    (admin-design-system-data.mjs:44). Widen `CONSOLE_CONTRAST_PAIRS`: the dark-status `.map()` at
    lines 84-90 needs ZERO structural change (its `bg: status-${state}-surface` token name is unchanged;
    differentiation is purely the tokens.json value change above, and `resolve()` now finds the distinct
    hexes under theme:dark — RESEARCH Pitfall 3). ADD new elevation `dark-text` contrast pairs cloning
    the plain-object entry shape `{ fg: 'dark-text', bg: 'elevation-{n}', theme: 'dark', min: 4.5,
    context: ... }` for elevation-1/2/3 (elevation-0 == dark-bg is already the app base). The six
    dark-status ratios and the elevation dark-text ratios are pre-computed in UI-SPEC and clear AA with
    margin — do not recompute.

    RESOLVE the `diagram` asymmetry explicitly (the D-94-08 surfaced trap): the existing `motion` block
    emits a `diagram` key that is NOT in `MOTION_TOKENS` today, so `requiredMotionUses` does not require
    `diagram` to be USED. DECISION: keep `diagram` OUT of `MOTION_TOKENS` (preserve the existing
    asymmetry — `diagram` is an emitted duration with no enforced consumer and that is the current
    contract; touching it is out of scope), and add ONLY the three new easing presets to `MOTION_TOKENS`.
    This means each new easing MUST be consumed by at least one CSS rule (handled in Task 2) or
    `requiredMotionUses` fails. State this decision in the SUMMARY.

    SCOPE FENCE: admin generators ONLY. Author NO `cohort.css` / `.ck-*` CSS (Phase 96). You MAY add a
    short comment documenting the parallel `--ck-*` vocabulary as a coherence reference for Phase 96
    (D-94-06) — but write no Cohort stylesheet, token file, or build step.
  </action>
  <acceptance_criteria>
    - node -e "const t=require('./brandbook/tokens/tokens.json'); process.exit(t.elevation && t.elevation['2']==='#1E282E' ? 0 : 1)" exits 0 (elevation object present with the locked value).
    - grep -c 'dark-ready-surface' brandbook/tokens/tokens.json is >= 2 (raw hex declaration + the dark status role pointing at it).
    - The six status-*-surface roles no longer resolve to {dark-surface}: node assertion confirms color.semantic.dark['status-ready-surface'] !== '{dark-surface}'.
    - grep -c 'easing-standard\|easing-decelerate\|easing-accelerate' brandbook/src/admin-design-system-data.mjs is >= 3 (all three in MOTION_TOKENS).
    - grep -c "bg: 'elevation-" brandbook/src/admin-design-system-data.mjs is >= 3 (elevation dark-text pairs added for elevation-1/2/3).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/rindle && node -e "const t=require('./brandbook/tokens/tokens.json'); if(!(t.elevation && t.elevation['2']==='#1E282E')) throw new Error('elevation missing'); if(t.color.raw['dark-ready-surface']!=='#16241E') throw new Error('dark-ready-surface missing'); if(t.color.semantic.dark['status-ready-surface']==='{dark-surface}') throw new Error('status-ready-surface still collapses'); console.log('TOKENS_OK')"</automated>
  </verify>
  <done>tokens.json carries the four new categories with locked values; the six dark status surfaces are differentiated (no longer {dark-surface}); MOTION_TOKENS has the three new easings (diagram decision recorded); CONSOLE_CONTRAST_PAIRS has the elevation dark-text pairs.</done>
</task>

<task type="auto">
  <name>Task 2: Emit the new categories from admin-css-build.mjs, register them in the parity arrays, and regenerate + sync</name>
  <read_first>
    - brandbook/src/admin-css-build.mjs (the emit loops at 52-75 — spacing/radius/border/shadow/motion; the exact(MOTION_TOKENS, ...) literal at 39; the per-state CSS-rule loop at 212-219; the transition rules at 105/145/237/449/468/484 that reference var(--rindle-motion-easing); the media query at 531; the parity block at 595-606)
    - brandbook/src/admin-design-system-data.mjs (the now-extended MOTION_TOKENS from Task 1)
    - brandbook/src/sync-admin-css.mjs (created by Plan 01 — the single sync mechanism to run after regen)
    - 94-PATTERNS.md "admin-css-build.mjs" Touchpoint B + C section (the emit-loop clones, the single-line->object-loop conversion for shadow, requiredMotionUses/requiredTokenUses)
    - 94-RESEARCH.md Pitfall 4 (emitted-but-unused motion fails parity) + Pitfall 5 (exact() literal lockstep)
  </read_first>
  <action>
    Touchpoint B — add the emit loops to `admin-css-build.mjs`, cloning the existing analogs:
    (1) `elevation` — NEW Object.entries(T.elevation) loop emitting `--rindle-elevation-{k}` with
    `deref(v)` (clone the border deref loop at line 67 because elevation values reference
    `{dark-bg}`/`{dark-surface}`). (2) `shadow` — convert the single `--rindle-shadow-card` line (68) to
    an Object.entries(T.shadow) loop emitting `shadow-card`/`shadow-raised`/`shadow-overlay`. (3) fluid
    display type — emit `--rindle-text-{role}-fluid` for hero/h1/h2/h3 only, reading the `clamp` field
    added in Task 1 (clone the typography.scale loop at 57-61). (4) fluid space — emit
    `--rindle-space-fluid-gutter` and `--rindle-space-fluid-section`. (5) breakpoints — emit
    `--rindle-bp-{k}` for sm/md/lg/xl. The motion easings need NO new loop — the existing motion loop
    (71-74) emits every key except `rules`.

    Touchpoint C — register every new category so omission/non-use hard-fails (D-94-08):
    - Update the `exact(MOTION_TOKENS, [...])` literal at line 39 to include `easing-standard`,
      `easing-decelerate`, `easing-accelerate` in lockstep with the export (Pitfall 5 — both sources of
      truth must match or `exact()` throws).
    - `requiredMotionUses` (595) auto-derives from `MOTION_TOKENS`, so it now REQUIRES each new easing
      to be USED in a rule (Pitfall 4). Point existing transition rules (e.g. one or more of
      105/145/237/449/468/484) at the new presets: use `var(--rindle-motion-easing-standard)` for the
      default enter/move transitions, and consume `easing-decelerate`/`easing-accelerate` in at least
      one rule each (e.g. enter vs exit on a drawer/popover transition) so all three appear in `written`.
    - Add the new category vars to `requiredTokenUses` (596): at minimum `var(--rindle-elevation-1)` (or
      the elevation step actually consumed by a surface rule), the fluid display vars, the fluid space
      gutters, and a breakpoint var — so an emitted-but-unused category fails the self-check. Ensure each
      var you add to `requiredTokenUses` is genuinely consumed by a rule (apply elevation tints to the
      relevant dark surface rules, fluid type to the display headings, fluid gutters to shell padding) —
      registration without use is itself a parity failure.

    Then regenerate and sync: run `node brandbook/src/admin-css-build.mjs` (parity self-check must print
    OK and exit 0), then `node brandbook/src/sync-admin-css.mjs` (Plan 01's mechanism) to mirror the
    output to `priv/static/rindle_admin/rindle-admin.css`, then `node brandbook/src/admin-contrast.mjs`
    (the widened pairs must pass AA). Do NOT hand-edit either generated CSS — the generator + sync script
    are the only writers (each carries the `do not edit by hand` header). SCOPE FENCE: admin generator
    only — no `tokens-build.mjs` change unless a category must also appear in the broader `tokens.css`
    (RESEARCH Open Question 1 / A2 default: admin-only; the Plan 04 diff gate catches either decision).
  </action>
  <acceptance_criteria>
    - node brandbook/src/admin-css-build.mjs exits 0 and its parity self-check passes (no `missing:` output — every new category is emitted AND used; exact(MOTION_TOKENS,...) does not throw).
    - grep -c -- '--rindle-elevation-2' brandbook/tokens/rindle-admin.css is >= 1 (elevation ladder emitted).
    - grep -c -- '--rindle-shadow-overlay\|--rindle-bp-lg\|--rindle-space-fluid-gutter' brandbook/tokens/rindle-admin.css is >= 3 (shadow ladder, breakpoints, fluid space all emitted).
    - grep -c -- 'var(--rindle-motion-easing-standard)' brandbook/tokens/rindle-admin.css is >= 1 (new easing is USED in a rule, satisfying requiredMotionUses).
    - After node brandbook/src/admin-css-build.mjs && node brandbook/src/sync-admin-css.mjs, diff -q brandbook/tokens/rindle-admin.css priv/static/rindle_admin/rindle-admin.css reports IDENTICAL.
    - node brandbook/src/admin-contrast.mjs exits 0 (all pairs pass, including the new dark-status and elevation dark-text pairs).
    - A second regen+sync followed by git diff --exit-code -- brandbook/tokens/rindle-admin.css priv/static/rindle_admin/rindle-admin.css exits 0 (deterministic / committed).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/rindle && node brandbook/src/admin-css-build.mjs && node brandbook/src/sync-admin-css.mjs && node brandbook/src/admin-contrast.mjs && diff -q brandbook/tokens/rindle-admin.css priv/static/rindle_admin/rindle-admin.css && grep -q -- '--rindle-elevation-2' brandbook/tokens/rindle-admin.css && grep -q -- 'var(--rindle-motion-easing-standard)' brandbook/tokens/rindle-admin.css && echo EMIT_OK</automated>
  </verify>
  <done>All four new categories are emitted from admin-css-build.mjs and registered in the parity arrays (exact() + requiredMotionUses + requiredTokenUses); the parity self-check passes; new easings are used; both CSS copies are byte-identical and synced by the script; the contrast gate is green with the widened pairs; a second regen is an empty diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-tree source (tokens.json) -> generated artifact | The only input is the repo's own trusted `tokens.json`; no external/user input crosses here. `deref()` throws on unknown `{token}` references. |
| generator output -> shipped package copy | The sync (Plan 01's script) writes into `priv/`, which ships in the Hex package (mix.exs files:). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-06 | Tampering | a new token category is emitted but silently omitted/unused, weakening downstream coverage | mitigate | The 3-touchpoint pattern (D-94-08): every category registered in exact()/requiredMotionUses/requiredTokenUses, so omission or non-use is a hard generator self-check failure; the Plan 04 diff gate is the merge-blocker. |
| T-94-07 | Tampering | a differentiated dark status surface fails AA against its dark text | mitigate | All six ratios + elevation dark-text ratios are pre-computed in 94-UI-SPEC.md and added to CONSOLE_CONTRAST_PAIRS; admin-contrast.mjs exits non-zero on any sub-AA pair. |
| T-94-08 | Tampering | a contrast pair references a token resolve() cannot find (Pitfall 3) | mitigate | New raw hexes added to color.raw AND the dark status roles re-pointed at them, so both fg and bg resolve under theme:dark; the gate emits `unknown token` and fails otherwise. |
| T-94-09 | Information Disclosure | this plan reads no secrets | accept | Pure Node + file I/O; no HEX_API_KEY / MinIO creds. Below HIGH — accepted. |
| T-94-10 | Tampering | hand-editing a generated CSS file bypasses the generator | accept (mitigated downstream) | `do not edit by hand` headers + the Plan 04 diff gate catch it; no runtime risk (CSS custom properties only). Below HIGH — accepted. |
</threat_model>

<verification>
- node brandbook/src/admin-css-build.mjs exits 0 (parity self-check passes for all new categories).
- node brandbook/src/sync-admin-css.mjs then diff -q over both CSS copies reports IDENTICAL.
- node brandbook/src/admin-contrast.mjs exits 0 (widened pairs pass AA).
- A second regen + sync produces an empty git diff over both CSS copies (deterministic / committed).
</verification>

<success_criteria>
- All four new categories (motion easing presets, dark elevation/shadow ladder, fluid type+space + named breakpoints, differentiated dark status surfaces) are in tokens.json and emitted by the admin generator with locked UI-SPEC values.
- Every category is registered in the parity arrays (emitted AND used); the generator self-check passes.
- The widened WCAG gate is green for all new dark-status and elevation dark-text pairs.
- Both committed CSS copies are regenerated and byte-identical; admin-only scope held (no cohort.css authored).
</success_criteria>

<output>
Create `.planning/phases/94-foundation-token-pipeline-ci-gate-new-token-categories/94-03-SUMMARY.md` when done (record the `diagram`/MOTION_TOKENS decision and the parallel `--ck-*` coherence note).
</output>

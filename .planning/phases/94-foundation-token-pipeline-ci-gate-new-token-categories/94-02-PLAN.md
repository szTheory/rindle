---
phase: 94-foundation-token-pipeline-ci-gate-new-token-categories
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/adoption_demo/e2e/support/admin-polish.js
autonomous: true
requirements: [VIS-01]
must_haves:
  truths:
    - "assertAdminPolish accepts { root, interactiveSelectors } options that default to today's admin values."
    - "The existing admin-screenshots spec passes UNCHANGED (it calls assertAdminPolish without root/interactiveSelectors)."
    - "There is no auto-detection of the root — the root is always passed or defaulted explicitly."
  artifacts:
    - path: "examples/adoption_demo/e2e/support/admin-polish.js"
      provides: "Parameterized computed-style polish harness, surface-agnostic, ready for Cohort (Phase 102) to pass [data-ck-root] / .ck-*"
      contains: "interactiveSelectors"
  key_links:
    - from: "assertAdminPolish options { root, interactiveSelectors }"
      to: "assertNoClippedText / assertReadableContrast / assertTargetSizes / assertNoInteractiveOverlap"
      via: "threaded parameters defaulting to DEFAULT_ROOT / DEFAULT_INTERACTIVE_SELECTORS"
      pattern: "root = DEFAULT_ROOT"
---

<objective>
Generalize the `admin-polish.js` deterministic computed-style harness so it can target any root
selector and interactive-selector set, while the existing admin spec passes completely unchanged
(D-94-07, VIS-01 groundwork). This is the seam Phase 102 uses to run the SAME gate over Cohort by
passing `[data-ck-root]` / `.ck-*`. Phase 94 only parameterizes the harness — it writes no Cohort
spec.

Purpose: VIS-01 (the single merge-blocking visual gate) is owned by Phase 102; Phase 94 delivers
the parameterized harness so that later work needs no harness surgery. Backward compatibility is
the acceptance test: the admin spec must not change.
Output: Modified `examples/adoption_demo/e2e/support/admin-polish.js` with threaded
`root`/`interactiveSelectors` parameters.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-foundation-token-pipeline-ci-gate-new-token-categories/94-CONTEXT.md
@.planning/phases/94-foundation-token-pipeline-ci-gate-new-token-categories/94-RESEARCH.md
@.planning/phases/94-foundation-token-pipeline-ci-gate-new-token-categories/94-PATTERNS.md

<interfaces>
<!-- Current admin-polish.js structure (verified in-session). The 4 consumers of ROOT/INTERACTIVE_SELECTORS. -->

examples/adoption_demo/e2e/support/admin-polish.js (current):
```
line 16:  const ROOT = "[data-rindle-admin-root]";                         // module constant
line 28:  const INTERACTIVE_SELECTORS = [ ... 9 entries ... ];             // module constant
line 91:  async function assertNoClippedText(page)         // reads ROOT inside page.evaluate({ ROOT, CLIP_TOLERANCE }) at 93/151
line 162: async function assertReadableContrast(page)      // reads ROOT inside page.evaluate({ ROOT }) at 164/250
line 271: async function assertTargetSizes(page)           // page.locator(INTERACTIVE_SELECTORS.join(",")) at 272
line 299: async function assertNoInteractiveOverlap(page)  // page.locator(INTERACTIVE_SELECTORS.join(",")) at 300
line 383: async function assertAdminPolish(page, { viewport, surface } = {})   // entry point
```

Acceptance call site — MUST stay unchanged (examples/adoption_demo/e2e/admin-screenshots.spec.js:79):
```javascript
await assertAdminPolish(page, { viewport, surface });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread root + interactiveSelectors through assertAdminPolish with admin defaults</name>
  <read_first>
    - examples/adoption_demo/e2e/support/admin-polish.js (FULL read — the module constants at 16 + 28-38, the four consumers at 91/162/271/299, the entry point at 383, and the run() dispatch site that calls each sub-assertion)
    - examples/adoption_demo/e2e/admin-screenshots.spec.js (line 79 — the call site that is the backward-compat acceptance test; do NOT edit it)
    - 94-PATTERNS.md "admin-polish.js" section (the exact threading pattern + which line passes which param) and 94-RESEARCH.md Pattern 3
  </read_first>
  <action>
    Rename the module constants `ROOT` (line 16) → `DEFAULT_ROOT` and `INTERACTIVE_SELECTORS`
    (line 28) → `DEFAULT_INTERACTIVE_SELECTORS`. Extend the entry-point signature (line 383) to
    `assertAdminPolish(page, { viewport, surface, root = DEFAULT_ROOT, interactiveSelectors = DEFAULT_INTERACTIVE_SELECTORS } = {})`.
    Thread `root` into the two consumers that read it: `assertNoClippedText` and
    `assertReadableContrast` — add a `root` parameter to each and pass it through the
    `page.evaluate(...)` closure-arg object in place of the module-level `ROOT` (currently
    `{ ROOT, CLIP_TOLERANCE }` at 93/151 and `{ ROOT }` at 164/250). Thread `interactiveSelectors`
    into the two locator consumers: `assertTargetSizes` and `assertNoInteractiveOverlap` — add an
    `interactiveSelectors` parameter and use it in `page.locator(interactiveSelectors.join(","))`
    (272/300). Update the dispatch site inside `assertAdminPolish` so each sub-assertion receives
    the threaded value (which falls back to the module default when the caller omits it). NO
    auto-detection of the root (D-94-07 forbids it — a page mounting both surfaces would match both
    roots). Because admin-screenshots.spec.js:79 passes only `{ viewport, surface }`, the defaults
    apply and the admin spec is byte-for-byte backward-compatible — that is the acceptance test.
  </action>
  <acceptance_criteria>
    - `grep -c 'DEFAULT_ROOT' examples/adoption_demo/e2e/support/admin-polish.js` ≥ 2 (declaration + default in signature).
    - `grep -c 'interactiveSelectors' examples/adoption_demo/e2e/support/admin-polish.js` ≥ 3 (signature default + the two locator consumers).
    - `grep -vc '^//' examples/adoption_demo/e2e/admin-screenshots.spec.js` shows the spec file is unchanged from HEAD (`git diff --exit-code examples/adoption_demo/e2e/admin-screenshots.spec.js` exits 0).
    - No occurrence of auto-detection: `grep -c 'querySelector.*ck-root\|detectRoot\|autoRoot' examples/adoption_demo/e2e/support/admin-polish.js` is 0.
    - The existing admin polish spec passes unchanged: `cd examples/adoption_demo && npx playwright test admin-screenshots` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/rindle && git diff --exit-code examples/adoption_demo/e2e/admin-screenshots.spec.js && cd examples/adoption_demo && npx playwright test admin-screenshots</automated>
  </verify>
  <done>assertAdminPolish accepts root/interactiveSelectors with admin defaults; all four sub-assertions consume the threaded params; the admin-screenshots spec is unchanged and green; no auto-detection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → rendered page | The harness asserts computed styles against a locally-rendered gallery/console; no external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-04 | Tampering | a future caller passing an unintended root weakens the gate | mitigate | No auto-detection (D-94-07); root must be explicit; admin default preserves current coverage exactly. |
| T-94-05 | Denial of Service | flaky harness blocks merges | accept | Existing freezeMotion / animations:disabled / workers:1 controls are unchanged by this plan; parameterization adds no nondeterminism. Below HIGH — accepted. |
</threat_model>

<verification>
- `cd examples/adoption_demo && npx playwright test admin-screenshots` exits 0 (admin spec green, unchanged).
- `git diff --exit-code examples/adoption_demo/e2e/admin-screenshots.spec.js` exits 0 (spec untouched).
</verification>

<success_criteria>
- `assertAdminPolish` is parameterized over `{ root, interactiveSelectors }` defaulting to admin values; the existing admin spec passes unchanged; no auto-detection.
</success_criteria>

<output>
Create `.planning/phases/94-foundation-token-pipeline-ci-gate-new-token-categories/94-02-SUMMARY.md` when done.
</output>

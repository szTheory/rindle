---
phase: 94-foundation-token-pipeline-ci-gate-new-token-categories
plan: 05
type: gap_closure
wave: 4
depends_on: ["04"]
files_modified:
  - scripts/setup_branch_protection.sh
  - RUNNING.md
autonomous: false
requirements: [PIPE-01]
gap_source: 94-VERIFICATION.md#BP-94-01
---

# Phase 94 Plan 05: Gap Closure — Require `brandbook-tokens` in Branch Protection

<objective>
Close verification gap BP-94-01 by making `brandbook-tokens` part of the repository's branch-protection source of truth. Phase 94 already added and locally verified the CI job; the remaining gap is that protected PRs do not currently require the new check.

Output: `scripts/setup_branch_protection.sh` and `RUNNING.md` both list `brandbook-tokens` as a merge-blocking required check, and the maintainer apply path can reassert branch protection with that context.
</objective>

<context>
@.planning/phases/94-foundation-token-pipeline-ci-gate-new-token-categories/94-VERIFICATION.md
@scripts/setup_branch_protection.sh
@RUNNING.md
@.github/workflows/branch-protection-apply.yml
</context>

<diagnosis>
`gh api repos/szTheory/rindle/branches/main/protection/required_status_checks` returns `strict: true` but omits `brandbook-tokens`. The repo has an owner-controlled branch-protection reassertion path: `.github/workflows/branch-protection-apply.yml` runs `scripts/setup_branch_protection.sh main` with `BRANCH_PROTECTION_PAT`. The script's `REQUIRED_CHECKS` array and printed expected-check text are missing the new job.
</diagnosis>

<tasks>

<task type="auto">
  <name>Add `brandbook-tokens` to required-check source of truth</name>
  <read_first>
    - scripts/setup_branch_protection.sh
    - RUNNING.md
  </read_first>
  <action>
    Add `brandbook-tokens` to `REQUIRED_CHECKS` in `scripts/setup_branch_protection.sh` and to the `print_expected_text` required-check list. Place it with the other repo-only proof lanes after `Cohort Demo Smoke` unless a maintainer chooses a different ordering.

    Update `RUNNING.md` so the CI lane table marks `brandbook-tokens` as merge-blocking and the Release train required-check paragraph includes it.
  </action>
  <verify>
    - `scripts/setup_branch_protection.sh --print-expected | rg 'brandbook-tokens'`
    - `scripts/setup_branch_protection.sh --print-expected-json | jq -e '.required_status_checks.contexts | index("brandbook-tokens")'`
  </verify>
</task>

<task type="manual_or_admin_token">
  <name>Apply and verify remote branch protection</name>
  <action>
    After the phase branch is pushed and GitHub has seen the `brandbook-tokens` check context, run the existing owner-controlled apply path:

    - Preferred: dispatch `.github/workflows/branch-protection-apply.yml` with `BRANCH_PROTECTION_PAT` configured.
    - Equivalent local maintainer command: `GH_TOKEN=<admin-PAT> scripts/setup_branch_protection.sh main`.

    Do not use a non-admin token; branch protection writes require repository Administration permission.
  </action>
  <verify>
    - `gh api repos/szTheory/rindle/branches/main/protection/required_status_checks --jq '.contexts | index("brandbook-tokens")'` returns a non-null index.
    - The PR check rollup shows `brandbook-tokens` present and passing for the phase branch.
  </verify>
</task>

</tasks>

<success_criteria>
- `brandbook-tokens` appears in `scripts/setup_branch_protection.sh --print-expected`.
- `brandbook-tokens` appears in `scripts/setup_branch_protection.sh --print-expected-json`.
- `RUNNING.md` documents `brandbook-tokens` as merge-blocking.
- Remote branch protection requires `brandbook-tokens` after the maintainer apply path runs.
- `$gsd-verify-work 94` can move `94-VERIFICATION.md` from `gaps_found` to complete.
</success_criteria>

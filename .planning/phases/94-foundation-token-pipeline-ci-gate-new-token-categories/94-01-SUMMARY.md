---
phase: 94-foundation-token-pipeline-ci-gate-new-token-categories
plan: 01
subsystem: infra
tags: [brandbook, design-tokens, css-pipeline, node, generator, ci-baseline]

# Dependency graph
requires:
  - phase: v1.18 (Phase 88)
    provides: token-generated rindle-admin CSS, brandbook generators, byte-identical priv copy
provides:
  - Drift-free brand token baseline (tokens.css regenerated; stale dark text-on-brand corrected)
  - brandbook/src/sync-admin-css.mjs — single committed CSS-mirror mechanism (D-94-03)
  - Clean idempotency anchor (fresh regen + sync = empty git diff)
affects: [94-02, 94-03, 94-04, brandbook-tokens-ci-gate, token-pipeline]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Scripted single-source CSS mirror replacing hand-copy (sync-admin-css.mjs)"
    - "Generator-is-only-writer + do-not-edit-by-hand baseline for shipped artifacts"

key-files:
  created:
    - brandbook/src/sync-admin-css.mjs
  modified:
    - brandbook/tokens/tokens.css

key-decisions:
  - "Sync as its own invokable step (not folded into admin-css-build.mjs) so Plan 04 CI calls it discretely in D-94-02 order"
  - "Byte-for-byte read->write copy, not a re-render — generator already wrote canonical output"

patterns-established:
  - "Single committed sync mechanism: generator output -> shipped priv/ copy, runnable identically by CI and local devs"

requirements-completed: [PIPE-01]

# Metrics
duration: 7min
completed: 2026-06-15
---

# Phase 94 Plan 01: Foundation — Drift-Free Token Baseline & CSS-Sync Mechanism Summary

**Corrected the live `tokens.css` drift (stale dark `--rindle-text-on-brand: #F7F4EA` → `#101417`) and added `sync-admin-css.mjs` as the single committed mechanism mirroring generator output to the shipped package copy, establishing a clean idempotency anchor before the Plan 04 CI gate lands.**

## Performance

- **Duration:** ~7 min
- **Started:** 2026-06-15T02:43:00Z
- **Completed:** 2026-06-15T02:50:00Z
- **Tasks:** 2
- **Files modified:** 2 (1 created, 1 modified)

## Accomplishments
- Regenerated every committed brand artifact from `tokens.json`; corrected the two stale dark/auto `--rindle-text-on-brand` lines (`#F7F4EA` → `#101417`, the cream-on-green AA fix already in the source). `:root` (light) correctly stays `#F7F4EA`.
- `rindle-admin.css` and `admin-gallery/index.html` regenerated byte-identically (only `tokens.css` had real drift).
- Created `brandbook/src/sync-admin-css.mjs`: the single committed step that copies `brandbook/tokens/rindle-admin.css` → `priv/static/rindle_admin/rindle-admin.css` byte-identically (D-94-03), replacing the prior undocumented hand-mirror.
- Verified the clean baseline: a fresh `tokens-build → admin-css-build → sync` produces an empty `git diff` on tracked artifacts (D-94-10 idempotency anchor).
- Contrast gate green (41/41); ExUnit two-copy byte-equality assertion stays green (4/4 integration tests pass).

## Task Commits

Each task was committed atomically:

1. **Task 1: Regenerate and commit all stale generated artifacts (clean baseline)** — `1a6c14d` (fix)
2. **Task 2: Create the scripted CSS-sync mechanism and replace the hand-mirror** — `c8eade6` (feat)

## Files Created/Modified
- `brandbook/src/sync-admin-css.mjs` (created) — single committed mechanism mirroring `brandbook/tokens/rindle-admin.css` to `priv/static/rindle_admin/rindle-admin.css` byte-identically.
- `brandbook/tokens/tokens.css` (modified) — regenerated; dark/auto `--rindle-text-on-brand` drift corrected to `#101417`.

## Decisions Made
- Kept the sync as its own invokable step rather than having `admin-css-build.mjs` write the second destination, so the Plan 04 CI gate can call it as a discrete step in the D-94-02 order.
- The sync is a straight byte-for-byte read→write copy, not a re-render, since the generator already wrote the canonical output.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- The acceptance criterion `mix test test/brandbook/admin_design_system_validation_test.exs` was a no-op under the default tag set (module is `@moduletag :integration`, excluded by default). Re-ran with `--include integration`; all 4 tests pass including the line-213 byte-equality assertion. No code change required — the criterion is satisfied, it just needs the integration tag to actually execute.
- `priv/static/rindle_admin/rindle-admin.css` produced no diff at sync time because it was already byte-identical to the regenerated source (no live drift in the shipped copy); the script is now the committed mechanism that maintains it.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- The token→CSS pipeline now has a clean, drift-free, idempotent baseline. Plan 04's `brandbook-tokens` CI gate can go red→green honestly against a fully-current tree.
- `sync-admin-css.mjs` is ready to be wired as a discrete CI step (Plan 04) in the D-94-02 order.
- No blockers.

## Self-Check: PASSED

- FOUND: brandbook/src/sync-admin-css.mjs
- FOUND: brandbook/tokens/tokens.css
- FOUND: 94-01-SUMMARY.md
- FOUND commit: 1a6c14d (Task 1)
- FOUND commit: c8eade6 (Task 2)

---
*Phase: 94-foundation-token-pipeline-ci-gate-new-token-categories*
*Completed: 2026-06-15*

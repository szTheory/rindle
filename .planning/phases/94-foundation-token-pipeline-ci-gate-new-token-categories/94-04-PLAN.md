---
phase: 94-foundation-token-pipeline-ci-gate-new-token-categories
plan: 04
type: execute
wave: 3
depends_on: ["01", "03"]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [PIPE-01]
must_haves:
  truths:
    - "A new standalone brandbook-tokens CI job regenerates rindle-admin.css (+ tokens.css, gallery index.html) from tokens.json via the .mjs scripts, runs the WCAG contrast gate, and FAILS the build on any uncommitted diff."
    - "Re-running the generators with unchanged source produces a byte-identical, empty-diff artifact — the git diff --exit-code over the regenerated tree is the merge-blocking idempotency anchor."
    - "The gate covers BOTH committed CSS copies (brandbook + the shipped priv/ copy) plus tokens.css and the gallery index.html — so the shipped package artifact cannot drift silently."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Standalone brandbook-tokens job: needs [quality, optional-dependencies], node-20, playwright-in-adoption_demo, regen -> contrast -> gallery-check -> sync -> git diff --exit-code"
      contains: "brandbook-tokens"
  key_links:
    - from: ".github/workflows/ci.yml brandbook-tokens job"
      to: "git diff --exit-code over the regenerated tree"
      via: "node generators + sync-admin-css.mjs run before the diff step"
      pattern: "git diff --exit-code"
    - from: "brandbook-tokens job"
      to: "quality + optional-dependencies jobs"
      via: "needs: [quality, optional-dependencies] (proof-lane convention)"
      pattern: "needs: \\[quality, optional-dependencies\\]"
---

<objective>
Stand up the merge-blocking `brandbook-tokens` CI job (PIPE-01) — the single deliverable that makes
the token->CSS pipeline gated. It is a NEW standalone top-level job in `.github/workflows/ci.yml`
(D-94-01), `needs: [quality, optional-dependencies]`, that regenerates every committed generated
artifact, runs the WCAG contrast gate and the gallery browser proof, syncs the shipped CSS copy via
Plan 01's script, and fails on any `git diff`. This plan depends on Plan 01 (the sync script + clean
baseline) and Plan 03 (the new categories) so the gate diffs the FINAL tree — the gate must go
red->green honestly against the complete artifact set.

Purpose: Generated CSS can no longer drift from `tokens.json` source — the canonical PITFALL #6 this
phase exists to kill. The `git diff --exit-code` over the regenerated tree is both the drift gate and
the idempotency anchor (D-94-10 — a single regen is sufficient; the generators are verified
deterministic).
Output: One new `brandbook-tokens` job appended to `.github/workflows/ci.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-foundation-token-pipeline-ci-gate-new-token-categories/94-CONTEXT.md
@.planning/phases/94-foundation-token-pipeline-ci-gate-new-token-categories/94-RESEARCH.md
@.planning/phases/94-foundation-token-pipeline-ci-gate-new-token-categories/94-PATTERNS.md
@.planning/phases/94-foundation-token-pipeline-ci-gate-new-token-categories/94-UI-SPEC.md

<interfaces>
<!-- Sibling-job analogs in .github/workflows/ci.yml (line numbers verified in-session). -->

cohort-demo-smoke (lines 631-647) — the minimal standalone proof-lane skeleton to clone:
  name, runs-on: ubuntu-22.04, needs: [quality, optional-dependencies] (line 640),
  if: github.repository == 'szTheory/rindle', steps: Checkout (actions/checkout@v4) + one run step.

adoption-demo-e2e (lines 649-757) — the Node/Playwright template:
  - setup-node block (lines 697-699): actions/setup-node@v4 with node-version: "20" (identical to ci.yml:467-470)
  - working-directory: examples/adoption_demo for npm ci (the playwright resolution anchor)
  - upload-artifact@v4 failure block (lines 751-757) — optional, if: failure()

Step order (D-94-02 — LOCKED): npm ci (examples/adoption_demo) -> npx playwright install --with-deps chromium ->
  node brandbook/src/tokens-build.mjs -> node brandbook/src/admin-css-build.mjs ->
  node brandbook/src/admin-contrast.mjs -> node brandbook/src/admin-gallery-check.mjs ->
  node brandbook/src/sync-admin-css.mjs (Plan 01 script) -> git diff --exit-code

Why npm ci in examples/adoption_demo first (D-94-02 / RESEARCH Pitfall 2): admin-gallery-check.mjs:14-15
  resolves `playwright` via that package's createRequire; skipping it red-herrings on
  "Cannot find module 'playwright'" instead of reporting drift.

Locked drift-failure copy (94-UI-SPEC.md Copywriting Contract, line 206 — exact string, not paraphrasable):
  "Generated CSS is out of sync with tokens.json. Run the brandbook generators and commit the result."

Diff coverage (D-94-03/04): a tree-wide `git diff --exit-code` covers brandbook/tokens/rindle-admin.css,
  priv/static/rindle_admin/rindle-admin.css, brandbook/tokens/tokens.css, and brandbook/admin-gallery/index.html
  because every generator + the sync script write into the working tree before the diff.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the standalone brandbook-tokens job to ci.yml in the D-94-02 step order</name>
  <read_first>
    - .github/workflows/ci.yml (the cohort-demo-smoke skeleton at 631-647 — clone its name/runs-on/needs/if/Checkout shape; the adoption-demo-e2e setup-node block at 697-699 and its working-directory npm-ci pattern and the upload-artifact failure block at 751-757)
    - brandbook/src/admin-gallery-check.mjs (lines 14-15 — the createRequire that resolves playwright via examples/adoption_demo; lines 24-25 — it internally re-runs admin-css-build + gallery.mjs, so running admin-css-build explicitly first is idempotent)
    - brandbook/src/sync-admin-css.mjs (Plan 01's sync script — the single mechanism the job invokes as the sync step)
    - scripts/ci/adoption_demo_e2e.sh (the npm ci + npx playwright install --with-deps chromium incantation)
    - 94-PATTERNS.md ".github/workflows/ci.yml" section (the exact skeleton + setup + playwright + failure-artifact clones) and 94-RESEARCH.md Pattern 1 (the full job YAML shape) + Pitfall 1 (the gate goes red on the pre-existing drift, which Plan 01 already fixed) + Pitfall 2
    - 94-UI-SPEC.md Copywriting Contract line 206 (the LOCKED drift-failure message — copy verbatim)
  </read_first>
  <action>
    Append a NEW top-level job named `brandbook-tokens` to `.github/workflows/ci.yml` (D-94-01) —
    do NOT fold it into any existing lane (the brandbook pipeline is pure Node/Playwright and shares
    no Elixir/Postgres/MinIO/ffmpeg setup). Clone the `cohort-demo-smoke` skeleton (631-647) for the
    `name`, `runs-on: ubuntu-22.04`, `needs: [quality, optional-dependencies]`,
    `if: github.repository == 'szTheory/rindle'`, and the `Checkout` step (actions/checkout@v4). Copy
    the setup-node block verbatim from adoption-demo-e2e (697-699): `actions/setup-node@v4` with
    `node-version: "20"`.

    Steps in the LOCKED D-94-02 order: (1) Install adoption_demo node deps with
    `working-directory: examples/adoption_demo`, `run: npm ci` — this is the playwright resolution
    anchor (admin-gallery-check.mjs:14-15 createRequire; RESEARCH Pitfall 2). (2)
    `working-directory: examples/adoption_demo`, `run: npx playwright install --with-deps chromium`.
    (3) `node brandbook/src/tokens-build.mjs`. (4) `node brandbook/src/admin-css-build.mjs`. (5)
    `node brandbook/src/admin-contrast.mjs` (the WCAG gate). (6) `node brandbook/src/admin-gallery-check.mjs`
    (the browser proof; it internally re-runs css-build + gallery.mjs so the gallery index.html — the
    D-94-04 third artifact — is regenerated; running css-build explicitly at step 4 is idempotent and
    keeps failure attribution clean). (7) `node brandbook/src/sync-admin-css.mjs` (Plan 01's script — the
    single sync mechanism that mirrors the generator output to the shipped priv/ copy). (8) The
    merge-blocking diff step: run `git diff --exit-code`; on non-empty diff, print the LOCKED message
    (94-UI-SPEC.md line 206 — verbatim, via `::error::`) and exit 1.

    The tree-wide `git diff --exit-code` covers BOTH committed CSS copies (D-94-03:
    `brandbook/tokens/rindle-admin.css` AND `priv/static/rindle_admin/rindle-admin.css`) plus
    `brandbook/tokens/tokens.css` and the gallery `index.html` (D-94-04), because every generator + the
    sync script write into the working tree before the diff — a single regen is sufficient as the
    idempotency anchor (D-94-10). Optionally append the adoption-demo-e2e upload-artifact@v4 failure block
    (`if: failure()`) to capture the gallery report on failure. Do NOT add any secrets
    (no HEX_API_KEY / MinIO creds) — the job reads none. SCOPE FENCE: this job gates `rindle-admin.css`
    only; it does NOT generate or diff `cohort.css` (hand-authored, Phase 96 — D-94-05).
  </action>
  <acceptance_criteria>
    - The job exists: grep -c '^  brandbook-tokens:' .github/workflows/ci.yml is >= 1 (a new top-level job key, correctly indented).
    - The workflow YAML parses: python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))" exits 0 (or `actionlint .github/workflows/ci.yml` exits 0 if available).
    - The job declares needs: [quality, optional-dependencies]: a YAML-load assertion confirms the brandbook-tokens job's `needs` equals ['quality','optional-dependencies'].
    - The step order matches D-94-02: the `run` commands for tokens-build, admin-css-build, admin-contrast, admin-gallery-check, sync-admin-css, and git diff --exit-code appear in that order within the job (verify via a yaml-load that extracts the job's step run strings in sequence).
    - The locked drift message is present verbatim: grep -c 'Generated CSS is out of sync with tokens.json. Run the brandbook generators and commit the result.' .github/workflows/ci.yml is >= 1.
    - Local dry-run on a clean tree exits 0: running the same command sequence (node tokens-build && node admin-css-build && node admin-contrast && node admin-gallery-check && node sync-admin-css && git diff --exit-code) exits 0 after Plans 01+03 are committed.
    - The gate actually fails on drift: after intentionally dirtying a generated file (e.g. appending a byte to brandbook/tokens/rindle-admin.css), `git diff --exit-code` exits non-zero; restore the file afterward.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/rindle && python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); j=d['jobs']['brandbook-tokens']; assert j['needs']==['quality','optional-dependencies'], j['needs']; runs=[s.get('run','') for s in j['steps']]; order=[i for cmd in ['tokens-build.mjs','admin-css-build.mjs','admin-contrast.mjs','admin-gallery-check.mjs','sync-admin-css.mjs','git diff --exit-code'] for i,r in enumerate(runs) if cmd in r]; assert order==sorted(order), order; print('JOB_OK')" && grep -q 'Generated CSS is out of sync with tokens.json. Run the brandbook generators and commit the result.' .github/workflows/ci.yml && echo GATE_OK</automated>
  </verify>
  <done>A standalone brandbook-tokens job exists in ci.yml with needs: [quality, optional-dependencies], node-20, the playwright-in-adoption_demo anchor, the D-94-02 step order, the locked drift message, and a git diff --exit-code merge-blocker covering both CSS copies + tokens.css + gallery index.html; the YAML parses; a clean-tree dry-run exits 0 and a dirtied tree fails.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> repo working tree | The job clones the repo and regenerates artifacts in the runner's working tree; the only input is the in-tree trusted tokens.json. |
| npm ci -> examples/adoption_demo node_modules | Installs the pre-existing, already-pinned devDependencies (lockfile-driven); phase adds zero new packages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-11 | Tampering | a hand-edited or un-regenerated generated artifact ships (the gate's whole purpose) | mitigate | git diff --exit-code over the regenerated tree (both CSS copies + tokens.css + gallery index.html) is the merge-blocker; the job runs every generator + the sync script before diffing. |
| T-94-12 | Tampering (CI supply-chain) | npm ci pulls a tampered/typosquatted dependency | mitigate | Zero new packages this phase (RESEARCH Package Legitimacy Audit: none added); npm ci is lockfile-pinned to the pre-existing examples/adoption_demo deps; playwright is the only browser dep and is pre-existing. |
| T-94-13 | Gate integrity (false-green) | the gallery check red-herrings on "Cannot find module 'playwright'" and masks real drift | mitigate | D-94-02 step order installs npm ci + playwright in examples/adoption_demo BEFORE the generators, so a missing-module error cannot masquerade as a passing gate. |
| T-94-14 | Information Disclosure | a secret is exposed in the new job | accept | The job declares no secrets (no HEX_API_KEY / MinIO creds); it is pure Node + git. Below HIGH — accepted. |
| T-94-15 | Denial of Service | a flaky gallery browser proof blocks merges | accept | Reuses the existing deterministic admin-gallery-check.mjs already exercised by adoption-demo-e2e; no new nondeterminism introduced. Below HIGH — accepted. |
</threat_model>

<verification>
- python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" exits 0 (workflow parses).
- The brandbook-tokens job declares needs: [quality, optional-dependencies] and the D-94-02 step order (verified by the YAML-load assertion).
- Local dry-run of the command sequence on a clean tree (post Plans 01+03) exits 0; a dirtied generated file makes git diff --exit-code exit non-zero.
- The locked drift-failure message is present verbatim.
</verification>

<success_criteria>
- A standalone merge-blocking brandbook-tokens CI job regenerates rindle-admin.css (+ tokens.css, gallery index.html) from tokens.json, runs the WCAG contrast gate + gallery proof, syncs the shipped copy, and fails on any uncommitted diff.
- The gate covers BOTH committed CSS copies plus tokens.css and the gallery index.html (no silent shipped-package drift).
- A single regen on unchanged source is an empty diff (idempotency anchor); a dirtied tree fails the gate.
- No new dependencies, no secrets; cohort.css explicitly out of scope (Phase 96).
</success_criteria>

<output>
Create `.planning/phases/94-foundation-token-pipeline-ci-gate-new-token-categories/94-04-SUMMARY.md` when done.
</output>

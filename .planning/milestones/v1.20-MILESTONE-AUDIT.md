---
milestone: v1.20
milestone_name: CI/CD Performance
audited: 2026-06-22T21:00:00Z
burndown: 2026-06-22T22:30:00Z
status: passed   # was tech_debt at audit time; debt burned down — see Burndown 2026-06-22
scores:
  requirements: 18/18
  phases: 5/5
  integration: 6/6 cross-phase connections wired
  flows: 1/1 (PR → CI Summary → branch protection → push:main full matrix → release gate)
gaps:
  requirements: []   # none unsatisfied; none orphaned
  integration: []    # all WIRED, no blockers (1 cosmetic prose-only warning, not a defect)
  flows: []          # end-to-end pipeline chain intact
tech_debt:
  - phase: 107-reliability-security-dx-hardening
    items:
      - "PRE-EXISTING (not milestone-introduced) test failure: test/install_smoke/release_docs_parity_test.exs:319 (`assert running =~ \"Package Consumer Proof Matrix\"`) fails on the current working tree, so local `mix ci` (a HARD-03 deliverable) exits non-zero. Root cause: the parked `.planning`/docs-archive-cleanup working-tree changeset (see Working-Tree State). Confirmed phase-independent (fails identically with 107 changes reverted; stable across seeds). HARD-03 itself is satisfied — the alias is correctly composed; the failure is environmental."
      - "--partitions wiring DEFERRED (D-01, intentional/evidence-gated). Tracked as DEFER-02 (larger runners only if core-starvation is measured)."
  - phase: cross-milestone (working tree)
    items:
      - "Uncommitted working-tree state: a large archive-cleanup changeset deletes prior-milestone .planning/phases/{100,101,102,...} files (v1.19 phases). This is the root cause of the release_docs_parity failure above. It is parked/un-owned and should be committed or stashed under its own change set before relying on a clean local `mix ci` exit 0."
  - phase: nyquist-coverage
    items:
      - "Phase 103: VALIDATION.md exists but status: draft / nyquist_compliant: false → PARTIAL."
      - "Phase 104: no VALIDATION.md → MISSING."
      - "Phase 106: no VALIDATION.md → MISSING."
  - phase: 999.1-v1-20-ci-green-up-s3-cred-test-failures
    items:
      - "Empty backlog directory (0 plans). NOT part of the v1.20 definition-of-done (DoD = Phases 103–107). The CI green-up work it captured was effectively performed in the Phase 105 reconciliation (json_polyfill/--check-locked fix 0751fdb; S3-IMDS test stub 6d2a385; CI Summary ran green on main; live flip executed). Recommend closing/removing the stale backlog stub."
  - phase: traceability-hygiene
    items:
      - "SUMMARY.md `requirements-completed` frontmatter is sparsely populated (the weakest of the 3 cross-reference sources): present on only 8/16 milestone SUMMARYs. Did not affect status determination — VERIFICATION.md coverage tables + REQUIREMENTS.md traceability (the two strong sources) both confirm 18/18 SATISFIED — but worth backfilling for clean 3-source provenance."
nyquist:
  compliant_phases: [103, 104, 105, 106, 107]   # 103 closed post-burndown (commits f1dbea4 + 8131a19); re-audit 2026-06-22
  partial_phases: []
  missing_phases: []
  overall: compliant
re_audit: 2026-06-22   # /gsd-audit-milestone re-run: confirmed 18/18, 5/5 passed, integration PASS, Nyquist now 5/5
---

# v1.20 CI/CD Performance — Milestone Audit

**Audited:** 2026-06-22 · **Debt burned down:** 2026-06-22 (see [Burndown](#burndown-2026-06-22))
**Status:** `passed` (post-burndown) — all 18 requirements satisfied, all cross-phase wiring intact, no critical blockers. At audit time this was `tech_debt`; the material debt has since been closed: local `mix ci` is green (1186 tests, 0 failures), the working tree is clean, and 4/5 phases are now Nyquist-compliant with committed regression tests. Remaining items are cosmetic/accepted (Phase 103 draft VALIDATION; a stale backlog stub).
**Milestone goal:** Cut PR CI feedback time and harden gate determinism/reliability — without dropping real quality signal — via a measure → classify → restructure pass shipped as stepwise PRs. Non-feature / DX-infrastructure milestone: **ZERO `lib/` public-API change**.

## Definition of Done

Phases **103–107** (the 999.1 entry is a *backlog* item, not part of DoD). Load-bearing dependency order delivered intact: observability → cache/tooling → aggregate-required-check → lane-split → reliability/security/DX. All three hard invariants held across every phase:

- `ci.yml` filename + `name: CI` byte-unchanged (release-train coupling preserved). ✓
- `CI Summary` treats `skipped` as pass (fork-PR safety). ✓
- Release full-verification gate never weakened (full 5-profile matrix runs on push:main and feeds `gate-ci-green`). ✓

## Requirements Coverage — 18/18 satisfied (3-source cross-reference)

Each REQ-ID cross-referenced across VERIFICATION.md coverage tables, SUMMARY frontmatter, and the REQUIREMENTS.md traceability table. No unsatisfied requirements; no orphans (every REQ-ID appears in its phase's VERIFICATION coverage table and maps 1:1 in REQUIREMENTS.md).

| REQ | Phase | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS.md | Integration | Final |
|-----|-------|--------------|---------------------|-----------------|-------------|-------|
| OBS-01 | 103 | passed (SATISFIED) | — | `[x]` | WIRED | **satisfied** |
| OBS-02 | 103 | passed (SATISFIED) | — | `[x]` | WIRED | **satisfied** |
| OBS-03 | 103 | passed (SATISFIED) | listed (103-04) | `[x]` | WIRED (self-contained) | **satisfied** |
| CACHE-01 | 104 | passed (SATISFIED) | listed (104-02/03/04) | `[x]` | WIRED | **satisfied** |
| CACHE-02 | 104 | passed (SATISFIED) | listed (104-03) | `[x]` | WIRED | **satisfied** |
| CACHE-03 | 104 | passed (SATISFIED) | listed (104-02) | `[x]` | WIRED (moved to nightly) | **satisfied** |
| CACHE-04 | 104 | passed (SATISFIED) | listed (104-02) | `[x]` | WIRED | **satisfied** |
| CACHE-05 | 104 | passed (SATISFIED) | listed (104-02/04) | `[x]` | WIRED | **satisfied** |
| GATE-01 | 105 | passed (SATISFIED) | — | `[x]` | WIRED | **satisfied** |
| GATE-02 | 105 | passed (SATISFIED) | — | `[x]` | WIRED | **satisfied** |
| LANE-01 | 106 | passed (SATISFIED) | — | `[x]` | WIRED | **satisfied** |
| LANE-02 | 106 | passed (SATISFIED) | — | `[x]` | WIRED | **satisfied** |
| LANE-03 | 106 | passed (SATISFIED) | — | `[x]` | WIRED | **satisfied** |
| LANE-04 | 106 | passed (SATISFIED) | — | `[x]` | WIRED | **satisfied** |
| HARD-01 | 107 | passed (SATISFIED) | listed (107-01) | `[x]` | WIRED (self-contained) | **satisfied** |
| HARD-02 | 107 | passed (SATISFIED) | — | `[x]` | WIRED | **satisfied** |
| HARD-03 | 107 | passed (SATISFIED) | listed (107-03) | `[x]` | WIRED | **satisfied** |
| HARD-04 | 107 | passed (SATISFIED) | listed (107-04) | `[x]` | WIRED | **satisfied** |

> Note: SUMMARY `requirements-completed` frontmatter is under-populated (the weak source). Per the status-determination matrix this would normally flag "verify manually," but the two authoritative sources (VERIFICATION coverage tables with explicit SATISFIED + evidence, and REQUIREMENTS.md `[x]`) independently confirm every REQ. Backfill recommended (see tech debt) but not a coverage gap.

## Phase Verification — 5/5 passed

| Phase | Score | Status | VERIFICATION date |
|-------|-------|--------|-------------------|
| 103 Observability / Baseline | 19/19 | passed | 2026-06-20 |
| 104 Cache & Tooling Hygiene | 24/24 | passed | 2026-06-21 |
| 105 Aggregate Required Check + Branch-Protection Flip | 6/6 | passed (human-verif items reconciled 2026-06-22 — **live flip executed**) | 2026-06-21 |
| 106 Trigger Split + Matrix/Lane Refinement | 5/5 | passed | 2026-06-22 |
| 107 Reliability, Security & DX Hardening | 14/14 | passed | 2026-06-22 |

All five VERIFICATION.md files are present and report `status: passed` with zero critical gaps. Phase 105's two post-merge human-verification items (live branch-protection flip; fork-PR skip-as-pass) were both resolved — the flip was executed live (`bash scripts/setup_branch_protection.sh main` → live `.contexts[] == ["CI Summary"]`, empty diff) and the skip-as-pass logic was extracted to `scripts/ci/eval_ci_summary.sh` and pinned by a merge-blocking `ci-script-tests` job.

## Cross-Phase Integration — all WIRED

Independent integration check traced all six cross-phase connections against the actual files (not summaries):

1. **103→104 cache handoff** — OBS-01 summary rows read the composite's `deps/build-cache-hit` outputs; zero dangling pre-composite cache step refs. WIRED.
2. **104→105 aggregate** — all 10 `ci-summary.needs` entries map to real surviving job keys. WIRED.
3. **105→106 lane split** — `ci-summary.needs` internally consistent post-split (`package-consumer-full`, soak/live, demo, observability correctly excluded); `REQUIRED_CHECKS=("CI Summary")` byte-matches the job `name:`. WIRED.
4. **Release-train coupling** — `name: CI` + filename intact; `release-please-automerge.yml` → `workflows: [CI]`; `nightly.yml` structurally invisible; `gate-ci-green` waits on the full-matrix push:main run conclusion. Gate NOT weakened by 106. WIRED.
5. **107 over settled topology** — every `uses:` SHA-pinned across all 4 workflows + 2 composites (no mutable `@vX` survives); least-privilege permissions consistent; `mix ci` mirrors the real PR merge-blocking set; README/CONTRIBUTING reference the real `CI Summary` check. WIRED.
6. **E2E pipeline flow** — PR → fast lane (concurrency cancel-stale, PR-only) → `CI Summary` aggregate → branch protection (only CI Summary) → merge → push:main full matrix → `release.yml gate-ci-green` → publish. No broken link. WIRED.

The single integration "warning" was a cosmetic imprecision in the audit prompt's own prose about the `gate-ci-green` trigger; the underlying coupling is sound. **No integration blockers.**

## Tech Debt (non-blocking — review before close)

> **⚠ Audit-time snapshot — mostly superseded.** Items 1 and 2 were closed in the
> [Burndown 2026-06-22](#burndown-2026-06-22) (root-cause `mix ci` fix; Nyquist 104+106 now
> compliant). The detail below is retained as the as-audited record.

### 1. Parked archive-cleanup changeset → red local `mix ci` (most material)
A large uncommitted working-tree changeset deletes prior-milestone (`v1.19`) `.planning/phases/{100,101,102,…}` files. It causes `test/install_smoke/release_docs_parity_test.exs:319` to fail, so local `mix ci` exits non-zero on the current tree. The failure is **pre-existing and phase-independent** (fails identically with Phase 107 reverted; stable across seeds) and does **not** invalidate HARD-03 — the `mix ci` alias is correctly composed and runs the default-tag suite; the lone failure is environmental. **Action:** commit or stash the archive-cleanup changeset under its own change set (and fix or update the RUNNING.md docs-parity assertion) so local `mix ci` exits 0.

### 2. Nyquist validation coverage — partial
The validate-phase step hook is active for this project. VALIDATION.md status across the milestone:

| Phase | VALIDATION.md | nyquist_compliant | Status | Suggested action |
|-------|---------------|-------------------|--------|------------------|
| 103 | exists (draft) | false | PARTIAL | `/gsd-validate-phase 103` |
| 104 | missing | — | MISSING | `/gsd-validate-phase 104` |
| 105 | exists (approved) | true | COMPLIANT | — |
| 106 | missing | — | MISSING | `/gsd-validate-phase 106` |
| 107 | exists (approved) | true | COMPLIANT | — |

These are validation-strategy/coverage gaps, not requirement failures — phases verified `passed` independently. Discovery only.

### 3. Stale backlog stub
`999.1-v1-20-ci-green-up-s3-cred-test-failures/` is an empty (0-plan) backlog directory, not part of DoD. Its CI green-up intent was satisfied during the Phase 105 reconciliation. Recommend removing the stale stub.

### 4. Traceability hygiene
Backfill `requirements-completed` frontmatter on the milestone SUMMARYs that omit it (8/16), for clean 3-source provenance on future audits.

### 5. Intentional deferrals (tracked, not debt to act on now)
`--partitions` (HARD-01, D-01) deferred pending measured core-starvation evidence (DEFER-02); DEFER-01 (flaky-quarantine lane) and DEFER-03 (property-based nightly expansion) tracked in REQUIREMENTS.md.

## Verdict

v1.20 **achieved its definition of done**: 18/18 requirements satisfied across 5/5 verified phases, all cross-phase wiring intact, the headline PR-lane wall-clock cut delivered, and all three hard release-coupling invariants preserved (with the live branch-protection flip executed). Status is `tech_debt` rather than `passed` only because of accumulated non-blocking debt — chiefly a parked archive-cleanup changeset that reddens local `mix ci`, plus partial Nyquist validation coverage on phases 103/104/106. None of these block milestone completion; they are review-and-close items.

## Burndown 2026-06-22

Tech-debt items addressed immediately after the audit (maintainer-directed):

| Audit item | Disposition | Evidence |
|------------|-------------|----------|
| **Red local `mix ci`** (release_docs_parity_test:336) | **FIXED at real root cause.** Re-investigation showed the failure was NOT the parked `.planning` changeset (mis-attributed in the 107 notes) — it was committed Phase-106 staleness: RUNNING.md was realigned to the `package-consumer` split in `5c30b7f` but the parity test still asserted the retired "Package Consumer Proof Matrix" job name. Updated the assertion to the shipped `package-consumer-full`. | `31f849d`; `mix ci` → 1186 tests, 0 failures, exit 0 |
| **Parked archive-cleanup changeset** (284 deleted prior-milestone phase docs) | **RESTORED** (maintainer chose clean-tree-keep-history). Raw delete, not a proper archive-move; unrelated to the `mix ci` fix. Working tree now clean; full planning history intact. A real `/gsd-cleanup` can archive 81–102 properly later if desired. | `git restore`; tree clean |
| **Untracked 103/105 PATTERNS.md** | **TRACKED** (belonged to current-milestone phases). | `0e67911` |
| **Nyquist MISSING: Phase 104** | **CLOSED → COMPLIANT.** Added committed parity test `test/install_smoke/ci_cache_hygiene_test.exs` (11 tests, CACHE-01/02/03/05; CACHE-04 already regressed by `mix ci`) + `104-VALIDATION.md`. | `934b35d`, `ce0a972` |
| **Nyquist MISSING: Phase 106** | **CLOSED → COMPLIANT.** Added committed parity test `test/install_smoke/ci_lane_split_test.exs` (15 tests, LANE-01..04 + release-coupling invariant) + `106-VALIDATION.md`. | `3b9c9df`, `d6fc2a6` |
| **Nyquist PARTIAL: Phase 103** | **ACCEPTED-PARTIAL** (maintainer-scoped out). VALIDATION.md exists (draft); phase independently verified `passed` (19/19). Run `/gsd-validate-phase 103` later to fully close. | n/a |
| **Stale backlog stub `999.1/`** | **LEFT** (empty `.gitkeep` backlog placeholder; cosmetic). Remove when convenient. | n/a |
| **SUMMARY `requirements-completed` frontmatter** | **SKIPPED** (inconsistent schema across files; the audit confirmed it does not affect coverage — VERIFICATION + REQUIREMENTS are authoritative). | n/a |

**Net effect:** the milestone moved from `tech_debt` to `passed`. Two new merge-blocking regression
tests now guard the CI cache-hygiene and lane-split topology (exactly the drift class that produced
the original red `mix ci`). Nyquist coverage: 4/5 phases compliant, 1 accepted-partial.

## Re-Audit 2026-06-22

A second `/gsd-audit-milestone` run confirms the milestone is fully clean and improves on
the burndown's one remaining loose end:

- **Requirements:** 18/18 satisfied — every REQ-ID re-checked SATISFIED in its phase
  VERIFICATION.md coverage table (3-source cross-reference holds; no orphans).
- **Phases:** 5/5 `passed` (103:19/19, 104:24/24, 105:6/6, 106:5/5, 107:14/14).
- **Integration:** re-run via gsd-integration-checker against live files — **PASS**, 8/8
  wiring claims WIRED, 0 critical. One by-design WARNING only: `brandbook-tokens` is
  repo-gated without an `event_name` clause yet sits in `ci-summary.needs`, so on fork PRs
  it skips→pass and `CI Summary` carries no brandbook-drift signal on forks (intentional
  secrets-style gating; drift still caught on same-repo PR / push:main).
- **Nyquist: now 5/5 COMPLIANT.** The burndown's lone accepted-partial (Phase 103) was
  closed afterward — `103-VALIDATION.md` is now `status: approved, nyquist_compliant: true,
  wave_0_complete: true` (commits `f1dbea4` test + `8131a19` strategy). overall: compliant.
- **Live flip / 999.1:** confirmed — `setup_branch_protection.sh`, `eval_ci_summary.sh`,
  `nightly.yml`, `branch-protection-apply.yml`, composite action, dependabot, and the 999.1
  green-up commits (`0751fdb`, `6d2a385`, `ca70075`) all present on disk.

**Verdict: `passed`.** No critical gaps. Residual items are cosmetic only (sparse SUMMARY
`requirements-completed` frontmatter; 107 VALIDATION `wave_0_complete: false`; empty 999.1
backlog stub). Ready for `/gsd-complete-milestone v1.20`.

---
_Audited 2026-06-22 by Claude (gsd-audit-milestone orchestrator). Integration verified by gsd-integration-checker against live files. Burndown 2026-06-22: parity-test fix + tree restore + Nyquist closure for phases 104/106._

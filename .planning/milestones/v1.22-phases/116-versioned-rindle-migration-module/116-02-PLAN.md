---
phase: 116-versioned-rindle-migration-module
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/rindle/ops/runtime_checks_test.exs
  - test/rindle/doctor_test.exs
  - test/rindle/ops/runtime_status_test.exs
  - test/rindle/runtime_status_task_test.exs
autonomous: true
requirements:
  - MIGRATE-01
  - MIGRATE-02
must_haves:
  truths:
    - "MIGRATE-01: doctor health accepts both fresh `Rindle.Migration` installs and healthy legacy packaged migration installs."
    - "MIGRATE-02: doctor and runtime status report host-owned Oban readiness without claiming Rindle owns `oban_jobs`."
    - "Runtime status setup failures are actionable and avoid raw missing-table crashes."
  artifacts:
    - path: "test/rindle/ops/runtime_checks_test.exs"
      provides: "Hybrid marker/catalog/legacy/Oban doctor health contract"
      contains: "doctor.rindle_schema.ready"
    - path: "test/rindle/doctor_test.exs"
      provides: "CLI doctor output contract for warning/error semantics"
      contains: "doctor.oban_jobs.ready"
    - path: "test/rindle/ops/runtime_status_test.exs"
      provides: "Runtime setup preflight service contract"
      contains: "setup_incomplete"
    - path: "test/rindle/runtime_status_task_test.exs"
      provides: "Runtime status CLI failure copy contract"
      contains: "Oban.Migration"
  key_links:
    - from: "test/rindle/ops/runtime_checks_test.exs"
      to: "lib/rindle/ops/runtime_checks.ex"
      via: "injectable doctor inputs for marker/catalog/legacy/Oban readiness"
      pattern: "doctor\\.(rindle_schema|oban_jobs)\\.ready"
    - from: "test/rindle/ops/runtime_status_test.exs"
      to: "lib/rindle/ops/runtime_status.ex"
      via: "runtime preflight returns structured setup failures before report queries"
      pattern: "setup_incomplete"
---

<objective>
Create the Wave 1 RED contract for doctor and runtime compatibility.

Purpose: Ensure the implementation replaces file-history-only health with a hybrid model while preserving existing adopters and making host-owned Oban failures explicit.
Output: Failing tests for doctor/runtime marker, catalog, legacy, and `oban_jobs` readiness semantics.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/116-versioned-rindle-migration-module/116-CONTEXT.md
@.planning/phases/116-versioned-rindle-migration-module/116-RESEARCH.md
@.planning/phases/116-versioned-rindle-migration-module/116-PATTERNS.md
@.planning/phases/116-versioned-rindle-migration-module/116-VALIDATION.md
@lib/rindle/ops/runtime_checks.ex
@lib/rindle/ops/runtime_status.ex
@lib/mix/tasks/rindle.doctor.ex
@lib/mix/tasks/rindle.runtime_status.ex
@test/rindle/ops/runtime_checks_test.exs
@test/rindle/doctor_test.exs
@test/rindle/ops/runtime_status_test.exs
@test/rindle/runtime_status_task_test.exs
</context>

## Artifacts this phase produces

- Public module: `Rindle.Migration`.
- Public functions: `Rindle.Migration.up/1`, `Rindle.Migration.down/1`.
- Hidden implementation modules: `Rindle.Migration.Options`, `Rindle.Migration.V1`.
- Internal helper functions: `Rindle.Migration.Options.validate!/1`; `Rindle.Migration.V1.up/1`, `down/1`, `current_version/0`, `marker_table/0`, `rindle_tables/0`, `catalog_requirements/0`.
- Supported migration options: `:version` with supported value `1`; `:prefix` with default `"public"`.
- Marker table: `rindle_migration_versions`, owned by Rindle, prefix-aware, not Ecto `schema_migrations`.
- Doctor/status identifiers: existing `doctor.migrations.pending` and `doctor.migrations.unresolved` retained where practical; new `doctor.rindle_schema.ready` and `doctor.oban_jobs.ready`; runtime errors `{:setup_incomplete, :rindle_schema}` and `{:setup_incomplete, :oban_jobs}`.
- New source paths: `lib/rindle/migration.ex`, `lib/rindle/migration/options.ex`, `lib/rindle/migration/v1.ex`, `test/rindle/migration_test.exs`.
- Updated legacy path: `priv/repo/migrations/20260424205942_create_oban_tables.exs` becomes a compatibility stub while preserving the filename.
- Docs snippets: pinned host migration calling `Rindle.Migration.up(version: 1)` and `down(version: 1)`; host-owned `Oban.Migration` migration; default schema remains `public`; rollback backup note; legacy packaged migrations remain valid.

<tasks>

<task type="auto">
  <name>Task 1: Add RED hybrid doctor migration-health tests</name>
  <files>test/rindle/ops/runtime_checks_test.exs, test/rindle/doctor_test.exs</files>
  <read_first>
    - .planning/phases/116-versioned-rindle-migration-module/116-CONTEXT.md
    - .planning/phases/116-versioned-rindle-migration-module/116-RESEARCH.md
    - .planning/phases/116-versioned-rindle-migration-module/116-PATTERNS.md
    - .planning/phases/116-versioned-rindle-migration-module/116-VALIDATION.md
    - lib/rindle/ops/runtime_checks.ex
    - lib/mix/tasks/rindle.doctor.ex
    - test/rindle/ops/runtime_checks_test.exs
    - test/rindle/doctor_test.exs
  </read_first>
  <action>Extend runtime-check and doctor CLI tests for the hybrid model required by D-21 through D-27. Add fixtures or injected inputs that represent a fresh marker/catalog install, a healthy legacy 15-file install, incomplete Rindle-owned schema, unresolved legacy file-history drift with healthy catalog, missing Oban config, and missing host-owned `oban_jobs`. Require `OK` when either fresh marker/catalog or healthy legacy catalog is current per D-22, `ERROR` when Rindle-owned tables or host-owned Oban readiness is missing per D-23 and D-25, and warning/history-only copy for healthy legacy drift per D-24 and D-41. Preserve existing stable IDs where practical and lock any new IDs, including `doctor.rindle_schema.ready` and `doctor.oban_jobs.ready`, rather than weakening assertions per D-27. The copy must say host apps install Oban through `Oban.Migration`; Rindle no longer manages `oban_jobs` per D-10 and D-32.</action>
  <verify>
    <automated>set +e; output=$(mix test test/rindle/ops/runtime_checks_test.exs test/rindle/doctor_test.exs --seed 0 2&gt;&amp;1); status=$?; printf '%s\n' "$output"; test $status -ne 0 &amp;&amp; printf '%s\n' "$output" | rg 'doctor\\.rindle_schema\\.ready|doctor\\.oban_jobs\\.ready|Oban\\.Migration|oban_jobs|legacy'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: runtime-check tests cover fresh marker/catalog success and healthy legacy success as separate cases.
    - Source assertion: doctor tests assert warning-only legacy drift when catalog checks prove Rindle-owned schema readiness.
    - Source assertion: doctor tests assert missing `oban_jobs` is an error with host-owned `Oban.Migration` setup copy.
    - Behavior assertion: the focused command is RED until runtime checks implement the hybrid health model.
  </acceptance_criteria>
  <done>Doctor RED tests lock hybrid migration health, warning/error semantics, stable IDs, and host-owned Oban setup copy.</done>
</task>

<task type="auto">
  <name>Task 2: Add RED runtime status setup-preflight tests</name>
  <files>test/rindle/ops/runtime_status_test.exs, test/rindle/runtime_status_task_test.exs</files>
  <read_first>
    - .planning/phases/116-versioned-rindle-migration-module/116-CONTEXT.md
    - .planning/phases/116-versioned-rindle-migration-module/116-RESEARCH.md
    - .planning/phases/116-versioned-rindle-migration-module/116-PATTERNS.md
    - lib/rindle/ops/runtime_status.ex
    - lib/mix/tasks/rindle.runtime_status.ex
    - test/rindle/ops/runtime_status_test.exs
    - test/rindle/runtime_status_task_test.exs
  </read_first>
  <action>Extend runtime-status service and task tests for setup preflight per D-21, D-25, and D-26. Add injectable readiness fixtures so tests can prove `RuntimeStatus.runtime_status/1` returns `{:error, {:setup_incomplete, :rindle_schema}}` when Rindle-owned tables are absent and `{:error, {:setup_incomplete, :oban_jobs}}` when host-owned `oban_jobs` is absent before normal report queries run. Assert the CLI exits non-zero and prints bounded operator copy naming `mix rindle.doctor`, `Oban.Migration`, and that Rindle does not manage `oban_jobs` per D-23, D-25, D-32, and D-38. Preserve the existing success report shape for healthy setups.</action>
  <verify>
    <automated>set +e; output=$(mix test test/rindle/ops/runtime_status_test.exs test/rindle/runtime_status_task_test.exs --seed 0 2&gt;&amp;1); status=$?; printf '%s\n' "$output"; test $status -ne 0 &amp;&amp; printf '%s\n' "$output" | rg 'setup_incomplete|rindle_schema|oban_jobs|Oban\\.Migration'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: service tests assert both `{:setup_incomplete, :rindle_schema}` and `{:setup_incomplete, :oban_jobs}` error reasons.
    - Source assertion: task tests assert non-zero exit and operator copy for missing host-owned Oban setup.
    - Behavior assertion: healthy runtime-status tests still assert the existing report keys and recommendations remain available.
    - Behavior assertion: the focused command is RED until runtime preflight is implemented.
  </acceptance_criteria>
  <done>Runtime status RED tests lock setup preflight behavior and CLI copy without changing the successful report contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| database catalog -&gt; doctor output | Catalog and migration-history observations become operator guidance. |
| runtime-status CLI -&gt; host operator | Setup failures are rendered as actionable terminal output. |
| host Oban state -&gt; Rindle diagnostics | Rindle observes `oban_jobs` readiness without owning the table. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-05 | Tampering | doctor migration-health model | mitigate | Tests require catalog readiness plus marker/legacy compatibility instead of file-history-only truth. |
| T-116-06 | Tampering | legacy repair copy | mitigate | Tests require healthy legacy drift to be warning/history-only and never instruct deletion or replay. |
| T-116-07 | Information Disclosure / Repudiation | runtime status setup failures | mitigate | Tests require bounded structured errors and operator copy instead of raw database exceptions. |
| T-116-08 | Tampering / Denial of Service | host-owned `oban_jobs` boundary | mitigate | Tests require Rindle diagnostics to observe host Oban readiness and direct setup to `Oban.Migration`. |
| T-116-SC | Tampering | package installs | accept | No package-manager install task is planned; existing dependencies are used. |
</threat_model>

<verification>
- RED doctor/runtime checks: `mix test test/rindle/ops/runtime_checks_test.exs test/rindle/doctor_test.exs --seed 0` fails on missing hybrid implementation.
- RED runtime status checks: `mix test test/rindle/ops/runtime_status_test.exs test/rindle/runtime_status_task_test.exs --seed 0` fails on missing setup preflight implementation.
</verification>

<success_criteria>
- MIGRATE-01 has RED doctor tests accepting both fresh marker/catalog installs and healthy legacy installs.
- MIGRATE-02 has RED doctor/runtime tests that preserve host ownership of Oban and make missing `oban_jobs` actionable.
- No test asks existing adopters to remove or replay legacy packaged migrations.
</success_criteria>

<output>
Create `.planning/phases/116-versioned-rindle-migration-module/116-02-SUMMARY.md` when done.
</output>

---
phase: 114-oss-trust-governance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - test/install_smoke/package_metadata_test.exs
  - scripts/verify_hex_package_metadata.sh
  - .github/workflows/release.yml
  - guides/release_publish.md
autonomous: true
requirements: [META-01, META-02]

must_haves:
  truths:
    - "hex.pm surfaces a 'Changelog' link (GitHub blob URL on main) and a 'Docs' link (hexdocs.pm/rindle) alongside the existing 'GitHub' link"
    - "The release public-verification path checks the Hex API for the owner-derived maintainer signal (`owners[].username == \"sztheory\"`) after publish"
    - "The merge-blocking package_metadata_test passes with the new three-entry links map and release-verifier wiring assertions (the META-01 meta-test coupling footgun is closed)"
  artifacts:
    - mix.exs
    - test/install_smoke/package_metadata_test.exs
    - scripts/verify_hex_package_metadata.sh
    - .github/workflows/release.yml
    - guides/release_publish.md
  key_links:
    - "mix.exs package.links edit <-> package_metadata_test.exs links assertion (line ~71-72): MUST change together in the same task or the merge-blocking Quality lane red-CIs (RESEARCH Pitfall 1)"
    - "Changelog link -> https://github.com/szTheory/rindle/blob/main/CHANGELOG.md (CHANGELOG.md already ships in the tarball; hex.pm specially surfaces a link named 'Changelog')"
    - "Docs link -> https://hexdocs.pm/rindle (no version suffix -> always latest)"
    - "META-02 owner signal -> scripts/verify_hex_package_metadata.sh -> Hex package API owners[] (Hex no longer serializes mix.exs :maintainers into hex_metadata.config)"
---

<objective>
Add the conventional Hex package links so hex.pm surfaces the expected Changelog and Docs entries (META-01), and wire release public verification for the owner-derived maintainer signal (META-02). Single small edit to mix.exs paired with the meta-test assertion update and release verifier wiring.

Purpose: A visitor on the hex.pm package page sees Changelog + Docs links (not just GitHub) and a named maintainer — the conventional trust signals for a published Hex library.

Output: mix.exs `package/0` gains two `links` entries ("Changelog", "Docs"); `test/install_smoke/package_metadata_test.exs` updated to expect the three-entry links map and public-verifier wiring; release public verification checks the Hex API owner signal so the merge-blocking and release gates stay green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-oss-trust-governance/114-RESEARCH.md
@.planning/phases/114-oss-trust-governance/114-VALIDATION.md
@mix.exs
@test/install_smoke/package_metadata_test.exs
</context>

<artifacts_produced>
## Artifacts this plan produces

| Artifact | Kind | Requirement |
|----------|------|-------------|
| mix.exs `package/0` `links` entries "Changelog" + "Docs" | added map entries | META-01 |
| `test/install_smoke/package_metadata_test.exs` links + release-verifier assertions | modified meta-test | META-01, META-02 |
| `scripts/verify_hex_package_metadata.sh` | new release verification script | META-02 |
| `.github/workflows/release.yml` public verification step | modified workflow | META-02 |
| `guides/release_publish.md` runbook owner-verification note | modified docs | META-02 |

NOT touched: mix.exs `files:` list (no governance files added — that is Plan 01's concern and stays repo-only).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: mix.exs links + maintainers, paired with meta-test update (META-01, META-02)</name>
  <files>mix.exs, test/install_smoke/package_metadata_test.exs</files>
  <read_first>
    - mix.exs (lines 279-288 — the `defp package do` block; `@source_url` is defined at line 5 as "https://github.com/szTheory/rindle")
    - test/install_smoke/package_metadata_test.exs (lines 61-78 — the "unpacked metadata contains the expected identity" test; the `links` assertion is at lines 71-72)
    - .planning/phases/114-oss-trust-governance/114-RESEARCH.md (META-01/02 sections lines 211-266 + Pitfall 1 lines 278-289 — pinned syntax and the exact serialized metadata format)
  </read_first>
  <action>
Edit `mix.exs` `defp package do` (lines 280-287). Add two entries to the `links` map (META-01): `"Changelog" => "#{@source_url}/blob/main/CHANGELOG.md"` and `"Docs" => "https://hexdocs.pm/rindle"`, keeping the existing `"GitHub" => @source_url`. Leave `files:` and `licenses:` unchanged. Use `@source_url` interpolation for the Changelog URL (do not hard-code the GitHub org/repo). Do not add `maintainers:` to `package/0`; Hex owner/maintainer display is owner-derived and verified after publish.

In the SAME task (this is the RESEARCH Pitfall 1 / install_smoke meta-test coupling footgun — the edit and the test MUST land together or the merge-blocking Quality lane goes red), update `test/install_smoke/package_metadata_test.exs`. The current assertion at lines 71-72 asserts the serialized `links` equals exactly `{<<"links">>,[{<<"GitHub">>,<<"https://github.com/szTheory/rindle">>}]}.` — that whole-list equality will break. Replace it with order-independent per-entry assertions: assert `metadata =~ ~s({<<"GitHub">>,<<"https://github.com/szTheory/rindle">>})`, assert `metadata =~ ~s({<<"Changelog">>,<<"https://github.com/szTheory/rindle/blob/main/CHANGELOG.md">>})`, and assert `metadata =~ ~s({<<"Docs">>,<<"https://hexdocs.pm/rindle">>})`. Add META-02 assertions that the release workflow runs `scripts/verify_hex_package_metadata.sh "$VERSION"` after Hex indexing and that the script checks `owners[]` for `sztheory`. Do not touch `@required_paths` or `@prohibited_paths` — governance files stay repo-only and are Plan 01's concern.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rindle && grep -q 'Changelog' mix.exs && grep -q 'hexdocs.pm/rindle' mix.exs && grep -q 'verify_hex_package_metadata' .github/workflows/release.yml && mix test test/install_smoke/package_metadata_test.exs</automated>
  </verify>
  <done>mix.exs `package/0` declares a three-entry `links` map (GitHub + Changelog + Docs using `@source_url` interpolation); package_metadata_test.exs asserts all three links per-entry plus release public-verifier wiring and passes GREEN. `files:`/`@required_paths`/`@prohibited_paths` unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer build config -> hex.pm package page | Metadata keys in mix.exs are serialized into the published tarball and rendered on the public hex.pm page. |

## STRIDE Threat Register

This phase changes two static metadata keys — no executable code, no new attack surface. Severities are LOW and honestly right-sized.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-114-05 | Spoofing / Information Disclosure | package.links targets | low | mitigate | Links point only to first-party canonical URLs (GitHub repo on `main`, hexdocs.pm/rindle) via `@source_url` interpolation — no third-party or user-controlled redirect target. |
| T-114-06 | Tampering | meta-test ↔ mix.exs coupling | low | mitigate | Pair the mix.exs edit with the package_metadata_test assertion update in one task so the merge-blocking lane is never left red and the published metadata is verified before release (RESEARCH Pitfall 1). |
| T-114-07 | Spoofing | Hex owner signal | low | mitigate | Verify the public Hex package API exposes `sztheory` in `owners[]` after publish; do not rely on unsupported local `mix.exs` maintainer keys. |

No npm/pip/cargo installs — no `T-114-SC` supply-chain threat applies.
</threat_model>

<verification>
- `mix test test/install_smoke/package_metadata_test.exs` is GREEN with the new three-entry links map and release public-verifier assertions.
- `mix ci` is GREEN (the merge-blocking Quality lane) — confirms META-01 did not red-CI the meta-test.
- Release public verification: after publish, `scripts/verify_hex_package_metadata.sh "$VERSION"` checks the package API for Changelog + Docs links and the `sztheory` owner-derived maintainer signal. No human verification gate is required for this plan.
</verification>

<success_criteria>
- mix.exs `package/0` `links` map has GitHub + Changelog + Docs (Changelog uses `@source_url` interpolation).
- package_metadata_test.exs asserts each links entry independently plus release public-verifier wiring, and passes.
- No change to `files:`, `@required_paths`, or `@prohibited_paths`.
</success_criteria>

<output>
Create `.planning/phases/114-oss-trust-governance/114-02-SUMMARY.md` when done.
</output>

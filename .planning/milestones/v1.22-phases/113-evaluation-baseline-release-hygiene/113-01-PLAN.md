---
phase: 113-evaluation-baseline-release-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.22-OSS-QUALITY-EVAL.md
  - .planning/seeds/SEED-003-ci-cd-performance-audit.md
  - .planning/seeds/SEED-004-ci-epipe-double-suite-run-flake.md
autonomous: true
requirements: [EVAL-01, HYGIENE-02]
must_haves:
  truths:
    - "A maintainer can read a ~1-page evidence-cited scored-weakness summary at .planning/milestones/v1.22-OSS-QUALITY-EVAL.md naming the weak dimensions (governance/trust, versioning/positioning, README, host-app respectfulness) vs the strong ones (per EVAL-01)."
    - "The EVAL doc's weakness-to-closing-phase column maps byte-faithfully to REQUIREMENTS.md traceability (114/115/116) so those phases consume it as a prioritized work-list (D-03)."
    - "SEED-003 and SEED-004 frontmatter reads status: consumed (not open) with companion consumed:/consumed_by: fields (HYGIENE-02 / D-14)."
  artifacts:
    - .planning/milestones/v1.22-OSS-QUALITY-EVAL.md
  key_links:
    - "EVAL doc dimension-to-phase column equals REQUIREMENTS.md Traceability (TRUST/META to 114, VERSION/README to 115, MIGRATE to 116, schema-flip to v1.23)"
---

<objective>
Track A, Wave 1. Author the milestone's opening artifact — the EVAL-01 scored-weakness summary (D-01/D-02/D-03) — and correct the stale SEED-003/SEED-004 lifecycle frontmatter (HYGIENE-02 / D-14). Both are pure planning-doc edits with no human or release dependency.

Purpose: EVAL-01 is the map Phases 114/115/116 consume as their prioritized work-list; its dimension-to-phase column IS the contract. HYGIENE-02 closes the lifecycle bookkeeping gap (two seeds that shipped but still read status: open).
Output: One new doc (v1.22-OSS-QUALITY-EVAL.md) and two frontmatter edits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# House-style analog for the EVAL doc (frontmatter + Scorecard table shape):
@.planning/milestones/v1.21-MILESTONE-AUDIT.md

# SEED frontmatter precedent (consumed/consumed_by field order):
@.planning/seeds/SEED-002-design-system-stress-test.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the EVAL-01 scored-weakness summary (D-01/D-02/D-03)</name>
  <files>.planning/milestones/v1.22-OSS-QUALITY-EVAL.md</files>
  <read_first>
    - .planning/milestones/v1.21-MILESTONE-AUDIT.md (frontmatter lines 1-9 plus the Scorecard table lines 41-49 — mirror format/tone/length)
    - .planning/REQUIREMENTS.md Traceability plus Phase distribution (the byte-faithful dimension-to-phase-to-req mapping the EVAL column must equal)
    - .planning/phases/113-evaluation-baseline-release-hygiene/113-RESEARCH.md Code Examples (the EVAL skeleton plus frontmatter to reuse) and Pitfall 2 (mapping)
  </read_first>
  <action>
    Create .planning/milestones/v1.22-OSS-QUALITY-EVAL.md per D-01 (bare OSS-QUALITY-EVAL noun — NOT MILESTONE-AUDIT, which is reserved for the v1.22 closing audit produced by gsd-audit-milestone). Mirror v1.21-MILESTONE-AUDIT.md frontmatter and scored-table house style. Frontmatter keys: milestone v1.22; name OSS Quality and Trust Hardening; evaluated 2026-06-29; source SEED-005 (software-quality consolidation recon, 2026-06-29); a one-line headline verdict ("Engineering strong; project-surface weak — v1.22 fixes the surface, not the engine.").

    Body per D-02 (about one page, 1.5 max): a method note (scores 1-5, 5 equals strong, lifted from SEED-005 — do NOT re-derive), then TWO scored tables, then an out-of-this-milestone block.

    Table 1 "Weak dimensions v1.22 fixes" — columns Dimension, Score, Evidence (present / named-absence / path), Closing phase to reqs. Rows mapped byte-faithfully to REQUIREMENTS.md per D-03:
    - OSS governance / trust — 2/5 — evidence: absent SECURITY.md, CODE_OF_CONDUCT.md, .github/ISSUE_TEMPLATE/; thin Hex package.links; no maintainers — 114 to TRUST-01, TRUST-02, TRUST-03, META-01, META-02
    - Versioning / path-to-1.0 — 2/5 — absent: stated SemVer/pre-1.0 policy in README plus CONTRIBUTING — 115 to VERSION-01, VERSION-02
    - README positioning — 2.5/5 — README leads AV-heavy (FFmpeg/libvips) first-run; no "when not to use" block — 115 to README-01, README-02
    - Host-app respectfulness — 3.5/5 — raw 15-file Ecto.Migrator install; Rindle creates shared oban_jobs (latent host-Oban collision) — 116 to MIGRATE-01, MIGRATE-02 (breaking schema flip deferred to v1.23)

    Table 2 "Strong dimensions v1.22 does NOT touch" — columns Dimension, Score, Evidence: telemetry 5/5, docs/ExDoc IA 4.5/5, public API plus Rindle.Error 4/5, CI/testing (strong — merge-blocking gate plus exact-SHA release proof).

    Out-of-this-milestone block: the four strong dimensions (no work planned) plus the breaking Postgres schema-default flip to rindle to v1.23 (0.4.0), ISO23-01..04.

    Stay about one page (D-02). Borrow the accrue 176-SCORECARD.md shape NOT its ~590-line length. Tone: calm/explicit/anti-hype per the brand voice.
  </action>
  <verify>
    <automated>test -f .planning/milestones/v1.22-OSS-QUALITY-EVAL.md && for tok in TRUST-01 TRUST-02 TRUST-03 META-01 META-02 VERSION-01 VERSION-02 README-01 README-02 MIGRATE-01 MIGRATE-02 ISO23; do grep -q "$tok" .planning/milestones/v1.22-OSS-QUALITY-EVAL.md || { echo "MISSING $tok"; exit 1; }; done && grep -qE '2/5|2\.5/5|3\.5/5|5/5|4\.5/5|4/5' .planning/milestones/v1.22-OSS-QUALITY-EVAL.md && echo OK</automated>
  </verify>
  <done>EVAL doc exists at the locked path with two scored tables; every weak-dimension row carries the exact REQUIREMENTS.md req-IDs for its closing phase (114/115/116) and the schema flip is deferred to v1.23; the doc is about one page.</done>
</task>

<task type="auto">
  <name>Task 2: Correct SEED-003 / SEED-004 lifecycle frontmatter (HYGIENE-02 / D-14)</name>
  <files>.planning/seeds/SEED-003-ci-cd-performance-audit.md, .planning/seeds/SEED-004-ci-epipe-double-suite-run-flake.md</files>
  <read_first>
    - .planning/seeds/SEED-002-design-system-stress-test.md frontmatter (the verified precedent: status: consumed plus consumed:/consumed_by: placed after planted_during:)
    - .planning/phases/113-evaluation-baseline-release-hygiene/113-CONTEXT.md D-14 (the verbatim consumed/consumed_by strings)
  </read_first>
  <action>
    In both seed files change the frontmatter `status: open` to `status: consumed` (the canonical done-after-ship token — NOT `promoted`, which means promoted into a charter; per D-14). Add two companion fields immediately after the existing `planted_during:` line (mirroring the SEED-002 field order), using the D-14 verbatim strings:

    SEED-003 (`.planning/seeds/SEED-003-ci-cd-performance-audit.md`):
      consumed: 2026-06-22
      consumed_by: "v1.20 CI/CD Performance (chartered 2026-06-20, shipped 2026-06-22; 18/18 reqs). Reliability tail continued as SEED-004 -> v1.21."

    SEED-004 (`.planning/seeds/SEED-004-ci-epipe-double-suite-run-flake.md`):
      consumed: 2026-06-29
      consumed_by: "v1.21 CI/DX Reliability Tail (chartered 2026-06-26, shipped 2026-06-29; 24/24 reqs)."

    Touch ONLY the frontmatter block of each file; leave all body content unchanged.
  </action>
  <verify>
    <automated>for f in .planning/seeds/SEED-003-ci-cd-performance-audit.md .planning/seeds/SEED-004-ci-epipe-double-suite-run-flake.md; do awk '/^---$/{n++} n==1' "$f" | grep -q '^status: consumed' || { echo "FAIL status $f"; exit 1; }; awk '/^---$/{n++} n==1' "$f" | grep -q '^consumed_by:' || { echo "FAIL consumed_by $f"; exit 1; }; grep -q '^status: open' "$f" && { echo "STILL OPEN $f"; exit 1; }; done && echo OK</automated>
  </verify>
  <done>Both SEED-003 and SEED-004 frontmatter read status: consumed with a consumed: date and a consumed_by: attribution; neither still reads status: open; bodies unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only authors/edits local planning Markdown. No untrusted input, no network, no execution surface, no secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-113-01 | Tampering | EVAL doc dimension-to-phase mapping | low | mitigate | Verify gate greps every req-ID (TRUST/META/VERSION/README/MIGRATE/ISO23) so a drifted mapping fails the task; mapping is byte-checked against REQUIREMENTS.md (D-03). |
| T-113-02 | Repudiation | SEED lifecycle accuracy | low | accept | consumed_by strings record charter+ship dates; rationale is bookkeeping correctness, not a security control. |
| T-113-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this plan (or this phase) — no supply-chain surface. |
</threat_model>

<verification>
- `test -f .planning/milestones/v1.22-OSS-QUALITY-EVAL.md` and every v1.22 req-ID present (Task 1).
- Both SEED files: `status: consumed` + `consumed_by:` present, `status: open` absent (Task 2).
- No edits outside the three declared files.
</verification>

<success_criteria>
EVAL-01 doc exists at `.planning/milestones/v1.22-OSS-QUALITY-EVAL.md`, ~1 page, two scored tables, mapping byte-faithful to REQUIREMENTS.md. SEED-003/004 read `status: consumed` with companion fields.
</success_criteria>

<output>
Create `.planning/phases/113-evaluation-baseline-release-hygiene/113-01-SUMMARY.md` when done.
</output>
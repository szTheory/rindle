---
phase: 98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco
plan: 04
type: execute
wave: 5
depends_on: ["98-03"]
files_modified:
  - test/brandbook/admin_design_system_validation_test.exs
  - examples/adoption_demo/e2e/support/admin-polish.js
  - examples/adoption_demo/e2e/admin-screenshots.spec.js
autonomous: false
requirements: [UPLIFT-03, UPLIFT-04, UPLIFT-05, UPLIFT-06, UPLIFT-07, UPLIFT-08]
tags: [admin, validation, gates, exunit, playwright, computed-style, merge-gate, phase-seal]

must_haves:
  truths:
    - "ExUnit (admin_design_system_validation_test.exs) asserts all UNCONDITIONAL §A/§B/§D/§E/§F clauses over the real surfaces: §A slot order + no page-local display:grid + :work required; §B transition-property subset {opacity,transform} + no transition:all; §D dead-markup aria-pressed + skip-link/main + caption/scope + live_indicator role/no-tabindex + contrast 58/58; §E nav order/labels/hrefs + triage DOM order + deep-link params + affirmative all-clear; §F denylist + frozen lexicon + off-voice replacements + confirmation shape (D-98-05)."
    - "Playwright (admin-polish.js) asserts the five NON-INFERABLE computed-style backstops: (1) two-pane track count + 760-1023px band at a safe-inside literal (~900px); (2) <td>::before content:attr(data-label) resolution + display flip at 759/761 and 1023/1025; (3) reduced-motion transitionDuration 0s read UN-FROZEN; (4) dialog-open inert+aria-hidden reset-on-close AND survives reconnect; (5) :focus-visible-vs-pointer differentiation (D-98-05/06, RESEARCH backstops 1-5)."
    - "The toHaveLength(22) literal AND expectedScreenshots in admin-screenshots.spec.js are bumped IN LOCKSTEP by the count of net-new distinct viewport/theme/interaction states (deliberately, as 97-04 bumped 10->18) (D-98-06, Pitfall 9)."
    - "All new admin-polish.js checks are HARD admin-root-only against [data-rindle-admin-root] / DEFAULT_INTERACTIVE_SELECTORS; the {root, interactiveSelectors} generalization stays intact and warn->fail is NOT flipped (that is Phase 102) (D-98-06)."
    - "The §A<->§C 760-1023px two-pane band is reconciled and asserted: shell sidebar + single-column work + NO side :aside, detail via :show routes (checker note 2, D-98-15)."
    - "The full pipeline is green: mix test (incl the new clauses + drift + contrast 58/58 + byte-equality) AND the adoption-demo-e2e Playwright lane (all 22+N states)."
  artifacts:
    - path: "test/brandbook/admin_design_system_validation_test.exs"
      provides: "New ExUnit clauses for all unconditional §A/§B/§D/§E/§F gates over real surfaces"
      contains: "render_to_string"
    - path: "examples/adoption_demo/e2e/support/admin-polish.js"
      provides: "Five new computed-style backstop sub-assertions (offender-returning, admin-root-only)"
      contains: "DEFAULT_ROOT"
    - path: "examples/adoption_demo/e2e/admin-screenshots.spec.js"
      provides: "Bumped expectedScreenshots + toHaveLength(N) in lockstep"
      contains: "toHaveLength"
  key_links:
    - from: "examples/adoption_demo/e2e/support/admin-polish.js new sub-assertions"
      to: "examples/adoption_demo/e2e/admin-screenshots.spec.js assertAdminPolish runner"
      via: "each new check registered in the runner + module.exports, against [data-rindle-admin-root]; reduced-motion read runs un-frozen"
      pattern: "module.exports"
    - from: "test/brandbook/admin_design_system_validation_test.exs new clauses"
      to: "lib/rindle/admin/* real surfaces"
      via: "render_to_string grep / source scan / run_node generators + assert_generated_clean over the migrated surfaces"
      pattern: "render_to_string"
---

<objective>
Gate close-out (mirrors Phase-97 P4): extend BOTH test homes to assert all six §A–§F merge-gates over the REAL migrated surfaces, deliberately bump the `toHaveLength(22)` literal for net-new e2e states, reconcile/assert the §A↔§C 760–1023px two-pane band (checker note 2), and run the full pipeline green. Implements D-98-04, D-98-05, D-98-06, D-98-15.

Purpose: P1 authored the CSS, P2a/P2b shipped the primitives + migrations, P3 layered IA/microcopy/route. Now the gate clauses become EXECUTABLE assertions over real surfaces. The split line (D-98-05): static substring/token/regenerate-diff/`render_to_string`-grep → ExUnit; cascade/viewport/theme/`:focus-visible`/`::before content:attr()`/dialog-open mutation → Playwright (the five non-inferable backstops). This is the autonomous:false sealing plan — a blocking human-verify checkpoint confirms the full pipeline before phase verification.

Output: both homes assert all six gates; the five computed-style backstops land in Playwright (reduced-motion un-frozen); the screenshot literal bumped in lockstep; the band reconciled; full pipeline green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-CONTEXT.md
@.planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-UI-SPEC.md
@.planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-RESEARCH.md
@.planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-VALIDATION.md
@.planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-PATTERNS.md
@.planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (P4) creates net-new TEST symbols only — exclude from drift/undefined verification:
- New ExUnit test clauses in `admin_design_system_validation_test.exs` (the unconditional §A/§B/§D/§E/§F gates).
- New offender-returning sub-assertions in `admin-polish.js` (the five computed-style backstops), registered in the `assertAdminPolish` runner + `module.exports`, against `[data-rindle-admin-root]`/`DEFAULT_INTERACTIVE_SELECTORS`.
- Bumped `expectedScreenshots` array + `toHaveLength(N)` literal in `admin-screenshots.spec.js` (and any new desktopCases/mobileCases entries).
NOTE: P4 adds NO production code and NO CSS. If a clause cannot pass because markup/CSS is wrong, that is a defect in P1/P2/P3 to fix in-scope — P4 only authors assertions.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend ExUnit with all unconditional §A/§B/§D/§E/§F gate clauses over the real surfaces</name>
  <read_first>
    - test/brandbook/admin_design_system_validation_test.exs (READ FULLY — DS-01 ~L40-100, ADMIN-02 ~L204-256, run_node/1 ~L258-274, assert_generated_clean/1 ~L276-288, @implementation_files + forbidden regex ~L241-250, contrast/DS-03)
    - lib/rindle/admin/components.ex + lib/rindle/admin/live/*_live.ex (the real migrated surfaces the clauses assert over)
    - .planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-RESEARCH.md § Validation Architecture (the per-clause ExUnit rows for §A/§B/§D/§E/§F) + § "Net-new Playwright states"
    - .planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-UI-SPEC.md (the six merge-gate assertion blocks §A-§F)
    - .planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-VALIDATION.md (per-gate proving home table) + 98-CONTEXT.md (D-98-05/06)
  </read_first>
  <action>
    Add ExUnit clauses for every UNCONDITIONAL gate clause (D-98-05 — the static/substring/render_to_string-grep half). Use the existing idioms (substring over generated CSS; `render_to_string` grep over surfaces; source scan; `run_node` + `assert_generated_clean`):
    - §A: scaffold exposes canonical slot order in DOM order (render page/1 with fixture slots, assert order); `:work` required (a missing-slot test raises at compile — assert via a focused compile-failure test or document the compile-time guarantee); NO surface declares page-local `display:grid` outside the scaffold selector (source scan over the six `*_live.ex` + the generated CSS); `--rindle-shadow-card` consumed (the requiredTokenUses guard already runs in the generator — assert the generator output includes it).
    - §B: every animated selector `transition-property ⊆ {opacity, transform}` (CSS-text scan rejecting width/height/top/left/margin/color/background-color); NO `transition: all`/`transition:all` substring anywhere.
    - §D (ExUnit half): `theme_picker` `aria-pressed` present in SERVER-RENDERED (dead) markup via `render_to_string` (D-98-06 — a live read can't prove server-ownership); skip-link is first focusable child → `#rindle-admin-main`; `<main>` has `id` + `tabindex="-1"`; data `<table>` has `<caption>` + `<th scope>` + `scope="row"`; `live_indicator` has `role="status"` + NO `tabindex`; contrast 58/58 via `admin-contrast.mjs` (ExUnit-ONLY, source-hex full-float — do NOT re-derive in browser, D-98-06).
    - §E: nav renders exactly six items in order with relabeled text + correct hrefs (ordered `render_to_string` grep); no literal "Actions"/"Home/Status"/"Variants/Jobs"/"Runtime/Doctor" (negative substring); Overview DOM order = needs-attention → system-health → recent activity → vanity totals last (order check over a fixture); each needs-attention entry is an `<a href>` with the documented query param; all-clear renders the affirmative copy on a healthy fixture.
    - §F: denylist scan over rendered admin markup finds ZERO R4 hype words + ZERO R5 vague standalone labels (PRIMARY in ExUnit); every status chip label ∈ the frozen lexicon for its domain; action labels ≤4 words; no "!" in confirmation/destructive bodies; the six off-voice strings no longer appear in source + replacements do; confirmation headings match `"{Verb} this {noun}?"`.
    COMMENT-TEXT DISCIPLINE: do NOT write any negative-greped literal (e.g. "transition:all", "Retry load", "Variants/Jobs", an R4 hype word) as bare prose in a test COMMENT in a way that would self-invalidate a scan over the test file — these assertions scan PRODUCTION source/markup, not the test file, so this is low-risk here, but keep denylist tokens inside string literals/arrays, not free-text comments.
  </action>
  <verify>
    <automated>mix test test/brandbook/admin_design_system_validation_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - New clauses exist: `grep -c "render_to_string" test/brandbook/admin_design_system_validation_test.exs` increased; clauses for nav order, caption/scope, aria-pressed dead-markup, denylist, off-voice replacements present.
    - `mix test test/brandbook/admin_design_system_validation_test.exs` passes (all unconditional §A/§B/§D/§E/§F clauses green over real surfaces) — 0 failures.
    - Contrast stays ExUnit-only (no new browser contrast added); 58/58.
    - Drift + ADMIN-02 byte-equality still green (no CSS changed in P4).
  </acceptance_criteria>
  <done>ExUnit asserts every unconditional §A/§B/§D/§E/§F clause over the real migrated surfaces; full ExUnit gate green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the five computed-style backstops to admin-polish.js, bump toHaveLength in lockstep, reconcile the 760-1023px band</name>
  <read_first>
    - examples/adoption_demo/e2e/support/admin-polish.js (READ FULLY — DEFAULT_ROOT ~L21 [data-rindle-admin-root], DEFAULT_INTERACTIVE_SELECTORS ~L33, freezeMotion ~L63, assertFocusVisibleTokens ~L319, assertTargetSizes ~L291, assertReadableContrast, assertNoHorizontalScroll, the assertAdminPolish runner ~L574-607 incl freezeMotion injection ~L583 + run() calls ~L598, module.exports ~L625-641, OVERLAP_ENFORCED)
    - examples/adoption_demo/e2e/admin-screenshots.spec.js (READ FULLY — expectedScreenshots ~L39-62 (22 entries), toHaveLength(22) ~L146, desktopCases/mobileCases ~L18-37, the capture flow, 1480x900 desktop / 390x900 mobile)
    - .planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-RESEARCH.md § Validation Architecture (the Playwright rows + the five "Held-out / backstop" assertions + "Net-new Playwright states" guidance) + Pitfall 6 (freezeMotion masks reduced-motion) + Pitfall 9 (toHaveLength drift)
    - .planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-CONTEXT.md (D-98-05/06/15 — band reconciliation, admin-root-only, no warn->fail flip) + 98-UI-SPEC.md Checker note 2
    - .planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-PATTERNS.md (offender-returning sub-assertion pattern + registration)
  </read_first>
  <action>
    Add the five NON-INFERABLE computed-style backstops to `admin-polish.js` as offender-returning sub-assertions (NEVER throw — return an array; `assertAdminPolish` aggregates and throws once). Build each against the default `[data-rindle-admin-root]` / `DEFAULT_INTERACTIVE_SELECTORS` seam — do NOT generalize over Cohort and do NOT flip warn→fail (Phase 102). Register each in the `assertAdminPolish` runner `run(...)` calls and in `module.exports`. Add new interactive selectors (disclosure button, sort-th control, stacked-row tap target) to `DEFAULT_INTERACTIVE_SELECTORS` so `assertFocusVisibleTokens`/`assertTargetSizes` cover them.
    The five backstops (RESEARCH held-out list):
    1. TWO-PANE TRACK COUNT + 760-1023px BAND (§A↔§C, checker note 2, D-98-15): at ≥1024px the two-pane region computed `grid-template-columns` resolves to two tracks; below 1024 one track. At a SAFE-INSIDE band literal (~900px) assert: sidebar-shell present + single-column `:work` + NO side `:aside` (detail reached via `:show` routes). This reconciles the band identically in scaffold CSS and the responsive media layer.
    2. STACKED CARD (§C): at <760 (use 759) the data `<table>`/`<tr>`/`<td>` computed `display === "block"` and each `<td>::before` `content` resolves to its `data-label`; at ≥760 (use 761) they are `table`/`table-row`/`table-cell` with empty `::before`. Also assert at 1023/1025 the shell stop. Assert NO State/Job column is `display:none` at any tested viewport.
    3. REDUCED-MOTION (§B): under `page.emulateMedia({ reducedMotion: "reduce" })` every animated selector computes `transitionDuration === "0s"`; under no-preference it equals the token duration (e.g. `0.2s` toast). CRITICAL (Pitfall 6): run this read UN-FROZEN — `freezeMotion` injects `transition:none !important` and would make the check pass vacuously. Read computed `transitionDuration` BEFORE/without freeze, or via `emulateMedia` directly, as a check separate from the frozen polish run.
    4. DIALOG INERT (§D, D-98-11): open a confirm dialog → assert `main`+`nav` have `inert` AND `aria-hidden`, dialog has `role="dialog"`/`alertdialog` + `aria-modal` + `aria-labelledby`; close → assert `inert`/`aria-hidden` REMOVED; then simulate a reconnect (or dead-render) with the dialog CLOSED and assert `main` is NOT left inert (the critical landmine).
    5. FOCUS-VISIBLE vs POINTER (§D): keyboard `:focus-visible` yields `outline: 2px solid var(--rindle-focus-ring)` + `outline-offset: 2px` with ring color matching per `data-theme`; mouse-click (pointer) focus yields NO ring (negative).
    Then BUMP the screenshot literal IN LOCKSTEP (D-98-06, Pitfall 9): count the distinct NET-NEW viewport/theme/interaction STATES that require a new screenshot capture (many checks — focus-visible, dialog-open, reduced-motion — can RIDE existing captures; count only states needing a new capture). Update `expectedScreenshots` (add the named entries) AND `toHaveLength(22)` → `toHaveLength(22 + N)` together; add matching `desktopCases`/`mobileCases` entries if a new viewport/interaction capture is introduced. If N=0 (all backstops ride existing captures), leave the literal at 22 and document why in the SUMMARY.
  </action>
  <verify>
    <automated>cd examples/adoption_demo && npx playwright test 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - Five new sub-assertions exist in admin-polish.js (`grep -nE 'TwoPane|StackedCard|ReducedMotion|DialogInert|FocusVisible|Band' examples/adoption_demo/e2e/support/admin-polish.js` shows the new functions) and each is in `module.exports` + the `assertAdminPolish` runner.
    - The reduced-motion check is NOT inside the frozen run (`freezeMotion` not applied for that read) — verify by reading the new function's emulateMedia usage.
    - All new checks target `[data-rindle-admin-root]`/`DEFAULT_INTERACTIVE_SELECTORS`; `OVERLAP_ENFORCED`/warn→fail is unchanged (no Phase-102 flip): `git -C /Users/jon/projects/rindle diff examples/adoption_demo/e2e/support/admin-polish.js` shows no Cohort generalization + no warn→fail flip.
    - `expectedScreenshots` length and the `toHaveLength(N)` literal MATCH each other (lockstep); if bumped, both changed in the same commit.
    - The adoption-demo-e2e Playwright lane passes (all states green, including the five backstops).
  </acceptance_criteria>
  <done>The five computed-style backstops land in Playwright (reduced-motion un-frozen, dialog reset-on-reconnect, band at ~900px, ::before attr resolution, focus-visible-vs-pointer), admin-root-only with no warn→fail flip; the screenshot literal is bumped in lockstep; Playwright lane green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase-98 seal — full-pipeline + visual verification checkpoint</name>
  <action>Blocking human-verify checkpoint: confirm the full Phase-98 pipeline is green (ExUnit static gates + contrast + drift + byte-equality; full mix test; adoption-demo-e2e Playwright lane incl the five backstops + lockstep screenshot literal) and spot-check the console visually before sealing the phase. See how-to-verify for exact steps.</action>
  <what-built>
    The full Phase-98 admin polish pass is complete and both merge-gate homes assert all six §A–§F gates over the real surfaces. ExUnit (static §A/§B/§D/§E/§F + contrast 58/58 + drift + byte-equality) and the adoption-demo-e2e Playwright lane (the five computed-style backstops + bumped screenshot literal) are green.
  </what-built>
  <how-to-verify>
    1. Run `mix test test/brandbook/admin_design_system_validation_test.exs` — expect 0 failures (all unconditional §A/§B/§D/§E/§F clauses + contrast + drift + ADMIN-02 byte-equality).
    2. Run the full `mix test` suite — expect admin surface behavior tests green (migrations preserved contracts).
    3. Run `cd examples/adoption_demo && npx playwright test` — expect the admin lane green including the five backstops (two-pane band @~900px, stacked-card ::before @759/761, reduced-motion 0s un-frozen, dialog inert reset-on-reconnect, focus-visible-vs-pointer) and the bumped `toHaveLength(N)` matching `expectedScreenshots`.
    4. Confirm NO warn→fail flip and NO Cohort generalization leaked in (Phase 102 owns those): `git diff examples/adoption_demo/e2e/support/admin-polish.js` shows only admin-root-only additions.
    5. Spot-check the console visually: nav reads [Overview, Assets, Upload sessions, Processing, Doctor, Maintenance]; Overview shows a needs-attention triage list (not inspect/1 output); a confirm flow opens an accessible dialog and ESC/Cancel returns focus; narrow the viewport <760 and confirm tables stack as label:value cards with no lost State/Job columns.
  </how-to-verify>
  <resume-signal>Type "approved" to seal Phase 98, or describe failures/regressions to address.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate → merge | The two test homes are the merge-blocking gate; a vacuous/green-on-broken assertion would let a regressed console merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-04-01 | Tampering | A viewport/theme/interaction clause asserted only via ExUnit substring ships GREEN on a broken `@media` (Pitfall 1) | mitigate | The five non-inferable clauses are Playwright computed-style backstops at real viewports/focus-states; reduced-motion read un-frozen (Pitfall 6) |
| T-98-04-02 | Tampering | `toHaveLength` bumped without adding states (hides missing coverage) or states added without bump (fails spuriously) | mitigate | Bump `expectedScreenshots` + `toHaveLength(N)` in lockstep, deliberately, counting distinct net-new states (Pitfall 9, D-98-06) |
| T-98-04-03 | Elevation of Privilege | Accidentally flipping warn→fail / generalizing over Cohort here (Phase-102 scope) destabilizes the gate early | mitigate | Keep new checks admin-root-only against `[data-rindle-admin-root]`; `OVERLAP_ENFORCED`/warn-mode unchanged; `git diff` review in the checkpoint |
| T-98-04-SC | Tampering | npm/node package installs | mitigate | N/A — zero new packages this phase (Playwright lane already exists) |
</threat_model>

<verification>
- `mix test test/brandbook/admin_design_system_validation_test.exs` 0 failures (all unconditional gates + contrast 58/58 + drift + byte-equality).
- Full `mix test` green (surface behavior preserved).
- adoption-demo-e2e Playwright lane green (five backstops + lockstep screenshot literal).
- No warn→fail flip / no Cohort generalization (Phase 102 boundary held).
- Blocking human-verify checkpoint confirms the full pipeline + a visual spot-check before phase seal.
</verification>

<success_criteria>
- Both test homes assert all six §A–§F merge-gates over the real surfaces.
- The five computed-style backstops land in Playwright (reduced-motion un-frozen, dialog reset-on-reconnect).
- `toHaveLength` + `expectedScreenshots` bumped in lockstep (or documented N=0).
- §A↔§C 760–1023px band reconciled and asserted.
- Full pipeline green; checkpoint approved.
</success_criteria>

<output>
Create `.planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-04-SUMMARY.md` when done.
</output>

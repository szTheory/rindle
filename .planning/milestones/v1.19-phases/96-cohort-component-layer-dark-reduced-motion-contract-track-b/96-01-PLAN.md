---
phase: 96-cohort-component-layer-dark-reduced-motion-contract-track-b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/adoption_demo/priv/static/assets/cohort.css
autonomous: true
requirements: [COHORT-06]

must_haves:
  truths:
    - "cohort.css carries an explicit [data-theme=\"light\"] and [data-theme=\"dark\"] attribute contract distinct from the prefers-color-scheme fallback"
    - "A prefers-reduced-motion: reduce block exists and zeroes animation/transition on .ck descendants while keeping color/state changes"
    - "A hex/rgb/rgba scan of cohort.css finds color literals only inside token-definition blocks (:root / [data-theme])"
    - "color-scheme is set per theme so native controls theme correctly"
  artifacts:
    - path: "examples/adoption_demo/priv/static/assets/cohort.css"
      provides: "Dark [data-theme] contract, reduced-motion block, surface-overlay ladder, shadow-ink base tokens, literal-clean rule bodies"
      contains: "[data-theme=\"dark\"]"
  key_links:
    - from: "cohort.css [data-theme=\"dark\"]"
      to: "cohort.css @media (prefers-color-scheme: dark) :root:not([data-theme])"
      via: "duplicated dark token block (banner-commented controlled duplication)"
      pattern: "prefers-color-scheme: dark"
    - from: "cohort.css --ck-shadow-sm/md/lg formulas"
      to: "--ck-shadow-ink bare-channel base token"
      via: "rgb(var(--ck-shadow-ink) / <alpha>)"
      pattern: "--ck-shadow-ink"
---

<objective>
Author the net-new dark `[data-theme]` theme contract, the surface-elevation ladder, the
shadow/glow ink base tokens, the per-theme `color-scheme`, and the net-new
`prefers-reduced-motion: reduce` block in the hand-authored `cohort.css` — and remove every
color literal from rule bodies so the only literal sink is the `:root`/`[data-theme]` token
blocks.

Purpose: This is the foundation wave for Phase 96 (COHORT-06). Every later plan (primitives,
gallery, contrast gate) depends on these tokens and selectors existing first. Satisfies
UI-SPEC acceptance gates 2 (theme contract), 3 (reduced-motion), 4 (grep-clean tokens).
Output: A `cohort.css` whose dark mode is switchable + auto-fallback, whose elevation is
lightness-based, whose shadows derive from one per-theme ink token, and whose rule bodies are
literal-free.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-cohort-component-layer-dark-reduced-motion-contract-track-b/96-UI-SPEC.md
@.planning/phases/96-cohort-component-layer-dark-reduced-motion-contract-track-b/96-CONTEXT.md
@.planning/phases/96-cohort-component-layer-dark-reduced-motion-contract-track-b/96-PATTERNS.md

<constraints>
- D-94-05/06: cohort.css stays hand-authored vanilla CSS. NEVER route `--ck-*` through
  tokens.json, never share a stylesheet/build step with rindle-admin.
- D-96-23: NO `--ck-*` color VALUES change in this file. `--ck-faint` keeps `#8a9a92` light /
  `#6f857b` dark. This plan changes token *structure* (new selectors, new ink tokens, overlay
  step) and rule-body literals — never the existing palette hexes.
- D-96-11: theme contract shape is `:root, [data-theme="light"]` + `[data-theme="dark"]` +
  `@media (prefers-color-scheme: dark) { :root:not([data-theme]) { … } }`. NO `light-dark()`,
  NO `@layer`, NO Tailwind. The reduced-motion block is the ONLY `!important` site in the file.
- D-96-13: use `.001ms` not `0` for reduced durations so transitionend/animationend still fire.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote dark to a [data-theme] contract + surface ladder + shadow-ink tokens + color-scheme</name>
  <read_first>
    - examples/adoption_demo/priv/static/assets/cohort.css (the file being modified — read the
      `:root` block at lines 33-91, the `@media (prefers-color-scheme: dark)` block at 93-121,
      and the `.ck` box-sizing/focus block at 124-153)
    - 96-UI-SPEC.md "Color" section + "Theme Contract (net-new — [data-theme])" table (lines
      106-141) for the FIXED light/dark hexes and the elevation-by-lightness rule
    - 96-PATTERNS.md "cohort.css (config / stylesheet — modified)" section (lines 221-267):
      D-96-11 theme-contract shape, D-96-12 elevation/shadow ink mechanism, the current-state
      line map
  </read_first>
  <action>
    Restructure the light `:root` token block (cohort.css:33-91) into a combined
    `:root, [data-theme="light"]` selector and add `color-scheme: light` to it. Author a new
    `[data-theme="dark"]` selector carrying the dark token set currently inside the
    `@media (prefers-color-scheme: dark)` block (cohort.css:93-121) verbatim values, plus
    `color-scheme: dark`. Keep the existing `@media (prefers-color-scheme: dark)` block but
    change its inner selector to `:root:not([data-theme])` and add `color-scheme: dark`; add a
    banner comment marking it the "controlled dark-token duplication (parity-checked by
    cohort-contrast.mjs D-96-18)". Add the `--ck-surface-overlay` step to BOTH the light and
    dark token sets, sitting above `--ck-surface` in the lightness ladder
    (`--ck-bg → --ck-surface-2 → --ck-surface → --ck-surface-overlay`) per D-96-12 — pick a
    light value lighter/separated from `--ck-surface #ffffff` (e.g. a faint tint), and a dark
    value LIGHTER than `--ck-surface #111d18` (elevation = lightness, not a drop shadow).
    Introduce per-theme bare-channel base tokens `--ck-shadow-ink` and `--ck-glow-ink` in
    each token set (light ink ≈ the `15 27 23` channel already baked into the light shadows;
    dark ink ≈ `0 0 0`). Rewrite `--ck-shadow-sm/md/lg` and `--ck-bg-glow` as ONE shared formula
    per token using `rgb(var(--ck-shadow-ink) / <alpha>)` / `rgb(var(--ck-glow-ink) / <alpha>)`
    so only the ink flips per theme (D-96-12). Do not alter the existing palette hexes
    (D-96-23) — only their structure/selectors and the shadow/glow derivation.
  </action>
  <acceptance_criteria>
    - UI-SPEC gate 2: `grep -c '\[data-theme="dark"\]' examples/adoption_demo/priv/static/assets/cohort.css` returns ≥ 1, and `grep -c ':root, \[data-theme="light"\]\|:root:not(\[data-theme\])' …/cohort.css` confirms the light combined selector + the auto-fallback selector both exist.
    - `grep -c 'color-scheme' …/cohort.css` returns ≥ 3 (light, dark, auto-fallback).
    - `grep -c -- '--ck-shadow-ink\|--ck-glow-ink\|--ck-surface-overlay' …/cohort.css` returns ≥ 6 (each token defined in light + dark sets, plus referenced in formulas).
    - `grep -c 'rgb(var(--ck-shadow-ink)\|rgb(var(--ck-glow-ink)' …/cohort.css` returns ≥ 1 (formula uses the ink token, not baked rgba).
    - Existing palette hexes unchanged: `grep -c -- '--ck-faint: #8a9a92' …/cohort.css` returns 1 and `grep -c -- '--ck-bg: #f7f8f6' …/cohort.css` returns 1 (D-96-23: no value change).
    - Demo compiles: `cd examples/adoption_demo && mix compile --warnings-as-errors` exits 0 (CSS is static so this only proves no template breakage; primary CSS check is the grep gates above).
  </acceptance_criteria>
  <done>
    `[data-theme="dark"]` is a real attribute selector distinct from the
    `prefers-color-scheme` fallback; the surface ladder gains an overlay step; shadows/glows
    derive from per-theme ink tokens; `color-scheme` is set per theme; no existing palette hex
    changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author the prefers-reduced-motion reduce block + remove rule-body color/font literals</name>
  <read_first>
    - examples/adoption_demo/priv/static/assets/cohort.css (the existing
      `prefers-reduced-motion: no-preference` reveal block at 589-599 — keep as-is; the
      `:focus-visible` rule at 151-153; the literal offender sites at 368, 404, 470, 485, 503,
      and the shadow rgba sites at 58-60 / 113-115)
    - 96-UI-SPEC.md "Motion Contract" table (lines 215-228) for the reduce-block contract +
      "Color-literal removal" table (lines 143-155)
    - 96-PATTERNS.md "D-96-13 reduced-motion block" (lines 245-255) + "D-96-10 literal removals"
      table (lines 257-267) — the exact line→token map
  </read_first>
  <action>
    Add a net-new `@media (prefers-reduced-motion: reduce)` block (after the existing
    `no-preference` block, leaving that one untouched) scoped to `.ck *, .ck *::before,
    .ck *::after` that sets `animation-duration: .001ms !important; animation-iteration-count:
    1 !important; transition-duration: .001ms !important;` plus `.ck-reveal { opacity: 1;
    transform: none; }`. Use `.001ms` (not `0`) per D-96-13 so transitionend/animationend still
    fire and LiveView JS does not hang. This is the ONLY `!important` site permitted in the
    file. KEEP color/border/state changes (they convey state, not decoration). Then remove the
    confirmed rule-body literals per the D-96-10 map: `#fff` at the `.ck-copy[data-copied="true"]`
    rule (cohort.css:404) → `var(--ck-on-brand)`; the legacy `rem` font literals at
    `0.875rem` (line 368), `0.95rem` `.ck-card__desc` (line 470), `0.78rem` `.ck-card__path`
    (line 485), `0.72rem` `.ck-badge` (line 503) → the nearest `--ck-step-*` token
    (`--ck-step--1` = 0.8125rem is nearest for the 0.72/0.78/0.875 mono/label sizes;
    `--ck-step-0` = 1rem is nearest for 0.95rem — choose the closest step per value). Confirm
    the only remaining rule-body shadow rgba (lines 58-60/113-115) were already folded into the
    ink-token formula in Task 1; any residual baked rgba in a rule body must move to the
    formula. After this, the only hex/rgb/rgba literals in the file live inside `:root` /
    `[data-theme]` token blocks (UI-SPEC gate 4).
  </action>
  <acceptance_criteria>
    - UI-SPEC gate 3: `grep -c 'prefers-reduced-motion: reduce' …/cohort.css` returns 1, and `grep -A6 'prefers-reduced-motion: reduce' …/cohort.css | grep -c '\.001ms'` returns ≥ 2 (animation + transition duration), and the block contains `.ck-reveal` with `opacity: 1` and `transform: none`.
    - The existing positive block is preserved: `grep -c 'prefers-reduced-motion: no-preference' …/cohort.css` returns 1.
    - UI-SPEC gate 4 (literal removal): `grep -n '#fff' …/cohort.css` shows NO match at the `.ck-copy[data-copied="true"]` rule (replaced by `var(--ck-on-brand)`); `grep -c 'var(--ck-on-brand)' …/cohort.css` increased by ≥ 1.
    - No remaining `rem` font literal at usage sites: `grep -nE 'font-size:\s*0\.(72|78|875|95)rem' …/cohort.css` returns 0 matches.
    - `!important` appears ONLY in the reduce block: `grep -c '!important' …/cohort.css` equals the count inside the reduce block (3) — `grep -n '!important' …/cohort.css` shows every hit is inside the `prefers-reduced-motion: reduce` block.
    - `cd examples/adoption_demo && mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>
    A net-new reduce block immediately settles `.ck-reveal` and zeroes motion (keeping state
    color), the existing no-preference block is intact, and a hex/rgb/rgba scan finds color
    literals only in token blocks. Plan 04's literal scanner (D-96-20) will mechanically
    enforce this.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → static CSS | `cohort.css` is a static stylesheet served to the browser; no user input crosses it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01 | Information Disclosure | cohort.css | accept | Static design tokens only; no secrets, no PII. Public by nature. |
| T-96-02 | Tampering | cohort.css edits | mitigate | Plan 04's literal scanner + parity check (D-96-18/20) mechanically catch drift/regression in CI before merge. |

No high-severity threats: this plan edits a static stylesheet with no data, auth, or
execution surface.
</threat_model>

<verification>
- UI-SPEC gates 2, 3, 4 all pass via the grep gates above.
- `cd examples/adoption_demo && mix compile --warnings-as-errors` exits 0.
- No existing palette hex changed (D-96-23 grep checks pass).
</verification>

<success_criteria>
- `[data-theme="dark"]` attribute contract exists, distinct from the auto fallback.
- Net-new `prefers-reduced-motion: reduce` block exists, settles `.ck-reveal`, keeps state color.
- Surface-overlay ladder + per-theme shadow/glow ink tokens exist; shadows derive from ink.
- Rule bodies are color-literal-free; only token blocks hold literals.
</success_criteria>

<output>
Create `.planning/phases/96-cohort-component-layer-dark-reduced-motion-contract-track-b/96-01-SUMMARY.md` when done
</output>

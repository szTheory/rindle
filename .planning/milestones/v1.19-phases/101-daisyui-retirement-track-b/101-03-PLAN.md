---
phase: 101-daisyui-retirement-track-b
plan: "03"
type: execute
wave: 2
depends_on:
  - "101-01"
  - "101-02"
files_modified:
  - examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs
  - examples/adoption_demo/lib/adoption_demo_web/components/layouts/root.html.heex
autonomous: true
requirements:
  - COHORT-05
must_haves:
  truths:
    - "The retirement gate scans full composed renders, not only the `[data-ck-root]` page body."
    - "The source/file gate catches conditional flash markup, layout wrapper literals, and deleted generator files."
    - "The root layout no longer links `default.css`, while `app.css` and `cohort.css` remain linked."
    - "`default.css` is still present until the final destructive plan."
  artifacts:
    - path: examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs
      provides: "Full composed render and source/file retirement ratchet"
    - path: examples/adoption_demo/lib/adoption_demo_web/components/layouts/root.html.heex
      provides: "Root stylesheet list without `default.css`"
  key_links:
    - from: examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs
      to: examples/adoption_demo/lib/adoption_demo_web/components/layouts.ex
      via: "source and full-render assertions prove layout wrapper removal"
      pattern: "full composed"
    - from: examples/adoption_demo/lib/adoption_demo_web/components/layouts/root.html.heex
      to: examples/adoption_demo/priv/static/assets/cohort.css
      via: "root keeps app.css and cohort.css while removing default.css"
      pattern: "cohort.css"
---

<objective>
Promote the existing Cohort migration contract into the Phase 101 retirement ratchet and remove the root `default.css` link after the rendered/source scaffold has been migrated by Plans 01 and 02.

Purpose: make the grep-clean condition decisive before the committed static asset is deleted.
Output: full composed render/source assertions and a root layout that loads only `app.css` plus `cohort.css`.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/101-daisyui-retirement-track-b/101-CONTEXT.md
@.planning/phases/101-daisyui-retirement-track-b/101-RESEARCH.md
@.planning/phases/101-daisyui-retirement-track-b/101-VALIDATION.md
@.planning/phases/101-daisyui-retirement-track-b/101-UI-SPEC.md
@.planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-RESEARCH.md
@.planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-01-SUMMARY.md
@examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs
@examples/adoption_demo/lib/adoption_demo_web/components/core_components.ex
@examples/adoption_demo/lib/adoption_demo_web/components/layouts.ex
@examples/adoption_demo/lib/adoption_demo_web/components/layouts/root.html.heex
@examples/adoption_demo/lib/adoption_demo_web/router.ex
</context>

## Artifacts This Phase Produces

- Test helper behavior changed in `cohort_migration_contract_test.exs`: `assert_daisyui_retired/1` scans full composed route HTML for Phase 101 rather than the old page-body slice.
- Test names added in `cohort_migration_contract_test.exs`: `composed Cohort routes are free of retired shared scaffold`, `Phase 101 source and deleted generator files stay retired`, `root layout no longer links default css`.
- Removed root link: `<link phx-track-static rel="stylesheet" href={~p"/assets/default.css"} />`.
- Root links kept: `/assets/css/app.css` and `/assets/cohort.css`.
- Final plan artifact still pending after this plan: deletion of `examples/adoption_demo/priv/static/assets/default.css` and the file-absence ratchet.

## Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | - | daisyUI/Tailwind scaffold gone from inner pages and demo grep-clean | 101-01..101-04 | COVERED | This plan makes the clean scan decisive. |
| REQ | COHORT-05 | Remove `default.css` link only once grep is clean | 101-03 | COVERED | Depends on Plans 01 and 02. |
| RESEARCH | - | Promote existing ExUnit module, no new CI tooling | 101-03 | COVERED | Implements D-101-10. |
| RESEARCH | - | Anchor banned literals to class boundaries to avoid `.ck-*` false failures | 101-03 | COVERED | Contract task. |
| CONTEXT | D-101-10 | Full composed render + source/file assertions | 101-03, 101-04 | COVERED | Link assertion lands here; file absence lands in Plan 04. |
| CONTEXT | D-101-11 | Gate before destructive delete | 101-03 | COVERED | `default.css` file remains until Plan 04. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote the Cohort migration contract to a full Phase 101 ratchet</name>
  <files>examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs</files>
  <read_first>
    - .planning/phases/101-daisyui-retirement-track-b/101-CONTEXT.md
    - .planning/phases/101-daisyui-retirement-track-b/101-RESEARCH.md
    - .planning/phases/101-daisyui-retirement-track-b/101-VALIDATION.md
    - .planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-01-SUMMARY.md
    - examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs
    - examples/adoption_demo/lib/adoption_demo_web/components/core_components.ex
    - examples/adoption_demo/lib/adoption_demo_web/components/layouts.ex
    - examples/adoption_demo/lib/adoption_demo_web/router.ex
  </read_first>
  <behavior>
    - `assert_daisyui_retired/1` checks full composed route HTML after Phase 101, including `Layouts.app` and `flash_group`.
    - Existing route contract tests for `/dashboard`, `/ops`, `/account`, member, lesson, post, media, and all `/upload` tabs call the widened scan unchanged.
    - Source/file assertions cover `core_components.ex`, `layouts.ex`, `root.html.heex`, and the deleted generator landing paths.
  </behavior>
  <action>Update the module documentation and helper comments so Phase 101 no longer claims the shared `Layouts.app` chrome is out of scope. Change `assert_daisyui_retired/1` to scan the full rendered HTML rather than `page_body/1`; keep or remove `page_body/1` only if another helper still needs it. Extend `@retired_daisyui_classes` with the locked shared-scaffold class-boundary literals from D-101-10 for the old layout wrapper and old flash surface. Add a source/file test that reads `core_components.ex`, `layouts.ex`, `root.html.heex`, and checks the generator landing file paths; it must assert the Plan 01 and Plan 02 artifacts remain retired. Keep checks exact and class-boundary anchored so valid Cohort classes cannot false-fail. Do not add shell-only grep tooling or CI jobs.</action>
  <verify>
    <automated>cd examples/adoption_demo &amp;&amp; mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The existing page and upload contract tests now fail if a retired shared layout or flash class appears anywhere in the composed route render.
    - The source/file test fails if the retired `core_components.ex` or `layouts.ex` scaffold returns.
    - The source/file test fails if the deleted generator landing files reappear.
    - The test module still refutes `raw(` in rendered route HTML.
  </acceptance_criteria>
  <done>The retirement ratchet covers composed renders and source/file state before `default.css` is unlinked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove the root default.css stylesheet link</name>
  <files>examples/adoption_demo/lib/adoption_demo_web/components/layouts/root.html.heex, examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs</files>
  <read_first>
    - .planning/phases/101-daisyui-retirement-track-b/101-CONTEXT.md
    - .planning/phases/101-daisyui-retirement-track-b/101-RESEARCH.md
    - .planning/phases/101-daisyui-retirement-track-b/101-VALIDATION.md
    - examples/adoption_demo/lib/adoption_demo_web/components/layouts/root.html.heex
    - examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs
  </read_first>
  <behavior>
    - `root.html.heex` links `app.css` and `cohort.css`.
    - `root.html.heex` does not link `/assets/default.css`.
    - The committed `default.css` file is not deleted by this plan; deletion remains blocked on Plan 04.
  </behavior>
  <action>Remove only the `default.css` stylesheet link from `root.html.heex`; keep the `app.css`, `cohort.css`, JavaScript, CSRF, and live title lines intact. In the Phase 101 source/file test, assert the root template no longer references `default.css`. Do not delete `examples/adoption_demo/priv/static/assets/default.css` in this plan.</action>
  <verify>
    <automated>cd examples/adoption_demo &amp;&amp; mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `root.html.heex` has no `default.css` link.
    - `root.html.heex` still links `app.css` and `cohort.css`.
    - `examples/adoption_demo/priv/static/assets/default.css` still exists after this plan.
    - Targeted ExUnit passes with the root-link assertion.
  </acceptance_criteria>
  <done>The root layout no longer loads the scaffold stylesheet, but the asset deletion is still reserved for the final wave.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Full route render -> static assertions | Rendered HTML includes layout, flash, and page body; source assertions must avoid broad false positives. |
| Root layout -> static assets | Stylesheet links determine browser-visible CSS dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-101-01 | Tampering / XSS | Render/source ratchet | mitigate | Preserve `raw(` refutation and do not add raw rendering while widening composed render checks. |
| T-101-05 | Tampering | Root stylesheet links | mitigate | Assert `root.html.heex` keeps only intended stylesheet links before deleting the static asset. |
| T-101-AUTH | Spoofing / Elevation | Auth/access | accept | N/A: no auth, route, permission, or storage behavior changes. |
| T-101-SC | Tampering | npm/pip/cargo installs | accept | N/A: no package install task and research package audit is N/A. |
</threat_model>

<verification>
Plan-level check:
- `cd examples/adoption_demo && mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs`
</verification>

<success_criteria>
COHORT-05 gate-promotion scope is complete when the existing ExUnit module proves full composed renders and source/file state are clean, and `root.html.heex` no longer links the scaffold stylesheet.
</success_criteria>

<output>
Create `.planning/phases/101-daisyui-retirement-track-b/101-03-SUMMARY.md` when done.
</output>

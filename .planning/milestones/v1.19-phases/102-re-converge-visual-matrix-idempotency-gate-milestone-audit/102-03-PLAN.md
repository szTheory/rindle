---
phase: 102-re-converge-visual-matrix-idempotency-gate-milestone-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/adoption_demo/lib/adoption_demo_web/cohort_theme.ex
  - examples/adoption_demo/lib/adoption_demo_web/live/dashboard_live.ex
  - examples/adoption_demo/lib/adoption_demo_web/live/ops_live.ex
  - examples/adoption_demo/lib/adoption_demo_web/live/account_live.ex
  - examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs
autonomous: true
requirements:
  - VIS-01
  - VIS-02
requirements_addressed:
  - VIS-01
  - VIS-02
must_haves:
  truths:
    - "D-102-06: dashboard, ops, and account erasure dark coverage is driven by rendered `data-theme` route state, not media emulation alone."
    - "D-102-08: Cohort matrix surfaces begin carrying a common light/dark route contract."
    - "D-102-09: existing Cohort migration contract remains the no-regression backstop."
  artifacts:
    - path: "examples/adoption_demo/lib/adoption_demo_web/cohort_theme.ex"
      provides: "shared Cohort theme enum normalization"
      exports: ["normalize/2"]
    - path: "examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs"
      provides: "rendered dark-route assertions for static Cohort surfaces"
  key_links:
    - from: "examples/adoption_demo/e2e/cohort-pages.spec.js"
      to: "examples/adoption_demo/lib/adoption_demo_web/live/dashboard_live.ex"
      via: "route query `?theme=dark` renders `[data-ck-root][data-theme=\"dark\"]`"
      pattern: "data-theme"
  prohibitions:
    - "D-102-06: do not use Playwright color-scheme emulation as the only dark proof."
    - "D-102-10: do not generate `cohort.css` from `tokens.json`."
    - "D-102-09: do not weaken Cohort contract or daisyUI retirement assertions."
---

<objective>
Add explicit rendered theme route support to the static Cohort surfaces that already render through `ck_page/1`.

Purpose: The hard-fail Cohort matrix needs deterministic dark URLs for `/dashboard`, `/ops`, and account erasure. This plan establishes the shared enum helper and first set of route-backed dark proofs.
Output: `?theme=dark` renders `data-theme="dark"` on the Cohort root for these surfaces, with invalid values normalized to light.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/102-re-converge-visual-matrix-idempotency-gate-milestone-audit/102-CONTEXT.md
@.planning/phases/102-re-converge-visual-matrix-idempotency-gate-milestone-audit/102-RESEARCH.md
@guides/ui_principles.md
@examples/adoption_demo/AGENTS.md
@.planning/phases/96-cohort-component-layer-dark-reduced-motion-contract-track-b/96-CONTEXT.md
@.planning/phases/99-cohort-page-migrations-the-small-7-track-b/99-01-SUMMARY.md
@examples/adoption_demo/lib/adoption_demo_web/components/cohort_components.ex
@examples/adoption_demo/lib/adoption_demo_web/live/dashboard_live.ex
@examples/adoption_demo/lib/adoption_demo_web/live/ops_live.ex
@examples/adoption_demo/lib/adoption_demo_web/live/account_live.ex
@examples/adoption_demo/lib/adoption_demo_web/live/upload_live.ex
@examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs
</context>

<must_haves>
<truths>
- D-102-06: dark pages prove the explicit rendered root state.
- D-102-08: `/styleguide`, `/dashboard`, `/ops`, and account erasure are prepared for the Phase 102 matrix.
- D-102-09: contract tests still preserve IDs, testids, hooks, daisyUI retirement, and source/file ratchets.
</truths>
<prohibitions>
- No client storage or body-level theme root.
- No invalid theme value rendering into `data-theme`.
- No Cohort CSS generation or token-source merge with admin.
</prohibitions>
</must_haves>

## Artifacts this phase produces

- `AdoptionDemoWeb.CohortTheme.normalize/2`.
- `DashboardLive.mount/3` route-param theme support.
- `OpsLive.mount/3` route-param theme support.
- `AccountLive.mount/3` route-param theme support.
- Contract-test dark assertions for `/dashboard?theme=dark`, `/ops?theme=dark`, and `/account/:id/delete?theme=dark`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared Cohort theme normalization</name>
  <files>examples/adoption_demo/lib/adoption_demo_web/cohort_theme.ex, examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs</files>
  <read_first>
    - `examples/adoption_demo/lib/adoption_demo_web/live/upload_live.ex` for the existing `normalize_theme/2` behavior.
    - `examples/adoption_demo/lib/adoption_demo_web/components/cohort_components.ex` for `ck_page/1` `attr :theme` values.
    - `examples/adoption_demo/AGENTS.md` for Phoenix/LiveView test style.
  </read_first>
  <behavior>
    - `normalize("dark", "light")` returns `"dark"`.
    - `normalize("light", "dark")` returns `"light"`.
    - Unknown or missing values return the supplied default without atom creation.
  </behavior>
  <action>
    Add `AdoptionDemoWeb.CohortTheme` with a small `normalize/2` function that allowlists only `"light"` and `"dark"` and returns the provided default for every other value. Use string matching only; do not convert user input to atoms. Add a focused assertion in `cohort_migration_contract_test.exs` or a nearby existing test module that proves valid and invalid values. This helper implements the D-102-06/D-102 security requirement for route-driven theme state.
  </action>
  <acceptance_criteria>
    - The helper exists in a web namespace file and exports `normalize/2`.
    - Invalid theme strings cannot render as `data-theme` values.
    - No new dependency is introduced.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/adoption_demo &amp;&amp; mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs</automated>
  </verify>
  <done>Shared theme normalization is available for all Cohort pages and is regression-covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire static Cohort surfaces to rendered theme route state</name>
  <files>examples/adoption_demo/lib/adoption_demo_web/live/dashboard_live.ex, examples/adoption_demo/lib/adoption_demo_web/live/ops_live.ex, examples/adoption_demo/lib/adoption_demo_web/live/account_live.ex, examples/adoption_demo/test/adoption_demo_web/live/cohort_migration_contract_test.exs</files>
  <read_first>
    - `dashboard_live.ex`, `ops_live.ex`, and `account_live.ex` for current `mount/3` signatures and `ck_page theme={@theme}` wiring.
    - `cohort_migration_contract_test.exs` for `render_route/2`, `assert_frozen_contract/2`, and `assert_daisyui_retired/1`.
    - `upload_live.ex` for the existing route-backed theme precedent.
  </read_first>
  <behavior>
    - `/dashboard?theme=dark`, `/ops?theme=dark`, and `/account/:id/delete?theme=dark` render `[data-ck-root]` with `data-theme="dark"`.
    - The same routes with an invalid theme render `data-theme="light"`.
    - Existing frozen DOM selectors remain present.
  </behavior>
  <action>
    Change the three LiveViews to accept `params` in `mount/3`, set `:theme` through `AdoptionDemoWeb.CohortTheme.normalize(params["theme"], "light")`, and keep their existing `ck_page theme={@theme}` usage. Update the contract test to render dark and invalid-theme variants for these routes and assert the actual root attribute while still calling `assert_frozen_contract/2` and `assert_daisyui_retired/1`. Preserve every existing ID, `data-testid`, and route behavior.
  </action>
  <acceptance_criteria>
    - The three routes can be opened with `?theme=dark` and render a dark Cohort root.
    - Invalid theme params normalize to light.
    - Existing route contract tests still pass with no removed selectors.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/adoption_demo &amp;&amp; mix test test/adoption_demo_web/live/cohort_migration_contract_test.exs</automated>
    <automated>node brandbook/src/cohort-contrast.mjs</automated>
  </verify>
  <done>Dashboard, ops, and account erasure are ready for hard-fail light/dark matrix cases using rendered route state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query -> LiveView assigns | `theme` query values cross from untrusted user input into rendered `data-theme`. |
| LiveView DOM -> Playwright matrix | Browser proof relies on root attributes rendered by the server. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-03 | Tampering | Cohort `theme` query param | mitigate | Allowlist `"light"`/`"dark"` in `CohortTheme.normalize/2`; default all other values. |
| T-102-02 | Repudiation | dark-mode proof | mitigate | Contract tests assert rendered `[data-ck-root][data-theme="dark"]`; no media-only proof. |
| T-102-05 | Tampering | Cohort CSS ownership | mitigate | Route-state only; no Cohort CSS generator or token-source merge. |
| T-102-SC | Tampering | npm/pip/cargo installs | accept | No package install is planned. |
</threat_model>

<verification>
Run the Cohort migration contract and Cohort contrast scanner. This plan is not complete if dark coverage is only proven with browser media emulation.
</verification>

<success_criteria>
The first non-upload Cohort surfaces are route-dark capable and contract-tested without weakening existing behavior gates.
</success_criteria>

<output>
Create `.planning/phases/102-re-converge-visual-matrix-idempotency-gate-milestone-audit/102-03-SUMMARY.md` when done.
</output>

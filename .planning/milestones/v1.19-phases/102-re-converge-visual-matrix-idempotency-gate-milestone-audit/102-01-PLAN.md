---
phase: 102-re-converge-visual-matrix-idempotency-gate-milestone-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/adoption_demo/e2e/support/admin.js
autonomous: true
requirements:
  - VIS-01
  - VIS-02
requirements_addressed:
  - VIS-01
  - VIS-02
must_haves:
  truths:
    - "VIS-01/D-102-11: the existing admin Playwright matrix can run without strict-locator ambiguity before Cohort is promoted."
    - "D-102-02/D-102-07: admin remains hard-fail over an explicit `[data-rindle-admin-root]` root and retains the 24-state Phase 98 matrix/backstops."
  artifacts:
    - path: "examples/adoption_demo/e2e/support/admin.js"
      provides: "strict, unique admin shell locator helpers"
      exports: ["adminRoot", "expectAdminShell"]
  key_links:
    - from: "examples/adoption_demo/e2e/support/admin.js"
      to: "examples/adoption_demo/e2e/admin-screenshots.spec.js"
      via: "adminRoot/expectAdminShell imported by the existing admin matrix"
      pattern: "adminRoot|expectAdminShell"
  prohibitions:
    - "D-102-01: do not create a second merge-blocking visual lane."
    - "D-102-02: do not add root auto-detection; the admin root stays explicit."
    - "D-102-07: do not reduce the admin 24-state matrix or Phase 98 computed-style backstops."
---

<objective>
Fix the current admin strict-locator/root ambiguity so the existing admin matrix is a trustworthy prerequisite for VIS-01 and VIS-02.

Purpose: Phase 101 left the full wrapper red because `[data-rindle-admin-root]` matches both shell and page nodes. Phase 102 cannot claim a single merge-blocking visual gate until the admin hard-fail path is strict-safe.
Output: `adminRoot`/`expectAdminShell` continue to expose explicit admin-root helpers while resolving to one visible shell root.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/102-re-converge-visual-matrix-idempotency-gate-milestone-audit/102-CONTEXT.md
@.planning/phases/102-re-converge-visual-matrix-idempotency-gate-milestone-audit/102-RESEARCH.md
@.planning/phases/102-re-converge-visual-matrix-idempotency-gate-milestone-audit/102-VALIDATION.md
@guides/ui_principles.md
@examples/adoption_demo/AGENTS.md
@.planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-VERIFICATION.md
@.planning/phases/101-daisyui-retirement-track-b/101-VERIFICATION.md
@examples/adoption_demo/e2e/support/admin.js
@examples/adoption_demo/e2e/admin-screenshots.spec.js
</context>

<must_haves>
<truths>
- D-102-11: `scripts/ci/adoption_demo_e2e.sh` is no longer blocked by duplicate admin roots.
- D-102-02: admin root selection remains explicit and deterministic.
- D-102-07: admin screenshot/check matrix remains 24 states and keeps Phase 98 backstop calls.
</truths>
<prohibitions>
- No second merge-blocking screenshot or visual regression lane per D-102-01.
- No root auto-detection, body fallback, first-match fallback, or mixed admin/Cohort root inference per D-102-02 and D-94-07.
- No weakening admin coverage, deleting expected screenshot entries, or removing Phase 98 backstop checks per D-102-07.
</prohibitions>
</must_haves>

## Artifacts this phase produces

- `adminRoot(page)` with a unique explicit admin shell selector.
- `expectAdminShell(page, surface)` strict assertions that prove the shell root is unique and still carries `data-rindle-admin-surface`.
- Source-level preservation of the existing admin 24-state matrix and Phase 98 computed-style backstops.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make admin root lookup strict-safe</name>
  <files>examples/adoption_demo/e2e/support/admin.js</files>
  <read_first>
    - `examples/adoption_demo/e2e/support/admin.js` for `adminRoot`, `expectAdminShell`, theme selection, and no-horizontal-scroll helper shape.
    - `examples/adoption_demo/e2e/admin-screenshots.spec.js` for current imports and 24-state matrix usage.
    - `.planning/phases/102-re-converge-visual-matrix-idempotency-gate-milestone-audit/102-RESEARCH.md` Wave 0 gap for strict admin root disambiguation.
  </read_first>
  <behavior>
    - Test before implementation: visiting any admin surface and calling `expectAdminShell(page, surface)` does not hit Playwright strict-mode duplicate-root errors.
    - Test before implementation: a page with both shell and page `[data-rindle-admin-root]` nodes still resolves `adminRoot(page)` to exactly one shell root.
  </behavior>
  <action>
    Update `adminRoot(page)` per D-102-02/D-102-11 so it returns the explicit admin shell root, not every node with the root marker. Use the existing admin shell marker/class already rendered by the console, such as the shell node carrying both `[data-rindle-admin-root]` and `.rindle-admin-shell`; keep the selector literal explicit. Update `expectAdminShell(page, surface)` to assert that the shell-root locator has count 1 before asserting visibility, `data-rindle-admin-surface`, nav, page header, live indicator, and theme controls. Do not add a fallback to the first matching root and do not infer roots from the current page.
  </action>
  <acceptance_criteria>
    - `adminRoot(page)` uses a selector that uniquely targets the admin shell root while still containing `[data-rindle-admin-root]`.
    - `expectAdminShell(page, surface)` asserts a single shell root before reading attributes.
    - Existing exports and call sites keep the same function names.
  </acceptance_criteria>
  <verify>
    <automated>node --check examples/adoption_demo/e2e/support/admin.js examples/adoption_demo/e2e/admin-screenshots.spec.js</automated>
    <automated>cd examples/adoption_demo &amp;&amp; npx playwright test e2e/admin-screenshots.spec.js --grep "captures admin-screenshots light and dark matrix"</automated>
  </verify>
  <done>Admin shell lookup is strict-safe, explicit-root-only, and the existing admin matrix can execute without duplicate-root failures.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve admin matrix and Phase 98 backstop invariants</name>
  <files>examples/adoption_demo/e2e/support/admin.js</files>
  <read_first>
    - `examples/adoption_demo/e2e/admin-screenshots.spec.js` for `expectedScreenshots`, `toHaveLength(24)`, and Phase 98 backstop test.
    - `.planning/phases/98-admin-level-3-page-composition-motion-mobile-a11y-ia-microco/98-VERIFICATION.md` for the five runtime backstops delegated to Phase 102.
  </read_first>
  <action>
    After the root helper change, run source assertions against `admin-screenshots.spec.js` without editing the matrix unless the strict-root fix exposes a real failing assertion. Preserve `expectedScreenshots.length === 24`, the two band/stacked screenshot entries, and calls to `assertTwoPaneBand`, `assertStackedCard`, `assertReducedMotion`, `assertDialogInert`, and `assertFocusVisibleVsPointer` per D-102-07. If a targeted Playwright run still fails, fix only the strict-locator issue in `admin.js`; do not remove admin cases.
  </action>
  <acceptance_criteria>
    - Admin matrix still contains exactly 24 expected screenshots.
    - Phase 98 backstop identifiers remain present in `admin-screenshots.spec.js`.
    - The helper change does not alter screenshot artifact paths or add a new screenshot assertion lane.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('examples/adoption_demo/e2e/admin-screenshots.spec.js','utf8');for (const token of ['toHaveLength(24)','assertTwoPaneBand','assertStackedCard','assertReducedMotion','assertDialogInert','assertFocusVisibleVsPointer']) { if (!s.includes(token)) throw new Error('missing '+token); }"</automated>
  </verify>
  <done>Admin coverage is unchanged except for the strict-safe root helper required by D-102-11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright test harness -> admin LiveView DOM | Browser assertions consume rendered admin markup and must not hide duplicate roots. |
| CI wrapper -> merge decision | `adoption-demo-e2e` remains the merge-blocking browser lane for visual proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-01 | Tampering | `adminRoot(page)` | mitigate | Use a unique explicit shell selector and assert count 1; no fallback/root inference. |
| T-102-02 | Repudiation | admin matrix evidence | mitigate | Preserve 24-state matrix and Phase 98 backstop source assertions. |
| T-102-04 | Denial of Service | Playwright lane | mitigate | Fix strict-locator failure instead of retrying or disabling admin tests. |
| T-102-SC | Tampering | npm/pip/cargo installs | accept | No package install is planned in this plan; use existing Playwright dependency. |
</threat_model>

<verification>
Plan-level proof is `node --check` plus targeted admin screenshot matrix run. The plan is not complete if admin failures are resolved by deleting cases, loosening roots, or moving proof outside the existing lane.
</verification>

<success_criteria>
Admin matrix readiness is restored, VIS-01/VIS-02 can rely on admin hard-fail proof, and D-102-01/D-102-02/D-102-07/D-102-11 remain satisfied.
</success_criteria>

<output>
Create `.planning/phases/102-re-converge-visual-matrix-idempotency-gate-milestone-audit/102-01-SUMMARY.md` when done.
</output>

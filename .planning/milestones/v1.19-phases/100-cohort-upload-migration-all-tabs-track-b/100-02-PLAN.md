---
phase: 100-cohort-upload-migration-all-tabs-track-b
plan: 02
type: execute
wave: 2
depends_on:
  - 100-01
files_modified:
  - examples/adoption_demo/e2e/cohort-pages.spec.js
autonomous: true
requirements:
  - COHORT-02
user_setup: []

must_haves:
  truths:
    - "Each /upload?tab=X passes the warn-mode polish gate (focus-visible ring, 44px targets, effective-bg contrast) over [data-ck-root] (D-100-07)"
    - "/upload?tab=image&theme=dark renders [data-ck-root][data-theme=\"dark\"] and passes the polish gate in dark — actually exercising dark tokens (Pitfall F option 1)"
    - "The 6 existing behavior specs ({image,video,multipart,liveview,mux,tus}-*.spec.js) stay green post-migration — the frozen-contract runtime backstop (D-100-07)"
    - "The tus-resume ?tab=tus deep link still works (the routed patch model was preserved, not replaced by events)"
  artifacts:
    - path: "examples/adoption_demo/e2e/cohort-pages.spec.js"
      provides: "6 per-tab /upload polish cases (a for loop) + 1 dark image-tab case, reusing assertCohortPagePolish UNCHANGED"
      contains: "upload-image-dark-cohort"
  key_links:
    - from: "examples/adoption_demo/e2e/cohort-pages.spec.js"
      to: "examples/adoption_demo/e2e/support/admin-polish.js"
      via: "assertCohortPagePolish reuses assertAdminPolish UNCHANGED (D-96-06) over [data-ck-root]"
      pattern: "assertCohortPagePolish"
    - from: "examples/adoption_demo/e2e/cohort-pages.spec.js"
      to: "examples/adoption_demo/lib/adoption_demo_web/live/upload_live.ex"
      via: "the dark case drives route /upload?tab=image&theme=dark → server data-theme=\"dark\""
      pattern: "theme=dark"
---

<objective>
Prove the migrated `/upload` surface at runtime: add the per-tab warn-mode polish cases (6, one per tab) + one dark case on the image tab to the existing Phase-99 polish harness, and confirm the 6 existing upload behavior specs stay green post-migration (the frozen-contract runtime backstop). This closes COHORT-02 SC1 (polish gate covers the upload surface), SC2 (behavior e2e green across tabs), and SC3 (a light/dark polish case covers the upload surface).

This plan EXTENDS (never forks) the existing `e2e/cohort-pages.spec.js` — it reuses the exported `assertCohortPagePolish(page, {route, surface})` UNCHANGED (which reuses `admin-polish.js`'s `assertAdminPolish` UNCHANGED, D-96-06). It depends on Plan 01: the migrated `.ck` DOM (so `[data-ck-root]` exists and the polish gate is non-vacuous, Pitfall D) and the validated `?theme=dark` server read (so the dark case actually exercises dark tokens, Pitfall F — not `emulateMedia`, which leaves the explicit `data-theme="light"` authoritative).

Purpose: COHORT-02's success criteria explicitly require "a light/dark polish case covers the upload surface" and "behavior e2e stays green across tabs." The static gate (Plan 01) proves the contract survived; this plan proves it renders and works.

Output: 6 per-tab + 1 dark polish case in `cohort-pages.spec.js`; documented green-confirmation of the 6 unchanged behavior specs + the full `adoption-demo-e2e` lane.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/100-cohort-upload-migration-all-tabs-track-b/100-CONTEXT.md
@.planning/phases/100-cohort-upload-migration-all-tabs-track-b/100-RESEARCH.md
@.planning/phases/100-cohort-upload-migration-all-tabs-track-b/100-PATTERNS.md
@.planning/phases/100-cohort-upload-migration-all-tabs-track-b/100-VALIDATION.md

# The polish harness to EXTEND (exported helper reused unchanged)
@examples/adoption_demo/e2e/cohort-pages.spec.js

# The reused-unchanged polish engine (DO NOT EDIT — D-96-06)
@examples/adoption_demo/e2e/support/admin-polish.js

# The 6 behavior specs (UNCHANGED — confirm green; tus-resume uses ?tab=tus)
@examples/adoption_demo/e2e/tus-resume.spec.js
@examples/adoption_demo/e2e/image-upload.spec.js

# What Plan 01 produced (the migrated DOM + the validated ?theme read)
@examples/adoption_demo/lib/adoption_demo_web/live/upload_live.ex
@.planning/phases/100-cohort-upload-migration-all-tabs-track-b/100-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

- **New Playwright test cases (`cohort-pages.spec.js`):** a `for (const tab of ["image","tus","video","multipart","liveview","mux"])` loop emitting 6 `test(...)` cases, each calling `assertCohortPagePolish(page, {route: "/upload?tab=X", surface: "upload-X-cohort"})` (warn mode).
- **New dark case:** `test("/upload?tab=image renders on the Cohort DS in dark …")` driving `route: "/upload?tab=image&theme=dark"` → server `data-theme="dark"` (Pitfall F option 1), reusing `assertCohortPagePolish`.

No edits to `admin-polish.js`, `assertCohortPagePolish` itself, the 6 behavior specs, `upload_live.ex`, or `cohort.css`. Surface strings are at Claude's discretion (e.g. `upload-X-cohort` / `upload-image-dark-cohort`).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add the 6 per-tab polish cases + 1 dark image-tab case to cohort-pages.spec.js</name>
  <files>examples/adoption_demo/e2e/cohort-pages.spec.js</files>
  <read_first>
    - examples/adoption_demo/e2e/cohort-pages.spec.js — the existing single-route entries (e.g. the `/ops` and `/dashboard` `test(...)` calls) to mirror; the exported `assertCohortPagePolish` helper (it goto's, waits for the LiveSocket, asserts `[data-ck-root]` visibility FIRST — Pitfall D guard — then runs `assertAdminPolish` in warn mode); the `interactiveSelectors` array (already includes `.ck-tab`).
    - .planning/phases/100-cohort-upload-migration-all-tabs-track-b/100-PATTERNS.md — Pattern Assignment 4 (the exact `for`-loop shape + the dark-case shape).
    - examples/adoption_demo/lib/adoption_demo_web/live/upload_live.ex — confirm the `?theme=dark` read Plan 01 added so the dark route yields `data-theme="dark"`.
  </read_first>
  <action>
    EXTEND `cohort-pages.spec.js` (do NOT fork; do NOT edit `assertCohortPagePolish` or `admin-polish.js` — D-96-06). Add a `for (const tab of ["image", "tus", "video", "multipart", "liveview", "mux"])` loop emitting one `test(...)` per tab that calls `assertCohortPagePolish(page, { route: \`/upload?tab=${tab}\`, surface: \`upload-${tab}-cohort\` })` (warn mode; the helper's `[data-ck-root]` guard prevents a vacuous pass, and `load_member!(nil)` falls back to the first seeded member so no member id is needed).

    Add ONE dark case on the image tab (Pitfall F option 1 — the corrected mechanism): `test("/upload?tab=image renders on the Cohort DS in dark (polish, warn mode)", …)` calling `assertCohortPagePolish(page, { route: "/upload?tab=image&theme=dark", surface: "upload-image-dark-cohort" })`. The `?theme=dark` drives the SERVER `data-theme="dark"` assign (added in Plan 01) — do NOT use `emulateMedia({colorScheme:"dark"})` (the explicit `data-theme` is authoritative over the `@media` fallback, so emulateMedia alone would silently measure the light theme). Within the dark case, after the polish assertion, additionally assert the surface actually rendered dark: `await expect(page.locator('[data-ck-root]')).toHaveAttribute('data-theme', 'dark')` (the only place this plan adds an assertion beyond the shared helper, to prove the dark path is real, not vacuous).

    Pick the `surface:` strings at your discretion (the `upload-X-cohort` / `upload-image-dark-cohort` convention above mirrors the existing `*-cohort` naming).

    Validate the spec parses: `cd examples/adoption_demo && npx playwright test e2e/cohort-pages.spec.js --list`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rindle/examples/adoption_demo && npx playwright test e2e/cohort-pages.spec.js --list 2>&1 | grep -E 'upload-(image|tus|video|multipart|liveview|mux)|upload\?tab=' | head -20</automated>
    <automated>cd /Users/jon/projects/rindle/examples/adoption_demo && grep -q 'theme=dark' e2e/cohort-pages.spec.js && grep -q "toHaveAttribute('data-theme', 'dark')" e2e/cohort-pages.spec.js && ! grep -q 'emulateMedia' e2e/cohort-pages.spec.js && echo DARK_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cohort-pages.spec.js` lists 6 new `/upload?tab=X` polish cases (one per tab) + 1 dark image-tab case under `--list`.
    - Each case calls the exported `assertCohortPagePolish` UNCHANGED; `admin-polish.js` and the helper are not edited.
    - The dark case drives `route: ".../?...theme=dark"` (server `data-theme`), NOT `emulateMedia`, and additionally asserts `[data-ck-root]` has `data-theme="dark"`.
    - `surface:` strings follow the `*-cohort` convention.
  </acceptance_criteria>
  <done>The polish harness covers /upload as 6 per-tab warn-mode cases + 1 real dark case, reusing the shared engine unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Run the polish cases + confirm the 6 behavior specs + the full lane stay green</name>
  <files>examples/adoption_demo/e2e/cohort-pages.spec.js</files>
  <read_first>
    - examples/adoption_demo/e2e/cohort-pages.spec.js — the 6 + 1 cases added in Task 1.
    - examples/adoption_demo/e2e/{image-upload,video-upload,multipart-upload,liveview-upload,mux-streaming,tus-resume}.spec.js — the behavior backstop (UNCHANGED; they key on testids/ids/hooks, not classes). `tus-resume.spec.js` uses the `?tab=tus` deep link.
    - .planning/reference_minio_local_test_run.md (if reading the user memory) — running the @minio/tus S3 integration suite locally needs a temp ownership_timeout bump + disk-backed MinIO; the `adoption-demo-e2e` lane boots `mix phx.server` + seeds + MinIO/Mux.
  </read_first>
  <action>
    Run the new polish subset and the 6 behavior specs against the running `adoption-demo-e2e` environment (the lane boots `mix phx.server` + seeds + MinIO/Mux). The 6 behavior specs are the frozen-contract runtime backstop and MUST stay green unchanged — a class-only swap that preserved every id/testid/hook/form/submit will not regress them; if any fails it means an element was restructured out of existence (the single hardest constraint) and must be fixed in Plan 01 (re-open, do not modify the behavior specs).

    If the live MinIO/Mux lane cannot run locally (per the MinIO note + prior Phase 96/98/99 CI-delegation precedent — local Postgres/MinIO saturation), CI-delegate the full lane run and document it in the SUMMARY (matching how Phases 98-04/99 handled the lane). At minimum run the polish subset (`cohort-pages.spec.js`) and the `tus-resume` deep-link spec locally if the environment allows; otherwise record the exact CI-delegated commands.

    Re-run `node brandbook/src/cohort-contrast.mjs` once more as the phase-gate sanity check.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/rindle && node brandbook/src/cohort-contrast.mjs</automated>
    <human-check>The full adoption-demo-e2e lane (6 behavior specs + the new 6 per-tab + 1 dark /upload polish cases) is green — run locally if MinIO/Mux is available, else confirm the CI lane is green on the PR. The tus-resume ?tab=tus deep link passes (proves the routed patch model survived). Record the result + commands in the SUMMARY.</human-check>
  </verify>
  <acceptance_criteria>
    - `node brandbook/src/cohort-contrast.mjs` exits 0.
    - The 6 per-tab + 1 dark `/upload` polish cases run (warn mode) over `[data-ck-root]`.
    - The 6 behavior specs (`{image,video,multipart,liveview,mux,tus}-*.spec.js`) are green and UNCHANGED; the `tus-resume` `?tab=tus` deep link passes.
    - If the live lane is CI-delegated, the SUMMARY records the exact commands + the CI-green confirmation (matching the Phase 98/99 precedent).
  </acceptance_criteria>
  <done>The runtime gates (per-tab + dark polish, 6 behavior specs, contrast) confirm /upload migrated without regressing behavior — COHORT-02 SC2/SC3 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → running app | the polish/behavior specs drive the seeded demo app over HTTP; no new server surface is introduced by tests |
| client → LiveView (via the dark case route) | the dark case exercises the `?theme=dark` param read (validated in Plan 01) — same boundary as T-100-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-03 | Spoofing / vacuous-pass (test integrity) | the new per-tab polish cases | mitigate | Reuse `assertCohortPagePolish` UNCHANGED — it asserts `[data-ck-root]` visibility FIRST (Pitfall D guard), so a case where the `.ck` shell failed to render fails rather than passing vacuously. The dark case additionally asserts `data-theme="dark"` so it cannot silently measure the light theme (Pitfall F). |
| T-100-SC | Tampering | npm/hex installs | accept | This plan installs ZERO packages (Playwright is the existing lane). Per RESEARCH `## Package Legitimacy Audit` (N/A). No `[ASSUMED]`/`[SUS]` package. |

No high-severity threats: this plan adds test cases only; it edits no server code, no CSS, no `admin-polish.js` (D-96-06). The only param boundary touched (the dark route) is the enum-gated `?theme` read mitigated in Plan 01 (T-100-01).
</threat_model>

<verification>
- `npx playwright test e2e/cohort-pages.spec.js --list` shows the 6 per-tab + 1 dark `/upload` cases.
- The dark case drives `?theme=dark` (server) + asserts `data-theme="dark"`; no `emulateMedia` in the spec.
- The 6 behavior specs stay green and UNCHANGED; `tus-resume`'s `?tab=tus` deep link passes.
- `node brandbook/src/cohort-contrast.mjs` exits 0.
- `admin-polish.js`, `assertCohortPagePolish`, the behavior specs, `upload_live.ex`, and `cohort.css` are unchanged by this plan.
</verification>

<success_criteria>
- A light AND dark polish case covers the `/upload` surface (6 per-tab light + 1 real dark image case) — COHORT-02 SC3.
- The upload behavior e2e specs stay green across tabs (the frozen contract holds at runtime) — COHORT-02 SC2.
- The polish gate is non-vacuous (`[data-ck-root]` guarded) and the dark case actually exercises dark tokens (`data-theme="dark"` asserted).
- The routed `?tab=` patch model (incl. the `tus-resume` `?tab=tus` deep link) survived — proven by the green behavior spec.
</success_criteria>

<output>
Create `.planning/phases/100-cohort-upload-migration-all-tabs-track-b/100-02-SUMMARY.md` when done.
</output>

---
phase: 94-foundation-token-pipeline-ci-gate-new-token-categories
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/tokens/tokens.css
  - brandbook/tokens/rindle-admin.css
  - brandbook/admin-gallery/index.html
  - priv/static/rindle_admin/rindle-admin.css
  - brandbook/src/sync-admin-css.mjs
autonomous: true
requirements: [PIPE-01]
must_haves:
  truths:
    - "Re-running the generators on a fresh checkout produces an empty git diff (clean baseline)."
    - "A single committed script (sync-admin-css.mjs) produces priv/static/rindle_admin/rindle-admin.css from the generator output."
    - "The shipped package CSS copy is byte-identical to brandbook/tokens/rindle-admin.css and produced by the script, not hand-copied."
  artifacts:
    - path: "brandbook/src/sync-admin-css.mjs"
      provides: "Single scripted mechanism mirroring generator output to the shipped package copy (D-94-03)"
      contains: "priv/static/rindle_admin/rindle-admin.css"
    - path: "brandbook/tokens/tokens.css"
      provides: "Regenerated brand token sheet with the corrected dark text-on-brand value"
      contains: "--rindle-text-on-brand: #101417"
    - path: "priv/static/rindle_admin/rindle-admin.css"
      provides: "Shipped package CSS copy, regenerated and synced"
  key_links:
    - from: "brandbook/src/sync-admin-css.mjs"
      to: "priv/static/rindle_admin/rindle-admin.css"
      via: "readFileSync(brandbook/tokens/rindle-admin.css) + writeFileSync(priv copy)"
      pattern: "writeFileSync.*rindle_admin"
---

<objective>
Establish a clean, drift-free baseline for the token→CSS pipeline so the new `brandbook-tokens`
gate (Plan 04) can go red→green honestly. Two things: (1) regenerate every committed generated
artifact and commit the corrected output (the live `tokens.css` drift — stale dark
`--rindle-text-on-brand: #F7F4EA` vs source `{ink}` `#101417` — is the canonical PITFALL #6 this
phase exists to kill), and (2) replace today's hand-mirrored shipped CSS copy with a single
committed sync script so the package artifact can never drift silently (D-94-03).

Purpose: A non-empty diff on a fresh checkout right now proves the pipeline is ungated. This plan
removes that drift and creates the single sync mechanism BEFORE any new categories or the CI gate
land, so the gate diffs a fully-current tree.
Output: Regenerated `tokens.css` / `rindle-admin.css` / gallery `index.html`, the new
`brandbook/src/sync-admin-css.mjs`, and the synced shipped copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-foundation-token-pipeline-ci-gate-new-token-categories/94-CONTEXT.md
@.planning/phases/94-foundation-token-pipeline-ci-gate-new-token-categories/94-RESEARCH.md
@.planning/phases/94-foundation-token-pipeline-ci-gate-new-token-categories/94-PATTERNS.md

<interfaces>
<!-- Generator I/O + path-resolution boilerplate the sync script clones. Verified in-session. -->

From brandbook/src/admin-css-build.mjs (top, lines 4-20, and write at 571):
```javascript
import { readFileSync, writeFileSync } from 'node:fs';
import { dirname, join } from 'node:path';
import { fileURLToPath } from 'node:url';
const here = dirname(fileURLToPath(import.meta.url));
const tokensDir = join(here, '..', 'tokens');     // brandbook/tokens
// ...
writeFileSync(adminCssPath, css);                  // adminCssPath = join(tokensDir, 'rindle-admin.css')
```

From brandbook/src/admin-gallery-check.mjs (line 11) — repo-root resolution pattern:
```javascript
const repoRoot = join(here, '..', '..');           // from brandbook/src → repo root
```

Generated-file header convention (admin-css-build.mjs:48, tokens-build.mjs:18):
```
/* generated by brandbook/src/<script>.mjs from tokens.json - do not edit by hand */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate and commit all stale generated artifacts (clean baseline)</name>
  <read_first>
    - brandbook/src/tokens-build.mjs (the emit loops + write at line 59; confirm it is the writer of tokens.css)
    - brandbook/src/admin-css-build.mjs (emit loops 52-75, write line 571 — the writer of rindle-admin.css)
    - brandbook/src/admin-gallery-check.mjs (lines 11, 24-25 — internally re-runs css-build + gallery.mjs; resolves playwright via examples/adoption_demo createRequire)
    - 94-RESEARCH.md Pitfall 1 (the live tokens.css drift: stale --rindle-text-on-brand) and Code Examples (determinism verification commands)
  </read_first>
  <action>
    Run the brandbook generators in order and commit ALL regenerated artifacts so the working tree
    is clean. First install the playwright resolution anchor: `cd examples/adoption_demo && npm ci`
    then `npx playwright install --with-deps chromium` (admin-gallery-check.mjs resolves `playwright`
    via examples/adoption_demo's createRequire — skipping this red-herrings on "Cannot find module
    'playwright'", per PITFALL #2). Then from repo root run `node brandbook/src/tokens-build.mjs`,
    `node brandbook/src/admin-css-build.mjs`, and `node brandbook/src/admin-gallery-check.mjs` (the
    last internally re-runs admin-css-build + admin-gallery.mjs and regenerates
    brandbook/admin-gallery/index.html). Expect `tokens.css` to change its two
    `--rindle-text-on-brand` lines from `#F7F4EA` to `#101417` (the cream-on-green AA fix already
    landed in tokens.json source). Do NOT hand-edit any generated file — every one carries the
    `do not edit by hand` header; the generator is the only writer. This is regeneration of a
    pre-existing stale baseline, NOT a phase bug.
  </action>
  <acceptance_criteria>
    - After running `node brandbook/src/tokens-build.mjs && node brandbook/src/admin-css-build.mjs && node brandbook/src/admin-gallery-check.mjs`, a SECOND run of the same sequence followed by `git diff --exit-code -- brandbook/tokens/tokens.css brandbook/tokens/rindle-admin.css brandbook/admin-gallery/index.html` exits 0 (idempotent / committed baseline).
    - `grep -c '#F7F4EA' brandbook/tokens/tokens.css` reports the stale dark value is gone from the dark/auto `--rindle-text-on-brand` lines (the two regenerated lines now read `#101417`).
    - `node brandbook/src/admin-contrast.mjs` exits 0 (all pairs pass — 41/41 today).
    - `node brandbook/src/admin-gallery-check.mjs` exits 0 (gallery renders, dark-chip contrast holds).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/rindle && node brandbook/src/tokens-build.mjs && node brandbook/src/admin-css-build.mjs && git diff --exit-code -- brandbook/tokens/tokens.css brandbook/tokens/rindle-admin.css</automated>
  </verify>
  <done>tokens.css, rindle-admin.css, and admin-gallery/index.html are regenerated and committed; a fresh regen produces an empty diff; contrast gate green.</done>
</task>

<task type="auto">
  <name>Task 2: Create the scripted CSS-sync mechanism and replace the hand-mirror</name>
  <read_first>
    - brandbook/src/admin-css-build.mjs (lines 4-20 import + path boilerplate, line 571 writeFileSync — the exact pattern to clone)
    - brandbook/src/admin-gallery-check.mjs (line 11 `const repoRoot = join(here, '..', '..')` — repo-root resolution to reach priv/)
    - test/brandbook/admin_design_system_validation_test.exs (line 213 — the existing ExUnit assertion that the two CSS copies are byte-identical; this MUST stay green)
    - mix.exs (line ~279 — `priv/static/rindle_admin` listed in `files:`; the shipped artifact path)
    - 94-RESEARCH.md Pattern 1 "The CSS-sync mechanism" + 94-PATTERNS.md sync-admin-css.mjs section
  </read_first>
  <action>
    Create `brandbook/src/sync-admin-css.mjs` as the single committed mechanism that mirrors the
    generator output to the shipped package copy (D-94-03), replacing today's undocumented
    hand-mirror. Clone the import + path-resolution boilerplate from admin-css-build.mjs (node:fs
    readFileSync/writeFileSync, node:path dirname/join, node:url fileURLToPath) and the repo-root
    resolution from admin-gallery-check.mjs (`join(here, '..', '..')`). The script reads
    `brandbook/tokens/rindle-admin.css` and writes `priv/static/rindle_admin/rindle-admin.css`
    byte-identically (a straight read→write copy, not a re-render — the generator already wrote the
    canonical output). Print a one-line confirmation on success. Run it once so the shipped copy is
    produced by the script. This makes the copy a single mechanism both CI and local developers run,
    and keeps the existing Elixir equality test (admin_design_system_validation_test.exs:213) green.
    Do NOT have admin-css-build.mjs write the second destination — keep the sync as its own
    invokable step so the CI gate (Plan 04) can call it as a discrete step in the D-94-02 order.
  </action>
  <acceptance_criteria>
    - `node brandbook/src/sync-admin-css.mjs` exits 0 and prints a confirmation line.
    - After running it, `diff -q brandbook/tokens/rindle-admin.css priv/static/rindle_admin/rindle-admin.css` reports the files are identical.
    - `grep -c 'priv/static/rindle_admin/rindle-admin.css' brandbook/src/sync-admin-css.mjs` ≥ 1 (the dest path is the script's write target).
    - `mix test test/brandbook/admin_design_system_validation_test.exs` passes (the two-copy equality assertion stays green).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/rindle && node brandbook/src/sync-admin-css.mjs && diff -q brandbook/tokens/rindle-admin.css priv/static/rindle_admin/rindle-admin.css && echo SYNC_OK</automated>
  </verify>
  <done>sync-admin-css.mjs exists, reads the generator output and writes the shipped copy byte-identically; the two copies are identical; the Elixir equality test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-tree source → generated artifact | Only input is the repo's own trusted `tokens.json`; no external/user input crosses here. |
| generator output → shipped package copy | The sync writes into `priv/` which ships in the Hex package (mix.exs `files:`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-01 | Tampering | shipped `priv/static/rindle_admin/rindle-admin.css` drifts from generator output | mitigate | Single scripted sync (sync-admin-css.mjs) + the diff gate (Plan 04) makes drift a hard CI failure; ExUnit equality test is the secondary guard. |
| T-94-02 | Tampering | hand-edited generated CSS bypasses the generator | accept (mitigated downstream) | `do not edit by hand` headers + the Plan 04 diff gate catch it; no runtime risk — CSS custom properties only. |
| T-94-03 | Information Disclosure | this plan reads no secrets | accept | Pure Node + git + file I/O; no `HEX_API_KEY`/MinIO creds involved. Below HIGH — accepted. |
</threat_model>

<verification>
- `node brandbook/src/tokens-build.mjs && node brandbook/src/admin-css-build.mjs && node brandbook/src/sync-admin-css.mjs && git diff --exit-code` exits 0 after commit (the full local idempotency anchor).
- `node brandbook/src/admin-contrast.mjs` exits 0.
- `mix test test/brandbook/admin_design_system_validation_test.exs` passes.
</verification>

<success_criteria>
- A fresh regen + sync produces an empty `git diff` (clean baseline; D-94-10 idempotency anchor satisfied).
- The shipped CSS copy is produced by `sync-admin-css.mjs`, not hand-mirrored, and is byte-identical to the generator output.
- The pre-existing `tokens.css` drift is corrected and committed.
</success_criteria>

<output>
Create `.planning/phases/94-foundation-token-pipeline-ci-gate-new-token-categories/94-01-SUMMARY.md` when done.
</output>

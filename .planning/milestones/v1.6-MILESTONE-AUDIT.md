---
milestone: v1.6
milestone_name: Provider Boundary + Mux
audited: 2026-05-07T20:10:00Z
status: gaps_found
scores:
  requirements: 27/28 fully closed
  phases: 3/4 fully closed; 1/4 human_needed
  integration: 3/4
  flows: 3/4 complete; 1/4 blocked
gaps:
  requirements:
    - id: MUX-18
      status: unsatisfied
      phase: 36-public-dx-onboarding-ci-proof
      claimed_by_plans: ["36-03-PLAN.md"]
      completed_by_plans: ["36-03-SUMMARY.md"]
      verification_status: human_needed
      evidence: "The generated-app mux proof is now wired through `ProcessVariant -> MuxIngestVariant`, but a clean package-consumer build still fails before the smoke can complete because optional GCS / Finch / JOSE structs are referenced unguarded in library code."
  integration:
    - from: generated-app package consumer
      to: mux-enabled install smoke
      issue: "The smoke script now drives provider ingest correctly, but the generated app cannot finish compiling `:rindle` in a clean consumer environment without optional GCS / Finch / JOSE dependencies present."
  flows:
    - flow: package-consumer mux cassette proof
      status: blocked
      affected_requirements: [MUX-18]
      issue: "The runtime mux flow is fixed, but `scripts/install_smoke.sh mux` still stops in generated-app dependency compilation before the proof can close."
tech_debt:
  - phase: planning-state
    items:
      - "The active v1.6 roadmap state still lags the archived milestone evidence."
  - phase: nyquist
    items:
      - "Phase 33, 34, and 36 VALIDATION artifacts remain draft, and Phase 35 still has no VALIDATION artifact."
  - phase: phase-36-dx-hardening
    items:
      - "The remaining closeout work is package-consumer proof hardening, not provider-runtime wiring."
nyquist:
  compliant_phases: []
  partial_phases: [33, 34, 36]
  missing_phases: [35]
  overall: needs_validation_closure
  detail:
    phase_33: "PARTIAL (status=draft, nyquist_compliant=false, wave_0_complete=false)"
    phase_34: "PARTIAL (status=draft, nyquist_compliant=false, wave_0_complete=false)"
    phase_35: "MISSING (no 35-VALIDATION.md present)"
    phase_36: "PARTIAL (status=draft, nyquist_compliant=false, wave_0_complete=false)"
---

# v1.6 Milestone Audit - Provider Boundary + Mux

**Audited:** 2026-05-07  
**Scope:** Phases 33-36 shipped; Phase 37 deferred  
**Result:** gaps_found

## Verdict

The two runtime integration breaks found in the first audit pass are now fixed in live code:

- [ProcessVariant](/Users/jon/projects/rindle/lib/rindle/workers/process_variant.ex:105) now hands off the configured streaming source variant into `Rindle.Workers.MuxIngestVariant` after the local AV variant reaches `ready`.
- [Rindle.HTML](/Users/jon/projects/rindle/lib/rindle/html.ex:203) now passes the full asset/variant struct into [Rindle.Delivery.streaming_url/3](/Users/jon/projects/rindle/lib/rindle/delivery.ex:174), and [Rindle.Delivery](/Users/jon/projects/rindle/lib/rindle/delivery.ex:392) now accepts variant-like maps by `asset_id` for provider-row lookup.

That closes the previously broken server-push ingest path and the streaming-enabled Phoenix HTML playback path.

The milestone is still not ready to archive because Phase 36 package-consumer proof remains incomplete. A clean generated-app smoke run still fails while compiling the packaged dependency, before the mux cassette proof can finish.

## Fixed Since Prior Audit

### F1 - Runtime handoff into provider ingest is live

- [lib/rindle/workers/process_variant.ex](/Users/jon/projects/rindle/lib/rindle/workers/process_variant.ex:105) now calls `maybe_enqueue_streaming_ingest/3` after `persist_ready/7`.
- The enqueue path is gated to the configured streaming source variant and uses `Rindle.Workers.MuxIngestVariant.unique_job_opts/0`, so the live flow now reaches the same idempotent worker contract Phase 34 verified in isolation.
- This closes the system-level gap for `MUX-03`, `MUX-05`, `MUX-06`, and the upstream side of `MUX-07`.

### F2 - HTML playback now honors the provider-aware dispatch contract

- [lib/rindle/html.ex](/Users/jon/projects/rindle/lib/rindle/html.ex:203) no longer sends a bare binary `storage_key` into `streaming_url/3` on streaming-enabled profiles.
- [lib/rindle/delivery.ex](/Users/jon/projects/rindle/lib/rindle/delivery.ex:394) resolves provider rows from either `id` or `asset_id`, so ready variant structs used by `video_tag/3` and `audio_tag/3` resolve against the parent asset's provider row.
- This closes the prior `STREAM-06` / `MUX-19` consumer-surface gap.

## Verification Run

Targeted automated verification passed after the fixes:

- `mix test test/rindle/workers/process_variant_test.exs`
- `mix test test/rindle/html_test.exs`
- `mix test test/rindle/delivery/streaming_dispatch_test.exs`

Combined rerun result: `38 tests, 0 failures`

The generated-app mux smoke helper was also updated to drive the real handoff sequence:

1. `ProcessVariant`
2. `MuxIngestVariant`
3. `MuxSyncProviderAsset` (cassette lane only)

## Remaining Gap

### G1 - Clean package-consumer mux proof still fails before smoke verification

- **Phase:** 36-public-dx-onboarding-ci-proof
- **Affected requirement:** `MUX-18`
- **Evidence:** `bash scripts/install_smoke.sh mux` still fails in the generated app while compiling the packaged `:rindle` dependency. The failing sites reference optional dependency structs directly, including:
  - `Finch.Response.__struct__/1` in [lib/rindle/storage/gcs/client.ex](/Users/jon/projects/rindle/lib/rindle/storage/gcs/client.ex:1)
  - `JOSE.JWK.__struct__/1` in [lib/rindle/ops/runtime_checks.ex](/Users/jon/projects/rindle/lib/rindle/ops/runtime_checks.ex:1)
  - `GcsSignedUrl.Client.__struct__/1` in [lib/rindle/storage/gcs/signer.ex](/Users/jon/projects/rindle/lib/rindle/storage/gcs/signer.ex:1)
- **Impact:** The mux-enabled package-consumer proof is still blocked in a clean dependency environment, so the milestone cannot be closed automatically yet.

## Non-Blocking Debt

- The active roadmap state for v1.6 still lags the archived milestone evidence.
- Phase 33, 34, and 36 validation artifacts still need Nyquist closure; Phase 35 still needs a validation artifact.

## Recommended Next Step

Close the remaining package-consumer compile gap around optional GCS / Finch / JOSE references, rerun `bash scripts/install_smoke.sh mux`, then rerun the milestone audit.

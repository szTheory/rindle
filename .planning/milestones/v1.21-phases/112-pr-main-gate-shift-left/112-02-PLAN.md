---
phase: 112-pr-main-gate-shift-left
plan: 02
type: execute
wave: 2
depends_on: [112-01]
files_modified:
  - .github/workflows/ci.yml
  - test/install_smoke/ci_lane_split_test.exs
autonomous: false
requirements: [GATE-01, GATE-03, GATE-04]
must_haves:
  truths:
    - "The `adoption-demo-e2e-smoke` lane enters `ci-summary.needs` AND `ci-observability.needs` ONLY after an operator confirms phases 108/109/110 landed AND 3 consecutive green push:main `Adoption Demo E2E` runs (GATE-04 ordering)."
    - "After wiring, the smoke lane is gated TRANSITIVELY through `CI Summary` (the sole required check) — never as a second required branch-protection context."
    - "`scripts/ci/eval_ci_summary.sh` and `scripts/setup_branch_protection.sh` are byte-unchanged (`git diff --exit-code` returns 0) — the needs edit is the only change to wire the lane in."
    - "A merge-blocking shipped-artifact meta-test (in `ci_lane_split_test.exs`) asserts the smoke job exists with no `if:` gate AND is present in both `ci-summary.needs` and `ci-observability.needs` — locking GATE-01."
  artifacts:
    - ".github/workflows/ci.yml (adoption-demo-e2e-smoke added to ci-summary.needs + ci-observability.needs)"
    - "test/install_smoke/ci_lane_split_test.exs (GATE-01/GATE-04 shipped-artifact lock)"
  key_links:
    - "ci-summary.needs entry -> eval_ci_summary.sh auto-iterates `toJSON(needs)` (drift-proof; no script edit) -> CI Summary aggregate result."
    - "Operator green-run confirmation (3 consecutive green push:main `Adoption Demo E2E`) -> the `needs:` wiring commit (GATE-04 load-bearing ordering)."
---

<objective>
Wire the (already-existing, already-green-on-PR) `adoption-demo-e2e-smoke` lane into the merge gate —
but ONLY after a human operator confirms the GATE-04 precondition. This is the load-bearing
"needs-wiring" half of the split: gating a still-live flake would import it into the required gate,
the exact failure this milestone exists to prevent.

Purpose: complete GATE-01 (lane in `CI Summary.needs` + `ci-observability.needs`) and GATE-04
(ordering) so the browser smoke becomes merge-blocking transitively through `CI Summary`, with the
byte-frozen gate scripts proven unchanged and a durable shipped-artifact lock.
Output: two new `needs:` lines in ci.yml + a GATE meta-test in the existing
`ci_lane_split_test.exs`.

Locked context (NON-NEGOTIABLE):
- N = 3 consecutive green push:main `Adoption Demo E2E` runs (operator decision, this session).
- The wiring is a `needs:`-ONLY edit. `eval_ci_summary.sh` auto-iterates whatever is in `needs:`
  (drift-proof), and `setup_branch_protection.sh` keeps `REQUIRED_CHECKS=("CI Summary")` as the sole
  context — BOTH stay byte-unchanged. Add the lane as a TRANSITIVE need, never a second required check.
- Do NOT touch `name: CI`, the ci.yml filename, `eval_ci_summary.sh`, or `setup_branch_protection.sh`.
- Zero `lib/` change.
- `autonomous: false`: the checkpoint MUST gate the wiring — an autonomous executor must not wire the
  gate before the operator confirms the green runs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-pr-main-gate-shift-left/112-RESEARCH.md
@.planning/phases/112-pr-main-gate-shift-left/112-VALIDATION.md
@.planning/phases/112-pr-main-gate-shift-left/112-01-SUMMARY.md

# Live files to mirror exactly (read before editing)
@.github/workflows/ci.yml
@scripts/ci/eval_ci_summary.sh
@scripts/setup_branch_protection.sh
@test/install_smoke/ci_lane_split_test.exs
</context>

<artifacts_this_phase_produces>
Drift verification must EXCLUDE these as newly-created (see Plan 01's table). New in THIS plan:
the two `adoption-demo-e2e-smoke` `needs:` entries (ci-summary + ci-observability) and the GATE
meta-test in `ci_lane_split_test.exs`. Everything else in ci.yml / the gate scripts is byte-frozen.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: GATE-04 operator checkpoint — confirm de-flake + 3 green main runs before wiring</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    BLOCKING human checkpoint. The executor must STOP here and obtain explicit operator approval
    before running Task 2 (the `needs:` wiring). Do not wire the lane into `ci-summary.needs` /
    `ci-observability.needs` until this checkpoint is approved — wiring early would import a
    still-live flake into the required merge gate (GATE-04, the load-bearing ordering this milestone
    exists to enforce). Present the verification steps below to the operator and wait for the
    resume-signal.
  </action>
  <what-built>
    Plan 01 added the `adoption-demo-e2e-smoke` lane to ci.yml (runs on every PR, not yet in any
    `needs:`). The de-flake phases that must precede gate-wiring are complete: Phase 108 (Coverage
    single-run), Phase 109 (Subprocess `:epipe`), Phase 110 (Async isolation) all show
    `status: phase_complete` in STATE.md.
  </what-built>
  <how-to-verify>
    GATE-04 is the load-bearing ordering gate — confirm BOTH conditions before approving:

    1. De-flake phases landed (already true per STATE.md): phases 108, 109, 110 are COMPLETE.
       Spot-check: `grep -nE 'Phase 108|Phase 109|Phase 110' .planning/STATE.md` shows them complete.

    2. THREE consecutive green push:main `Adoption Demo E2E` runs. Run:
       `gh run list --workflow=CI --branch=main --limit 20`
       and confirm the THREE most recent push:main `CI` runs each show the `Adoption Demo E2E` job
       (the FULL push:main lane) concluding `success` — i.e. 3 consecutive green, no intervening
       failure/cancel of that job. (`gh run view <run-id> --json jobs` can confirm the per-job
       conclusion if the list view is ambiguous.)

    If fewer than 3 consecutive green `Adoption Demo E2E` runs are observed, DO NOT approve — let CI
    accumulate more green main runs first (the lane must be provably de-flaked before it gates merges).
  </how-to-verify>
  <resume-signal>
    Type "approved" once phases 108/109/110 are complete AND 3 consecutive green push:main
    `Adoption Demo E2E` runs are confirmed. Otherwise describe what you observed (e.g. "only 2 green,
    one cancelled") and hold.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Wire adoption-demo-e2e-smoke into ci-summary.needs + ci-observability.needs</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml:1232-1242 (ci-observability `needs:` list)
    - .github/workflows/ci.yml:1326-1336 (ci-summary `needs:` list)
    - scripts/ci/eval_ci_summary.sh (confirm it iterates `toJSON(needs)` — no edit needed)
    - scripts/setup_branch_protection.sh:17-19 (confirm `REQUIRED_CHECKS=("CI Summary")` — no edit needed)
  </read_first>
  <action>
    Add `- adoption-demo-e2e-smoke` to TWO `needs:` lists only:
    - `ci-summary.needs` (ci.yml:~1326) — append the entry (placement among the existing entries is
      free; keep it adjacent to `adoption-demo-unit` for readability).
    - `ci-observability.needs` (ci.yml:~1232) — append the same entry for timing parity.
    This is the ONLY change required to make the lane merge-blocking: `eval_ci_summary.sh` iterates
    `to_entries[]` of `toJSON(needs)`, so the new need is auto-evaluated (skip==pass; the lane never
    skips on PR, so it contributes a real success/fail). Do NOT add a second required branch-protection
    context — the lane is gated TRANSITIVELY via `CI Summary`. Do NOT edit `eval_ci_summary.sh`,
    `setup_branch_protection.sh`, `name: CI`, or the filename. Do NOT add `package-consumer-full` or
    any other off-PR lane to these needs (the existing `ci_lane_split_test.exs` `refute` on
    `package-consumer-full` in ci-summary.needs must still hold).
  </action>
  <verify>
    <automated>actionlint .github/workflows/ci.yml && git diff --exit-code scripts/ci/eval_ci_summary.sh scripts/setup_branch_protection.sh && bash scripts/ci/test_ci_summary_gate.sh</automated>
  </verify>
  <done>
    `adoption-demo-e2e-smoke` appears in BOTH `ci-summary.needs` and `ci-observability.needs`.
    `actionlint` passes. `eval_ci_summary.sh` + `setup_branch_protection.sh` show zero `git diff`.
    `test_ci_summary_gate.sh` still passes (gate logic unchanged). `package-consumer-full` is still
    absent from `ci-summary.needs`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add the GATE shipped-artifact meta-test (job exists, no if:, in both needs)</name>
  <files>test/install_smoke/ci_lane_split_test.exs</files>
  <read_first>
    - test/install_smoke/ci_lane_split_test.exs (whole file — mirror its `setup_all` + `File.read!`
      `=~`/`refute =~` style; reuse the `ci_summary_needs_block/1` helper at line ~280; note the
      block-isolator pattern for `refute` scoping)
  </read_first>
  <action>
    Add merge-blocking GATE regression assertions to the existing `ci_lane_split_test.exs` (it rides
    the already-required `quality` lane via the default `mix test` suite — no new required check, no
    new file needed; assert SHIPPED artifacts ONLY, never `.planning/`). Add a `GATE` section with:

    - A test that the `adoption-demo-e2e-smoke:` job exists in ci.yml
      (`assert ci =~ "\n  adoption-demo-e2e-smoke:\n"`).
    - A test that the smoke job runs on EVERY PR — isolate the smoke job block (from its key to the
      next top-level `\n  <job>:\n`, mirroring the existing block-isolator helpers) and
      `refute smoke_block =~ "if: github.repository"` and
      `refute smoke_block =~ "github.event_name != 'pull_request'"` (GATE-01: no repo/event gate, so
      it never skips-as-pass), plus `assert smoke_block =~ ~s(ADOPTION_DEMO_E2E_SPECS:)` and that the
      two-spec subset string is present (`assert smoke_block =~ "e2e/smoke.spec.js e2e/admin-console.spec.js"`)
      and the screenshot spec is excluded (`refute smoke_block =~ "admin-screenshots.spec.js"`).
    - A test that the lane is in BOTH needs lists: reuse `ci_summary_needs_block/1` and
      `assert needs =~ "- adoption-demo-e2e-smoke\n"`; add a sibling `ci_observability_needs_block/1`
      isolator (split on `\n  ci-observability:\n` then `\n    needs:\n` up to `\n    if:`) and assert
      the same entry is present (GATE-01 + GATE-04: the wiring is locked).

    Add a new block-isolator helper for the smoke job body alongside the existing
    `package_consumer_full_block/1` / `ci_summary_needs_block/1` helpers. Do NOT couple to any
    `.planning/` path, any SHA-pinned `@vX` action tag, or any mutable detail — only the GATE topology
    facts. Keep the module `async: true` (it is read-only `File.read!`).
  </action>
  <verify>
    <automated>mix test test/install_smoke/ci_lane_split_test.exs</automated>
  </verify>
  <done>
    `mix test test/install_smoke/ci_lane_split_test.exs` passes with the new GATE assertions: the
    smoke job exists, has no repo/event `if:` gate, sets the two-spec `ADOPTION_DEMO_E2E_SPECS`
    (excluding the screenshot spec), and is present in both `ci-summary.needs` and
    `ci-observability.needs`. No `.planning/` path or mutable action tag is asserted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ci.yml `needs:` → CI Summary aggregate | The smoke lane's result now flows into the sole required check. |
| GitHub branch protection (live) → required contexts | Stores `["CI Summary"]`; the new lane must NOT become a second required context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-112-05 | Denial of Service (of merges) | importing a live flake into the required gate | high | mitigate | The blocking-human checkpoint enforces GATE-04: the lane enters `needs:` ONLY after 108/109/110 land AND N=3 consecutive green push:main `Adoption Demo E2E` runs are confirmed. `autonomous: false` prevents an unattended executor from wiring early. |
| T-112-06 | Spoofing (gate status) | a second required branch-protection context | high | mitigate | The lane is gated TRANSITIVELY via `CI Summary.needs` only; `setup_branch_protection.sh` `REQUIRED_CHECKS=("CI Summary")` stays byte-unchanged (asserted by `git diff --exit-code` + the existing RELEASE-COUPLING meta-test). No second required context is created. |
| T-112-07 | Tampering | accidental edit to the byte-frozen gate scripts | high | mitigate | Task 1's verify runs `git diff --exit-code` on `eval_ci_summary.sh` + `setup_branch_protection.sh`; the wiring is a `needs:`-only edit by construction. |

block-on:high — every high-severity threat is mitigated by the checkpoint ordering (T-112-05), the
transitive-gating constraint (T-112-06), and the byte-frozen-script diff guard (T-112-07). No
unmitigated high-severity threat remains.
</threat_model>

<verification>
- The blocking-human checkpoint was approved (108/109/110 complete + 3 consecutive green push:main
  `Adoption Demo E2E` runs) BEFORE the `needs:` wiring commit.
- `adoption-demo-e2e-smoke` present in BOTH `ci-summary.needs` and `ci-observability.needs`.
- `git diff --exit-code scripts/ci/eval_ci_summary.sh scripts/setup_branch_protection.sh` returns 0.
- `bash scripts/ci/test_ci_summary_gate.sh` passes; `actionlint .github/workflows/ci.yml` passes.
- `scripts/setup_branch_protection.sh --print-expected` output is unchanged (sole context `CI Summary`).
- `mix test test/install_smoke/ci_lane_split_test.exs` passes (GATE lock + existing LANE asserts).
- `head -1 .github/workflows/ci.yml` is exactly `name: CI`; no `lib/` file changed.
</verification>

<success_criteria>
- GATE-01 (wiring half): the lean lane is in `CI Summary.needs` + `ci-observability.needs`.
- GATE-03: the byte-frozen gate scripts are unchanged; `CI Summary` remains the sole required check;
  no second required context.
- GATE-04: the wiring landed ONLY after the operator confirmed the de-flake phases + 3 consecutive
  green push:main `Adoption Demo E2E` runs.
- The GATE topology is durably locked by a shipped-artifact meta-test on the already-required `quality` lane.
</success_criteria>

<output>
Create `.planning/phases/112-pr-main-gate-shift-left/112-02-SUMMARY.md` when done.
</output>

---
phase: 111-regression-locks
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [LOCK-02]
must_haves:
  truths:
    - "On every PR, the package-consumer lane uninstalls the phx.new archive BEFORE running the built-artifact smoke"
    - "The cold-runner phx.new self-install path in install_smoke.sh is genuinely exercised every PR (not warm-cache theater)"
  artifacts:
    - ".github/workflows/ci.yml (new run: step in the lean package-consumer job)"
  key_links:
    - "Purge step is positioned BEFORE the `Run built-artifact image-only…` smoke step (~:651)"
    - "Purge lives in the lean PR-gating `package-consumer` job (:541), NOT package-consumer-full (:678, off-PR)"
---

<objective>
Add ONE `run:` step to the lean PR-gating `package-consumer` job in `.github/workflows/ci.yml`
that purges the phx.new hex archive (`mix archive.uninstall phx_new --force || true`) immediately
BEFORE the built-artifact smoke step. This makes the cold-runner self-install path in
`scripts/install_smoke.sh` genuinely exercised on every PR.

Purpose: An honest cold-path lock, not theater (RESEARCH §6 footgun #1). A guard that only runs
in the same warm cache that hid the original bug proves nothing; purging the archive forces the
real self-install path each PR, so a deleted guard would fail for real.
Output: `.github/workflows/ci.yml` with the new purge step.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Edit target (read the package-consumer job range before editing — re-verify the live line)
@.github/workflows/ci.yml
</context>

<artifacts_this_phase_produces>
NEW symbol introduced by this plan:
- ci.yml step name `Purge phx.new archive to exercise the cold-runner self-install path` in the `package-consumer` job
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: LOCK-02 — purge phx.new archive before the package-consumer smoke</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml — the `package-consumer:` job key (`:541`); `Set up MinIO for S3-compatible package-consumer proofs` step (`:645`); `Run built-artifact image-only package-consumer proof against MinIO` → `bash scripts/install_smoke.sh image` (`:651-652`); confirm `package-consumer-full:` is the OFF-PR job at `:678` (do NOT edit it). RE-VERIFY these line numbers live at execute time — they may have drifted.
  </read_first>
  <action>
    In `.github/workflows/ci.yml`, insert a new step into the lean PR-gating `package-consumer` job
    (job key `:541`), positioned IMMEDIATELY BEFORE the `Run built-artifact image-only
    package-consumer proof against MinIO` step (`:651`) — after the `Set up MinIO…` step (`:645`).
    The step (exact YAML, name is Claude's Discretion per D, position is mandatory):

      - name: Purge phx.new archive to exercise the cold-runner self-install path
        run: mix archive.uninstall phx_new --force || true

    Match the surrounding 6-space step indentation. The `|| true` is required so the step is a
    no-op when the archive is already absent (the cold-runner case) — the point is to GUARANTEE the
    archive is gone before the smoke, so `install_smoke.sh:31` probe fails cold and `:33` self-install
    runs. Do NOT add this to `package-consumer-full` (`:678`, off-PR) — it MUST be in the lean
    `package-consumer` (`:541`) which runs on every PR (rides `ci_lanes_only_on_main`). Do NOT touch
    `name: CI`, the workflow filename, the job key line, or the `CI Summary` needs block (release-train
    coupling + OBS-02 content-drift invariants). Per LOCK-02.
  </action>
  <verify>
    <automated>grep -n 'mix archive.uninstall phx_new --force' .github/workflows/ci.yml | grep -v '^#'</automated>
  </verify>
  <done>
    The purge step exists in the `package-consumer` job and its line number is LESS than the
    `Run built-artifact image-only…` smoke step's line number (purge precedes smoke). The
    `package-consumer-full` job is unchanged. `name: CI`, the filename, and the `CI Summary` needs
    block are byte-unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner archive state → smoke | A warm/stale phx.new archive masking a deleted self-install guard |

This is a single CI `run:` step over a transient, idempotent runner artifact (a hex archive). No
untrusted input, no new secrets, no new attack surface, no `lib/` change. The security-relevant
threat is **false-confidence theater** — a smoke that passes only because the archive is already warm.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-111-04 | Spoofing (false-green) | package-consumer smoke (warm cache) | high | mitigate | Purge the phx.new archive BEFORE the smoke so the cold self-install path is taken every PR (Task 1) — converts warm-cache theater into an honest cold-path exercise |
| T-111-05 | Tampering | wrong job placement (off-PR full job) | medium | mitigate | Step is placed in the lean PR-gating `package-consumer` job (:541), explicitly NOT `package-consumer-full` (:678) |
| T-111-SC | Tampering | hex archive install/uninstall | low | accept | `phx_new` is an already-in-use first-party archive self-reinstalled by `install_smoke.sh`; no NEW package added → no legitimacy gate required (RESEARCH §Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `grep -n 'mix archive.uninstall phx_new --force' .github/workflows/ci.yml` returns a line inside the `package-consumer` job, before the `Run built-artifact image-only…` step.
- YAML well-formed: `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0 (or equivalent linter).
- LOCK-02 is behavioral (CI-exercised) — confirmed by reading ci.yml; a live PR run takes the self-install path. Recorded as the manual-only verification in 111-VALIDATION.
- OBS-02 content-drift guard: `grep -rn "Run built-artifact\|package-consumer" test/` confirms no existing meta-test asserts a literal this edit changed (RESEARCH Pitfall 1 verified none at risk; re-confirm in-phase since this is the same phase editing ci.yml).
</verification>

<success_criteria>
- The purge step exists in the lean `package-consumer` job, before the smoke step, with `|| true`.
- No change to `name: CI`, the workflow filename, the `CI Summary` needs block, `package-consumer-full`, or `setup_branch_protection.sh`. Zero `lib/` change.
</success_criteria>

<output>
Create `.planning/phases/111-regression-locks/111-02-SUMMARY.md` when done.
</output>

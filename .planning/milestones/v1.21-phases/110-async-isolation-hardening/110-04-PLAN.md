---
phase: 110-async-isolation-hardening
plan: 04
type: tdd
wave: 3
depends_on: ["110-01", "110-02"]
files_modified:
  - test/rindle/config/repo_override_isolation_test.exs
autonomous: true
requirements: [ISO-05]
must_haves:
  truths:
    - "[NON-INFERABLE — held-out concurrency proof] A concurrency regression test proves isolation: in process A, with_counting_repo(1, fn -> … end) force-fails A's 1st transaction and asserts Config.repo() == CountingFailingTxnRepo; a SEPARATE spawned process B (no override in its dict, NOT a $callers descendant of A) asserts Config.repo() == Rindle.Repo and that B's transaction/1 succeeds concurrently inside A's window. The test must FAIL against the old global-put_env impl and PASS against the process-scoped one — that delta is the proof."
    - "The isolation test module is async: true (the canonical async-safe demonstration)."
  artifacts:
    - "test/rindle/config/repo_override_isolation_test.exs — the ISO-05 concurrency proof"
  key_links:
    - "process A (override active) -> spawns B as an UNRELATED process (not via Task.async from A, so no $callers link) -> B reads Config.repo() == Rindle.Repo and runs a successful transaction while A's window is open"
  prohibitions:
    - "Process B must NOT be a $callers descendant of A (do NOT spawn B with Task.async from inside the with_counting_repo callback) — otherwise B would correctly inherit A's override and the proof would be inverted. B is started as an unrelated process whose result A awaits."
---

<objective>
Ship the ISO-05 concurrency proof (research §8 sketch): demonstrate that the now-process-scoped counting
double cannot pollute a concurrent reader in an unrelated process tree. This is the held-out, executable
proof of the whole phase — it FAILS against the old global-`put_env` implementation and PASSES against the
process-scoped resolver (Plan 01) + migrated double (Plan 02). That old→new delta is the deliverable.

Purpose: make the isolation property executable and un-droppable (research §6 — "the proof test plus the
guard rule together make the property executable and un-droppable").
Output: `test/rindle/config/repo_override_isolation_test.exs`, an `async: true` module.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/110-async-isolation-hardening/110-CONTEXT.md
@.planning/research/v1.21-ASYNC-ISOLATION.md
@.planning/phases/110-async-isolation-hardening/110-01-SUMMARY.md
@.planning/phases/110-async-isolation-hardening/110-02-SUMMARY.md
@test/support/counting_failing_txn_repo.ex
@test/support/data_case.ex
</context>

<artifacts_this_phase_produces>
This plan (110-04) produces `test/rindle/config/repo_override_isolation_test.exs` — the ISO-05 proof. It
consumes `Config.repo/0` + `Config.put_repo_override/1` (110-01) and the process-scoped
`with_counting_repo/2` (110-02). No new lib/ symbols.
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Write the ISO-05 concurrency isolation proof test</name>
  <files>test/rindle/config/repo_override_isolation_test.exs</files>
  <read_first>
    - .planning/research/v1.21-ASYNC-ISOLATION.md §8 "How to PROVE it" (lines 262-285) — the proof sketch: process A runs `with_counting_repo(1, …)`, asserts `Config.repo() == CountingFailingTxnRepo` and its 1st txn returns `{:error, :plan, _, _}`; the reader (process B) asserts `{Rindle.Repo, {:ok, :ok}}`; against old global put_env the reader sees the double and force-fails → red; against process-scoped → green.
    - .planning/phases/110-async-isolation-hardening/110-CONTEXT.md D-12 (lines 120-128) — B must NOT be a `$callers` descendant of A; module is `async: true`; planner picks the path (chosen: test/rindle/config/repo_override_isolation_test.exs).
    - test/support/counting_failing_txn_repo.ex — `with_counting_repo/2` (process-scoped after Plan 02) + `transaction/1` force-fail semantics (`{:error, :plan, reason, %{}}`) + the count starting at 1.
    - test/support/data_case.ex:22-25 — the `shared: not tags[:async]` Sandbox setup; under `async: true` each process gets an isolated non-shared owner. Process B will need an explicit Sandbox allowance to run a real transaction (use `Ecto.Adapters.SQL.Sandbox.allow/3` from A's owner to B, OR have B run a transaction that does not require DB access if the proof only needs `Config.repo().transaction(fn -> :ok end)` to succeed against the real repo — confirm which against the real repo's transaction contract during RED).
  </read_first>
  <behavior>
    RED → GREEN proof. The single test asserts, in one `with_counting_repo(1, fn -> … end)` window:
    - In process A (the callback's process): `Config.repo() == Rindle.Test.CountingFailingTxnRepo` and
      `Config.repo().transaction(fn -> :ok end)` returns `{:error, :plan, _, _}` (the force-failed 1st txn).
    - Process B is an UNRELATED process (started so it is NOT a `$callers` descendant of A — e.g. spawned
      from the test process BEFORE entering the `with_counting_repo` callback, or via a process whose
      `$callers` does not include A), gated so it reads `Config.repo()` WHILE A's window is open: B asserts
      `Config.repo() == Rindle.Repo` and that its own `Config.repo().transaction(fn -> :ok end)` succeeds
      (`{:ok, :ok}`).
    - A coordinates with B via message passing (send `:go` to B from inside the window; `Task.await`/receive
      B's `{repo, txn_result}` tuple inside the window) so the concurrency is real, not sequential.
    - The module header declares `async: true`.
    The proof property: this test PASSES on the Plan 01+02 implementation and FAILS on the pre-phase
    global-`put_env` implementation (where B would observe the double and its txn would force-fail).
  </behavior>
  <action>
    Create `test/rindle/config/repo_override_isolation_test.exs` as `use Rindle.DataCase, async: true`.
    Follow research §8 verbatim for the assertion shape, adapting process B so it is NOT a `$callers`
    descendant of A: start B as an independent reader (e.g. `reader = Task.async(fn -> receive do :go ->
    {Config.repo(), Config.repo().transaction(fn -> :ok end)} end end)` created in the TEST process, BEFORE
    the `with_counting_repo` call — a Task.async child carries the test process in `$callers`, not A's
    override, since the override is only set inside the callback's process at run time; confirm during RED
    that B resolves `Rindle.Repo`, not the double). Inside `with_counting_repo(1, fn -> … end)`: assert A's
    `Config.repo()` is the double and its 1st `transaction(fn -> :ok end)` returns `{:error, :plan, _, _}`;
    `send(reader.pid, :go)`; assert `{Rindle.Repo, {:ok, :ok}} = Task.await(reader)`. If B's real
    transaction needs DB access, grant it via `Ecto.Adapters.SQL.Sandbox.allow(Rindle.Repo, self(),
    reader.pid)` before sending `:go` (decide during RED whether a DB-touching transaction is needed or a
    no-op `fn -> :ok end` transaction against the real repo already proves the point — keep it minimal).
    During RED, verify the test FAILS if the resolver/double migration were absent (you can sanity-check by
    reasoning about the old impl; do not actually revert lib/); during GREEN it must pass on the current
    tree. Add a top-of-module comment naming this as the ISO-05 proof and the old→new delta it locks.
  </action>
  <verify>
    <automated>grep -q 'async: true' test/rindle/config/repo_override_isolation_test.exs && mix test test/rindle/config/repo_override_isolation_test.exs 2>&1 | tail -10</automated>
  </verify>
  <done>
    `test/rindle/config/repo_override_isolation_test.exs` exists, is `async: true`, and passes on the
    current (Plan 01+02) tree: process A sees the double and a force-failed 1st txn while unrelated process
    B concurrently reads `Config.repo() == Rindle.Repo` and runs a successful transaction. The test encodes
    the old→new delta (would fail against the pre-phase global-`put_env` impl).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Test-only proof; no boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-110-06 | Tampering | silent regression of the isolation property | low | mitigate | The ISO-05 proof is `async: true` and runs in the default suite; any future regression that re-globalizes the repo swap makes process B observe the double and the test goes RED. It is the executable lock on the property. |

Test-only change. No external input, no auth/data-flow surface, no schema/migration changes in scope. No high-severity threats.
</threat_model>

<verification>
- The test module is `async: true` and lives at `test/rindle/config/repo_override_isolation_test.exs`.
- Process B is NOT a `$callers` descendant of process A (no `Task.async` of B from inside the window).
- The test passes on the current tree and encodes the old→new delta (FAILS against the old global-put_env impl).
- `mix test test/rindle/config/repo_override_isolation_test.exs` is green.
</verification>

<success_criteria>
ISO-05 satisfied: a concurrency regression test proves the counting double in process A force-fails its
transaction while an unrelated spawned process B reads `Config.repo() == Rindle.Repo` and its transaction
succeeds; the test fails on the old impl and passes on the new.
</success_criteria>

<output>
Create `.planning/phases/110-async-isolation-hardening/110-04-SUMMARY.md` when done.
</output>

---
phase: 09-install-release-confidence
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
autonomous: true
requirements:
  - RELEASE-02
must_haves:
  truths:
    - PR CI validates installability from the built package artifact before merge.
    - Release checks reuse the same consumer smoke helper as CI so package-consumer proof cannot drift silently.
    - Release retains the deeper packaging posture around tarball inspection and dry-run publish without widening the PR smoke lane.
  artifacts:
    - path: .github/workflows/ci.yml
      provides: Narrow built-artifact consumer smoke job in PR CI
    - path: .github/workflows/release.yml
      provides: Shared-helper release verification plus existing tarball and dry-run gates
  key_links:
    - from: .github/workflows/ci.yml
      to: scripts/install_smoke.sh
      via: Shared PR consumer smoke invocation
      pattern: scripts/install_smoke\\.sh
    - from: .github/workflows/release.yml
      to: scripts/install_smoke.sh
      via: Shared release consumer smoke invocation
      pattern: scripts/install_smoke\\.sh
---

<objective>
Wire the shared consumer smoke helper into CI and release so RELEASE-02 is enforced by automation without forking the package-consumer setup logic.

Purpose: satisfy D-04, D-05, and D-06 by keeping PR smoke narrow and release deeper while both prove the same built-artifact install path.
Output: one slim CI smoke job plus release workflow reuse of the same helper alongside existing tarball inspection and `hex.publish --dry-run` posture.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-install-release-confidence/09-CONTEXT.md
@.planning/phases/09-install-release-confidence/09-RESEARCH.md
@.planning/phases/09-install-release-confidence/09-VALIDATION.md
@.planning/phases/05-ci-1-0-readiness/05-CONTEXT.md
@.planning/phases/05-ci-1-0-readiness/05-03-SUMMARY.md
@.github/workflows/ci.yml
@.github/workflows/release.yml
@scripts/install_smoke.sh

<interfaces>
From .github/workflows/release.yml:
```yaml
- name: Build package artifact
  run: mix hex.build --unpack

- name: Dry-run publish (optional — A1 risk tolerance)
  run: mix hex.publish package --dry-run --yes
```

From 05-03-SUMMARY.md:
```text
Pattern 2: System deps install BEFORE mix deps.get on CI runner.
Pattern 3: Replace duplicated suite execution with one must-pass command per lane.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the narrow package-consumer smoke lane to PR CI</name>
  <files>.github/workflows/ci.yml</files>
  <action>Modify `ci.yml` to add a dedicated package-consumer smoke job that depends on the existing prerequisite gates at the narrowest useful point, then runs the shared `scripts/install_smoke.sh` helper from Plan 09-01. Per D-04 and D-05, the job must build and unpack the package artifact, generate the consumer app, wire only the minimal runtime pieces, and prove the canonical presigned PUT lifecycle. Keep the lane package-consumer-first: it should fail on tarball contents, install drift, migration drift, or canonical flow drift, but must not expand into a second full integration suite or a multipart proof lane.</action>
  <verify>
    <automated>rg -n "scripts/install_smoke.sh|hex.build --unpack|package-consumer|install_smoke|phx.new" .github/workflows/ci.yml && bash scripts/install_smoke.sh</automated>
  </verify>
  <done>The PR workflow contains a dedicated built-artifact consumer smoke job that reuses the shared runner and materially enforces RELEASE-02 before merge.</done>
</task>

<task type="auto">
  <name>Task 2: Reuse the same smoke helper inside the release workflow without weakening existing gates</name>
  <files>.github/workflows/release.yml</files>
  <action>Update `release.yml` to call the shared `scripts/install_smoke.sh` helper in addition to its current tarball assertions and `mix hex.publish package --dry-run --yes` posture. Per D-06, do not replace the deeper release checks; compose them around the same helper so the package-consumer proof cannot silently diverge between PR and release workflows. Preserve the current release environment boundary and dry-run secret posture from Phase 5.</action>
  <verify>
    <automated>rg -n "scripts/install_smoke.sh|hex.publish package --dry-run|Assert required paths present in tarball|Assert prohibited paths absent from tarball" .github/workflows/release.yml && bash scripts/install_smoke.sh</automated>
  </verify>
  <done>Both workflows invoke the same consumer smoke entrypoint, while release still owns the deeper tarball and dry-run checks that do not belong in the PR lane.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions workflow -> shared smoke helper | workflow YAML hands execution to a shared repo script that must remain the single proof path |
| PR CI lane -> release lane | drift between pre-merge and release validation can create false confidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | R | .github/workflows/ci.yml, .github/workflows/release.yml | mitigate | Route both workflows through `scripts/install_smoke.sh` so the same proof path is observable and auditable in PR and release runs. |
| T-09-05 | D | .github/workflows/ci.yml | mitigate | Keep the PR lane narrow and deterministic around one built-artifact smoke command so failures point to install/setup drift rather than broad integration flake. |
| T-09-06 | E | .github/workflows/release.yml | accept | Preserve the existing protected `environment: release` and dry-run secret posture from Phase 5; this plan adds no broader secret scope. |
</threat_model>

<verification>
After each workflow edit, run the corresponding `rg` contract checks plus `bash scripts/install_smoke.sh`. Before closing the plan, verify both workflows reference the shared helper and only the release workflow retains tarball plus dry-run publish checks.
</verification>

<success_criteria>
- `RELEASE-02` is covered by a dedicated PR CI smoke lane that proves installability from the built artifact.
- CI and release no longer duplicate consumer-smoke logic inline; both call the same helper path.
- Release remains the only workflow carrying the deeper tarball and dry-run publish posture.
</success_criteria>

<output>
After completion, create `.planning/phases/09-install-release-confidence/09-02-SUMMARY.md`
</output>

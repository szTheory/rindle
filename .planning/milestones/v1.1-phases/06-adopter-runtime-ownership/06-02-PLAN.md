---
phase: 06-adopter-runtime-ownership
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - lib/rindle/upload/broker.ex
  - config/test.exs
  - test/test_helper.exs
  - test/support/data_case.ex
  - test/adopter/canonical_app/lifecycle_test.exs
  - test/rindle/upload/lifecycle_integration_test.exs
  - test/rindle/upload/broker_test.exs
autonomous: true
requirements:
  - ADOPT-02
  - ADOPT-03
  - ADOPT-04
must_haves:
  truths:
    - "Direct-upload runtime flows use the configured adopter Repo for session and asset persistence."
    - "The canonical adopter lifecycle succeeds end-to-end while public calls are configured to use the adopter Repo."
    - "The proxied upload runtime flow proves `Rindle.upload/3` succeeds under `config :rindle, :repo, Rindle.Adopter.CanonicalApp.Repo` instead of only proving a refactor by grep."
    - "Oban scope is explicit: Phase 6 proves adopter-owned runtime Repo boundaries and default-Oban compatibility, but does not add named-instance ownership support."
    - "Public broker examples no longer tell adopters they need `Rindle.Repo` running."
  artifacts:
    - path: "lib/rindle/upload/broker.ex"
      provides: "Repo-neutral direct-upload broker entrypoints and adopter-facing examples"
      exports: ["initiate_session/2", "sign_url/2", "verify_completion/2"]
    - path: "test/adopter/canonical_app/lifecycle_test.exs"
      provides: "Canonical adopter runtime proof against adopter Repo config"
    - path: "test/rindle/upload/lifecycle_integration_test.exs"
      provides: "Dedicated adopter-repo proxied-upload proof for `Rindle.upload/3`"
    - path: "test/rindle/upload/broker_test.exs"
      provides: "Broker regression coverage for runtime Repo resolution"
    - path: "test/support/data_case.ex"
      provides: "Per-test sandbox ownership selection so adopter proof can deny `Rindle.Repo` fallback"
  key_links:
    - from: "lib/rindle/upload/broker.ex"
      to: "lib/rindle/config.ex"
      via: "runtime repo lookup for session/asset persistence"
      pattern: "Rindle\\.Config\\.repo\\("
    - from: "test/adopter/canonical_app/lifecycle_test.exs"
      to: "config :rindle, :repo"
      via: "test-scoped env override for adopter repo proof"
      pattern: "Application\\.put_env\\(:rindle, :repo"
    - from: "test/adopter/canonical_app/lifecycle_test.exs"
      to: "default Oban semantics"
      via: "explicit scope note and assertions aligned to current enqueue path"
      pattern: "Oban"
    - from: "test/rindle/upload/lifecycle_integration_test.exs"
      to: "config :rindle, :repo"
      via: "test-scoped adopter Repo override around `Rindle.upload/3` proof"
      pattern: "Application\\.put_env\\(:rindle, :repo, Rindle\\.Adopter\\.CanonicalApp\\.Repo"
    - from: "test/support/data_case.ex"
      to: "test/adopter/canonical_app/lifecycle_test.exs"
      via: "adopter-only sandbox owner so a hidden `Rindle.Repo` fallback raises instead of silently passing"
      pattern: "sandbox_repo"
---

<objective>
Convert the direct-upload broker, the canonical adopter lane, and the proxied `Rindle.upload/3` path from documented aspirations into enforced runtime contracts.

Purpose: ADOPT-03 is the outside-in proof that the repo seam from Plan 06-01 actually covers both direct-upload broker flows and proxied upload facade flows, not just the refactor itself.
Output: A repo-neutral broker, broker regression tests, a canonical adopter lifecycle that runs against the adopter Repo configuration, and a dedicated adopter-repo proxied-upload proof for `Rindle.upload/3`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-ci-1-0-readiness/05-CONTEXT.md
@.planning/phases/06-adopter-runtime-ownership/06-RESEARCH.md
@.planning/phases/06-adopter-runtime-ownership/06-PATTERNS.md
@.planning/phases/06-adopter-runtime-ownership/06-01-PLAN.md
@lib/rindle/upload/broker.ex
@config/test.exs
@test/test_helper.exs
@test/support/data_case.ex
@test/adopter/canonical_app/lifecycle_test.exs
@test/rindle/upload/lifecycle_integration_test.exs
@test/rindle/upload/broker_test.exs

<interfaces>
From lib/rindle/upload/broker.ex:
```elixir
def initiate_session(profile_module, opts \\ [])
def sign_url(session_id, opts \\ [])
def verify_completion(session_id, opts \\ [])
```

From test/adopter/canonical_app/lifecycle_test.exs:
```elixir
use Oban.Testing, repo: Rindle.Repo
@moduletag :adopter
```

From test/support/data_case.ex:
```elixir
def setup_sandbox(tags) do
  pid = Sandbox.start_owner!(Rindle.Repo, shared: not tags[:async])
  on_exit(fn -> Sandbox.stop_owner(pid) end)
end
```

From test/rindle/upload/lifecycle_integration_test.exs:
```elixir
use Rindle.DataCase, async: false
use Oban.Testing, repo: Rindle.Repo

test "proxied upload promotes the asset and generates a ready variant" do
  {:ok, asset} = Rindle.upload(LocalProfile, %{...})
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move broker persistence paths onto the runtime Repo seam</name>
  <files>lib/rindle/upload/broker.ex, test/rindle/upload/broker_test.exs</files>
  <behavior>
    - Test 1: `initiate_session/2` still creates a staged asset and initialized session, but via the resolved Repo module.
    - Test 2: `sign_url/2` still transitions to `signed` without falling back to `Rindle.Repo`.
    - Test 3: `verify_completion/2` still verifies storage, persists state changes, enqueues promotion, and emits telemetry after commit through the configured Repo.
  </behavior>
  <action>Refactor `lib/rindle/upload/broker.ex` to use `Rindle.Config.repo/0` instead of the `alias Rindle.Repo` pattern documented in the leak map. Preserve return shapes, telemetry behavior, and public function signatures. Use the transaction repo consistently across reads, preloads, updates, and the `Ecto.Multi` execution path so ADOPT-02 covers direct-upload flows as well as the facade. In the same file, update every adopter-facing `@doc` example so it no longer says “Requires `Rindle.Repo`”; the examples must describe a configured adopter Repo and remain honest about default-`Oban` scope.</action>
  <verify>
    <automated>mix test test/rindle/upload/broker_test.exs && ! rg -n "Requires Rindle\\.Repo|alias Rindle\\.Repo" lib/rindle/upload/broker.ex</automated>
  </verify>
  <done>Broker entrypoints no longer hard-code `Rindle.Repo`, the public broker examples stop teaching `Rindle.Repo` as a consumer requirement, and the existing broker regression suite still passes with the same external behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add adopter-repo proofs for both the canonical lane and proxied `Rindle.upload/3`</name>
  <files>config/test.exs, test/test_helper.exs, test/support/data_case.ex, test/adopter/canonical_app/lifecycle_test.exs, test/rindle/upload/lifecycle_integration_test.exs</files>
  <behavior>
    - Test 1: the canonical adopter test sets `config :rindle, :repo, Rindle.Adopter.CanonicalApp.Repo` for the test scope and exercises broker plus public facade APIs end-to-end.
    - Test 2: only the adopter Repo receives sandbox ownership for the canonical lane, so a hidden fallback to `Rindle.Repo` would fail instead of passing through the shared physical database.
    - Test 3: the canonical adopter lane explicitly covers direct-upload verification plus `Rindle.attach/4` and `Rindle.detach/3`, with lifecycle reads, attachment checks, and `assert_enqueued` expectations expressed against the adopter Repo contract rather than direct `Rindle.Repo` calls.
    - Test 4: a dedicated proxied-upload proof wraps `Rindle.upload/3` in `Application.put_env(:rindle, :repo, Rindle.Adopter.CanonicalApp.Repo)`, uses the adopter-only sandbox owner, and asserts all follow-up reads and job expectations through `Rindle.Adopter.CanonicalApp.Repo`.
    - Test 5: Oban expectations remain honest: default `Oban` compatibility is proven, while named-instance ownership is explicitly not added in this phase.
  </behavior>
  <action>Update the outside-in proofs so success criterion 2 is complete. Extend `test/support/data_case.ex` with a per-test repo selector such as `@moduletag sandbox_repo: Rindle.Adopter.CanonicalApp.Repo`, leaving `Rindle.Repo` as the suite default for unrelated tests. In `test/adopter/canonical_app/lifecycle_test.exs`, opt into that adopter-only sandbox owner, replace `use Oban.Testing, repo: Rindle.Repo` with the adopter Repo, set `Application.put_env(:rindle, :repo, Rindle.Adopter.CanonicalApp.Repo)` in setup with `on_exit` restoration, and route every post-action read (`get!`, `all`, attachment checks) through `Rindle.Adopter.CanonicalApp.Repo` while keeping the direct-upload verification plus `Rindle.attach/4` and `Rindle.detach/3` path intact. In `test/rindle/upload/lifecycle_integration_test.exs`, add or split out a dedicated adopter-only proxied-upload proof derived from the existing `Rindle.upload/3` case: apply the same repo override and sandbox selector, use `use Oban.Testing, repo: Rindle.Adopter.CanonicalApp.Repo`, call `Rindle.upload/3`, and assert promotion/variant reads through `Rindle.Adopter.CanonicalApp.Repo` so the test fails if proxied upload falls back to `Rindle.Repo`. Adjust `config/test.exs` and `test/test_helper.exs` only as needed so the default `Oban` test path remains compatible with the adopter Repo in these lanes; do not introduce named-instance support or a new `:oban_name` seam.</action>
  <verify>
    <automated>mix test test/adopter/canonical_app/lifecycle_test.exs test/rindle/upload/lifecycle_integration_test.exs && rg -n "Application\\.put_env\\(:rindle, :repo, Rindle\\.Adopter\\.CanonicalApp\\.Repo\\)|use Oban\\.Testing, repo: Rindle\\.Adopter\\.CanonicalApp\\.Repo|Rindle\\.upload\\(" test/adopter/canonical_app/lifecycle_test.exs test/rindle/upload/lifecycle_integration_test.exs && ! rg -n "TODO\\(adopter-repo\\)" test/adopter/canonical_app/lifecycle_test.exs && rg -n "sandbox_repo" test/support/data_case.ex</automated>
  </verify>
  <done>The canonical adopter lifecycle passes while `:rindle, :repo` points at the adopter Repo, only the adopter Repo has sandbox ownership in that lane, direct-upload verification plus `Rindle.attach/4` and `Rindle.detach/3` are asserted through the adopter Repo contract, the dedicated proxied-upload proof shows `Rindle.upload/3` succeeding under the same adopter Repo override, and the old TODO-era loophole text is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| broker → storage adapter | untrusted storage-completion state is verified before DB promotion |
| broker/adopter flow → Repo + Oban | public runtime flow persists records and enqueues jobs across transaction boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | T | `lib/rindle/upload/broker.ex` | mitigate | use the same resolved Repo for all broker reads and writes so direct-upload state cannot split across repos |
| T-06-02-02 | R | canonical adopter proof | mitigate | remove the TODO-era loophole and make the adopter test set `:rindle, :repo` explicitly so the proof is executable and auditable |
| T-06-02-03 | D | Oban ownership expectations | mitigate | keep scope explicit: default `Oban` compatibility is covered in both the canonical and proxied adopter proofs, while named-instance resolution is deferred rather than implied |
</threat_model>

<verification>
Run broker regressions first, then the canonical adopter proof:

- `mix test test/rindle/upload/broker_test.exs`
- `mix test test/adopter/canonical_app/lifecycle_test.exs`
- `mix test test/rindle/upload/lifecycle_integration_test.exs`

After tests pass, grep `lib/rindle/upload/broker.ex` and `test/adopter/canonical_app/lifecycle_test.exs` to confirm no stale TODO wording or `alias Rindle.Repo` remains on broker runtime paths.
After tests pass, also confirm `test/support/data_case.ex` exposes the per-test sandbox repo selector, that the canonical adopter lane runs with the adopter Repo override for direct-upload verification plus `attach/4` and `detach/3`, and that `test/rindle/upload/lifecycle_integration_test.exs` contains the dedicated adopter-repo `Rindle.upload/3` proof rather than leaving proxied upload as a refactor-only check.
</verification>

<success_criteria>
- Broker entrypoints use the runtime Repo seam introduced in Plan 06-01.
- The canonical adopter lifecycle is the actual fail-on-wrong-repo proof of adopter-owned direct-upload verification plus `attach/4` and `detach/3`.
- A dedicated adopter-repo integration proof shows `Rindle.upload/3` succeeds under `config :rindle, :repo, Rindle.Adopter.CanonicalApp.Repo`.
- Oban ownership is documented and tested as default-instance compatibility only; named-instance support is not implied.
- Public broker examples no longer claim adopters must run `Rindle.Repo`.
</success_criteria>

<output>
After completion, create `.planning/phases/06-adopter-runtime-ownership/06-adopter-runtime-ownership-02-SUMMARY.md`
</output>

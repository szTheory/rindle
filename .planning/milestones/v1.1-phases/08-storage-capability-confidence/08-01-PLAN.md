---
phase: 08-storage-capability-confidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/storage.ex
  - lib/rindle/storage/capabilities.ex
  - lib/rindle/delivery.ex
  - lib/rindle/upload/broker.ex
  - test/rindle/storage/storage_adapter_test.exs
  - test/rindle/delivery_test.exs
  - test/rindle/upload/broker_test.exs
autonomous: true
requirements:
  - CAP-01
  - CAP-04
must_haves:
  truths:
    - "Upload and delivery capability checks resolve through one shared seam instead of separate ad hoc helpers."
    - "Current tagged capability failures stay stable for adopters while future resumable-style capability names are reserved additively."
    - "Adapter capability lists are validated against one documented vocabulary so docs and tests can drift less easily."
  artifacts:
    - path: "lib/rindle/storage/capabilities.ex"
      provides: "Central capability vocabulary, normalization, and tagged error helpers."
      exports: ["known/0", "safe/1", "supports?/2", "require_upload/2", "require_delivery/2"]
    - path: "lib/rindle/storage.ex"
      provides: "Storage capability type contract and forward-compatible typedocs."
    - path: "test/rindle/storage/storage_adapter_test.exs"
      provides: "Capability vocabulary and adapter-truth regression coverage."
  key_links:
    - from: "lib/rindle/delivery.ex"
      to: "lib/rindle/storage/capabilities.ex"
      via: "private-delivery capability gate"
      pattern: "Rindle\\.Storage\\.Capabilities\\.(require_delivery|supports\\?)"
    - from: "lib/rindle/upload/broker.ex"
      to: "lib/rindle/storage/capabilities.ex"
      via: "upload capability gate"
      pattern: "Rindle\\.Storage\\.Capabilities\\.(require_upload|supports\\?)"
    - from: "test/rindle/storage/storage_adapter_test.exs"
      to: "lib/rindle/storage/capabilities.ex"
      via: "known capability subset assertions"
      pattern: "Capabilities\\.known"
---

<objective>
Centralize capability truth without changing current adopter-facing error contracts.

Purpose: CAP-01 and CAP-04 only hold if capability names, normalization, and tagged failures stop drifting between delivery, broker, tests, and docs while staying additive for future resumable adapters.
Output: A shared capability helper seam, typed capability vocabulary, refactored delivery/upload gates, and regression coverage locking current plus future-safe semantics.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-storage-capability-confidence/08-RESEARCH.md
@.planning/phases/08-storage-capability-confidence/08-VALIDATION.md
@.planning/phases/08-storage-capability-confidence/08-PATTERNS.md
@.planning/phases/06-adopter-runtime-ownership/06-adopter-runtime-ownership-02-SUMMARY.md
@.planning/phases/07-multipart-uploads/07-01-SUMMARY.md
@lib/rindle/storage.ex
@lib/rindle/delivery.ex
@lib/rindle/upload/broker.ex
@test/rindle/storage/storage_adapter_test.exs
@test/rindle/delivery_test.exs
@test/rindle/upload/broker_test.exs
<interfaces>
From lib/rindle/storage.ex:
```elixir
@callback presigned_put(key :: String.t(), expires_in :: pos_integer(), opts :: keyword()) ::
            {:ok, map()} | {:error, term()}
@callback initiate_multipart_upload(key :: String.t(), part_size :: pos_integer(), opts :: keyword()) ::
            {:ok, map()} | {:error, term()}
@callback head(key :: String.t(), opts :: keyword()) ::
            {:ok, map()} | {:error, term()}
@callback capabilities() :: [atom()]
```

From lib/rindle/delivery.ex:
```elixir
@spec url(module(), String.t(), keyword()) :: {:ok, String.t()} | {:error, term()}
def url(profile, key, opts \\ [])
```

From lib/rindle/upload/broker.ex:
```elixir
def initiate_session(profile_module, opts \\ [])
def initiate_multipart_session(profile_module, opts \\ [])
def sign_url(session_id, opts \\ [])
def sign_multipart_part(session_id, part_number, opts \\ [])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the shared capability vocabulary and future-safe typed contract</name>
  <files>lib/rindle/storage.ex, lib/rindle/storage/capabilities.ex, test/rindle/storage/storage_adapter_test.exs</files>
  <behavior>
    - Test 1: `Rindle.Storage.Capabilities.known/0` returns the shipped atoms `:presigned_put`, `:multipart_upload`, and `:signed_url`, plus reserved future resumable atoms without forcing current adapters to advertise them.
    - Test 2: `safe/1` returns `[]` for malformed capability implementations instead of raising.
    - Test 3: adapter capability lists must be a subset of `known/0`, and Local/S3 keep their current advertised atoms unchanged.
  </behavior>
  <action>Add `lib/rindle/storage/capabilities.ex` as the shared seam per D-01 with a public `@type capability` vocabulary, `known/0`, `safe/1`, `supports?/2`, and tagged helper functions that preserve current tuple shapes: `{:error, {:upload_unsupported, capability}}` and `{:error, {:delivery_unsupported, capability}}`. Update `lib/rindle/storage.ex` to reference the shared capability type in typedocs and callback docs, and reserve additive resumable-oriented atoms per D-05 without adding new storage callbacks or a GCS adapter. Extend `test/rindle/storage/storage_adapter_test.exs` to assert Local/S3 capabilities remain truthful, every advertised atom is known, and malformed capability sources normalize safely rather than leaking runtime exceptions.</action>
  <verify>
    <automated>mix test test/rindle/storage/storage_adapter_test.exs</automated>
  </verify>
  <done>The repo has one capability vocabulary source, current adapters keep their existing advertised atoms, and future resumable capability names are reserved additively without changing shipped adopter contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route delivery and broker capability gates through the shared seam</name>
  <files>lib/rindle/delivery.ex, lib/rindle/upload/broker.ex, test/rindle/delivery_test.exs, test/rindle/upload/broker_test.exs</files>
  <behavior>
    - Test 1: private delivery still returns `{:error, {:delivery_unsupported, :signed_url}}` when the adapter lacks signed delivery support.
    - Test 2: multipart entrypoints still return `{:error, {:upload_unsupported, :multipart_upload}}` before adapter-specific work on unsupported adapters.
    - Test 3: a reserved future capability such as resumable-session upload can be checked through the shared helper and yields the same tagged unsupported upload error shape.
  </behavior>
  <action>Refactor `lib/rindle/delivery.ex` and `lib/rindle/upload/broker.ex` so both stop open-coding capability normalization and instead call `Rindle.Storage.Capabilities` per D-01 and D-02. Preserve the public function signatures and current tagged failure tuples exactly, keep post-upload verification generic via `adapter.head/2`, and do not branch on provider names in callers. Extend `test/rindle/delivery_test.exs` and `test/rindle/upload/broker_test.exs` to lock the shared helper usage, keep current signed-delivery and multipart unsupported behavior green, and prove a future reserved upload capability can fail explicitly without any API-contract rewrite per D-05.</action>
  <verify>
    <automated>mix test test/rindle/delivery_test.exs test/rindle/upload/broker_test.exs</automated>
  </verify>
  <done>Delivery and upload capability gates share one implementation seam, current adopter-facing tagged errors are unchanged, and reserved future capability names already fit the same negotiation contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public delivery/upload APIs → capability gate | untrusted requests ask the library to expose a backend-specific flow |
| capability helper → adapter declarations | adapter-provided capability lists may be malformed, incomplete, or over-claim support |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | T | `lib/rindle/storage/capabilities.ex` | mitigate | normalize malformed `capabilities/0` results to `[]` and centralize tagged errors so callers cannot accidentally bypass unsupported-flow handling |
| T-08-02 | E | `lib/rindle/upload/broker.ex` | mitigate | require upload capabilities through the shared seam before adapter-specific work, preserving explicit `{:upload_unsupported, capability}` failures |
| T-08-03 | R | `lib/rindle/delivery.ex` | mitigate | keep private-delivery gating explicit and test-locked so adapters without `:signed_url` cannot silently degrade to unsigned URLs |
</threat_model>

<verification>
Run `mix test test/rindle/storage/storage_adapter_test.exs test/rindle/delivery_test.exs test/rindle/upload/broker_test.exs`.

Before closing the plan, grep for stale open-coded capability guards:
`! rg -n "safe_capabilities|ensure_capability\\(|ensure_signed_delivery_support" lib/rindle/delivery.ex lib/rindle/upload/broker.ex`
</verification>

<success_criteria>
- Capability atoms and tagged unsupported helpers live in one shared module.
- Delivery and broker callers stop duplicating capability normalization logic.
- Current adopter-facing tagged errors stay stable while future resumable vocabulary is reserved additively.
</success_criteria>

<output>
After completion, create `.planning/phases/08-storage-capability-confidence/08-01-SUMMARY.md`
</output>

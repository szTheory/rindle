---
phase: 08-storage-capability-confidence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/rindle/storage/s3_test.exs
  - test/rindle/upload/lifecycle_integration_test.exs
  - test/adopter/canonical_app/lifecycle_test.exs
autonomous: true
requirements:
  - CAP-02
must_haves:
  truths:
    - "The same MinIO-backed S3 adapter proves both presigned PUT and multipart upload capabilities against real storage."
    - "The canonical adopter lane can rely on those upload capabilities end to end without swapping harnesses or bypassing the broker."
    - "Capability honesty is proven with real MinIO behavior, not only with mocks."
  artifacts:
    - path: "test/rindle/storage/s3_test.exs"
      provides: "Low-level MinIO proof that the S3 adapter advertises and satisfies both shipped upload capabilities."
    - path: "test/rindle/upload/lifecycle_integration_test.exs"
      provides: "Broker-level MinIO lifecycle proof that both capability-gated upload paths still re-enter verification and promotion correctly."
    - path: "test/adopter/canonical_app/lifecycle_test.exs"
      provides: "Canonical adopter MinIO proof for both direct-upload flows under the same capability contract."
  key_links:
    - from: "test/rindle/storage/s3_test.exs"
      to: "lib/rindle/storage/s3.ex"
      via: "real MinIO-backed presigned PUT and multipart round-trips"
      pattern: "(presigned_put|multipart)"
    - from: "test/rindle/upload/lifecycle_integration_test.exs"
      to: "lib/rindle/upload/broker.ex"
      via: "broker lifecycle integration over MinIO for both upload lanes"
      pattern: "(sign_url|complete_multipart_upload|verify_upload)"
    - from: "test/adopter/canonical_app/lifecycle_test.exs"
      to: "lib/rindle/upload/broker.ex"
      via: "broker-owned direct-upload flows over MinIO"
      pattern: "(sign_url|initiate_multipart_upload|complete_multipart_upload)"
---

<objective>
Prove the current upload capability contract against the existing MinIO-backed harnesses.

Purpose: CAP-02 is only credible if the same shipped S3-compatible adapter and canonical adopter flow exercise both presigned PUT and multipart end to end under real storage semantics.
Output: Capability-focused MinIO regression coverage in the adapter, broker integration, and adopter suites, with both direct-upload lanes proven through the existing harnesses.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-storage-capability-confidence/08-RESEARCH.md
@.planning/phases/08-storage-capability-confidence/08-VALIDATION.md
@.planning/phases/08-storage-capability-confidence/08-PATTERNS.md
@.planning/phases/07-multipart-uploads/07-VERIFICATION.md
@.planning/phases/07-multipart-uploads/07-01-SUMMARY.md
@test/rindle/storage/s3_test.exs
@test/rindle/upload/lifecycle_integration_test.exs
@test/adopter/canonical_app/lifecycle_test.exs
<interfaces>
Expected shipped upload APIs:
```elixir
{:ok, %{session: session, presigned: %{url: url}}} =
  Rindle.Upload.Broker.sign_url(session_id)

{:ok, %{session: session, multipart: %{upload_id: upload_id}}} =
  Rindle.initiate_multipart_upload(Profile, filename: "large.bin")

{:ok, %{session: completed, asset: asset}} =
  Rindle.complete_multipart_upload(session.id, [%{part_number: 1, etag: etag}])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the MinIO S3 adapter proof so both upload capabilities are exercised under one contract</name>
  <files>test/rindle/storage/s3_test.exs</files>
  <behavior>
    - Test 1: the MinIO-backed S3 adapter can issue a presigned PUT URL, accept a real HTTP PUT, and `head/2` the object afterward.
    - Test 2: the same MinIO-backed adapter can initiate multipart, sign parts, complete with captured ETags, and `head/2` the final object.
    - Test 3: the test asserts the adapter capability list includes both `:presigned_put` and `:multipart_upload` before exercising those flows.
  </behavior>
  <action>Extend `test/rindle/storage/s3_test.exs` per D-03 so the MinIO suite proves the current upload-capability contract as a single story, not as disconnected happy paths. Keep the existing env-driven MinIO setup, exercise the real presigned PUT lane via `:httpc` rather than `store/3`, keep the production-valid multipart part sizing from Phase 7, and assert the adapter capability list before running the remote calls. Do not add a new provider harness or mock around MinIO behavior.</action>
  <verify>
    <automated>mix test test/rindle/storage/s3_test.exs --include minio</automated>
  </verify>
  <done>The MinIO-backed adapter suite proves that the same S3 adapter advertises and satisfies both shipped upload capabilities end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-run the broker and adopter lifecycle proofs through the same capability contract</name>
  <files>test/rindle/upload/lifecycle_integration_test.exs, test/adopter/canonical_app/lifecycle_test.exs</files>
  <behavior>
    - Test 1: the broker-level lifecycle integration lane asserts the MinIO-backed adapter advertises both shipped upload capabilities, then proves presigned PUT still flows through verify/promotion without bypassing the broker.
    - Test 2: the same broker-level lane asserts the capability contract before multipart completion and still proves verify/promotion on the completed object.
    - Test 3: the canonical adopter lane asserts the same capability contract and succeeds through both direct-upload paths into promotion, ready variants, signed delivery, attach, and detach under the adopter-owned runtime repo.
  </behavior>
  <action>Refine `test/rindle/upload/lifecycle_integration_test.exs` and `test/adopter/canonical_app/lifecycle_test.exs` per D-03 so CAP-02 is proven at both the broker lifecycle seam and the canonical adopter seam. Add capability assertions near the start of each MinIO-backed presigned PUT and multipart scenario, keep the real HTTP uploads and multipart ETag handling, and preserve the existing broker-owned path into verify/promotion plus the adopter-owned path into variants, signed delivery, attach, and detach. Do not introduce live R2 assumptions, a second storage harness, or alternate helper shortcuts that bypass the public lifecycle path.</action>
  <verify>
    <automated>mix test test/rindle/upload/lifecycle_integration_test.exs --include integration && mix test test/adopter/canonical_app/lifecycle_test.exs --include minio</automated>
  </verify>
  <done>The broker lifecycle and canonical adopter MinIO lanes both prove the shipped direct-upload flows under one capability contract and preserve the runtime/adopter boundaries established in earlier phases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test client → MinIO presigned URLs | real remote uploads and part ETags must be captured from the backend, not fabricated |
| adopter flow → broker/runtime repo | end-to-end proofs must stay on the shipped broker and adopter repo seams |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | T | `test/rindle/storage/s3_test.exs` | mitigate | use real `:httpc` uploads and actual MinIO ETags so the proof matches production request/response behavior |
| T-08-05 | R | `test/adopter/canonical_app/lifecycle_test.exs` | mitigate | keep assertions on promotion, variant processing, signed delivery, and attach/detach so the adopter proof cannot stop at raw upload success |
| T-08-06 | D | MinIO-backed upload proof | mitigate | extend the existing harness rather than adding mock-only or parallel test paths, ensuring regressions hit the same storage semantics adopters rely on |
</threat_model>

<verification>
With MinIO available, run:
- `mix test test/rindle/storage/s3_test.exs --include minio`
- `mix test test/rindle/upload/lifecycle_integration_test.exs --include integration`
- `mix test test/adopter/canonical_app/lifecycle_test.exs --include minio`

Treat CAP-02 as incomplete if any of the adapter, broker lifecycle, or adopter suites passes only one flow or reverts to adapter helper shortcuts instead of real presigned HTTP uploads.
</verification>

<success_criteria>
- The MinIO-backed S3 suite proves presigned PUT and multipart under one adapter contract.
- The broker lifecycle and canonical adopter suites both prove the same direct-upload lanes under the MinIO-backed capability story.
- No new harness or provider-specific branch is introduced for this proof.
</success_criteria>

<output>
After completion, create `.planning/phases/08-storage-capability-confidence/08-02-SUMMARY.md`
</output>

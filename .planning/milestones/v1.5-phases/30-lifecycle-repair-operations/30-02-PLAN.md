---
phase: 30-lifecycle-repair-operations
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - lib/rindle.ex
  - lib/rindle/error.ex
  - lib/rindle/ops/lifecycle_repair.ex
  - lib/rindle/workers/process_variant.ex
  - test/rindle/api_surface_boundary_test.exs
  - test/rindle/error_test.exs
  - test/rindle/ops/lifecycle_repair_test.exs
  - test/rindle/workers/process_variant_test.exs
autonomous: true
requirements:
  - REPAIR-02
  - REPAIR-02-01
must_haves:
  truths:
    - "Targeted public repair stays asset-scoped and enqueue-only."
    - "Only `failed` and `cancelled` variants are eligible for targeted repair; ready, queued, processing, stale, and missing siblings are not implicitly pulled in."
    - "Unknown variant names are loud errors, while empty/no-op selections still return deterministic report data."
    - "The public contract mismatch that mentions nonexistent `Rindle.regenerate_variant/2` is resolved in code, docs, and tests while broad regeneration stays on `mix rindle.regenerate_variants`."
  artifacts:
    - path: "lib/rindle.ex"
      provides: "Public asset-scoped targeted repair entrypoint."
      contains: "def requeue_variants"
    - path: "lib/rindle/ops/lifecycle_repair.ex"
      provides: "Hidden targeted-repair orchestration with report semantics and variant-name validation."
      contains: "def requeue_failed_variants"
    - path: "test/rindle/ops/lifecycle_repair_test.exs"
      provides: "Coverage for allowed states, unknown variant names, idempotent requeue, and ready-sibling preservation."
      contains: "describe \"requeue_failed_variants/2\""
  key_links:
    - from: "lib/rindle/ops/lifecycle_repair.ex"
      to: "lib/rindle/workers/process_variant.ex"
      via: "targeted repair reuses existing ProcessVariant job builders and uniqueness semantics"
      pattern: "job_args_for_variant|new"
    - from: "lib/rindle/error.ex"
      to: "lib/rindle.ex"
      via: "public operator guidance names real repair entrypoints only"
      pattern: "regenerate_variant|requeue"
---

<objective>
Lock the explicit targeted repair lane before broad maintenance parity and docs
work land.

Purpose: give operators one supported asset-scoped way to requeue failed or
cancelled variants without widening the public facade into a generic filter DSL
or disturbing ready siblings.
Output: a public targeted-repair API, hidden enqueue-only orchestration,
deterministic repair reports, and corrected user-facing messaging about how to
recover from cancellation/failure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-lifecycle-repair-operations/30-CONTEXT.md
@.planning/phases/30-lifecycle-repair-operations/30-RESEARCH.md
@.planning/phases/30-lifecycle-repair-operations/30-PATTERNS.md
@lib/rindle.ex
@lib/rindle/error.ex
@lib/rindle/ops/variant_maintenance.ex
@lib/rindle/workers/process_variant.ex
@test/rindle/error_test.exs
@test/rindle/workers/process_variant_test.exs
@test/rindle/ops/variant_maintenance_test.exs

<interfaces>
From .planning/ROADMAP.md success criteria:
```text
Operator can requeue failed or cancelled variants for a specific asset
through an idempotent public repair surface.
```

From lib/rindle/error.ex / tests:
```text
re-trigger with Rindle.regenerate_variant/2 if needed
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement asset-scoped failed/cancelled requeue as enqueue-only hidden orchestration</name>
  <files>lib/rindle/ops/lifecycle_repair.ex, lib/rindle/workers/process_variant.ex, test/rindle/ops/lifecycle_repair_test.exs, test/rindle/workers/process_variant_test.exs</files>
  <behavior>
    - Only `failed` and `cancelled` variants are eligible.
    - Unknown variant names fail loudly instead of silently widening to all variants.
    - Oban uniqueness prevents duplicate work while preserving deterministic skipped/error counters.
    - Ready siblings remain untouched.
  </behavior>
  <action>Extend the hidden repair service with one asset-scoped targeted-requeue function. Accept an asset id plus optional explicit variant-name selection, validate the names against variants belonging to that asset, reject unknown names loudly, and only enqueue eligible `failed`/`cancelled` rows. Reuse existing `ProcessVariant` job-building and uniqueness behavior rather than inventing a second worker contract. Return a report with deterministic counters for selected, enqueued, skipped, and errors, plus typed failure entries where a row could not be requeued.</action>
  <verify>
    <automated>mix test test/rindle/ops/lifecycle_repair_test.exs test/rindle/workers/process_variant_test.exs</automated>
  </verify>
  <done>The repo has one hidden targeted-repair service that requeues only failed/cancelled variants, validates explicit variant names, and preserves ready siblings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose the public targeted-repair API and remove stale recovery messaging</name>
  <files>lib/rindle.ex, lib/rindle/error.ex, test/rindle/api_surface_boundary_test.exs, test/rindle/error_test.exs, test/rindle/ops/lifecycle_repair_test.exs</files>
  <behavior>
    - The public API remains asset-scoped on `Rindle`.
    - The recovery guidance names real supported entrypoints instead of a nonexistent `Rindle.regenerate_variant/2`.
    - The public return contract is consistent with the report-based repair posture.
  </behavior>
  <action>Add one public targeted-repair facade entrypoint on `Rindle` with explicit naming that matches the locked context and operator teaching surface; `requeue_variants/2` is the default recommendation unless a narrower name is required by the surrounding code. Update `Rindle.Error` and tests so cancellation/failure guidance points at the real asset-scoped repair API or the existing broad `mix rindle.regenerate_variants` lane as appropriate. Extend the boundary tests so the new public function is documented, while hidden ops modules remain hidden.</action>
  <verify>
    <automated>mix test test/rindle/api_surface_boundary_test.exs test/rindle/error_test.exs test/rindle/ops/lifecycle_repair_test.exs</automated>
  </verify>
  <done>Operators have a real public targeted-repair API on `Rindle`, stale recovery messaging is removed, and the public/internal boundary remains explicit.</done>
</task>

</tasks>

<verification>
Run `mix test test/rindle/api_surface_boundary_test.exs test/rindle/error_test.exs test/rindle/ops/lifecycle_repair_test.exs test/rindle/workers/process_variant_test.exs`.
</verification>

<success_criteria>
REPAIR-02 and the public targeted portion of REPAIR-03 are satisfied when a
maintainer can requeue failed or cancelled variants for one asset through a
real asset-scoped `Rindle` API, idempotent enqueueing preserves ready siblings,
and user-facing recovery guidance no longer references nonexistent functions.
</success_criteria>

<output>
After completion, create `.planning/phases/30-lifecycle-repair-operations/30-02-SUMMARY.md`
</output>

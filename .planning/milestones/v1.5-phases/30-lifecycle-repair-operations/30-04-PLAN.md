---
phase: 30-lifecycle-repair-operations
plan: 04
type: execute
wave: 3
depends_on:
  - 30-01
  - 30-02
  - 30-03
files_modified:
  - lib/rindle/ops/lifecycle_repair.ex
  - lib/mix/tasks/rindle.sweep_orphaned_temp_files.ex
  - guides/operations.md
  - guides/troubleshooting.md
  - test/rindle/ops/lifecycle_repair_test.exs
  - test/rindle/ops/sweep_orphaned_temp_files_test.exs
  - test/rindle/error_test.exs
autonomous: true
requirements:
  - REPAIR-05
  - REPAIR-05-01
must_haves:
  truths:
    - "Repair APIs and tasks return deterministic counters plus typed failure entries; logs and telemetry are additive, not the only operator-facing surface."
    - "Mix tasks print summary counters first and only emit bounded tagged failure lines after the summary when needed."
    - "Operator guides teach the supported repair verbs `reprobe`, `requeue`, `regenerate`, `cleanup`, and `sweep` as separate explicit tools."
    - "No persisted audit table or history model is added in Phase 30."
  artifacts:
    - path: "lib/rindle/ops/lifecycle_repair.ex"
      provides: "Shared report and failure-entry semantics for public repair operations."
      contains: "failure_class"
    - path: "guides/operations.md"
      provides: "Canonical operator guide for the Phase 30 repair surface."
      contains: "reprobe"
    - path: "guides/troubleshooting.md"
      provides: "Recovery matrix that points at the correct supported repair lane for each failure mode."
      contains: "requeue"
  key_links:
    - from: "guides/troubleshooting.md"
      to: "guides/operations.md"
      via: "troubleshooting recovery advice names the same supported repair commands the operator guide teaches"
      pattern: "reprobe|requeue|regenerate|cleanup|sweep"
    - from: "lib/rindle/ops/lifecycle_repair.ex"
      to: "lib/mix/tasks/rindle.sweep_orphaned_temp_files.ex"
      via: "repair surfaces converge on one report/failure vocabulary even when task-specific counters differ"
      pattern: "failure|report"
---

<objective>
Normalize the operator-facing repair contract after the concrete repair surfaces
exist.

Purpose: freeze deterministic reporting, tagged failure output, additive
telemetry, and support docs across the new and existing repair surfaces so
operators do not need to infer recovery from logs or internal module names.
Output: stable repair report semantics, bounded task output, and operations +
troubleshooting guides that teach the exact supported Phase 30 repair verbs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-lifecycle-repair-operations/30-CONTEXT.md
@.planning/phases/30-lifecycle-repair-operations/30-RESEARCH.md
@.planning/phases/30-lifecycle-repair-operations/30-PATTERNS.md
@guides/operations.md
@guides/troubleshooting.md
@lib/rindle/error.ex
@lib/mix/tasks/rindle.verify_storage.ex
@lib/mix/tasks/rindle.cleanup_orphans.ex
@lib/rindle/ops/metadata_backfill.ex
@lib/rindle/ops/upload_maintenance.ex

<interfaces>
From .planning/phases/30-lifecycle-repair-operations/30-CONTEXT.md:
```text
structured {:ok, report} results
typed failure entries with stable reason atoms, human-readable messages, and low-cardinality failure classes
summary counters first, bounded tagged failure lines after the summary
logs are a breadcrumb surface, not the only contract
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze shared report and tagged failure semantics across repair surfaces</name>
  <files>lib/rindle/ops/lifecycle_repair.ex, lib/mix/tasks/rindle.sweep_orphaned_temp_files.ex, test/rindle/ops/lifecycle_repair_test.exs, test/rindle/ops/sweep_orphaned_temp_files_test.exs</files>
  <behavior>
    - Public repair APIs expose deterministic report counters and typed failure entries.
    - Mix tasks keep human-readable summaries stable and bounded.
    - Logs and telemetry remain additive breadcrumbs, not the only place failure detail exists.
  </behavior>
  <action>Once Plans 30-01 through 30-03 land, tighten the report contracts so the new repair APIs and temp sweep task share one stable posture: deterministic counts, typed failure entries with stable reason atoms and human-readable messages, and bounded task output that prints summary counters first. Add or update tests that fail on logs-only failure visibility, unstable counter naming, or unbounded task spam. Keep telemetry additive and low-cardinality, but do not turn telemetry into the only operator-facing failure surface.</action>
  <verify>
    <automated>mix test test/rindle/ops/lifecycle_repair_test.exs test/rindle/ops/sweep_orphaned_temp_files_test.exs</automated>
  </verify>
  <done>The new repair APIs and temp sweep task expose stable, test-locked report and failure semantics that remain operator-readable without needing logs or telemetry alone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite operations and troubleshooting guidance around the supported repair verbs</name>
  <files>guides/operations.md, guides/troubleshooting.md, test/rindle/error_test.exs</files>
  <behavior>
    - Operator guides teach `reprobe`, `requeue`, `regenerate`, `cleanup`, and `sweep` as separate explicit tools.
    - Recovery advice points at real supported commands or public APIs only.
    - No direct DB-row-flip guidance remains where a supported repair command now exists.
  </behavior>
  <action>Update `guides/operations.md` and `guides/troubleshooting.md` so the public/operator repair story matches the actual Phase 30 implementation: use `reprobe` for probe drift, the public targeted repair API for failed/cancelled asset-scoped work, `mix rindle.regenerate_variants` for broad drift, `mix rindle.cleanup_orphans` for upload residue, and the temp sweep task for AV run-dir cleanup. Remove or rewrite manual guidance that implies unsupported public APIs or direct row mutation where a supported repair lane now exists. Keep `test/rindle/error_test.exs` aligned with the new recovery vocabulary.</action>
  <verify>
    <automated>mix test test/rindle/error_test.exs</automated>
  </verify>
  <done>The operator docs and recovery guidance match the shipped repair verbs exactly and no longer depend on nonexistent APIs or ad hoc row-flip advice.</done>
</task>

</tasks>

<verification>
Run `mix test test/rindle/ops/lifecycle_repair_test.exs test/rindle/ops/sweep_orphaned_temp_files_test.exs test/rindle/error_test.exs`.
</verification>

<success_criteria>
REPAIR-05 is satisfied when the repair surface is frozen around deterministic
reports, tagged failure output, additive telemetry, and operator docs that name
the exact supported repair verbs without logs-only or row-flip-only recovery.
</success_criteria>

<output>
After completion, create `.planning/phases/30-lifecycle-repair-operations/30-04-SUMMARY.md`
</output>

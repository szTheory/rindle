---
phase: 31-runtime-diagnostics-drift-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/rindle.doctor.ex
  - lib/rindle/config.ex
  - lib/rindle/ops/runtime_checks.ex
  - test/rindle/doctor_test.exs
  - test/rindle/ops/runtime_checks_test.exs
autonomous: true
requirements:
  - DIAG-01
must_haves:
  truths:
    - "`mix rindle.doctor` stays a fast, read-only prerequisite checker and does not become a stuck-work report or auto-fix command."
    - "Doctor output is deterministic, grouped by stable documented check IDs, and each failing check includes explicit operator guidance."
    - "Doctor detects runtime capability drift, profile-to-capability mismatch, required default-`Oban` queue presence, delivery/local-playback misconfiguration, and pending or unresolved Rindle migration drift."
    - "The `rindle_media` queue is required only when discovered profiles declare AV-capable variants; image-only adopters are not failed for an unused AV queue."
    - "Doctor exit behavior remains CI-friendly: all checks run, text ordering is stable, and failure exits happen only after the summary is emitted."
  artifacts:
    - path: "lib/rindle/ops/runtime_checks.ex"
      provides: "Hidden check registry and result formatter for read-only runtime and migration checks."
      contains: "def run"
    - path: "lib/mix/tasks/rindle.doctor.ex"
      provides: "Deterministic operator-facing doctor task over the hidden check registry."
      contains: "def run_checks"
    - path: "test/rindle/ops/runtime_checks_test.exs"
      provides: "Contract coverage for stable check IDs, queue/delivery/migration checks, and failure guidance."
      contains: "describe \"run/1\""
  key_links:
    - from: "lib/mix/tasks/rindle.doctor.ex"
      to: "lib/rindle/ops/runtime_checks.ex"
      via: "public Mix task delegates to hidden structured check runner"
      pattern: "RuntimeChecks|run"
    - from: "lib/rindle/ops/runtime_checks.ex"
      to: "lib/rindle/config.ex"
      via: "doctor resolves configured profiles and required queue/runtime seams from config instead of hardcoding host-app assumptions"
      pattern: "profile_modules|queue"
---

<objective>
Freeze the expanded doctor contract before status-report and telemetry work land.

Purpose: deliver DIAG-01 as one deterministic, read-only prerequisite and drift
checker that operators can run in CI or before escalation without widening it
into a repair or reporting control plane.
Output: one hidden runtime-check registry, an expanded `mix rindle.doctor`
surface, and test coverage that locks stable check IDs plus actionable fix
guidance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/31-runtime-diagnostics-drift-visibility/31-CONTEXT.md
@.planning/phases/31-runtime-diagnostics-drift-visibility/31-RESEARCH.md
@.planning/phases/31-runtime-diagnostics-drift-visibility/31-PATTERNS.md
@lib/mix/tasks/rindle.doctor.ex
@lib/rindle/config.ex
@guides/background_processing.md
@test/rindle/doctor_test.exs

<interfaces>
From .planning/phases/31-runtime-diagnostics-drift-visibility/31-CONTEXT.md:
```text
D-01 through D-05: doctor stays deterministic, read-only, separate from stuck-work reporting, and uses stable check IDs with actionable fix guidance.
D-17: migration drift means pending local Rindle migrations or applied DB versions missing from local code.
```

From lib/rindle/config.ex:
```elixir
@spec profile_modules() :: [module()]
def profile_modules do
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a hidden doctor check registry with stable IDs and read-only runtime probes</name>
  <files>lib/rindle/ops/runtime_checks.ex, lib/rindle/config.ex, test/rindle/ops/runtime_checks_test.exs</files>
  <behavior>
    - Each doctor check returns a stable ID, status, component, summary, and fix guidance instead of free-form ad hoc strings.
    - Runtime checks stay read-only and cover the locked DIAG-01 classes only.
    - Queue and migration checks distinguish actionable misconfiguration from ordinary healthy absence of stuck work.
  </behavior>
  <action>Create hidden `Rindle.Ops.RuntimeChecks` per D-01 through D-05 and D-17. Model doctor as a structured registry that runs all checks and returns deterministic ordered results for: FFmpeg/runtime capability drift, profile-to-capability fit from `Rindle.Config.profile_modules/0`, required default-`Oban` queue presence and default-instance ownership sanity from the queues documented in `guides/background_processing.md`, delivery/local-playback configuration checks, and version-based Rindle migration drift only. Require `rindle_media` only when discovered profiles include AV-capable variants; keep the image-only queue contract narrower so adopters are not failed for unused AV infrastructure. Keep every check read-only per D-03, emit stable check IDs plus fix text per D-05, and avoid widening into stuck-work or per-row lifecycle inspection per D-04.</action>
  <verify>
    <automated>mix test test/rindle/ops/runtime_checks_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The repo has one hidden, test-locked doctor registry that covers the Phase 31 prerequisite and drift checks with stable IDs and guidance while remaining read-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebuild `mix rindle.doctor` on the structured check runner with deterministic operator output</name>
  <files>lib/mix/tasks/rindle.doctor.ex, test/rindle/doctor_test.exs, test/rindle/ops/runtime_checks_test.exs</files>
  <behavior>
    - `mix rindle.doctor` prints deterministic text output first and exits non-zero only after emitting the check summary.
    - The task remains CI-friendly and does not create queues, fix config, mutate migrations, or surface stuck-work reports.
    - Failing output points to existing verbs and guides rather than new repair language.
  </behavior>
  <action>Refactor `Mix.Tasks.Rindle.Doctor` to delegate to the hidden registry and render stable text output ordered by check ID per D-27. Keep the task narrow per D-01 through D-04: no auto-remediation, no queue creation, no migration mutation, and no stuck-work reporting. Preserve explicit profile arguments, add deterministic failure summaries and fix guidance, and ensure misconfiguration text points operators at existing configuration or repair verbs per D-28 instead of inventing new verbs.</action>
  <verify>
    <automated>mix test test/rindle/doctor_test.exs test/rindle/ops/runtime_checks_test.exs --warnings-as-errors</automated>
  </verify>
  <done>`mix rindle.doctor` is now a deterministic, read-only, actionable prerequisite checker whose output and exit behavior are locked by tests.</done>
</task>

</tasks>

<verification>
Run `mix test test/rindle/doctor_test.exs test/rindle/ops/runtime_checks_test.exs --warnings-as-errors`.
</verification>

<success_criteria>
DIAG-01 is satisfied when `mix rindle.doctor` remains a fast read-only command,
emits stable check IDs and fix guidance, detects queue/delivery/capability and
migration drift accurately, and never widens into stuck-work reporting or
mutation.
</success_criteria>

<output>
After completion, create `.planning/phases/31-runtime-diagnostics-drift-visibility/31-01-SUMMARY.md`
</output>

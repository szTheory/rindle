---
phase: 20-v1.3-verification-and-metadata-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/15-ci-integrity-and-publish-preflight/15-VERIFICATION.md
  - .planning/phases/16-live-publish-execution-and-post-publish-verification/16-VERIFICATION.md
  - .planning/phases/16-live-publish-execution-and-post-publish-verification/16-01-SUMMARY.md
  - .planning/phases/16-live-publish-execution-and-post-publish-verification/16-02-SUMMARY.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - PUBLISH-01
  - PUBLISH-02
  - PUBLISH-03
  - VERIFY-01
  - RELEASE-01
  - RELEASE-02
commit_message: "docs(20): retrofit 15/16 VERIFICATION.md, fix 16 SUMMARY frontmatter, reconcile REQUIREMENTS.md (closes G1, G2, G3)"
must_haves:
  truths:
    - "15-VERIFICATION.md exists at .planning/phases/15-ci-integrity-and-publish-preflight/15-VERIFICATION.md and records goal-backward verification for PUBLISH-01 and PUBLISH-02 (D-01, D-02, D-04)"
    - "16-VERIFICATION.md exists at .planning/phases/16-live-publish-execution-and-post-publish-verification/16-VERIFICATION.md and records goal-backward verification for PUBLISH-03, VERIFY-01, RELEASE-01, RELEASE-02 (D-01, D-02, D-04)"
    - "16-VERIFICATION.md marks VERIFY-02 as SATISFIED (functional) with forward_reference: phase-21 — NOT 'partial' (D-03, D-09)"
    - "16-01-SUMMARY.md requirements_completed = [PUBLISH-03, RELEASE-01, VERIFY-01] (D-05)"
    - "16-02-SUMMARY.md requirements_completed = [PUBLISH-03, RELEASE-01, VERIFY-02, RELEASE-02] (D-06)"
    - "16-01-SUMMARY.md L31 'remain uncommitted' claim is replaced with a tracked-in-git statement (D-07)"
    - "REQUIREMENTS.md traceability rows L85-91 (PUBLISH-01/02/03, VERIFY-01, RELEASE-01, RELEASE-02) and L97-99 (API-06/07/08) flipped to Complete; VERIFY-02 row stays Pending (D-08, D-09)"
    - "REQUIREMENTS.md Active checkboxes at L10-12 (PUBLISH-01/02/03), L16 (VERIFY-01 only — NOT VERIFY-02), L21-22 (RELEASE-01/02), L40-42 (API-06/07/08) flipped from [ ] to [x] (D-08)"
    - "REQUIREMENTS.md bold-span literal newlines fixed at L26 (API-01), L28 (API-02), L35 (API-05), L46 (API-09), L48 (API-10), L50 (API-11) — pattern '**ID\\n**:' becomes '**ID**:' (D-08)"
    - "REQUIREMENTS.md coverage note at L108 updated from 'Pending closure: 7' to 'Pending closure: 1 (VERIFY-02 routed to Phase 21)' (D-08)"
    - "Single docs(20) commit lands all six edits atomically (atomic-commit discipline; D-16)"
  artifacts:
    - path: ".planning/phases/15-ci-integrity-and-publish-preflight/15-VERIFICATION.md"
      provides: "Goal-backward verification record for Phase 15 (PUBLISH-01, PUBLISH-02)"
      contains: "PUBLISH-01, PUBLISH-02, release_docs_parity_test.exs, package_metadata_test.exs, release_preflight.sh, status: passed"
    - path: ".planning/phases/16-live-publish-execution-and-post-publish-verification/16-VERIFICATION.md"
      provides: "Goal-backward verification record for Phase 16 (PUBLISH-03, VERIFY-01, VERIFY-02, RELEASE-01, RELEASE-02)"
      contains: "PUBLISH-03, VERIFY-01, VERIFY-02, RELEASE-01, RELEASE-02, forward_reference: phase-21, hex_release_exists_test.exs, 16-REVERT-REHEARSAL.md, status: passed"
    - path: ".planning/phases/16-live-publish-execution-and-post-publish-verification/16-01-SUMMARY.md"
      provides: "Phase 16 plan 01 SUMMARY with corrected requirements_completed frontmatter"
      contains: "VERIFY-01"
    - path: ".planning/phases/16-live-publish-execution-and-post-publish-verification/16-02-SUMMARY.md"
      provides: "Phase 16 plan 02 SUMMARY with corrected requirements_completed frontmatter"
      contains: "VERIFY-02, RELEASE-02"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Reconciled traceability table + Active checkboxes + bold-span fixes + coverage note"
      contains: "Pending closure: 1"
  key_links:
    - from: ".planning/phases/15-ci-integrity-and-publish-preflight/15-VERIFICATION.md"
      to: "test/install_smoke/release_docs_parity_test.exs, test/install_smoke/package_metadata_test.exs, scripts/release_preflight.sh"
      via: "Evidence citations in Required Artifacts + Behavioral Spot-Checks"
      pattern: "release_docs_parity_test.exs|package_metadata_test.exs|release_preflight.sh"
    - from: ".planning/phases/16-live-publish-execution-and-post-publish-verification/16-VERIFICATION.md"
      to: ".github/workflows/release.yml:332-348, .github/workflows/release.yml:447-467, test/install_smoke/hex_release_exists_test.exs, 16-REVERT-REHEARSAL.md, test/install_smoke/release_docs_parity_test.exs:252"
      via: "Evidence citations in Required Artifacts + Behavioral Spot-Checks"
      pattern: "release\\.yml.*332|release\\.yml.*447|hex_release_exists_test|16-REVERT-REHEARSAL|release_docs_parity_test\\.exs:252"
    - from: ".planning/REQUIREMENTS.md traceability table"
      to: ".planning/phases/15-* + 16-* + 18-* SUMMARY frontmatters"
      via: "Three-source consistency required by /gsd-audit-milestone v1.3"
      pattern: "Complete"
---

<objective>
Close G1, G2, G3, and TD-Req from `.planning/v1.3-MILESTONE-AUDIT.md` in a single docs commit so `/gsd-audit-milestone v1.3` can re-run and report `passed`.

Five concrete edits:
1. Author `15-VERIFICATION.md` retroactively (G1) — Phase 15 implementation is sound but lacks the goal-backward artifact.
2. Author `16-VERIFICATION.md` retroactively (G2) — declares all five Phase 16 requirements satisfied; VERIFY-02 marked SATISFIED (functional) with forward_reference to Phase 21 (D-03).
3. Fix `16-01-SUMMARY.md` and `16-02-SUMMARY.md` `requirements_completed` frontmatter (G3) — split per D-05/D-06; strike stale "remain uncommitted" claim per D-07.
4. Reconcile `.planning/REQUIREMENTS.md`: flip 7 traceability rows (L85-91) and 3 API rows (L97-99) to Complete; flip Active-section checkboxes; fix 6 bold-span literal newlines (L26/28/35/46/48/50); update coverage note at L108.

Purpose: This unblocks the milestone audit re-run. 20-02 (LiveView) and 20-03 (onboarding prose) close TD-17/TD-19 but do not touch milestone-audit signals.

Output: Two new VERIFICATION.md files, two corrected SUMMARY.md files, one reconciled REQUIREMENTS.md, one atomic git commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.3-MILESTONE-AUDIT.md
@.planning/phases/20-v1.3-verification-and-metadata-closure/20-CONTEXT.md
@.planning/phases/15-ci-integrity-and-publish-preflight/15-01-SUMMARY.md
@.planning/phases/15-ci-integrity-and-publish-preflight/15-02-SUMMARY.md
@.planning/phases/16-live-publish-execution-and-post-publish-verification/16-01-SUMMARY.md
@.planning/phases/16-live-publish-execution-and-post-publish-verification/16-02-SUMMARY.md
@.planning/phases/16-live-publish-execution-and-post-publish-verification/16-REVERT-REHEARSAL.md
@.planning/phases/17-api-surface-boundary-audit/17-VERIFICATION.md
@.planning/phases/18-documentation-and-typespec-coverage/18-VERIFICATION.md
@.planning/phases/19-convenience-api-additions/19-VERIFICATION.md

<interfaces>
<!-- Canonical VERIFICATION.md frontmatter shapes (extracted directly from existing files) -->

Short-form variant (Phase 17/19) — frontmatter fields between YAML document markers:

```yaml
# (open with the standard --- YAML document marker)
phase: <phase-id>
verified: <ISO-8601-Z>
status: passed
score: <N>/<N> must-haves verified
overrides_applied: 0
# (close with the standard --- YAML document marker)
```

Success-Criteria-driven variant (Phase 18; USE THIS for both 15 and 16 since their ROADMAP blocks declare explicit success criteria) — frontmatter fields between YAML document markers:

```yaml
# (open with the standard --- YAML document marker)
phase: <phase-id>
verified: <ISO-8601-Z>
status: passed
score: <N>/<N> success criteria verified (plus <M>/<M> supporting must-haves)
criteria_total: <N>
criteria_pass: <N>
criteria_fail: 0
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
# (close with the standard --- YAML document marker)
```

(NOTE TO EXECUTOR: when you author the actual VERIFICATION.md files in Tasks 1 and 2, write the literal `---` separator lines at the top and bottom of the frontmatter block — do NOT include the `# (...)` comments above. They appear here only because nested `---` lines would confuse this plan's own frontmatter parser. The Task 1/2 action blocks below contain indented exemplars where the literal `---` lines are safely indented out of column-0.)

Section progression (both variants share these sections after the header):
1. Goal Achievement → Success Criteria (or Observable Truths) table
2. Required Must-Haves (or Observable Truths) table
3. Required Artifacts table
4. Key Link Verification table
5. Data-Flow Trace (Level 4) table
6. Behavioral Spot-Checks table
7. Requirements Coverage table
8. Anti-Patterns Found table (or "None" if none)
9. Human Verification Required ("None required" is acceptable)
10. Gaps Summary

VERIFY-02 frontmatter override (D-03) — embed inside 16-VERIFICATION.md Requirements Coverage row for VERIFY-02:
- Status column: "SATISFIED (functional)"
- forward_reference: phase-21
- Evidence: "Functional contract met via `mix hex.publish --yes` docs upload + `mix docs --warnings-as-errors` build gate + `guides/release_publish.md:108,144` repair path. Rendered-HTML reachability HTTP probe of hexdocs.pm/rindle/<version> deferred to Phase 21 (G4) per ROADMAP."
- Do NOT mark this requirement 'partial' anywhere — that would re-flag G4.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 15-VERIFICATION.md retroactively (closes G1, D-01/D-02/D-04)</name>
  <files>.planning/phases/15-ci-integrity-and-publish-preflight/15-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/18-documentation-and-typespec-coverage/18-VERIFICATION.md (canonical Success-Criteria-driven format — Phase 15 ROADMAP block has 4 explicit success criteria, so use this variant)
    - .planning/phases/15-ci-integrity-and-publish-preflight/15-01-SUMMARY.md (PUBLISH-02 claim mapping; the package_metadata_test.exs CHANGELOG enforcement)
    - .planning/phases/15-ci-integrity-and-publish-preflight/15-02-SUMMARY.md (PUBLISH-01 claim mapping; SHA 6dd0d54..., CI run URL, exact-SHA boundary)
    - .planning/v1.3-MILESTONE-AUDIT.md L24-72 (3-source matrix template; status / claimed_by_plans / completed_by_plans / verification_status / evidence)
    - .planning/ROADMAP.md L58-71 (Phase 15 block: Goal + Success Criteria + Requirements + Plans)
    - test/install_smoke/release_docs_parity_test.exs (PUBLISH-01 evidence — exact-SHA remote-proof assertions; spot-check that lines 240-265 still contain the canonical revert/retire snippets and L52-58 still contains exact-SHA proof language for PUBLISH-01)
    - test/install_smoke/package_metadata_test.exs (PUBLISH-02 evidence — CHANGELOG 0.1.0 enforcement at ~L69-73)
    - scripts/release_preflight.sh (PUBLISH-02 evidence — wraps mix hex.build --unpack with RINDLE_INSTALL_SMOKE_PACKAGE_ROOT)
  </read_first>
  <action>
    Create `.planning/phases/15-ci-integrity-and-publish-preflight/15-VERIFICATION.md` using the Phase 18 Success-Criteria-driven format. Phase 15 ROADMAP block declares 4 success criteria (ROADMAP.md:62-67); reproduce them as the criteria_total.

    Use this exact frontmatter (substitute today's UTC ISO-8601 timestamp for `verified`). The literal `---` lines below are safe to write into the OUTPUT file — they only need to be column-0 in the artifact you create:

    ```yaml
    ---
    phase: 15-ci-integrity-and-publish-preflight
    verified: 2026-05-01T00:00:00Z
    status: passed
    score: 4/4 success criteria verified (plus 5/5 supporting must-haves)
    criteria_total: 4
    criteria_pass: 4
    criteria_fail: 0
    overrides_applied: 0
    re_verification:
      previous_status: none
      previous_score: n/a
      gaps_closed: []
      gaps_remaining: []
      regressions: []
    ---
    ```

    Body sections (mirror Phase 18's structure exactly — see 18-VERIFICATION.md for canonical column shapes):

    1. **# Phase 15: CI Integrity and Publish Preflight Verification Report** + Phase Goal quote (from ROADMAP.md:59) + Verified/Status/Re-verification metadata block.

    2. **## Goal Achievement → ### Success Criteria (from ROADMAP.md)** — 4-row table with Criterion / Status / Evidence columns. Map per the 3-source matrix in v1.3-MILESTONE-AUDIT.md:122-123:
       - SC-1 (CI green on RC commit): VERIFIED — `15-02-SUMMARY.md:38-40` records SHA `6dd0d54081c89b68c630d9642a40453d310008c6` against CI run `https://github.com/szTheory/rindle/actions/runs/25135464796` (success, 2026-04-29T21:49:44Z); `release_docs_parity_test.exs` enforces the exact-SHA remote-proof boundary.
       - SC-2 (package metadata + CHANGELOG 0.1.0 + name available): VERIFIED — `15-01-SUMMARY.md:20-21` records `package_metadata_test.exs` extension enforcing `CHANGELOG.md` with `0.1.0` entry; `release_docs_parity_test.exs` parity enforcement covers maintainer commands `mix hex.user whoami`, `mix hex.owner list rindle`, manual `rindle` package-name availability check.
       - SC-3 (`mix hex.build --unpack` tarball contents): VERIFIED — `scripts/release_preflight.sh` (`15-01-SUMMARY.md:17-18`) hardened to unpack into a short temp directory outside the checkout; `RINDLE_INSTALL_SMOKE_PACKAGE_ROOT` exported to downstream install-smoke checks; `package_metadata_test.exs` enforces tarball metadata, MIT license, GitHub link, required shipped paths from the unpacked tarball.
       - SC-4 (all preflight gates pass on exact tag commit): VERIFIED — `MIX_ENV=dev bash scripts/release_preflight.sh` validated package build + first two ExUnit gates per `15-01-SUMMARY.md:27`; full preflight at `15-02-SUMMARY.md:33` confirmed via `gh run view 25135464796 --json conclusion` returning `success`.

    3. **### Required Must-Haves (from PLAN frontmatter)** — derive ≥5 must-haves across the two Phase 15 plans (read 15-01-SUMMARY.md and 15-02-SUMMARY.md to enumerate). Examples: `scripts/release_preflight.sh` artifact-unpack hardening; `RINDLE_RELEASE_PREFLIGHT_KEEP_ARTIFACT` opt-out flag; `package_metadata_test.exs` CHANGELOG enforcement; `15-RELEASE-CANDIDATE-CHECKLIST.md` blocking-evidence template; release_docs_parity_test.exs exact-SHA + maintainer-only checks parity assertions.

    4. **### Required Artifacts** — Artifact / Expected / Status / Details columns. Cite (with line numbers): `scripts/release_preflight.sh`, `test/install_smoke/release_docs_parity_test.exs`, `test/install_smoke/package_metadata_test.exs`, `.planning/phases/15-ci-integrity-and-publish-preflight/15-RELEASE-CANDIDATE-CHECKLIST.md`.

    5. **### Key Link Verification** — From / To / Via / Status / Details. Examples:
       - `scripts/release_preflight.sh` → `test/install_smoke/package_metadata_test.exs` via `RINDLE_INSTALL_SMOKE_PACKAGE_ROOT` env handoff (WIRED).
       - `release_docs_parity_test.exs` → `guides/release_publish.md` via exact-SHA remote-proof string parity (WIRED).
       - `15-RELEASE-CANDIDATE-CHECKLIST.md` → CI run `25135464796` via SHA `6dd0d54...` (WIRED).

    6. **### Data-Flow Trace (Level 4)** — table with Artifact / Data Variable / Source / Produces Real Data / Status. Real data: tarball contents from `mix hex.build --unpack`, CHANGELOG body matched against `0.1.0` regex, gh CLI JSON conclusion field.

    7. **### Behavioral Spot-Checks** — Behavior / Command / Result / Status. Re-state the integration-checker run from v1.3-MILESTONE-AUDIT.md:151: `MIX_ENV=test mix test test/install_smoke/release_docs_parity_test.exs test/install_smoke/package_metadata_test.exs` → "32 tests, 0 failures" (run by integration checker 2026-05-01); `bash -n scripts/release_preflight.sh` → exit 0; `gh run view 25135464796 --json conclusion` → "success".

    8. **### Requirements Coverage** — Requirement / Source Plan / Description / Status / Evidence. PUBLISH-01 satisfied by 15-02; PUBLISH-02 satisfied by 15-01 + 15-02. Cite line numbers in package_metadata_test.exs (~L69-73) and release_docs_parity_test.exs (exact-SHA assertions L52-58 per audit; verify by re-reading the file).

    9. **### Anti-Patterns Found** — None on the Phase 15 deliverable surface. State explicitly that the integration checker found no blockers (v1.3-MILESTONE-AUDIT.md:151 "32/32 tests pass").

    10. **### Human Verification Required** — None required. All four success criteria are programmatically verifiable via the citations above.

    11. **### Gaps Summary** — None. PUBLISH-01 and PUBLISH-02 both satisfied. Reference v1.3-MILESTONE-AUDIT.md classification "metadata gap, not implementation gap" (L143).

    12. Trailing line: `_Verified: <timestamp>Z_` and `_Verifier: Claude (gsd-planner, retrofit per phase-20)_`.

    Note: This artifact is being authored retroactively (D-01) — the integration checker already validated implementation per v1.3-MILESTONE-AUDIT.md:151 (32/32 tests pass). Do NOT invoke /gsd-verify-work.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-ci-integrity-and-publish-preflight/15-VERIFICATION.md &amp;&amp; grep -q "PUBLISH-01" .planning/phases/15-ci-integrity-and-publish-preflight/15-VERIFICATION.md &amp;&amp; grep -q "PUBLISH-02" .planning/phases/15-ci-integrity-and-publish-preflight/15-VERIFICATION.md &amp;&amp; grep -q "release_docs_parity_test.exs" .planning/phases/15-ci-integrity-and-publish-preflight/15-VERIFICATION.md &amp;&amp; grep -q "package_metadata_test.exs" .planning/phases/15-ci-integrity-and-publish-preflight/15-VERIFICATION.md &amp;&amp; grep -q "release_preflight.sh" .planning/phases/15-ci-integrity-and-publish-preflight/15-VERIFICATION.md &amp;&amp; grep -q "status: passed" .planning/phases/15-ci-integrity-and-publish-preflight/15-VERIFICATION.md &amp;&amp; grep -q "criteria_total: 4" .planning/phases/15-ci-integrity-and-publish-preflight/15-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the target path.
    - Frontmatter contains `phase: 15-ci-integrity-and-publish-preflight`, `status: passed`, `criteria_total: 4`, `criteria_pass: 4`, `overrides_applied: 0`.
    - PUBLISH-01 and PUBLISH-02 both appear with status SATISFIED in the Requirements Coverage table.
    - All three evidence citations (release_docs_parity_test.exs, package_metadata_test.exs, release_preflight.sh) appear in the body.
    - Sections present: Goal Achievement, Required Must-Haves, Required Artifacts, Key Link Verification, Data-Flow Trace, Behavioral Spot-Checks, Requirements Coverage, Anti-Patterns Found, Human Verification Required, Gaps Summary.
  </acceptance_criteria>
  <done>15-VERIFICATION.md exists, mirrors Phase 18 Success-Criteria format, cites all three evidence artifacts, and declares PUBLISH-01 + PUBLISH-02 SATISFIED.</done>
</task>

<task type="auto">
  <name>Task 2: Author 16-VERIFICATION.md retroactively (closes G2 + G3 framing, D-01/D-02/D-03/D-04)</name>
  <files>.planning/phases/16-live-publish-execution-and-post-publish-verification/16-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/18-documentation-and-typespec-coverage/18-VERIFICATION.md (canonical Success-Criteria-driven format — Phase 16 ROADMAP block has 5 explicit success criteria)
    - .planning/phases/16-live-publish-execution-and-post-publish-verification/16-01-SUMMARY.md (PUBLISH-03 + RELEASE-01 claim mapping + idempotency probe)
    - .planning/phases/16-live-publish-execution-and-post-publish-verification/16-02-SUMMARY.md (workflow wiring + index-wait + public smoke + parity)
    - .planning/phases/16-live-publish-execution-and-post-publish-verification/16-REVERT-REHEARSAL.md (RELEASE-02 evidence — full rehearsal recorded 2026-04-30)
    - .planning/v1.3-MILESTONE-AUDIT.md L24-72 (3-source matrix template) and L161-167 (G3 evidence per requirement)
    - .planning/ROADMAP.md L72-86 (Phase 16 block: Goal + 5 Success Criteria + 5 Requirements + Plans)
    - .github/workflows/release.yml lines 320-370 (idempotency probe + skip gates) and 440-470 (index wait + public smoke)
    - test/install_smoke/hex_release_exists_test.exs (PUBLISH-03 idempotency proof — 7 tests)
    - test/install_smoke/release_docs_parity_test.exs (RELEASE-02 parity assertion at lines 248-265 — `mix hex.publish --revert VERSION`, retire reasons, 24h/1h window semantics)
    - guides/release_publish.md (VERIFY-02 docs repair path — search for `mix hex.docs publish` and rendered-HTML reachability mentions)
  </read_first>
  <action>
    Create `.planning/phases/16-live-publish-execution-and-post-publish-verification/16-VERIFICATION.md` using the Phase 18 Success-Criteria-driven format. Phase 16 ROADMAP block declares 5 success criteria (ROADMAP.md:76-81); reproduce them as criteria_total=5.

    Frontmatter (substitute today's UTC ISO-8601 for `verified`). The literal `---` lines below are safe to write into the OUTPUT file:

    ```yaml
    ---
    phase: 16-live-publish-execution-and-post-publish-verification
    verified: 2026-05-01T00:00:00Z
    status: passed
    score: 5/5 success criteria verified (plus N/N supporting must-haves)
    criteria_total: 5
    criteria_pass: 5
    criteria_fail: 0
    overrides_applied: 0
    re_verification:
      previous_status: none
      previous_score: n/a
      gaps_closed: []
      gaps_remaining: []
      regressions: []
    forward_references:
      - id: VERIFY-02
        target_phase: phase-21
        rationale: "Functional contract met via `mix hex.publish --yes` docs upload + build-time `mix docs --warnings-as-errors` gate + `guides/release_publish.md` docs repair path. Rendered-HTML reachability HTTP probe of hexdocs.pm/rindle/<version> is deferred to Phase 21 (G4 from v1.3-MILESTONE-AUDIT.md)."
    ---
    ```

    Body sections (mirror Phase 18 structure):

    1. **# Phase 16: Live Publish Execution and Post-Publish Verification Verification Report** + Phase Goal quote (ROADMAP.md:73) + metadata block.

    2. **## Goal Achievement → ### Success Criteria (from ROADMAP.md)** — 5-row table:
       - SC-1 (recovery rerun against immutable ref skips publish safely): VERIFIED — `release.yml:332-337` runs `scripts/hex_release_exists.sh` and emits `already_published=true|false`; `release.yml:340` and `:348` gate publish steps with `if: ${{ steps.idempotency.outputs.already_published != 'true' }}`; `release.yml:359-368` emits the idempotent publish summary; `hex_release_exists_test.exs` 7/7 pass per integration checker.
       - SC-2 (`{:rindle, "~> 0.1.0"}` resolves from Hex.pm in fresh app): VERIFIED — `release.yml:447-461` waits for Hex.pm to index the version with 5-minute deadline + 15s polling; `release.yml:463-467` invokes `scripts/public_smoke.sh "$VERSION"`; `test/install_smoke/support/generated_app_helper.ex:138-144` is the `mix deps.get` probe per integration checker. NOT declared in any prior SUMMARY — closure of G3 routes VERIFY-01 to 16-01-SUMMARY.md (see Task 3).
       - SC-3 (hexdocs.pm/rindle module documentation reachable): **SATISFIED (functional)** — `mix hex.publish --yes` (release.yml:357) uploads docs alongside the package; `mix docs --warnings-as-errors` build-time gate prevents broken renders; `guides/release_publish.md:108,144` documents `mix hex.docs publish` repair path. **forward_reference: phase-21** for the explicit HTTP reachability probe (G4 — observability gap, not functional gap). Per D-03: do NOT mark this 'partial'.
       - SC-4 (step-by-step routine-release runbook reflecting 0.1.0–0.1.4 deviations): VERIFIED — `guides/release_publish.md` updated with revert/retire decision matrix (matches `16-REVERT-REHEARSAL.md` decision matrix); `release_docs_parity_test.exs:240-265` enforces parity for the canonical commands and 24h/1h window semantics.
       - SC-5 (`mix hex.publish --revert VERSION` runbook step within correction window): VERIFIED — `16-REVERT-REHEARSAL.md` (signed 2026-04-30) records the canonical command, retire peer procedure with valid reasons (`renamed`, `deprecated`, `security`, `invalid`, `other`), docs-only repair path, and window semantics; `release_docs_parity_test.exs:252-263` parity assertion enforces all of these are present in `guides/release_publish.md`.

    3. **### Required Must-Haves** — derive ≥4 from 16-01-SUMMARY.md and 16-02-SUMMARY.md (e.g., probe stdout contract `already_published=true|false`; `fake_hex_bin.sh` test shim; four-job topology comment in release.yml; rollback language parity).

    4. **### Required Artifacts** — artifact / expected / status / details. Cite line numbers: `.github/workflows/release.yml:332-348` (idempotency probe + skip gates), `release.yml:447-467` (index wait + public smoke), `scripts/hex_release_exists.sh`, `test/install_smoke/hex_release_exists_test.exs`, `test/install_smoke/support/fake_hex_bin.sh`, `16-REVERT-REHEARSAL.md`, `test/install_smoke/release_docs_parity_test.exs:248-265`, `guides/release_publish.md` (specific line refs L108, L144 for docs-publish repair path).

    5. **### Key Link Verification** — From / To / Via / Status / Details:
       - `release.yml:332-337 (idempotency step)` → `scripts/hex_release_exists.sh` via `bash` invocation (WIRED)
       - `release.yml:340/:348 (publish + dry-run gates)` → `steps.idempotency.outputs.already_published` via GITHUB_OUTPUT (WIRED)
       - `release.yml:447-461 (index wait)` → `mix hex.info rindle "$VERSION"` via 5-min deadline polling (WIRED)
       - `release.yml:463-467 (public_verify)` → `scripts/public_smoke.sh "$VERSION"` (WIRED)
       - `guides/release_publish.md` → `16-REVERT-REHEARSAL.md` decision matrix via canonical-command parity (WIRED, enforced by release_docs_parity_test.exs:248-265)

    6. **### Data-Flow Trace (Level 4)** — real data: probe stdout (`already_published=true|false`), `mix hex.info` JSON, gh CLI conclusion field, hex.info index polling result, public_smoke.sh exit code.

    7. **### Behavioral Spot-Checks** — re-state from integration checker (v1.3-MILESTONE-AUDIT.md:158-167): `MIX_ENV=test mix test test/install_smoke/hex_release_exists_test.exs` → 7/7 pass; `MIX_ENV=test mix test test/install_smoke/release_docs_parity_test.exs test/install_smoke/package_metadata_test.exs` → 32/32 pass; manual `gh run view 25135464796 --json conclusion` → success.

    8. **### Requirements Coverage** — 5-row table:
       - PUBLISH-03 SATISFIED (claimed by 16-01, 16-02; evidence `release.yml:332-348`, `hex_release_exists_test.exs` 7/7)
       - VERIFY-01 SATISFIED (claimed by 16-01 — see Task 3; evidence `release.yml:447-461` index wait + `scripts/public_smoke.sh` + `test/install_smoke/support/generated_app_helper.ex:138-144` mix deps.get probe)
       - VERIFY-02 **SATISFIED (functional) — forward_reference: phase-21** (claimed by 16-02 — see Task 4; evidence `mix hex.publish --yes` docs upload + `mix docs --warnings-as-errors` gate + `guides/release_publish.md:108,144`; observability HTTP probe routed to Phase 21 per ROADMAP.md:150-159)
       - RELEASE-01 SATISFIED (claimed by 16-01, 16-02; evidence `guides/release_publish.md` 0.1.0–0.1.4 deviation history + `release_docs_parity_test.exs` parity)
       - RELEASE-02 SATISFIED (claimed by 16-02 — see Task 4; evidence `16-REVERT-REHEARSAL.md` signed 2026-04-30 + `release_docs_parity_test.exs:252` parity assertion)

    9. **### Anti-Patterns Found** — Document the stale `16-01-SUMMARY.md:31` "remain uncommitted" claim as INFO severity, note that it is being corrected by Task 3 of the same plan in this same commit. Otherwise: None.

    10. **### Human Verification Required** — None required (manual `gh run view 25135464796` was already executed by integration checker; see v1.3-MILESTONE-AUDIT.md:147-167 for the audit run).

    11. **### Gaps Summary** — 0 functional gaps. 1 observability gap (rendered-HTML hexdocs.pm reachability probe) explicitly forward-referenced to Phase 21 per ROADMAP.md:150-159 — NOT a Phase 16 gap, by design.

    12. Trailing line: `_Verified: <timestamp>Z_` / `_Verifier: Claude (gsd-planner, retrofit per phase-20)_`.

    Critical: VERIFY-02 must NEVER be marked 'partial' in this artifact (D-03). Use 'SATISFIED (functional)' with the forward_reference. Marking partial would re-flag G4 in the next milestone audit.
  </action>
  <verify>
    <automated>test -f .planning/phases/16-live-publish-execution-and-post-publish-verification/16-VERIFICATION.md &amp;&amp; grep -q "PUBLISH-03" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-VERIFICATION.md &amp;&amp; grep -q "VERIFY-01" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-VERIFICATION.md &amp;&amp; grep -q "VERIFY-02" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-VERIFICATION.md &amp;&amp; grep -q "RELEASE-01" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-VERIFICATION.md &amp;&amp; grep -q "RELEASE-02" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-VERIFICATION.md &amp;&amp; grep -q "phase-21" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-VERIFICATION.md &amp;&amp; grep -q "SATISFIED (functional)" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-VERIFICATION.md &amp;&amp; grep -q "16-REVERT-REHEARSAL.md" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-VERIFICATION.md &amp;&amp; grep -q "hex_release_exists_test" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-VERIFICATION.md &amp;&amp; grep -q "release_docs_parity_test.exs:252" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-VERIFICATION.md &amp;&amp; ! grep -E "VERIFY-02.*\bpartial\b" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-VERIFICATION.md &amp;&amp; grep -q "criteria_total: 5" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at target path.
    - Frontmatter contains `phase: 16-live-publish-execution-and-post-publish-verification`, `status: passed`, `criteria_total: 5`, `criteria_pass: 5`, and a `forward_references` block referencing VERIFY-02 → phase-21.
    - All five Phase 16 requirements (PUBLISH-03, VERIFY-01, VERIFY-02, RELEASE-01, RELEASE-02) appear in Requirements Coverage table.
    - VERIFY-02 status reads exactly `SATISFIED (functional)` and the row contains `forward_reference: phase-21` (or in the body section).
    - Citations present: `release.yml:332-348`, `release.yml:447-467`, `hex_release_exists_test.exs`, `16-REVERT-REHEARSAL.md`, `release_docs_parity_test.exs:252`.
    - No row marks VERIFY-02 as 'partial' (negative grep gate is critical).
    - All 10 standard sections present (Goal Achievement → Gaps Summary).
  </acceptance_criteria>
  <done>16-VERIFICATION.md exists, declares all five Phase 16 requirements satisfied (VERIFY-02 functional + forward_reference), cites the wired implementation evidence, and avoids the 'partial' framing that would re-flag G4.</done>
</task>

<task type="auto">
  <name>Task 3: Fix 16-01-SUMMARY.md frontmatter and strike stale 'uncommitted' claim (closes G3 part 1, D-05 + D-07)</name>
  <files>.planning/phases/16-live-publish-execution-and-post-publish-verification/16-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/16-live-publish-execution-and-post-publish-verification/16-01-SUMMARY.md (current state — confirm L6-8 frontmatter and L31 stale claim still match the values described in 20-CONTEXT.md before editing)
  </read_first>
  <action>
    Two precise edits to `.planning/phases/16-live-publish-execution-and-post-publish-verification/16-01-SUMMARY.md`:

    **Edit 1 (frontmatter L6-8):** Replace the existing `requirements_completed` block:

    Find:
    ```yaml
    requirements_completed:
      - PUBLISH-03
      - RELEASE-01
    ```

    Replace with:
    ```yaml
    requirements_completed:
      - PUBLISH-03
      - RELEASE-01
      - VERIFY-01
    ```

    Rationale (D-05): 16-01 shipped the idempotency probe + ExUnit harness — the probe is what makes `mix deps.get` resolve cleanly post-publish, satisfying VERIFY-01. The integration checker confirmed this implementation is wired in `release.yml:447-461` + `scripts/public_smoke.sh` + `test/install_smoke/support/generated_app_helper.ex:138-144` (mix deps.get probe).

    **Edit 2 (body L31, "Notes" section):** Replace the stale-uncommitted claim.

    Find:
    ```
    - The probe and its tests remain uncommitted in the current working tree; this summary records verified execution state, not a pushed Git history boundary.
    ```

    Replace with:
    ```
    - All four idempotency artifacts (`scripts/hex_release_exists.sh`, `test/install_smoke/hex_release_exists_test.exs`, `test/install_smoke/support/fake_hex_bin.sh`, `.github/workflows/release.yml`) are tracked in git as of 2026-04-30; the v1.3 milestone audit reflects this at `.planning/v1.3-MILESTONE-AUDIT.md:200-201` (TD-16 entry).
    ```

    Rationale (D-07): All four artifacts are now in `git ls-files`; the original claim is stale. The audit cross-reference at L200-201 documents the correction.

    Do NOT modify any other line, frontmatter key, or body section. Use `Edit` (not `Write`) so unrelated content is preserved verbatim.
  </action>
  <verify>
    <automated>grep -q "  - VERIFY-01" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-01-SUMMARY.md &amp;&amp; grep -q "  - PUBLISH-03" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-01-SUMMARY.md &amp;&amp; grep -q "  - RELEASE-01" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-01-SUMMARY.md &amp;&amp; ! grep -q "remain uncommitted" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-01-SUMMARY.md &amp;&amp; grep -q "All four idempotency artifacts" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `requirements_completed` array contains exactly three IDs: PUBLISH-03, RELEASE-01, VERIFY-01 (in that order).
    - The literal string "remain uncommitted" no longer appears anywhere in the file.
    - The replacement string "All four idempotency artifacts" appears in the file.
    - All other content (Accomplishments, Verification, body Notes paragraph 1) is unchanged.
  </acceptance_criteria>
  <done>16-01-SUMMARY.md frontmatter declares VERIFY-01 in addition to PUBLISH-03 and RELEASE-01; the stale "remain uncommitted" claim is replaced with a tracked-in-git statement.</done>
</task>

<task type="auto">
  <name>Task 4: Fix 16-02-SUMMARY.md frontmatter (closes G3 part 2, D-06)</name>
  <files>.planning/phases/16-live-publish-execution-and-post-publish-verification/16-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/16-live-publish-execution-and-post-publish-verification/16-02-SUMMARY.md (current state — confirm L6-8 frontmatter still shows only PUBLISH-03 and RELEASE-01)
  </read_first>
  <action>
    Single precise edit to `.planning/phases/16-live-publish-execution-and-post-publish-verification/16-02-SUMMARY.md`:

    **Frontmatter L6-8:** Replace the existing `requirements_completed` block:

    Find:
    ```yaml
    requirements_completed:
      - PUBLISH-03
      - RELEASE-01
    ```

    Replace with:
    ```yaml
    requirements_completed:
      - PUBLISH-03
      - RELEASE-01
      - VERIFY-02
      - RELEASE-02
    ```

    Rationale (D-06): 16-02 shipped the workflow wiring — the public-smoke + index-wait at `release.yml:447-467` satisfies VERIFY-02 transitively via `mix hex.publish --yes` docs upload (functional contract; HTTP probe deferred to Phase 21 per D-03), and the parity assertion at `test/install_smoke/release_docs_parity_test.exs:252` plus the landed `16-REVERT-REHEARSAL.md` satisfy RELEASE-02 (`mix hex.publish --revert VERSION` runbook step).

    Do NOT modify any other line, frontmatter key, or body section. Do NOT add VERIFY-01 here — that belongs in 16-01-SUMMARY.md per D-05 (Task 3).
  </action>
  <verify>
    <automated>grep -q "  - PUBLISH-03" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-02-SUMMARY.md &amp;&amp; grep -q "  - RELEASE-01" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-02-SUMMARY.md &amp;&amp; grep -q "  - VERIFY-02" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-02-SUMMARY.md &amp;&amp; grep -q "  - RELEASE-02" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-02-SUMMARY.md &amp;&amp; ! grep -q "  - VERIFY-01" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `requirements_completed` array contains exactly four IDs in this order: PUBLISH-03, RELEASE-01, VERIFY-02, RELEASE-02.
    - VERIFY-01 does NOT appear in 16-02-SUMMARY.md (it belongs in 16-01).
    - All other body content is unchanged.
  </acceptance_criteria>
  <done>16-02-SUMMARY.md frontmatter declares VERIFY-02 and RELEASE-02 in addition to PUBLISH-03 and RELEASE-01.</done>
</task>

<task type="auto">
  <name>Task 5: Reconcile REQUIREMENTS.md traceability table, Active checkboxes, bold-span fixes, coverage note (closes TD-Req, D-08 + D-09)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — confirm exact line numbers for L10-12, L16-17, L21-22, L26, L28, L35, L40-42, L46, L48, L50, L85-91, L97-99, L108 before editing; line numbers may have shifted slightly during prior phase work)
    - .planning/v1.3-MILESTONE-AUDIT.md L122-141 (3-source matrix shows which IDs flip to Complete)
  </read_first>
  <action>
    Multiple precise edits to `.planning/REQUIREMENTS.md`. Re-read the file before each edit to confirm line numbers — they may shift slightly after each edit. Use `Edit` for each change so unrelated lines are preserved.

    **Edit Group A — Active checkbox flips (L10-12, L16, L21-22, L40-42).** Flip `[ ]` to `[x]` only on the listed lines. Do NOT flip VERIFY-02 (L17) — it stays unchecked because Phase 21 is still pending.

    Find each pattern, replace `[ ]` → `[x]`:
    - L10: `- [ ] **PUBLISH-01**:` → `- [x] **PUBLISH-01**:`
    - L11: `- [ ] **PUBLISH-02**:` → `- [x] **PUBLISH-02**:`
    - L12: `- [ ] **PUBLISH-03**:` → `- [x] **PUBLISH-03**:`
    - L16: `- [ ] **VERIFY-01**:` → `- [x] **VERIFY-01**:`
    - **L17: leave `- [ ] **VERIFY-02**:` UNCHANGED** (D-09 — VERIFY-02 closure routed to Phase 21, still pending)
    - L21: `- [ ] **RELEASE-01**:` → `- [x] **RELEASE-01**:`
    - L22: `- [ ] **RELEASE-02**:` → `- [x] **RELEASE-02**:`
    - L40: `- [ ] **API-06**:` → `- [x] **API-06**:`
    - L41: `- [ ] **API-07**:` → `- [x] **API-07**:`
    - L42: `- [ ] **API-08**:` → `- [x] **API-08**:`

    **Edit Group B — Bold-span literal-newline fixes (L26, L28, L35, L46, L48, L50).** The current pattern is `**ID\n**:` — a literal newline inside the bold span. Replace with `**ID**:`.

    The six broken rows (per 20-CONTEXT.md D-08):
    - L26 `**API-01\n**:` → `**API-01**:`
    - L28 `**API-02\n**:` → `**API-02**:`
    - L35 `**API-05\n**:` → `**API-05**:`
    - L46 `**API-09\n**:` → `**API-09**:`
    - L48 `**API-10\n**:` → `**API-10**:`
    - L50 `**API-11\n**:` → `**API-11**:`

    Use the `Edit` tool with multi-line `old_string` capturing the broken bold opening + newline + `**:` and replacing with the inline form. Example for L26:
    ```
    old_string:
    - [x] **API-01
    **: Maintainer has resolved the `verify_upload/2` vs `complete_multipart_upload/3` vocabulary inconsistency — either renamed to a consistent verb or explicitly documented as distinct operations

    new_string:
    - [x] **API-01**: Maintainer has resolved the `verify_upload/2` vs `complete_multipart_upload/3` vocabulary inconsistency — either renamed to a consistent verb or explicitly documented as distinct operations
    ```

    Repeat for L28, L35, L46, L48, L50 (note that some rows already have `[x]` from Edit Group A — preserve whatever the box state is after Group A executes).

    **Edit Group C — Traceability table flips (L85-91 and L97-99).** Replace the Status column for the listed rows from `Pending` to `Complete`. VERIFY-02 (L89) stays Pending (D-09).

    For each of these rows in the Traceability table, replace `| Pending |` (final column) with `| Complete |`:
    - L85: `| PUBLISH-01 | Phase 15 → Phase 20 (closure) | Pending |` → `| PUBLISH-01 | Phase 15 → Phase 20 (closure) | Complete |`
    - L86: `| PUBLISH-02 | Phase 15 → Phase 20 (closure) | Pending |` → `| PUBLISH-02 | Phase 15 → Phase 20 (closure) | Complete |`
    - L87: `| PUBLISH-03 | Phase 16 → Phase 20 (closure) | Pending |` → `| PUBLISH-03 | Phase 16 → Phase 20 (closure) | Complete |`
    - L88: `| VERIFY-01 | Phase 16 → Phase 20 (closure) | Pending |` → `| VERIFY-01 | Phase 16 → Phase 20 (closure) | Complete |`
    - **L89: VERIFY-02 row stays `Pending` (D-09)**
    - L90: `| RELEASE-01 | Phase 16 → Phase 20 (closure) | Pending |` → `| RELEASE-01 | Phase 16 → Phase 20 (closure) | Complete |`
    - L91: `| RELEASE-02 | Phase 16 → Phase 20 (closure) | Pending |` → `| RELEASE-02 | Phase 16 → Phase 20 (closure) | Complete |`
    - L97: `| API-06 | Phase 18 | Pending |` → `| API-06 | Phase 18 | Complete |`
    - L98: `| API-07 | Phase 18 | Pending |` → `| API-07 | Phase 18 | Complete |`
    - L99: `| API-08 | Phase 18 | Pending |` → `| API-08 | Phase 18 | Complete |`

    **Edit Group D — Coverage note (L108).** Replace:

    Find:
    ```
    - Pending closure: 7 (routed to Phase 20 process/metadata closure and Phase 21 hexdocs reachability probe per `.planning/v1.3-MILESTONE-AUDIT.md`)
    ```

    Replace with:
    ```
    - Pending closure: 1 (VERIFY-02 routed to Phase 21 hexdocs reachability probe per `.planning/v1.3-MILESTONE-AUDIT.md` G4 — Phase 20 closed PUBLISH-01/02/03, VERIFY-01, RELEASE-01/02 process gaps and API-06/07/08 traceability flips on 2026-05-01)
    ```

    **Edit Group E — Footer last-updated line (L112).** Update the trailing metadata:

    Find:
    ```
    *Last updated: 2026-05-01 — gap closure phases 20/21 assigned after v1.3 milestone audit*
    ```

    Replace with:
    ```
    *Last updated: 2026-05-01 — Phase 20 closed v1.3 process/metadata gaps; VERIFY-02 routed to Phase 21*
    ```

    Note (D-09): VERIFY-02 explicitly stays at `[ ]` and `Pending` because Phase 21 has not yet shipped. Once Phase 21 lands, a future docs commit will flip both. This plan does NOT preemptively close VERIFY-02.
  </action>
  <verify>
    <automated>grep -E "^- \[x\] \*\*PUBLISH-01\*\*:" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^- \[x\] \*\*PUBLISH-02\*\*:" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^- \[x\] \*\*PUBLISH-03\*\*:" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^- \[x\] \*\*VERIFY-01\*\*:" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^- \[ \] \*\*VERIFY-02\*\*:" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^- \[x\] \*\*RELEASE-01\*\*:" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^- \[x\] \*\*RELEASE-02\*\*:" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^- \[x\] \*\*API-06\*\*:" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^- \[x\] \*\*API-07\*\*:" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^- \[x\] \*\*API-08\*\*:" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^- \[x\] \*\*API-01\*\*:" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^- \[x\] \*\*API-02\*\*:" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^- \[x\] \*\*API-05\*\*:" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^- \[x\] \*\*API-09\*\*:" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^- \[x\] \*\*API-10\*\*:" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^- \[x\] \*\*API-11\*\*:" .planning/REQUIREMENTS.md &amp;&amp; ! grep -Pzo '\*\*[A-Z]+-[0-9]+\n\*\*:' .planning/REQUIREMENTS.md &amp;&amp; grep -E "^\| PUBLISH-01.*\| Complete \|" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^\| PUBLISH-02.*\| Complete \|" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^\| PUBLISH-03.*\| Complete \|" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^\| VERIFY-01.*\| Complete \|" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^\| VERIFY-02.*\| Pending \|" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^\| RELEASE-01.*\| Complete \|" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^\| RELEASE-02.*\| Complete \|" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^\| API-06.*\| Complete \|" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^\| API-07.*\| Complete \|" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^\| API-08.*\| Complete \|" .planning/REQUIREMENTS.md &amp;&amp; grep -q "Pending closure: 1" .planning/REQUIREMENTS.md &amp;&amp; grep -q "Phase 20 closed v1.3 process/metadata gaps" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - All 10 Active-section checkbox flips applied (PUBLISH-01/02/03, VERIFY-01, RELEASE-01/02, API-06/07/08).
    - VERIFY-02 active-section checkbox stays `[ ]` (NOT flipped — D-09).
    - Negative grep gate: NO line in REQUIREMENTS.md matches the broken pattern `**ID\n**:` (zero matches when the regex `\*\*[A-Z]+-[0-9]+\n\*\*:` is run with `grep -Pzo`).
    - All 9 traceability rows flipped to Complete (PUBLISH-01/02/03, VERIFY-01, RELEASE-01/02, API-06/07/08).
    - VERIFY-02 traceability row stays Pending.
    - Coverage note states "Pending closure: 1".
    - Last-updated footer contains the literal string `Phase 20 closed v1.3 process/metadata gaps` (asserts the new footer string, not just a Phase 20 mention).
  </acceptance_criteria>
  <done>REQUIREMENTS.md is the single source of truth for v1.3 closure: 17 of 18 requirements show Complete in both checkboxes and traceability table; VERIFY-02 explicitly stays Pending pending Phase 21; bold-span artifacts are eliminated; coverage note reflects 1 remaining.</done>
</task>

<task type="auto">
  <name>Task 6: Atomic docs commit (D-16, atomic-commit discipline)</name>
  <files>(none — git operation only)</files>
  <read_first>
    - (run `git status` and `git diff --stat` first to confirm only the five expected files are modified by this plan)
  </read_first>
  <action>
    Stage exactly the five files modified by Tasks 1-5 and create a single commit:

    ```bash
    git add \
      .planning/phases/15-ci-integrity-and-publish-preflight/15-VERIFICATION.md \
      .planning/phases/16-live-publish-execution-and-post-publish-verification/16-VERIFICATION.md \
      .planning/phases/16-live-publish-execution-and-post-publish-verification/16-01-SUMMARY.md \
      .planning/phases/16-live-publish-execution-and-post-publish-verification/16-02-SUMMARY.md \
      .planning/REQUIREMENTS.md

    git commit -m "$(cat <<'EOF'
    docs(20): retrofit 15/16 VERIFICATION.md, fix 16 SUMMARY frontmatter, reconcile REQUIREMENTS.md

    Closes G1, G2, G3 from .planning/v1.3-MILESTONE-AUDIT.md and TD-Req traceability inconsistency.

    - Author 15-VERIFICATION.md retroactively for PUBLISH-01, PUBLISH-02 (G1)
    - Author 16-VERIFICATION.md retroactively for PUBLISH-03, VERIFY-01, RELEASE-01, RELEASE-02; VERIFY-02 marked SATISFIED (functional) with forward_reference: phase-21 (G2, D-03)
    - 16-01-SUMMARY.md requirements_completed += VERIFY-01 (D-05); strike stale "remain uncommitted" claim (D-07)
    - 16-02-SUMMARY.md requirements_completed += VERIFY-02, RELEASE-02 (D-06)
    - REQUIREMENTS.md: flip 9 traceability rows + 10 Active checkboxes to Complete (D-08), keep VERIFY-02 Pending pending Phase 21 (D-09), fix 6 bold-span literal-newline artifacts, update coverage note from "Pending closure: 7" to "Pending closure: 1"

    /gsd-audit-milestone v1.3 should now report passed.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```

    Do NOT include the working-tree changes on `lib/rindle/live_view.ex` or `test/rindle/live_view_test.exs` in this commit (those belong to Plan 20-02, atomic-commit discipline per D-16). Verify with `git diff --cached --stat` before committing — staged changes must include exactly the five expected files. If `lib/rindle/live_view.ex` or `test/rindle/live_view_test.exs` is staged, unstage with `git restore --staged` and re-stage only the five docs files.
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -q "docs(20)" &amp;&amp; git show HEAD --stat | grep -q "15-VERIFICATION.md" &amp;&amp; git show HEAD --stat | grep -q "16-VERIFICATION.md" &amp;&amp; git show HEAD --stat | grep -q "16-01-SUMMARY.md" &amp;&amp; git show HEAD --stat | grep -q "16-02-SUMMARY.md" &amp;&amp; git show HEAD --stat | grep -q "REQUIREMENTS.md" &amp;&amp; ! git show HEAD --stat | grep -q "lib/rindle/live_view.ex" &amp;&amp; ! git show HEAD --stat | grep -q "test/rindle/live_view_test.exs"</automated>
  </verify>
  <acceptance_criteria>
    - HEAD commit message starts with `docs(20):`.
    - Commit touches exactly five files (the two new VERIFICATION.md, two SUMMARY.md edits, REQUIREMENTS.md).
    - Commit does NOT include `lib/rindle/live_view.ex` or `test/rindle/live_view_test.exs` (those go in 20-02).
    - Working tree still shows the LiveView diff unstaged (Plan 20-02 will commit it).
  </acceptance_criteria>
  <done>Single atomic docs commit lands all five edits; working tree remaining diff is exactly the LiveView corrective patch awaiting Plan 20-02.</done>
</task>

</tasks>

<threat_model>
Documentation-only changes; no new attack surface introduced. All five files are planning metadata or docs. No code paths, dependencies, env vars, or external integrations modified.
</threat_model>

<verification>
After all six tasks complete, the milestone audit re-run preconditions are satisfied:

```bash
# G1, G2 closed:
test -f .planning/phases/15-ci-integrity-and-publish-preflight/15-VERIFICATION.md
test -f .planning/phases/16-live-publish-execution-and-post-publish-verification/16-VERIFICATION.md

# G3 closed (3-source consistency):
grep -q "VERIFY-01" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-01-SUMMARY.md
grep -q "VERIFY-02" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-02-SUMMARY.md
grep -q "RELEASE-02" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-02-SUMMARY.md

# TD-Req closed:
grep -E "^\| PUBLISH-01.*\| Complete \|" .planning/REQUIREMENTS.md
grep -q "Pending closure: 1" .planning/REQUIREMENTS.md

# VERIFY-02 NOT prematurely closed (G4 stays as Phase 21's job):
grep -E "^- \[ \] \*\*VERIFY-02\*\*:" .planning/REQUIREMENTS.md
grep -E "^\| VERIFY-02.*\| Pending \|" .planning/REQUIREMENTS.md
grep -q "phase-21" .planning/phases/16-live-publish-execution-and-post-publish-verification/16-VERIFICATION.md
```

After this plan ships, `/gsd-audit-milestone v1.3` should report `passed`.
</verification>

<success_criteria>
- 15-VERIFICATION.md and 16-VERIFICATION.md exist, both with `status: passed` frontmatter and the required citations.
- 16-VERIFICATION.md marks VERIFY-02 as SATISFIED (functional) with forward_reference: phase-21 — never as 'partial'.
- 16-01-SUMMARY.md `requirements_completed` = [PUBLISH-03, RELEASE-01, VERIFY-01]; "remain uncommitted" claim is gone.
- 16-02-SUMMARY.md `requirements_completed` = [PUBLISH-03, RELEASE-01, VERIFY-02, RELEASE-02].
- REQUIREMENTS.md: 17/18 v1.3 requirements show Complete; only VERIFY-02 stays Pending; zero broken-bold-span instances; coverage note reads "Pending closure: 1".
- Single `docs(20):` commit lands these atomically — LiveView changes remain unstaged for Plan 20-02.
</success_criteria>

<output>
After completion, create `.planning/phases/20-v1.3-verification-and-metadata-closure/20-01-SUMMARY.md` summarising the six edits + the resulting milestone-audit closure status.
</output>

---
phase: 14
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "10-VALIDATION.md frontmatter status is 'complete' (not 'ready')"
    - "10-VALIDATION.md frontmatter wave_0_complete is 'true' (not 'false')"
    - "All three Per-Task Map rows show File Exists '✅' (no '❌ W0' remaining for Phase 10 tasks)"
    - "All three Per-Task Map rows show Status '✅ green' (no '⬜ pending' remaining for Phase 10 tasks)"
    - "All four Wave 0 checklist items are checked '[x]' (no unchecked '[ ]' items remaining)"
    - "Quick run command line references both release_docs_parity_test.exs and package_metadata_test.exs (the 'plan-owned targeted ExUnit file once it exists' hedge is removed)"
    - "All six Validation Sign-Off checkboxes are '[x]' (the four currently '[ ]' are flipped; the two already '[x]' are preserved)"
    - "Approval line reads 'approved' (not 'pending')"
  artifacts:
    - path: ".planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md"
      provides: "Phase 10 Nyquist validation artifact at completed, evidence-backed state"
      contains: "wave_0_complete: true"
  key_links:
    - from: ".planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md"
      to: ".planning/milestones/v1.2-phases/10-publish-readiness/10-VERIFICATION.md"
      via: "evidence cross-reference (existing test files, behavioral spot-check results)"
      pattern: "wave_0_complete: true"
---

<objective>
Bring `.planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md` from
its current `status: ready` / `wave_0_complete: false` / pending-approval state to
the fully-completed end state matching the only existing analog
(`12-VALIDATION.md`).

Purpose: The v1.2 milestone audit (`v1.2-MILESTONE-AUDIT.md`, 2026-04-29) returned
`tech_debt` rather than `passed` partly because Phase 10's VALIDATION still shows
draft Nyquist state even though every referenced test file exists, every
behavioral spot-check passed in `10-VERIFICATION.md`, and Phase 10 itself is
marked complete in ROADMAP.md and STATE.md. This plan closes that residue.

Output: A single edited `10-VALIDATION.md` whose frontmatter, Per-Task
Verification Map, Wave 0 checklist, Quick-Run command line, and Validation
Sign-Off all reflect the verified evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-validation-closure-for-publish-milestone/14-CONTEXT.md
@.planning/phases/14-validation-closure-for-publish-milestone/14-RESEARCH.md
@.planning/phases/14-validation-closure-for-publish-milestone/14-PATTERNS.md
@.planning/phases/14-validation-closure-for-publish-milestone/14-VALIDATION.md

# Source-of-truth files the executor MUST treat as authoritative evidence
@.planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md
@.planning/milestones/v1.2-phases/10-publish-readiness/10-VERIFICATION.md
@.planning/milestones/v1.2-phases/10-publish-readiness/10-01-SUMMARY.md
@.planning/milestones/v1.2-phases/10-publish-readiness/10-02-SUMMARY.md

# Analog file — the only project file already at the target end state
@.planning/phases/12-public-verification-and-release-operations/12-VALIDATION.md

<interfaces>
<!-- Stable shape contracts. Executor edits text per these patterns; do not invent new field names. -->

Frontmatter schema (top of every VALIDATION.md): a YAML block with these fields:
- `phase` (int)
- `slug` (kebab-case)
- `status` (one of: draft, ready, complete; target value: complete)
- `nyquist_compliant` (bool; already true)
- `wave_0_complete` (bool; target value: true)
- `created` (YYYY-MM-DD)

Per-Task Map columns (pipe-delimited markdown table, header is row 1):
`Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status`

Status legend (preserved verbatim, do not modify the legend line):
`*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*`

Sign-Off checkbox lines (markdown unordered list, preserve wording):
`- [ ] <text>` or `- [x] <text>`

Approval line (last line of Validation Sign-Off):
`**Approval:** <pending | approved | revised> [date]`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Phase 10 VALIDATION frontmatter, Per-Task Map, Wave 0 checklist, and Quick-Run command</name>
  <files>.planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md (the file being edited — read in full to see current stale state)
    - .planning/milestones/v1.2-phases/10-publish-readiness/10-VERIFICATION.md (authoritative evidence — Behavioral Spot-Checks confirm 4 tests/0 failures for both files)
    - .planning/milestones/v1.2-phases/10-publish-readiness/10-01-SUMMARY.md (RELEASE-04 completion evidence)
    - .planning/milestones/v1.2-phases/10-publish-readiness/10-02-SUMMARY.md (RELEASE-05 completion evidence; warning cleanup, release_preflight.sh wiring)
    - .planning/phases/12-public-verification-and-release-operations/12-VALIDATION.md (analog at target end state — copy frontmatter pattern)
    - .planning/phases/14-validation-closure-for-publish-milestone/14-PATTERNS.md (exact diff specification)
  </read_first>
  <action>
    Make EXACTLY these edits to `.planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md`. Do not modify any other content. Use the Edit tool, not Write — this preserves untouched sections.

    **Edit 1 — Frontmatter line 4:** change `status: ready` to `status: complete`

    **Edit 2 — Frontmatter line 6:** change `wave_0_complete: false` to `wave_0_complete: true`

    **Edit 3 — Test Infrastructure table, Quick run command row (line 22):**
    Replace the cell value
    `` `mix test test/install_smoke/docs_parity_test.exs` (and use the plan-owned targeted ExUnit file once it exists) ``
    with
    `` `mix test test/install_smoke/release_docs_parity_test.exs` or `mix test test/install_smoke/package_metadata_test.exs` ``

    **Edit 4 — Per-Task Verification Map, row 10-01-01 (line 41):** in the last two columns, change `❌ W0` to `✅` and change `⬜ pending` to `✅ green`. Preserve every other column verbatim (Task ID, Plan, Wave, Requirement RELEASE-04, Threat Ref T-10-01, Secure Behavior text, Test Type, Automated Command).

    **Edit 5 — Per-Task Verification Map, row 10-02-01 (line 42):** in the last two columns, change `❌ W0` to `✅` and change `⬜ pending` to `✅ green`. Preserve every other column verbatim.

    **Edit 6 — Per-Task Verification Map, row 10-02-02 (line 43):** in the last two columns, change `❌ W0` to `✅` and change `⬜ pending` to `✅ green`. Preserve every other column verbatim (note this row's Threat Ref is `T-10-05 / T-10-06`).

    **Edit 7 — Wave 0 Requirements list (lines 51–54):** flip every `[ ]` to `[x]`. Do NOT alter the trailing description text on any of the four items. The four items (in order) are:
    1. `test/install_smoke/release_docs_parity_test.exs` — verify maintainer release guide presence plus key owner/versioning/checklist instructions for `RELEASE-04`
    2. `test/install_smoke/package_metadata_test.exs` — assert unpacked `hex_metadata.config` and exact package file expectations for `RELEASE-05`
    3. Release workflow/build command for `mix docs --warnings-as-errors`
    4. Warning cleanup for the current docs warning around `Rindle.LiveView.allow_upload/4`

    Rationale citations (do NOT add these to the file — they are for the executor's own verification): RELEASE-04 satisfied per `10-01-SUMMARY.md` `requirements-completed: [RELEASE-04]`; RELEASE-05 satisfied per `10-02-SUMMARY.md` `requirements-completed: [RELEASE-05]`; release_preflight.sh wiring per `10-VERIFICATION.md` Required Artifacts row for `.github/workflows/release.yml`; LiveView warning cleanup per `10-02-SUMMARY.md` key-files modified.

    **Sign-Off and Approval line edits are deferred to Task 2** — keep this task focused on body content.

    Do not change: section headers, the `*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*` legend line, the Sampling Rate section, the Manual-Only Verifications section, or the Validation Sign-Off section.
  </action>
  <acceptance_criteria>
    - `grep -c "^status: complete$" .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md` outputs `1`
    - `grep -c "^wave_0_complete: true$" .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md` outputs `1`
    - `grep -c "status: ready" .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md` outputs `0`
    - `grep -c "wave_0_complete: false" .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md` outputs `0`
    - `grep -v '^\*Status:' .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md | grep -c "❌ W0"` outputs `0`
    - `grep -v '^\*Status:' .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md | grep -c "⬜ pending"` outputs `0`
    - `grep -c "✅ green" .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md` outputs `3` or more (three Per-Task Map rows; legend line excluded by grep filter)
    - `grep -c "release_docs_parity_test.exs" .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md` outputs `2` or more (Per-Task Map row + Quick run command + Wave 0 checklist all reference it)
    - `grep -c "package_metadata_test.exs" .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md` outputs `2` or more
    - `grep -c "plan-owned targeted ExUnit file once it exists" .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md` outputs `0` (the stale hedge is gone)
    - `grep -cE '^- \[ \] `test/install_smoke/release_docs_parity_test\.exs`' .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md` outputs `0`
    - `grep -cE '^- \[ \] `test/install_smoke/package_metadata_test\.exs`' .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md` outputs `0`
    - `grep -cE '^- \[x\] `test/install_smoke/release_docs_parity_test\.exs`' .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md` outputs `1`
    - `grep -cE '^- \[x\] `test/install_smoke/package_metadata_test\.exs`' .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md` outputs `1`
    - `mix test test/install_smoke/release_docs_parity_test.exs` exits 0 (independent re-confirmation that the cited evidence is still valid; do NOT change file based on output)
    - `mix test test/install_smoke/package_metadata_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^status: complete$" .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md | grep -q "^1$" && grep -c "^wave_0_complete: true$" .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md | grep -q "^1$" && [ "$(grep -v '^\*Status:' .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md | grep -c '❌ W0')" = "0" ] && [ "$(grep -v '^\*Status:' .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md | grep -c '⬜ pending')" = "0" ]</automated>
  </verify>
  <done>
    Frontmatter, Per-Task Map, Wave 0 checklist, and Quick-Run command line in
    `10-VALIDATION.md` reflect the completed state. All grep gates above pass.
    Sign-Off section and Approval line are still pending — Task 2 will close them.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Phase 10 Validation Sign-Off checkboxes and Approval line</name>
  <files>.planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md (re-read after Task 1 — confirm body changes are present before editing sign-off)
    - .planning/phases/12-public-verification-and-release-operations/12-VALIDATION.md (analog showing the target Sign-Off + Approval pattern)
    - .planning/phases/14-validation-closure-for-publish-milestone/14-PATTERNS.md (Approval line spec — use literal token `approved`)
  </read_first>
  <action>
    Apply EXACTLY these edits to the Validation Sign-Off section of
    `.planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md`. Use the
    Edit tool. Preserve every checkbox label verbatim — change ONLY the marker.

    **Edit 1 — line 68:** change `- [ ] All tasks have \`<automated>\` verify or Wave 0 dependencies` to `- [x] All tasks have \`<automated>\` verify or Wave 0 dependencies`

    **Edit 2 — line 69:** change `- [ ] Sampling continuity: no 3 consecutive tasks without automated verify` to `- [x] Sampling continuity: no 3 consecutive tasks without automated verify`

    **Edit 3 — line 70:** change `- [ ] Wave 0 covers all MISSING references` to `- [x] Wave 0 covers all MISSING references`

    **Edit 4 — line 71:** change `- [ ] No watch-mode flags` to `- [x] No watch-mode flags`

    **Edit 5 — line 72:** preserve as-is (`- [x] Feedback latency < 30s for task-level probes` is already checked)

    **Edit 6 — line 73:** preserve as-is (`- [x] \`nyquist_compliant: true\` set in frontmatter` is already checked)

    **Edit 7 — Approval line (line 75):** change `**Approval:** pending` to `**Approval:** approved`

    Do NOT add a date suffix to the Approval line — the analog (`12-VALIDATION.md` line 62) uses `approved 2026-04-28`, but Phase 11's plan in this same phase will use bare `approved` per `14-PATTERNS.md`. Use bare `approved` here for consistency across both Phase 14 plans. If a future audit prefers dated approvals, that is a follow-up edit, not Phase 14's scope.

    Do not change: any other section, any other line, the wording of any checkbox label.
  </action>
  <acceptance_criteria>
    - `grep -cE '^- \[x\] All tasks have' .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md` outputs `1`
    - `grep -cE '^- \[x\] Sampling continuity:' .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md` outputs `1`
    - `grep -cE '^- \[x\] Wave 0 covers all MISSING references$' .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md` outputs `1`
    - `grep -cE '^- \[x\] No watch-mode flags$' .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md` outputs `1`
    - `grep -cE '^- \[x\] Feedback latency < 30s' .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md` outputs `1`
    - `grep -cE '^- \[x\] `nyquist_compliant: true`' .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md` outputs `1`
    - `grep -cE '^- \[ \]' .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md` outputs `0` (entire file has zero unchecked boxes — covers Sign-Off and Wave 0 sections together)
    - `grep -cE '^\*\*Approval:\*\* approved$' .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md` outputs `1`
    - `grep -cE '^\*\*Approval:\*\* pending$' .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md` outputs `0`
  </acceptance_criteria>
  <verify>
    <automated>[ "$(grep -cE '^- \[ \]' .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md)" = "0" ] && [ "$(grep -cE '^\*\*Approval:\*\* approved$' .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md)" = "1" ] && [ "$(grep -cE '^\*\*Approval:\*\* pending$' .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md)" = "0" ]</automated>
  </verify>
  <done>
    All six Validation Sign-Off checkboxes are `[x]`, no `[ ]` remains anywhere in
    the file, and the Approval line reads `**Approval:** approved`. Phase 10
    VALIDATION artifact is now at the completed, evidence-backed end state and
    matches the analog `12-VALIDATION.md` for the audit's Nyquist closure check.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan modifies only `.planning/` markdown. No data flow, no code path, no auth surface, no public artifact, no network call. The edits never cross a trust boundary because there is no boundary to cross — the file is read by humans and the milestone audit tool, both of which already operate inside the developer trust zone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Spoofing | 10-VALIDATION.md content | accept | No identity claim. The file is checked into git under the maintainer's signed commit; provenance is enforced by the existing repo identity model, not by this plan. |
| T-14-01-02 | Tampering | 10-VALIDATION.md content | accept | Out-of-band tampering would be caught by `git diff` review before merge. No additional in-plan mitigation needed. |
| T-14-01-03 | Repudiation | git commit author | accept | Standard repo signing/blame applies; not unique to this plan. |
| T-14-01-04 | Information Disclosure | n/a | accept | The file contains no secrets, no PII, no credentials. It is already public in the repo. |
| T-14-01-05 | Denial of Service | n/a | accept | A markdown file cannot be DoS'd via content edits; downstream audit tooling is local-only. |
| T-14-01-06 | Elevation of Privilege | audit tooling | accept | The audit reads the file but does not execute any field as code. Even maliciously misleading content would only mislead a human reviewer, not gain privilege. |

**Disposition rationale:** ASVS L1 does not apply to inert documentation under
`.planning/`. Every threat is `accept` because no realistic mitigation maps onto
a markdown edit. Security enforcement is satisfied by recording the analysis,
not by inventing controls for a non-boundary.
</threat_model>

<verification>
After both tasks complete, the file should pass all of these in a single shell:

```bash
F=.planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md
[ "$(grep -c '^status: complete$' $F)" = "1" ] && \
  [ "$(grep -c '^wave_0_complete: true$' $F)" = "1" ] && \
  [ "$(grep -v '^\*Status:' $F | grep -c '❌ W0')" = "0" ] && \
  [ "$(grep -v '^\*Status:' $F | grep -c '⬜ pending')" = "0" ] && \
  [ "$(grep -cE '^- \[ \]' $F)" = "0" ] && \
  [ "$(grep -cE '^\*\*Approval:\*\* approved$' $F)" = "1" ] && \
  [ "$(grep -c 'plan-owned targeted ExUnit file once it exists' $F)" = "0" ] && \
  echo "Phase 10 VALIDATION: complete"
```

Re-run the cited evidence as a sanity check (do not edit based on results):
```bash
mix test test/install_smoke/release_docs_parity_test.exs
mix test test/install_smoke/package_metadata_test.exs
```

Both should exit 0 — confirming the evidence cited in the now-complete VALIDATION
file is still valid.
</verification>

<success_criteria>
1. `10-VALIDATION.md` frontmatter shows `status: complete` and `wave_0_complete: true`
2. All three Per-Task Map rows show `✅` File Exists and `✅ green` Status
3. All four Wave 0 checklist items are `[x]`
4. Quick-Run command line references the actual targeted test files (no
   "once it exists" hedge)
5. All six Validation Sign-Off checkboxes are `[x]`
6. Approval line reads `**Approval:** approved`
7. Cited test files (`release_docs_parity_test.exs`, `package_metadata_test.exs`)
   still pass when re-run
</success_criteria>

<output>
After completion, create `.planning/phases/14-validation-closure-for-publish-milestone/14-01-SUMMARY.md`
</output>

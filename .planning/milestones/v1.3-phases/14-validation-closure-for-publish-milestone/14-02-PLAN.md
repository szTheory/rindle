---
phase: 14
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "11-VALIDATION.md frontmatter status is 'complete' (not 'draft')"
    - "11-VALIDATION.md frontmatter wave_0_complete is 'true' (not 'false')"
    - "Per-Task Map row 11-01-01 shows Status '✅ green' (was '⬜ pending')"
    - "Per-Task Map row 11-02-01 shows File Exists '✅' (was '❌ W0') and Status '✅ green' (was '⬜ pending')"
    - "Wave 0 checklist item for scripts/assert_version_match.sh is '[x]' and references the verified evidence"
    - "Manual-Only Verifications row notes that the Hex API Key check has been superseded by automated CI dry-run"
    - "Approval line reads 'approved' (not 'pending'); the six Sign-Off checkboxes remain '[x]' as they already are"
  artifacts:
    - path: ".planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md"
      provides: "Phase 11 Nyquist validation artifact at completed, evidence-backed state"
      contains: "wave_0_complete: true"
  key_links:
    - from: ".planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md"
      to: ".planning/milestones/v1.2-phases/11-protected-publish-automation/11-VERIFICATION.md"
      via: "evidence cross-reference (assert_version_match.sh existence, GHA env config, CI dry-run automation)"
      pattern: "wave_0_complete: true"
    - from: ".planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md"
      to: ".planning/milestones/v1.2-phases/11-protected-publish-automation/11-03-SUMMARY.md"
      via: "Manual-Only supersession reference (CI dry-run automation cited as the replacement for the previously manual Hex API Key check)"
      pattern: "Superseded"
---

<objective>
Bring `.planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md`
from its current `status: draft` / `wave_0_complete: false` / pending-approval
state to the fully-completed end state matching the analog `12-VALIDATION.md`.

Purpose: The v1.2 milestone audit (`v1.2-MILESTONE-AUDIT.md`, 2026-04-29) flagged
Phase 11's validation as `partial` because the frontmatter still says `draft`,
the Wave 0 item for `scripts/assert_version_match.sh` is unchecked, two Per-Task
Map rows still show `⬜ pending`, and the Manual-Only Verifications table still
lists the Hex API Key check that was actually superseded by Phase 11 Plan 03's
automated CI dry-run. Closing this residue is the second half of unblocking the
milestone audit's Nyquist closure.

Output: A single edited `11-VALIDATION.md` whose frontmatter, Per-Task
Verification Map, Wave 0 checklist, Manual-Only Verifications table, and
Approval line all reflect the verified evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-validation-closure-for-publish-milestone/14-CONTEXT.md
@.planning/phases/14-validation-closure-for-publish-milestone/14-RESEARCH.md
@.planning/phases/14-validation-closure-for-publish-milestone/14-PATTERNS.md
@.planning/phases/14-validation-closure-for-publish-milestone/14-VALIDATION.md

# Source-of-truth files the executor MUST treat as authoritative evidence
@.planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md
@.planning/milestones/v1.2-phases/11-protected-publish-automation/11-VERIFICATION.md
@.planning/milestones/v1.2-phases/11-protected-publish-automation/11-01-SUMMARY.md
@.planning/milestones/v1.2-phases/11-protected-publish-automation/11-02-SUMMARY.md
@.planning/milestones/v1.2-phases/11-protected-publish-automation/11-03-SUMMARY.md

# Analog file — the only project file already at the target end state
@.planning/phases/12-public-verification-and-release-operations/12-VALIDATION.md

<interfaces>
<!-- Stable shape contracts. Executor edits text per these patterns; do not invent new field names. -->

Frontmatter schema (top of every VALIDATION.md): a YAML block with these fields:
- `phase` (int)
- `slug` (kebab-case)
- `status` (one of: draft, ready, complete; target value: complete)
- `nyquist_compliant` (bool; already true)
- `wave_0_complete` (bool; target value: true)
- `created` (YYYY-MM-DD)

Per-Task Map columns (pipe-delimited markdown table, header is row 1):
`Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status`

Status legend (preserved verbatim):
`*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*`

Manual-Only Verifications columns:
`Behavior | Requirement | Why Manual | Test Instructions`

Approval line (last line of Validation Sign-Off):
`**Approval:** <pending | approved | revised> [date]`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Phase 11 VALIDATION frontmatter, Per-Task Map, Wave 0 checklist, and Manual-Only Verifications table</name>
  <files>.planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md (the file being edited — read in full to see current stale state)
    - .planning/milestones/v1.2-phases/11-protected-publish-automation/11-VERIFICATION.md (authoritative evidence — Behavioral Spot-Checks confirm `Version matches: 0.1.0-dev`; Observable Truths confirm GHA env wiring and `mix hex.publish --yes`; "Human Verification Required: None" confirms Manual-Only supersession)
    - .planning/milestones/v1.2-phases/11-protected-publish-automation/11-02-SUMMARY.md (assert_version_match.sh creation evidence)
    - .planning/milestones/v1.2-phases/11-protected-publish-automation/11-03-SUMMARY.md (CI dry-run automation evidence — supersedes the Manual-Only Hex API Key check)
    - .planning/phases/12-public-verification-and-release-operations/12-VALIDATION.md (analog at target end state)
    - .planning/phases/14-validation-closure-for-publish-milestone/14-PATTERNS.md (exact diff specification)
  </read_first>
  <action>
    Make EXACTLY these edits to `.planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md`. Use the Edit tool. Do not modify any other content.

    **Edit 1 — Frontmatter line 4:** change `status: draft` to `status: complete`

    **Edit 2 — Frontmatter line 6:** change `wave_0_complete: false` to `wave_0_complete: true`

    **Edit 3 — Per-Task Map row 11-01-01 (line 41):** change ONLY the final cell `⬜ pending` to `✅ green`. The File Exists column is already `✅` — preserve it. The Automated Command cell `(Verified by GHA environment config)` stays verbatim. Preserve every other column exactly as written (Task ID `11-01-01`, Plan `01`, Wave `1`, Requirement `RELEASE-06`, Threat Ref `—`, Secure Behavior `Hex publish via GHA`, Test Type `unit/workflow`).

    **Edit 4 — Per-Task Map row 11-02-01 (line 42):** change `❌ W0` to `✅` and change `⬜ pending` to `✅ green`. Preserve every other column verbatim (Requirement `RELEASE-07`, Secure Behavior `Abort on version mismatch`, Test Type `script`, Automated Command `` `bash scripts/assert_version_match.sh` ``).

    **Edit 5 — Wave 0 Requirements list (line 50):** replace the single bullet
    `- [ ] \`scripts/assert_version_match.sh\` — stubs for REQ-07`
    with
    `- [x] \`scripts/assert_version_match.sh\` — exists and executable; validated by \`11-VERIFICATION.md\` behavioral spot-check (\`Version matches: 0.1.0-dev\`)`

    **Edit 6 — Manual-Only Verifications table (lines 56–59):** replace the single data row
    `| Hex API Key | RELEASE-06 | GitHub Actions environment configuration cannot be verified locally | Check GitHub repository settings for environment "release" and secret "HEX_API_KEY" |`
    with
    `| Hex API Key | RELEASE-06 | Superseded — automated CI dry-run (\`mix hex.publish --dry-run --yes\`) in the \`package-consumer\` job verifies the publish path without a live secret; \`11-VERIFICATION.md\` confirms "Human Verification Required: None" | Verified automatically by CI per \`11-03-SUMMARY.md\` and \`11-VERIFICATION.md\` |`

    Preserve the table header row, the `## Manual-Only Verifications` heading, and the column order. Do not delete the row entirely — replacing the cell content keeps the audit trail explicit (a future reader sees that this check existed and was automated).

    **Approval line update is deferred to Task 2** — Task 1 stays focused on body content.

    Do not change: section headers, the `*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*` legend line, the Test Infrastructure section (the existing `bash scripts/assert_version_match.sh` quick-run is already accurate per `14-RESEARCH.md`), the Sampling Rate section, or the Validation Sign-Off section.
  </action>
  <acceptance_criteria>
    - `grep -c "^status: complete$" .planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md` outputs `1`
    - `grep -c "^wave_0_complete: true$" .planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md` outputs `1`
    - `grep -c "status: draft" .planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md` outputs `0`
    - `grep -c "wave_0_complete: false" .planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md` outputs `0`
    - `grep -v '^\*Status:' .planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md | grep -c "❌ W0"` outputs `0`
    - `grep -v '^\*Status:' .planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md | grep -c "⬜ pending"` outputs `0`
    - `grep -c "✅ green" .planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md` outputs `2` or more (two Per-Task Map rows)
    - `grep -cE '^- \[ \] `scripts/assert_version_match\.sh`' .planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md` outputs `0`
    - `grep -cE '^- \[x\] `scripts/assert_version_match\.sh`' .planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md` outputs `1`
    - `grep -c "stubs for REQ-07" .planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md` outputs `0` (the placeholder phrasing is gone)
    - `grep -c "Version matches: 0.1.0-dev" .planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md` outputs `1` (evidence citation now present in Wave 0 line)
    - `grep -c "Superseded" .planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md` outputs `1` (Manual-Only row updated)
    - `grep -c "11-03-SUMMARY.md" .planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md` outputs `1` (Manual-Only row cites automation evidence)
    - `grep -c "Check GitHub repository settings for environment" .planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md` outputs `0` (stale manual instruction is gone)
    - `bash scripts/assert_version_match.sh` exits 0 (independent re-confirmation that the cited evidence is still valid; do NOT change file based on output)
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md; [ "$(grep -c '^status: complete$' $F)" = "1" ] && [ "$(grep -c '^wave_0_complete: true$' $F)" = "1" ] && [ "$(grep -v '^\*Status:' $F | grep -c '❌ W0')" = "0" ] && [ "$(grep -v '^\*Status:' $F | grep -c '⬜ pending')" = "0" ] && [ "$(grep -c 'Superseded' $F)" = "1" ]</automated>
  </verify>
  <done>
    Frontmatter, both Per-Task Map rows, the Wave 0 checklist item, and the
    Manual-Only Verifications row in `11-VALIDATION.md` reflect the completed,
    evidence-backed state. All grep gates above pass. Approval line is still
    pending — Task 2 will close it.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Phase 11 Approval line</name>
  <files>.planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md (re-read after Task 1 — confirm body changes are present and the six Sign-Off checkboxes are still all `[x]` before flipping Approval)
    - .planning/phases/12-public-verification-and-release-operations/12-VALIDATION.md (analog showing target Approval line)
    - .planning/phases/14-validation-closure-for-publish-milestone/14-PATTERNS.md (Approval line spec — use literal token `approved` to match Plan 14-01)
  </read_first>
  <action>
    Apply EXACTLY this edit to the Validation Sign-Off section of
    `.planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md`. Use the Edit tool.

    **Edit 1 — Approval line (line 71):** change `**Approval:** pending` to `**Approval:** approved`

    Do not add a date suffix — match the bare-token form used in Plan 14-01 for
    Phase 10 so both Phase 14 deliverables share one Approval format. The six
    Validation Sign-Off checkboxes (lines 64–69) are already all `[x]` per the
    current file content; do not modify them. Do not change any other line.
  </action>
  <acceptance_criteria>
    - `grep -cE '^\*\*Approval:\*\* approved$' .planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md` outputs `1`
    - `grep -cE '^\*\*Approval:\*\* pending$' .planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md` outputs `0`
    - `grep -cE '^- \[x\] All tasks have' .planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md` outputs `1` (regression check — Sign-Off section was not damaged)
    - `grep -cE '^- \[x\] `nyquist_compliant: true`' .planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md` outputs `1` (regression check)
    - `grep -cE '^- \[ \]' .planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md` outputs `0` (no unchecked boxes anywhere)
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md; [ "$(grep -cE '^\*\*Approval:\*\* approved$' $F)" = "1" ] && [ "$(grep -cE '^\*\*Approval:\*\* pending$' $F)" = "0" ] && [ "$(grep -cE '^- \[ \]' $F)" = "0" ]</automated>
  </verify>
  <done>
    Approval line reads `**Approval:** approved`, no `[ ]` remains anywhere in
    the file, and the six previously-checked Sign-Off boxes are still `[x]`.
    Phase 11 VALIDATION artifact is now at the completed, evidence-backed end
    state and matches the analog `12-VALIDATION.md` for the audit's Nyquist
    closure check.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan modifies only `.planning/` markdown. No data flow, no code path, no auth surface, no public artifact, no network call. The Manual-Only Verifications row references — but does not invoke — the live publish credential `HEX_API_KEY`; the secret is never read or written by this edit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Spoofing | 11-VALIDATION.md content | accept | No identity claim. The file's provenance is enforced by the existing repo signing/identity model, not by this plan. |
| T-14-02-02 | Tampering | 11-VALIDATION.md content | accept | Out-of-band tampering would be caught by `git diff` review before merge. No additional in-plan mitigation. |
| T-14-02-03 | Repudiation | git commit author | accept | Standard repo blame applies; not unique to this plan. |
| T-14-02-04 | Information Disclosure | Manual-Only row referencing HEX_API_KEY | mitigate | The updated row references the secret name only (`HEX_API_KEY` is replaced with the description "live secret"); no key value, no environment-specific token, no path that could leak the credential. The pre-edit row already named the env/secret pair publicly — the new row is strictly less revealing, so disclosure surface is reduced, not added. |
| T-14-02-05 | Denial of Service | n/a | accept | A markdown file cannot be DoS'd via content edits; downstream audit tooling is local-only. |
| T-14-02-06 | Elevation of Privilege | audit tooling, CI | accept | The audit reads the file but does not execute any field as code. CI uses `release.yml` directly (already shipped); this edit does not alter CI behavior. |

**Disposition rationale:** ASVS L1 does not apply to inert documentation under
`.planning/`. The one mitigation (T-14-02-04) is satisfied by the action wording
itself — the executor instructions explicitly avoid quoting the secret value or
adding new credential references.
</threat_model>

<verification>
After both tasks complete, the file should pass all of these in a single shell:

```bash
F=.planning/milestones/v1.2-phases/11-protected-publish-automation/11-VALIDATION.md
[ "$(grep -c '^status: complete$' $F)" = "1" ] && \
  [ "$(grep -c '^wave_0_complete: true$' $F)" = "1" ] && \
  [ "$(grep -v '^\*Status:' $F | grep -c '❌ W0')" = "0" ] && \
  [ "$(grep -v '^\*Status:' $F | grep -c '⬜ pending')" = "0" ] && \
  [ "$(grep -cE '^- \[ \]' $F)" = "0" ] && \
  [ "$(grep -cE '^\*\*Approval:\*\* approved$' $F)" = "1" ] && \
  [ "$(grep -c 'Superseded' $F)" = "1" ] && \
  [ "$(grep -c 'stubs for REQ-07' $F)" = "0" ] && \
  echo "Phase 11 VALIDATION: complete"
```

Re-run the cited evidence as a sanity check (do not edit based on results):
```bash
bash scripts/assert_version_match.sh
```

Should exit 0 — confirming the evidence cited in the now-complete VALIDATION
file is still valid.
</verification>

<success_criteria>
1. `11-VALIDATION.md` frontmatter shows `status: complete` and `wave_0_complete: true`
2. Per-Task Map row 11-01-01 shows Status `✅ green` (File Exists already `✅`)
3. Per-Task Map row 11-02-01 shows File Exists `✅` and Status `✅ green`
4. Wave 0 checklist item for `scripts/assert_version_match.sh` is `[x]` and cites
   the verified `Version matches: 0.1.0-dev` spot-check evidence
5. Manual-Only Verifications row notes that the Hex API Key check has been
   superseded by the automated CI dry-run, citing `11-03-SUMMARY.md` and
   `11-VERIFICATION.md`
6. Approval line reads `**Approval:** approved`
7. The six Validation Sign-Off checkboxes remain `[x]`
8. `bash scripts/assert_version_match.sh` still exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/14-validation-closure-for-publish-milestone/14-02-SUMMARY.md`
</output>

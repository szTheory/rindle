---
phase: 21-verify-02-hexdocs-reachability-probe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release.yml
  - guides/release_publish.md
  - test/install_smoke/release_docs_parity_test.exs
  - test/install_smoke/package_metadata_test.exs
autonomous: true
requirements:
  - VERIFY-02
must_haves:
  truths:
    - "The post-publish public_verify job fails if https://hexdocs.pm/rindle/<version> is not reachable after redirect resolution within the bounded propagation window."
    - "CI enforces that the documented HexDocs probe remains part of the shipped release flow and preserves its placement and retry contract against drift."
    - "The maintainer runbook names the HexDocs probe as part of the shipped routine release sequence."
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "public_verify HexDocs reachability probe between index wait and package smoke"
      contains: "curl --fail --location"
    - path: "guides/release_publish.md"
      provides: "runbook parity for the HexDocs probe step and workflow contract"
      contains: "hexdocs.pm/rindle"
    - path: "test/install_smoke/release_docs_parity_test.exs"
      provides: "step-name and command parity assertions for the docs probe"
      contains: "hexdocs"
    - path: "test/install_smoke/package_metadata_test.exs"
      provides: "workflow wiring assertions for the docs probe in public_verify"
      contains: "curl --fail --location"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "guides/release_publish.md"
      via: "shared step name and workflow-contract command text"
      pattern: "hexdocs\\.pm/rindle|curl --fail --location"
    - from: "test/install_smoke/release_docs_parity_test.exs"
      to: ".github/workflows/release.yml"
      via: "literal step-name and command parity assertions"
      pattern: "Wait for Hex\\.pm index \\(post-publish\\).*Verify"
    - from: "test/install_smoke/package_metadata_test.exs"
      to: ".github/workflows/release.yml"
      via: "public_verify wiring assertions"
      pattern: "public_verify:|hexdocs\\.pm/rindle"
---

<objective>
Close `VERIFY-02` by making versioned HexDocs reachability a first-party, post-publish CI assertion instead of an inferred side effect of `mix hex.publish`.

Purpose: keep the public release contract observable and failure-driven in the same workflow lane that already proves Hex.pm indexing and package installability.
Output: one atomic release-contract update across the workflow, parity tests, and runbook.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-verify-02-hexdocs-reachability-probe/21-CONTEXT.md
@.planning/phases/21-verify-02-hexdocs-reachability-probe/21-RESEARCH.md
@.planning/phases/21-verify-02-hexdocs-reachability-probe/21-PATTERNS.md
@.github/workflows/release.yml
@guides/release_publish.md
@test/install_smoke/release_docs_parity_test.exs
@test/install_smoke/package_metadata_test.exs

<interfaces>
From `.github/workflows/release.yml`:
```yaml
- name: Wait for Hex.pm index (post-publish)
- name: Verify public Hex.pm artifact
```

From `guides/release_publish.md`:
```text
Run this sequence on every release after the inaugural publish:
## Release Workflow Contract
```

From `test/install_smoke/release_docs_parity_test.exs`:
```elixir
step_names = [
  "Release Please",
  "Wait for CI to finish green on release SHA",
  ...
]
commands = [
  "bash scripts/release_preflight.sh",
  ...
]
```

From `test/install_smoke/package_metadata_test.exs`:
```elixir
assert workflow =~ "public_verify:"
assert workflow =~ "name: Wait for Hex.pm index (post-publish)"
assert workflow =~ ~s(bash scripts/public_smoke.sh "$VERSION")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the bounded HexDocs reachability probe to public_verify</name>
  <files>.github/workflows/release.yml</files>
  <action>Add one new step inside `public_verify` per D-01 through D-04, placed after `Wait for Hex.pm index (post-publish)` and before `Verify public Hex.pm artifact`. Probe `https://hexdocs.pm/rindle/$VERSION` with a redirect-following GET using `curl --fail --location`, reuse the existing 5-minute / 15-second retry posture already trusted for `mix hex.info`, and fail with a clear terminal message when the final response never becomes 2xx. Keep the probe inline in YAML, keep `VERSION: ${{ needs.publish.outputs.release_version }}` as the input, and do not broaden scope into `scripts/public_smoke.sh`, helper-script extraction, or general workflow cleanup.</action>
  <verify>
    <automated>rg -n "name: .*hexdocs|curl --fail --location|https://hexdocs.pm/rindle/\\$VERSION|DEADLINE=\\(\\( SECONDS \\+ 300 \\)\\)|sleep 15|Release blocked: HexDocs did not serve" .github/workflows/release.yml</automated>
  </verify>
  <done>`public_verify` contains a dedicated HexDocs probe step at the required location, and the step uses the bounded retry contract required by D-01 through D-04.</done>
</task>

<task type="auto">
  <name>Task 2: Sync the maintainer runbook to the shipped docs probe contract</name>
  <files>guides/release_publish.md</files>
  <action>Update the release runbook per D-06 so the routine release step list and `Release Workflow Contract` both mention the HexDocs reachability probe by the exact workflow step name and concrete probe behavior. Keep the change in the existing parity style: factual, imperative, and narrowly scoped to the new probe. Do not expand into broader release-history cleanup, rollback edits, or unrelated prose refactors.</action>
  <verify>
    <automated>rg -n "hexdocs\\.pm/rindle|curl --fail --location|Wait for Hex\\.pm index \\(post-publish\\)|Verify public Hex\\.pm artifact" guides/release_publish.md</automated>
  </verify>
  <done>The runbook names the HexDocs probe in the routine release sequence and mirrors the shipped workflow contract closely enough for the parity harness to guard drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend install-smoke parity tests to lock the probe in place</name>
  <files>test/install_smoke/release_docs_parity_test.exs, test/install_smoke/package_metadata_test.exs</files>
  <behavior>
    - Test 1: The release guide step-name parity list includes the new HexDocs probe step and matches the workflow literal.
    - Test 2: The release guide command parity list includes the concrete docs probe command or distinctive probe snippet mirrored from the workflow.
    - Test 3: The workflow wiring test asserts `public_verify` contains the new probe step between the Hex.pm index wait and `bash scripts/public_smoke.sh "$VERSION"`.
    - Test 4: The workflow wiring test also locks the bounded retry contract by asserting the distinctive cadence/failure semantics (`DEADLINE=$(( SECONDS + 300 ))`, `sleep 15`, and the terminal timeout message) so future drift in retry posture fails CI.
  </behavior>
  <action>Extend the existing string-membership assertions per D-05 rather than introducing live network tests, YAML parsers, or a new workflow-level harness. Keep `release_docs_parity_test.exs` responsible for guide/workflow parity and `package_metadata_test.exs` responsible for concrete `public_verify` wiring assertions, including the distinctive retry cadence/failure substrings. Choose the exact split under D-05 as long as future drift in step naming, command text, placement, or retry posture fails CI.</action>
  <verify>
    <automated>mix test test/install_smoke/release_docs_parity_test.exs test/install_smoke/package_metadata_test.exs</automated>
  </verify>
  <done>The install-smoke suite fails when the HexDocs probe step, command contract, or `public_verify` placement drifts from the workflow/runbook contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner -> `hexdocs.pm` | Untrusted public network response is used as release-verification evidence. |
| Workflow YAML -> runbook/test harness | Contract drift here can silently reduce release verification unless parity checks catch it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | S | HexDocs probe step | mitigate | Probe the versioned public URL with `curl --fail --location` per D-02/D-03 so redirects are followed and success is based on the final response, not an intermediate redirect. |
| T-21-02 | T | `public_verify` workflow wiring | mitigate | Add explicit install-smoke assertions in both parity tests so removal, renaming, or reordering of the probe fails CI before release time. |
| T-21-03 | R | release runbook vs workflow | mitigate | Mirror the exact step name and probe contract in `guides/release_publish.md`, then assert literal parity against `.github/workflows/release.yml`. |
| T-21-04 | D | post-publish verification lane | accept | Reusing the existing 5-minute / 15-second retry envelope can still time out during upstream propagation delays, but that bounded failure is the intended behavior for a release gate and is already accepted for Hex.pm indexing. |
</threat_model>

<verification>
Run `mix test test/install_smoke/release_docs_parity_test.exs test/install_smoke/package_metadata_test.exs` and confirm both the runbook parity assertions and `public_verify` wiring assertions pass with the new HexDocs probe in place.
</verification>

<success_criteria>
1. `.github/workflows/release.yml` adds a dedicated HexDocs reachability probe inside `public_verify`, between the post-publish Hex.pm index wait and the public package smoke step.
2. The probe follows redirects with a GET, fails on final non-2xx response, and uses the same 5-minute / 15-second retry posture required by D-02 through D-04.
3. `test/install_smoke/release_docs_parity_test.exs` and `test/install_smoke/package_metadata_test.exs` fail if the probe step, its contract text, or its workflow placement drifts.
4. `guides/release_publish.md` documents the probe in the same parity contract used for prior release-flow changes.
</success_criteria>

<output>
After completion, create `.planning/phases/21-verify-02-hexdocs-reachability-probe/21-01-SUMMARY.md`
</output>

---
phase: 18-documentation-and-typespec-coverage
plan: 05
type: execute
wave: 5
depends_on:
  - 18-documentation-and-typespec-coverage-04
files_modified:
  - .doctor.exs
  - CHANGELOG.md
  - lib/rindle/storage.ex
  - lib/rindle/authorizer.ex
  - lib/rindle/analyzer.ex
  - lib/rindle/scanner.ex
  - lib/rindle/processor.ex
autonomous: true
requirements:
  - API-06
  - API-07
  - API-08
must_haves:
  truths:
    - ".doctor.exs configures min_module_doc_coverage: 100, min_overall_doc_coverage: 100, min_overall_moduledoc_coverage: 100, min_module_spec_coverage: 95, min_overall_spec_coverage: 95 (D-07 target)"
    - "test/rindle/doctor_thresholds_test.exs is GREEN (5/5 assertions pass against ratcheted .doctor.exs)"
    - "MIX_ENV=test mix doctor --full --raise exits 0 against the D-07 target thresholds"
    - "CHANGELOG.md has an entry under [Unreleased] (or the next pending version) documenting the doc/spec sweep, named-type tightening, mix doctor CI gate, and Rindle.Processor.Image promotion"
    - "Manual probe: downgrading one @doc to @doc false on a covered public function in a throwaway commit makes mix doctor exit non-zero (API-08-T2)"
    - "OPTIONAL D-21: each of the 5 behaviour modules has a Membrane-style callback summary in @moduledoc (~3 callback names per behaviour; hand-written, not macro-generated) IF this plan stays under ~6 file edits"
  artifacts:
    - path: ".doctor.exs"
      provides: "ratcheted thresholds at D-07 target (100/100/100/95/95)"
      contains: "min_module_doc_coverage: 100"
    - path: "CHANGELOG.md"
      provides: "Phase 18 release-notes entry"
      contains: "documentation"
    - path: "test/rindle/doctor_thresholds_test.exs"
      provides: "ratchet harness — now passing 5/5"
      contains: "Rindle.DoctorThresholdsTest"
  key_links:
    - from: ".doctor.exs"
      to: "test/rindle/doctor_thresholds_test.exs"
      via: "test reads .doctor.exs and asserts D-07 target values; ratchet flips test from RED to GREEN"
      pattern: "min_module_doc_coverage: 100"
    - from: "CHANGELOG.md"
      to: "lib/rindle.ex (and the rest of the public surface)"
      via: "release-please reads CHANGELOG.md to surface the v0.1.5/v0.2.0 publish notes"
      pattern: "(?i)(documentation|typespec|@doc|@spec|doctor)"
---

<objective>
Ratchet `.doctor.exs` from baseline (Plan 18-01) to the D-07 target thresholds (100/100/100/95/95), turning the failing `doctor_thresholds_test.exs` harness GREEN. Add the CHANGELOG entry summarizing Phase 18's doc/spec sweep. Optionally add Membrane-style callback summaries to behaviour `@moduledoc`s (D-21) IF this plan stays compact.

Purpose: This is the closure / ratchet wave per D-22 / D-23. After this plan, CI enforces 100% module-doc / 100% overall-doc / 100% moduledoc / 95% module-spec / 95% overall-spec coverage on every commit. Future doc/spec regressions block merge automatically. The CHANGELOG entry feeds into the release-please publishing pipeline.
Output: Ratcheted `.doctor.exs`, green ratchet test, CHANGELOG entry, optional callback summaries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-documentation-and-typespec-coverage/18-CONTEXT.md
@.planning/phases/18-documentation-and-typespec-coverage/18-RESEARCH.md
@.planning/phases/18-documentation-and-typespec-coverage/18-PATTERNS.md
@.planning/phases/18-documentation-and-typespec-coverage/18-VALIDATION.md
@.planning/phases/18-documentation-and-typespec-coverage/18-01-SUMMARY.md
@.planning/phases/18-documentation-and-typespec-coverage/18-02-SUMMARY.md
@.planning/phases/18-documentation-and-typespec-coverage/18-03-SUMMARY.md
@.planning/phases/18-documentation-and-typespec-coverage/18-04-SUMMARY.md
@.doctor.exs
@test/rindle/doctor_thresholds_test.exs
@CHANGELOG.md
@lib/rindle/storage.ex
@lib/rindle/authorizer.ex
@lib/rindle/analyzer.ex
@lib/rindle/scanner.ex
@lib/rindle/processor.ex

<interfaces>
<!-- D-07 target thresholds (the values .doctor.exs ratchets to in Task 1): -->
- `min_module_doc_coverage: 100`
- `min_overall_doc_coverage: 100`
- `min_overall_moduledoc_coverage: 100`
- `min_module_spec_coverage: 95`
- `min_overall_spec_coverage: 95`

<!-- D-21 (optional) — Membrane-style callback summary in @moduledoc.
Example shape (insert near the top of the @moduledoc, before the prose):

@moduledoc """
Behaviour contract for delivery authorization hooks.

## Callbacks

  * `c:authorize/3` — authorize a delivery action.

(rest of existing @moduledoc...)
"""

5 behaviour modules x ~1-3 callbacks each = 15 callback names total.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ratchet .doctor.exs to D-07 target; turn doctor_thresholds_test green</name>
  <read_first>
    - .doctor.exs (current baseline thresholds from Plan 18-01)
    - test/rindle/doctor_thresholds_test.exs (the 5 assertions that must pass)
    - .planning/phases/18-documentation-and-typespec-coverage/18-CONTEXT.md sections D-07, D-22, D-23
    - .planning/phases/18-documentation-and-typespec-coverage/18-01-SUMMARY.md (baseline values captured by Plan 18-01)
    - .planning/phases/18-documentation-and-typespec-coverage/18-RESEARCH.md "Validation Architecture" section (the locked threshold values + R-6 risk note about empty @moduledocs counting as missing)
  </read_first>
  <files>.doctor.exs, test/rindle/doctor_thresholds_test.exs</files>
  <action>
**Step 1 — Ratchet .doctor.exs to D-07 target.** Edit `.doctor.exs` and set the threshold keys to:

```elixir
min_module_doc_coverage: 100,
min_overall_doc_coverage: 100,
min_overall_moduledoc_coverage: 100,
min_module_spec_coverage: 95,
min_overall_spec_coverage: 95,
```

Leave the `ignore_modules:` block and any other configuration fields (e.g., `umbrella`, `failed`, `exception_moduledoc_required`) untouched — only the 5 threshold values change.

**Step 2 — Run mix doctor locally and resolve any failure.** Run:

```
MIX_ENV=test mix doctor --full --raise
```

It MUST exit 0. If it does not:
- Per Risk Register R-6: the most likely culprit is an EMPTY `@moduledoc` (string `""`) on some module — those count as "missing" by doctor. Find any such module and either fill in a real `@moduledoc` or add it to `ignore_modules:` with a comment explaining why.
- If a public function lacks `@doc`, add the `@doc` (this should not happen if Plans 18-02 / 18-03 / 18-04 ran correctly).
- If a public function lacks `@spec`, the 95% threshold means a small number of @impl-inheriting functions (Mix task `run/1`, worker `perform/1` after Plan 18-04 narrows it, etc.) are accommodated — but if the threshold still fails, identify the culprit module via `mix doctor --full` output and either add the missing `@spec` or audit whether the module should be in `ignore_modules:`.

Do NOT lower the threshold values to fit current-state coverage — the values are locked at the D-07 target. Instead, fix the gap source.

**Step 3 — Verify doctor_thresholds_test.exs is now GREEN.** Run:

```
mix test test/rindle/doctor_thresholds_test.exs --color
```

All 5 tests must pass (5 tests, 0 failures). This is the visible end of the D-23 ratchet contract — Plan 18-01 shipped this test RED, Plan 18-05 turns it green.

**Step 4 — Run the full quality gate locally** to confirm everything is green together:

```
mix compile --warnings-as-errors
mix format --check-formatted
mix credo --strict
MIX_ENV=test mix doctor --full --raise
mix test
mix dialyzer --format github
mix docs --warnings-as-errors
```

All commands must exit 0.
  </action>
  <verify>
    <automated>grep -F 'min_module_doc_coverage: 100' .doctor.exs && grep -F 'min_overall_doc_coverage: 100' .doctor.exs && grep -F 'min_overall_moduledoc_coverage: 100' .doctor.exs && grep -F 'min_module_spec_coverage: 95' .doctor.exs && grep -F 'min_overall_spec_coverage: 95' .doctor.exs && MIX_ENV=test mix doctor --full --raise && mix test test/rindle/doctor_thresholds_test.exs --color && mix compile --warnings-as-errors && mix dialyzer --format github && mix docs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'min_module_doc_coverage: 100' .doctor.exs` exits 0
    - `grep -F 'min_overall_doc_coverage: 100' .doctor.exs` exits 0
    - `grep -F 'min_overall_moduledoc_coverage: 100' .doctor.exs` exits 0
    - `grep -F 'min_module_spec_coverage: 95' .doctor.exs` exits 0
    - `grep -F 'min_overall_spec_coverage: 95' .doctor.exs` exits 0
    - `MIX_ENV=test mix doctor --full --raise` exits 0 (target thresholds passing)
    - `mix test test/rindle/doctor_thresholds_test.exs --color` exits 0 (5 tests, 0 failures — RED harness now GREEN)
    - `mix test` exits 0 (full suite green; ratchet test no longer the lone failure)
    - `mix compile --warnings-as-errors` exits 0
    - `mix credo --strict` exits 0
    - `mix dialyzer --format github` exits 0
    - `mix docs --warnings-as-errors` exits 0
    - `mix format --check-formatted` exits 0
  </acceptance_criteria>
  <done>
.doctor.exs at D-07 target; doctor_thresholds_test 5/5 green; full quality gate (compile/format/credo/doctor/test/dialyzer/docs) all green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CHANGELOG entry; manual failing-doc regression probe (API-08-T2); optional D-21 callback summaries</name>
  <read_first>
    - CHANGELOG.md (existing entries — match style/heading conventions)
    - mix.exs (line 4 — `@version "0.1.4"` to determine the next pending version label)
    - .planning/phases/18-documentation-and-typespec-coverage/18-CONTEXT.md sections D-21 (optional callback summaries) + Claude's Discretion + R-5 in RESEARCH.md
    - .planning/phases/18-documentation-and-typespec-coverage/18-VALIDATION.md "Manual-Only Verifications" section (API-08-T2 failing-doc regression demo)
    - lib/rindle/storage.ex (where the optional D-21 summary would go — top of @moduledoc)
    - lib/rindle/{authorizer,analyzer,scanner,processor}.ex (sibling behaviour modules — same D-21 placement)
  </read_first>
  <files>CHANGELOG.md, lib/rindle/storage.ex, lib/rindle/authorizer.ex, lib/rindle/analyzer.ex, lib/rindle/scanner.ex, lib/rindle/processor.ex</files>
  <action>
**Step 1 — Add a CHANGELOG.md entry** documenting Phase 18's doc/spec sweep. Match the existing heading conventions in the file (likely `## [Unreleased]` or `## [0.1.5]` / `## [0.2.0]` pending depending on the project's release-please configuration). Use this content (adjust heading to match repo conventions):

```markdown
## [Unreleased]

### Added

- `@doc` annotations on every public `@callback` across `Rindle.Storage`,
  `Rindle.Authorizer`, `Rindle.Analyzer`, `Rindle.Scanner`, and
  `Rindle.Processor`, surfacing the contract for each behaviour callback in
  ExDoc (API-06).
- Behaviour-level named result types on `Rindle.Storage`
  (`put_result`, `delete_result`, `url_result`, `presign_result`,
  `multipart_init_result`, `multipart_complete_result`, `head_result`),
  replacing opaque `map()` returns in callback specs (API-07).
- Module-level named-type aliases on `Rindle.Upload.Broker`
  (`session_only_result`, `initiate_multipart_result`, `presigned_payload`,
  `sign_url_result`, `sign_part_result`, `verify_result`) for adopters using
  Dialyzer (API-07).
- `@spec` annotations on every public function of `Rindle.Upload.Broker`
  (the largest pre-existing spec gap, now closed).
- `@doc` and `@spec` on `Rindle.Profile.__using__/1` macro and `Rindle.HTML.picture_tag/3` helper.
- `Rindle.Processor.Image` promoted to documented public adapter, symmetric
  with `Rindle.Storage.S3` and `Rindle.Storage.Local`. The `variant_spec`
  keys (`:width`, `:height`, `:mode`, `:format`, `:quality`) and supported
  modes (`:fit`, `:crop`, `:fill`) are now documented in the adapter's
  `@moduledoc`.
- `mix doctor` (`~> 0.22.0`) added as a dev/test-only static analyzer, with
  `MIX_ENV=test mix doctor --full --raise` enforced in the CI quality job
  on both Elixir 1.15 and 1.17 lanes (API-08).
- ExDoc grouping: "Storage Adapters" renamed to "Storage and Processor
  Adapters" to host the bundled adapters across both behaviour families.

### Changed

- Public `@spec`s on `Rindle` facade functions (`initiate_upload/2`,
  `initiate_multipart_upload/2`, `sign_multipart_part/3`,
  `complete_multipart_upload/3`, `verify_completion/2`, `verify_upload/2`,
  `attach/4`, `upload/3`) now use `MediaAsset.t()`, `MediaUploadSession.t()`,
  `MediaAttachment.t()`, and named `Broker.*_result()` types instead of
  `{:ok, map()}` / `{:ok, struct()}`.
- `Rindle.log_variant_processing_failure/3` (the hidden facade shim) now
  emits a compile-time deprecation warning via `@deprecated`. Use
  `Rindle.Internal.VariantFailureLogger.log/3` directly.

### Notes

- Error branches across all tightened specs retain `{:error, term()}` to
  preserve the 0.1.x semver posture (narrowing error terms is a Dialyzer-
  breaking change for adopters pattern-matching on them).
```

The repo uses release-please (per planning_context); the heading conventions there should drive the version label. If the project conventionally uses `## [Unreleased]` as the staging slot, use that.

**Step 2 — Optional D-21: hand-written callback summaries in @moduledoc** (only if this plan stays compact — see CONTEXT.md Claude's Discretion). For each of the 5 behaviour modules, add a brief `## Callbacks` section near the top of the `@moduledoc` listing the callbacks with their topic line. Example for `lib/rindle/authorizer.ex`:

```elixir
@moduledoc """
Behaviour contract for delivery authorization hooks.

Authorization decisions must be made before URL issuance, and any storage I/O
involved in delivery should occur outside database transactions.

## Callbacks

  * `c:authorize/3` — authorizes a delivery action for an actor against a subject.
"""
```

Apply same shape to the other 4 behaviour modules:
- `lib/rindle/analyzer.ex` — list `c:analyze/1`
- `lib/rindle/scanner.ex` — list `c:scan/1`
- `lib/rindle/processor.ex` — list `c:process/3`
- `lib/rindle/storage.ex` — list all 11 callbacks (`c:store/3`, `c:download/3`, `c:delete/2`, `c:url/2`, `c:presigned_put/3`, `c:initiate_multipart_upload/3`, `c:presigned_upload_part/5`, `c:complete_multipart_upload/4`, `c:abort_multipart_upload/3`, `c:head/2`, `c:capabilities/0`)

**Decision rule** per CONTEXT.md `<discretion>` and Risk Register R-5: include D-21 if this plan stays at ≤ 6 file edits total. CHANGELOG (1) + .doctor.exs (1, from Task 1) + 5 behaviour modules (5) = 7 files. **At the boundary.** Recommendation: include D-21 in this plan since it's mechanical and short (~1-2 added lines per behaviour module after the existing first paragraph).

If the executor finds the D-21 work pushes file count substantially higher (e.g. requires reflowing existing `@moduledoc` text), defer D-21 to a future polish phase and update the plan summary noting the deferral.

**Step 3 — Manual probe: failing-doc regression demo (API-08-T2 from VALIDATION.md).** This is a manual one-off CI probe documented as evidence in the PR. Steps:

1. On a throwaway local branch (e.g., `git checkout -b throwaway-doctor-regression-demo`), pick one public function (e.g., `Rindle.Delivery.url/3` at `lib/rindle/delivery.ex:96`) and downgrade its `@doc """..."""` to `@doc false`.
2. Run `MIX_ENV=test mix doctor --full --raise` locally.
3. Confirm it exits NON-ZERO with a "missing @doc" error pointing at `Rindle.Delivery.url/3`.
4. Capture the output (or first 10 lines of it) for the plan summary as evidence.
5. **REVERT** the throwaway branch — `git checkout main && git branch -D throwaway-doctor-regression-demo` — DO NOT merge this change.

This proves API-08-T2: a failing `@doc`/`@spec` regression makes the doctor CI step exit non-zero. Captured in the plan summary as evidence of the gate's bite.

**Step 4 — Final verification.** Run the same full quality gate:

```
mix compile --warnings-as-errors
mix format --check-formatted
mix credo --strict
MIX_ENV=test mix doctor --full --raise
mix test
mix dialyzer --format github
mix docs --warnings-as-errors
```

All must exit 0. The phase is complete.
  </action>
  <verify>
    <automated>grep -E -i '(doctor|@doc|@spec|documentation|typespec)' CHANGELOG.md | head -1 && grep -F 'Rindle.Processor.Image' CHANGELOG.md && grep -F 'mix doctor' CHANGELOG.md && (grep -F '## Callbacks' lib/rindle/storage.ex || echo 'D-21 deferred for storage') && (grep -F '## Callbacks' lib/rindle/authorizer.ex || echo 'D-21 deferred for authorizer') && (grep -F '## Callbacks' lib/rindle/analyzer.ex || echo 'D-21 deferred for analyzer') && (grep -F '## Callbacks' lib/rindle/scanner.ex || echo 'D-21 deferred for scanner') && (grep -F '## Callbacks' lib/rindle/processor.ex || echo 'D-21 deferred for processor') && mix compile --warnings-as-errors && mix credo --strict && MIX_ENV=test mix doctor --full --raise && mix test && mix dialyzer --format github && mix docs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'mix doctor' CHANGELOG.md` exits 0 (Phase 18 entry present)
    - `grep -F 'Rindle.Processor.Image' CHANGELOG.md` exits 0 (D-27 promotion noted)
    - `grep -E -i '(named.*type|named-type|typespec|@spec)' CHANGELOG.md` exits 0 (named-type tightening noted)
    - `grep -F 'API-06' CHANGELOG.md || grep -F 'API-07' CHANGELOG.md || grep -F 'API-08' CHANGELOG.md || grep -E '@doc|@spec' CHANGELOG.md` exits 0
    - If D-21 included: `grep -F '## Callbacks' lib/rindle/authorizer.ex` exits 0 AND `grep -F 'c:authorize/3' lib/rindle/authorizer.ex` exits 0 (and same shape for analyzer, scanner, processor, storage). If D-21 deferred: plan summary explicitly says "D-21 deferred to a future polish phase per Risk R-5"
    - `mix compile --warnings-as-errors` exits 0
    - `mix format --check-formatted` exits 0
    - `mix credo --strict` exits 0
    - `MIX_ENV=test mix doctor --full --raise` exits 0
    - `mix test` exits 0 (full suite — including doctor_thresholds_test.exs, behaviour_docs_test.exs, api_surface_boundary_test.exs all green)
    - `mix dialyzer --format github` exits 0
    - `mix docs --warnings-as-errors` exits 0
    - Plan summary documents the manual API-08-T2 probe evidence (output of `MIX_ENV=test mix doctor --full --raise` on the throwaway branch where one @doc was downgraded — must show non-zero exit and a "missing @doc" line)
    - Phase 18 success criteria from ROADMAP.md all satisfied: (1) every intentionally public module/function/callback has @doc; (2) named struct types replace opaque map()/term() on public specs; (3) mix doctor --raise passes in CI and a failing @doc/@spec causes non-zero exit
  </acceptance_criteria>
  <done>
CHANGELOG entry committed; optional D-21 callback summaries decided (included or deferred); manual API-08-T2 probe evidence captured in plan summary; full quality gate green; Phase 18 closure achieved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation/typespec phase — no new attack surface. |

## STRIDE Threat Register

No new attack surface — Phase 18 closure plan ratchets static-analysis thresholds, adds CHANGELOG prose, and optionally enriches behaviour `@moduledoc` blocks with callback summaries. None expose new runtime behavior. The CI ratchet to 100/100/100/95/95 is a defense-in-depth posture: future doc/spec regressions on public surfaces are caught at PR time, reducing the chance of accidental visibility leaks (which is a documentation-quality benefit, not a new attack surface).
</threat_model>

<verification>
- `MIX_ENV=test mix doctor --full --raise` exits 0 against ratcheted thresholds
- `mix test test/rindle/doctor_thresholds_test.exs` GREEN (5 tests, 0 failures — D-23 ratchet contract closed)
- `mix test` GREEN (full suite — RED harness now resolved)
- `mix dialyzer --format github` clean
- `mix docs --warnings-as-errors` clean
- `mix credo --strict` clean
- CHANGELOG.md has a Phase 18 entry
- Manual API-08-T2 probe evidence in plan summary
- (Optional) D-21 callback summaries on 5 behaviour modules OR explicit deferral
</verification>

<success_criteria>
- [ ] .doctor.exs ratcheted to D-07 target (100/100/100/95/95)
- [ ] doctor_thresholds_test.exs GREEN (5/5)
- [ ] Full quality gate green (compile/format/credo/doctor/test/dialyzer/docs)
- [ ] CHANGELOG entry present documenting Phase 18 sweep
- [ ] Manual API-08-T2 failing-doc regression demo captured as PR evidence
- [ ] D-21 decision recorded (included vs deferred)
- [ ] Phase 18 ROADMAP success criteria 1, 2, 3 ALL satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/18-documentation-and-typespec-coverage/18-05-SUMMARY.md` with:
- Final .doctor.exs threshold values (verbatim)
- doctor_thresholds_test.exs run output (5/5 passing)
- Full `mix test` output summary (test count + 0 failures)
- CHANGELOG entry committed (verbatim)
- Manual API-08-T2 probe evidence: the throwaway-branch `mix doctor --full --raise` output showing non-zero exit and the "missing @doc" line for the downgraded function (e.g., Rindle.Delivery.url/3)
- D-21 decision: "INCLUDED" with the 5 behaviour-module diffs, OR "DEFERRED to a future polish phase" with rationale
- Phase 18 closure statement: each ROADMAP success criterion (1, 2, 3) marked satisfied with the evidence pointer
</output>

---
milestone: v1.2
audited: 2026-04-29T01:04:56Z
status: tech_debt
scores:
  requirements: 2/6
  phases: 3/3
  integration: 3/3
  flows: 3/3
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 10-publish-readiness
    items:
      - "RELEASE-04 and RELEASE-05 are verified and listed in phase summaries, but REQUIREMENTS.md still leaves both checkboxes pending."
      - "The summary metadata uses `requirements-completed`, which the SDK extractor did not resolve via `requirements_completed`; the audit used direct frontmatter reading instead."
  - phase: 11-protected-publish-automation
    items:
      - "11-01/02/03-SUMMARY.md do not declare `requirements-completed`, so RELEASE-06 and RELEASE-07 remain partial in the strict three-source audit despite passing verification."
  - phase: 12-public-verification-and-release-operations
    items:
      - "guides/release_publish.md still says live `HEX_API_KEY` automation is deferred to Phase 11, which contradicts the shipped release workflow."
      - "12-01/12-02 summary frontmatter does not use `requirements-completed`, so RELEASE-08 and RELEASE-09 remain partial in the strict three-source audit despite passing verification."
nyquist:
  compliant_phases: [12]
  partial_phases: [10, 11]
  missing_phases: []
  overall: partial
---

# Milestone v1.2 Audit

## Verdict

Milestone `v1.2` meets its delivery intent at the phase, wiring, and end-to-end
flow levels, but the strict milestone audit lands at `tech_debt` rather than
`passed`.

There are no missing phase verification reports, no unsatisfied milestone
requirements, and no broken cross-phase flows in the shipped release path.
The downgrade comes from two non-blocking classes of debt:

1. Requirement trace metadata is inconsistent across phase summaries, so only
   `RELEASE-04` and `RELEASE-05` can be marked fully satisfied by the required
   three-source cross-check.
2. The maintainer runbook still contains one stale statement about live
   `HEX_API_KEY` automation that no longer matches the Phase 11 workflow.

## Scope

- Phase 10: Publish Readiness
- Phase 11: Protected Publish Automation
- Phase 12: Public Verification and Release Operations

Milestone requirements in scope:
`RELEASE-04`, `RELEASE-05`, `RELEASE-06`, `RELEASE-07`, `RELEASE-08`,
`RELEASE-09`

## Phase Summary

| Phase | Verification | Status | Notes |
| --- | --- | --- | --- |
| 10 Publish Readiness | `10-VERIFICATION.md` | passed | First-publish runbook, release-doc parity gate, package metadata proof, and shared preflight are verified. |
| 11 Protected Publish Automation | `11-VERIFICATION.md` | passed | Live Hex publish, version drift gate, concurrency guard, and CI dry-run publish are verified. |
| 12 Public Verification and Release Operations | `12-VERIFICATION.md` | passed | Fresh-runner public verify job, network install proof path, and rollback/routine release docs are verified. |

Phase verification coverage score: `3/3`

## Requirements Coverage

This audit cross-referenced three sources for each milestone requirement:

1. `.planning/REQUIREMENTS.md` traceability and checkbox state
2. Phase `*-VERIFICATION.md` requirement tables
3. Phase `*-SUMMARY.md` frontmatter `requirements-completed`

| Requirement | Phase | REQUIREMENTS.md | VERIFICATION.md | SUMMARY frontmatter | Final Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| RELEASE-04 | 10 | `[ ]` | passed | listed | satisfied | `10-01-SUMMARY.md` lists `requirements-completed: [RELEASE-04]`; `10-VERIFICATION.md` marks it satisfied. |
| RELEASE-05 | 10 | `[ ]` | passed | listed | satisfied | `10-02-SUMMARY.md` lists `requirements-completed: [RELEASE-05]`; `10-VERIFICATION.md` marks it satisfied. |
| RELEASE-06 | 11 | `[x]` | passed | missing | partial | `11-VERIFICATION.md` marks it satisfied, but Phase 11 summaries do not declare `requirements-completed`. |
| RELEASE-07 | 11 | `[x]` | passed | missing | partial | `11-VERIFICATION.md` marks it satisfied, but Phase 11 summaries do not declare `requirements-completed`. |
| RELEASE-08 | 12 | `[ ]` | passed | missing | partial | `12-VERIFICATION.md` marks it verified, but `12-01-SUMMARY.md` uses `requirement:` instead of `requirements-completed`. |
| RELEASE-09 | 12 | `[ ]` | passed | missing | partial | `12-VERIFICATION.md` marks it verified, but `12-02-SUMMARY.md` uses `requirements:` instead of `requirements-completed`. |

Requirement coverage score: `2/6 satisfied`, `4/6 partial`,
`0 unsatisfied`, `0 orphaned`

## Integration And E2E Flows

Integration verdict: `pass`

The milestone seams are wired end to end:

- Phase 10's shared preflight script feeds both the protected release workflow
  and the CI package-consumer lane.
- Phase 11's live publish step is guarded by version matching and release
  environment credential scoping.
- Phase 12 adds a fresh-runner `public_verify` job that clears `HEX_API_KEY`
  and proves network resolution through `scripts/public_smoke.sh`.

Verified milestone seams:

- `scripts/release_preflight.sh` is consumed by
  `.github/workflows/release.yml` and `.github/workflows/ci.yml`.
- `scripts/assert_version_match.sh` is consumed by both the live release path
  and the CI dry-run publish path.
- `public_verify` depends on `release`, clears `HEX_API_KEY`, and passes the
  tag-derived version into `scripts/public_smoke.sh`.
- `GeneratedAppHelper.prove_package_install!/0` switches to `~> version`
  resolution when `RINDLE_INSTALL_SMOKE_NETWORK_VERSION` is present and polls
  for Hex propagation.

Broken flows found: none.

Residual external-proof limitation:

- This audit did not perform a new real Hex.pm publish. `RELEASE-08` remains
  locally supported by wiring and tests, with the live publish proof delegated
  to the shipped release automation on the next real tag.

## Tech Debt

### Requirement Trace Metadata

- `RELEASE-04` and `RELEASE-05` are implemented and verified, but
  `.planning/REQUIREMENTS.md` still leaves both boxes unchecked.
- Phase 11 summaries do not expose `requirements-completed`, leaving
  `RELEASE-06` and `RELEASE-07` partial in the strict audit.
- Phase 12 summaries use `requirement:` / `requirements:` rather than
  `requirements-completed`, leaving `RELEASE-08` and `RELEASE-09` partial in
  the strict audit.

### Runbook Drift

- [guides/release_publish.md](/Users/jon/projects/rindle/guides/release_publish.md:48)
  still says live `HEX_API_KEY` automation is not wired and defers that work
  to Phase 11.
- The shipped workflow already performs live publish in
  [.github/workflows/release.yml](/Users/jon/projects/rindle/.github/workflows/release.yml:108).
- Affected requirement surface: `RELEASE-09` directly, `RELEASE-06`
  secondarily.

### Coverage Drift

- The existing release-doc parity test still protects the Phase 10 contract,
  but it does not assert that the Phase 12 runbook language matches the live
  workflow contract. That allows documentation drift to persist unnoticed.

## Nyquist Discovery

`workflow.nyquist_validation` is not explicitly disabled, so discovery is
enabled by default.

| Phase | Validation File | Nyquist | Notes |
| --- | --- | --- | --- |
| 10 | `10-VALIDATION.md` | PARTIAL | `nyquist_compliant: true`, but `wave_0_complete: false` and approval still pending. |
| 11 | `11-VALIDATION.md` | PARTIAL | `nyquist_compliant: true`, but validation remains draft/pending with `wave_0_complete: false`. |
| 12 | `12-VALIDATION.md` | COMPLIANT | `nyquist_compliant: true` and `wave_0_complete: true`. |

Phases needing validation review: run `$gsd-validate-phase 10` and
`$gsd-validate-phase 11` if you want the validation artifacts brought to a
fully complete state before milestone closure.

## Verification Evidence Used In This Audit

- Existing phase verification reports:
  `10-VERIFICATION.md`, `11-VERIFICATION.md`, `12-VERIFICATION.md`
- Fresh audit-time checks:
  `bash -n scripts/public_smoke.sh`
  `bash -n scripts/assert_version_match.sh`
  `mix test test/install_smoke/release_docs_parity_test.exs test/install_smoke/package_metadata_test.exs`

Result: `10 tests, 0 failures`

## Conclusion

Milestone `v1.2` is complete from a release-system standpoint. The release
path is wired from preflight through protected publish to post-publish public
verification, and every in-scope phase has a passing verification report.

The remaining issues are traceability metadata drift, partially complete
validation artifacts for Phases 10 and 11, and one stale runbook statement.
Those are non-blocking, so the milestone audit status is `tech_debt` rather
than `gaps_found`.

---
id: 69-02
plan_number: 02
plan: 02
phase: 69
type: execute
wave: 2
depends_on: [69-01]
files_modified:
  - test/rindle/batch_owner_erasure_task_test.exs
  - test/rindle/api_surface_boundary_test.exs
autonomous: true
requirements: [OPS-02]
requirements_addressed: [OPS-02]
tags: [elixir, mix-task, operator, tests, boundary]

must_haves:
  truths:
    - "Default task invocation runs preview and prints [DRY RUN] banner"
    - "Execute requires --execute or --no-dry-run flag"
    - "Invalid owners file exits 1 before facade call"
    - "JSON format emits parseable batch report"
    - "Mix.Tasks.Rindle.BatchOwnerErasure is registered in api_surface_boundary_test @public_modules"
  artifacts:
    - path: "test/rindle/batch_owner_erasure_task_test.exs"
      provides: "CLI integration proof for OPS-02"
      contains: "BatchOwnerErasure"
    - path: "test/rindle/api_surface_boundary_test.exs"
      provides: "Public compiled-docs boundary"
      contains: "Mix.Tasks.Rindle.BatchOwnerErasure"
  key_links:
    - from: "test/rindle/batch_owner_erasure_task_test.exs"
      to: "lib/mix/tasks/rindle.batch_owner_erasure.ex"
      via: "BatchOwnerErasure.run/1"
      pattern: "BatchOwnerErasure\\.run"

threat_model:
  assets:
    - "Public API surface list for compiled docs"
  trust_boundaries:
    - "Mix task visibility in hex docs boundary"
  threats:
    - id: T-69-04
      category: tampering
      description: "New operator task omitted from @public_modules and hidden from adopters"
      severity: low
      mitigation: "Register Mix.Tasks.Rindle.BatchOwnerErasure in api_surface_boundary_test.exs"
  block_on: high
---

<objective>
Prove the batch owner-erasure Mix task CLI contract with integration tests and register the task
on the public compiled-docs boundary.

Purpose: OPS-02 verification (dry-run default, execute opt-in, input validation, output formats).
Output: `batch_owner_erasure_task_test.exs` + boundary test update.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-operator-mix-task/69-CONTEXT.md
@.planning/phases/69-operator-mix-task/69-RESEARCH.md
@.planning/phases/69-operator-mix-task/69-PATTERNS.md
@.planning/phases/69-operator-mix-task/69-VALIDATION.md
@lib/mix/tasks/rindle.batch_owner_erasure.ex
@test/rindle/runtime_status_task_test.exs
@test/rindle/owner_erasure_batch_test.exs
@test/rindle/api_surface_boundary_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add batch owner erasure task integration tests</name>
  <read_first>
    - test/rindle/runtime_status_task_test.exs (Mix.Shell.Process harness, JSON output assertion)
    - test/rindle/owner_erasure_batch_test.exs (User struct, insert_asset/insert_attachment helpers)
    - lib/mix/tasks/rindle.batch_owner_erasure.ex (run/1, exported format helpers)
    - lib/rindle.ex (batch API)
  </read_first>
  <files>test/rindle/batch_owner_erasure_task_test.exs</files>
  <action>
    Create `test/rindle/batch_owner_erasure_task_test.exs`:

    ```elixir
    defmodule Rindle.BatchOwnerErasureTaskTest do
      use Rindle.DataCase, async: false
      import Mox
      alias Mix.Tasks.Rindle.BatchOwnerErasure, as: Task
    ```

    Reuse patterns from `owner_erasure_batch_test.exs`:
    - `defmodule User, do: defstruct([:id])`
    - `TestProfile` with `Rindle.StorageMock`
    - `setup :set_mox_from_context` and `setup :verify_on_exit!`
    - Copy `insert_asset/1` and `insert_attachment/3` helpers (minimal duplication OK)

    Setup: `Mix.shell(Mix.Shell.Process)` with restore on exit.

    Helper `write_owners_file!(owners)`:
    - Write temp file under `System.tmp_dir!()`
    - JSON encode list of `%{"owner_type" => "Elixir.Rindle.BatchOwnerErasureTaskTest.User", "owner_id" => owner.id}`
    - Return path; delete in `on_exit`

    Tests:

    1. `"missing --owners-file exits 1"` — `catch_exit(Task.run([])) == {:shutdown, 1}`; assert_received `{:mix_shell, :error, [msg]}` with `--owners-file`

    2. `"default run previews and prints dry run banner"` — insert attachment for one owner; run with `--owners-file`; assert_received info lines containing `[DRY RUN]` and bucket counts; attachment still in DB (preview)

    3. `"--execute runs destructive batch"` — insert attachment; run with `--owners-file` and `--execute`; refute Repo.get attachment; assert_received info WITHOUT requiring `[DRY RUN]` prefix on success line OR assert mode execute in output

    4. `"--format json emits batch report"` — preview with `--format json`; capture single info line; `Jason.decode!` succeeds; decoded has `"attachments_to_detach"` and `"owners"` keys

    5. `"invalid owners file exits 1 before facade"` — empty JSON array `[]` in file; catch_exit 1; error message mentions owner

    6. `"unknown owner_type module exits 1"` — JSON with `"owner_type": "Elixir.Nonexistent.Module"`; catch_exit 1

    Use `Mox` expectations from batch tests for storage if execute test needs them.
  </action>
  <acceptance_criteria>
    - File `test/rindle/batch_owner_erasure_task_test.exs` exists
    - File contains `Mix.Tasks.Rindle.BatchOwnerErasure`
    - File contains test name `"default run previews and prints dry run banner"`
    - File contains test name `"--execute runs destructive batch"`
    - `mix test test/rindle/batch_owner_erasure_task_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rindle/batch_owner_erasure_task_test.exs</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Register task in public API boundary test</name>
  <read_first>
    - test/rindle/api_surface_boundary_test.exs (@public_modules list ~lines 25-30)
    - lib/mix/tasks/rindle.batch_owner_erasure.ex
  </read_first>
  <files>test/rindle/api_surface_boundary_test.exs</files>
  <action>
    Add `Mix.Tasks.Rindle.BatchOwnerErasure` to `@public_modules` in alphabetical order among other
    `Mix.Tasks.Rindle.*` entries (after `BackfillMetadata`, before `CleanupOrphans`).
  </action>
  <acceptance_criteria>
    - `grep 'Mix.Tasks.Rindle.BatchOwnerErasure' test/rindle/api_surface_boundary_test.exs` matches
    - `mix test test/rindle/api_surface_boundary_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rindle/api_surface_boundary_test.exs</automated>
  </verify>
</task>

</tasks>

<verification>
After both tasks:
- `mix test test/rindle/batch_owner_erasure_task_test.exs test/rindle/api_surface_boundary_test.exs` exits 0
- `mix compile --warnings-as-errors` exits 0
</verification>

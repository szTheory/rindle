---
id: 70-02
plan_number: 02
plan: 02
phase: 70
type: execute
wave: 2
depends_on: [70-01]
files_modified:
  - guides/user_flows.md
  - guides/operations.md
  - guides/getting_started.md
  - test/install_smoke/docs_parity_test.exs
autonomous: true
requirements: [TRUTH-03]
requirements_addressed: [TRUTH-03]
tags: [elixir, docs, guides, parity, owner-erasure, batch]

must_haves:
  truths:
    - "user_flows.md Story 5 documents batch owner erasure as supported multi-owner surface with API, mix task, partial_report, and idempotent rerun guidance"
    - "Stale bulk orchestration deferral replaced with shipped batch API plus deferred admin UI, force-delete, and scheduler jobs"
    - "operations.md has thin batch pointer without JSON schema, flag table, or --owners-file"
    - "getting_started.md forwards batch orchestration readers to user_flows.md"
    - "docs_parity_test.exs freezes batch vocabulary and refutes stale deferral and ops contract duplication"
  artifacts:
    - path: "guides/user_flows.md"
      provides: "Canonical batch erasure support truth"
      contains: "Batch owner erasure"
    - path: "guides/operations.md"
      provides: "Thin batch operator pointer"
      contains: "batch_owner_erasure"
    - path: "test/install_smoke/docs_parity_test.exs"
      provides: "TRUTH-03 vocabulary freeze"
      contains: "preview_batch_owner_erasure"
  key_links:
    - from: "guides/operations.md"
      to: "guides/user_flows.md"
      via: "markdown link to batch subsection"
      pattern: "user_flows\\.md"
    - from: "test/install_smoke/docs_parity_test.exs"
      to: "guides/user_flows.md"
      via: "snippet assertions"
      pattern: "preview_batch_owner_erasure"

threat_model:
  assets:
    - "Adopter-facing support truth in published guides"
  trust_boundaries:
    - "Guide narrative vs mix task @moduledoc contract"
  threats:
    - id: T-70-03
      category: information_disclosure
      description: "Operations guide duplicates CLI flag contract causing drift from @moduledoc"
      severity: medium
      mitigation: "Thin ops pointer only; parity test refutes --owners-file and owner_type in operations.md"
    - id: T-70-04
      category: tampering
      description: "Docs claim bulk orchestration still deferred while batch API shipped"
      severity: low
      mitigation: "Replace deferral prose and update parity test in same change"
  block_on: high
---

<objective>
Document batch owner erasure as the supported multi-owner orchestration surface (TRUTH-03) in
guides and freeze vocabulary via docs_parity_test.exs. Explicitly defer force-delete, admin UI,
and scheduler workflows.

Purpose: Adopter/operator guidance closing v1.14; parity flip atomic with guide prose.
Output: Updated `guides/user_flows.md`, `guides/operations.md`, `guides/getting_started.md`,
`test/install_smoke/docs_parity_test.exs`
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/70-proof-adopter-guidance/70-CONTEXT.md
@.planning/phases/70-proof-adopter-guidance/70-RESEARCH.md
@.planning/phases/70-proof-adopter-guidance/70-PATTERNS.md
@.planning/phases/70-proof-adopter-guidance/70-VALIDATION.md
@lib/mix/tasks/rindle.batch_owner_erasure.ex
@lib/rindle.ex
@guides/user_flows.md
@guides/operations.md
@guides/getting_started.md
@test/install_smoke/docs_parity_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add batch erasure subsection to user_flows.md Story 5</name>
  <read_first>
    - guides/user_flows.md (Story 5 lines 214-265, especially stale deferral at 263-265)
    - lib/rindle.ex (@moduledoc for preview_batch_owner_erasure/2, erase_batch_owner_erasure/2)
    - lib/mix/tasks/rindle.batch_owner_erasure.ex (@moduledoc cross-links)
    - .planning/phases/70-proof-adopter-guidance/70-CONTEXT.md (D-13 through D-15)
  </read_first>
  <files>guides/user_flows.md</files>
  <action>
    In `guides/user_flows.md` Story 5, after the single-owner `erase_owner/2` example block and
    before the cleanup_orphans paragraph:

    Add subsection heading `#### Batch owner erasure` (~15-25 lines) covering:

    - Multi-owner orchestration via `Rindle.preview_batch_owner_erasure/2` and
      `Rindle.erase_batch_owner_erasure/2`
    - Minimal 2-owner example:
      ```elixir
      owners = [user_a, user_b]
      {:ok, preview} = Rindle.preview_batch_owner_erasure(owners)
      {:ok, report} = Rindle.erase_batch_owner_erasure(owners)
      ```
    - Operator lane: `mix rindle.batch_owner_erasure --owners-file PATH` with pointer to
      `mix help rindle.batch_owner_erasure` for full contract
    - Semantics bullets:
      - Sequential per-owner transactions (early owners may commit before a later failure)
      - Input dedupe by owner ref; optional `max_owners` limit
      - On failure: `{:error, {:batch_owner_failed, detail}}` with `detail.partial_report`
      - Idempotent rerun for remaining owners after fixing the failing owner
      - Shared assets: same `retained_shared_assets` bucket as single-owner erasure

    Replace lines 263-265 stale deferral:

    **Remove:**
    ```
    Admin UI, bulk orchestration, and
    force-delete policy for still-shared assets remain deferred.
    ```

    **Replace with:**
    ```
    Batch multi-owner erasure is supported via `Rindle.preview_batch_owner_erasure/2`,
    `Rindle.erase_batch_owner_erasure/2`, and `mix rindle.batch_owner_erasure`.
    Admin UI, force-delete policy for still-shared assets, and scheduler/cron erasure
    jobs remain deferred.
    ```

    Use **batch** terminology in adopter-facing prose (D-15); do not introduce new "bulk" API names.
  </action>
  <acceptance_criteria>
    - `grep -q 'Batch owner erasure' guides/user_flows.md`
    - `grep -q 'preview_batch_owner_erasure' guides/user_flows.md`
    - `grep -q 'batch_owner_erasure' guides/user_flows.md`
    - `grep -q 'partial_report' guides/user_flows.md`
    - `grep -q 'bulk orchestration' guides/user_flows.md` returns exit 1 (phrase removed)
    - `grep -q 'scheduler/cron erasure jobs remain deferred' guides/user_flows.md`
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Batch owner erasure' guides/user_flows.md && grep -q 'preview_batch_owner_erasure' guides/user_flows.md && ! grep -q 'bulk orchestration' guides/user_flows.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add thin batch pointers to operations.md and getting_started.md</name>
  <read_first>
    - guides/operations.md (owner-erasure blurb ~lines 30-35)
    - guides/getting_started.md (owner erasure pointer ~lines 242-244)
    - .planning/phases/70-proof-adopter-guidance/70-CONTEXT.md (D-16, D-17)
  </read_first>
  <files>guides/operations.md, guides/getting_started.md</files>
  <action>
    In `guides/operations.md`, extend the existing account-deletion blurb (~8-12 lines total addition)
    near the supported account-deletion surface paragraph:

    - Mention `Rindle.preview_batch_owner_erasure/2` and `Rindle.erase_batch_owner_erasure/2`
    - Mention `mix rindle.batch_owner_erasure` as operator entry point
    - Link to [`user_flows.md`](user_flows.md) batch subsection and `mix help rindle.batch_owner_erasure`

    **Do NOT add:** JSON schema, `--owners-file` flag details, `owner_type` field docs, exit code table,
    or repair-verb table rows (D-16).

    In `guides/getting_started.md`, after the existing owner-erasure pointer (~line 242), add one sentence:

    ```
    For batch (multi-owner) erasure, see the **Batch owner erasure** subsection in
    [`user_flows.md`](user_flows.md).
    ```

    Keep getting_started thin — no API examples or mix flag details (D-17).
  </action>
  <acceptance_criteria>
    - `grep -q 'batch_owner_erasure' guides/operations.md`
    - `grep -q 'preview_batch_owner_erasure' guides/operations.md`
    - `grep -q 'user_flows.md' guides/operations.md`
    - `grep -q 'Batch owner erasure' guides/getting_started.md`
    - `! grep -q '\-\-owners-file' guides/operations.md`
    - `! grep -q 'owner_type' guides/operations.md`
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'batch_owner_erasure' guides/operations.md && grep -q 'Batch owner erasure' guides/getting_started.md && ! grep -q -- '--owners-file' guides/operations.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Extend docs_parity_test.exs for batch TRUTH-03</name>
  <read_first>
    - test/install_smoke/docs_parity_test.exs (owner-erasure tests lines 251-304)
    - guides/user_flows.md (after Task 1 edits)
    - guides/operations.md (after Task 2 edits)
    - .planning/phases/70-proof-adopter-guidance/70-CONTEXT.md (D-18 through D-21)
  </read_first>
  <files>test/install_smoke/docs_parity_test.exs</files>
  <action>
    Update `"user flows guide freezes the canonical owner-erasure support truth"` test:

    **Remove** from required snippets list:
    - `"bulk orchestration"`

    **Add** to required snippets list (lowercase normalized match):
    - `"preview_batch_owner_erasure"`
    - `"erase_batch_owner_erasure"`
    - `"batch owner erasure"` (or `"batch owner erasure"` matching heading)
    - `"batch_owner_erasure"`
    - `"batch_owner_failed"`
    - `"partial_report"`

    Add **refute** after the for-loop:
    ```elixir
    refute normalized =~ "bulk orchestration"
    ```

    Add new test `"user flows and operations document batch erasure without duplicating mix task contract"`:

    ```elixir
    test "user flows and operations document batch erasure without duplicating mix task contract", %{
      user_flows: user_flows
    } do
      operations = File.read!(Path.expand("../../guides/operations.md", __DIR__))
      normalized_flows = String.downcase(user_flows)
      normalized_ops = String.downcase(operations)

      assert normalized_flows =~ "preview_batch_owner_erasure"
      assert normalized_flows =~ "batch_owner_erasure"
      assert normalized_ops =~ "batch_owner_erasure"
      assert normalized_ops =~ "user_flows.md"

      refute normalized_ops =~ "--owners-file"
      refute normalized_ops =~ "owner_type"
    end
    ```

    Extend `"getting-started and operations stay thin while pointing to the canonical owner-erasure flow"`:

    Add assertion:
    ```elixir
    assert guide =~ "Batch owner erasure"
    assert guide =~ "user_flows.md"
    ```

    Do NOT create `batch_owner_erasure_docs_parity_test.exs` (D-18).
  </action>
  <acceptance_criteria>
    - `grep -q 'preview_batch_owner_erasure' test/install_smoke/docs_parity_test.exs`
    - `grep -q 'document batch erasure without duplicating mix task contract' test/install_smoke/docs_parity_test.exs`
    - `grep -q 'refute normalized =~ "bulk orchestration"' test/install_smoke/docs_parity_test.exs`
    - Test file does NOT contain `"bulk orchestration"` in the required snippets for loop (removed)
    - `mix test test/install_smoke/docs_parity_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/install_smoke/docs_parity_test.exs</automated>
  </verify>
</task>

</tasks>

<verification>
After all tasks (full Phase 70 verification per D-22):
- `mix test test/rindle/owner_erasure_batch_test.exs test/rindle/owner_erasure_batch_proof_test.exs test/rindle/owner_erasure_batch_boundary_test.exs test/rindle/owner_erasure_batch_error_test.exs test/rindle/owner_erasure_batch_contract_test.exs test/rindle/owner_erasure_test.exs test/rindle/batch_owner_erasure_task_test.exs test/install_smoke/docs_parity_test.exs` exits 0
</verification>

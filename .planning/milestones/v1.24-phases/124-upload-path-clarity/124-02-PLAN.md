---
phase: 124-upload-path-clarity
plan: "02"
type: execute
wave: 2
depends_on: ["124-01"]
files_modified:
  - lib/rindle/upload/tus_plug.ex
  - lib/rindle/upload/tus_protocol.ex
  - test/rindle/upload/tus_plug_test.exs
  - test/rindle/api_surface_boundary_test.exs
autonomous: true
requirements: [UPLOAD-01, SAFE-01]
must_haves:
  truths:
    - "Tus token verification, protocol parsing/normalization, and reason-to-status mapping have one hidden owner while method routing and final Plug.Conn responses remain in TusPlug."
    - "Token/session/authorizer/content-type/offset/length/checksum gates keep their current order; 415 and 409 happen before body read, temporary-file creation, or storage mutation."
    - "All current tus header strings, status codes, response bodies, claim rules, deferred-length semantics, and checksum outcomes remain exact."
  artifacts:
    - path: "lib/rindle/upload/tus_protocol.ex"
      provides: "Hidden tus 1.0 parsing, token, normalization, and status vocabulary mechanics."
      exports: ["verify_token/2", "check_not_expired/1", "parse_upload_length/1", "metadata_content_type/1", "require_offset_octet_stream/1", "parse_upload_offset/1", "check_offset_match/2", "parse_upload_checksum/1", "normalize_length/1", "check_max_size/2", "effective_length/2", "status_for/1", "http_date/1", "location_base/1"]
    - path: "lib/rindle/upload/tus_plug.ex"
      provides: "Facade-owned method dispatch, persistence-bearing gate order, authorization, and response construction."
  key_links:
    - from: "Rindle.Upload.TusPlug.handle_head/2"
      to: "Rindle.Upload.TusProtocol.verify_token/2"
      via: "secret key and final request path segment before session lookup/authorization"
    - from: "Rindle.Upload.TusPlug.handle_patch/2"
      to: "Rindle.Upload.TusProtocol"
      via: "ordered content-type, offset, length, and checksum validation before any stream call"
    - from: "Rindle.Upload.TusPlug.tus_error/3"
      to: "Rindle.Upload.TusProtocol.status_for/1"
      via: "unchanged reason atom to HTTP status mapping"
---

<objective>
Separate tus token/protocol mechanics from the Plug transport while preserving the exact order-sensitive security and resumability contract.

Purpose: Complete the parsing and protocol-validation responsibility of UPLOAD-01 without moving persistence, authorization policy, or response ownership away from TusPlug.
Output: Hidden `Rindle.Upload.TusProtocol`, ordered facade calls, and strengthened behavior/compiled-boundary proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RUNNING.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-upload-path-clarity/124-CONTEXT.md
@.planning/phases/124-upload-path-clarity/124-RESEARCH.md
@.planning/phases/124-upload-path-clarity/124-PATTERNS.md
@.planning/phases/124-upload-path-clarity/124-VALIDATION.md
@.planning/phases/124-upload-path-clarity/124-01-SUMMARY.md
@lib/rindle/upload/tus_plug.ex
@lib/rindle/upload/tus_creation.ex
@test/rindle/upload/tus_plug_test.exs
@test/rindle/api_surface_boundary_test.exs

<interfaces>
`TusProtocol` receives `Plug.Conn` only for header/path parsing and receives the already resolved secret/max size as explicit arguments. It returns the existing tagged values/reason atoms; it never loads or updates a row, invokes an adapter, authorizes an actor, emits an event, broadcasts, or sends a response. `TusPlug` retains `load_active_session/1`, `authorize_resume/5`, deferred-length persistence, method clauses, `tus_error/3`, and all response headers/bodies.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Extract token and HEAD protocol mechanics without weakening authorization order</name>
  <reversibility rating="reversible">The hidden protocol seam can be recomposed into TusPlug without changing the signed token or HTTP contract.</reversibility>
  <read_first>lib/rindle/upload/tus_plug.ex (`handle_head/2`, token extraction/verification, expiry/session load, status mapping, response helpers, resume authorization); test/rindle/upload/tus_plug_test.exs (token/HEAD and optional authorizer groups); test/rindle/api_surface_boundary_test.exs; 124-RESEARCH.md (`PATCH rejection`, `Token/security`).</read_first>
  <files>lib/rindle/upload/tus_plug.ex, lib/rindle/upload/tus_protocol.ex, test/rindle/upload/tus_plug_test.exs, test/rindle/api_surface_boundary_test.exs</files>
  <behavior>
    - Missing/tampered token maps to 404, validly signed expired token to 401, expired row to 410, and resume rejection to 401 with no existence leak.
    - HEAD and forwarded GET-with-Tus-Resumable retain authoritative offset/length, no-store, expiry, 204, and empty body.
    - Token verification completes before session lookup; session lookup completes before optional authorizer invocation and any storage operation.
  </behavior>
  <action>Add behavior assertions only where the current token/HEAD/authorization suite lacks a direct order or shape lock, then move final-segment token extraction, Plug.Crypto verification with the existing salt, claim expiry validation, HTTP-date formatting, effective-length selection, and reason-to-status mapping into hidden `Rindle.Upload.TusProtocol`. Route HEAD and forwarded GET through tagged collaborator results while retaining session lookup, authorizer configuration/call shape, facade response construction, and all public functions/types in TusPlug. Mark the module and every callable seam hidden in compiled docs, remove moved duplicates, and do not expose claims, add a request object, or re-resolve configuration inside the collaborator.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/rindle/upload/tus_plug_test.exs test/rindle/api_surface_boundary_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - HEAD/token/authorizer statuses, headers, body, and call order remain exact.
    - TusProtocol owns actual token/protocol mechanics and has no persistence/storage/event/response responsibility.
    - TusPlug and Broker stay visible; TusProtocol and all callable seams are compiled-hidden.
  </acceptance_criteria>
  <done>Token and authoritative-offset behavior are traceable through a cohesive hidden protocol owner without changing the security boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract ordered PATCH and creation parsing vocabulary</name>
  <reversibility rating="reversible">Parsing ownership changes only internal call sites; all reason atoms and wire values remain fixed.</reversibility>
  <read_first>lib/rindle/upload/tus_plug.ex (`handle_post/2`, `handle_patch/2`, content-type/offset/length/checksum/metadata/max-size helpers); lib/rindle/upload/tus_protocol.ex from Task 1; test/rindle/upload/tus_plug_test.exs (POST, PATCH gates, checksum/defer groups); 124-PATTERNS.md (`PATCH boundary`).</read_first>
  <files>lib/rindle/upload/tus_plug.ex, lib/rindle/upload/tus_protocol.ex, test/rindle/upload/tus_plug_test.exs</files>
  <behavior>
    - Wrong PATCH content type returns 415 and wrong offset returns 409 before body consumption, temp-file creation, storage calls, or offset mutation.
    - Negative/malformed lengths and offsets, max-size overflow, malformed/unsupported checksums, and checksum mismatch retain exact reason/status/body behavior, including status 460.
    - Deferred length is persisted only after valid parsing/max-size checks and before streaming; metadata remains opaque except the existing `filetype` value.
  </behavior>
  <action>Move the existing pure/header-based parsing and normalization mechanics into TusProtocol: upload length/defer parsing, metadata decoding/content type, PATCH content type, offset parsing/match, checksum parsing, max-size checks, length normalization, and location-base derivation. Keep the full PATCH `with` sequence in TusPlug in its current token → session → authorizer → content type → offset parse/match → effective length/persistence → checksum → stream order. Keep deferred-length row mutation, stream invocation, status response construction, exact body strings, and OPTIONS response construction in the facade. Strengthen behavior-backed no-read/no-storage/no-offset-mutation assertions where necessary; never assert source/helper strings.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/rindle/upload/tus_plug_test.exs --seed 0 && bash scripts/maintainer/refactor_contract.sh</automated>
  </verify>
  <acceptance_criteria>
    - Every protocol atom, header string, status, response body, and ordering invariant in the research inventory is preserved.
    - 415/409 tripwires prove no body read or storage/persistence effect.
    - TusProtocol contains cohesive parsing/normalization mechanics, not facade callbacks or response wrappers.
  </acceptance_criteria>
  <done>Maintainers can read protocol parsing and vocabulary independently while TusPlug visibly retains the load-bearing request order and final response contract.</done>
</task>

</tasks>

<threat_model asvs_level="L1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Bearer path/header input to session lookup | Untrusted tokens and protocol headers decide whether durable session/storage state is reachable. |
| PATCH headers to body/storage lane | Offset, content type, length, and checksum must authorize body consumption and mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-124-01 | Spoofing / Elevation of Privilege | token/session/authorizer order | high | mitigate | Preserve verify-before-load, load-before-authorize, existing authorizer call shape, and exact 404/401/410 tests; block on any high finding. |
| T-124-02 | Tampering | PATCH offset/content/checksum gates | high | mitigate | Keep 415/409 and checksum/size validation before stream mutation; assert body/storage/offset tripwires behaviorally. |
| T-124-03 | Denial of Service | length/checksum parsing | high | mitigate | Preserve non-negative bounded parsing/max size before storage and fixed checksum algorithms without whole-body handling in this collaborator. |
| T-124-SC | Tampering | package supply chain | low | accept | No installs or dependency changes occur. |
</threat_model>

<source_audit>
## Source Coverage for This Plan

| Source | ID | Feature / requirement | Status | Ownership |
|---|---|---|---|---|
| GOAL | — | Clear tus protocol responsibility | COVERED | TusProtocol plus facade-owned transport/order. |
| REQ | UPLOAD-01 | Parsing and validation cohesive; protocol/resumability/errors unchanged | COVERED | Tasks 1-2. |
| REQ | SAFE-01 | API/telemetry/error/schema/release preservation | COVERED | Facade tests, metadata test, contract runner. |
| RESEARCH | order | Auth/load/authorizer and 415/409 before body/storage | COVERED | Facade order remains explicit and tripwire-tested. |
| RESEARCH | protocol inventory | Header/status/claim/deferred/checksum vocabulary exact | COVERED | TusProtocol tagged values consumed by unchanged responses. |
| CONTEXT | discretion | Behavior-backed cohesive internal boundary | COVERED | No source snapshots or public helper surface. |

All deferred protocol/product additions remain excluded by the approved phase boundary.
</source_audit>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- New file/module: `lib/rindle/upload/tus_protocol.ex` — `Rindle.Upload.TusProtocol` (`@moduledoc false`).
- New hidden callable symbols: `verify_token/2`, `check_not_expired/1`, `parse_upload_length/1`, `metadata_content_type/1`, `require_offset_octet_stream/1`, `parse_upload_offset/1`, `check_offset_match/2`, `parse_upload_checksum/1`, `normalize_length/1`, `check_max_size/2`, `effective_length/2`, `status_for/1`, `http_date/1`, and `location_base/1`; all `@doc false`.
- Modified facade/proof: `lib/rindle/upload/tus_plug.ex`, `test/rindle/upload/tus_plug_test.exs`, and `test/rindle/api_surface_boundary_test.exs`.
- No response DSL, request object, public symbol, config/dependency/schema/migration/event/error/storage/CI/UI/Admin change is produced.
</artifacts_this_phase_produces>

<verification>
1. Run task-local gates after each commit.
2. Run `MIX_ENV=test mix compile --force --warnings-as-errors`.
3. Run the complete TusPlug suite and SAFE-01.
4. Confirm forbidden surfaces remain unchanged with `git diff --quiet main...HEAD -- mix.exs mix.lock priv/repo/migrations .github/workflows lib/rindle/admin`.
</verification>

<success_criteria>
- UPLOAD-01 parsing/protocol responsibility is independently readable and behaviorally locked.
- All high security threats are mitigated by preserved order and direct tests.
- Public API, response, error, telemetry, storage, and persistence behavior remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/124-upload-path-clarity/124-02-SUMMARY.md` when done.
</output>

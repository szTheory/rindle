---
phase: 125-behavioral-test-support
plan: "03"
type: execute
wave: 3
depends_on: ["125-02"]
files_modified:
  - test/install_smoke/support/generated_app_helper.ex
  - test/install_smoke/support/generated_app/command_runner.ex
  - test/install_smoke/support/generated_app/workspace.ex
  - test/install_smoke/generated_app_smoke_test.exs
autonomous: true
requirements: [TEST-01, TEST-02, SAFE-01]
must_haves:
  truths:
    - "Generated child commands retain structured exit/output results, the 20-minute bound, stage-labelled failures, and brutal timeout cleanup."
    - "Workspace allocation produces distinct OS-global temporary roots and preserves package/network provenance with no repository fallback."
    - "Command and workspace tests execute behavior instead of reading implementation text."
  artifacts:
    - path: test/install_smoke/support/generated_app/command_runner.ex
      provides: "bounded stage-aware child-process execution"
    - path: test/install_smoke/support/generated_app/workspace.ex
      provides: "temporary consumer/package/provenance lifecycle"
  key_links:
    - from: test/install_smoke/support/generated_app/workspace.ex
      to: test/install_smoke/support/generated_app/command_runner.ex
      via: "explicit cwd/argv/env/stage command calls"
      pattern: "CommandRunner"
---

<objective>
Extract generated consumer process and workspace effects into two cohesive hidden owners with executable behavior proof.

Purpose: Remove the timeout and temporary-directory self-snapshots while preserving packed-consumer provenance and diagnostics.
Output: Hidden CommandRunner and Workspace modules, stable facade orchestration, and objective tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/125-behavioral-test-support/125-RESEARCH.md
@.planning/phases/125-behavioral-test-support/125-PATTERNS.md
@test/install_smoke/support/generated_app_helper.ex
@test/install_smoke/generated_app_smoke_test.exs
@scripts/install_smoke.sh
@.planning/phases/125-behavioral-test-support/125-02-SUMMARY.md

<interfaces>
CommandRunner accepts explicit `cwd`, argv list, env, and stage and returns the current structured result or raises the current stage-labelled diagnostic. Workspace owns OS-global temp allocation, package build/unpack/network source, Phoenix generation, dependency fetch/retry, provenance, and cleanup inputs; it never chooses a profile or report contract.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove bounded child-command behavior and extract CommandRunner</name>
  <files>test/install_smoke/support/generated_app_helper.ex, test/install_smoke/support/generated_app/command_runner.ex, test/install_smoke/generated_app_smoke_test.exs</files>
  <behavior>
    - Fast success returns the current exit code/output/timed-out shape.
    - Nonzero exit and timeout retain the supplied stage in diagnostics; timeout kills the child and marks `timed_out?: true`.
    - Facade callers pass argv/env explicitly and retain current command ordering and 20-minute production timeout.
  </behavior>
  <action>First replace the command-runner source snapshot with executable fast success, nonzero, and short test-time timeout cases through a narrow explicit support seam. Then move `run_cmd/3`, stage-aware raising, timeout task lifecycle, and result shaping into hidden `GeneratedApp.CommandRunner`. Keep the production timeout constant unchanged; allow only an explicit test call to supply a shorter timeout, never an environment/config lookup. Delegate all facade/workspace command sites with their existing stage labels and argv lists. Do not introduce shell-string evaluation, backgrounding, or output masking.</action>
  <verify><automated>MIX_ENV=test mix test test/install_smoke/generated_app_smoke_test.exs --only phase_120_upgrade_contract --seed 0</automated></verify>
  <done>Timeout and diagnostic semantics are proven by executed commands, and all child-process mechanics have one hidden owner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove real temporary roots and extract Workspace</name>
  <files>test/install_smoke/support/generated_app_helper.ex, test/install_smoke/support/generated_app/workspace.ex, test/install_smoke/support/generated_app/command_runner.ex, test/install_smoke/generated_app_smoke_test.exs</files>
  <behavior>
    - Two allocations produce distinct existing directories below the OS temp root and cleanup removes each parent exactly once.
    - Package mode uses an unpacked Hex build and rejects repository-path fallback; network mode preserves `hex:VERSION` provenance.
    - Workspace passes resolved root/source/env facts to callers and never derives profile behavior.
  </behavior>
  <action>Replace the temp-allocation source snapshot with a filesystem behavior test that allocates twice, asserts distinct OS-global roots, and cleans both in `on_exit`. Extract temporary root creation, package build/unpack, Phoenix app generation, package/network source/provenance, dependency fetch/retry, shared environment assembly, and cleanup mechanics into hidden `GeneratedApp.Workspace`. Pass CommandRunner explicitly where commands are needed and return explicit workspace/provenance facts to the facade. Preserve `generated_app_root`, package root, install mode/source, retry, and no-local-fallback behavior exactly.</action>
  <verify><automated>MIX_ENV=test mix test test/install_smoke/generated_app_smoke_test.exs --seed 0 &amp;&amp; bash scripts/maintainer/refactor_contract.sh</automated></verify>
  <done>Workspace and command effects are cohesive, observable, and facade-compatible with no private-source snapshots.</done>
</task>

</tasks>

<threat_model asvs_level="L1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Explicit argv/env → child process | Test harness inputs cross into OS execution and diagnostic output. |
| Repository/package source → temporary consumer | Packed-adopter proof must never silently fall back to repo-local code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-125-02 | Repudiation | command result | high | mitigate | Preserve original status, stage, timeout flag, and direct foreground result; executable tests cover all branches. |
| T-125-03 | Information disclosure | command diagnostics | medium | mitigate | Preserve bounded existing output behavior and never dump the full environment. |
| T-125-04 | Tampering | package provenance | high | mitigate | Behavior tests and generated-app report assertions reject repository fallback and retain unpacked/network facts. |
| T-125-SC | Tampering | package supply chain | low | accept | Existing dependencies only; temporary generated consumer uses current authorized package/network paths. |
</threat_model>

<verification>
Run generated-app tests, SAFE-01, and confirm all current facade symbols and report fields remain available.
</verification>

<success_criteria>
- Command and workspace responsibilities are separate and explicit.
- Both former implementation snapshots are replaced with executed behavior.
- Packed-adopter provenance and SAFE-01 are unchanged.
</success_criteria>

<output>
Create `.planning/phases/125-behavioral-test-support/125-03-SUMMARY.md` when done.
</output>

---
phase: 125-behavioral-test-support
plan: "07"
type: execute
wave: 7
depends_on: ["125-06"]
files_modified:
  - test/install_smoke/docs_parity_test.exs
  - test/install_smoke/docs_parity/support.ex
  - test/install_smoke/docs_parity/install_and_migrations_test.exs
autonomous: true
requirements: [TEST-03, SAFE-01]
must_haves:
  truths:
    - "Shared docs support owns only read-once loading, section/order/fence/migration parsing, whitespace normalization, and compiled-doc lookup."
    - "Install/migration assertions retain exact pinned Rindle.Migration, host-owned Oban, public/default schema, upgrade, troubleshooting, and generated fixture parity."
    - "Failures name the install or migration public contract rather than an extraction detail."
  artifacts:
    - path: test/install_smoke/docs_parity/support.ex
      provides: "explicit shared docs-parity mechanics"
    - path: test/install_smoke/docs_parity/install_and_migrations_test.exs
      provides: "install, migration, upgrade, and ownership contract suite"
  key_links:
    - from: test/install_smoke/docs_parity/install_and_migrations_test.exs
      to: test/install_smoke/docs_parity/support.ex
      via: "named document map and explicit helper calls"
      pattern: "DocsParity.Support"
---

<objective>
Create the shared documentation-parity seam and move the highest-coupling install/migration contract domain intact.

Purpose: Prove domain-based decomposition without duplicating document reads or mixing release/admin ownership.
Output: Shared Support plus one complete install/migrations suite; remaining aggregate assertions stay green for later moves.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/125-behavioral-test-support/125-RESEARCH.md
@.planning/phases/125-behavioral-test-support/125-PATTERNS.md
@test/install_smoke/docs_parity_test.exs
@test/install_smoke/release_docs_parity_test.exs
@.planning/phases/125-behavioral-test-support/125-06-SUMMARY.md

<interfaces>
`Rindle.InstallSmoke.DocsParity.Support` exposes explicit test-only document loading and the current `introductory_section`, ordering, section extraction, fenced Elixir extraction, migration-call parsing, compiled docs/moduledoc, and whitespace normalization semantics. It does not own assertions or whole-document snapshots.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Extract shared read-once documentation mechanics</name>
  <files>test/install_smoke/docs_parity_test.exs, test/install_smoke/docs_parity/support.ex</files>
  <behavior>
    - A named path map loads each requested shipped document once and returns explicit keyed contents.
    - Section/order/fence/migration/compiled-doc/normalization helpers retain current failure behavior and messages identify the missing boundary.
    - Support contains no public-contract assertion or generic full-document snapshot.
  </behavior>
  <action>Create hidden `Rindle.InstallSmoke.DocsParity.Support` and move only the genuinely shared helper block from the aggregate. Add a named `load_docs!/1`/equivalent that reads an explicit map once; keep suite-owned path lists and assertions outside Support. Update the aggregate to import/alias explicit helpers and keep every existing test green. Do not move release-specific setup from `release_docs_parity_test.exs`, change docs, or create a helper that hides which contract failed.</action>
  <verify><automated>MIX_ENV=test mix test test/install_smoke/docs_parity_test.exs test/install_smoke/release_docs_parity_test.exs --seed 0</automated></verify>
  <done>Shared mechanics have one narrow owner while all 1,118-line aggregate assertions still pass unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move install, migration, upgrade, and ownership parity as one domain</name>
  <files>test/install_smoke/docs_parity_test.exs, test/install_smoke/docs_parity/install_and_migrations_test.exs, test/install_smoke/docs_parity/support.ex</files>
  <behavior>
    - The domain retains tests for pinned Rindle.Migration, host-owned Oban, fresh fixtures, compiled migration moduledoc, populated move, troubleshooting ownership, historical legacy scope, and upgrade navigation/sequence.
    - Existing test assertions are moved equivalently, not weakened, and each setup map contains only this domain's documents/source contracts.
    - The aggregate no longer duplicates moved tests.
  </behavior>
  <action>Create `InstallAndMigrationsTest` and move current aggregate blocks for migration/install/upgrade ownership (including lines 176–527 and 653–738 by the pattern map) as whole tests. Use Support for shared mechanics and a domain-specific read-once setup map. Preserve assertion content and compiled/generated migration links exactly; rename only module/failure context needed to name the owning public contract. Remove moved blocks from the aggregate and run both files together to detect omission or duplicate module/test registration.</action>
  <verify><automated>MIX_ENV=test mix test test/install_smoke/docs_parity_test.exs test/install_smoke/docs_parity/install_and_migrations_test.exs --seed 0 &amp;&amp; bash scripts/maintainer/refactor_contract.sh</automated></verify>
  <done>The first documentation domain is independently runnable with equivalent assertions, shared mechanics, and explicit install/migration ownership.</done>
</task>

</tasks>

<threat_model asvs_level="L1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Shipped documents/compiled docs → domain assertions | A split can silently drop or duplicate adopter contract checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-125-04 | Tampering | install/migration documentation contract | high | mitigate | Move whole tests, run old remainder plus new domain together, retain compiled/generated fixture links, and run SAFE-01. |
| T-125-SC | Tampering | package supply chain | low | accept | No dependency or package changes. |
</threat_model>

<verification>
Run aggregate remainder, new domain, release domain, and SAFE-01; compare test-name inventory before/after for moved blocks.
</verification>

<success_criteria>
- Shared support is narrow and read-once.
- Install/migration domain is complete and independently runnable.
- No docs/product/release behavior changes.
</success_criteria>

<output>
Create `.planning/phases/125-behavioral-test-support/125-07-SUMMARY.md` when done.
</output>

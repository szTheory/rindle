---
phase: 125-behavioral-test-support
plan: "06"
type: execute
wave: 6
depends_on: ["125-05"]
files_modified:
  - test/install_smoke/support/generated_app_helper.ex
  - test/install_smoke/support/generated_app/smoke_source.ex
  - test/install_smoke/support/generated_app/profile_helpers.ex
  - test/install_smoke/generated_app_smoke_test.exs
  - test/install_smoke/phoenix_tus_truth_parity_test.exs
autonomous: true
requirements: [TEST-01, TEST-02, SAFE-01]
must_haves:
  truths:
    - "Generated image/video/tus/gcs/mux/upgrade lifecycle tests and profile helpers retain exact emitted behavior and report fields."
    - "Phoenix tus parity observes public compiled metadata/capabilities and a real generated tus lifecycle report, never helper or library implementation text."
    - "GeneratedAppHelper remains the sole facade and all seven extracted collaborators remain hidden test support."
  artifacts:
    - path: test/install_smoke/support/generated_app/smoke_source.ex
      provides: "generated lifecycle and upgrade test source"
    - path: test/install_smoke/support/generated_app/profile_helpers.ex
      provides: "profile-specific generated helper source and app naming"
  key_links:
    - from: test/install_smoke/phoenix_tus_truth_parity_test.exs
      to: generated tus report and Rindle.LiveView compiled docs
      via: "observable lifecycle + Code.fetch_docs/function_exported"
      pattern: "Code.fetch_docs|function_exported"
---

<objective>
Finish the GeneratedAppHelper decomposition and replace Phoenix tus implementation snapshots with consumer outcomes and compiled metadata.

Purpose: Close TEST-01/TEST-02 for generated support while preserving every packed-adopter profile behind the original facade.
Output: Hidden SmokeSource/ProfileHelpers, a thin orchestration facade, objective tus parity, and packed image proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RUNNING.md
@.planning/phases/125-behavioral-test-support/125-RESEARCH.md
@.planning/phases/125-behavioral-test-support/125-PATTERNS.md
@test/install_smoke/support/generated_app_helper.ex
@test/install_smoke/generated_app_smoke_test.exs
@test/install_smoke/phoenix_tus_truth_parity_test.exs
@scripts/install_smoke.sh
@.planning/phases/125-behavioral-test-support/125-05-SUMMARY.md

<interfaces>
SmokeSource owns emitted image/video/tus/gcs/mux lifecycle and upgrade test bodies. ProfileHelpers owns profile-specific helper source/imports/tags/app naming. Both receive explicit app/profile/fixture inputs. Facade retains proof entry points, scenario orchestration, report normalization, cleanup delegation, and all current exported names/defaults.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Extract emitted lifecycle source by cohesive generated responsibility</name>
  <files>test/install_smoke/support/generated_app_helper.ex, test/install_smoke/support/generated_app/smoke_source.ex, test/install_smoke/support/generated_app/profile_helpers.ex, test/install_smoke/generated_app_smoke_test.exs</files>
  <behavior>
    - Image, video, tus, gcs, mux, and upgrade branches emit the same generated tests, imports/tags/helpers, app names, and JSON report keys.
    - The facade calls the two source owners with explicit app/profile/fixture inputs and performs no large inline source-template ownership.
    - Existing scenario tags/profile gates and generated command sequence remain unchanged.
  </behavior>
  <action>Create hidden `GeneratedApp.SmokeSource` for lifecycle/upgrade test bodies and hidden `GeneratedApp.ProfileHelpers` for profile helper/import/tag/app-name source. Move the existing branches intact, split by responsibility rather than arbitrary line count, and pass explicit app/profile/fixture facts from the facade. Keep generated test bodies in the temporary app as the observer; do not fold them into orchestration callbacks or generic templates. Remove moved private functions and preserve facade exports, scenario predicates, tags, command ordering, and report normalization.</action>
  <verify><automated>MIX_ENV=test mix test test/install_smoke/generated_app_smoke_test.exs --seed 0</automated></verify>
  <done>The 3,955-line mixed owner is replaced by a stable orchestration facade plus seven focused hidden modules, with all profile cases green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace Phoenix tus source snapshots and prove packed facade authority</name>
  <files>test/install_smoke/generated_app_smoke_test.exs, test/install_smoke/phoenix_tus_truth_parity_test.exs, test/install_smoke/support/generated_app_helper.ex</files>
  <behavior>
    - Public `Rindle.LiveView.allow_tus_upload/4` and its documented uploader/consume/completion contract are verified through compiled exports/docs.
    - The real tus generated-app report exposes endpoint, uploader, upload URL, session/asset identifiers, completion surface, state sequence, and error state matching shipped guides.
    - Packed image install still builds/unpacks outside the repo, compiles, migrates, boots, and reports no local fallback.
  </behavior>
  <action>Remove `@live_view_path`/`@generated_helper_path` implementation reads from PhoenixTusTruthParityTest. Keep shipped guide reads because documents are the contract; query `function_exported?/3`, `Code.fetch_docs/1`, storage capabilities, and an explicit tus report contract exposed by the stable facade/real generated tus result. Extend the tus generated-app assertion group only as needed for endpoint/uploader/session/asset/completion/state/error facts. Then run the packaged image consumer through `scripts/install_smoke.sh image`. Do not assert collaborator strings, change public docs/library code, or alter profile report schemas.</action>
  <verify><automated>MIX_ENV=test mix test test/install_smoke/generated_app_smoke_test.exs test/install_smoke/phoenix_tus_truth_parity_test.exs --seed 0 &amp;&amp; bash scripts/maintainer/refactor_contract.sh &amp;&amp; bash scripts/install_smoke.sh image</automated></verify>
  <done>Generated helper self-inspection is eliminated, public/consumer tus truth agrees, and the unchanged facade passes a real packed image proof.</done>
</task>

</tasks>

<threat_model asvs_level="L1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Emitted source → temporary consumer runtime | Large source movement can alter lifecycle behavior or report truth. |
| Public docs/metadata → generated tus outcome | Parity must compare shipped contracts without inspecting private implementation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-125-04 | Tampering | generated profile/lifecycle source | high | mitigate | All profile cases, compiled docs/capabilities, tus outcome fields, packed image proof, and SAFE-01 must pass. |
| T-125-SC | Tampering | package supply chain | low | accept | Existing packed Hex build path and lockfile only; no dependency changes. |
</threat_model>

<verification>
Run the two focused suites, SAFE-01, and packed image smoke; audit that no test reads internal generated support or `lib/rindle/live_view.ex` for implementation strings.
</verification>

<success_criteria>
- TEST-01 is complete across facade plus seven cohesive hidden owners.
- TEST-02 is complete for generated-app and Phoenix tus offenders.
- Packed consumer, public compiled contracts, and SAFE-01 remain exact.
</success_criteria>

<output>
Create `.planning/phases/125-behavioral-test-support/125-06-SUMMARY.md` when done.
</output>

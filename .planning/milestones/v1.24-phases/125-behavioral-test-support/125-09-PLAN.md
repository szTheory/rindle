---
phase: 125-behavioral-test-support
plan: "09"
type: execute
wave: 9
depends_on: ["125-08"]
files_modified:
  - test/install_smoke/docs_parity_test.exs
  - test/install_smoke/docs_parity/product_and_admin_test.exs
  - .github/workflows/ci.yml
  - RUNNING.md
autonomous: true
requirements: [TEST-03, SAFE-01]
must_haves:
  truths:
    - "Owner-erasure/user-flow/Admin assertions have one product-and-admin domain owner and the 1,118-line aggregate is retired."
    - "Proof invokes every split docs-parity domain explicitly before link hygiene, with no missing or duplicate domain."
    - "CI Summary needs, skip-as-pass semantics, workflow names/topology, release gates, and single-run Quality coverage remain unchanged."
  artifacts:
    - path: test/install_smoke/docs_parity/product_and_admin_test.exs
      provides: "owner erasure, user-flow roadmap, batch erasure, and Admin truth parity"
    - path: .github/workflows/ci.yml
      provides: "exact focused Proof command listing all four new domains"
  key_links:
    - from: .github/workflows/ci.yml
      to: test/install_smoke/docs_parity/*_test.exs
      via: "one explicit Proof mix test argv"
      pattern: "docs_parity/.+_test.exs"
---

<objective>
Complete the documentation-domain split and wire exact Proof coverage without changing CI topology.

Purpose: Retire the oversized aggregate only after every public-contract domain has an explicit owner and carrier.
Output: Product/Admin suite, removed aggregate, exact Proof command, and reconciled RUNNING contract.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RUNNING.md
@.github/workflows/ci.yml
@.planning/phases/125-behavioral-test-support/125-RESEARCH.md
@.planning/phases/125-behavioral-test-support/125-PATTERNS.md
@test/install_smoke/docs_parity_test.exs
@test/install_smoke/docs_parity/support.ex
@test/install_smoke/refactor_contract_test.exs
@.planning/phases/125-behavioral-test-support/125-08-SUMMARY.md

<interfaces>
Proof retains one `mix test` step before `check_docs_links.sh`; only its explicit file argv changes from the retired aggregate to the four new domain test files. `release_docs_parity_test.exs` and streaming/cancel suites retain their existing carriers. Quality coverage command and CI Summary topology are byte-preservation surfaces.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Move product, owner-erasure, user-flow, and Admin truth then retire the aggregate</name>
  <files>test/install_smoke/docs_parity_test.exs, test/install_smoke/docs_parity/product_and_admin_test.exs</files>
  <behavior>
    - Canonical owner-erasure support truth, batch erasure, thin guide pointers, Admin facade/guide/extras/README truth, and demand-gated tus/mux roadmap assertions remain exact.
    - Product/Admin setup reads only owned documents and uses shared Support.
    - After moving whole tests, the aggregate has no assertion body and is deleted rather than becoming an empty compatibility shell.
  </behavior>
  <action>Create `ProductAndAdminTest` and move the remaining whole aggregate blocks (current lines 879–1024) with equivalent assertions and domain-specific failure ownership. Use DocsParity.Support, remove the moved tests, and delete `docs_parity_test.exs` once empty. Run all four domain suites plus the separate release suite explicitly to prove complete ownership. Do not edit product/Admin docs or move release/streaming tests.</action>
  <verify><automated>MIX_ENV=test mix test test/install_smoke/docs_parity/install_and_migrations_test.exs test/install_smoke/docs_parity/onboarding_and_capabilities_test.exs test/install_smoke/docs_parity/operations_test.exs test/install_smoke/docs_parity/product_and_admin_test.exs test/install_smoke/release_docs_parity_test.exs --seed 0</automated></verify>
  <done>The oversized aggregate is gone and all four public-contract domains are independently runnable with equivalent assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Point Proof at every domain with exact topology preservation</name>
  <files>.github/workflows/ci.yml, RUNNING.md</files>
  <behavior>
    - The existing Proof step invokes exactly the four split domain files in one Mix command, then link hygiene runs as before.
    - No Quality coverage command, job/step name, needs graph, trigger, permission, matrix, CI Summary expression, or release workflow changes.
    - RUNNING names the four domain carriers and retains the single-run coverage/required-check contract.
  </behavior>
  <action>Replace only the Proof docs-parity command's retired path with the exact four domain file paths in one foreground `mix test` invocation. Update RUNNING's Proof description/reproduction command to those domains. Add no job, step, invocation, matrix, partition, needs edge, or second coverage run per R-03. Validate the workflow diff is restricted to that argv and the documentation diff to corresponding path text; preserve `name: CI`, `CI Summary`, skip-as-pass, release coupling, and the Quality `coveralls.multiple` line exactly.</action>
  <verify><automated>MIX_ENV=test mix test test/install_smoke/docs_parity/install_and_migrations_test.exs test/install_smoke/docs_parity/onboarding_and_capabilities_test.exs test/install_smoke/docs_parity/operations_test.exs test/install_smoke/docs_parity/product_and_admin_test.exs --seed 0 &amp;&amp; MIX_ENV=test mix test test/install_smoke/refactor_contract_test.exs --seed 0 &amp;&amp; bash scripts/maintainer/refactor_contract.sh</automated></verify>
  <done>Proof has exact, explicit coverage of every docs domain with zero topology, coverage-count, required-check, or release-gate expansion.</done>
</task>

</tasks>

<threat_model asvs_level="L1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Domain test files → Proof merge gate | A carrier edit can silently stop executing a public contract domain. |
| Workflow diff → release/required-check topology | Adjacent YAML changes can weaken or duplicate authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-125-04 | Tampering | product/Admin parity | high | mitigate | Move whole tests and run all domains/release suite explicitly before aggregate deletion. |
| T-125-05 | Tampering / Denial of service | Proof/Quality/CI Summary | high | mitigate | Restrict workflow diff to one argv, execute each domain once, and assert Quality/needs/summary/release surfaces unchanged. |
| T-125-SC | Tampering | package supply chain | low | accept | No dependency/install action occurs. |
</threat_model>

<verification>
Run all four domains, release domain, SAFE-01, and a workflow diff audit proving only the Proof argv changed and the sole Quality coverage invocation remains one per existing job.
</verification>

<success_criteria>
- TEST-03 is complete with four focused public-contract owners plus existing separate release/streaming domains.
- Proof explicitly runs every new domain.
- CI topology, coverage invocation count, CI Summary, release gates, and SAFE-01 are unchanged.
</success_criteria>

<output>
Create `.planning/phases/125-behavioral-test-support/125-09-SUMMARY.md` when done.
</output>

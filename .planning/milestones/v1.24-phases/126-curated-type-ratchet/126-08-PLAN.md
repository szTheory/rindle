---
phase: 126-curated-type-ratchet
plan: "08"
type: execute
wave: 8
depends_on: ["126-07"]
files_modified:
  - .dialyzer_ignore.exs
  - .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md
  - lib/rindle.ex
  - lib/rindle/upload/broker.ex
  - lib/rindle/workers/promote_asset.ex
  - test/rindle/convenience_api_test.exs
  - test/rindle/upload/broker_test.exs
  - test/rindle/workers/promote_asset_test.exs
autonomous: true
requirements: [TYPE-01, TYPE-02, SAFE-01]
must_haves:
  truths:
    - "Facade, Broker, and both PromoteAsset atom filters are supported-probed before source edits."
    - "Public facade, broker lifecycle, promotion behavior, telemetry, and errors remain unchanged."
    - "All 45 starting entries have complete supported dispositions by this plan's end."
  artifacts:
    - path: .dialyzer_ignore.exs
      provides: "complete curated candidate baseline"
    - path: .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md
      provides: "45-of-45 disposition ledger before final authority"
  key_links:
    - from: lib/rindle.ex
      to: test/rindle/convenience_api_test.exs
      via: "public delegate signatures and result terms"
      pattern: "Rindle"
    - from: lib/rindle/workers/promote_asset.ex
      to: test/rindle/workers/promote_asset_test.exs
      via: "promotion/idempotency lifecycle"
      pattern: "PromoteAsset"
---

<objective>
Reconcile facade, Broker, and PromoteAsset historical filters and complete all 45 dispositions.

Purpose: Finish the remaining public/lifecycle boundary separately from Tus and Mux.
Output: A complete 45-entry ledger, supported historical-atom dispositions, and the final candidate baseline.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/126-curated-type-ratchet/126-RESEARCH.md
@.planning/phases/126-curated-type-ratchet/126-PATTERNS.md
@.planning/phases/126-curated-type-ratchet/126-VALIDATION.md
@.planning/phases/126-curated-type-ratchet/126-07-SUMMARY.md
@.dialyzer_ignore.exs
@scripts/maintainer/refactor_contract.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe facade, Broker, and PromoteAsset atoms on supported 1.17/27</name>
  <precondition>The Tus/Mux slice is supported-green and `gh auth status` succeeds.</precondition>
  <read_first>.dialyzer_ignore.exs, lib/rindle.ex, lib/rindle/upload/broker.ex, lib/rindle/workers/promote_asset.ex, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</read_first>
  <files>.dialyzer_ignore.exs, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</files>
  <action>Remove only legacy opaque/pattern atoms for `rindle.ex`, Broker, and both PromoteAsset entries. Commit/push the source-unchanged probe, obtain or verified-reuse the exact-head Nightly receipt under the timestamped capture, unique-ID polling, and terminal named-job protocol in `126-PATTERNS.md`, inspect Dialyzer and Nightly Summary, and record every absent/reproduced discriminator. No owner fix or restoration may precede this receipt.</action>
  <verify><automated>set -e; HEAD_SHA=$(git rev-parse HEAD); REPO=$(gh repo view --json nameWithOwner -q .nameWithOwner); BRANCH=$(git branch --show-current); RUN_ID=$(gh api --method GET --paginate --slurp "repos/$REPO/actions/workflows/nightly.yml/runs" -f branch="$BRANCH" -f event=workflow_dispatch -f per_page=100 | jq -er --arg sha "$HEAD_SHA" '[.[].workflow_runs[] | select(.head_sha == $sha and .event == "workflow_dispatch" and .status == "completed")] | if length == 1 then .[0].id else error("expected exactly one terminal exact-head Nightly receipt") end'); RUN_JSON=$(gh run view "$RUN_ID" --json headSha,event,status,conclusion,jobs); test "$(printf '%s' "$RUN_JSON" | jq -r .headSha)" = "$HEAD_SHA"; test "$(printf '%s' "$RUN_JSON" | jq -r .event)" = workflow_dispatch; test "$(printf '%s' "$RUN_JSON" | jq -r .status)" = completed; gh run view "$RUN_ID" --log > /tmp/rindle-126-final-atoms-probe.log; test -s /tmp/rindle-126-final-atoms-probe.log; rg -q 'lib/rindle.ex|upload/broker|promote_asset' .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md; MIX_ENV=test mix test test/install_smoke/dialyzer_ignore_policy_test.exs --seed 0</automated></verify>
  <acceptance_criteria>Every remaining historical atom has exact supported-head evidence before any affected source changes.</acceptance_criteria>
  <done>The last owner group is classified from the sole acceptance authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile facade, Broker, and PromoteAsset and close the 45-entry ledger</name>
  <read_first>.planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md, lib/rindle.ex, test/rindle/convenience_api_test.exs, lib/rindle/upload/broker.ex, test/rindle/upload/broker_test.exs, lib/rindle/workers/promote_asset.ex, test/rindle/workers/promote_asset_test.exs, .dialyzer_ignore.exs</read_first>
  <files>lib/rindle.ex, lib/rindle/upload/broker.ex, lib/rindle/workers/promote_asset.ex, test/rindle/convenience_api_test.exs, test/rindle/upload/broker_test.exs, test/rindle/workers/promote_asset_test.exs, .dialyzer_ignore.exs, .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md</files>
  <behavior>
    - Public convenience delegates preserve signatures, return/error terms, and opaque Ecto/Oban handling.
    - Broker initiation/completion, compensation, persistence, telemetry, broadcasts, and result shaping remain unchanged.
    - PromoteAsset preserves promotion/idempotency, transitions, retries/discards, telemetry, and errors.
  </behavior>
  <action>Remove every supported-absent historical atom. For reproduced actionable findings, add owner behavior first and tighten producer/consumer specs or private patterns without unwrapping opaque values. Retain only exact supported analyzer noise with adjacent evidence. Reconcile all 45 starting tuples to one disposition, run owner tests, policy, and SAFE-01, commit/push the candidate, obtain or verified-reuse its exact-head Nightly receipt per `126-PATTERNS.md`, poll the captured ID to terminal, and require the overall workflow, Dialyzer, and Nightly Summary to conclude success.</action>
  <verify><automated>set -e; MIX_ENV=test mix test test/rindle/convenience_api_test.exs test/rindle/upload/broker_test.exs test/rindle/workers/promote_asset_test.exs --seed 0; bash scripts/maintainer/refactor_contract.sh; MIX_ENV=test mix test test/install_smoke/dialyzer_ignore_policy_test.exs --seed 0; test "$(rg -c '^\| `\{' .planning/phases/126-curated-type-ratchet/126-TYPE-EVIDENCE.md)" -eq 45; HEAD_SHA=$(git rev-parse HEAD); REPO=$(gh repo view --json nameWithOwner -q .nameWithOwner); BRANCH=$(git branch --show-current); RUN_ID=$(gh api --method GET --paginate --slurp "repos/$REPO/actions/workflows/nightly.yml/runs" -f branch="$BRANCH" -f event=workflow_dispatch -f per_page=100 | jq -er --arg sha "$HEAD_SHA" '[.[].workflow_runs[] | select(.head_sha == $sha and .event == "workflow_dispatch" and .status == "completed")] | if length == 1 then .[0].id else error("expected exactly one terminal exact-head Nightly receipt") end'); RUN_JSON=$(gh run view "$RUN_ID" --json headSha,event,status,conclusion,jobs); test "$(printf '%s' "$RUN_JSON" | jq -r .headSha)" = "$HEAD_SHA"; test "$(printf '%s' "$RUN_JSON" | jq -r .event)" = workflow_dispatch; test "$(printf '%s' "$RUN_JSON" | jq -r .status)" = completed; test "$(printf '%s' "$RUN_JSON" | jq -r .conclusion)" = success; test "$(gh run view "$RUN_ID" --json jobs --jq '.jobs[] | select(.name == "Dialyzer") | .conclusion')" = success; test "$(gh run view "$RUN_ID" --json jobs --jq '.jobs[] | select(.name == "Nightly Summary") | .conclusion')" = success</automated></verify>
  <acceptance_criteria>All 45 entries have a complete disposition, no obsolete/actionable suppression remains, retained noise is exact/supported, owner behavior and SAFE-01 pass, and the exact candidate's terminal overall workflow, Dialyzer, and Nightly Summary conclusions are success.</acceptance_criteria>
  <done>The curated baseline is fully reconciled and ready for final authority.</done>
</task>

</tasks>

<threat_model asvs_level="L1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Public facade/broker/worker → adopter lifecycle | Type edits could alter public results, persistence, telemetry, or errors. |
| Opaque Ecto/Oban values → local patterns | Unsafe inspection could violate library boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-126-01 | Tampering | final atom group | high | mitigate | Supported source-unchanged probe and complete 45-row ledger. |
| T-126-07 | Tampering | opaque values | high | mitigate | Tighten producer/consumer boundaries without unwrapping opaque terms. |
| T-126-08 | Tampering / Repudiation | facade/lifecycle workers | high | mitigate | Owner behavior, telemetry/error contracts, SAFE-01, and exact-head green. |
| T-126-SC | Tampering | package supply chain | low | accept | No dependency or package install is introduced. |
</threat_model>

<verification>
Require a source-unchanged supported probe, focused facade/Broker/PromoteAsset tests, policy, SAFE-01, 45 complete dispositions, and exact-head supported green.
</verification>

<success_criteria>
- Every starting filter is obsolete, actionable-fixed, or explicitly supported analyzer noise.
- Public API, lifecycle, telemetry, error, dependency, Admin, and release behavior remains unchanged.
- The candidate baseline passes the exact supported Nightly gate.
</success_criteria>

<output>
Create `.planning/phases/126-curated-type-ratchet/126-08-SUMMARY.md` when done.
</output>

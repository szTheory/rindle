---
phase: 123-runtime-operations-decomposition
plan: "02"
type: execute
wave: 2
depends_on: ["123-01"]
files_modified:
  - lib/rindle/migration/v1.ex
  - lib/rindle/migration/v1/snapshot.ex
  - lib/rindle/migration/v1/preflight.ex
  - test/rindle/migration_fast_test.exs
  - test/rindle/migration_test.exs
autonomous: true
requirements: [OPS-02, SAFE-01]
must_haves:
  truths:
    - "Per D-123-03, Migration.V1 remains authoritative for the exact six-table-plus-marker catalog, DDL, move order, transaction behavior, and reversal."
    - "Per D-123-04, named snapshot and preflight internals make directional refusal precedence explicit without duplicating or configuring the owned relation catalog."
    - "Forward and reverse moves still refuse before DDL, preserve exact outcome/error vocabulary, and roll back every injected or lock failure."
  artifacts:
    - path: "lib/rindle/migration/v1.ex"
      provides: "Unchanged V1 migration façade, catalog authority, DDL, ordered movement, and bounded public errors."
    - path: "lib/rindle/migration/v1/snapshot.ex"
      provides: "Read-only catalog, marker, ownership, and privilege observation over V1-provided requirements."
    - path: "lib/rindle/migration/v1/preflight.ex"
      provides: "Deterministic directional classification with visible ordered refusal precedence."
  key_links:
    - from: "lib/rindle/migration/v1.ex"
      to: "lib/rindle/migration/v1/snapshot.ex"
      via: "fixed V1-owned relation catalog and version requirements passed into one immutable read"
    - from: "lib/rindle/migration/v1.ex"
      to: "lib/rindle/migration/v1/preflight.ex"
      via: "direction plus immutable snapshot to existing outcome vocabulary"
    - from: "preflight success"
      to: "V1 move_owned_relations/3"
      via: "refusal-before-provision/DDL sequencing inside the existing host transaction"
---

<objective>
Separate populated-install observation and validation from mutation while keeping all migration authority and effects in `Rindle.Migration.V1` (D-123-03, D-123-04).

Purpose: Satisfy OPS-02 by making refusal decisions inspectable without weakening the fixed ownership and transaction safety contract.
Output: Internal snapshot and directional preflight modules wired through the unchanged V1 façade, with full forward/reverse database proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-runtime-operations-decomposition/123-CONTEXT.md
@.planning/phases/123-runtime-operations-decomposition/123-PATTERNS.md
@.planning/phases/123-runtime-operations-decomposition/123-RESEARCH.md
@.planning/phases/123-runtime-operations-decomposition/123-01-SUMMARY.md
@.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-VERIFICATION.md
@lib/rindle/migration.ex
@lib/rindle/migration/v1.ex
@test/rindle/migration_fast_test.exs
@test/rindle/migration_test.exs

<interfaces>
`Rindle.Migration` and `Rindle.Migration.V1` retain all existing documented and `@doc false` signatures, including directional moves/preflights and catalog helpers. `Snapshot` accepts V1-owned catalog/version requirements and only observes database state. `Preflight.classify/2` accepts a direction and snapshot and returns the exact current atoms/tuples; it neither queries nor mutates.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove observe-classify-refuse on the forward populated move</name>
  <files>lib/rindle/migration/v1.ex, lib/rindle/migration/v1/snapshot.ex, lib/rindle/migration/v1/preflight.ex, test/rindle/migration_fast_test.exs</files>
  <behavior>
    - `preflight_public_to_rindle/0` returns the exact existing idempotent, provisionable, movable, or refusal result for each fixed-catalog snapshot.
    - Refusal precedence remains deterministic and occurs before target provisioning or relation movement.
    - `current_version/0`, `marker_table/0`, `rindle_tables/0`, `owned_relations/0`, and `catalog_requirements/0` retain exact values and remain owned by V1.
  </behavior>
  <action>Per D-123-03, begin with fast behavioral cases that exercise the public/hidden V1 boundary and fixed catalog rather than private source text. Move only `migration_snapshot/0` observation mechanics into `Rindle.Migration.V1.Snapshot`, passing `owned_relations/0`, marker/version, source/target schemas, and existing privilege test seams from V1 so no catalog is copied or generalized. Per D-123-04, implement one internal `Preflight.classify(direction, snapshot)` with explicit ordered branches for `:public_to_rindle` and wire `preflight_public_to_rindle/0` through observe then classify. Retain the existing result atoms/tuples and bounded error path in V1; do not move provisioning, DDL, movement, failure injection, transaction behavior, or public exports.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/rindle/migration_fast_test.exs --seed 0</automated>
  </verify>
  <done>The forward populated-install path reads one bounded snapshot, classifies it with obvious current precedence, refuses before mutation, and leaves all catalog/mutation authority in V1 per D-123-03/D-123-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve reverse classification, ordered moves, and rollback safety</name>
  <files>lib/rindle/migration/v1.ex, lib/rindle/migration/v1/preflight.ex, test/rindle/migration_test.exs</files>
  <behavior>
    - Reverse preflight preserves its exact idempotence/refusal vocabulary and ordered first refusal.
    - Successful forward and reverse moves affect only the fixed Rindle catalog in its existing order; host-owned Oban and schema migration relations remain untouched.
    - Injected movement failure, lock-not-available, invalid marker, privilege denial, and mixed-catalog cases retain current exceptions/messages and transaction rollback.
  </behavior>
  <action>Per D-123-04, add the reverse direction to the shared classifier only if its branch order remains plainly readable; if shared predicates obscure either direction's refusal precedence, use two thin direction functions in the same `Preflight` module over shared pure predicates. Wire `preflight_rindle_to_public/0` without changing result terms. Per D-123-03, leave `move_owned_relations/3`, `move_relation!/3`, schema provisioning, fixed catalog constants, error rendering, failure injection, and all forward/reverse transaction effects in V1. Strengthen `migration_test.exs` at the database behavior boundary for no-DDL-on-refusal, exact owned-relation movement order/effect, host-table exclusion, rollback, lock guidance, and safe reverse. Per D-123-07/D-123-08, run fresh compile, the entire focused migration matrix, and SAFE-01 before runtime-status work; do not add dynamic discovery, nested transactions, schema redesign, new migration files, or source-string assertions.</action>
  <verify>
    <automated>MIX_ENV=test mix compile --force &amp;&amp; MIX_ENV=test mix test test/rindle/migration_fast_test.exs test/rindle/migration_test.exs --seed 0 &amp;&amp; bash scripts/maintainer/refactor_contract.sh</automated>
  </verify>
  <done>Both directions retain exact refusal and mutation behavior, populated-install rollback/reversal proof passes, and SAFE-01 is green before Plan 123-03 starts per D-123-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PostgreSQL catalog/privilege state to preflight | Host-owned database observations determine whether Rindle may mutate owned relations. |
| Preflight decision to migration DDL | A mistaken success or reordered refusal could move host data or mutate before safety checks finish. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-123-04 | Tampering | fixed migration catalog | critical | mitigate | Keep the catalog solely in V1, pass it into snapshot inspection, and assert exact owned/host relation effects. |
| T-123-05 | Elevation of Privilege | privilege/ownership preflight | high | mitigate | Preserve ownership and privilege observations plus ordered fail-before-DDL refusals. |
| T-123-06 | Repudiation | transaction rollback and lock failures | high | mitigate | Run real database failure-injection/lock tests and retain existing bounded exception guidance. |
| T-123-SC | Tampering | package supply chain | low | accept | No package-manager install occurs in this plan; existing dependency lock remains unchanged. |
</threat_model>

<verification>
- Fresh test compile succeeds.
- Fast catalog/API tests and the full migration integration suite pass.
- `bash scripts/maintainer/refactor_contract.sh` passes after the complete OPS-02 slice.
- No schema definition, migration effect/order, catalog, public API, telemetry, error, workflow, dependency, Admin, archive, or release-topology change is introduced.
</verification>

<success_criteria>
- OPS-02 is observable as V1-owned mutation around named snapshot and preflight internals.
- D-123-03 and D-123-04 are implemented exactly, including refusal precedence and reversal safety.
- SAFE-01 remains green before runtime-status decomposition begins.
</success_criteria>

<output>
Create `.planning/phases/123-runtime-operations-decomposition/123-02-SUMMARY.md` when done.
</output>

---
phase: 121-truthful-quality-signals-mechanical-hygiene
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .doctor.exs
  - mix.exs
  - lib/rindle/processor/av.ex
  - test/rindle/doctor_thresholds_test.exs
autonomous: true
requirements: [SIGNAL-02]
must_haves:
  truths:
    - "The documentation doctor measures the supported public module set in MIX_ENV=dev and exits successfully at the configured ratchet."
    - "A test inspects Doctor's measured report, not only numeric values stored in .doctor.exs."
    - "Internal modules are excluded explicitly by ownership; new modules remain checked by default."
  artifacts:
    - path: ".doctor.exs"
      provides: "Curated public-surface Doctor policy"
    - path: "test/rindle/doctor_thresholds_test.exs"
      provides: "Measured coverage ratchet test"
    - path: "lib/rindle/processor/av.ex"
      provides: "Complete docs/specs for the public AV processor boundary"
  key_links:
    - from: "test/rindle/doctor_thresholds_test.exs"
      to: ".doctor.exs"
      via: "Doctor.CLI report generation and health evaluation"
    - from: "mix.exs"
      to: ".doctor.exs"
      via: "ExDoc public module grouping used to justify the checked surface"
---

<objective>
Restore Doctor as an honest measured public-documentation ratchet instead of a green threshold-literal test beside a red report.

Purpose: SIGNAL-02 requires maintainers to see actual public module/function/spec health and to receive a failure when that measured health regresses.
Output: a curated Doctor scope, completed public AV processor documentation, and report-driven regression tests.

Planner assumption for the unclassified doctor input surface: `MIX_ENV=dev` is the supported documentation build because `mix.exs` declares Doctor for dev/test and ExDoc consumes production `lib/` modules, while `MIX_ENV=test` also compiles test-support modules that are not public package documentation. Do not change application behavior or treat test-support functions as adopter API.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-CONTEXT.md
@.doctor.exs
@mix.exs
@lib/rindle/processor/av.ex
@test/rindle/doctor_thresholds_test.exs
@deps/doctor/lib/cli/cli.ex
@deps/doctor/lib/config.ex
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make the public Doctor report pass for measured reasons</name>
  <read_first>.doctor.exs, mix.exs, lib/rindle/processor/av.ex, deps/doctor/lib/cli/cli.ex, deps/doctor/lib/config.ex</read_first>
  <files>.doctor.exs, mix.exs, lib/rindle/processor/av.ex</files>
  <behavior>
    - `MIX_ENV=dev mix doctor --full --raise` reports zero failed checked modules and meets the configured module-doc, overall-doc, moduledoc, module-spec, and overall-spec floors.
    - `Rindle.Processor.AV` remains a public processor boundary and receives docs/specs rather than being hidden from Doctor.
    - Internal implementation modules are ignored by explicit module entries or narrowly named internal namespaces; no catch-all `Rindle.*` exclusion exists.
  </behavior>
  <action>Start from the live Doctor report (80.4% docs, 89.1% specs in dev at planning time) and the ExDoc groups in `mix.exs`. Preserve the existing 100/100/100/95/95 ratchet. Add missing `@doc`/`@spec` coverage to the public `Rindle.Processor.AV` functions and include that boundary in the appropriate ExDoc group. Curate `.doctor.exs` so modules with explicitly internal roles (`@moduledoc false`, nested Admin implementation modules, AV subprocess/probe helpers, Migration V1 helpers, processor recipe implementations, signing/client helpers, telemetry emit helpers) are named as internal exclusions, while every existing module named in `groups_for_modules` and the public AV processor remains checked. Prefer explicit atoms; use namespace regexes only for established internal namespaces and add comments recording the ownership reason. Do not lower thresholds, add dependencies, or add public functions. Commit intent is `docs(121-03)`.</action>
  <acceptance_criteria>The dev Doctor command exits 0 at the unchanged ratchet, all checked public modules pass, and the config has no broad exclusion capable of hiding a newly added public module.</acceptance_criteria>
  <verify>
    <automated>MIX_ENV=dev mix doctor --full --raise</automated>
  </verify>
  <done>The public documentation surface meets the enforced Doctor ratchet with measured health.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace threshold-literal confidence with measured Doctor proof</name>
  <read_first>test/rindle/doctor_thresholds_test.exs, .doctor.exs, deps/doctor/lib/cli/cli.ex, deps/doctor/lib/module_report.ex</read_first>
  <files>test/rindle/doctor_thresholds_test.exs</files>
  <behavior>
    - The test constructs the Doctor config and evaluates the actual compiled dev module reports.
    - It asserts zero unhealthy checked modules and measured totals at or above all five configured ratchets.
    - Removing a public function doc or spec causes the measured test to fail even if threshold literals remain unchanged.
  </behavior>
  <action>Rewrite the existing five threshold-only assertions into a report-driven contract using Doctor's public/configured report-generation functions. Retain assertions that the intended ratchet values have not been lowered, then assert measured module and aggregate health from the compiled dev surface. If nested Mix invocation would contend with the current test VM, implement a small test helper that calls `Doctor.CLI.generate_module_report_list/1` directly; do not parse colorized console text and do not shell out with a success fallback.</action>
  <acceptance_criteria>The test fails on measured public doc/spec regression and cannot pass merely because `.doctor.exs` still contains the expected numbers.</acceptance_criteria>
  <verify>
    <automated>MIX_ENV=dev mix test test/rindle/doctor_thresholds_test.exs --seed 0</automated>
  </verify>
  <done>Doctor coverage is enforced from the live report rather than configuration prose.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compiled module metadata -> Doctor | The gate trusts compiled docs/spec metadata to represent the shipped public surface. |
| ignore policy -> coverage verdict | Over-broad exclusions could create a green report while hiding public drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-121-07 | Tampering | `.doctor.exs` ignore list | high | mitigate | Explicit ownership-scoped exclusions, unchanged thresholds, measured report test, and no catch-all public namespace exclusion. |
| T-121-08 | Repudiation | Doctor console/config mismatch | medium | mitigate | Test the report objects and command exit, not only configured literals or console prose. |
| T-121-09 | Information Disclosure | generated documentation | low | accept | Added docs describe existing public behavior only and contain no runtime values or credentials. |
</threat_model>

## Artifacts this phase produces

- Curated public-surface policy in `.doctor.exs`.
- Public docs/spec metadata for `Rindle.Processor.AV` and its ExDoc group membership.
- Report-driven `Rindle.DoctorThresholdsTest`.

<verification>
Run `MIX_ENV=dev mix test test/rindle/doctor_thresholds_test.exs --seed 0` and `MIX_ENV=dev mix doctor --full --raise`; both must exit 0 and the Doctor summary must show zero failed modules.
</verification>

<success_criteria>
Doctor's actual measured public documentation health meets the unchanged ratchet and regresses red when public docs/specs disappear.
</success_criteria>

<output>
Create `.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-03-SUMMARY.md` when done.
</output>

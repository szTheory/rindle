---
phase: 121-truthful-quality-signals-mechanical-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/background_processing.md
autonomous: true
requirements: [SIGNAL-01]
must_haves:
  truths:
    - "The deterministic contract suite is green against current public telemetry documentation and is eligible to become blocking."
    - "AV-dependent proof exercises real FFmpeg/ffprobe and libvips/Vix behavior after CI installs those prerequisites, without depending on host DB/Oban readiness."
  artifacts:
    - path: "guides/background_processing.md"
      provides: "Current telemetry allowlist prose consumed by the existing contract suite"
  key_links:
    - from: "test/rindle/contracts/telemetry_contract_test.exs"
      to: "guides/background_processing.md"
      via: "existing public-event documentation assertions"
    - from: ".github/workflows/ci.yml"
      to: "test/rindle/probe/av_probe_test.exs"
      via: "existing real FFmpeg/ffprobe fixture behavior after the install step"
    - from: ".github/workflows/ci.yml"
      to: "test/rindle/processor/image_test.exs"
      via: "existing real Image/Vix transformation behavior after libvips installation"
---

<objective>
Make the existing deterministic Contract proof green and give the AV-dependent leg a truthful prerequisite gate before CI severity changes.

Purpose: SIGNAL-01 must block real deterministic regressions without converting absent host tooling into an unexplained red result.
Output: corrected telemetry prose plus an executor-verified selection of existing real AV behavior tests for the final CI wiring.

Planner assumption for the specless SIGNAL-01 probe: retain the existing `:contract` tag and public-event allowlist semantics exactly; only reconcile stale prose and prerequisite detection, with no new telemetry events or AV behavior.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-CONTEXT.md
@guides/background_processing.md
@test/rindle/contracts/telemetry_contract_test.exs
@test/rindle/probe/av_probe_test.exs
@test/rindle/processor/image_test.exs
@RUNNING.md
@scripts/ci/install_ffmpeg.sh
@scripts/ci/install_apt_packages.sh
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Restore the real deterministic Contract path</name>
  <read_first>test/rindle/contracts/telemetry_contract_test.exs, guides/background_processing.md, test/rindle/contracts/behaviour_contract_test.exs, test/test_helper.exs</read_first>
  <files>guides/background_processing.md</files>
  <behavior>
    - `mix test --only contract` executes a non-empty suite and passes.
    - The guide names the exact existing public telemetry allowlist and AV event triplet expected by the contract test.
    - No telemetry name, metadata key, or runtime event emission changes.
  </behavior>
  <action>Use the constants and assertions already present in `Rindle.Contracts.TelemetryContractTest` as the sole source of truth. Reconcile the stale public-event section in the background-processing guide so the current contract suite passes; preserve the existing link to the contract test and describe the allowlist without changing runtime modules. Do not weaken or delete assertions. Commit intent is `docs(121-02)` because this is shipped-prose reconciliation, not an adopter-visible runtime change.</action>
  <acceptance_criteria>The previously observed `@public_events` contract failure is gone, all 15 current contract-tagged tests execute, and no `lib/` file changes.</acceptance_criteria>
  <verify>
    <automated>mix test --only contract --seed 0</automated>
  </verify>
  <done>The deterministic public contract suite is green and ready for blocking CI integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> contract test | Shipped prose is treated as public telemetry contract evidence. |
| installed AV tools -> focused behavior tests | Real FFmpeg/ffprobe and Vix/libvips behavior is exercised without host database state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-121-04 | Tampering | Contract documentation assertions | high | mitigate | Keep existing assertions and derive prose from the runtime-tested allowlist; never mask the suite. |
| T-121-05 | Spoofing | installed AV capabilities | medium | mitigate | Run real fixture creation/probing and image transformation tests after the existing install steps. |
| T-121-06 | Denial of Service | full runtime doctor in Quality | medium | mitigate | Keep DB/Oban/profile-dependent public doctor advisory; use bounded focused AV tests as the blocker. |
</threat_model>

## Artifacts this phase produces

- Updated public telemetry allowlist section in `guides/background_processing.md`.
- Selected existing AV blocker: `test/rindle/probe/av_probe_test.exs` plus `test/rindle/processor/image_test.exs` after CI installs.

<verification>
Run `mix test --only contract --seed 0`. On a host with the documented FFmpeg/libvips prerequisites, also run `mix test test/rindle/probe/av_probe_test.exs test/rindle/processor/image_test.exs --seed 0`; both commands must exit 0.
</verification>

<success_criteria>
Deterministic Contract failures are no longer hidden by a pre-existing red baseline, and AV proof distinguishes unmet prerequisites from product regressions.
</success_criteria>

<output>
Create `.planning/phases/121-truthful-quality-signals-mechanical-hygiene/121-02-SUMMARY.md` when done.
</output>

---
phase: 26-delivery-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/delivery.ex
  - lib/rindle/delivery/content_disposition.ex
  - lib/rindle/streaming/provider.ex
  - test/rindle/delivery_test.exs
autonomous: true
requirements:
  - AV-04-01
  - AV-04-02
  - AV-04-08
must_haves:
  truths:
    - "`Rindle.Delivery.streaming_url/3` is a separate additive public API that returns `{:ok, %{url, kind: :progressive, mime}}` while preserving the exact `url/3` tuple contract and existing image/private/public call sites (D-01, D-02, D-03)."
    - "`streaming_url/3` reuses the same authorization, TTL injection, request-time `:expires_in` override semantics, and tagged errors as `url/3`; only the wrapped return shape differs (D-05, D-06)."
    - "Download filename and disposition intent is explicit and sanitized; Rindle emits RFC 5987 `filename*=` values and never trusts container metadata or raw storage keys as the public-facing filename source (D-13, D-14, D-15, D-16, D-17)."
    - "Phase 26 reserves a callback-only `Rindle.Streaming.Provider` namespace without introducing runtime branching, adapter lookup, or delivery-surface widening before a real non-progressive provider exists (D-04)."
  artifacts:
    - path: "lib/rindle/delivery.ex"
      provides: "Additive `streaming_url/3` surface plus shared delivery option normalization."
      contains: "def streaming_url"
    - path: "lib/rindle/delivery/content_disposition.ex"
      provides: "Shared RFC 5987 filename/disposition builder used by both redirect and local delivery paths."
      contains: "filename*="
    - path: "lib/rindle/streaming/provider.ex"
      provides: "Reserved streaming-provider behaviour with callbacks only."
      contains: "@callback streaming_url"
    - path: "test/rindle/delivery_test.exs"
      provides: "Contract coverage for streaming wrapper semantics and disposition normalization."
      contains: "streaming_url"
  key_links:
    - from: "lib/rindle/delivery.ex"
      to: "lib/rindle/streaming/provider.ex"
      via: "reserved surface exists as a namespace only; no runtime dispatch uses it in v1.4"
      pattern: "Rindle\\.Streaming\\.Provider"
    - from: "lib/rindle/delivery.ex"
      to: "lib/rindle/delivery/content_disposition.ex"
      via: "public delivery opts normalize to one internal filename/disposition representation before adapter calls"
      pattern: "disposition"
    - from: "lib/rindle/delivery.ex"
      to: "test/rindle/delivery_test.exs"
      via: "shared auth/TTL/error semantics and mime default/override stay locked by tests"
      pattern: "streaming_url"
---

<objective>
Reserve the future-stable playback API without changing the existing delivery contract.

Purpose: give Phase 27 and adopters one AV playback call site now while preserving the established `url/3` behavior, keeping filename/disposition policy explicit, and avoiding a premature streaming-provider abstraction.
Output: additive `streaming_url/3`, shared disposition builder, reserved provider behaviour, and delivery contract tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-delivery-surface/26-CONTEXT.md
@.planning/phases/26-delivery-surface/26-RESEARCH.md
@.planning/phases/26-delivery-surface/26-PATTERNS.md
@.planning/phases/25-rindle-processor-av/25-CONTEXT.md
@lib/rindle/delivery.ex
@lib/rindle/security/filename.ex
@test/rindle/delivery_test.exs

<interfaces>
From lib/rindle/delivery.ex:
```elixir
@spec url(module(), String.t(), keyword()) :: {:ok, String.t()} | {:error, term()}
def url(profile, key, opts \\ []) do
  ...
end
```

From lib/rindle/security/filename.ex:
```elixir
@spec sanitize(String.t()) :: String.t()
```

From .planning/phases/26-delivery-surface/26-CONTEXT.md:
```elixir
{:ok, %{url: url, kind: :progressive, mime: mime}}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the reserved streaming surface without changing `url/3` semantics</name>
  <files>lib/rindle/delivery.ex, lib/rindle/streaming/provider.ex, test/rindle/delivery_test.exs</files>
  <behavior>
    - `streaming_url/3` returns `{:ok, %{url, kind: :progressive, mime}}` while reusing the same auth, capability, TTL, and `{:error, reason}` flow as `url/3`.
    - Explicit `:mime` opts win; otherwise the progressive fallback defaults to `"video/mp4"` per D-06.
    - Existing `url/3` callers, including image/public/private flows and request-time `:expires_in` overrides, keep their current return shape and behavior.
    - `Rindle.Streaming.Provider` exists only as a reserved callback namespace; no adapter registry, runtime provider lookup, or branching lands in v1.4.
  </behavior>
  <action>Extend `Rindle.Delivery` per D-01 through D-06 with an additive `streaming_url/3` that wraps `url/3` rather than duplicating delivery logic. Keep `url/3` returning `{:ok, binary}` exactly as-is for existing callers per D-03, preserve `authorize_delivery/4`, `Capabilities.require_delivery/2`, and `Keyword.put_new(opts, :expires_in, ttl)` semantics, and emit the streaming wrapper result only after the shared URL path succeeds. Add `lib/rindle/streaming/provider.ex` as a callback-only reserved namespace for AV-04-02; do not wire config, discovery, or runtime dispatch to it in this phase per D-04.</action>
  <verify>
    <automated>mix test test/rindle/delivery_test.exs</automated>
  </verify>
  <done>`streaming_url/3` exists as a thin additive wrapper, `url/3` behavior remains unchanged, and the reserved provider behaviour is present without any live abstraction or template churn.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize download filename/disposition policy for redirect-compatible delivery</name>
  <files>lib/rindle/delivery.ex, lib/rindle/delivery/content_disposition.ex, test/rindle/delivery_test.exs</files>
  <behavior>
    - Delivery opts accept explicit caller intent such as `filename:` and `disposition:` and never infer public-facing names from container metadata.
    - Sanitized basenames are encoded as RFC 5987 `filename*=` values and are ready for both signed-redirect adapter opts and LocalPlug headers.
    - Attachment fallback naming prefers trusted app context or sanitized upload/storage naming when the caller omits a filename, but never promotes raw container metadata and never turns raw storage keys into the preferred public name.
  </behavior>
  <action>Create a focused delivery-header helper, e.g. `Rindle.Delivery.ContentDisposition`, and thread it through `Rindle.Delivery` per D-13 through D-17. Reuse `Rindle.Security.Filename.sanitize/1`, keep the public opts shallow and keyword-based (`filename:` and `disposition:`), and normalize to one internal representation that adapters can translate into redirect query params or headers later. Lock the research decision that redirect-capable adapters receive normalized disposition data rather than a prebuilt header string, so the same normalized representation can also be consumed by `LocalPlug` in Plan 02. Do not widen the delivery DSL, do not read container metadata here, and do not couple this work to any specific storage adapter implementation.</action>
  <verify>
    <automated>mix test test/rindle/delivery_test.exs</automated>
  </verify>
  <done>Delivery filename/disposition intent is explicit, sanitized, RFC-5987 encoded, and reusable by both redirect and local delivery paths without widening the public DSL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller opts -> delivery API | Untrusted filename/disposition input crosses into a public redirect surface. |
| delivery API -> future provider namespace | Reserved future surface must not accidentally become a live abstraction before a real provider exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | `lib/rindle/delivery/content_disposition.ex` | mitigate | Sanitize basenames with `Rindle.Security.Filename`, emit RFC 5987 `filename*=` only from trusted caller intent, and never trust container metadata per D-13 through D-17. |
| T-26-02 | Repudiation | `lib/rindle/delivery.ex` | mitigate | Keep streaming resolution on the same auth/TTL/error path as `url/3` so callers cannot bypass existing delivery policy with the new API. |
| T-26-03 | Elevation of Privilege | `lib/rindle/streaming/provider.ex` | mitigate | Reserve the behaviour as callbacks only and forbid runtime provider dispatch in Phase 26 per D-04. |
</threat_model>

<verification>
Run `mix test test/rindle/delivery_test.exs`.
</verification>

<success_criteria>
AV-04-01, AV-04-02, and AV-04-08 are satisfied when the repo exposes an additive `streaming_url/3`, keeps `url/3` unchanged for current adopters, reserves the future provider seam without runtime branching, and normalizes safe RFC 5987 download filenames from explicit caller intent only.
</success_criteria>

<output>
After completion, create `.planning/phases/26-delivery-surface/26-01-SUMMARY.md`
</output>

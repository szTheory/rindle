---
phase: 26-delivery-surface
plan: 03
type: execute
wave: 3
depends_on: ["26-01", "26-02"]
files_modified:
  - lib/rindle/delivery.ex
  - lib/rindle/delivery/local_plug.ex
  - test/rindle/contracts/telemetry_contract_test.exs
  - test/rindle/delivery_test.exs
  - test/rindle/html_test.exs
autonomous: true
requirements:
  - AV-04-06
  - AV-04-07
  - AV-04-08
must_haves:
  truths:
    - "Operators get a frozen public telemetry contract for `[:rindle, :delivery, :streaming, :resolved]` and `[:rindle, :delivery, :range_request]`, with numeric measurements and stable metadata keyed the same way as existing delivery telemetry (D-21, D-22)."
    - "Hexdocs-facing delivery docs explain TTL guidance without widening the profile DSL: image 900s, audio 3600s, video VOD 7200s, and long-form playback uses adopter-side token refresh strategy (D-18, D-19, D-20)."
    - "The local plug and delivery docs make production posture explicit: local byte serving is dev parity only, production signed redirect remains the normative path, and existing HTML image helpers stay unchanged by the new AV seam (D-03, D-08, D-20)."
    - "The same explicit sanitized filename/disposition contract is visible in both runtime tests and public docs so adopters learn one delivery model instead of separate local and redirect conventions (D-15, D-17)."
  artifacts:
    - path: "test/rindle/contracts/telemetry_contract_test.exs"
      provides: "Frozen allowlist and metadata/measurement shape for the new delivery events."
      contains: "[:rindle, :delivery, :streaming, :resolved]"
    - path: "lib/rindle/delivery.ex"
      provides: "Published doc guidance for TTL and streaming surface caveats."
      contains: "long-form"
    - path: "lib/rindle/delivery/local_plug.ex"
      provides: "Published dev-only caveats and shared filename/disposition behavior notes."
      contains: "dev-parity-only"
    - path: "test/rindle/html_test.exs"
      provides: "Regression guard that existing image helper behavior did not churn while delivery surfaces expanded."
      contains: "picture_tag"
  key_links:
    - from: "test/rindle/contracts/telemetry_contract_test.exs"
      to: "lib/rindle/delivery.ex"
      via: "streaming resolution event name and metadata become a locked public contract"
      pattern: "streaming, :resolved"
    - from: "test/rindle/contracts/telemetry_contract_test.exs"
      to: "lib/rindle/delivery/local_plug.ex"
      via: "range-request emission shape is frozen at the plug seam"
      pattern: "range_request"
    - from: "test/rindle/html_test.exs"
      to: "lib/rindle/delivery.ex"
      via: "image helper path remains thin and unchanged while AV helpers gain the reserved streaming seam later"
      pattern: "picture_tag"
---

<objective>
Freeze the public contract and docs posture after both delivery seams exist.

Purpose: close Phase 26 by locking telemetry names and shapes, documenting the per-content TTL guidance without widening the DSL, and proving the new delivery work did not churn existing helper behavior.
Output: telemetry contract coverage, finalized module docs, TTL guidance, and image-helper regression guards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-delivery-surface/26-CONTEXT.md
@.planning/phases/26-delivery-surface/26-RESEARCH.md
@.planning/phases/26-delivery-surface/26-PATTERNS.md
@lib/rindle/delivery.ex
@lib/rindle/delivery/local_plug.ex
@lib/rindle/html.ex
@test/rindle/contracts/telemetry_contract_test.exs
@test/rindle/html_test.exs

<interfaces>
From test/rindle/contracts/telemetry_contract_test.exs:
```elixir
@public_events [
  [:rindle, :delivery, :signed]
]
```

From lib/rindle/html.ex:
```elixir
@spec picture_tag(module(), map(), keyword()) :: Phoenix.HTML.safe()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze the new delivery telemetry events in the public contract lane</name>
  <files>test/rindle/contracts/telemetry_contract_test.exs, lib/rindle/delivery.ex, lib/rindle/delivery/local_plug.ex</files>
  <behavior>
    - `[:rindle, :delivery, :streaming, :resolved]` appears in the public event allowlist and is asserted with numeric measurements plus stable metadata matching the existing delivery contract style.
    - `[:rindle, :delivery, :range_request]` appears in the same contract lane and proves that local range telemetry is scoped to `LocalPlug`, not generalized across all adapters.
    - The measurement and metadata schema for both events is published in moduledoc/function docs alongside the runtime emitters so operators have a stable human-readable contract, not just tests.
    - No undocumented event drift is introduced while closing the phase.
  </behavior>
  <action>Extend `test/rindle/contracts/telemetry_contract_test.exs` to freeze the two new Phase 26 delivery events per D-21 and D-22, using the existing allowlist and measurement/metadata helper style already established in the repo. In the same plan, publish the measurement and metadata schema in `lib/rindle/delivery.ex` and/or `lib/rindle/delivery/local_plug.ex` next to the event emitters so the public contract is documented as well as tested. Add only the event assertions needed to prove the public contract and update the runtime emitters if the earlier plans left metadata alignment gaps. Do not create a second telemetry contract mechanism or rename the existing `:delivery :signed` event.</action>
  <verify>
    <automated>mix test test/rindle/contracts/telemetry_contract_test.exs --only contract</automated>
  </verify>
  <done>The delivery telemetry allowlist and shape are locked for both streaming resolution and local range requests, and any future event-name or metadata drift fails the contract lane.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish TTL guidance and no-churn docs/tests for the delivery surface</name>
  <files>lib/rindle/delivery.ex, lib/rindle/delivery/local_plug.ex, test/rindle/delivery_test.exs, test/rindle/html_test.exs</files>
  <behavior>
    - `Rindle.Delivery` docs explain the TTL guidance values and explicitly steer materially different policies toward separate profiles rather than a widened DSL.
    - `LocalPlug` docs state dev-parity-only caveats clearly and describe the same filename/disposition policy used by redirect delivery.
    - Existing `picture_tag/3` image behavior remains unchanged, with regression coverage proving the new delivery seams did not churn image helpers.
  </behavior>
  <action>Finalize the Hexdocs-facing contract per D-03, D-08, and D-18 through D-20 by updating module/function docs in `lib/rindle/delivery.ex` and `lib/rindle/delivery/local_plug.ex` instead of widening `Rindle.Profile.Validator`. Add focused assertions in `test/rindle/delivery_test.exs` for any remaining TTL-guidance-adjacent delivery behavior and extend `test/rindle/html_test.exs` only to guard that existing image helper semantics stay thin and unchanged while Phase 27 later adopts `streaming_url/3` for AV helpers. Do not introduce new profile DSL keys, HTML helper AV behavior, or production proxy guidance in this phase.</action>
  <verify>
    <automated>mix test test/rindle/delivery_test.exs test/rindle/html_test.exs</automated>
  </verify>
  <done>TTL guidance and caveats are published in module docs, no new delivery DSL appears, and image helper behavior is regression-guarded against Phase 26 churn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime emitters -> operator dashboards | Telemetry names and metadata become an external contract for alerts and dashboards. |
| docs/examples -> adopter configuration | Published guidance can accidentally imply a widened DSL or production-local delivery posture if not explicit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-07 | Repudiation | `test/rindle/contracts/telemetry_contract_test.exs` | mitigate | Freeze event names, metadata keys, and numeric measurements so operator tooling cannot silently drift. |
| T-26-08 | Tampering | `lib/rindle/delivery.ex` docs | mitigate | Document TTL guidance as guidance only and explicitly keep `signed_url_ttl_seconds` as the single runtime policy surface per D-18 through D-20. |
| T-26-09 | Information Disclosure | `lib/rindle/delivery/local_plug.ex` docs | mitigate | State that local byte serving is dev parity only and shares the same sanitized filename/disposition policy, preventing adopters from learning an unsafe second delivery model. |
</threat_model>

<verification>
Run `mix test test/rindle/contracts/telemetry_contract_test.exs --only contract test/rindle/delivery_test.exs test/rindle/html_test.exs`.
</verification>

<success_criteria>
AV-04-06, AV-04-07, and the public-contract closure for AV-04-08 are satisfied when the new delivery telemetry events are frozen in the contract lane, TTL guidance is documented without any DSL widening, local delivery caveats are explicit, and existing image helpers remain unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/26-delivery-surface/26-03-SUMMARY.md`
</output>

---
phase: 27-html-helpers-liveview-integration
plan: 04
type: execute
wave: 4
depends_on: ["27-01", "27-02", "27-03"]
files_modified:
  - lib/rindle/error.ex
  - lib/rindle/delivery.ex
  - lib/rindle/delivery/local_plug.ex
  - lib/rindle/workers/process_variant.ex
  - test/rindle/error_test.exs
  - test/rindle/delivery_test.exs
  - test/rindle/delivery/local_plug_test.exs
  - test/rindle/workers/process_variant_test.exs
autonomous: true
requirements:
  - AV-05-05
  - AV-05-06
  - AV-05-07
threat_model: true

must_haves:
  truths:
    - "The eight AV-facing reason atoms are normalized at runtime seams and rendered through one public source of truth: `Rindle.Error.message/1`."
    - "Message text is fix-oriented, concrete, and parity-locked byte-for-byte; wording drift is treated as a public-contract regression."
    - "Delivery and local-range seams use the locked AV vocabulary where applicable, including `:streaming_not_configured` and `:range_unparseable`."
    - "Cancellation and worker failures surface the normalized AV reason vocabulary rather than leaking lower-level internal tuples into adopter-facing messages."
  artifacts:
    - path: "lib/rindle/error.ex"
      provides: "Frozen AV message vocabulary rendered through the public error surface."
      contains: "processor_capability_missing"
    - path: "test/rindle/error_test.exs"
      provides: "Byte-for-byte parity tests for the eight locked messages."
      contains: "variant_processing_cancelled"
    - path: "lib/rindle/delivery.ex"
      provides: "Normalized streaming misconfiguration reason mapping."
      contains: "streaming_not_configured"
    - path: "lib/rindle/delivery/local_plug.ex"
      provides: "Normalized range-parse reason handling for the local playback seam."
      contains: "range_unparseable"
  key_links:
    - from: "lib/rindle/error.ex"
      to: "test/rindle/error_test.exs"
      via: "exact public message text is parity-locked"
      pattern: "message("
    - from: "lib/rindle/delivery.ex"
      to: "lib/rindle/delivery/local_plug.ex"
      via: "delivery/runtime AV reasons are normalized consistently across streaming and local playback"
      pattern: "streaming_not_configured|range_unparseable"
    - from: "lib/rindle/workers/process_variant.ex"
      to: "lib/rindle/error.ex"
      via: "worker-visible cancellation/failure reasons resolve to public AV vocabulary"
      pattern: "variant_processing_cancelled"
---

<objective>
Close the phase by freezing the AV-facing public vocabulary and exact message text.

Purpose: ensure adopters get one coherent error contract across helper, delivery, worker, and cancellation paths, with exact byte-for-byte messages that tell them what to fix next.
Output: normalized AV reason producers, extended `Rindle.Error.message/1`, and parity tests that fail on copy drift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-html-helpers-liveview-integration/27-CONTEXT.md
@.planning/phases/27-html-helpers-liveview-integration/27-RESEARCH.md
@.planning/phases/27-html-helpers-liveview-integration/27-PATTERNS.md
@lib/rindle/error.ex
@lib/rindle/delivery.ex
@lib/rindle/delivery/local_plug.ex
@lib/rindle/workers/process_variant.ex

<interfaces>
From .planning/research/v1.4/SYNTHESIS.md:
```text
1. :processor_capability_missing
2. :ffmpeg_not_found
3. :capability_drift
4. :variant_source_not_found
5. :unsupported_codec
6. :streaming_not_configured
7. :variant_processing_cancelled
8. :range_unparseable
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize runtime seams to the locked AV reason vocabulary</name>
  <files>lib/rindle/delivery.ex, lib/rindle/delivery/local_plug.ex, lib/rindle/workers/process_variant.ex, test/rindle/delivery_test.exs, test/rindle/delivery/local_plug_test.exs, test/rindle/workers/process_variant_test.exs</files>
  <behavior>
    - Delivery-level AV misconfiguration uses `:streaming_not_configured` instead of leaking lower-level local-route or capability details to adopters.
    - Local range parsing can surface or log `:range_unparseable` in a stable way without widening the public delivery API.
    - Worker/cancellation paths normalize adopter-facing failure reasons to the locked AV vocabulary where applicable, especially `:variant_processing_cancelled`, `:variant_source_not_found`, and `:unsupported_codec`.
  </behavior>
  <action>Audit the key runtime seams introduced by Phases 25–27 and normalize the adopter-facing reasons to the eight locked AV atoms instead of relying on incidental lower-level tuples. In `lib/rindle/delivery.ex`, prefer `:streaming_not_configured` when the local-playback route context required for AV streaming is absent. In `lib/rindle/delivery/local_plug.ex`, keep graceful `200 + full body` range fallback but make the `range_unparseable` reason observable in a stable, testable way. In worker/cancellation code, normalize visible cancellation and deterministic runtime failures to the locked AV vocabulary instead of leaking raw internals. Do not create a second public error type or widen the delivery API just to report these reasons.</action>
  <verify>
    <automated>mix test test/rindle/delivery_test.exs test/rindle/delivery/local_plug_test.exs test/rindle/workers/process_variant_test.exs</automated>
  </verify>
  <done>The relevant runtime seams emit or preserve the locked AV reason atoms, and adopter-facing behavior no longer depends on incidental internal tuples.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze exact `Rindle.Error.message/1` text for the eight AV reasons</name>
  <files>lib/rindle/error.ex, test/rindle/error_test.exs</files>
  <behavior>
    - `Rindle.Error.message/1` renders exact fix-oriented text for the eight locked AV reasons.
    - The parity lane fails on wording drift, not just missing branches.
    - The public error surface remains centralized in `Rindle.Error`; no parallel formatter module lands.
  </behavior>
  <action>Extend `lib/rindle/error.ex` with exact branches for the eight locked AV-facing reasons and write parity tests that assert the exact expected strings byte-for-byte. Keep the copy concrete and `mix phx.gen`-style: name what failed, then point at the next fix. Preserve existing generic branches for non-AV reasons, but make the AV vocabulary load-bearing and explicit. Do not split message rendering into a second public module or hide the parity contract behind broad regex assertions.</action>
  <verify>
    <automated>mix test test/rindle/error_test.exs</automated>
  </verify>
  <done>The eight locked AV reason atoms render exact stable text through `Rindle.Error.message/1`, and any wording drift fails the parity lane immediately.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime reasons -> adopter-facing copy | Internal tuples become stable public guidance once rendered through `Rindle.Error.message/1`. |
| delivery/local range behavior -> public diagnostics | Graceful fallback paths still need consistent visible reasons for operators and adopters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-10 | Repudiation | `lib/rindle/error.ex` | mitigate | Freeze exact message text with byte-for-byte tests so public copy cannot drift silently. |
| T-27-11 | Information Disclosure | delivery/worker error normalization | mitigate | Map lower-level internals to the locked AV vocabulary instead of surfacing incidental private tuples. |
| T-27-12 | Tampering | `lib/rindle/delivery/local_plug.ex` range handling | mitigate | Keep graceful range fallback, but normalize/log the parse reason in one stable vocabulary rather than ad hoc text. |
</threat_model>

<verification>
Run `mix test test/rindle/error_test.exs test/rindle/delivery_test.exs test/rindle/delivery/local_plug_test.exs test/rindle/workers/process_variant_test.exs`.
</verification>

<success_criteria>
AV-05-05 through AV-05-07 are satisfied when the AV-facing public reason vocabulary is normalized across runtime seams, `Rindle.Error.message/1` renders exact parity-locked text for all eight reasons, and contract tests fail on drift.
</success_criteria>

<output>
After completion, create `.planning/phases/27-html-helpers-liveview-integration/27-04-SUMMARY.md`
</output>

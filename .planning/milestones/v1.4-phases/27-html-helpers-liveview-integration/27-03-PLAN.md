---
phase: 27-html-helpers-liveview-integration
plan: 03
type: execute
wave: 3
depends_on: ["27-02"]
files_modified:
  - lib/rindle.ex
  - lib/rindle/workers/process_variant.ex
  - test/rindle/workers/process_variant_test.exs
  - test/rindle/api_surface_boundary_test.exs
autonomous: true
requirements:
  - AV-05-04
threat_model: true

must_haves:
  truths:
    - "`Rindle.cancel_processing/1` is the only public cancellation API in this phase and returns `:ok | {:error, :not_processing}`."
    - "Cancellation addresses both queued and executing Oban jobs for all variants of the asset; a queued-only partial cancel does not satisfy the contract."
    - "Affected variants transition to `cancelled`, asset aggregate state is recomputed, and `:variant_cancelled` events are broadcast on the public event contract."
    - "The path never succeeds silently when nothing is processing; the explicit negative result is `{:error, :not_processing}`."
  artifacts:
    - path: "lib/rindle.ex"
      provides: "Public asset-scoped cancellation façade."
      contains: "def cancel_processing"
    - path: "lib/rindle/workers/process_variant.ex"
      provides: "Cancellation-aware worker lifecycle and public `:variant_cancelled` event emission."
      contains: "variant_cancelled"
    - path: "test/rindle/workers/process_variant_test.exs"
      provides: "Queued/executing cancellation proof and variant-state assertions."
      contains: "cancelled"
    - path: "test/rindle/api_surface_boundary_test.exs"
      provides: "Boundary visibility guard for the new public façade."
      contains: "cancel_processing"
  key_links:
    - from: "lib/rindle.ex"
      to: "lib/rindle/workers/process_variant.ex"
      via: "public cancellation flows through worker lifecycle and public event broadcasting"
      pattern: "variant_cancelled"
    - from: "test/rindle/workers/process_variant_test.exs"
      to: "test/rindle/api_surface_boundary_test.exs"
      via: "runtime cancellation behavior and public façade visibility stay aligned"
      pattern: "cancel_processing"
---

<objective>
Expose a narrow public cancellation API and make it real end to end.

Purpose: let adopters cancel active AV processing at the asset level without learning worker internals, while ensuring queued and executing jobs are both addressed and the resulting lifecycle is externally visible.
Output: `Rindle.cancel_processing/1`, cancellation-aware worker behavior, and regression tests for state and event outcomes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-html-helpers-liveview-integration/27-CONTEXT.md
@.planning/phases/27-html-helpers-liveview-integration/27-RESEARCH.md
@.planning/phases/27-html-helpers-liveview-integration/27-PATTERNS.md
@.planning/phases/24-domain-model-dsl-extension/24-CONTEXT.md
@lib/rindle.ex
@lib/rindle/workers/process_variant.ex
@test/rindle/workers/process_variant_test.exs

<interfaces>
From .planning/ROADMAP.md Phase 27:
```elixir
Rindle.cancel_processing(asset_id) #=> :ok | {:error, :not_processing}
```

From .planning/phases/24-domain-model-dsl-extension/24-CONTEXT.md:
```text
`cancelled` variant state is already part of the locked domain model.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the public `Rindle.cancel_processing/1` façade with explicit return semantics</name>
  <files>lib/rindle.ex, test/rindle/api_surface_boundary_test.exs, test/rindle/workers/process_variant_test.exs</files>
  <behavior>
    - `cancel_processing/1` is publicly documented and visible from the facade.
    - The function returns `:ok` when it actually finds cancellable work and `{:error, :not_processing}` when there is nothing queued/executing for the asset.
    - Public callers stay asset-scoped and do not learn per-variant or job-id internals.
  </behavior>
  <action>Add `cancel_processing/1` to `lib/rindle.ex` as the single public cancellation entrypoint for Phase 27. Keep the return contract narrow and explicit, and hide any Oban query/cancellation mechanics behind private helpers or an internal module if needed. Do not expose variant-scoped public cancellation, job ids, or a second arity in this phase. Extend the boundary lane only enough to keep the new public façade visible and documented.</action>
  <verify>
    <automated>mix test test/rindle/api_surface_boundary_test.exs</automated>
  </verify>
  <done>The façade exposes one public asset-scoped cancellation function with the locked return contract and no worker-internal leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cancel queued/executing work, persist `cancelled`, and broadcast `:variant_cancelled`</name>
  <files>lib/rindle/workers/process_variant.ex, test/rindle/workers/process_variant_test.exs</files>
  <behavior>
    - Both queued and executing jobs tied to the asset’s variants are addressed.
    - Affected variants transition to `cancelled`, aggregate asset state is recomputed, and `:variant_cancelled` is emitted on the public event contract.
    - Cancellation remains visible and deterministic rather than a silent no-op.
  </behavior>
  <action>Implement the runtime side of `cancel_processing/1` against the current Oban + variant model. Query the asset’s in-flight variants and the corresponding queued/executing jobs, cancel both classes, persist `cancelled` where appropriate, recompute aggregate asset state, and reuse the public event-broadcasting seam from Plan 02 for `:variant_cancelled`. Keep the implementation additive and asset-scoped; if worker cooperation is needed to recognize cancellation while already executing, confine that logic to the worker lifecycle instead of teaching adopters any new protocol.</action>
  <verify>
    <automated>mix test test/rindle/workers/process_variant_test.exs</automated>
  </verify>
  <done>Adopters can cancel an asset’s AV processing, queued and executing jobs are both handled, variant rows become `cancelled`, and the cancellation outcome is visible on the public event stream.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public façade -> Oban/runtime state | A simple public API fans into job-system side effects and lifecycle transitions. |
| cancellation request -> public event stream | Cancellation outcomes become operator/adopter-visible contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-07 | Tampering | `lib/rindle.ex` cancellation path | mitigate | Keep the public surface asset-scoped and explicit; do not expose variant or job-id cancellation knobs in v1.4. |
| T-27-08 | Repudiation | `lib/rindle/workers/process_variant.ex` | mitigate | Persist `cancelled`, recompute aggregate state, and broadcast `:variant_cancelled` so cancellation is externally visible rather than an implicit side effect. |
| T-27-09 | Denial of Service | Oban cancellation path | mitigate | Limit the query and cancellation scope to variants belonging to the requested asset and only queued/executing states. |
</threat_model>

<verification>
Run `mix test test/rindle/workers/process_variant_test.exs test/rindle/api_surface_boundary_test.exs`.
</verification>

<success_criteria>
AV-05-04 is satisfied when `Rindle.cancel_processing/1` cancels queued and executing AV work for an asset, persists visible `cancelled` lifecycle outcomes, broadcasts `:variant_cancelled`, and returns `{:error, :not_processing}` when nothing is in flight.
</success_criteria>

<output>
After completion, create `.planning/phases/27-html-helpers-liveview-integration/27-03-SUMMARY.md`
</output>

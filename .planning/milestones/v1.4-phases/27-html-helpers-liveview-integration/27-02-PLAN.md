---
phase: 27-html-helpers-liveview-integration
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - lib/rindle/live_view.ex
  - lib/rindle/workers/process_variant.ex
  - test/rindle/live_view_test.exs
  - test/rindle/workers/process_variant_test.exs
  - test/rindle/api_surface_boundary_test.exs
autonomous: true
requirements:
  - AV-05-03
threat_model: true

must_haves:
  truths:
    - "`Rindle.LiveView.subscribe/2` and `unsubscribe/1` stay thin PubSub-facing helpers and support exactly `:variant`, `:asset`, and `:upload_session` scopes."
    - "The adopter-facing event contract is `{:rindle_event, type, payload}` with exactly `:variant_started`, `:variant_progress`, `:variant_ready`, `:variant_failed`, and `:variant_cancelled` event types."
    - "Worker-side broadcasting remains the canonical source of truth for event cadence and continues to roll messages onto both variant and asset topics."
    - "Progress remains bounded at <= 2 events/second per variant; LiveView helpers do not introduce a second throttle layer."
  artifacts:
    - path: "lib/rindle/live_view.ex"
      provides: "Public subscription helpers and adopter-facing docs for `handle_info/2`."
      contains: "def subscribe"
    - path: "lib/rindle/workers/process_variant.ex"
      provides: "Public event-tuple broadcaster for variant and asset topics."
      contains: ":variant_progress"
    - path: "test/rindle/live_view_test.exs"
      provides: "Contract coverage for topic mapping and public helper visibility."
      contains: "subscribe"
    - path: "test/rindle/workers/process_variant_test.exs"
      provides: "Event-shape and topic-fan-out regression tests."
      contains: ":rindle_event"
  key_links:
    - from: "lib/rindle/live_view.ex"
      to: "lib/rindle/workers/process_variant.ex"
      via: "LiveView remains a thin subscriber helper while the worker owns public event production"
      pattern: "rindle_event"
    - from: "test/rindle/workers/process_variant_test.exs"
      to: "test/rindle/live_view_test.exs"
      via: "topic helpers and public message shape stay aligned across documentation and runtime behavior"
      pattern: "subscribe"
---

<objective>
Publish the LiveView-facing progress contract without turning LiveView into a state manager.

Purpose: give Phoenix adopters a simple `subscribe/2` / `unsubscribe/1` API and a stable `handle_info/2` tuple contract while keeping event production in the worker lifecycle seam.
Output: thin LiveView subscription helpers, public event-tuple broadcasting, and contract tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-html-helpers-liveview-integration/27-CONTEXT.md
@.planning/phases/27-html-helpers-liveview-integration/27-RESEARCH.md
@.planning/phases/27-html-helpers-liveview-integration/27-PATTERNS.md
@lib/rindle/live_view.ex
@lib/rindle/workers/process_variant.ex
@test/rindle/live_view_test.exs
@test/rindle/workers/process_variant_test.exs

<interfaces>
From .planning/research/v1.4/DELIVERY-DX.md:
```elixir
@spec subscribe(:variant | :asset | :upload_session, term()) :: String.t()
@spec unsubscribe(String.t()) :: :ok
```

From lib/rindle/workers/process_variant.ex:
```elixir
PubSub.broadcast(pubsub_server(), topic, {:rindle_variant_progress, payload})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add thin LiveView topic helpers for the three supported scopes</name>
  <files>lib/rindle/live_view.ex, test/rindle/live_view_test.exs, test/rindle/api_surface_boundary_test.exs</files>
  <behavior>
    - `subscribe/2` supports `:variant`, `:asset`, and `:upload_session`, subscribes through the configured PubSub server, and returns the topic string for later unsubscribe.
    - `unsubscribe/1` is symmetrical and keeps adopters ignorant of topic formatting details.
    - Public docs show `handle_info({:rindle_event, type, payload}, socket)` usage rather than raw worker tuples.
    - The new public functions stay visible in the compiled-doc boundary lane.
  </behavior>
  <action>Extend `lib/rindle/live_view.ex` with `subscribe/2` and `unsubscribe/1` as thin wrappers over Phoenix PubSub. Keep scope-to-topic mapping private, support exactly the three locked scopes, and document the public tuple contract using realistic `handle_info/2` examples. Reuse the existing module’s documentation/testing style instead of adding a new LiveView-specific helper module or registry. Do not add stateful subscription bookkeeping in this plan.</action>
  <verify>
    <automated>mix test test/rindle/live_view_test.exs test/rindle/api_surface_boundary_test.exs</automated>
  </verify>
  <done>Adopters can subscribe and unsubscribe without knowing topic naming details, and the public docs teach the new event tuple contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Promote worker progress broadcasts to the public `{:rindle_event, ...}` contract</name>
  <files>lib/rindle/workers/process_variant.ex, test/rindle/workers/process_variant_test.exs</files>
  <behavior>
    - Variant and asset topics receive `{:rindle_event, :variant_started | :variant_progress | :variant_ready | :variant_failed | :variant_cancelled, payload}` messages.
    - The worker remains the source of truth for cadence and continues to fan out onto both per-variant and per-asset topics.
    - The public tuple is stable even if any raw internal tuple remains temporarily for private compatibility.
    - No helper-side second throttle layer appears; rate limiting stays worker-owned.
  </behavior>
  <action>Adapt `lib/rindle/workers/process_variant.ex` so the public contract emitted on Phase 25’s existing topic structure is the locked `{:rindle_event, type, payload}` tuple rather than asking adopters to parse `{:rindle_variant_progress, payload}`. Preserve the existing topic names and keep cadence control in the worker. If transitional private compatibility requires dual-emission for one plan, keep the public tuple authoritative and add tests that fail on public-contract drift. Do not invent a separate broadcaster process or move event shaping into `Rindle.LiveView`.</action>
  <verify>
    <automated>mix test test/rindle/workers/process_variant_test.exs</automated>
  </verify>
  <done>ProcessVariant emits the public LiveView-facing event tuple on the existing variant and asset topics, and the contract is frozen by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker internals -> adopter LiveView handlers | Private worker details become public UI contract once events are broadcast. |
| helper scope -> PubSub topic | Incorrect scope/topic mapping can subscribe adopters to the wrong events or leak internals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04 | Repudiation | `lib/rindle/workers/process_variant.ex` | mitigate | Freeze exact public event types and tuple shape in tests so dashboards and LiveViews cannot silently drift onto private messages. |
| T-27-05 | Information Disclosure | `lib/rindle/live_view.ex` | mitigate | Keep scope mapping explicit and private; do not expose raw topic-construction requirements to adopters. |
| T-27-06 | Denial of Service | `lib/rindle/workers/process_variant.ex` | mitigate | Keep the worker-owned <=2/sec variant progress cap and avoid a second broadcast layer in LiveView. |
</threat_model>

<verification>
Run `mix test test/rindle/live_view_test.exs test/rindle/workers/process_variant_test.exs test/rindle/api_surface_boundary_test.exs`.
</verification>

<success_criteria>
AV-05-03 is satisfied when LiveView adopters can subscribe via a thin helper API and receive the locked `{:rindle_event, type, payload}` tuple contract on the existing variant and asset topics with bounded progress cadence.
</success_criteria>

<output>
After completion, create `.planning/phases/27-html-helpers-liveview-integration/27-02-SUMMARY.md`
</output>

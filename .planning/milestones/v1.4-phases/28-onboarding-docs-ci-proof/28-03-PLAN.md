---
phase: 28-onboarding-docs-ci-proof
plan: 03
type: execute
wave: 3
depends_on: ["28-02"]
files_modified:
  - test/adopter/canonical_app/lifecycle_test.exs
  - test/adopter/canonical_app/profile.ex
  - test/support/fixtures/smartphone/
  - test/rindle/profile/presets_web_test.exs
autonomous: true
requirements:
  - AV-06-04
  - AV-06-06
threat_model: true

must_haves:
  truths:
    - "The canonical adopter lane proves a realistic smartphone-source video through the full lifecycle: upload, probe, transcode, ready variant, poster, and signed URL."
    - "The proof uses the stock `Rindle.Profile.Presets.Web` surface and `Rindle.Adopter.CanonicalApp.VideoProfile`, not a bespoke test-only recipe."
    - "The fixture matrix explicitly covers container/codec/rotation variance: one portrait `mov` source with rotation metadata and one second source from a different container/codec family."
    - "The lane remains adopter-shaped with real MinIO/Postgres and actual public Rindle calls instead of a lower-level worker-only shortcut."
  artifacts:
    - path: "test/adopter/canonical_app/lifecycle_test.exs"
      provides: "Canonical end-to-end AV round-trip proof."
      contains: "web_720p"
    - path: "test/adopter/canonical_app/profile.ex"
      provides: "Canonical adopter AV profile rooted in `Rindle.Profile.Presets.Web`."
      contains: "use Rindle.Profile.Presets.Web"
    - path: "test/support/fixtures/smartphone/"
      provides: "Two-fixture smartphone-source matrix for CI proof."
      contains: ".mp4"
    - path: "test/rindle/profile/presets_web_test.exs"
      provides: "Focused preset-contract coverage aligned with the canonical onboarding story."
      contains: "web_720p"
  key_links:
    - from: "test/adopter/canonical_app/profile.ex"
      to: "lib/rindle/profile/presets/web.ex"
      via: "canonical adopter AV story stays bound to the stock preset module"
      pattern: "Presets\\.Web"
    - from: "test/adopter/canonical_app/lifecycle_test.exs"
      to: "guides/getting_started.md"
      via: "adopter proof remains the source of truth for the public onboarding story"
      pattern: "source of truth"
    - from: "test/adopter/canonical_app/lifecycle_test.exs"
      to: "Rindle.Delivery"
      via: "proof must reach signed URL resolution after variant and poster readiness"
      pattern: "signed URL"
---

<objective>
Convert the stock preset AV story into the final release-proof integration lane.

Purpose: prove that the public onboarding story really works on a realistic
smartphone-style input and stays centered on `Rindle.Profile.Presets.Web`.
Output: strengthened canonical adopter AV proof with a realistic fixture and
stock-preset alignment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-onboarding-docs-ci-proof/28-CONTEXT.md
@.planning/phases/28-onboarding-docs-ci-proof/28-RESEARCH.md
@.planning/phases/28-onboarding-docs-ci-proof/28-PATTERNS.md
@test/adopter/canonical_app/lifecycle_test.exs
@test/adopter/canonical_app/profile.ex
@lib/rindle/profile/presets/web.ex
@test/rindle/profile/presets_web_test.exs
@lib/rindle/delivery.ex
@lib/rindle/workers/process_variant.ex

<interfaces>
From .planning/ROADMAP.md success criteria:
```text
upload -> probe -> transcode -> variant ready -> poster -> signed URL
using the stock `Rindle.Profile.Presets.Web` preset
```

From test/adopter/canonical_app/profile.ex:
```elixir
defmodule Rindle.Adopter.CanonicalApp.VideoProfile do
  use Rindle.Profile.Presets.Web, ...
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a realistic smartphone-source AV fixture and route the canonical adopter lane through it</name>
  <files>test/adopter/canonical_app/lifecycle_test.exs, test/support/fixtures/smartphone/</files>
  <behavior>
    - The adopter lane uses a two-fixture smartphone matrix rather than a synthetic-only clip: one portrait `mov` with rotation metadata and one second file from a different container/codec family.
    - The proof stays on public Rindle calls and the real MinIO/Postgres harness already used by the canonical adopter lane.
    - The test asserts the full lifecycle endpoints promised by AV-06-04, including signed URL resolution after processing completes.
  </behavior>
  <action>Commit a two-fixture smartphone matrix under `test/support/fixtures/smartphone/`: one portrait `mov` source with rotation metadata and one second source from a different container/codec family so the phase explicitly proves codec, container, and rotation variance. Extend the canonical adopter AV lifecycle test to iterate that matrix. For each fixture, keep the test adopter-shaped: initiate upload, verify completion, run promotion and variant processing, assert `web_720p` and `poster` readiness, and verify the signed URL path at the end. Reuse the existing MinIO-backed harness and public Rindle API calls; do not bypass the lifecycle with worker-only shortcuts or local-file-only probes.</action>
  <verify>
    <automated>mix test --only adopter test/adopter/canonical_app/lifecycle_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The canonical adopter AV lane now proves the roadmap’s full smartphone-video journey on a two-fixture matrix that explicitly covers codec, container, and rotation variance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tighten the canonical AV profile and stock-preset contract around the onboarding story</name>
  <files>test/adopter/canonical_app/profile.ex, test/rindle/profile/presets_web_test.exs, test/adopter/canonical_app/lifecycle_test.exs</files>
  <behavior>
    - The canonical AV profile remains rooted in `Rindle.Profile.Presets.Web` with no bespoke recipe drift.
    - Focused preset tests and end-to-end adopter proof both reinforce the same `web_720p` + `poster` story.
    - The onboarding proof remains teachable and small rather than widening into extra variants or advanced options.
  </behavior>
  <action>Audit the canonical adopter AV profile and preset tests so the public demo story remains exactly the stock preset path. If the lifecycle proof needs minor profile or fixture-support adjustments, make them in `test/adopter/canonical_app/profile.ex` rather than creating a second hidden test profile. Extend `test/rindle/profile/presets_web_test.exs` only where necessary to lock the same public preset posture the adopter lane now proves. Keep the story narrow: `web_720p`, `poster`, and the signed-delivery end state.</action>
  <verify>
    <automated>mix test test/rindle/profile/presets_web_test.exs test/adopter/canonical_app/lifecycle_test.exs --warnings-as-errors --include adopter</automated>
  </verify>
  <done>The stock preset surface and the canonical adopter AV proof teach the same minimal onboarding story with no bespoke recipe drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public onboarding docs -> canonical adopter proof | If the proof uses a different profile or hidden shortcuts, the docs can claim a path that CI never actually proves. |
| smartphone fixture -> AV processing pipeline | Real-world container/rotation quirks can expose lifecycle bugs that synthetic fixtures miss. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Repudiation | `test/adopter/canonical_app/lifecycle_test.exs` | mitigate | Keep the canonical adopter lane as the source-of-truth proof for the public onboarding story and run it in CI. |
| T-28-05 | Tampering | `test/adopter/canonical_app/profile.ex` | mitigate | Bind the proof to `Rindle.Profile.Presets.Web` so the stock preset story cannot drift into a private bespoke recipe. |
| T-28-06 | Reliability | smartphone fixture set | mitigate | Use realistic smartphone-style fixtures that exercise the roadmap’s codec/container/rotation claims instead of synthetic-only clips. |
</threat_model>

<verification>
Run `mix test --only adopter test/adopter/canonical_app/lifecycle_test.exs` and `mix test test/rindle/profile/presets_web_test.exs`.
</verification>

<success_criteria>
AV-06-04 and AV-06-06 are satisfied when CI can prove the stock web preset
story on a realistic smartphone-source video through upload, probe, transcode,
poster generation, and signed delivery URL resolution.
</success_criteria>

<output>
After completion, create `.planning/phases/28-onboarding-docs-ci-proof/28-03-SUMMARY.md`
</output>

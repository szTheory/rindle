---
phase: 28-onboarding-docs-ci-proof
plan: 04
type: execute
wave: 4
depends_on: ["28-01", "28-02", "28-03"]
files_modified:
  - guides/troubleshooting.md
  - guides/background_processing.md
  - test/rindle/error_test.exs
  - test/rindle/contracts/telemetry_contract_test.exs
  - test/install_smoke/docs_parity_test.exs
autonomous: true
requirements:
  - AV-06-05
  - AV-06-07
threat_model: true

must_haves:
  truths:
    - "The eight locked AV error reasons stay frozen as exact public text and are documented in one public docs surface that matches runtime byte-for-byte where promised."
    - "Telemetry naming stays anchored to the existing public-event allowlist and documented `:start / :stop / :exception` conventions rather than a second source of truth."
    - "The final parity lane closes docs/runtime drift after the onboarding story and canonical proof have already settled."
    - "This plan does not expand the runtime surface; it hardens the public contract around already-shipped behavior."
  artifacts:
    - path: "guides/troubleshooting.md"
      provides: "Public AV error-vocabulary guidance aligned to runtime messaging."
      contains: "mix rindle.doctor"
    - path: "guides/background_processing.md"
      provides: "Public telemetry/event naming guidance aligned to the contract lane."
      contains: ":start"
    - path: "test/rindle/error_test.exs"
      provides: "Exact AV error-message parity gate."
      contains: "exact("
    - path: "test/rindle/contracts/telemetry_contract_test.exs"
      provides: "Telemetry allowlist and event-shape contract gate."
      contains: "@public_events"
  key_links:
    - from: "guides/troubleshooting.md"
      to: "test/rindle/error_test.exs"
      via: "documented AV error text stays aligned with the runtime contract"
      pattern: "ffmpeg|capability|cancelled|range"
    - from: "guides/background_processing.md"
      to: "test/rindle/contracts/telemetry_contract_test.exs"
      via: "documented telemetry names and triplet conventions stay aligned with the allowlist"
      pattern: "telemetry"
    - from: "test/install_smoke/docs_parity_test.exs"
      to: "guides/troubleshooting.md"
      via: "public docs parity extends beyond onboarding into the locked troubleshooting/contract surfaces"
      pattern: "troubleshooting"
---

<objective>
Freeze the final public AV contract at the docs/runtime boundary.

Purpose: close v1.4 by making the AV error vocabulary and telemetry naming
conventions explicitly documented and locked by tests after the onboarding path
and end-to-end proof are stable.
Output: exact contract docs plus final parity gates for error text and telemetry
naming.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-onboarding-docs-ci-proof/28-CONTEXT.md
@.planning/phases/28-onboarding-docs-ci-proof/28-RESEARCH.md
@.planning/phases/28-onboarding-docs-ci-proof/28-PATTERNS.md
@lib/rindle/error.ex
@test/rindle/error_test.exs
@test/rindle/contracts/telemetry_contract_test.exs
@guides/troubleshooting.md
@guides/background_processing.md
@test/install_smoke/docs_parity_test.exs

<interfaces>
From .planning/REQUIREMENTS.md:
```text
Capability-mismatch error vocabulary frozen with an ExUnit parity gate
Telemetry event names verified against documented conventions
```

From test/rindle/contracts/telemetry_contract_test.exs:
```elixir
@public_events [...]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Document and freeze the AV error vocabulary against runtime text</name>
  <files>guides/troubleshooting.md, test/rindle/error_test.exs, test/install_smoke/docs_parity_test.exs</files>
  <behavior>
    - Public troubleshooting docs teach the locked AV reasons and recovery posture without creating a second wording authority detached from `Rindle.Error.message/1`.
    - The exact AV-facing error-message tests remain the byte-for-byte runtime contract.
    - Docs parity asserts the troubleshooting surface actually links or references the public error/doctor guidance promised by the roadmap.
  </behavior>
  <action>Extend `guides/troubleshooting.md` with a focused AV section that documents the locked error vocabulary in a way that stays subordinate to `Rindle.Error.message/1`. Keep exact message ownership in `test/rindle/error_test.exs`, but add any missing exact cases or rationale needed to close AV-06-05. Extend `test/install_smoke/docs_parity_test.exs` only enough to ensure the public docs surface references the troubleshooting/error guidance and `mix rindle.doctor` remediation path; do not duplicate the full runtime copy into the install-smoke lane.</action>
  <verify>
    <automated>mix test test/rindle/error_test.exs test/install_smoke/docs_parity_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The eight AV error reasons are publicly documented, runtime-owned, and locked by exact tests with docs parity guarding the public troubleshooting path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Document telemetry triplet conventions and lock them to the public-event allowlist</name>
  <files>guides/background_processing.md, test/rindle/contracts/telemetry_contract_test.exs</files>
  <behavior>
    - Public docs explain the `:start / :stop / :exception` naming convention for AV-facing telemetry without inventing undocumented extra events.
    - The telemetry contract lane remains the source of truth for the public event-name allowlist and emitted metadata/measurement shape.
    - The documented conventions and the allowlist stay aligned so a naming drift fails tests before release.
  </behavior>
  <action>Update `guides/background_processing.md` (or the equivalent public operator-facing docs section) to describe the AV telemetry triplet conventions and any additional public event names already locked by the contract lane. Keep `test/rindle/contracts/telemetry_contract_test.exs` as the runtime source of truth; extend it only where needed to prove the documented names/conventions and fail on drift. Do not create a second telemetry registry in docs or tests.</action>
  <verify>
    <automated>mix test test/rindle/contracts/telemetry_contract_test.exs --warnings-as-errors --include contract</automated>
  </verify>
  <done>The public telemetry docs and the runtime allowlist say the same thing, and the contract lane fails if event names drift from the documented convention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime contract -> public docs | Docs can easily drift from the frozen AV error and telemetry surfaces if not tied back to executable tests. |
| docs wording -> semver-sensitive API expectations | Error and telemetry names are effectively public contracts for adopters and operators. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-07 | Repudiation | `test/rindle/error_test.exs` / docs | mitigate | Keep exact message ownership in runtime tests and use docs only as a linked public explanation layer. |
| T-28-08 | Tampering | telemetry naming docs/tests | mitigate | Anchor documentation to the existing `@public_events` allowlist and fail on naming drift in the contract lane. |
| T-28-09 | Information Disclosure | troubleshooting guidance | mitigate | Keep remediation concrete but bounded; document public reasons and operator actions without leaking internal-only debugging surfaces. |
</threat_model>

<verification>
Run `mix test test/rindle/error_test.exs test/rindle/contracts/telemetry_contract_test.exs test/install_smoke/docs_parity_test.exs`.
</verification>

<success_criteria>
AV-06-05 and AV-06-07 are satisfied when the AV error vocabulary and telemetry
naming conventions are publicly documented, runtime-owned, and frozen by exact
contract tests that fail on drift.
</success_criteria>

<output>
After completion, create `.planning/phases/28-onboarding-docs-ci-proof/28-04-SUMMARY.md`
</output>

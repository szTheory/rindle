---
phase: 28-onboarding-docs-ci-proof
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - .github/workflows/ci.yml
  - lib/mix/tasks/rindle.doctor.ex
  - test/rindle/doctor_test.exs
  - scripts/assert_av_hygiene.sh
autonomous: true
requirements:
  - AV-06-03
  - AV-06-08
threat_model: true

must_haves:
  truths:
    - "CI provisions FFmpeg in the relevant AV lanes and runs the public `mix rindle.doctor` surface as an explicit ship gate rather than relying on implicit environment assumptions."
    - "`mix rindle.doctor` can validate the fixture/example profile set that Phase 28 cares about without forcing CI to reach into private probe internals."
    - "The anti-pattern gate fails the build on banned FFmpeg/FFprobe launch surfaces under `lib/rindle/` only: `System.shell/2`, `:os.cmd/1`, raw `Port.open/2`, and string-interpolated argv patterns."
    - "Failure output is actionable: adopters/operators see why doctor failed, maintainers see exactly which banned pattern triggered the AV hygiene gate."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Explicit AV ship-gate steps for FFmpeg provisioning, doctor, and hygiene enforcement."
      contains: "mix rindle.doctor"
    - path: "lib/mix/tasks/rindle.doctor.ex"
      provides: "Public environment-check task capable of Phase-28-level fixture/profile validation."
      contains: "def run"
    - path: "test/rindle/doctor_test.exs"
      provides: "Stable public doctor output/behavior tests."
      contains: "Rindle: Environment checks passed"
    - path: "scripts/assert_av_hygiene.sh"
      provides: "Deterministic AV anti-pattern grep gate with explicit failure messages."
      contains: "System.shell"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "lib/mix/tasks/rindle.doctor.ex"
      via: "CI runs the public Mix task instead of calling private probe modules"
      pattern: "rindle\\.doctor"
    - from: "scripts/assert_av_hygiene.sh"
      to: "lib/rindle/"
      via: "gate scans only the locked source boundary for banned FFmpeg/FFprobe invocation styles"
      pattern: "lib/rindle"
    - from: "lib/mix/tasks/rindle.doctor.ex"
      to: "test/rindle/doctor_test.exs"
      via: "public output and exit posture remain locked by tests"
      pattern: "Environment checks"
---

<objective>
Turn AV environment validation and subprocess-hygiene enforcement into explicit
release gates.

Purpose: make CI prove that every AV-capable lane has the right runtime
dependencies and that the codebase does not regress to banned FFmpeg invocation
patterns.
Output: CI workflow updates, a Phase-28-complete doctor surface, and a narrow AV
hygiene gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-onboarding-docs-ci-proof/28-CONTEXT.md
@.planning/phases/28-onboarding-docs-ci-proof/28-RESEARCH.md
@.planning/phases/28-onboarding-docs-ci-proof/28-PATTERNS.md
@.github/workflows/ci.yml
@lib/mix/tasks/rindle.doctor.ex
@test/rindle/doctor_test.exs
@test/adopter/canonical_app/profile.ex
@test/adopter/canonical_app/lifecycle_test.exs
@lib/rindle/error.ex
@test/rindle/security/argv_test.exs

<interfaces>
From .planning/REQUIREMENTS.md:
```text
CI runs `mix rindle.doctor` against every example / fixture profile
anti-pattern grep gate in CI under `lib/rindle/`
```

From lib/mix/tasks/rindle.doctor.ex:
```elixir
mix rindle.doctor
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote `mix rindle.doctor` into a fixture-aware AV ship gate and wire it into CI</name>
  <files>.github/workflows/ci.yml, lib/mix/tasks/rindle.doctor.ex, test/rindle/doctor_test.exs</files>
  <behavior>
    - CI installs or provisions FFmpeg in the AV lanes before invoking doctor.
    - The public Mix task is capable of validating the fixture/example profiles that matter to the AV onboarding story, not only raw binary presence on PATH.
    - Doctor failures return a non-zero exit and print stable, actionable output suitable for CI logs.
  </behavior>
  <action>Extend `mix rindle.doctor` so it can cover the Phase 28 fixture/example profile set through its public API, for example by accepting profile module arguments or by enumerating a small explicit fixture list. Keep the task public and maintainable; CI should still invoke the Mix task rather than private probe functions. Update `.github/workflows/ci.yml` to provision FFmpeg in the AV lanes, run doctor after dependency install/compile setup, and fail fast if any fixture/example profile does not pass. Preserve the existing workflow style of explicit named jobs and steps instead of burying the AV check in a monolithic shell command.</action>
  <verify>
    <automated>mix test test/rindle/doctor_test.exs --warnings-as-errors && mix rindle.doctor Rindle.Adopter.CanonicalApp.Profile Rindle.Adopter.CanonicalApp.VideoProfile</automated>
  </verify>
  <done>CI explicitly proves the AV runtime environment and the public doctor task validates the Phase 28 fixture/profile story instead of only checking bare PATH state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the narrow AV anti-pattern hygiene gate to CI</name>
  <files>.github/workflows/ci.yml, scripts/assert_av_hygiene.sh</files>
  <behavior>
    - The hygiene gate scans `lib/rindle/` only and fails on `System.shell/2`, `:os.cmd/1`, raw `Port.open/2` for FFmpeg/FFprobe, and string-interpolated argv patterns.
    - The gate does not ban legitimate `System.cmd/3` usage globally and does not scan unrelated tests/fixtures that are outside AV-06-08's boundary.
    - Failure output names the offending pattern and file so the fix is obvious.
  </behavior>
  <action>Create a small deterministic script such as `scripts/assert_av_hygiene.sh` using the repo’s existing shell-gate style (`set -euo pipefail`, explicit failure messages, and `rg`-first searching). Scope the scan to `lib/rindle/`, match the exact anti-patterns locked by AV-06-08, and wire the script into `.github/workflows/ci.yml` as a dedicated named step. In the same plan, replace the stale adopter docs drift gate that still greps for `Broker.initiate_session`, `Broker.verify_completion`, and `Rindle.Delivery.url` with a Phase-28-accurate parity check tied to the AV onboarding story and the public facade calls now documented in `README.md` and `guides/getting_started.md`. Do not broaden the gate into a generic process-launch ban; this is a release-proof guard for unsafe FFmpeg/FFprobe invocation styles specifically.</action>
  <verify>
    <automated>bash scripts/assert_av_hygiene.sh</automated>
  </verify>
  <done>The repo has a narrow AV subprocess-hygiene gate, and CI fails with actionable diagnostics if unsafe invocation patterns reappear under `lib/rindle/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI environment -> AV runtime proof | Missing FFmpeg or stale environment setup can create false confidence unless doctor runs as a first-class gate. |
| Source tree -> FFmpeg subprocess boundary | Unsafe launch surfaces under `lib/rindle/` would bypass the security posture established in earlier phases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Repudiation | `.github/workflows/ci.yml` / doctor gate | mitigate | Run the public `mix rindle.doctor` in CI with stable failure output so AV environment drift is visible and attributable. |
| T-28-02 | Elevation of Privilege | `lib/rindle/` FFmpeg invocation surfaces | mitigate | Add a narrow grep gate that blocks shell-based or raw-port FFmpeg/FFprobe execution patterns under the locked source boundary. |
| T-28-03 | Denial of Service | Over-broad hygiene gate | mitigate | Scope the gate to `lib/rindle/` and the exact banned patterns so legitimate tests/tools are not blocked. |
</threat_model>

<verification>
Run `mix test test/rindle/doctor_test.exs`, `mix rindle.doctor Rindle.Adopter.CanonicalApp.Profile Rindle.Adopter.CanonicalApp.VideoProfile`, and `bash scripts/assert_av_hygiene.sh`.
</verification>

<success_criteria>
AV-06-03 and AV-06-08 are satisfied when CI explicitly provisions FFmpeg,
runs the public doctor task against the relevant fixture/example profiles, and
fails on the locked unsafe subprocess patterns under `lib/rindle/`.
</success_criteria>

<output>
After completion, create `.planning/phases/28-onboarding-docs-ci-proof/28-02-SUMMARY.md`
</output>

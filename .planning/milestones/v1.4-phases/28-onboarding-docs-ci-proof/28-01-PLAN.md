---
phase: 28-onboarding-docs-ci-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - guides/getting_started.md
  - RUNNING.md
  - test/install_smoke/docs_parity_test.exs
  - test/install_smoke/release_docs_parity_test.exs
autonomous: true
requirements:
  - AV-06-01
  - AV-06-02
must_haves:
  truths:
    - "A fresh adopter can copy-paste one AV install path from public docs: `mix deps.get`, install FFmpeg >= 6.0 for the target platform, declare one `:kind => :video` variant, and run `mix rindle.doctor` before touching background jobs."
    - "`README.md` remains the narrow quickstart while `guides/getting_started.md` remains the canonical deep guide; any new `RUNNING.md` exists only as a linked install/runtime reference rather than a competing onboarding tree."
    - "Per-platform FFmpeg guidance covers the locked roadmap targets: macOS/Homebrew, Ubuntu or Debian/apt, Alpine/apk, Fly.io Dockerfile, Heroku Aptfile, Render Dockerfile, and GitHub Actions using `FedericoCarboni/setup-ffmpeg`."
    - "Docs drift is enforced by exact tests rather than trusting prose; stale image-first onboarding or missing AV doctor guidance breaks the lane."
  artifacts:
    - path: "README.md"
      provides: "Narrow public AV quickstart with explicit link to deeper guidance."
      contains: "mix rindle.doctor"
    - path: "guides/getting_started.md"
      provides: "Canonical copy-pasteable AV onboarding path for adopters."
      contains: "kind: :video"
    - path: "RUNNING.md"
      provides: "Public runtime/install matrix for FFmpeg on supported platforms."
      contains: "setup-ffmpeg"
    - path: "test/install_smoke/docs_parity_test.exs"
      provides: "Exact docs parity guard for AV onboarding and doctor guidance."
      contains: "FFmpeg"
  key_links:
    - from: "README.md"
      to: "guides/getting_started.md"
      via: "quickstart stays narrow while the deep guide carries the full adopter path"
      pattern: "guides/getting_started\\.md"
    - from: "guides/getting_started.md"
      to: "test/adopter/canonical_app/lifecycle_test.exs"
      via: "guide remains tied to executable adopter proof instead of prose-only examples"
      pattern: "source of truth"
    - from: "RUNNING.md"
      to: ".github/workflows/ci.yml"
      via: "the same platform install posture is documented for adopters and used in CI"
      pattern: "setup-ffmpeg|apt|apk|Dockerfile|Aptfile"
---

<objective>
Lock the public AV onboarding path before the release-proof lanes depend on it.

Purpose: convert the current image-first docs into the exact Phase 28 AV
onboarding story without creating a second documentation system.
Output: updated quickstart/deep-guide docs, a durable FFmpeg install matrix, and
exact docs parity tests that fail on drift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-onboarding-docs-ci-proof/28-CONTEXT.md
@.planning/phases/28-onboarding-docs-ci-proof/28-RESEARCH.md
@.planning/phases/28-onboarding-docs-ci-proof/28-PATTERNS.md
@README.md
@guides/getting_started.md
@test/install_smoke/docs_parity_test.exs
@test/install_smoke/release_docs_parity_test.exs
@test/adopter/canonical_app/profile.ex
@test/adopter/canonical_app/lifecycle_test.exs
@lib/mix/tasks/rindle.doctor.ex
@lib/rindle/profile/presets/web.ex

<interfaces>
From .planning/ROADMAP.md success criteria:
```text
per-platform FFmpeg install snippet
declare one `:kind => :video` variant
run `mix rindle.doctor`
```

From README.md / guides/getting_started.md:
```text
README.md is the narrow quickstart.
guides/getting_started.md is the canonical deep adopter guide.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish a durable per-platform FFmpeg install surface and cross-link it from onboarding docs</name>
  <files>README.md, guides/getting_started.md, RUNNING.md, test/install_smoke/docs_parity_test.exs, test/install_smoke/release_docs_parity_test.exs</files>
  <behavior>
    - Public docs teach FFmpeg >= 6.0 installation for the roadmap platforms with one-command snippets or the platform-equivalent minimal install surface.
    - `README.md` and `guides/getting_started.md` clearly link to the install/runtime matrix instead of inlining fragmented platform advice in multiple places.
    - If `RUNNING.md` is added, it is a linked extension of onboarding rather than a separate doc tree with conflicting copy.
  </behavior>
  <action>Author the public FFmpeg install matrix promised by AV-06-01. Keep `README.md` narrow, keep `guides/getting_started.md` as the canonical deep path, and place the platform table/snippets in `RUNNING.md` only if that makes the install posture clearer than crowding the existing guides. Cover the exact roadmap targets: macOS with Homebrew, Ubuntu/Debian with apt, Alpine with apk, Fly.io Dockerfile, Heroku Aptfile, Render Dockerfile, and GitHub Actions via `FedericoCarboni/setup-ffmpeg`. Link this surface from both onboarding docs and extend the existing docs parity tests to assert those public entrypoints mention the install/runtime path and do not drift back to image-only onboarding.</action>
  <verify>
    <automated>mix test test/install_smoke/docs_parity_test.exs test/install_smoke/release_docs_parity_test.exs</automated>
  </verify>
  <done>The repo has one durable, linked public install surface for FFmpeg and tests fail if the onboarding docs stop pointing adopters to it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite the onboarding story around the smallest AV path and lock it with parity tests</name>
  <files>README.md, guides/getting_started.md, test/install_smoke/docs_parity_test.exs</files>
  <behavior>
    - The first AV onboarding path is explicit: `mix deps.get`, install FFmpeg, define one `:kind => :video` variant, run `mix rindle.doctor`, then follow the stock `Rindle.Profile.Presets.Web`-compatible lifecycle.
    - The docs remain facade-first and do not widen the public teaching surface into raw worker, FFmpeg, or transport internals.
    - The updated copy stays executable against the canonical adopter fixtures and does not introduce a bespoke demo profile disconnected from `Rindle.Profile.Presets.Web`.
  </behavior>
  <action>Refactor `README.md` and `guides/getting_started.md` from image-first onboarding into the locked AV path from AV-06-02. Teach a single canonical video variant declaration, call out `mix rindle.doctor` immediately after dependency/runtime setup, and use the stock `web_720p` + `poster` story rather than raw FFmpeg recipes or advanced LiveView/cancellation details. Extend `test/install_smoke/docs_parity_test.exs` with exact assertions for the AV onboarding path, including the `:kind => :video` declaration, doctor invocation, and cross-links back to the deep guide/install surface. Preserve the existing docs pattern: quickstart in `README.md`, deeper explanation in `guides/getting_started.md`, and executable proof tied back to the adopter lane.</action>
  <verify>
    <automated>mix test test/install_smoke/docs_parity_test.exs</automated>
  </verify>
  <done>The public docs teach one copy-pasteable AV onboarding path and the install-smoke parity lane fails on any drift away from that path.</done>
</task>

</tasks>

<verification>
Run `mix test test/install_smoke/docs_parity_test.exs test/install_smoke/release_docs_parity_test.exs`.
</verification>

<success_criteria>
AV-06-01 and AV-06-02 are satisfied when public docs teach the smallest AV
install path, link to a durable platform-install matrix, and are locked by
exact docs parity tests.
</success_criteria>

<output>
After completion, create `.planning/phases/28-onboarding-docs-ci-proof/28-01-SUMMARY.md`
</output>

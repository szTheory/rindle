---
phase: 25-rindle-processor-av
plan: 06
type: execute
wave: 5
depends_on: ["25-02", "25-03", "25-04", "25-05"]
files_modified:
  - lib/rindle/profile/presets/web.ex
  - lib/rindle/workers/process_variant.ex
  - test/rindle/contracts/telemetry_contract_test.exs
  - test/rindle/api_surface_boundary_test.exs
  - test/rindle/workers/process_variant_test.exs
  - test/rindle/profile/presets_web_test.exs
  - test/adopter/canonical_app/profile.ex
  - test/adopter/canonical_app/lifecycle_test.exs
autonomous: true
requirements:
  - AV-03-16
  - AV-03-17
  - AV-03-18
threat_model: true

must_haves:
  truths:
    - "Operators get the documented `[:rindle, :media, :transcode, :start | :stop | :exception]` event family and it is locked by contract tests before the phase closes (AV-03-16)."
    - "Per-variant progress is explicit PubSub output on `rindle:variant:#{id}` and rate-limited to at most two events per second, with the same updates rolled up onto `rindle:asset:#{asset_id}` and no log-scraping inference path (D-31, AV-03-17)."
    - "Adopters get a real stock preset story: explicit `:web_720p` video plus explicit `:poster` image, with optional scrub strip only when declared (D-10, AV-03-18)."
    - "The canonical adopter lifecycle proves the stock preset round-trip end to end instead of leaving Phase 25 at unit-test level only."
  artifacts:
    - path: "lib/rindle/profile/presets/web.ex"
      provides: "Stock preset surface for the onboarding story."
      contains: "web_720p"
    - path: "test/rindle/contracts/telemetry_contract_test.exs"
      provides: "Frozen telemetry event-name/measurement/metadata contract for AV processing."
      contains: "transcode"
    - path: "test/rindle/profile/presets_web_test.exs"
      provides: "Preset contract proof for the explicit web_720p/poster story."
      contains: "poster"
    - path: "test/adopter/canonical_app/lifecycle_test.exs"
      provides: "Real adopter round-trip proof for the stock AV preset workflow."
      contains: "web_720p"
  key_links:
    - from: "lib/rindle/workers/process_variant.ex"
      to: "test/rindle/contracts/telemetry_contract_test.exs"
      via: "public telemetry event family and metadata schema"
      pattern: "media, :transcode"
    - from: "lib/rindle/workers/process_variant.ex"
      to: "Phoenix.PubSub"
      via: "rate-limited explicit per-variant progress emission plus asset-topic roll-up"
      pattern: "rindle:(variant|asset):"
    - from: "test/adopter/canonical_app/profile.ex"
      to: "lib/rindle/profile/presets/web.ex"
      via: "stock preset consumed by canonical adopter profile"
      pattern: "web_720p"
---

<objective>
Freeze the operator-facing and adopter-facing Phase 25 contract.

Purpose: close the phase with telemetry, progress, preset, and adopter proof work so the AV wedge is observable and teachable, not just internally functional.
Output: telemetry contract tests, throttled progress emission, stock preset module, and canonical adopter round-trip proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-rindle-processor-av/25-CONTEXT.md
@.planning/phases/25-rindle-processor-av/25-RESEARCH.md
@.planning/phases/25-rindle-processor-av/25-PATTERNS.md
@lib/rindle/workers/process_variant.ex
@test/rindle/contracts/telemetry_contract_test.exs
@test/adopter/canonical_app/profile.ex
@test/adopter/canonical_app/lifecycle_test.exs

<interfaces>
From .planning/ROADMAP.md success criteria:
```text
rate-limited (≤ 2/sec) per-variant progress on `rindle:variant:#{id}`
documented `[:rindle, :media, :transcode, :start | :stop | :exception]` telemetry triplet
```

From .planning/phases/25-rindle-processor-av/25-CONTEXT.md:
```elixir
web_720p: %{kind: :video, preset: :web_720p}
poster: %{kind: :image, preset: :video_poster_scene}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze the AV telemetry and progress contract</name>
  <files>lib/rindle/workers/process_variant.ex, test/rindle/contracts/telemetry_contract_test.exs, test/rindle/api_surface_boundary_test.exs, test/rindle/workers/process_variant_test.exs</files>
  <behavior>
    - The AV telemetry event family uses the exact documented public names and stable metadata/measurement keys (AV-03-16).
    - Progress events are explicit PubSub broadcasts keyed by variant id, never inferred from logs or temp files, and are throttled to <= 2/sec per variant (D-31, AV-03-17).
    - Each emitted variant progress update also rolls up onto `rindle:asset:#{asset_id}` so asset-scoped subscribers see the same bounded progress stream without polling siblings (AV-03-17).
    - Public contract tests fail on event-name drift or metadata-shape drift before the phase can be called complete.
  </behavior>
  <action>Extend `ProcessVariant` with the final public telemetry/progress surface and lock it with contract tests. Reuse the repo’s existing telemetry-contract style, but add the AV-specific `:media/:transcode` family, the per-variant progress topic throttle, and the asset-topic roll-up required by AV-03-16 and AV-03-17. The worker should emit one bounded logical progress update fan-out to both `rindle:variant:#{variant_id}` and `rindle:asset:#{asset_id}`; do not make asset subscribers reconstruct progress by listening to every sibling topic. If a small helper is needed for throttling timestamps or deduplication, keep it internal to the worker path; the public contract is the event/topic shape, not the helper implementation. Do not infer progress from logs or partial files per D-31.</action>
  <verify>
    <automated>mix test test/rindle/contracts/telemetry_contract_test.exs test/rindle/api_surface_boundary_test.exs test/rindle/workers/process_variant_test.exs</automated>
  </verify>
  <done>The AV telemetry triplet plus both variant-topic and asset-topic PubSub progress contracts are implemented, rate-limited, and frozen by tests that fail on public-contract drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship the stock web preset and prove it through the canonical adopter flow</name>
  <files>lib/rindle/profile/presets/web.ex, test/rindle/profile/presets_web_test.exs, test/adopter/canonical_app/profile.ex, test/adopter/canonical_app/lifecycle_test.exs</files>
  <behavior>
    - The stock preset teaches the explicit `web_720p` plus `poster` story from D-10 and keeps strip generation optional.
    - The canonical adopter profile can consume the preset without inventing its own raw FFmpeg recipe.
    - The adopter lifecycle test proves a real AV round-trip, not only compile-time preset expansion.
  </behavior>
  <action>Create the stock preset surface promised by AV-03-18 as `Rindle.Profile.Presets.Web` (or the exact repo-local equivalent named in code) and teach the explicit `web_720p` + `poster` story from D-10. Keep strip generation optional and explicit per D-09. Extend the canonical adopter fixture/profile and lifecycle test so the preset-driven AV flow is exercised end to end using the existing adopter harness instead of a one-off integration script.</action>
  <verify>
    <automated>mix test test/rindle/profile/presets_web_test.exs test/adopter/canonical_app/lifecycle_test.exs --include adopter</automated>
  </verify>
  <done>The repo ships a reusable stock AV preset and the canonical adopter app proves the preset-driven video/poster lifecycle end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker internals -> operator telemetry | Public event names and metadata become a stability contract for dashboards and alerts. |
| worker -> PubSub subscribers | Progress broadcasts must be explicit and rate-limited so subscribers are not flooded. |
| stock preset module -> adopter profile | The onboarding story must not smuggle hidden sidecars or raw FFmpeg policy back into the public surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-16 | Repudiation | `test/rindle/contracts/telemetry_contract_test.exs` | mitigate | Freeze public AV event names and metadata so operator tooling cannot silently drift. |
| T-25-17 | Denial of Service | `lib/rindle/workers/process_variant.ex` progress path | mitigate | Throttle PubSub progress to <= 2/sec per variant and keep emission explicit. |
| T-25-18 | Tampering | `lib/rindle/profile/presets/web.ex` | mitigate | Keep the stock preset explicit (`web_720p` + `poster`) and avoid hidden strip sidecars or widened raw recipe knobs. |
</threat_model>

<verification>
Run `mix test test/rindle/contracts/telemetry_contract_test.exs test/rindle/api_surface_boundary_test.exs test/rindle/workers/process_variant_test.exs test/rindle/profile/presets_web_test.exs test/adopter/canonical_app/lifecycle_test.exs --include adopter`.
</verification>

<success_criteria>
AV-03-16 through AV-03-18 are satisfied when the telemetry/progress contract is frozen, the stock preset is teachable and explicit, and the canonical adopter app proves the round-trip end to end.
</success_criteria>

<output>
After completion, create `.planning/phases/25-rindle-processor-av/25-06-SUMMARY.md`
</output>

---
phase: 25-rindle-processor-av
plan: 04
type: execute
wave: 3
depends_on: ["25-01", "25-03"]
files_modified:
  - lib/rindle/processor/av.ex
  - lib/rindle/processor/av/audio.ex
  - lib/rindle/processor/waveform.ex
  - lib/rindle/profile/validator.ex
  - test/rindle/processor/av_test.exs
  - test/rindle/processor/waveform_test.exs
  - test/rindle/profile/validator_test.exs
autonomous: true
requirements:
  - AV-03-05
  - AV-03-06
  - AV-03-07
threat_model: true

must_haves:
  truths:
    - "Preset-led audio recipes produce real AAC/m4a and MP3 outputs at named bitrates with optional channel reduction, without opening raw codec/container knobs (D-02, D-03, D-04)."
    - "Loudnorm is available as the default single-pass path with an explicit `two_pass: true` branch for higher-fidelity workflows, matching the locked narrow override envelope (D-04)."
    - "Waveform output is a first-class derivative for audio assets and for video assets that actually have audio, and its public JSON contract is exactly `%{length, sample_rate, peaks}` with normalized floats (D-12 through D-18)."
    - "Silent video fails deterministically at waveform-processing time with a variant-level failure, not an asset quarantine or silent empty waveform (D-12, D-20)."
  artifacts:
    - path: "lib/rindle/processor/av/audio.ex"
      provides: "Audio transcode and loudnorm branches for preset-led AV recipes."
      contains: "loudnorm"
    - path: "lib/rindle/processor/waveform.ex"
      provides: "Narrow waveform helper that emits the v1.4 JSON contract."
      contains: "peaks"
    - path: "lib/rindle/profile/validator.ex"
      provides: "Waveform public-surface narrowing and compile-time rejection of extra DSP knobs."
      contains: "@waveform_variant_schema"
    - path: "test/rindle/processor/waveform_test.exs"
      provides: "Waveform contract proof on real fixtures."
      contains: "sample_rate"
  key_links:
    - from: "lib/rindle/processor/av.ex"
      to: "lib/rindle/processor/av/audio.ex"
      via: "normalized `:audio` dispatch"
      pattern: "m4a_128k"
    - from: "lib/rindle/processor/av/audio.ex"
      to: "lib/rindle/processor/waveform.ex"
      via: "waveform generation for audio-capable sources"
      pattern: "waveform"
    - from: "lib/rindle/profile/validator.ex"
      to: "lib/rindle/processor/waveform.ex"
      via: "compile-time narrowing of the waveform public contract to the runtime JSON shape"
      pattern: "overview"
---

<objective>
Ship the audio-output half of the AV wedge and freeze the waveform contract.

Purpose: complete the processor’s media coverage without slipping into a broad DSP toolkit or resurrecting the provisional Phase 24 waveform surface.
Output: preset-led audio transcodes, loudnorm branches, waveform helper, and compile-time/runtime tests that lock the JSON contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-rindle-processor-av/25-CONTEXT.md
@.planning/phases/25-rindle-processor-av/25-RESEARCH.md
@.planning/phases/25-rindle-processor-av/25-PATTERNS.md
@.planning/research/v1.4/LIFECYCLE.md
@lib/rindle/processor/av.ex
@lib/rindle/profile/validator.ex
@lib/rindle/probe/av_probe.ex
@test/rindle/probe/av_probe_test.exs
@test/rindle/profile/validator_test.exs

<interfaces>
From .planning/phases/25-rindle-processor-av/25-CONTEXT.md:
```elixir
%{length: bucket_count, sample_rate: analysis_rate, peaks: [[min, max], ...]}
```

From lib/rindle/profile/validator.ex current seam:
```elixir
@audio_variant_schema [...]
@waveform_variant_schema [...]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement preset-led audio transcodes and loudnorm branches</name>
  <files>lib/rindle/processor/av.ex, lib/rindle/processor/av/audio.ex, test/rindle/processor/av_test.exs</files>
  <behavior>
    - `preset: :m4a_128k` and `preset: :mp3_128k` produce real audio artifacts with validated container/codec pairing (AV-03-05).
    - `normalize: true` applies the single-pass EBU R128 loudnorm filter by default, while `two_pass: true` selects the explicit higher-fidelity branch (AV-03-06, D-04).
    - Channel reduction remains optional and bounded to the allowed values already locked by the validator.
  </behavior>
  <action>Extend `Rindle.Processor.AV` with audio dispatch backed by a dedicated `lib/rindle/processor/av/audio.ex` helper. Implement named audio presets only, preserve the narrow override envelope from D-04, and keep loudnorm behavior explicit: single-pass by default, `two_pass: true` only when requested. Do not add arbitrary filter strings, codec passthrough, or caller-owned bitrate arithmetic beyond the preset family.</action>
  <verify>
    <automated>mix test test/rindle/processor/av_test.exs</automated>
  </verify>
  <done>Named m4a/mp3 recipes and loudnorm branches produce real audio outputs and are verified by real-fixture tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Narrow the waveform public surface and implement the v1.4 JSON contract</name>
  <files>lib/rindle/processor/waveform.ex, lib/rindle/profile/validator.ex, lib/rindle/processor/av.ex, test/rindle/processor/waveform_test.exs, test/rindle/profile/validator_test.exs</files>
  <behavior>
    - Waveform output is preset-led and emits exactly `%{length, sample_rate, peaks}` with normalized float tuples (AV-03-07, D-15, D-16).
    - The compile-time DSL rejects extra public waveform knobs such as `channels`, `bits`, or raw peak-shaping controls, even if internal normalization still handles legacy shape where necessary (D-17, D-18).
    - Video without an audio track fails deterministically when asked for a waveform instead of emitting an empty contract (D-12).
  </behavior>
  <action>Create a narrow waveform helper and matching tests that implement AV-03-07 per D-12 through D-18. Collapse the documented public DSL to the preset-owned `:overview` story and the exact `%{length, sample_rate, peaks}` contract; if backward-compatible internal normalization is needed, keep it internal and undocumented. Use FFmpeg + Elixir/Jason only; do not depend on `audiowaveform` or widen the public API into an editor/DSP surface.</action>
  <verify>
    <automated>mix test test/rindle/processor/waveform_test.exs test/rindle/profile/validator_test.exs</automated>
  </verify>
  <done>Waveform output is first-class, contract-frozen, and compile-time/runtime tests prove the narrowed `%{length, sample_rate, peaks}` surface plus deterministic silent-video failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| normalized audio recipe -> audio filters | Loudnorm and channel options must stay bounded to the approved override surface. |
| probed source -> waveform contract | Missing audio tracks or malformed analysis output must fail explicitly rather than generating misleading JSON. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-10 | Tampering | `lib/rindle/profile/validator.ex` waveform branch | mitigate | Reject extra public DSP knobs and keep waveform public shape locked to preset-led `%{length, sample_rate, peaks}`. |
| T-25-11 | Information Disclosure | waveform output | mitigate | Emit only normalized peak pairs and analysis rate; do not surface raw internal decoder details or extra metadata. |
| T-25-12 | Denial of Service | loudnorm/two-pass path | mitigate | Keep two-pass normalization opt-in and bounded by preset-owned execution rules rather than exposing arbitrary filter graphs. |
</threat_model>

<verification>
Run `mix test test/rindle/processor/av_test.exs test/rindle/processor/waveform_test.exs test/rindle/profile/validator_test.exs`.
</verification>

<success_criteria>
AV-03-05 through AV-03-07 are satisfied when named audio presets, loudnorm branches, and the frozen waveform JSON contract all work on real fixtures and the public DSL stays narrow.
</success_criteria>

<output>
After completion, create `.planning/phases/25-rindle-processor-av/25-04-SUMMARY.md`
</output>

---
phase: 25-rindle-processor-av
plan: 03
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - lib/rindle/processor/av.ex
  - lib/rindle/processor/av/video.ex
  - test/rindle/processor/av_test.exs
autonomous: true
requirements:
  - AV-03-02
  - AV-03-03
  - AV-03-04
threat_model: true

must_haves:
  truths:
    - "A preset-led `:video` recipe produces a playable H.264 + AAC mp4 with `+faststart` and validated dimensions/bitrates; no raw filter or codec strings are reachable (D-02, D-03, D-04)."
    - "Poster extraction is a first-class explicit image variant, not a hidden sidecar, and it follows the scene-change -> first I-frame -> first frame fallback chain (D-07, D-08, D-11)."
    - "Thumbnail strips remain explicit opt-in image outputs for scrubbing UIs and are not silently bundled into the default onboarding preset (D-09, D-10)."
  artifacts:
    - path: "lib/rindle/processor/av/video.ex"
      provides: "Video transcode, poster, and strip argv builders plus execution helpers."
      contains: "scene"
    - path: "lib/rindle/processor/av.ex"
      provides: "Public dispatch from normalized `:video` and image-output AV recipes into video helpers."
      contains: "output_kind"
    - path: "test/rindle/processor/av_test.exs"
      provides: "Real-fixture proof for mp4 transcode, poster fallback, and explicit strip generation."
      contains: "faststart"
  key_links:
    - from: "lib/rindle/processor/av.ex"
      to: "lib/rindle/processor/av/video.ex"
      via: "normalized `kind/output_kind/preset` dispatch"
      pattern: "web_720p"
    - from: "lib/rindle/processor/av/video.ex"
      to: "lib/rindle/av/subprocess.ex"
      via: "bounded FFmpeg execution for mp4, poster, and strip outputs"
      pattern: "Subprocess"
    - from: "test/rindle/processor/av_test.exs"
      to: "lib/rindle/probe/av_probe.ex"
      via: "probe produced artifacts instead of trusting file extension only"
      pattern: "AVProbe"
---

<objective>
Ship the video-output half of the AV processor on top of the normalized Phase 25 boundary.

Purpose: prove the bundled processor can generate the Phase 25 video derivatives the roadmap promises without re-opening raw FFmpeg surface area.
Output: preset-led mp4 transcode, explicit poster extraction, optional strip generation, and real-media tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-rindle-processor-av/25-CONTEXT.md
@.planning/phases/25-rindle-processor-av/25-RESEARCH.md
@.planning/phases/25-rindle-processor-av/25-PATTERNS.md
@.planning/research/v1.4/LIFECYCLE.md
@lib/rindle/processor/av.ex
@lib/rindle/av/subprocess.ex
@lib/rindle/probe/av_probe.ex
@test/rindle/probe/av_probe_test.exs
@test/rindle/processor/ffmpeg_test.exs

<interfaces>
From .planning/phases/25-rindle-processor-av/25-CONTEXT.md:
```elixir
poster: %{kind: :image, preset: :video_poster_scene}
web_720p: %{kind: :video, preset: :web_720p}
```

From .planning/phases/25-rindle-processor-av/25-RESEARCH.md:
```elixir
AV-03-03 -> first I-frame after scene change > 0.4
fallback -> first I-frame
fallback -> first frame
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement preset-led mp4 video transcode on `Rindle.Processor.AV`</name>
  <files>lib/rindle/processor/av.ex, lib/rindle/processor/av/video.ex, test/rindle/processor/av_test.exs</files>
  <behavior>
    - `preset: :web_720p` yields mp4/H.264/AAC output with validated dimensions, CRF/bitrate envelope, and `+faststart` by default (AV-03-02).
    - The processor accepts only preset-owned codec/container choices and the narrow override envelope already normalized in Plan 01 (D-02, D-03, D-04).
    - Real output is probeable/playable rather than a dummy file or stringly mock.
  </behavior>
  <action>Extend `Rindle.Processor.AV` with the concrete video branch for AV-03-02 using a dedicated `lib/rindle/processor/av/video.ex` helper. Keep all low-level FFmpeg execution behind the existing durable subprocess seam; validate dimensions/container/codec from the normalized preset map and emit a real mp4 artifact with `+faststart`. Do not reintroduce arbitrary `-vf`, arbitrary codec names, or ad-hoc caller-owned bitrates beyond the Plan 01 normalization envelope.</action>
  <verify>
    <automated>mix test test/rindle/processor/av_test.exs</automated>
  </verify>
  <done>A normalized `:web_720p` recipe produces a real mp4 artifact that probes as video, respects the preset envelope, and is verified by a real-fixture test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add explicit poster and strip outputs without hidden sidecars</name>
  <files>lib/rindle/processor/av.ex, lib/rindle/processor/av/video.ex, test/rindle/processor/av_test.exs</files>
  <behavior>
    - Poster generation is invoked only for an explicit declared variant and follows the scene-detection fallback chain from D-11.
    - Poster output lands as an image artifact suitable for the existing image delivery path; it is not auto-generated as a hidden sibling (D-07, D-08).
    - Thumbnail strips are explicit opt-in derivatives and stay out of the stock onboarding path unless declared (D-09, D-10).
  </behavior>
  <action>Implement the explicit image-output AV paths in the video helper: poster extraction with the Rails-style scene/I-frame fallback chain, plus optional strip generation for scrubbing UIs. Treat both as normal declared variants per D-07 through D-11. Keep the stock `web_720p` onboarding story poster-focused and make strip generation conditional on an explicit strip preset or explicit output-kind recipe; do not hide poster/strip creation inside the mp4 branch.</action>
  <verify>
    <automated>mix test test/rindle/processor/av_test.exs</automated>
  </verify>
  <done>Explicit poster variants and opt-in strip variants produce real image artifacts, and tests prove the fallback chain rather than merely checking command assembly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| normalized video recipe -> FFmpeg args | Preset-owned data must remain the only source of codec/filter/container choices. |
| source video -> poster/strip extraction | Untrusted input must not silently create undeclared derivative side effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-07 | Tampering | `lib/rindle/processor/av/video.ex` | mitigate | Keep codec/container/filter choices preset-owned and reject widened caller input. |
| T-25-08 | Information Disclosure | poster/strip side effects | mitigate | Generate poster/strip only for explicit declared variants so no hidden derivative rows or undeclared files appear. |
| T-25-09 | Denial of Service | strip extraction | mitigate | Keep strip generation explicit opt-in and bounded by preset-owned frame count/dimensions. |
</threat_model>

<verification>
Run `mix test test/rindle/processor/av_test.exs`.
</verification>

<success_criteria>
AV-03-02 through AV-03-04 are satisfied when the processor emits real preset-led mp4, poster, and explicit strip outputs with no raw-FFmpeg caller escape hatch and no hidden derivative sidecars.
</success_criteria>

<output>
After completion, create `.planning/phases/25-rindle-processor-av/25-03-SUMMARY.md`
</output>

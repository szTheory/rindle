---
phase: 25-rindle-processor-av
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - lib/rindle/av/temp_run_dir.ex
  - lib/rindle/domain/asset_aggregate.ex
  - lib/rindle/workers/process_variant.ex
  - lib/rindle/workers/promote_asset.ex
  - test/rindle/workers/process_variant_test.exs
  - test/rindle/workers/promote_asset_test.exs
autonomous: true
requirements:
  - AV-03-08
  - AV-03-10
  - AV-03-11
  - AV-03-12
threat_model: true

must_haves:
  truths:
    - "The same `(asset_id, variant_name)` replay is safe: deterministic `storage_key`, explicit active-job uniqueness, and no partial tempfiles survive a normal success/failure path (D-26, D-27, D-28)."
    - "AV jobs route to a dedicated `:rindle_media` queue while image work keeps the short queue, preserving queue isolation without introducing a second variant worker abstraction (D-26, D-27)."
    - "Every run gets a single `Rindle.tmp/<uuid>/` subtree and terminal cleanup path, so temp-root accounting is deterministic before the sweeper exists (D-28)."
    - "After every terminal variant outcome, the asset aggregate state is recomputed from persisted sibling rows: `transcoding`, `ready`, or `degraded` per D-19 through D-25."
  artifacts:
    - path: "lib/rindle/av/temp_run_dir.ex"
      provides: "Single-run temp root creation and cleanup helpers."
      contains: "Rindle.tmp"
    - path: "lib/rindle/domain/asset_aggregate.ex"
      provides: "Aggregate asset-state recomputation from persisted variant rows."
      contains: "def recompute"
    - path: "lib/rindle/workers/process_variant.ex"
      provides: "Idempotent AV-capable variant worker with unique/timeout/atomic-ready guards."
      contains: "use Oban.Worker"
    - path: "lib/rindle/workers/promote_asset.ex"
      provides: "Queue-aware variant enqueueing using normalized specs."
      contains: "ProcessVariant.new"
  key_links:
    - from: "lib/rindle/workers/promote_asset.ex"
      to: "lib/rindle/workers/process_variant.ex"
      via: "queue-aware job construction from each declared variant row"
      pattern: "rindle_media"
    - from: "lib/rindle/workers/process_variant.ex"
      to: "lib/rindle/domain/asset_aggregate.ex"
      via: "recompute aggregate asset state after every terminal variant write"
      pattern: "recompute"
    - from: "lib/rindle/workers/process_variant.ex"
      to: "lib/rindle/av/temp_run_dir.ex"
      via: "download/process/upload lifecycle uses a single run dir per job"
      pattern: "TempRunDir"
---

<objective>
Evolve the durable worker seam so AV processing remains one row, one job, one terminal state.

Purpose: land the non-negotiable worker guarantees before codec-specific output work expands the surface: idempotency, queue isolation, atomic ready checks, aggregate recompute, and disciplined temp-root handling.
Output: AV-capable `ProcessVariant` worker evolution, queue-aware variant enqueueing, aggregate-state helper, and run-directory helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-rindle-processor-av/25-CONTEXT.md
@.planning/phases/25-rindle-processor-av/25-RESEARCH.md
@.planning/phases/25-rindle-processor-av/25-PATTERNS.md
@lib/rindle/workers/process_variant.ex
@lib/rindle/workers/promote_asset.ex
@lib/rindle/domain/media_asset.ex
@lib/rindle/domain/media_variant.ex
@lib/rindle/domain/asset_fsm.ex
@lib/rindle/domain/variant_fsm.ex
@lib/rindle/profile.ex
@test/rindle/workers/process_variant_test.exs
@test/rindle/workers/promote_asset_test.exs

<interfaces>
From lib/rindle/domain/media_variant.ex:
```elixir
field :name, :string
field :state, :string, default: "planned"
field :recipe_digest, :string
field :storage_key, :string
field :output_kind, :string, default: "image"
```

From .planning/phases/25-rindle-processor-av/25-RESEARCH.md:
```elixir
with {:ok, spec} <- Rindle.Processor.AV.normalize(variant_spec),
     {:ok, run_dir} <- TempRunDir.create(),
     {:ok, artifact} <- Rindle.Processor.AV.process(source_tmp, spec, run_dir),
     {:ok, storage_meta} <- upload_variant(asset, variant, artifact),
     :ok <- ReadyGuard.check_asset_unchanged!(repo, asset, variant) do
  persist_ready(...)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make ProcessVariant idempotent, queue-aware, and run-dir disciplined</name>
  <files>lib/rindle/av/temp_run_dir.ex, lib/rindle/workers/process_variant.ex, lib/rindle/workers/promote_asset.ex, test/rindle/workers/process_variant_test.exs, test/rindle/workers/promote_asset_test.exs</files>
  <behavior>
    - AV jobs build on the existing `ProcessVariant` worker rather than a new hidden pipeline (D-26).
    - AV variants enqueue onto `:rindle_media` with explicit uniqueness and timeout policy, while image variants keep the existing queue behavior (D-27).
    - Every job creates one `Rindle.tmp/<uuid>/` subtree, downloads source + writes outputs inside it, and guarantees cleanup on both success and handled failure (D-28).
    - Re-running the same job reuses deterministic storage-key derivation from `recipe_digest` and does not create duplicate active jobs or unstable output locations (AV-03-08, AV-03-11).
  </behavior>
  <action>Refactor `Rindle.Workers.ProcessVariant` and `Rindle.Workers.PromoteAsset` so the worker stays the single durable variant seam per D-26, but AV-capable variants get their own queue/unique/timeout controls per D-27 and AV-03-11. Add a small `Rindle.AV.TempRunDir` helper that owns `Rindle.tmp/<uuid>/` creation, path joins, and cleanup per D-28. Use normalized recipe data from Plan 01 when deciding queue, expected extension, deterministic storage key, and timeout. Do not introduce a multi-output job, a hidden batch worker, or scattered `System.tmp_dir!/0` writes.</action>
  <verify>
    <automated>mix test test/rindle/workers/process_variant_test.exs test/rindle/workers/promote_asset_test.exs</automated>
  </verify>
  <done>`ProcessVariant` remains the only variant worker, AV jobs route to `:rindle_media` with uniqueness/timeout behavior, and all AV tempfiles live under one per-run `Rindle.tmp/<uuid>/` subtree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recompute aggregate asset state and enforce atomic ready promotion</name>
  <files>lib/rindle/domain/asset_aggregate.ex, lib/rindle/workers/process_variant.ex, test/rindle/workers/process_variant_test.exs</files>
  <behavior>
    - The first active AV variant pushes the asset into `transcoding`; fully ready siblings move it to `ready`; mixed terminal results move it to `degraded` (D-19 through D-25).
    - A source re-upload during transcode prevents the terminal ready write by comparing persisted source identity before flip-to-ready (AV-03-10).
    - Failed or cancelled siblings do not purge or invalidate already-ready outputs; the asset remains usable but degraded (D-19, D-23, D-25).
  </behavior>
  <action>Add an explicit aggregate-state helper, e.g. `Rindle.Domain.AssetAggregate`, so `ProcessVariant` can recompute the asset state from persisted sibling rows after every terminal variant write per D-21 through D-25. Implement the atomic-ready guard from AV-03-10 by reloading the asset and relevant variant row just before the terminal `ready` update and aborting if source identity changed. Keep quarantine reserved for source-trust failures per D-20; transcode failures should drive `variant.state = failed` and asset `state = degraded`, not asset quarantine.</action>
  <verify>
    <automated>mix test test/rindle/workers/process_variant_test.exs</automated>
  </verify>
  <done>Aggregate state moves through `transcoding`/`ready`/`degraded` based on persisted sibling rows, and stale-source races abort the ready write instead of attaching an old derivative.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PromoteAsset -> ProcessVariant job args | Queue routing and uniqueness must not let long AV work starve image jobs or stampede duplicates. |
| Worker temp root -> storage upload | Partial local files must not become durable ready variants. |
| Worker terminal write -> asset row | A re-uploaded source can invalidate the in-flight derivative just before promotion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | Denial of Service | `lib/rindle/workers/promote_asset.ex` enqueue path | mitigate | Route AV work to `:rindle_media` with active-job uniqueness so long transcodes cannot clog the short image queue. |
| T-25-05 | Tampering | `lib/rindle/workers/process_variant.ex` ready transition | mitigate | Reload asset/variant identity before final ready write and abort stale-source promotions. |
| T-25-06 | Information Disclosure | `lib/rindle/av/temp_run_dir.ex` | mitigate | Keep all temporary artifacts in one sweepable subtree and guarantee cleanup through `try/after` style finalization. |
</threat_model>

<verification>
Run `mix test test/rindle/workers/process_variant_test.exs test/rindle/workers/promote_asset_test.exs`.
</verification>

<success_criteria>
AV-03-08, AV-03-10, AV-03-11, and AV-03-12 are satisfied when AV work remains one row/one job, uses a dedicated queue, cleans up one run root per job, and recomputes aggregate asset state without stale-source promotion bugs.
</success_criteria>

<output>
After completion, create `.planning/phases/25-rindle-processor-av/25-02-SUMMARY.md`
</output>

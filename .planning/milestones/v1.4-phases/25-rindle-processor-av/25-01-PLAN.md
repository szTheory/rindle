---
phase: 25-rindle-processor-av
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/processor/av.ex
  - lib/rindle/processor/av/recipe.ex
  - lib/rindle/processor/ffmpeg.ex
  - lib/rindle/profile.ex
  - lib/rindle/profile/validator.ex
  - test/rindle/processor/av_test.exs
  - test/rindle/profile/validator_test.exs
  - test/rindle/backward_compat/v13_digest_snapshot_test.exs
autonomous: true
requirements:
  - AV-03-01
threat_model: true

must_haves:
  truths:
    - "Profiles can declare AV variants on the existing flat `variants: [...]` DSL with preset-led recipes only; no nested AV builder surface appears (D-01, D-02, D-03)."
    - "`Rindle.Processor.AV` is the public processor boundary and exposes `capabilities/0` plus canonical recipe normalization before any digest or argv work (D-05)."
    - "The allowed override envelope is narrow and explicit: only `normalize`, `two_pass`, `channels`, `faststart`, and explicit poster/waveform hooks survive validation; raw codec/filter passthrough is rejected (D-04, D-06)."
    - "Recipe digests stay stable across equivalent preset declarations and map ordering, preserving the Phase 24 digest guarantees while adding AV normalization."
  artifacts:
    - path: "lib/rindle/processor/av.ex"
      provides: "Public AV processor boundary implementing `Rindle.Processor` and `capabilities/0`."
      contains: "def capabilities"
    - path: "lib/rindle/processor/av/recipe.ex"
      provides: "Canonical AV recipe normalization and preset expansion."
      contains: "def normalize"
    - path: "lib/rindle/profile/validator.ex"
      provides: "Preset-led AV variant validation with narrow override allowlist."
      contains: "@video_variant_schema"
    - path: "test/rindle/backward_compat/v13_digest_snapshot_test.exs"
      provides: "Digest stability guard against Phase 25 normalization regressions."
      contains: "@v13_thumb_digest"
  key_links:
    - from: "lib/rindle/profile/validator.ex"
      to: "lib/rindle/processor/av/recipe.ex"
      via: "validated AV specs remain plain maps that normalize into one canonical internal shape"
      pattern: "preset"
    - from: "lib/rindle/profile.ex"
      to: "lib/rindle/processor/av/recipe.ex"
      via: "recipe_digest/1 must hash normalized AV recipes rather than raw caller ordering"
      pattern: "recipe_digest"
    - from: "lib/rindle/processor/ffmpeg.ex"
      to: "lib/rindle/processor/av.ex"
      via: "prototype seam becomes a thin delegate or is retired so Phase 25 centers on AV boundary, not raw argv plumbing"
      pattern: "Rindle\\.Processor\\.AV"
---

<objective>
Define the Phase 25 public AV processor seam before worker or codec work lands.

Purpose: lock the repo on `Rindle.Processor.AV` plus canonical recipe normalization so every downstream plan builds against one stable preset-led contract instead of expanding the prototype `Rindle.Processor.Ffmpeg` seam.
Output: AV processor boundary, normalization helper, validator narrowing, and digest-stability tests that preserve Phase 24 compatibility.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-rindle-processor-av/25-CONTEXT.md
@.planning/phases/25-rindle-processor-av/25-RESEARCH.md
@.planning/phases/25-rindle-processor-av/25-PATTERNS.md
@.planning/phases/24-domain-model-dsl-extension/24-CONTEXT.md
@.planning/phases/24-domain-model-dsl-extension/24-RESEARCH.md
@.planning/phases/24-domain-model-dsl-extension/24-PATTERNS.md
@.planning/phases/24-domain-model-dsl-extension/24-05-PLAN.md
@.planning/research/v1.4/SYNTHESIS.md
@.planning/research/v1.4/ADAPTER.md
@lib/rindle/processor.ex
@lib/rindle/processor/ffmpeg.ex
@lib/rindle/profile.ex
@lib/rindle/profile/validator.ex
@test/rindle/backward_compat/v13_digest_snapshot_test.exs

<interfaces>
From lib/rindle/processor.ex:
```elixir
@callback process(source :: Path.t(), variant_spec :: map(), destination :: Path.t()) ::
            {:ok, Path.t()} | {:error, term()}
```

From lib/rindle/profile.ex:
```elixir
@spec variants() :: [Rindle.Profile.variant_entry()]
@spec recipe_digest(atom()) :: String.t()
```

From .planning/phases/25-rindle-processor-av/25-RESEARCH.md:
```elixir
%{
  kind: :video,
  output_kind: :video,
  preset: :web_720p,
  container: :mp4,
  video_codec: :h264,
  audio_codec: :aac,
  width: 1280,
  height: 720,
  crf: 23,
  audio_bitrate_kbps: 128,
  faststart: true,
  normalize: false,
  two_pass: false
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the public AV processor boundary and canonical recipe normalizer</name>
  <files>lib/rindle/processor/av.ex, lib/rindle/processor/av/recipe.ex, lib/rindle/processor/ffmpeg.ex, test/rindle/processor/av_test.exs</files>
  <behavior>
    - `Rindle.Processor.AV.capabilities/0` returns the exact Phase 25 capability list required by AV-03-01.
    - `Rindle.Processor.AV.normalize/1` turns equivalent caller specs into the same canonical map before dispatch or digest work (D-05).
    - Unsupported raw FFmpeg-style keys are rejected at the boundary instead of being threaded into argv (D-03, D-04, D-06).
    - `Rindle.Processor.Ffmpeg` is no longer the phase’s primary seam; it either delegates to `Rindle.Processor.AV` or is reduced to a compatibility wrapper with no new recipe logic.
  </behavior>
  <action>Implement `Rindle.Processor.AV` as the only public AV processor seam per D-01 through D-06. Put preset expansion and canonical-map building in `lib/rindle/processor/av/recipe.ex`; keep `lib/rindle/processor/av.ex` as the public boundary that implements `Rindle.Processor`, exposes `capabilities/0`, and provides `normalize/1`/dispatch helpers. Do not add a nested DSL, raw codec passthrough, arbitrary filter strings, or a registry abstraction. Fold the repo-local correction from `25-RESEARCH.md` into this plan by de-emphasizing `lib/rindle/processor/ffmpeg.ex`: keep it only as a thin internal delegate if a compatibility shim is needed, but do not grow it further.</action>
  <verify>
    <automated>mix test test/rindle/processor/av_test.exs</automated>
  </verify>
  <done>`Rindle.Processor.AV` exists, advertises the exact AV capability list, normalizes preset-led specs into one deterministic internal map, and no new caller-facing behavior is added to `Rindle.Processor.Ffmpeg`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Narrow validator inputs and preserve digest stability across normalized AV recipes</name>
  <files>lib/rindle/profile.ex, lib/rindle/profile/validator.ex, test/rindle/profile/validator_test.exs, test/rindle/backward_compat/v13_digest_snapshot_test.exs</files>
  <behavior>
    - Video/audio/waveform recipes compile on the existing flat `variants: [...]` surface with `preset` as the primary public knob (D-01, D-02).
    - Only the approved override envelope compiles; attempts to inject broader knobs raise `ArgumentError` at compile time (D-04).
    - Normalized AV recipes produce stable digests independent of map ordering or redundant equivalent fields (D-05).
    - The v1.3 image digest snapshot remains unchanged after Phase 25 normalization work.
  </behavior>
  <action>Update `Rindle.Profile.Validator` and any `Rindle.Profile` digest plumbing so Phase 25 AV recipes remain plain validated maps, but the allowed public surface matches D-01 through D-05 exactly. Add compile-time tests for accepted preset-led video/audio/waveform declarations and rejected broad knobs; extend the backward-compat snapshot coverage so image-only digest stability still fails loudly if AV normalization contaminates existing recipes. If normalized digest computation requires a repo-local hook in `Rindle.Profile`, keep it narrow and deterministic rather than spreading hashing rules into the worker.</action>
  <verify>
    <automated>mix test test/rindle/profile/validator_test.exs test/rindle/backward_compat/v13_digest_snapshot_test.exs</automated>
  </verify>
  <done>AV recipe validation is preset-led and narrow, equivalent normalized AV specs hash identically, and the v1.3 image digest snapshot still passes unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile DSL -> normalized recipe | Untrusted adopter-supplied recipe keys must not widen the subprocess surface. |
| normalized recipe -> argv builder | Canonical recipe data is the last place to reject raw FFmpeg escape hatches. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | `lib/rindle/profile/validator.ex` | mitigate | Reject raw codec/filter/argv-like keys and keep the override envelope to D-04 only. |
| T-25-02 | Elevation of Privilege | `lib/rindle/processor/av/recipe.ex` | mitigate | Normalize to preset-owned canonical maps before any subprocess-facing code sees user-controlled data. |
| T-25-03 | Repudiation | `lib/rindle/profile.ex` digest path | mitigate | Hash normalized recipes so equivalent declarations cannot silently fork storage keys or stale detection outcomes. |
</threat_model>

<verification>
Run `mix test test/rindle/processor/av_test.exs test/rindle/profile/validator_test.exs test/rindle/backward_compat/v13_digest_snapshot_test.exs`.
</verification>

<success_criteria>
AV-03-01 is satisfied when the repo has one public AV processor boundary, one canonical recipe normalizer, compile-time rejection for widened DSL inputs, and digest-stability proof that preserves the existing image contract.
</success_criteria>

<output>
After completion, create `.planning/phases/25-rindle-processor-av/25-01-SUMMARY.md`
</output>

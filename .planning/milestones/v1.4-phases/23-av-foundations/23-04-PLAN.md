---
phase: 23-av-foundations
plan: 04
type: execute
wave: 3
depends_on: [23-02, 23-03]
files_modified:
  - lib/rindle/processor/ffmpeg.ex
  - test/rindle/processor/ffmpeg_test.exs
autonomous: true
requirements: [AV-CMD]
must_haves:
  truths:
    - "AV adapter implements the Rindle.Processor behaviour"
    - "Adapter successfully processes capabilities using the Subprocess wrapper"
  artifacts:
    - path: "lib/rindle/processor/ffmpeg.ex"
      provides: "The processor adapter implementation"
  key_links:
    - from: "lib/rindle/processor/ffmpeg.ex"
      to: "lib/rindle/av/subprocess.ex"
      via: "calls Rindle.AV.Subprocess.run/3"
      pattern: "Subprocess\\.run"
---

<objective>
Implement the FFmpeg processor adapter using the `Rindle.Processor` behaviour and integrating with the MuonTrap subprocess wrapper.

Purpose: Provide a compliant adapter to be plugged into the Rindle storage/processing architecture for audio/video manipulation.
Output: Working `Rindle.Processor.Ffmpeg` adapter module and tests.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>FFmpeg Processor Adapter</name>
  <files>lib/rindle/processor/ffmpeg.ex, test/rindle/processor/ffmpeg_test.exs</files>
  <behavior>
    Adapter (`Rindle.Processor.Ffmpeg`):
    - Must implement the `@behaviour Rindle.Processor`.
    - `process(source_path, variant_spec, destination_path)` models the capability from `variant_spec` (e.g., via `Rindle.AV.Capability`) to `ffmpeg` CLI arguments.
    - Argument generation must use `Rindle.Security.Argv` for validation and prevent shell injection.
    - Executes via `Rindle.AV.Subprocess.run/3`.
    - Returns `{:ok, destination_path}` on success, `{:error, reason}` on failure.
  </behavior>
  <action>
    Follow the `lib/rindle/processor/image.ex` (analog) structure for the behaviour implementation. Wire it to use `Rindle.AV.Subprocess`.
  </action>
  <verify>
    <automated>mix test test/rindle/processor/ffmpeg_test.exs</automated>
  </verify>
  <done>The adapter successfully executes FFmpeg operations through the subprocess wrapper, passing integration tests and correctly returning output paths.</done>
</task>
</tasks>
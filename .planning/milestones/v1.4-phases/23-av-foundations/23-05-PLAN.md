---
phase: 23-av-foundations
plan: 05
type: execute
wave: 3
depends_on: [23-02]
files_modified:
  - lib/rindle/av/ffprobe.ex
  - test/rindle/av/ffprobe_test.exs
  - lib/rindle/ops/orphan_reaper.ex
  - test/rindle/ops/orphan_reaper_test.exs
autonomous: true
requirements: [AV-PROBE, AV-ORPHAN]
must_haves:
  truths:
    - "System extracts metadata via FFprobe while treating it as untrusted UGC"
    - "System routinely cleans up orphaned AV temporary files in Rindle.tmp/"
  artifacts:
    - path: "lib/rindle/av/ffprobe.ex"
      provides: "FFprobe metadata extractor shim"
    - path: "lib/rindle/ops/orphan_reaper.ex"
      provides: "Scheduled Rindle.tmp/ orphan cleanup"
  key_links: []
---

<objective>
Implement the FFprobe shim for safe metadata extraction and an orphan reaper for temporary file cleanup.

Purpose: Provide safe container metadata extraction treating output as untrusted UGC, and prevent storage bloat from temporary files.
Output: Working FFprobe shim module and scheduled orphan reaper operation.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: FFprobe Metadata Extractor Shim</name>
  <files>lib/rindle/av/ffprobe.ex, test/rindle/av/ffprobe_test.exs</files>
  <behavior>
    - Runs `ffprobe` using the `Rindle.AV.Subprocess` wrapper.
    - Extracts format and stream metadata in JSON format.
    - Treats all parsed metadata fields as untrusted UGC, ensuring strings are properly encoded/escaped before yielding.
  </behavior>
  <action>
    Implement `Rindle.AV.Ffprobe` to extract metadata from media files. Use JSON output (`-print_format json`) and decode it safely. Ensure returned metadata strings are sanitized or flagged as UGC to prevent XSS down the line.
  </action>
  <verify>
    <automated>mix test test/rindle/av/ffprobe_test.exs</automated>
  </verify>
  <done>FFprobe shim correctly parses metadata and returns sanitized data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rindle.tmp/ Scheduled Orphan Reaper</name>
  <files>lib/rindle/ops/orphan_reaper.ex, test/rindle/ops/orphan_reaper_test.exs</files>
  <behavior>
    - Scans a configured `Rindle.tmp/` directory for temporary files older than a specified threshold (e.g., 24 hours).
    - Safely deletes orphaned files.
    - Operates as a background job or periodic operation.
  </behavior>
  <action>
    Implement `Rindle.Ops.OrphanReaper` taking inspiration from existing Ops modules. It should traverse the temporary directory and delete files exceeding their time-to-live threshold.
  </action>
  <verify>
    <automated>mix test test/rindle/ops/orphan_reaper_test.exs</automated>
  </verify>
  <done>Reaper correctly identifies and deletes only expired temporary files.</done>
</task>
</tasks>
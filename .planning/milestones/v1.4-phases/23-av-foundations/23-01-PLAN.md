---
phase: 23-av-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/av/capability.ex
  - test/rindle/av/capability_test.exs
  - lib/rindle/security/argv.ex
  - test/rindle/security/argv_test.exs
autonomous: true
requirements: [AV-CAP, AV-ARGV]
must_haves:
  truths:
    - "System can represent video and audio capabilities as domain terms"
    - "System rejects format strings containing shell interpolation"
  artifacts:
    - path: "lib/rindle/av/capability.ex"
      provides: "Domain vocabulary for processing caps"
    - path: "lib/rindle/security/argv.ex"
      provides: "Argv sanitization and format validation"
  key_links: []
---

<objective>
Establish the foundational AV domain vocabulary and secure argument string validation logic.

Purpose: Prevent command injection and provide a strict internal taxonomy for media capabilities.
Output: Working capability module and argv validator with full test coverage.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Capability Vocabulary and Security Argv Hygiene</name>
  <files>lib/rindle/av/capability.ex, test/rindle/av/capability_test.exs, lib/rindle/security/argv.ex, test/rindle/security/argv_test.exs</files>
  <behavior>
    Capabilities:
    - Should define a struct or set of atoms (like `:video_transcode`, `:audio_normalize`)
    
    Argv Validator (`Rindle.Security.Argv`):
    - Must reject strings containing shell interpolation patterns (e.g., `;`, `&`, `|`, `$( )`, backticks)
    - Must enforce that protocol whitelist is explicitly included for ffmpeg commands
    - Must explicitly reject HLS / DASH / MKV ingest inputs.
    - Returns `{:ok, valid_string}` or `{:error, :invalid_format}`
  </behavior>
  <action>
    Implement `Rindle.AV.Capability` leveraging the structure from `lib/rindle/processor.ex` (analog).
    Implement `Rindle.Security.Argv.validate/1` and `sanitize/1`Modeled after `lib/rindle/security/filename.ex`. Use Regex to strictly forbid shell injection vectors and explicitly reject unsupported manifest formats like HLS/DASH/MKV.
  </action>
  <verify>
    <automated>mix test test/rindle/av/capability_test.exs test/rindle/security/argv_test.exs</automated>
  </verify>
  <done>Tests pass showing explicit rejection of shell characters and explicit rejection of HLS/DASH/MKV ingest.</done>
</task>
</tasks>
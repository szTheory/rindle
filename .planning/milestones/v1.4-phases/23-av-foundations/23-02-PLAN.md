---
phase: 23-av-foundations
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - lib/rindle/av/subprocess.ex
  - test/rindle/av/subprocess_test.exs
autonomous: true
requirements: [AV-CMD]
must_haves:
  truths:
    - "Subprocesses execute via MuonTrap and clean up orphans on parent death"
    - "Four-cap resource limits are enforced on all subprocess invocations"
  artifacts:
    - path: "lib/rindle/av/subprocess.ex"
      provides: "MuonTrap execution wrapper + 4-cap enforcement"
  key_links: []
---

<objective>
Implement the MuonTrap execution wrapper and four-cap execution enforcement to safely execute `ffmpeg` and `ffprobe`.

Purpose: Sandbox process execution using cgroups on Linux to prevent zombie processes and limit system resource abuse (CPU and memory).
Output: A testable OS-aware wrapper around MuonTrap with 4-cap rules.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>MuonTrap Subprocess Discipline &amp; Four-Cap Enforcement</name>
  <files>lib/rindle/av/subprocess.ex, test/rindle/av/subprocess_test.exs</files>
  <behavior>
    Subprocess Wrapper (`Rindle.AV.Subprocess`):
    - Implements `run/3` wrapping `MuonTrap.cmd`.
    - Detects OS: Applies cgroups conditionally (only on `:os.type() == {:unix, :linux}` when `:use_cgroups` opts are enabled).
    - Enforces Four-Caps automatically: Adds `-timelimit`, `-t`, `-fs` limits to arguments and uses `timeout` option for MuonTrap.
    - Forces `-protocol_whitelist file,crypto,data`.
  </behavior>
  <action>
    Follow Pattern 1 ("MuonTrap OS-Aware Cgroup Execution") and Pattern 2 ("Four-Cap Resource Enforcement") from the RESEARCH document. Ensure all args are lists to avoid shell injection.
  </action>
  <verify>
    <automated>mix test test/rindle/av/subprocess_test.exs</automated>
  </verify>
  <done>Subprocess correctly applies cgroup configurations conditionally based on OS, appends resource limits, and runs successfully.</done>
</task>
</tasks>
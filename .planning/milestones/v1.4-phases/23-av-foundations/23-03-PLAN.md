---
phase: 23-av-foundations
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/av/probe.ex
  - test/rindle/av/probe_test.exs
  - lib/mix/tasks/rindle.doctor.ex
  - test/rindle/doctor_test.exs
autonomous: true
requirements: [AV-PROBE]
must_haves:
  truths:
    - "System halts at boot if FFmpeg version is lower than 6.0"
    - "mix rindle.doctor task provides clear feedback on the FFmpeg environment"
  artifacts:
    - path: "lib/rindle/av/probe.ex"
      provides: "Synchronous boot check logic"
    - path: "lib/mix/tasks/rindle.doctor.ex"
      provides: "User-facing environment check"
  key_links: []
---

<objective>
Implement synchronous environment integrity checks for FFmpeg to ensure requirements are met before executing capabilities.

Purpose: Guarantee that FFmpeg >= 6.0 is present on the host to avoid runtime failures and to utilize modern flags.
Output: A boot probe module and a diagnostic mix task `rindle.doctor`.
</objective>

<context>
@.planning/milestones/v1.4-phases/23-av-foundations/23-PATTERNS.md
@.planning/milestones/v1.4-phases/23-av-foundations/23-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Boot Probe</name>
  <files>lib/rindle/av/probe.ex, test/rindle/av/probe_test.exs</files>
  <behavior>
    - Probe should execute `System.cmd("ffmpeg", ["-version"])`
    - Parses output for version `Regex.run(~r/ffmpeg version (\d+\.\d+)/, output)`
    - Halts or raises if version &lt; 6.0.0
    - Returns `:ok` if &gt;= 6.0.0
  </behavior>
  <action>
    Implement `Rindle.AV.Probe.check_ffmpeg!/0` based on the code example in `23-RESEARCH.md`. Add unit tests that mock the return of `System.cmd`.
  </action>
  <verify>
    <automated>mix test test/rindle/av/probe_test.exs</automated>
  </verify>
  <done>Probe raises exception for older versions and passes for &gt;= 6.0.</done>
</task>

<task type="auto">
  <name>Task 2: Implement rindle.doctor Mix Task</name>
  <files>lib/mix/tasks/rindle.doctor.ex, test/rindle/doctor_test.exs</files>
  <action>
    Implement `Mix.Tasks.Rindle.Doctor`. Follow the error handling and execution pattern from `lib/mix/tasks/rindle.verify_storage.ex` (analog). It should call `Rindle.AV.Probe.check_ffmpeg!` and print user-friendly output using `Mix.shell().info/1`. If `ffmpeg` is missing or outdated, it prints an error and halts with `System.halt(1)`.
  </action>
  <verify>
    <automated>mix rindle.doctor</automated>
  </verify>
  <done>Doctor task evaluates environment and provides correct CLI feedback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Elixir -&gt; Host OS | Boot probe running local binaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04 | Elevation | `Rindle.AV.Probe` | accept | Boot probe strictly uses `["-version"]` without user input, posing no injection risk. Assumes `ffmpeg` in PATH is trusted. |
</threat_model>

<verification>
mix test test/rindle/av/probe_test.exs test/rindle/doctor_test.exs
</verification>

<success_criteria>
`mix rindle.doctor` correctly identifies the FFmpeg version and `Rindle.AV.Probe.check_ffmpeg!/0` accurately enforces &gt;= 6.0 constraint.
</success_criteria>

<output>
After completion, create `.planning/phases/23-av-foundations/23-03-SUMMARY.md`
</output>
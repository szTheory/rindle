---
status: complete
phase: 105-aggregate-required-check-branch-protection-flip
source: [105-VERIFICATION.md]
started: 2026-06-21T19:56:25Z
updated: 2026-06-21T20:19:50Z
---

## Current Test

[testing complete — both items superseded by automation; 0 human steps remain]

## Tests

### 1. Post-merge live branch-protection flip + read-back (Task 4, D-11/D-13)
expected: |
  Live branch protection on main collapses to required `.contexts[]` == ["CI Summary"]
  with an empty diff vs setup_branch_protection.sh --print-expected-json.
result: pass
note: |
  SUPERSEDED BY AUTOMATION — no human flip needed. The deferral was never a
  human-judgment requirement; it was an ordering hazard (D-12 "pending-forever trap":
  GitHub does not validate that a required context name was ever produced, so requiring
  "CI Summary" before a check-run with that exact name has posted on main would hang
  every PR forever).
  That hazard is now removed STRUCTURALLY: scripts/setup_branch_protection.sh refuses to
  apply unless every REQUIRED_CHECKS name already exists as a check-run on the target
  branch's latest commit (and refuses when it cannot verify), with a --force bootstrap
  escape hatch. The apply is therefore safe to run unattended — it self-defers until the
  check exists, then flips. Execution is already automated by the pre-existing
  .github/workflows/branch-protection-apply.yml (cron 17 7 * * * + workflow_dispatch);
  scripts/ci/check_required_checks.sh main verifies the empty diff. No maintainer go/no-go.
  Verified locally (read-only): guard correctly ABORTS on main today (CI Summary not yet
  posted) and mutates nothing — live contexts still 13. It will pass and flip once main is
  pushed and one CI run posts "CI Summary".

### 2. Fork-PR skip-as-pass behavior (optional, D-05)
expected: |
  A fork PR where the repo-gated jobs (cohort-demo-smoke, adoption-demo-e2e,
  brandbook-tokens) skip reports `CI Summary` as success rather than hanging
  pending-forever.
result: pass
note: |
  SUPERSEDED BY AUTOMATION — converted from a manual fork-PR observation into a
  merge-blocking unit test. The gate logic is extracted to scripts/ci/eval_ci_summary.sh
  (the ci-summary step now calls it, byte-identical behavior) and exercised by
  scripts/ci/test_ci_summary_gate.sh, which feeds mock toJSON(needs) payloads:
  roots success + repo-gated jobs skipped → exit 0 (the fork case); any failure/cancelled
  → exit 1; failure-alongside-skip is not masked. Wired into CI as the `ci-script-tests`
  job, transitively gating via ci-summary's needs: (CI Summary stays the single required
  context). Ran locally: 6/6 cases pass.
  Out of scope (documented): GitHub's own `if: github.repository == ...` fork evaluation is
  platform behavior, exercised by the real needs: graph, not unit-testable here.

## Summary

total: 2
passed: 2
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none — both deferred items closed by automation, not code gaps]

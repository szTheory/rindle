---
phase: 107-reliability-security-dx-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/async_safety_guard_test.exs
  - test/rindle/admin/queries_test.exs
  - test/rindle/attach_detach_test.exs
  - test/rindle/delivery/streaming_dispatch_test.exs
  - test/rindle/domain/migration_test.exs
  - test/rindle/ops/variant_maintenance_test.exs
  - test/rindle/owner_erasure_batch_boundary_test.exs
  - test/rindle/owner_erasure_batch_opts_test.exs
  - test/rindle/owner_erasure_batch_proof_test.exs
  - test/rindle/owner_erasure_batch_test.exs
  - test/rindle/owner_erasure_test.exs
  - test/rindle/runtime_status_task_test.exs
  - test/rindle/telemetry/emission_test.exs
  - test/rindle/upload/resumable_telemetry_test.exs
  - test/rindle/workers/ingest_provider_webhook_test.exs
  - test/rindle/workers/purge_storage_test.exs
autonomous: true
requirements: [HARD-01]

must_haves:
  truths:
    - "An async-safety static guard test exists and passes, asserting every async:true module uses no unsafe shared-state primitive (per D-02)"
    - "The 15 RESEARCH-classified CLEAN modules are flipped from async:false to async:true and the full default suite stays green"
    - "A future contributor who adds an unsafe primitive (Application.put_env, set_mox_global, named process, fixed-path File mutation) to an async:true module gets a red gate"
  artifacts:
    - path: "test/async_safety_guard_test.exs"
      provides: "AST meta-test that globs test/**/*_test.exs, walks each async:true module's AST, asserts no unsafe primitive"
      contains: "Macro.prewalk"
      min_lines: 60
  key_links:
    - from: "test/async_safety_guard_test.exs"
      to: "test/**/*_test.exs"
      via: "Path.wildcard glob + Code.string_to_quoted! per file"
      pattern: "Path\\.wildcard"
  prohibitions:
    - "No lib/ file is created or modified (whole-milestone invariant)"
    - "No module on the RESEARCH GENUINELY-UNSAFE (53) list is flipped to async:true"
    - "No --partitions / DB-per-partition / merged-coverage wiring is added (DEFERRED — D-01)"
    - "The async:false sibling module pattern in owner_erasure_batch_opts_test.exs is converted per-module, not via a blind file-wide header flip (Pitfall 1)"
---

<objective>
Land the ExUnit async-safety static guard, then convert the 15 RESEARCH-verified CLEAN
test modules from `async: false` to `async: true` — strictly in that order (D-02).

Purpose: Make async-correctness a self-enforcing gate. The guard proves every module that
*claims* `async: true` uses no concurrency-unsafe shared-state primitive; the conversions
are a free in-runner speedup that ride green through the guard. `--partitions` is DEFERRED
this phase (D-01) — the Quality job runs ~140s avg / 184s p95, far under the ≤7-min PR
budget, so partitioning is not yet justified.
Output: `test/async_safety_guard_test.exs` (NEW) + 15 one-line header flips.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/107-reliability-security-dx-hardening/107-RESEARCH.md
@.planning/phases/107-reliability-security-dx-hardening/107-PATTERNS.md
@test/support/data_case.ex
@test/test_helper.exs
@test/install_smoke/docs_parity_test.exs
</context>

<artifacts_produced>
## Artifacts this plan produces
- NEW file: `test/async_safety_guard_test.exs` — the AST-walking meta-test (`async: true` itself).
- EDIT (15 one-line header flips, `async: false` → `async: true`): `test/rindle/admin/queries_test.exs`,
  `test/rindle/attach_detach_test.exs`, `test/rindle/delivery/streaming_dispatch_test.exs`,
  `test/rindle/domain/migration_test.exs`, `test/rindle/ops/variant_maintenance_test.exs`,
  `test/rindle/owner_erasure_batch_boundary_test.exs`,
  `test/rindle/owner_erasure_batch_opts_test.exs` (the `async: false` module ONLY),
  `test/rindle/owner_erasure_batch_proof_test.exs`, `test/rindle/owner_erasure_batch_test.exs`,
  `test/rindle/owner_erasure_test.exs`, `test/rindle/runtime_status_task_test.exs`,
  `test/rindle/telemetry/emission_test.exs`, `test/rindle/upload/resumable_telemetry_test.exs`,
  `test/rindle/workers/ingest_provider_webhook_test.exs`, `test/rindle/workers/purge_storage_test.exs`.
- A reusable allowlist escape hatch convention (`@async_safety_allow [...]` module attribute) recognised by the guard.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Land the async-safety AST meta-test guard (D-02 — FIRST)</name>
  <files>test/async_safety_guard_test.exs</files>
  <read_first>
    - test/install_smoke/docs_parity_test.exs (analog: meta-test that globs+reads source files, builds a readable failure list, asserts over it; header `use ExUnit.Case, async: true`)
    - 107-RESEARCH.md §"HARD-01 — Guard mechanism recommendation" (the AST node shapes to match in Macro.prewalk/2; the allowlist escape hatch; fail-closed default)
    - 107-PATTERNS.md §"test/async_safety_guard_test.exs" (the exact node-shape match clauses + file-set/load pattern)
    - test/support/data_case.ex (why DataCase modules are async-safe by construction — `shared: not tags[:async]`; the guard need NOT flag Ecto sandbox)
    - test/test_helper.exs (sandbox `:manual` for both repos; inline-Oban + private-Mox are async-safe)
  </read_first>
  <behavior>
    - Test: the guard test module is itself `use ExUnit.Case, async: true` and passes its own check (read-only over the filesystem, no unsafe primitive).
    - Test: globbing `test/**/*_test.exs`, reading each file, and parsing via `Code.string_to_quoted!/1` yields zero offenders against the CURRENT tree (the 60 already-async modules are clean) — the guard is GREEN before any conversion.
    - Test: for each module whose header is `async: true`, an AST walk (`Macro.prewalk/2`) collects calls matching the unsafe-primitive set; the assertion is `offenders == []` with a readable `file:line + primitive` failure message.
    - Edge: a module carrying `@async_safety_allow [...]` (or a `# async-safety: justified — <reason>` comment) for a named primitive is excluded from that primitive's check (fail-closed default otherwise).
    - Edge: a `File.write`/`mkdir` whose path derives from `tmp_dir`/`System.unique_integer` is NOT flagged; only fixed-shared-path File mutation is unsafe.
  </behavior>
  <action>
    Create `test/async_safety_guard_test.exs` as `use ExUnit.Case, async: true`, mirroring
    the meta-test shape of `docs_parity_test.exs` (module-attr/`setup_all` to load the file
    set; build a readable list-of-failures rather than a bare boolean). Per D-02 this guard
    MUST land and pass FIRST — before Task 2's conversions.

    Mechanism (Claude's discretion per CONTEXT, RESEARCH recommends AST meta-test): glob
    `Path.wildcard("test/**/*_test.exs")`, `File.read!/1` each, `Code.string_to_quoted!/1`,
    and `Macro.prewalk/2` each module body. For every module whose `use ExUnit.Case` /
    `use Rindle.DataCase` header carries `async: true`, collect AST nodes matching the
    unsafe-primitive node shapes enumerated in 107-PATTERNS.md §"Detection heuristic" and
    107-RESEARCH.md HARD-01: `Application.put_env`/`delete_env`, `System.put_env`/`delete_env`,
    `set_mox_global`, any `start_link`/`start_supervised`/`GenServer.start` with a `:name`
    kwarg, `File` mutation (`cd/write/mkdir/rm/cp/rename/touch` and bang variants) whose path
    arg is NOT derived from a `tmp_dir`/`unique_integer` expression, `:ets.new` with
    `:named_table`/`:public`, `:persistent_term.put`, and `Sandbox.mode(_, {:shared, _})`.

    Do NOT flag the Ecto sandbox itself — DataCase keys isolation on `tags[:async]`
    (`shared: not tags[:async]`), so a DataCase async:true module is safe by construction.
    Support an allowlist escape hatch: a `@async_safety_allow [<primitive-atoms>]` module
    attribute (and/or a `# async-safety: justified — <reason>` line) opts a module out of a
    named primitive's check; default is fail-closed. Assert the offender list is empty with a
    message listing each `file:line` + primitive. The guard must be GREEN against the current
    tree (the 60 pre-existing async modules) so it can land before any conversion.

    Do NOT inline a literal that the brandbook negative-grep gates depend on; this file
    touches no brandbook strings. Touch NO `lib/` file.
  </action>
  <verify>
    <automated>mix test test/async_safety_guard_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/async_safety_guard_test.exs` passes against the current (pre-conversion) tree.
    - The test header is `async: true`; the test globs `test/**/*_test.exs` and walks each async:true module's AST.
    - A deliberately-introduced `Application.put_env(...)` in any async:true module makes the guard fail with a `file:line` + primitive message (spot-confirm by reasoning over the matcher; do not commit the deliberate break).
  </acceptance_criteria>
  <done>Guard test exists, is `async: true`, passes green on the current tree, and would red-flag an unsafe primitive added to any async:true module.</done>
</task>

<task type="auto">
  <name>Task 2: Convert the 15 CLEAN modules to async:true (AFTER guard is green)</name>
  <files>test/rindle/admin/queries_test.exs, test/rindle/attach_detach_test.exs, test/rindle/delivery/streaming_dispatch_test.exs, test/rindle/domain/migration_test.exs, test/rindle/ops/variant_maintenance_test.exs, test/rindle/owner_erasure_batch_boundary_test.exs, test/rindle/owner_erasure_batch_opts_test.exs, test/rindle/owner_erasure_batch_proof_test.exs, test/rindle/owner_erasure_batch_test.exs, test/rindle/owner_erasure_test.exs, test/rindle/runtime_status_task_test.exs, test/rindle/telemetry/emission_test.exs, test/rindle/upload/resumable_telemetry_test.exs, test/rindle/workers/ingest_provider_webhook_test.exs, test/rindle/workers/purge_storage_test.exs</files>
  <read_first>
    - 107-RESEARCH.md §"Per-file classification" (the CLEAN-15 table with per-file basis; the GENUINELY-UNSAFE 53 are OFF-LIMITS)
    - 107-RESEARCH.md §"Common Pitfalls" Pitfall 1 (owner_erasure_batch_opts_test.exs double-defines modules — convert only the async:false one)
  </read_first>
  <action>
    For each of the 15 CLEAN files from RESEARCH's "Per-file classification" table, flip the
    module header `async: false` → `async: true` (one-line edit per module). This is ONLY the
    15-file CLEAN list — do NOT touch any of the 53 GENUINELY-UNSAFE modules.

    Special case (Pitfall 1): `test/rindle/owner_erasure_batch_opts_test.exs` double-defines
    two modules (one already `async: true`, one `async: false`). Convert ONLY the `async: false`
    module's header; leave its already-async sibling untouched. Do not do a blind file-wide
    flip.

    After flipping, run the async-safety guard (it must stay green — these 15 contain no
    unsafe primitive) and the full default suite (re-run to catch any hidden ordering
    dependency; the suite is ~140s, cheap to re-run). Do NOT wire `--partitions` (DEFERRED,
    D-01). Touch NO `lib/` file.
  </action>
  <verify>
    <automated>mix test test/async_safety_guard_test.exs && mix compile --warnings-as-errors && mix test</automated>
  </verify>
  <acceptance_criteria>
    - All 15 listed files now declare `async: true` on the targeted module; the `owner_erasure_batch_opts_test.exs` sibling pairing is preserved.
    - `mix test test/async_safety_guard_test.exs` stays green (the 15 are clean).
    - `mix compile --warnings-as-errors && mix test` (full default suite) is green with no ordering regression.
    - `grep -rn 'async: false' test/rindle/owner_erasure_test.exs` returns nothing (representative spot-check the flip landed).
  </acceptance_criteria>
  <done>The 15 CLEAN modules run `async: true`, the guard stays green, and the full default suite passes with no flake/ordering regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent-test-process → shared global state | An `async: true` module sharing global mutable state (app env, named procs, global Mox, fixed FS paths) races other tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-01 | Tampering | future `async: true` conversions adding an unsafe primitive | mitigate | The AST meta-test guard (Task 1) fails the merge-blocking `quality` suite if any async:true module uses an unsafe shared-state primitive (D-02 inverse-guard) |

> No new attack surface: this is a test-tooling track with ZERO `lib/` change. The single
> "threat" is a contributor silently reintroducing an async race; the guard closes it.
</threat_model>

<verification>
- `mix test test/async_safety_guard_test.exs` passes (guard green on current + converted tree).
- `mix compile --warnings-as-errors && mix test` passes (full default ~140s suite, no regression).
- `grep -c 'async: true' test/async_safety_guard_test.exs` ≥ 1 (the guard is itself async).
- No `lib/` file changed: `git diff --name-only | grep -c '^lib/'` is 0.
</verification>

<success_criteria>
- The async-safety guard lands and passes BEFORE any conversion (D-02 ordering honored).
- Exactly the 15 CLEAN modules are converted to `async: true`; zero GENUINELY-UNSAFE modules touched.
- No `--partitions` wiring added (DEFERRED — D-01).
- Full default suite green; guard would red-flag a future unsafe-primitive regression.
</success_criteria>

<output>
Create `.planning/phases/107-reliability-security-dx-hardening/107-01-SUMMARY.md` when done.
</output>

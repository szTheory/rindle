---
phase: 107-reliability-security-dx-hardening
plan: 03
type: execute
wave: 2
depends_on: ["107-02"]
files_modified:
  - mix.exs
  - CONTRIBUTING.md
  - README.md
autonomous: true
requirements: [HARD-03]

must_haves:
  truths:
    - "A single `ci` alias in mix.exs `aliases/0` runs the same merge-blocking checks the PR gate runs (compile --warnings-as-errors, format --check-formatted, lockfile drift gates, brandbook drift gates, the gating test suite) (D-07)"
    - "`mix ci` runs the default-tag suite (skips :minio/:integration legs by default) and exits 0 on a clean tree; the full-parity MinIO command is documented separately (D-07/A4)"
    - "CONTRIBUTING.md's reserved `Phase 107, HARD-03` section is filled with the lanes, the SOLE required check (`CI Summary`, skipped==pass), the local `mix ci` command, and prerequisites (D-08)"
    - "README badge keeps the existing `ci.yml/badge.svg?branch=main` workflow-run badge and docs state it reflects the `CI Summary` gate; no custom per-check badge endpoint (D-09)"
  artifacts:
    - path: "mix.exs"
      provides: "the `ci` alias mirroring the PR merge-blocking set"
      contains: "ci:"
    - path: "CONTRIBUTING.md"
      provides: "filled local-command section: lanes + required check + mix ci + prerequisites"
      contains: "mix ci"
  key_links:
    - from: "mix.exs `ci` alias"
      to: "the PR merge-blocking lane set"
      via: "the alias task list mirrors what the PR gate runs (source of truth for 'what a PR must pass')"
      pattern: "ci:"
    - from: "CONTRIBUTING.md"
      to: "mix ci + CI Summary"
      via: "documents the single local command + the sole required check"
      pattern: "CI Summary"
  prohibitions:
    - "No `lib/` file is created or modified (whole-milestone invariant)"
    - "`mix ci` does NOT hard-fail on a fresh clone lacking MinIO — the base alias runs the default-tag suite only (Pitfall 5); the full-parity command is documented, not embedded"
    - "No custom per-check badge endpoint is built; the existing workflow-run badge is kept (D-09)"
    - "No new CI lane / topology change — the lane set is settled as of Phase 106; this plan only DOCUMENTS and mirrors it"
    - "`mix ci` mirrors only the MERGE-BLOCKING set — it does NOT include push:main/nightly-only lanes (package-consumer-full, adoption-demo-e2e, mux-soak, broad matrix, Dialyzer)"
---

<objective>
Ship the DX layer that describes the now-settled fast-PR check set: a single `mix ci` alias
that reproduces the PR verdict locally, a filled CONTRIBUTING local-command section, and a
README badge clarified to reflect the meaningful `CI Summary` gate.

Purpose: Give contributors one command that mirrors what a PR must pass, and truthful docs
describing the lane split + sole required check settled in Phase 106. Docs/tooling only —
ZERO `lib/` change.
Output: `mix.exs` `ci:` alias + filled `CONTRIBUTING.md` reserved section + `README.md` badge clarification.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/107-reliability-security-dx-hardening/107-RESEARCH.md
@.planning/phases/107-reliability-security-dx-hardening/107-PATTERNS.md
@mix.exs
@CONTRIBUTING.md
@README.md
@.planning/phases/106-trigger-split-matrix-lane-refinement/106-LANE-CLASSIFICATION.md
</context>

<artifacts_produced>
## Artifacts this plan produces
- EDIT: `mix.exs` `aliases/0` — adds the `ci:` alias (the task list mirroring the PR merge-blocking set).
- EDIT: `CONTRIBUTING.md` — fills the reserved `Phase 107, HARD-03` local-command section (lanes + `CI Summary` sole required check + `mix ci` + MinIO/Playwright prerequisites + documented full-parity command).
- EDIT: `README.md` — keeps the `ci.yml/badge.svg?branch=main` badge; adds a docs line clarifying it reflects the `CI Summary` gate.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add the `mix ci` alias mirroring the PR merge-blocking set (D-07)</name>
  <files>mix.exs</files>
  <read_first>
    - 107-RESEARCH.md §"`mix ci` alias recommendation (D-07)" (the exact ordered task list; MinIO skip-with-note; whether to include the browser gallery proof) + §"The merge-blocking PR lane set"
    - 107-PATTERNS.md §"`mix.exs` aliases/0 — add `ci:`" (the current alias list at mix.exs:284; `precommit: ["test"]` is the closest analog)
    - 107-RESEARCH.md §"Common Pitfalls" Pitfall 5 (do NOT include :minio/:integration legs — the base alias must run on a fresh clone)
  </read_first>
  <action>
    Add a `ci:` entry to `mix.exs` `aliases/0` (none exists today; `precommit: ["test"]` is the
    closest). Mirror the PR merge-blocking set, ordered fast-static → compile → format → drift
    gates → tests last, per RESEARCH's recommended list:
    `deps.get --check-locked`, `deps.unlock --check-unused`, `compile --warnings-as-errors`,
    `format --check-formatted`, the brandbook drift gates (`cmd node brandbook/src/tokens-build.mjs`,
    `cmd node brandbook/src/admin-css-build.mjs`, `cmd node brandbook/src/admin-contrast.mjs`,
    `cmd node brandbook/src/sync-admin-css.mjs`), and the gating test suite (`coveralls` or `test`)
    last.

    MinIO-leg handling (D-07 / A4, skip-with-note): the base `ci` alias runs ONLY the
    default-tag suite — which `test_helper.exs` already excludes `:minio`/`:integration`/
    `:contract`/`:adopter` from — so `mix ci` runs on a fresh clone without MinIO and does NOT
    hard-fail (Pitfall 5). Do NOT embed the MinIO legs in the alias; the full-parity command is
    documented in CONTRIBUTING (Task 2). The browser gallery proof (`admin-gallery-check.mjs`)
    is Playwright-dependent — OMIT it from the base alias to keep `mix ci` Elixir-toolchain-only
    (it is covered by `brandbook-tokens` in CI + `scripts/ci/e2e_local.sh` locally per Plan 04);
    note this omission. Mirror ONLY the merge-blocking set — exclude push:main/nightly-only
    lanes. Touch NO `lib/` file.
  </action>
  <verify>
    <automated>mix ci</automated>
  </verify>
  <acceptance_criteria>
    - `mix.exs` `aliases/0` contains a `ci:` entry with the ordered task list above.
    - `mix ci` exits 0 on a clean tree WITHOUT a running MinIO (default-tag suite only).
    - The alias does NOT reference `--include minio` / `--include integration` (Pitfall 5).
    - `grep -c 'ci:' mix.exs` ≥ 1.
  </acceptance_criteria>
  <done>`mix ci` reproduces the PR merge-blocking verdict locally, runs on a fresh clone without MinIO, and excludes push:main/nightly-only lanes.</done>
</task>

<task type="auto">
  <name>Task 2: Fill the CONTRIBUTING reserved section + clarify the README badge (D-08, D-09)</name>
  <files>CONTRIBUTING.md, README.md</files>
  <read_first>
    - 107-RESEARCH.md §"CONTRIBUTING.md (D-08)" (fill the reserved Phase-107/HARD-03 placeholder: lanes, sole required check `CI Summary` skipped==pass, `mix ci` + MinIO/Playwright prerequisites + full-parity command) + §"README badge (D-09)"
    - 107-PATTERNS.md §"CONTRIBUTING.md / README.md" + §"Brandbook gate output stability"
    - CONTRIBUTING.md:9-12 (the literal reserved placeholder text to replace) + README.md:10 (the existing badge line)
    - .planning/phases/106-trigger-split-matrix-lane-refinement/106-LANE-CLASSIFICATION.md (already linked from CONTRIBUTING — keep coherent)
  </read_first>
  <action>
    Replace the reserved placeholder in `CONTRIBUTING.md` (~lines 9-12, the
    "being added in a follow-up (Phase 107, HARD-03)" text) with the local-command section:
    (1) the PR lane split (keep coherent with the prose already above it and `RUNNING.md`
    §"CI lane severity" + the linked `106-LANE-CLASSIFICATION.md`); (2) the SOLE required check
    is `CI Summary` (with `skipped` == pass for fork-PR safety); (3) the local `mix ci` command,
    its prerequisites (Postgres required; Node for the brandbook drift gates), and the documented
    FULL-parity command for the storage legs:
    `RINDLE_MINIO_URL=… mix test --include minio --include integration` with a one-line note that
    it requires a local MinIO. Also document the `scripts/ci/e2e_local.sh` path (Plan 04) for the
    faithful browser repro.

    In `README.md`, KEEP the existing `…/actions/workflows/ci.yml/badge.svg?branch=main`
    workflow-run badge (line 10) and add a docs line stating it represents the `CI Summary`
    gate (GitHub has no native per-check badge). Do NOT build a custom badge endpoint (D-09).
    Touch NO `lib/` file.
  </action>
  <verify>
    <automated>grep -q 'mix ci' CONTRIBUTING.md && grep -q 'CI Summary' CONTRIBUTING.md && grep -q 'ci.yml/badge.svg' README.md</automated>
  </verify>
  <acceptance_criteria>
    - CONTRIBUTING.md no longer contains the "being added in a follow-up (Phase 107, HARD-03)" placeholder; it documents the lanes, `CI Summary` (skipped==pass) as the sole required check, the `mix ci` command, prerequisites, and the full-parity MinIO command.
    - README.md retains the `ci.yml/badge.svg?branch=main` badge and adds a line stating it reflects the `CI Summary` gate.
    - No custom badge endpoint / shields.io custom URL is introduced.
  </acceptance_criteria>
  <done>CONTRIBUTING's reserved section is filled with lanes + sole required check + local command + prerequisites; the README badge is kept and clarified to reflect `CI Summary`.</done>
</task>

</tasks>

<threat_model>
N/A — no new attack surface. HARD-03 is a docs + build-alias track with ZERO `lib/` change,
no new CI lane, and no new network/secret surface. The `mix ci` alias only re-runs existing
merge-blocking commands locally.
</threat_model>

<verification>
- `mix ci` exits 0 on a clean tree without MinIO (default-tag suite only).
- CONTRIBUTING.md contains `mix ci` + `CI Summary` and no longer carries the HARD-03 placeholder.
- README.md retains `ci.yml/badge.svg` and states it reflects `CI Summary`.
- No `lib/` change (`git diff --name-only | grep -c '^lib/'` is 0); no new CI lane added.
</verification>

<success_criteria>
- `mix ci` mirrors the PR merge-blocking set, runs on a fresh clone, excludes push:main/nightly lanes.
- CONTRIBUTING reserved section filled (lanes + sole required check + command + prerequisites + full-parity command).
- README badge kept and clarified; no custom per-check badge.
</success_criteria>

<output>
Create `.planning/phases/107-reliability-security-dx-hardening/107-03-SUMMARY.md` when done.
</output>

---
id: 41-02
plan_number: 02
plan: 02
phase: 41
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/ops/runtime_checks.ex
  - lib/mix/tasks/rindle.doctor.ex
  - test/rindle/ops/runtime_checks_test.exs
  - test/rindle/ops/runtime_checks_streaming_test.exs
  - test/rindle/doctor_test.exs
autonomous: true
requirements: [RESUMABLE-13]
must_haves:
  truths:
    - "`mix rindle.doctor` stays a single command surface."
    - "Non-GCS adopters see zero new GCS noise."
    - "The new resumable CORS-suspected finding is advisory, renders distinctly, and does not fail the command."
    - "Errors still fail the doctor command exactly as before."
  artifacts:
    - path: "lib/rindle/ops/runtime_checks.ex"
      provides: "warning-capable resumable-aware GCS doctor check"
      contains: "doctor.gcs_resumable_cors"
    - path: "lib/mix/tasks/rindle.doctor.ex"
      provides: "distinct warning rendering with existing exit semantics for errors"
      contains: "[WARN]"
  key_links:
    - from: "lib/rindle/ops/runtime_checks.ex"
      to: "lib/rindle/capability.ex"
      via: "profile-aware GCS and resumable capability discovery"
      pattern: "configured_gcs_profiles"
---

<objective>
Ship the doctor/runtime-check slice of Phase 41.

Purpose: close RESUMABLE-13 by adding a resumable-aware GCS onboarding check
to `mix rindle.doctor` without creating a second task and without introducing
noise or failing on advisory CORS findings.
Output: updated runtime-check report model, CLI rendering, and focused tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-onboarding-docs-doctor-package-consumer-proof/41-CONTEXT.md
@.planning/phases/41-onboarding-docs-doctor-package-consumer-proof/41-RESEARCH.md
@.planning/phases/41-onboarding-docs-doctor-package-consumer-proof/41-PATTERNS.md
@lib/rindle/ops/runtime_checks.ex
@lib/mix/tasks/rindle.doctor.ex
@lib/rindle/capability.ex
@test/rindle/ops/runtime_checks_test.exs
@test/rindle/ops/runtime_checks_streaming_test.exs
@test/rindle/doctor_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add warning-capable resumable GCS checks to `RuntimeChecks`</name>
  <files>
    lib/rindle/ops/runtime_checks.ex
    test/rindle/ops/runtime_checks_test.exs
    test/rindle/ops/runtime_checks_streaming_test.exs
  </files>
  <read_first>
    - lib/rindle/ops/runtime_checks.ex
    - lib/rindle/capability.ex
    - test/rindle/ops/runtime_checks_test.exs
    - test/rindle/ops/runtime_checks_streaming_test.exs
    - .planning/phases/41-onboarding-docs-doctor-package-consumer-proof/41-CONTEXT.md
  </read_first>
  <action>
Extend `RuntimeChecks` so report items can express an advisory warning status
in addition to `:ok` and `:error`.

Implement the new resumable-specific GCS onboarding check in the existing
pipeline, keeping all current GCS checks intact:
- check id: `doctor.gcs_resumable_cors`
- emit it only when at least one discovered profile is GCS-backed AND
  advertises `:resumable_upload_session`
- inspect bucket CORS metadata or equivalent bucket-config shape rather than
  generic response headers
- produce a warning, not an error, when the bucket appears missing the required
  browser-facing config
- warning summary/fix text must explicitly mention app origins, `PUT`,
  `PATCH`, `Content-Range`, `x-goog-resumable`, `session_uri` secrecy, one-week
  expiry, and region pinning

Keep existing behavior unchanged for:
- non-GCS adopters: no new rows
- GCS but non-resumable adopters: only the existing GCS foundation rows
- existing `doctor.gcs_goth_running`, `doctor.gcs_bucket_reachable`,
  `doctor.gcs_signing_key`: still deterministic errors on failure

Add or update tests so they prove:
- the new row is absent when no resumable GCS profile exists
- the new row is present and `:warn` when the bucket lacks required CORS shape
- warnings do not increment the failure count
- errors still do
  </action>
  <acceptance_criteria>
    - `lib/rindle/ops/runtime_checks.ex` contains `doctor.gcs_resumable_cors`
    - `lib/rindle/ops/runtime_checks.ex` contains a warning result helper or equivalent `status: :warn`
    - `lib/rindle/ops/runtime_checks.ex` contains `x-goog-resumable`
    - `lib/rindle/ops/runtime_checks.ex` contains `Content-Range`
    - `test/rindle/ops/runtime_checks_test.exs` contains `doctor.gcs_resumable_cors`
    - at least one test asserts warning semantics explicitly
    - `mix test test/rindle/ops/runtime_checks_test.exs test/rindle/ops/runtime_checks_streaming_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rindle/ops/runtime_checks_test.exs test/rindle/ops/runtime_checks_streaming_test.exs</automated>
  </verify>
  <done>
    `RuntimeChecks` can represent non-failing warnings and emits the new
    resumable GCS onboarding finding only for relevant profiles.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render warnings distinctly in `mix rindle.doctor` without changing error exit policy</name>
  <files>
    lib/mix/tasks/rindle.doctor.ex
    test/rindle/doctor_test.exs
  </files>
  <read_first>
    - lib/mix/tasks/rindle.doctor.ex
    - lib/rindle/ops/runtime_checks.ex
    - test/rindle/doctor_test.exs
    - .planning/phases/41-onboarding-docs-doctor-package-consumer-proof/41-CONTEXT.md
  </read_first>
  <action>
Update `Mix.Tasks.Rindle.Doctor` so advisory warnings render clearly but do not
change the existing failure contract for errors.

Implement concrete output semantics:
- warning lines render as `[WARN] ...`
- error lines remain `[ERROR] ...`
- ok lines remain `[OK] ...`
- final success/failure decision still depends only on real errors
- the task remains a thin shell over `RuntimeChecks.run/2`

Extend `test/rindle/doctor_test.exs` with assertions that:
- a warning row is rendered as `[WARN]`
- warning-only reports do not raise when `exit_on_failure?` is true
- reports with any `:error` still raise as before
  </action>
  <acceptance_criteria>
    - `lib/mix/tasks/rindle.doctor.ex` contains `[WARN]` output behavior
    - `test/rindle/doctor_test.exs` contains a warning-rendering assertion
    - `test/rindle/doctor_test.exs` contains a warning-only non-raise assertion
    - `mix test test/rindle/doctor_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/rindle/doctor_test.exs</automated>
  </verify>
  <done>
    `mix rindle.doctor` stays a single command, warnings are visible but
    non-failing, and error exit behavior is preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime checks -> operator output | The command must distinguish advisory warnings from failing errors without confusing CI or humans. |
| doctor gating -> profile discovery | Non-relevant adopters must not receive noisy or misleading GCS rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04 | D | doctor exit semantics | mitigate | Keep warnings non-failing and preserve existing error-only failure policy. |
| T-41-05 | I | operator guidance | mitigate | Warning fix text explicitly names the required CORS methods/headers and secrecy caveats. |
| T-41-06 | R | profile discovery | mitigate | Reuse existing GCS gating plus resumable capability checks so unrelated adopters see no new rows. |
</threat_model>

<verification>
Run `mix test test/rindle/ops/runtime_checks_test.exs test/rindle/ops/runtime_checks_streaming_test.exs test/rindle/doctor_test.exs`.
</verification>

<success_criteria>
`mix rindle.doctor` stays single-surface and profile-aware, emits the new
resumable GCS onboarding finding as a warning, and preserves existing error
failure semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/41-onboarding-docs-doctor-package-consumer-proof/41-02-SUMMARY.md`.
</output>

---
id: 41-03
plan_number: 03
plan: 03
phase: 41
type: execute
wave: 2
depends_on: [41-01, 41-02]
files_modified:
  - scripts/install_smoke.sh
  - test/install_smoke/support/generated_app_helper.ex
  - test/install_smoke/generated_app_smoke_test.exs
  - .github/workflows/ci.yml
  - test/install_smoke/docs_parity_test.exs
  - test/install_smoke/release_docs_parity_test.exs
autonomous: true
requirements: [RESUMABLE-14]
must_haves:
  truths:
    - "The generated-app harness gains a real `:gcs` / `gcs-enabled` profile instead of overloading existing profiles."
    - "Always-on structural package-consumer proof and secret-gated live-bucket proof stay separate."
    - "The live lane uses unique per-run object prefixes, avoids logging raw `session_uri`, and cleans up even on failure."
  artifacts:
    - path: "scripts/install_smoke.sh"
      provides: "gcs profile dispatch"
      contains: "gcs"
    - path: "test/install_smoke/support/generated_app_helper.ex"
      provides: "generated-app GCS profile proof harness"
      contains: ":gcs"
    - path: ".github/workflows/ci.yml"
      provides: "package-consumer structural GCS lane plus secret-gated live proof job"
      contains: "GOOGLE_APPLICATION_CREDENTIALS_JSON"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/install_smoke.sh"
      via: "package-consumer and live GCS proof job both dispatch through the same smoke script"
      pattern: "install_smoke.sh gcs"
---

<objective>
Ship the generated-app package-consumer proof slice of Phase 41.

Purpose: close RESUMABLE-14 by adding a GCS profile to the generated-app
harness and wiring both structural and live CI proof without making routine PRs
depend on cloud secrets.
Output: `:gcs` smoke profile, generated-app assertions, and CI lane updates.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/41-onboarding-docs-doctor-package-consumer-proof/41-CONTEXT.md
@.planning/phases/41-onboarding-docs-doctor-package-consumer-proof/41-RESEARCH.md
@.planning/phases/41-onboarding-docs-doctor-package-consumer-proof/41-PATTERNS.md
@scripts/install_smoke.sh
@test/install_smoke/support/generated_app_helper.ex
@test/install_smoke/generated_app_smoke_test.exs
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the generated-app install-smoke harness with a `:gcs` profile</name>
  <files>
    scripts/install_smoke.sh
    test/install_smoke/support/generated_app_helper.ex
    test/install_smoke/generated_app_smoke_test.exs
  </files>
  <read_first>
    - scripts/install_smoke.sh
    - test/install_smoke/support/generated_app_helper.ex
    - test/install_smoke/generated_app_smoke_test.exs
    - test/rindle/upload/broker_test.exs
    - .planning/phases/41-onboarding-docs-doctor-package-consumer-proof/41-CONTEXT.md
  </read_first>
  <action>
Extend the existing install-smoke harness with a fourth profile mode for GCS.

Concrete changes:
- update `scripts/install_smoke.sh` case dispatch to accept `gcs`
- extend `profile_enabled?/1` and `prove_package_install!/1` to accept `:gcs`
- patch the generated Phoenix app so the GCS profile wires the adopted
  `Rindle.Storage.GCS` path, runs `mix rindle.doctor`, and exercises the
  canonical resumable lifecycle in this exact order: initiate resumable
  session, chunked upload through the returned session URI, explicit
  status-convergence assertion via `resumable_session_status/2` or an
  equivalent public adopter-visible status path, verify completion, asset
  promoted
- ensure the report map includes any GCS-specific proof fields needed by the
  smoke test assertions
- keep install-source assertions and package-vs-network behavior identical to
  existing profiles

Add or update generated-app smoke tests so there is a GCS-specific ExUnit
module mirroring the existing profile modules. Structural assertions should be
always-on; the live bucket path may be separately gated inside the harness or
workflow, but the profile itself must exist and be test-addressable.
  </action>
  <acceptance_criteria>
    - `scripts/install_smoke.sh` accepts `gcs` in the profile case block
    - `test/install_smoke/support/generated_app_helper.ex` contains `:gcs`
    - `test/install_smoke/generated_app_smoke_test.exs` contains a GCS-specific smoke module or equivalent `:gcs` assertions
    - generated-app helper still supports `:image`, `:video`, and `:mux`
    - at least one smoke assertion references `mix rindle.doctor` for the GCS path
    - at least one smoke assertion references `resumable_session_status` or an equivalent explicit status-convergence proof before verify-completion / asset-promotion assertions
  </acceptance_criteria>
  <verify>
    <automated>RINDLE_INSTALL_SMOKE_PROFILE=gcs mix test test/install_smoke/generated_app_smoke_test.exs</automated>
  </verify>
  <done>
    The generated-app harness exposes a first-class GCS profile and the smoke
    tests can prove the new adopter path without replacing existing profiles.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire structural and live GCS proof lanes into CI safely</name>
  <files>
    .github/workflows/ci.yml
    test/install_smoke/docs_parity_test.exs
    test/install_smoke/release_docs_parity_test.exs
  </files>
  <read_first>
    - .github/workflows/ci.yml
    - scripts/install_smoke.sh
    - test/install_smoke/docs_parity_test.exs
    - test/install_smoke/release_docs_parity_test.exs
    - .planning/phases/41-onboarding-docs-doctor-package-consumer-proof/41-CONTEXT.md
  </read_first>
  <action>
Update `.github/workflows/ci.yml` to add the GCS generated-app proof in two
layers:

1. always-on structural proof in `package-consumer`
2. secret-gated sibling top-level live GCS proof job

Concrete CI requirements:
- the always-on `package-consumer` job dispatches the new `gcs` smoke profile
  only for structural/non-secret proof
- the live GCS generated-app job is gated on
  `GOOGLE_APPLICATION_CREDENTIALS_JSON` (and any required bucket secret)
- the live job uses a unique per-run object prefix
- the live job avoids printing raw `session_uri` values
- the live job performs cleanup with `if: always()` or equivalent
- the workflow remains fork-safe when secrets are absent
- the workflow contract is testable from file contents with exact gate and
  cleanup strings, while the structural harness is exercised by the smoke test
  command above

Update docs parity tests only if needed to keep CI's release-doc / smoke
expectations aligned with the new optional GCS path.
  </action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains `install_smoke.sh gcs`
    - `.github/workflows/ci.yml` contains `GOOGLE_APPLICATION_CREDENTIALS_JSON`
    - `.github/workflows/ci.yml` contains a secret-gated condition for the live GCS lane
    - `.github/workflows/ci.yml` contains `if: always()` cleanup in the live GCS lane or an equivalent unconditional cleanup step
    - `.github/workflows/ci.yml` contains a unique per-run object-prefix env var or command wiring for the live GCS lane
    - `.github/workflows/ci.yml` contains a log-hygiene note or step text that avoids raw `session_uri` output
    - the workflow still contains the existing non-GCS package-consumer and soak lanes
    - docs parity tests still pass if they are modified
  </acceptance_criteria>
  <verify>
    <automated>RINDLE_INSTALL_SMOKE_PROFILE=gcs mix test test/install_smoke/generated_app_smoke_test.exs && rg -n "install_smoke\\.sh gcs|GOOGLE_APPLICATION_CREDENTIALS_JSON|if: \\$\\{\\{ secrets\\.GOOGLE_APPLICATION_CREDENTIALS_JSON != '' \\}\\}|if: always\\(\\)|prefix|session_uri" .github/workflows/ci.yml</automated>
  </verify>
  <done>
    CI proves the generated-app GCS adopter path structurally on routine PRs
    and against a real bucket only when secrets are present.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated-app proof -> cloud bucket | The live lane exercises real resumable uploads and must not leak credentials or session URIs. |
| workflow gating -> fork PRs | Secret-gated proof must skip safely when secrets are unavailable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-07 | I | CI logs | mitigate | Do not print raw `session_uri`; use unique prefixes and explicit cleanup. |
| T-41-08 | E | workflow secrets | mitigate | Gate the live lane on secrets and keep the `pull_request` safe posture. |
| T-41-09 | D | contributor CI | mitigate | Keep structural proof always-on and live proof separate so routine PRs do not depend on cloud credentials. |
</threat_model>

<verification>
Verify the file-level harness and workflow wiring with grep checks, then use the
live CI lane itself as the end-to-end proof when secrets are present.
</verification>

<success_criteria>
The generated-app harness has a first-class GCS profile, package-consumer gets
structural GCS proof, and a secret-gated sibling live lane proves the real
bucket adopter path safely.
</success_criteria>

<output>
After completion, create `.planning/phases/41-onboarding-docs-doctor-package-consumer-proof/41-03-SUMMARY.md`.
</output>

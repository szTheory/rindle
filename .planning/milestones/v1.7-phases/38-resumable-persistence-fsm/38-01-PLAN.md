---
id: 38-01
plan_number: 01
plan: 01
phase: 38
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/20260507160000_extend_media_upload_sessions_for_resumable.exs
  - lib/rindle/ops/runtime_checks.ex
  - test/rindle/domain/migration_test.exs
  - test/rindle/ops/runtime_checks_test.exs
autonomous: true
requirements: [RESUMABLE-01]
must_haves:
  truths:
    - "Adopters have a packaged migration template that extends media_upload_sessions for resumable storage without Rindle running the migration for them."
    - "The upload-session table can persist session_uri, session_uri_expires_at, last_known_offset, and region_hint while widening upload_strategy to resumable."
    - "mix rindle.doctor can detect when the adopter-owned table is missing the resumable columns or expiry partial index."
  artifacts:
    - path: "priv/repo/migrations/20260507160000_extend_media_upload_sessions_for_resumable.exs"
      provides: "Packaged additive migration template for resumable session persistence"
      contains: "add :session_uri, :text"
    - path: "lib/rindle/ops/runtime_checks.ex"
      provides: "Schema-only doctor check for resumable upload-session drift"
      contains: "doctor.resumable_session_schema"
    - path: "test/rindle/domain/migration_test.exs"
      provides: "Catalog-introspection assertions for resumable columns, defaults, and partial index"
      contains: "media_upload_sessions"
  key_links:
    - from: "priv/repo/migrations/20260507160000_extend_media_upload_sessions_for_resumable.exs"
      to: "media_upload_sessions"
      via: "alter table plus partial index creation"
      pattern: "upload_strategy = 'resumable'"
    - from: "lib/rindle/ops/runtime_checks.ex"
      to: "Rindle.Repo.query/2"
      via: "schema-only catalog introspection for columns/defaults/index shape"
      pattern: "doctor\\.resumable_session_schema"
---

<objective>
Ship the packaged resumable persistence migration and the narrow schema-drift doctor check locked for Phase 38.

Purpose: Implement D-01 through D-05 and D-24 through D-27 without pulling in any Phase 39 resumable runtime behavior.
Output: One additive migration template under `priv/repo/migrations`, one new `doctor.resumable_session_schema` check, and migration/doctor regression coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-resumable-persistence-fsm/38-CONTEXT.md
@.planning/phases/38-resumable-persistence-fsm/38-RESEARCH.md
@.planning/phases/38-resumable-persistence-fsm/38-PATTERNS.md
@.planning/phases/37-gcs-adapter-foundation/37-CONTEXT.md
@.planning/phases/37-gcs-adapter-foundation/37-04-SUMMARY.md
@.planning/research/v1.6-CANDIDATE-GCS.md
@priv/repo/migrations/20260425090200_create_media_upload_sessions.exs
@priv/repo/migrations/20260428110000_extend_media_upload_sessions_for_multipart.exs
@lib/rindle/ops/runtime_checks.ex
@test/rindle/domain/migration_test.exs
@test/rindle/ops/runtime_checks_test.exs
@guides/getting_started.md
@guides/upgrading.md

<interfaces>
From `priv/repo/migrations/20260428110000_extend_media_upload_sessions_for_multipart.exs`:

```elixir
def change do
  alter table(:media_upload_sessions) do
    add :upload_strategy, :string, null: false, default: "presigned_put"
    add :multipart_upload_id, :string
    add :multipart_parts, :map, null: false, default: %{}
  end
end
```

From `lib/rindle/ops/runtime_checks.ex`:

```elixir
defp run_check(fun) do
  started_at = System.monotonic_time()
  result = fun.()

  :telemetry.execute(
    [:rindle, :runtime, :check, :stop],
    %{duration_us: elapsed_us(started_at)},
    %{check: result.id, status: result.status, component: result.component}
  )

  result
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the packaged resumable migration template per D-01..D-05</name>
  <files>priv/repo/migrations/20260507160000_extend_media_upload_sessions_for_resumable.exs, test/rindle/domain/migration_test.exs</files>
  <read_first>
    - priv/repo/migrations/20260425090200_create_media_upload_sessions.exs
    - priv/repo/migrations/20260428110000_extend_media_upload_sessions_for_multipart.exs
    - test/rindle/domain/migration_test.exs
    - .planning/phases/38-resumable-persistence-fsm/38-CONTEXT.md
    - .planning/phases/38-resumable-persistence-fsm/38-RESEARCH.md
    - .planning/phases/38-resumable-persistence-fsm/38-PATTERNS.md
    - .planning/research/v1.6-CANDIDATE-GCS.md
  </read_first>
  <behavior>
    - Migration adds `session_uri :text`, `session_uri_expires_at :utc_datetime_usec`, `last_known_offset :bigint, default: 0, null: false`, and `region_hint :string, size: 64` per D-02 and D-03.
    - Migration widens `upload_strategy` default/check posture to admit `"resumable"` without removing `"presigned_put"` or `"multipart"` per D-05.
    - Migration creates a partial index on `session_uri_expires_at` filtered by `upload_strategy = 'resumable'` per D-04.
    - Migration remains reversible via `change/0`; no custom `up/0` or `down/0`.
    - Migration file includes a short comment noting the packaged default is plaintext `:text` and adopters may substitute an encrypted app-local field posture before rollout per D-08.
  </behavior>
  <action>
Create `priv/repo/migrations/20260507160000_extend_media_upload_sessions_for_resumable.exs` as a small additive `alter table(:media_upload_sessions)` migration that follows the multipart migration structure exactly.

Inside `alter table(:media_upload_sessions)`:
- add `:session_uri, :text` per D-02
- add `:session_uri_expires_at, :utc_datetime_usec`
- add `:last_known_offset, :bigint, null: false, default: 0`
- add `:region_hint, :string, size: 64`
- modify the existing `:upload_strategy` field so the schema and migration posture admit `"resumable"` alongside `"presigned_put"` and `"multipart"` per D-05; if the repo has no DB-level check constraint today, keep the migration additive and leave enforcement to the schema layer rather than inventing a new DB constraint

After the `alter table` block, create:

```elixir
create index(:media_upload_sessions, [:session_uri_expires_at],
         where: "upload_strategy = 'resumable'",
         name: :media_upload_sessions_resumable_expiry_idx
       )
```

Extend `test/rindle/domain/migration_test.exs` with resumable-specific catalog assertions:
- required column names on `media_upload_sessions`
- `last_known_offset` column default contains `0`
- `last_known_offset` is `NO` for `is_nullable`
- `session_uri` data type resolves to `text`
- at least one `pg_indexes.indexdef` contains both `session_uri_expires_at` and `WHERE ((upload_strategy = 'resumable'::text))` or the equivalent Postgres rendering

Do not add any broker, adapter, runtime-status, or GCS-runtime semantics in this task. This plan is schema-only groundwork per D-05.
  </action>
  <verify>
    <automated>mix test test/rindle/domain/migration_test.exs -x</automated>
  </verify>
  <acceptance_criteria>
    - `priv/repo/migrations/20260507160000_extend_media_upload_sessions_for_resumable.exs` exists
    - `grep -F 'add :session_uri, :text' priv/repo/migrations/20260507160000_extend_media_upload_sessions_for_resumable.exs` exits 0
    - `grep -F 'add :session_uri_expires_at, :utc_datetime_usec' priv/repo/migrations/20260507160000_extend_media_upload_sessions_for_resumable.exs` exits 0
    - `grep -F 'add :last_known_offset, :bigint, null: false, default: 0' priv/repo/migrations/20260507160000_extend_media_upload_sessions_for_resumable.exs` exits 0
    - `grep -F 'add :region_hint, :string, size: 64' priv/repo/migrations/20260507160000_extend_media_upload_sessions_for_resumable.exs` exits 0
    - `grep -F "where: \"upload_strategy = 'resumable'\"" priv/repo/migrations/20260507160000_extend_media_upload_sessions_for_resumable.exs` exits 0
    - `grep -F 'media_upload_sessions_resumable_expiry_idx' priv/repo/migrations/20260507160000_extend_media_upload_sessions_for_resumable.exs` exits 0
    - `grep -F 'stricter at-rest requirements' priv/repo/migrations/20260507160000_extend_media_upload_sessions_for_resumable.exs` exits 0
    - `mix test test/rindle/domain/migration_test.exs -x` exits 0
  </acceptance_criteria>
  <done>
    The packaged migration template exists under `priv/repo/migrations`, adds the four locked columns plus the partial expiry index, and migration smoke tests prove the adopter-owned table shape matches the Phase 38 persistence contract.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add schema-only doctor drift detection for resumable upload sessions per D-24..D-27</name>
  <files>lib/rindle/ops/runtime_checks.ex, test/rindle/ops/runtime_checks_test.exs</files>
  <read_first>
    - lib/rindle/ops/runtime_checks.ex
    - test/rindle/ops/runtime_checks_test.exs
    - test/rindle/doctor_test.exs
    - .planning/phases/38-resumable-persistence-fsm/38-CONTEXT.md
    - .planning/phases/38-resumable-persistence-fsm/38-RESEARCH.md
    - .planning/phases/38-resumable-persistence-fsm/38-PATTERNS.md
    - guides/operations.md
  </read_first>
  <behavior>
    - `RuntimeChecks.run/2` appends exactly one new check row with id `doctor.resumable_session_schema`.
    - The check introspects only columns, default/nullability, and partial-index presence/shape on `media_upload_sessions`.
    - The check does not mention Goth, bucket reachability, GCS profiles, capability advertisement, CORS, or resumable runtime semantics per D-27.
    - PASS summary states all resumable columns and the expiry index are present.
    - FAIL summary names missing columns, missing index shape, or `last_known_offset` default/nullability drift.
  </behavior>
  <action>
Edit `lib/rindle/ops/runtime_checks.ex` to append one new check function call near the existing migration checks:

```elixir
fn -> check_resumable_session_schema() end
```

Implement `check_resumable_session_schema/0` and any small private helpers it needs using direct `Rindle.Repo.query/2` catalog introspection. The check must verify:
- `media_upload_sessions` has `session_uri`
- `media_upload_sessions` has `session_uri_expires_at`
- `media_upload_sessions` has `last_known_offset`
- `media_upload_sessions` has `region_hint`
- `last_known_offset` is `NOT NULL`
- `last_known_offset` default contains `0`
- at least one partial index definition covers `session_uri_expires_at` with a `WHERE upload_strategy = 'resumable'` predicate

Return through existing `ok_result/4` or `error_result/4` helpers with id `doctor.resumable_session_schema` and component `:migrations`.

Extend `test/rindle/ops/runtime_checks_test.exs` to cover:
- stable check ordering includes `doctor.resumable_session_schema`
- success case with catalog stubs or direct repo data after test migrations
- failure case when injected catalog rows omit a required column or the filtered index

Keep the check always present, unlike the Phase 37 GCS profile-gated checks, because this is adopter-owned schema drift for a core table, not optional runtime config.
  </action>
  <verify>
    <automated>mix test test/rindle/ops/runtime_checks_test.exs test/rindle/doctor_test.exs -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'doctor.resumable_session_schema' lib/rindle/ops/runtime_checks.ex` exits 0
    - `grep -F 'check_resumable_session_schema' lib/rindle/ops/runtime_checks.ex` exits 0
    - `grep -F 'session_uri_expires_at' lib/rindle/ops/runtime_checks.ex` exits 0
    - `grep -F 'last_known_offset' lib/rindle/ops/runtime_checks.ex` exits 0
    - `grep -F "upload_strategy = 'resumable'" lib/rindle/ops/runtime_checks.ex` exits 0
    - `mix test test/rindle/ops/runtime_checks_test.exs test/rindle/doctor_test.exs -x` exits 0
    - doctor output assertions still reference `Rindle: Environment checks` in `test/rindle/doctor_test.exs`
  </acceptance_criteria>
  <done>
    `mix rindle.doctor` gains a single schema-only resumable drift check that proves the Phase 38 migration template was applied without adding unrelated GCS or resumable runtime noise.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adopter Repo migration runner -> adopter database | Packaged migration mutates adopter-owned schema; drift or bad defaults break resumable persistence silently. |
| doctor CLI -> database catalog | Schema introspection surfaces environment state; over-broad checks could leak into unrelated runtime concerns. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | T | packaged migration | mitigate | Add explicit column/default/index assertions in `test/rindle/domain/migration_test.exs` so D-02..D-04 shape drift blocks the plan. |
| T-38-02 | I | `session_uri` persistence posture | mitigate | Keep the migration comment explicit that plaintext `:text` is the packaged default and adopters may substitute encrypted-field posture per D-08; do not print example URIs anywhere. |
| T-38-03 | D | `RuntimeChecks` | mitigate | Keep `doctor.resumable_session_schema` schema-only per D-27 so missing GCS runtime config or optional adapters cannot block unrelated adopters. |
</threat_model>

<verification>
Run `mix test test/rindle/domain/migration_test.exs test/rindle/ops/runtime_checks_test.exs test/rindle/doctor_test.exs -x`.
</verification>

<success_criteria>
The repo ships a packaged resumable migration template and one schema-only doctor check. Tests prove the new columns, default/nullability, and filtered expiry index exist and that doctor reports drift deterministically.
</success_criteria>

<output>
After completion, create `.planning/phases/38-resumable-persistence-fsm/38-01-SUMMARY.md`.
</output>

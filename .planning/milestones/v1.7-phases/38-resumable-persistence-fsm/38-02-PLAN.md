---
id: 38-02
plan_number: 02
plan: 02
phase: 38
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/domain/media_upload_session.ex
  - lib/rindle/domain/upload_session_fsm.ex
  - test/rindle/domain/lifecycle_fsm_test.exs
  - test/rindle/domain/media_upload_session_test.exs
autonomous: true
requirements: [RESUMABLE-02]
must_haves:
  truths:
    - "MediaUploadSession accepts the new resumable persistence fields in its changeset without widening required-field semantics."
    - "The durable upload-session FSM understands explicit recovery via signed -> resuming -> uploading and still rejects passive status probes as state changes."
    - "Inspecting a MediaUploadSession never reveals the raw session_uri."
  artifacts:
    - path: "lib/rindle/domain/media_upload_session.ex"
      provides: "Resumable fields, changeset casting, and centralized session_uri redaction"
      contains: "def redact_session_uri"
    - path: "lib/rindle/domain/upload_session_fsm.ex"
      provides: "Locked resuming transition lane"
      contains: "\"resuming\""
    - path: "test/rindle/domain/media_upload_session_test.exs"
      provides: "Changeset and inspect redaction coverage for MediaUploadSession"
      contains: "session_uri"
  key_links:
    - from: "lib/rindle/domain/media_upload_session.ex"
      to: "defimpl Inspect"
      via: "centralized session_uri redaction helper"
      pattern: "\\[REDACTED\\]"
    - from: "lib/rindle/domain/upload_session_fsm.ex"
      to: "test/rindle/domain/lifecycle_fsm_test.exs"
      via: "transition matrix assertions for signed -> resuming -> uploading and cancel-from-resuming"
      pattern: "resuming"
---

<objective>
Extend the upload-session domain model and FSM for resumable persistence while enforcing absolute inspect redaction of `session_uri`.

Purpose: Implement D-10 through D-16 exactly, keeping `"resuming"` narrow and durable without pulling forward resumable status/cancel broker semantics.
Output: Updated `MediaUploadSession` schema/changeset/Inspect implementation, updated `UploadSessionFSM`, and domain-level tests proving the locked transition lane and redaction invariant.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-resumable-persistence-fsm/38-CONTEXT.md
@.planning/phases/38-resumable-persistence-fsm/38-RESEARCH.md
@.planning/phases/38-resumable-persistence-fsm/38-PATTERNS.md
@lib/rindle/domain/media_upload_session.ex
@lib/rindle/domain/upload_session_fsm.ex
@lib/rindle/domain/media_provider_asset.ex
@test/rindle/domain/lifecycle_fsm_test.exs
@test/rindle/domain/media_provider_asset_test.exs

<interfaces>
From `lib/rindle/domain/media_upload_session.ex`:

```elixir
@states [
  "initialized",
  "signed",
  "uploading",
  "uploaded",
  "verifying",
  "completed",
  "aborted",
  "expired",
  "failed"
]
```

From `lib/rindle/domain/upload_session_fsm.ex`:

```elixir
@spec transition(state(), state(), map()) :: :ok | transition_error()
def transition(current_state, target_state, context \\ %{})
```

From `lib/rindle/domain/media_provider_asset.ex`:

```elixir
defimpl Inspect, for: Rindle.Domain.MediaProviderAsset do
  def inspect(asset, opts) do
    redacted = %{
      asset
      | provider_asset_id: Rindle.Domain.MediaProviderAsset.redact_id(asset.provider_asset_id),
        raw_provider_metadata: %{redacted: true}
    }

    Inspect.Any.inspect(redacted, opts)
  end
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend MediaUploadSession schema and Inspect redaction per D-14..D-16</name>
  <files>lib/rindle/domain/media_upload_session.ex, test/rindle/domain/media_upload_session_test.exs</files>
  <read_first>
    - lib/rindle/domain/media_upload_session.ex
    - lib/rindle/domain/media_provider_asset.ex
    - .planning/phases/38-resumable-persistence-fsm/38-CONTEXT.md
    - .planning/phases/38-resumable-persistence-fsm/38-RESEARCH.md
    - .planning/phases/38-resumable-persistence-fsm/38-PATTERNS.md
  </read_first>
  <behavior>
    - `MediaUploadSession` schema gains fields `session_uri`, `session_uri_expires_at`, `last_known_offset`, and `region_hint`.
    - `changeset/2` casts those four fields while preserving the current required-field list.
    - Module exposes a small helper such as `redact_session_uri/1` that returns `nil` for `nil` and `"[REDACTED]"` for any populated binary.
    - `defimpl Inspect` for `Rindle.Domain.MediaUploadSession` rewrites only `session_uri`, leaving the rest of the struct inspectable.
    - ExUnit coverage proves raw `session_uri` never appears in `inspect(session)` output.
  </behavior>
  <action>
Edit `lib/rindle/domain/media_upload_session.ex` in place.

Update `@states` only if needed for `"resuming"` in Task 2; do not add any other new state strings here.

Inside `schema "media_upload_sessions"` add:
- `field :session_uri, :string`
- `field :session_uri_expires_at, :utc_datetime_usec`
- `field :last_known_offset, :integer, default: 0`
- `field :region_hint, :string`

Inside `changeset/2` add these four fields to the existing `cast/3` list per D-14. Keep `validate_required/2` unchanged: the new fields are additive and optional at Phase 38.

Add a module helper:

```elixir
@spec redact_session_uri(nil | String.t()) :: nil | String.t()
```

with behavior:
- `nil -> nil`
- any binary -> `"[REDACTED]"`
- any non-binary populated value -> `"[REDACTED]"`

Add a `defimpl Inspect, for: Rindle.Domain.MediaUploadSession` that copies the `MediaProviderAsset` pattern but only rewrites `session_uri` through `redact_session_uri/1`.

Create `test/rindle/domain/media_upload_session_test.exs` with focused coverage for:
- changeset casts each of the four new fields
- `inspect/1` on a struct containing a realistic resumable URI includes `"[REDACTED]"` and excludes the original URI string
- `redact_session_uri(nil) == nil`
- `redact_session_uri("https://storage.googleapis.com/upload/...") == "[REDACTED]"`

Do not add logger translators, encryption dependencies, or guide edits in this plan. Those are deferred or documented later per D-07..D-09.
  </action>
  <verify>
    <automated>mix test test/rindle/domain/media_upload_session_test.exs -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'field :session_uri, :string' lib/rindle/domain/media_upload_session.ex` exits 0
    - `grep -F 'field :session_uri_expires_at, :utc_datetime_usec' lib/rindle/domain/media_upload_session.ex` exits 0
    - `grep -F 'field :last_known_offset, :integer, default: 0' lib/rindle/domain/media_upload_session.ex` exits 0
    - `grep -F 'field :region_hint, :string' lib/rindle/domain/media_upload_session.ex` exits 0
    - `grep -F 'def redact_session_uri' lib/rindle/domain/media_upload_session.ex` exits 0
    - `grep -F 'defimpl Inspect, for: Rindle.Domain.MediaUploadSession' lib/rindle/domain/media_upload_session.ex` exits 0
    - `grep -F '[REDACTED]' lib/rindle/domain/media_upload_session.ex` exits 0
    - `mix test test/rindle/domain/media_upload_session_test.exs -x` exits 0
  </acceptance_criteria>
  <done>
    `MediaUploadSession` can persist the resumable fields, and any inspect surface shows `session_uri: "[REDACTED]"` instead of the raw bearer credential.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the explicit resuming lane to UploadSessionFSM per D-10..D-13</name>
  <files>lib/rindle/domain/upload_session_fsm.ex, test/rindle/domain/lifecycle_fsm_test.exs</files>
  <read_first>
    - lib/rindle/domain/upload_session_fsm.ex
    - test/rindle/domain/lifecycle_fsm_test.exs
    - lib/rindle/upload/broker.ex
    - .planning/phases/38-resumable-persistence-fsm/38-CONTEXT.md
    - .planning/phases/38-resumable-persistence-fsm/38-RESEARCH.md
  </read_first>
  <behavior>
    - `"signed"` allows transition to `"resuming"` and `"resuming"` allows transition to `"uploading"`.
    - `"resuming"` also allows cancel/failure terminal exits consistent with the existing non-terminal cancel posture.
    - No read-only status or offset helper is added that mutates durable state.
    - Existing nominal lane `"uploading" -> "uploaded" -> "verifying" -> "completed"` remains unchanged.
    - Tests prove `"signed" -> "uploading"` remains allowed for nominal flow and `"signed" -> "resuming" -> "uploading"` works for explicit recovery.
  </behavior>
  <action>
Edit `lib/rindle/domain/upload_session_fsm.ex` only in `@allowed_transitions`.

Make these exact changes:
- in `"signed"` allowlist, insert `"resuming"` ahead of `"uploading"`
- add a new key `"resuming"` with value `["uploading", "aborted", "expired", "failed"]`

Do not add `"resuming"` to `"initialized"` or `"uploaded"` transitions, and do not create backward edges such as `"uploading" -> "resuming"`. D-11 and D-12 lock `"resuming"` to explicit recovery only.

Extend `test/rindle/domain/lifecycle_fsm_test.exs` with upload-session assertions for:
- `UploadSessionFSM.transition("signed", "resuming") == :ok`
- `UploadSessionFSM.transition("resuming", "uploading") == :ok`
- `UploadSessionFSM.transition("resuming", "aborted") == :ok`
- `UploadSessionFSM.transition("uploading", "resuming") == {:error, {:invalid_transition, "uploading", "resuming"}}`
- existing `"signed" -> "uploading"` and `"uploading" -> "uploaded"` happy-path assertions remain present to guard regression

Do not change broker code in this plan. The phase explicitly defers resume/status runtime semantics; this plan only extends the durable FSM vocabulary that later phases will use.
  </action>
  <verify>
    <automated>mix test test/rindle/domain/lifecycle_fsm_test.exs test/rindle/domain/media_upload_session_test.exs -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '\"signed\" => [\"resuming\", \"uploading\"' lib/rindle/domain/upload_session_fsm.ex` exits 0
    - `grep -F '\"resuming\" => [\"uploading\", \"aborted\", \"expired\", \"failed\"]' lib/rindle/domain/upload_session_fsm.ex` exits 0
    - `grep -F 'UploadSessionFSM.transition(\"signed\", \"resuming\")' test/rindle/domain/lifecycle_fsm_test.exs` exits 0
    - `grep -F 'UploadSessionFSM.transition(\"resuming\", \"uploading\")' test/rindle/domain/lifecycle_fsm_test.exs` exits 0
    - `grep -F 'UploadSessionFSM.transition(\"uploading\", \"resuming\")' test/rindle/domain/lifecycle_fsm_test.exs` exits 0
    - `mix test test/rindle/domain/lifecycle_fsm_test.exs test/rindle/domain/media_upload_session_test.exs -x` exits 0
  </acceptance_criteria>
  <done>
    The durable upload-session FSM supports the locked recovery lane and still rejects using `"resuming"` as a generic status-probe state.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller attrs -> `MediaUploadSession.changeset/2` | Untrusted persisted fields enter the upload-session schema. |
| internal state transitions -> FSM guard | Runtime code attempts to mutate durable upload-session state. |
| inspect/log/test output -> operator surfaces | Secret-bearing structs can leak into failures or debugging output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-04 | I | `MediaUploadSession` inspect output | mitigate | Add centralized `redact_session_uri/1` plus `defimpl Inspect` and explicit ExUnit assertions that the raw URI string never appears. |
| T-38-05 | T | `UploadSessionFSM` | mitigate | Restrict `"resuming"` to the exact D-10 lane and add regression tests that block `"uploading" -> "resuming"` and other broadened edges. |
| T-38-06 | R | session-state mutations | mitigate | Keep the plan free of new status-probe APIs so there is no ambiguous path that could mutate state without explicit recovery intent per D-12. |
</threat_model>

<verification>
Run `mix test test/rindle/domain/lifecycle_fsm_test.exs test/rindle/domain/media_upload_session_test.exs -x`.
</verification>

<success_criteria>
The upload-session schema accepts the new resumable fields, the FSM exposes only the locked `resuming` recovery lane, and `inspect/2` always redacts `session_uri`.
</success_criteria>

<output>
After completion, create `.planning/phases/38-resumable-persistence-fsm/38-02-SUMMARY.md`.
</output>

---
id: 38-03
plan_number: 03
plan: 03
phase: 38
type: execute
wave: 2
depends_on: [38-02]
files_modified:
  - lib/rindle/upload/resumable_telemetry.ex
  - guides/storage_gcs.md
  - test/rindle/contracts/telemetry_contract_test.exs
  - test/rindle/upload/resumable_telemetry_test.exs
autonomous: true
requirements: [RESUMABLE-03]
must_haves:
  truths:
    - "The public telemetry contract now reserves exactly two resumable events: status and cancel."
    - "Later resumable code has a centralized internal emit helper that enforces required metadata and excludes session_uri."
    - "Parity tests prove session_uri stays out of resumable telemetry metadata and inspect output when telemetry helpers are used."
    - "A narrow `guides/storage_gcs.md` note documents the `Logger.add_translator` / logger-metadata filter recipe for `:session_uri` without expanding into full Phase 41 onboarding."
  artifacts:
    - path: "lib/rindle/upload/resumable_telemetry.ex"
      provides: "Centralized internal helper for resumable status/cancel telemetry emission"
      contains: "[:rindle, :upload, :resumable, :status]"
    - path: "guides/storage_gcs.md"
      provides: "Defense-in-depth logger metadata filter recipe for `:session_uri`"
      contains: "Logger.add_translator"
    - path: "test/rindle/contracts/telemetry_contract_test.exs"
      provides: "Frozen public allowlist including resumable status/cancel"
      contains: "[:rindle, :upload, :resumable, :cancel]"
    - path: "test/rindle/upload/resumable_telemetry_test.exs"
      provides: "Redaction parity coverage for resumable telemetry helpers"
      contains: "session_uri"
  key_links:
    - from: "lib/rindle/upload/resumable_telemetry.ex"
      to: ":telemetry.execute/3"
      via: "central emit helpers for status and cancel"
      pattern: "telemetry\\.execute"
    - from: "test/rindle/upload/resumable_telemetry_test.exs"
      to: "Rindle.Domain.MediaUploadSession.redact_session_uri/1"
      via: "struct fixtures include raw session_uri but emitted metadata omits it"
      pattern: "session_uri"
---

<objective>
Freeze the resumable public telemetry contract and add redaction-parity coverage without inventing Phase 39 broker or adapter APIs.

Purpose: Implement D-17 through D-23 while honoring the research guidance to keep Phase 38 narrow: create internal emit helpers and contract tests, not public resumable runtime entrypoints.
Output: Internal `Rindle.Upload.ResumableTelemetry` emit helpers, widened public telemetry allowlist, and parity tests proving `session_uri` never crosses telemetry metadata.
Also output a narrow `guides/storage_gcs.md` note containing the logger metadata filter recipe required by `RESUMABLE-03`, without turning this phase into full GCS onboarding.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-resumable-persistence-fsm/38-CONTEXT.md
@.planning/phases/38-resumable-persistence-fsm/38-RESEARCH.md
@.planning/phases/38-resumable-persistence-fsm/38-PATTERNS.md
@lib/rindle/upload/broker.ex
@lib/rindle/domain/media_upload_session.ex
@lib/rindle/storage/gcs.ex
@test/rindle/contracts/telemetry_contract_test.exs
@test/rindle/streaming/provider/mux/telemetry_test.exs

<interfaces>
From `test/rindle/contracts/telemetry_contract_test.exs`:

```elixir
@public_events [
  [:rindle, :upload, :start],
  [:rindle, :upload, :stop],
  [:rindle, :asset, :state_change],
  [:rindle, :variant, :state_change]
]
```

From `lib/rindle/upload/broker.ex`:

```elixir
emit_upload_start(profile_name, profile_module.storage_adapter(), session.id)
```

Required Phase 38 resumable contract from CONTEXT D-17..D-20:

```elixir
[:rindle, :upload, :resumable, :status]
[:rindle, :upload, :resumable, :cancel]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add internal resumable telemetry emit helpers with locked metadata and measurements</name>
  <files>lib/rindle/upload/resumable_telemetry.ex, test/rindle/upload/resumable_telemetry_test.exs</files>
  <read_first>
    - lib/rindle/upload/broker.ex
    - lib/rindle/domain/media_upload_session.ex
    - test/rindle/streaming/provider/mux/telemetry_test.exs
    - .planning/phases/38-resumable-persistence-fsm/38-CONTEXT.md
    - .planning/phases/38-resumable-persistence-fsm/38-RESEARCH.md
  </read_first>
  <behavior>
    - Internal module exposes exactly two emit helpers: one for `:status`, one for `:cancel`.
    - Required metadata keys are `:profile` and `:adapter`.
    - Allowed metadata keys are `:state`, `:outcome`, `:reason`, `:source`, and optional `:session_id`.
    - `:status` measurements include `:committed_bytes`, optional `:offset_delta`, and `:system_time`.
    - `:cancel` measurements include `:duration_us` and `:system_time`.
    - Helper input may include a `%MediaUploadSession{}` with raw `session_uri`, but emitted metadata must not contain `:session_uri`, storage keys, response bodies, or raw session identifiers.
  </behavior>
  <action>
Create `lib/rindle/upload/resumable_telemetry.ex` with `@moduledoc false`.

Export two functions only:
- `emit_status/4`
- `emit_cancel/4`

Use a narrow, internal contract such as:

```elixir
emit_status(profile, adapter, session_or_nil, metadata_overrides, measurements_overrides)
emit_cancel(profile, adapter, session_or_nil, metadata_overrides, measurements_overrides)
```

or an equivalent helper split that still lets tests pass a `%MediaUploadSession{}` carrying a raw `session_uri`.

Rules the helper must enforce:
- always emit `:profile` and `:adapter`
- include `:session_id` only from `session.id`, never from `session.session_uri`
- drop or ignore any incoming `:session_uri`, `:upload_key`, `:headers`, `:body`, or arbitrary non-allowlisted metadata keys
- add `system_time: System.system_time()` if the caller does not provide it
- call `:telemetry.execute/3` with exactly `[:rindle, :upload, :resumable, :status]` and `[:rindle, :upload, :resumable, :cancel]`

Create `test/rindle/upload/resumable_telemetry_test.exs` modeled on the Mux parity test but without Mux fixtures. The test module should:
- attach handlers to the two resumable events
- build a `%Rindle.Domain.MediaUploadSession{id: "sess-1", session_uri: "https://storage.googleapis.com/upload/..."}`
- call the helper functions
- assert emitted metadata contains `profile`, `adapter`, and optional `session_id`
- assert emitted metadata keys are a subset of `[:profile, :adapter, :state, :outcome, :reason, :source, :session_id]`
- assert the raw URI string is absent from every metadata value and from `inspect(session)`

Do not wire these helpers into new broker or adapter entrypoints in Phase 38. The point is to freeze the contract and parity rules now so Phase 39 can reuse the helper additively.
  </action>
  <verify>
    <automated>mix test test/rindle/upload/resumable_telemetry_test.exs -x</automated>
  </verify>
  <acceptance_criteria>
    - `lib/rindle/upload/resumable_telemetry.ex` exists
    - `grep -F '[:rindle, :upload, :resumable, :status]' lib/rindle/upload/resumable_telemetry.ex` exits 0
    - `grep -F '[:rindle, :upload, :resumable, :cancel]' lib/rindle/upload/resumable_telemetry.ex` exits 0
    - `grep -F 'committed_bytes' lib/rindle/upload/resumable_telemetry.ex` exits 0
    - `grep -F 'duration_us' lib/rindle/upload/resumable_telemetry.ex` exits 0
    - `grep -F 'session_id' lib/rindle/upload/resumable_telemetry.ex` exits 0
    - `grep -F 'session_uri' test/rindle/upload/resumable_telemetry_test.exs` exits 0
    - `mix test test/rindle/upload/resumable_telemetry_test.exs -x` exits 0
  </acceptance_criteria>
  <done>
    The repo has centralized resumable emit helpers that later phases can reuse, and tests prove the helpers never expose `session_uri` across the telemetry boundary.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze the public telemetry allowlist to exactly the two resumable events per D-17..D-23</name>
  <files>test/rindle/contracts/telemetry_contract_test.exs, test/rindle/upload/resumable_telemetry_test.exs</files>
  <read_first>
    - test/rindle/contracts/telemetry_contract_test.exs
    - test/rindle/upload/resumable_telemetry_test.exs
    - .planning/phases/38-resumable-persistence-fsm/38-CONTEXT.md
    - .planning/phases/38-resumable-persistence-fsm/38-RESEARCH.md
  </read_first>
  <behavior>
    - `@public_events` includes exactly two new entries for resumable status/cancel and no resumable start/stop or GCS-specific names.
    - allowlist length increases by exactly 2.
    - numeric-measurement assertions still hold for the new events.
    - helper-driven tests prove the two events are actually emitted, not just listed in the contract file.
  </behavior>
  <action>
Edit `test/rindle/contracts/telemetry_contract_test.exs`:
- add `[:rindle, :upload, :resumable, :status]`
- add `[:rindle, :upload, :resumable, :cancel]`
- update the exact `length(@public_events)` assertion by `+2`
- add one focused contract test that invokes `Rindle.Upload.ResumableTelemetry.emit_status/4-or-5` and `emit_cancel/4-or-5` and then asserts:
  - both events are received
  - `profile` and `adapter` exist in metadata
  - every measurement value is numeric

Do not add resumable `:start`, resumable `:stop`, or deeper GCS event names. D-18 forbids broadening the public family in Phase 38.

Keep contract coverage narrow and additive. The test should prove the public contract exists now without inventing resumable status APIs on `Rindle.Upload.Broker`.
  </action>
  <verify>
    <automated>mix test test/rindle/contracts/telemetry_contract_test.exs test/rindle/upload/resumable_telemetry_test.exs -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '[:rindle, :upload, :resumable, :status]' test/rindle/contracts/telemetry_contract_test.exs` exits 0
    - `grep -F '[:rindle, :upload, :resumable, :cancel]' test/rindle/contracts/telemetry_contract_test.exs` exits 0
    - `grep -F 'assert length(@public_events) == 18' test/rindle/contracts/telemetry_contract_test.exs` exits 0
    - `mix test test/rindle/contracts/telemetry_contract_test.exs test/rindle/upload/resumable_telemetry_test.exs -x` exits 0
  </acceptance_criteria>
  <done>
    The public telemetry contract is frozen to exactly the two Phase 38 resumable events, and contract tests fail if later work renames or broadens them prematurely.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add the narrow `session_uri` logger-filter recipe required by RESUMABLE-03</name>
  <files>guides/storage_gcs.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/38-resumable-persistence-fsm/38-CONTEXT.md
    - .planning/phases/38-resumable-persistence-fsm/38-RESEARCH.md
    - guides/getting_started.md
    - guides/troubleshooting.md
    - lib/rindle/storage/gcs.ex
  </read_first>
  <behavior>
    - `guides/storage_gcs.md` is created as a minimal Phase 38 placeholder rather than a full onboarding guide.
    - The file explicitly documents a defense-in-depth `Logger.add_translator` / logger metadata filtering recipe for `:session_uri`.
    - The file states that broader GCS setup, bucket wiring, and resumable operational guidance remain Phase 41 work.
    - The doc does not include any real session URI, credential material, or claims about shipped Phase 39-41 APIs.
  </behavior>
  <action>
Create `guides/storage_gcs.md` with a short maintainer-facing stub that does exactly three things:
- explains that `session_uri` is a bearer credential and must never appear in logs
- provides a concrete `Logger.add_translator` or equivalent logger metadata filtering recipe that strips or rewrites `:session_uri`
- states that this is an interim Phase 38 defense-in-depth note and that the full GCS storage onboarding guide lands in Phase 41

Use exact strings that make the requirement easy to grep:
- `Logger.add_translator`
- `:session_uri`
- `Phase 41`
- `bearer credential`

Keep the file narrow. Do not document bucket setup, Goth, Finch, signing keys, resumable broker APIs, or CORS posture here.
  </action>
  <verify>
    <automated>grep -F 'Logger.add_translator' guides/storage_gcs.md && grep -F ':session_uri' guides/storage_gcs.md && grep -F 'Phase 41' guides/storage_gcs.md</automated>
  </verify>
  <acceptance_criteria>
    - `guides/storage_gcs.md` exists
    - `grep -F 'Logger.add_translator' guides/storage_gcs.md` exits 0
    - `grep -F ':session_uri' guides/storage_gcs.md` exits 0
    - `grep -F 'bearer credential' guides/storage_gcs.md` exits 0
    - `grep -F 'Phase 41' guides/storage_gcs.md` exits 0
  </acceptance_criteria>
  <done>
    `RESUMABLE-03`'s logger metadata filter recipe is explicitly planned in a narrow guide stub, while the broader GCS onboarding work stays deferred to Phase 41.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime metadata input -> telemetry boundary | Internal code can accidentally pass sensitive fields into public telemetry. |
| public telemetry contract -> future phases | Premature event-name sprawl would create semver-sensitive surface before real emit sites land. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-07 | I | resumable telemetry metadata | mitigate | Centralize emission in `Rindle.Upload.ResumableTelemetry`, allowlist metadata keys, and assert `session_uri` is absent in `test/rindle/upload/resumable_telemetry_test.exs`. |
| T-38-08 | T | public telemetry contract | mitigate | Freeze exactly two resumable event names in `test/rindle/contracts/telemetry_contract_test.exs`; block additional resumable event families in this phase per D-18. |
| T-38-09 | R | future instrumentation reuse | mitigate | Depend on Plan 38-02 and reuse the domain redaction posture so later phases inherit the same secret-handling rules instead of hand-rolling emit sites. |
</threat_model>

<verification>
Run `mix test test/rindle/contracts/telemetry_contract_test.exs test/rindle/upload/resumable_telemetry_test.exs -x`.
</verification>

<success_criteria>
Phase 38 publishes only the resumable `status` and `cancel` telemetry names, with helper-backed tests proving measurements stay numeric and `session_uri` never reaches public metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/38-resumable-persistence-fsm/38-03-SUMMARY.md`.
</output>

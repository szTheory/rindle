---
id: 39-03
plan_number: 03
plan: 03
phase: 39
type: execute
wave: 3
depends_on: [39-01, 39-02]
files_modified:
  - lib/rindle/upload/broker.ex
  - test/rindle/upload/broker_test.exs
autonomous: true
requirements: [RESUMABLE-06, RESUMABLE-07]
must_haves:
  truths:
    - "The broker exposes initiate/status/cancel resumable entrypoints with the locked return shapes."
    - "Remote resumable initiation happens before DB persistence, and persist failure triggers compensating remote cancel."
    - "Status polling is observational and bookkeeping-only; it does not broaden the FSM into a second completion or recovery path."
    - "verify_completion/2 remains the sole broker trust gate and still uses adapter.head/2."
  artifacts:
    - path: "lib/rindle/upload/broker.ex"
      provides: "Public broker entrypoints and resumable persistence/compensation helpers"
      contains: "def initiate_resumable_session"
    - path: "test/rindle/upload/broker_test.exs"
      provides: "Broker lifecycle tests for resumable initiation, status, cancel, compensation, and trust-gate preservation"
      contains: "resumable"
  key_links:
    - from: "lib/rindle/upload/broker.ex"
      to: "lib/rindle/storage/capabilities.ex"
      via: "require_upload/2 capability gates for initiate vs status/cancel"
      pattern: "Capabilities\\.require_upload"
    - from: "lib/rindle/upload/broker.ex"
      to: "lib/rindle/storage.ex"
      via: "adapter resumable callbacks for initiation/status/cancel and head/2 for completion"
      pattern: "(initiate_resumable_upload|resumable_upload_status|cancel_resumable_upload|head)"
---

<objective>
Wire resumable lifecycle entrypoints into the broker without changing the existing completion trust model.

Purpose: Deliver RESUMABLE-06 and the broker-facing half of RESUMABLE-07 by reusing the multipart lifecycle posture: capability gate, remote initiation, DB persistence, compensation on persist failure, and final convergence through `verify_completion/2`.
Output: New broker resumable entrypoints, persistence/compensation helpers, and broker regression coverage that defends the single trust-gate rule.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-resumable-adapter-behaviour-broker-wiring/39-CONTEXT.md
@.planning/phases/39-resumable-adapter-behaviour-broker-wiring/39-RESEARCH.md
@.planning/phases/39-resumable-adapter-behaviour-broker-wiring/39-PATTERNS.md
@.planning/phases/39-resumable-adapter-behaviour-broker-wiring/39-01-PLAN.md
@.planning/phases/39-resumable-adapter-behaviour-broker-wiring/39-02-PLAN.md
@lib/rindle/upload/broker.ex
@lib/rindle/domain/media_upload_session.ex
@lib/rindle/storage/capabilities.ex
@test/rindle/upload/broker_test.exs

<interfaces>
From `.planning/research/v1.6-CANDIDATE-GCS.md`:

```elixir
@spec initiate_resumable_session(profile_module :: module(), opts :: keyword()) ::
        {:ok, %{session: MediaUploadSession.t(),
                resumable: %{session_uri: String.t(),
                             upload_id: String.t(),
                             expires_at: DateTime.t()}}}
        | {:error, term()}

@spec resumable_session_status(session_id :: binary(), opts :: keyword()) ::
        {:ok, %{session: MediaUploadSession.t(),
                committed_bytes: non_neg_integer(),
                state: :in_progress | :complete | :expired}}
        | {:error, term()}

@spec cancel_resumable_session(session_id :: binary(), opts :: keyword()) ::
        {:ok, %{session: MediaUploadSession.t()}}
        | {:error, term()}
```

From `lib/rindle/upload/broker.ex`:

```elixir
with :ok <- Capabilities.require_upload(adapter, :multipart_upload),
     {:ok, multipart} <- adapter.initiate_multipart_upload(storage_key, part_size, opts),
     {:ok, session} <- persist_multipart_session(repo, adapter, session_seed, multipart, opts) do
  ...
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add initiate_resumable_session/2 with multipart-style compensation per D-08..D-09</name>
  <files>lib/rindle/upload/broker.ex, test/rindle/upload/broker_test.exs</files>
  <behavior>
    - `initiate_resumable_session/2` returns the locked `%{session:, resumable: %{session_uri, upload_id, expires_at}}}` shape.
    - Broker gates initiation on `:resumable_upload`, not `:resumable_upload_session`, per D-03 and D-19.
    - Remote initiation happens before DB persistence, and persistence failure triggers `cancel_resumable_upload/3` on the adapter per D-08 and D-09.
    - Newly created resumable rows persist `upload_strategy: "resumable"` plus `session_uri`, `session_uri_expires_at`, `last_known_offset`, and any `region_hint`.
  </behavior>
  <action>
Edit `lib/rindle/upload/broker.ex` to add a new `initiate_resumable_session/2` public entrypoint and its supporting private persistence/compensation helper.

Mirror the multipart flow closely:
- generate asset id, storage key, filename, expiry timestamp up front
- resolve the adapter and gate via `Capabilities.require_upload(adapter, :resumable_upload)`
- call `adapter.initiate_resumable_upload(storage_key, expected_size_or_nil, opts)` before any repo transaction
- persist the session row only after the remote call succeeds
- if persistence fails, call `adapter.cancel_resumable_upload(storage_key, session_uri, opts)` as best-effort compensation before returning the DB error

Persist resumable-specific fields on the session row and keep the initial durable state aligned with the existing direct-upload lifecycle. Do not invent a second transaction model or proxy bytes through the broker.

Extend `test/rindle/upload/broker_test.exs` with Mox-backed initiation tests for:
- successful resumable session bootstrap
- no DB writes when remote initiation fails
- compensation via `cancel_resumable_upload/3` when persistence fails
- malformed capability declarations and Local/S3-style unsupported adapters returning tagged resumable unsupported errors
  </action>
  <verify>
    <automated>mix test test/rindle/upload/broker_test.exs -x</automated>
  </verify>
  <done>
    The broker can mint resumable sessions through a remote-first, compensating-persist flow that mirrors multipart instead of inventing a second lifecycle pattern.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add status/cancel entrypoints while preserving head/2 completion trust per D-05..D-11</name>
  <files>lib/rindle/upload/broker.ex, test/rindle/upload/broker_test.exs</files>
  <behavior>
    - `resumable_session_status/2` gates on `:resumable_upload_session`, calls the adapter status callback, and only refreshes bookkeeping fields like `last_known_offset`, `session_uri_expires_at`, and `region_hint`.
    - Status polling alone does not move the durable session into `"resuming"`, `"uploading"`, or `"completed"` per D-10 and D-11.
    - `cancel_resumable_session/2` gates on `:resumable_upload_session`, calls the adapter cancel callback, and persists the broker's session outcome without weakening later maintenance semantics.
    - `verify_completion/2` remains untouched in behavior: broker tests prove it still calls `adapter.head/2` and never depends on `verify_resumable_completion/3`.
  </behavior>
  <action>
Continue editing `lib/rindle/upload/broker.ex` to add `resumable_session_status/2` and `cancel_resumable_session/2`, plus any narrow helpers they need.

Locked constraints:
- look up the session row first and reject non-resumable rows with `{:error, {:upload_unsupported, :resumable_upload_session}}` per D-19
- gate status/cancel via `Capabilities.require_upload(adapter, :resumable_upload_session)`
- treat status as observational: persist refreshed offsets/expiry/region data, but do not broaden FSM semantics or reuse `"resuming"` for routine polling
- keep `verify_completion/2` on the existing `adapter.head/2` path; if executor is tempted to call `verify_resumable_completion/3` from broker code, reject that change explicitly

Extend `test/rindle/upload/broker_test.exs` to prove:
- status updates `last_known_offset` and returns the locked `%{session:, committed_bytes:, state:}` shape
- ordinary status polling leaves the durable state unchanged
- cancel uses the adapter callback and returns `%{session: session}`
- unsupported resumable status/cancel calls against Local/S3 or a non-resumable row return `{:error, {:upload_unsupported, :resumable_upload_session}}`
- `verify_completion/2` still mocks `head/2` only; no expectation should be added for `verify_resumable_completion/3`
  </action>
  <verify>
    <automated>mix test test/rindle/upload/broker_test.exs -x</automated>
  </verify>
  <done>
    The broker owns resumable initiation/status/cancel while keeping completion convergence exactly where it already lives: `verify_completion/2` plus `head/2`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| broker -> storage adapter | The broker must invoke the right resumable callback family without falling back to unsupported adapters or alternate completion logic. |
| broker -> adopter database | Remote initiation and DB persistence happen on opposite sides of the transaction boundary, so compensation discipline matters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-07 | T | `lib/rindle/upload/broker.ex` | mitigate | Mirror the multipart remote-first + compensating-cancel pattern per D-08 and D-09 instead of inventing a bespoke failure path. |
| T-39-08 | E | broker capability gates | mitigate | Use `Capabilities.require_upload/2` with `:resumable_upload` for initiate and `:resumable_upload_session` for status/cancel, then test unsupported paths explicitly. |
| T-39-09 | I | broker completion path | mitigate | Add regression tests that `verify_completion/2` still mocks `head/2` only, blocking any accidental shift to session-URI trust. |
</threat_model>

<verification>
Run `mix test test/rindle/upload/broker_test.exs -x`.
</verification>

<success_criteria>
The broker owns resumable session lifecycle entrypoints, persists resumable bookkeeping safely, compensates failed persists, and still trusts only `head/2` for promotion.
</success_criteria>

<output>
After completion, create `.planning/phases/39-resumable-adapter-behaviour-broker-wiring/39-03-SUMMARY.md`.
</output>

---
id: 39-02
plan_number: 02
plan: 02
phase: 39
type: execute
wave: 2
depends_on: [39-01]
files_modified:
  - lib/rindle/storage/gcs.ex
  - lib/rindle/storage/gcs/client.ex
  - test/rindle/storage/gcs/client_test.exs
  - test/rindle/storage/gcs_test.exs
autonomous: true
requirements: [RESUMABLE-05, RESUMABLE-08]
must_haves:
  truths:
    - "Rindle.Storage.GCS implements the four resumable callbacks through the existing Finch-based client path."
    - "GCS advertises resumable capabilities only once the real callbacks exist."
    - "Resumable protocol failures map to the locked public atoms/tuples instead of leaking raw transport details."
    - "Region pinning is visible as metadata/telemetry input, not a returned broker error."
  artifacts:
    - path: "lib/rindle/storage/gcs/client.ex"
      provides: "Hand-rolled GCS resumable initiation/status/cancel/verification helpers"
      contains: "uploadType=resumable"
    - path: "lib/rindle/storage/gcs.ex"
      provides: "Public GCS adapter delegating resumable callbacks and advertising honest capabilities"
      contains: "def initiate_resumable_upload"
    - path: "test/rindle/storage/gcs_test.exs"
      provides: "Adapter-level resumable capability and error-mapping coverage"
      contains: "resumable"
  key_links:
    - from: "lib/rindle/storage/gcs.ex"
      to: "lib/rindle/storage/gcs/client.ex"
      via: "thin delegators using bucket/goth/finch injection"
      pattern: "Client\\.(initiate_resumable_upload|resumable_upload_status|cancel_resumable_upload|verify_resumable_completion)"
    - from: "test/rindle/storage/gcs/client_test.exs"
      to: "lib/rindle/storage/gcs/client.ex"
      via: "Bypass protocol assertions for 308/404/410/error mapping"
      pattern: "Bypass\\.expect_once"
---

<objective>
Implement the GCS resumable callback surface on top of the existing hand-rolled client, then flip GCS capability advertisement honestly.

Purpose: Deliver RESUMABLE-05 and the adapter half of RESUMABLE-08 without introducing a second transport stack or leaking provider-specific failure noise into the public contract.
Output: Resumable-capable `Rindle.Storage.GCS`, extended `Rindle.Storage.GCS.Client`, and focused adapter/client regression coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-resumable-adapter-behaviour-broker-wiring/39-CONTEXT.md
@.planning/phases/39-resumable-adapter-behaviour-broker-wiring/39-RESEARCH.md
@.planning/phases/39-resumable-adapter-behaviour-broker-wiring/39-PATTERNS.md
@.planning/phases/39-resumable-adapter-behaviour-broker-wiring/39-01-PLAN.md
@.planning/research/v1.6-CANDIDATE-GCS.md
@lib/rindle/storage.ex
@lib/rindle/storage/gcs.ex
@lib/rindle/storage/gcs/client.ex
@lib/rindle/domain/media_upload_session.ex
@test/rindle/storage/gcs/client_test.exs
@test/rindle/storage/gcs_test.exs

<interfaces>
From `.planning/research/v1.6-CANDIDATE-GCS.md`:

```elixir
@callback initiate_resumable_upload(key :: String.t(), expected_size :: pos_integer() | nil, opts :: keyword()) ::
          {:ok, resumable_init_result()} | {:error, term()}

@callback resumable_upload_status(key :: String.t(), session_uri :: String.t(), opts :: keyword()) ::
          {:ok, resumable_status_result()} | {:error, term()}

@callback cancel_resumable_upload(key :: String.t(), session_uri :: String.t(), opts :: keyword()) ::
          {:ok, %{cancelled: boolean()}} | {:error, term()}

@callback verify_resumable_completion(key :: String.t(), session_uri :: String.t(), opts :: keyword()) ::
          {:ok, head_result()} | {:error, term()}
```

From `lib/rindle/storage/gcs/client.ex`:

```elixir
defp url_for(:upload, bucket, _key, opts) do
  "#{base_url(opts)}/upload/storage/v1/b/#{bucket}/o?uploadType=multipart"
end

defp authed_headers(opts) do
  ...
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the hand-rolled GCS client with resumable protocol helpers per D-12..D-16</name>
  <files>lib/rindle/storage/gcs/client.ex, test/rindle/storage/gcs/client_test.exs</files>
  <behavior>
    - Client initiates resumable uploads via the JSON API `uploadType=resumable` path, not a new SDK, per D-12.
    - Status parsing treats 308 Resume Incomplete and `Range` as the canonical committed-byte signal, returning `committed_bytes` plus `:in_progress | :complete | :expired`.
    - 404 maps to `:session_uri_unknown`, 410 maps to `:session_uri_expired`, offset disagreements map to `{:offset_mismatch, %{server: _, client: _}}`, and all other non-2xx responses preserve the existing `{:gcs_http_error, %{status, body}}` envelope per D-12 and D-19.
    - Any region-pin hint is returned as advisory metadata such as `region_hint` or `advisories`; it is not surfaced as `{:error, :region_pinned_initiation}` per D-13..D-16.
    - Session URIs never appear in inspect output, logs, or test expectation strings beyond direct request wiring.
  </behavior>
  <action>
Edit `lib/rindle/storage/gcs/client.ex` to add resumable helpers that reuse the existing `url_for/4`, `authed_headers/1`, `finch_request/2`, and error-envelope style instead of bypassing the client.

Implement helpers for:
- resumable initiation against `.../upload/storage/v1/b/$BUCKET/o?uploadType=resumable`
- resumable status against the session URI
- resumable cancel against the session URI
- adapter-level resumable completion verification returning `head_result`

Locked rules to preserve:
- use the existing hand-rolled Finch path only; do not add `google_api_storage` or a second transport stack
- keep session-URI failure mapping exact: `404 -> :session_uri_unknown`, `410 -> :session_uri_expired`, `308 + Range -> status result`
- if the caller provides a client offset for a status or verification check, compare it to the server-committed offset and return `{:error, {:offset_mismatch, %{server: server, client: client}}}` on disagreement
- if initiation can infer region pinning from headers/metadata, surface it as non-fatal metadata only; do not return `:region_pinned_initiation`

Extend `test/rindle/storage/gcs/client_test.exs` with Bypass-backed cases for initiation, 308 status parsing, 404/410 mapping, cancel success/idempotent shapes, offset mismatch, and any advisory region-hint extraction. Keep the tests on the existing `:base_url` seam so protocol behavior remains isolated from the live bucket.
  </action>
  <verify>
    <automated>mix test test/rindle/storage/gcs/client_test.exs -x</automated>
  </verify>
  <done>
    The internal GCS client can mint, inspect, cancel, and adapter-verify resumable uploads through the existing Finch path while returning only the locked public error vocabulary.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire resumable callbacks into Rindle.Storage.GCS and flip capabilities honestly per D-17..D-19</name>
  <files>lib/rindle/storage/gcs.ex, test/rindle/storage/gcs_test.exs</files>
  <behavior>
    - `Rindle.Storage.GCS` delegates all four resumable callbacks through the same bucket/goth/finch injection path used by `store/3` and `head/2`.
    - `capabilities/0` becomes exactly `[:signed_url, :head, :resumable_upload, :resumable_upload_session]` per D-17.
    - S3 and Local are untouched and remain non-resumable per D-18.
    - GCS tests are rewritten from "unsupported resumable" assertions to "implemented resumable" assertions without changing the unsupported multipart stance.
  </behavior>
  <action>
Edit `lib/rindle/storage/gcs.ex` so the module becomes the one honest resumable adapter in the repo.

Specifically:
- replace the Phase 37 unsupported stubs for the four resumable callbacks with thin delegators into `Client`
- preserve `bucket/1`, `ensure_goth_loaded/0`, and `inject_credentials/1` as the shared preflight path
- update the moduledoc capability section to the exact Phase 39 list
- do not add any resumable code to `Rindle.Storage.S3` or `Rindle.Storage.Local`

Rewrite `test/rindle/storage/gcs_test.exs` so always-on coverage proves:
- GCS capabilities equal the exact four-atom list
- multipart remains unsupported on GCS
- resumable callbacks return `:missing_bucket` when config is absent
- resumable protocol errors map to the locked atoms/tuples

Do not add real-bucket end-to-end PATCH proof in this task. That belongs in Plan 39-04 after the broker surface exists.
  </action>
  <verify>
    <automated>mix test test/rindle/storage/gcs/client_test.exs test/rindle/storage/gcs_test.exs -x</automated>
  </verify>
  <done>
    `Rindle.Storage.GCS` is resumable-capable and advertises it honestly, while unit tests pin the transport/error contract without touching the live bucket yet.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter client -> GCS resumable session URI | Bearer session URIs cross the provider boundary and must not leak into logs, telemetry, or public error strings. |
| GCS HTTP responses -> public adapter contract | Provider-specific statuses and headers must be normalized into stable public tuples/atoms. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-04 | I | `lib/rindle/storage/gcs/client.ex` | mitigate | Keep session URI handling inside the client and forbid public error/log surfaces from echoing the raw URI. |
| T-39-05 | T | resumable status mapping | mitigate | Add Bypass tests for 308/404/410/offset mismatch so status-code drift or header parsing bugs fail fast. |
| T-39-06 | R | `lib/rindle/storage/gcs.ex` | mitigate | Make GCS capability advertisement contingent on the real callback implementation in the same plan. |
</threat_model>

<verification>
Run `mix test test/rindle/storage/gcs/client_test.exs test/rindle/storage/gcs_test.exs -x`.
</verification>

<success_criteria>
GCS implements the resumable adapter surface through the existing client path, advertises resumable capabilities honestly, and returns only the locked public resumable failures.
</success_criteria>

<output>
After completion, create `.planning/phases/39-resumable-adapter-behaviour-broker-wiring/39-02-SUMMARY.md`.
</output>

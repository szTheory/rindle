---
id: 39-04
plan_number: 04
plan: 04
phase: 39
type: execute
wave: 4
depends_on: [39-02, 39-03]
files_modified:
  - test/rindle/storage/gcs_test.exs
  - test/rindle/storage/storage_adapter_test.exs
  - test/rindle/upload/broker_test.exs
  - lib/rindle/storage/gcs/client.ex
  - lib/rindle/upload/broker.ex
autonomous: true
requirements: [RESUMABLE-08]
must_haves:
  truths:
    - "A real GCS bucket proof streams a resumable upload through the broker-owned session lifecycle and converges through verify_completion/2."
    - "Every locked public resumable error atom or tuple is exercised by a real adapter path or a contract test tied to the implemented GCS/broker seams."
    - "Unsupported adapters remain resumable-honest after the broker and GCS changes are integrated."
  artifacts:
    - path: "test/rindle/storage/gcs_test.exs"
      provides: "Secret-gated real-bucket resumable proof and adapter error-closure coverage"
      contains: "@tag :gcs"
    - path: "test/rindle/upload/broker_test.exs"
      provides: "Broker trust-gate and unsupported-adapter closure coverage after resumable wiring"
      contains: "verify_completion"
  key_links:
    - from: "test/rindle/storage/gcs_test.exs"
      to: "lib/rindle/upload/broker.ex"
      via: "real-bucket resumable session initiation, streamed PUTs, and final verify_completion/2 promotion"
      pattern: "verify_completion"
    - from: "test/rindle/storage/storage_adapter_test.exs"
      to: "lib/rindle/storage/gcs.ex"
      via: "final honest capability assertions across Local, S3, and GCS"
      pattern: "capabilities"
---

<objective>
Close Phase 39 with the live-bucket proof and the final public-contract error coverage.

Purpose: Prove the full resumable path against a real GCS bucket after the adapter and broker seams are in place, and leave the phase with no ambiguity about error vocabulary, unsupported-adapter behavior, or the single completion trust gate.
Output: Secret-gated real-bucket resumable coverage, contract-closure tests, and any minimal code fixes needed to satisfy the locked public contract.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-resumable-adapter-behaviour-broker-wiring/39-CONTEXT.md
@.planning/phases/39-resumable-adapter-behaviour-broker-wiring/39-RESEARCH.md
@.planning/phases/39-resumable-adapter-behaviour-broker-wiring/39-PATTERNS.md
@.planning/phases/39-resumable-adapter-behaviour-broker-wiring/39-02-PLAN.md
@.planning/phases/39-resumable-adapter-behaviour-broker-wiring/39-03-PLAN.md
@lib/rindle/storage/gcs.ex
@lib/rindle/storage/gcs/client.ex
@lib/rindle/upload/broker.ex
@lib/rindle/domain/media_upload_session.ex
@test/rindle/storage/gcs_test.exs
@test/rindle/storage/storage_adapter_test.exs
@test/rindle/upload/broker_test.exs

<interfaces>
From `.planning/research/v1.6-CANDIDATE-GCS.md` locked broker shape:

```elixir
@spec verify_completion(binary(), keyword()) ::
        {:ok, %{session: MediaUploadSession.t(), asset: MediaAsset.t()}}
        | {:error, term()}
```

Locked outward failures to close in tests:

```elixir
{:upload_unsupported, :resumable_upload}
{:upload_unsupported, :resumable_upload_session}
:session_uri_expired
:session_uri_unknown
{:offset_mismatch, %{server: integer(), client: integer()}}
{:gcs_http_error, %{status: integer(), body: binary() | nil}}
:goth_unconfigured
:missing_bucket
:storage_object_missing
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the secret-gated real-bucket resumable proof per D-20..D-22</name>
  <files>test/rindle/storage/gcs_test.exs, test/rindle/upload/broker_test.exs</files>
  <behavior>
    - A `@tag :gcs` proof mints a resumable session, streams chunked PUT requests through Finch to the session URI, then calls `verify_completion/2`.
    - The final promoted asset path proves broker completion still converges through `head/2`, not `verify_resumable_completion/3`, per D-20..D-22.
    - The live-bucket test updates the durable session row and asset row to the locked terminal states without leaking `session_uri` in failure output.
  </behavior>
  <action>
Extend the existing GCS secret-gated test surface to add one end-to-end resumable proof.

Use the same environment-gating discipline already present in `test/rindle/storage/gcs_test.exs`:
- `GOOGLE_APPLICATION_CREDENTIALS_JSON`
- `RINDLE_GCS_BUCKET`

The proof should:
1. Start Goth and Finch test instances.
2. Call the broker entrypoint to mint a resumable session.
3. Use Finch to send chunked PUT requests from a streamed body to the returned session URI.
4. Call `verify_completion/2`.
5. Assert the object is visible via the existing GCS `head/2` path and the asset/session rows reach the expected promoted states.

Keep the proof secret-gated and isolated from ordinary unit runs. If the cleanest place is `test/rindle/storage/gcs_test.exs`, keep it there; if a broker-specific helper case is necessary inside `test/rindle/upload/broker_test.exs`, keep the live-bucket setup shared and minimal. Do not add a new proof lane file unless the existing files become unmanageably large.
  </action>
  <verify>
    <automated>mix test test/rindle/storage/gcs_test.exs --only gcs -x</automated>
  </verify>
  <done>
    A real GCS bucket proves the resumable session lifecycle end to end, and the broker still promotes only after `verify_completion/2` observes the stored object through `head/2`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close the public resumable error vocabulary and unsupported-adapter contract</name>
  <files>test/rindle/storage/gcs_test.exs, test/rindle/storage/storage_adapter_test.exs, test/rindle/upload/broker_test.exs, lib/rindle/storage/gcs/client.ex, lib/rindle/upload/broker.ex</files>
  <behavior>
    - Every locked public resumable failure is exercised by an implemented adapter or broker path.
    - `:region_pinned_initiation` is not asserted as a returned error anywhere; any region-pin visibility remains telemetry/operator-only per D-13..D-16.
    - Final contract tests still prove Local and S3 do not advertise resumable atoms and broker entrypoints do not silently fall back.
    - If any closure fix is needed in the client or broker, it is minimal and preserves the earlier plan boundaries.
  </behavior>
  <action>
Finish Phase 39 by extending tests and only then making any small closure edits required in `lib/rindle/storage/gcs/client.ex` or `lib/rindle/upload/broker.ex`.

Required closure matrix:
- `:missing_bucket` from adapter preflight
- `:goth_unconfigured` from auth preflight
- `:session_uri_unknown` from real status/cancel path or Bypass-backed GCS adapter path
- `:session_uri_expired` from 410 or elapsed-expiry path
- `{:offset_mismatch, %{server: _, client: _}}` from status or completion mismatch
- `{:gcs_http_error, %{status: _, body: _}}` generic fallthrough
- `:storage_object_missing` from broker `verify_completion/2`
- `{:upload_unsupported, :resumable_upload}` and `{:upload_unsupported, :resumable_upload_session}` from unsupported adapters / non-resumable rows

Important constraint: the repo's current `Rindle.Error.t()` already leaves `reason: term()`. Do not turn this plan into a public exception redesign. Only touch `lib/rindle/error.ex` if executor later narrows that type elsewhere during implementation; otherwise the closure here is test-and-contract coverage, not a semver-risky error-surface rewrite.
  </action>
  <verify>
    <automated>mix test test/rindle/storage/storage_adapter_test.exs test/rindle/storage/gcs_test.exs test/rindle/upload/broker_test.exs -x && mix test test/rindle/storage/gcs_test.exs --only gcs -x</automated>
  </verify>
  <done>
    Phase 39 closes with a live-bucket proof and an explicit, fully exercised resumable error contract, with unsupported adapters still failing loudly and no second completion truth introduced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live bucket proof -> CI secrets/runtime | Real GCS credentials and bucket state are involved; the proof must stay gated and must not expose bearer session URIs. |
| final public tests -> shipped contract | Missing one outward error path or capability assertion would leave Phase 39 semantically ambiguous even if the happy path works. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-10 | I | live-bucket resumable proof | mitigate | Reuse the existing secret-gated `@tag :gcs` discipline and keep session URIs out of assertions, logs, and inspect output. |
| T-39-11 | T | public error vocabulary | mitigate | Build a closure matrix test for every locked outward failure before allowing any final code tweaks. |
| T-39-12 | R | unsupported-adapter behavior | mitigate | Re-run final cross-adapter capability tests after broker/GCS wiring so no silent fallback survives integration. |
</threat_model>

<verification>
Run `mix test test/rindle/storage/storage_adapter_test.exs test/rindle/storage/gcs_test.exs test/rindle/upload/broker_test.exs -x` and, when GCS secrets are present, `mix test test/rindle/storage/gcs_test.exs --only gcs -x`.
</verification>

<success_criteria>
The live GCS resumable path is proven end to end, every locked outward resumable failure is covered, and unsupported adapters remain explicitly unsupported after full integration.
</success_criteria>

<output>
After completion, create `.planning/phases/39-resumable-adapter-behaviour-broker-wiring/39-04-SUMMARY.md`.
</output>

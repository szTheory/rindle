---
id: 39-01
plan_number: 01
plan: 01
phase: 39
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/storage.ex
  - lib/rindle/storage/capabilities.ex
  - test/rindle/storage/storage_adapter_test.exs
autonomous: true
requirements: [RESUMABLE-04, RESUMABLE-07]
must_haves:
  truths:
    - "The public storage behaviour exposes the four resumable callbacks with the locked arities and result shapes."
    - "Capability docs and tests describe :resumable_upload and :resumable_upload_session as shipped broker-facing contracts, not future placeholders."
    - "Non-resumable adapters remain contractually honest: Local and S3 do not gain resumable support implicitly."
  artifacts:
    - path: "lib/rindle/storage.ex"
      provides: "Public resumable callback/typespec contract on the Storage behaviour"
      contains: "@callback initiate_resumable_upload"
    - path: "lib/rindle/storage/capabilities.ex"
      provides: "Capability vocabulary and broker-facing semantics for resumable atoms"
      contains: ":resumable_upload_session"
    - path: "test/rindle/storage/storage_adapter_test.exs"
      provides: "Cross-adapter contract assertions for resumable callbacks and capability honesty"
      contains: "resumable"
  key_links:
    - from: "lib/rindle/storage.ex"
      to: "lib/rindle/upload/broker.ex"
      via: "callback/result shapes that the broker consumes later in Phase 39"
      pattern: "resumable_(init|status)_result"
    - from: "test/rindle/storage/storage_adapter_test.exs"
      to: "lib/rindle/storage/capabilities.ex"
      via: "known capability and require_upload/2 assertions"
      pattern: "Capabilities\\.(known|require_upload)"
---

<objective>
Freeze the public resumable behaviour contract before any adapter or broker implementation lands.

Purpose: Implement RESUMABLE-04 first, and set the honest-capability guardrails for RESUMABLE-07 so later plans build against a stable contract instead of discovering it mid-implementation.
Output: Updated `Rindle.Storage` types/callbacks, refreshed capability semantics, and cross-adapter contract tests that pin the public resumable surface.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-resumable-adapter-behaviour-broker-wiring/39-CONTEXT.md
@.planning/phases/39-resumable-adapter-behaviour-broker-wiring/39-RESEARCH.md
@.planning/phases/39-resumable-adapter-behaviour-broker-wiring/39-PATTERNS.md
@.planning/phases/37-gcs-adapter-foundation/37-CONTEXT.md
@.planning/phases/38-resumable-persistence-fsm/38-CONTEXT.md
@.planning/research/v1.6-CANDIDATE-GCS.md
@lib/rindle/storage.ex
@lib/rindle/storage/capabilities.ex
@lib/rindle/storage/gcs.ex
@lib/rindle/storage/s3.ex
@lib/rindle/storage/local.ex
@test/rindle/storage/storage_adapter_test.exs

<interfaces>
From `.planning/research/v1.6-CANDIDATE-GCS.md`:

```elixir
@type resumable_init_result :: %{
        required(:session_uri) => String.t(),
        required(:upload_id) => String.t(),
        required(:expires_at) => DateTime.t(),
        optional(:region_hint) => String.t() | nil,
        optional(atom()) => term()
      }

@type resumable_status_result :: %{
        required(:committed_bytes) => non_neg_integer(),
        required(:state) => :in_progress | :complete | :expired,
        optional(atom()) => term()
      }
```

From `lib/rindle/storage/capabilities.ex`:

```elixir
@spec require_upload(module(), capability()) ::
        :ok | {:error, {:upload_unsupported, capability()}}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the resumable behaviour/typespec contract per D-01..D-04</name>
  <files>lib/rindle/storage.ex, lib/rindle/storage/capabilities.ex</files>
  <behavior>
    - `Rindle.Storage` defines `resumable_init_result` and `resumable_status_result` matching the locked candidate shapes.
    - `Rindle.Storage` exposes `initiate_resumable_upload/3`, `resumable_upload_status/3`, `cancel_resumable_upload/3`, and `verify_resumable_completion/3` as optional callbacks per D-01 and D-02.
    - Callback docs make the broker distinction explicit per D-03..D-07: `verify_resumable_completion/3` exists for adapter parity, but broker completion still converges through `head/2`.
    - `lib/rindle/storage/capabilities.ex` stops describing resumable atoms as future placeholders and instead documents the shipped broker-facing meaning of `:resumable_upload` and `:resumable_upload_session` per D-03.
  </behavior>
  <action>
Edit `lib/rindle/storage.ex` to add the two resumable result types and four callbacks from the locked candidate, using the repo's existing `@callback ... :: {:ok, ...} | {:error, term()}` style. Mark them as optional callbacks, not required callbacks, per D-01 and D-02.

In the callback docs, explicitly state all three of these constraints:
- `initiate_resumable_upload/3` and `verify_resumable_completion/3` are the minimum adapter surface behind `:resumable_upload` per D-03.
- `resumable_upload_status/3` and `cancel_resumable_upload/3` are the additional operational surface behind `:resumable_upload_session` per D-03.
- `verify_resumable_completion/3` must not redefine broker trust; `Rindle.Upload.Broker.verify_completion/2` remains `head/2`-based per D-04..D-07.

Edit `lib/rindle/storage/capabilities.ex` only for vocabulary/docs. Do not change `@known`, `safe/1`, or `require_upload/2` behavior. The contract shift in this plan is semantic and documentary, not algorithmic.
  </action>
  <verify>
    <automated>mix test test/rindle/storage/storage_adapter_test.exs -x</automated>
  </verify>
  <done>
    The storage behaviour advertises the resumable contract with the locked arities and result shapes, and the capability docs no longer frame resumable atoms as merely reserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze cross-adapter contract tests for resumable honesty per D-17..D-19</name>
  <files>test/rindle/storage/storage_adapter_test.exs</files>
  <behavior>
    - Contract tests assert the four resumable callbacks are visible on the behaviour.
    - Contract tests assert Local and S3 remain non-resumable and `Capabilities.require_upload/2` returns tagged unsupported errors for resumable capabilities.
    - Contract tests do not prematurely require GCS resumable capability advertisement; that flip happens only when the real callbacks land in Plan 39-02.
  </behavior>
  <action>
Extend `test/rindle/storage/storage_adapter_test.exs` to pin the public contract without forcing implementation order.

Add assertions for:
- all four new resumable callback names/arity pairs exist on `Rindle.Storage.behaviour_info(:callbacks)`
- `Capabilities.known/0` still contains both resumable atoms
- `Capabilities.require_upload(Rindle.Storage.Local, :resumable_upload)` returns `{:error, {:upload_unsupported, :resumable_upload}}`
- `Capabilities.require_upload(Rindle.Storage.Local, :resumable_upload_session)` returns `{:error, {:upload_unsupported, :resumable_upload_session}}`
- same unsupported assertions for `Rindle.Storage.S3`

Do not change the existing GCS capability assertion in this plan; keep the GCS capability flip and its test rewrite coupled to the real callback implementation in Plan 39-02 so capability honesty is preserved.
  </action>
  <verify>
    <automated>mix test test/rindle/storage/storage_adapter_test.exs -x</automated>
  </verify>
  <done>
    Cross-adapter tests freeze the resumable callback surface and prove that unsupported adapters fail loudly instead of silently degrading.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public storage behaviour -> adapter implementations | A widened behaviour contract can mislead adapters or callers if capabilities and callback semantics drift apart. |
| capability helpers -> broker callers | Incorrect capability semantics would cause unsupported adapters to fall through into the wrong lifecycle path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | T | `lib/rindle/storage.ex` | mitigate | Freeze the resumable callback/result shapes with tests before GCS or broker changes land. |
| T-39-02 | E | `lib/rindle/storage/capabilities.ex` | mitigate | Keep `require_upload/2` behavior unchanged and add explicit unsupported-adapter assertions for resumable atoms. |
| T-39-03 | I | callback docs | mitigate | Document that broker completion remains `head/2`-based so no second completion truth leaks into the public API. |
</threat_model>

<verification>
Run `mix test test/rindle/storage/storage_adapter_test.exs -x`.
</verification>

<success_criteria>
The resumable behaviour contract exists, its broker-facing semantics are explicit, and contract tests prevent Local/S3 from being treated as resumable by accident.
</success_criteria>

<output>
After completion, create `.planning/phases/39-resumable-adapter-behaviour-broker-wiring/39-01-SUMMARY.md`.
</output>

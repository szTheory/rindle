---
id: 40-03
plan_number: 03
plan: 03
phase: 40
type: execute
wave: 3
depends_on: [40-01, 40-02]
files_modified:
  - lib/rindle/ops/runtime_status.ex
  - lib/mix/tasks/rindle.runtime_status.ex
  - test/rindle/ops/runtime_status_test.exs
  - test/rindle/runtime_status_task_test.exs
  - test/rindle/upload/broker_test.exs
autonomous: true
requirements: [RESUMABLE-10, RESUMABLE-11]
must_haves:
  truths:
    - "`runtime_status` keeps resumable visibility under `upload_sessions` and reports only bounded counters, never `session_uri` or raw provider details."
    - "Operators can see pending resumable sessions, expired resumable sessions, and stale retained session URIs through the existing text/JSON runtime-status surfaces."
    - "The existing secret-gated live GCS proof demonstrates both maintenance scenarios step-by-step: cancel path and expired-idempotent path, with runtime-status visibility before cleanup."
  artifacts:
    - path: "lib/rindle/ops/runtime_status.ex"
      provides: "Nested resumable summary under upload_sessions"
      contains: "resumable_sessions_pending"
    - path: "lib/mix/tasks/rindle.runtime_status.ex"
      provides: "Text/JSON rendering of resumable counters inside the existing upload_sessions section"
      contains: "Upload sessions:"
    - path: "test/rindle/ops/runtime_status_test.exs"
      provides: "Counter and redaction regression coverage for resumable runtime status"
      contains: "resumable_session_uris_stale"
    - path: "test/rindle/upload/broker_test.exs"
      provides: "Secret-gated live GCS maintenance proofs reusing the existing resumable harness"
      contains: "@tag :gcs"
  key_links:
    - from: "lib/rindle/ops/runtime_status.ex"
      to: "lib/mix/tasks/rindle.runtime_status.ex"
      via: "existing report-map-driven formatter path under upload_sessions"
      pattern: "upload_sessions"
    - from: "test/rindle/upload/broker_test.exs"
      to: "lib/rindle/ops/upload_maintenance.ex"
      via: "live maintenance scenarios calling abort, runtime_status, and cleanup in sequence"
      pattern: "abort_incomplete_uploads|cleanup_orphans"
---

<objective>
Close Phase 40 with operator visibility and live proof coverage.

Purpose: Deliver RESUMABLE-11 and the end-to-end proof obligations for RESUMABLE-10 by extending the existing `runtime_status` surfaces and the existing secret-gated GCS resumable proof file, without adding a new top-level resumable report or a new harness.
Output: Nested resumable counters under `upload_sessions`, task-output regression coverage, and live GCS maintenance proofs for cancel and expire cleanup scenarios.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-maintenance-cancel-contract/40-CONTEXT.md
@.planning/phases/40-maintenance-cancel-contract/40-RESEARCH.md
@.planning/phases/40-maintenance-cancel-contract/40-PATTERNS.md
@.planning/phases/39-resumable-adapter-behaviour-broker-wiring/39-04-SUMMARY.md
@lib/rindle/ops/runtime_status.ex
@lib/mix/tasks/rindle.runtime_status.ex
@test/rindle/ops/runtime_status_test.exs
@test/rindle/runtime_status_task_test.exs
@test/rindle/upload/broker_test.exs

<interfaces>
From `lib/rindle/ops/runtime_status.ex`:

```elixir
@type report :: %{
        generated_at: DateTime.t(),
        filters: filters(),
        runtime_checks: map(),
        assets: map(),
        variants: map(),
        upload_sessions: map(),
        provider_assets: map(),
        recommendations: [map()]
      }
```

From `lib/mix/tasks/rindle.runtime_status.ex`:

```elixir
format_section("upload_sessions", report.upload_sessions.counts)
format_upload_findings(report.upload_sessions.findings)
```

From `test/rindle/upload/broker_test.exs`:

```elixir
@tag :gcs
test "streams a resumable upload through the broker lifecycle and converges via verify_completion/2" do
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bounded resumable counters under `upload_sessions` and keep formatter output additive</name>
  <files>lib/rindle/ops/runtime_status.ex, lib/mix/tasks/rindle.runtime_status.ex, test/rindle/ops/runtime_status_test.exs, test/rindle/runtime_status_task_test.exs</files>
  <behavior>
    - `runtime_status/1` keeps resumable visibility under `upload_sessions`, not a new top-level section, per D-09.
    - The resumable summary carries exactly `resumable_sessions_pending`, `resumable_sessions_expired`, and `resumable_session_uris_stale` per D-10.
    - Text and JSON output remain report-map driven and never render `session_uri`, URI fragments, offsets, or provider headers per D-12.
    - Recommendations stay on the existing cleanup surface rather than inventing resumable-only verbs, per D-11 and D-16.
  </behavior>
  <action>
Edit `lib/rindle/ops/runtime_status.ex` and `lib/mix/tasks/rindle.runtime_status.ex` together to extend the existing upload-session report instead of creating a second resumable reporting path.

In `lib/rindle/ops/runtime_status.ex`:
- compute a nested resumable summary under `report.upload_sessions`
- implement exactly the three locked counters from D-10:
  - `resumable_sessions_pending`: resumable rows in `"signed"`, `"resuming"`, or `"uploading"`
  - `resumable_sessions_expired`: resumable rows with `session_uri_expires_at < now()`
  - `resumable_session_uris_stale`: resumable rows whose `session_uri_expires_at < now()` and `session_uri IS NOT NULL`
- keep any new recommendations on the existing cleanup/maintenance verbs; do not invent new actions or a second top-level report per D-11

In `lib/mix/tasks/rindle.runtime_status.ex`:
- keep section ordering unchanged
- render the resumable counters inside the existing `Upload sessions:` section, driven entirely by the report map
- ensure neither text nor JSON serialization can expose `session_uri` or partial fragments

Extend `test/rindle/ops/runtime_status_test.exs` and `test/rindle/runtime_status_task_test.exs` to prove:
- all three resumable counters appear with correct values
- profile/older-than filtering preserves the same top-level shape
- serialized text/JSON output does not contain `session_uri`
- upload-session section ordering still precedes provider findings and recommendations

Choose the exact nested placement deterministically inside `upload_sessions` and document it in assertions; per D-16, only escalate if the existing report shape makes the locked counters impossible without a public semver break.
  </action>
  <verify>
    <automated>mix test test/rindle/ops/runtime_status_test.exs test/rindle/runtime_status_task_test.exs</automated>
  </verify>
  <done>
    `runtime_status` exposes the three locked resumable counters under `upload_sessions`, the Mix task renders them in both text and JSON without adding a new report family, and no URI material leaks into output.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the existing live GCS proof with the two maintenance scenarios</name>
  <files>test/rindle/upload/broker_test.exs</files>
  <behavior>
    - The secret-gated live GCS proof reuses the existing resumable setup and adds exactly the two maintenance scenarios from D-13.
    - Each scenario asserts stepwise operator-visible state: initiation, abort/runtime-status visibility, then cleanup, per D-13 and D-14.
    - Rich branch/error permutations remain in local ExUnit tests rather than the live lane, per D-15.
  </behavior>
  <action>
Extend `test/rindle/upload/broker_test.exs` instead of creating a new harness, per the locked phase constraint and D-13 through D-15.

Add two `@tag :gcs` maintenance proofs that reuse the existing Goth/Finch/live-bucket setup from Phase 39:
1. initiate -> cancel/idempotent cancel -> runtime_status visible -> cleanup
2. initiate -> expire -> runtime_status visible -> cleanup, where the maintenance cancel path resolves through `:session_uri_expired`

For each scenario:
- create the resumable session through the broker
- drive the maintenance lane through `UploadMaintenance.abort_incomplete_uploads/1` and `UploadMaintenance.cleanup_orphans/1`, not through broker-facing cancel helpers
- assert the intermediate persisted row state and `runtime_status` counters before cleanup
- assert the final row removal after cleanup
- preserve redaction assertions so the live output never includes the raw `session_uri`

Keep the live lane thin: use existing local tests for taxonomy branches and avoid turning this task into a full GCS conformance matrix.
  </action>
  <verify>
    <automated>mix test test/rindle/upload/broker_test.exs test/rindle/ops/runtime_status_test.exs test/rindle/runtime_status_task_test.exs</automated>
  </verify>
  <done>
    The repo proves both maintenance scenarios end to end in the existing secret-gated GCS harness, with runtime-status visibility between abort and cleanup and no second proof harness introduced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime-status report -> CLI/JSON output | Operator-facing output must remain bounded and must never expose bearer session URIs or protocol internals. |
| live GCS proof -> real credentials and bucket state | Secret-gated proof touches a real resumable endpoint and must remain thin, deterministic, and redaction-safe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-08 | I | `runtime_status` output | mitigate | Keep resumable visibility to the three D-10 counters under `upload_sessions` and add negative assertions that `session_uri` never appears in text or JSON. |
| T-40-09 | R | operator recommendations | mitigate | Reuse existing cleanup recommendations per D-11 so the runtime report does not invent ambiguous new repair verbs. |
| T-40-10 | I | live GCS maintenance proof | mitigate | Reuse the Phase 39 secret-gated harness and preserve existing redaction assertions in `test/rindle/upload/broker_test.exs`. |
| T-40-11 | D | live proof scope | mitigate | Limit live coverage to the two D-13 scenarios and keep branch permutations in local ExUnit coverage per D-15. |
</threat_model>

<verification>
Run `mix test test/rindle/ops/runtime_status_test.exs test/rindle/runtime_status_task_test.exs test/rindle/upload/broker_test.exs`. When GCS secrets are present, re-run `mix test test/rindle/upload/broker_test.exs --only gcs`.
</verification>

<success_criteria>
Operators get bounded resumable counters inside the existing `upload_sessions` runtime-status surface, and the existing live GCS proof harness now demonstrates both maintenance scenarios step by step without leaking secret session details.
</success_criteria>

<output>
After completion, create `.planning/phases/40-maintenance-cancel-contract/40-03-SUMMARY.md`.
</output>

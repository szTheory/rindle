---
id: 40-02
plan_number: 02
plan: 02
phase: 40
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - lib/rindle/ops/upload_maintenance.ex
  - lib/rindle/workers/cleanup_orphans.ex
  - lib/mix/tasks/rindle.cleanup_orphans.ex
  - test/rindle/ops/upload_maintenance_test.exs
  - test/rindle/workers/maintenance_workers_test.exs
autonomous: true
requirements: [RESUMABLE-10]
must_haves:
  truths:
    - "Cleanup deletes resumable rows only after a local proof marker shows remote cancel already succeeded or was idempotently resolved."
    - "Expired resumable rows that still retain `session_uri` are skipped and surfaced as operator-visible drift instead of being silently deleted."
    - "Cleanup remains local-only; no remote resumable cancel happens in `cleanup_orphans/1`, the cleanup worker, or the cleanup Mix task."
  artifacts:
    - path: "lib/rindle/ops/upload_maintenance.ex"
      provides: "Resumable cleanup eligibility guards and additive cleanup drift reporting"
      contains: "session_uri"
    - path: "lib/rindle/workers/cleanup_orphans.ex"
      provides: "Worker reporting for resumable cleanup skips without adding remote side effects"
      contains: "storage_errors"
    - path: "lib/mix/tasks/rindle.cleanup_orphans.ex"
      provides: "Operator-facing cleanup report printing for proof-gated resumable skips"
      contains: "Sessions found"
    - path: "test/rindle/ops/upload_maintenance_test.exs"
      provides: "Proof-gated cleanup deletion coverage for resumable rows"
      contains: "cleanup_orphans"
  key_links:
    - from: "lib/rindle/ops/upload_maintenance.ex"
      to: "cleanup_orphans/1"
      via: "local proof check before delete_session_and_object/3"
      pattern: "session_uri == nil"
    - from: "lib/rindle/workers/cleanup_orphans.ex"
      to: "lib/mix/tasks/rindle.cleanup_orphans.ex"
      via: "shared additive report fields for skipped resumable cleanup"
      pattern: "storage_skipped|resumable"
---

<objective>
Make cleanup proof-gated and local-only for resumable sessions.

Purpose: Deliver the cleanup-half of RESUMABLE-10 by ensuring resumable rows are deleted only after local proof of remote cancel success, while surfacing proof-missing drift through the existing cleanup worker/task/report seams.
Output: Cleanup eligibility guards in `UploadMaintenance`, additive cleanup reporting, and worker/task tests proving resumable rows are skipped rather than silently deleted when proof is missing.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-maintenance-cancel-contract/40-CONTEXT.md
@.planning/phases/40-maintenance-cancel-contract/40-RESEARCH.md
@.planning/phases/40-maintenance-cancel-contract/40-PATTERNS.md
@.planning/phases/40-maintenance-cancel-contract/40-01-PLAN.md
@lib/rindle/ops/upload_maintenance.ex
@lib/rindle/workers/cleanup_orphans.ex
@lib/mix/tasks/rindle.cleanup_orphans.ex
@test/rindle/ops/upload_maintenance_test.exs
@test/rindle/workers/maintenance_workers_test.exs

<interfaces>
From `lib/rindle/ops/upload_maintenance.ex`:

```elixir
@type cleanup_report :: %{
        sessions_found: non_neg_integer(),
        sessions_deleted: non_neg_integer(),
        objects_deleted: non_neg_integer(),
        storage_errors: non_neg_integer(),
        storage_skipped: non_neg_integer()
      }
```

From `lib/rindle/workers/cleanup_orphans.ex`:

```elixir
handle_cleanup_result(
  UploadMaintenance.cleanup_orphans(cleanup_opts),
  dry_run?,
  storage_mod
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate resumable cleanup on the local cancel-proof marker</name>
  <files>lib/rindle/ops/upload_maintenance.ex, test/rindle/ops/upload_maintenance_test.exs</files>
  <behavior>
    - `cleanup_orphans/1` deletes resumable rows only when `state == "expired"` and the local proof marker shows cancel already resolved, per D-06 and D-07.
    - The preferred proof marker is a cleared `session_uri`; rows still carrying `session_uri` are counted and skipped rather than deleted per D-06.
    - Cleanup remains local-only and never issues provider cancel requests per D-05.
    - Proof-missing resumable rows become visible in the cleanup report through additive counters instead of hidden deletion.
  </behavior>
  <action>
Edit `lib/rindle/ops/upload_maintenance.ex` to make cleanup proof-gated for resumable rows after Plan 1 has established the proof marker semantics.

Required changes:
- add a narrow resumable eligibility helper used by `process_cleanup/3` or `delete_session_and_object/3`
- allow resumable deletion only when `session.upload_strategy == "resumable"`, `session.state == "expired"`, and the local proof marker is satisfied (preferred implementation: `session_uri == nil`) per D-06 and D-07
- if a resumable row is `"expired"` but still retains `session_uri`, skip deletion, increment an additive drift/skip counter in the cleanup report, and leave the row untouched for operator follow-up
- preserve current multipart/presigned cleanup behavior
- do not call `cancel_resumable_upload/3` or `Broker.cancel_resumable_session/2` anywhere in cleanup per D-05

Extend `test/rindle/ops/upload_maintenance_test.exs` with cases for:
- resumable `"expired"` rows with cleared `session_uri` being deleted
- resumable `"expired"` rows with retained `session_uri` being skipped and preserved
- non-resumable cleanup continuing to behave exactly as before
- dry-run output reflecting resumable skip counts without mutating rows

Do not add a new durable field or separate proof table in this phase; reuse the locked proof marker posture from D-06.
  </action>
  <verify>
    <automated>mix test test/rindle/ops/upload_maintenance_test.exs</automated>
  </verify>
  <done>
    Cleanup is resumable-aware but local-only: proof-marked rows are deleted, proof-missing rows are retained and counted, and no second remote-cancel path exists.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface cleanup proof-gating through the cleanup worker and Mix task</name>
  <files>lib/rindle/workers/cleanup_orphans.ex, lib/mix/tasks/rindle.cleanup_orphans.ex, test/rindle/workers/maintenance_workers_test.exs</files>
  <behavior>
    - The cleanup worker remains a thin orchestrator and does not add remote side effects, per D-05.
    - Worker logs/telemetry and Mix task output surface the additive resumable skip/drift count so operators can detect proof-missing rows.
    - Existing `[:rindle, :cleanup, :run]` telemetry remains worker-only per the locked telemetry boundary.
  </behavior>
  <action>
Update the cleanup worker and task surfaces to reflect the new proof-gated report shape from Task 1.

In `lib/rindle/workers/cleanup_orphans.ex`:
- keep the current `resolve_storage_adapter/1` and `handle_cleanup_result/3` structure
- add logging/telemetry fields for any new resumable skip counter introduced by Task 1
- do not alter the service/worker responsibility split or add resumable cancel calls

In `lib/mix/tasks/rindle.cleanup_orphans.ex`:
- print the additive resumable skip/drift counter in the existing cleanup report
- add a warning or explanatory line when proof-missing resumable rows were skipped so the operator action remains the same maintenance lane per D-11
- keep exit semantics tied to actual cleanup/storage errors, not mere drift visibility, unless the implementation discovers an existing operator contract that already treats skipped rows as non-zero

Extend `test/rindle/workers/maintenance_workers_test.exs` to verify:
- cleanup worker successfully completes while leaving proof-missing resumable rows in place
- worker output/telemetry includes the additive resumable skip counter
- no remote resumable cancel mock is expected from cleanup code

Keep the detailed skip/deletion matrix in `upload_maintenance_test.exs`; this task only closes the worker/task boundary.
  </action>
  <verify>
    <automated>mix test test/rindle/workers/maintenance_workers_test.exs test/rindle/ops/upload_maintenance_test.exs</automated>
  </verify>
  <done>
    The cleanup worker/task surfaces show operators when resumable rows were intentionally skipped for missing cancel proof, without widening cleanup into a second remote-side-effect lane.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cleanup service -> adopter database | Destructive deletion must respect the durable proof marker so cleanup cannot discard the only cancel handle. |
| cleanup worker/task -> operator surfaces | Drift reporting must expose actionable counts without leaking `session_uri` or turning skipped rows into silent success. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-05 | T | `cleanup_orphans/1` | mitigate | Require the D-06 proof marker before resumable deletion and add tests that retained `session_uri` blocks deletion. |
| T-40-06 | I | cleanup reporting | mitigate | Surface only additive counts/warnings for skipped resumable rows; never render `session_uri`, offsets, or raw provider details. |
| T-40-07 | E | cleanup ownership split | mitigate | Assert in worker/service tests that cleanup never calls remote resumable cancel and stays local-only per D-05. |
</threat_model>

<verification>
Run `mix test test/rindle/ops/upload_maintenance_test.exs test/rindle/workers/maintenance_workers_test.exs`.
</verification>

<success_criteria>
Cleanup deletes only proof-marked resumable rows, skips proof-missing rows visibly, and preserves the existing worker/task/telemetry ownership split without adding a second remote cancel path.
</success_criteria>

<output>
After completion, create `.planning/phases/40-maintenance-cancel-contract/40-02-SUMMARY.md`.
</output>

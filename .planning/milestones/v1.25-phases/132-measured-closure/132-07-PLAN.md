---
phase: 132-measured-closure
plan: 07
type: execute
wave: 7
depends_on: [132-06]
files_modified:
  - .planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
autonomous: true
gap_closure: true
requirements: [CI-14, COV-05, SAFE-02]
must_haves:
  truths:
    - "The final code-bearing remediation subject passes the helper contract, exact CI topology/summary contracts, quality signals, SAFE-01, automation-first policy, repository hygiene, authoritative coverage, and packed image consumer per D-01 and D-06."
    - "Authoritative one-run coverage remains at or above the inclusive 82.13% floor without percentage-only tests per D-07."
    - "The preservation receipt records one full immutable subject SHA and a bounded diff proving only the approved helper, test, and timing-evidence surfaces changed."
    - statement: "SAFE-02 adjacency backstop: exactly 82.13% coverage passes; equality at the later 480-second median or 600-second p95 also passes."
      verification: backstop
    - statement: "SAFE-02 ordering backstop: preservation authorities run only after the final source edit and before live sampling."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT add tests for the purpose of increasing the coverage percentage."
      category: values
      status: unresolved
      verification: null
      flagged_unverified: true
    - statement: "MUST NOT describe product, API, data, telemetry/error, dependency, Admin, or release drift as behavior-preserving."
      category: transparency
      status: unresolved
      verification: null
      flagged_unverified: true
  artifacts:
    - path: ".planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md"
      provides: "Final gap-closure subject identity, command evidence, exact coverage arithmetic, and prohibited-surface diff census"
      contains: "82.13"
  key_links:
    - from: "cover/excoveralls.json"
      to: ".planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md"
      via: "covered/relevant entries and inclusive integer threshold arithmetic"
      pattern: "covered.*relevant.*82.13"
    - from: "scripts/maintainer/refactor_contract.sh"
      to: ".planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md"
      via: "SAFE-01 output is captured against the preserved SHA"
      pattern: "refactor_contract"
---

<objective>
Bind the apt remediation to an immutable subject and re-prove every behavior-preserving authority before spending live timing samples.

Purpose: Make COV-05 and SAFE-02 executable final-subject postconditions and prevent a timing improvement from concealing quality or proof drift per D-01, D-06, and D-07.
Output: An appended final gap-closure preservation census in `132-PRESERVATION-RECEIPT.md`.
</objective>

## Artifacts this phase produces

- A `Gap-closure final preservation census` section in `132-PRESERVATION-RECEIPT.md` with the full preserved-subject SHA, exact bounded diff, command/exit evidence, covered and relevant line counts, exact percentage, inclusive integer comparison, and packed-consumer result. No new script, module, function, CLI flag, dependency, or product symbol is created.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/132-measured-closure/132-CONTEXT.md
@.planning/phases/132-measured-closure/132-VERIFICATION.md
@.planning/phases/132-measured-closure/132-06-SUMMARY.md
@.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
@RUNNING.md
@.github/workflows/ci.yml
@scripts/ci/install_apt_packages.sh
@test/install_smoke/ci_cache_hygiene_test.exs
@scripts/maintainer/refactor_contract.sh
@scripts/install_smoke.sh
</context>

<assumptions>
- The CI-14 unclassified probe remains open after local preservation; only Plan 132-08's comparable live sample can resolve it.
- The COV-05 unclassified probe is resolved by the authoritative `mix coveralls.multiple --type local --type json` output and inclusive integer arithmetic, not by recalled counts.
- SAFE-02 empty-input and concurrency behavior is covered by Plan 132-06's helper and controller contracts; this plan resolves final-subject ordering and threshold adjacency through executable authorities.
- Prohibition items remain descriptor-less and flagged-unverified; no fabricated check descriptor is added.
- Evidence-only receipt updates are reversible and introduce no one-way decision.
</assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Re-census the immutable remediation subject</name>
  <reversibility rating="reversible">The task records reproducible evidence in one planning artifact and does not alter a published or persisted contract.</reversibility>
  <files>.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md</files>
  <read_first>
    - AGENTS.md
    - RUNNING.md
    - .planning/phases/132-measured-closure/132-CONTEXT.md
    - .planning/phases/132-measured-closure/132-VERIFICATION.md
    - .planning/phases/132-measured-closure/132-06-SUMMARY.md
    - .planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
    - .github/workflows/ci.yml
    - scripts/ci/install_apt_packages.sh
    - test/install_smoke/ci_cache_hygiene_test.exs
    - test/install_smoke/ci_lane_split_test.exs
    - test/install_smoke/ci_observability_test.exs
    - scripts/ci/test_ci_summary_gate.sh
    - scripts/maintainer/refactor_contract.sh
    - scripts/install_smoke.sh
  </read_first>
  <action>
Record the full HEAD SHA after Plan 132-06 as the preserved gap-closure subject. Run the helper/cache contract together with CI lane-split and observability contracts, then the summary gate, `mix quality_signals`, SAFE-01, automation-first contract, CI-mode repository hygiene, one authoritative `mix coveralls.multiple --type local --type json`, and the complete packed image consumer in that order. Derive covered/relevant counts from the fresh non-empty JSON artifact and use integer arithmetic for the inclusive 82.13% comparison per D-07; do not repeat coverage. Append commands, observed exit statuses/counts, subject SHA, and a bounded name-only diff from the Plan 132-05 measured head through the subject. Explicitly census workflow/topology, Admin, public API, schema/migration, telemetry/error, dependency/lockfile, and release-proof paths per D-01 and D-06. Update the receipt only with observed output; do not invent counts or modify any implementation/workflow file in this task.
  </action>
  <acceptance_criteria>
    - Every listed authority exits zero after the final source edit and before live sampling.
    - Fresh authoritative coverage is recorded from one invocation with covered/relevant counts, exact percentage, and a passing inclusive 82.13% integer comparison.
    - `CI Summary` remains the sole required check with exact job membership and skip-as-pass semantics; the helper correction changes no `ci.yml` topology or job proof.
    - The packed image consumer proves built-package provenance, generation/patch/dependency/compile ordering, migrations, boot, lifecycle, and cleanup.
    - The receipt records a full preserved-subject SHA and an exact diff showing no product/API, schema/migration, telemetry/error, dependency-set, Admin, or release-proof drift.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/install_smoke/ci_cache_hygiene_test.exs test/install_smoke/ci_lane_split_test.exs test/install_smoke/ci_observability_test.exs --seed 0 &amp;&amp; bash scripts/ci/test_ci_summary_gate.sh &amp;&amp; mix quality_signals &amp;&amp; bash scripts/maintainer/refactor_contract.sh &amp;&amp; ./scripts/maintainer/automation_first_contract.sh &amp;&amp; ./scripts/maintainer/repo_hygiene_check.sh --ci &amp;&amp; mix coveralls.multiple --type local --type json &amp;&amp; test -s cover/excoveralls.json &amp;&amp; bash scripts/install_smoke.sh image &amp;&amp; test -s .planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md</automated>
  </verify>
  <done>The exact remediation SHA has fresh executable COV-05 and SAFE-02 evidence and is ready for one automated comparable CI-14 receipt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| implementation commit -> preservation receipt | Evidence must describe the final code-bearing SHA, not a prior or mixed subject. |
| coverage JSON -> threshold verdict | Missing, stale, or repeated coverage output must not satisfy COV-05. |
| built package -> generated consumer | Packed proof must use the unpacked artifact rather than repository source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-132-07-01 | Tampering | preservation subject identity | high | mitigate | Record full HEAD and exact diff before accepting command evidence. |
| T-132-07-02 | Repudiation | coverage arithmetic | medium | mitigate | Require a fresh non-empty artifact, record counts, and apply inclusive integer comparison once. |
| T-132-07-03 | Elevation of privilege | CI topology | high | mitigate | Run lane-split and summary-gate authorities and record the unchanged required-job set. |
| T-132-07-04 | Information disclosure | evidence logs | low | accept | Commands use existing local fixtures and record totals/paths only; no new secret or remote credential output is introduced. |
</threat_model>

<verification>
All preservation authorities must pass in the declared order against the same code-bearing subject. Verify the receipt's SHA and diff mechanically before Plan 132-08 publishes or samples it.
</verification>

<success_criteria>
- COV-05 passes from one fresh authoritative run at or above 82.13%.
- SAFE-02 passes focused, quality, SAFE-01, automation, hygiene, and packed-consumer authorities.
- The sole required gate, required-job set, skip semantics, and prohibited product/release surfaces remain intact.
</success_criteria>

<output>
Create `.planning/phases/132-measured-closure/132-07-SUMMARY.md` when done.
</output>

---
phase: 132-measured-closure
plan: 15
type: execute
wave: 15
depends_on: [132-14]
files_modified:
  - .planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
autonomous: true
gap_closure: true
requirements: [CI-14, COV-05, SAFE-02]
must_haves:
  truths:
    - "A new immutable preserved subject includes the completed Plan 132-14 interface repair, and a schema-v2 manifest records the complete three-stage chronology from the prior preserved subject."
    - "Stage 1 owns the completed Plan 132-12 controller/test delta, stage 2 owns the completed Plan 132-13 formatter delta, and stage 3 owns the separate Plan 132-14 repair controller/test delta plus explicit planning evidence."
    - "Formatter, controller/topology/summary tests, `mix quality_signals`, SAFE-01, automation-first, hygiene, one authoritative coverage run, and the packed image consumer are green on the repaired subject per D-01, D-06, and D-09."
    - "Fresh ExCoveralls evidence has a positive relevant-line denominator and satisfies the inclusive 82.13% floor without percentage-only tests per D-07."
    - "A mutation-free strict-ancestor or exact-head preflight consumes and validates the same manifest that terminal Plan 132-16 must use, with repository `szTheory/rindle`, PR `96`, exact-ten, two-sequence, and inclusive 480/600 identity."
  artifacts:
    - path: ".planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md"
      provides: "Post-repair preservation census and schema-v2 three-stage transition manifest"
      contains: "PRESERVATION_TRANSITION_V2_BEGIN"
  key_links:
    - from: "three historical plan anchors"
      to: "PRESERVATION_TRANSITION_V2 manifest"
      via: "exact contiguous Git name/status and target blob census"
      pattern: "controller_correction_sha|formatter_correction_sha|repair_sha"
    - from: "132-PRESERVATION-RECEIPT.md"
      to: "collect_pr_timing_receipt.sh preflight"
      via: "the required --transition-manifest interface implemented in Plan 132-14"
      pattern: "transition-manifest"
---

<objective>
Preserve and preflight the repaired immutable subject before terminal live sampling.

Purpose: Re-establish COV-05/SAFE-02 on the actual post-repair chronology and prevent sampling from outrunning the missing-interface correction.
Output: A fresh preservation census, schema-v2 transition manifest, and mutation-free successful preflight.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RUNNING.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/132-measured-closure/132-CONTEXT.md
@.planning/phases/132-measured-closure/132-12-PLAN.md
@.planning/phases/132-measured-closure/132-12-SUMMARY.md
@.planning/phases/132-measured-closure/132-13-PLAN.md
@.planning/phases/132-measured-closure/132-13-SUMMARY.md
@.planning/phases/132-measured-closure/132-14-SUMMARY.md
@.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
@.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
@scripts/ci/collect_pr_timing_receipt.sh
@test/install_smoke/ci_timing_automation_test.exs
@test/install_smoke/ci_lane_split_test.exs
@.github/workflows/ci.yml
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Re-census and preflight the post-repair immutable subject</name>
  <reversibility rating="reversible">This appends reproducible evidence to one planning receipt and performs an explicitly mutation-free preflight.</reversibility>
  <files>.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md</files>
  <read_first>
    - AGENTS.md
    - RUNNING.md
    - .planning/phases/132-measured-closure/132-CONTEXT.md
    - .planning/phases/132-measured-closure/132-12-SUMMARY.md
    - .planning/phases/132-measured-closure/132-13-SUMMARY.md
    - .planning/phases/132-measured-closure/132-14-SUMMARY.md
    - .planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
    - .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
    - scripts/ci/collect_pr_timing_receipt.sh
    - scripts/ci/test_ci_summary_gate.sh
    - scripts/maintainer/refactor_contract.sh
    - scripts/install_smoke.sh
    - test/install_smoke/ci_timing_automation_test.exs
    - test/install_smoke/ci_lane_split_test.exs
    - test/install_smoke/ci_observability_test.exs
    - .github/workflows/ci.yml
  </read_first>
  <action>
Derive all anchors from Git history rather than plan prose. `PRIOR_PRESERVED_SHA` is the last preserved-subject SHA already recorded before this census. `CONTROLLER_CORRECTION_SHA` is the completed Plan 132-12 boundary immediately before the first Plan 132-13 implementation commit; require it to include commits `9e70f4b` and `5a61ab9` and the completed 132-12 summary/tracking history. `FORMATTER_CORRECTION_SHA` is the completed Plan 132-13 tracking commit `214e56c...`; require commit `541b992` and the immutable 132-13 summary to be ancestors. `REPAIR_SHA` and `PRESERVED_SHA` are the current full HEAD after 132-14 completion and must be equal. Assert strict causal ancestry and do not rewrite completed Plans 132-12/13 or their summaries.

Build one schema-version-2 JSON object between exactly one `PRESERVATION_TRANSITION_V2_BEGIN` / `PRESERVATION_TRANSITION_V2_END` marker pair. Populate normalized repo `szTheory/rindle`, numeric PR `96`, the five anchors, and exactly the three contiguous stages required by Plan 132-14. Generate each stage's complete name/status set from Git with rename detection disabled, partition every entry explicitly into `.planning/**` and non-planning, and record the target blob OID for every added/modified non-planning target. Require stage 1's non-planning set to be exactly `scripts/ci/collect_pr_timing_receipt.sh` and `test/install_smoke/ci_timing_automation_test.exs`; stage 2 exactly `test/install_smoke/ci_lane_split_test.exs`; stage 3 exactly the controller/test pair from the dedicated repair. Reject any other source path, classification, status, missing blob, discontinuity, or SHA mismatch before appending evidence.

After the final source edit, run in order: repository formatter; focused controller/lane-split/observability tests; Bash summary gate; `mix quality_signals`; SAFE-01; automation-first contract; CI repo hygiene; exactly one `mix coveralls.multiple --type local --type json`; fail-closed ExCoveralls structural parsing and inclusive integer threshold; packed image consumer; prohibited-surface census; and `git diff --check`. Coverage parsing must require a fresh nonempty artifact, valid source coverage vectors, integer or null entries only, a positive relevant denominator, and `covered * 10000 >= relevant * 8213` per D-06/D-07. Confirm no workflow, required membership, public API, schema/migration, telemetry/error, dependency/lockfile, Admin, or release-proof drift per D-01/D-08/D-09.

Reparse the just-recorded manifest and independently compare every field to Git plus `gh pr view`. Snapshot PR head/labels, exact-head workflow IDs, receipt hash/current marker counts, and state/lock existence. Run `collect_pr_timing_receipt.sh preflight --no-publish` for repo `szTheory/rindle`, PR `96`, `ci.yml`, `CI Summary`, `ci-timing-sample`, ten samples, two sequences, 480/600 limits, `--correction-sha "$CONTROLLER_CORRECTION_SHA"`, `--preserved-subject-sha "$PRESERVED_SHA"`, and `--transition-manifest "$PRESERVATION"`. Require exact-head or strict-ancestor readiness, exit zero, and byte/identity equality for every snapshot. Any failure blocks Plan 132-16; no publication, label trigger, rerun, state creation, or sampling is permitted here.
  </action>
  <acceptance_criteria>
    - The preservation receipt contains exactly one valid schema-v2 manifest whose three contiguous stages reproduce actual Git history and separately attribute Plans 132-12, 132-13, and the Plan 132-14 repair.
    - All post-repair preservation authorities pass, including one authoritative positive-denominator coverage census at or above 82.13% and the packed image consumer.
    - Required topology, gate semantics, and prohibited product/release surfaces remain unchanged per D-01/D-08/D-09.
    - No-publish preflight validates the same manifest and exact CLI identity Plan 132-16 will consume, then proves remote/local observable state unchanged.
    - CI-14 remains open; this preservation receipt cannot substitute for the terminal API-backed exact-ten live receipt.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
mix format --check-formatted
mix test test/install_smoke/ci_timing_automation_test.exs test/install_smoke/ci_lane_split_test.exs test/install_smoke/ci_observability_test.exs --seed 0
bash scripts/ci/test_ci_summary_gate.sh
mix quality_signals
bash scripts/maintainer/refactor_contract.sh
./scripts/maintainer/automation_first_contract.sh
./scripts/maintainer/repo_hygiene_check.sh --ci
mix coveralls.multiple --type local --type json
test -s cover/excoveralls.json
bash scripts/install_smoke.sh image
git diff --check
PRESERVATION=.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
TRANSITION_JSON=$(awk '$0 == "PRESERVATION_TRANSITION_V2_BEGIN" {take=1; next} $0 == "PRESERVATION_TRANSITION_V2_END" {take=0} take' "$PRESERVATION")
test "$(grep -c '^PRESERVATION_TRANSITION_V2_BEGIN$' "$PRESERVATION")" -eq 1
test "$(grep -c '^PRESERVATION_TRANSITION_V2_END$' "$PRESERVATION")" -eq 1
jq -e '.schema_version == 2 and .repo == "szTheory/rindle" and .pr == 96 and (.stages | length) == 3 and .stages[0].id == "plan-132-12" and .stages[1].id == "plan-132-13" and .stages[2].id == "plan-132-14-repair" and .stages[0].to_sha == .stages[1].from_sha and .stages[1].to_sha == .stages[2].from_sha and .repair_sha == .preserved_subject_sha and all(.stages[].planning[]; .path | startswith(".planning/")) and all(.stages[].non_planning[]; ((.path | startswith(".planning/")) | not) and (.status == "A" or .status == "M") and (.blob_oid | test("^[0-9a-f]{40}$")))' &lt;&lt;&lt;"$TRANSITION_JSON" &gt;/dev/null
CONTROLLER_CORRECTION_SHA=$(jq -r .controller_correction_sha &lt;&lt;&lt;"$TRANSITION_JSON")
PRESERVED_SHA=$(jq -r .preserved_subject_sha &lt;&lt;&lt;"$TRANSITION_JSON")
test "$PRESERVED_SHA" = "$(git rev-parse HEAD)"
bash scripts/ci/collect_pr_timing_receipt.sh preflight --repo szTheory/rindle --pr 96 --workflow ci.yml --summary-job 'CI Summary' --label ci-timing-sample --samples 10 --max-sequences 2 --median-max 480 --p95-max 600 --correction-sha "$CONTROLLER_CORRECTION_SHA" --preserved-subject-sha "$PRESERVED_SHA" --transition-manifest "$PRESERVATION" --receipt .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md --state-dir .gsd/ci-timing/phase-132-final-preflight --no-publish</automated>
  </verify>
  <done>The repaired subject has fresh COV-05/SAFE-02 proof, a truthful three-stage manifest, and a successful mutation-free preflight authorizing only Plan 132-16's bounded sampler.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Git history -> preservation receipt | Recorded stages must reproduce actual immutable source chronology. |
| coverage JSON -> COV-05 | Generated counts decide preservation only after structural validation and integer comparison. |
| preflight -> GitHub | Read-only readiness checks must not become publication or sampling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-132-15-01 | Tampering | stage chronology | high | mitigate | Recompute three exact contiguous Git deltas and target blobs, including a distinct repair stage. |
| T-132-15-02 | Repudiation | coverage verdict | high | mitigate | Require one fresh artifact, valid vectors, positive denominator, and inclusive basis-point arithmetic. |
| T-132-15-03 | Elevation of privilege | preflight | high | mitigate | Snapshot all remote/local mutation surfaces and require exact equality after no-publish. |
| T-132-15-04 | Spoofing | repository/PR/subject | high | mitigate | Cross-check manifest, CLI, Git, and PR API identity. |
| T-132-15-05 | Information disclosure | credentials | low | accept | Record only public run and Git evidence; never persist tokens. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Re-prove repaired subject before exact-ten sampling | 132-15, 132-16 | COVERED | Serial dependency prevents premature sampling.
REQ | CI-14 | Preserved immutable subject ready for terminal receipt | 132-15, 132-16 | COVERED | Preflight is readiness only.
REQ | COV-05 | Coverage remains >=82.13% | 132-15 | COVERED | One fresh integer-backed authority.
REQ | SAFE-02 | Focused/full preservation and bounded drift | 132-15 | COVERED | All required automated authorities.
RESEARCH | — | Existing controller and repository-native proof | 132-15 | COVERED | No new integration.
CONTEXT | D-01/D-02/D-03 | Preserve gates and evidence policy | 132-15 | COVERED | No mutation or alternate exception.
CONTEXT | D-04/D-05 | Exact-ten and inclusive limits | 132-16 | COVERED | Terminal only.
CONTEXT | D-06/D-07 | Fresh preservation and coverage floor | 132-15 | COVERED | Direct task acceptance.
CONTEXT | D-08/D-09 | Exact topology exception only | 132-15 | COVERED | Source census rejects broader drift.
</source_audit>

<verification>
All preservation authorities run after the repair, and the final preflight must validate the exact same manifest without mutating any observed surface.
</verification>

<success_criteria>
- COV-05 and SAFE-02 are freshly proven on the repaired immutable subject.
- The transition manifest is chronologically and cryptographically truthful.
- Terminal sampling remains blocked until this plan completes.
</success_criteria>

<output>
Create `.planning/phases/132-measured-closure/132-15-SUMMARY.md` when done.
</output>

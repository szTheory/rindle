---
phase: 132-measured-closure
plan: 11
type: execute
wave: 11
depends_on: [132-10]
files_modified:
  - .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
autonomous: true
requirements: [CI-14, COV-05, SAFE-02]
must_haves:
  truths:
    - "One locked live controller produces one fresh current receipt containing exactly ten consecutive successful, non-cancelled, first-attempt `pull_request` runs from one immutable preserved head per D-04."
    - "Every receipt row is resolved through the Actions API to the same SHA, attempt 1, successful run, exactly one successful `CI Summary`, deterministic chronology, and non-overlap; a non-ten or partial set fails."
    - "Median uses sorted ranks 5 and 6, nearest-rank p95 uses rank 10, ties retain stable numeric ordering, and equality at 480 seconds or 600 seconds passes per D-05."
    - "CI-14 closes only when both inclusive thresholds pass; any identity, chronology, sequence, median, or p95 miss leaves an honest `gaps_found` verdict without human/UAT or external-runner substitution per D-03."
    - "The 132-10 immutable preservation subject remains the sampled subject, and post-sample topology/summary contracts confirm D-01, D-08, and D-09 still hold."
    - statement: "SAFE-02 concurrency backstop: collection is sequential, resumable after interruption, protected by one state lock, non-overlapping, and bounded to at most two complete sequence attempts."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT run the superseded 132-08 sampler, start a second controller, select around a slow success, mix heads, count a rerun/cancellation, or exceed two complete sequence attempts."
      category: safety
      status: unresolved
      verification: null
      flagged_unverified: true
    - statement: "MUST NOT weaken thresholds, required membership, job bodies, summary semantics, rerun policy, or exception policy in response to a miss."
      category: values
      status: unresolved
      verification: null
      flagged_unverified: true
    - statement: "MUST NOT replace API-backed evidence with local timing, projection, screenshots, human verification, UAT, or an unsupported external-runner exception."
      category: transparency
      status: unresolved
      verification: null
      flagged_unverified: true
  artifacts:
    - path: ".planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md"
      provides: "Recovery current table/source manifest, immutable head, exact ten API-backed runs, sorted values, statistics, integrity verdict, and CI-14 verdict"
      contains: "CI_TIMING_CURRENT_SOURCE_BEGIN"
    - path: ".planning/phases/132-measured-closure/132-11-PLAN.md"
      provides: "Executable locked live-sampler and honest-verdict plan"
      contains: "exactly ten"
    - path: ".planning/phases/132-measured-closure/132-VALIDATION.md"
      provides: "Recovery task-to-live-verifier mapping for 132-11"
      contains: "132-11-01"
    - path: ".planning/ROADMAP.md"
      provides: "Wave 11 final live acceptance ordering"
      contains: "132-11-PLAN.md"
  key_links:
    - from: ".planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md"
      to: "scripts/ci/collect_pr_timing_receipt.sh"
      via: "recorded correction and preserved-subject SHAs bind controller ancestry and allowed drift"
      pattern: "Recovery topology final preservation census"
    - from: "scripts/ci/collect_pr_timing_receipt.sh"
      to: ".planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md"
      via: "single locked run mode appends one current table and source manifest; verify mode re-resolves Actions evidence"
      pattern: "CI_TIMING_CURRENT_(TABLE|SOURCE)_BEGIN"
    - from: ".planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md"
      to: "scripts/ci/collect_pr_timing_receipt.sh"
      via: "API-backed verification recomputes exact-head chronology, table/manifest agreement, median, p95, and inclusive verdict"
      pattern: "median_seconds|p95_seconds"
---

<objective>
Run one fresh locked live sampler against the preserved D-08/D-09 topology subject and close CI-14 only on a fully verified passing receipt.

Purpose: Replace projection and partial-run evidence with the repository's only authorized acceptance source: ten sequential first-attempt successful PR runs on one immutable head.
Output: One API-backed recovery current receipt and an automated PASS or honest `gaps_found` verdict.
</objective>

## Artifacts this phase produces

- New `.planning/phases/132-measured-closure/132-11-PLAN.md`: the executable live-controller prompt, threat model, and source audit.
- Updated `.planning/ROADMAP.md` and `.planning/phases/132-measured-closure/132-VALIDATION.md`: Wave 11 final acceptance ordering and exact automated live-verifier mapping.
- Updated `.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md`: one appended `CI_TIMING_CURRENT_TABLE_*` section and one appended `CI_TIMING_CURRENT_SOURCE_*` manifest with the full immutable SHA, exactly ten source-linked runs, API identity/chronology evidence, sorted durations, rank calculations, threshold verdicts, and final PASS or `gaps_found`. All legacy failed receipts, the gap-closure causal census, partial-sequence anomaly exclusions, and historical prose remain unchanged. No controller, test, workflow, product symbol, API, dependency, or receipt statistic is created or modified by this plan.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RUNNING.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/132-measured-closure/132-CONTEXT.md
@.planning/phases/132-measured-closure/132-RESEARCH.md
@.planning/phases/132-measured-closure/132-10-SUMMARY.md
@.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
@.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
@.planning/phases/132-measured-closure/COVERAGE.md
@scripts/ci/collect_pr_timing_receipt.sh
@test/install_smoke/ci_timing_automation_test.exs
@test/install_smoke/ci_lane_split_test.exs
@test/install_smoke/ci_observability_test.exs
@scripts/ci/eval_ci_summary.sh
@scripts/ci/test_ci_summary_gate.sh
@.github/workflows/ci.yml
</context>

<assumptions>
- CI-14's unclassified generic probe is resolved only by the explicit live predicate in this task. COV-05 and SAFE-02 remain bound to 132-10's immutable subject and are rechecked at their focused CI boundary after sampling.
- SAFE-02 adjacency, empty, ordering, and concurrency are applicable as follows: 480/600 equality passes; anything other than exactly ten qualifying rows fails; percentile ordering is stable and numeric; sampling is locked, sequential, resumable, and non-overlapping.
- `gh` authentication, PR #96 being open, and the owned `ci-timing-sample` label existing are external runtime preconditions, not manual acceptance.
- One foreground invocation of the shipped controller is the sole live mutation authority. Its existing state lock, pending-trigger persistence, delayed-discovery adoption, publication-trigger quiescence, rate-limit backoff, label cleanup, and historical anomaly exclusions remain unchanged.
- No external-runner exception is planned. A slow valid result is included; a threshold miss is `gaps_found` unless D-03's separate job evidence, named owner, and dated follow-up are explicitly supplied in a later authorized plan.
- Receipt evidence is reversible; the live GitHub runs are durable external history but require no one-way decision checkpoint because the user explicitly authorized this bounded sampler.
</assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: Collect and independently judge the exact ten-run recovery receipt</name>
  <precondition>Plan 132-10 completed green; `gh auth status` succeeds for `szTheory/rindle`; PR #96 is open; its trigger label exists and is not already applied; the receipt has no current-section markers; and no live Phase 132 controller or state lock is active.</precondition>
  <reversibility rating="reversible">The owned label is cleaned on exit and the receipt append can be reverted; sampled workflow runs remain auditable GitHub history.</reversibility>
  <files>.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md</files>
  <read_first>
    - AGENTS.md
    - RUNNING.md
    - .planning/phases/132-measured-closure/132-CONTEXT.md
    - .planning/phases/132-measured-closure/132-RESEARCH.md
    - .planning/phases/132-measured-closure/132-10-SUMMARY.md
    - .planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
    - .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
    - .planning/phases/132-measured-closure/COVERAGE.md
    - scripts/ci/collect_pr_timing_receipt.sh
    - test/install_smoke/ci_timing_automation_test.exs
    - test/install_smoke/ci_lane_split_test.exs
    - test/install_smoke/ci_observability_test.exs
    - scripts/ci/eval_ci_summary.sh
    - scripts/ci/test_ci_summary_gate.sh
    - .github/workflows/ci.yml
  </read_first>
  <action>
First run the offline timing-controller, topology, observability, and Bash summary-gate contracts. Confirm the receipt contains zero current marker lines, derive the full correction SHA and preserved-subject SHA from the 132-10 receipt/summary, confirm no existing recovery state lock/process owns the same PR/head, and confirm the owned label is absent. Do not edit the controller or its tests in this plan. If an offline contract exposes a controller defect, stop and request a new TDD correction plan rather than patching while the sampler is in scope.

Launch exactly one foreground `collect_pr_timing_receipt.sh run` invocation for repository `szTheory/rindle`, PR `96`, workflow `ci.yml`, summary job `CI Summary`, owned label `ci-timing-sample`, ten samples, maximum two complete sequences, median maximum 480, p95 maximum 600, the recorded correction/preserved SHAs, a dedicated `.gsd/ci-timing/phase-132-recovery` state directory, and fast-forward-only head publication. Do not invoke 132-08, start another controller, relabel manually, rerun a workflow, or select around a slow successful run. Allow the existing lock, pending-trigger, delayed-discovery, quiescence, backoff, resume, and cleanup paths to operate. A failed/cancelled/non-attempt-1 sequence may consume one bounded sequence attempt but can never enter the receipt; a second complete-sequence failure exhausts the controller and remains honest failure evidence.

After a terminal controller result, require exactly one complete current table/source pair and use independent verify mode with the locked 10/480/600 parameters. If run mode returns PASS, verify mode must return zero. If run mode returns the documented timing-miss exit, independently verify that API identity, exact-head chronology, contiguity, non-overlap, table/manifest agreement, and recomputed statistics all passed before the verifier emitted only the inclusive threshold-miss diagnostic; record `gaps_found`, keep all ten honest rows, and do not weaken anything. Any other controller/verifier failure is an integrity failure and cannot complete the task. Re-run focused topology/observability and summary-gate contracts, confirm the owned label and lock are absent, and record PASS only when median is at most 480 and p95 at most 600; otherwise leave CI-14 and the phase open with `gaps_found`. No human/UAT acceptance or local/projection substitute is permitted.
  </action>
  <acceptance_criteria>
    - Exactly one controller invocation owns mutation and at most two full sequence attempts; final receipt acceptance contains exactly ten consecutive successful, non-cancelled, attempt-1 PR runs from one immutable head.
    - Every row and exactly one successful `CI Summary` per run are re-resolved through the Actions API; chronology is complete, deterministic, consecutive, and non-overlapping, with table/manifest equality.
    - Sorted ranks 5/6 and 10 are recomputed; equality at 480/600 passes, while any larger result produces honest `gaps_found` with the full receipt retained.
    - The controller's delayed-discovery, pending-trigger, lock, resume, anomaly exclusion, publication, and cleanup behavior remains unchanged; no label or lock remains after exit.
    - Focused topology, observability, summary membership, sole required check, `if: always()`, skip-as-pass, adopter prerequisites, job bodies, rerun policy, and the 132-10 preservation subject remain intact.
    - CI-14 closes only on both thresholds passing. COV-05 and SAFE-02 retain 132-10's automated evidence on the same subject regardless of timing verdict.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/install_smoke/ci_timing_automation_test.exs test/install_smoke/ci_lane_split_test.exs test/install_smoke/ci_observability_test.exs --seed 0 &amp;&amp; bash scripts/ci/test_ci_summary_gate.sh &amp;&amp; RECEIPT_SHA=$(awk '$0 == "CI_TIMING_CURRENT_SOURCE_BEGIN" {getline; print; exit}' .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md | jq -r .sha) &amp;&amp; STATE_FILE=".gsd/ci-timing/phase-132-recovery/pr-96-${RECEIPT_SHA}.json" &amp;&amp; jq -e '.status == "complete" and (.verdict == "PASS" or .verdict == "FAIL") and (.runs | length == 10)' "$STATE_FILE" &gt;/dev/null &amp;&amp; if [ "$(jq -r .verdict "$STATE_FILE")" = PASS ]; then bash scripts/ci/collect_pr_timing_receipt.sh verify --repo szTheory/rindle --workflow ci.yml --summary-job "CI Summary" --samples 10 --median-max 480 --p95-max 600 --receipt .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md; else VERIFY_LOG=$(mktemp); if bash scripts/ci/collect_pr_timing_receipt.sh verify --repo szTheory/rindle --workflow ci.yml --summary-job "CI Summary" --samples 10 --median-max 480 --p95-max 600 --receipt .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md &gt;"$VERIFY_LOG" 2&gt;&amp;1; then rm -f "$VERIFY_LOG"; exit 1; fi; grep -Fq "current receipt misses inclusive CI-14 timing thresholds" "$VERIFY_LOG"; RC=$?; rm -f "$VERIFY_LOG"; exit $RC; fi</automated>
  </verify>
  <done>One immutable-head exact-ten receipt is API-verified; both thresholds close CI-14, or any miss is retained and reported as `gaps_found` without gate weakening, reruns, mixed heads, or human acceptance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (ASVS L1; high severity blocks execution)

| Boundary | Description |
|----------|-------------|
| local controller -> GitHub PR | Authenticated publication/label mutation must remain single-owner, fast-forward-only, bounded, and cleaned up. |
| GitHub Actions API -> current receipt | Run/job identities, chronology, and durations cross from remote evidence into durable acceptance. |
| receipt statistics -> requirement verdict | Partial, reordered, manipulated, or threshold-missing evidence must never close CI-14. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-132-11-01 | Spoofing | run/head/summary identity | high | mitigate | Re-resolve every run and jobs payload; match full SHA, PR event, attempt, status, conclusion, timestamps, and exactly one successful Summary. |
| T-132-11-02 | Tampering | receipt rows and statistics | high | mitigate | Require one marker pair, exact table/manifest agreement, numeric sorting, rank recomputation, and inclusive locked thresholds. |
| T-132-11-03 | Repudiation | slow/failing sample exclusion | high | mitigate | Require the complete consecutive eligible exact-head slice; preserve bounded sequence errors and never cherry-pick a faster success. |
| T-132-11-04 | Elevation of privilege | controller mutation credentials | high | mitigate | One state lock, fast-forward ancestry, PR identity, owned-label checks, no token output, and cleanup traps guard mutation. |
| T-132-11-05 | Denial of service | interruption/rate limit/delayed trigger | medium | mitigate | Preserve pending-trigger persistence, delayed discovery, bounded backoff, resumable state, two-sequence ceiling, and sequential non-overlap. |
| T-132-11-06 | Information disclosure | receipt/state evidence | medium | mitigate | Persist public run metadata and controller state only; never print or store authenticated token material. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Replace failed measurement with fresh comparable ten-run receipt | 132-11 | COVERED | Single live API-backed authority.
REQ | CI-14 | Median at most 480 and p95 at most 600 | 132-11 | COVERED | Exact-ten independent verification.
REQ | COV-05 | Coverage remains at least 82.13% | 132-10, 132-11 | COVERED | Immutable preserved subject is sampled unchanged.
REQ | SAFE-02 | Focused/full preservation and bounded drift | 132-09, 132-10, 132-11 | COVERED | Post-sample focused backstop.
RESEARCH | — | Existing current-section controller is final authority | 132-11 | COVERED | Reused unchanged.
RESEARCH | — | Do not sample before topology/preservation gates | 132-09, 132-10, 132-11 | COVERED | Serial dependencies enforce order.
CONTEXT | D-01 | Sole required summary and skip behavior retained | 132-09, 132-11 | COVERED | Focused post-sample contracts.
CONTEXT | D-02 | Evidence-guided correction | 132-09 | COVERED | Causal correction precedes live test.
CONTEXT | D-03 | No unsupported external-runner exception | 132-11 | COVERED | Miss remains gaps_found.
CONTEXT | D-04 | Fresh ten sequential exact-head runs | 132-11 | COVERED | Direct controller contract.
CONTEXT | D-05 | Both inclusive thresholds | 132-11 | COVERED | Direct verifier contract.
CONTEXT | D-06 | Final preservation before sample | 132-10 | COVERED | Dependency gate.
CONTEXT | D-07 | Inclusive authoritative coverage | 132-10 | COVERED | Same sampled subject.
CONTEXT | D-08 | Exact six-edge graph | 132-09, 132-11 | COVERED | Sampled graph is source-checked.
CONTEXT | D-09 | All other topology/body/policy fixed | 132-09, 132-10, 132-11 | COVERED | Bounded diff and post-sample checks.
</source_audit>

<verification>
The live Actions API and the unchanged controller's independent verify mode are the only CI-14 acceptance authority. A threshold-specific verified miss is a successful evidence-collection task with a `gaps_found` phase verdict; any identity/integrity failure remains a task failure.
</verification>

<success_criteria>
- One locked controller yields exactly ten qualifying sequential runs on one immutable head.
- Median is &lt;=480 seconds and nearest-rank p95 is &lt;=600 seconds to close CI-14.
- Any miss remains machine-verifiable `gaps_found`; no human, exception, rerun, or gate change substitutes.
</success_criteria>

<output>
Create `.planning/phases/132-measured-closure/132-11-SUMMARY.md` when done, recording either CI-14 PASS or honest `gaps_found`.
</output>

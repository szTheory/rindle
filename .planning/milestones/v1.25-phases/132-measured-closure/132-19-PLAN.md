---
phase: 132-measured-closure
plan: 19
type: execute
wave: 19
depends_on: [132-18]
files_modified:
  - scripts/ci/collect_pr_timing_receipt.sh
  - test/install_smoke/ci_timing_automation_test.exs
autonomous: true
gap_closure: true
requirements: [CI-14, SAFE-02]
must_haves:
  truths:
    - "A persisted terminal `failed` controller state is immutable evidence: re-entry fails before receipt, label, run counter, state, or GitHub mutation, so the lifetime ceiling remains at most two sequences per D-04/D-05."
    - "`--max-sequences` accepts only 1 or 2 and rejects every integer above 2 before publication, label ownership, state creation, or receipt mutation."
    - "A resumed `running` state is accepted only when schema version, repository, PR, immutable SHA, label, sequence bound, attempt, run collection, pending trigger, and current-run identity agree with the invocation; missing, null, malformed, or foreign identity fails closed."
    - "A pending trigger has one persisted creation epoch and one absolute creation deadline across interruption/resume; permanent non-creation terminalizes within the configured bound without relabeling."
    - "Canonical eligible-run discovery consumes every Actions API page and deterministically orders equal start times by run ID before comparing the complete population to the receipt."
    - "Exactly touching samples are non-overlapping, zero through nine valid accumulated samples preserve insertion chronology, equal durations do not change rank statistics, and ten samples are required before receipt emission."
    - "D-01/D-08/D-09 remain intact: CI Summary membership, skip-as-pass behavior, the exact six scheduling-edge exception, job bodies, proof commands, runners, matrices, caches, partitions, and no-rerun policy do not change."
  artifacts:
    - path: "scripts/ci/collect_pr_timing_receipt.sh"
      provides: "Terminal-state, resume-schema, creation-deadline, and paginated-population fail-closed controller"
    - path: "test/install_smoke/ci_timing_automation_test.exs"
      provides: "Executable regressions for every verified controller defect and state-resume edge"
  key_links:
    - from: "persisted state status and identity"
      to: "controller run entry"
      via: "validate_running_state before any reset, trigger, or resume"
      pattern: "validate_running_state|status.*failed"
    - from: "pending_trigger.triggered_at_epoch"
      to: "wait_for_new_run"
      via: "absolute creation deadline retained across process interruption"
      pattern: "creation_timeout|triggered_at_epoch"
    - from: "GitHub Actions workflow-runs API"
      to: "canonical_eligible_run_ids"
      via: "paginated, slurped population normalized before stable ordering"
      pattern: "--paginate|--slurp|sort_by"
  prohibitions:
    - statement: "The timing controller must not silently cherry-pick faster successes or omit a later API page from the canonical population."
      status: unverified
      flagged: true
    - statement: "The timing controller must not turn terminal failure into fresh mutation authority or exceed the two-sequence safety budget."
      status: unverified
      flagged: true
    - statement: "The repair must not weaken, rename, bypass, or reinterpret required CI proof to manufacture a timing pass."
      status: unverified
      flagged: true
---

<objective>
Repair the four verified timing-controller defects through one production-quality offline tracer and its fail-closed expansions.

Purpose: Restore truthful bounded/no-rerun/complete-population authority before any new GitHub mutation.
Output: A hardened controller and executable regressions that prove terminal-state, resume, deadline, and pagination behavior.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RUNNING.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/132-measured-closure/132-CONTEXT.md
@.planning/phases/132-measured-closure/132-RESEARCH.md
@.planning/phases/132-measured-closure/132-VERIFICATION.md
@.planning/phases/132-measured-closure/132-18-SUMMARY.md
@scripts/ci/collect_pr_timing_receipt.sh
@test/install_smoke/ci_timing_automation_test.exs
@test/install_smoke/ci_lane_split_test.exs
@test/install_smoke/ci_observability_test.exs
@scripts/ci/test_ci_summary_gate.sh
@.github/workflows/ci.yml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Fail closed from invocation through terminal-state and running-state resume</name>
  <files>scripts/ci/collect_pr_timing_receipt.sh, test/install_smoke/ci_timing_automation_test.exs</files>
  <read_first>
    - .planning/phases/132-measured-closure/132-CONTEXT.md
    - .planning/phases/132-measured-closure/132-VERIFICATION.md
    - scripts/ci/collect_pr_timing_receipt.sh
    - test/install_smoke/ci_timing_automation_test.exs
  </read_first>
  <behavior>
    - "RED: `--max-sequences 3` exits nonzero before state, label, counter, receipt, or remote mutation; values 1 and 2 remain valid."
    - "RED: re-entering an existing `failed` state preserves its bytes and refuses to create a new sequence."
    - "RED: malformed JSON, unsupported schema, mismatched repo/PR/SHA/label/max-sequences, out-of-range sequence attempt, invalid run arrays, and incoherent pending/current-run combinations fail before resume mutation."
    - "GREEN: a valid schema-version-2 `running` state resumes its discovered run or owned pending trigger without a duplicate label trigger; an absent state initializes once."
    - "GREEN: INT/TERM cleanup releases only the owned label/lock while durable pending/current identity remains resumable."
  </behavior>
  <action>
Write failing fixture-driven ExUnit cases first, then harden the run entry point. Validate numeric controller limits before `gh auth`, manifest publication, label inspection, state-directory creation, or receipt mutation. Enforce `max_sequences` in the inclusive range 1..2 per D-04/D-05; retain the locked production invocation at 2.

Replace the branch that initializes over `failed` with an explicit terminal refusal. Initialize only when the SHA-scoped state file is absent. Add `validate_running_state` (or one equivalently named cohesive helper) and invoke it before using an existing running state. Validate schema version 2 and the existing identity model only: `repo`, numeric `pr`, 40-hex `sha`, `label`, `max_sequences`, `sequence_attempt`, `status`, `runs`, `errors`, `pending_trigger`, `current_run_id`, and `current_run_status`; require invocation equality for repo/PR/head/label/max-sequences and coherent nullable pending/current-run combinations. Do not generalize identity beyond this controller or add compatibility acceptance for malformed legacy state. Update existing resume fixtures to the valid schema-version-2 contract.

Keep the SHA-scoped lock and durable resume behavior. A clean interrupt may remove the owned label and lock, but it must not erase or synthesize pending/current trigger identity. Preserve completed-state API revalidation. Do not touch `.github/workflows/ci.yml`, topology tests, proof commands, rerun policy, thresholds, or receipt history per D-01/D-08/D-09.
  </action>
  <acceptance_criteria>
    - Terminal failed state cannot be reset or used to authorize any additional sequence.
    - Values above two for the sequence ceiling are rejected before any local or remote mutation.
    - Only a schema-valid invocation-matching running state can resume, and valid pending/current identities resume without duplicate triggers.
    - The controller still completes the fixture-backed ten-run happy path and completed-state live-API revalidation.
  </acceptance_criteria>
  <verify>
    <automated>mix format --check-formatted &amp;&amp; mix test test/install_smoke/ci_timing_automation_test.exs --seed 0 &amp;&amp; test ! -e .gsd/ci-timing/phase-132-gap-repair</automated>
  </verify>
  <done>The offline controller tracer proves a valid ten-run path while terminal failure, excessive sequence authority, and malformed/foreign resume state all fail before mutation.</done>
  <reversibility rating="reversible">The controller and fixture changes are isolated source edits that can be reverted without migrating persisted public data.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound delayed-run discovery and prove the complete paginated population</name>
  <files>scripts/ci/collect_pr_timing_receipt.sh, test/install_smoke/ci_timing_automation_test.exs</files>
  <read_first>
    - scripts/ci/collect_pr_timing_receipt.sh
    - test/install_smoke/ci_timing_automation_test.exs
    - scripts/ci/collect_ci_baseline.sh
  </read_first>
  <behavior>
    - "RED: a label trigger whose Actions run never appears reaches one creation deadline and records a bounded non-qualifying sequence result instead of polling forever."
    - "RED: resuming that pending trigger uses its original persisted epoch, never refreshes the deadline, and never reapplies the label."
    - "RED: a qualifying page-two run makes receipt population equality fail when it is not selected; a nonqualifying page-two run is filtered normally."
    - "GREEN: exactly touching samples are admitted as non-overlapping and equal-start eligible runs are ordered by `(run_started_at, id)`."
  </behavior>
  <action>
Extend the GH shim and controller tests first with deterministic controls for a permanently absent run and a second workflow-run page. Wire the already parsed `--creation-timeout` into `wait_for_new_run`: validate it as a non-negative integer, persist `pending_trigger.triggered_at_epoch` alongside the UTC display timestamp when label ownership is taken, and derive one absolute deadline from that stored epoch on every fresh or resumed poll. A creation timeout must return a distinct bounded failure to the sequence controller, clean the owned label, retain honest error evidence, and never relabel the same pending trigger.

Change only canonical eligible-population discovery to consume all GitHub Actions pages using the repository's `gh api --paginate --slurp` precedent, with authenticated rate-limit retry around the complete request. Normalize page arrays before filtering PR number, exact SHA, attempt 1, completed success, and population-boundary exclusions. Keep deterministic `sort_by(.run_started_at, .id)` ordering so equal timestamps are stable. Do not cap at the first 100 records or accept a selected subset of the eligible population.

Retain `current_start >= previous_end` as the explicit adjacency decision: samples that touch exactly do not overlap. Preserve accumulated run insertion order for zero through nine state entries, while the receipt remains exact-ten and rank statistics remain duration-sorted. Finish by running the focused controller suite and the topology/observability/summary authorities, proving D-01/D-08/D-09 were untouched.
  </action>
  <acceptance_criteria>
    - A never-created Actions run cannot keep a controller alive beyond the persisted creation deadline, including after restart.
    - Canonical population equality observes qualifying records on every API page and uses stable timestamp-plus-ID order.
    - Adjacency, empty/partial state, equal-order, and interruption semantics are executable and fail closed.
    - Required CI topology, skip-as-pass behavior, and no-rerun policy remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>mix format --check-formatted &amp;&amp; mix test test/install_smoke/ci_timing_automation_test.exs --seed 0 &amp;&amp; mix test test/install_smoke/ci_lane_split_test.exs test/install_smoke/ci_observability_test.exs --seed 0 &amp;&amp; bash scripts/ci/test_ci_summary_gate.sh &amp;&amp; ./scripts/maintainer/automation_first_contract.sh &amp;&amp; git diff --check</automated>
  </verify>
  <done>All four verification defects and the associated running-state resume risk have executable regressions, and the repaired controller remains bounded, complete-population, no-rerun, and topology-preserving.</done>
  <reversibility rating="reversible">The deadline, pagination, and fixture behavior can be reverted as a single controller/test change without an external migration.</reversibility>
</task>

</tasks>

<spec_less_resolution>
The repository contract resolves all six probe items into executable predicates rather than leaving them implicit:

- CI-14 unclassified: exact ten, one immutable PR head, attempt 1, success, complete API population, median <=480, p95 <=600.
- COV-05 unclassified: one authoritative coverage invocation, positive denominator, structural JSON parse, and integer floor `covered * 10000 >= relevant * 8213` in Plan 132-20.
- SAFE-02 adjacency: `current_start == previous_end` is accepted as non-overlap.
- SAFE-02 empty/null: absent state initializes once; malformed/null existing state fails; zero through nine valid runs may resume; receipt emission requires ten.
- SAFE-02 ordering: canonical population sorts by start time then run ID; equal durations do not affect rank results.
- SAFE-02 concurrency: one SHA-scoped lock owns mutation; interrupt preserves trigger/run identity; terminal failure cannot resume.
</spec_less_resolution>

<assumption_delta>
Decision: no-change. Historical uses of `fallback` and `optional` describe existing CI/package behavior and do not justify a broader controller identity model. This repair validates only the existing repository/PR/SHA/label/sequence state identity and adds the minimum pending-trigger epoch needed for a durable deadline.
</assumption_delta>

<prohibition_recall>
Descriptor-less flagged prohibitions are retained in `must_haves.prohibitions` because no SPEC prohibition section exists. Credential disclosure, command injection, and generic API abuse are canon security candidates; breadcrumb them to the Phase 132 STRIDE review / `$gsd-secure-phase` rather than minting duplicate prohibition descriptors.
</prohibition_recall>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| CLI/state file -> controller authority | Untrusted or stale persisted bytes can otherwise expand mutation authority. |
| GitHub API pages -> canonical population | Remote paginated records determine which runs may close CI-14. |
| label trigger -> Actions run | An asynchronous remote event may be delayed, duplicated, or never created. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-132-19-01 | Elevation of privilege | failed-state re-entry / max-sequences | high | mitigate | Refuse terminal state and enforce the 1..2 bound before all mutation; regression-prove byte stability. |
| T-132-19-02 | Spoofing | running-state identity | high | mitigate | Validate schema and repo/PR/SHA/label/sequence identity before resume. |
| T-132-19-03 | Denial of service | delayed trigger discovery | high | mitigate | Persist one trigger epoch and enforce an absolute creation deadline across restart. |
| T-132-19-04 | Tampering | canonical eligible population | high | mitigate | Fetch every API page, normalize, filter, stably sort, and require equality with selected IDs. |
| T-132-19-05 | Repudiation | interrupt/resume chronology | medium | mitigate | Persist pending/current identity and error history; retain the SHA-scoped owner lock. |
| T-132-19-06 | Information disclosure | GitHub credentials | medium | mitigate | Store only public run metadata; never echo or persist tokens. |
</threat_model>

<artifacts_this_phase_produces>
- New controller helper: `validate_running_state` (or equivalently named single owner).
- Existing CLI flag made authoritative: `--creation-timeout`.
- New durable state field: `pending_trigger.triggered_at_epoch`.
- Paginated canonical-population helper/path using `gh api --paginate --slurp`.
- GH fixture controls for permanent non-creation and page-two eligible/noneligible records.
- No new package, workflow, schema, frontend, public API, cache, runner, matrix, partition, or rerun surface.
</artifacts_this_phase_produces>

<verification>
The focused controller suite must exercise each gap without live mutation, then the lane-split, observability, CI Summary, automation-first, formatting, and diff authorities must all pass.
</verification>

<success_criteria>
- All four verifier defects have failing-first regressions and passing implementations.
- Running-state schema/identity, adjacency, empty/null, ordering, and interruption behavior are executable.
- No workflow or required-proof surface changes.
</success_criteria>

<output>
Create `.planning/phases/132-measured-closure/132-19-SUMMARY.md` when done, recording the exact source commit that contains both controller and regression repairs.
</output>

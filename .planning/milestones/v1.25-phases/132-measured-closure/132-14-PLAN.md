---
phase: 132-measured-closure
plan: 14
type: tdd
wave: 14
depends_on: [132-13]
files_modified:
  - scripts/ci/collect_pr_timing_receipt.sh
  - test/install_smoke/ci_timing_automation_test.exs
autonomous: true
gap_closure: true
requirements: [CI-14, SAFE-02]
must_haves:
  truths:
    - "`preflight` and `run` require `--transition-manifest PATH` and call one `validate_transition_manifest` authority before publication, label mutation, state creation, or receipt mutation."
    - "The manifest represents the actual three-stage chronology: prior preservation through completed Plan 132-12, completed Plan 132-12 through completed Plan 132-13, and completed Plan 132-13 through this repair."
    - "Every stage is contiguous, binds full from/to SHAs, and exactly enumerates Git name/status entries; non-planning targets also bind target blob OIDs while `.planning/**` entries remain explicit."
    - "The Plan 132-12 controller/test pair, Plan 132-13 formatter target, and this repair's controller/test pair are assigned to their truthful stages; executable drift cannot be hidden as planning evidence."
    - "Exact-head and strict-ancestor no-publish preflight remain mutation-free, while invalid schema, chronology, SHA, path, status, blob, or CLI/manifest identity fails closed per D-01, D-03, D-04, D-05, D-08, and D-09."
  artifacts:
    - path: "scripts/ci/collect_pr_timing_receipt.sh"
      provides: "Required transition-manifest option and fail-closed three-stage validator"
      contains: "validate_transition_manifest"
    - path: "test/install_smoke/ci_timing_automation_test.exs"
      provides: "Process regressions for valid chronology, tampering, and mutation-free rejection"
      contains: "transition manifest"
  key_links:
    - from: "--transition-manifest PATH"
      to: "validate_transition_manifest"
      via: "argument parsing followed by validation before GitHub mutation or controller state ownership"
      pattern: "transition-manifest|validate_transition_manifest"
    - from: "manifest stages"
      to: "git diff-tree and git rev-parse"
      via: "exact name/status, ancestry, continuity, and target blob comparisons"
      pattern: "diff-tree|rev-parse"
---

<objective>
Repair the controller contract omitted during completed Plan 132-12 without rewriting Plans 132-12 or 132-13.

Purpose: Make the preservation handoff executable and cryptographically truthful before any new preservation census or live sampling.
Output: A tested `--transition-manifest` interface and three-stage `validate_transition_manifest` authority.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RUNNING.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/132-measured-closure/132-CONTEXT.md
@.planning/phases/132-measured-closure/132-12-PLAN.md
@.planning/phases/132-measured-closure/132-12-SUMMARY.md
@.planning/phases/132-measured-closure/132-13-PLAN.md
@.planning/phases/132-measured-closure/132-13-SUMMARY.md
@scripts/ci/collect_pr_timing_receipt.sh
@test/install_smoke/ci_timing_automation_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED/GREEN the missing transition-manifest controller seam</name>
  <reversibility rating="reversible">This is an internal maintainer-controller input contract with process tests; reverting it restores the prior fail-closed inability to publish.</reversibility>
  <files>scripts/ci/collect_pr_timing_receipt.sh, test/install_smoke/ci_timing_automation_test.exs</files>
  <read_first>
    - AGENTS.md
    - RUNNING.md
    - .planning/phases/132-measured-closure/132-CONTEXT.md
    - .planning/phases/132-measured-closure/132-12-PLAN.md
    - .planning/phases/132-measured-closure/132-12-SUMMARY.md
    - .planning/phases/132-measured-closure/132-13-PLAN.md
    - .planning/phases/132-measured-closure/132-13-SUMMARY.md
    - scripts/ci/collect_pr_timing_receipt.sh
    - test/install_smoke/ci_timing_automation_test.exs
  </read_first>
  <behavior>
    - "RED: `preflight` and `run` reject a missing `--transition-manifest`; the current controller fails this regression because the option does not exist."
    - "GREEN: a marker-bounded schema-version-2 manifest with three contiguous stages validates when every SHA, path status, planning classification, and target blob matches Git."
    - "CHRONOLOGY: stage 1 begins at the prior preserved subject and ends at the completed Plan 132-12 anchor; stage 2 continues to the completed Plan 132-13 anchor; stage 3 continues to the completed Plan 132-14 repair anchor/preserved subject."
    - "IDENTITY: normalized repository, numeric PR, controller-correction SHA, formatter-correction SHA, repair SHA, and preserved-subject SHA agree across CLI arguments, manifest, Git, and `gh pr view`."
    - "FAIL CLOSED: duplicate/missing markers, malformed JSON, unsupported version, discontinuous or reversed stages, wrong stage count/order, SHA mismatch, extra/missing/renamed/deleted path, executable-as-planning classification, or blob mismatch exits nonzero before observable mutation."
    - "READ ONLY: every rejected preflight and successful `--no-publish` preflight preserves PR head/labels, workflow-run IDs, receipt bytes/marker counts, state and lock presence, and push/label counters."
  </behavior>
  <action>
Write the failing process fixtures first, then implement the missing interface promised by Plan 132-12. Add `--transition-manifest PATH` to usage and parsing and require it for `preflight` and `run`; `verify` remains bound to its current receipt/API authority and does not accept this publication credential. Add `validate_transition_manifest` and invoke it after resolving CLI SHAs, local HEAD, authenticated read-only PR identity, and required label existence but before fetch/push, label mutation, controller lock/state ownership, or receipt mutation.

Define schema version 2 as one JSON object between exactly one `PRESERVATION_TRANSITION_V2_BEGIN` and `PRESERVATION_TRANSITION_V2_END` pair. Required top-level fields are `schema_version: 2`, `repo`, numeric `pr`, `prior_preserved_sha`, `controller_correction_sha`, `formatter_correction_sha`, `repair_sha`, `preserved_subject_sha`, and exactly three ordered `stages`. Each stage has `id`, `from_sha`, `to_sha`, `non_planning`, and `planning`; each path entry has Git `status` and `path`, and every non-planning entry also has the target `blob_oid`. Stage IDs and boundaries are fixed as `plan-132-12` from prior preservation to the completed Plan 132-12 anchor, `plan-132-13` from that anchor to the completed Plan 132-13 anchor, and `plan-132-14-repair` from that anchor to the completed repair anchor. Require adjacent `to_sha`/`from_sha` equality, strict ancestry, top-level anchor equality, and final `repair_sha == preserved_subject_sha`. The existing `--correction-sha` binds `controller_correction_sha`; `--preserved-subject-sha` binds the final subject.

For each stage, derive the complete first-parent-independent Git tree delta with rename detection disabled and compare the normalized `status + path` multiset exactly to the union of declared planning and non-planning entries. Planning entries must start with `.planning/`; non-planning entries must not. Permit only added or modified targets because the authorized chronology contains no rename/delete; reject other statuses. Resolve every declared non-planning target at that stage's `to_sha` and require its full Git blob OID. Across all stages, require the only non-planning paths to be the Plan 132-12 controller/test pair, the Plan 132-13 lane-split formatter target, and this Plan 132-14 controller/test pair in their named stages. After the manifest's preserved subject, keep the existing planning-only rule but remove the stale executable allowlist: any non-planning delta from preserved subject to local HEAD fails.

Extend the existing temporary-repository and `gh`-shim fixtures to create all three historical stages and the post-manifest planning delta. Prove the valid strict-ancestor no-publish path and exact-head no-publish path both succeed without mutation. Add one mutation case for each schema, chronology, identity, classification, path/status, blob, and post-subject drift invariant. Preserve D-01/D-08/D-09 topology and D-03/D-04/D-05 evidence policy; do not edit workflow, product, dependency, migration, public API, telemetry/error, Admin, or release surfaces.
  </action>
  <acceptance_criteria>
    - `preflight` and `run` require `--transition-manifest`, and one validator executes before any GitHub mutation or durable controller ownership.
    - Schema v2 represents the complete three-stage post-preservation chronology and assigns the Plan 132-12 omission repair to its own final stage rather than presenting it as part of Plan 132-12.
    - Every declared stage equals its Git delta exactly, every executable target blob matches, planning classification is explicit, and no post-subject executable allowlist remains.
    - Valid exact-head and strict-ancestor no-publish fixtures pass; every tampered fixture fails with all snapshotted local/remote surfaces unchanged.
    - Focused controller tests, shell syntax, formatter, and automation-first policy pass without changing required CI topology or terminal timing authority.
  </acceptance_criteria>
  <verify>
    <automated>mix format --check-formatted test/install_smoke/ci_timing_automation_test.exs &amp;&amp; mix test test/install_smoke/ci_timing_automation_test.exs --seed 0 &amp;&amp; bash -n scripts/ci/collect_pr_timing_receipt.sh &amp;&amp; ./scripts/maintainer/automation_first_contract.sh &amp;&amp; git diff --check</automated>
  </verify>
  <done>The missing manifest interface is implemented test-first, validates the truthful three-stage chronology, and fails closed before mutation; Plans 132-12 and 132-13 remain unchanged historical evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| preservation manifest -> controller | Planning evidence authorizes publication only after it is reproduced from Git. |
| local Git -> GitHub PR | A validated immutable local subject may be fast-forward published only under the existing bounded controller. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-132-14-01 | Tampering | transition JSON | high | mitigate | Enforce unique markers, schema v2, exact three-stage Git deltas, anchor continuity, and target blob equality. |
| T-132-14-02 | Repudiation | historical repair attribution | high | mitigate | Give completed Plans 12, 13, and the omitted-interface repair separate contiguous stages. |
| T-132-14-03 | Elevation of privilege | publication path | high | mitigate | Validate before auth/mutation/state ownership and remove the stale executable allowlist. |
| T-132-14-04 | Spoofing | repository/PR/SHA identity | high | mitigate | Cross-check CLI, manifest, Git, and PR API identities. |
| T-132-14-05 | Information disclosure | credentials | low | accept | Tests persist no tokens; controller continues using authenticated public metadata only. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Restore executable preservation-to-sampling topology | 132-14..132-16 | COVERED | Repair precedes preservation and sampling.
REQ | CI-14 | Immutable exact-ten authority remains terminal | 132-14, 132-16 | COVERED | This plan repairs publication provenance only.
REQ | COV-05 | Fresh coverage census on repaired subject | 132-15 | COVERED | No coverage claim in repair.
REQ | SAFE-02 | Automated fail-closed repair and preservation | 132-14, 132-15 | COVERED | Focused TDD plus full recensus.
RESEARCH | — | Reuse existing controller; fail closed | 132-14 | COVERED | No alternate sampler or dependency.
CONTEXT | D-01 | Preserve sole summary and required membership | 132-14 | COVERED | No workflow edit.
CONTEXT | D-02/D-03 | No new remediation or runner exception | 132-14 | COVERED | Provenance repair only.
CONTEXT | D-04/D-05 | Exact-ten and inclusive thresholds | 132-16 | COVERED | Terminal live plan unchanged in authority.
CONTEXT | D-06/D-07 | Final preservation and coverage floor | 132-15 | COVERED | Re-census follows repair.
CONTEXT | D-08/D-09 | Exact six-edge exception and no broader drift | 132-14, 132-15 | COVERED | Manifest proves bounded source chronology.
</source_audit>

<verification>
The focused process suite must fail before implementation, then pass with the same mutation snapshots and no workflow edits. Automation-first acceptance is the only completion authority.
</verification>

<success_criteria>
- The missing `--transition-manifest` / `validate_transition_manifest` contract exists and is process-tested.
- The manifest schema records three truthful stages, including a distinct omission-repair stage.
- No live publication or sampling occurs in this plan.
</success_criteria>

<output>
Create `.planning/phases/132-measured-closure/132-14-SUMMARY.md` when done.
</output>

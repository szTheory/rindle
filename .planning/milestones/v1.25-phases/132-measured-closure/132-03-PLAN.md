---
phase: 132-measured-closure
plan: 03
type: execute
wave: 3
depends_on: [132-01, 132-02]
files_modified:
  - scripts/ci/collect_pr_timing_receipt.sh
  - scripts/maintainer/automation_first_contract.sh
  - scripts/maintainer/repo_hygiene_check.sh
  - test/install_smoke/ci_timing_automation_test.exs
  - test/install_smoke/automation_first_contract_test.exs
  - AGENTS.md
  - RUNNING.md
  - .planning/METHODOLOGY.md
autonomous: true
requirements: [CI-14, SAFE-02]
must_haves:
  truths:
    - "One command safely publishes a fast-forward-only PR head, drives ten sequential pull_request:labeled runs, validates CI Summary, calculates the receipt, and requires no human verification."
    - "A failed or cancelled sample restarts the full sequence at most once; divergence, exhausted retries, mixed heads, reruns, and overlaps fail closed."
    - "Future active phases reject manual verification/UAT while still allowing explicit credential or irreversible-action authorization checkpoints."
  artifacts:
    - path: scripts/ci/collect_pr_timing_receipt.sh
      provides: resumable unattended PR timing controller and receipt verifier
    - path: scripts/maintainer/automation_first_contract.sh
      provides: executable zero-human acceptance policy
---

<objective>
Replace Phase 132's manual timing checkpoint with tested maintainer automation and make automation-first acceptance the forward project default.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Build the unattended PR timing controller test-first</name>
  <files>scripts/ci/collect_pr_timing_receipt.sh, test/install_smoke/ci_timing_automation_test.exs</files>
  <action>Implement preflight, run, and verify modes; fast-forward-only publication; label ownership; exact-run discovery; sequential waiting; one bounded restart; ignored resumable state; atomic receipt rendering; inclusive median/p95 gates; and fail-closed diagnostics. Use a stateful gh fixture so the orchestration loop is exercised without remote mutation.</action>
  <verify><automated>mix test test/install_smoke/ci_timing_automation_test.exs --seed 0</automated></verify>
  <done>The controller's success, restart, cleanup, receipt parity, and partial-evidence failures are executable offline.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce automation-first acceptance in planning and release hygiene</name>
  <files>scripts/maintainer/automation_first_contract.sh, scripts/maintainer/repo_hygiene_check.sh, test/install_smoke/automation_first_contract_test.exs, AGENTS.md, RUNNING.md, .planning/METHODOLOGY.md</files>
  <action>Reject active manual-only verification, human verification checkpoints, and requirement-bearing human actions. Allow only explicit authorization or credential-bootstrap actions that do not carry acceptance. Wire the checker into repo hygiene and durable agent/methodology guidance without changing ci.yml.</action>
  <verify><automated>mix test test/install_smoke/automation_first_contract_test.exs --seed 0 &amp;&amp; ./scripts/maintainer/automation_first_contract.sh</automated></verify>
  <done>Active planning and release hygiene fail before a manual verification/UAT dependency can ship.</done>
</task>

</tasks>

<verification>
Run both focused ExUnit files, bash syntax checks, the automation-first contract, and git diff hygiene.
</verification>

<success_criteria>
- No human verification or UAT remains in the active Phase 132 plans.
- The controller is reversible, resumable, bounded, and tested without live runner expenditure.
- No product API, dependency, schema, release gate, or ci.yml topology changes.
</success_criteria>

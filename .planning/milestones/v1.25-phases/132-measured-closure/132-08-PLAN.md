---
phase: 132-measured-closure
plan: 08
type: execute
wave: 8
depends_on: [132-07]
files_modified:
  - scripts/ci/collect_pr_timing_receipt.sh
  - test/install_smoke/ci_timing_automation_test.exs
  - .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
autonomous: true
gap_closure: true
requirements: [CI-14]
must_haves:
  truths:
    - "A fresh comparable sequence contains exactly ten consecutive successful, non-cancelled, first-attempt `pull_request` runs from one immutable preserved implementation head per D-04."
    - "Workflow start through `CI Summary` completion has median at most 480 seconds and nearest-rank p95 at most 600 seconds, enforced inclusively per D-05."
    - "The prior failed receipts and causal census remain intact and separate from the new source manifest/table."
    - "Independent verify mode rejects the retained failed receipt, resolves every fresh run and `CI Summary` job through the GitHub Actions API, and fails unless identity, chronology, exact-head comparability, and both inclusive CI-14 thresholds pass."
    - "No newly introduced rerun, overlapping sample, mixed head, weaker gate, or unsupported external-runner exception contributes to closure per D-01 and D-03."
    - statement: "SAFE-02 timing adjacency backstop: values equal to 480-second median or 600-second nearest-rank p95 pass; ties retain deterministic numeric rank treatment."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT replace a failed or partial automated receipt with human verification, UAT, or an unsupported exception."
      category: transparency
      status: unresolved
      verification: null
      flagged_unverified: true
    - statement: "MUST NOT count a rerun, cancellation, overlapping run, mixed SHA, or non-`pull_request` event as a qualifying sample."
      category: safety
      status: unresolved
      verification: null
      flagged_unverified: true
  artifacts:
    - path: "scripts/ci/collect_pr_timing_receipt.sh"
      provides: "Explicit current-section rendering plus GitHub Actions API-backed independent CI-14 verification"
      contains: "CI_TIMING_CURRENT_SOURCE_BEGIN"
    - path: "test/install_smoke/ci_timing_automation_test.exs"
      provides: "Failed-receipt, API-identity/chronology, and inclusive threshold regression contracts"
      contains: "480"
    - path: ".planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md"
      provides: "Fresh gap-closure table, source manifest, immutable-head identity, statistics, and machine verdict"
      contains: "CI_TIMING_CURRENT_SOURCE_BEGIN"
  key_links:
    - from: ".planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md"
      to: "scripts/ci/collect_pr_timing_receipt.sh"
      via: "controller reads the preserved code-bearing SHA and rejects non-planning drift"
      pattern: "Preserved.*SHA"
    - from: "scripts/ci/collect_pr_timing_receipt.sh"
      to: ".planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md"
      via: "run mode renders exactly one explicitly current table/source manifest while preserving legacy failed blocks, and verify mode checks the current section against live Actions API evidence"
      pattern: "CI_TIMING_CURRENT_(TABLE|SOURCE)_BEGIN"
    - from: "test/install_smoke/ci_timing_automation_test.exs"
      to: "scripts/ci/collect_pr_timing_receipt.sh"
      via: "PATH-isolated GitHub API fixtures prove the failed legacy receipt is non-accepting and exercise identity, chronology, contiguity, rerun, and inclusive-boundary verdicts"
      pattern: "collect_pr_timing_receipt"
---

<objective>
Replace the failed measurement with one fresh, comparable, machine-verifiable ten-run receipt and close CI-14 only if both inclusive thresholds pass.

Purpose: Establish the actual required-path effect of the bounded remediation without reruns, gate weakening, mixed heads, external-runner hand-waving, or human acceptance per D-01 and D-03 through D-05.
Output: A controller/test contract and distinct gap-closure receipt section with ten source-linked rows, source JSON, median, nearest-rank p95, live API integrity checks, and automated verdict.
</objective>

## Artifacts this phase produces

- Updated `scripts/ci/collect_pr_timing_receipt.sh` behavior: legacy `CI_TIMING_TABLE_*` and `CI_TIMING_SOURCE_*` blocks remain untouched as historical failed evidence; run mode writes exactly one fresh `CI_TIMING_CURRENT_TABLE_BEGIN/END` and `CI_TIMING_CURRENT_SOURCE_BEGIN/END` section; verify mode reads only that explicit current section and validates it against GitHub Actions API run/job evidence and the inclusive CI-14 verdict.
- Expanded `test/install_smoke/ci_timing_automation_test.exs` coverage for the retained failed receipt, duplicate/missing current sections, immutable-head/run/job identity, chronology and contiguous exact-head first-attempt selection, and passing values exactly at 480-second median and 600-second nearest-rank p95.
- A `Gap-closure fresh receipt` section in `132-CI-TIMING-RECEIPT.md` with the new current marker namespace, immutable SHA, ten run rows, sorted durations, median, nearest-rank p95, integrity verdict, and overall verdict. The existing failed tables, manifests, prose, and causal census are not deleted or rewritten; no new dependency, product/API surface, topology, rerun policy, or receipt statistic is introduced.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/132-measured-closure/132-CONTEXT.md
@.planning/phases/132-measured-closure/132-VERIFICATION.md
@.planning/phases/132-measured-closure/132-07-SUMMARY.md
@.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
@.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
@scripts/ci/collect_pr_timing_receipt.sh
@test/install_smoke/ci_timing_automation_test.exs
@.github/workflows/ci.yml
</context>

<assumptions>
- CI-14 and COV-05 were unclassified by the generic edge probe; CI-14 is resolved only by the explicit live contract here, while COV-05 is already resolved by Plan 132-07's authoritative census.
- SAFE-02 adjacency/order/concurrency map to inclusive percentile boundaries, deterministic tie ranking, and sequential interruption-safe collection. The generic collection-shaped empty case is applicable: fewer or more than ten qualifying samples is invalid.
- The controller's authenticated `gh` session and open PR #96 are external runtime preconditions, not human acceptance; an authentication failure stops execution and does not satisfy a requirement.
- No external-runner exception is planned. If new qualifying job-level evidence supports one, D-03 still requires the receipt to include evidence, a named owner, and a dated follow-up; the executor may not infer an exception from a slow result.
- The receipt update is reversible and introduces no one-way decision.
</assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish, collect, and independently verify the fresh exact-head receipt</name>
  <precondition>`gh auth status` succeeds for `szTheory/rindle`, PR #96 is open, and the full preserved-subject SHA recorded by Plan 132-07 exists locally.</precondition>
  <reversibility rating="reversible">Label cleanup and receipt evidence are reversible; fast-forward publication is guarded by the controller's existing ancestry and planning-only drift checks.</reversibility>
  <files>
    scripts/ci/collect_pr_timing_receipt.sh
    test/install_smoke/ci_timing_automation_test.exs
    .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
  </files>
  <read_first>
    - AGENTS.md
    - RUNNING.md
    - .planning/phases/132-measured-closure/132-CONTEXT.md
    - .planning/phases/132-measured-closure/132-VERIFICATION.md
    - .planning/phases/132-measured-closure/132-07-SUMMARY.md
    - .planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
    - .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
    - scripts/ci/collect_pr_timing_receipt.sh
    - test/install_smoke/ci_timing_automation_test.exs
    - .github/workflows/ci.yml
    - scripts/ci/eval_ci_summary.sh
  </read_first>
  <behavior>
    - "HISTORY: the existing failed receipt keeps its legacy machine blocks and all failed values verbatim; only an explicitly named current section is eligible for CI-14 acceptance."
    - "NEGATIVE: verify mode rejects the current failed receipt because it has no current section, and rejects missing/duplicate current markers, API head/run/job mismatches, failed/cancelled/rerun/non-PR samples, overlapping or non-consecutive chronology, altered durations, and either threshold miss."
    - "POSITIVE BOUNDARY: an API-backed ten-run current section whose recomputed median is exactly 480 seconds and nearest-rank p95 is exactly 600 seconds passes."
    - "PRESERVATION: topology, sole-required-gate membership, skip-as-pass behavior, and absence of newly introduced reruns remain executable contracts rather than receipt prose."
  </behavior>
  <action>
First extend `test/install_smoke/ci_timing_automation_test.exs`, using its existing PATH-isolated `gh` shim and the real `.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md` as the failed-receipt regression source. Prove that independent verification rejects that retained receipt, then add negative API fixtures for wrong head SHA, wrong event or attempt, unsuccessful/cancelled run, missing/unsuccessful or duplicate `CI Summary`, altered table/manifest duration, overlapping chronology, and a selected set that is not the consecutive exact-head eligible slice. Add positive fixtures whose sorted ten durations produce median exactly 480 and nearest-rank p95 exactly 600, and prove both inclusive boundaries pass per D-04 and D-05.

Change `scripts/ci/collect_pr_timing_receipt.sh` to use one explicit current-section contract. Do not delete, rename, or rewrite the existing legacy `CI_TIMING_TABLE_*` / `CI_TIMING_SOURCE_*` failed evidence. Run mode must permit those legacy blocks, refuse a pre-existing `CI_TIMING_CURRENT_SOURCE_BEGIN` or partial/duplicate current marker set, and append exactly one `CI_TIMING_CURRENT_TABLE_BEGIN/END` plus `CI_TIMING_CURRENT_SOURCE_BEGIN/END` section. Independent verify mode must parse only the explicit current section; require `--repo szTheory/rindle`, workflow `ci.yml`, summary job `CI Summary`, and inclusive maxima; call the Actions API for every recorded run and its jobs; match the manifest's full immutable SHA, run ID, `pull_request` event, `run_attempt: 1`, completed/success conclusion, run start, exactly one completed/success `CI Summary`, summary completion, and recomputed non-negative duration; reject cancellation, overlap, duplicate IDs, mixed heads, and a selection that is not ten consecutive eligible successful non-cancelled attempt-1 same-head PR runs in API chronology. Recompute table/manifest agreement, median from sorted ranks 5 and 6, nearest-rank p95 from rank 10, and require median at most 480 and p95 at most 600 before returning zero. The current failed receipt must therefore fail before an API-backed passing current section exists; do not claim marker shape or arithmetic alone proves CI-14.

After the controller contract is green, run its preflight and run mode for repository `szTheory/rindle`, PR `96`, workflow `ci.yml`, summary job `CI Summary`, owned label `ci-timing-sample`, exactly ten samples, at most two full sequences, inclusive 480-second median and 600-second p95 thresholds, and fast-forward-only head publication. Source the implementation and preserved-subject identities from `132-PRESERVATION-RECEIPT.md`; permit later commits only when their diff is planning-only as the controller already enforces. Collect sequentially and clean the owned label on every exit per D-04. Preserve all prior receipt content and append the explicit current section. Run the API-backed verify mode against the completed file, then run the existing CI topology, `CI Summary`, skip-as-pass, packed-consumer/adopter, and rerun-policy contracts so D-01 is independently enforced alongside receipt verification. A threshold or integrity miss must persist honest evidence and return `gaps_found`; no human/UAT substitution, topology change, gate weakening, newly introduced rerun, or external-runner exception is allowed per D-01 and D-03.
  </action>
  <acceptance_criteria>
    - `scripts/ci/collect_pr_timing_receipt.sh` and `test/install_smoke/ci_timing_automation_test.exs` implement and test the explicit current-section contract; the complete legacy failed receipt remains present and byte-faithful before the appended current section.
    - Independent verify mode exits non-zero on the current failed receipt and on fixtures with wrong API head/run/job identity, non-qualifying event/attempt/conclusion, cancellation, non-consecutive or overlapping chronology, changed duration, or either threshold above its inclusive maximum.
    - A fixture with median exactly 480 seconds and nearest-rank p95 exactly 600 seconds passes API-backed independent verification.
    - The new receipt names one full immutable head and contains exactly ten consecutive successful, non-cancelled, first-attempt `pull_request` rows with source URLs and no overlap.
    - Every current source row resolves through the Actions API to the recorded head, recorded run chronology, and exactly one successful completed `CI Summary`; current table and JSON source manifest agree exactly while legacy blocks are ignored for acceptance.
    - Median is calculated from sorted ranks 5 and 6, nearest-rank p95 uses rank 10, and both observed values pass the inclusive 480/600 thresholds.
    - `CI Summary` remains the sole required result with unchanged required-job membership and skip-as-pass behavior; executable topology/summary contracts pass and zero newly introduced reruns are counted.
    - The controller removes its owned label and leaves no partial state presented as acceptance; independent verify mode exits zero for the complete passing receipt.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/install_smoke/ci_timing_automation_test.exs test/install_smoke/ci_lane_split_test.exs test/install_smoke/ci_observability_test.exs --seed 0 &amp;&amp; bash scripts/ci/collect_pr_timing_receipt.sh verify --repo szTheory/rindle --workflow ci.yml --summary-job "CI Summary" --samples 10 --median-max 480 --p95-max 600 --receipt .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md &amp;&amp; bash scripts/ci/test_ci_summary_gate.sh &amp;&amp; ./scripts/maintainer/automation_first_contract.sh</automated>
  </verify>
  <done>A fresh exact-head ten-run receipt passes sample integrity, median <=480 seconds, nearest-rank p95 <=600 seconds, and independent verification without any weakened gate, new rerun, or human acceptance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local controller -> GitHub PR | Authenticated label/publication mutations must remain bounded, fast-forward-only, and cleaned up. |
| GitHub Actions API -> receipt | Remote run/job metadata must match the immutable head, event, attempt, chronology, and summary result. |
| receipt statistics -> phase closure | Partial or manipulated evidence must never become a passing CI-14 verdict. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-132-08-01 | Spoofing | run/head identity | high | mitigate | Resolve every current-section run and job through the Actions API and match exact SHA, event, attempt, conclusion, timestamps, and exactly one `CI Summary`. |
| T-132-08-02 | Tampering | receipt rows/statistics | high | mitigate | Parse only the explicit current markers, compare its table/manifest to API evidence, and recompute sorted median/p95 plus inclusive verdict. |
| T-132-08-03 | Repudiation | excluded slow/failed runs | high | mitigate | Require consecutive successful first attempts; bounded restart persists honest failure and never silently excludes a slow success. |
| T-132-08-04 | Denial of service | interrupted/rate-limited collection | medium | mitigate | Reuse persisted state, bounded backoff/restart, sequential sampling, and guaranteed owned-label cleanup. |
| T-132-08-05 | Elevation of privilege | fast-forward publication and label mutation | high | mitigate | Existing controller ancestry, PR identity, planning-only drift, and label-ownership checks fail closed before mutation. |
| T-132-08-06 | Information disclosure | GitHub credentials | medium | mitigate | Use the existing authenticated `gh` session without printing tokens; receipt stores public run metadata only. |
</threat_model>

<verification>
The live controller's API-backed current-section verify mode plus the executable topology, summary-gate, skip-as-pass, and rerun-policy contracts are the acceptance authority. The retained failed legacy section must fail independent verification until one complete passing current section exists. Re-run preservation commands only if the published head contains non-planning drift from Plan 132-07; otherwise the recorded preserved SHA remains authoritative.
</verification>

<success_criteria>
- Exactly ten comparable first-attempt runs pass integrity checks on one immutable head.
- Median is <=480 seconds and nearest-rank p95 is <=600 seconds.
- No required proof, required-job membership, skip semantics, or rerun policy changed.
- CI-14 closes by executable evidence; COV-05 and SAFE-02 remain bound to the same preserved subject.
</success_criteria>

<output>
Create `.planning/phases/132-measured-closure/132-08-SUMMARY.md` when done.
</output>

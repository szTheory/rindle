---
phase: 132-measured-closure
plan: 21
type: execute
wave: 21
depends_on: [132-20]
files_modified:
  - .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
autonomous: true
gap_closure: true
requirements: [CI-14, COV-05, SAFE-02]
must_haves:
  truths:
    - "Exactly one repaired controller publishes one fast-forward immutable planning-tail HEAD and owns all subsequent label mutation; it never reruns a workflow and never exceeds two complete sequences."
    - "One current receipt contains exactly ten successful, non-cancelled, first-attempt PR-96 runs on that immutable head, ordered without overlap, and selected IDs equal the complete paginated canonical eligible population."
    - "Explicit verify and completed-state re-entry independently re-fetch every API identity/job, page through the population, reproduce table/source equality, and recompute median ranks 5/6 plus nearest-rank p95 rank 10 without mutation."
    - "CI-14 closes only when median <=480 seconds and p95 <=600 seconds inclusively; partial, mixed-head, rerun-inclusive, page-truncated, or threshold-missing evidence remains non-green per D-04/D-05."
    - "The active preservation manifest binds the Plan 132-19 source subject; the published tail is planning-only and COV-05/SAFE-02 plus D-01/D-08/D-09 remain intact."
  artifacts:
    - path: ".planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md"
      provides: "Fresh exact-ten API-backed current table/source manifest and inclusive CI-14 verdict"
      contains: "CI_TIMING_CURRENT_SOURCE_BEGIN"
  key_links:
    - from: ".planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md"
      to: "live controller run"
      via: "active Git-reproduced manifest and planning-only published tail"
      pattern: "PRESERVATION_TRANSITION_V2_BEGIN"
    - from: "controller trigger/state records"
      to: "current timing receipt"
      via: "one immutable head, attempt-1 identity, sequential non-overlap, and exact-ten population"
      pattern: "population_boundary_ids|runs|sequence_attempt"
    - from: "current timing receipt"
      to: "CI-14 verdict"
      via: "explicit API verification and completed-state revalidation"
      pattern: "verify_api_backed_receipt"
  prohibitions:
    - statement: "The live sampler must not discard a slow qualifying success, select around a failure, or hide an eligible run on another API page."
      status: unverified
      flagged: true
    - statement: "The live sampler must not create reruns, exceed two sequences, or weaken required proof after a timing miss."
      status: unverified
      flagged: true
    - statement: "The fresh receipt must not overwrite or relabel historical failed evidence as a pass."
      status: unverified
      flagged: true
---

<objective>
Collect and independently API-reverify the one fresh exact-ten timing receipt authorized after repaired preservation.

Purpose: Close CI-14 only with complete, bounded, no-rerun live evidence while retaining COV-05, SAFE-02, and all required-gate semantics.
Output: A current immutable-head timing receipt with a machine-recomputed inclusive threshold verdict.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RUNNING.md
@guides/release_publish.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/132-measured-closure/132-CONTEXT.md
@.planning/phases/132-measured-closure/132-19-SUMMARY.md
@.planning/phases/132-measured-closure/132-20-SUMMARY.md
@.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
@.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
@scripts/ci/collect_pr_timing_receipt.sh
@test/install_smoke/ci_timing_automation_test.exs
@test/install_smoke/ci_lane_split_test.exs
@test/install_smoke/ci_observability_test.exs
@scripts/ci/test_ci_summary_gate.sh
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish once, collect exact ten, and independently reverify the live receipt</name>
  <precondition>Plan 132-20 is machine-green; its active manifest exists exactly once and binds the repaired source subject; all post-subject paths are planning-only; `gh auth status` succeeds; PR #96 is open at an equal or strict-ancestor head; `ci-timing-sample` exists and is absent; the new SHA-scoped state/lock is absent; and the timing receipt has no current markers.</precondition>
  <files>.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md</files>
  <read_first>
    - AGENTS.md
    - RUNNING.md
    - guides/release_publish.md
    - .planning/phases/132-measured-closure/132-CONTEXT.md
    - .planning/phases/132-measured-closure/132-19-SUMMARY.md
    - .planning/phases/132-measured-closure/132-20-SUMMARY.md
    - .planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
    - .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
    - scripts/ci/collect_pr_timing_receipt.sh
    - test/install_smoke/ci_timing_automation_test.exs
    - test/install_smoke/ci_lane_split_test.exs
    - test/install_smoke/ci_observability_test.exs
    - scripts/ci/test_ci_summary_gate.sh
    - .github/workflows/ci.yml
  </read_first>
  <action>
Re-run the formatter, repaired focused controller suite, topology/observability tests, CI Summary gate, automation-first contract, and `git diff --check`. Extract the active manifest and require repository `szTheory/rindle`, numeric PR 96, a valid controller correction SHA, the Plan 132-19 preserved source SHA from Plan 132-20, exact Git-reproducible stages, and a planning-only tail through current HEAD. Snapshot PR head/labels, receipt hash/current marker counts, exact-head workflow runs, and state/lock absence. Repeat `preflight --no-publish` with exactly 10 samples, `--max-sequences 2`, creation/run deadlines, and inclusive 480/600 thresholds; require success and snapshot equality before mutation.

Start exactly one foreground `run --publish-head` controller using that manifest, workflow `ci.yml`, summary job `CI Summary`, label `ci-timing-sample`, and a fresh `.gsd/ci-timing/phase-132-gap-final` state directory. Permit one fast-forward publication of current HEAD. Bind the qualifying publication-triggered PR run into the new population, then allow only controller-owned sequential label triggers. Never use Actions rerun endpoints, manual label actions, parallel controllers, state deletion/reset, or more than two complete sequences. A failed/nonqualifying run may restart the entire sequence once; terminal failed state is permanent for this head.

Admit a receipt only when it has exactly ten non-overlapping successful `pull_request` attempt-1 runs on one immutable PR-96 head and the selected IDs equal the complete paginated canonical eligible population after boundary exclusions. Preserve historical receipt sections; append one current table/source section atomically. Invoke the uncertain live `run` with `set +e` scoped only around that call, capture combined output and its documented terminal status, then restore `set -e`; accept only status 0 (threshold PASS) or status 2 (bounded exact-ten threshold miss). Any other status remains a controller/integrity execution failure and must not be relabeled as a timing result.

For either accepted live status, require completed state and an exact-ten current receipt, snapshot PR head, sorted labels, all paginated exact-head `ci.yml` run IDs, receipt bytes/markers, and state bytes, then independently invoke explicit `verify` and completed-state `run --no-publish`. Scope `set +e` separately around each uncertain verifier call and capture its output/status. Enforce the shipped status contract: a live status 0 requires explicit verify 0, completed-state re-entry 0, and state verdict `PASS`; a live status 2 requires the explicit verifier's documented threshold-refusal status 1 with its threshold-miss diagnostic, completed-state re-entry 2, and state verdict `FAIL`. Both branches must reach both API-backed checks. Re-snapshot and require no PR-head, label, run-ID, receipt, or state mutation during verification; require no owned label, lock, pid file, or matching controller process afterward.

CI-14 is PASS only when median `(rank5 + rank6)/2 <= 480` and nearest-rank p95 `rank10 <= 600`. Emit a machine-readable terminal summary object whose status is `PASS` only for live status 0 and `gaps_found` for live status 2, and carry the captured controller/verifier outputs and statuses into `132-21-SUMMARY.md`. A bounded threshold miss therefore completes the evidence task honestly without closing CI-14 or crashing the executor. Timeout, integrity error, exhausted sequence, or incomplete population remains machine-recorded in controller state/output and leaves the plan non-green because it is outside the accepted 0/2 terminal contract. Do not weaken gates, thresholds, topology, proof, rerun, or exception policy in response.

After either accepted terminal branch, re-run the offline focused/topology/summary/automation-first authorities, prove owned label/lock/process cleanup and no post-preserved-subject non-planning drift, and record exact run IDs, API URLs, timestamps, durations, population boundary, sorted durations, metrics, captured statuses, and honest verdict. Human credential bootstrap may unblock execution but cannot satisfy any requirement.
  </action>
  <acceptance_criteria>
    - One immutable published head yields a current exact-ten attempt-1 successful PR population with no overlap, omission, rerun, mixed head, or more than two sequences.
    - The complete population includes qualifying records from every API page and is identical to the receipt selection.
    - Explicit verify and completed-state re-entry independently reproduce all identities, durations, statistics, and inclusive thresholds without mutation.
    - Median is at most 480 seconds and nearest-rank p95 is at most 600 seconds before CI-14 is marked closed.
    - Live status 2 reaches both API-backed revalidation paths and produces `gaps_found`, never PASS; unexpected controller/integrity statuses fail the task without being recast as timing evidence.
    - COV-05, SAFE-02, sole CI Summary membership, skip semantics, exact D-08 edges, and every D-09 prohibition remain preserved.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
mix format --check-formatted
mix test test/install_smoke/ci_timing_automation_test.exs --seed 0
mix test test/install_smoke/ci_lane_split_test.exs test/install_smoke/ci_observability_test.exs --seed 0
bash scripts/ci/test_ci_summary_gate.sh
./scripts/maintainer/automation_first_contract.sh
PRESERVATION=.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
RECEIPT=.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
TRANSITION_JSON=$(awk '$0 == "PRESERVATION_TRANSITION_V2_BEGIN" {take=1; next} $0 == "PRESERVATION_TRANSITION_V2_END" {take=0} take' "$PRESERVATION")
SUBJECT=$(jq -r .preserved_subject_sha &lt;&lt;&lt;"$TRANSITION_JSON")
CORRECTION=$(jq -r .controller_correction_sha &lt;&lt;&lt;"$TRANSITION_JSON")
test -z "$(git diff --name-only "$SUBJECT..HEAD" | grep -Ev '^\.planning/' || true)"
bash scripts/ci/collect_pr_timing_receipt.sh preflight --repo szTheory/rindle --pr 96 --workflow ci.yml --summary-job 'CI Summary' --label ci-timing-sample --samples 10 --max-sequences 2 --median-max 480 --p95-max 600 --correction-sha "$CORRECTION" --preserved-subject-sha "$SUBJECT" --transition-manifest "$PRESERVATION" --receipt "$RECEIPT" --state-dir .gsd/ci-timing/phase-132-gap-final-preflight --no-publish
test ! -e .gsd/ci-timing/phase-132-gap-final-preflight
RUN_ARGS=(--repo szTheory/rindle --pr 96 --workflow ci.yml --summary-job 'CI Summary' --label ci-timing-sample --samples 10 --max-sequences 2 --median-max 480 --p95-max 600 --correction-sha "$CORRECTION" --preserved-subject-sha "$SUBJECT" --transition-manifest "$PRESERVATION" --receipt "$RECEIPT" --state-dir .gsd/ci-timing/phase-132-gap-final --creation-timeout 300 --run-timeout 1800)
set +e
RUN_OUTPUT=$(bash scripts/ci/collect_pr_timing_receipt.sh run "${RUN_ARGS[@]}" --publish-head 2&gt;&amp;1)
RUN_STATUS=$?
set -e
printf '%s\n' "$RUN_OUTPUT"
STATE_FILE=".gsd/ci-timing/phase-132-gap-final/pr-96-$(git rev-parse HEAD).json"
LOCK_PATH="${STATE_FILE}.lock"
test ! -e "$LOCK_PATH"
test ! -e "$LOCK_PATH/pid"
if pgrep -f 'collect_pr_timing_receipt[.]sh run .*phase-132-gap-final' &gt;/dev/null; then exit 1; fi
case "$RUN_STATUS" in
  0) TERMINAL_STATUS=PASS ;;
  2) TERMINAL_STATUS=gaps_found ;;
  *) printf 'unexpected live controller status: %s\n' "$RUN_STATUS" &gt;&amp;2; exit 1 ;;
esac
test -s "$STATE_FILE"
test "$(jq -r .status "$STATE_FILE")" = complete
test "$(grep -c '^CI_TIMING_CURRENT_SOURCE_BEGIN$' "$RECEIPT")" -eq 1
test "$(grep -c '^CI_TIMING_CURRENT_SOURCE_END$' "$RECEIPT")" -eq 1
test "$(grep -c '^CI_TIMING_CURRENT_TABLE_BEGIN$' "$RECEIPT")" -eq 1
test "$(grep -c '^CI_TIMING_CURRENT_TABLE_END$' "$RECEIPT")" -eq 1
snapshot_remote() {
  local head labels runs
  head=$(gh pr view 96 --repo szTheory/rindle --json headRefOid --jq .headRefOid)
  labels=$(gh pr view 96 --repo szTheory/rindle --json labels --jq '[.labels[].name] | sort')
  runs=$(gh api --paginate --slurp -H 'Accept: application/vnd.github+json' 'repos/szTheory/rindle/actions/workflows/ci.yml/runs?event=pull_request&amp;per_page=100' | jq -c --arg sha "$head" '[.[] | .workflow_runs[]? | select(.head_sha == $sha) | .id] | sort')
  jq -cn --arg head "$head" --argjson labels "$labels" --argjson runs "$runs" '{head:$head,labels:$labels,exact_head_run_ids:$runs}'
}
snapshot_receipt() {
  jq -cn \
    --arg sha256 "$(shasum -a 256 "$RECEIPT" | awk '{print $1}')" \
    --argjson table_begin "$(grep -c '^CI_TIMING_CURRENT_TABLE_BEGIN$' "$RECEIPT" || true)" \
    --argjson table_end "$(grep -c '^CI_TIMING_CURRENT_TABLE_END$' "$RECEIPT" || true)" \
    --argjson source_begin "$(grep -c '^CI_TIMING_CURRENT_SOURCE_BEGIN$' "$RECEIPT" || true)" \
    --argjson source_end "$(grep -c '^CI_TIMING_CURRENT_SOURCE_END$' "$RECEIPT" || true)" \
    '{sha256:$sha256,table_begin:$table_begin,table_end:$table_end,source_begin:$source_begin,source_end:$source_end}'
}
REMOTE_BEFORE_VERIFY=$(snapshot_remote)
RECEIPT_BEFORE_VERIFY=$(snapshot_receipt)
STATE_BEFORE_VERIFY=$(shasum -a 256 "$STATE_FILE" | awk '{print $1}')
jq -e '(.labels | index("ci-timing-sample")) == null' &lt;&lt;&lt;"$REMOTE_BEFORE_VERIFY" &gt;/dev/null
set +e
VERIFY_OUTPUT=$(bash scripts/ci/collect_pr_timing_receipt.sh verify --repo szTheory/rindle --pr 96 --workflow ci.yml --summary-job 'CI Summary' --samples 10 --median-max 480 --p95-max 600 --receipt "$RECEIPT" 2&gt;&amp;1)
VERIFY_STATUS=$?
set -e
printf '%s\n' "$VERIFY_OUTPUT"
set +e
REENTRY_OUTPUT=$(bash scripts/ci/collect_pr_timing_receipt.sh run "${RUN_ARGS[@]}" --no-publish 2&gt;&amp;1)
REENTRY_STATUS=$?
set -e
printf '%s\n' "$REENTRY_OUTPUT"
REMOTE_AFTER_VERIFY=$(snapshot_remote)
RECEIPT_AFTER_VERIFY=$(snapshot_receipt)
STATE_AFTER_VERIFY=$(shasum -a 256 "$STATE_FILE" | awk '{print $1}')
test "$REMOTE_AFTER_VERIFY" = "$REMOTE_BEFORE_VERIFY"
test "$RECEIPT_AFTER_VERIFY" = "$RECEIPT_BEFORE_VERIFY"
test "$STATE_AFTER_VERIFY" = "$STATE_BEFORE_VERIFY"
test ! -e "$LOCK_PATH"
test ! -e "$LOCK_PATH/pid"
if pgrep -f 'collect_pr_timing_receipt[.]sh run .*phase-132-gap-final' &gt;/dev/null; then exit 1; fi
if [ "$RUN_STATUS" -eq 0 ]; then
  test "$VERIFY_STATUS" -eq 0
  test "$REENTRY_STATUS" -eq 0
  test "$(jq -r .verdict "$STATE_FILE")" = PASS
else
  test "$VERIFY_STATUS" -eq 1
  grep -Fq 'current receipt misses inclusive CI-14 timing thresholds' &lt;&lt;&lt;"$VERIFY_OUTPUT"
  test "$REENTRY_STATUS" -eq 2
  test "$(jq -r .verdict "$STATE_FILE")" = FAIL
fi
TERMINAL_SUMMARY=$(jq -cn --arg status "$TERMINAL_STATUS" --argjson run_status "$RUN_STATUS" --argjson verify_status "$VERIFY_STATUS" --argjson reentry_status "$REENTRY_STATUS" '{status:$status,run_status:$run_status,verify_status:$verify_status,completed_reentry_status:$reentry_status,ci_14_closed:($status == "PASS")}')
if [ "$RUN_STATUS" -eq 0 ]; then jq -e '.status == "PASS" and .ci_14_closed == true' &lt;&lt;&lt;"$TERMINAL_SUMMARY" &gt;/dev/null; else jq -e '.status == "gaps_found" and .ci_14_closed == false' &lt;&lt;&lt;"$TERMINAL_SUMMARY" &gt;/dev/null; fi
printf 'PHASE_132_TERMINAL_SUMMARY=%s\n' "$TERMINAL_SUMMARY"
mix format --check-formatted
mix test test/install_smoke/ci_timing_automation_test.exs --seed 0
mix test test/install_smoke/ci_lane_split_test.exs test/install_smoke/ci_observability_test.exs --seed 0
bash scripts/ci/test_ci_summary_gate.sh
test -z "$(git diff --name-only "$SUBJECT..HEAD" | grep -Ev '^\.planning/' || true)"
./scripts/maintainer/automation_first_contract.sh
git diff --check</automated>
  </verify>
  <done>A fresh exact-ten receipt reaches explicit and completed-state API revalidation without mutation; `132-21-SUMMARY.md` records PASS/CI-14 closure only for live status 0 and records `gaps_found` without CI-14 closure for the truthful bounded status-2 miss.</done>
  <reversibility rating="costly">The receipt append and label are reversible/cleaned, but published commits and GitHub Actions runs are durable external evidence; the bounded authorization makes them costly rather than one-way.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| local Git/controller -> GitHub PR | Authenticated publication and labels trigger durable external runs. |
| Actions API -> current receipt | Remote paginated run/job data becomes CI-14 acceptance evidence. |
| receipt/state -> requirement verdict | Local durable bytes must not bypass live identity, completeness, or threshold checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-132-21-01 | Spoofing | repo/PR/SHA/run identity | high | mitigate | Cross-check manifest, PR association, exact SHA/event/attempt, and exactly one successful CI Summary through live API. |
| T-132-21-02 | Tampering | population/table/statistics | high | mitigate | Paginate all eligible runs, require set/order equality, and recompute table durations plus ranks twice. |
| T-132-21-03 | Repudiation | omitted slow run or exhausted sequence | high | mitigate | Persist boundary/errors/attempts; make failed state terminal; retain historical evidence and full eligible population. |
| T-132-21-04 | Elevation of privilege | push/label/rerun mutation | high | mitigate | Machine-green preflight, fast-forward-only publish, one SHA lock, controller-owned label cleanup, no rerun verb, two-sequence ceiling. |
| T-132-21-05 | Denial of service | delayed/missing/long Actions run | medium | mitigate | Persist trigger epoch/current ID and enforce creation plus completion deadlines across resume. |
| T-132-21-06 | Information disclosure | GitHub credentials | medium | mitigate | Never print/store tokens; receipt contains public run metadata only. |
| T-132-21-07 | Tampering | preservation handoff | high | mitigate | Reproduce active Git manifest and reject any post-subject non-planning path before publication. |
</threat_model>

<artifacts_this_phase_produces>
- One appended `CI_TIMING_CURRENT_TABLE_BEGIN/END` current exact-ten table.
- One appended `CI_TIMING_CURRENT_SOURCE_BEGIN/END` machine-readable manifest.
- One new SHA-scoped durable state path under `.gsd/ci-timing/phase-132-gap-final/` and its transient `.lock` (lock absent on completion).
- No new source symbol, CLI flag, state field, fixture/helper, package, schema, API, frontend, workflow, rerun, or required-check path.
</artifacts_this_phase_produces>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Close measured median gap, preserve ratchets, replace failed receipt | 132-19..21 | COVERED | Repair -> preservation -> live evidence.
REQ | CI-14 | Exact-ten median <=480 and p95 <=600 without weaker gates/reruns | 132-19, 132-21 | COVERED | Controller integrity plus live receipt.
REQ | COV-05 | Coverage >=82.13%, no percentage-only tests | 132-20, 132-21 | COVERED | Fresh authoritative census and source-drift backstop.
REQ | SAFE-02 | Focused/full preservation and prohibited-surface safety | 132-19..21 | COVERED | Offline regressions, full census, live backstop.
RESEARCH | — | Reuse existing GitHub Actions controller; no new external integration | 132-19..21 | COVERED | Existing gh/jq path only; COVERAGE.md determination retained.
RESEARCH | — | API-backed exact-head complete-population statistics | 132-19, 132-21 | COVERED | Pagination, identity, ranks, thresholds.
CONTEXT | D-01 | Sole CI Summary, membership, skip semantics | 132-19..21 | COVERED | Existing topology authorities rerun.
CONTEXT | D-02 | Evidence-guided narrow correction | 132-19 | COVERED | Only verified controller defects repaired.
CONTEXT | D-03 | No unsupported external-runner exception | 132-21 | COVERED | Receipt must pass thresholds; no exception substituted.
CONTEXT | D-04 | Fresh exact-ten immutable first-attempt PR sequence | 132-19, 132-21 | COVERED | Bounded controller and live population.
CONTEXT | D-05 | Both inclusive median/p95 thresholds | 132-19, 132-21 | COVERED | Explicit verifier and completed-state verifier.
CONTEXT | D-06 | Fresh final preservation census | 132-20, 132-21 | COVERED | Full authorities and live source link.
CONTEXT | D-07 | Coverage floor/no percentage-only tests | 132-20 | COVERED | Structural arithmetic and test-purpose review.
CONTEXT | D-08 | Exactly six scheduling-edge removals | 132-19..21 | COVERED | Lane split regression remains green.
CONTEXT | D-09 | No broader topology/job/proof/runner/matrix/cache/partition/rerun drift | 132-19..21 | COVERED | Bounded source census and offline reruns.
VERIFICATION | G1 | Failed state reset | 132-19 | COVERED | Terminal immutable regression.
VERIFICATION | G2 | Values above two accepted | 132-19 | COVERED | Pre-mutation CLI bound.
VERIFICATION | G3 | Trigger polling lacks creation deadline | 132-19 | COVERED | Persisted epoch and timeout regression.
VERIFICATION | G4 | Canonical discovery reads first page only | 132-19 | COVERED | Paginated population regression.
VERIFICATION | G5 | Running-state schema/identity resume validation | 132-19 | COVERED | Valid/invalid resume matrix.
VERIFICATION | G6 | Fresh preservation and immutable subject | 132-20 | COVERED | Full receipt and preflight.
VERIFICATION | G7 | Fresh qualifying exact-ten API receipt | 132-21 | COVERED | Sole live acceptance authority.
</source_audit>

<verification>
Only the Actions API-backed explicit verifier and completed-state verifier may close CI-14. Any incomplete, threshold-missing, identity-mismatched, page-truncated, rerun-inclusive, or sequence-exhausted result remains non-green without human substitution.
</verification>

<success_criteria>
- One current exact-ten receipt passes median <=480 and p95 <=600.
- Complete paginated population, identity, non-overlap, and no-rerun properties reverify from live APIs.
- COV-05, SAFE-02, and D-01/D-08/D-09 remain preserved on the bound source subject.
</success_criteria>

<output>
Create `.planning/phases/132-measured-closure/132-21-SUMMARY.md` when done. Include the captured live, explicit-verify, and completed-reentry statuses and outputs; record PASS only for the validated 0/0/0 branch, record `gaps_found` for the validated 2/1/2 threshold-miss branch, and never mark CI-14 closed unless both live thresholds and every integrity/preservation authority pass.
</output>

---
phase: 132-measured-closure
plan: 12
type: execute
wave: 12
depends_on: [132-11]
files_modified:
  - scripts/ci/collect_pr_timing_receipt.sh
  - test/install_smoke/ci_timing_automation_test.exs
autonomous: true
gap_closure: true
requirements: [CI-14, COV-05, SAFE-02]
must_haves:
  truths:
    - "Collection and API verification use one PR-bound canonical population: every qualifying successful first-attempt pull-request run associated by the GitHub API with the same repository and numeric PR on the exact immutable head, with population equality rather than contiguous-subset selection per D-04."
    - "A qualifying publication-triggered run is sample 1 and only nine label triggers follow; a failed, cancelled, or non-attempt-1 publication run is excluded and consumes the first of the existing two bounded sequences."
    - "A locally completed PASS state never exits zero until the current exact-ten canonical population is re-resolved against the live GitHub Actions API and both inclusive CI-14 thresholds pass per D-03 through D-05."
    - "Explicit verify and completed-state run use one live threshold authority: equality at median 480 and p95 600 passes, while median 481 or p95 601 returns the documented honest timing-miss result even if local state says PASS."
    - "Read-only preflight accepts exact remote/local equality or the authorized publication-ready topology where the remote PR head is a strict ancestor of the local preserved head, validates the source-derived two-stage transition, and mutates no remote or durable local surface."
    - "A stale, forged, mixed-head, rerun, failed-summary, non-consecutive, overlapping, or threshold-missing completed PASS state fails closed without mutating the receipt or triggering a workflow."
    - "Run, explicit verify, completed-state revalidation, controller state, and the current source manifest all bind and cross-check the same repository plus numeric PR identity; missing, ambiguous, or mismatched identity fails before evidence can be accepted."
    - "The state lock, two-sequence ceiling, current receipt markers, required `CI Summary` identity, and D-01 gate semantics remain unchanged while controller-owned trigger provenance and immutable PR identity are added to state/source evidence."
    - statement: "SAFE-02 empty and ordering backstop: a missing current section or any count other than ten fails before a verdict; equal durations retain numeric order and inclusive 480/600 comparisons."
      verification: backstop
    - statement: "SAFE-02 concurrency backstop: completed-state revalidation runs under the existing SHA-scoped controller lock and performs no label or workflow mutation."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT accept local controller state, receipt shape, or a stored PASS verdict as a substitute for live Actions identity and timing evidence."
      category: transparency
      status: unverified
      flagged_unverified: true
    - statement: "MUST NOT weaken required-result membership, timing thresholds, attempt/event constraints, or the no-rerun policy to make completed state pass."
      category: safety
      status: unverified
      flagged_unverified: true
    - statement: "MUST NOT erase or rewrite historical failed receipts while correcting completed-state verification."
      category: transparency
      status: unverified
      flagged_unverified: true
  artifacts:
    - path: "scripts/ci/collect_pr_timing_receipt.sh"
      provides: "Shared repository-and-numeric-PR-bound API identity/population/statistics/threshold verification plus publication-ready read-only preflight"
      contains: "verify_api_backed_receipt"
    - path: "test/install_smoke/ci_timing_automation_test.exs"
      provides: "Process-level publication-population, bounded-sequence, no-eleventh-run, and forged/stale completed PASS regressions"
      contains: "completed"
  key_links:
    - from: "publication-triggered exact-head run"
      to: "controller state and current receipt row 1"
      via: "the same qualification helper used for label-triggered runs"
      pattern: "wait_and_validate_run|append_run_state"
    - from: "scripts/ci/collect_pr_timing_receipt.sh completed-state branch"
      to: "verify_api_backed_receipt"
      via: "the same live verifier used by explicit verify mode"
      pattern: "verify_api_backed_receipt"
    - from: "verify --repo/--pr, completed state, and current source manifest"
      to: "GitHub PR and Actions APIs"
      via: "cross-checked repository plus numeric PR identity passed to the shared canonical population authority"
      pattern: "--pr|canonical_eligible_run_ids|pull_requests"
    - from: "preservation transition manifest"
      to: "preflight/run publication authority"
      via: "source-derived prior-to-correction and correction-to-preserved SHA/path/blob validation"
      pattern: "transition-manifest|validate_transition_manifest"
---

<objective>
Make collection, verification, and publication readiness share one fail-closed controller contract.

Purpose: Close both verification blockers: publication-run exclusion and locally plausible completed PASS state bypassing the only CI-14 authority.
Output: Canonical population/threshold verification, source-derived publication preflight, and process-level mutation-invariant regressions.
</objective>

## Artifacts this phase produces

- Modified `scripts/ci/collect_pr_timing_receipt.sh`: adopts one qualifying publication-triggered exact-head run as sample 1; requires repository/numeric-PR-bound selected/canonical population equality; requires `--pr` for explicit verify; persists repository plus numeric PR in state and current source manifest; routes explicit verify and completed-state run through one live threshold authority; and adds a `--transition-manifest` contract used by preflight/run to validate the preservation receipt's source-derived transition.
- New test behavior in `test/install_smoke/ci_timing_automation_test.exs`: publication success yields exactly ten eligible rows with nine later triggers; publication failure/cancellation/attempt-2 stays excluded within the two-sequence ceiling; missing/wrong/mismatched PR identity and same-SHA runs associated with another PR are rejected; threshold, integrity, population, and publication-ready preflight rejections preserve every observed surface.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RUNNING.md
@.planning/phases/132-measured-closure/132-CONTEXT.md
@.planning/phases/132-measured-closure/132-VERIFICATION.md
@.planning/phases/132-measured-closure/132-11-SUMMARY.md
@scripts/ci/collect_pr_timing_receipt.sh
@test/install_smoke/ci_timing_automation_test.exs
</context>

<assumption_delta_decision>
Primary noun: one immutable-head exact-ten receipt. Decision: no-change — detector hits on existing fallback/optional CI terminology, but this correction adds no identity variant or alternate authority.
</assumption_delta_decision>

<assumptions>
- CI-14 generic probe: this plan makes collection and verification use the same repository/numeric-PR-bound full exact-head eligible population, counts a qualifying publication run as sample 1, and resolves the completed-state acceptance edge through process-level regressions and `verify_api_backed_receipt`; the terminal threshold verdict remains explicitly deferred to dependent Plan 132-15 after Plan 132-14 preservation.
- COV-05 generic probe: no coverage verdict is taken here; the authoritative fresh ExCoveralls census and inclusive 82.13% integer comparison are explicitly deferred to dependent Plan 132-14.
- SAFE-02 adjacency probe: the existing controller contract exercised by `mix test test/install_smoke/ci_timing_automation_test.exs --seed 0` remains the authority for inclusive 480/600 boundaries; Plan 132-15 rechecks those same inclusive predicates against live evidence.
- SAFE-02 empty-input probe: this plan's process-level suite requires a current exact-ten section and rejects missing, partial, or count-not-ten completed-state evidence before a verdict.
- SAFE-02 ordering probe: the same controller suite remains the automated authority for numeric duration sorting, stable tie treatment, median ranks 5/6, and nearest-rank p95 rank 10; live receipt agreement is deferred to Plan 132-15.
- SAFE-02 concurrency probe: this plan exercises the existing SHA-scoped state lock and proves completed-state revalidation is read-only; interruption/resume and single-owner live collection are rechecked by Plan 132-15.
- Descriptor-less prohibitions remain flagged-unverified; no check descriptor or silent green status is invented for them.
</assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Canonicalize live thresholds, population, and publication-ready preflight</name>
  <reversibility rating="reversible">The internal controller branch and isolated regression can be reverted without changing a public or persisted product contract.</reversibility>
  <files>scripts/ci/collect_pr_timing_receipt.sh, test/install_smoke/ci_timing_automation_test.exs</files>
  <read_first>
    - AGENTS.md
    - RUNNING.md
    - .planning/phases/132-measured-closure/132-CONTEXT.md
    - .planning/phases/132-measured-closure/132-VERIFICATION.md
    - .planning/phases/132-measured-closure/132-03-SUMMARY.md
    - .planning/phases/132-measured-closure/132-11-SUMMARY.md
    - scripts/ci/collect_pr_timing_receipt.sh
    - test/install_smoke/ci_timing_automation_test.exs
  </read_first>
  <behavior>
    - "RED: simulate fast-forward publication whose first-attempt exact-head PR run succeeds, then prove the old controller excludes it, launches ten more triggers, and cannot satisfy verifier population equality."
    - "GREEN: the successful publication run is validated and appended as sample 1, exactly nine label-triggered samples follow, selected IDs equal the complete API-derived eligible population, and no eleventh run is created."
    - "BOUND: a failed, cancelled, or attempt-2 publication run contributes no row, records the first sequence failure, and leaves only sequence 2 for one exact-ten retry; it never grants two additional sequences."
    - "FAIL CLOSED: an extra successful exact-head run, a forged/stale completed PASS receipt, or any table/API population mismatch exits nonzero without receipt, label, state-run-count, or workflow-run mutation."
    - "PR IDENTITY: explicit verify requires `--pr`; missing PR, wrong PR, same-SHA run associated only with another PR, or CLI/state/current-source-manifest/API repository-or-PR mismatch exits nonzero without mutating receipt, state, labels, locks, or run population."
    - "THRESHOLDS: explicit verify and completed-state run accept API-valid equality at 480/600, return the honest timing-miss result for API-valid 481/600 or 480/601 evidence, and treat forged PASS plus live identity/population/statistics disagreement as integrity failure rather than a timing miss."
    - "PREFLIGHT: exact-head and strict-remote-ancestor publication-ready no-publish cases pass only with an exact source-derived two-stage transition manifest; non-ancestor, undeclared/extra executable path, SHA/blob mismatch, or planning-path concealment fails closed."
    - "READ ONLY: every no-publish case snapshots and preserves remote head, owned-label state, matching workflow-run IDs, controller state/lock, receipt hash/current markers, and performs no push or label mutation."
    - "PRESERVE: a completed PASS state whose exact ten rows equal the live canonical population and meet inclusive thresholds exits zero without triggering or rewriting evidence."
  </behavior>
  <action>
Extend the existing temporary repository, receipt, state directory, and `gh` shim so publish mode can expose a distinct publication-triggered run before label mutations. First add fixture cases for a successful attempt-1 exact-head pull_request publication run followed by nine successes, asserting ten selected IDs equal all API-derived eligible IDs associated with the bound numeric PR, only nine label triggers occur, receipt/state contain exactly ten rows, and no additional run exists. Add paired cases where the publication run is failure, cancelled, or attempt 2: it contributes no timing row, records sequence 1 as failed, and the controller has only sequence 2 available for its bounded retry. Add fixtures for missing `--pr` in explicit verify, a wrong numeric PR, a same-SHA success associated only with another PR, CLI/state/current-source-manifest PR mismatch, repository mismatch, and the valid exact-PR population. Require run finalization, explicit verify, and completed-state verification to reject every mismatch before acceptance. Add a fixture with one extra qualifying exact-head success on the bound PR and require all three paths to reject it, closing any contiguous-slice or cherry-pick loophole. Every rejection snapshots byte-identical receipt and state, label and lock state, and complete workflow-run population. Retain the existing identity/Summary/duration/chronology drift fixtures for all selected rows.

In the controller, extract the run-list eligibility predicate into one `canonical_eligible_run_ids` helper and make collection finalization, explicit `verify`, and completed-state revalidation call that same authority. Its inputs are the normalized repository, workflow, numeric PR, immutable full SHA, and the persisted sequence-start/publication boundary; it resolves the numeric PR through `gh pr view`, confirms repository/PR/head-ref identity, and enumerates only workflow runs whose API `pull_requests` association contains that exact PR, event is `pull_request`, full `head_sha` matches, attempt is 1, status is completed, conclusion is success, exactly one successful completed `CI Summary` supplies complete nonnegative chronology, and ordering is deterministic by `run_started_at` then numeric ID. Branch name may narrow the API query but is never PR identity. Require `--pr` in explicit verify. Persist normalized `repo` and numeric `pr` in both controller state and the `CI_TIMING_CURRENT_SOURCE` JSON; run, explicit verify, and completed-state revalidation must cross-check CLI, state when present, source manifest, `gh pr view`, and every Actions run association before population/statistics evaluation. Missing, nonnumeric, ambiguous, or mismatched identity is integrity failure.

Snapshot the bound-PR exact-head run IDs before fast-forward publication, persist the start/publication boundary, wait for the published head to quiesce, require exactly one newly observed publication-triggered run associated with that PR, and feed that run through `wait_and_validate_run`, chronology validation, and `append_run_state` just like a label-triggered sample. Persist `.publication_trigger` identity/disposition plus controller-owned `.trigger_provenance.publication_run_id` and `.trigger_provenance.label_run_ids` in state so interruption, trigger-count proof, and diagnostics remain attributable without changing receipt table row shape; provenance is evidence, not a selectable boundary. If the publication run is nonqualifying, call the existing sequence-error/restart machinery so it consumes sequence 1 and is excluded from `.runs`; do not add a third opportunity. Replace the post-collection contiguous-subsequence predicate with exact equality between selected IDs and the sorted complete bound-PR canonical population returned by the shared helper. A same-SHA success belonging to another PR is excluded, while any extra eligible success on the bound PR causes equality failure; the boundary cannot be moved to select around it. Keep the additional Summary, duration, and non-overlap checks; do not add a receipt selector or user-controlled exclusion.

Make threshold evaluation an explicit shared sub-verifier inside the API-backed receipt authority. It must recompute median and nearest-rank p95 from the ten live-validated durations, compare the recomputed values both to the stored manifest statistics and inclusively to `median_max`/`p95_max`, and distinguish return semantics: an API-valid receipt that misses a threshold returns the controller's honest timing-miss status; malformed, forged, identity/population/chronology/statistics disagreement returns an integrity failure. Route explicit `verify` and the completed-state `run` branch through that complete authority before consulting stored `.verdict`, so a forged PASS cannot bypass live checks. Add process fixtures for each entry point covering 480/600 equality, 481 median miss, 601 p95 miss, stale/forged PASS, identity/API failure, recomputation mismatch, and extra/partial eligible population. On every rejection assert byte-identical receipt, state and marker counts, unchanged owned-label state and workflow-run population, and no new trigger; retain the positive completed-state no-retrigger case.

Replace the controller's post-preservation fixed non-planning allowlist with a required `--transition-manifest PATH` interface for `preflight` and `run`. The manifest is the single machine-readable JSON block embedded between dedicated markers in the post-Plan-13 preservation receipt. Define and test its schema as two auditable stages: prior preserved subject -> Plan 132-12 controller correction, then correction -> post-Plan-13 preserved subject. Each stage carries full from/to SHAs, its complete non-planning name/status set, and target blob OIDs; `.planning/**` changes are enumerated separately rather than silently ignored. Validate marker uniqueness, schema, SHA ancestry, exact `git diff --name-status` equality, target blob identities, correction/preserved arguments, and absence of any non-planning delta after the preserved subject. Reject undeclared, extra, moved, or deleted executable paths and any attempt to classify executable drift as planning evidence. The manifest is provenance, never a user-selectable population boundary.

Change no-publish preflight semantics after manifest validation: exact remote/local head equality passes; otherwise require the open PR's remote head to be a strict ancestor of the manifest-bound local preserved head. Validate PR/repository/workflow/Summary/label identity, fast-forward ancestry, absence of controller ownership, current-marker/state/lock prerequisites, sample/sequence/threshold constants, and all existing safety invariants, but do not fetch/push, add/remove labels, create state/lock files, rewrite the receipt, or trigger/rerun Actions. Keep run mode's actual fast-forward-only push, single-publication ownership, and non-ancestor rejection unchanged. Extend the `gh`/git process fixture with equal-head success, strict-ancestor publication-ready success, non-ancestor rejection, transition-manifest SHA/blob/path rejection, and before/after equality for remote head, label, matching run IDs, state, lock, receipt hash/current-marker counts, and push/label mutation counters.
  </action>
  <acceptance_criteria>
    - Source and process tests prove collection, explicit verify, and completed-state revalidation call one `canonical_eligible_run_ids` authority and derive the identical repository/workflow/numeric-PR/full-SHA/boundary-scoped population with exact selected/population equality.
    - A successful publication run is row 1, nine later triggers yield exactly ten rows, and no eleventh workflow run exists; failed, cancelled, and attempt-2 publication variants are excluded while consuming sequence 1.
    - Extra eligible runs and stale/forged completed PASS evidence exit nonzero without receipt, label, state-run-count, or workflow-run mutation; a live-valid inclusive-boundary receipt still passes.
    - Explicit verify and completed-state run both accept live 480/600 equality, return the honest timing-miss result for 481/600 or 480/601, and reject forged PASS/integrity failures through a distinct non-green path with all observed surfaces unchanged.
    - Explicit verify requires `--pr`; state and current source manifest persist repository plus numeric PR; missing/wrong PR, same-SHA other-PR association, and CLI/state/manifest/API mismatch fail before verdict with receipt, state, labels, locks, and workflow-run population unchanged.
    - `preflight --no-publish` passes for exact head and strict remote-ancestor publication-ready topology only when the preservation transition manifest exactly matches Git source; non-ancestor or unauthorized path/blob drift fails, and every preflight outcome is mutation-free.
    - No workflow, topology, product, schema/migration, dependency, public API, telemetry/error, Admin, or release-proof file changes.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/install_smoke/ci_timing_automation_test.exs --seed 0 &amp;&amp; bash -n scripts/ci/collect_pr_timing_receipt.sh &amp;&amp; ./scripts/maintainer/automation_first_contract.sh</automated>
  </verify>
  <done>Collection, explicit verification, and completed-state resume accept the same repository-and-numeric-PR-bound complete exact-ten eligible population; publication cannot create an excluded eleventh success or an unbounded retry, another PR's same-SHA run cannot enter the population, and every PASS is live API revalidated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (ASVS L1; high severity blocks execution)

| Boundary | Description |
| --- | --- |
| local state/receipt -> controller verdict | Disk evidence is attacker- or staleness-modifiable and cannot decide CI-14. |
| GitHub Actions API -> receipt verifier | Remote run/job identity and timestamps become acceptance evidence only after exact validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-132-12-01 | Tampering | completed state, receipt, and stored thresholds | high | mitigate | Recompute statistics and enforce both inclusive thresholds inside the shared live verifier before honoring PASS. |
| T-132-12-02 | Spoofing | run/SHA/summary identity | high | mitigate | Reuse exact run, event, attempt, SHA, status, conclusion, and single-summary API checks. |
| T-132-12-07 | Spoofing | repository/PR population identity | high | mitigate | Require explicit numeric PR identity, persist repo/PR in state and source manifest, cross-check CLI/state/manifest/PR API, and filter every run by API PR association through the shared canonical helper. |
| T-132-12-03 | Repudiation | publication run / selected population | high | mitigate | Adopt a qualifying publication run as row 1 and require full selected/API population equality, never a contiguous subset. |
| T-132-12-04 | Denial of service | publication failure and repeated resume | medium | mitigate | Charge a nonqualifying publication run to sequence 1, preserve the two-sequence ceiling, and keep completed revalidation read-only. |
| T-132-12-05 | Information disclosure | gh credentials | low | accept | Existing `gh` calls expose public run metadata only and never persist or print tokens. |
| T-132-12-06 | Elevation of privilege | publication-ready preflight | high | mitigate | Require strict ancestry plus exact source-derived transition manifest and prove no-publish leaves remote/local durable state unchanged. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Safe measurable closure without weaker evidence | 132-12..15 | COVERED | Controller fix precedes preservation and live receipt.
REQ | CI-14 | API-backed exact-ten timing verdict | 132-12, 132-15 | COVERED | Publication, collection, and both verifier paths share one repository/numeric-PR-bound population authority.
REQ | COV-05 | Coverage remains >=82.13% | 132-14 | COVERED | Same final subject before sampling.
REQ | SAFE-02 | Focused/full preservation and edge behavior | 132-12, 132-14 | COVERED | Stale-state and empty/exact-ten paths are executable.
RESEARCH | — | Reuse the existing controller and fail closed | 132-12 | COVERED | Shared live thresholds and source-derived preflight extend the existing authority.
CONTEXT | D-01 | Preserve required summary/gates | 132-12 | COVERED | No workflow edit.
CONTEXT | D-03..D-05 | Strict exception, exact-ten identity, inclusive thresholds | 132-12, 132-15 | COVERED | Live verifier is mandatory.
</source_audit>

<verification>
Run the focused process-level controller suite, shell syntax check, and automation-first contract. The regression must demonstrate a fail-first stale/forged completed PASS case.
</verification>

<success_criteria>
- Local completed PASS state cannot close CI-14 without fresh live API revalidation.
- Run, explicit verify, and completed-state revalidation accept only one cross-checked repository/numeric-PR identity, and same-SHA runs from another PR cannot enter or redefine the population.
- Valid completed state remains safely resumable without retriggering.
- Controller and required-gate policies remain unchanged outside the verified branch.
</success_criteria>

<output>
Create `.planning/phases/132-measured-closure/132-12-SUMMARY.md` when done.
</output>

---
phase: 132-measured-closure
plan: 10
type: execute
wave: 10
depends_on: [132-09]
files_modified:
  - .planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
autonomous: true
requirements: [CI-14, COV-05, SAFE-02]
must_haves:
  truths:
    - "The final topology correction is bound to one full immutable code-bearing SHA and an exact diff containing only the D-08 workflow edge change plus its test and deterministic fixture."
    - "The exact topology/self-containment contract, summary evaluator, quality signals, SAFE-01, automation-first policy, hygiene, authoritative coverage, and packed image consumer all pass after the final source edit and before live sampling per D-01, D-06, and D-09."
    - "One authoritative coverage invocation produces a valid ExCoveralls `source_files[].coverage[]` artifact; machine-extracted integer covered/relevant counts have a positive denominator, satisfy the inclusive 82.13% COV-05 floor, and are recorded with their ratio in the preservation receipt per D-07."
    - "A fail-closed sampler preflight binds correction SHA, preserved subject SHA, repository, PR, workflow, summary name, ten samples, two-sequence ceiling, and inclusive thresholds without triggering or sampling a workflow."
    - statement: "SAFE-02 empty/singleton backstop: missing artifacts, missing expected graph members, a partial six-edge correction, or an empty coverage JSON fails preservation."
      verification: backstop
    - statement: "SAFE-02 ordering/concurrency backstop: preservation runs after the final code edit and sampler preflight confirms one immutable subject before the single live controller may acquire its lock."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT launch, label-trigger, rerun, or select a CI timing sample before every preservation and preflight gate passes."
      category: safety
      status: unresolved
      verification: null
      flagged_unverified: true
    - statement: "MUST NOT add tests to increase coverage or describe Admin, public API, schema/migration, telemetry/error, dependency, release, or broader workflow drift as preserved."
      category: transparency
      status: unresolved
      verification: null
      flagged_unverified: true
    - statement: "MUST NOT edit the controller's delayed-discovery lock, pending-trigger recovery, anomaly exclusions, receipt statistics, or rerun policy during this evidence-only plan."
      category: safety
      status: unresolved
      verification: null
      flagged_unverified: true
  artifacts:
    - path: ".planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md"
      provides: "Recovery topology subject SHA, exact diff, ordered command evidence, canonical machine-derived coverage counts/ratio, prohibited-surface census, and no-trigger sampler preflight"
      contains: "Recovery topology final preservation census"
    - path: ".planning/phases/132-measured-closure/132-10-PLAN.md"
      provides: "Executable final preservation and preflight plan"
      contains: "D-06"
    - path: ".planning/phases/132-measured-closure/132-VALIDATION.md"
      provides: "Recovery task-to-test mapping for 132-10"
      contains: "132-10-01"
    - path: ".planning/ROADMAP.md"
      provides: "Wave 10 final preservation ordering before live acceptance"
      contains: "132-10-PLAN.md"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: ".planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md"
      via: "immutable SHA and bounded name/status diff census identify the exact D-08 subject"
      pattern: "six-edge|D-08"
    - from: "cover/excoveralls.json"
      to: ".planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md"
      via: "repository-native `source_files[].coverage[]` parsing emits canonical covered/relevant/ratio JSON that is compared byte-for-byte with the bounded receipt record"
      pattern: "RECOVERY_COVERAGE_CENSUS_V1_BEGIN"
    - from: ".planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md"
      to: "scripts/ci/collect_pr_timing_receipt.sh"
      via: "recorded correction and preserved-subject SHAs become the locked sampler inputs"
      pattern: "correction.*SHA|preserved.*SHA"
---

<objective>
Bind the exact topology recovery to an immutable head and re-prove all local preservation authorities before any live sample is permitted.

Purpose: Prevent a projected or eventual timing improvement from concealing gate, coverage, product-surface, or evidence-controller drift.
Output: A `Recovery topology final preservation census` with exact subject identity, ordered automated evidence, inclusive coverage arithmetic, prohibited-surface diff review, and immutable sampler preflight.
</objective>

## Artifacts this phase produces

- New `.planning/phases/132-measured-closure/132-10-PLAN.md`: the executable final-census prompt, threat model, and source audit.
- Updated `.planning/ROADMAP.md` and `.planning/phases/132-measured-closure/132-VALIDATION.md`: Wave 10 ordering and 132-10 automated verification mapping.
- Updated `.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md`: appended recovery topology code-bearing SHA, exact bounded diff, focused/full command results, fresh covered/relevant counts and inclusive arithmetic, prohibited-surface census, and controller preflight inputs/result. No workflow, controller, test, application symbol, or public API is modified by this evidence-only plan.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RUNNING.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/132-measured-closure/132-CONTEXT.md
@.planning/phases/132-measured-closure/132-RESEARCH.md
@.planning/phases/132-measured-closure/132-09-SUMMARY.md
@.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
@.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
@.github/workflows/ci.yml
@scripts/ci/collect_pr_timing_receipt.sh
@scripts/ci/eval_ci_summary.sh
@scripts/ci/test_ci_summary_gate.sh
@scripts/maintainer/refactor_contract.sh
@scripts/install_smoke.sh
@test/install_smoke/ci_lane_split_test.exs
@test/install_smoke/ci_observability_test.exs
@test/install_smoke/ci_timing_automation_test.exs
@test/fixtures/ci_timing/phase_132_topology_projection.json
</context>

<assumptions>
- CI-14 remains open after this plan; immutable-head local evidence and controller preflight are necessary but cannot substitute for 132-11's live API receipt.
- COV-05 is resolved here by one authoritative coverage run and inclusive integer arithmetic, not by recalled counts from 132-07.
- SAFE-02's generic adjacency/equality case is inclusive at 82.13% here and 480/600 in 132-11. Empty/partial inputs fail via non-empty artifact, exact topology membership, and exact subject checks. Ordering is the locked preservation-before-sampling sequence.
- The generic CI-14/COV-05 probe classification remains explicitly flagged rather than silently inferred. Repository-native authorities below are the defensible concrete predicates.
- The preflight may use authenticated read-only `gh` queries but must run with `--no-publish`; a remote-head mismatch is a failure to record, not permission to push or sample.
- This receipt-only change is reversible and contains no one-way decision.
</assumptions>

<tasks>

<!-- planner-discipline-allow: RECOVERY_COVERAGE_CENSUS_V1_BEGIN -->
<!-- planner-discipline-allow: RECOVERY_COVERAGE_CENSUS_V1_END -->

<task type="tracer">
  <name>Task 1: Re-census and preflight the immutable recovery subject</name>
  <precondition>`.planning/phases/132-measured-closure/132-09-SUMMARY.md` exists, its focused topology/gate suite is green, and its recorded source commit resolves locally.</precondition>
  <reversibility rating="reversible">The task appends reproducible evidence to one planning receipt and does not alter source, persisted data, or a published interface.</reversibility>
  <files>.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md</files>
  <read_first>
    - AGENTS.md
    - RUNNING.md
    - .planning/phases/132-measured-closure/132-CONTEXT.md
    - .planning/phases/132-measured-closure/132-RESEARCH.md
    - .planning/phases/132-measured-closure/132-09-SUMMARY.md
    - .planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
    - .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
    - .github/workflows/ci.yml
    - scripts/ci/collect_pr_timing_receipt.sh
    - scripts/ci/eval_ci_summary.sh
    - scripts/ci/test_ci_summary_gate.sh
    - scripts/maintainer/refactor_contract.sh
    - scripts/install_smoke.sh
    - test/install_smoke/ci_lane_split_test.exs
    - test/install_smoke/ci_observability_test.exs
    - test/install_smoke/ci_timing_automation_test.exs
    - test/fixtures/ci_timing/phase_132_topology_projection.json
  </read_first>
  <action>
Resolve the final 132-09 source commit and current full HEAD, then append a `Recovery topology final preservation census` to `132-PRESERVATION-RECEIPT.md`. Record both full SHAs and an exact name/status diff from the previously preserved subject through the new code-bearing subject. Prove that the only new non-planning paths are `.github/workflows/ci.yml`, `test/install_smoke/ci_lane_split_test.exs`, and `test/fixtures/ci_timing/phase_132_topology_projection.json`; inspect the workflow patch itself and record that it removes exactly the three authorized declarations, no other executable workflow line, and no Admin, product/public API, schema/migration, telemetry/error, dependency/lockfile, release-proof, controller, or rerun surface per D-09.

Run, in order and after the final source edit: the focused topology/observability/timing automation suite; the Bash `CI Summary` evaluator contract; `mix quality_signals`; SAFE-01; automation-first contract; CI hygiene; exactly one `mix coveralls.multiple --type local --type json` invocation; then the packed image consumer. Parse the fresh artifact using the repository's established ExCoveralls shape: flatten `source_files[].coverage[]`, reject a missing/empty `source_files` array, reject any missing/non-array coverage vector, reject any non-null coverage entry that is not a non-negative integer, count non-null entries as `relevant`, and count positive entries as `covered`. Fail when `relevant` is not a positive integer or `covered` is outside `0..relevant`; enforce `covered * 10000 &gt;= relevant * 8213` so equality passes per D-07.

Generate one canonical compact JSON object directly from that parser with integer `covered`, integer `relevant`, numeric `ratio` (`covered / relevant`), `threshold_basis_points: 8213`, and `passes: true`. Append that exact machine output between unique `RECOVERY_COVERAGE_CENSUS_V1_BEGIN` / `RECOVERY_COVERAGE_CENSUS_V1_END` lines in the immutable preservation receipt; do not transcribe or round the counts/ratio by hand. Record commands and exits without inventing totals.

Finally run the existing controller's `preflight` with `--no-publish`, repository `szTheory/rindle`, PR `96`, workflow `ci.yml`, summary job `CI Summary`, label `ci-timing-sample`, exactly ten samples, at most two sequences, inclusive 480/600 thresholds, correction SHA from 132-09, and the full current preserved subject. This preflight may read GitHub identity/auth state but must not publish, label, trigger, or sample. Record pass only if the PR head already equals the immutable subject and every controller identity/ancestry/ownership check succeeds; otherwise record the exact fail-closed condition for 132-11's publication path. Confirm no live controller/lock or owned label remains. Any focused, preservation, coverage, diff, or identity failure blocks 132-11.
  </action>
  <acceptance_criteria>
    - The receipt names one full code-bearing topology SHA and one final preserved-subject SHA and provides an exact, bounded diff census.
    - Focused graph/self-containment/projection, observability, timing-controller, summary evaluator, quality, SAFE-01, automation-first, hygiene, coverage, and packed image authorities all pass after the final source edit.
    - Fresh ExCoveralls coverage parsing fails closed on missing shape, non-integer entries, or a zero denominator; its integer counts satisfy the inclusive 82.13% floor from one authoritative invocation, with no percentage-only test.
    - The receipt contains exactly one bounded recovery coverage record whose canonical machine-derived `covered`, `relevant`, and `ratio` object exactly matches the fresh artifact.
    - The no-publish preflight records all locked sampler parameters and either passes on the exact remote head or fails closed without mutation; no sample, label trigger, rerun, or controller lock remains.
    - Receipt evidence explicitly preserves D-08/D-09 scope, the complete summary/observability memberships, adopter prerequisites, job bodies, controller recovery/locking behavior, and all prohibited surfaces.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail; RECEIPT=.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md; STATE_DIR=.gsd/ci-timing/phase-132-recovery-preflight; CORRECTION_SHA="$(git log -n 1 --format=%H -- .github/workflows/ci.yml)"; PRESERVED_SUBJECT_SHA="$(git rev-parse HEAD)"; test "${#CORRECTION_SHA}" -eq 40; test "${#PRESERVED_SUBJECT_SHA}" -eq 40; git cat-file -e "$CORRECTION_SHA^{commit}"; git cat-file -e "$PRESERVED_SUBJECT_SHA^{commit}"; git merge-base --is-ancestor "$CORRECTION_SHA" "$PRESERVED_SUBJECT_SHA"; grep -Fq "$CORRECTION_SHA" "$RECEIPT"; grep -Fq "$PRESERVED_SUBJECT_SHA" "$RECEIPT"; test ! -e "$STATE_DIR"; ! gh pr view 96 --repo szTheory/rindle --json labels --jq '.labels[].name' | grep -Fxq ci-timing-sample; RECEIPT_HASH_BEFORE="$(shasum -a 256 "$RECEIPT")"; RUNS_BEFORE="$(mktemp)"; RUNS_AFTER="$(mktemp)"; trap 'rm -f "$RUNS_BEFORE" "$RUNS_AFTER"' EXIT; gh run list --repo szTheory/rindle --workflow ci.yml --event pull_request --limit 100 --json databaseId,headSha,createdAt &gt; "$RUNS_BEFORE"; PREFLIGHT_OUTPUT="$(bash scripts/ci/collect_pr_timing_receipt.sh preflight --repo szTheory/rindle --pr 96 --workflow ci.yml --summary-job "CI Summary" --label ci-timing-sample --samples 10 --max-sequences 2 --median-max 480 --p95-max 600 --correction-sha "$CORRECTION_SHA" --preserved-subject-sha "$PRESERVED_SUBJECT_SHA" --receipt "$RECEIPT" --state-dir "$STATE_DIR" --no-publish --poll-seconds 15)"; printf '%s\n' "$PREFLIGHT_OUTPUT" | grep -Fqx "[ci-timing] preflight passed: szTheory/rindle#96 @ $PRESERVED_SUBJECT_SHA"; test "$(shasum -a 256 "$RECEIPT")" = "$RECEIPT_HASH_BEFORE"; test ! -e "$STATE_DIR"; ! gh pr view 96 --repo szTheory/rindle --json labels --jq '.labels[].name' | grep -Fxq ci-timing-sample; gh run list --repo szTheory/rindle --workflow ci.yml --event pull_request --limit 100 --json databaseId,headSha,createdAt &gt; "$RUNS_AFTER"; cmp -s "$RUNS_BEFORE" "$RUNS_AFTER"; mix test test/install_smoke/ci_lane_split_test.exs test/install_smoke/ci_observability_test.exs test/install_smoke/ci_timing_automation_test.exs --seed 0 &amp;&amp; bash scripts/ci/test_ci_summary_gate.sh &amp;&amp; mix quality_signals &amp;&amp; bash scripts/maintainer/refactor_contract.sh &amp;&amp; ./scripts/maintainer/automation_first_contract.sh &amp;&amp; ./scripts/maintainer/repo_hygiene_check.sh --ci &amp;&amp; mix coveralls.multiple --type local --type json &amp;&amp; test -s cover/excoveralls.json; COVERAGE_FACT="$(jq -ceS 'if ((.source_files | type) != "array" or (.source_files | length) == 0) then error("missing source_files coverage data") elif any(.source_files[]; ((.coverage | type) != "array") or any(.coverage[]; . != null and ((type != "number") or (. &lt; 0) or (floor != .)))) then error("coverage entries must be null or non-negative integers") else [.source_files[].coverage[] | select(. != null)] as $lines | ([$lines[] | select(. &gt; 0)] | length) as $covered | ($lines | length) as $relevant | if (($covered | type) != "number" or ($covered | floor) != $covered or ($relevant | type) != "number" or ($relevant | floor) != $relevant or $relevant == 0 or $covered &gt; $relevant) then error("invalid covered/relevant counts") else {covered:$covered,relevant:$relevant,ratio:($covered / $relevant),threshold_basis_points:8213,passes:($covered * 10000 &gt;= $relevant * 8213)} | select(.passes == true) end end' cover/excoveralls.json)"; test "$(grep -Fxc 'RECOVERY_COVERAGE_CENSUS_V1_BEGIN' "$RECEIPT")" -eq 1; test "$(grep -Fxc 'RECOVERY_COVERAGE_CENSUS_V1_END' "$RECEIPT")" -eq 1; RECORDED_COVERAGE="$(awk '$0 == "RECOVERY_COVERAGE_CENSUS_V1_BEGIN" {getline; print; found=1; exit} END {if (!found) exit 1}' "$RECEIPT")"; test "$RECORDED_COVERAGE" = "$COVERAGE_FACT"; printf '%s\n' "$RECORDED_COVERAGE" | jq -e '(.covered | type) == "number" and (.covered | floor) == .covered and (.relevant | type) == "number" and (.relevant | floor) == .relevant and .relevant &gt; 0 and .covered * 10000 &gt;= .relevant * 8213 and .passes == true' &gt;/dev/null; bash scripts/install_smoke.sh image</automated>
  </verify>
  <done>The exact recovery subject is immutable, COV-05 and SAFE-02 are freshly proven, and a no-trigger preflight authorizes 132-11 to become the sole live controller only after all local gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (ASVS L1; high severity blocks execution)

| Boundary | Description |
|----------|-------------|
| git history -> preservation receipt | Subject identity and bounded diff determine what evidence applies to. |
| test/coverage outputs -> closure evidence | Generated artifacts must be non-empty, current, and arithmetically recomputed. |
| local controller preflight -> GitHub PR | Authentication and PR identity are read, but mutation is forbidden in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-132-10-01 | Spoofing | preserved subject identity | high | mitigate | Resolve full SHAs locally, verify ancestry, and record exact name/status and workflow-patch censuses. |
| T-132-10-02 | Tampering | preservation receipt and coverage arithmetic | high | mitigate | Run authorities after final source edit, require non-empty JSON, and derive fresh integer coverage arithmetic. |
| T-132-10-03 | Elevation of privilege | GitHub preflight credentials | high | mitigate | Use existing authenticated `gh` without printing tokens and force `--no-publish`; any head mismatch fails closed. |
| T-132-10-04 | Repudiation | omitted preservation failure | medium | mitigate | Record every ordered command and exit; any miss blocks 132-11 instead of becoming an exception. |
| T-132-10-05 | Denial of service | duplicate controller/label | medium | mitigate | Do not enter run mode; confirm no lock/process or owned trigger label remains after preflight. |
| T-132-10-06 | Information disclosure | receipt contents | low | accept | Store public SHAs, commands, counts, and GitHub identities only; never store credentials or raw secret-bearing environment. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Preserve ratchets before fresh measurement | 132-10 | COVERED | Immutable subject and ordered authorities gate sampling.
REQ | CI-14 | Comparable immutable-head live acceptance | 132-10, 132-11 | COVERED | Preflight here; live receipt next.
REQ | COV-05 | Authoritative coverage at least 82.13% | 132-10 | COVERED | One fresh non-empty run and inclusive arithmetic.
REQ | SAFE-02 | Focused/full proof and prohibited-surface review | 132-10 | COVERED | Complete ordered census.
RESEARCH | — | Do not blindly re-sample | 132-09, 132-10 | COVERED | Topology then preservation/preflight.
RESEARCH | — | Reuse existing API-backed controller | 132-10, 132-11 | COVERED | No new measurement system.
CONTEXT | D-01 | Required summary/gate preserved | 132-09, 132-10 | COVERED | Re-run source and evaluator contracts.
CONTEXT | D-02 | Evidence-guided correction | 132-09 | COVERED | Already causal and bounded.
CONTEXT | D-03 | Exception requires evidence/owner/date | 132-11 | COVERED | No exception planned here.
CONTEXT | D-04 | Fresh sequential exact-head ten-run set | 132-11 | COVERED | This plan gates it.
CONTEXT | D-05 | Inclusive 480/600 acceptance | 132-11 | COVERED | Parameters frozen in preflight.
CONTEXT | D-06 | Final correction preservation census | 132-10 | COVERED | Direct task action.
CONTEXT | D-07 | Inclusive coverage/no percentage-only tests | 132-10 | COVERED | Direct task action.
CONTEXT | D-08 | Exact six scheduling edges | 132-09, 132-10 | COVERED | Diff census re-proves it.
CONTEXT | D-09 | Every other graph/body/proof surface fixed | 132-09, 132-10 | COVERED | Source contract plus bounded diff.
</source_audit>

<verification>
No live run is an acceptable substitute for a failed local authority. The preservation receipt must be complete, current, immutable-head-bound, and automation-first before 132-11 starts.
</verification>

<success_criteria>
- COV-05 and SAFE-02 pass on the exact D-08/D-09 source subject.
- The sampler's immutable inputs and mutation boundary are preflighted without launching CI.
- Any failed authority leaves CI-14 open and blocks the live sampler.
</success_criteria>

<output>
Create `.planning/phases/132-measured-closure/132-10-SUMMARY.md` when done.
</output>

---
phase: 132-measured-closure
plan: 22
type: execute
wave: 22
depends_on: [132-21]
files_modified:
  - scripts/ci/collect_pr_timing_receipt.sh
  - test/install_smoke/ci_timing_automation_test.exs
  - scripts/maintainer/automation_first_contract.sh
  - test/install_smoke/automation_first_contract_test.exs
  - .planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
autonomous: true
gap_closure: true
requirements: [SAFE-02]
must_haves:
  truths:
    - "Every GitHub API request made while waiting for trigger creation, publication quiescence, run completion, jobs, or canonical population is bounded by the caller's absolute deadline; persistent primary or secondary rate limiting produces a distinguishable timeout, terminal failed state, and owned label/lock/process cleanup."
    - "The automation-first contract discovers every checkpoint:human-action task regardless of opening-tag attribute order or single/double quote style, rejects any non-authorization purpose or acceptance/verification-bearing action, and continues to allow authorization-only credential bootstrap that cannot satisfy a requirement."
    - "The already accepted CI-14 exact-ten receipt remains byte-stable and independently API-verifies at 453-second median and 481-second p95; no new timing sample is triggered because this repair does not alter `.github/workflows/ci.yml` or its required path."
    - "COV-05 remains at least 82.13% from one structurally valid positive-denominator coverage artifact, and the fixed source subject passes focused proof, quality signals, SAFE-01, packed-consumer, topology, hygiene, and automation-first authorities per D-01/D-06/D-07/D-08/D-09."
    - "Spec-less SAFE-02 backstops remain executable: exact-deadline adjacency is inclusive, missing/empty API or task-block inputs fail closed, deadline and task-block evaluation order is deterministic, and concurrent controller ownership remains single-lock bounded."
  artifacts:
    - path: "scripts/ci/collect_pr_timing_receipt.sh"
      provides: "Deadline-aware GitHub API retry and terminal cleanup across every controller wait/poll path"
    - path: "test/install_smoke/ci_timing_automation_test.exs"
      provides: "Deterministic persistent-rate-limit regressions for creation, quiescence, run, jobs, and canonical population"
    - path: "scripts/maintainer/automation_first_contract.sh"
      provides: "Order- and quote-independent checkpoint task-block discovery with authorization-only enforcement"
    - path: "test/install_smoke/automation_first_contract_test.exs"
      provides: "Adversarial reordered/single-quoted acceptance and verification fixtures plus allowed credential authorization"
    - path: ".planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md"
      provides: "Git-reproduced fixed-source manifest and replayed CI-14/COV-05/SAFE-02 machine evidence"
  key_links:
    - from: "persisted/local absolute wait deadlines"
      to: "gh_api_json"
      via: "deadline argument checked before every request and before every retry sleep, with a distinct timeout status propagated to terminal failure"
      pattern: "gh_api_json|deadline|124"
    - from: "XML-like task opening tag"
      to: "authorization-only policy"
      via: "complete task-block extraction followed by order/quote-independent type-attribute classification"
      pattern: "checkpoint:human-action|authorization|credential-bootstrap"
    - from: "fixed source subject"
      to: "Phase 132 acceptance evidence"
      via: "focused suites, full preservation census, current receipt verifier, and Git-derived active manifest"
      pattern: "PRESERVATION_TRANSITION_V2_BEGIN|CI_TIMING_CURRENT_SOURCE_BEGIN"
  prohibitions:
    - statement: "A persistent rate-limit response must not retain controller mutation authority beyond a configured deadline."
      status: unverified
      flagged: true
    - statement: "Alternate valid attribute ordering or quote style must not convert human acceptance into automated evidence."
      status: unverified
      flagged: true
    - statement: "The repairs must not rewrite the accepted exact-ten receipt or alter required CI topology, proof, rerun, threshold, coverage, or release contracts."
      status: unverified
      flagged: true
---

<objective>
Close the two automated SAFE-02 blockers without reopening the accepted CI-14 or COV-05 evidence.

Purpose: Make controller deadlines and automation-first plan enforcement genuinely fail closed under adversarial API and markup inputs.
Output: Two TDD-backed safety repairs plus a Git-reproduced preservation receipt that replays every machine authority on the fixed source subject.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@RUNNING.md
@guides/release_publish.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/132-measured-closure/132-CONTEXT.md
@.planning/phases/132-measured-closure/132-RESEARCH.md
@.planning/phases/132-measured-closure/132-REVIEW.md
@.planning/phases/132-measured-closure/132-VERIFICATION.md
@.planning/phases/132-measured-closure/132-20-SUMMARY.md
@.planning/phases/132-measured-closure/132-21-SUMMARY.md
@.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
@.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
@scripts/ci/collect_pr_timing_receipt.sh
@test/install_smoke/ci_timing_automation_test.exs
@scripts/maintainer/automation_first_contract.sh
@test/install_smoke/automation_first_contract_test.exs
@.github/workflows/ci.yml
</context>

## Artifacts this phase produces

- A controller whose authenticated API retry loop cannot outlive creation, publication-quiescence, run, jobs, or population deadlines.
- An automation-first parser that classifies complete human-action task blocks independently of attribute order and quote style.
- A refreshed preservation receipt binding the final repair commit while retaining the existing exact-ten timing receipt unchanged.
- `132-22-SUMMARY.md` with RED/GREEN commits, exact commands, coverage counts, receipt-verifier output, and bounded-diff evidence.

## Spec-less probe fallback

- **CI-14 unclassified — resolved by preserved machine evidence:** explicit live API verification of the existing exact-ten receipt must still return success with its immutable head, ten IDs, 453-second median, and 481-second p95. No timing controller `run` or label mutation is authorized.
- **COV-05 unclassified — resolved by a fresh authority:** one `mix coveralls.multiple --type local --type json` run must yield a structurally valid positive denominator and satisfy `covered * 10000 >= relevant * 8213`.
- **SAFE-02 adjacency — resolved:** the API helper may perform work while `now < deadline`, but `now == deadline` is expired and cannot request or sleep again; coverage equality at 82.13% remains passing.
- **SAFE-02 empty — resolved:** missing/malformed deadline, empty API output, truncated/unclosed task blocks, and missing type/purpose fields fail closed; a phase with no prohibited checkpoint continues to pass.
- **SAFE-02 ordering — resolved:** callers establish the absolute deadline before their first API call, API retry checks precede requests and sleeps, task extraction completes before policy classification, and preservation runs only after both source repairs are committed.
- **SAFE-02 concurrency — resolved:** each controller retains one SHA-scoped owner lock; timeout terminalization removes owned labels/locks/processes without permitting a second owner or resetting durable failure.
- Descriptor-less value/safety prohibitions remain flagged-unverified in frontmatter; no fabricated descriptor or silent green status is assigned.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bound every controller API retry by its owning wait deadline</name>
  <files>scripts/ci/collect_pr_timing_receipt.sh, test/install_smoke/ci_timing_automation_test.exs</files>
  <read_first>
    - AGENTS.md
    - RUNNING.md
    - .planning/phases/132-measured-closure/132-REVIEW.md
    - .planning/phases/132-measured-closure/132-VERIFICATION.md
    - .planning/phases/132-measured-closure/132-19-SUMMARY.md
    - scripts/ci/collect_pr_timing_receipt.sh
    - test/install_smoke/ci_timing_automation_test.exs
  </read_first>
  <behavior>
    - RED: a permanently rate-limited workflow-run discovery during trigger creation exits at the persisted creation deadline, writes terminal `failed` state, removes the owned label/lock/process, and cannot resume into mutation authority.
    - RED: permanent rate limiting during publication quiescence, run-detail polling, jobs polling, and canonical-population verification each terminates deterministically at its owning absolute deadline with the same cleanup guarantees.
    - RED: `now == deadline` permits neither another API request nor another sleep; a retry delay is capped to remaining budget and cannot overshoot into an unobserved retry.
    - GREEN: transient rate limiting before the deadline still retries and succeeds, preserving pagination, exact-ten population, terminal-state, and completed-state verification behavior.
  </behavior>
  <action>
Start RED by extending the existing fake `gh` fixture with endpoint/path-selectable permanent and transient rate-limit responses plus deterministic time/sleep control; replace the presence-only source assertion with process-level cases for creation discovery, `wait_for_published_head_quiescence`, `wait_for_run` run detail, jobs retrieval, and `canonical_eligible_run_ids`. Assert exit status, bounded request/sleep counts, terminal state error/status, label absence, lock/PID/process absence, receipt non-mutation, and immutable failed-state re-entry. Commit the failing behavioral tests as `test(132-22): expose unbounded API rate-limit waits` before source changes.

In GREEN, change `gh_api_json` to require an explicit absolute deadline (or an equivalent single bounded retry budget derived once by the caller), validate it as an epoch, check expiry before each request and before each sleep, cap any reset/backoff delay to remaining budget, and return a distinct timeout status such as 124 without hiding it behind generic JSON failure. Thread the persisted trigger-creation deadline through `same_sha_runs`/`wait_for_new_run`; derive one deadline at entry for publication quiescence and each run/jobs wait; give canonical population and explicit receipt verification a finite absolute deadline rather than an unbounded internal retry. Propagate timeout distinctly through command substitutions and pipelines so the controller writes its existing durable terminal failure record and executes owned label/lock/process cleanup. Preserve D-04/D-05 sequence/state identity, pagination and stable ordering, existing threshold semantics, and completed-state read-only behavior. Commit the green implementation as `fix(132-22): bound controller API retries by deadlines`.
  </action>
  <acceptance_criteria>
    - Persistent rate limiting in each creation, quiescence, run-detail, jobs, and population path terminates within deterministic fixture bounds and records a durable failed state.
    - Expiry is inclusive at the deadline, retry sleep is bounded by remaining budget, and timeout is distinguishable from malformed/non-rate-limit API failure.
    - Owned label, lock, PID, process, state, and receipt behavior is fail closed after every timeout; terminal failed re-entry performs no mutation.
    - Transient retry, pagination, exact-ten population, and existing controller behavior remain green per D-04/D-05/D-06.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
mix format --check-formatted
mix test test/install_smoke/ci_timing_automation_test.exs --seed 0
git log --oneline --grep='^test(132-22): expose unbounded API rate-limit waits' -1 | grep -q .
git log --oneline --grep='^fix(132-22): bound controller API retries by deadlines' -1 | grep -q .
git diff --check</automated>
  </verify>
  <done>Every API-bearing controller wait returns or terminalizes within its owning absolute deadline, including permanent rate limits, with deterministic state and cleanup evidence.</done>
  <reversibility rating="reversible">The deadline parameter and fixture controls are internal script/test contracts that can be changed locally without altering published CI topology or receipt data.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parse and enforce human-action checkpoints independent of XML-like syntax ordering</name>
  <files>scripts/maintainer/automation_first_contract.sh, test/install_smoke/automation_first_contract_test.exs</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/132-measured-closure/132-REVIEW.md
    - .planning/phases/132-measured-closure/132-VERIFICATION.md
    - scripts/maintainer/automation_first_contract.sh
    - test/install_smoke/automation_first_contract_test.exs
  </read_first>
  <behavior>
    - RED: gate-before-type and unrelated-attribute-before-type opening tags are classified as human-action for both single- and double-quoted `type` values.
    - RED: reordered/single-quoted human actions with a non-authorization purpose, `<acceptance_criteria>`, `<verify>`, or `<verification>` fail with the existing actionable policy diagnostics.
    - RED: authorization-only and credential-bootstrap blocks pass for both quote styles/orderings only when they contain no acceptance or verification field.
    - RED: multiline complete blocks are checked; truncated/unclosed human-action blocks fail closed rather than disappearing from policy input.
  </behavior>
  <action>
Start RED by adding table-driven temporary-plan fixtures for gate-before-type, additional attributes around type, both quote styles, acceptance-bearing and verification-bearing blocks, authorization and credential-bootstrap positive controls, and an unclosed human-action task. Execute the public script and assert exact pass/fail class and diagnostic. Commit the failing fixtures as `test(132-22): expose human-action attribute bypass`.

In GREEN, replace the literal type-first AWK opener with complete task-element block extraction followed by order-independent attribute classification supporting valid single or double quotes. Keep matching scoped to the opening tag, not comments or task body prose. Apply the existing purpose allowlist and no-acceptance/no-verification checks only after the entire block is collected; fail closed on an opened but unclosed human-action block. Preserve the existing automated-plan and authorization-only behavior, avoid adding an XML dependency, and keep diagnostics stable enough for CI triage. Commit the repair as `fix(132-22): make human-action policy syntax independent`.
  </action>
  <acceptance_criteria>
    - Attribute order and quote style cannot bypass checkpoint:human-action discovery.
    - Non-authorization purpose and any acceptance/verify/verification field are rejected for every supported opening-tag form.
    - Authorization-only and credential-bootstrap checkpoints remain allowed but cannot satisfy acceptance.
    - Empty automated phases pass and malformed/unclosed human-action blocks fail closed deterministically.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
mix format --check-formatted
mix test test/install_smoke/automation_first_contract_test.exs --seed 0
./scripts/maintainer/automation_first_contract.sh --phase-dir .planning/phases/132-measured-closure
git log --oneline --grep='^test(132-22): expose human-action attribute bypass' -1 | grep -q .
git log --oneline --grep='^fix(132-22): make human-action policy syntax independent' -1 | grep -q .
git diff --check</automated>
  </verify>
  <done>The merge-blocking contract enforces authorization-only human actions for all supported attribute orders and quote styles, with adversarial machine fixtures and no human acceptance path.</done>
  <reversibility rating="reversible">The parser remains a repository-local policy script with fixture-locked public behavior and no external schema migration.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Rebind preservation and replay existing CI-14, COV-05, and SAFE-02 authorities</name>
  <files>.planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md</files>
  <read_first>
    - AGENTS.md
    - RUNNING.md
    - guides/release_publish.md
    - .planning/phases/132-measured-closure/132-CONTEXT.md
    - .planning/phases/132-measured-closure/132-VERIFICATION.md
    - .planning/phases/132-measured-closure/132-20-SUMMARY.md
    - .planning/phases/132-measured-closure/132-21-SUMMARY.md
    - .planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md
    - .planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
    - scripts/ci/collect_pr_timing_receipt.sh
    - scripts/maintainer/automation_first_contract.sh
    - .github/workflows/ci.yml
  </read_first>
  <action>
After Tasks 1 and 2 source commits exist, derive the final repair SHA and every changed path/blob OID from Git. Preserve historical receipt prose/blocks. Replace only the single active `PRESERVATION_TRANSITION_V2_BEGIN/END` payload so its cumulative final stage binds the final source commit and includes exactly the four authorized non-planning repair/test paths. Record a rename-disabled census proving no `.github/workflows/ci.yml`, Admin, public API, schema/migration, telemetry/error, dependency/lockfile, release-proof, CI topology/job/proof, runner, matrix, cache, partition, rerun, threshold, or exception drift per D-01/D-08/D-09.

Run the two focused suites, timing topology/observability suites, CI Summary gate, formatter, `mix quality_signals`, SAFE-01, the now-hardened automation-first contract, repository hygiene, one authoritative coverage invocation with structural positive-denominator parsing and inclusive 82.13% integer comparison, packed image-consumer proof, and `git diff --check`. Then invoke only `collect_pr_timing_receipt.sh verify` against PR 96 and the existing current receipt with exactly 10 samples and 480/600 thresholds. Require current receipt SHA-256 and marker counts unchanged before/after verification, success against the same immutable sampled head and ten run IDs, median 453 and p95 481, and no PR label/head/run/state/lock mutation. Do not invoke controller `run`, publish, label, dispatch, rerun, or collect a replacement sample because the source diff does not touch the required workflow timing path.

Append a bounded Plan 132-22 preservation subsection with final source SHA, target blob OIDs, focused/full command results, coverage counts, receipt hash/markers and verifier output, remote mutation snapshots, and an explicit `new_live_sample_required: false` rationale. Commit the receipt and `132-22-SUMMARY.md` separately as `docs(132-22): record automated safety closure` after all authorities pass.
  </action>
  <acceptance_criteria>
    - The active transition manifest is Git-reproducible and binds exactly the four repaired source/test paths at the final source commit.
    - Both focused suites and every SAFE-02 preservation authority pass without a human checkpoint or UAT.
    - One fresh coverage artifact is structurally valid, has a positive denominator, and remains at least 82.13% per D-07.
    - The existing exact-ten receipt independently verifies unchanged at 453-second median and 481-second p95; no new run, label, publication, rerun, or receipt rewrite occurs.
    - `132-22-SUMMARY.md` records atomic RED/GREEN commits and exact machine evidence for both review blockers.
  </acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
mix format --check-formatted
mix test test/install_smoke/ci_timing_automation_test.exs test/install_smoke/automation_first_contract_test.exs --seed 0
mix test test/install_smoke/ci_lane_split_test.exs test/install_smoke/ci_observability_test.exs --seed 0
bash scripts/ci/test_ci_summary_gate.sh
mix quality_signals
bash scripts/maintainer/refactor_contract.sh
./scripts/maintainer/automation_first_contract.sh --phase-dir .planning/phases/132-measured-closure
./scripts/maintainer/repo_hygiene_check.sh --ci
mix coveralls.multiple --type local --type json
jq -e '(.source_files | type == "array" and length > 0) and all(.source_files[]; (.coverage | type == "array") and all(.coverage[]; . == null or (type == "number" and . >= 0 and floor == .)))' cover/excoveralls.json >/dev/null
COVERED=$(jq '[.source_files[].coverage[] | select(. != null and . > 0)] | length' cover/excoveralls.json)
RELEVANT=$(jq '[.source_files[].coverage[] | select(. != null)] | length' cover/excoveralls.json)
test "$RELEVANT" -gt 0
test $((COVERED * 10000)) -ge $((RELEVANT * 8213))
bash scripts/install_smoke.sh image
RECEIPT=.planning/phases/132-measured-closure/132-CI-TIMING-RECEIPT.md
RECEIPT_BEFORE=$(shasum -a 256 "$RECEIPT" | awk '{print $1}')
MARKERS_BEFORE=$(grep -E -c '^CI_TIMING_CURRENT_(TABLE|SOURCE)_(BEGIN|END)$' "$RECEIPT")
bash scripts/ci/collect_pr_timing_receipt.sh verify --repo szTheory/rindle --pr 96 --workflow ci.yml --summary-job 'CI Summary' --samples 10 --median-max 480 --p95-max 600 --receipt "$RECEIPT"
test "$(shasum -a 256 "$RECEIPT" | awk '{print $1}')" = "$RECEIPT_BEFORE"
test "$(grep -E -c '^CI_TIMING_CURRENT_(TABLE|SOURCE)_(BEGIN|END)$' "$RECEIPT")" -eq "$MARKERS_BEFORE"
test "$MARKERS_BEFORE" -eq 4
test "$(grep -c '^PRESERVATION_TRANSITION_V2_BEGIN$' .planning/phases/132-measured-closure/132-PRESERVATION-RECEIPT.md)" -eq 1
git diff --check</automated>
  </verify>
  <done>The final repaired source is fully preserved by machine evidence, the current CI-14 receipt and COV-05 floor remain valid, and SAFE-02 has no remaining automated blocker.</done>
  <reversibility rating="reversible">The active receipt block is marker-scoped and Git-reproducible; it changes evidence attribution without mutating GitHub or the accepted timing receipt.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| GitHub API/error metadata -> controller retry loop | Untrusted remote failures and reset hints influence local waiting and mutation lifetime. |
| PLAN.md XML-like text -> automation acceptance gate | Maintainer-authored text determines whether a human checkpoint is policy-valid. |
| Git/source/remote evidence -> phase verdict | Local commits, coverage, receipt bytes, and live API identities become SAFE-02/CI-14/COV-05 acceptance evidence. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-132-22-01 | Denial of Service | `gh_api_json` rate-limit retry | high | mitigate | Require caller-owned absolute deadlines, inclusive expiry checks before request/sleep, bounded delay, distinct timeout propagation, terminal state, and cleanup fixtures for every wait path. |
| T-132-22-02 | Elevation of Privilege | human-action task classification | high | mitigate | Extract complete task blocks and classify type independent of attribute order/quotes before enforcing authorization-only/no-acceptance policy. |
| T-132-22-03 | Tampering | timing/coverage/preservation evidence | high | mitigate | Keep timing receipt byte-stable, verify it from live API, generate coverage once, and bind final source/path/blob identities from Git in the active manifest. |
| T-132-22-04 | Repudiation | timeout and cleanup result | medium | mitigate | Persist terminal error/status and record deterministic request/sleep counts, state, label, lock, PID, process, and receipt identities in fixtures and summary. |
| T-132-22-05 | Spoofing | quote/order variants and comments | medium | mitigate | Match the `type` attribute only within actual opening tags, cover single/double quotes and reordered attributes, and ignore body/comment literals. |
| T-132-22-06 | Information Disclosure | rate-limit stderr/fixtures | low | accept | Tests record statuses and endpoint classes only; no credentials, tokens, or response headers are persisted. |
| T-132-22-SC | Tampering | package installation | low | accept | No package-manager dependency is added; existing Bash, AWK, ExUnit, jq, gh, and Git facilities are reused. |
</threat_model>

<verification>
- Focused persistent-rate-limit and parser adversarial suites pass with RED-before-GREEN commit evidence.
- Full Phase 132 preservation authorities pass on the final Git-bound source subject.
- The current CI timing receipt re-verifies from GitHub Actions without any controller run or mutation.
- Automation-first contract passes the phase directory and rejects all adversarial non-authorization/acceptance fixtures.
- Coverage remains structurally valid and at least 82.13%; CI Summary topology and D-08/D-09 boundaries are unchanged.
</verification>

<success_criteria>
- Both critical findings in `132-REVIEW.md` are closed by executable behavior, not source-string checks or human verification.
- SAFE-02 reaches automated green while CI-14 and COV-05 remain closed on their existing authoritative evidence.
- The only implementation diff is the two scripts and their two focused test files; all other changed paths are planning evidence.
- No new live timing sample is collected unless an automated check first proves required workflow-path drift, which this plan prohibits.
</success_criteria>

## Multi-source coverage audit

| Source | ID | Feature/requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Close measured CI evidence while preserving quality ratchets | 132-22 | COVERED | Existing exact-ten receipt is replayed; safety ratchets are repaired and re-censused. |
| REQ | CI-14 | Exact-ten PR timing <=480 median / <=600 p95 | 132-21 preserved by 132-22 | COVERED | Existing API-backed receipt is reverified unchanged; no new sample is necessary. |
| REQ | COV-05 | Coverage >=82.13% without percentage-only tests | 132-20 preserved by 132-22 | COVERED | Task 3 runs one structural positive-denominator authority. |
| REQ | SAFE-02 | Focused/full preservation and no prohibited drift | 132-22 | COVERED | Both verified blockers receive TDD repairs and full recensus. |
| RESEARCH | — | API-backed receipt and locked CI topology | 132-22 | COVERED | Verify-only replay plus D-01/D-08/D-09 diff census. |
| CONTEXT | D-01 | Sole CI Summary required-check semantics | 132-22 | COVERED | Topology/summary tests and bounded diff. |
| CONTEXT | D-02 | Evidence-bounded correction | 132-22 | COVERED | No new timing/topology optimization; only verified safety defects are repaired. |
| CONTEXT | D-03 | External-runner exception requirements | 132-22 | COVERED | No exception is introduced or used. |
| CONTEXT | D-04 | Fresh comparable ten-run sequence contract | 132-21 preserved by 132-22 | COVERED | Previously accepted immutable exact-ten receipt remains authoritative. |
| CONTEXT | D-05 | Inclusive median/p95 and no partial/rerun evidence | 132-22 | COVERED | Explicit verifier replays 480/600 thresholds and exact-ten identities. |
| CONTEXT | D-06 | Reconfirm COV-05 and SAFE-02 on final correction | 132-22 | COVERED | Task 3 runs full preservation after both source commits. |
| CONTEXT | D-07 | Coverage floor; no percentage-only tests | 132-22 | COVERED | No coverage-only test; structural integer authority is fresh. |
| CONTEXT | D-08 | Exact six topology-edge exception | 132-22 | COVERED | Workflow is untouched and topology fixtures rerun. |
| CONTEXT | D-09 | Every other topology/proof surface frozen | 132-22 | COVERED | Git path/blob census and focused topology authorities enforce it. |

<output>
Create `.planning/phases/132-measured-closure/132-22-SUMMARY.md` when done.
</output>

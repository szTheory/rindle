---
phase: 10-publish-readiness
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - test/install_smoke/package_metadata_test.exs
  - scripts/release_preflight.sh
  - .github/workflows/release.yml
  - lib/rindle/live_view.ex
autonomous: true
requirements:
  - RELEASE-05
must_haves:
  truths:
    - Maintainers can run one preflight path that proves the exact packaged metadata, packaged files, release-guide presence, and docs build health before any live publish is wired.
    - The release workflow enforces docs generation with warnings treated as errors instead of assuming `mix hex.publish package --dry-run` covered docs.
    - Phase 10 does not broaden publish credentials; the workflow stays preflight-only and leaves live `HEX_API_KEY` automation to Phase 11.
  artifacts:
    - path: test/install_smoke/package_metadata_test.exs
      provides: Executable package metadata and tarball-content assertions against the unpacked artifact
    - path: scripts/release_preflight.sh
      provides: Single repo-level preflight command for tarball, release-doc, and docs gates
    - path: .github/workflows/release.yml
      provides: Release workflow wiring for the new preflight path without live publish credentials
    - path: lib/rindle/live_view.ex
      provides: Docs-warning cleanup needed for `mix docs --warnings-as-errors`
  key_links:
    - from: scripts/release_preflight.sh
      to: test/install_smoke/package_metadata_test.exs
      via: Preflight metadata gate
      pattern: package_metadata_test\.exs
    - from: scripts/release_preflight.sh
      to: mix docs --warnings-as-errors
      via: Explicit docs gate
      pattern: mix docs --warnings-as-errors
    - from: .github/workflows/release.yml
      to: scripts/release_preflight.sh
      via: Shared release preflight invocation
      pattern: scripts/release_preflight\.sh
---

<objective>
Harden the release preflight so maintainers can inspect the exact tarball and docs output, and so the workflow proves docs health without adding live publish credentials yet.

Purpose: satisfy `RELEASE-05`, close the docs-warning gap called out by research, and preserve the Phase 10 boundary that excludes real `HEX_API_KEY` automation.
Output: metadata/tarball tests, a shared preflight script, release workflow wiring, and the LiveView docs fix required for warning-free docs generation.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v1.2-phases/10-publish-readiness/10-RESEARCH.md
@.planning/milestones/v1.2-phases/10-publish-readiness/10-PATTERNS.md
@.planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md
@.planning/milestones/v1.1-phases/09-install-release-confidence/09-01-SUMMARY.md
@.planning/milestones/v1.1-phases/09-install-release-confidence/09-02-SUMMARY.md
@.github/workflows/release.yml
@scripts/install_smoke.sh
@lib/rindle/live_view.ex
@rindle-0.1.0-dev/hex_metadata.config
@test/install_smoke/docs_parity_test.exs
@test/install_smoke/support/generated_app_helper.ex

<interfaces>
From .github/workflows/release.yml:
```yaml
- name: Build package artifact
  run: mix hex.build --unpack

- name: Run package-consumer install smoke
  run: bash scripts/install_smoke.sh

- name: Dry-run publish (optional — A1 risk tolerance)
  run: mix hex.publish package --dry-run --yes
```

From rindle-0.1.0-dev/hex_metadata.config:
```erlang
{<<"name">>,<<"rindle">>}.
{<<"version">>,<<"0.1.0-dev">>}.
{<<"licenses">>,[<<"MIT">>]}.
{<<"files">>, [...,<<"mix.exs">>,<<"README.md">>,<<"LICENSE">>,<<"guides">>, ...]}.
```

From lib/rindle/live_view.ex:
```elixir
Wraps `Phoenix.LiveView.Upload.allow_upload/3` and sets the `:external`
option to a function that initiates a Rindle upload session
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add metadata and tarball-content proof for the unpacked package</name>
  <files>test/install_smoke/package_metadata_test.exs, scripts/release_preflight.sh</files>
  <read_first>test/install_smoke/docs_parity_test.exs, test/install_smoke/support/generated_app_helper.ex, rindle-0.1.0-dev/hex_metadata.config, scripts/install_smoke.sh, .planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md</read_first>
  <behavior>
    - Test 1: the unpacked package metadata contains the expected `name`, `version`, `licenses`, `links`, and required packaged paths, including `guides/release_publish.md`.
    - Test 2: prohibited paths such as `_build`, `.planning`, `test`, `.github`, and `coveralls.json` are absent from the unpacked artifact.
    - Test 3: one repo script runs the metadata test, release-doc parity test, install smoke, `mix hex.build --unpack`, and `mix docs --warnings-as-errors` in the release-preflight order.
  </behavior>
  <action>Create `test/install_smoke/package_metadata_test.exs` as a small file-oriented ExUnit gate. Reuse the narrow helper style from `docs_parity_test.exs` and the command-execution pattern from `GeneratedAppHelper` only if the artifact needs to be built on demand. Assert the unpacked `hex_metadata.config` includes `name = rindle`, a version string matching the current `@version`, `licenses = ["MIT"]`, the GitHub link, and `files` entries for `mix.exs`, `README.md`, `LICENSE`, `guides/getting_started.md`, and `guides/release_publish.md`. Refute packaged `_build`, `.planning`, `test`, `.github`, and `coveralls.json` paths. Create `scripts/release_preflight.sh` as the single Phase 10 preflight command with `set -euo pipefail` that runs, in order: `mix hex.build --unpack`, `mix test test/install_smoke/package_metadata_test.exs`, `mix test test/install_smoke/release_docs_parity_test.exs`, `bash scripts/install_smoke.sh`, and `mix docs --warnings-as-errors`. Keep the script preflight-only: do not call `mix hex.publish`, do not export `HEX_API_KEY`, and do not delete the artifact before the tests inspect it.</action>
  <acceptance_criteria>
    - `rg -n 'defmodule Rindle\\.InstallSmoke\\.PackageMetadataTest' test/install_smoke/package_metadata_test.exs` finds the metadata test module.
    - `rg -n 'release_publish\\.md|coveralls\\.json|\\.planning|_build' test/install_smoke/package_metadata_test.exs` finds required and prohibited path assertions.
    - `rg -n 'mix hex.build --unpack|package_metadata_test\\.exs|release_docs_parity_test\\.exs|install_smoke\\.sh|mix docs --warnings-as-errors' scripts/release_preflight.sh` finds the ordered preflight commands.
    - `rg -n 'HEX_API_KEY|mix hex\\.publish' scripts/release_preflight.sh` returns no matches.
    - `mix test test/install_smoke/package_metadata_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/install_smoke/package_metadata_test.exs && bash scripts/release_preflight.sh</automated>
  </verify>
  <done>The repo has a reproducible preflight path that proves package metadata, packaged file contents, release-guide inclusion, install smoke reuse, and docs readiness before any publish command is considered.</done>
</task>

<task type="auto">
  <name>Task 2: Wire workflow preflight and clear the docs-warning blocker</name>
  <files>.github/workflows/release.yml, lib/rindle/live_view.ex</files>
  <read_first>.github/workflows/release.yml, lib/rindle/live_view.ex, scripts/release_preflight.sh, .planning/milestones/v1.2-phases/10-publish-readiness/10-RESEARCH.md</read_first>
  <action>Update `.github/workflows/release.yml` so the release job calls `bash scripts/release_preflight.sh` as the must-pass Phase 10 gate instead of scattering the same checks inline. Preserve `environment: release`, preserve the built-artifact consumer smoke coverage from Phase 9, and keep the existing dry-run publish step only as a non-live metadata check after preflight succeeds. Do not introduce `${{ secrets.HEX_API_KEY }}`, do not switch to `mix hex.publish --yes`, and do not remove the warning comments that explain live credential wiring belongs to Phase 11. In `lib/rindle/live_view.ex`, replace the hidden `Phoenix.LiveView.Upload.allow_upload/3` doc reference with public wording that points to LiveView upload configuration without linking to the hidden function, so `mix docs --warnings-as-errors` passes cleanly.</action>
  <acceptance_criteria>
    - `rg -n 'scripts/release_preflight\\.sh' .github/workflows/release.yml` finds the shared preflight invocation.
    - `rg -n 'HEX_API_KEY' .github/workflows/release.yml` finds only the existing dry-run placeholder comment or env shim, not a new live secret reference.
    - `rg -n '\\$\\{\\{ secrets\\.HEX_API_KEY \\}\\}|mix hex\\.publish --yes' .github/workflows/release.yml` returns no matches.
    - `rg -n 'Phoenix\\.LiveView\\.Upload\\.allow_upload/3' lib/rindle/live_view.ex` returns no matches.
    - `mix docs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/release_preflight.sh</automated>
  </verify>
  <done>The release workflow uses the shared preflight gate, the docs build becomes warning-clean, and Phase 10 still stops short of real publish credentials or live Hex publication.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo checkout -> packaged artifact | build output can drift from intended shipped contents |
| repo workflow -> release environment | release YAML controls whether publish credentials stay preflight-only or broaden prematurely |
| source docs -> generated HexDocs output | warning-producing source docs can silently degrade published docs if not treated as errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | T | test/install_smoke/package_metadata_test.exs, scripts/release_preflight.sh | mitigate | Assert exact required and prohibited paths from `hex_metadata.config` and the unpacked tarball so package drift fails before publication. |
| T-10-05 | E | .github/workflows/release.yml | mitigate | Keep Phase 10 preflight-only: no `${{ secrets.HEX_API_KEY }}` and no live `mix hex.publish`; reuse the protected `environment: release` boundary while deferring real credential wiring to Phase 11. |
| T-10-06 | I | lib/rindle/live_view.ex, docs build | mitigate | Remove the hidden LiveView function reference and require `mix docs --warnings-as-errors` in the shared preflight so broken or misleading release docs cannot pass silently. |
</threat_model>

<verification>
Run `mix test test/install_smoke/package_metadata_test.exs` and `mix test test/install_smoke/release_docs_parity_test.exs` while building the plan-owned tests. Before closing the plan, run `bash scripts/release_preflight.sh` and confirm it passes without adding any live publish credential handling.
</verification>

<success_criteria>
- `RELEASE-05` is enforced by an executable metadata/tarball gate plus a warning-clean docs build.
- The release workflow reuses one shared preflight script instead of drifting inline checks.
- Phase 10 preserves the boundary that real `HEX_API_KEY` automation belongs to Phase 11.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.2-phases/10-publish-readiness/10-02-SUMMARY.md`
</output>

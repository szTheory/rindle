---
phase: 10-publish-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - guides/release_publish.md
  - guides/operations.md
  - test/install_smoke/release_docs_parity_test.exs
autonomous: true
requirements:
  - RELEASE-04
must_haves:
  truths:
    - Maintainers have a single release guide that states the first-publish versioning steps, auth expectations, and owner follow-up instead of relying on maintainer memory.
    - The release guide is included in generated docs and linked from maintainer-facing docs without bloating adopter onboarding docs.
    - The repo has an executable docs gate that fails when maintainer release guidance drifts away from the intended first-publish contract.
  artifacts:
    - path: mix.exs
      provides: ExDoc extras wiring that publishes the maintainer release guide
    - path: guides/release_publish.md
      provides: Explicit first-publish runbook covering versioning, `mix hex.user`, and `mix hex.owner`
    - path: guides/operations.md
      provides: Maintainer-facing cross-link to the release runbook without moving release steps into README or getting started docs
    - path: test/install_smoke/release_docs_parity_test.exs
      provides: Executable release-doc parity gate for first-publish guidance
  key_links:
    - from: mix.exs
      to: guides/release_publish.md
      via: `docs().extras`
      pattern: guides/release_publish\.md
    - from: guides/operations.md
      to: guides/release_publish.md
      via: Maintainer cross-link
      pattern: release_publish
    - from: test/install_smoke/release_docs_parity_test.exs
      to: guides/release_publish.md
      via: Markdown content assertions
      pattern: mix hex\.user|mix hex\.owner|0\.1\.0
---

<objective>
Make first-publish package metadata, versioning, and owner/auth expectations explicit before any live `Hex.pm` automation exists.

Purpose: satisfy `RELEASE-04` and the roadmap split by turning implicit maintainer knowledge into shipped, test-backed release guidance while preserving the adopter-doc boundary.
Output: a maintainer-only release guide, ExDoc wiring for it, an operations cross-link, and an executable parity test that guards the guidance.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v1.2-phases/10-publish-readiness/10-RESEARCH.md
@.planning/milestones/v1.2-phases/10-publish-readiness/10-PATTERNS.md
@.planning/milestones/v1.2-phases/10-publish-readiness/10-VALIDATION.md
@.planning/milestones/v1.1-phases/09-install-release-confidence/09-03-SUMMARY.md
@mix.exs
@guides/operations.md
@test/install_smoke/docs_parity_test.exs

<interfaces>
From mix.exs:
```elixir
@version "0.1.0-dev"

defp docs do
  [
    extras: [
      "README.md",
      "guides/getting_started.md",
      "guides/core_concepts.md",
      "guides/storage_capabilities.md",
      "guides/profiles.md",
      "guides/secure_delivery.md",
      "guides/background_processing.md",
      "guides/operations.md",
      "guides/troubleshooting.md"
    ]
  ]
end
```

From guides/operations.md:
```markdown
## Task Reference
...
## Operational Tips
...
## When to Escalate
```

From test/install_smoke/docs_parity_test.exs:
```elixir
setup_all do
  {:ok, %{readme: File.read!(@readme_path), guide: File.read!(@guide_path)}}
end

for doc <- [readme, guide] do
  assert doc =~ "presigned PUT"
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the maintainer release runbook and wire it into ExDoc</name>
  <files>guides/release_publish.md, mix.exs, guides/operations.md</files>
  <read_first>guides/operations.md, mix.exs, .planning/milestones/v1.2-phases/10-publish-readiness/10-RESEARCH.md, .planning/milestones/v1.2-phases/10-publish-readiness/10-PATTERNS.md</read_first>
  <behavior>
    - Test 1: the maintainer guide states that the first public cut changes `@version "0.1.0-dev"` to `0.1.0`, tags `v0.1.0`, and bumps back to the next `-dev` version after the release.
    - Test 2: the guide names the exact pre-publish auth and ownership commands `mix hex.user whoami`, `mix hex.owner list rindle`, and the first-publish follow-up `mix hex.owner add rindle USERNAME`.
    - Test 3: maintainer guidance lives in `guides/release_publish.md` and maintainer-facing cross-links, not in `README.md` or `guides/getting_started.md`.
  </behavior>
  <action>Create `guides/release_publish.md` per the Phase 10 roadmap split: it must be a maintainer-facing runbook with sections for `Versioning`, `Hex auth check`, `First publish owner model`, `Preflight commands`, and `Post-publish follow-up`. Include the literal commands `mix hex.user whoami`, `mix hex.owner list rindle`, `mix hex.build --unpack`, `mix docs --warnings-as-errors`, and `mix hex.owner add rindle USERNAME`. State the explicit first-release versioning sequence as: replace `@version "0.1.0-dev"` with `0.1.0`, create tag `v0.1.0`, publish, then bump to the next `-dev` version on the main branch. Update `mix.exs` `docs().extras` to include `"guides/release_publish.md"` and preserve the existing extras ordering style. Update `guides/operations.md` with a short maintainer cross-link section pointing to `release_publish.md`; do not add release-owner or API-key steps to `README.md` or `guides/getting_started.md`, per the adopter-doc separation constraint.</action>
  <acceptance_criteria>
    - `rg -n 'guides/release_publish\\.md' mix.exs` finds the new ExDoc extra.
    - `rg -n 'mix hex\\.user whoami|mix hex\\.owner list rindle|mix hex\\.owner add rindle USERNAME' guides/release_publish.md` finds the exact commands.
    - `rg -n '@version \"0\\.1\\.0-dev\"|v0\\.1\\.0|next -dev' guides/release_publish.md` finds the versioning sequence.
    - `rg -n 'release_publish' guides/operations.md` finds the maintainer cross-link.
    - `rg -n 'HEX_API_KEY|mix hex\\.owner|mix hex\\.user' README.md guides/getting_started.md` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/install_smoke/release_docs_parity_test.exs</automated>
  </verify>
  <done>The repo ships a maintainer-only release runbook, ExDoc includes it, operations docs point to it, and no adopter-facing doc absorbs maintainer auth or owner instructions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add an executable parity gate for release guidance drift</name>
  <files>test/install_smoke/release_docs_parity_test.exs</files>
  <read_first>test/install_smoke/docs_parity_test.exs, guides/release_publish.md, guides/operations.md, mix.exs</read_first>
  <behavior>
    - Test 1: the release guide includes the required first-publish versioning, auth, and owner strings.
    - Test 2: `mix.exs` publishes the guide through `docs().extras`.
    - Test 3: maintainer-facing docs link to the guide while adopter-facing docs stay free of publish-owner/auth wording.
  </behavior>
  <action>Create `test/install_smoke/release_docs_parity_test.exs` using the same `setup_all` plus direct string-assert pattern as `docs_parity_test.exs`. Load `guides/release_publish.md`, `guides/operations.md`, `README.md`, and `guides/getting_started.md`. Assert the exact presence of `mix hex.user whoami`, `mix hex.owner list rindle`, `mix hex.owner add rindle USERNAME`, `mix hex.build --unpack`, `mix docs --warnings-as-errors`, `0.1.0`, and `v0.1.0` in the release guide. Assert `guides/operations.md` links the guide. Refute maintainer-release strings like `HEX_API_KEY`, `mix hex.owner`, and `mix hex.user` in `README.md` and `guides/getting_started.md`. Keep the test file narrow and file-oriented; do not shell out or rebuild packages in this test.</action>
  <acceptance_criteria>
    - `rg -n 'defmodule Rindle\\.InstallSmoke\\.ReleaseDocsParityTest' test/install_smoke/release_docs_parity_test.exs` finds the test module.
    - `rg -n 'mix hex\\.user whoami|mix hex\\.owner list rindle|mix docs --warnings-as-errors|v0\\.1\\.0' test/install_smoke/release_docs_parity_test.exs` finds the asserted release strings.
    - `rg -n 'README\\.md|guides/getting_started\\.md|refute' test/install_smoke/release_docs_parity_test.exs` shows the adopter-doc negative checks.
    - `mix test test/install_smoke/release_docs_parity_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/install_smoke/release_docs_parity_test.exs</automated>
  </verify>
  <done>Release guidance drift now fails in ExUnit when the maintainer runbook, ExDoc extras, or adopter-doc separation contract changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer policy -> shipped docs | release ownership, versioning, and auth expectations cross from human memory into repo-tracked guidance |
| maintainer docs -> adopter docs | maintainer-only instructions must not spill into the public onboarding path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | E | guides/release_publish.md | mitigate | Make the first-publish owner model explicit with `mix hex.owner list rindle` and `mix hex.owner add rindle USERNAME` so package ownership is not left to a single publisher's memory. |
| T-10-02 | T | mix.exs, guides/release_publish.md | mitigate | State the exact `@version "0.1.0-dev"` -> `0.1.0` -> next `-dev` sequence and gate it with `release_docs_parity_test.exs` to prevent versioning drift before publication. |
| T-10-03 | I | README.md, guides/getting_started.md | mitigate | Keep maintainer auth and owner commands out of adopter-facing docs; the parity test refutes `HEX_API_KEY`, `mix hex.user`, and `mix hex.owner` in onboarding docs. |
</threat_model>

<verification>
Run `mix test test/install_smoke/release_docs_parity_test.exs` after each task. Before closing the plan, confirm `mix.exs` lists `guides/release_publish.md` and the guide contains the explicit versioning, auth, and owner commands.
</verification>

<success_criteria>
- `RELEASE-04` is backed by a concrete maintainer runbook rather than implied process memory.
- ExDoc includes the new release guide and maintainer-facing docs cross-link it.
- An executable parity test preserves the maintainer-doc boundary and the first-publish command contract.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.2-phases/10-publish-readiness/10-01-SUMMARY.md`
</output>

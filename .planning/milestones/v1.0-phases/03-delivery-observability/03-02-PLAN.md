---
phase: 03-delivery-observability
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - lib/rindle/telemetry.ex
  - lib/rindle.ex
  - lib/rindle/delivery.ex
  - lib/rindle/upload/broker.ex
  - lib/rindle/domain/asset_fsm.ex
  - lib/rindle/domain/variant_fsm.ex
  - lib/rindle/domain/upload_session_fsm.ex
  - lib/rindle/workers/promote_asset.ex
  - lib/rindle/workers/process_variant.ex
  - lib/rindle/workers/purge_storage.ex
  - test/rindle/telemetry_test.exs
autonomous: true
requirements:
  - TEL-01
  - TEL-02
  - TEL-03
  - TEL-04
  - TEL-05
  - TEL-06
  - TEL-07
  - TEL-08
must_haves:
  truths:
    - All upload paths emit start and stop telemetry.
    - Asset, variant, delivery, and cleanup events use the locked public event names.
    - Every public event includes profile and adapter metadata.
    - Telemetry measurements stay numeric and never carry free-form strings.
    - Event emission does not leak private filenames, storage paths, or other extra payload data.
  artifacts:
    - path: lib/rindle/telemetry.ex
      provides: Public telemetry helpers and contract documentation
      min_lines: 120
      exports: ["upload_start/2", "upload_stop/3", "asset_state_change/5", "variant_state_change/5", "delivery_signed/4", "cleanup_run/3"]
    - path: lib/rindle/upload/broker.ex
      provides: Direct upload telemetry hooks
      min_lines: 130
      contains: "telemetry"
    - path: lib/rindle/domain/asset_fsm.ex
      provides: Asset state-change telemetry emission
      min_lines: 70
      contains: "asset.state_change"
    - path: lib/rindle/workers/purge_storage.ex
      provides: Cleanup-run telemetry emission
      min_lines: 60
      contains: "cleanup.run"
  key_links:
    - from: lib/rindle.ex
      to: lib/rindle/telemetry.ex
      via: proxied upload start/stop hooks
      pattern: "rindle, :upload"
    - from: lib/rindle/upload/broker.ex
      to: lib/rindle/telemetry.ex
      via: direct upload lifecycle hooks
      pattern: "rindle, :upload"
    - from: lib/rindle/domain/*.ex and lib/rindle/workers/*.ex
      to: lib/rindle/telemetry.ex
      via: state-change and cleanup helpers
      pattern: "rindle, :.*state_change|rindle, :cleanup"
---

<objective>
Lock the public telemetry contract and instrument upload, lifecycle, delivery, and cleanup boundaries with stable numeric measurements.

Purpose: Give adopters a documented event surface they can alert and dashboard against without hidden payload drift.
Output: Telemetry contract module, lifecycle instrumentation, and event-shape tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-delivery-observability/03-CONTEXT.md

<interfaces>
From lib/rindle/upload/broker.ex:
```elixir
def initiate_session(profile_module, opts \\ []) do
def sign_url(session_id, opts \\ []) do
def verify_completion(session_id, opts \\ []) do
```

From lib/rindle/domain/asset_fsm.ex:
```elixir
@spec transition(state(), state(), map()) :: :ok | transition_error()
def transition(current_state, target_state, context \\ %{}) do
```

From lib/rindle/domain/variant_fsm.ex:
```elixir
@spec transition(state(), state(), map()) :: :ok | transition_error()
def transition(current_state, target_state, _context \\ %{}) do
```

From lib/rindle/domain/upload_session_fsm.ex:
```elixir
@spec transition(state(), state(), map()) :: :ok | transition_error()
def transition(current_state, target_state, context \\ %{}) do
```

From lib/rindle/workers/purge_storage.ex:
```elixir
@impl Oban.Worker
def perform(%Oban.Job{args: %{"asset_id" => asset_id, "profile" => profile_name}}) do
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the telemetry contract surface</name>
  <files>lib/rindle/telemetry.ex</files>
  <behavior>
    - The public contract lists the locked event names, measurement keys, and required metadata keys.
    - Helper functions only accept numeric measurements and structured metadata built from profile and adapter context.
    - The contract stays small and explicit; no extra event families are introduced.
  </behavior>
  <action>Create the central telemetry helper module with documented emitters for upload, asset, variant, delivery, and cleanup events. Keep the contract numeric-only for measurements and require `profile`/`adapter` metadata where the phase contract demands it.</action>
  <verify><automated>mix test test/rindle/telemetry_test.exs</automated></verify>
  <done>The telemetry contract module exists, the public event family is documented in code, and the helper API is ready for wiring into the lifecycle code paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instrument the lifecycle boundaries</name>
  <files>lib/rindle.ex, lib/rindle/delivery.ex, lib/rindle/upload/broker.ex, lib/rindle/domain/asset_fsm.ex, lib/rindle/domain/variant_fsm.ex, lib/rindle/domain/upload_session_fsm.ex, lib/rindle/workers/promote_asset.ex, lib/rindle/workers/process_variant.ex, lib/rindle/workers/purge_storage.ex</files>
  <behavior>
    - Proxied and direct upload paths emit start/stop telemetry with numeric measurements.
    - Asset and variant transitions emit state-change telemetry from the lifecycle boundary, not from callers.
    - Delivery signed events and cleanup-run events use the same contract helper and share metadata shape rules.
  </behavior>
  <action>Thread telemetry through the existing upload, delivery, FSM, and worker boundaries without changing their functional behavior. Use the new helper module for all public events, preserve tagged-tuple error semantics, and keep state changes observable at the point they occur.</action>
  <verify><automated>mix test test/rindle/telemetry_test.exs test/rindle/upload/broker_test.exs test/rindle/domain/lifecycle_fsm_test.exs</automated></verify>
  <done>Every public boundary in Phase 3 emits the locked telemetry names with numeric measurements and the required metadata keys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the contract with telemetry tests</name>
  <files>test/rindle/telemetry_test.exs</files>
  <behavior>
    - Tests attach handlers and capture the exact event family expected by the roadmap.
    - Assertions cover numeric measurements, profile metadata, adapter metadata, and event-name stability.
    - Negative cases prove no extra payload fields or string measurements leak into the public contract.
  </behavior>
  <action>Add focused tests that attach telemetry handlers around the existing upload, delivery, and worker flows. Verify the emitted event names, numeric measurements, and metadata keys match the phase contract exactly.</action>
  <verify><automated>mix test test/rindle/telemetry_test.exs</automated></verify>
  <done>The public telemetry contract is locked by tests and ready to serve as an API boundary for adopters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rindle internals → host telemetry handler | Internal lifecycle events cross into adopter-owned handlers and dashboards |
| Rindle internals → event metadata | Structured metadata is observable outside the library and must not leak sensitive payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | S | `lib/rindle/telemetry.ex` | mitigate | Centralize event emission so only locked event names and approved metadata keys are exposed. |
| T-03-05 | I | `lib/rindle/telemetry.ex` | mitigate | Emit only numeric measurements and profile/adapter metadata; never include filenames, paths, or raw storage responses. |
| T-03-06 | D | `lib/rindle/*.ex` telemetry call sites | accept | Host handlers may still fail independently; library code must stay deterministic and tests should prove events are emitted without raising. |
</threat_model>

<verification>
- `mix test test/rindle/telemetry_test.exs` passes with attached telemetry handlers.
- `mix test test/rindle/upload/broker_test.exs test/rindle/domain/lifecycle_fsm_test.exs` still passes after instrumentation.
- Event names, measurement types, and metadata keys match the phase contract exactly.
</verification>

<success_criteria>
- Upload start/stop, asset state changes, variant state changes, delivery signed, and cleanup run telemetry are all emitted.
- Every public telemetry event includes `profile` and `adapter` metadata and numeric measurements only.
- The telemetry contract is documented in code and guarded by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-delivery-observability/03-02-SUMMARY.md`
</output>

---
phase: 01-foundation
plan: "06"
type: execute
wave: 2
depends_on:
  - "01-02"
  - "01-03"
files_modified:
  - mix.exs
  - config/config.exs
  - config/runtime.exs
  - lib/rindle/storage/local.ex
  - lib/rindle/storage/s3.ex
  - lib/rindle/config.ex
  - lib/rindle.ex
  - test/rindle/storage/storage_adapter_test.exs
  - test/rindle/config/config_test.exs
autonomous: true
requirements:
  - STOR-01
  - STOR-02
  - STOR-03
  - STOR-04
  - STOR-05
  - STOR-06
  - STOR-07
  - CONF-01
  - CONF-03
  - CONF-04
  - CONF-05
  - ERR-01
  - ERR-02
user_setup: []
must_haves:
  truths:
    - "Local and S3 adapters implement `Rindle.Storage` with truthful capability lists."
    - "Adapter operations return tagged tuples and log storage processing failures with asset/variant context."
    - "Rindle configuration defaults for queue name and TTLs are centralized under `config :rindle` and retrievable via code."
  artifacts:
    - path: "lib/rindle/storage/local.ex"
      provides: "Local adapter implementation with `capabilities/0 == [:local]`"
      contains: "def capabilities, do: [:local]"
    - path: "lib/rindle/storage/s3.ex"
      provides: "S3 adapter implementation with presigned put support"
      contains: "def capabilities, do: [:presigned_put]"
    - path: "lib/rindle/config.ex"
      provides: "Runtime accessor for queue and TTL defaults"
      contains: "def queue_name"
    - path: "test/rindle/storage/storage_adapter_test.exs"
      provides: "Storage adapter conformance tests and MinIO-gated integration hook"
      min_lines: 100
  key_links:
    - from: "lib/rindle/storage/s3.ex"
      to: "lib/rindle/storage.ex"
      via: "behaviour implementation and callback contract"
      pattern: "@behaviour Rindle.Storage"
    - from: "lib/rindle/config.ex"
      to: "config/config.exs"
      via: "application env key lookup"
      pattern: "Application\\.fetch_env!"
---

<objective>
Implement Phase 1 storage adapters plus foundational configuration/error contracts, including dependency baseline alignment and adapter-level conformance tests.

Purpose: provide production-ready storage seams and explicit configuration defaults that downstream upload/processing phases can consume without interface churn.
Output: local+s3 adapters, config access module, and storage/config test coverage.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-RESEARCH.md
@mix.exs
@config/config.exs
@config/runtime.exs
@lib/rindle/storage.ex
@lib/rindle.ex
</context>

<tasks>
<task type="auto">
  <name>Task 1: Align Phase 1 dependency baseline and foundation config defaults</name>
  <files>mix.exs, config/config.exs, config/runtime.exs, lib/rindle/config.ex</files>
  <read_first>mix.exs, config/config.exs, config/runtime.exs, .planning/phases/01-foundation/01-RESEARCH.md, .planning/REQUIREMENTS.md</read_first>
  <action>Update `mix.exs` dependencies to `{:oban, "~> 2.21"}`, `{:image, "~> 0.65"}`, add `{:ex_aws, "~> 2.5"}`, `{:ex_aws_s3, "~> 2.5"}`, `{:ex_marcel, "~> 0.2"}`, and test deps `{:mox, "~> 1.2", only: :test}`, `{:bypass, "~> 2.1", only: :test}`, `{:ex_machina, "~> 2.7", only: :test}`. In `config/config.exs`, keep existing repo config and add `config :rindle, :queue, :rindle`, `config :rindle, :signed_url_ttl_seconds, 900`, and `config :rindle, :upload_session_ttl_seconds, 86_400`. Add `lib/rindle/config.ex` exposing `queue_name/0`, `signed_url_ttl_seconds/0`, and `upload_session_ttl_seconds/0` using `Application.fetch_env!`/`Application.get_env` defaults. Keep runtime DB ownership in host runtime config; do not move credentials into library-specific env variables.</action>
  <verify>mix deps.get && mix compile --warnings-as-errors</verify>
  <acceptance_criteria>
    - `mix.exs` contains `{:oban, "~> 2.21"}`
    - `mix.exs` contains `{:image, "~> 0.65"}`
    - `mix.exs` contains `{:ex_aws_s3, "~> 2.5"}`
    - `config/config.exs` contains `config :rindle, :queue, :rindle`
    - `config/config.exs` contains `config :rindle, :upload_session_ttl_seconds, 86_400`
    - `lib/rindle/config.ex` contains `def queue_name do`
  </acceptance_criteria>
  <done>Dependencies and config defaults are aligned with Phase 1 storage and runtime contract requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Local and S3 storage adapters with truthful capabilities and tagged error tuples</name>
  <files>lib/rindle/storage/local.ex, lib/rindle/storage/s3.ex, lib/rindle.ex</files>
  <read_first>lib/rindle/storage.ex, lib/rindle.ex, .planning/REQUIREMENTS.md, .planning/phases/01-foundation/01-PATTERNS.md</read_first>
  <action>Create `Rindle.Storage.Local` and `Rindle.Storage.S3` implementing `@behaviour Rindle.Storage`. In local adapter, implement file-copy `store/3`, filesystem `delete/2`, and deterministic `url/2`; return `{:error, reason}` on failures and `capabilities/0` as exactly `[:local]`. In S3 adapter, implement `store/3`, `delete/2`, `url/2`, and `presigned_put/3` using ExAws APIs; return `capabilities/0` as exactly `[:presigned_put]`. Add helper wrappers in `Rindle` for adapter invocation that return tagged tuples (`ERR-01`) and log storage processing failures at error level with `asset_id`, `variant_name`, and `reason` metadata (`ERR-02`). Adapter resolution must dispatch per profile module via `profile.storage_adapter/0` (STOR-05), not a global application-level adapter setting.</action>
  <verify>mix compile --warnings-as-errors && rg "def capabilities, do: \\[:(local|presigned_put)\\]" lib/rindle/storage</verify>
  <acceptance_criteria>
    - `lib/rindle/storage/local.ex` contains `@behaviour Rindle.Storage`
    - `lib/rindle/storage/local.ex` contains `def capabilities, do: [:local]`
    - `lib/rindle/storage/s3.ex` contains `def capabilities, do: [:presigned_put]`
    - `lib/rindle/storage/s3.ex` contains `def presigned_put(key, expires_in, opts) do`
    - `lib/rindle.ex` contains `def storage_adapter_for(profile) do`
    - `lib/rindle.ex` contains `profile.storage_adapter()`
    - `lib/rindle.ex` contains `{:ok, result} | {:error, reason}`
    - `lib/rindle.ex` contains `Logger.error("rindle.storage.variant_processing_failed"`
  </acceptance_criteria>
  <done>Storage adapters satisfy behaviour contracts and expose truthful capability/error semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Add adapter conformance tests and MinIO integration hook for STOR-07 evidence</name>
  <files>test/rindle/storage/storage_adapter_test.exs, test/rindle/config/config_test.exs</files>
  <read_first>lib/rindle/storage/local.ex, lib/rindle/storage/s3.ex, lib/rindle/config.ex, .planning/phases/01-foundation/01-VALIDATION.md</read_first>
  <action>Add `Rindle.Storage.StorageAdapterTest` verifying both adapters implement all callbacks, capability lists match expected values, and failure paths return tagged tuples. Include a profile-level adapter dispatch test that defines two profile modules with different `storage` selections and asserts `Rindle` routes operations by profile module (STOR-05) rather than global config. Include an S3 integration test block gated by `System.get_env("RINDLE_MINIO_URL")` that runs only when MinIO env vars are present; otherwise mark with `@tag :minio` and skip with explicit message. Add `Rindle.Config.ConfigTest` checking queue and TTL defaults from `config/config.exs` and per-test override behavior. Keep tests Phase 1 scoped; do not introduce upload/delivery workflows.</action>
  <verify>mix test test/rindle/storage/storage_adapter_test.exs test/rindle/config/config_test.exs</verify>
  <acceptance_criteria>
    - `test/rindle/storage/storage_adapter_test.exs` contains `assert [:local] == Rindle.Storage.Local.capabilities()`
    - `test/rindle/storage/storage_adapter_test.exs` contains `assert [:presigned_put] == Rindle.Storage.S3.capabilities()`
    - `test/rindle/storage/storage_adapter_test.exs` contains `defmodule LocalProfile do`
    - `test/rindle/storage/storage_adapter_test.exs` contains `defmodule S3Profile do`
    - `test/rindle/storage/storage_adapter_test.exs` contains `assert Rindle.storage_adapter_for(LocalProfile) == Rindle.Storage.Local`
    - `test/rindle/storage/storage_adapter_test.exs` contains `assert Rindle.storage_adapter_for(S3Profile) == Rindle.Storage.S3`
    - `test/rindle/storage/storage_adapter_test.exs` contains `@tag :minio`
    - `test/rindle/storage/storage_adapter_test.exs` contains `System.get_env("RINDLE_MINIO_URL")`
    - `test/rindle/config/config_test.exs` contains `assert :rindle == Rindle.Config.queue_name()`
  </acceptance_criteria>
  <done>Storage/config requirements have executable tests plus an explicit MinIO verification hook for integration evidence.</done>
</task>
</tasks>

<threat_model>
trust_boundaries:
  - "Boundary between adapter implementations and external storage providers"
  - "Boundary between configuration defaults and runtime overrides"
  - "Boundary between public API wrappers and low-level adapter failure details"
stride_register:
  - threat_id: T-01-06-S
    category: spoofing
    component: adapter capability advertising
    disposition: mitigate
    mitigation_plan: "Hardcode truthful capability lists (`[:local]`, `[:presigned_put]`) and assert them in adapter tests."
  - threat_id: T-01-06-T
    category: tampering
    component: storage operation error handling
    disposition: mitigate
    mitigation_plan: "Return tagged tuples from adapter calls and disallow silent raises in domain paths to preserve consistent caller handling."
  - threat_id: T-01-06-I
    category: information_disclosure
    component: storage error logging
    disposition: mitigate
    mitigation_plan: "Log structured context keys (asset_id, variant_name, reason) without leaking secrets from provider credentials."
  - threat_id: T-01-06-D
    category: denial_of_service
    component: missing/minimal config defaults
    disposition: mitigate
    mitigation_plan: "Centralize queue/TTL defaults in `config :rindle` and code accessors to prevent nil-config crash loops."
</threat_model>

<verification>
- [ ] `mix deps.get` succeeds
- [ ] `mix compile --warnings-as-errors` succeeds
- [ ] `mix test test/rindle/storage/storage_adapter_test.exs test/rindle/config/config_test.exs` succeeds
- [ ] `rg "config :rindle, :signed_url_ttl_seconds, 900" config/config.exs` returns one match
</verification>

<success_criteria>
- Local and S3 adapters exist with complete callback implementations and truthful capability flags
- Config defaults for queue and TTLs are codified and testable
- Public/adapter interfaces return tagged tuples and storage failure logs include required context
- Frontmatter covers all storage/config/error IDs assigned to this plan
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-06-SUMMARY.md` with adapter conformance and config verification evidence.
</output>

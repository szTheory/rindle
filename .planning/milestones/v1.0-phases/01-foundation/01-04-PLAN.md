---
phase: 01-foundation
plan: "04"
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-03"
files_modified:
  - lib/rindle/domain/asset_fsm.ex
  - lib/rindle/domain/variant_fsm.ex
  - lib/rindle/domain/upload_session_fsm.ex
  - lib/rindle/domain/stale_policy.ex
  - test/rindle/domain/lifecycle_fsm_test.exs
autonomous: true
requirements:
  - ASM-01
  - ASM-02
  - ASM-03
  - ASM-04
  - ASM-05
  - ASM-06
  - ASM-07
  - ASM-08
  - ASM-09
  - ASM-10
  - VSM-01
  - VSM-02
  - VSM-03
  - VSM-04
  - VSM-05
  - VSM-06
  - VSM-07
  - VSM-08
  - USM-01
  - USM-02
  - USM-03
  - USM-04
  - USM-05
  - USM-06
  - USM-07
  - USM-08
  - USM-09
  - STALE-02
  - STALE-03
  - ERR-03
  - ERR-04
  - ERR-05
user_setup: []
must_haves:
  truths:
    - "Asset, variant, and upload-session state transitions are explicitly allowlisted and reject illegal jumps."
    - "Lifecycle failures and quarantine/expiry events emit structured warning/info logs with contextual IDs."
    - "Stale variant behavior policy and stale-targeting query primitives exist for downstream URL/mix-task consumers."
  artifacts:
    - path: "lib/rindle/domain/asset_fsm.ex"
      provides: "Asset transition matrix and transition function"
      contains: "@allowed_transitions"
    - path: "lib/rindle/domain/upload_session_fsm.ex"
      provides: "Upload session transition matrix including expired/failed/aborted terminals"
      contains: "initialized"
    - path: "lib/rindle/domain/stale_policy.ex"
      provides: "Serve-stale/fallback policy and stale scope helper for regeneration targeting"
      contains: "def resolve_stale_variant"
    - path: "test/rindle/domain/lifecycle_fsm_test.exs"
      provides: "Valid and invalid transition matrix coverage"
      min_lines: 110
  key_links:
    - from: "lib/rindle/domain/variant_fsm.ex"
      to: "lib/rindle/domain/stale_policy.ex"
      via: "stale transition and downstream stale handling policy"
      pattern: "\"stale\""
    - from: "lib/rindle/domain/upload_session_fsm.ex"
      to: "test/rindle/domain/lifecycle_fsm_test.exs"
      via: "session expiry/failure transition tests"
      pattern: "expired"
---

<objective>
Implement explicit lifecycle state-machine modules for assets, variants, and upload sessions, including stale-behavior foundations and structured transition logging.

Purpose: make Phase 1 lifecycle invariants enforceable and testable before upload/delivery workers are added in later phases.
Output: three FSM modules, stale policy module, and transition matrix tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-RESEARCH.md
@lib/rindle/domain/media_asset.ex
@lib/rindle/domain/media_variant.ex
@lib/rindle/domain/media_upload_session.ex
@test/support/data_case.ex
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add explicit transition allowlists for asset, variant, and upload session lifecycles</name>
  <files>lib/rindle/domain/asset_fsm.ex, lib/rindle/domain/variant_fsm.ex, lib/rindle/domain/upload_session_fsm.ex</files>
  <read_first>.planning/REQUIREMENTS.md, lib/rindle/domain/media_asset.ex, lib/rindle/domain/media_variant.ex, lib/rindle/domain/media_upload_session.ex</read_first>
  <action>Create `Rindle.Domain.AssetFSM`, `Rindle.Domain.VariantFSM`, and `Rindle.Domain.UploadSessionFSM` with `@allowed_transitions` maps keyed by current state string. Implement `transition(current_state, target_state, context \\ %{})` returning `:ok | {:error, {:invalid_transition, current_state, target_state}}`. Encode exact required paths: asset `staged -> validating -> analyzing -> promoting -> available -> processing -> ready`, plus ASM-07/08/09 transitions `ready -> degraded` (partial variant failure), `available/processing/degraded -> quarantined` (scanner or MIME validation failure), and `ready/degraded/quarantined -> deleted` (post-purge terminal state). Variant path is `planned -> queued -> processing -> ready` with terminal `failed/stale/missing/purged`; upload session path is `initialized -> signed -> uploading -> uploaded -> verifying -> completed` with `aborted/expired/failed` terminal branches. Reject direct `staged -> ready` and invalid-source transitions such as `staged -> degraded`, `validating -> quarantined`, and `analyzing -> deleted`.</action>
  <verify>mix compile --warnings-as-errors && rg "@allowed_transitions" lib/rindle/domain/*_fsm.ex && rg "\"(degraded|quarantined|deleted)\"" lib/rindle/domain/asset_fsm.ex</verify>
  <acceptance_criteria>
    - `lib/rindle/domain/asset_fsm.ex` contains `"staged" => ["validating"]`
    - `lib/rindle/domain/asset_fsm.ex` contains `{:invalid_transition, "staged", "ready"}`
    - `lib/rindle/domain/asset_fsm.ex` contains `"ready" => ["degraded", "deleted"]`
    - `lib/rindle/domain/asset_fsm.ex` contains `"processing" => ["ready", "quarantined"]`
    - `lib/rindle/domain/asset_fsm.ex` contains `"quarantined" => ["deleted"]`
    - `lib/rindle/domain/asset_fsm.ex` contains `{:invalid_transition, "staged", "degraded"}`
    - `lib/rindle/domain/asset_fsm.ex` contains `{:invalid_transition, "analyzing", "deleted"}`
    - `lib/rindle/domain/variant_fsm.ex` contains `"planned" => ["queued"]`
    - `lib/rindle/domain/upload_session_fsm.ex` contains `"uploaded" => ["verifying"]`
    - `lib/rindle/domain/upload_session_fsm.ex` contains `"verifying" => ["completed", "failed"]`
  </acceptance_criteria>
  <done>FSM modules compile and encode all required valid/invalid state transition rules.</done>
</task>

<task type="auto">
  <name>Task 2: Add lifecycle log helpers for transition failures, session expiry, and quarantine</name>
  <files>lib/rindle/domain/asset_fsm.ex, lib/rindle/domain/upload_session_fsm.ex</files>
  <read_first>lib/rindle/domain/asset_fsm.ex, lib/rindle/domain/upload_session_fsm.ex, .planning/REQUIREMENTS.md</read_first>
  <action>Augment FSM modules with helper functions that log structured events using `Logger.warning/2` and `Logger.info/2`. On invalid transition, log attempted transition, current state, and reason map (`ERR-03`). Add `log_session_expiry(session_id, elapsed_seconds)` in upload session FSM to emit info-level log (`ERR-04`). Add `log_quarantine(asset_id, detected_mime, reason)` in asset FSM to emit warning-level log (`ERR-05`). Ensure log metadata keys are exactly `asset_id`, `session_id`, `from_state`, `to_state`, `detected_mime`, and `reason` where applicable.</action>
  <verify>mix test test/rindle/domain/lifecycle_fsm_test.exs --seed 0</verify>
  <acceptance_criteria>
    - `lib/rindle/domain/asset_fsm.ex` contains `Logger.warning("rindle.asset.transition_failed"`
    - `lib/rindle/domain/asset_fsm.ex` contains `Logger.warning("rindle.asset.quarantined"`
    - `lib/rindle/domain/upload_session_fsm.ex` contains `Logger.info("rindle.upload_session.expired"`
    - `lib/rindle/domain/asset_fsm.ex` contains `asset_id:`
    - `lib/rindle/domain/upload_session_fsm.ex` contains `session_id:`
    - `lib/rindle/domain/asset_fsm.ex` contains `from_state:`
    - `lib/rindle/domain/asset_fsm.ex` contains `to_state:`
    - `lib/rindle/domain/asset_fsm.ex` contains `detected_mime:`
    - `lib/rindle/domain/asset_fsm.ex` contains `reason:`
  </acceptance_criteria>
  <done>Lifecycle failure paths produce structured warning/info logs with required context keys.</done>
</task>

<task type="auto">
  <name>Task 3: Implement stale handling policy primitives and transition matrix tests</name>
  <files>lib/rindle/domain/stale_policy.ex, test/rindle/domain/lifecycle_fsm_test.exs</files>
  <read_first>lib/rindle/domain/variant_fsm.ex, lib/rindle/profile/digest.ex, .planning/REQUIREMENTS.md, .planning/phases/01-foundation/01-VALIDATION.md</read_first>
  <action>Create `Rindle.Domain.StalePolicy` with `resolve_stale_variant(stale_mode, variant_state, original_url)` where `stale_mode` accepts `:serve_stale` or `:fallback_original`; return `{:serve_variant, :stale}` or `{:serve_original, original_url}` to provide Phase 1 foundation for STALE-02. Add `stale_regeneration_scope(query)` that applies `where([v], v.state == "stale")` for Phase 4 mix-task targeting (STALE-03 foundation). Build `lifecycle_fsm_test.exs` with valid + invalid transition matrix assertions for ASM/VSM/USM and stale policy behavior checks.</action>
  <verify>mix test test/rindle/domain/lifecycle_fsm_test.exs</verify>
  <acceptance_criteria>
    - `lib/rindle/domain/stale_policy.ex` contains `def resolve_stale_variant(stale_mode, variant_state, original_url) do`
    - `lib/rindle/domain/stale_policy.ex` contains `where([v], v.state == "stale")`
    - `test/rindle/domain/lifecycle_fsm_test.exs` contains `assert {:error, {:invalid_transition, "staged", "ready"}}`
    - `test/rindle/domain/lifecycle_fsm_test.exs` contains `assert :ok == Rindle.Domain.VariantFSM.transition("queued", "processing")`
    - `test/rindle/domain/lifecycle_fsm_test.exs` contains `assert {:serve_original,`
  </acceptance_criteria>
  <done>FSM invariants and stale policy foundations are automated and enforceable.</done>
</task>
</tasks>

<threat_model>
trust_boundaries:
  - "Boundary between persisted state values and requested lifecycle transitions"
  - "Boundary between stale variant serving policy and URL generation consumers"
  - "Boundary between upload session timers and external upload client behavior"
stride_register:
  - threat_id: T-01-04-T
    category: tampering
    component: state transition requests
    disposition: mitigate
    mitigation_plan: "Use explicit allowlist maps and reject invalid transitions with tagged errors so callers cannot force arbitrary lifecycle jumps."
  - threat_id: T-01-04-R
    category: repudiation
    component: lifecycle failure events
    disposition: mitigate
    mitigation_plan: "Emit structured warning/info logs containing state and reason metadata for transition failures, quarantines, and expiries."
  - threat_id: T-01-04-D
    category: denial_of_service
    component: stale variant regeneration targeting
    disposition: mitigate
    mitigation_plan: "Provide stale-only query scope helper to avoid full variant sweeps when downstream regeneration tasks are introduced."
  - threat_id: T-01-04-E
    category: elevation_of_privilege
    component: direct terminal-state mutation
    disposition: mitigate
    mitigation_plan: "Constrain terminal transitions (failed/purged/deleted/expired) to allowed source states only; reject all other transitions."
</threat_model>

<verification>
- [ ] `mix compile --warnings-as-errors` succeeds
- [ ] `mix test test/rindle/domain/lifecycle_fsm_test.exs` succeeds
- [ ] `rg "\"staged\" => \\[\"validating\"\\]" lib/rindle/domain/asset_fsm.ex` returns one match
- [ ] `rg "Logger\\.warning\\(\"rindle\\.asset\\.quarantined\"" lib/rindle/domain/asset_fsm.ex` returns one match
</verification>

<success_criteria>
- FSM modules enforce all ASM/VSM/USM transitions and reject invalid jumps
- Structured logs exist for transition failures, expiries, and quarantines
- Stale policy primitives provide foundation for stale serving and stale-only regeneration targeting
- Frontmatter requirement coverage includes all state-machine families plus stale/error requirements
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md` with transition matrix evidence and stale policy behavior notes.
</output>

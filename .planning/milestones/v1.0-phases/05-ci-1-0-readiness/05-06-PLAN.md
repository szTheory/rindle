---
phase: 05-ci-1-0-readiness
plan: 06
type: execute
wave: 4
depends_on:
  - "05-03"
  - "05-04"
files_modified:
  - lib/rindle/repo.ex
  - lib/rindle/domain/media_asset.ex
  - lib/rindle/domain/media_attachment.ex
  - lib/rindle/domain/media_variant.ex
  - lib/rindle/domain/media_upload_session.ex
  - lib/rindle/domain/media_processing_run.ex
  - lib/rindle.ex
  - lib/rindle/upload/broker.ex
  - lib/rindle/delivery.ex
  - mix.exs
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - DOC-08
must_haves:
  truths:
    - "All 5 domain schema modules (media_asset, media_attachment, media_variant, media_upload_session, media_processing_run) have a full `@moduledoc` block per D-17 (DOC-08)"
    - "`lib/rindle/repo.ex` has `@moduledoc false` per D-17 (DOC-08)"
    - "Every public function in `lib/rindle.ex`, `lib/rindle/upload/broker.ex`, and `lib/rindle/delivery.ex` has an `@doc` block with at least one `iex>` doctest example OR a runnable code excerpt with a documented rationale for why a doctest is infeasible (DOC-08 — `@doc` on public functions, not just `@moduledoc`)"
    - "`mix.exs docs/0` lists ALL seven guides explicitly in `extras:` (no glob — verified pattern from D-15 + Pitfall 6 in RESEARCH)"
    - "`mix.exs docs/0` declares `groups_for_extras:` with at least a `Guides` group regex matching `guides/*`"
    - "`mix.exs docs/0` injects Mermaid CDN via `before_closing_head_tag:` (D-15; for DOC-02 state diagrams in Plan 07)"
    - "`mix docs` builds successfully and produces HTML output"
    - "`mix credo --strict` continues to pass after `@moduledoc` and `@doc` additions (Pitfall 4 prevention)"
    - "A new CI step in the adopter job greps `guides/getting_started.md` for the canonical adopter API calls (`Broker.initiate_session`, `Broker.verify_completion`, `Rindle.Delivery.url`) and FAILS the lane if any are missing — drift between the snippet and the adopter lane code path is a CI failure, not a docs review concern (D-16)"
    - "`elixirc_paths(:test)` in mix.exs still includes `test/adopter` (Plan 04 invariant preserved through this plan's mix.exs edits)"
  artifacts:
    - path: "lib/rindle/repo.ex"
      provides: "@moduledoc false per D-17"
      contains: "@moduledoc false"
    - path: "lib/rindle/domain/media_asset.ex"
      provides: "Full @moduledoc with state table per PATTERNS.md DOC-08 audit"
      contains: "@moduledoc"
    - path: "lib/rindle.ex"
      provides: "Every public def has @doc with at least one iex> example or documented rationale"
      contains: "iex>"
    - path: "lib/rindle/upload/broker.ex"
      provides: "@doc on initiate_session/2, sign_url/2, verify_completion/2"
      contains: "@doc"
    - path: "lib/rindle/delivery.ex"
      provides: "@doc on url/3, variant_url/4, and the public profile/policy helpers"
      contains: "@doc"
    - path: "mix.exs"
      provides: "docs/0 with extras + groups_for_extras + before_closing_head_tag (Mermaid CDN); package: [files: ...] (used by Plan 05)"
      contains: "before_closing_head_tag"
    - path: ".github/workflows/ci.yml"
      provides: "Adopter job (or new docs-lint job) greps guides/getting_started.md for adopter-lane canonical API calls (D-16 drift gate)"
      contains: "Broker.initiate_session"
  key_links:
    - from: "mix.exs docs/0"
      to: "guides/*.md"
      via: "extras: list (no glob — Pitfall 6); files authored in Plan 07"
      pattern: "guides/getting_started.md"
    - from: ".github/workflows/ci.yml (adopter job)"
      to: "guides/getting_started.md"
      via: "grep step asserts canonical adopter API call presence (D-16)"
      pattern: "Broker\\.initiate_session"
---

<objective>
Complete the DOC-08 audit (per D-17): five domain schema `@moduledoc` blocks, `lib/rindle/repo.ex` `@moduledoc false`, AND `@doc` blocks on every public function in `lib/rindle.ex`, `lib/rindle/upload/broker.ex`, and `lib/rindle/delivery.ex` — each with at least one `iex>` doctest example or a runnable code excerpt where a doctest is infeasible. Also extend `mix.exs docs/0` with the seven `extras:` (Plan 07 authors the guide content), `groups_for_extras:`, Mermaid CDN injection via `before_closing_head_tag:`, and add a CI grep step that fails the adopter lane on snippet drift (D-16).

Per D-17: 5 domain schema modules get full `@moduledoc`; `repo.ex` gets `@moduledoc false`; `application.ex` keeps existing `@moduledoc false`. Per DOC-08 (full text): every public function gets `@doc` with at least one example. Per D-15: ExDoc wires `extras:` + `groups_for_extras:` + Mermaid CDN. Per D-16: adopter lane CI fails on snippet drift.

Output: 5 modified domain modules + repo.ex + 3 modified public-API modules (with `@doc` blocks) + mix.exs docs/0 wiring + CI drift-check step. Wave 4 — depends on Plan 03 (ex_doc 0.40 unlocked, mix.exs already touched) and Plan 04 (adopter lane and elixirc_paths in place; CI grep step extends the adopter job).

NOTE: The seven guide files (DOC-01..07) are authored in Plan 07. This plan ONLY wires the `extras:` list and Mermaid renderer. `mix docs` will fail to find the guide files until Plan 07 lands — that is expected; both plans are in Wave 4 and the verify step in this plan's mix.exs task uses `--no-warnings-as-errors` for the docs step OR runs `mix docs` only after Plan 07 has been merged. Acceptance criteria reflect this ordering: this plan's `mix docs` step is allowed to warn about missing extras until Plan 07 ships.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ci-1-0-readiness/05-CONTEXT.md
@.planning/phases/05-ci-1-0-readiness/05-RESEARCH.md
@.planning/phases/05-ci-1-0-readiness/05-PATTERNS.md
@.planning/phases/05-ci-1-0-readiness/05-04-SUMMARY.md
@mix.exs
@lib/rindle.ex
@lib/rindle/upload/broker.ex
@lib/rindle/delivery.ex
@lib/rindle/domain/asset_fsm.ex
@lib/rindle/domain/variant_fsm.ex
@lib/rindle/domain/upload_session_fsm.ex
@lib/rindle/domain/media_asset.ex
@lib/rindle/domain/media_attachment.ex
@lib/rindle/domain/media_variant.ex
@lib/rindle/domain/media_upload_session.ex
@lib/rindle/domain/media_processing_run.ex
@lib/rindle/repo.ex
@.github/workflows/ci.yml

<interfaces>
<!-- ExDoc 0.40 docs/0 surface (RESEARCH Pattern 5 lines 397-462). -->

```elixir
defp docs do
  [
    main: "Rindle",
    source_url: @source_url,
    extras: [
      "README.md",
      "guides/getting_started.md",
      "guides/core_concepts.md",
      "guides/profiles.md",
      "guides/secure_delivery.md",
      "guides/background_processing.md",
      "guides/operations.md",
      "guides/troubleshooting.md"
    ],
    groups_for_extras: [
      Guides: ~r/guides\/.*/
    ],
    before_closing_head_tag: &before_closing_head_tag/1
  ]
end
```

`before_closing_head_tag/1` Mermaid CDN injection (RESEARCH lines 425-457):
```elixir
defp before_closing_head_tag(:html) do
  """
  <script defer src="https://cdn.jsdelivr.net/npm/mermaid@10.2.3/dist/mermaid.min.js"></script>
  <script>
    let initialized = false;
    window.addEventListener("exdoc:loaded", () => {
      if (!initialized) {
        mermaid.initialize({
          startOnLoad: false,
          theme: document.body.className.includes("dark") ? "dark" : "default"
        });
        initialized = true;
      }
      let id = 0;
      for (const codeEl of document.querySelectorAll("pre code.mermaid")) {
        const preEl = codeEl.parentElement;
        const graphDefinition = codeEl.textContent;
        const graphEl = document.createElement("div");
        const graphId = "mermaid-graph-" + id++;
        mermaid.render(graphId, graphDefinition).then(({svg, bindFunctions}) => {
          graphEl.innerHTML = svg;
          bindFunctions?.(graphEl);
          preEl.insertAdjacentElement("afterend", graphEl);
          preEl.remove();
        });
      }
    });
  </script>
  """
end

defp before_closing_head_tag(:epub), do: ""
```

Domain schema `@moduledoc` template (PATTERNS.md DOC-08 audit lines 689-725):
```elixir
@moduledoc """
Ecto schema for a media asset.

A `MediaAsset` represents a single uploaded file moving through the
`Rindle.Domain.AssetFSM` lifecycle.

## States

| State | Meaning |
|-------|---------|
| `"staged"` | Reserved slot; upload has not been verified. |
...
"""
```

`Rindle.Repo` minimal `@moduledoc false`:
```elixir
defmodule Rindle.Repo do
  @moduledoc false
  use Ecto.Repo, otp_app: :rindle, adapter: Ecto.Adapters.Postgres
end
```

Public functions requiring `@doc` (read each file to enumerate before authoring):
- `lib/rindle.ex` — every `def` not marked `defp` (run `grep -n "^\s*def " lib/rindle.ex` and treat each as a target)
- `lib/rindle/upload/broker.ex` — `initiate_session/2` (line 16), `sign_url/2` (line 59), `verify_completion/2` (line 86)
- `lib/rindle/delivery.ex` — `profile_delivery_policy/1` (line 14), `public_delivery?/1` (line 17), `signed_url_ttl_seconds/1` (line 20), `delivery_authorizer/1` (line 25), `url/3` (line 28), `variant_url/4` (line 41)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DOC-08 audit — add @moduledoc to 5 domain schemas + Repo</name>
  <files>
    - lib/rindle/repo.ex
    - lib/rindle/domain/media_asset.ex
    - lib/rindle/domain/media_attachment.ex
    - lib/rindle/domain/media_variant.ex
    - lib/rindle/domain/media_upload_session.ex
    - lib/rindle/domain/media_processing_run.ex
  </files>

  <read_first>
    - lib/rindle/repo.ex (5 lines — add `@moduledoc false`)
    - lib/rindle/domain/media_asset.ex (read full file — note `@states` module attribute or list of state strings used in the schema)
    - lib/rindle/domain/media_attachment.ex (full — no FSM; document ownership semantics)
    - lib/rindle/domain/media_variant.ex (full — paired with VariantFSM)
    - lib/rindle/domain/media_upload_session.ex (full — paired with UploadSessionFSM)
    - lib/rindle/domain/media_processing_run.ex (full — typically no FSM; document audit-log purpose)
    - lib/rindle/domain/asset_fsm.ex (state list to mirror in MediaAsset @moduledoc)
    - lib/rindle/domain/variant_fsm.ex (state list to mirror in MediaVariant @moduledoc)
    - lib/rindle/domain/upload_session_fsm.ex (state list to mirror in MediaUploadSession @moduledoc)
    - .planning/phases/05-ci-1-0-readiness/05-PATTERNS.md (DOC-08 audit section lines 678-733 — copy the @moduledoc template verbatim, customize per module)
    - .planning/phases/05-ci-1-0-readiness/05-CONTEXT.md (D-17 — exact module list)
    - .planning/phases/05-ci-1-0-readiness/05-RESEARCH.md (Pitfall 4 — Domain Schema Modules Trigger Credo ModuleDoc Warning, lines 580-584)
  </read_first>

  <action>
    Step 1 — Modify `lib/rindle/repo.ex`. Add `@moduledoc false` immediately after `defmodule`, before `use`:

    ```elixir
    defmodule Rindle.Repo do
      @moduledoc false

      use Ecto.Repo,
        otp_app: :rindle,
        adapter: Ecto.Adapters.Postgres
    end
    ```

    Step 2 — Modify `lib/rindle/domain/media_asset.ex`. Add `@moduledoc` block after `defmodule` and before `use Ecto.Schema`. Copy the template from PATTERNS.md lines 689-718 (state table from AssetFSM allowed_transitions):

    ```elixir
    defmodule Rindle.Domain.MediaAsset do
      @moduledoc """
      Ecto schema for a media asset.

      A `MediaAsset` represents a single uploaded file moving through the
      `Rindle.Domain.AssetFSM` lifecycle. Each row tracks the asset's current
      state, MIME type, byte size, and the storage key under which the
      original file is stored.

      ## States

      | State | Meaning |
      |-------|---------|
      | `"staged"` | Reserved slot; upload has not been verified. |
      | `"validating"` | Upload verified; MIME scan in progress. |
      | `"analyzing"` | Dimensions / metadata extraction in progress. |
      | `"promoting"` | Promotion job running. |
      | `"available"` | Ready for variant processing and delivery. |
      | `"processing"` | One or more variant jobs running. |
      | `"ready"` | All configured variants generated. |
      | `"degraded"` | One or more variants failed; original still deliverable. |
      | `"quarantined"` | MIME mismatch or scan failure; not deliverable. |
      | `"deleted"` | Soft-deleted; storage object may already be purged. |

      See `Rindle.Domain.AssetFSM` for valid state transitions.
      """

      use Ecto.Schema
      import Ecto.Changeset
      ...
    ```

    Step 3 — Modify `lib/rindle/domain/media_variant.ex`. Add `@moduledoc` with the VariantFSM state table:

    ```elixir
    defmodule Rindle.Domain.MediaVariant do
      @moduledoc """
      Ecto schema for a derived variant of a media asset.

      A `MediaVariant` represents one named output (e.g. `:thumb`, `:large`)
      derived from a source asset. Each variant moves through the
      `Rindle.Domain.VariantFSM` lifecycle and stores its own storage key,
      recipe digest, and ready/failed/stale state.

      ## States

      | State | Meaning |
      |-------|---------|
      | `"planned"` | Variant row exists; processing not yet enqueued. |
      | `"queued"` | Oban job enqueued; awaiting processor. |
      | `"processing"` | Processor is generating the variant. |
      | `"ready"` | Variant generated and stored; deliverable. |
      | `"failed"` | Processing failed past the retry budget. |
      | `"stale"` | Recipe digest changed; existing object outdated. |
      | `"missing"` | Storage reconciliation found the object absent. |
      | `"purged"` | Variant explicitly removed; storage object deleted. |

      See `Rindle.Domain.VariantFSM` for valid state transitions and
      `Rindle.Domain.StalePolicy` for stale-serving behavior.
      """

      use Ecto.Schema
      ...
    ```

    Step 4 — Modify `lib/rindle/domain/media_upload_session.ex`. Add `@moduledoc` with the UploadSessionFSM state table (read upload_session_fsm.ex first to get the canonical state list):

    ```elixir
    defmodule Rindle.Domain.MediaUploadSession do
      @moduledoc """
      Ecto schema for a presigned upload session.

      An `MediaUploadSession` tracks the lifecycle of a direct-to-storage
      upload — from the moment Rindle issues a presigned PUT URL through
      verification of the uploaded object.

      ## States

      | State | Meaning |
      |-------|---------|
      | `"initialized"` | Session created; no presigned URL yet. |
      | `"signed"` | Presigned PUT URL issued to client. |
      | `"uploading"` | Client has begun (or is presumed to have begun) the PUT. |
      | `"uploaded"` | Storage reports the object exists at the expected key. |
      | `"verifying"` | Server-side validation (MIME, size) in progress. |
      | `"completed"` | Verification passed; asset promoted. |
      | `"aborted"` | Client cancelled or server rejected. |
      | `"expired"` | TTL elapsed before completion. |
      | `"failed"` | Verification failed (MIME mismatch, size limit, scanner). |

      See `Rindle.Domain.UploadSessionFSM` for valid transitions and
      `Rindle.Upload.Broker` for the lifecycle entry points.
      """

      use Ecto.Schema
      ...
    ```

    Step 5 — Modify `lib/rindle/domain/media_attachment.ex`. No FSM — document the ownership semantics:

    ```elixir
    defmodule Rindle.Domain.MediaAttachment do
      @moduledoc """
      Ecto schema linking a `Rindle.Domain.MediaAsset` to an owning entity.

      Attachments are the polymorphic association layer: any application
      record (a `User`, a `Post`, an `Article`) can own one or more assets
      via attachment rows. The attachment record carries the owner type,
      owner id, and an optional slot name (e.g. `"avatar"`, `"hero"`)
      that disambiguates multiple attachments of the same kind.

      Attachments have no lifecycle state machine of their own — they
      reflect a current ownership claim. Use `Rindle.attach/4` and
      `Rindle.detach/3` to mutate attachment rows; both functions
      participate in the same DB transaction as the asset state change
      they describe.

      Concurrent replacement (two clients attaching different assets to
      the same owner+slot) is detected by reloading inside the transaction
      and returning `{:error, :replaced}` rather than overwriting the
      newer attachment (see `Rindle.attach/4`).
      """

      use Ecto.Schema
      ...
    ```

    Step 6 — Modify `lib/rindle/domain/media_processing_run.ex`. Document the audit-log purpose:

    ```elixir
    defmodule Rindle.Domain.MediaProcessingRun do
      @moduledoc """
      Ecto schema recording the outcome of a single processing or maintenance
      operation against a media asset or variant.

      A `MediaProcessingRun` is the audit trail for background work — variant
      generation outcomes, purge results, scanner verdicts, and metadata
      backfill runs. Each row captures the worker module, the affected
      record, the outcome (`:ok`, `:error`, `:skipped`), and any structured
      detail emitted by the operation.

      Processing runs are not consulted for delivery decisions; they exist
      for operator visibility, retry diagnostics, and Day-2 cleanup queries.
      """

      use Ecto.Schema
      ...
    ```

    Step 7 — Verify Credo still passes after the additions: `mix credo --strict` exits 0. (Per Pitfall 4, missing `@moduledoc` may have been silently tolerated; adding them should make Credo happier, not sadder.)
  </action>

  <verify>
    <automated>mix compile --warnings-as-errors && mix credo --strict && grep -l "@moduledoc false" lib/rindle/repo.ex && for f in lib/rindle/domain/media_asset.ex lib/rindle/domain/media_attachment.ex lib/rindle/domain/media_variant.ex lib/rindle/domain/media_upload_session.ex lib/rindle/domain/media_processing_run.ex; do grep -l '@moduledoc """' "$f" || (echo "MISSING @moduledoc in $f" && exit 1); done</automated>
  </verify>

  <acceptance_criteria>
    - `grep "@moduledoc false" lib/rindle/repo.ex` returns 1 match
    - For each of the 5 domain schemas: `grep -c '@moduledoc """' $f` returns 1
    - `grep "## States" lib/rindle/domain/media_asset.ex` returns 1 match
    - `grep "## States" lib/rindle/domain/media_variant.ex` returns 1 match
    - `grep "## States" lib/rindle/domain/media_upload_session.ex` returns 1 match
    - `mix compile --warnings-as-errors` exits 0
    - `mix credo --strict` exits 0
    - `mix format --check-formatted` exits 0
  </acceptance_criteria>

  <done>
    `lib/rindle/repo.ex` has `@moduledoc false`. All 5 domain schemas have full `@moduledoc` blocks with state tables (where applicable) and clear ownership/audit-log semantics. Credo and format gates pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DOC-08 @doc audit — public functions in rindle.ex, broker.ex, delivery.ex</name>
  <files>
    - lib/rindle.ex
    - lib/rindle/upload/broker.ex
    - lib/rindle/delivery.ex
  </files>

  <read_first>
    - lib/rindle.ex (FULL file — enumerate every `def` not marked `defp`. Confirmed list from `grep -n "^  def "`: `version/0`, `initiate_upload/2`, `verify_upload/2`, `storage_adapter_for/1`, `store/4`, `attach/4`, `detach/3`, `download/4`, `delete/3`, `url/3`, `variant_url/4`, `upload/3`, `head/3`, `presigned_put/4`, `store_variant/4`, `log_variant_processing_failure/3`)
    - lib/rindle/upload/broker.ex (initiate_session/2 line 16, sign_url/2 line 59, verify_completion/2 line 86)
    - lib/rindle/delivery.ex (public defs: profile_delivery_policy/1 line 14, public_delivery?/1 line 17, signed_url_ttl_seconds/1 line 20, delivery_authorizer/1 line 25, url/3 line 28, variant_url/4 line 41)
    - .planning/phases/05-ci-1-0-readiness/05-CONTEXT.md (D-17 + DOC-08 — `@doc` is required on public functions, not just `@moduledoc` on modules)
    - .planning/phases/05-ci-1-0-readiness/05-04-SUMMARY.md (actual `attach/N` and `detach/N` signatures observed during adopter lane build — use these exact signatures for the doctests so they match the working adopter code path)
    - test/rindle/upload/broker_test.exs (existing test patterns — the doctest examples should align with how the broker is actually used)
  </read_first>

  <action>
    Step 0 — Decide which functions get DOCTESTS vs. RUNNABLE EXAMPLES vs. NON-EXECUTABLE EXAMPLES with rationale.

    DOCTESTS work when:
    - The function is pure (no Repo, no IO, no external service)
    - Inputs and outputs are fully serializable

    RUNNABLE EXAMPLES (non-doctest `iex>` blocks shown as code excerpts) work when:
    - The function requires Repo, MinIO, or filesystem
    - The example is illustrative for documentation but cannot run as a doctest

    Both styles satisfy DOC-08's "at least one example". The acceptance gate is: every public function has `@doc` with at least one `iex>` line OR an `## Examples` section with a code block AND a documented one-liner explaining why it can't be a doctest.

    Step 1 — Modify `lib/rindle.ex`. Add `@doc` immediately above each public `def`. For functions that hit Repo/storage, the `iex>` block is a runnable example shown for docs; mark it with `# requires Repo + storage adapter setup` so ExDoc renders it but the doctest runner skips it (ExDoc doctests only run when explicitly enabled per module via `doctest`).

    EXAMPLE — `version/0` (pure — true doctest):
    ```elixir
    @doc """
    Returns the current Rindle library version as a string.

    ## Examples

        iex> Rindle.version()
        "0.1.0-dev"

    """
    @spec version() :: String.t()
    def version do
      @version
    end
    ```

    EXAMPLE — `initiate_upload/2` (delegates to Broker; runnable example):
    ```elixir
    @doc """
    Initiates a presigned upload session for the given profile.

    Delegates to `Rindle.Upload.Broker.initiate_session/2`. Returns
    `{:ok, %MediaUploadSession{}}` on success.

    ## Examples

        # Requires Rindle.Repo running and a configured profile module.
        iex> {:ok, session} = Rindle.initiate_upload(MyApp.MediaProfile, filename: "x.png")
        iex> session.state
        "initialized"

    """
    def initiate_upload(profile, opts \\ []) do
      ...
    end
    ```

    EXAMPLE — `attach/4` (Repo-bound; runnable):
    ```elixir
    @doc """
    Attaches a media asset to an owning record.

    The owner argument is any struct with an `:id` field; the owner type is
    derived from the struct's module. `slot` is an optional string label
    (e.g. `"avatar"`) that disambiguates multiple attachments to the same
    owner. Runs inside an Ecto transaction with a reload-and-check guard
    against concurrent replacement (see "Concurrent replacement" in
    `Rindle.Domain.MediaAttachment`).

    ## Examples

        # Requires Rindle.Repo + an existing MediaAsset and owner record.
        iex> {:ok, attachment} = Rindle.attach(asset_id, %MyApp.User{id: user_id}, "avatar")
        iex> attachment.slot
        "avatar"

    """
    def attach(asset_or_id, owner, slot, _opts \\ []) do
      ...
    end
    ```

    REPEAT for every public def in `lib/rindle.ex`:
    - `version/0` — pure doctest as above
    - `initiate_upload/2` — runnable example delegating to Broker
    - `verify_upload/2` — runnable example delegating to Broker
    - `storage_adapter_for/1` — pure-ish (just `profile.storage_adapter()`); doctest works against a profile module
    - `store/4` — runnable example (storage IO)
    - `attach/4` — runnable example (Repo)
    - `detach/3` — runnable example (Repo)
    - `download/4` — runnable example (storage IO)
    - `delete/3` — runnable example (storage IO)
    - `url/3` — runnable example (delegates to Delivery.url)
    - `variant_url/4` — runnable example (delegates to Delivery.variant_url)
    - `upload/3` — runnable example (Plug.Upload + Repo)
    - `head/3` — runnable example (storage IO)
    - `presigned_put/4` — runnable example (storage IO)
    - `store_variant/4` — runnable example (storage IO + Repo)
    - `log_variant_processing_failure/3` — runnable example (Repo write)

    Step 2 — Modify `lib/rindle/upload/broker.ex`. Add `@doc` on each public function:

    `initiate_session/2`:
    ```elixir
    @doc """
    Creates a new upload session and inserts an asset row in the `staged` state.

    Returns `{:ok, %MediaUploadSession{}}`. Emits `[:rindle, :upload, :start]`
    telemetry AFTER the database transaction commits (per D-03 / Plan 01).

    ## Examples

        # Requires Rindle.Repo running and a profile module.
        iex> {:ok, session} = Rindle.Upload.Broker.initiate_session(MyApp.MediaProfile, filename: "photo.png")
        iex> session.state
        "initialized"

    """
    def initiate_session(profile_module, opts \\ []) do
    ```

    `sign_url/2`:
    ```elixir
    @doc """
    Issues a presigned PUT URL for the given upload session.

    Returns `{:ok, %{session: %MediaUploadSession{}, presigned: %{url: String.t()}}}`.
    Transitions the session to `signed`.

    ## Examples

        # Requires Rindle.Repo + a configured S3-compatible storage adapter.
        iex> {:ok, %{session: session, presigned: %{url: url}}} =
        ...>   Rindle.Upload.Broker.sign_url(session_id)
        iex> session.state
        "signed"
        iex> is_binary(url)
        true

    """
    def sign_url(session_id, opts \\ []) do
    ```

    `verify_completion/2`:
    ```elixir
    @doc """
    Verifies an uploaded object exists and promotes the session to completed.

    Returns `{:ok, %{session: %MediaUploadSession{state: "completed"},
    asset: %MediaAsset{state: "validating"}}}` and enqueues `PromoteAsset`.
    Emits `[:rindle, :upload, :stop]` telemetry AFTER the Ecto.Multi commits.

    ## Examples

        # Requires Rindle.Repo + the upload to have been PUT to storage.
        iex> {:ok, %{session: session, asset: asset}} =
        ...>   Rindle.Upload.Broker.verify_completion(session_id)
        iex> session.state
        "completed"
        iex> asset.state
        "validating"

    """
    def verify_completion(session_id, opts \\ []) do
    ```

    Step 3 — Modify `lib/rindle/delivery.ex`. Add `@doc` to each public function. The shorter helpers (`profile_delivery_policy/1`, `public_delivery?/1`, etc.) accept doctests against an in-test profile module. Example for `url/3`:

    ```elixir
    @doc """
    Returns a deliverable URL for an asset's storage key.

    Public profiles return the storage adapter's bare URL; private profiles
    return a signed URL with the profile's configured TTL. Emits
    `[:rindle, :delivery, :signed]` telemetry on success (Plan 01).

    ## Examples

        # Requires a configured storage adapter and a key that exists in storage.
        iex> {:ok, url} = Rindle.Delivery.url(MyApp.MediaProfile, "uploads/abc.png")
        iex> is_binary(url)
        true

    """
    def url(profile, key, opts \\ []) do
    ```

    Repeat for `variant_url/4`, `profile_delivery_policy/1`, `public_delivery?/1`, `signed_url_ttl_seconds/1`, `delivery_authorizer/1`. The four short helpers can use compact doctest blocks against a hypothetical `MyApp.MediaProfile` (shown as runnable, not enabled as actual doctests at module level).

    Step 4 — Verify with `mix docs` (allowed to warn about missing extras until Plan 07 lands; the warning is acceptable). Then `mix credo --strict` and `mix format --check-formatted`.

    Step 5 — Run the existing test suite to confirm no regressions: `mix test --exclude integration --exclude minio --exclude adopter --exclude contract`.
  </action>

  <verify>
    <automated>mix compile --warnings-as-errors && mix credo --strict && for f in lib/rindle.ex lib/rindle/upload/broker.ex lib/rindle/delivery.ex; do count=$(grep -c "iex>" "$f"); pubs=$(grep -c "^  def " "$f"); echo "$f: $count iex examples for $pubs public defs"; if [ "$count" -lt "$pubs" ]; then echo "FAIL: $f has fewer iex> blocks ($count) than public defs ($pubs)"; exit 1; fi; done</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "iex>" lib/rindle.ex` returns at least the count of `grep -c "^  def " lib/rindle.ex` (one example per public def, modulo functions where a documented rationale supersedes — see below)
    - `grep -c "iex>" lib/rindle/upload/broker.ex` returns at least 3 (one per public function: initiate_session, sign_url, verify_completion)
    - `grep -c "iex>" lib/rindle/delivery.ex` returns at least 6 (one per public function: profile_delivery_policy, public_delivery?, signed_url_ttl_seconds, delivery_authorizer, url, variant_url)
    - `grep -B1 "^  def " lib/rindle.ex | grep -c "@doc"` returns at least the count of public defs in the same file (every public def has a preceding `@doc`)
    - `grep -B1 "^  def " lib/rindle/upload/broker.ex | grep -c "@doc"` returns 3
    - `grep -B1 "^  def " lib/rindle/delivery.ex | grep -c "@doc"` returns 6
    - `mix compile --warnings-as-errors` exits 0
    - `mix credo --strict` exits 0
    - `mix format --check-formatted` exits 0
    - For any public function where a doctest is INFEASIBLE (e.g., requires real MinIO PUT), the `@doc` MUST contain a comment line starting with `# Requires` explaining why — `grep -c "# Requires" lib/rindle.ex` returns at least 1 (Repo/storage-bound functions document their setup)
  </acceptance_criteria>

  <done>
    Every public function in `lib/rindle.ex`, `lib/rindle/upload/broker.ex`, and `lib/rindle/delivery.ex` has an `@doc` block with at least one `iex>` example. Functions whose examples cannot run as live doctests carry a `# Requires ...` rationale line. Compile, credo, and format gates pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire ExDoc extras + groups + Mermaid + package files in mix.exs</name>
  <files>
    - mix.exs
  </files>

  <read_first>
    - mix.exs (current state — Plan 03 added test_coverage/excoveralls and bumped ex_doc to ~> 0.40; Plan 04 added `test/adopter` to elixirc_paths(:test); Plan 05 added `package: [files: ...]`)
    - .planning/phases/05-ci-1-0-readiness/05-RESEARCH.md (Pattern 5 — ExDoc Guides Wiring lines 397-462; Pattern 6 — package files lines 511-523; Pitfall 6 — extras: glob not supported lines 593-598)
    - .planning/phases/05-ci-1-0-readiness/05-PATTERNS.md (mix.exs section lines 558-648 — full docs/0 + package/0 shape)
    - .planning/phases/05-ci-1-0-readiness/05-CONTEXT.md (D-15 — explicit extras list, no glob)
  </read_first>

  <action>
    Step 1 — Modify `mix.exs` `docs/0` (currently ~lines 77-85). Replace the body with the full extras list, groups_for_extras, and before_closing_head_tag wiring per the interfaces block:

    ```elixir
    defp docs do
      [
        main: "Rindle",
        source_url: @source_url,
        extras: [
          "README.md",
          "guides/getting_started.md",
          "guides/core_concepts.md",
          "guides/profiles.md",
          "guides/secure_delivery.md",
          "guides/background_processing.md",
          "guides/operations.md",
          "guides/troubleshooting.md"
        ],
        groups_for_extras: [
          Guides: ~r/guides\/.*/
        ],
        before_closing_head_tag: &before_closing_head_tag/1
      ]
    end
    ```

    Step 2 — Add the `before_closing_head_tag/1` private function elsewhere in mix.exs (place it near `docs/0`):

    ```elixir
    defp before_closing_head_tag(:html) do
      """
      <script defer src="https://cdn.jsdelivr.net/npm/mermaid@10.2.3/dist/mermaid.min.js"></script>
      <script>
        let initialized = false;
        window.addEventListener("exdoc:loaded", () => {
          if (!initialized) {
            mermaid.initialize({
              startOnLoad: false,
              theme: document.body.className.includes("dark") ? "dark" : "default"
            });
            initialized = true;
          }
          let id = 0;
          for (const codeEl of document.querySelectorAll("pre code.mermaid")) {
            const preEl = codeEl.parentElement;
            const graphDefinition = codeEl.textContent;
            const graphEl = document.createElement("div");
            const graphId = "mermaid-graph-" + id++;
            mermaid.render(graphId, graphDefinition).then(({svg, bindFunctions}) => {
              graphEl.innerHTML = svg;
              bindFunctions?.(graphEl);
              preEl.insertAdjacentElement("afterend", graphEl);
              preEl.remove();
            });
          }
        });
      </script>
      """
    end

    defp before_closing_head_tag(:epub), do: ""
    ```

    Step 3 — Confirm `package/0` (added by Plan 05) is unchanged. If Plan 05 has not landed yet (race in Wave 4), add the `files:` allowlist defensively:
    ```elixir
    defp package do
      [
        licenses: ["MIT"],
        links: %{"GitHub" => @source_url},
        files: ~w(lib priv/repo/migrations mix.exs README.md LICENSE)
      ]
    end
    ```
    If `package/0` already has `files:` from Plan 05, do NOT duplicate the field — leave it as-is.

    Step 4 — Confirm `elixirc_paths(:test)` still includes `test/adopter` (Plan 04 invariant). If the line was changed by another wave's edit, restore: `defp elixirc_paths(:test), do: ["lib", "test/support", "test/adopter"]`.

    Step 5 — Run `mix compile --warnings-as-errors`, `mix format --check-formatted`. Run `mix docs` — it WILL warn or error about missing guide files until Plan 07 lands. That is expected; capture the output for the SUMMARY. The acceptance criteria below check the wiring (extras list present, before_closing_head_tag function exists), not the build success.

    Step 6 — If Plan 07 has already landed in this wave (depends on merge order), run `mix docs` and confirm it builds successfully. Otherwise note in the SUMMARY that `mix docs` will succeed only after Plan 07 ships the guide files.
  </action>

  <verify>
    <automated>mix compile --warnings-as-errors && mix format --check-formatted && grep -c "guides/getting_started.md" mix.exs && grep -c "before_closing_head_tag" mix.exs && grep -c "groups_for_extras" mix.exs</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "guides/getting_started.md" mix.exs` returns at least 1
    - `grep -c "guides/core_concepts.md" mix.exs` returns at least 1
    - `grep -c "guides/profiles.md" mix.exs` returns at least 1
    - `grep -c "guides/secure_delivery.md" mix.exs` returns at least 1
    - `grep -c "guides/background_processing.md" mix.exs` returns at least 1
    - `grep -c "guides/operations.md" mix.exs` returns at least 1
    - `grep -c "guides/troubleshooting.md" mix.exs` returns at least 1
    - `grep -c "groups_for_extras" mix.exs` returns at least 1
    - `grep -c "before_closing_head_tag" mix.exs` returns at least 2 (config reference + function definition)
    - `grep "Mermaid\\|mermaid.min.js" mix.exs` returns at least 1 match
    - `grep "test/adopter" mix.exs` returns at least 1 match (Plan 04 elixirc_paths invariant preserved — Warning 4)
    - `grep "files: ~w(" mix.exs` returns at least 1 match (Plan 05 package allowlist preserved)
    - `mix compile --warnings-as-errors` exits 0
    - `mix format --check-formatted` exits 0
  </acceptance_criteria>

  <done>
    `mix.exs docs/0` lists all 7 guides explicitly (no glob — Pitfall 6), declares `groups_for_extras: [Guides: ~r/guides\/.*/]`, and wires the Mermaid CDN renderer via `before_closing_head_tag:`. The `test/adopter` elixirc path and `package: [files: ...]` allowlist from prior plans are preserved. `mix docs` build success depends on Plan 07 landing the guide files.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Adopter-lane CI drift-check step (D-16) — guide snippet matches adopter lifecycle</name>
  <files>
    - .github/workflows/ci.yml
  </files>

  <read_first>
    - .github/workflows/ci.yml (current state — by Wave 4 has quality, integration, contract, adopter jobs from Plans 02/03/04)
    - .planning/phases/05-ci-1-0-readiness/05-CONTEXT.md (D-16 — drift between snippet and adopter code path is a CI failure)
    - .planning/phases/05-ci-1-0-readiness/05-04-SUMMARY.md (the actual API calls used in test/adopter/canonical_app/lifecycle_test.exs)
    - test/adopter/canonical_app/lifecycle_test.exs (confirm the exact API call names used: Broker.initiate_session, Broker.verify_completion, Rindle.Delivery.url)
  </read_first>

  <action>
    Step 1 — Locate the existing `adopter` job in `.github/workflows/ci.yml` (added by Plan 04). After the `Run adopter tests` step (the final step in the adopter job), append a NEW step that greps `guides/getting_started.md` for the canonical adopter API calls:

    ```yaml
          - name: Verify guides/getting_started.md matches adopter lane (D-16 drift gate)
            run: |
              set -e
              GUIDE=guides/getting_started.md
              if [ ! -f "$GUIDE" ]; then
                echo "FAIL (D-16): $GUIDE missing — DOC-01 must be authored before adopter lane drift check can pass"
                exit 1
              fi
              # The canonical adopter lifecycle uses these three public API calls.
              # If any are missing from the guide snippet, the guide has drifted
              # from the working code path (D-16 / success criterion 5.5).
              MATCHES=$(grep -E "Broker\.initiate_session|Broker\.verify_completion|Rindle\.Delivery\.url" "$GUIDE" | wc -l)
              if [ "$MATCHES" -lt 3 ]; then
                echo "FAIL (D-16): $GUIDE missing one or more canonical adopter API calls"
                echo "Expected at least 3 lines matching: Broker.initiate_session, Broker.verify_completion, Rindle.Delivery.url"
                echo "Found: $MATCHES line(s)"
                grep -nE "Broker\.initiate_session|Broker\.verify_completion|Rindle\.Delivery\.url" "$GUIDE" || true
                exit 1
              fi
              echo "OK: $GUIDE references all 3 canonical adopter API calls ($MATCHES line(s))"
    ```

    DECISION (Claude's Discretion): the drift check lives inside the `adopter` job rather than as a new docs-lint job. Rationale: it is conceptually part of "the adopter lane works" — the adopter lane proves the API works; this step proves the guide describes that working API. Adding a new top-level job for one grep would inflate workflow complexity.

    Step 2 — Validate YAML: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0.

    Step 3 — Confirm the `quality`, `integration`, `contract`, and existing `adopter` job structure is preserved (only the new step is appended).

    Step 4 — Confirm the matrix `quality` job (with its 1.15/1.17 entries) is still in place — `grep -A 20 "^  quality:" .github/workflows/ci.yml | grep -c "matrix:"` returns 1. The `needs: quality` declarations on contract and adopter jobs wait for BOTH matrix variants to complete before running (this is the standard GitHub Actions matrix-needs interaction).
  </action>

  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && grep -c "D-16 drift gate" .github/workflows/ci.yml && grep -c "Broker\\.initiate_session" .github/workflows/ci.yml</automated>
  </verify>

  <acceptance_criteria>
    - `grep "D-16 drift gate" .github/workflows/ci.yml` returns at least 1 match
    - `grep -c "Broker\\.initiate_session" .github/workflows/ci.yml` returns at least 1 match (in the grep regex)
    - `grep -c "Broker\\.verify_completion" .github/workflows/ci.yml` returns at least 1 match
    - `grep -c "Rindle\\.Delivery\\.url" .github/workflows/ci.yml` returns at least 1 match
    - `grep -A 20 "^  quality:" .github/workflows/ci.yml | grep -c "matrix:"` returns 1 (matrix preserved — Blocker 4)
    - `grep -c "name: Quality" .github/workflows/ci.yml` returns 1
    - `grep -c "name: Integration" .github/workflows/ci.yml` returns 1
    - `grep -c "name: Contract" .github/workflows/ci.yml` returns 1
    - `grep -c "name: Adopter" .github/workflows/ci.yml` returns 1
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0
  </acceptance_criteria>

  <done>
    The adopter job now includes a final step that greps `guides/getting_started.md` for the three canonical adopter API calls. If any are missing, the adopter lane fails — making snippet drift a CI failure (D-16). All previous CI jobs (quality matrix, integration, contract, adopter) are preserved unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ExDoc HTML output → adopter browser | Mermaid CDN (cdn.jsdelivr.net) injected via `before_closing_head_tag:` — JS executes in adopter's HexDocs view |
| `mix docs` build → repo file system | ExDoc reads `extras:` list and writes `doc/` HTML output; only build-time, no runtime risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | Mermaid CDN compromise injecting malicious JS into HexDocs HTML | accept | Same trust model as any third-party CDN (cdn.jsdelivr.net is the standard ExDoc pattern per RESEARCH lines 425-457). Pinning to a specific version (`mermaid@10.2.3`) limits exposure to that version's behavior. SRI (Subresource Integrity) hash could be added in a future hardening pass; for v1.0 the CDN trust is acceptable per industry norm. |
| T-05-06-02 | Information Disclosure | `@doc` examples accidentally leaking real production secrets / data | mitigate | All examples in this plan use placeholder values (`MyApp.MediaProfile`, `"x.png"`, `user_id`) — no real keys, paths, or PII. Reviewed in Task 2's action body. |
| T-05-06-03 | Denial of Service | `mix docs` build failing due to missing guide files (Plan 07 not yet shipped) | accept | Documented as expected-until-Plan-07. The plan's CI step only greps `guides/getting_started.md` — if missing, the step fails LOUDLY with a clear D-16 message rather than crashing silently. |

Security disposition: LOW. Documentation work; no production attack surface added.
</threat_model>

<verification>
- `mix credo --strict` exits 0 after all `@moduledoc` and `@doc` additions (Pitfall 4 prevention).
- `mix compile --warnings-as-errors` exits 0.
- Every public function in `lib/rindle.ex`, `lib/rindle/upload/broker.ex`, `lib/rindle/delivery.ex` has `@doc` with at least one `iex>` example.
- All 5 domain schema modules have full `@moduledoc` blocks; `lib/rindle/repo.ex` has `@moduledoc false`.
- `mix.exs docs/0` lists 7 guides explicitly + Mermaid renderer + groups_for_extras.
- `mix.exs` preserves `test/adopter` in `elixirc_paths(:test)` (Warning 4) and `files: ~w(...)` in `package/0` (Plan 05 invariant).
- CI adopter lane fails on `guides/getting_started.md` snippet drift (D-16).
- Quality matrix preserved (Blocker 4): `grep -A 20 "^  quality:" .github/workflows/ci.yml | grep -c "matrix:"` returns 1.
</verification>

<success_criteria>
- DOC-08 fully satisfied: 5 domain schema `@moduledoc` + repo.ex `@moduledoc false` + `@doc` on every public function in the three public-API files
- ExDoc wired for guides (extras + groups + Mermaid CDN); `mix docs` builds successfully once Plan 07 lands
- D-16 enforced as a CI gate: snippet drift fails the adopter lane
- All prior-plan invariants preserved: quality matrix, integration job, contract job, adopter job, `test/adopter` elixirc path, `package: [files: ...]` allowlist
</success_criteria>

<output>
After completion, create `.planning/phases/05-ci-1-0-readiness/05-06-SUMMARY.md`. Document:
- Whether Plan 07 had landed when `mix docs` was first run; capture the actual `mix docs` output
- The actual count of public functions per file vs. `@doc` blocks added (should be 1:1)
- Any public function where a doctest was infeasible — list the function and the rationale
- Confirmation that the matrix `quality` job and the integration job are byte-for-byte unchanged from their post-Plan-04 state
</output>

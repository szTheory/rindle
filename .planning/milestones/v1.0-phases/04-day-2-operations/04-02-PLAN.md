---
phase: 04-day-2-operations
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/ops/variant_maintenance.ex
  - lib/mix/tasks/rindle.regenerate_variants.ex
  - lib/mix/tasks/rindle.verify_storage.ex
  - test/rindle/ops/variant_maintenance_test.exs
autonomous: true
requirements:
  - OPS-03
  - OPS-04
  - OPS-05
  - OPS-06
  - OPS-09
must_haves:
  truths:
    - `mix rindle.regenerate_variants` targets only stale or missing variants that match the requested filters.
    - `mix rindle.verify_storage` compares DB records to storage objects and marks missing variants as `missing`.
    - `mix rindle.verify_storage` prints a summary with total checked, missing, present, and errors.
    - Both tasks fail loudly when queries or storage checks fail.
  artifacts:
    - path: lib/rindle/ops/variant_maintenance.ex
      provides: Shared variant maintenance operations
      min_lines: 140
      exports: ["regenerate_variants/1", "verify_storage/1"]
    - path: lib/mix/tasks/rindle.regenerate_variants.ex
      provides: Variant regeneration CLI
      min_lines: 90
      contains: "regenerate_variants"
    - path: lib/mix/tasks/rindle.verify_storage.ex
      provides: Storage reconciliation CLI
      min_lines: 90
      contains: "verify_storage"
    - path: test/rindle/ops/variant_maintenance_test.exs
      provides: Variant regeneration and reconciliation coverage
      min_lines: 140
      contains: "verify_storage"
  key_links:
    - from: lib/mix/tasks/rindle.regenerate_variants.ex
      to: lib/rindle/ops/variant_maintenance.ex
      via: regeneration delegation
      pattern: "VariantMaintenance"
    - from: lib/mix/tasks/rindle.verify_storage.ex
      to: Rindle.Storage
      via: head checks against storage objects
      pattern: "head|verify_storage"
    - from: lib/rindle/ops/variant_maintenance.ex
      to: lib/rindle/domain/media_variant.ex
      via: stale/missing queries and state updates
      pattern: "state|recipe_digest"
---

<objective>
Build the variant maintenance lane so operators can requeue stale work and reconcile missing storage objects without hand-written SQL.

Purpose: Keep variant state observable and recoverable after storage drift or profile changes.
Output: Shared variant maintenance service, two Mix tasks, and reconciliation tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
From lib/rindle/domain/media_variant.ex:
```elixir
field :name, :string
field :state, :string, default: "planned"
field :recipe_digest, :string
field :storage_key, :string
```

From lib/rindle/domain/stale_policy.ex:
```elixir
@spec stale_regeneration_scope(Ecto.Queryable.t()) :: Ecto.Query.t()
def stale_regeneration_scope(query) do
```

From lib/rindle/storage.ex:
```elixir
@callback head(key :: String.t(), opts :: keyword()) :: {:ok, map()} | {:error, term()}
```

From lib/rindle/workers/process_variant.ex:
```elixir
@impl Oban.Worker
def perform(%Oban.Job{args: %{"asset_id" => asset_id, "variant_name" => variant_name}}) do
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement variant regeneration and storage reconciliation</name>
  <files>lib/rindle/ops/variant_maintenance.ex, lib/mix/tasks/rindle.regenerate_variants.ex, lib/mix/tasks/rindle.verify_storage.ex</files>
  <behavior>
    - Regeneration filters by profile and variant name, then enqueues jobs only for stale or missing variants.
    - Storage verification walks variant records, HEAD-checks the object, and flips absent entries to `missing`.
    - The verification command reports a stable summary so operators can see the reconciliation result.
  </behavior>
  <action>Build the shared variant-maintenance module around the queryable state columns and recipe-digest/stale policy already in the codebase. Expose the two Mix tasks as thin entrypoints so operators can target stale or missing variants and reconcile storage drift in one pass.</action>
  <verify><automated>mix test test/rindle/ops/variant_maintenance_test.exs</automated></verify>
  <done>Variant regeneration and storage reconciliation are available as reusable operations with CLI wrappers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove targeting, reconciliation, and exit behavior</name>
  <files>test/rindle/ops/variant_maintenance_test.exs</files>
  <behavior>
    - Regeneration only touches stale/missing variants that match filters.
    - Verification marks missing variants correctly and preserves present ones.
    - Error paths cause non-zero exits so scripted use is safe.
  </behavior>
  <action>Add regression tests that seed variant fixtures, assert the enqueued regeneration scope, verify storage reconciliation counts, and confirm missing variants are updated to `missing`. Cover the failure path so the tasks stay CI-friendly.</action>
  <verify><automated>mix test test/rindle/ops/variant_maintenance_test.exs</automated></verify>
  <done>Variant maintenance behavior is locked by automated tests and matches the phase contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell / operator input → Mix task | Profile and variant filters determine the scope of regeneration and verification |
| Mix task → storage backend | HEAD checks read external storage state and may mutate DB state based on it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | T | `lib/mix/tasks/rindle.regenerate_variants.ex` | mitigate | Restrict regeneration to stale/missing variants and filter inputs explicitly before enqueueing jobs. |
| T-04-05 | I | `lib/mix/tasks/rindle.verify_storage.ex` | mitigate | Emit summaries only and update missing states deterministically from HEAD results. |
| T-04-06 | D | `lib/rindle/ops/variant_maintenance.ex` | accept | Large catalogs may take time; keep the command streaming and return structured counts instead of buffering everything in memory. |
</threat_model>

<verification>
- `mix test test/rindle/ops/variant_maintenance_test.exs` passes.
- Regeneration scope respects profile/variant filters and stale/missing state.
- Storage reconciliation updates missing variants and reports summary counts.
</verification>

<success_criteria>
- Operators can requeue stale or missing variants without manual SQL.
- Storage reconciliation finds and marks missing objects.
- Summary output is deterministic and script-friendly.
- Failures are visible via non-zero exits.
</success_criteria>

<output>
After completion, create `.planning/phases/04-day-2-operations/04-02-SUMMARY.md`
</output>

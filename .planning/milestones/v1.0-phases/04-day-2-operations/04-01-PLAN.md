---
phase: 04-day-2-operations
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle/ops/upload_maintenance.ex
  - lib/mix/tasks/rindle.cleanup_orphans.ex
  - lib/mix/tasks/rindle.abort_incomplete_uploads.ex
  - test/rindle/ops/upload_maintenance_test.exs
autonomous: true
requirements:
  - OPS-01
  - OPS-02
  - OPS-07
  - OPS-09
must_haves:
  truths:
    - `mix rindle.cleanup_orphans` deletes expired upload sessions and their staged objects on a live run.
    - `mix rindle.cleanup_orphans --dry-run` reports the same records without deleting them.
    - `mix rindle.abort_incomplete_uploads` transitions timed-out `signed` and `uploading` sessions to `expired`.
    - Both tasks exit non-zero when a query, storage, or transition step fails.
  artifacts:
    - path: lib/rindle/ops/upload_maintenance.ex
      provides: Shared upload-session cleanup operations
      min_lines: 120
      exports: ["cleanup_orphans/1", "abort_incomplete_uploads/1"]
    - path: lib/mix/tasks/rindle.cleanup_orphans.ex
      provides: Cleanup orphaned upload sessions CLI
      min_lines: 80
      contains: "cleanup_orphans"
    - path: lib/mix/tasks/rindle.abort_incomplete_uploads.ex
      provides: Expired upload-session abort CLI
      min_lines: 80
      contains: "abort_incomplete_uploads"
    - path: test/rindle/ops/upload_maintenance_test.exs
      provides: Upload cleanup and abort task coverage
      min_lines: 120
      contains: "cleanup_orphans"
  key_links:
    - from: lib/mix/tasks/rindle.cleanup_orphans.ex
      to: lib/rindle/ops/upload_maintenance.ex
      via: maintenance delegation
      pattern: "UploadMaintenance"
    - from: lib/mix/tasks/rindle.abort_incomplete_uploads.ex
      to: lib/rindle/domain/media_upload_session.ex
      via: TTL/state query and expiry transition
      pattern: "expires_at|expired|uploading|signed"
    - from: lib/rindle/ops/upload_maintenance.ex
      to: Rindle.Storage
      via: staged-object deletion after DB cleanup
      pattern: "delete|head|Storage"
---

<objective>
Build the upload-session maintenance lane so operators can expire incomplete uploads and remove orphaned staged objects without manual SQL.

Purpose: Prevent storage leaks while keeping the cleanup path scriptable and safe.
Output: Shared upload maintenance service, two Mix tasks, and regression tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
From lib/rindle/domain/media_upload_session.ex:
```elixir
field :state, :string, default: "initialized"
field :upload_key, :string
field :expires_at, :utc_datetime_usec
field :verified_at, :utc_datetime_usec
```

From lib/rindle/domain/upload_session_fsm.ex:
```elixir
@spec transition(state(), state(), map()) :: :ok | transition_error()
def transition(current_state, target_state, context \\ %{}) do
```

From lib/rindle/storage.ex:
```elixir
@callback delete(key :: String.t(), opts :: keyword()) :: {:ok, term()} | {:error, term()}
@callback head(key :: String.t(), opts :: keyword()) :: {:ok, map()} | {:error, term()}
```

From lib/rindle/config.ex:
```elixir
@spec upload_session_ttl_seconds() :: pos_integer()
def upload_session_ttl_seconds do
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement upload-session cleanup operations and CLI wrappers</name>
  <files>lib/rindle/ops/upload_maintenance.ex, lib/mix/tasks/rindle.cleanup_orphans.ex, lib/mix/tasks/rindle.abort_incomplete_uploads.ex</files>
  <behavior>
    - Cleanup targets only expired upload sessions and their staged objects; dry-run prints/logs the planned actions.
    - Abort task transitions incomplete sessions past their TTL from `signed`/`uploading` to `expired` before cleanup can remove them.
    - Storage side effects stay outside DB transactions and all failures surface as tagged errors.
  </behavior>
  <action>Create a shared upload-maintenance module that performs the TTL query, state transition, and staged-object deletion logic. Then build the two Mix task entrypoints on top of it so both dry-run and live-run behavior are scriptable from the shell.</action>
  <verify><automated>mix test test/rindle/ops/upload_maintenance_test.exs</automated></verify>
  <done>Cleanup and abort maintenance flows exist as reusable operations with CLI wrappers and explicit failure signaling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock upload maintenance behavior in tests</name>
  <files>test/rindle/ops/upload_maintenance_test.exs</files>
  <behavior>
    - Dry-run mode reports affected sessions/objects without deleting or mutating DB rows.
    - Live cleanup deletes expired sessions and staged objects, and abort updates timed-out sessions to `expired`.
    - Failure cases return non-zero exit behavior so CI and cron scripts can detect problems.
  </behavior>
  <action>Add regression tests that exercise the shared upload-maintenance service and both Mix commands under real Ecto sandbox conditions. Verify the visible counts, state transitions, and storage deletion side effects rather than relying on internal implementation details.</action>
  <verify><automated>mix test test/rindle/ops/upload_maintenance_test.exs</automated></verify>
  <done>Upload-session maintenance behavior is proven by automated tests and the CLI contract is fixed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell / CI cron → Mix task | Untrusted operator input selects cleanup scope and dry-run mode |
| Mix task → database/storage | Cleanup deletes DB rows and staged objects across the storage boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | `lib/mix/tasks/rindle.cleanup_orphans.ex` | mitigate | Keep dry-run separate from destructive execution and verify TTL/state before deleting anything. |
| T-04-02 | D | `lib/rindle/ops/upload_maintenance.ex` | mitigate | Batch work and return tagged errors so a single broken session does not silently short-circuit the entire cleanup lane. |
| T-04-03 | I | `lib/rindle/ops/upload_maintenance.ex` | mitigate | Report counts and session IDs only; do not leak storage secrets or private asset contents. |
</threat_model>

<verification>
- `mix test test/rindle/ops/upload_maintenance_test.exs` passes.
- Dry-run and live-run behaviors diverge exactly as required.
- Incomplete uploads are expired before cleanup deletes staged objects.
</verification>

<success_criteria>
- Operators can abort incomplete uploads and clean orphaned sessions from the CLI.
- Dry-run mode is safe and observable.
- Cleanup removes expired sessions and their staged objects without manual SQL.
- Failures are visible through non-zero exits.
</success_criteria>

<output>
After completion, create `.planning/phases/04-day-2-operations/04-01-SUMMARY.md`
</output>

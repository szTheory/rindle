---
phase: 90-console-ops-actions
plan: 02
type: execute
wave: 2
depends_on: ["90-01"]
files_modified:
  - lib/rindle/admin/queries.ex
  - lib/rindle/admin/live/actions_live.ex
  - test/rindle/admin/live/actions_live_test.exs
autonomous: true
requirements:
  - ADMIN-04
must_haves:
  truths:
    - User can execute asset-scoped lifecycle repair using reprobe and requeue.
    - User can trigger broad variant regeneration with an inline receipt showing Oban queue counts.
    - User can view a read-only quarantine triage panel with descriptive routing.
  artifacts:
    - path: lib/rindle/admin/live/actions_live.ex
      provides: LiveView panels for regeneration, repair, and quarantine review.
  key_links:
    - from: lib/rindle/admin/live/actions_live.ex
      to: Rindle.reprobe/1
      via: direct call
    - from: lib/rindle/admin/live/actions_live.ex
      to: Rindle.requeue_variants/2
      via: direct call
    - from: lib/rindle/admin/live/actions_live.ex
      to: Rindle.Ops.VariantMaintenance.regenerate_variants/1
      via: direct call
---

<objective>
Implement non-destructive operations: Variant Regeneration, Lifecycle Repair, and Quarantine Review triage within the Actions hub.

Purpose: Provide admins with robust tools for correcting state drift, recovering stuck processing, and reviewing blocked uploads without relying on operator CLI.
Output: Working repair, regeneration, and quarantine review panels inside the Actions LiveView.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/90-console-ops-actions/90-CONTEXT.md
@.planning/phases/90-console-ops-actions/90-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Lifecycle Repair Workflow</name>
  <files>lib/rindle/admin/queries.ex, lib/rindle/admin/live/actions_live.ex, test/rindle/admin/live/actions_live_test.exs</files>
  <behavior>
    - Test 1: `Queries.actions_directory` marks `:lifecycle_repair` enabled.
    - Test 2: Given an asset ID, calls `Rindle.reprobe/1` and returns a success receipt.
    - Test 3: Calls `Rindle.requeue_variants/2` for variant requeueing and returns a success receipt.
  </behavior>
  <action>Update `Queries.actions_directory/0` to enable `:lifecycle_repair` (D-90-12). Build the Lifecycle Repair panel inside `ActionsLive` (D-90-11). It should take an `asset_id` input and provide buttons/forms to perform `Rindle.reprobe(asset)` or `Rindle.requeue_variants(asset)`. This is a normal confirm/submit flow (no typed destruction phrases needed, per D-90-16, D-90-23). Execution transitions to a `:receipt` state showing what action was taken and whether it succeeded (D-90-15). Use stable data attributes (e.g. `data-rindle-admin-*`) for LiveView test assertions (D-90-22).</action>
  <verify>
    <automated>mix test test/rindle/admin/queries_test.exs && mix test test/rindle/admin/live/actions_live_test.exs</automated>
  </verify>
  <done>Lifecycle Repair panel is functional with reprobe and requeue flows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Variant Regeneration Workflow</name>
  <files>lib/rindle/admin/queries.ex, lib/rindle/admin/live/actions_live.ex, test/rindle/admin/live/actions_live_test.exs</files>
  <behavior>
    - Test 1: `Queries.actions_directory` marks `:variant_regeneration` enabled.
    - Test 2: Accepts optional `profile` and `variant_name` filters.
    - Test 3: Execution calls `Rindle.Ops.VariantMaintenance.regenerate_variants/1` and reports the count of enqueued jobs without waiting for synchronous job completion.
  </behavior>
  <action>Update `Queries.actions_directory/0` to enable `:variant_regeneration`. Implement the panel in `ActionsLive`. Expose filter inputs matching the `Rindle.Ops.VariantMaintenance` contract: `profile` and `variant_name` (D-90-14, D-90-24, D-90-25). Use a deliberate extra confirmation step (like a modal or checkbox) since this triggers broad regeneration, but no exact typed phrase is required. On submit, call `Rindle.Ops.VariantMaintenance.regenerate_variants(%{profile: p, variant_name: v})` directly (do not add a new `Rindle` facade wrapper, D-90-13, D-90-14). Render a `:receipt` state displaying `enqueued`, `skipped`, and `errors` directly from the result map. Microcopy must clarify work continues asynchronously via Oban (D-90-15). Use stable data attributes (e.g. `data-rindle-admin-*`) for LiveView test assertions (D-90-22).</action>
  <verify>
    <automated>mix test test/rindle/admin/queries_test.exs && mix test test/rindle/admin/live/actions_live_test.exs</automated>
  </verify>
  <done>Broad variant regeneration is available with filters and asynchronous job enqueue receipts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Quarantine Review Triage</name>
  <files>lib/rindle/admin/queries.ex, lib/rindle/admin/live/actions_live.ex, test/rindle/admin/live/actions_live_test.exs</files>
  <behavior>
    - Test 1: `Queries.actions_directory` marks `:quarantine_review` enabled.
    - Test 2: Renders a read-only instructional panel.
  </behavior>
  <action>Update `Queries.actions_directory/0` to enable `:quarantine_review`. Implement the `:quarantine_review` panel in `ActionsLive`. This is entirely read-only triage (D-90-17). The panel must state plainly that Rindle Admin does not release assets from quarantine, and they are blocked from delivery (D-90-18). It should contain a link to the Asset List view pre-filtered by `state=quarantined` (or instructions on how to filter) and indicate that removal requires standard owner erasure via the Owner Erasure panel (D-90-19). Use stable data attributes (e.g. `data-rindle-admin-*`) for LiveView test assertions (D-90-22).</action>
  <verify>
    <automated>mix test test/rindle/admin/queries_test.exs && mix test test/rindle/admin/live/actions_live_test.exs</automated>
  </verify>
  <done>Quarantine Review is implemented as a safe triage point without introducing new lifecycle semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI → Ops Facade | Variant regeneration requests could trigger large workloads. Quarantine Review must not accidentally bypass FSM lock states. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-03 | Denial of Service | Variant Regeneration | accept | Admin is an authenticated maintainer boundary. Rate limits/large scale enqueues are accepted as intentional operator work. |
| T-90-04 | Elevation of Privilege | Quarantine Review | mitigate | Adhere strictly to D-90-17. Implement no mutations, button actions, or state transitions in the quarantine review panel. |
</threat_model>

<verification>
mix test test/rindle/admin/live/actions_live_test.exs
</verification>

<success_criteria>
Repair, Regeneration, and Quarantine operations are wired to existing Ops/Facade capabilities with appropriate UI constraints and inline receipts.
</success_criteria>

<output>
Create `.planning/phases/90-console-ops-actions/90-02-SUMMARY.md` when done
</output>

---
phase: 92-e2e-screenshot-driven-polish-loop
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/adoption_demo/e2e/support/admin.js
  - lib/rindle/admin/live/actions_live.ex
  - lib/rindle/admin/live/assets_live.ex
  - lib/rindle/admin/live/upload_sessions_live.ex
  - test/rindle/admin/live/actions_live_test.exs
  - test/rindle/admin/live/home_assets_upload_test.exs
autonomous: true
requirements:
  - E2E-01
must_haves:
  truths:
    - "D-92-01: Admin console Playwright work runs inside the existing adoption_demo harness."
    - "D-92-04: Admin console tests use data-rindle-admin-* selectors instead of data-testid."
    - "Executors have one shared Playwright helper for /admin/rindle navigation, shell assertions, theme toggling, detail links, and secret-redaction checks."
    - "Narrow selector additions exist for Actions forms/buttons/previews and list detail links without changing admin behavior."
  artifacts:
    - path: "examples/adoption_demo/e2e/support/admin.js"
      provides: "Shared admin Playwright helper"
      exports: ["ADMIN_BASE", "adminPath", "visitAdmin", "expectAdminShell", "selectAdminTheme", "firstAdminDetailHref", "expectNoAdminRawSecrets", "expectNoHorizontalScroll"]
    - path: "lib/rindle/admin/live/actions_live.ex"
      provides: "Stable admin action selectors"
      contains: "data-rindle-admin-action"
    - path: "lib/rindle/admin/live/assets_live.ex"
      provides: "Stable asset detail-link selector"
      contains: "data-rindle-admin-detail-link=\"asset\""
    - path: "lib/rindle/admin/live/upload_sessions_live.ex"
      provides: "Stable upload-session detail-link selector"
      contains: "data-rindle-admin-detail-link=\"upload-session\""
  key_links:
    - from: "examples/adoption_demo/e2e/support/admin.js"
      to: "examples/adoption_demo/e2e/support/liveview.js"
      via: "waitForLiveSocket import"
      pattern: "require\\(\"./liveview\"\\)"
    - from: "examples/adoption_demo/e2e/support/admin.js"
      to: "/admin/rindle"
      via: "ADMIN_BASE constant"
      pattern: "ADMIN_BASE.*admin/rindle"
    - from: "examples/adoption_demo/e2e/support/admin.js"
      to: "lib/rindle/admin/live/actions_live.ex"
      via: "data-rindle-admin-* selectors"
      pattern: "data-rindle-admin-action"
---

<objective>
Create the selector and helper foundation for Phase 92 admin-console browser proof.

Purpose: Behavior, destructive-flow, theme, and screenshot specs must not rely on brittle text-only locators or test IDs inside the shipped admin package.
Output: CommonJS admin Playwright helpers and narrowly scoped `data-rindle-admin-*` selector additions with ExUnit coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/92-e2e-screenshot-driven-polish-loop/92-CONTEXT.md
@.planning/phases/92-e2e-screenshot-driven-polish-loop/92-RESEARCH.md
@.planning/phases/92-e2e-screenshot-driven-polish-loop/92-VALIDATION.md
@.planning/phases/92-e2e-screenshot-driven-polish-loop/92-PATTERNS.md
@.planning/phases/92-e2e-screenshot-driven-polish-loop/92-UI-SPEC.md
@AGENTS.md
@guides/ui_principles.md
@examples/adoption_demo/AGENTS.md
@examples/adoption_demo/playwright.config.js
@examples/adoption_demo/e2e/support/liveview.js
@examples/adoption_demo/e2e/support/cohort.js
@priv/static/rindle_admin/rindle-admin.js
@lib/rindle/admin/components.ex
@lib/rindle/admin/live/actions_live.ex
@lib/rindle/admin/live/assets_live.ex
@lib/rindle/admin/live/upload_sessions_live.ex
@test/rindle/admin/live/actions_live_test.exs
@test/rindle/admin/live/home_assets_upload_test.exs
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Make console behavior and polish deterministic through merge-blocking Playwright and all-screens screenshot iteration | 92-01, 92-02, 92-03, 92-04, 92-05 | COVERED | Helper/selectors, behavior specs, action specs, screenshots, and CI proof together close the goal.
REQ | E2E-01 | Deterministic Playwright specs for happy paths, main error cases, boundaries, theme switching, destructive flows in merge-blocking CI | 92-01, 92-02, 92-03, 92-05 | COVERED | Foundation, browser specs, destructive specs, proof matrix.
REQ | E2E-02 | Automated all-screens light/dark screenshot capture feeding analyze-to-fix polish passes | 92-04, 92-05 | COVERED | Screenshot spec, artifact assertions, polish iteration, matrix/docs.
RESEARCH | - | Use existing examples/adoption_demo Playwright harness | 92-01, 92-02, 92-03, 92-04, 92-05 | COVERED | No standalone app planned.
RESEARCH | - | Use data-rindle-admin-* selectors and add narrowly scoped selectors where required | 92-01 | COVERED | This plan owns selectors.
RESEARCH | - | Add admin-console, admin-actions, admin-theme specs | 92-02, 92-03 | COVERED | Split for parallel execution after selectors.
RESEARCH | - | Add admin-screenshots spec under ignored Playwright/test-results paths | 92-04 | COVERED | Screenshot output path is fixed.
RESEARCH | - | Update proof matrix and drift gate for new spec filenames | 92-05 | COVERED | Final CI/proof plan owns this.
CONTEXT | D-92-01 | Execute Admin Console E2E in adoption_demo Playwright harness | 92-01, 92-02, 92-03, 92-04, 92-05 | COVERED | All plans target examples/adoption_demo.
CONTEXT | D-92-02 | Screenshot polish loop targets live Phoenix LiveView app | 92-04 | COVERED | Static gallery is read only as an analog.
CONTEXT | D-92-03 | Toggle app theme picker programmatically | 92-01, 92-02, 92-04 | COVERED | Helper and specs click data-rindle-admin-theme.
CONTEXT | D-92-04 | Use data-rindle-admin-* selectors instead of data-testid | 92-01, 92-02, 92-03, 92-04 | COVERED | Selector grep gates enforce this for new admin files.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared admin Playwright helper</name>
  <files>examples/adoption_demo/e2e/support/admin.js</files>
  <read_first>
    examples/adoption_demo/e2e/support/admin.js
    examples/adoption_demo/e2e/support/liveview.js
    examples/adoption_demo/e2e/support/cohort.js
    examples/adoption_demo/playwright.config.js
    priv/static/rindle_admin/rindle-admin.js
    .planning/phases/92-e2e-screenshot-driven-polish-loop/92-CONTEXT.md
    .planning/phases/92-e2e-screenshot-driven-polish-loop/92-RESEARCH.md
    .planning/phases/92-e2e-screenshot-driven-polish-loop/92-UI-SPEC.md
    guides/ui_principles.md
  </read_first>
  <behavior>
    - `adminPath()` returns `/admin/rindle`; `adminPath("assets")` returns `/admin/rindle/assets`; leading slashes in suffixes do not produce duplicate slashes.
    - `visitAdmin(page, suffix)` navigates to the admin path and waits for `window.liveSocket.isConnected()`.
    - `expectAdminShell(page, surface)` asserts `[data-rindle-admin-root]`, `[data-rindle-admin-surface="${surface}"]`, nav items, page header, live indicator, theme picker, and current nav item when `surface` is one of the six top-level slugs.
    - `selectAdminTheme(page, theme)` clicks `[data-rindle-admin-theme="${theme}"]`, then asserts `[data-rindle-admin-root]` has `data-theme="${theme}"` and the clicked control has `aria-pressed="true"`.
    - `expectNoAdminRawSecrets(page)` rejects `provider-secret`, `raw-session-secret-token`, `raw-secret-token`, and `https://storage.example` in visible page text.
  </behavior>
  <action>Create `examples/adoption_demo/e2e/support/admin.js` as a CommonJS module. Import `expect` from `@playwright/test` and `waitForLiveSocket` from `./liveview`. Export `ADMIN_BASE = "/admin/rindle"`, `adminPath(suffix = "")`, `visitAdmin(page, suffix = "")`, `adminRoot(page)`, `expectAdminShell(page, surface)`, `selectAdminTheme(page, theme)`, `firstAdminDetailHref(page, detailKind)`, `expectNoAdminRawSecrets(page)`, and `expectNoHorizontalScroll(page)`. `firstAdminDetailHref` must read the first anchor matching `[data-rindle-admin-detail-link="${detailKind}"]` and throw when the href is missing. `expectNoHorizontalScroll` must assert `document.documentElement.scrollWidth <= document.documentElement.clientWidth` and the same check for `[data-rindle-admin-root]`. Do not introduce `data-testid` usage in this file per D-92-04.</action>
  <verify>
    <automated>node -e "const admin = require('./examples/adoption_demo/e2e/support/admin'); if (admin.ADMIN_BASE !== '/admin/rindle') throw new Error('bad base'); if (admin.adminPath('assets') !== '/admin/rindle/assets') throw new Error('bad assets path'); if (admin.adminPath('/actions') !== '/admin/rindle/actions') throw new Error('bad slash path'); for (const key of ['visitAdmin','expectAdminShell','selectAdminTheme','firstAdminDetailHref','expectNoAdminRawSecrets','expectNoHorizontalScroll']) if (typeof admin[key] !== 'function') throw new Error('missing '+key);"</automated>
  </verify>
  <acceptance_criteria>
    Source assertion: `examples/adoption_demo/e2e/support/admin.js` exports `ADMIN_BASE`, `adminPath`, `visitAdmin`, `expectAdminShell`, `selectAdminTheme`, `firstAdminDetailHref`, `expectNoAdminRawSecrets`, and `expectNoHorizontalScroll`.
    Source assertion: `examples/adoption_demo/e2e/support/admin.js` contains `/admin/rindle`, `[data-rindle-admin-root]`, `[data-rindle-admin-theme="`, `[data-rindle-admin-detail-link="`, and `waitForLiveSocket`.
    Source assertion: `rg -n "data-testid" examples/adoption_demo/e2e/support/admin.js` returns no matches.
    Behavior assertion: the Node export/path check exits 0.
  </acceptance_criteria>
  <done>Admin Playwright helper exists, exports the required helpers, and encodes the live app route/theme/selector contract from D-92-01, D-92-03, and D-92-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add stable admin selectors for browser flows</name>
  <files>lib/rindle/admin/live/actions_live.ex, lib/rindle/admin/live/assets_live.ex, lib/rindle/admin/live/upload_sessions_live.ex, test/rindle/admin/live/actions_live_test.exs, test/rindle/admin/live/home_assets_upload_test.exs</files>
  <read_first>
    lib/rindle/admin/live/actions_live.ex
    lib/rindle/admin/live/assets_live.ex
    lib/rindle/admin/live/upload_sessions_live.ex
    lib/rindle/admin/components.ex
    test/rindle/admin/live/actions_live_test.exs
    test/rindle/admin/live/home_assets_upload_test.exs
    examples/adoption_demo/e2e/support/admin.js
    examples/adoption_demo/AGENTS.md
    guides/ui_principles.md
    .planning/phases/92-e2e-screenshot-driven-polish-loop/92-PATTERNS.md
    .planning/phases/92-e2e-screenshot-driven-polish-loop/92-UI-SPEC.md
  </read_first>
  <behavior>
    - Actions tabs expose `data-rindle-admin-action` values `owner_erasure`, `batch_erasure`, `lifecycle_repair`, `variant_regeneration`, and `quarantine_review`.
    - Actions forms expose `data-rindle-admin-form` values `owner_erasure_preview`, `owner_erasure_execute`, `batch_erasure_preview`, `batch_erasure_execute`, `lifecycle_repair`, and `variant_regeneration`.
    - Actions preview containers expose `data-rindle-admin-preview="owner_erasure"` and `data-rindle-admin-preview="batch_erasure"`.
    - Submit controls expose `data-rindle-admin-submit` values matching their LiveView event names.
    - Asset and upload-session list anchors expose `data-rindle-admin-detail-link="asset"` and `data-rindle-admin-detail-link="upload-session"` respectively.
  </behavior>
  <action>Add only semantic `data-rindle-admin-*` attributes; do not change event names, confirmation copy, form field names, router paths, or operation semantics. In `actions_live.ex`, add `data-rindle-admin-action={action.id}` to action tab buttons, add `data-rindle-admin-action-panel={@active_action_id}` to the rendered action panel wrapper, add `data-rindle-admin-form` attributes to each form, add `data-rindle-admin-input` attributes to owner type, owner id, batch owners, asset id, repair action, profile, variant name, and confirm controls, add `data-rindle-admin-submit` attributes to submit buttons, and add preview selectors for owner and batch erasure reports. In `assets_live.ex`, add `data-rindle-admin-detail-link="asset"` to the list "Inspect asset" anchor. In `upload_sessions_live.ex`, add `data-rindle-admin-detail-link="upload-session"` to the list "Review session" anchor and `data-rindle-admin-detail-link="asset"` to the detail "Inspect asset" anchor. Extend the existing ExUnit tests to assert these selectors with `has_element?/2` and source HTML assertions. Do not add `data-testid` anywhere under `lib/rindle/admin`.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/rindle/admin/live/actions_live_test.exs test/rindle/admin/live/home_assets_upload_test.exs</automated>
    <automated>rg -n "data-testid" lib/rindle/admin && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    Source assertion: `lib/rindle/admin/live/actions_live.ex` contains `data-rindle-admin-action`, `data-rindle-admin-action-panel`, `data-rindle-admin-form`, `data-rindle-admin-input`, `data-rindle-admin-submit`, `data-rindle-admin-preview="owner_erasure"`, and `data-rindle-admin-preview="batch_erasure"`.
    Source assertion: `lib/rindle/admin/live/assets_live.ex` contains `data-rindle-admin-detail-link="asset"`.
    Source assertion: `lib/rindle/admin/live/upload_sessions_live.ex` contains both `data-rindle-admin-detail-link="upload-session"` and `data-rindle-admin-detail-link="asset"`.
    Behavior assertion: the focused admin LiveView tests pass.
    Source assertion: `rg -n "data-testid" lib/rindle/admin` returns no matches.
  </acceptance_criteria>
  <done>Admin browser flows have stable selectors and the existing LiveView tests prove those selectors render without altering operation behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser test runner -> Live Phoenix app | Playwright drives untrusted browser actions against `/admin/rindle`. |
| Test assertions -> packaged admin DOM | Selectors added to shipped admin LiveView source become part of the published package surface. |
| Fixture data -> screenshots/test logs | Seeded provider/session values can be exposed through DOM text, screenshots, traces, and CI artifacts. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-92-01 | High | Information Disclosure | `examples/adoption_demo/e2e/support/admin.js` | mitigate | Task 1 adds `expectNoAdminRawSecrets(page)` rejecting `provider-secret`, `raw-session-secret-token`, `raw-secret-token`, and `https://storage.example`. |
| T-92-02 | Medium | Tampering | `lib/rindle/admin/live/actions_live.ex` selector additions | mitigate | Task 2 adds data attributes only and verifies existing action tests still pass, preserving event names and confirmation semantics. |
| T-92-03 | Medium | Spoofing | Playwright selectors | mitigate | Task 2 uses namespaced `data-rindle-admin-*` selectors and forbids `data-testid` under `lib/rindle/admin`, preventing generic test identifiers from leaking into the packaged admin package. |
| T-92-04 | Low | Denial of Service | Browser helper waits | mitigate | Task 1 uses `waitForLiveSocket` and Playwright expectations instead of sleeps, keeping tests deterministic and bounded by configured timeouts. |
| T-92-SC | Low | Tampering | npm installs | accept | This plan adds no npm package-manager install task and uses existing `@playwright/test`; no Package Legitimacy Audit gate is triggered. |
</threat_model>

<verification>
Run both task verification commands, then confirm `examples/adoption_demo/e2e/support/admin.js` can be required from repo root and `lib/rindle/admin` contains no `data-testid` strings.
</verification>

<success_criteria>
Plan succeeds when the admin helper and selector contract are present, focused LiveView tests pass, no `data-testid` is introduced into admin package source, and downstream plans can use `data-rindle-admin-*` selectors without text-only fallbacks.
</success_criteria>

<output>
Create `.planning/phases/92-e2e-screenshot-driven-polish-loop/92-01-SUMMARY.md` when done.
</output>

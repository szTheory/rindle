---
phase: 93-truth-docs-milestone-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/rindle.ex
  - guides/operations.md
  - guides/troubleshooting.md
  - guides/user_flows.md
autonomous: true
requirements: [TRUTH-07]

must_haves:
  truths:
    - "The Rindle facade @moduledoc no longer lists 'admin UI' among things the facade does not promise"
    - "The facade @moduledoc truthfully points to the mountable console (Rindle.Admin.Router.rindle_admin/2)"
    - "operations.md and troubleshooting.md no longer claim Rindle 'intentionally has no dashboard'"
    - "user_flows.md no longer lists 'admin UI' as deferred or out of scope"
    - "Force-delete and scheduler/cron erasure remain explicitly deferred/out-of-scope on every edited surface"
  artifacts:
    - path: "lib/rindle.ex"
      provides: "Truthful facade @moduledoc (no admin-UI denial)"
      contains: "rindle_admin"
    - path: "guides/operations.md"
      provides: "Operator guide with console mention, no 'no dashboard' claim"
    - path: "guides/troubleshooting.md"
      provides: "Troubleshooting guide with no 'no dashboard' claim"
    - path: "guides/user_flows.md"
      provides: "Job map with admin UI removed from deferred/out-of-scope lists"
  key_links:
    - from: "lib/rindle.ex @moduledoc"
      to: "Rindle.Admin.Router.rindle_admin/2"
      via: "doc pointer to the mountable console + guides/admin_console.md"
      pattern: "rindle_admin"
---

<objective>
Flip the four code/guide surfaces that falsely deny the shipped admin console. This plan
edits the `lib/rindle.ex` facade `@moduledoc` (F1), `guides/operations.md` (F2),
`guides/troubleshooting.md` (F3), and `guides/user_flows.md` (F4, F5) so each tells the
truth: the mountable console (`Rindle.Admin.Router.rindle_admin/2`) now exists, while
force-delete and scheduler/cron erasure stay deferred.

Purpose: Closes part of TRUTH-07 — the HexDocs landing moduledoc and the rendered operator
guides currently teach adopters that Rindle has no admin UI. That is false as of v1.18.
Output: Four edited surfaces with the false "admin UI" / "no dashboard" claims removed and
truthful console pointers added. The parity-test lock for these edits lands in Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/93-truth-docs-milestone-audit/93-RESEARCH.md
@.planning/phases/93-truth-docs-milestone-audit/93-VALIDATION.md

<interfaces>
<!-- Console ground truth the docs MUST describe accurately (do not over-claim). -->
<!-- Verified from lib/rindle/admin/router.ex this phase's research. -->

Public entry point (ONLY new public surface this milestone):
  Rindle.Admin.Router.rindle_admin/2  (defmacro rindle_admin(path, opts \\ []))

Mount/auth model: host owns auth pipeline + LiveView :on_mount; Rindle owns route
expansion + static asset serving. Production refuses unsafe mounts (requires non-empty
:on_mount OR auth_guarded?: true). allow_unauthenticated?: true is dev/test-only.

Admin read queries live in Rindle.Admin.Queries (INTERNAL — do NOT promote to the public
Rindle facade). Console actions reuse existing facade verbs — no new lifecycle semantics.

Adopter guide: guides/admin_console.md (created in Plan 03).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the facade @moduledoc admin-UI denial (F1)</name>
  <read_first>
    - lib/rindle.ex (the @moduledoc, lines ~18–53; target sentence at line ~46–48)
    - lib/rindle/admin/router.ex (source-of-truth for the rindle_admin/2 macro + mount/auth model)
    - test/install_smoke/docs_parity_test.exs (asserts on compiled facade docs via Code.fetch_docs/1 — DO NOT break existing facade assertions; this plan does not edit the test, Plan 04 does)
  </read_first>
  <action>
    In `lib/rindle.ex` `@moduledoc`, edit the sentence "This facade contract does not promise
    inline storage deletion, admin UI, scheduler/cron erasure jobs, or force-delete behavior
    for assets with surviving attachments." Remove ONLY "admin UI" from that negative list —
    keep "inline storage deletion", "scheduler/cron erasure jobs", and "force-delete behavior
    for assets with surviving attachments" intact (those remain unsupported by the facade).
    Then add one or two sentences stating the truth: Rindle now ships a mountable admin console,
    mounted via `Rindle.Admin.Router.rindle_admin/2` (host-authenticated, separate from this
    facade), documented in `guides/admin_console.md`; admin read composition lives in
    `Rindle.Admin.Queries`, not on this facade. Do NOT promote `Rindle.Admin.Queries` functions
    onto the `Rindle` facade. Keep the existing alias list and surrounding owner-erasure prose
    unchanged. Do not introduce fenced code blocks into the moduledoc edit.
  </action>
  <acceptance_criteria>
    - `MIX_ENV=test mix run -e 'IO.puts(elem(Code.fetch_docs(Rindle), 4) |> Map.get("en"))' | grep -i "admin ui"` returns NO match (no admin-UI denial in compiled doc).
    - `grep -q "rindle_admin" lib/rindle.ex` succeeds (truthful console pointer present).
    - `grep -q "force-delete" lib/rindle.ex` still succeeds (deferral preserved).
    - `grep -q "scheduler/cron erasure" lib/rindle.ex` still succeeds.
    - File still compiles: `mix compile --warnings-as-errors` passes.
  </acceptance_criteria>
  <verify>
    <automated>mix compile --warnings-as-errors && grep -q "rindle_admin" lib/rindle.ex && (MIX_ENV=test mix run -e 'IO.puts(elem(Code.fetch_docs(Rindle), 4) |> Map.get("en"))' | grep -iq "admin ui" && echo FAIL || echo OK)</automated>
  </verify>
  <done>Facade moduledoc compiles, no longer denies admin UI, points to rindle_admin/2 + guides/admin_console.md, and still lists force-delete + cron erasure as unsupported.</done>
</task>

<task type="auto">
  <name>Task 2: Reverse the "no dashboard" claim in operations.md and troubleshooting.md (F2, F3)</name>
  <read_first>
    - guides/operations.md (target line ~43: "The current contract intentionally has no dashboard and no auto-remediation.")
    - guides/troubleshooting.md (target line ~21: "The contract intentionally has no dashboard and no auto-remediation layer in this release.")
    - lib/rindle/admin/router.ex (source-of-truth: the console executes existing operator verbs, it does not auto-remediate)
    - test/install_smoke/docs_parity_test.exs (lines ~324–340 currently assert `operations =~ "no dashboard"` — this assertion WILL be updated in Plan 04; do not delete the auto-remediation truth that test also checks)
  </read_first>
  <action>
    In `guides/operations.md`, replace the "intentionally has no dashboard" sentence with truth:
    Rindle now ships a mountable admin console (link `guides/admin_console.md` /
    `admin_console.html`) for read + operational surfaces; the contract still has no
    auto-remediation — the console executes existing operator verbs, it does not act on its own.
    Keep the "no auto-remediation" idea (the parity test asserts `operations =~ "no auto-remediation"`).
    Apply the SAME correction to `guides/troubleshooting.md` line ~21. Do not touch the
    doctor-vs-runtime_status split prose or the nine-mix-tasks content. Phrase both edits so the
    word "dashboard"/"console" now affirms existence, not absence.
  </action>
  <acceptance_criteria>
    - `refute File.read!("guides/operations.md") =~ "intentionally has no dashboard"` (no match).
    - `refute File.read!("guides/troubleshooting.md") =~ "intentionally has no dashboard"` (no match).
    - `grep -q "no auto-remediation" guides/operations.md` still succeeds (auto-remediation absence preserved).
    - `grep -qi "admin console\|admin_console" guides/operations.md` succeeds (console now mentioned).
  </acceptance_criteria>
  <verify>
    <automated>(grep -q "intentionally has no dashboard" guides/operations.md guides/troubleshooting.md && echo FAIL || echo OK) && grep -q "no auto-remediation" guides/operations.md && grep -qi "admin_console\|admin console" guides/operations.md</automated>
  </verify>
  <done>Neither operations.md nor troubleshooting.md claims "intentionally has no dashboard"; both mention the console; both retain the "no auto-remediation" truth.</done>
</task>

<task type="auto">
  <name>Task 3: Remove admin UI from user_flows.md deferred + out-of-scope lists (F4, F5)</name>
  <read_first>
    - guides/user_flows.md (target line ~301: "Admin UI, force-delete policy for still-shared assets, and scheduler/cron erasure jobs remain deferred."; target line ~354: "broad PDF/Office handling, an admin UI, or a CDN replacement.")
    - lib/rindle/admin/router.ex (console ground truth)
    - test/install_smoke/docs_parity_test.exs (line ~360 currently asserts user_flows contains "admin ui" normalized — this assertion is UPDATED in Plan 04; force-delete + cron erasure must stay)
  </read_first>
  <action>
    In `guides/user_flows.md` line ~301, remove "Admin UI, " from the deferred list so it reads
    that force-delete policy for still-shared assets and scheduler/cron erasure jobs remain
    deferred (force-delete + cron erasure stay). Optionally add a short sentence that the
    lifecycle can now be managed in a mountable admin console (point to admin_console.html). In
    line ~354, remove "an admin UI, " from the deliberately-out-of-scope list — the other
    non-goals (full HLS/DASH platform, DRM, AI/GPU processing, broad PDF/Office handling, CDN
    replacement) stand. Do not alter the owner-erasure facade truth elsewhere in the file (the
    parity test freezes preview_owner_erasure/2, erase_owner/2, batch terms, force-delete).
  </action>
  <acceptance_criteria>
    - `refute File.read!("guides/user_flows.md") =~ "an admin UI"` (out-of-scope hit removed).
    - `refute File.read!("guides/user_flows.md") =~ "Admin UI, force-delete"` (deferred hit removed).
    - `grep -q "force-delete" guides/user_flows.md` still succeeds.
    - `grep -qi "scheduler/cron erasure\|cron erasure" guides/user_flows.md` still succeeds.
  </acceptance_criteria>
  <verify>
    <automated>(grep -q "an admin UI" guides/user_flows.md && echo FAIL || echo OK) && (grep -q "Admin UI, force-delete" guides/user_flows.md && echo FAIL || echo OK) && grep -q "force-delete" guides/user_flows.md</automated>
  </verify>
  <done>user_flows.md no longer lists an admin UI as deferred or out-of-scope; force-delete and cron erasure deferrals remain intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HexDocs reader → facade moduledoc | Adopters form deployment expectations from rendered docs. |

## STRIDE Threat Register

This plan edits documentation only — no new routes, no auth, no user input handling. No new
attack surface is introduced.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01 | Information disclosure | facade @moduledoc / operator guides | mitigate | Doc edits MUST accurately state the host-auth requirement and the production-refusal rule (via the guides link); MUST NOT imply the console auto-disables or is safe-by-default unauthenticated, so adopters do not mis-deploy. Wording affirms "host owns auth". |
| T-93-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (Markdown + @moduledoc + ExUnit only). No legitimacy gate required. |
</threat_model>

<verification>
- Repo-wide false-phrase scan returns OK:
  `grep -rni -E "an admin UI|intentionally has no dashboard" lib/rindle.ex guides/operations.md guides/troubleshooting.md guides/user_flows.md | grep -v admin_console || echo "OK: no false claims"`
- `mix compile --warnings-as-errors` passes (facade recompiles).
</verification>

<success_criteria>
F1, F2, F3, F4, F5 corrected: the facade moduledoc and the three guides no longer deny the
admin console; force-delete and cron erasure deferrals preserved everywhere.
</success_criteria>

<output>
Create `.planning/phases/93-truth-docs-milestone-audit/93-01-SUMMARY.md` when done.
</output>

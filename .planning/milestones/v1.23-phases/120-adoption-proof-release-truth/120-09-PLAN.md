---
phase: 120-adoption-proof-release-truth
plan: 09
type: execute
wave: 5
depends_on: [120-07, 120-08]
files_modified: []
autonomous: false
gap_closure: true
requirements: [PROOF-01, PROOF-02, DOCS-01]
nyquist_compliant: true
must_haves:
  truths:
    - "D-120-01: clean packed explicit-public and populated-upgrade commands complete successfully against built artifacts."
    - "D-120-03/D-120-04: the Cohort Compose pathway builds, migrates, seeds, boots, serves, and proves Rindle=rindle with Oban/ledger=public."
    - "The exact immutable release-candidate SHA has green external workflow evidence for Proof, package consumers, Adoption Demo Unit, Cohort Demo Smoke, and the release gates required by guides/release_publish.md."
  artifacts:
    - path: ".planning/phases/120-adoption-proof-release-truth/120-09-SUMMARY.md"
      provides: "Command receipts and exact-SHA evidence references"
  key_links:
    - from: "packed compatibility/upgrade commands"
      to: "test/install_smoke/generated_app_smoke_test.exs"
      via: "focused image-profile tags against generated consumers"
      pattern: "phase_120_public_compat|phase_120_isolation_upgrade"
    - from: "exact release-candidate SHA"
      to: "CI and Release workflows"
      via: "immutable GitHub Actions run URLs and green job/step conclusions"
      pattern: "Package Consumer Proof Matrix|Cohort Demo Smoke"
---

<objective>
Turn the repaired proof and documentation contracts into clean packed, Cohort, and immutable exact-SHA evidence (D-120-01, D-120-03, D-120-04).

Purpose: Close the environment-backed PROOF-01/PROOF-02 gaps without treating checkout-only tests as release authority.
Output: A summary containing exact command receipts plus a human-confirmed immutable external evidence chain.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-adoption-proof-release-truth/120-VERIFICATION.md
@.planning/phases/120-adoption-proof-release-truth/120-07-SUMMARY.md
@.planning/phases/120-adoption-proof-release-truth/120-08-SUMMARY.md
@guides/release_publish.md
@RUNNING.md
@test/install_smoke/generated_app_smoke_test.exs
@scripts/ci/cohort_demo_smoke.sh
@.github/workflows/ci.yml
@.github/workflows/release.yml
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Run the repaired packed compatibility, packed upgrade, and Cohort pathways</name>
  <read_first>
    .planning/phases/120-adoption-proof-release-truth/120-VERIFICATION.md
    .planning/phases/120-adoption-proof-release-truth/120-07-SUMMARY.md
    .planning/phases/120-adoption-proof-release-truth/120-08-SUMMARY.md
    test/install_smoke/generated_app_smoke_test.exs
    scripts/ci/cohort_demo_smoke.sh
    RUNNING.md
  </read_first>
  <files>None; verification-only task, with receipts recorded in 120-09-SUMMARY.md</files>
  <precondition>Docker is reachable, PostgreSQL/MinIO prerequisites used by the existing smoke harness are available, and Docker has capacity to allocate a Compose network.</precondition>
  <action>From a clean checkout of the candidate commit, run the focused docs/release parity gate, then the packed public-compatibility command, then the packed isolation-upgrade command, then the Cohort Compose smoke exactly as listed in verify. Do not combine the two generated-app tags into one BEAM instance: their public/default compile-time roots must remain separate. Preserve complete exit codes and the final schema/integrity summary lines in `120-09-SUMMARY.md`, including marker `[1]`, named FK/index evidence, exact Oban snapshot equality, and Cohort relation/ledger counts. If Docker reports exhausted address pools or a generated dependency fetch stalls, stop with the raw diagnostic; do not mark the gate passed or weaken it.</action>
  <verify>
    <automated>mix test test/install_smoke/docs_parity_test.exs test/install_smoke/release_docs_parity_test.exs --seed 0 &amp;&amp; RINDLE_INSTALL_SMOKE_PROFILE=image mix test test/install_smoke/generated_app_smoke_test.exs --include minio --only phase_120_public_compat --seed 0 &amp;&amp; RINDLE_INSTALL_SMOKE_PROFILE=image mix test test/install_smoke/generated_app_smoke_test.exs --include minio --only phase_120_isolation_upgrade --seed 0 &amp;&amp; bash scripts/ci/cohort_demo_smoke.sh</automated>
  </verify>
  <acceptance_criteria>
    - Both packed commands exit 0 from separate generated app roots and built package sources.
    - The upgrade receipt includes exact marker/FK/index proof and exact before/after Oban snapshot equality.
    - Cohort builds, migrates, seeds, serves its existing HTTP checks, proves seven Rindle relations in rindle only, and proves public Oban plus both host ledger entries.
    - No interrupted, detached, stalled, or environment-blocked command is recorded as success.
  </acceptance_criteria>
  <done>All three previously incomplete local integration pathways have complete passing receipts for the candidate commit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm immutable exact-SHA release evidence</name>
  <read_first>
    guides/release_publish.md
    RUNNING.md
    .github/workflows/ci.yml
    .github/workflows/release.yml
    .planning/phases/120-adoption-proof-release-truth/120-09-SUMMARY.md
  </read_first>
  <files>None; external verification evidence is referenced from 120-09-SUMMARY.md</files>
  <action>The executor first resolves and records the 40-character release-candidate SHA, then uses available GitHub CLI/API access to inspect runs for that exact SHA. Record immutable run URLs, run IDs, conclusions, and the relevant job/step names. Pause only because publishing/pushing the candidate and observing GitHub-hosted results are external state outside this plan; do not accept a branch-head run, a different commit, or local-only output.</action>
  <verify>
    <automated>SHA=$(git rev-parse HEAD) &amp;&amp; test ${#SHA} -eq 40 &amp;&amp; gh run list --commit "$SHA" --workflow ci.yml --json databaseId,headSha,conclusion,url --limit 10</automated>
    <human-check>For the recorded 40-character SHA, confirm a green `ci.yml` run whose live jobs include `Proof`, lean `Package Consumer Proof Matrix + Release Preflight`, and `Adoption Demo Unit`; on the push-to-main candidate also confirm green `Package Consumer Full Matrix + Release Preflight` and `Cohort Demo Smoke`. Confirm the `Release` workflow for that same immutable SHA reaches `Run release preflight`, `Verify version alignment`, `Dry run Hex publish`, and `Verify public Hex.pm artifact` successfully when release authorization is being granted. Attach or record direct run URLs and job/step conclusions; a run on any other SHA is insufficient.</human-check>
  </verify>
  <acceptance_criteria>
    - Evidence names one exact 40-character SHA and every referenced run reports that same `headSha`.
    - All required CI jobs and Release steps have successful conclusions, with direct immutable URLs recorded.
    - Local Task 1 receipts are present as diagnostic/package-path evidence but are not represented as release authorization.
  </acceptance_criteria>
  <what-built>Repaired marker/FK/index/Oban proof, both-direction public docs parity, and clean local packed/Cohort receipts for the candidate.</what-built>
  <how-to-verify>Review the recorded exact-SHA CI and Release URLs and confirm every named job/step above is green on the identical SHA.</how-to-verify>
  <resume-signal>Type "approved" with the exact SHA and run URLs, or describe the missing/failed external evidence.</resume-signal>
  <done>The immutable external evidence chain authorizes the 0.4.0 schema-contract claim for the exact candidate SHA.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| Source | ID | Feature/constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Packaged installation through upgrade, demo operation, docs, and release evidence | 120-09 Tasks 1-2 | COVERED |
| REQ | PROOF-01 | Packed public/upgrade/runtime/Oban integration | 120-09 Task 1 | COVERED |
| REQ | PROOF-02 | Packed generated app and Cohort end to end | 120-09 Tasks 1-2 | COVERED |
| REQ | DOCS-01 | Docs/release parity plus exact-SHA Proof result | 120-09 Tasks 1-2 | COVERED |
| RESEARCH | — | Focused existing package/demo/release lanes | 120-09 Tasks 1-2 | COVERED |
| CONTEXT | D-120-01 | Packed artifact and exact-SHA authority | 120-09 Tasks 1-2 | COVERED |
| CONTEXT | D-120-02 | Separate explicit-public consumer | 120-09 Task 1 | COVERED |
| CONTEXT | D-120-03 | Reuse existing harnesses and lanes | 120-09 Tasks 1-2 | COVERED |
| CONTEXT | D-120-04 | Cross-surface ownership contract | 120-09 Tasks 1-2 | COVERED |
| CONTEXT | D-120-05 | Operational and release honesty | 120-09 Task 2 | COVERED |
| CONTEXT | D-120-06 | No routing/migration/host-ownership redesign | Verification only | COVERED |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Checkout to packed generated consumers | Passing repository tests cannot substitute for built-artifact behavior. |
| Local executor to GitHub Actions | Release authorization depends on immutable external run identity and conclusions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-120-G09-01 | Repudiation | integration receipts | high | mitigate | Record exact commands, exit codes, candidate SHA, and non-detached completion evidence. |
| T-120-G09-02 | Tampering | exact-SHA signoff | high | mitigate | Match every external run `headSha` to one recorded 40-character candidate SHA and preserve direct URLs. |
| T-120-G09-03 | Denial of Service | Docker/package setup | medium | mitigate | Fail closed with raw diagnostics for stalled installs or exhausted Docker networks; never convert interruption to pass. |
| T-120-SC | Tampering | package installs | high | mitigate | Tests consume the existing audited packed dependency graph; no new package is introduced. |
</threat_model>

<verification>
Local executor gate:

1. `mix test test/install_smoke/docs_parity_test.exs test/install_smoke/release_docs_parity_test.exs --seed 0`
2. `RINDLE_INSTALL_SMOKE_PROFILE=image mix test test/install_smoke/generated_app_smoke_test.exs --include minio --only phase_120_public_compat --seed 0`
3. `RINDLE_INSTALL_SMOKE_PROFILE=image mix test test/install_smoke/generated_app_smoke_test.exs --include minio --only phase_120_isolation_upgrade --seed 0`
4. `bash scripts/ci/cohort_demo_smoke.sh`

External blocking evidence: direct GitHub Actions URLs showing the exact identical candidate SHA and successful conclusions for every job/step named in Task 2.
</verification>

<success_criteria>
- The two packed generated-app pathways and Cohort pathway complete cleanly with full receipts.
- Exact-SHA CI and Release evidence is recorded and matches the candidate commit byte-for-byte.
- Checkout diagnostics are never substituted for release authorization.
</success_criteria>

<output>Create `.planning/phases/120-adoption-proof-release-truth/120-09-SUMMARY.md` when done.</output>

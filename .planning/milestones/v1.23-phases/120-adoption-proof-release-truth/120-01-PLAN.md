---
phase: 120-adoption-proof-release-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/install_smoke/support/generated_app_helper.ex
  - test/install_smoke/generated_app_smoke_test.exs
autonomous: true
requirements: [PROOF-01, PROOF-02]
nyquist_compliant: true
must_haves:
  truths:
    - "D-120-01: the unpacked Hex-equivalent artifact is installed into a generated Phoenix host, migrated by host-owned files, booted, and exercised through a real Rindle persistence path."
    - "D-120-01/D-120-04: default Rindle relations exist only in rindle while public.oban_jobs and public.schema_migrations remain host-owned and usable."
    - "D-120-03: the existing GeneratedAppHelper/report convention remains the sole package-consumer harness."
    - "Fast source/report contract tests fail within the normal unit-test feedback window, while the packed image scenario remains the authoritative integration gate."
  artifacts:
    - path: "test/install_smoke/support/generated_app_helper.ex"
      provides: "Schema-qualified packed default-install report and persistence lifecycle"
      contains: "prove_package_install!"
    - path: "test/install_smoke/generated_app_smoke_test.exs"
      provides: "Repository-side assertions over package source, ownership, routing, boot, and persistence"
  key_links:
    - from: "test/install_smoke/generated_app_smoke_test.exs"
      to: "test/install_smoke/support/generated_app_helper.ex"
      via: "one setup_all report from prove_package_install!(:image)"
      pattern: "prove_package_install!"
    - from: "generated host migrations"
      to: "packed Rindle.Migration"
      via: "separate Oban.Migration and Rindle.Migration.up(version: 1) host files"
      pattern: "Rindle.Migration.up\(version: 1\)"
---

<objective>
Prove one production-quality fresh default install from the packed artifact through host migrations, boot, schema-qualified ownership checks, and real persistence (D-120-01, D-120-03).

Purpose: Make the release authority catch checkout-only behavior and public-schema false positives.
Output: An enriched generated-app report and image-profile assertions for the default `rindle` contract.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/120-adoption-proof-release-truth/120-CONTEXT.md
@.planning/phases/120-adoption-proof-release-truth/120-RESEARCH.md
@.planning/phases/120-adoption-proof-release-truth/120-PATTERNS.md
@.planning/phases/118-isolated-migration-safe-upgrade/118-VERIFICATION.md
@test/install_smoke/support/generated_app_helper.ex
@test/install_smoke/generated_app_smoke_test.exs
@lib/rindle/migration/v1.ex

<interfaces>
- `GeneratedAppHelper.prove_package_install!/1` builds/unpacks the package, generates a Phoenix app, applies generated host migrations, boots it, and returns a cleanup-aware report map.
- `Rindle.Migration.V1.owned_relations/0` is the only authoritative Rindle relation list; reports must not duplicate it.
- Generated fixtures observe JSON-safe facts; repository ExUnit modules assert policy.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one packed default installation end to end</name>
  <reversibility rating="costly">D-120-01 makes packed-artifact proof release-critical; weakening it later can admit checkout-only regressions.</reversibility>
  <read_first>
    .planning/phases/120-adoption-proof-release-truth/120-CONTEXT.md
    .planning/phases/120-adoption-proof-release-truth/120-PATTERNS.md
    test/install_smoke/support/generated_app_helper.ex
    test/install_smoke/generated_app_smoke_test.exs
    lib/rindle/migration/v1.ex
  </read_first>
  <files>test/install_smoke/support/generated_app_helper.ex, test/install_smoke/generated_app_smoke_test.exs</files>
  <behavior>
    - The image scenario installs only from the unpacked package root and rejects a repository path fallback.
    - Separate generated host migration files call `Oban.Migration.up/0` and default `Rindle.Migration.up(version: 1)`.
    - Every fixed Rindle relation is found in `rindle` and absent from `public`; `public.oban_jobs` and `public.schema_migrations` exist after both migrations.
    - A focused source/report-shape test proves the generated migration calls and required report keys without building a Phoenix consumer.
  </behavior>
  <action>Write a fast failing `phase_120_fast_contract` source/report-shape assertion first, then the image-profile integration assertions. The fast test must inspect the generated host migration template and pure report projection without invoking `phx.new`, package build, dependency fetch, PostgreSQL, or MinIO. Extend the existing migration runner/report instead of adding a harness (D-120-03): replace the public-only relation helper with an explicit `(schema, relation)` catalog check, derive the Rindle list from `Rindle.Migration.V1.owned_relations/0`, and report selected-schema presence, decoy-schema absence, host migration paths, package-root provenance, and public host relations. Keep the Oban and Rindle host migrations separate and default the Rindle call exactly per D-120-01/D-120-04. Preserve cleanup, network mode, existing profiles, and the legacy upgrade scenario.</action>
  <verify><automated>mix test test/install_smoke/generated_app_smoke_test.exs --only phase_120_fast_contract --seed 0</automated></verify>
  <acceptance_criteria>
    - The report proves all seven owned relations in `rindle` and none in `public`.
    - `public.oban_jobs` and `public.schema_migrations` remain present and no Rindle migration claims either relation.
    - The migration path is host-owned and excludes `deps/rindle` and package-directory replay.
    - Existing package-source, compile, boot, cleanup, and profile assertions remain green.
  </acceptance_criteria>
  <done>The lean package-consumer path fails if the packed default installs into public, uses checkout source, or takes ownership of host relations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exercise real default-routed persistence after boot</name>
  <read_first>
    test/install_smoke/support/generated_app_helper.ex
    test/install_smoke/generated_app_smoke_test.exs
    test/install_smoke/fixture_app/smoke_test.exs
  </read_first>
  <files>test/install_smoke/support/generated_app_helper.ex, test/install_smoke/generated_app_smoke_test.exs</files>
  <behavior>
    - After normal host migrations and boot, a public Rindle facade lifecycle writes and reads data through schemas compiled for `rindle`.
    - The report exposes stable lifecycle facts without embedding release policy assertions inside fixture strings.
    - Host Oban and ledger ownership remains unchanged after persistence.
    - The fast report-contract test asserts the lifecycle result keys before the packed scenario is run.
  </behavior>
  <action>Extend the fast `phase_120_fast_contract` assertion with the lifecycle report-key contract, then extend the existing generated app-local smoke test with the narrowest real persistence lifecycle already used by the image proof—create/initiate, verify, attach/read as supported by the fixture—and return JSON-safe identifiers/results to the repository report. Assert those results beside schema ownership in the repository integration test. Do not use raw Repo inserts as the sole runtime proof, mutate application env to retarget routing, or add a third prefix mode (D-120-02/D-120-06).</action>
  <verify><automated>mix test test/install_smoke/generated_app_smoke_test.exs --only phase_120_fast_contract --seed 0</automated></verify>
  <acceptance_criteria>
    - A real public Rindle operation persists and reads back after the generated application boots.
    - The lifecycle uses the compiled default `rindle` routing and the packed dependency.
    - Public Oban and the host ledger still pass schema-qualified ownership assertions afterward.
  </acceptance_criteria>
  <done>The packed application proves both database layout and usable runtime behavior, not migration DDL alone.</done>
</task>

</tasks>

## Artifacts this plan produces

- Enriched `GeneratedAppHelper` report fields for package provenance, explicit schema ownership, host migration ownership, and persistence outcomes.
- Default-image generated fixture migrations that separately hand Oban and Rindle setup to their public APIs.
- Image-profile package-consumer assertions that make the packed artifact, `rindle` default, public host infrastructure, boot, and persistence one executable contract.

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Packaged installation and runtime verification | Tasks 1–2 |
| REQ | PROOF-01 fresh default/runtime/Oban | Tasks 1–2 |
| REQ | PROOF-02 packed generated app | Tasks 1–2 |
| RESEARCH | schema-qualified report; real persistence; package authority | Tasks 1–2 |
| CONTEXT | D-120-01, D-120-03, D-120-04, D-120-06 | Cited and enforced in actions/criteria |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Repository to unpacked package | The test must consume built package contents, not silently resolve checkout source. |
| Generated host to PostgreSQL catalogs | Schema-qualified fixed-scope observations determine ownership truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-120-01 | Tampering | package dependency resolution | high | mitigate | Assert unpacked/network source facts and reject path fallback before schema assertions. |
| T-120-02 | Elevation of Privilege | catalog ownership proof | high | mitigate | Query only the fixed owned relation set plus public Oban/ledger with explicit schemas. |
| T-120-03 | Denial of Service | generated fixture cleanup | medium | mitigate | Preserve setup_all/on_exit cleanup and existing command diagnostics. |
| T-120-SC | Tampering | package installs | high | mitigate | No new dependency is planned; stop for package-legitimacy review if execution proposes one. |
</threat_model>

<verification>
Fast task loop: `mix test test/install_smoke/generated_app_smoke_test.exs --only phase_120_fast_contract --seed 0`.

Authoritative plan gate: `bash scripts/install_smoke.sh image` passes from a built artifact and demonstrates the complete default slice. The fast check is a companion, never a substitute for packed proof.
</verification>

<success_criteria>
- Packed default installation, host migrations, schema ownership, boot, and real persistence pass in one generated Phoenix consumer.
- A public-schema fallback or checkout-source fallback makes the proof fail.
- No migration/routing/diagnostic redesign or host-infrastructure mutation is introduced.
</success_criteria>

<output>Create `.planning/phases/120-adoption-proof-release-truth/120-01-SUMMARY.md` when done.</output>

---
phase: 120-adoption-proof-release-truth
plan: 04
type: execute
wave: 3
depends_on: [120-02]
files_modified:
  - README.md
  - guides/getting_started.md
  - lib/rindle/migration.ex
  - test/install_smoke/docs_parity_test.exs
autonomous: true
requirements: [DOCS-01]
nyquist_compliant: true
must_haves:
  truths:
    - "D-120-04: README, Getting Started, and Rindle.Migration moduledocs teach default rindle, explicit public compatibility, and separate host-owned Oban/ledger responsibility."
    - "D-120-05: fresh-install copy is operationally concrete and does not imply an automatic, online, arbitrary-prefix, or package-directory migration flow."
    - "D-120-03: docs parity mechanically locks the same host migration snippets used by generated-app and Cohort fixtures."
    - "D-120-04: docs parity reads the generated-app helper source and mechanically compares its default/public host migration fixture text and ownership prohibitions with public docs/API docs."
  artifacts:
    - path: "README.md"
      provides: "Concise first-install 0.4.0 migration contract"
    - path: "guides/getting_started.md"
      provides: "Detailed adopter host-migration workflow"
    - path: "lib/rindle/migration.ex"
      provides: "Canonical public API ownership/default documentation"
    - path: "test/install_smoke/docs_parity_test.exs"
      provides: "Executable fresh-install documentation parity"
  key_links:
    - from: "README.md"
      to: "lib/rindle/migration.ex"
      via: "identical version-pinned default/public host migration contract"
      pattern: "Rindle.Migration.up\(version: 1"
    - from: "test/install_smoke/support/generated_app_helper.ex"
      to: "README.md, guides/getting_started.md, lib/rindle/migration.ex"
      via: "docs parity extracts generated host migration templates and compares behavior-bearing calls/ownership boundaries"
      pattern: "Oban.Migration|Rindle.Migration.up"
---

<objective>
Make fresh-install public documentation and migration API docs state one executable 0.4.0 contract (D-120-03 through D-120-05).

Purpose: Let adopters copy the same host-owned setup the packed and Cohort proofs execute.
Output: Aligned README, Getting Started, moduledocs, and section-scoped parity tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-adoption-proof-release-truth/120-CONTEXT.md
@.planning/phases/120-adoption-proof-release-truth/120-PATTERNS.md
@.planning/phases/118-isolated-migration-safe-upgrade/118-04-SUMMARY.md
@README.md
@guides/getting_started.md
@lib/rindle/migration.ex
@test/install_smoke/docs_parity_test.exs
@test/install_smoke/support/generated_app_helper.ex
@.planning/phases/120-adoption-proof-release-truth/120-02-SUMMARY.md

<interfaces>
- Greenfield host migration calls default `Rindle.Migration.up(version: 1)`; only a public-compiled compatibility release pairs with `prefix: "public"`.
- Host owns normal `mix ecto.migrate`, `Oban.Migration`, `public.oban_jobs`, and `public.schema_migrations`.
- Legacy package-directory replay may appear only in its historical upgrade fence.
- The completed generated helper contains distinct default and explicit-public host migration templates; parity consumes source text and does not invoke the expensive package scenarios.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Lock one copy-pasteable fresh-install story across public entrypoints</name>
  <read_first>
    .planning/phases/120-adoption-proof-release-truth/120-CONTEXT.md
    README.md
    guides/getting_started.md
    test/install_smoke/docs_parity_test.exs
    test/install_smoke/support/generated_app_helper.ex
  </read_first>
  <files>README.md, guides/getting_started.md, test/install_smoke/docs_parity_test.exs</files>
  <behavior>
    - Both adopter entrypoints show separate host Oban and Rindle migrations followed by normal `mix ecto.migrate` and `mix rindle.doctor`.
    - Default calls omit prefix and explicitly create Rindle state in rindle; public compatibility requires both the option and public-compiled build.
    - Both state that Oban and the host migration ledger stay in public and outside Rindle ownership.
    - The fast parity test extracts the generated default and explicit-public migration fixture sections and compares their exact public API calls and forbidden replay/ownership claims with both docs.
  </behavior>
  <action>Extend the existing migration-section parity test first. Read `test/install_smoke/support/generated_app_helper.ex` as an explicit parity input and extract the generated host migration template regions for separate `Oban.Migration`, default `Rindle.Migration.up(version: 1)`, and explicit `Rindle.Migration.up(version: 1, prefix: "public")`. Assert those behavior-bearing calls agree with the README/Getting Started sections, that public compatibility is paired with a public-compiled fixture, and that fixture/docs exclude package-directory replay and any Rindle-created Oban/ledger path. Then revise only the corresponding README and Getting Started sections using calm direct language. Preserve historical replay only in the existing historical section and reject greenfield raw Ecto.Migrator/package-path instructions, arbitrary prefixes, search_path, or Rindle ownership of host relations (D-120-04/D-120-06). This source-parity test must remain fast and must not build a package or generated app.</action>
  <verify><automated>mix test test/install_smoke/docs_parity_test.exs --seed 0</automated></verify>
  <acceptance_criteria>
    - README and Getting Started contain behavior-equivalent, copy-pasteable host migration snippets.
    - Both name rindle as default, explicit public compatibility, public Oban/ledger ownership, normal migration command, and doctor verification.
    - Section-scoped tests reject raw package replay, arbitrary prefixes, search_path, and host-relation ownership claims.
    - Parity fails if either generated default/public migration template drifts from the public calls, prefix pairing, separate-file topology, or ownership statements taught in docs.
  </acceptance_criteria>
  <done>An adopter can install the packed release with the same migration topology exercised by automated proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make public migration API docs the same authority</name>
  <read_first>
    lib/rindle/migration.ex
    README.md
    guides/getting_started.md
    test/install_smoke/docs_parity_test.exs
    test/install_smoke/support/generated_app_helper.ex
  </read_first>
  <files>lib/rindle/migration.ex, test/install_smoke/docs_parity_test.exs</files>
  <behavior>
    - Migration moduledocs name the default/compatibility pairing and exact host/Rindle ownership boundary.
    - API docs direct populated installs to the directional maintenance-window helper and do not broaden its API.
    - Parity reads the module source and couples behavior-bearing snippets to public docs.
    - The same parity assertion couples migration moduledocs to both generated host migration fixture variants.
  </behavior>
  <action>Add the migration module source to docs parity setup and assertions before revising its docs. Reuse the Task 1 generated-fixture extraction and require the moduledoc examples to match the default and explicit-public fixture calls, separate host Oban ownership, and replay prohibitions. Align its examples and ownership statements with the two entrypoints while retaining exact existing function signatures. Cross-link populated installs to the upgrade guide rather than duplicating or redesigning the move. Do not change executable migration behavior, options, routing, or diagnostic classifications (D-120-06).</action>
  <verify><automated>mix format --check-formatted lib/rindle/migration.ex test/install_smoke/docs_parity_test.exs &amp;&amp; mix test test/install_smoke/docs_parity_test.exs test/rindle/migration_fast_test.exs --seed 0</automated></verify>
  <acceptance_criteria>
    - Moduledocs, README, and Getting Started agree on all behavior-bearing symbols and ownership statements.
    - No public function, migration option, routing authority, or move semantic changes.
    - Docs parity fails if the moduledoc drifts from the fresh-install contract.
    - Docs parity also fails if generated fixture source and moduledoc disagree while README/Getting Started still agree with each other.
  </acceptance_criteria>
  <done>The public migration API documentation and adopter entrypoints form one tested contract.</done>
</task>

</tasks>

## Artifacts this plan produces

- A concise README fresh-install path and a deeper Getting Started equivalent.
- Canonical `Rindle.Migration` moduledocs aligned to shipped behavior without API change.
- Section-scoped parity assertions over all three surfaces, including positive contract symbols and forbidden ownership/routing claims.
- Generated-helper source parity covering separate Oban/default/public migration fixture text without invoking slow package integration.

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Documentation verification for packaged install | Tasks 1–2 |
| REQ | DOCS-01 default, escape hatch, Oban ownership | Tasks 1–2 |
| RESEARCH | executable docs contract and host migration handoff | Tasks 1–2 |
| CONTEXT | D-120-03 through D-120-06 | Cited and enforced |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Public docs to operator migration | Copy can cause database mutations and must preserve exact ownership/order. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-120-10 | Tampering | migration snippets | high | mitigate | Parity-test exact pinned APIs, prefix pairing, and separate host ownership. |
| T-120-11 | Elevation of Privilege | ownership wording | high | mitigate | Explicitly scope Rindle to its fixed relations and public host relations to the app. |
| T-120-12 | Denial of Service | misleading install workflow | medium | mitigate | Require normal host migrations and doctor verification; reject raw replay/implicit routing. |
| T-120-SC | Tampering | package installs | high | mitigate | No package install is planned. |
</threat_model>

<verification>`mix test test/install_smoke/docs_parity_test.exs test/rindle/migration_fast_test.exs --seed 0` passes.</verification>

<success_criteria>
- Three public fresh-install surfaces agree mechanically on the shipped 0.4.0 contract.
- No implementation, migration semantics, or routing behavior changes.
</success_criteria>

<output>Create `.planning/phases/120-adoption-proof-release-truth/120-04-SUMMARY.md` when done.</output>

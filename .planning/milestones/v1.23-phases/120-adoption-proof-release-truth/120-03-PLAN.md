---
phase: 120-adoption-proof-release-truth
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/adoption_demo/priv/repo/migrations/20260528120100_add_oban.exs
  - examples/adoption_demo/priv/repo/migrations/20260809000000_install_rindle.exs
  - examples/adoption_demo/priv/rindle_migrate.exs
  - examples/adoption_demo/mix.exs
  - examples/adoption_demo/test/rindle_migration_contract_test.exs
  - .github/workflows/ci.yml
  - scripts/ci/cohort_demo_smoke.sh
autonomous: true
requirements: [PROOF-02]
nyquist_compliant: true
must_haves:
  truths:
    - "D-120-03/D-120-04: Cohort provisions Rindle through a checked-in host migration under normal ecto.migrate, not raw packaged-directory replay."
    - "D-120-04/D-120-06: Cohort owns separate Oban and Rindle migrations; Rindle relations are in rindle while Oban and the Ecto ledger stay in public."
    - "The demo boots and executes real Rindle persistence after normal migration setup, and existing CI/cold-start paths use that same setup."
    - "The established Docker cold-start proof asserts the running Cohort stack has every Rindle relation in rindle, none in public, and oban_jobs/schema_migrations in public."
  artifacts:
    - path: "examples/adoption_demo/priv/repo/migrations/20260809000000_install_rindle.exs"
      provides: "Host-owned pinned Rindle install migration"
    - path: "examples/adoption_demo/test/rindle_migration_contract_test.exs"
      provides: "Schema ownership and real persistence contract"
    - path: "examples/adoption_demo/mix.exs"
      provides: "One normal ecto migration setup path"
    - path: "scripts/ci/cohort_demo_smoke.sh"
      provides: "Container-build, migration, seed, boot, HTTP, persistence, and schema-ownership release/main proof"
  key_links:
    - from: "examples/adoption_demo/mix.exs"
      to: "examples/adoption_demo/priv/repo/migrations"
      via: "ecto.setup/test aliases run ecto.migrate once"
      pattern: "ecto.migrate"
    - from: ".github/workflows/ci.yml"
      to: "examples/adoption_demo/mix.exs"
      via: "adoption-demo-unit runs normal migration and tests without rindle.migrate"
      pattern: "adoption-demo-unit"
---

<objective>
Convert Cohort to the documented host-owned migration handoff and prove its normal setup, boot, ownership, and persistence end to end (D-120-03, D-120-04, D-120-06).

Purpose: Ensure the permanent adoption demo demonstrates the same 0.4.0 contract as generated consumers.
Output: Checked-in host migrations, a focused database contract test, retired raw replay, and aligned aliases/CI.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-adoption-proof-release-truth/120-CONTEXT.md
@.planning/phases/120-adoption-proof-release-truth/120-PATTERNS.md
@examples/adoption_demo/AGENTS.md
@examples/adoption_demo/mix.exs
@examples/adoption_demo/priv/rindle_migrate.exs
@examples/adoption_demo/priv/repo/migrations/20260528120100_add_oban.exs
@.github/workflows/ci.yml

<interfaces>
- Cohort's `ecto.setup`, `test`, CI, and Docker cold-start paths must converge on checked-in host migrations.
- Rindle migration: `Rindle.Migration.up(version: 1)` / `down(version: 1)`; Oban migration remains a distinct host-owned call.
- Demo dependency source remains its existing configurable local/package seam; this plan changes migration ownership, not dependency topology or UI.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Install Rindle through a Cohort-owned migration and persist</name>
  <read_first>
    examples/adoption_demo/AGENTS.md
    examples/adoption_demo/mix.exs
    examples/adoption_demo/priv/rindle_migrate.exs
    lib/rindle/migration.ex
    lib/rindle/migration/v1.ex
    examples/adoption_demo/test/support/data_case.ex
  </read_first>
  <files>examples/adoption_demo/priv/repo/migrations/20260809000000_install_rindle.exs, examples/adoption_demo/test/rindle_migration_contract_test.exs</files>
  <behavior>
    - A checked-in `AdoptionDemo.Repo.Migrations.InstallRindle` calls default `Rindle.Migration.up(version: 1)` and version-pinned down.
    - After ordinary `mix ecto.migrate`, all fixed relations exist in rindle and a real Rindle write/read succeeds.
    - No Rindle relation exists in public and public host relations are independently asserted.
  </behavior>
  <action>Use the demo's Ecto migration conventions to create one host-owned install file and write the focused contract test first. Derive relations from `Rindle.Migration.V1.owned_relations/0`, query explicit schemas, assert `public.oban_jobs` and `public.schema_migrations`, then execute a real Rindle facade or configured-schema write/read. Keep the test database-oriented and avoid UI changes. Do not replay `Application.app_dir(:rindle, ...)` or reproduce Rindle DDL (D-120-03/D-120-06).</action>
  <verify><automated>cd examples/adoption_demo &amp;&amp; mix ecto.create --quiet &amp;&amp; mix ecto.migrate --quiet &amp;&amp; mix test test/rindle_migration_contract_test.exs --seed 0</automated></verify>
  <acceptance_criteria>
    - The new migration is in the host ledger and calls only the pinned public Rindle migration API.
    - Seven owned relations are in rindle, absent from public, and usable through a real persistence path.
    - `public.oban_jobs` and `public.schema_migrations` exist and remain host-owned.
  </acceptance_criteria>
  <done>Cohort can be created, normally migrated, booted, and used without the raw package migration runner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize the separate host-owned Oban migration</name>
  <read_first>
    examples/adoption_demo/priv/repo/migrations/20260528120100_add_oban.exs
    examples/adoption_demo/mix.lock
    examples/adoption_demo/test/rindle_migration_contract_test.exs
  </read_first>
  <files>examples/adoption_demo/priv/repo/migrations/20260528120100_add_oban.exs, examples/adoption_demo/test/rindle_migration_contract_test.exs</files>
  <behavior>
    - The locked Oban 2.23.0 dependency exports `Oban.Migration.up/0` and `down/0`; the plan selects that exact singular API before editing.
    - Oban up/down stays in its own migration and creates only host-owned public infrastructure.
    - Rindle migration execution never changes the Oban relation snapshot.
  </behavior>
  <action>Run this blocking precheck before editing: `cd examples/adoption_demo &amp;&amp; mix run --no-start -e 'Code.ensure_loaded!(Oban.Migration); unless function_exported?(Oban.Migration, :up, 0) and function_exported?(Oban.Migration, :down, 0), do: System.halt(1)'`. The checked-in lock/dependency source establishes Oban 2.23.0 and the precheck must pass; halt rather than choose conditionally if it does not. Normalize the migration to the selected exact singular `Oban.Migration.up/0` and `down/0` contract, then extend the contract test to snapshot public Oban before/after Rindle behavior. Do not combine migrations or configure/prefix Oban from Rindle (D-120-04/D-120-06).</action>
  <verify><automated>cd examples/adoption_demo &amp;&amp; mix run --no-start -e 'Code.ensure_loaded!(Oban.Migration); unless function_exported?(Oban.Migration, :up, 0) and function_exported?(Oban.Migration, :down, 0), do: System.halt(1)' &amp;&amp; mix compile --warnings-as-errors &amp;&amp; mix test test/rindle_migration_contract_test.exs --seed 0</automated></verify>
  <acceptance_criteria>
    - The migration compiles against the locked Oban version and remains separate from Rindle setup.
    - The test proves the public Oban table remains unchanged by Rindle migration/persistence.
  </acceptance_criteria>
  <done>Cohort visibly owns Oban and Rindle as separate migration responsibilities.</done>
</task>

<task type="auto">
  <name>Task 3: Retire raw replay and align all automated demo setup</name>
  <read_first>
    examples/adoption_demo/mix.exs
    examples/adoption_demo/priv/rindle_migrate.exs
    .github/workflows/ci.yml
    RUNNING.md
    scripts/ci/cohort_demo_smoke.sh
  </read_first>
  <files>examples/adoption_demo/mix.exs, examples/adoption_demo/priv/rindle_migrate.exs, examples/adoption_demo/test/rindle_migration_contract_test.exs, .github/workflows/ci.yml, scripts/ci/cohort_demo_smoke.sh</files>
  <action>Remove the `rindle.migrate` alias and its invocations from `ecto.setup`, test, and adoption-demo-unit CI now that the checked-in host migration is part of `ecto.migrate`; delete the obsolete raw runner file. Preserve existing setup ordering, seeds, server env, CI job names, `ci.yml`/`name: CI`, CI Summary semantics, and cold-start topology. Extend `scripts/ci/cohort_demo_smoke.sh` after the existing boot/seed HTTP assertions with a clearly delimited fixed-relation array and a non-interactive query against the Compose PostgreSQL service: assert every fixed Rindle v1 relation and marker is present in `rindle`, absent from `public`, `public.oban_jobs` exists, and `public.schema_migrations` records the host migrations. Extend `rindle_migration_contract_test.exs` with a fast source assertion that the script's relation array exactly equals `Rindle.Migration.V1.owned_relations/0`, preventing proof drift. Emit bounded relation names/counts on failure and retain cleanup. Search all non-historical demo/CI paths for the retired alias and package-directory replay. Do not alter lane gating or release topology.</action>
  <verify><automated>bash -n scripts/ci/cohort_demo_smoke.sh &amp;&amp; cd examples/adoption_demo &amp;&amp; mix test test/rindle_migration_contract_test.exs --seed 0 &amp;&amp; cd ../.. &amp;&amp; ! rg 'mix rindle\.migrate|priv/rindle_migrate\.exs|Application\.app_dir\(:rindle' examples/adoption_demo .github/workflows/ci.yml scripts/ci/cohort_demo_smoke.sh &amp;&amp; bash scripts/ci/cohort_demo_smoke.sh</automated></verify>
  <acceptance_criteria>
    - Demo setup/test/CI runs one normal host migration flow and seeds afterward.
    - The obsolete runner and alias have no live references.
    - Existing CI identifiers, dependencies, trigger gates, and aggregate semantics are unchanged.
    - A clean Compose build starts Cohort, serves the existing HTTP/seed assertions, and then proves all Rindle relations in rindle only plus public Oban and host ledger state.
    - The cold-smoke relation array is mechanically equal to `Rindle.Migration.V1.owned_relations/0` in the fast demo contract test.
  </acceptance_criteria>
  <done>Every Cohort automation path proves the checked-in host migration contract rather than a private package directory.</done>
</task>

</tasks>

## Artifacts this plan produces

- Separate checked-in Cohort host migrations for Oban and default Rindle setup.
- A focused demo contract test covering explicit schema ownership, host ledger, and real persistence.
- A single normal `ecto.migrate` path across aliases and CI, with the raw packaged-migration runner removed.

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Demo operation verification | Tasks 1–3 |
| REQ | PROOF-02 Cohort rindle/public E2E | Tasks 1–3 |
| RESEARCH | host migration files; retire raw replay; align CI | Tasks 1–3 |
| CONTEXT | D-120-03, D-120-04, D-120-06 | Cited and enforced |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host migration files to dependency APIs | Cohort owns invocation timing while libraries own only their relations. |
| CI workflow to demo setup | Automation must execute the same host migration contract users run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-120-07 | Elevation of Privilege | demo migrations | high | mitigate | Keep distinct migrations and schema-qualified ownership assertions. |
| T-120-08 | Tampering | demo aliases/CI | medium | mitigate | Remove raw replay references and run the focused contract through normal migrations. |
| T-120-09 | Denial of Service | CI topology | high | mitigate | Preserve job names, needs, triggers, and CI Summary semantics while changing only commands. |
| T-120-SC | Tampering | package installs | high | mitigate | No dependency install/change is planned; verify existing Oban API locally. |
</threat_model>

<verification>
Fast loop: `cd examples/adoption_demo &amp;&amp; mix test test/rindle_migration_contract_test.exs --seed 0` and `bash -n scripts/ci/cohort_demo_smoke.sh`.

Authoritative Cohort gate: `bash scripts/ci/cohort_demo_smoke.sh` builds a clean image, runs normal migrations/seeds, boots the application, exercises HTTP/persisted rows, and asserts Rindle=`rindle`, Oban/ledger=`public` from the running stack.
</verification>

<success_criteria>
- Cohort provisions Rindle in rindle and Oban/ledger in public through host-owned files.
- Demo persistence works after normal setup, and no live raw package replay path remains.
</success_criteria>

<output>Create `.planning/phases/120-adoption-proof-release-truth/120-03-SUMMARY.md` when done.</output>

---
phase: 120-adoption-proof-release-truth
plan: 12
type: execute
wave: 8
depends_on: [120-11]
files_modified: []
autonomous: false
gap_closure: true
requirements: [PROOF-01, PROOF-02, DOCS-01]
nyquist_compliant: true
must_haves:
  truths:
    - "D-120-01/D-120-03: exactly one open milestone PR is resolved before detachment, and its asserted headRefOid owns clean packed explicit-public, packed populated-upgrade, Cohort, and repository-hygiene receipts from a clean detached worktree with one mode-700 TMPDIR."
    - "The same candidate has successful PR CI evidence for both Quality cells, Proof, Package Consumer Proof Matrix + Release Preflight, Adoption Demo Unit, Integration, Contract, Adopter, and CI Summary."
    - "D-120-04/D-120-05: release authorization remains pending until the immutable merge SHA captured from the authorized milestone PR has successful push-main Package Consumer Full Matrix + Release Preflight and Cohort Demo Smoke evidence, then the protected Release workflow succeeds with its tooling SHA recorded separately and its recovery_ref plus gate-ci-green release_sha bound to the immutable release candidate."
    - "The failed ec3aae5d85225dbdd43992f28a5d30e16fb8aea5 CI run and its passing local receipt are retained as separate historical facts and are never combined into a passing release chain."
  artifacts:
    - path: ".planning/phases/120-adoption-proof-release-truth/120-12-SUMMARY.md"
      provides: "Candidate reconciliation; unique PR number/URL/headRefOid; clean-worktree and mode-700 TMPDIR receipts; immutable milestone merge SHA; exact-SHA PR/main/Release URLs; separate Release tooling and source identities; job conclusions; and explicit pending or failed gates"
  key_links:
    - from: "clean detached candidate SHA"
      to: "packed public/upgrade and Cohort commands"
      via: "one dedicated TMPDIR and complete exit-code receipts"
      pattern: "phase_120_public_compat|phase_120_isolation_upgrade"
    - from: "unique milestone PR headRefOid"
      to: "GitHub Actions CI"
      via: "run headSha equality plus named live-job conclusions"
      pattern: "Proof|Package Consumer Proof Matrix|Adoption Demo Unit|CI Summary"
    - from: "authorized milestone PR transition"
      to: "push-main GitHub Actions CI"
      via: "persisted mergeCommit.oid queried directly, never mutable refs/heads/main"
      pattern: "mergeCommit|Package Consumer Full Matrix|Cohort Demo Smoke"
    - from: "recorded release candidate SHA"
      to: "Release workflow"
      via: "separate workflow tooling headSha plus recovery_ref and gate-ci-green release_sha source binding, consumed exact-SHA ci.yml gate, preflight, version alignment, dry-run publish, and public artifact verification"
      pattern: "recovery_ref|release_sha|Run release preflight|Verify version alignment|Dry run Hex publish|Verify public Hex.pm artifact"
  prohibitions:
    - statement: "MUST NOT combine local receipts from one SHA with GitHub Actions results from another SHA."
      status: unverified
      flagged: true
    - statement: "MUST NOT push, merge, dispatch a protected release, publish, or claim release authorization without explicit maintainer approval."
      status: unverified
      flagged: true
---

<objective>
Reconcile the split Phase 120 evidence chain after Plan 120-11: the locally proven `ec3aae5...` candidate later failed PR CI, while the subsequent `fcd8066...` candidate has successful PR CI but no same-SHA local packed/Cohort receipt or main/Release evidence (D-120-01 through D-120-06).

Purpose: Close PROOF-01/PROOF-02 only when one immutable candidate owns each applicable local and hosted proof stage, while preserving the PR-first green-main release train.
Output: A fail-closed evidence summary for the current candidate and a blocking maintainer checkpoint for merge, push-main, Release Please, and protected Release evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-adoption-proof-release-truth/120-CONTEXT.md
@.planning/phases/120-adoption-proof-release-truth/120-VERIFICATION.md
@.planning/phases/120-adoption-proof-release-truth/120-10-SUMMARY.md
@.planning/phases/120-adoption-proof-release-truth/120-11-SUMMARY.md
@.planning/DEVELOPMENT-TRAIN.md
@.planning/RELEASE-TRAIN.md
@RUNNING.md
@guides/release_publish.md
@scripts/ensure_minio.sh
@scripts/ci/cohort_demo_smoke.sh
@scripts/maintainer/repo_hygiene_check.sh
@test/install_smoke/generated_app_smoke_test.exs
@.github/workflows/ci.yml
@.github/workflows/release.yml

<interfaces>
- Plan 120-11's local receipt belongs only to `ec3aae5d85225dbdd43992f28a5d30e16fb8aea5`; GitHub run `31448759145` later failed both Quality cells and skipped downstream proof jobs.
- Candidate `fcd806685fa86445a4db203a7b338dee23f9ae94` has successful PR CI run `31450662853`, including both Quality cells, Proof, lean Package Consumer, Adoption Demo Unit, Integration, Contract, Adopter, and CI Summary; push-main-only jobs are correctly skipped on that PR run.
- `RUNNING.md`, `.planning/DEVELOPMENT-TRAIN.md`, `.planning/RELEASE-TRAIN.md`, and `guides/release_publish.md` are authoritative for PR, main, Release Please, and protected Release gates.
- A squash/merge commit or Release Please version/changelog commit is a new candidate SHA. Evidence must be recorded per immutable SHA; it cannot inherit a predecessor's authorization.
</interfaces>
</context>

<gap_context>
Plans 120-07, 120-08, and 120-10 closed the verifier's catalog/content, Oban-snapshot, and explicit-public rollback defects. Plan 120-11 closed local packed/Cohort behavior for `ec3aae5...`, but the subsequently created exact-SHA PR run failed before Proof/package/demo jobs. Commits `61a6dc9` and `fcd8066` followed, and `fcd8066...` now has green PR CI. No single current SHA has both the complete local Phase 120 receipt and every applicable hosted release-train result, so this plan records the missing same-candidate chain rather than duplicating implementation fixes.
</gap_context>

<assumption_resolution>
No Phase 120 SPEC exists. The deterministic edge probes are handled explicitly:

1. **PROOF-01 boundary:** the rerun remains bounded to the fixed V1 Rindle relations, marker `[1]`, named FK/index evidence, exact `public.oban_jobs` snapshot equality, and public host ledger per D-120-06.
2. **PROOF-01 precision:** every receipt is keyed to the full 40-character SHA, GitHub `headSha`, run ID, job name, conclusion, command exit code, and catalog equality; no branch-name or short-SHA inference is accepted.
3. **PROOF-02 unclassified:** local packed/Cohort behavior, PR CI, push-main CI, and protected Release are distinct evidence stages; each must be reported with its real status.
4. **DOCS-01 concurrency:** docs/release parity runs in the clean candidate before integration, and Release Please's generated 0.4.0 changelog is reviewed on the later release candidate instead of assuming the milestone PR text remains unchanged.
</assumption_resolution>

<tasks>

<task type="tracer">
  <name>Task 1: Establish one candidate's clean local and PR-hosted proof chain</name>
  <reversibility rating="reversible">This task records read-only verification evidence and creates no product, schema, workflow, or release mutation.</reversibility>
  <precondition>The current milestone PR head resolves to a 40-character commit with a completed successful PR `ci.yml` run; Docker and PostgreSQL are reachable; loopback ports 14102, 19000, and 19001 are unused; and a clean detached worktree plus mode-700 dedicated TMPDIR can be created.</precondition>
  <read_first>
    .planning/phases/120-adoption-proof-release-truth/120-11-SUMMARY.md
    .planning/DEVELOPMENT-TRAIN.md
    .planning/RELEASE-TRAIN.md
    RUNNING.md
    guides/release_publish.md
    scripts/ensure_minio.sh
    scripts/ci/cohort_demo_smoke.sh
    scripts/maintainer/repo_hygiene_check.sh
    test/install_smoke/generated_app_smoke_test.exs
    .github/workflows/ci.yml
  </read_first>
  <files>None; record receipts only in .planning/phases/120-adoption-proof-release-truth/120-12-SUMMARY.md</files>
  <action>Before creating or entering a detached worktree, list open PRs with the explicit head `milestone/v1.23-postgres-schema-isolation`; require exactly one result, persist its PR number and URL, and resolve that PR explicitly through its number or URL. Require its full 40-character `headRefOid` to equal the fetched `origin/milestone/v1.23-postgres-schema-isolation` commit; do not run argument-free `gh pr view`, and do not substitute the dirty shared checkout HEAD, a branch label, or Plan 120-11's `ec3aae5...` SHA. Query `ci.yml` for that exact headRefOid and record run URL/ID, event, status, conclusion, `headSha`, and live conclusions for both Quality cells, Proof, Package Consumer Proof Matrix + Release Preflight, Adoption Demo Unit, Integration, Contract, Adopter, and CI Summary. Fail closed if the PR is not unique, the OIDs differ, or any required PR job is absent, skipped, pending, cancelled, or unsuccessful. Create a clean detached worktree at the asserted headRefOid and a dedicated TMPDIR under a fresh receipt root, set the TMPDIR directory to mode 700, export it, assert its mode, run `TMPDIR="$TMPDIR" MIX_ENV=test mix deps.get` from the detached worktree, and prefix every receipt command with that same TMPDIR. Preserve separate BEAM invocations for public compatibility and populated upgrade, supported Cohort loopback publish overrides, exact marker/FK/index/Oban snapshot facts, Cohort HTTP/schema/ledger facts, repository-hygiene summary, and every exit code. Keep run `31448759145` as explicit failed historical evidence and never combine it with this candidate's results. Do not edit implementation, workflow, Compose, release, or existing summary files; any discovery, setup, local, or PR failure ends the task with raw diagnostics.</action>
  <verify>
    <automated>PR_JSON=$(gh pr list --state open --head milestone/v1.23-postgres-schema-isolation --json number,url,headRefOid --limit 2) &amp;&amp; test "$(printf '%s' "$PR_JSON" | jq 'length')" -eq 1 &amp;&amp; PR_NUMBER=$(printf '%s' "$PR_JSON" | jq -r '.[0].number') &amp;&amp; PR_URL=$(printf '%s' "$PR_JSON" | jq -r '.[0].url') &amp;&amp; CANDIDATE_SHA=$(gh pr view "$PR_NUMBER" --json headRefOid --jq '.headRefOid') &amp;&amp; test ${#CANDIDATE_SHA} -eq 40 &amp;&amp; git fetch origin milestone/v1.23-postgres-schema-isolation &amp;&amp; test "$CANDIDATE_SHA" = "$(git rev-parse origin/milestone/v1.23-postgres-schema-isolation)" &amp;&amp; PR_RUNS=$(gh run list --commit "$CANDIDATE_SHA" --workflow ci.yml --event pull_request --json databaseId,headSha,status,conclusion,url,event --limit 20) &amp;&amp; PR_RUN_ID=$(printf '%s' "$PR_RUNS" | jq -er --arg sha "$CANDIDATE_SHA" '[.[] | select(.headSha == $sha and .event == "pull_request" and .status == "completed" and .conclusion == "success")][0].databaseId') &amp;&amp; test -n "$PR_RUN_ID" &amp;&amp; test "$(gh run view "$PR_RUN_ID" --json headSha --jq '.headSha')" = "$CANDIDATE_SHA" &amp;&amp; RECEIPT_ROOT=$(mktemp -d) &amp;&amp; TMPDIR="$RECEIPT_ROOT/tmp" &amp;&amp; WORKTREE="$RECEIPT_ROOT/worktree" &amp;&amp; mkdir -m 700 "$TMPDIR" &amp;&amp; test "$(stat -f '%Lp' "$TMPDIR" 2&gt;/dev/null || stat -c '%a' "$TMPDIR")" = 700 &amp;&amp; git worktree add --detach "$WORKTREE" "$CANDIDATE_SHA" &amp;&amp; cd "$WORKTREE" &amp;&amp; export TMPDIR &amp;&amp; TMPDIR="$TMPDIR" MIX_ENV=test mix deps.get &amp;&amp; TMPDIR="$TMPDIR" mix format --check-formatted test/install_smoke/support/generated_app_helper.ex test/install_smoke/generated_app_smoke_test.exs &amp;&amp; TMPDIR="$TMPDIR" mix test test/install_smoke/generated_app_smoke_test.exs --only phase_120_upgrade_contract --seed 0 &amp;&amp; TMPDIR="$TMPDIR" mix test test/install_smoke/generated_app_smoke_test.exs --only phase_120_fast_contract --seed 0 &amp;&amp; TMPDIR="$TMPDIR" mix test test/install_smoke/docs_parity_test.exs test/install_smoke/release_docs_parity_test.exs --seed 0 &amp;&amp; TMPDIR="$TMPDIR" bash scripts/ensure_minio.sh &amp;&amp; TMPDIR="$TMPDIR" RINDLE_INSTALL_SMOKE_PROFILE=image mix test test/install_smoke/generated_app_smoke_test.exs --include minio --only phase_120_public_compat --seed 0 &amp;&amp; TMPDIR="$TMPDIR" RINDLE_INSTALL_SMOKE_PROFILE=image mix test test/install_smoke/generated_app_smoke_test.exs --include minio --only phase_120_isolation_upgrade --seed 0 &amp;&amp; TMPDIR="$TMPDIR" COHORT_DEMO_PORT=14102 COHORT_MINIO_PORT=19000 COHORT_MINIO_CONSOLE_PORT=19001 bash scripts/ci/cohort_demo_smoke.sh &amp;&amp; TMPDIR="$TMPDIR" ./scripts/maintainer/repo_hygiene_check.sh</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one open `milestone/v1.23-postgres-schema-isolation` PR is resolved explicitly before detachment; its number, URL, 40-character headRefOid, and equality with the fetched remote milestone commit are recorded.
    - That headRefOid owns the clean detached worktree, the mode-700 dedicated TMPDIR, dependency fetch, every TMPDIR-prefixed local command, and the successful PR CI run.
    - Both Quality cells, Proof, lean Package Consumer, Adoption Demo Unit, Integration, Contract, Adopter, and CI Summary are successful in the recorded PR run; push-main-only skips are labeled pending, not failures or passes.
    - Packed public compatibility, packed populated upgrade, Cohort, docs/release parity, and repository hygiene all exit 0 with exact catalog/HTTP/ledger facts recorded.
    - The failed `ec3aae5...` run and successful `fcd8066...` or newer candidate evidence remain separate immutable records.
  </acceptance_criteria>
  <done>The current milestone PR candidate has a complete same-SHA local diagnostic chain and green merge-blocking PR evidence, with later main/Release gates still explicitly pending.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Authorize PR-first progression and confirm main plus protected Release evidence</name>
  <read_first>
    .planning/phases/120-adoption-proof-release-truth/120-12-SUMMARY.md
    .planning/DEVELOPMENT-TRAIN.md
    .planning/RELEASE-TRAIN.md
    RUNNING.md
    guides/release_publish.md
    CHANGELOG.md
    .release-please-manifest.json
    .github/workflows/ci.yml
    .github/workflows/release.yml
  </read_first>
  <files>None; append human-authorized immutable evidence references to .planning/phases/120-adoption-proof-release-truth/120-12-SUMMARY.md</files>
  <action>Pause before any push, merge, Release Please merge, workflow dispatch, or publish. After the maintainer performs or explicitly authorizes the reviewed milestone PR transition, resolve that same explicit PR number/URL and persist its `mergeCommit.oid` as `MILESTONE_MERGE_SHA` before querying CI; require a 40-character value and never derive this evidence identity from mutable `refs/heads/main` or the current remote main tip. Query `ci.yml` only with the persisted milestone merge SHA and record direct URL, run ID, event, `headSha`, successful conclusion, and successful `Package Consumer Full Matrix + Release Preflight` plus `Cohort Demo Smoke`; require the run `headSha` to equal `MILESTONE_MERGE_SHA` and also require all green-main merge-blocking jobs named in RUNNING.md. For the Release Please candidate, verify the generated `[0.4.0]` block contains the staged schema-isolation notes and the staging marker is removed, then persist the reviewed release PR's `mergeCommit.oid` as `RELEASE_CANDIDATE_SHA`. Model protected Release evidence with two identities: record the workflow run URL/ID and run `headSha` as the current trusted tooling identity, while recording the authorized workflow_dispatch `recovery_ref` and `gate-ci-green.outputs.release_sha` as release-source identity. Require both source values to equal `RELEASE_CANDIDATE_SHA`; separately require the `ci.yml` run consumed by `gate-ci-green` to have `headSha == RELEASE_CANDIDATE_SHA` and a successful conclusion. Do not require the protected Release workflow run `headSha` to equal the release candidate, because current trusted workflow tooling may execute from a newer main SHA while materializing the frozen recovery source. Accept protected Release evidence only when this source binding is demonstrated and the workflow records successful `Run release preflight`, `Verify version alignment`, `Dry run Hex publish` or an explicitly recorded already-published idempotency skip, and `Verify public Hex.pm artifact`. If either reviewed PR creates a different merge SHA or Release Please changes package contents, record the new identity and require its own applicable evidence; never inherit authorization from a predecessor. A missing, failed, pending, skipped, or identity-ambiguous main/Release gate leaves the checkpoint blocked. Local diagnostics remain non-authoritative.</action>
  <verify>
    <automated>MILESTONE_PR_JSON=$(gh pr list --state all --head milestone/v1.23-postgres-schema-isolation --json number,url,headRefOid,mergedAt,mergeCommit --limit 2) &amp;&amp; test "$(printf '%s' "$MILESTONE_PR_JSON" | jq 'length')" -eq 1 &amp;&amp; MILESTONE_MERGE_SHA=$(printf '%s' "$MILESTONE_PR_JSON" | jq -er '.[0].mergeCommit.oid') &amp;&amp; test ${#MILESTONE_MERGE_SHA} -eq 40 &amp;&amp; MAIN_RUN=$(gh run list --commit "$MILESTONE_MERGE_SHA" --workflow ci.yml --event push --json databaseId,headSha,status,conclusion,url,event --limit 20 | jq -cer --arg sha "$MILESTONE_MERGE_SHA" '[.[] | select(.headSha == $sha and .event == "push" and .status == "completed" and .conclusion == "success")][0]') &amp;&amp; test "$(printf '%s' "$MAIN_RUN" | jq -r '.headSha')" = "$MILESTONE_MERGE_SHA" &amp;&amp; MAIN_RUN_ID=$(printf '%s' "$MAIN_RUN" | jq -r '.databaseId') &amp;&amp; gh run view "$MAIN_RUN_ID" --json jobs --jq '.jobs[] | [.name,.conclusion] | @tsv'</automated>
    <human-check>Confirm the reviewed milestone PR was merged through the repository's PR-first lane and its persisted mergeCommit.oid—not a later main tip—owns green merge-blocking CI plus successful push-main Package Consumer Full Matrix + Release Preflight and Cohort Demo Smoke. Confirm Release Please generated and removed the correct 0.4.0 staging text. For the protected Release run, supply the run URL/ID and tooling headSha separately from the release source; confirm the authorized recovery_ref and gate-ci-green release_sha both equal the reviewed release PR merge SHA, the consumed ci.yml run headSha equals that source SHA, and Run release preflight, Verify version alignment, Dry run Hex publish (or the recorded idempotent already-published path), and Verify public Hex.pm artifact succeeded.</human-check>
  </verify>
  <acceptance_criteria>
    - No external mutation occurs without explicit maintainer authorization, and the plan records who supplied the resume signal plus the exact transition authorized.
    - PR head, immutable milestone `mergeCommit.oid`, reviewed Release Please `mergeCommit.oid`, protected Release tooling `headSha`, authorized `recovery_ref`, and `gate-ci-green.outputs.release_sha` are recorded as separate fields.
    - The push-main run `headSha` equals the persisted milestone merge SHA; the Release recovery_ref and gate-ci-green release_sha equal the persisted release candidate SHA; and the consumed ci.yml run `headSha` equals that release candidate SHA. The Release workflow tooling headSha is recorded but is not substituted for release-source identity.
    - Every required main job and protected Release step conclusion is successful, with an already-published dry-run skip accepted only when the idempotency result and successful public verification are recorded.
    - Missing push-main, Release Please, or protected Release evidence remains pending; local receipts and green PR CI never authorize publication.
  </acceptance_criteria>
  <what-built>A same-SHA local and green PR proof chain for the current milestone candidate, with no release-train mutation performed.</what-built>
  <how-to-verify>Review the recorded PR, immutable milestone merge, Release Please, and protected Release URLs. Confirm direct headSha equality for PR and push-main CI; for Release, confirm the tooling headSha is recorded separately while recovery_ref, gate-ci-green release_sha, and the consumed ci.yml headSha all bind to the reviewed release candidate SHA. Confirm the named jobs/steps and the generated 0.4.0 changelog contract before publication.</how-to-verify>
  <resume-signal>Type "approved" with the milestone PR URL, exact PR head and milestone merge SHAs, reviewed Release Please merge SHA, immutable main/CI/Release URLs, protected Release tooling headSha, authorized recovery_ref, gate-ci-green release_sha, and consumed ci.yml URL/headSha; or describe the missing, pending, failed, or unauthorized transition.</resume-signal>
  <done>Adopters and maintainers have an honest immutable evidence chain from packed candidate behavior through green main and the protected 0.4.0 Release workflow, or the phase remains blocked at the precisely named external gate.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/120-adoption-proof-release-truth/120-12-SUMMARY.md` — unique PR number/URL/headRefOid; immutable milestone and Release Please merge SHAs; clean-worktree and mode-700 TMPDIR identity; local command exits; packed marker/FK/index/Oban facts; Cohort HTTP/schema/ledger facts; repository-hygiene result; push-main exact-SHA evidence; protected Release tooling headSha separated from recovery_ref/gate release_sha/consumed CI source identity; immutable GitHub run/job/step URLs and conclusions; explicit pending/failed gates.
- New symbols: None.
- New flags: None.
- New fields: None.
- Product, migration, routing, diagnostics, workflow, Compose, Oban, and host-ledger paths: None.

## Multi-Source Coverage Audit

| Source | ID | Feature/constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Verify packaged install, upgrade, demo, docs, and release truth | 120-12 Tasks 1-2 | COVERED | One immutable identity per evidence stage; no cross-SHA composition. |
| REQ | PROOF-01 | Default/public/upgrade/runtime/Oban proof | 120-07, 120-08, 120-10, 120-12 Task 1 | COVERED | Defects are fixed; current candidate reruns exact packed facts. |
| REQ | PROOF-02 | Packed generated app and Cohort end to end | 120-12 Tasks 1-2 | COVERED | Local, PR, push-main, and protected Release remain distinct. |
| REQ | DOCS-01 | Docs/release parity and generated 0.4.0 truth | 120-08, 120-12 Tasks 1-2 | COVERED | Candidate parity plus Release Please changelog review. |
| RESEARCH | — | Packed artifact rather than checkout is release authority | 120-12 Tasks 1-2 | COVERED | Local receipt is diagnostic; hosted release chain authorizes. |
| RESEARCH | — | Existing generated-app, Cohort, parity, and workflow lanes | 120-12 Tasks 1-2 | COVERED | No parallel proof framework. |
| CONTEXT | D-120-01 | Packed artifact authority | 120-12 Tasks 1-2 | COVERED | Same-SHA package behavior and hosted evidence. |
| CONTEXT | D-120-02 | Explicit public compatibility only | 120-12 Task 1 | COVERED | Separate public fixture/BEAM invocation. |
| CONTEXT | D-120-03 | Reuse existing harnesses | 120-12 Task 1 | COVERED | Existing scripts/tests only. |
| CONTEXT | D-120-04 | Cross-surface default/public/Oban/ledger agreement | 120-12 Tasks 1-2 | COVERED | Catalog, Cohort, docs, changelog, and workflow evidence. |
| CONTEXT | D-120-05 | Honest ordered operation and guarded release truth | 120-12 Task 2 | COVERED | PR-first and protected Release checkpoint. |
| CONTEXT | D-120-06 | No routing/move/diagnostic or host-table redesign | Verification-only | COVERED | No implementation file is modified. |

No deferred idea is planned. No new product external API integration, package install, schema-push surface, frontend surface, or architectural decision is introduced, so no API COVERAGE.md, package-legitimacy, UI-SPEC, or reversibility decision checkpoint is triggered.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Shared dirty checkout to clean candidate | User edits and branch-head ambiguity must not contaminate release receipts. |
| Local candidate to packed consumers/Cohort | Checkout tests cannot substitute for built-package and composed-demo behavior. |
| Milestone PR to main and Release Please | GitHub may create new immutable commits whose evidence cannot be inherited from the PR head. |
| Local executor to GitHub Actions/Hex release | Hosted conclusions and protected authorization are external state requiring exact identity and human authority. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-120-G12-01 | Repudiation | composite evidence chain | high | mitigate | Record full SHA, run ID/URL, command exit, live job/step name, and conclusion for every stage; prohibit cross-SHA composition. |
| T-120-G12-02 | Tampering | dirty checkout/candidate selection | high | mitigate | Resolve the PR head through GitHub and execute local proof only in a clean detached worktree with a dedicated TMPDIR. |
| T-120-G12-03 | Elevation of Privilege | merge/release operations | high | mitigate | Block before push, merge, protected dispatch, or publish; require explicit maintainer authorization and preserve Release Please ownership. |
| T-120-G12-04 | Denial of Service | packed/Cohort services | medium | mitigate | Preflight Docker/PostgreSQL/ports, use supported isolated ports, retain bounded commands, and fail closed on stalled cleanup. |
| T-120-G12-05 | Information Disclosure | receipts | low | accept | Record public run metadata and generated catalog facts only; exclude credentials, tokens, connection strings, and package keys. |
| T-120-SC | Tampering | committed dependency graph | high | mitigate | Fetch only the candidate's committed lockfile dependencies; introduce no new package. |
</threat_model>

<verification>
1. Task 1 must produce a complete clean local receipt and successful required PR job list for one exact candidate SHA.
2. Task 2 must retain distinct immutable identities for PR, main, Release Please, and protected Release stages and stop at any absent or unsuccessful external gate.
3. `node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs query frontmatter.validate .planning/phases/120-adoption-proof-release-truth/120-12-PLAN.md --schema plan`
4. `node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs query verify.plan-structure .planning/phases/120-adoption-proof-release-truth/120-12-PLAN.md`
</verification>

<success_criteria>
- One current milestone candidate owns its complete local packed/Cohort proof and green merge-blocking PR CI evidence.
- Any new main or Release Please SHA is treated as a new immutable evidence stage, never silently equated with the PR head.
- Push-main package/Cohort jobs and protected Release steps are successful before release authorization is claimed.
- External mutations remain human-authorized, and every absent/failed gate is represented as pending or blocked.
</success_criteria>

<output>Create `.planning/phases/120-adoption-proof-release-truth/120-12-SUMMARY.md` when done.</output>

---
phase: 120-adoption-proof-release-truth
plan: 02
type: execute
wave: 2
depends_on: [120-01]
files_modified:
  - test/install_smoke/support/generated_app_helper.ex
  - test/install_smoke/generated_app_smoke_test.exs
autonomous: true
requirements: [PROOF-01]
nyquist_compliant: true
must_haves:
  truths:
    - "D-120-02: a distinct public-compiled generated consumer proves explicit prefix: public compatibility without implying runtime selection or arbitrary schemas."
    - "D-120-04/D-120-05: a populated public install moves through a host-owned maintenance migration to rindle, preserves relational state, then boots and persists under default routing."
    - "D-120-06: both scenarios leave public.oban_jobs and public.schema_migrations untouched and reuse Phase 118 directional helpers."
    - "Planner resolution: public compatibility and populated upgrade always use separate generated app roots, database names, reports, and compile-time builds; no scenario mutates routing inside one BEAM instance."
    - "Fast scenario-topology/report contract tests precede the authoritative packed compatibility and upgrade integrations."
  artifacts:
    - path: "test/install_smoke/support/generated_app_helper.ex"
      provides: "Isolated public-compatibility and populated-isolation-upgrade scenarios"
    - path: "test/install_smoke/generated_app_smoke_test.exs"
      provides: "Policy assertions for both non-default proof paths"
  key_links:
    - from: "populated upgrade host migration"
      to: "Rindle.Migration.move_public_to_rindle/1"
      via: "SET LOCAL lock_timeout followed by the version-pinned helper"
      pattern: "move_public_to_rindle\(version: 1\)"
---

<objective>
Expand the proven packed harness with isolated explicit-public compatibility and populated public-to-`rindle` upgrade slices (D-120-02 through D-120-06).

Purpose: Cover the remaining breaking-contract paths without compile-time prefix contamination between scenarios.
Output: Two named generated-app scenarios and report assertions for compatibility, data-preserving upgrade, and host ownership.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-adoption-proof-release-truth/120-CONTEXT.md
@.planning/phases/120-adoption-proof-release-truth/120-RESEARCH.md
@.planning/phases/118-isolated-migration-safe-upgrade/118-CONTEXT.md
@.planning/phases/118-isolated-migration-safe-upgrade/118-02-SUMMARY.md
@.planning/phases/118-isolated-migration-safe-upgrade/118-03-SUMMARY.md
@.planning/phases/120-adoption-proof-release-truth/120-01-SUMMARY.md
@test/install_smoke/support/generated_app_helper.ex
@test/install_smoke/generated_app_smoke_test.exs

<interfaces>
- Prefix selection is compile-time and supports exactly `rindle` or explicit `public`; each proof needs its own generated app/build.
- Resolved topology: public compatibility and populated upgrade are separate named generated applications with unique roots/databases/reports; this is fixed before execution, not an open implementation choice.
- `move_public_to_rindle(version: 1)` is the sole forward upgrade primitive and moves only `Migration.V1.owned_relations/0`.
- The prior AV-era `prove_upgrade_install!/0` remains a separate historical proof and must not be overloaded.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove explicit public compatibility in its own packed consumer</name>
  <read_first>
    .planning/phases/120-adoption-proof-release-truth/120-CONTEXT.md
    .planning/phases/117-prefix-routing-architecture/117-VERIFICATION.md
    test/install_smoke/support/generated_app_helper.ex
    test/install_smoke/generated_app_smoke_test.exs
  </read_first>
  <files>test/install_smoke/support/generated_app_helper.ex, test/install_smoke/generated_app_smoke_test.exs</files>
  <behavior>
    - A public-compiled generated app uses a host migration calling `Rindle.Migration.up(version: 1, prefix: "public")`.
    - All owned relations are in public and absent from rindle for that scenario; runtime persistence succeeds through public-compiled schemas.
    - Compatibility does not accept an arbitrary prefix or mutate prefix configuration at runtime.
    - A fast topology contract asserts a distinct public-compatibility entrypoint, app root/database/report identity, migration template, and expected report keys without generating the app.
  </behavior>
  <action>Add a fast failing `phase_120_compat_contract` test first that inspects scenario topology/template/report projection without package build, Phoenix generation, dependency fetch, PostgreSQL, or MinIO. Then add a separately named public-compatibility entrypoint/report and its integration assertions. Generate a fresh app/database compiled with the supported public configuration and a distinct host migration that passes only `prefix: "public"`; boot and exercise the same real lifecycle as the default slice. Reuse shared package/cleanup/report helpers per D-120-03. Enforce the resolved separate-app topology: unique root, database, report file, and compile invocation; do not reuse the default BEAM instance, expose a generic prefix argument, or present compatibility as a third routing model (D-120-02).</action>
  <verify><automated>mix test test/install_smoke/generated_app_smoke_test.exs --only phase_120_compat_contract --seed 0</automated></verify>
  <acceptance_criteria>
    - The scenario is package-backed, isolated from the default build, and explicitly public-compiled.
    - Exactly the fixed Rindle relations and marker exist in public, none in rindle, and real persistence succeeds.
    - Public Oban and ledger remain host-owned; no arbitrary prefix or runtime retargeting interface exists.
  </acceptance_criteria>
  <done>The sole compatibility escape hatch is executable and cannot be mistaken for the default or a generic routing feature.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove a populated public install moves and runs under the default</name>
  <reversibility rating="costly">The proof codifies the published populated-upgrade contract and its exact relation/integrity expectations.</reversibility>
  <read_first>
    .planning/phases/118-isolated-migration-safe-upgrade/118-CONTEXT.md
    .planning/phases/118-isolated-migration-safe-upgrade/118-VERIFICATION.md
    .planning/phases/120-adoption-proof-release-truth/120-PATTERNS.md
    test/install_smoke/support/generated_app_helper.ex
    test/install_smoke/generated_app_smoke_test.exs
  </read_first>
  <files>test/install_smoke/support/generated_app_helper.ex, test/install_smoke/generated_app_smoke_test.exs</files>
  <behavior>
    - A public-compiled package consumer installs and seeds relational Rindle state before the move.
    - A host migration queues `SET LOCAL lock_timeout = '5s'` then calls `move_public_to_rindle(version: 1)`.
    - The default-compiled consumer boots afterward, reads the seeded state, performs a new write/read, and proves indexes/FKs/marker survived.
    - A fast upgrade-contract test asserts separate public/default build identities, the directional migration template, and required integrity/report keys.
  </behavior>
  <action>Add the fast failing `phase_120_upgrade_contract` source/report/topology assertion first. Create a narrowly named isolation-upgrade scenario rather than changing the legacy AV upgrade helper. Use separate public/default generated roots and database/report identities, distinct compile stages, and one host-owned directional migration; report row identity/counts, marker, a representative FK/index check, all schema-qualified ownership facts, post-deploy doctor/runtime readiness, and new default-routed persistence. Assert public Oban and the ledger before and after. Do not reproduce Phase 118 move SQL, call destructive down/1, move any relation outside the fixed authority, or mutate routing within one running build (D-120-05/D-120-06).</action>
  <verify><automated>mix test test/install_smoke/generated_app_smoke_test.exs --only phase_120_upgrade_contract --seed 0</automated></verify>
  <acceptance_criteria>
    - Seeded public data, its relationships, indexes, and marker are observable in rindle after the host migration.
    - No Rindle owned relation remains in public, and the default build completes a new persistence operation.
    - Doctor/runtime verification reports the matching default layout without auto-remediation.
    - `public.oban_jobs` and `public.schema_migrations` remain present and unchanged in ownership.
  </acceptance_criteria>
  <done>The packaged upgrade proof exercises the actual 0.4.0 move/deploy/runtime sequence with populated data.</done>
</task>

</tasks>

## Artifacts this plan produces

- A public-compatibility generated consumer with explicit compile-time routing and host migration ownership.
- A separate populated isolation-upgrade consumer that spans old layout, directional host migration, matching deploy, diagnostics, and post-upgrade persistence.
- Stable report fields and focused tags for running each expensive scenario independently.

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Upgrade and compatibility verification | Tasks 1–2 |
| REQ | PROOF-01 explicit public, populated move, routing, Oban | Tasks 1–2 |
| RESEARCH | separate apps; do not overload legacy helper; preserve relational integrity | Tasks 1–2 |
| CONTEXT | D-120-02 through D-120-06 | Cited and enforced |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Compile-time public/default builds | Build artifacts must not share mutable prefix state. |
| Populated host migration | Only the pinned directional API may relocate the fixed Rindle set. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-120-04 | Tampering | compatibility fixture | high | mitigate | Use a separate public-compiled app and assert exact public/rindle relation distribution. |
| T-120-05 | Tampering | upgrade migration | high | mitigate | Call the fixed helper from a host migration and assert host relations before/after. |
| T-120-06 | Denial of Service | upgrade ordering | high | mitigate | Encode lock timeout, matching deploy, diagnostics, and post-move persistence in the scenario. |
| T-120-SC | Tampering | package installs | high | mitigate | No new dependency; stop if one is proposed. |
</threat_model>

<verification>
Fast task loop:

1. `mix test test/install_smoke/generated_app_smoke_test.exs --only phase_120_compat_contract --seed 0`
2. `mix test test/install_smoke/generated_app_smoke_test.exs --only phase_120_upgrade_contract --seed 0`

Authoritative plan gates:

1. `RINDLE_INSTALL_SMOKE_PROFILE=image mix test test/install_smoke/generated_app_smoke_test.exs --include minio --only phase_120_public_compat --seed 0`
2. `RINDLE_INSTALL_SMOKE_PROFILE=image mix test test/install_smoke/generated_app_smoke_test.exs --include minio --only phase_120_isolation_upgrade --seed 0`
3. `bash scripts/install_smoke.sh image`

The source/report checks shorten feedback; they do not replace any packed scenario.
</verification>

<success_criteria>
- Explicit public compatibility and populated public-to-rindle upgrade are package-backed, isolated, and executable.
- Runtime routing matches each compiled consumer and all host-owned public infrastructure remains untouched.
</success_criteria>

<output>Create `.planning/phases/120-adoption-proof-release-truth/120-02-SUMMARY.md` when done.</output>

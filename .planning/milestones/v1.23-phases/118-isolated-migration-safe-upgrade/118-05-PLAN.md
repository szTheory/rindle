---
phase: 118-isolated-migration-safe-upgrade
plan: 05
type: execute
wave: 5
depends_on: [118-04]
files_modified:
  - test/rindle/migration_test.exs
  - guides/upgrading.md
  - test/install_smoke/docs_parity_test.exs
autonomous: true
gap_closure: true
requirements: [MIGRATE-01, MIGRATE-02, MIGRATE-03]
nyquist_compliant: true
must_haves:
  truths:
    - "D-118-02/D-118-06: an adopter-owned Ecto migration invokes each pinned directional helper directly from up/0 or down/0, so the helper queues the fixed seven-relation move without nesting Ecto migration commands."
    - "D-118-07/D-118-08: the exact documented forward and guarded reverse migration runs through Ecto.Migrator inside a host transaction with transaction-local lock_timeout."
    - "D-118-09/D-118-10/D-118-11: executable proof and documentation agree on maintenance, ownership, rollback, and all-seven-relation behavior."
    - "D-118-01/D-118-05: the corrected populated-upgrade path does not regress fresh rindle provisioning or explicit public compatibility."
  artifacts:
    - path: "test/rindle/migration_test.exs"
      provides: "Ecto.Migrator integration proof for the exact documented forward and reverse host migration"
      contains: "Ecto.Migrator"
    - path: "guides/upgrading.md"
      provides: "Copy-pasteable non-nested host migration"
      contains: "Rindle.Migration.move_public_to_rindle(version: 1)"
    - path: "test/install_smoke/docs_parity_test.exs"
      provides: "Structural documentation contract that rejects nested helper callbacks"
  key_links:
    - from: "guides/upgrading.md"
      to: "Rindle.Migration.move_public_to_rindle/1"
      via: "direct migration-body call after queued SET LOCAL lock_timeout"
      pattern: "execute.*lock_timeout.*move_public_to_rindle"
    - from: "test/rindle/migration_test.exs"
      to: "Ecto.Migrator"
      via: "the documented migration module's up/down callbacks"
      pattern: "Ecto\\.Migrator\\.(up|down)"
---

<objective>
Close the disconnected adopter path with a production-quality end-to-end tracer: run the exact documented host migration through Ecto.Migrator, move all seven Rindle relations forward and back, and publish only that proven callback shape.

Purpose: The direct-helper unit harness passes today, but the guide's nested callback is rejected by Ecto.Migration.Runner before any relation moves.
Output: A real Ecto.Migrator regression test, corrected upgrade guide, and documentation parity guard.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/118-isolated-migration-safe-upgrade/118-CONTEXT.md
@.planning/phases/118-isolated-migration-safe-upgrade/118-VERIFICATION.md
@.planning/phases/118-isolated-migration-safe-upgrade/118-04-SUMMARY.md
@lib/rindle/migration.ex
@lib/rindle/migration/v1.ex
@test/rindle/migration_test.exs
@guides/upgrading.md
@test/install_smoke/docs_parity_test.exs

<interfaces>
- `Rindle.Migration.move_public_to_rindle(version: 1)` and `move_rindle_to_public(version: 1)` are Ecto migration command producers and must be called at migration-body scope, not from inside another `execute/1` callback.
- `execute("SET LOCAL lock_timeout = '5s'")` queues the host's transaction-local policy before the direct directional helper call.
- `Ecto.Migrator.up/4` and `down/4` are the adopter-real execution boundary; the custom `run_migration/3` helper alone is insufficient proof for this gap.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Run the documented host migration through Ecto.Migrator</name>
  <reversibility rating="reversible">The test module and invocation harness can be replaced locally without changing the published migration API or persisted layout.</reversibility>
  <read_first>
    .planning/phases/118-isolated-migration-safe-upgrade/118-VERIFICATION.md
    lib/rindle/migration.ex
    lib/rindle/migration/v1.ex
    test/rindle/migration_test.exs
    guides/upgrading.md
  </read_first>
  <files>test/rindle/migration_test.exs</files>
  <behavior>
    - MIGRATE-02/D-118-02/D-118-06: a nested test migration module queues `SET LOCAL lock_timeout = '5s'`, then calls `Rindle.Migration.move_public_to_rindle(version: 1)` directly in `up/0`; its `down/0` does the same for the guarded reverse.
    - `Ecto.Migrator.up/4` moves exactly `Rindle.Migration.V1.owned_relations/0` from public to rindle and preserves the populated fixture, index names, foreign-key enforcement, marker, public oban_jobs, and host schema_migrations ownership.
    - `Ecto.Migrator.down/4` returns the same seven relations to public without dropping rindle or invoking destructive `Rindle.Migration.down/1`.
    - MIGRATE-01: the existing default-rindle and explicit-public fresh-install cases remain green.
  </behavior>
  <action>First add a failing regression test and a nested `DocumentedMoveMigration` test module whose `up/0` and `down/0` match the intended guide body exactly: queue the literal `SET LOCAL lock_timeout = '5s'` statement with Ecto's SQL form of `execute/1`, then call the relevant pinned Rindle helper directly at migration-body scope. Exercise that module with `Ecto.Migrator.up/4` and `Ecto.Migrator.down/4`, using a collision-free migration version and cleaning its ledger row/state in `on_exit`. Start from the existing populated public fixture and reuse the existing catalog/data/index/FK/host-relation assertions. Assert all seven relations move after `up`, all seven return after `down`, the empty rindle schema remains after reverse, and host relations keep their original snapshots. Do not use the custom `run_move/1` helper as the proof for this test, and do not weaken the existing direct-helper tests.</action>
  <verify>
    <automated>mix format --check-formatted test/rindle/migration_test.exs &amp;&amp; mix test test/rindle/migration_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - The regression test reaches Ecto.Migrator/Runner and completes without `cannot execute nested commands`.
    - Forward and reverse assertions locate all seven owned relations and preserve the populated fixture's rows, index, FK, and marker behavior.
    - `public.oban_jobs` and the host migration ledger are not moved or adopted by Rindle.
  </acceptance_criteria>
  <done>The exact host migration callback shape works end to end through the adopter-real Ecto migration boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish and lock the proven non-nested migration shape</name>
  <reversibility rating="reversible">Documentation and parity assertions can be edited together without changing the already-published directional function signatures.</reversibility>
  <read_first>
    guides/upgrading.md
    test/install_smoke/docs_parity_test.exs
    test/rindle/migration_test.exs
    .planning/phases/118-isolated-migration-safe-upgrade/118-CONTEXT.md
  </read_first>
  <files>guides/upgrading.md, test/install_smoke/docs_parity_test.exs</files>
  <behavior>
    - MIGRATE-03/D-118-07/D-118-09: the copy-pasteable guide uses the Ecto.Migrator-proven direct helper calls and retains maintenance-window, backup, quiescence, local timeout, deployment, verification, and bounded reverse guidance.
    - D-118-11: docs parity requires the direct migration-body shape and fails if either directional helper is placed inside an `execute(fn -> ... end)` wrapper.
    - D-118-08: the reverse remains distinct from destructive `Rindle.Migration.down/1` and leaves the rindle schema intact.
  </behavior>
  <action>Replace the guide's nested directional callbacks with the exact `DocumentedMoveMigration` shape proven in Task 1: use SQL `execute/1` for the transaction-local timeout and call each directional helper directly from `up/0` or `down/0`. Keep all existing operational and ownership prose. Strengthen the section-scoped parity test to assert the ordering and direct-call form structurally within the fenced migration snippet, while rejecting function-callback wrapping around either directional helper. Keep the assertion scoped to the populated-install section so unrelated `execute/1` examples remain valid.</action>
  <verify>
    <automated>mix format --check-formatted guides/upgrading.md test/install_smoke/docs_parity_test.exs &amp;&amp; mix test test/install_smoke/docs_parity_test.exs test/rindle/migration_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - The guide's forward and reverse bodies match the Ecto.Migrator-tested module.
    - Docs parity fails on the previously published nested-helper form and passes on the direct helper form.
    - Maintenance, rollback, seven-relation ownership, Oban non-ownership, and host-ledger guidance remain present.
  </acceptance_criteria>
  <done>Adopters receive a copy-pasteable migration whose exact callback structure is executable and regression-locked.</done>
</task>

</tasks>

## Artifacts this phase produces

- Modified `test/rindle/migration_test.exs`: adopter-real Ecto.Migrator forward/reverse proof for the documented module.
- Modified `guides/upgrading.md`: executable direct-call migration snippet.
- Modified `test/install_smoke/docs_parity_test.exs`: section-scoped structural guard against nested helper callbacks.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host migration module → Ecto Runner | Adopter-authored callback structure determines whether Rindle commands are queued legally. |
| Rindle helper → PostgreSQL schemas | The helper may relocate only the fixed seven owned relations in the host transaction. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-118-G05-01 | Denial of Service | documented Ecto callback | high | mitigate | Execute the exact documented module through Ecto.Migrator and forbid nested directional helper callbacks in docs parity. |
| T-118-G05-02 | Tampering | forward/reverse migration | high | mitigate | Assert the moved set equals V1.owned_relations/0 and snapshot oban_jobs plus schema_migrations before and after. |
| T-118-G05-03 | Repudiation | operational guide | medium | mitigate | Keep explicit host ledger, maintenance, deployment, and verification steps bound to an executable integration test. |
| T-118-G05-SC | Tampering | package installs | high | mitigate | No package install is planned; any new install blocks until a Package Legitimacy Audit and human approval exist. |
</threat_model>

<verification>
Run `mix test test/rindle/migration_test.exs --seed 0`, then `mix test test/install_smoke/docs_parity_test.exs test/rindle/migration_fast_test.exs --seed 0`. All commands must pass without changing production code or host-owned database objects.
</verification>

<success_criteria>
- The documented forward and reverse callbacks execute through Ecto.Migrator.
- Exactly seven Rindle relations move with data and integrity intact.
- The guide cannot regress to a nested Ecto command shape unnoticed.
</success_criteria>

<output>
Create `.planning/phases/118-isolated-migration-safe-upgrade/118-05-SUMMARY.md` when done.
</output>

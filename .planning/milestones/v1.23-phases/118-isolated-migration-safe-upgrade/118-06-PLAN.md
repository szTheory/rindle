---
phase: 118-isolated-migration-safe-upgrade
plan: 06
type: execute
wave: 6
depends_on: [118-05]
files_modified:
  - lib/rindle/migration/v1.ex
  - test/rindle/migration_test.exs
autonomous: true
gap_closure: true
requirements: [MIGRATE-01, MIGRATE-02, MIGRATE-03]
nyquist_compliant: true
must_haves:
  truths:
    - "D-118-07/D-118-09: a PostgreSQL lock timeout becomes bounded Rindle operator guidance naming maintenance/quiescence and retry, rather than an unhandled Postgrex error."
    - "D-118-10: a synchronized second connection proves lock contention, transaction atomicity, and SET LOCAL timeout locality without timing-only sleeps."
    - "D-118-04: absent-target database-CREATE denial and existing-empty-target schema-CREATE denial both refuse before schema creation or any of the seven moves."
    - "D-118-03/D-118-05/D-118-06: V1 remains the sole fixed ownership, privilege, quoted-DDL, and move authority; fresh provisioning and host infrastructure remain unchanged."
  artifacts:
    - path: "lib/rindle/migration/v1.ex"
      provides: "Bounded lock-timeout translation and deterministic internal privilege-test seam around the fixed move"
      contains: "lock_timeout"
    - path: "test/rindle/migration_test.exs"
      provides: "Second-connection contention/locality proof and both privilege-refusal branch proofs"
  key_links:
    - from: "PostgreSQL SQLSTATE 55P03"
      to: "Rindle bounded migration error"
      via: "narrow rescue around immediate ALTER TABLE execution inside the queued Ecto callback"
      pattern: "55P03|lock_not_available"
    - from: "privilege probe"
      to: "V1 preflight classifier"
      via: "captured test-only override or real catalog booleans before any DDL"
      pattern: "database_create|target_usable"
---

<objective>
Close the remaining safety proof around the now-executable host migration: translate lock acquisition timeout into bounded operator guidance, prove rollback and transaction-local timeout scope under synchronized contention, and behaviorally exercise both privilege-refusal branches.

Purpose: Operators must fail quickly and safely when traffic or privileges make the move unsafe, with no partial relocation and no ownership of host infrastructure.
Output: Focused V1 error handling plus deterministic PostgreSQL and privilege-branch integration coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/118-isolated-migration-safe-upgrade/118-CONTEXT.md
@.planning/phases/118-isolated-migration-safe-upgrade/118-RESEARCH.md
@.planning/phases/118-isolated-migration-safe-upgrade/118-VALIDATION.md
@.planning/phases/118-isolated-migration-safe-upgrade/118-VERIFICATION.md
@.planning/phases/118-isolated-migration-safe-upgrade/118-05-SUMMARY.md
@lib/rindle/migration/v1.ex
@test/rindle/migration_test.exs

<interfaces>
- Plan 05 proves the direct helper call through Ecto.Migrator; all contention and privilege cases must exercise that runnable shape or the same public helper through the established Runner.
- PostgreSQL lock timeout is SQLSTATE `55P03` / `:lock_not_available`; translation must be narrow and preserve unrelated Postgrex failures.
- Test control remains internal and process-scoped. No public migration option, arbitrary callback, generic mover, or configurable ownership list may be introduced.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Translate and prove transaction-local lock contention</name>
  <reversibility rating="costly">The operator-facing failure contract and V1 move execution seam are shared by both published directional helpers, so undo requires coordinated code, test, and documentation changes.</reversibility>
  <read_first>
    lib/rindle/migration/v1.ex
    test/rindle/migration_test.exs
    .planning/phases/118-isolated-migration-safe-upgrade/118-VERIFICATION.md
    .planning/phases/118-isolated-migration-safe-upgrade/118-05-SUMMARY.md
  </read_first>
  <files>lib/rindle/migration/v1.ex, test/rindle/migration_test.exs</files>
  <behavior>
    - MIGRATE-03/D-118-07: SQLSTATE 55P03 raised by a fixed relation move becomes an ArgumentError with calm, bounded next action to keep writers/workers quiesced and retry the host migration; other Postgrex errors retain their original behavior.
    - A dedicated second PostgreSQL connection acquires the conflicting table lock and signals readiness before the migration begins; no sleep establishes correctness.
    - With `SET LOCAL lock_timeout = '100ms'`, the migration fails within the bounded database timeout and every owned relation remains in public with rows, indexes, FKs, marker, oban_jobs, and schema_migrations unchanged.
    - After rollback, `SHOW lock_timeout` on the migration connection returns its pre-transaction value, proving SET LOCAL did not leak.
  </behavior>
  <action>Write the synchronized contention test first. Start a dedicated Postgrex connection from the test Repo configuration, begin a transaction, acquire an `ACCESS EXCLUSIVE` lock on the first fixed public relation, notify the test only after PostgreSQL confirms the lock, and hold it until an explicit release message in `on_exit`; do not use sleep or elapsed wall time as the readiness assertion. Run the Plan 05 documented migration shape with a transaction-local 100ms timeout and assert the bounded error plus complete public-only snapshots. In V1, change the move execution seam so each fixed ALTER runs inside its queued Ecto callback via `repo().query!/2`, allowing the callback to rescue only Postgrex lock-not-available code 55P03 and raise the D-118-09 operator-directed ArgumentError. Keep all seven statements in the host transaction, preserve injected rollback testing, and re-raise every non-lock Postgrex error unchanged. Capture and compare `SHOW lock_timeout` before and after the failed transaction on the same checked-out migration connection.</action>
  <verify>
    <automated>mix format --check-formatted lib/rindle/migration/v1.ex test/rindle/migration_test.exs &amp;&amp; mix test test/rindle/migration_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - The contention test is message-synchronized and uses a genuinely separate PostgreSQL backend.
    - Lock timeout yields bounded Rindle guidance, while a test for a non-lock Postgrex error proves it is not mistranslated.
    - No owned relation, row, index, FK, marker, oban_jobs, or schema_migrations state changes after failure.
    - The effective lock_timeout after rollback equals its value before the transaction.
  </acceptance_criteria>
  <done>Lock contention fails boundedly, atomically, and locally on the adopter-real migration path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Behaviorally prove both privilege-refusal branches</name>
  <reversibility rating="reversible">The deterministic test seam is internal and removable without changing the public migration API, persisted data, or production catalog-query behavior.</reversibility>
  <read_first>
    lib/rindle/migration/v1.ex
    test/rindle/migration_test.exs
    .planning/phases/118-isolated-migration-safe-upgrade/118-CONTEXT.md
    .planning/phases/118-isolated-migration-safe-upgrade/118-VERIFICATION.md
  </read_first>
  <files>lib/rindle/migration/v1.ex, test/rindle/migration_test.exs</files>
  <behavior>
    - MIGRATE-03/D-118-04: complete public plus absent rindle and a denied database-CREATE result raises the existing bounded database-create guidance, creates no rindle schema, and moves no relation.
    - Complete public plus an existing empty rindle schema and a denied schema USAGE/CREATE result raises bounded target-schema guidance and moves no relation.
    - Both cases invoke the public pinned forward helper and flush the Ecto Runner; all seven relations, populated state, and host snapshots remain in public.
    - Without the test override, production reads the real PostgreSQL privilege functions exactly as before.
  </behavior>
  <action>Add the smallest deterministic equivalent of a disposable non-owner role: a process-scoped, test-only privilege snapshot override captured when the directional helper is invoked and threaded only into V1's private snapshot construction. It may override `database_create?`, `target_usable?`, and `public_usable?` booleans for tests, but must not bypass relation discovery, ownership checks, marker validation, or the public helper. Production with no override must execute the existing bound PostgreSQL privilege queries. Add two full behavioral cases using populated public fixtures: absent target with database creation denied, and pre-created empty target with target schema use/create denied. Invoke `Rindle.Migration.move_public_to_rindle(version: 1)`, flush the runner, assert the exact bounded reason/next action, then inspect both schemas and host snapshots. Clear the process override in `after` and `on_exit` so no case can contaminate another.</action>
  <verify>
    <automated>mix format --check-formatted lib/rindle/migration/v1.ex test/rindle/migration_test.exs &amp;&amp; mix test test/rindle/migration_test.exs test/rindle/migration_fast_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - Both privilege branches are reached through the public helper and produce their distinct bounded guidance.
    - The absent-target case does not create rindle; the existing-target case leaves rindle empty.
    - Every Rindle-owned relation and populated fixture remains in public, and host relations are unchanged.
    - A no-override assertion proves production still obtains privilege state from PostgreSQL.
  </acceptance_criteria>
  <done>The two previously unverified insufficient-permission states have deterministic behavioral proof with zero mutation.</done>
</task>

</tasks>

## Artifacts this phase produces

- Modified `lib/rindle/migration/v1.ex`: immediate fixed-ALTER callback execution, narrow SQLSTATE 55P03 translation, and a private process-scoped privilege-test seam.
- Modified `test/rindle/migration_test.exs`: synchronized second-connection contention/atomicity/locality proof and both public-helper privilege refusal cases.

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Fresh isolated install or populated legacy move without data loss or host-infrastructure ownership | 05, 06 | COVERED | Ecto.Migrator tracer plus safety expansions; fresh cases stay in the focused suite. |
| REQ | MIGRATE-01 | Selected-schema provisioning remains idempotent | 05, 06 | COVERED | Existing fresh/default proof is a required regression gate in both plans. |
| REQ | MIGRATE-02 | Exact seven-relation data-preserving host move | 05 | COVERED | Exact documented migration runs forward/reverse through Ecto.Migrator. |
| REQ | MIGRATE-03 | Unsafe states fail before mutation with truthful guidance | 05, 06 | COVERED | Runnable docs, lock translation, atomicity, locality, and both privilege branches. |
| RESEARCH | — | Host owns transaction/ledger; Rindle owns fixed allowlist and preflight | 05, 06 | COVERED | Direct migration-body helpers and V1-only safety seam preserve responsibility map. |
| RESEARCH | — | Transaction-local bounded lock timeout and no traffic-quiescence assumption | 05, 06 | COVERED | Exact guide shape plus synchronized lock proof. |
| CONTEXT | D-118-01–D-118-03 | Narrow rindle/public and version-pinned V1 authority | 05, 06 | COVERED | No public API widening; V1 remains sole authority. |
| CONTEXT | D-118-04–D-118-06 | Complete-state preflight, validated DDL, fixed transactional move | 05, 06 | COVERED | Privilege cases prove refusal; Ecto.Migrator proves exact move. |
| CONTEXT | D-118-07–D-118-09 | Maintenance, local timeout, guarded reverse, calm operator copy | 05, 06 | COVERED | Guide and errors are executable and behaviorally tested. |
| CONTEXT | D-118-10–D-118-11 | Integration proof and truthful migration documentation | 05, 06 | COVERED | All verifier gaps map to automated tests and parity. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Concurrent host connection → migration locks | Untrusted live traffic can block ACCESS EXCLUSIVE acquisition and exhaust deploy time. |
| Database privilege state → V1 preflight | Role capabilities determine whether schema creation or relocation is authorized. |
| Test seam → production classifier | Test control must not become a public bypass or alter production privilege discovery. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-118-G06-01 | Denial of Service | ALTER TABLE lock acquisition | high | mitigate | Use host SET LOCAL lock_timeout, translate only SQLSTATE 55P03, and prove bounded synchronized contention. |
| T-118-G06-02 | Tampering / Elevation | privilege preflight | high | mitigate | Refuse database-CREATE and target-schema-CREATE denial before DDL; exercise both through the public helper. |
| T-118-G06-03 | Tampering | internal test override | high | mitigate | Keep the override private, process-scoped, boolean-only, cleared after each test, and absent from public options. |
| T-118-G06-04 | Information Disclosure | operator error | low | accept | Guidance names required privilege/action but contains no credentials, SQL values, or host-specific identifiers. |
| T-118-G06-SC | Tampering | package installs | high | mitigate | No package install is planned; any new install blocks until a Package Legitimacy Audit and human approval exist. |
</threat_model>

<verification>
Run `mix test test/rindle/migration_test.exs test/rindle/migration_fast_test.exs --seed 0`, then `mix test test/install_smoke/docs_parity_test.exs test/rindle/api_surface_boundary_test.exs test/rindle/schema_prefix_contract_test.exs --seed 0`. Finish with the RUNNING.md release-train gate: `mix coveralls.multiple --type local --type json --slowest 20`.
</verification>

<success_criteria>
- Lock contention produces bounded Rindle guidance within the transaction-local timeout and rolls back completely.
- The timeout setting does not leak outside the failed transaction.
- Both required privilege-denial states refuse before schema creation or relocation.
- The complete Phase 118 migration and documentation suites remain green.
</success_criteria>

<output>
Create `.planning/phases/118-isolated-migration-safe-upgrade/118-06-SUMMARY.md` when done.
</output>

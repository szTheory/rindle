---
phase: 119-ownership-boundaries-diagnostics
plan: 05
type: execute
wave: 5
depends_on: [119-04]
files_modified:
  - lib/rindle/ops/runtime_status.ex
  - lib/mix/tasks/rindle.runtime_status.ex
  - lib/rindle/ops/runtime_checks.ex
  - lib/rindle/admin/live/runtime_doctor_live.ex
  - test/rindle/runtime_status_task_test.exs
  - test/rindle/ops/runtime_checks_test.exs
  - test/rindle/doctor_test.exs
  - test/rindle/admin/queries_test.exs
  - test/rindle/admin/live/variants_runtime_actions_test.exs
autonomous: true
gap_closure: true
requirements: [BOUNDARY-01, BOUNDARY-02, OPS-01]
nyquist_compliant: true
must_haves:
  truths:
    - "D-119-09/D-119-11: doctor, runtime-status text/JSON, the admin facade, and the Runtime/Doctor LiveView expose only bounded classifications, ownership, safe prefix fields, and doctor-first actions; Postgrex, SQL, credential, and arbitrary binding-prefix payloads remain internal."
    - "D-119-10/D-119-12: a refused ownership snapshot reaches the existing admin Runtime/Doctor page with runtime_status: nil, renders the bounded refusal and doctor rows, and invokes no report query."
    - "D-119-07/D-119-08: migration-inspection failure still produces the existing actionable doctor check model and both stable readiness IDs without incorporating exception-derived text."
    - "D-119-01/D-119-02/D-119-03: gap closure preserves the compile-time Rindle prefix authority and canonical, independently validated default-host Oban binding; no renderer becomes a routing or inference authority."
    - "D-119-04/D-119-05/D-119-06: the shared fixed-scope snapshot, validated catalog boundary, and exact two-schema mismatch rules remain unchanged while unsafe output projection is closed."
    - "D-119-13: all changes remain read-only diagnosis in the existing doctor/runtime/admin surfaces and add no remediation, discovery, queue, deployment, or dashboard capability."
  artifacts:
    - path: "lib/rindle/ops/runtime_status.ex"
      provides: "Safe projection of known refusal detail fields before they cross the runtime boundary"
      contains: "bounded_refusal"
    - path: "lib/mix/tasks/rindle.runtime_status.ex"
      provides: "Defense-in-depth bounded text and JSON formatting for known refusal tuples"
      contains: "format_json_error"
    - path: "lib/rindle/ops/runtime_checks.ex"
      provides: "Constant migration-inspection failure status without exception-derived summary data"
      contains: "migration_statuses"
    - path: "lib/rindle/admin/live/runtime_doctor_live.ex"
      provides: "Nil-safe rendering of deliberate runtime refusal models"
      contains: "runtime_findings"
    - path: "test/rindle/admin/live/variants_runtime_actions_test.exs"
      provides: "Mounted-route proof for bounded refusal rendering and report-query short circuit"
    - path: "test/rindle/runtime_status_task_test.exs"
      provides: "Hostile known-refusal text/JSON regression proof"
    - path: "test/rindle/doctor_test.exs"
      provides: "Rendered doctor migration-failure redaction proof"
    - path: "test/rindle/admin/queries_test.exs"
      provides: "Admin model redaction proof across doctor and runtime refusal data"
  key_links:
    - from: "lib/rindle/ops/runtime_status.ex"
      to: "lib/mix/tasks/rindle.runtime_status.ex"
      via: "known tagged refusal details are safely projected before both text and JSON formatting"
      pattern: "bounded_refusal|format_error|error_details"
    - from: "lib/rindle/admin/queries.ex"
      to: "lib/rindle/admin/live/runtime_doctor_live.ex"
      via: "runtime_status: nil is consumed as a deliberate refusal model while diagnostic fields and doctor checks still render"
      pattern: "runtime_status|runtime_findings"
    - from: "lib/rindle/ops/runtime_checks.ex"
      to: "lib/mix/tasks/rindle.doctor.ex"
      via: "fixed migration-inspection failure marker flows through the existing check map and doctor renderer"
      pattern: "migration inspection failed|check_migration_pending"
    - from: "lib/rindle/ops/runtime_checks.ex"
      to: "lib/rindle/admin/queries.ex"
      via: "the admin facade consumes the same redacted doctor report rather than a separate error path"
      pattern: "RuntimeChecks.run"
  prohibitions:
    - statement: "D-119-01/D-119-02/D-119-03: renderers MUST NOT derive or silently choose either Rindle or Oban routing from hostile refusal details."
      status: planned
      flagged: false
    - statement: "D-119-04/D-119-05/D-119-06: this gap closure MUST NOT widen catalog scope, authorize arbitrary identifiers, or reinterpret ambiguous catalog state."
      status: planned
      flagged: false
    - statement: "D-119-09/D-119-11: no raw adapter exception, query text, connection/credential material, or arbitrary configuration value may cross a doctor, text, JSON, facade, or HTML refusal boundary."
      status: planned
      flagged: false
    - statement: "D-119-10/D-119-12: a refused snapshot MUST NOT invoke any runtime report query or be replaced with fabricated empty operational report data."
      status: planned
      flagged: false
    - statement: "D-119-13: diagnostics MUST NOT mutate oban_jobs, schema_migrations, host Oban configuration, queues, lifecycle state, migrations, or deployment state."
      status: planned
      flagged: false
---

<objective>
Close the three verified Phase 119 refusal-path defects without reopening completed Plans 01–04: safely project known runtime refusal fields, render the deliberate nil-report admin model, and replace exception-derived doctor summaries with a fixed bounded failure.

Purpose: Complete the phase goal so operators receive actionable ownership diagnostics instead of leaked database/configuration data or an admin crash.
Output: Hardened runtime and doctor boundaries plus focused regression tests for CLI text/JSON, doctor/admin models, and the mounted Runtime/Doctor refusal page.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/119-ownership-boundaries-diagnostics/119-CONTEXT.md
@.planning/phases/119-ownership-boundaries-diagnostics/119-RESEARCH.md
@.planning/phases/119-ownership-boundaries-diagnostics/119-PATTERNS.md
@.planning/phases/119-ownership-boundaries-diagnostics/119-VERIFICATION.md
@.planning/phases/119-ownership-boundaries-diagnostics/119-03-SUMMARY.md
@.planning/phases/119-ownership-boundaries-diagnostics/119-04-SUMMARY.md
@lib/rindle/ops/runtime_status.ex
@lib/mix/tasks/rindle.runtime_status.ex
@lib/rindle/ops/runtime_checks.ex
@lib/rindle/admin/queries.ex
@lib/rindle/admin/live/runtime_doctor_live.ex
@test/rindle/runtime_status_task_test.exs
@test/rindle/ops/runtime_checks_test.exs
@test/rindle/doctor_test.exs
@test/rindle/admin/queries_test.exs
@test/rindle/admin/live/variants_runtime_actions_test.exs

<interfaces>
- `Rindle.Ops.RuntimeStatus.runtime_status/1` returns existing tagged refusal tuples only after its shared ownership snapshot preflight; its configured `:report_query` seam must remain unreachable on refusal.
- `Mix.Tasks.Rindle.RuntimeStatus.format_error/1` and `format_json_error/1` are the shared bounded renderers consumed by the Mix task, admin facade, and adoption-demo presentation.
- `Rindle.Admin.Queries.runtime_doctor/1` intentionally returns `runtime_status: nil` with populated `diagnostic`, `diagnostic_text`, and doctor report when runtime preflight refuses.
- `Rindle.Ops.RuntimeChecks.run/2` accepts `:migration_statuses` for focused rendering tests; production `migration_statuses/1` must translate inspection failure to a constant internal status rather than exception text.
- Stable doctor IDs remain `doctor.rindle_schema.ready` and `doctor.oban_jobs.ready`; no new diagnostic surface or public snapshot API is permitted.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Render one hostile ownership refusal through the real admin route</name>
  <read_first>
    .planning/phases/119-ownership-boundaries-diagnostics/119-CONTEXT.md
    .planning/phases/119-ownership-boundaries-diagnostics/119-VERIFICATION.md
    .planning/phases/119-ownership-boundaries-diagnostics/119-PATTERNS.md
    lib/rindle/ops/runtime_status.ex
    lib/rindle/admin/queries.ex
    lib/rindle/admin/live/runtime_doctor_live.ex
    test/rindle/admin/live/variants_runtime_actions_test.exs
    test/rindle/runtime_status_task_test.exs
  </read_first>
  <files>lib/rindle/ops/runtime_status.ex, lib/rindle/admin/live/runtime_doctor_live.ex, test/rindle/admin/live/variants_runtime_actions_test.exs</files>
  <behavior>
    - D-119-09/D-119-10/D-119-11: a `:rindle_prefix_mismatch` or `:oban_binding_drift` snapshot carrying an error-like prefix payload is projected to the supported safe prefix representation or a constant unknown value before the tagged public refusal reaches any renderer.
    - D-119-10/D-119-12: the mounted `/admin/rindle/runtime-doctor` route renders `Runtime status unavailable`, the stable classification/owner/next action, and doctor rows when `runtime_status` is nil; its report-query tripwire is not invoked.
    - D-119-07/D-119-13: the existing Runtime/Doctor layout, route, controls, and stable doctor check rows remain intact, with no fabricated report data or repair action.
  </behavior>
  <action>Write the mounted-route refusal test first. Configure the existing `Rindle.Ops.RuntimeStatus` ownership-snapshot seam with one known mismatch whose prefix field contains a recognizable database/query/credential-like payload, and configure `:report_query` to raise if called. Mount the real Runtime/Doctor route and assert a successful error-state render containing the bounded classification, owner, doctor-first action, and existing doctor rows, while excluding the hostile payload and never raising the tripwire. Then update `bounded_refusal/3` to project prefix values through the existing supported prefix/identifier rules before constructing the tagged refusal; preserve classification, component, owner, and the established tuple shapes per D-119-01 through D-119-03 and D-119-10. Make `runtime_findings/1` return no findings for the deliberate nil report while retaining its current behavior for a real report. Do not replace nil with `empty_model/0`, run a second query, alter `Queries.runtime_doctor/1`, or redesign the page, per D-119-11 through D-119-13.</action>
  <verify>
    <automated>mix format --check-formatted lib/rindle/ops/runtime_status.ex lib/rindle/admin/live/runtime_doctor_live.ex test/rindle/admin/live/variants_runtime_actions_test.exs &amp;&amp; mix test test/rindle/admin/live/variants_runtime_actions_test.exs --only phase_119_refusal --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - The tagged mismatch/drift refusal keeps its stable classification, component, owner, and doctor-first action while prefix fields contain only accepted safe values or the fixed unknown representation.
    - The real Runtime/Doctor route renders its refusal section, doctor rows, error state, and navigation with `runtime_status: nil`.
    - The configured report-query tripwire is not invoked.
    - The hostile PostgreSQL/query/credential-like prefix sentinel is absent from the rendered HTML and inspected refusal model.
    - No fake runtime report, new route, new control, migration action, or ownership mutation is introduced.
  </acceptance_criteria>
  <done>An operator can open the existing admin Runtime/Doctor page during a real bounded ownership refusal and see actionable, redacted diagnostics without a nil dereference or report query.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound known runtime-status tuples in both text and JSON</name>
  <read_first>
    .planning/phases/119-ownership-boundaries-diagnostics/119-CONTEXT.md
    .planning/phases/119-ownership-boundaries-diagnostics/119-VERIFICATION.md
    .planning/phases/119-ownership-boundaries-diagnostics/119-PATTERNS.md
    lib/rindle/ops/runtime_status.ex
    lib/mix/tasks/rindle.runtime_status.ex
    test/rindle/runtime_status_task_test.exs
  </read_first>
  <files>lib/mix/tasks/rindle.runtime_status.ex, test/rindle/runtime_status_task_test.exs</files>
  <behavior>
    - D-119-09/D-119-11: forged or legacy known refusal tuples cannot make arbitrary expected/observed prefix, component, or owner values appear in text or encoded JSON.
    - D-119-10: legitimate `rindle`/`public` mismatch and validated host-prefix details remain observable with stable classification, component, owner, no-query statement, and `mix rindle.doctor` action.
    - D-119-12: both `:rindle_prefix_mismatch` and `:oban_binding_drift` are covered with hostile binding-prefix and error-like sentinel inputs in text and JSON.
  </behavior>
  <action>Extend the existing known-refusal table tests before implementation with hostile binary and non-binary detail fields, including a malicious host-binding prefix and database/query/credential-like content. Assert both `format_error/1` and Jason-encoded `format_json_error/1` retain the safe classification, component/owner when valid, the no-report-query statement, and doctor-first action while excluding every hostile field. Harden the known mismatch/drift clauses as defense in depth: accept only the narrow safe prefix representation already projected by RuntimeStatus, validate atom fields against the known component/owner vocabulary, and use the same fixed unknown/omission policy in text and JSON so the two outputs cannot diverge. Preserve the legacy setup-incomplete and invalid-format clauses and the constant unknown fallback. Do not inspect or stringify arbitrary refusal terms, per D-119-09 through D-119-11.</action>
  <verify>
    <automated>mix format --check-formatted lib/mix/tasks/rindle.runtime_status.ex test/rindle/runtime_status_task_test.exs &amp;&amp; mix test test/rindle/runtime_status_task_test.exs:88 test/rindle/runtime_status_task_test.exs:135 --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - Known mismatch/drift text and JSON exclude malicious binding-prefix, PostgreSQL, SQL, connection, and credential sentinel content.
    - Safe known fields remain observable and identical in meaning across text and JSON: classification, accepted component/owner, safe expected/observed prefixes when present, no-report-query status, and `mix rindle.doctor` next action.
    - The end-to-end JSON refusal still exits non-zero and its report-query tripwire remains unreachable.
    - Existing setup-incomplete, inspection-failed, invalid-format, and unknown refusal behavior remains bounded and compatible.
  </acceptance_criteria>
  <done>Runtime-status CLI text and JSON remain useful for supported known refusals while rejecting unsafe tuple content at the final rendering boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace exception-derived doctor summaries with a bounded failure</name>
  <read_first>
    .planning/phases/119-ownership-boundaries-diagnostics/119-CONTEXT.md
    .planning/phases/119-ownership-boundaries-diagnostics/119-VERIFICATION.md
    .planning/phases/119-ownership-boundaries-diagnostics/119-PATTERNS.md
    lib/rindle/ops/runtime_checks.ex
    lib/rindle/admin/queries.ex
    test/rindle/ops/runtime_checks_test.exs
    test/rindle/doctor_test.exs
    test/rindle/admin/queries_test.exs
  </read_first>
  <files>lib/rindle/ops/runtime_checks.ex, test/rindle/ops/runtime_checks_test.exs, test/rindle/doctor_test.exs, test/rindle/admin/queries_test.exs</files>
  <behavior>
    - D-119-07/D-119-08: migration inspection failure retains the existing doctor check model, deterministic stable readiness IDs, failed status, and one actionable next step.
    - D-119-09/D-119-11: neither returned check data, doctor text, nor the admin facade model contains the exception/query/credential sentinel.
    - D-119-04/D-119-12/D-119-13: the shared read-only diagnostic flow remains intact and no mutation, discovery, report query, or remediation is introduced.
  </behavior>
  <action>Add focused failures first at the existing seams: assert the structured RuntimeChecks report, captured doctor command output, and `Queries.runtime_doctor/1` model all redact a recognizable migration-inspection sentinel while retaining the failed migration check, both stable ownership check IDs, bounded runtime refusal, and doctor-first action. In the admin test, pass `doctor_opts` through the existing facade option and configure RuntimeStatus to refuse with a report-query tripwire so no unrelated database report is needed. Then replace both error and rescue branches in `migration_statuses/1` with one constant internal failure status/name that drives the established `check_migration_pending/2` failure path; discard the caught reason after classification and do not interpolate exception messages or introduce a new public error shape. Preserve the existing migration-status tuple contract, fixed remediation copy, check ordering, ownership checks, and telemetry. This closes only the verified output leak and must not alter migration execution, catalog scope, Oban ownership, or operator funnel per D-119-04 through D-119-13.</action>
  <verify>
    <automated>mix format --check-formatted lib/rindle/ops/runtime_checks.ex test/rindle/ops/runtime_checks_test.exs test/rindle/doctor_test.exs test/rindle/admin/queries_test.exs &amp;&amp; mix test test/rindle/ops/runtime_checks_test.exs --only phase_119_redaction --seed 0 &amp;&amp; mix test test/rindle/doctor_test.exs --only phase_119_redaction --seed 0 &amp;&amp; mix test test/rindle/admin/queries_test.exs --only phase_119_redaction --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - Production migration inspection error and rescue branches no longer incorporate exception-derived text into a migration name or doctor summary.
    - Structured RuntimeChecks output, captured doctor text, and the admin facade model exclude the PostgreSQL/query/credential sentinel.
    - The migration check remains failed and actionable; `doctor.rindle_schema.ready` and `doctor.oban_jobs.ready` remain present with their established ownership fields.
    - The admin refusal retains `runtime_status: nil`, bounded diagnostic fields, and the no-report-query/doctor-first message without invoking the report-query tripwire.
    - No raw reason is logged or rendered, and no diagnostic path mutates host tables, migration ledger, Oban configuration, queues, or lifecycle state.
  </acceptance_criteria>
  <done>Migration inspection failures remain visible and actionable through doctor and admin while their underlying database exception text is removed at the source boundary.</done>
</task>

</tasks>

## Artifacts this phase produces

- Modified `lib/rindle/ops/runtime_status.ex`: safely projected known-refusal prefix details before public tagged errors leave the service boundary.
- Modified `lib/mix/tasks/rindle.runtime_status.ex`: matching defense-in-depth validation for text and JSON renderers.
- Modified `lib/rindle/ops/runtime_checks.ex`: constant bounded migration-inspection failure marker with no exception-derived summary.
- Modified `lib/rindle/admin/live/runtime_doctor_live.ex`: deliberate nil-report refusal handling without fabricated operational data.
- Extended focused tests in `test/rindle/runtime_status_task_test.exs`, `test/rindle/ops/runtime_checks_test.exs`, `test/rindle/doctor_test.exs`, `test/rindle/admin/queries_test.exs`, and `test/rindle/admin/live/variants_runtime_actions_test.exs` proving redaction, safe-field observability, no report query, and successful refusal rendering.

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Resolve prefix problems without raw database failures | Tasks 1–3 close the two leaks and the admin refusal crash; completed ownership architecture is retained. |
| REQ | BOUNDARY-01 | Tasks 1 and 3 preserve host ownership, nil-as-no-report semantics, and no-query/no-mutation behavior. |
| REQ | BOUNDARY-02 | Tasks 1–2 project and render only validated prefix authorities; no SQL/catalog scope is widened. |
| REQ | OPS-01 | Tasks 1–3 prove bounded doctor, runtime-status text/JSON, admin model, and mounted refusal output. |
| RESEARCH | Bounded diagnostic data before every renderer | Tasks 1–3 enforce the shared safe projection and constant failure pattern from `119-PATTERNS.md`. |
| RESEARCH | Assert model and rendered output redaction | Focused tests cover structured report/refusal data and doctor text, JSON, facade, and HTML output. |
| CONTEXT | D-119-01 through D-119-13 | Each locked decision is cited in must-haves, prohibitions, task behavior/actions, and acceptance criteria; completed architecture is preserved rather than replanned. |

Deferred CONTEXT ideas and Phase 120 packed-adopter/Cohort/release proof remain excluded. No source item applicable to these three verification gaps is missing.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Snapshot/configuration to runtime refusal | Snapshot fields may contain malformed or hostile configuration data and must be projected before public errors. |
| Runtime refusal to text/JSON/admin HTML | Only stable classification, ownership, safe prefixes, and fixed next actions may reach operator output. |
| Migration inspection to doctor/admin | Database exceptions must become a fixed bounded failure before check summaries are constructed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-119-05 | Information Disclosure | `RuntimeStatus.bounded_refusal/3` and Mix formatters | high | mitigate | Project prefix fields through the accepted diagnostic vocabulary, validate component/owner, use one fixed unknown/omission policy, and test hostile values in text, JSON, model, and HTML. |
| T-119-06 | Information Disclosure | `RuntimeChecks.migration_statuses/1` | high | mitigate | Discard caught exception details at the service boundary, emit a constant failure marker, and test the structured report plus doctor/admin renderers with sentinels. |
| T-119-07 | Denial of Service | `RuntimeDoctorLive.runtime_findings/1` | medium | mitigate | Treat `runtime_status: nil` as an intentional refusal state, return no findings, and mount the real route under a refused snapshot. |
| T-119-08 | Tampering | diagnostic report-query boundary | high | mitigate | Retain snapshot-first refusal and assert a configured report-query tripwire is never invoked from the admin or CLI refusal paths. |
| T-119-SC | Tampering | package installs | high | mitigate | No package install is planned; if execution introduces one, stop for a Package Legitimacy Audit and slopcheck gate. |
</threat_model>

<verification>
Run only focused, fixture-independent Phase 119 checks because `119-VERIFICATION.md` records inherited broad database-fixture failures:

1. `mix test test/rindle/admin/live/variants_runtime_actions_test.exs --only phase_119_refusal --seed 0`
2. `mix test test/rindle/runtime_status_task_test.exs:88 test/rindle/runtime_status_task_test.exs:135 --seed 0`
3. `mix test test/rindle/ops/runtime_checks_test.exs --only phase_119_redaction --seed 0`
4. `mix test test/rindle/doctor_test.exs --only phase_119_redaction --seed 0`
5. `mix test test/rindle/admin/queries_test.exs --only phase_119_redaction --seed 0`
6. `mix compile --warnings-as-errors`

Do not claim the broad suite as a gap-closure gate until the inherited Phase 118 database fixture state is repaired independently.
</verification>

<success_criteria>
- Doctor migration-inspection failure never exposes raw database exception, SQL, connection, or credential text through check data, CLI output, or the admin facade.
- Runtime-status known refusals retain safe classification/ownership/action fields while hostile prefix/error-like values are absent from both text and JSON.
- The mounted admin refusal page renders with `runtime_status: nil`, shows the bounded diagnostic and doctor rows, and never invokes a report query.
- BOUNDARY-01, BOUNDARY-02, and OPS-01 are closed without changing the shared snapshot scope, routing authorities, public error compatibility, or diagnosis-only ownership fence.
</success_criteria>

<output>
Create `.planning/phases/119-ownership-boundaries-diagnostics/119-05-SUMMARY.md` when done.
</output>

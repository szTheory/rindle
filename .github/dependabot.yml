version: 2
# HARD-02 (D-04): grouped, weekly, low-noise dependency updates for both the
# GitHub Actions SHA pins (D-03) and the hex dep tree.
#
# commit-message.prefix MUST be a NON-release Conventional-Commits type
# (ci / build / chore) so dependabot PRs never trigger a spurious release-please
# minor (Pitfall 2). Groups bundle minor+patch updates for Actions and development
# Mix dependencies. Runtime Mix dependencies and all major bumps stay separate so
# their compatibility surface remains reviewable.
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
      - "ci"
    commit-message:
      prefix: "ci"
      include: "scope"
    groups:
      actions:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"

  - package-ecosystem: "mix"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
    commit-message:
      prefix: "build"
      include: "scope"
    groups:
      dev-dependencies:
        dependency-type: "development"
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"
